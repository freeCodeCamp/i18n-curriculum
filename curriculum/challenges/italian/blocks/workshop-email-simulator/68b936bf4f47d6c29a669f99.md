---
id: 68b936bf4f47d6c29a669f99
title: Passaggio 16
challengeType: 20
dashedName: step-16
---

# --description--

Sempre all'interno del metodo `send_email`, chiama il metodo `receive_email` della `receiver` di `inbox` per aggiungere l'email nella casella di posta del destinatario.

# --hints--

Dovresti aggiungere `receiver.inbox.receive_email(email)` al metodo `send_email`.

```js
({
    test: () => {
        assert(runPython(`_Node(_code).find_class("User").find_function("send_email").has_stmt("receiver.inbox.receive_email(email)")`));
    }
})
```

# --seed--

## --seed-contents--

```py
class Email:
    def __init__(self, sender, receiver, subject, body):
        self.sender = sender
        self.receiver = receiver
        self.subject = subject
        self.body = body
        self.read = False

    def mark_as_read(self):
        self.read = True

class User:
    def __init__(self, name):
        self.name = name
        self.inbox = Inbox()
        
--fcc-editable-region--
    def send_email(self, receiver, subject, body):
        email = Email(sender=self, receiver=receiver, subject=subject, body=body)

--fcc-editable-region--

class Inbox:
    def __init__(self):
        self.emails = []

    def receive_email(self, email):
        self.emails.append(email)
```
