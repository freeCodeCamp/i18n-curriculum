---
id: 65e97251484dd50f720e6fe8
title: データ型と条件分岐のレッスンCを学ぶ
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-c
---
# --description--

以下の例を見て、文字列に`loves`という単語が含まれていることを確認したいです。

```javascript
let sentence = "freeCodeCamp loves The Odin Project!";
```

<a href="https://www.freecodecamp.org/news/javascript-string-handbook" target="_blank"> ガイドへのリンク </a>

# --questions--

## --text--

文字列に`loves`という単語が含まれているかどうかを確認するには、どのメソッドを使うべきですか？

## --answers--

文字列に`includes()`という単語が含まれているかどうかを確認するには、`loves`メソッドを使うべきです。

---

文字列に`indexOf()`という単語が含まれているかどうかを確認するには、`loves`メソッドを使うべきです。

---

文字列に`search()`という単語が含まれているかどうかを確認するには、`loves`メソッドを使うべきです。

---

文字列に`toUpperCase()`という単語が含まれているかどうかを確認するには、`loves`メソッドを使うべきです。

## --video-solution--

1
