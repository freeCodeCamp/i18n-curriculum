---
id: 66ed8ff4f45ce3ece4053eb4
title: CSS 레이아웃과 효과 퀴즈
challengeType: 8
dashedName: quiz-css-layout-and-effects
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

요소의 내용이 컨테이너 크기를 초과할 때 CSS에서 사용하는 용어는 무엇인가요?

#### --distractors--

언더플로우

---

압축

---

패딩

#### --answer--

오버플로우

### --question--

#### --text--

내용이 요소를 넘칠 때 CSS의 기본 동작은 무엇인가요?

#### --distractors--

내용을 숨깁니다.

---

내용을 잘라냅니다.

---

내용 크기를 조절합니다.

#### --answer--

내용을 가시 상태로 유지합니다.

### --question--

#### --text--

CSS가 오버플로우 시 일반적으로 내용을 숨기지 않는 이유는 무엇인가요?

#### --distractors--

원래 레이아웃과 디자인을 유지하기 위해서입니다.

---

브라우저 성능 문제를 방지하기 위해서입니다.

---

검색 엔진 최적화를 위해서입니다.

#### --answer--

모든 내용에 사용자가 접근할 수 있도록 하기 위해서입니다.

### --question--

#### --text--

CSS에서 수직 오버플로우를 처리할 때 내용을 숨기거나 스크롤바를 표시하는 방법은 무엇인가요?

#### --distractors--

내용을 숨기려면 `overflow-y: visible`를, 항상 스크롤바를 표시하려면 `overflow-y: auto`를 사용하세요.

---

내용을 숨기려면 `overflow-y: collapse`를, 스크롤바를 표시하려면 `overflow-y: expand`를 사용하세요.

---

내용을 숨기려면 `overflow-y: none`를, 스크롤바를 표시하려면 `overflow-y: show`를 사용하세요.

#### --answer--

내용을 숨기려면 `overflow-y: hidden`를, 항상 스크롤바를 표시하려면 `overflow-y: scroll`를 사용하세요.

### --question--

#### --text--

CSS에서 수평과 수직 스크롤 동작을 다르게 지정하려면 어떻게 해야 하나요?

#### --distractors--

`scroll-x`와 `scroll-y` 속성을 사용하세요.

---

`overflow-x`와 `overflow-y`에 같은 값을 지정하세요.

---

`overflow` 속성에 두 값을 지정하는데, 첫 번째 값은 `overflow-y`에, 두 번째 값은 `overflow-x`에 적용됩니다.

#### --answer--

`overflow` 속성에 두 값을 지정하는데, 첫 번째 값은 `overflow-x`에, 두 번째 값은 `overflow-y`에 적용됩니다.

### --question--

#### --text--

`transform` CSS 속성으로 무엇을 할 수 있나요?

#### --distractors--

요소의 색상과 폰트 스타일을 변경합니다.

---

요소의 여백과 패딩을 조절합니다.

---

요소의 가시성과 표시 방식을 수정합니다.

#### --answer--

요소를 회전, 확대, 기울이기, 이동할 수 있습니다.

### --question--

#### --text--

`transform: translate(15px, 200px)` 속성은 요소를 어떻게 수정하나요?

#### --distractors--

요소를 원래 위치에서 왼쪽으로 15픽셀, 아래로 200픽셀 이동합니다.

---

요소를 시계 방향으로 15도 회전합니다.

---

요소를 원래 위치에서 오른쪽으로 15픽셀, 위로 200픽셀 이동합니다.

#### --answer--

요소를 원래 위치에서 오른쪽으로 15픽셀, 아래로 200픽셀 이동합니다.

### --question--

#### --text--

CSS 속성 `overflow-x`와 `overflow-y`의 목적은 무엇인가요?

#### --distractors--

요소의 패딩과 여백을 조절합니다.

---

요소의 배경색과 폰트 스타일을 설정합니다.

---

요소의 가시성과 표시 속성을 정의합니다.

#### --answer--

요소 내용의 수평 및 수직 오버플로우를 제어합니다.

### --question--

#### --text--

CSS에서 `margin` 속성의 목적은 무엇인가요?

#### --distractors--

요소 내용과 테두리 사이의 공간을 정의합니다.

---

요소 테두리와 패딩 사이의 공간을 정의합니다.

---

요소의 너비와 높이를 정의합니다.

#### --answer--

요소 테두리 바깥쪽의 공간을 정의합니다.

### --question--

#### --text--

CSS에서 요소의 콘텐츠 영역은 무엇을 나타내나요?

#### --distractors--

요소 주변의 여백과 패딩을 정의합니다.

---

요소 테두리 두께를 정의합니다.

---

페이지에서 요소의 가시성을 제어합니다.

#### --answer--

텍스트, 이미지, 비디오 등 요소의 실제 내용을 포함합니다.

### --question--

#### --text--

CSS에서 `content-box`와 `border-box`의 차이는 무엇인가요?

#### --distractors--

`content-box`는 패딩과 테두리를 포함하고 `border-box`는 콘텐츠 크기만 포함합니다.

---

`content-box`와 `border-box`는 모두 테두리 크기만 포함합니다.

---

`content-box`는 2D 요소에, `border-box`는 3D 요소에 사용됩니다.

#### --answer--

`content-box`는 콘텐츠 크기만 포함하고 `border-box`는 콘텐츠, 패딩, 테두리를 모두 포함합니다.

### --question--

#### --text--

CSS 리셋의 목적은 무엇인가요?

#### --distractors--

모든 요소에 기본 스타일을 적용해 로딩 속도를 높입니다.

---

사용하지 않는 스타일을 제거해 CSS 파일 크기를 줄입니다.

---

이미지와 폰트를 최적화해 성능을 향상합니다.

#### --answer--

브라우저 기본 스타일을 제거해 브라우저 간 일관된 스타일을 보장합니다.

### --question--

#### --text--

다음 중 CSS 리셋의 일반적인 예는 무엇인가요?

#### --distractors--

bootstrap.css

---

undo.css

---

restore.css

#### --answer--

normalize.css

### --question--

#### --text--

요소에 회전, 확대, 이동 등의 변화를 적용하는 CSS 속성은 무엇인가요?

#### --distractors--

`box-shadow`

---

`opacity`

---

`z-index`

#### --answer--

`transform`

### --question--

#### --text--

`border-box` 값을 `box-sizing` 속성에 사용할 때 요소의 총 너비는 어떻게 계산되나요?

#### --distractors--

총 너비 = 콘텐츠 너비 + 테두리 + 여백

---

총 너비 = 콘텐츠 너비 + 패딩 + 테두리 + 여백

---

총 너비 = 콘텐츠 너비

#### --answer--

총 너비 = 콘텐츠 너비 + 패딩 + 테두리

### --question--

#### --text--

CSS에서 `filter` 속성의 목적은 무엇인가요?

#### --distractors--

요소의 크기와 회전을 변경합니다.

---

요소의 배경색과 질감을 변경합니다.

---

요소의 크기와 위치를 변경합니다.

#### --answer--

다양한 그래픽 효과로 요소의 시각적 모습을 수정합니다.

### --question--

#### --text--

CSS 속성 `filter: grayscale(50%);`는 요소에 어떤 효과를 주나요?

#### --distractors--

요소 밝기를 50% 증가시킵니다.

---

요소 대비를 50% 감소시킵니다.

---

요소에 세피아 효과를 적용합니다.

#### --answer--

요소에 50% 그레이스케일 효과를 적용합니다.

### --question--

#### --text--

CSS에서 `contrast()` 함수의 목적은 무엇인가요?

#### --distractors--

이미지 밝기를 조절하며 `0%`는 검정, `100%`는 흰색으로 만듭니다.

---

이미지 대비를 조절하며 `100%`는 회색, `0%`는 영향 없음, `100%` 이상은 대비 증가입니다.

---

이미지 불투명도를 조절하며 `0%`는 투명, `100%`는 불투명입니다.

#### --answer--

이미지 대비를 조절하며 `0%`는 회색, `100%`는 영향 없음, `100%` 이상은 대비 증가입니다.

### --question--

#### --text--

CSS에서 `hue-rotate()` 함수의 목적은 무엇인가요?

#### --distractors--

이미지 밝기를 변경합니다.

---

이미지를 시계 방향으로 일정 각도 회전합니다.

---

이미지 채도와 대비를 조절합니다.

#### --answer--

색상 변화를 적용하며 `angle` 값은 색상 원을 따라 회전할 각도를 정의합니다.

### --question--

#### --text--

CSS에서 마진 병합(margin collapsing)이란 무엇인가요?

#### --distractors--

요소 마진이 음수로 설정되어 레이아웃 문제를 일으키는 경우입니다.

---

요소 마진이 패딩만큼 줄어들어 요소 크기가 변하는 경우입니다.

---

브라우저가 요소 마진을 무시해 보이지 않는 경우입니다.

#### --answer--

인접 요소의 수직 마진이 겹쳐져 하나의 마진으로 합쳐지는 경우입니다.

## --quiz--

### --question--

#### --text--

다음 중 `translate()` 함수에 대해 올바른 설명은 무엇인가요?

#### --distractors--

요소 크기를 확대하는 데 사용됩니다.

---

요소 텍스트를 이동하는 데 사용됩니다.

---

요소 모양이나 크기를 변경하는 데 사용됩니다.

#### --answer--

요소를 현재 위치에서 이동하는 데 사용됩니다.

### --question--

#### --text--

CSS 박스 모델의 구성 요소 중 하나는 무엇인가요?

#### --distractors--

너비

---

높이

---

크기

#### --answer--

패딩

### --question--

#### --text--

요소의 `box-sizing`가 `content-box`로 설정되었을 때, `width`와 `height`는 박스 모델의 어느 부분에 적용되나요?

#### --distractors--

콘텐츠, 패딩, 테두리, 여백

---

콘텐츠와 패딩

---

콘텐츠, 패딩, 테두리

#### --answer--

콘텐츠

### --question--

#### --text--

요소의 `box-sizing`가 `border-box`로 설정되었을 때, `width`와 `height`는 CSS 박스 모델의 어느 부분에 적용되지 않나요?

#### --distractors--

콘텐츠 영역

---

패딩

---

테두리

#### --answer--

여백

### --question--

#### --text--

`brightness()` 함수에서 요소를 완전히 검은색으로 만드는 값은 무엇인가요?

#### --distractors--

`100%`

---

`0px`

---

`100px`

#### --answer--

`0%`

### --question--

#### --text--

`hue-rotate()` 함수는 어떤 종류의 값을 사용하나요?

#### --distractors--

픽셀

---

라디안

---

백분율

#### --answer--

도(degrees)

### --question--

#### --text--

CSS 속성 `filter: contrast(0%);`는 요소에 어떤 효과를 주나요?

#### --distractors--

요소를 완전히 흰색으로 만듭니다.

---

요소를 완전히 검은색으로 만듭니다.

---

요소에 아무 영향도 주지 않습니다.

#### --answer--

요소를 완전히 회색으로 만듭니다.

### --question--

#### --text--

밝기 증가가 시작되는 `brightness()` CSS 함수의 기준값은 무엇인가요?

#### --distractors--

`100px`

---

`50px`

---

`50%`

#### --answer--

`100%`

### --question--

#### --text--

`grayscale()` 함수는 어떤 종류의 값을 사용하나요?

#### --distractors--

픽셀

---

도(degrees)

---

라디안

#### --answer--

백분율

### --question--

#### --text--

`contrast()` 함수는 어떤 종류의 값을 사용하나요?

#### --distractors--

픽셀

---

도(degrees)

---

라디안

#### --answer--

백분율

### --question--

#### --text--

CSS에서 `overflow-x` 속성은 무엇을 제어하나요?

#### --distractors--

y축의 수평 오버플로우를 결정합니다.

---

y축의 수직 오버플로우를 결정합니다.

---

x축의 수직 오버플로우를 결정합니다.

#### --answer--

x축의 수평 오버플로우를 결정합니다.

### --question--

#### --text--

CSS에서 `overflow-y` 속성은 무엇을 제어하나요?

#### --distractors--

x축의 수평 오버플로우를 결정합니다.

---

x축의 수직 오버플로우를 결정합니다.

---

y축의 수평 오버플로우를 결정합니다.

#### --answer--

y축의 수직 오버플로우를 결정합니다.

### --question--

#### --text--

CSS 리셋의 서드파티 옵션 예시는 무엇인가요?

#### --distractors--

clean.css

---

restore.css

---

reset.css

#### --answer--

sanitize.css

### --question--

#### --text--

CSS에서 오버플로우 속성은 몇 차원으로 구성되나요?

#### --distractors--

1차원

---

3차원

---

4차원

#### --answer--

2차원

### --question--

#### --text--

단축 속성 `overflow`에 두 값을 지정하면 첫 번째 값을 사용하는 속성은 무엇인가요?

#### --distractors--

`overflow-y`

---

`overflow-w`

---

`overflow-z`

#### --answer--

`overflow-x`

### --question--

#### --text--

CSS에서 `blur()` 함수가 요소에 적용하는 블러 효과는 무엇인가요?

#### --distractors--

Giliani

---

Galilei

---

Galileo

#### --answer--

Gaussian

### --question--

#### --text--

`sepia()` 함수는 어떤 종류의 값을 사용하나요?

#### --distractors--

픽셀

---

라디안

---

도(degrees)

#### --answer--

백분율

### --question--

#### --text--

단축 속성 `overflow`에 두 값을 지정하면 두 번째 값을 사용하는 속성은 무엇인가요?

#### --distractors--

`overflow-w`

---

`overflow-x`

---

`overflow-z`

#### --answer--

`overflow-y`

### --question--

#### --text--

CSS 리셋은 무엇을 하나요?

#### --distractors--

기본적으로 브라우저를 다크 모드로 강제 설정합니다.

---

HTML 요소에서 인라인 스타일을 제거합니다.

---

CSS를 포함하는 브라우저 확장 기능을 비활성화합니다.

#### --answer--

브라우저 기본 스타일을 제거합니다.

### --question--

#### --text--

CSS 속성 `filter: grayscale(100%);`는 요소에 어떤 효과를 주나요?

#### --distractors--

요소에 아무 영향도 주지 않습니다.

---

요소를 완전히 검은색으로 만듭니다.

---

요소를 완전히 흰색으로 만듭니다.

#### --answer--

요소의 색상 채도를 제거하면서 톤 구조는 유지합니다.
