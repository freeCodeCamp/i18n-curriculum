---
id: 6733ab12b60bd7f6b2b0b0c0
title: 音频构造函数如何工作，以及一些常见的方法是什么？
challengeType: 19
dashedName: how-does-the-audio-constructor-work-and-what-are-some-common-methods
---

# --description--

让我们了解 `Audio` 构造函数及其常用方法。

`Audio` 构造函数，像其他构造函数一样，是一个使用 `new` 关键字调用的特殊函数。它返回一个 `HTMLAudioElement`，你可以用它为用户播放音频，或将其追加到 DOM 中，让用户自行控制。

当你调用构造函数时，可以可选地传入一个 URL 作为（唯一）参数。该 URL 应指向你想播放的音频文件的源。或者，如果你需要动态更改源，可以将 URL 赋值给返回的音频元素的 `src` 属性。

返回的音频元素提供了多种用于控制音频的控件。你最有可能使用 `play()` 方法，它开始音频播放。还有 `pause()` 方法，它暂停音频播放但保留当前轨道位置，允许 `play()` 从该点继续播放。

你可能会期望有一个 `stop()` 方法，用于暂停音频播放并将轨道重置到开头。但实际上，你应该调用 `pause()` 并直接设置 `currentTime` 属性。

最后，`canPlayType()` 方法可用于确定浏览器是否可能能够播放你特定的音频形式。你将在下一课中学习音频和视频形式。

# --questions--

## --text--

当使用 `new` 关键字调用 `Audio` 构造函数时，会返回什么？

## --answers--

一个 `AudioContext` 对象。

### --feedback--

构造函数 `return` 特定类型的 `HTML` 元素。

---

一个 `MediaElement`。

### --feedback--

构造函数 `return` 特定类型的 `HTML` 元素。

---

一个 `HTMLAudioElement`。

---

一个解析为音频文件的 Promise。

### --feedback--

构造函数 `return` 特定类型的 `HTML` 元素。

## --video-solution--

3

## --text--

你如何为一个 `Audio` 对象设置音频文件的来源？

## --answers--

通过将 URL 作为参数传递给 `Audio` 构造函数。

### --feedback--

设置音频源有两种方式。

---

通过为 `audio` 元素的 `src` 属性赋值 URL。

### --feedback--

设置音频源有两种方式。

---

通过使用 `setSource()` 方法。

### --feedback--

设置音频源有两种方式。

---

通过传递 URL 给构造函数或设置 `src` 属性。

## --video-solution--

4

## --text--

你应该使用哪种方法来暂停音频播放并将曲目重置到开头？

## --answers--

调用 `stop()` 方法。

### --feedback--

课程提到了你需要做的两件事来暂停并将音频重置回开头。

---

调用 `reset()` 方法。

### --feedback--

课程提到了你需要做的两件事来暂停并将音频重置回开头。

---

调用 `pause()` 方法，然后设置 `currentTime` 属性。

---

调用 `pause()` 方法，然后调用 `reset()` 方法。

### --feedback--

课程提到了你需要做的两件事来暂停并将音频重置回开头。

## --video-solution--

3
