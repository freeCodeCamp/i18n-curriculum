---
id: 672bb009952c7a7904a750cb
title: ما هي أفضل الممارسات لتصميم التمرير اللانهائي؟
challengeType: 19
dashedName: what-are-best-practices-for-designing-infinite-scrolls
---

# --interactive--

**ملاحظة**: قد تستخدم بعض الأمثلة التفاعلية CSS وJavaScript التي لم تتعلمها بعد. لا تقلق بشأن محاولة فهم كل الشفرة. الهدف من الأمثلة هو عرض معاينات لهذه المفاهيم التصميمية لتفهم كيف تعمل الأمور بشكل أفضل. لعرض المعاينات، يجب تفعيل المحرر التفاعلي.

التمرير اللانهائي هو نمط تصميم يُحمّل المزيد من المحتوى أثناء تمرير المستخدم لأسفل الصفحة. غالبًا ما يُستخدم هذا في مواقع التواصل الاجتماعي مثل Twitter. على سبيل المثال، إذا كنت مسجلاً الدخول وترغب في رؤية المزيد من التغريدات، يمكنك التمرير لأسفل وسيتم تحميل المزيد من التغريدات. هذا مثال على التمرير اللانهائي.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <div class="post">Post 4</div>
  <div class="post">Post 5</div>
  <div class="post">Post 6</div>
  <div class="post">Post 7</div>
  <div class="post">Post 8</div>
  <div class="post">Post 9</div>
  <div class="post">Post 10</div>
  <!-- More posts will load here as the user scrolls down -->
</div>

<script src="index.js"></script>
```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}
```

```js
// Simulate loading more content as the user scrolls
window.addEventListener('scroll', () => {
  if (window.innerHeight + window.scrollY >= document.body.offsetHeight) {
    loadMorePosts();
  }
});

function loadMorePosts() {
  const container = document.querySelector('.infinite-scroll');
  for (let i = 0; i < 3; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${container.children.length + 1}`;
    container.appendChild(post);
  }
}
```

:::

يُستخدم التمرير اللانهائي أيضًا كبديل للتقسيم إلى صفحات. التقسيم إلى صفحات هو نمط تصميم يقسم المحتوى إلى صفحات. يُستخدم هذا غالبًا عندما يكون هناك محتوى كبير للعرض. مثال على التقسيم إلى صفحات هو عندما تبحث عن شيء في Google وترى نتائج البحث موزعة على صفحات متعددة. مع التقسيم إلى صفحات، يجب النقر على زر للانتقال إلى الصفحة التالية. مع التمرير اللانهائي، تستمر في التمرير لأسفل وسيتم تحميل المزيد من المحتوى.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="pagination">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <!-- More posts will be on the next pages -->
</div>
<div class="pagination-controls">
  <button class="prev" disabled>Previous</button>
  <button class="next">Next</button>
</div>
<script src="index.js"></script>
```

```css
.pagination {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.pagination-controls {
  margin-top: 12px;
}

.pagination-controls button {
  padding: 8px 16px;
  margin-right: 8px;
}
```

```js
let currentPage = 1;
const postsPerPage = 3;
const totalPosts = 9; 
const totalPages = Math.ceil(totalPosts / postsPerPage);
const container = document.querySelector('.pagination');
const prevButton = document.querySelector('.prev');
const nextButton = document.querySelector('.next'); 

function renderPosts() {
  container.innerHTML = '';
  const start = (currentPage - 1) * postsPerPage;
  const end = start + postsPerPage;
  for (let i = start; i < end && i < totalPosts; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${i + 1}`;
    container.appendChild(post);
  }
  prevButton.disabled = currentPage === 1;
  nextButton.disabled = currentPage === totalPages;
}

prevButton.addEventListener('click', () => {
  if (currentPage > 1) {
    currentPage--;
    renderPosts();
  }
});

nextButton.addEventListener('click', () => {
  if (currentPage < totalPages) {
    currentPage++;
    renderPosts();
  }
});

renderPosts();
```

:::

عند دمج التمرير اللانهائي في تصميمك، هناك بعض أفضل الممارسات التي يجب وضعها في الاعتبار. الاعتبار الأول هو توفير زر "تحميل المزيد" الذي يحمل مجموعة النتائج التالية عند نقر المستخدم عليه. هذه طريقة جيدة لمنح المستخدم التحكم في متى يريد رؤية المزيد من المحتوى.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <!-- More posts will load when the user clicks "Load More" -->
</div>
<button class="load-more">Load More</button>
<script src="index.js"></script>
```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.load-more {
  margin-top: 12px;
  padding: 8px 16px;
}
```

```js
const loadMoreButton = document.querySelector('.load-more');
const container = document.querySelector('.infinite-scroll');   
loadMoreButton.addEventListener('click', () => {
  loadMorePosts();
});

function loadMorePosts() {
  for (let i = 0; i < 3; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${container.children.length + 1}`;
    container.appendChild(post);
  }
}
```

:::

اعتبار آخر هو إضافة زر "عودة". يمنح هذا المستخدمين القدرة على العودة إلى الصفحة السابقة دون الحاجة إلى التمرير إلى الأعلى بالكامل. هذا يخلق تجربة مستخدم أفضل ويمنحهم مزيدًا من التحكم في تجربة التصفح.

أحيانًا سترى تصاميم تحتوي على زر "العودة إلى الأعلى" الذي يعيد المستخدمين إلى أعلى صفحة النتائج. اعتبار آخر هو توفير مؤشر تحميل. يجب أن يكون لدى المستخدمين مؤشر واضح بأن المزيد من المحتوى يُحمّل؛ وإلا قد يظنون أن الصفحة معطلة.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <div class="post">Post 4</div>
  <div class="post">Post 5</div>
  <div class="post">Post 6</div>
  <div class="post">Post 7</div>
  <div class="post">Post 8</div>
  <div class="post">Post 9</div>
  <div class="post">Post 10</div>
  <!-- More posts will load here as the user scrolls down -->
</div>

<div class="loading-indicator" style="display: none;">Loading...</div>

<button id="back-to-top" class="back-to-top">↑ Back to Top</button>

<script src="index.js"></script>

```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.loading-indicator {
  margin-top: 12px;
  font-weight: bold;
}

.back-to-top {
  position: fixed;
  bottom: 30px;
  right: 30px;
  display: none;
  padding: 10px 15px;
  background-color: #007BFF;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
}

.back-to-top:hover {
  background-color: #0056b3;
}
```

```js
const container = document.querySelector('.infinite-scroll');
const loadingIndicator = document.querySelector('.loading-indicator');
const backToTopBtn = document.getElementById('back-to-top');

window.addEventListener('scroll', () => {
  if (window.scrollY > 400) {
    backToTopBtn.style.display = 'block';
  } else {
    backToTopBtn.style.display = 'none';
  }

  if (window.innerHeight + window.scrollY >= document.body.offsetHeight) {
    loadMorePosts();
  }
});

backToTopBtn.addEventListener('click', () => {
  window.scrollTo({ top: 0, behavior: 'smooth' });
});

function loadMorePosts() {
  loadingIndicator.style.display = 'block';
  setTimeout(() => {
    for (let i = 0; i < 3; i++) {
      const post = document.createElement('div');
      post.className = 'post';
      post.textContent = `Post ${container.children.length + 1}`;
      container.appendChild(post);
    }
    loadingIndicator.style.display = 'none';
  }, 1000);
}
``` 

:::

أحد الاعتبارات الأخيرة هو الحفاظ على تذييل الصفحة متاحًا للمستخدم. إذا كان التذييل يحتوي على معلومات مهمة، فيجب أن يكون متاحًا للمستخدم في جميع الأوقات.

في الختام، التمرير اللانهائي طريقة رائعة لعرض المحتوى على موقعك. مع ذلك، يجب أن تضع في اعتبارك أفضل الممارسات عند تصميم التمرير اللانهائي لتوفر أفضل تجربة مستخدم ممكنة.

# --questions--

## --text--

ما هو التمرير اللانهائي؟

## --answers--

نمط تصميم يغير شريط التمرير في الصفحة.

### --feedback--

راجع بداية الدرس حيث تم تقديم هذا المفهوم.

---

نمط تصميم يغير حجم الصفحة.

### --feedback--

راجع بداية الدرس حيث تم تقديم هذا المفهوم.

---

نمط تصميم يغير لون خلفية الصفحة.

### --feedback--

راجع بداية الدرس حيث تم تقديم هذا المفهوم.

---

نمط تصميم يُحمّل المزيد من المحتوى أثناء تمرير المستخدم لأسفل الصفحة

## --video-solution--

4

## --text--

ما هو دور زر "تحميل المزيد" في التمرير اللانهائي؟

## --answers--

يعيد تحميل الصفحة.

### --feedback--

الاسم يوضح دوره.

---

لمنح المستخدمين التحكم في متى يريدون رؤية المزيد من المحتوى.

---

يسمح للمستخدمين بالعودة إلى المحتوى الذي تم تحميله سابقًا.

### --feedback--

الاسم يوضح دوره.

---

يقيد كمية البيانات التي يمكن تحميلها.

### --feedback--

الاسم يوضح دوره.

## --video-solution--

2

## --text--

لماذا من المهم الحفاظ على تذييل الصفحة متاحًا أثناء استخدام التمرير اللانهائي؟

## --answers--

يساعد في إبراز الألوان أكثر على الصفحة.

### --feedback--

فكر في المعلومات التي يجب أن يكون لدى المستخدم وصول إليها.

---

يساعد في تحميل الصفحة بشكل أسرع.

### --feedback--

فكر في المعلومات التي يجب أن يكون لدى المستخدم وصول إليها.

---

ليس من المهم الحفاظ على تذييل الصفحة متاحًا.

### --feedback--

فكر في المعلومات التي يجب أن يكون لدى المستخدم وصول إليها.

---

لكي يتمكن المستخدم من الوصول إلى الروابط والمعلومات المهمة في التذييل في جميع الأوقات.

## --video-solution--

4
