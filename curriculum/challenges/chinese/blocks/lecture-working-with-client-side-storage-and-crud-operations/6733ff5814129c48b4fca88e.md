---
id: 6733ff5814129c48b4fca88e
title: 什么是 sessionStorage，以及一些常用的方法？
challengeType: 19
dashedName: what-is-sessionstorage-and-what-are-some-common-methods
---

# --description--

在上一课中，我们学习了如何使用 `localStorage`，并简要介绍了 `sessionStorage`。

请记住，`sessionStorage` 是指当用户关闭运行该网页应用的标签（页）或窗口时，数据即被清除。它非常适合数据只需在单个会话期间保持的情况，例如在导航过程中维护表单数据或在结账过程中存储临时状态信息。

与 `localStorage` 类似，`sessionStorage` 使用点对来保存和检索数据。`sessionStorage` 使用的方法也与 `localStorage` 相同，唯一真正的区别在于数据保存的时长。 

以下是使用不同方法的一些示例：

- `sessionStorage.setItem()`：在 `sessionStorage` 中保存一个密钥-值点对。

```js
sessionStorage.setItem('cart', '3 items');
```

- `sessionStorage.getItem()`：从 `sessionStorage` 中检索给定密钥的值。

```js
let cart = sessionStorage.getItem('cart');
console.log(cart); // Outputs: '3 items'
```

- `sessionStorage.removeItem()`：使用其密钥从 `sessionStorage` 中移除特定项。

```js
sessionStorage.removeItem('cart');
```

- `sessionStorage.clear()`：清除存储在 `sessionStorage` 中的所有数据。

```js
sessionStorage.clear();
```

让我们来看一个示例，其中我们将数据存储在 `sessionStorage` 中，该数据仅在浏览器标签（页）或窗口打开期间有效：

```js
// Store data in sessionStorage
sessionStorage.setItem('currentUser', 'JohnDoe');

// Retrieve the stored data
const user = sessionStorage.getItem('currentUser');
console.log(user); // 'JohnDoe'

// Remove a specific key from sessionStorage
sessionStorage.removeItem('currentUser');

// Clear all sessionStorage data
sessionStorage.clear();
```

在这个例子中，我们：

1. 将当前用户的名称（`JohnDoe`）保存到 `sessionStorage` 中。

2. 检索并显示它。

3. 移除与密钥 `currentUser` 关联的项。

4. 清除所有 `sessionStorage` 数据。

与 `localStorage` 的关键区别在于，只要用户关闭标签（页），所有保存的会话数据将会丢失。

`sessionStorage` 在以下场景中特别有用：

- 在多页表单进程中存储临时数据，例如表单条目。

- 存储不需要跨 `Session` 持久化的临时选择或偏好。

- 在不需要在标签（页）关闭后记住状态的单页应用中维护状态。

`sessionStorage` 确保一旦用户离开页面，会话数据就会被清除，这对于你不想在当前会话之外保留信息的场景非常有用。

# --questions--

## --text--

当浏览器标签（页）关闭时，存储在 `sessionStorage` 中的数据会发生什么？

## --answers--

数据会被永久保存，直到手动移除。

### --feedback--

`sessionStorage` 仅在浏览器会话期间持续存在。

---

数据会一直保留，直到计算机关闭。

### --feedback--

`sessionStorage` 仅在浏览器会话期间持续存在。

---

当标签（页）或窗口关闭时，数据将被清除。

---

数据与 `localStorage` 同步。

### --feedback--

`sessionStorage` 仅在浏览器会话期间持续存在。

## --video-solution--

3

## --text--

以下哪种方法用于从 `sessionStorage` 中移除单个密钥值点对？

## --answers--

`sessionStorage.removeItem()`

---

`sessionStorage.deleteItem()`

### --feedback--

方法名以 `remove` 开头，并影响一个特定的项。

---

`sessionStorage.clear()`

### --feedback--

方法名以 `remove` 开头，并影响一个特定的项。

---

`sessionStorage.unset()`

### --feedback--

方法名以 `remove` 开头，并影响一个特定的项。

## --video-solution--

1

## --text--

在以下哪种情况下，使用 `sessionStorage` 比使用 `localStorage` 更合适？

## --answers--

存储跨会话持久化的用户主题设置。

### --feedback--

考虑哪些数据只需要在单个会话中持久保存。

---

存储仅在当前浏览器会话期间需要的表单数据。

---

存储敏感的用户信息，如密码。

### --feedback--

考虑哪些数据只需要在单个会话中持久保存。

---

存储需要在不同浏览器窗口之间共享的 `data`。

### --feedback--

考虑哪些数据只需要在单个会话中持久保存。

## --video-solution--

2
