---
id: 6732719e2e3ad4d947410b65
title: 演算子の優先順位はどのように機能するのか？
challengeType: 19
dashedName: how-does-operator-precedence-work
---

# --interactive--

JavaScriptで複数の演算子を使った式を書くとき、どの演算子を先に評価するかはどう決まるのでしょうか？ここで登場するのが演算子の優先順位です。コード例を使って演算子の優先順位を詳しく見ていき、同じ優先順位の演算子がある場合にどうなるかも説明します。

演算子の優先順位は、式の中でどの操作が先に評価されるかを決めます。優先順位が高い演算子は、低い演算子より先に評価されます。数学で割り算や掛け算が足し算や引き算より先に行われるのと同じように考えてください。

このルールを守らなければ、基本的な計算式でも間違った答えが出てしまいます。JavaScriptも同じ仕組みで、どの演算子が最初に、次に、という順番で評価されるか独自のルールがあります。例えば、次の式を見てください：

:::interactive_editor

```js
const result = 2 + 3 * 4;

console.log(result); // 14
```

:::

もしJavaScriptがこの式を左から右に順番に評価したら、`2 + 3 = 5`の後に`5 * 4 = 20`になるはずです。しかし掛け算は足し算より優先順位が高いため、JavaScriptはまず`3 * 4`の部分を評価し、結果は`2 + 12 = 14`になります。

時には優先順位に関係なく、式の特定の部分を先に評価したいことがあります。その場合は丸括弧、`()`を使います。丸括弧の中は必ず最初に評価されます。前の例で`2 + 3`の部分を先に評価させてみましょう：

:::interactive_editor

```js
const result = (2 + 3) * 4;

console.log(result); // 20
```

:::

上の例では、丸括弧によってJavaScriptはまず`2 + 3`を評価し、その結果に`4`を掛けます。これにより`20`が得られ、`14`とは異なる結果になります。

割り算の演算子も足し算や引き算より優先順位が高いです：

:::interactive_editor

```js
const result = 2 + 6 / 3;

console.log(result); // 4
```

:::

もしJavaScriptがこの式を左から右に評価したら、`2 + 6 = 8`の後に`8 / 3 = 2.67`になるはずです。しかし割り算は足し算より優先順位が高いため、JavaScriptはまず割り算を評価し：`6 / 3 = 2`、その後`2 + 2`を足して結果は`4`になります。

つまり掛け算と割り算は、丸括弧で順序を変えない限り、足し算や引き算より先に必ず行われます。では、もし演算子の優先順位が同じ場合はどうなるでしょう？JavaScriptは結合規則を使って評価順を決めます。

結合規則は、演算子を左から右に評価するか右から左に評価するかをJavaScriptに指示します。足し算や掛け算のような多くの演算子は左から右の結合規則です。つまり、JavaScriptは式の左端から右へ処理します：

:::interactive_editor

```js
const result = 10 - 2 + 3;

console.log(result); // 11
```

:::

まず`10 - 2 = 8`、次に`8 + 3 = 11`です。この場合は左から右に進みます。代入（`=`）のように右から左の結合規則を持つ演算子もあります。これは式の右側が先に評価されることを意味します：

:::interactive_editor

```js
let a, b;
a = b = 5;

console.log(a); // 5
console.log(b); // 5
console.log(a + b); // 10
```

:::

上の例では、JavaScriptはまず`5`を`b`に代入し、その後`b`（今は`5`）を`a`に代入します。

べき乗演算子も右から左の結合規則です：

:::interactive_editor

```js
const result = 2 ** 3 ** 2;

console.log(result); // 512
```

:::

まずJavaScriptは`3 ** 2`を評価し、結果は`9`です。次に`2 ** 9`を評価し、結果は`512`になります。もしべき乗演算子が左から右の結合規則だったら、JavaScriptは最初に`2 ** 3`を計算して`8`を得て、その後`8 ** 2`を計算して`64`を得ていたでしょう。

# --questions--

## --text--

JavaScriptの演算子の優先順位は数学とどう違いますか？

## --answers--

足し算が掛け算や割り算より先に行われる。

### --feedback--

数学の計算での演算の順序を考えてみてください。

---

割り算と掛け算は足し算と引き算より先に行われます。数学と同じです。

---

すべての演算は現れる順に行われます。

### --feedback--

数学の計算での演算の順序を考えてみてください。

---

引き算がすべての演算より優先されます。

### --feedback--

数学の計算での演算の順序を考えてみてください。

## --video-solution--

2

## --text--

JavaScriptで演算子の優先順位をどうやってオーバーライドできますか？

## --answers--

足し算演算子（`+`）を使って演算の順序を変える。

### --feedback--

数学の計算で演算の順序を変える方法を考えてみてください。

---

丸括弧を使って式の特定の部分を先に評価させる。

---

式の中の演算子の順番を逆にする。

### --feedback--

数学の計算で演算の順序を変える方法を考えてみてください。

---

優先順位に関係なくすべて左から右に演算する。

### --feedback--

数学の計算で演算の順序を変える方法を考えてみてください。

## --video-solution--

2

## --text--

JavaScriptはべき乗演算子（`**`）を使った式をどう評価しますか？

## --answers--

左から右に評価する。

### --feedback--

式の中でべき乗がどの順番で行われるかを考えてみてください。

---

右から左に評価し、右端のべき乗を最初に計算する。

---

底を指定された回数だけ自分自身に乗算する。

### --feedback--

式の中でべき乗がどの順番で行われるかを考えてみてください。

---

指数を先に足し合わせてから結果を計算する。

### --feedback--

式の中でべき乗がどの順番で行われるかを考えてみてください。

## --video-solution--

2
