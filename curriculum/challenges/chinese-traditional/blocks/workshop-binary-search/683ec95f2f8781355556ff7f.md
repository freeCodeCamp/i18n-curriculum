---
id: 683ec95f2f8781355556ff7f
title: 第 7 步
challengeType: 20
dashedName: step-7
---

# --description--

現在，將 `value_at_middle` 包含在 `path_to_target` 列表中，以追蹤搜尋過程中所採取的步驟，無論它是否為標的。

# --hints--

你應該使用 `append()` 方法，為你的 `path_to_target` 列表添加 `value_at_middle`。

```js
({
    test: () =>
        assert(
            runPython(
                `_Node(_code).find_function("binary_search").find_whiles()[0].find_body().is_equivalent("mid = (low + high) // 2 \\nvalue_at_middle = search_list[mid] \\npath_to_target.append(value_at_middle)")`))
})
```

# --seed--

## --seed-contents--

```py
def binary_search(search_list, value):
    path_to_target = []
    low = 0
    high = len(search_list) - 1
    
--fcc-editable-region--
    while low <= high:
        mid = (low + high) // 2
        value_at_middle = search_list[mid]
        
--fcc-editable-region--
```
