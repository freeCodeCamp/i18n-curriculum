---
id: 646ce9d790d2a44de5f99e04
title: الخطوة 19
challengeType: 0
dashedName: step-19
---

# --description--

أنشئ عنصرين من نوع `div`، الأول داخل العنصر `.cat-left-ear` مع صنف `cat-left-inner-ear`، والثاني داخل العنصر `.cat-right-ear` مع صنف `cat-right-inner-ear`.

# --hints--

لا تغير عنصر `div` الموجود مع الصنف `cat-left-ear`.

```js
assert.lengthOf(document.querySelectorAll('div.cat-left-ear'), 1);
```

لا تغير عنصر `div` الموجود مع الصنف `cat-right-ear`.

```js
assert.lengthOf(document.querySelectorAll('div.cat-right-ear'), 1);
```

يجب أن يكون لديك عنصر `div` واحد داخل العنصر `.cat-left-ear`.

```js
assert.lengthOf(document.querySelectorAll('.cat-left-ear div'), 1);
```

يجب أن يكون لديك عنصر `div` واحد داخل العنصر `.cat-right-ear`.

```js
assert.lengthOf(document.querySelectorAll('.cat-right-ear div'), 1);
```

يجب أن يحتوي عنصر `div` الجديد داخل `.cat-left-ear` على الصنف `cat-left-inner-ear`.

```js
assert.isTrue(document.querySelectorAll('.cat-left-ear div')[0]?.classList.contains('cat-left-inner-ear'));
```

يجب أن يحتوي عنصر `div` الجديد داخل `.cat-right-ear` على الصنف `cat-right-inner-ear`.

```js
assert.isTrue(document.querySelectorAll('.cat-right-ear div')[0]?.classList.contains('cat-right-inner-ear'));
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>fCC Cat Painting</title>
    <link rel="stylesheet" href="./styles.css">
</head>
<body>
    <main>
      <div class="cat-head">
        <div class="cat-ears">
          --fcc-editable-region--
          <div class="cat-left-ear">

          </div>
          <div class="cat-right-ear">
            
          </div>
          --fcc-editable-region--
        </div>
      </div>
    </main>
</body>
</html>
```

```css
* {
  box-sizing: border-box;
}

body {
  background-color: #c9d2fc;
}

.cat-head {
  position: absolute;
  right: 0;
  left: 0;
  top: 0;
  bottom: 0;
  margin: auto;
  background: linear-gradient(#5e5e5e 85%, #45454f 100%);
  width: 205px;
  height: 180px;
  border: 1px solid #000;
  border-radius: 46%;
}
```
