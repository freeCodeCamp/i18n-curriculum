---
id: 5d822fd413a79914d39e98ce
title: Passaggio 6
challengeType: 0
dashedName: step-6
---

# --description--

Aggiungi anche un `box-sizing` di `border-box` a tutto. Questo farà in modo che il bordo che hai aggiunto non aumenti la dimensione dei tuoi elementi.

# --hints--

Dovresti usare la proprietà `box-sizing`.

```js
assert(new __helpers.CSSHelp(document).isPropertyUsed('box-sizing'));
```

Dovresti usare il selettore `*` esistente.

```js
// Two selectors create two CSSStyleRule objects
assert.equal(new __helpers.CSSHelp(document).getStyleDeclarations('*').length, 1);
```

Tutti gli elementi dovrebbero avere un `box-sizing` di `border-box`.

```js
const astStyles = new __helpers.CSSHelp(document).getStyle('*');
assert.equal(astStyles.boxSizing, 'border-box');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">    
  <head>
    <meta charset="UTF-8">
    <title>City Skyline</title>
    <link href="styles.css" rel="stylesheet" />
  </head>

  <body>
  </body>
</html>
```

```css
--fcc-editable-region--
* {
  border: 1px solid black;
}

--fcc-editable-region--

```
