---
id: 67f412ac59601c4151b763da
title: 딕셔너리와 집합 퀴즈
challengeType: 8
dashedName: quiz-dictionaries-and-sets
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

딕셔너리란 무엇인가요?

#### --distractors--

문자열과 리스트만 저장하는 자료 구조입니다.

---

큰 숫자만 허용하는 키-값 쌍의 모음인 자료 구조입니다.

---

튜플만 저장하는 자료 구조입니다.

#### --answer--

키-값 쌍의 모음인 자료 구조입니다.

### --question--

#### --text--

다음 중 딕셔너리의 올바른 구문은 무엇인가요?

#### --distractors--

```py
dictionary = {
    <key1: value1>,
    <key2: value2>
}
```

---

```py
dictionary = {
    (key1: value1),
    (key2: value2)
}
```

---

```py
dictionary = {
    [key1: value1],
    [key2: value2]
}
```

#### --answer--

```py
dictionary = {
    key1: value1,
    key2: value2
}
```

### --question--

#### --text--

딕셔너리에 대해 올바른 설명은 무엇인가요?

#### --distractors--

키는 최소 두 글자여야 합니다.

---

키에는 특수 문자가 포함되어야 합니다.

---

키에는 최소 하나의 숫자가 포함되어야 합니다.

#### --answer--

키는 고유해야 합니다.

### --question--

#### --text--

다음 중 딕셔너리를 생성하는 데 사용할 수 있는 생성자는 무엇인가요?

#### --distractors--

`diction()`

---

`dic()`

---

`dictionary()`

#### --answer--

`dict()`

### --question--

#### --text--

딕셔너리에서 값을 접근하는 올바른 방법은 무엇인가요?

#### --distractors--

`dictionary<key>`

---

`dictionary/key/`

---

`dictionary{key}`

#### --answer--

`dictionary[key]`

### --question--

#### --text--

딕셔너리에서 값을 업데이트하는 올바른 방법은 무엇인가요?

#### --distractors--

`pizza['name'] >> 'Margherita'`

---

`pizza['name'] << 'Margherita'`

---

`pizza['name'] == 'Margherita'`

#### --answer--

`pizza['name'] = 'Margherita'`

### --question--

#### --text--

키와 연관된 값을 가져오는 데 사용할 수 있는 메서드는 무엇인가요?

#### --distractors--

`access()`

---

`set()`

---

`retrieve()`

#### --answer--

`get()`

### --question--

#### --text--

뷰 객체(view object)란 무엇인가요?

#### --distractors--

문자열을 딕셔너리로 변환하는 데 사용하는 특별한 객체입니다.

---

딕셔너리에 키-값 쌍이 두 개 이상 있을 때만 내용을 볼 수 있는 방법입니다.

---

리스트를 딕셔너리로 변환하는 데 사용하는 특별한 객체입니다.

#### --answer--

데이터의 별도 복사본을 만들지 않고 딕셔너리 내용을 볼 수 있는 방법입니다.

### --question--

#### --text--

딕셔너리의 모든 키-값 쌍을 포함하는 뷰 객체를 반환하는 메서드는 무엇인가요?

#### --distractors--

`lists()`

---

`dictionaries()`

---

`collections()`

#### --answer--

`items()`

### --question--

#### --text--

딕셔너리에서 모든 키-값 쌍을 제거하는 메서드는 무엇인가요?

#### --distractors--

`empty()`

---

`replace()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

다음 코드의 출력 결과는 무엇인가요?

```py
products = {
    'Laptop': 990,
    'Smartphone': 600,
    'Tablet': 250,
    'Headphones': 70,
}

for product in products.items():
    print(product)
```

#### --distractors--

```py
<'Laptop', 990>
<'Smartphone', 600>
<'Tablet', 250>
<'Headphones', 70>
```

---

```py
'Laptop', 990
'Smartphone', 600
'Tablet', 250
'Headphones', 70
```

---

```py
['Laptop', 990]
['Smartphone', 600]
['Tablet', 250]
['Headphones', 70]
```

#### --answer--

```py
('Laptop', 990)
('Smartphone', 600)
('Tablet', 250)
('Headphones', 70)
```

### --question--

#### --text--

집합에 대해 올바른 설명은 무엇인가요?

#### --distractors--

집합은 변경 가능하며 역순으로 정렬되어 있습니다.

---

집합은 변경 가능하며 정렬되어 있습니다.

---

집합은 변경 불가능하며 정렬되어 있지 않습니다.

#### --answer--

집합은 변경 가능하며 정렬되어 있지 않습니다.

### --question--

#### --text--

집합을 생성하는 올바른 방법은 무엇인가요?

#### --distractors--

`my_set = (1, 2, 3, 4, 5)`

---

`my_set = <1, 2, 3, 4, 5>`

---

`my_set = [1, 2, 3, 4, 5]`

#### --answer--

`my_set = {1, 2, 3, 4, 5}`

### --question--

#### --text--

빈 집합을 생성하는 데 사용하는 함수는 무엇인가요?

#### --distractors--

`create_set()`

---

`empty_set()`

---

`sets()`

#### --answer--

`set()`

### --question--

#### --text--

터미널에 출력되는 결과는 무엇인가요?

```py
my_set = {1, 2, 3, 4, 5, 6}
my_set.add(5)

print(my_set)
```

#### --distractors--

`SyntaxError`

---

`RangeError`

---

`{1, 2, 3, 4, 5, 5, 6}`

#### --answer--

`{1, 2, 3, 4, 5, 6}`

### --question--

#### --text--

다음 중 집합이 부분집합인지 확인하는 메서드는 무엇인가요?

#### --distractors--

`issub()`

---

`isset()`

---

`subset()`

#### --answer--

`issubset()`

### --question--

#### --text--

다음 중 두 집합이 서로소인지 확인하는 메서드는 무엇인가요?

#### --distractors--

`joint()`

---

`isjoint()`

---

`disjoint()`

#### --answer--

`isdisjoint()`

### --question--

#### --text--

대칭 차이 연산자(`^`)는 무엇을 하나요?

#### --distractors--

집합들이 공통으로 가진 요소만 포함하는 새 집합을 반환합니다.

---

집합 간 차이를 찾아 첫 번째 집합을 그 결과로 업데이트합니다.

---

첫 번째 집합에만 있고 다른 집합에는 없는 요소들로 새 집합을 반환합니다.

#### --answer--

첫 번째 또는 두 번째 집합 중 하나에만 있고 둘 다에 있지 않은 요소들로 새 집합을 반환합니다.

### --question--

#### --text--

난수 생성을 위해 사용하는 내장 모듈은 무엇인가요?

#### --distractors--

`set_random`

---

`get_random`

---

`rand`

#### --answer--

`random`

### --question--

#### --text--

정규 표현식 작업에 사용하는 모듈은 무엇인가요?

#### --distractors--

`regex`

---

`reg`

---

`r`

#### --answer--

`re`
