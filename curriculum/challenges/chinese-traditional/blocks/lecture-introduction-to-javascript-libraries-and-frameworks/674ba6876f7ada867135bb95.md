---
id: 674ba6876f7ada867135bb95
title: 如何在 React 中匯入和匯出組件？
challengeType: 19
dashedName: how-can-you-import-and-export-components-in-react
---

# --description--

在之前的課程中，你學會了如何在 JavaScript 中使用匯入和匯出。在本課程中，我們將探討如何在 React 中匯入和匯出組件。

在這個範例中，我們有一個 `Cat` 組件，它屬於一個名為 `Cat.jsx` 的檔案。至於副檔名，我們使用 `.jsx` 副檔名，因為這個檔案主要處理 JSX。

這個 `Cat` 組件傳回一個 JSX 標記，其中包含一個標題和一張名為 Mr. Whiskers 的貓的影像。

```jsx
function Cat() {
  return (
    <div className="card">
      <h2>Mr. Whiskers</h2>
      <img
        src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"
        alt="Tuxedo cats running on dirt ground."
      />
    </div>
  );
}
```

如果我們想在另一個檔案中使用我們的 `Cat` 組件，我們需要先像這樣匯出它：

```jsx
function Cat() {
  return (
    <div className="card">
      <h2>Mr. Whiskers</h2>
      <img
        src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"
        alt="Tuxedo cats running on dirt ground."
      />
    </div>
  );
}

export default Cat;
```

我們使用 `default` 關鍵字，因為這將會是該模組的預設匯出。你也可以選擇在組件定義的同一行匯出該組件，如下所示：

```jsx
export default function Cat() {
  return (
    <div className="card">
      <h2>Mr. Whiskers</h2>
      <img
        src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"
        alt="Tuxedo cats running on dirt ground."
      />
    </div>
  );
}
```

你可以選擇在其他父組件檔案中匯入子組件，或是在根組件檔案中匯入它們。以此範例，我們將在根組件檔案中匯入 `Cat` 組件。

每個 React 專案都會有一個頂層組件，通常稱為 `App.jsx`：

```jsx
export default function App() {
  return (
    // return component here
  );
}
```

此檔案通常位於你專案的 `src` 目錄中。你將在未來的課程中學習更多關於常見專案佈局的內容。

要在根 `App` 組件中使用 `Cat` 組件，你需要這樣匯入它：

```jsx
import Cat from "./Cat";

export default function App() {
  return (
    // return component here
  );
}
```

現在，你可以像這樣在 `App` 組件內傳回 `Cat` 組件：

```jsx
import Cat from "./Cat";

export default function App() {
  return (
    <Cat />
  );
}
```

當你繼續建置你自己的 React 專案時，你會越來越熟悉如何組織組件、在需要的地方匯入它們，並透過組合這些模組化組件來建立複雜的使用者介面。

# --questions--

## --text--

以下哪一種是匯出 React 組件的正確方式？

## --answers--

```jsx
export default function Cat() {
  return (
    <div className="card">
      <h2>Mr. Whiskers</h2>
      <img
        src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"
        alt="Tuxedo cats running on dirt ground."
      />
    </div>
  );
}
```

---

```jsx
exportComponent function Cat() {
  return (
    <div className="card">
      <h2>Mr. Whiskers</h2>
      <img
        src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"
        alt="Tuxedo cats running on dirt ground."
      />
    </div>
  );
}
```

### --feedback--

檢視討論此事的中間部分。

---

```jsx
e.default function Cat() {
  return (
    <div className="card">
      <h2>Mr. Whiskers</h2>
      <img
        src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"
        alt="Tuxedo cats running on dirt ground."
      />
    </div>
  );
}
```

### --feedback--

檢視討論此事的中間部分。

---

```jsx
default function Cat() {
  return (
    <div className="card">
      <h2>Mr. Whiskers</h2>
      <img
        src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"
        alt="Tuxedo cats running on dirt ground."
      />
    </div>
  );
}
```

### --feedback--

檢視討論此事的中間部分。

## --video-solution--

1

## --text--

在 React 專案中，你通常會在哪裡匯入像 `Cat` 組件這樣的子組件？

## --answers--

在 `index.html` 檔案內。

### --feedback--

思考在 React 專案中組件通常會在哪裡被組合在一起。

---

在 `cat-photo-app.css` 檔案內。

### --feedback--

思考在 React 專案中組件通常會在哪裡被組合在一起。

---

在父組件或根組件檔案（通常是 `App.jsx`）中。

---

在 public 資料夾中。

### --feedback--

思考在 React 專案中組件通常會在哪裡被組合在一起。

## --video-solution--

3

## --text--

`App` 組件中 `import` 述語的目的為何？

## --answers--

它會自動為 `App` 組件套用樣式。

### --feedback--

請記得你在之前的課程中學會了如何處理匯入。

---

它匯入了 `Cat` 組件，使其能在 `App` 組件中使用。

---

它設定了 `App` 組件的預設狀態。

### --feedback--

請記得你在之前的課程中學會了如何處理匯入。

---

它允許 `App` 執行得更快。

### --feedback--

請記得你在之前的課程中學會了如何處理匯入。

## --video-solution--

2
