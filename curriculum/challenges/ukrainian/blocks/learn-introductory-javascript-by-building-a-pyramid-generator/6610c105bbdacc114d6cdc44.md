---
id: 6610c105bbdacc114d6cdc44
title: Крок 48
challengeType: 1
dashedName: step-48
---

# --description--

Щоб використати функцію, потрібно її викликати. <dfn>Виклик функції</dfn> повідомляє вашому застосунку запустити код із функції в тому місці, де ви її викликаєте. Синтаксис виклику функції — це ім’я функції, за яким ідуть круглі дужки. Наприклад, цей код визначає і викликає функцію `test`.

```js
function test() {

}
test();
```

Викличте вашу функцію `padRow`.

# --hints--

Ви маєте викликати функцію `padRow`.

```js
assert.lengthOf(__helpers.removeJSComments(code).match(/padRow\(\)/g), 2);
```

Тіло вашої функції `padRow` має бути порожнім.

```js
assert.match(padRow.toString(), /\{\s*\}/);
```

# --seed--

## --seed-contents--

```js
const character = "#";
const count = 8;
const rows = [];

--fcc-editable-region--
function padRow() {

}

--fcc-editable-region--


for (let i = 0; i < count; i = i + 1) {
  rows.push(character.repeat(i + 1))
}

let result = ""

for (const row of rows) {
  result = result + row + "\n";
}

console.log(result);
```
