---
id: 672bb015cfc889794359c4e0
title: ما هي أفضل الممارسات لتصميم نوافذ الحوار المنبثقة؟
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**ملاحظة**: قد تستخدم بعض الأمثلة التفاعلية CSS وJavaScript التي لم تتعلمها بعد. لا تقلق بشأن محاولة فهم كل الشفرة. الهدف من الأمثلة هو عرض معاينات لهذه المفاهيم التصميمية لتفهم كيف تعمل الأمور بشكل أفضل. لرؤية المعاينات، يجب تفعيل المحرر التفاعلي.

ما هو النافذة المنبثقة (modal)؟ هي نوع من النوافذ التي قد يعرضها موقع الويب فوق محتواه. يحتوي HTML على عنصر `dialog` يمكنك استخدامه لإنشاء نوافذ منبثقة.

عادةً ما يُخفّض سطوع المحتوى خلف النافذة المنبثقة. هذا يساعد المستخدم على التركيز بصريًا على المساحة التي تريد منه التفاعل معها – في هذه الحالة، النافذة المنبثقة.

من الجيد دائمًا السماح للمستخدم بالنقر خارج النافذة المنبثقة لإغلاقها.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

غالبًا ما ترى أزرارًا بارزة جدًا على النوافذ المنبثقة. تُسمى هذه CTAs، أو دعوة لاتخاذ إجراء. تريد أن تكون هذه الأزرار سهلة التعرف عليها لأن الغرض من مقاطعة تدفق المستخدم بالنافذة المنبثقة هو حثه على اتخاذ إجراء محدد.

يجب أن تحتوي النوافذ المنبثقة أيضًا على زر إغلاق. رغم أنك قد ترغب حقًا في أن ينقر المستخدم على CTAs الخاصة بك، من المهم أن تمنحه خيارًا للخروج من النافذة المنبثقة واستئناف ما كان يفعله سابقًا.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

بالطبع هناك مخاوف تتعلق بإمكانية الوصول مع النوافذ المنبثقة، مثل إدارة التركيز على العناصر بشكل صحيح. ومع ذلك، إذا استخدمت هذه الممارسات العامة كنقطة بداية، سيكون لديك أساس قوي للبناء عليه.

# --questions--

## --text--

ما هو الغرض من تخفيف سطوع المحتوى خلف نافذة الحوار المنبثقة؟

## --answers--

لحفظ عمر البطارية على الأجهزة المحمولة.

### --feedback--

الدرس يذكر سببًا محددًا لهذا التأثير البصري.

---

لمساعدة المستخدمين على تحديد المساحة التي يجب عليهم التفاعل معها بصريًا.

---

لجعل الموقع يحمل بشكل أسرع.

### --feedback--

الدرس يذكر سببًا محددًا لهذا التأثير البصري.

---

لمنع المستخدمين من رؤية معلومات حساسة.

### --feedback--

الدرس يذكر سببًا محددًا لهذا التأثير البصري.

## --video-solution--

2

## --text--

ماذا تعني CTA في سياق تصميم النوافذ المنبثقة؟

## --answers--

النقر للتفعيل.

### --feedback--

الدرس يوضح المصطلح الكامل لهذا الاختصار عند مناقشة الأزرار البارزة في النوافذ المنبثقة.

---

إكمال الإجراء.

### --feedback--

الدرس يوضح المصطلح الكامل لهذا الاختصار عند مناقشة الأزرار البارزة في النوافذ المنبثقة.

---

دعوة لاتخاذ إجراء.

---

إغلاق التطبيق.

### --feedback--

الدرس يوضح المصطلح الكامل لهذا الاختصار عند مناقشة الأزرار البارزة في النوافذ المنبثقة.

## --video-solution--

3

## --text--

لماذا من المهم تضمين زر إغلاق في نافذة الحوار المنبثقة؟

## --answers--

هذا مطلوب قانونيًا في بعض الدول.

### --feedback--

الدرس يؤكد أهمية السماح للمستخدمين بالخروج من النافذة المنبثقة.

---

لمنع النافذة المنبثقة من تعطيل المتصفح.

### --feedback--

الدرس يؤكد أهمية السماح للمستخدمين بالخروج من النافذة المنبثقة.

---

لمنح المستخدمين خيار الخروج من النافذة المنبثقة واستئناف نشاطهم السابق.

---

لجعل النافذة المنبثقة تبدو أكثر احترافية.

### --feedback--

الدرس يؤكد أهمية السماح للمستخدمين بالخروج من النافذة المنبثقة.

## --video-solution--

3
