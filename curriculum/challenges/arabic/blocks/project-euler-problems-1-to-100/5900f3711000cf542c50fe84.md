---
id: 5900f3711000cf542c50fe84
title: "المشكلة 5: أصغر مضاعف"
challengeType: 1
forumTopicId: 302160
dashedName: problem-5-smallest-multiple
---

# --description--

2520 هو أصغر عدد يمكن قسمته على كل الأعداد من 1 إلى 10 بدون أي باقي.

ما هو أصغر عدد موجب يقبل القسمة بالتساوي على كل الأعداد من 1 إلى `n`؟

# --hints--

`smallestMult(5)` يجب أن تُرجع عددًا.

```js
assert.isNumber(smallestMult(5));
```

`smallestMult(5)` يجب أن تُرجع 60.

```js
assert.strictEqual(smallestMult(5), 60);
```

`smallestMult(7)` يجب أن تُرجع 420.

```js
assert.strictEqual(smallestMult(7), 420);
```

`smallestMult(10)` يجب أن تُرجع 2520.

```js
assert.strictEqual(smallestMult(10), 2520);
```

`smallestMult(13)` يجب أن تُرجع 360360.

```js
assert.strictEqual(smallestMult(13), 360360);
```

`smallestMult(20)` يجب أن تُرجع 232792560.

```js
assert.strictEqual(smallestMult(20), 232792560);
```

# --seed--

## --seed-contents--

```js
function smallestMult(n) {

  return true;
}

smallestMult(20);
```

# --solutions--

```js
function smallestMult(n){
  function gcd(a, b) {
    return b === 0 ? a : gcd(b, a%b); // Euclidean algorithm
  }

  function lcm(a, b) {
    return a * b / gcd(a, b);
  }
  var result = 1;
  for(var i = 2; i <= n; i++) {
    result = lcm(result, i);
  }
  return result;
}
```
