---
id: 65c63da2ed6769a10e141341
title: Крок 20
challengeType: 20
dashedName: step-20
---

# --description--

Тепер ви маєте визначити базовий регістр для рекурсивного пошуку. Видаліть поточний `pass` і напишіть інструкцію `if`, яка перевіряє дві умови:

- Якщо `node` є `None`: це означає, що пошук досяг кінця гілки, не знайшовши ключ.
- Якщо `node.key == key`: це означає, що ключ знайдено у поточному вузлі.

Об’єднайте дві умови оператором `or` і поверніть поточний вузол у блоці `if`.

# --hints--

Ви маєте видалити ключове слово `pass` з методу `_search`.

```js
({
  test: () => {
    assert.isFalse(
      runPython(
        `_Node(_code).find_class("BinarySearchTree").find_function("_search").has_pass()`
      )
    );
  },
});

```

Ви маєте написати інструкцію `if`, яка перевіряє, чи `node` є `None` або чи `node.key` дорівнює `key`.

```js
({ test: () => assert.match(code, /^\s{8}if\s+node\s+is\s+None\s+or\s+node\.key\s*==\s*key\s*:/m) })
```

Ви маєте повернути `node` з блоку `if`.

```js
({ test: () => assert.match(code, /^(\s{8})if\s+node\s+is\s+None\s+or\s+node\.key\s*==\s*key\s*:\s*^\1\s{4}return\s+node/m) })
```

# --seed--

## --seed-contents--

```py

class TreeNode:

    def __init__(self, key):
        self.key = key
        self.left = None
        self.right = None


class BinarySearchTree:

    def __init__(self):
        self.root = None

    def _insert(self, node, key):
        if node is None:
            return TreeNode(key)

        if key < node.key:
            node.left = self._insert(node.left, key)
        elif key > node.key:

            node.right = self._insert(node.right, key)
        return node

    def insert(self, key):
        self.root = self._insert(self.root, key)
        
--fcc-editable-region--
    def _search(self, node, key):
        pass

--fcc-editable-region--
```
