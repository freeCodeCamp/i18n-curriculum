---
id: 66f1a2009e65c9a40a26d51e
title: Reactの基本クイズ
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

クイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

次のうち、JavaScriptのライブラリまたはフレームワークではないものはどれですか？

#### --distractors--

Angular

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

Reactで条件付きレンダリングを行う方法として正しくないものはどれですか？

#### --distractors--

三項演算子。

---

論理`&&`演算子。

---

`if`文。

#### --answer--

`setTimeout`

### --question--

#### --text--

Reactでリストをレンダリングするときに`key`プロップを追加する主な目的は何ですか？

#### --distractors--

各リスト項目にユニークなCSSスタイルを適用するため。

---

要素のテキストコンテンツとして使うため。

---

リスト内の各特定の項目にイベントリスナーを追加するため。

#### --answer--

Reactが効率的にレンダリングするために項目を識別できるようにするため。

### --question--

#### --text--

プロジェクトのパッケージやライブラリはどこにインストールされますか？

#### --distractors--

コンピューターのユーザーディレクトリ。

---

`package.json`ファイル。

---

`src`フォルダー。

#### --answer--

`node_modules`フォルダー。

### --question--

#### --text--

JSXについて正しいものはどれですか？

#### --distractors--

JSXは常にJavaScriptとは別のファイルに書く必要がある。

---

JSXはJavaScriptの式を使えない。

---

JSXはReactアプリケーションには推奨されない。

#### --answer--

JSXはJavaScriptコード内にHTMLを書くことを可能にする。

### --question--

#### --text--

SPAに共通する問題はどれですか？

#### --distractors--

ページのレンダリングが常に行われるためサーバーリソースが多く使われる。

---

頻繁なDOMの更新によりブラウザのキャッシュが信頼できなくなる。

---

複数のページ読み込みで状態管理が一貫しなくなる。

#### --answer--

スクリーンリーダーが動的に更新されるコンテンツに対応しづらい。

### --question--

#### --text--

シングルページアプリケーションは通常どのようにビューを更新しますか？

#### --distractors--

サーバーを完全にリロードすることで。

---

HTMLドキュメント全体をリロードすることで。

---

サーバーサイドレンダリングのみを使うことで。

#### --answer--

ページの必要な部分だけを更新することで。

### --question--

#### --text--

Reactプロジェクトを開発モードで起動するコマンドはどれですか？

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

次のうち有効なJSX要素でないものはどれですか？

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

JavaScriptの配列メソッドで、データの配列をJSX要素の配列に変換するのによく使われるものはどれですか？

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

Reactで`key`プロップを渡す際のベストプラクティスはどれですか？

#### --distractors--

すべての場合に配列のインデックスを使う。

---

レンダリングごとにランダムなキーを生成する。

---

`key`プロップを完全に省略する。

#### --answer--

データからユニークで安定したIDを使う。

### --question--

#### --text--

`package.json`にリストされた依存関係をインストールするコマンドはどれですか？

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

子コンポーネントに`name`プロップを渡すにはどうしますか？

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

Reactでスプレッド演算子を使って複数のプロップスを渡す正しい方法はどれですか？

#### --distractors--

`<Child person={name..., age..., city...} />`。

---

`<Child spreader={...person} />`。

---

`<Child spread={person} />`。

#### --answer--

`<Child {...person} />`。

### --question--

#### --text--

Reactの基本的な構成要素は何ですか？

#### --distractors--

関数とモジュール。

---

DOM要素とイベント。

---

クラスとオブジェクト。

#### --answer--

再利用可能なコンポーネント。

### --question--

#### --text--

Reactでインラインスタイルを追加する正しい方法はどれですか？

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

Reactコンポーネントを条件付きでレンダリングする正しい方法はどれですか？

#### --distractors--

JSX内で`if`文を使う。

---

`render()`メソッド内で新しいコンポーネントを宣言する。

---

JSX内で`while`ループを使う。

#### --answer--

JSX内で三項演算子や論理`&&`演算子を使う。

### --question--

#### --text--

コンポーネントを別のファイルに取り込むために使うキーワードはどれですか？

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

SPAの正式名称は何ですか？

#### --distractors--

サーバーページアーキテクチャ。

---

選択的ページアプリケーション。

---

静的プログレッシブアプリ。

#### --answer--

シングルページアプリケーション。

### --question--

#### --text--

ReactでUIを記述するために使われるJavaScript構文の拡張はどれですか？

#### --distractors--

TSC

---

HTML+

---

ReactML

#### --answer--

JSX
