---
id: 66ea845f54195c7020cc521b
title: Paso 5
challengeType: 1
dashedName: step-5
---

# --description--

La fórmula para generar un número aleatorio entre dos valores es la siguiente:

```js
Math.random() * (maximum - minimum) + minimum;
```

Esto producirá un resultado que es un número de punto flotante entre dos valores.

Crea una variable llamada `randomNum2` y asígnale el resultado de generar un valor entre los valores `min` y `max`.

Luego, registra la variable `randomNum2` para ver el resultado. Intenta actualizar la página para ver diferentes resultados.

# --hints--

Debe tener una variable llamada `randomNum2`.

```js
const explorer = await __helpers.Explorer(code);
assert.exists(explorer.variables.randomNum2);
```

Su variable `randomNum2` debe ser un número.

```js
assert.isNumber(randomNum2);
```

Debe asignar el resultado de generar un valor entre los valores `min` y `max`. Asegúrate de no establecer un valor fijo.  You should use the formula provided above.

```js
assert.isAtLeast(randomNum2, min);
assert.isAtMost(randomNum2, max);

const explorer = await __helpers.Explorer(code);
const expression = explorer.variables.randomNum2?.value.toString();
assert.include(expression, "Math.random()");
assert.match(expression, /\bmin\b/);
assert.match(expression, /\bmax\b/);
```

Debe registrar la variable `randomNum2` en la consola.

```js
assert.match(code, /console\.log\(\s*randomNum2\s*\);?/);
```

# --seed--

## --seed-contents--

```js
const botName = "MathBot";
const greeting = `Hi there! My name is ${botName} and I am here to teach you about the Math object!`;

console.log(greeting);

console.log("The Math.random() method returns a pseudo random number greater than or equal to 0 and less than 1.");

const randomNum = Math.random();
console.log(randomNum);

console.log("Now, generate a random number between two values.");

const min = 1;
const max = 100;
--fcc-editable-region--

--fcc-editable-region--
```
