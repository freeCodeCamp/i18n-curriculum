---
id: 68910fc037a90c285107af04
title: "Ni nini mbinu ya kugawanya na kushinda, na algoriti ya merge sort hufanya kazi vipi?"
challengeType: 19
dashedName: what-is-divide-and-conquer-and-how-does-merge-sort-work
---

# --description--

Mbinu ya kugawanya na kushinda katika sayansi ya kompyuta ni mbinu ya kugawanya changamoto kuwa changamoto ndogo ndogo kwa kutumia mchakato wa kurudia. Moja ya vipengele muhimu vya mbinu hii ni kurudia, ambayo hutokea wakati kitendakazi kinapojitoa wito mara kwa mara hadi kifikie kesi ya msingi. Katika funzo hili, tutaangalia algoriti ya merge sort ili kuelewa vyema jinsi mbinu ya kugawanya na kushinda inavyofanya kazi.

Tuseme tuna orodha hii ya nambari:

```md
42 37 53 17
```

Lengo ni kupanga orodha hiyo kutoka ndogo hadi kubwa kwa kutumia algoriti ya merge sort. Hatua ya kwanza ni kugawanya orodha hiyo nusu:

```md
42 37 | 53 17
```

Kisha tunahitaji kuangalia upande wa kushoto wa orodha:

```md
42 37
```

Tunachukua orodha ndogo hiyo na kuigawanya tena nusu hadi kila orodha ndogo iwe na kipengee kimoja tu:

```md
42 | 37
```

Orodha yenye kipengele kimoja tu imepangwa kwa chaguo-msingi. Ifuatayo tunahitaji kuunganisha kila orodha ndogo yenye kipengele kimoja kuwa orodha iliyopangwa:

```md
37 42
```

Kisha tunafuata mchakato ule ule kwa upande wa kulia wa orodha ya awali:

```py
# right side of original list
53 17

# divide the list in half
53 | 17

# merge the lists in sorted order
17 53
```

Sasa kwamba nusu zote mbili za orodha ya awali zimepangwa, tunaunganisha nusu hizo mbili pamoja na kupanga vipengele:

```markdown
17 37 42 53
```

Hivi ndivyo algoriti inavyoonekana katika msimbo:

```py
def merge_sort(arr):
    if len(arr) <= 1:
        return arr

    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])

    sorted_list = []
    i = 0
    j = 0

    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            sorted_list.append(left[i])
            i += 1
        else:
            sorted_list.append(right[j])
            j += 1

    sorted_list.extend(left[i:])
    sorted_list.extend(right[j:])

    return sorted_list
```

Ugumu wa wakati kwa algoriti ya merge sort utakuwa `O(n log n)` kwa sababu orodha hugawanywa mara kwa mara nusu (`log n`) kisha kuunganishwa pamoja (`O(n)`). Tofauti na algoriti nyingine za kupanga kama bubble sort, merge sort haipangi mahali pake na ina ugumu wa nafasi wa `O(n)`.

# --questions--

## --text--

Mbinu ya kugawanya na kushinda katika sayansi ya kompyuta ni nini?

## --answers--

Mbinu ya kugundua mzunguko katika majaribio ya thamani za kitendakazi kwa kutumia tu waendeshaji wawili.

### --feedback--

Kagua mwanzo wa funzo.

---

Algoriti ya kulinganisha vipengele viwili na kubadilishana kutoka kidogo hadi kikubwa ikiwa inahitajika.

### --feedback--

Kagua mwanzo wa funzo.

---

Mbinu ya kugawanya changamoto kuwa changamoto ndogo ndogo kwa kurudia.

---

Algoriti ya kuhesabu mtandao mfupi zaidi unaounganisha pointi katika ndege.

### --feedback--

Kagua mwanzo wa funzo.

## --video-solution--

3

## --text--

Ugumu wa wakati kwa algoriti ya merge sort ni upi?

## --answers--

`O(n log n)`

---

`O(log n²)`

### --feedback--

Kagua mwisho wa funzo.

---

`O(n³ log n)`

### --feedback--

Kagua mwisho wa funzo.

---

`O(log n³)`

### --feedback--

Kagua mwisho wa funzo.

## --video-solution--

1

## --text--

Ugumu wa nafasi kwa algoriti ya merge sort ni upi?

## --answers--

`O(n²)`

### --feedback--

Kagua mwisho wa funzo.

---

`O(1)`

### --feedback--

Kagua mwisho wa funzo.

---

`O(n log n)`

### --feedback--

Kagua mwisho wa funzo.

---

`O(n)`

## --video-solution--

4
