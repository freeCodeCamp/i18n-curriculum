---
id: 5900f3711000cf542c50fe84
title: '問題 5：最小倍數'
challengeType: 1
forumTopicId: 302160
dashedName: problem-5-smallest-multiple
---

# --description--

2520 是能被 1 到 10 之間每個數字整除且無任何餘數的最小數字。

從 1 到 `n` 的所有數字中，最小且能被整除的正整數是什麼？

# --hints--

`smallestMult(5)` 應該傳回一個數字。

```js
assert.isNumber(smallestMult(5));
```

`smallestMult(5)` 應該傳回 60。

```js
assert.strictEqual(smallestMult(5), 60);
```

`smallestMult(7)` 應該傳回 420。

```js
assert.strictEqual(smallestMult(7), 420);
```

`smallestMult(10)` 應該傳回 2520。

```js
assert.strictEqual(smallestMult(10), 2520);
```

`smallestMult(13)` 應該傳回 360360。

```js
assert.strictEqual(smallestMult(13), 360360);
```

`smallestMult(20)` 應該傳回 232792560。

```js
assert.strictEqual(smallestMult(20), 232792560);
```

# --seed--

## --seed-contents--

```js
function smallestMult(n) {

  return true;
}

smallestMult(20);
```

# --solutions--

```js
function smallestMult(n){
  function gcd(a, b) {
    return b === 0 ? a : gcd(b, a%b); // Euclidean algorithm
  }

  function lcm(a, b) {
    return a * b / gcd(a, b);
  }
  var result = 1;
  for(var i = 2; i <= n; i++) {
    result = lcm(result, i);
  }
  return result;
}
```
