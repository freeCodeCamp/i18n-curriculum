---
id: 671a872c17382a582e455c4c
title: Повторення таблиць і форм HTML
challengeType: 31
dashedName: review-html-tables-and-forms
---

# --interactive--

## Елементи та атрибути форми HTML

- **елемент `form`**: використовується для створення HTML-форми для введення користувачем.
- **атрибут `action`**: використовується для вказівки URL, куди мають бути надіслані дані форми.
- **атрибут `method`**: використовується для вказівки HTTP-методу, який слід застосувати при надсиланні даних форми. Найпоширеніші методи — `GET` і `POST`.

```html
<form method="value-goes-here" action="url-goes-here">
  <!-- inputs go inside here -->
</form>
```

- **елемент `input`**: використовується для створення поля введення для користувача.
- **атрибут `type`**: використовується для вказівки типу поля введення. Наприклад, `text`, `email`, `number`, `radio`, `checkbox` тощо.
- **атрибут `placeholder`**: використовується, щоб показати підказку користувачу, що вводити у поле введення.
- **атрибут `value`**: використовується для вказівки значення поля введення. Якщо поле має тип `button`, атрибут `value` можна використати для встановлення тексту кнопки.
- **атрибут `name`**: використовується, щоб призначити ім’я полю введення, яке слугує ключем при відправленні даних форми. Для радіокнопок, якщо їм призначити однаковий `name`, вони групуються, і можна вибрати лише одну опцію з групи одночасно.
- **атрибут `size`**: використовується для визначення кількості символів, які мають бути видимі під час введення в поле.
- **атрибут `min`**: можна використовувати з типами введення, такими як `number`, щоб вказати мінімальне допустиме значення в полі введення.
- **атрибут `max`**: можна використовувати з типами введення, такими як `number`, щоб вказати максимальне допустиме значення в полі введення.
- **атрибут `minlength`**: використовується для вказівки мінімальної кількості символів, необхідних у полі введення.
- **атрибут `maxlength`**: використовується для вказівки максимальної кількості символів, дозволених у полі введення.
- **атрибут `required`**: використовується, щоб вказати, що поле введення має бути заповнене перед відправленням форми.
- **атрибут `disabled`**: використовується, щоб вказати, що поле введення має бути вимкнене.
- **атрибут `readonly`**: використовується, щоб вказати, що поле введення є лише для читання.

:::interactive_editor

```html
<!-- Text input -->
<input 
  type="text"
  id="name"
  name="name"
  placeholder="e.g. Quincy Larson" 
  size="20"
  minlength="5"
  maxlength="30"
  required
/>

<!-- Number input -->
<input 
  type="number"
  id="quantity"
  name="quantity"
  min="2"
  max="10"
  disabled
/>

<!-- Button -->
<input type="button" value="Show Alert" />
```

:::

- **елемент `label`**: використовується для створення мітки для поля введення.
- **атрибут `for`**: використовується для вказівки, для якого поля введення призначена мітка.
- **Неявне зв’язування форми**: поля введення можна пов’язати з мітками, обгорнувши поле введення всередині елемента `label`.

:::interactive_editor

```html
<form action="">
  <label>
    Full Name:
    <input type="text" />
  </label>
</form>
```

:::

- **Явне зв’язування форми**: поля введення можна пов’язати з мітками, використовуючи атрибут `for` на елементі `label`.

:::interactive_editor

```html
<form action="">
  <label for="email">Email Address: </label>
  <input type="email" id="email" />
</form>
```

:::

- **елемент `button`**: використовується для створення активної кнопки. Кнопка також може мати атрибут `type`, який контролює поведінку кнопки при її активації. Наприклад, `submit`, `reset`, `button`.

:::interactive_editor

```html
<button type="button">Show Form</button>
<button type="submit">Submit Form</button>
<button type="reset">Reset Form</button>
```

:::

- **елемент `fieldset`**: використовується для групування пов’язаних полів введення.
- **елемент `legend`**: використовується для додавання підпису, що описує групу полів введення.

:::interactive_editor

```html
<!-- Radio group -->
<fieldset>
  <legend>Was this your first time at our hotel?</legend>

  <label for="yes-option">Yes</label>
  <input id="yes-option" type="radio" name="hotel-stay" value="yes" />

  <label for="no-option">No</label>
  <input id="no-option" type="radio" name="hotel-stay" value="no" />
</fieldset>

<!-- Checkbox group -->
<fieldset>
  <legend>
    Why did you choose to stay at our hotel? (Check all that apply)
  </legend>

  <label for="location">Location</label>
  <input type="checkbox" id="location" name="location" value="location" />

  <label for="price">Price</label>
  <input type="checkbox" id="price" name="price" value="price" />
</fieldset>
```

:::

- **Стан фокусу**: це стан поля введення, коли воно вибране користувачем.

## Робота з елементами та атрибутами таблиць HTML

- **елемент таблиці**: використовується для створення HTML-таблиці.
- **елемент заголовка таблиці (`thead`)**: використовується для групування заголовкового вмісту в HTML-таблиці.
- **елемент рядка таблиці (`tr`)**: використовується для створення рядка в HTML-таблиці.
- **елемент заголовної клітинки таблиці (`th`)**: використовується для створення заголовної клітинки в HTML-таблиці.
- **елемент тіла таблиці (`tbody`)**: використовується для групування основного вмісту в HTML-таблиці.
- **елемент клітинки даних таблиці (`td`)**: використовується для створення клітинки з даними в HTML-таблиці.
- **елемент нижнього колонтитула таблиці (`tfoot`)**: використовується для групування вмісту нижнього колонтитула в HTML-таблиці.
- **елемент `caption`**: використовується для додавання заголовка HTML-таблиці.
- **атрибут `colspan`**: використовується для вказівки кількості стовпців, які має охоплювати клітинка таблиці.

:::interactive_editor

```html
<table>
  <caption>Exam Grades</caption>

  <thead>
    <tr>
      <th>Last Name</th>
      <th>First Name</th>
      <th>Grade</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>Davis</td>
      <td>Alex</td>
      <td>54</td>
    </tr>

    <tr>
      <td>Doe</td>
      <td>Samantha</td>
      <td>92</td>
    </tr>

    <tr>
      <td>Rodriguez</td>
      <td>Marcus</td>
      <td>88</td>
    </tr>
  </tbody>

  <tfoot>
    <tr>
      <td colspan="2">Average Grade</td>
      <td>78</td>
    </tr>
  </tfoot>
</table>
```

:::

## Робота з інструментами HTML

- **HTML-валідатор**: інструмент, який перевіряє синтаксис HTML-коду, щоб переконатися, що він є дійсним.
- **інспектор DOM**: інструмент, який дозволяє переглядати та змінювати структуру HTML вебсторінки.
- **інструменти розробника**: набір інструментів веброзробника, вбудованих безпосередньо в браузер, які допомагають налагоджувати, профілювати та аналізувати вебсторінки.

# --assignment--

Повторіть теми та концепції таблиць і форм HTML.
