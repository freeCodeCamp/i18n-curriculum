---
id: 66ed902df45ce3ece4053ebc
title: HTML 表格和表单测验
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪个属性用于指定发送表单数据的 URL？

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

哪两个元素用于指定表格中的行和行标题？

#### --distractors--

`r` 元素用于行，`thead` 元素用于行标题。

---

`row` 元素用于行，`th` 元素用于行标题。

---

`tr` 元素用于行，`head` 元素用于行标题。

#### --answer--

`tr` 元素用于行，`th` 元素用于行标题。

### --question--

#### --text--

`td` 元素的用途是什么？

#### --distractors--

用于定义表格数据类型。

---

用于合并两列。

---

用于合并两行。

#### --answer--

用于定义表格单元格。

### --question--

#### --text--

`colspan` 属性的功能是什么？

#### --distractors--

定义表格中列的长度。

---

删除列中文本之间的分隔线。

---

用于定义列数。

#### --answer--

定义表格单元格应跨越的列数。

### --question--

#### --text--

`placeholder` 属性的作用是什么？

#### --distractors--

用于在表单提交时向服务器发送数据。

---

用于将 `label` 元素与 `input` 元素关联起来。

---

用于指定输入字段对用户是只读的。

#### --answer--

用于为输入字段提供提示。

### --question--

#### --text--

`thead` 元素的作用是什么？

#### --distractors--

用于在 HTML 表格中对表体内容进行分组。

---

用于在 HTML 表格中对描述进行分组。

---

用于在 HTML 表格中对表脚内容进行分组。

#### --answer--

用于在 HTML 表格中对表头内容进行分组。

### --question--

#### --text--

以下哪项是标签和输入之间显式表单关联的示例？

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

HTML 校验器的用途是什么？

#### --distractors--

校验器是一种能自动格式化 HTML 代码的工具。

---

校验器是一种对 HTML 应用样式的工具。

---

校验器是一种能让 HTML 代码运行得更快的工具。

#### --answer--

校验器器是一种检查 HTML 代码语法以确保其有效的工具。

### --question--

#### --text--

哪个元素用于定义表格中的单元格？

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

以下哪项是可以检查和修改网页 HTML 结构的工具？

#### --distractors--

DOM 移除器。

---

DOM 测试器。

---

DOM 校验器。

#### --answer--

DOM 检查器。

### --question--

#### --text--

以下哪项是对相关输入字段进行分组的正确方法？

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

以下哪项用于指定输入字段为只读字段？

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

哪个属性规定在提交表单前必须填写输入内容？

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

开发者工具的用途是什么？

#### --distractors--

这些工具直接内置在浏览器中，用于发现代码中的任何内核问题。

---

这些工具直接内置在浏览器中，用于自动格式化代码。

---

这些工具直接内置在浏览器中，用于帮助你确保代码 100% 的测试覆盖率。

#### --answer--

这些工具直接内置在浏览器中，用于帮助你调试、剖析和分析网页。

### --question--

#### --text--

以下哪项是禁用输入的正确方法？

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

以下哪项是 `type` 属性的有效值？

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

以下哪项是 `size` 属性的正确用法？

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

以下哪些属性用于指定输入字段所需的最少和最多字符数？

#### --distractors--

`minlen` 和 `maxlen`

---

`min` 和 `max`

---

`minimumLen` 和 `maximumLen`

#### --answer--

`minlength` 和 `maxlength`

### --question--

#### --text--

以下哪个按钮示例**没**使用 `type` 属性的正确值？

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

以下哪个属性用于指定按钮的值？

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

`form` 开始标签中的 `action` 属性有什么作用？

#### --distractors--

用于指定输入字段为只读。

---

用于定义用户输入时应显示的字符数。

---

用于向用户显示提示，告诉他们在输入框中输入什么内容。

#### --answer--

用于指定发送表单数据的 URL。

### --question--

#### --text--

以下哪个属性用于指定发送表单数据时使用的 HTTP 方法？

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

以下哪项是处理表格时常用的方法？

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

以下哪项不是 `type` 属性的有效值？

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

以下哪项是在表单中创建按钮的正确方法？

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

什么是隐式关联？

#### --distractors--

是指通过使用 `label` 元素上的 `action` 和 `for` 属性将输入与标签关联起来。

---

是指通过使用 `label` 元素上的 `required` 和 `for` 属性将输入与标签关联起来。

---

是指通过使用 `label` 元素上的 `for` 属性将输入与标签关联起来。

#### --answer--

是指通过把输入字段包装在 `label` 元素内，将输入与标签关联。

### --question--

#### --text--

`fieldset` 元素的作用是什么？

#### --distractors--

用于在 HTML 表格中对标题内容进行分组。

---

用于为输入字段创建标签。

---

用于添加说明输入组的标题。

#### --answer--

用于将相关的输入组合在一起。

### --question--

#### --text--

以下哪项用于在表单中创建复选框？

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

以下哪个元素用于为 HTML 表格添加标题？

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

`legend` 元素的作用是什么？

#### --distractors--

用于在 HTML 表格中对表体内容进行分组。

---

用于添加标题来描述表格的单元格。

---

用于将相关的输入组合在一起。

#### --answer--

用于添加说明输入组的标题。

### --question--

#### --text--

以下哪个元素用于在 HTML 表格中对标题内容进行分组？

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

以下哪项是在表格中添加行和数据单元格的正确方法？

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

什么是 DOM 检查器？

#### --distractors--

一种用于删除 DOM 中所有元素的工具。

---

一套直接内置在浏览器中的工具，可帮助你调试、剖析和分析网页。

---

一种检查 HTML 代码语法以确保其有效的工具。

#### --answer--

一种可以检查和修改网页 HTML 结构的工具。

### --question--

#### --text--

以下哪种工具可以检查 HTML 代码的语法以确保其有效？

#### --distractors--

超文本标记语言 验证

---

超文本标记语言 isValid

---

超文本标记语言 有效

#### --answer--

HTML 验证器

### --question--

#### --text--

以下哪项是指定表格中数据单元格列数的正确方法？

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

什么是输入字段的焦点状态？

#### --distractors--

这是用户移除焦点时输入框的状态。

---

这是输入字段被用户设置为只读时的状态。

---

这是输入字段被用户禁用时的状态。

#### --answer--

这是输入字段被用户选中时的状态。

### --question--

#### --text--

以下哪项是创建单选按钮的正确方法？

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

`for` 属性的作用是什么？ 

#### --distractors--

用于向用户显示提示。

---

用于指定输入值

---

用于为输入字段创建标签。

#### --answer--

用于指定标签所针对的输入字段。

### --question--

#### --text--

以下哪个属性用于定义用户在输入时应显示的字符数？

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

以下哪个属性用于向用户显示提示，告诉他们在输入框中输入什么内容？

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
