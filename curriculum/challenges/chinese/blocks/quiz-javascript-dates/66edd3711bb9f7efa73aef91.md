---
id: 66edd3711bb9f7efa73aef91
title: JavaScript 日期测验
challengeType: 8
dashedName: quiz-javascript-dates
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript 的 `Date` 对象用于什么？

#### --distractors--

仅处理日期。

---

仅处理时间。

---

仅处理闰年。

#### --answer--

同时处理日期和时间。

### --question--

#### --text--

哪个代码创建了一个新的 `Date` 对象实例？

#### --distractors--

`Date.new()`

---

`Date.fetch()`

---

`new.Date()`

#### --answer--

`new Date()`

### --question--

#### --text--

`Date.now()` 是如何工作的？

#### --distractors--

它返回自 1776 年 7 月 4 日以来的当前时间（纳秒）。

---

它返回自 1974 年 1 月 1 日以来的上一次时间（以毫秒为单位）。

---

它返回自 1970 年 1 月 1 日以来的当前日期（以分钟为单位）。

#### --answer--

它返回自 1970 年 1 月 1 日以来的当前时间（以毫秒为单位）。

### --question--

#### --text--

哪个方法返回当前年份？

#### --distractors--

`getNewYear()`

---

`getLastYear()`

---

`getLeapYear()`

#### --answer--

`getFullYear()`

### --question--

#### --text--

哪个方法返回当前月份，作为零索引的整数？

#### --distractors--

`getCalendar()`

---

`fetchMonth()`

---

`get.Month()`

#### --answer--

`getMonth()`

### --question--

#### --text--

哪个方法将日期格式化为字串？

#### --distractors--

`getMonth()`

---

`formatDate()`

---

`toStingy()`

#### --answer--

`toString()`

### --question--

#### --text--

如果是 2022 年 6 月 12 日，`console.log(new Date().getFullYear());` 的结果会是什么？

#### --distractors--

`"June 12th 2022"`

---

`"2023"`

---

`"06/12/2022"`

#### --answer--

`"2022"`

### --question--

#### --text--

`date.toLocaleDateString("fr-FR")` 中的 `fr-FR` 表现什么？

#### --distractors--

一个法裔加拿大局部的。

---

一个法意局部的。

---

一个法语-芬兰语局部的。

#### --answer--

一个法语局部的。

### --question--

#### --text--

如果你所在地区的时间格式为 `HH:MM:SS AM/PM`，哪一行代码能正确显示当前时间？

#### --distractors--

`console.log(new Date().toLocaleDateString());`

---

`console.log(new Date().toString());`

---

`console.log(new Date().toCityDateString());`

#### --answer--

`console.log(new Date().toLocaleTimeString());`

### --question--

#### --text--

`toISOString()` 方法以哪种形式返回日期？

#### --distractors--

USA-863 字串形式。

---

一种 MP3 字串形式。

---

UTC 字串格式。

#### --answer--

ISO-8601 字串形式。

### --question--

#### --text--

ISO-8601 日期格式应该是什么样的？

#### --distractors--

DD-MM-YYYY

---

DD-MM-YYTHH

---

YYYY-MM-DDTHH:mm:ssZ

#### --answer--

YYYY-MM-DDTHH:mm:ss.sssZ

### --question--

#### --text--

`console.log(new Date(2003, 6, 27).getMonth());` 对应的月份是什么？

#### --distractors--

6 月

---

四月

---

一月

#### --answer--

七月

### --question--

#### --text--

你如何将日期格式化为局部的字串或更易读的形式？

#### --distractors--

`.toLocaleDate()`

---

`.toLocaleString()`

---

`.toLocaleTimeString()`

#### --answer--

`.toLocaleDateString()`

### --question--

#### --text--

如果未提供 `locales` 参数，`toLocaleDateString()` 方法使用的默认区域设置是什么？

#### --distractors--

英语（英国）。  

---

最接近 United States 的局部的。

---

法语（法国）。

#### --answer--

用户的系统局部的。

### --question--

#### --text--

下面代码中赋值给 `now` 的是什么？

```js
const now = new Date();
```

#### --distractors--

自 1990 年 1 月 1 日以来的当前时间（毫秒）。

---

当前日期的毫秒数减去 Unix 纪元。

---

自 1990 年 1 月 1 日以来的当前纳秒时间。

#### --answer--

基于你的计算机系统时钟的当前日期和时间。

### --question--

#### --text--

`console.log(new Date(2003, 6, 27).getFullYear());` 的输出是什么？

#### --distractors--

`"27/6/2003"`

---

`27`

---

`6`

#### --answer--

`2003`

### --question--

#### --text--

当日期无效时，`getDate` 返回什么？

#### --distractors--

`0`

---

`null`

---

`undefined`

#### --answer--

`NaN`

### --question--

#### --text--

如果我们处于 10 月份，`console.log(new Date().getMonth());` 会输出什么？

#### --distractors--

`1`

---

`3`

---

`10`

#### --answer--

`9`

### --question--

#### --text--

以下哪个选项会为以下对象输出 `2021`？

```js
const d = new Date("2021-12-25");
```

#### --distractors--

`console.log(d.getTime())`

---

`console.log(d.toUTCString())`

---

`console.log(d.getDate())`

#### --answer--

`console.log(d.getFullYear())`

### --question--

#### --text--

以下哪个选项会为以下对象输出 `"2021-12-25T00:00:00.000Z"`？

```js
const d = new Date("2021-12-25");
```

#### --distractors--

`console.log(d.getFullYear())`

---

`console.log(d.toUTCString())`

---

`console.log(d.getTime())`

#### --answer--

`console.log(d.toISOString())`
