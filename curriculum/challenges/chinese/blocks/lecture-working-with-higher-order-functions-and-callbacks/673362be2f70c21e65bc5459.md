---
id: 673362be2f70c21e65bc5459
title: 什么是 Reduce 方法，它是如何工作的？
challengeType: 19
dashedName: what-is-the-reduce-method-and-how-does-it-work
---

# --interactive--

`reduce` 方法是 JavaScript 中的一个函数，允许你处理一个数组并将其压缩为单个值。这个单个值可以是数字、字串、对象，甚至是另一个数组。

它被称为 `reduce`，因为它将数组缩减为单个输出。虽然一开始看起来可能很复杂，但理解 `reduce` 可以在许多情况下极大地简化你的代码。

`reduce` 的核心工作原理是按顺序将一个函数应用到数组中的每个元素，将每次计算的结果传递给下一个。这个函数通常被称为 reducer 函数。

reducer 函数接受两个主要参数：累加器和当前值。累加器是你保存操作运行结果的地方，当前值是正在处理的数组元素。

让我们看一个例子来说明 `reduce` 是如何工作的：

:::interactive_editor

```js
const numbers = [1, 2, 3, 4, 5];
const sum = numbers.reduce(
  (accumulator, currentValue) => accumulator + currentValue,
  0
);

console.log(sum); // 15
```

:::

在这个例子中，我们使用 `reduce` 来获取数组中所有数字的总和。

reducer 函数接受累加器（从 `0` 开始，由传递给 `reduce` 的第二个参数指定）并将每个数字添加到它上面。

每次添加的结果成为下一次迭代的新建累加器。

`reduce` 方法也可以将初始值作为第二个参数传入。这是累加器开始时的值。在上面的例子中，我们将其设置为 `0`。

如果你不提供初始值，`reduce` 将使用数组的第一个元素作为初始累加器，并从第二个元素开始进程。

`reduce` 的一个优点是它的灵活性。因为你定义了 reducer 函数，所以你可以完全控制数组的处理方式以及你想要产生的结果类型。这使得 `reduce` 非常强大，但一开始也可能有点难以理解。

通过练习，你将掌握使用 `reduce` 方法。

# --questions--

## --text--

`reduce` 方法对数组做什么？

## --answers--

增加数组的大小。

### --feedback--

考虑在数组上使用 reduce 的最终结果。

---

从数组中移除元素。

### --feedback--

考虑在数组上使用 reduce 的最终结果。

---

将数组转换为单个值。

---

对数组的元素进行排序。

### --feedback--

考虑在数组上使用 reduce 的最终结果。

## --video-solution--

3

## --text--

在 `reduce` 方法中，什么是累加器？

## --answers--

`reduce` 操作的最终结果。

### --feedback--

考虑在 `reduce` 操作中从一次迭代传递到下一次的内容。

---

当前正在处理的元素。

### --feedback--

考虑在 `reduce` 操作中从一次迭代传递到下一次的内容。

---

用于计算的临时变量。

### --feedback--

考虑在 `reduce` 操作中从一次迭代传递到下一次的内容。

---

`reduce` 操作的运行结果。

## --video-solution--

4

## --text--

如果你不给 `reduce` 方法提供初始值，会发生什么？

## --answers--

会报错。

### --feedback--

考虑如果未提供初始值，`reduce` 需要什么来开始其操作。

---

它使用 `0` 作为初始值。

### --feedback--

考虑如果未提供初始值，`reduce` 需要什么来开始其操作。

---

它使用数组的第一个元素作为初始值。

---

它返回数组不变。

### --feedback--

考虑如果未提供初始值，`reduce` 需要什么来开始其操作。

## --video-solution--

3
