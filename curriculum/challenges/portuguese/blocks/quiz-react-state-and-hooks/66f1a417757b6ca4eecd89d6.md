---
id: 66f1a417757b6ca4eecd89d6
title: Questionário sobre state e hooks em React
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual das seguintes é a maneira correta de adicionar um evento de clique a um botão em React?

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

O que é um React Synthetic Event?

#### --distractors--

Um tipo de elemento JSX apenas para eventos `inputs` e `submit`.

---

Um tipo de elemento JSX apenas para eventos `submit`.

---

Uma forma de alterar a fase de bubbling da propagação de eventos para ser mais amigável ao usuário.

#### --answer--

Um invólucro em torno de eventos nativos como eventos `click` e `submit`.

### --question--

#### --text--

Qual dos seguintes é um prefixo comum usado para nomear funções manipuladoras de eventos?

#### --distractors--

isHandling

---

hasHandled

---

manuseio

#### --answer--

manipular

### --question--

#### --text--

Quais são os três principais estágios na renderização do React?

#### --distractors--

Handshake, loading e hydration.

---

Busca, decodificação e execução.

---

Montagem, atualização e desmontagem.

#### --answer--

Disparo, renderização e commit.

### --question--

#### --text--

Qual das seguintes é a maneira correta de trabalhar com o hook `useState`?

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

Qual dos seguintes NÃO é um atributo válido usado para manipular eventos React?

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

Qual das seguintes é a maneira correta de atualizar o estado do array?

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Frontend");
```

---

```js
setCertificates.push("Frontend");
```

---

```js
setCertificates().append("Frontend");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Frontend"]);
```

### --question--

#### --text--

O que há de errado com esta função?

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

Atualizar uma variável `useState` não é permitido dentro de uma função aninhada.

---

`previousState` precisa estar entre parênteses.

---

Funções arrow não devem ser usadas dentro de uma função setter de estado.

#### --answer--

`previousState` deve ser espalhado antes de `name` para garantir que quaisquer outras propriedades não sejam sobrescritas.

### --question--

#### --text--

Qual das seguintes é a maneira correta de atualizar o estado para remover itens de um array?

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

Para que o atributo `ref` é tipicamente usado no React?

#### --distractors--

É usado para auditar problemas de desempenho em componentes react.

---

É usado para auditar problemas de acessibilidade em componentes react.

---

É usado para remover nós do DOM.

#### --answer--

É usado para acessar um nó DOM.

### --question--

#### --text--

Qual das seguintes é a maneira correta de acessar o valor atual de um `ref`?

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

Quantas vezes esta mensagem será registrada no console?

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

Três vezes.

---

Duas vezes.

---

Nenhum, pois o array de dependências está vazio.

#### --answer--

Apenas uma vez na renderização inicial.

### --question--

#### --text--

Para que os hooks personalizados do React são normalmente usados?

#### --distractors--

Eles são usados para executar a funcionalidade mais complexa de um app.

---

Eles não são tipicamente usados, hooks embutidos são preferidos.

---

Eles substituem os métodos do ciclo de vida das versões anteriores do React.

#### --answer--

Eles são úteis para reutilizar lógica com estado entre componentes.

### --question--

#### --text--

Quantas vezes esta mensagem será registrada no console?

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

Será registrado sempre que um formulário for enviado ou um evento de clique acontecer.

---

Será registrado duas vezes.

---

Ele será registrado apenas uma vez.

#### --answer--

Será registrado cada vez que o componente renderizar.

### --question--

#### --text--

Qual das seguintes é a maneira correta de evitar um refresh do navegador para um evento `onSubmit`?

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

Qual dos seguintes hooks é tipicamente usado para buscar dados?

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

Qual dos seguintes NÃO é uma propriedade válida no objeto nativo `Event`?

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

Qual das seguintes etapas de renderização ocorre quando o React detecta que algo mudou e a interface do usuário (UI) pode precisar ser atualizada?

#### --distractors--

setter

---

commit

---

loop

#### --answer--

gatilho

### --question--

#### --text--

Qual das seguintes etapas se refere ao momento em que o React pega as mudanças preparadas do virtual DOM e as aplica no DOM real?

#### --distractors--

gatilho

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

Qual é o propósito das `dependencies` em um `useEffect`?

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

O argumento opcional `dependencies` não afeta nada relacionado ao `useEffect`.

---

O argumento opcional `dependencies` faz com que o efeito execute em um loop infinito.

---

O argumento opcional `dependencies` é usado para evitar que o efeito seja executado.

#### --answer--

O argumento opcional `dependencies` controla quando o efeito é executado.

