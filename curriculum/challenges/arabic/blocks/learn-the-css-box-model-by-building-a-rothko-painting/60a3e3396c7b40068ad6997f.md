---
id: 60a3e3396c7b40068ad6997f
title: الخطوة 22
challengeType: 0
dashedName: step-22
---

# --description--

إضافة 1 بكسل من الحشو إلى الأعلى والأسفل واليسار واليمين للوحة غيّر أبعادها إلى 502 بكسل × 602 بكسل.

استبدل خاصية `padding` بخاصية `overflow` مضبوطة على `hidden` - لإعادة اللوحة إلى أبعادها الأصلية.

# --hints--

يجب إزالة خاصية `padding` من محدِّد `.canvas`.

```js
const canvasPadding = new __helpers.CSSHelp(document).getStyle('.canvas').getPropertyValue('padding');
assert(!canvasPadding);
```

يجب ضبط خاصية `overflow` على `hidden`.

```js
const hasOverflow = new __helpers.CSSHelp(document).getCSSRules().some(x => x.style.overflow === 'hidden');
assert(hasOverflow);
```

يجب أن يكون لعنصر `.canvas` قيمة `overflow` تساوي `hidden`.

```js
const canvasOverflow = new __helpers.CSSHelp(document).getStyle('.canvas')?.getPropertyValue('overflow');
assert(canvasOverflow === 'hidden')
```

# --seed--

## --seed-contents--

```css
.canvas {
  width: 500px;
  height: 600px;
  background-color: #4d0f00;
--fcc-editable-region--
  padding: 1px;
--fcc-editable-region--
}

.frame {
  border: 50px solid black;
  width: 500px;
  padding: 50px;
  margin: 20px auto;
}

.one {
  width: 425px;
  height: 150px;
  background-color: #efb762;
  margin: 20px auto;
}
```

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Rothko Painting</title>
    <link href="./styles.css" rel="stylesheet">
  </head>
  <body>
    <div class="frame">
      <div class="canvas">
        <div class="one"></div>
      </div>
    </div>
  </body>
</html>
```
