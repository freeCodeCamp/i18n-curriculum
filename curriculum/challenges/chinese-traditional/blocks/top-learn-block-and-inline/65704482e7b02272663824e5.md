---
id: 65704482e7b02272663824e5
title: 學習區塊和行內課程 G
challengeType: 15
dashedName: learn-block-and-inline-lesson-g
---

# --description--

既然你已經對 `block` 和 `inline` 元素有一些基本了解，請打開你幾個問題前列出的 `block` 和 `inline` 元素清單，看看你是否能辨識以下哪些元素是 `block` 元素，哪些是 `inline` 元素。

# --questions--    

## --text--

以下哪一項不是 `inline` 元素？

## --answers--

表單元素

---

錨點元素

---

影像元素

---

span 元素

## --video-solution--

1
