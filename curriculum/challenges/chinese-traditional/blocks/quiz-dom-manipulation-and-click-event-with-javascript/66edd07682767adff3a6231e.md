---
id: 66edd07682767adff3a6231e
title: 使用 JavaScript 進行 DOM 操作與點擊事件測驗
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是 Document Object Model？

#### --distractors--

用於與 HTML 頁面互動的程式設計語言。

---

一組只包含浮點數的物件。

---

一組表述 HTML 元素的 JavaScript 類別。

#### --answer--

一個讓你與 HTML 文件互動的程式設計介面。

### --question--

#### --text--

API 代表什麼？

#### --distractors--

額外程式設計介面

---

添加程式設計介面

---

另一個程式設計介面

#### --answer--

應用程式介面

### --question--

#### --text--

你如何使用 JavaScript 找出所有缺少超鏈結的 HTML 錨點？

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

`textContent` 屬性有什麼作用？

#### --distractors--

它會移除所有 HTML 元素中的文字內容。

---

它會從你的 JavaScript 檔案中移除文字內容。

---

它會將文字添加到你 JavaScript 檔案中的所有陣列和物件。

#### --answer--

它會傳回元素的純文字內容。

### --question--

#### --text--

`innerHTML` 屬性是做什麼用的？

#### --distractors--

它是 `Element` 的一個屬性，用於取得 HTML 元素的所有文字內容。

---

它是 `Element` 的一個屬性，用於為你的 HTML 添加註解。

---

它是 `Element` 的一個屬性，用於觸發你 HTML 中的事件。 

#### --answer--

它是 `Element` 的一個屬性，用於設定或更新 HTML 標記的零件。

### --question--

#### --text--

以下哪一項會添加有效的事件監聽器？

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

以下哪一項不是 `document` 物件的有效方法？

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()` 

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

以下哪一項會將 `class` 屬性設為 `my-class`？

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

什麼是 `Event` 物件？

#### --distractors--

它是一個用於從你的 JavaScript 專案中移除事件的特殊物件。

---

它用於自動將事件添加到你的 JavaScript 專案中。

---

它是在處理陣列時使用的特殊物件。

#### --answer--

這是一個在使用者以某種方式與你的網頁交談、互動時觸發的有效負載。 

### --question--

#### --text--

以下哪一項是用來移除先前使用 `addEventListener()` 方法添加到元素的事件監聽器的方法？

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

什麼是行內事件處理常式？

#### --distractors--

HTML 元素上的特殊屬性，用於取消所有事件。

---

HTML 元素上的特殊屬性，僅用於在你的 JavaScript 程式碼中觸發表單提交。

---

用於移除事件的 HTML 元素上的特殊屬性。

#### --answer--

HTML 元素上的特殊屬性，用於在事件發生時執行 JavaScript 程式碼。

### --question--

#### --text--

網路 API 的兩大主要類別是什麼？

#### --distractors--

Java API 和第三方 API。

---

瀏覽器 API 和 Rust API。

---

系統 API 和第三方 API。

#### --answer--

瀏覽器 API 和第三方 API。

### --question--

#### --text--

當瀏覽器解析完 HTML 頁面中所有元素時，會觸發哪個事件？

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

以下哪一個是表述元素行內樣式的屬性？

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

以下哪一種是使用行內事件處理常式的正確方式？

#### --distractors--

```html
<button eventclick="validate()">Validate</button>
```

---

```html
<button click="validate()">Validate</button>
```

---

```html
<button clickevent="validate()">Validate</button>
```

#### --answer--

```html
<button onclick="validate()">Validate</button>
```

### --question--

#### --text--

以下哪一種方法用於延遲動作指定的時間？

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

以下哪一項可以用來直接在 JavaScript 中創建並控制動畫？

#### --distractors--

允許 Animations API

---

控制元件動畫 API 

---

創建 Animations API

#### --answer--

網頁動畫 API 

### --question--

#### --text--

Canvas API 做什麼？

#### --distractors--

它用於在你的 CSS 檔案中動畫化圖形。

---

它用於僅從你的 JavaScript 檔案中移除圖形。

---

它用於從你的 HTML 檔案中移除圖形。

#### --answer--

它用於透過你的 JavaScript 檔案操作圖形。

### --question--

#### --text--

什麼是事件委派？

#### --distractors--

監聽事件在事件被取消時的處理程序。

---

當發生點擊 `event` 時監聽 `event` 的處理程序。

---

當變更事件發生時，監聽事件的處理程序。

#### --answer--

監聽已冒泡至父層的事件的處理程序。

### --question--

#### --text--

以下哪一項不是你在處理對話窗和模態視窗時可以使用的方法？

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`

