---
id: 67fe859e9d3b3635197781c8
title: 整数と浮動小数点数の扱い方
challengeType: 19
dashedName: how-do-you-work-with-integers-and-floating-point-numbers
---

# --description--

整数と浮動小数点数はPythonの主要な数値データ型です。これらを使って数値データをストアし、数学的な演算を行うことができます。

整数と浮動小数点数とは何か、これらを使った算術計算の方法、そしてPythonが提供する両者の操作に役立ついくつかのメソッドを見ていきましょう。

整数は小数点を持たない正または負の全体の数です。

```python
my_int_1 = 56
my_int_2 = -4

print(type(my_int_1)) # <class 'int'>
print(type(my_int_2)) # <class 'int'>
```

整数で加算を行う方法は次の通りです。

```python
my_int_1 = 56
my_int_2 = 12

sum_ints = my_int_1 + my_int_2
print('Integer Addition:', sum_ints) # Integer Addition: 68
```

整数で減算を行う方法は次の通りです。

```python
my_int_1 = 56
my_int_2 = 12

# Subtraction
diff_ints = my_int_1 - my_int_2
print('Integer Subtraction:', diff_ints) # Integer Subtraction: 44
```

整数で乗算を行う方法は次の通りです。

```python
my_int_1 = 12
my_int_2 = 4

# Multiplication
product_ints = my_int_1 * my_int_2
print('Integer Multiplication:', product_ints) # Integer Multiplication: 48
```

そして整数で除算を行う方法は次の通りです。

```python
my_int_1 = 56
my_int_2 = 12

# Division
div_ints = my_int_1 / my_int_2
print('Integer Division:', div_ints) # Integer Division: 4.666666666666667
```

浮動小数点数は小数点を持つ正または負の数で、例えば`3.14`、`-0.5`、`0.0`のようなものです。

```python
my_float_1 = -12.0
my_float_2 = 4.9

print(type(my_float_1)) # <class 'float'>
print(type(my_float_2)) # <class 'float'>
```

浮動小数点数での加算は次の通りです。

```python
my_float_1 = 5.4
my_float_2 = 12.0

float_addition = my_float_1 + my_float_2
print('Float Addition:', float_addition) # Float Addition: 17.4
```

浮動小数点数での減算は次の通りです。

```python
my_float_1 = 5.4
my_float_2 = 12.0

float_subtraction = my_float_2 - my_float_1
print('Float Subtraction:', float_subtraction) # Float Subtraction: 6.6
```

浮動小数点数での乗算は次の通りです。

```python
my_float_1 = 5.4
my_float_2 = 12.0

float_multiplication = my_float_2 * my_float_1
print('Float Multiplication:', float_multiplication) # Float Multiplication: 64.80000000000001
```

そして浮動小数点数での除算は次の通りです。

```python
my_float_1 = 5.4
my_float_2 = 12.0

float_division = my_float_2 / my_float_1
print('Float Division:', float_division) # Float Division: 2.222222222222222
```

整数と浮動小数点数を加算すると、結果は自動的に浮動小数点数に変換されます。

```python
my_int = 56
my_float = 5.4

sum_int_and_float = my_int + my_float

print(sum_int_and_float) # 61.4
print(type(sum_int_and_float)) # <class 'float'>
```

これは減算、乗算、除算など他の基本的な算術演算でも同様です。整数と浮動小数点数を混ぜると、Pythonは結果を浮動小数点数として返します。

また、モジュラス演算子、床除算、べき乗など、整数と浮動小数点数の両方でより複雑な算術計算も行えます。

モジュラス演算子（`%`）は、左側の値を右側の値で割ったときの余りを返します。

```python
my_int_1 = 56
my_int_2 = 12

my_float_1 = 5.4
my_float_2 = 12.0

mod_ints = my_int_1 % my_int_2
mod_floats = my_float_2 % my_float_1

print('Integer Modulo:', mod_ints) # Integer Modulo: 8
print('Float Modulo:', mod_floats) # Float Modulo: 1.1999999999999993
```

床除算は2つの数を割り、その結果以下の最大の整数を返します。これは二重スラッシュ演算子（`//`）で行います。

```python
my_int_1 = 56
my_int_2 = 12

my_float_1 = 5.4
my_float_2 = 12.0

floor_div_ints = my_int_1 // my_int_2
floor_div_floats = my_float_2 // my_float_1

print('Integer Floor Division:', floor_div_ints) # Integer Floor Division: 4
print('Float Floor Division:', floor_div_floats) # Float Floor Division: 2.0
```

べき乗はある数を別の数のべき乗にし、二重アスタリスク演算子（`**`）で行います。

```python
my_int_1 = 56
my_int_2 = 12

my_float_1 = 5.4
my_float_2 = 12.0

exp_ints = my_int_1 ** my_int_2
exp_floats = my_float_1 ** my_float_2

print('Integer Exponentiation:', exp_ints) # Integer Exponentiation: 951166013805414055936
print('Float Exponentiation:',  exp_floats) # Float Exponentiation: 614787626.1765089
```

時々、浮動小数点数を含む演算の結果が予想より多くの10進数の桁を持つことがあります。例えば、`0.1 + 0.2`の合計は`0.30000000000000004`ではなく`0.3`になります。

これは数値が2進数形式でストアされており、一部の分数は2進数で正確に表せないためです。その結果、有限の近似値としてストアされます。これは、分数`1/3`が10進数で有限の桁数で表せず、無限の桁のうち一定の桁数で切り捨てられる（`0.33333...`）のと同じです。

これにより小さな丸め誤差が生じます。

Pythonはまた、数値データや文字列を整数や浮動小数点数に変換する組み込み関数も提供しています。

`float()`関数は与えられた数値から浮動小数点数を生成して返します。

```python
my_int_1 = 56
my_float_1 = float(my_int_1)

print(my_float_1)  # 56.0
print(type(my_float_1))  # <class 'float'>
```

`int()`関数は与えられた数値から整数を生成して返します。

```python
my_float = 12.92563
my_int = int(my_float)

print(my_int)  # 12
print(type(my_int))  # <class 'int'>
```

また、同じ組み込み関数を使って文字列を浮動小数点数または整数に変換することもできます。

```python
my_str_int = '45'
my_str_float = '7.8'

converted_int = int(my_str_int)
converted_float = float(my_str_float)

print(converted_int, type(converted_int))  # 45 <class 'int'>
print(converted_float, type(converted_float))  # 7.8 <class 'float'>
```

Pythonが整数と浮動小数点数の操作に提供するその他のメソッドは次の通りです。

- `round()`: 指定した小数点以下の桁数に数値を丸めます。デフォルトではこの関数は最も近い整数に丸め、小数点以下のない整数を返します。
    

```python
my_int_1 = 4.798
my_int_2 = 4.253

rounded_int_1 = round(my_int_1)
rounded_int_2 = round(my_int_2, 1)

print(rounded_int_1) # 5
print(rounded_int_2) # 4.3
```

- `abs()`: 数値の絶対値を返します。
    

```python
num = -15

absolute_value = abs(num)
print(absolute_value) # 15
```

- `pow()`: 数値をべき乗にしたり、モジュラーべき乗を行います。
    

```python
result_1 = pow(2, 3)  # Equivalent to 2 ** 3
print(result_1)  # 8

result_2 = pow(2, 3, 5)  # (2 ** 3) % 5
print(result_2)  # 3
```

# --questions--

## --text--

Pythonで二重スラッシュ（`//`）演算子は何をしますか？

## --answers--

2つの数を割り、その結果以下の最大の整数を返します。

---

通常の除算を行い、浮動小数点数を返します。

### --feedback--

この演算子は特別な種類の整数除算に使われます。

---

Pythonでコメントを表します。

### --feedback--

この演算子は特別な種類の整数除算に使われます。

---

数値をべき乗にします。

### --feedback--

この演算子は特別な種類の整数除算に使われます。

## --video-solution--

1

## --text--

Pythonの整数について正しく説明している文はどれですか？

## --answers--

整数は正の全体の数のみをストアできます。

### --feedback--

整数が表すもの、つまり小数点のない全体の値を考えてみてください。

---

整数は小数点を持つ数です。

### --feedback--

整数が表すもの、つまり小数点のない全体の値を考えてみてください。

---

整数は小数点を持たない正または負の全体の数です。

---

整数は除算を行うと自動的に浮動小数点数に変換されます。

### --feedback--

整数が表すもの、つまり小数点のない全体の値を考えてみてください。

## --video-solution--

3

## --text--

どのメソッドが数値の絶対値を返しますか？

## --answers--

`abs()`

---

`round()`

### --feedback--

常に非負の結果を返すメソッドを考えてみてください。

---

`sum()`

### --feedback--

常に非負の結果を返すメソッドを考えてみてください。

---

`max()`

### --feedback--

常に非負の結果を返すメソッドを考えてみてください。

## --video-solution--

1
