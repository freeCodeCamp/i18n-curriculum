---
id: 66ed8ff4f45ce3ece4053eb4
title: Quiz su layout ed effetti CSS
challengeType: 8
dashedName: quiz-css-layout-and-effects
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale termine si usa in CSS per descrivere quando il contenuto di un elemento supera la dimensione del suo contenitore?

#### --distractors--

Underflow

---

Compressione

---

Spaziatura interna

#### --answer--

Overflow

### --question--

#### --text--

Qual è il comportamento predefinito di CSS quando il contenuto trabocca da un elemento?

#### --distractors--

Nascondere il contenuto.

---

Ritagliare il contenuto.

---

Ridimensionare il contenuto.

#### --answer--

Mantenere il contenuto visibile.

### --question--

#### --text--

Perché CSS di solito evita di nascondere il contenuto quando c’è overflow?

#### --distractors--

Per mantenere il layout e il design originale.

---

Per prevenire problemi di prestazioni nel browser.

---

Per consentire una migliore ottimizzazione per i motori di ricerca.

#### --answer--

Per garantire che tutto il contenuto sia accessibile agli utenti.

### --question--

#### --text--

Come puoi gestire l’overflow verticale in CSS nascondendo il contenuto o mostrando le barre di scorrimento?

#### --distractors--

Usa `overflow-y: visible` per nascondere il contenuto e `overflow-y: auto` per mostrare sempre le barre di scorrimento.

---

Usa `overflow-y: collapse` per nascondere il contenuto e `overflow-y: expand` per mostrare le barre di scorrimento.

---

Usa `overflow-y: none` per nascondere il contenuto e `overflow-y: show` per mostrare le barre di scorrimento.

#### --answer--

Usa `overflow-y: hidden` per nascondere il contenuto e `overflow-y: scroll` per mostrare sempre le barre di scorrimento.

### --question--

#### --text--

Come puoi specificare comportamenti di scorrimento orizzontale e verticale diversi in CSS?

#### --distractors--

Usando le proprietà `scroll-x` e `scroll-y`.

---

Specificando lo stesso valore per `overflow-x` e `overflow-y`.

---

Specificando due valori per la proprietà `overflow`, dove il primo valore si applica a `overflow-y` e il secondo a `overflow-x`.

#### --answer--

Specificando due valori per la proprietà `overflow`, dove il primo valore si applica a `overflow-x` e il secondo a `overflow-y`.

### --question--

#### --text--

Cosa permette di fare la proprietà CSS `transform`?

#### --distractors--

Cambiare il colore e lo stile del carattere di un elemento.

---

Regolare il margine e la spaziatura interna di un elemento.

---

Modificare la visibilità e la visualizzazione di un elemento.

#### --answer--

Ruotare, scalare, inclinare e traslare un elemento.

### --question--

#### --text--

Come modifica un elemento la proprietà `transform: translate(15px, 200px)`?

#### --distractors--

Sposta l’elemento di 15 pixel a sinistra e 200 pixel verso il basso dalla sua posizione originale.

---

Ruota l’elemento di 15 gradi in senso orario.

---

Sposta l’elemento di 15 pixel a destra e 200 pixel verso l’alto dalla sua posizione originale.

#### --answer--

Sposta l’elemento di 15 pixel a destra e 200 pixel verso il basso dalla sua posizione originale.

### --question--

#### --text--

Qual è lo scopo delle proprietà CSS `overflow-x` e `overflow-y`?

#### --distractors--

Regolano la spaziatura interna e il margine di un elemento.

---

Impostano il colore di sfondo e lo stile del carattere di un elemento.

---

Definiscono le proprietà di visibilità e visualizzazione di un elemento.

#### --answer--

Controllano l’overflow orizzontale e verticale del contenuto di un elemento.

### --question--

#### --text--

Qual è lo scopo della proprietà `margin` in CSS?

#### --distractors--

Definisce lo spazio tra il contenuto e il bordo di un elemento.

---

Definisce lo spazio tra il bordo e la spaziatura interna di un elemento.

---

Definisce la larghezza e l’altezza di un elemento.

#### --answer--

Definisce lo spazio attorno a un elemento, esterno al suo bordo.

### --question--

#### --text--

Cosa rappresenta l’area del contenuto di un elemento in CSS?

#### --distractors--

Definisce i margini e la spaziatura interna attorno all’elemento.

---

Definisce lo spessore del bordo dell’elemento.

---

Controlla la visibilità dell’elemento sulla pagina.

#### --answer--

Contiene il contenuto effettivo dell’elemento, come testo, immagini e video.

### --question--

#### --text--

Qual è la differenza tra `content-box` e `border-box` in CSS?

#### --distractors--

`content-box` include spaziatura interna e bordo, mentre `border-box` include solo le dimensioni del contenuto.

---

Sia `content-box` che `border-box` includono solo le dimensioni del bordo.

---

`content-box` si usa per elementi 2D, mentre `border-box` per elementi 3D.

#### --answer--

`content-box` include solo le dimensioni del contenuto, mentre `border-box` include contenuto, spaziatura interna e bordo nelle dimensioni.

### --question--

#### --text--

Qual è lo scopo dei reset CSS?

#### --distractors--

Applicare stili predefiniti a tutti gli elementi per un caricamento più veloce.

---

Minimizzare la dimensione dei file CSS rimuovendo stili inutilizzati.

---

Ottimizzare immagini e caratteri tipografici per migliori prestazioni.

#### --answer--

Rimuovere gli stili predefiniti del browser e garantire uno stile coerente tra i browser.

### --question--

#### --text--

Qual è un esempio comune di reset CSS?

#### --distractors--

bootstrap.css

---

undo.css

---

restore.css

#### --answer--

normalize.css

### --question--

#### --text--

Quale proprietà CSS si usa per applicare modifiche come rotazione, scala e traslazione agli elementi?

#### --distractors--

`box-shadow`

---

`opacity`

---

`z-index`

#### --answer--

`transform`

### --question--

#### --text--

Come si calcola la larghezza totale di un elemento in CSS usando il valore `border-box` per la proprietà `box-sizing`?

#### --distractors--

Larghezza totale = larghezza contenuto + bordo + margine

---

Larghezza totale = larghezza contenuto + spaziatura interna + bordo + margine

---

Larghezza totale = larghezza contenuto

#### --answer--

Larghezza totale = larghezza contenuto + spaziatura interna + bordo

### --question--

#### --text--

Qual è lo scopo della proprietà `filter` in CSS?

#### --distractors--

Cambiare scala e rotazione di un elemento.

---

Cambiare colore di sfondo e texture di un elemento.

---

Modificare dimensione e posizione di un elemento.

#### --answer--

Modificare l’aspetto visivo di un elemento usando vari effetti grafici.

### --question--

#### --text--

Cosa fa la proprietà CSS `filter: grayscale(50%);` a un elemento?

#### --distractors--

Aumenta la luminosità dell’elemento del 50%.

---

Diminuisce il contrasto dell’elemento del 50%.

---

Applica un effetto seppia all’elemento.

#### --answer--

Applica un effetto scala di grigi al 50% all’elemento.

### --question--

#### --text--

Qual è lo scopo della funzione `contrast()` in CSS?

#### --distractors--

Regola la luminosità dell’immagine, dove `0%` la rende nera e `100%` la rende bianca.

---

Regola il contrasto dell’immagine, dove `100%` la rende grigia, `0%` non ha effetto e valori superiori a `100%` aumentano il contrasto.

---

Regola l’opacità dell’immagine, dove `0%` la rende trasparente e `100%` la rende opaca.

#### --answer--

Regola il contrasto dell’immagine, dove `0%` la rende grigia, `100%` non ha effetto e valori superiori a `100%` aumentano il contrasto.

### --question--

#### --text--

Qual è lo scopo della funzione `hue-rotate()` in CSS?

#### --distractors--

Cambia la luminosità di un’immagine.

---

Ruota un’immagine in senso orario di un certo numero di gradi.

---

Regola saturazione e contrasto di un’immagine.

#### --answer--

Applica uno spostamento di colore, dove il valore `angle` definisce il numero di gradi di rotazione attorno al cerchio dei colori.

### --question--

#### --text--

Cos’è il margin collapsing in CSS?

#### --distractors--

Quando il margine di un elemento è impostato su un numero negativo e questo causa problemi nel layout.

---

Quando il margine di un elemento viene diminuito dalla spaziatura interna e questo cambia le dimensioni dell’elemento.

---

Quando il margine di un elemento viene ignorato dal browser, quindi non è visibile.

#### --answer--

Quando i margini verticali di elementi adiacenti si sovrappongono, combinandosi in un unico margine.

## --quiz--

### --question--

#### --text--

Quale delle seguenti affermazioni è VERA riguardo alla funzione `translate()`?

#### --distractors--

Si usa per scalare la dimensione di un elemento.

---

Si usa per traslare il testo di un elemento.

---

Si usa per cambiare la forma o la dimensione di un elemento.

#### --answer--

Si usa per spostare un elemento dalla sua posizione attuale.

### --question--

#### --text--

Quale delle seguenti è una componente del modello a riquadro CSS?

#### --distractors--

Larghezza

---

Altezza

---

Dimensione

#### --answer--

Spaziatura interna

### --question--

#### --text--

Quando il `box-sizing` di un elemento è impostato su `content-box`, a quale parte del modello a riquadro si applicano `width` e `height`?

#### --distractors--

Contenuto, spaziatura interna, bordo e margine

---

Contenuto e spaziatura interna

---

Contenuto, spaziatura interna e bordo

#### --answer--

Contenuto

### --question--

#### --text--

Quando il `box-sizing` di un elemento è impostato su `border-box`, a quali parti del modello a riquadro CSS NON si applicano `width` e `height`?

#### --distractors--

L’area del contenuto.

---

Spaziatura interna

---

Bordo

#### --answer--

Margine

### --question--

#### --text--

Quale valore della funzione `brightness()` rende un elemento completamente nero?

#### --distractors--

`100%`

---

`0px`

---

`100px`

#### --answer--

`0%`

### --question--

#### --text--

Che tipo di valori usa la funzione `hue-rotate()`?

#### --distractors--

Pixel

---

Radianti

---

Percentuali

#### --answer--

Gradi

### --question--

#### --text--

Cosa fa la proprietà CSS `filter: contrast(0%);` a un elemento?

#### --distractors--

Fa apparire l’elemento completamente bianco.

---

Fa apparire l’elemento completamente nero.

---

Non ha effetto sull’elemento.

#### --answer--

Fa apparire l’elemento completamente grigio.

### --question--

#### --text--

Qual è il valore di base della funzione CSS `brightness()` da cui parte l’aumento di luminosità?

#### --distractors--

`100px`

---

`50px`

---

`50%`

#### --answer--

`100%`

### --question--

#### --text--

Che tipo di valori usa la funzione `grayscale()`?

#### --distractors--

Pixel

---

Gradi

---

Radianti

#### --answer--

Percentuali

### --question--

#### --text--

Che tipo di valori usa la funzione `contrast()`?

#### --distractors--

Pixel

---

Gradi

---

Radianti

#### --answer--

Percentuali

### --question--

#### --text--

Cosa controlla la proprietà `overflow-x` in CSS?

#### --distractors--

Determina l’overflow orizzontale sull’asse y.

---

Determina l’overflow verticale sull’asse y.

---

Determina l’overflow verticale sull’asse x.

#### --answer--

Determina l’overflow orizzontale sull’asse x.

### --question--

#### --text--

Cosa controlla la proprietà `overflow-y` in CSS?

#### --distractors--

Determina l’overflow orizzontale sull’asse x.

---

Determina l’overflow verticale sull’asse x.

---

Determina l’overflow orizzontale sull’asse y.

#### --answer--

Determina l’overflow verticale sull’asse y.

### --question--

#### --text--

Qual è un esempio di opzione di terze parti per un reset CSS?

#### --distractors--

clean.css

---

restore.css

---

reset.css

#### --answer--

sanitize.css

### --question--

#### --text--

Quante dimensioni ha la proprietà overflow in CSS?

#### --distractors--

Uno

---

Tre

---

Quattro

#### --answer--

Due

### --question--

#### --text--

Se alla proprietà abbreviata `overflow` vengono dati due valori, quale proprietà userà il primo valore?

#### --distractors--

`overflow-y`

---

`overflow-w`

---

`overflow-z`

#### --answer--

`overflow-x`

### --question--

#### --text--

Che tipo di effetto di sfocatura applica la funzione `blur()` a un elemento in CSS?

#### --distractors--

Giliani

---

Galilei

---

Galileo

#### --answer--

Gaussiana

### --question--

#### --text--

Che tipo di valori usa la funzione `sepia()`?

#### --distractors--

Pixel

---

Radianti

---

Gradi

#### --answer--

Percentuali

### --question--

#### --text--

Se alla proprietà abbreviata `overflow` vengono dati due valori, quale proprietà userà il secondo valore?

#### --distractors--

`overflow-w`

---

`overflow-x`

---

`overflow-z`

#### --answer--

`overflow-y`

### --question--

#### --text--

Cosa fa un reset CSS?

#### --distractors--

Costringe i browser a usare la modalità scura per impostazione predefinita.

---

Rimuove gli stili in linea dagli elementi HTML.

---

Disabilita le estensioni del browser che includono CSS.

#### --answer--

Rimuove gli stili predefiniti del browser.

### --question--

#### --text--

Cosa fa la proprietà CSS `filter: grayscale(100%);` a un elemento?

#### --distractors--

Non ha effetto sull’elemento.

---

Fa apparire l’elemento completamente nero.

---

Fa apparire l’elemento completamente bianco.

#### --answer--

Rimuove la saturazione del colore mantenendo la struttura tonale dell’elemento.
