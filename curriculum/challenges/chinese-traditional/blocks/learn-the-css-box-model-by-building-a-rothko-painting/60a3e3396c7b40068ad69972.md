---
id: 60a3e3396c7b40068ad69972
title: 第 9 步
challengeType: 0
dashedName: step-9
---

# --description--

為你的 `.canvas` 規則添加值為 `600px` 的 `height` 屬性。

# --hints--

你應該將 `height` 屬性設定為 `600px`。

```js
const hasHeight = new __helpers.CSSHelp(document).getCSSRules().some(x => x.style.height === '600px');
assert(hasHeight);
```

你的 `.canvas` 元素應該有 `height` 為 `600px`。

```js
const canvasHeight = new __helpers.CSSHelp(document).getStyle('.canvas')?.getPropertyValue('height');
assert(canvasHeight === '600px');
```

# --seed--

## --seed-contents--

```css
.canvas {
  width: 500px;
--fcc-editable-region--

--fcc-editable-region--
}
```

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Rothko Painting</title>
    <link href="./styles.css" rel="stylesheet">
  </head>
  <body>
    <div class="canvas">
    </div>
  </body>
</html>
```
