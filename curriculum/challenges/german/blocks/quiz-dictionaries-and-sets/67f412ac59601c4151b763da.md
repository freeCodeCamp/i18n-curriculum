---
id: 67f412ac59601c4151b763da
title: Quiz zu Dictionaries und Sets
challengeType: 8
dashedName: quiz-dictionaries-and-sets
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 folgenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist ein Dictionary?

#### --distractors--

Eine Datenstruktur, die nur Strings und Listen enthält.

---

Eine Datenstruktur, die eine Sammlung von Schlüssel-Wert-Paaren ist und nur große Zahlen akzeptiert.

---

Eine Datenstruktur, die nur eine Sammlung von Tuples enthält.

#### --answer--

Eine Datenstruktur, die eine Sammlung von Schlüssel-Wert-Paaren ist.

### --question--

#### --text--

Welche der folgenden ist die korrekte Syntax für ein Dictionary?

#### --distractors--

```py
dictionary = {
    <key1: value1>,
    <key2: value2>
}
```

---

```py
dictionary = {
    (key1: value1),
    (key2: value2)
}
```

---

```py
dictionary = {
    [key1: value1],
    [key2: value2]
}
```

#### --answer--

```py
dictionary = {
    key1: value1,
    key2: value2
}
```

### --question--

#### --text--

Welche der folgenden Aussagen über Dictionaries ist richtig?

#### --distractors--

Schlüssel müssen mindestens zwei Zeichen lang sein.

---

Schlüssel müssen ein Sonderzeichen enthalten.

---

Schlüssel müssen mindestens eine Zahl enthalten.

#### --answer--

Schlüssel müssen eindeutig sein.

### --question--

#### --text--

Welcher der folgenden Konstruktoren kann verwendet werden, um ein Dictionary zu erstellen?

#### --distractors--

`diction()`

---

`dic()`

---

`dictionary()`

#### --answer--

`dict()`

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um auf einen Wert in einem Dictionary zuzugreifen?

#### --distractors--

`dictionary<key>`

---

`dictionary/key/`

---

`dictionary{key}`

#### --answer--

`dictionary[key]`

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um einen Wert in einem Dictionary zu aktualisieren?

#### --distractors--

`pizza['name'] >> 'Margherita'`

---

`pizza['name'] << 'Margherita'`

---

`pizza['name'] == 'Margherita'`

#### --answer--

`pizza['name'] = 'Margherita'`

### --question--

#### --text--

Welche der folgenden Methoden kann verwendet werden, um einen Wert zu erhalten, der mit einem Schlüssel verknüpft ist?

#### --distractors--

`access()`

---

`set()`

---

`retrieve()`

#### --answer--

`get()`

### --question--

#### --text--

Was ist ein View-Objekt?

#### --distractors--

Ein spezielles Objekt, das verwendet wird, um Strings in Dictionaries umzuwandeln.

---

Eine Möglichkeit, den Inhalt eines Dictionaries nur zu sehen, wenn das Dictionary zwei oder mehr Schlüssel-Wert-Paare hat.

---

Ein spezielles Objekt, das verwendet wird, um Listen in Dictionaries umzuwandeln.

#### --answer--

Eine Möglichkeit, den Inhalt eines Dictionaries zu sehen, ohne eine separate Kopie der Daten zu erstellen.

### --question--

#### --text--

Welche der folgenden Methoden gibt ein View-Objekt mit allen Schlüssel-Wert-Paaren im Dictionary zurück?

#### --distractors--

`lists()`

---

`dictionaries()`

---

`collections()`

#### --answer--

`items()`

### --question--

#### --text--

Welche der folgenden Methoden entfernt alle Schlüssel-Wert-Paare aus dem Dictionary?

#### --distractors--

`empty()`

---

`replace()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

Was wird für den folgenden Code ausgegeben?

```py
products = {
    'Laptop': 990,
    'Smartphone': 600,
    'Tablet': 250,
    'Headphones': 70,
}

for product in products.items():
    print(product)
```

#### --distractors--

```py
<'Laptop', 990>
<'Smartphone', 600>
<'Tablet', 250>
<'Headphones', 70>
```

---

```py
'Laptop', 990
'Smartphone', 600
'Tablet', 250
'Headphones', 70
```

---

```py
['Laptop', 990]
['Smartphone', 600]
['Tablet', 250]
['Headphones', 70]
```

#### --answer--

```py
('Laptop', 990)
('Smartphone', 600)
('Tablet', 250)
('Headphones', 70)
```

### --question--

#### --text--

Welche der folgenden Aussagen über Sets ist richtig?

#### --distractors--

Sets sind veränderbar und in umgekehrter Reihenfolge sortiert.

---

Sets sind veränderbar und sortiert.

---

Sets sind unveränderlich und unsortiert.

#### --answer--

Sets sind veränderbar und unsortiert.

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um ein Set zu erstellen?

#### --distractors--

`my_set = (1, 2, 3, 4, 5)`

---

`my_set = <1, 2, 3, 4, 5>`

---

`my_set = [1, 2, 3, 4, 5]`

#### --answer--

`my_set = {1, 2, 3, 4, 5}`

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um ein leeres Set zu erstellen?

#### --distractors--

`create_set()`

---

`empty_set()`

---

`sets()`

#### --answer--

`set()`

### --question--

#### --text--

Was wird im Terminal ausgegeben?

```py
my_set = {1, 2, 3, 4, 5, 6}
my_set.add(5)

print(my_set)
```

#### --distractors--

`SyntaxError`

---

`RangeError`

---

`{1, 2, 3, 4, 5, 5, 6}`

#### --answer--

`{1, 2, 3, 4, 5, 6}`

### --question--

#### --text--

Welche der folgenden Methoden prüft, ob ein Set eine Teilmenge ist?

#### --distractors--

`issub()`

---

`isset()`

---

`subset()`

#### --answer--

`issubset()`

### --question--

#### --text--

Welche der folgenden Methoden prüft, ob zwei Sets disjunkt sind?

#### --distractors--

`joint()`

---

`isjoint()`

---

`disjoint()`

#### --answer--

`isdisjoint()`

### --question--

#### --text--

Was macht der symmetrische Differenzoperator (`^`)?

#### --distractors--

Er gibt ein neues Set mit nur den Elementen zurück, die die Sets gemeinsam haben.

---

Er findet die Differenz zwischen den Sets und aktualisiert das erste Set mit diesem Ergebnis.

---

Er gibt ein neues Set mit den Elementen des ersten Sets zurück, die nicht im anderen Set sind.

#### --answer--

Er gibt ein neues Set mit den Elementen zurück, die entweder im ersten oder im zweiten Set, aber nicht in beiden sind.

### --question--

#### --text--

Welches der folgenden eingebauten Module wird zur Erzeugung von Zufallszahlen verwendet?

#### --distractors--

`set_random`

---

`get_random`

---

`rand`

#### --answer--

`random`

### --question--

#### --text--

Welches der folgenden Module wird für die Arbeit mit regulären Ausdrücken verwendet?

#### --distractors--

`regex`

---

`reg`

---

`r`

#### --answer--

`re`

