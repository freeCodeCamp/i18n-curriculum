---
id: 69a890af247de743333bd4ce
title: "Zoezi la 225: Hakuna Kurudia Mfuatano wa Herufi Mfululizo"
challengeType: 28
dashedName: challenge-225
---

# --description--

Iliyopewa mfuatano wa herufi, amua kama haina herufi zinazojirudia.

- Mfuatano wa herufi hauna kurudia ikiwa huna herufi ileile mara mbili au zaidi mfululizo.

# --hints--

`hasNoRepeats("hi world")` inapaswa kurudisha `true`.

```js
assert.isTrue(hasNoRepeats("hi world"));
```

`hasNoRepeats("hello world")` inapaswa kurudisha `false`.

```js
assert.isFalse(hasNoRepeats("hello world"));
```

`hasNoRepeats("abcdefghijklmnopqrstuvwxyz")` inapaswa kurudisha `true`.

```js
assert.isTrue(hasNoRepeats("abcdefghijklmnopqrstuvwxyz"));
```

`hasNoRepeats("freeCodeCamp")` inapaswa kurudisha `false`.

```js
assert.isFalse(hasNoRepeats("freeCodeCamp"));
```

`hasNoRepeats("The quick brown fox jumped over the lazy dog.")` inapaswa kurudisha `true`.

```js
assert.isTrue(hasNoRepeats("The quick brown fox jumped over the lazy dog."));
```

`hasNoRepeats("Mississippi")` inapaswa kurudisha `false`.

```js
assert.isFalse(hasNoRepeats("Mississippi"));
```

# --seed--

## --seed-contents--

```js
function hasNoRepeats(str) {

  return str;
}
```

# --solutions--

```js
function hasNoRepeats(str) {
  for (let i = 1; i < str.length; i++) {
    if (str[i] === str[i - 1]) {
      return false;
    }
  }
  return true;
}
```
