---
id: 66edc25ae5ea80bf6f785552
title: JavaScript 變量和數據類型測驗
challengeType: 8
dashedName: quiz-javascript-variables-and-data-types
---

# --description--

要通過測驗，你必須正確回答以下 20 道題中的至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪項最恰當地描述了 JavaScript？

#### --distractors--

一種用於設計網站的標記語言。

---

一種服務器端編程語言。

---

數據庫管理系統。

#### --answer--

一種用於網絡開發的編程語言。

### --question--

#### --text--

以下哪項不是 JavaScript 數據類型？

#### --distractors--

`Number`

---

`Undefined`

---

`Object`

#### --answer--

`Double`

### --question--

#### --text--

以下哪項不是有效的 JavaScript 變量聲明？

#### --distractors--

`const x = 0;`

---

`let x;`

---

`var x;`

#### --answer--

`public Int x;`

### --question--

#### --text--

以下哪項是 JavaScript 中變量的常見命名規範？

#### --distractors--

JavaScript case。

---

Lightning case。

---

Giraffe case。

#### --answer--

Camel case。

### --question--

#### --text--

在 JavaScript 中，`let` 和 `const` 變量聲明有什麼區別？

#### --distractors--

`const` 不能重新賦值，也不能重新聲明。 `let` 可以重新賦值和聲明。

---

`const` 可以重新賦值和聲明。 `let` 不能重新賦值，也不能重新聲明。

---

`const` 可以重新賦值，但不能重新聲明。 `let` 可以重新聲明，但不能重新賦值。

#### --answer--

`const` 不能重新賦值或重新聲明。 `let` 可以重新賦值，但不能重新聲明。

### --question--

#### --text--

爲什麼在 JavaScript 中字符串是不可變的？

#### --distractors--

不能使用變量創建字符串。

---

字符串只能使用字面量創建。

---

可以更改字符串，但只能通過全局變量。

#### --answer--

字符串一旦創建，就不能直接更改其字符。

### --question--

#### --text--

如何在 JavaScript 的控制檯中打印出 `Hello, world!` 字符串？

#### --distractors--

`print("Hello, world!")`

---

`alert("Hello, world");`

---

`System.out.println("Hello, world!");`

#### --answer--

`console.log("Hello, world!");`

### --question--

#### --text--

下面的代碼有什麼作用？

```js
let hello = "Hello";
hello += " world";
console.log(hello);
```

#### --distractors--

將 `" world"` 重新賦值給變量 `hello` 並在控制檯中打印出來。

---

在控制檯中打印出 `Hello world` 中的字符數。

---

將 `"Hello"` 和 `" world"` 合併爲一個新字符串並打印出來，但 `hello` 的值仍然是 `"Hello"`。

#### --answer--

將 `hello` 的當前值與字符串 `" world"` 相結合，將其重新賦值給 `hello`，並打印到控制檯。

### --question--

#### --text--

哪個 JavaScript 字符用於標記語句的結束？

#### --distractors--

`:`

---

`,`

---

`.`

#### --answer--

`;`

### --question--

#### --text--

以下哪項是 JavaScript 中有效的單行註釋語法？

#### --distractors--

`# This is a single line comment`

---

`-- This is a single line comment`

---

`/* This is a single line comment */`

#### --answer--

`// This is a single line comment`

### --question--

#### --text--

JavaScript 中的動態類型是指：

#### --distractors--

在使用變量之前，需要聲明其數據類型。

---

不能將一種數據類型的值賦值給另一種類型的變量。

---

JavaScript 在執行前會進行類型檢查，這意味着與類型相關的錯誤會在代碼運行前拋出。

#### --answer--

JavaScript 會根據分配給變量的值推斷類型。

### --question--

#### --text--

以下哪項會在控制檯中打印出變量 `x` 的類型？

#### --distractors--

`console.log(x.type);`

---

`console.log(typeOf(x));`

---

`console.log(isNaN(x));`

#### --answer--

`console.log(typeof x);`

### --question--

#### --text--

`console.log(typeof null);` 的輸出結果是什麼？

#### --distractors--

`"undefined"`

---

`"null"`

---

`"NaN"`

#### --answer--

`"object"`

### --question--

#### --text--

以下哪項是 JavaScript 中有效的多行註釋？

#### --distractors--

```js
// This is a
// multi-line
// comment
```

---

```py
'''
This is a
multi-line
comment
'''
```

---

```html
<!--
This is a
multi-line
comment
-->
```

#### --answer--

```js
/*
This is a
multi-line
comment
*/
```

### --question--

#### --text--

以下哪項聲明瞭 JavaScript 中不可更改的變量？

#### --distractors--

`var x = 0;`

---

`let x = 0;`

---

`final x = 0;`

#### --answer--

`const x = 0;`

### --question--

#### --text--

以下哪項是 JavaScript 中有效的變量名？

#### --distractors--

`tart@n`

---

`4cats`

---

`!!dogs`

#### --answer--

`$number`

### --question--

#### --text--

以下哪項不是 JavaScript 中有效的字符串連接方法？

#### --distractors--

`console.log(string1 + " " + string2);`

---

`console.log("string1 " + string2);`

---

`console.log(string1.concat(" ", string2));`

#### --answer--

`console.log(string1 string2);`

### --question--

#### --text--

以下哪個變量名正確使用了駝峯大小寫？

#### --distractors--

`a_long_variable_name`

---

`alongvariableName`

---

`ALongVariableName`

#### --answer--

`aLongVariableName`

### --question--

#### --text--

爲什麼即使 JavaScript 具有自動分號插入功能，明確使用分號仍有好處？

#### --distractors--

提高代碼執行速度。

---

與其他編程語言保持一致。

---

以便更好地進行調試和錯誤跟蹤。

#### --answer--

提高代碼的可讀性和可靠性。

### --question--

#### --text--

以下哪項不是 JavaScript 數據類型？

#### --distractors--

`Symbol`

---

`Boolean`

---

`Null`

#### --answer--

`Float`
