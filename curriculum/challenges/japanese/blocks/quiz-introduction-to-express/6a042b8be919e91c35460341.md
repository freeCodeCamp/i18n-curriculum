---
id: 6a042b8be919e91c35460341
title: Express入門クイズ
challengeType: 8
dashedName: quiz-introduction-to-express
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

APIとウェブサービスの関係は何ですか？

#### --distractors--

すべてのAPIはウェブサービスですが、すべてのウェブサービスがAPIとは限りません。

---

APIとウェブサービスは同じ概念です。

---

ウェブサービスはRESTのみ対応し、APIは複数のプロトコルをサポートします。

#### --answer--

すべてのウェブサービスはAPIですが、すべてのAPIがウェブサービスとは限りません。

### --question--

#### --text--

クライアントがサーバーにどのレスポンス形式を望んでいるかを伝えるために使うHTTPヘッダーはどれですか？

#### --distractors--

`Content-Type`

---

`Authorization`

---

`X-Requested-With`

#### --answer--

`Accept`

### --question--

#### --text--

RESTアーキテクチャにおけるステートレスとは何を意味しますか？

#### --distractors--

サーバーはリクエスト間でクライアントのセッションデータを保存します。

---

クライアントはリクエスト間で自身の状態を保持しません。

---

RESTはすべてのリクエストを読み取り専用操作に制限します。

#### --answer--

サーバーは以前のリクエストの記憶を保持しません。

### --question--

#### --text--

リソースを削除するために使われるRESTのHTTPメソッドはどれですか？

#### --distractors--

`GET`

---

`POST`

---

`PUT`

#### --answer--

`DELETE`

### --question--

#### --text--

モノリシックアーキテクチャに比べてマイクロサービスの主な利点は何ですか？

#### --distractors--

デバッグが簡単でサービス間のネットワーク呼び出しが少ない。

---

すべてのチームが貢献する単一の共有コードベース。

---

すべてのサービスでインフラ管理が簡単。

#### --answer--

開発が速く、障害の隔離が良く、サービスごとに異なる言語を使えること。

### --question--

#### --text--

`express()`を呼び出すと何が返されますか？

#### --distractors--

新しいHTTPサーバー。

---

ルーターオブジェクト。

---

ミドルウェア関数。

#### --answer--

Expressアプリケーションの新しいインスタンス。

### --question--

#### --text--

Expressにおけるミドルウェアとは何ですか？

#### --distractors--

Expressサーバーを起動するための組み込みメソッド。

---

REST APIで使われる特別なHTTPメソッド。

---

ディレクトリから静的ファイルを提供する組み込みツール。

#### --answer--

リクエストを受け取ってレスポンスを送る間に実行されるコード。

### --question--

#### --text--

Expressで`app.listen(port, callback)`は何をしますか？

#### --distractors--

受信リクエストのルートを定義する。

---

Expressライブラリを読み込む。

---

新しいミドルウェア関数を作成する。

#### --answer--

サーバーを起動し、HTTPリクエストの受信を開始する。

### --question--

#### --text--

Expressにおけるルートパラメータとは何ですか？

#### --distractors--

`/home`のような固定URLパス。

---

URLに付加されるクエリ文字列。

---

静的ファイルを提供する方法。

#### --answer--

`:`が接頭辞のURL内のプレースホルダーで、動的な値をキャプチャする。

### --question--

#### --text--

Expressルートで`/post/42`のように定義されたURL `/post/:postId` からキャプチャした動的な値にどうアクセスしますか？

#### --distractors--

`req.query.postId`

---

`req.body.postId`

---

`req.url.postId`

#### --answer--

`req.params.postId`

### --question--

#### --text--

Expressで`res.json()`は何をしますか？

#### --distractors--

プレーンテキストのレスポンスを送る。

---

クライアントを別のURLにリダイレクトする。

---

テンプレートエンジンのビューをレンダリングしてHTMLを送る。

#### --answer--

JSONレスポンスを送り、JavaScriptオブジェクトを自動的に変換する。

### --question--

#### --text--

Expressで`res.redirect(url)`は何をしますか？

#### --distractors--

HTTPステータスコードを設定する。

---

JSONレスポンスを送る。

---

テンプレートエンジンを使ってビューをレンダリングする。

#### --answer--

クライアントを別のURLにリダイレクトする。

### --question--

#### --text--

Expressで`app.route()`の目的は何ですか？

#### --distractors--

Expressアプリケーションの新しいインスタンスを作成するため。

---

指定したディレクトリから静的ファイルを提供するため。

---

異なるURLパスに対して別々のルートを定義するため。

#### --answer--

同じパスに対して複数のHTTPメソッドハンドラを1つのブロックで連結するため。

### --question--

#### --text--

`express.Router()`は何を作成しますか？

#### --distractors--

すべてのサーバーロジックを処理するために`app`を置き換える新しいExpressアプリケーションインスタンス。

---

ルートレベルのエラーをキャッチして処理する専用のミドルウェア関数。

---

ファイルをURLパスに自動マッピングする組み込みの静的ファイルサーバー。

#### --answer--

メインアプリケーションにマウントしてルートを定義できる、ミニExpressアプリのようなルーターオブジェクト。

### --question--

#### --text--

メインのExpressアプリにルーターをマウントするにはどうしますか？

#### --distractors--

`router.listen()`を呼び出す。

---

`router.start()`を呼び出す。

---

ルーター内で直接`app.get()`を呼び出す。

#### --answer--

`app.use(path, router)`を呼び出す。

### --question--

#### --text--

静的ファイルを提供するために使われる組み込みExpressミドルウェアはどれですか？

#### --distractors--

`express.json()`

---

`express.urlencoded()`

---

`express.raw()`

#### --answer--

`express.static()`

### --question--

#### --text--

`express.static()`で設定されたディレクトリに存在しない静的ファイルをユーザーがリクエストした場合、何が起こりますか？

#### --distractors--

Expressがエラーをスローしてサーバーがクラッシュする。

---

Expressがユーザーをホームページにリダイレクトする。

---

Expressがリクエストされたパスに空のファイルを作成する。

#### --answer--

Expressが自動的に404で応答する。

### --question--

#### --text--

RESTと比べたSOAPについて正しいのはどれですか？

#### --distractors--

SOAPはJSONを使い、RESTより柔軟で現代的なアプリに最適。

---

SOAPは今日最も広く使われるウェブサービスで、多くのシステムでRESTに取って代わった。

---

SOAPはプレーンテキスト、JSON、バイナリデータなど複数のレスポンス形式をサポートする。

#### --answer--

SOAPはXMLのみを使い、より厳格な基準とセキュリティが必要な企業システムで一般的。

### --question--

#### --text--

Expressにおけるルートハンドラとは何ですか？

#### --distractors--

利用可能なすべてのルートパスをHTTPメソッドにマッピングする設定オブジェクト。

---

フォルダから静的ファイルを自動的にインターセプトして提供するミドルウェア関数。

---

ルート関連のデータベースクエリを処理するデータベースコネクタ。

#### --answer--

リクエストがルートにマッチしたときに実行されるコールバック関数で、`req`と`res`にアクセスできる。

### --question--

#### --text--

`express.Router()`を使う利点は何ですか？すべてのルートを直接`app.js`に定義する代わりに。

#### --distractors--

サーバーが同時に処理できるHTTPリクエスト数が大幅に増える。

---

ルートファイル名に基づいてREST APIエンドポイントを自動生成・登録する。

---

ポート番号を指定せずにサーバーを起動・実行できる。

#### --answer--

関連するルートを独自のモジュールに分けてメインファイルをすっきり保てる。
