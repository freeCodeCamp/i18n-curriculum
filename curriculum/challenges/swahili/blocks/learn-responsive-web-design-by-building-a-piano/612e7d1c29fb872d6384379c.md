---
id: 612e7d1c29fb872d6384379c
title: Hatua 4
challengeType: 0
dashedName: step-4
---

# --description--

Panga ndani `div` ya pili ndani ya `div` uliyonayo, na weka `class` iwe `keys`.

# --hints--

Unapaswa kuunda kipengele cha `div` cha pili.

```js
const divDiv = document.querySelectorAll('div');
assert(divDiv?.length === 2);
```

Kipengele chako kipya cha `div` kinapaswa kuwa ndani ya kipengele chako cha `div` uliyonacho.

```js
const div = document.querySelector('div');
assert(div?.children?.length === 1);
assert(div?.children?.[0]?.localName === 'div');
```

Kipengele chako kipya cha `div` kinapaswa kuwa na `class` iliyowekwa kuwa `keys`.

```js
const div = document.querySelector('div');
assert(div?.children?.[0]?.className === 'keys');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Piano</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    <div id="piano">
  --fcc-editable-region--

  --fcc-editable-region--
    </div>
  </body>
</html>
```

```css

```
