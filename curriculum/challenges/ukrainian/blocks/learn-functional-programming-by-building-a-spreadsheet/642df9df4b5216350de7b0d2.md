---
id: 642df9df4b5216350de7b0d2
title: Крок 6
challengeType: 0
dashedName: step-6
---

# --description--

Вам потрібна функція для генерації діапазону чисел.

Оголосіть порожню `range` функцію, яка приймає параметри `start` та `end`. Використайте конструктор `Array()` і неявно поверніть порожній масив.

# --hints--

Ви повинні оголосити змінну `range`.

```js
assert.match(code, /(?:let|var|const)\s+range/);
```

Ви повинні використати `const` для оголошення змінної `range`.

```js
assert.match(code, /const\s+range/);
```

Ваша змінна `range` має бути функцією.

```js
assert.isFunction(range);
```

Ваша функція `range` має використовувати стрілковий синтаксис.

```js
assert.match(code, /const\s+range\s*=\s*\(.*\)\s*=>/);
```

Ваша функція `range` має спочатку приймати параметр `start`.

```js
assert.match(code, /const\s+range\s*=\s*\(\s*start/)
```

Ваша функція `range` має другий параметр `end`.

```js
assert.match(code, /const\s+range\s*=\s*\(\s*start\s*,\s*end\s*\)/);
```

Ваша функція `range` має використовувати неявне повернення. Пам’ятайте, що це означає відсутність фігурних дужок.

```js
assert.notMatch(code, /const\s+range\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*{/);
```

Ваша функція `range` має використовувати конструктор `Array()`. Примітивні конструктори не потребують ключового слова `new`.

```js
assert.match(code, /const\s+range\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*Array\(/);
```

Ви не повинні передавати нічого конструктору `Array()`.

```js
assert.match(code, /const\s+range\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*Array\(\s*\)/);
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

```css
#container {
  display: grid;
  grid-template-columns: 50px repeat(10, 200px);
  grid-template-rows: repeat(11, 30px);
}

.label {
  background-color: lightgray;
  text-align: center;
  vertical-align: middle;
  line-height: 30px;
}
```

```js
--fcc-editable-region--

--fcc-editable-region--

window.onload = () => {
  const container = document.getElementById("container");
  const createLabel = (name) => {
    const label = document.createElement("div");
    label.className = "label";
    label.textContent = name;
    container.appendChild(label);
  }
}
```
