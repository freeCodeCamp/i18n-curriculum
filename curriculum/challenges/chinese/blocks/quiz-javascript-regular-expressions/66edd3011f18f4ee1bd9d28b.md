---
id: 66edd3011f18f4ee1bd9d28b
title: JavaScript 正则表达式测验
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项不是有效的正则表达式？

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

哪个 `RegExp` 包含与正则表达式 `/f[o0]{2} b[a4@]r/i` 相同的模式？

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

正则表达式方法 `test` 的返回类型是什么？

#### --distractors--

一个匹配正则表达式的字串数组。

---

包含关于正则表达式匹配的（子）字串的信息的对象。

---

`null`，方法 `test` 仅验证给定的正则表达式是否有效。

#### --answer--

一个布尔值，指示字串是否匹配正则表达式。

### --question--

#### --text--

当传入不带任何标记的正则表达式时，`match` 方法会做什么？

#### --distractors--

它查找与给定正则表达式匹配的所有字串，并将所有匹配作为数组返回。

---

它查找第一个完整匹配，并返回该匹配的起始索引。

---

它查找第一个完整匹配，并返回一个布尔值，指示是否找到匹配。

#### --answer--

它查找第一个完整匹配，并返回包含该第一个匹配的数组。

### --question--

#### --text--

`test` 的最佳使用分支是什么？

#### --distractors--

用于验证给定的正则表达式。

---

用于获取有关字串匹配的详细信息，例如匹配字串的长度。

---

从给定的字串中提取所有匹配。

#### --answer--

查看给定的字串是否匹配正则表达式。

### --question--

#### --text--

使用 `replace` 的目的是什么？

#### --distractors--

将当前的正则表达式替换为新建的正则表达式。

---

当给定一个索引和一个字串时，将指定索引处的字符替换为提供的字串。

---

从字串中移除所有匹配子串的实例。

#### --answer--

将匹配的字串替换为给定的替换字串。

### --question--

#### --text--

`match` 和 `matchAll` 有什么区别？

#### --distractors--

`match` 总是返回一个字串，而 `matchAll` 总是返回一个匹配的数组。

---

`match` 返回一个布尔值，表示是否找到匹配，而 `matchAll` 返回一个数字，表现找到的匹配数量。

---

`match` 返回一个数字，表示找到的第一个匹配项的起始索引，而 `matchAll` 返回一个包含匹配项索引的数字数组。

#### --answer--

`match` 返回一个匹配的字串数组，而 `matchAll` 返回一个包含所有匹配项的迭代器对象。

### --question--

#### --text--

以下哪项是 `replaceAll` 的正确用法？

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

以下哪个字符类等价于正则表达式 `/[a-zA-Z0-9_]/`？

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

以下哪个字符类最适合从电话号码中提取数字？

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

当在正则表达式中使用前瞻 `(?=)` 会发生什么？

#### --distractors--

前瞻将对字串的左侧进行断言，验证子模式存在。

---

前瞻是一种备用的正则表达式模式，用于当主模式匹配失败时。

---

前瞻将对字串右侧进行断言，验证子模式不存在。

#### --answer--

前瞻断言位于字串的右侧，验证子模式存在。

### --question--

#### --text--

在编写正则表达式时，`(?<=)` 应该放在哪里？

#### --distractors--

前瞻断言可以放置在正则表达式的任何位置。

---

前瞻应放置在正则表达式主模式的右侧。

---

向后查找应包括正则表达式的主模式。

#### --answer--

前瞻应放置在正则表达式主模式的左侧。

### --question--

#### --text--

哪个量词匹配前述的元素零次或一次？

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

以下哪个正则表达式只允许 0 到 999,999 之间的数字？

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

以下关于自定义字符类（`[]`）的语句哪项是正确的？

#### --distractors--

这是一组要从匹配中移除的字符。

---

它可以定义一组字符进行匹配，而无需转义任何特殊字符。

---

它表现为 Unicode 形式的一组字符。

#### --answer--

它可以定义一组自定义的字符以进行匹配。

### --question--

#### --text--

以下哪个字符类能正确匹配大写字母？

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

当在正则表达式中使用捕获组 `(...)` 会发生什么？

#### --distractors--

捕获组会立即被求值，无论它位于正则表达式的何处。

---

捕获组尝试使用给定的子模式进行匹配，并继续而不记忆结果。

---

捕获组是一个优先子模式，当找到子模式的匹配时会立即返回。

#### --answer--

捕获组尝试使用给定的子模式进行匹配，并将结果保存在内存中继续。

### --question--

#### --text--

当在正则表达式中使用非捕获组 `(?:...)` 会发生什么？

#### --distractors--

非捕获组尝试使用给定的子模式进行匹配，并将结果保存在内存中继续。

---

非捕获组在查找到给定子模式的匹配时立即终止。

---

非捕获组被视为可选的匹配，因此只有成功的匹配才会添加到结果中。

#### --answer--

非捕获组尝试使用给定的子 `pattern` 进行 `匹配`，并继续而不将结果保存在 `内存` 中。

### --question--

#### --text--

给定以下正则表达式，哪些测试将返回 `true`？

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

通配符字符类（`.`）在正则表达式中有什么作用？

#### --distractors--

它只匹配空白字符。

---

它匹配字串的开头。

---

它匹配一个数字。

#### --answer--

它匹配除换行符以外的任意单个字符。
