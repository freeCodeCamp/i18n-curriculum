---
id: 66ed8fe7f45ce3ece4053eb2
title: اختبار قصير في CSS Flexbox
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

لتجاوز الاختبار القصير، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هو flexbox؟

#### --distractors--

نموذج ثنائي الأبعاد للتخطيط.

---

نموذج ثلاثي الأبعاد للتخطيط.

---

نموذج متعدد الأبعاد للتخطيط.

#### --answer--

نموذج أحادي البعد للتخطيط.

### --question--

#### --text--

ما الخاصية في CSS التي تُعيّن لتمكين تخطيط flexbox لعنصر `div`؟

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

أي خاصية CSS تسمح لعناصر flex بالالتفاف إلى صف جديد عندما يمتلئ الصف الحالي؟

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

ماذا تتحكم خاصية flexbox `justify-content`؟

#### --distractors--

تتحكم في اتجاه العناصر الفرعية داخل حاوية flexbox.

---

تتحكم في نمط النص للعناصر الفرعية داخل حاوية flexbox.

---

تتحكم في محاذاة العناصر الفرعية داخل حاوية flexbox.

#### --answer--

تتحكم في توزيع العناصر الفرعية على المحور الرئيسي.

### --question--

#### --text--

ما الخاصية التي تتحكم في اتجاه العناصر داخل حاوية flexbox؟

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

أي خصائص CSS يمكن استخدامها لجعل عناصر flex تتدفق من اليسار إلى اليمين، ويظهر العنصر المرن الجديد فوق السابق إذا تجاوز عرض الصف الحالي؟

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

ما الخاصية التي تتحكم في تموضع العناصر على المحور المتقاطع؟

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

أي مما يلي ليس قيمة للخاصية `justify-content`؟

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

ما خصائص CSS التي تستخدمها لمركزة عنصر داخل حاوية مرنة أفقيًا وعموديًا؟

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

ما قيمة `flex-direction` التي تجعل عناصر flex تتحرك من اليمين إلى اليسار؟

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

ما الذي ليس قيمة للخاصية `flex-wrap`؟

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

ماذا تفعل القيمة `stretch` في `align-items`؟

#### --distractors--

تُحزم العناصر مباشرة بجانب بعضها نحو حافة البداية للحاوية.

---

تُحاذى العناصر بحيث تتطابق خطوط الأساس لحاوية flex الخاصة بها.

---

تُحزم العناصر مباشرة بجانب بعضها نحو حافة النهاية للحاوية.

#### --answer--

تُضبط العناصر تلقائيًا لتكون متساوية الحجم لملء الحاوية.

### --question--

#### --text--

أي قيمة لـ `justify-content` تضع العنصر الأول في البداية، والعنصر الأخير في النهاية، وتوزع العناصر الأخرى بالتساوي بينهما على المحور الرئيسي؟

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

أي قيمة لـ `justify-content` تباعد العناصر بحيث تكون الفجوات بين كل زوج من العناصر المتجاورة وحواف الحاوية متساوية؟

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

أي قيمة لـ `justify-content` تباعد العناصر بالتساوي على المحور الرئيسي مع ترك نصف تلك المسافة عند حواف الحاوية؟

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

أي مما يلي يؤدي إلى محاذاة العناصر عند بداية المحور المتقاطع؟

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

أي مما يلي يؤدي إلى محاذاة العناصر عند نهاية المحور المتقاطع؟

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

أي قيمة لـ `flex-direction` تجعل العناصر مصطفة على المحور الرئيسي من اليسار إلى اليمين؟

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

أي قيمة لـ `flex-direction` تجعل العناصر مصطفة على المحور المتقاطع من الأعلى إلى الأسفل؟

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

أي من حالات الاستخدام التالية غير مناسبة لـ flexbox؟

#### --distractors--

مركزة عنصر في حاوية.

---

محاذاة عمودية للعناصر في صف.

---

تعديل ديناميكي لتوزيع ومحاذاة العناصر لأطر عرض مختلفة.

#### --answer--

تحديد حجم ثابت ومسافات بين العناصر مثل الشبكة.

## --quiz--

### --question--

#### --text--

ما الغرض من نموذج تخطيط أحادي البعد مثل Flexbox؟

#### --distractors--

للتعامل مع التخطيط في بعدين، الصفوف والأعمدة في نفس الوقت.

---

للتحكم في ترتيب التكديس للعناصر المحددة الموضع باستخدام `z-index`.

---

لإنشاء تحويلات ثلاثية الأبعاد ورسوم متحركة معقدة على عناصر الصفحة.

#### --answer--

لوضع العناصر على خط أو محور واحد، إما كصف أو كعمود.

### --question--

#### --text--

أي خاصية CSS تصطف كل عناصر flex على الجانب الأيمن من عنصر `.container`؟

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

ما المحوران اللذان يحددان اتجاه تخطيط العناصر في نموذج Flex؟

#### --distractors--

المحور الأفقي والمحور العمودي.

---

محور x ومحور y.

---

المحور الأساسي والمحور الثانوي.

#### --answer--

المحور الرئيسي والمحور المتقاطع.

### --question--

#### --text--

أي من قواعد CSS التالية ترتب العناصر في عمود عمودي وتُركزها أفقيًا داخل الحاوية؟

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

ما الغرض الرئيسي من الخاصية `flex-wrap`؟

#### --distractors--

تحدد اتجاه وضع عناصر flex داخل الحاوية المرنة.

---

تُحاذي عناصر flex على المحور الرئيسي للحاوية.

---

تُحاذي عناصر flex على المحور المتقاطع للحاوية.

#### --answer--

تحدد ما إذا كان يجب على عناصر flex الالتفاف إلى أسطر جديدة لتناسب المساحة المتاحة.

### --question--

#### --text--

ما السلوك الافتراضي للإعلان `flex-wrap: nowrap;`؟

#### --distractors--

يجبر عناصر flex على الالتفاف إلى سطر جديد إذا كانت واسعة جدًا للحاوية.

---

يلتف العناصر إلى سطر جديد، لكن بالترتيب العكسي.

---

يمنع الحاوية المرنة من أن تكون أكبر من عنصرها الأب.

#### --answer--

يجبر عناصر flex على البقاء في سطر واحد، حتى لو تجاوزت الحاوية.

### --question--

#### --text--

أي خاصية CSS هي اختصار لتعيين كل من `flex-direction` و `flex-wrap` مرة واحدة؟

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

ما تأثير تعيين `flex-direction: column-reverse;` على حاوية مرنة؟

#### --distractors--

يرتب العناصر أفقيًا من اليمين إلى اليسار.

---

يعكس العناصر في صف، لكنه يبقيها في سطر واحد.

---

يرتب العناصر عموديًا من الأعلى إلى الأسفل.

#### --answer--

يرتب العناصر عموديًا بترتيب عكسي من الأسفل إلى الأعلى.

### --question--

#### --text--

أي مقتطف CSS يرتب عناصر flex في عمود عمودي ويسمح لها بالالتفاف إلى عمود جديد إذا تجاوزت ارتفاع الحاوية؟

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

أي قيمة لـ `justify-content` تصطف كل عناصر flex عند نهاية المحور الرئيسي للحاوية؟

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

ما تأثير قاعدة CSS التالية على عناصر flex؟

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

ترتب العناصر في عمود وستلتف إلى عمود جديد.

---

ترتب العناصر في صف وستلتف إلى صف جديد.

---

ترتب العناصر في عمود وستتجاوز السعة إذا لم تناسب.

#### --answer--

ترتب العناصر في صف وستتجاوز الحاوية إذا لم تناسب.

### --question--

#### --text--

كيف تصطف عناصر flex عند استخدام `justify-content: space-evenly;`؟

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

تكون العناصر متباعدة بالتساوي، بدون مساحة في البداية أو النهاية.

---

تُحزم العناصر معًا في وسط الحاوية.

---

تُدفع العناصر إلى الجانب الأيمن من الحاوية، مع تباعد متساوٍ بينها.

#### --answer--

تكون المسافات متساوية بين العناصر وعند طرفي الحاوية.

### --question--

#### --text--

أي خاصية تستخدم لمركزة العناصر عموديًا عند تعيين `flex-direction: row;`؟

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

ماذا تفعل القيمة `flex-start` للخاصية `justify-content`؟

#### --distractors--

تصطف العناصر عند بداية المحور المتقاطع.

---

تمتد العناصر لملء المحور الرئيسي للحاوية.

---

تُركز كل العناصر على المحور الرئيسي.

#### --answer--

تصطف العناصر عند بداية المحور الرئيسي للحاوية.

### --question--

#### --text--

كيف تضبط حاوية مرنة لترتيب عناصرها من اليمين إلى اليسار في سطر واحد، وأيضًا تموضعها في أعلى الحاوية؟

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

أي من الإعلانات التالية صالحة للخاصية `flex-flow`؟

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

أي من قواعد CSS التالية تجعل عناصر flex مصطفة أفقيًا وممتدة عموديًا لملء ارتفاع الحاوية؟

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

ما تأثير `align-items: stretch;` عند تعيين `flex-direction` إلى `column`؟

#### --distractors--

تمتد العناصر عموديًا لملء ارتفاع الحاوية.

---

لا تأثير له عندما يكون الاتجاه `column`.

---

تمتد العناصر عموديًا لتطابق أطول عنصر.

#### --answer--

تمتد العناصر أفقيًا لملء عرض الحاوية.

### --question--

#### --text--

إذا أردت عكس ترتيب العناصر في صف أفقي، أي إعلان تستخدم؟

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

أي قيمة لـ `align-items` تضع العناصر في أعلى الحاوية التي يكون فيها `flex-direction` هو `row`؟

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
