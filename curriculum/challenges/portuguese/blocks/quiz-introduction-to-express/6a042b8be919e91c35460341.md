---
id: 6a042b8be919e91c35460341
title: Introdução ao quiz sobre Express
challengeType: 8
dashedName: quiz-introduction-to-express
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual é a relação entre uma API e um serviço web?

#### --distractors--

Toda API é um serviço web, mas nem todo serviço web é uma API.

---

APIs e serviços web são conceitos idênticos.

---

Serviços web funcionam apenas com REST, enquanto APIs suportam múltiplos protocolos.

#### --answer--

Todo serviço web é uma API, mas nem toda API é um serviço web.

### --question--

#### --text--

Qual cabeçalho HTTP um cliente usa para informar ao servidor qual formato de resposta deseja?

#### --distractors--

`Content-Type`

---

`Authorization`

---

`X-Requested-With`

#### --answer--

`Accept`

### --question--

#### --text--

O que significa ser sem estado em uma arquitetura REST?

#### --distractors--

O servidor armazena os dados da sessão do cliente entre requisições.

---

O cliente não mantém nenhum estado próprio entre requisições.

---

REST restringe todas as requisições a operações somente de leitura.

#### --answer--

O servidor não mantém memória das requisições anteriores.

### --question--

#### --text--

Qual método HTTP REST é usado para remover um recurso?

#### --distractors--

`GET`

---

`POST`

---

`PUT`

#### --answer--

`DELETE`

### --question--

#### --text--

Qual é uma vantagem chave dos microserviços em relação a um monólito?

#### --distractors--

Depuração mais simples e menos chamadas de rede entre serviços.

---

Uma única base de código compartilhada para todas as equipes.

---

Gerenciamento de infraestrutura mais fácil em todos os serviços.

#### --answer--

Desenvolvimento mais rápido, melhor isolamento de falhas e a capacidade de usar diferentes linguagens por serviço.

### --question--

#### --text--

O que a chamada `express()` retorna?

#### --distractors--

Um novo servidor HTTP.

---

Um objeto roteador.

---

Uma função middleware.

#### --answer--

Uma nova instância da aplicação Express.

### --question--

#### --text--

O que é middleware no Express?

#### --distractors--

Um método embutido usado para iniciar o servidor Express.

---

Um tipo especial de método HTTP usado em APIs REST.

---

Uma ferramenta embutida para servir arquivos estáticos de um diretório.

#### --answer--

Código que roda entre o recebimento de uma requisição e o envio de uma resposta.

### --question--

#### --text--

O que `app.listen(port, callback)` faz no Express?

#### --distractors--

Define uma rota para requisições recebidas.

---

Carrega a biblioteca Express.

---

Cria uma nova função middleware.

#### --answer--

Inicia o servidor e começa a escutar requisições HTTP recebidas.

### --question--

#### --text--

O que é um parâmetro de rota no Express?

#### --distractors--

Um caminho de URL fixo como `/home`.

---

Uma string de consulta anexada à URL.

---

Um método para servir arquivos estáticos.

#### --answer--

Um marcador de posição na URL prefixado com `:` que captura um valor dinâmico.

### --question--

#### --text--

Como você acessa o valor dinâmico capturado de uma URL como `/post/42` em uma rota Express definida como `/post/:postId`?

#### --distractors--

`req.query.postId`

---

`req.body.postId`

---

`req.url.postId`

#### --answer--

`req.params.postId`

### --question--

#### --text--

O que `res.json()` faz no Express?

#### --distractors--

Envia uma resposta em texto simples.

---

Redireciona o cliente para uma URL diferente.

---

Renderiza uma view de um template engine e envia o HTML resultante.

#### --answer--

Envia uma resposta JSON e converte automaticamente um objeto JavaScript.

### --question--

#### --text--

O que `res.redirect(url)` faz no Express?

#### --distractors--

Define o código de status HTTP.

---

Envia uma resposta JSON.

---

Renderiza uma view usando um template engine.

#### --answer--

Redireciona o cliente para uma URL diferente.

### --question--

#### --text--

Qual é o propósito de `app.route()` no Express?

#### --distractors--

Criar uma nova instância da aplicação Express.

---

Servir arquivos estáticos de um diretório especificado.

---

Definir rotas separadas para diferentes caminhos de URL.

#### --answer--

Encadear múltiplos manipuladores de métodos HTTP para o mesmo caminho em um único bloco.

### --question--

#### --text--

O que `express.Router()` cria?

#### --distractors--

Uma nova instância da aplicação Express que substitui `app` para lidar com toda a lógica do servidor.

---

Uma função middleware dedicada a capturar e tratar erros em nível de rota.

---

Um servidor embutido de arquivos estáticos que mapeia automaticamente arquivos para caminhos de URL.

#### --answer--

Um objeto roteador, parecido com um mini app Express, onde você define rotas e monta na aplicação principal.

### --question--

#### --text--

Como você monta um roteador na aplicação Express principal?

#### --distractors--

Chamando `router.listen()`.

---

Chamando `router.start()`.

---

Chamando `app.get()` diretamente dentro do roteador.

#### --answer--

Chamando `app.use(path, router)`.

### --question--

#### --text--

Qual middleware embutido do Express é usado para servir arquivos estáticos?

#### --distractors--

`express.json()`

---

`express.urlencoded()`

---

`express.raw()`

#### --answer--

`express.static()`

### --question--

#### --text--

O que acontece quando um usuário solicita um arquivo estático que não existe no diretório configurado com `express.static()`?

#### --distractors--

Express lança um erro e o servidor trava.

---

Express redireciona o usuário para a página inicial.

---

Express cria um arquivo vazio no caminho solicitado.

#### --answer--

Express responde automaticamente com um 404.

### --question--

#### --text--

Qual das seguintes afirmações é verdadeira sobre SOAP comparado ao REST?

#### --distractors--

SOAP usa JSON e é mais flexível que REST, tornando-o ideal para aplicações modernas.

---

SOAP é o tipo de serviço web mais usado hoje, tendo substituído REST na maioria dos sistemas.

---

SOAP suporta múltiplos formatos de resposta, incluindo texto simples, JSON e dados binários.

#### --answer--

SOAP usa apenas XML e é comum em sistemas empresariais que precisam de padrões e segurança mais rígidos.

### --question--

#### --text--

O que é um manipulador de rota no Express?

#### --distractors--

Um objeto de configuração que mapeia todos os caminhos de rota disponíveis para seus métodos HTTP.

---

Uma função middleware que intercepta automaticamente e serve arquivos estáticos de uma pasta.

---

Um conector de banco de dados que processa e trata consultas relacionadas a rotas.

#### --answer--

Uma função de callback que roda quando uma requisição corresponde a uma rota, com acesso a `req` e `res`.

### --question--

#### --text--

Qual é um benefício de usar `express.Router()` em vez de definir todas as rotas diretamente em `app.js`?

#### --distractors--

Permite que o servidor lide com muito mais requisições HTTP concorrentes ao mesmo tempo.

---

Gera e registra automaticamente endpoints REST API baseados nos nomes dos arquivos de rota.

---

Permite que o servidor inicie e rode sem precisar especificar um número de porta.

#### --answer--

Separa responsabilidades e mantém o arquivo principal limpo ao mover rotas relacionadas para seus próprios módulos.
