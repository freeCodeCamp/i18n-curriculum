---
id: 67d3018062fe6ba92b7d8299
title: Wie funktionieren primitive Typen in TypeScript?
challengeType: 19
dashedName: how-do-primitive-types-work-in-typescript
---

# --description--

In dieser Lektion lernen Sie, wie Sie mit grundlegenden primitiven Typen arbeiten und wie Typannotationen für Variablen funktionieren.

Für die primitiven Datentypen `string`, `null`, `undefined`, `number`, `boolean` und `bigint` bietet TypeScript entsprechende Typ-Schlüsselwörter an.

In dieser Lektion konzentrieren wir uns auf die häufig verwendeten Primitiven und gehen nicht näher auf `bigint` ein. Der `bigint`-Datentyp ist ein numerischer Datentyp in JavaScript, um Zahlen darzustellen, die größer sind als der Standard-Ganzzahltyp.

Schauen wir uns jeden dieser primitiven Typen an, beginnend mit `string`. So können Sie in TypeScript einen String typisieren:

```ts
let firstName: string = "Angie";
```

Im Gegensatz zu reinem JavaScript erwartet die Variable `firstName` hier einen Typ vom Typ `string`. Wenn Sie also versuchen, ihr einen Wert vom Typ Zahl zuzuweisen, erhalten Sie einen Fehler wie diesen:

```ts
let firstName: string = "Angie";
firstName = 9 // Type 'number' is not assignable to type 'string'.
```

Der nächste Typ, den wir betrachten, ist der Typ `number`. Hier ein Beispiel:

```ts
let age: number = 16;
```

Sie können Ganzzahlen und Fließkommazahlen dem Typ `number` zuweisen. Wenn Sie jedoch versuchen, einen Wert vom Typ `bigint` zuzuweisen, erhalten Sie einen Fehler wie diesen:

```ts
let age: number = 16;

// Type 'bigint' is not assignable to type 'number'.
age = 123n;
```

Der nächste Typ, den wir betrachten, ist der Typ `boolean`. Hier einige Beispiele:

```ts
let isLoggedIn: boolean = true;
let isAdmin: boolean = false;
```

Die letzten beiden Typen sind die Typen `null` und `undefined`.

Wie Sie aus früheren JavaScript-Lektionen wissen, steht `null` für das absichtliche Fehlen eines Werts. `undefined` bedeutet, dass eine Variable deklariert wurde, aber noch kein Wert zugewiesen ist.

Sie werden später Beispiele sehen, wie Sie mit diesen Typen arbeiten, wenn Sie etwas über Funktionstypen und Unionstypen lernen.

Nachdem Sie nun gesehen haben, wie Typannotationen bei Variablen funktionieren, stellt sich die Frage, ob Sie Ihre Variablen immer explizit typisieren müssen.

Wenn Sie eine einfache Typannotation wie diese haben, müssen Sie möglicherweise keine explizite Typangabe verwenden.

```ts
let firstName: string = "Angie";
```

Der Grund dafür ist, dass TypeScript versucht, die Typen automatisch anhand des Werts zu ermitteln. Wenn Sie also Folgendes schreiben, versteht TypeScript, dass es sich um einen String handelt.

```js
let firstName = "Angie";
```

Sie sollten Ihre Variablen explizit typisieren, wenn TypeScript den Typ nicht eindeutig ermitteln kann oder wenn Sie einen bestimmten Typ erzwingen möchten, zum Beispiel bei Funktionsparametern oder komplexen Objekten.

# --questions--

## --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um einer Zahl-Variable explizit eine Typannotation hinzuzufügen?

## --answers--

```ts
let age: number = 16;
```

---

```ts
let age<> number = 16;
```

### --feedback--

Sehen Sie sich den Anfang der Lektion zu Zahlentypen an.

---

```ts
let age>> number = 16;
```

### --feedback--

Sehen Sie sich den Anfang der Lektion zu Zahlentypen an.

---

```ts
let age == number = 16;
```

### --feedback--

Sehen Sie sich den Anfang der Lektion zu Zahlentypen an.

## --video-solution--

1

## --text--

Welcher der folgenden Typen ist KEIN primitiver Datentyp?

## --answers--

`string`

### --feedback--

Sehen Sie sich den Anfang der Lektion an.

---

`boolean`

### --feedback--

Sehen Sie sich den Anfang der Lektion an.

---

`object`

---

`number`

### --feedback--

Sehen Sie sich den Anfang der Lektion an.

## --video-solution--

3

## --text--

Wann sollten Sie Ihre Variablen in TypeScript explizit typisieren?

## --answers--

Wenn Sie mit den Typen `number` und `bigint` arbeiten.

### --feedback--

Sehen Sie sich das Ende der Lektion an.

---

Wenn Sie mit den Typen `null` und `undefined` arbeiten.

### --feedback--

Sehen Sie sich das Ende der Lektion an.

---

Wenn TypeScript den Typ nicht eindeutig ermitteln kann.

---

Sie sollten immer explizite Typen in Ihrem Code haben.

### --feedback--

Sehen Sie sich das Ende der Lektion an.

## --video-solution--

3
