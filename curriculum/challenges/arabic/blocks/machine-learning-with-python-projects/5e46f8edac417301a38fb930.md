---
id: 5e46f8edac417301a38fb930
title: حاسبة تكاليف الرعاية الصحية باستخدام الانحدار الخطي
challengeType: 10
forumTopicId: 462379
dashedName: linear-regression-health-costs-calculator
---

# --description--

ستعمل <a href="https://colab.research.google.com/github/freeCodeCamp/boilerplate-linear-regression-health-costs-calculator/blob/master/fcc_predict_health_costs_with_regression.ipynb" target="_blank" rel="noopener noreferrer nofollow">على هذا المشروع باستخدام Google Colaboratory</a>.

بعد الذهاب إلى ذلك الرابط، أنشئ نسخة من الدفتر إما في حسابك الخاص أو محليًا. بمجرد إكمال المشروع واجتيازه للاختبار (المضمن في ذلك الرابط)، أرسل رابط مشروعك أدناه. إذا كنت ترسل رابط Google Colaboratory، فتأكد من تفعيل مشاركة الرابط لـ"أي شخص لديه الرابط".

نحن لا نزال نطور المحتوى التعليمي التفاعلي لمنهج تعلم الآلة. في الوقت الحالي، يمكنك متابعة تحديات الفيديو في هذه الشهادة. قد تحتاج أيضًا إلى البحث عن موارد تعلم إضافية، كما تفعل عند العمل على مشروع حقيقي.

# --instructions--

في هذا التحدٍّ، ستتنبأ بتكاليف الرعاية الصحية باستخدام خوارزمية انحدار.

تم تزويدك بمجموعة بيانات تحتوي على معلومات عن أشخاص مختلفين بما في ذلك تكاليف الرعاية الصحية الخاصة بهم. استخدم البيانات للتنبؤ بتكاليف الرعاية الصحية بناءً على بيانات جديدة.

أول خليتين في هذا الدفتر تستوردان المكتبات والبيانات.

تأكد من تحويل البيانات التصنيفية إلى أرقام. استخدم 80% من البيانات كـ `train_dataset` و20% كـ `test_dataset`.

قم بـ `pop` لعمود "expenses" من هذه المجموعات لإنشاء مجموعات جديدة تسمى `train_labels` و `test_labels`. استخدم هذه التسميات عند تدريب النموذج.

أنشئ نموذجًا ودربه باستخدام `train_dataset`. شغّل الخلية النهائية في هذا الدفتر لفحص نموذجك. ستستخدم الخلية النهائية `test_dataset` غير المرئية لفحص مدى تعميم النموذج.

لنجاح التحدٍّ، يجب أن تُرجع `model.evaluate` متوسط الخطأ المطلق أقل من 3500. هذا يعني أنه يتنبأ بتكاليف الرعاية الصحية بدقة ضمن 3500 دولار.

ستتنبأ الخلية النهائية أيضًا بالتكاليف باستخدام `test_dataset` وترسم النتائج بيانيًا.

# --hints--

يجب أن يجتاز جميع اختبارات Python.

```js

```

# --solutions--

```py
  # Python challenges don't need solutions,
  # because they would need to be tested against a full working project.
  # Please check our contributing guidelines to learn more.
```
