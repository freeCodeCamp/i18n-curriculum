---
id: 672bd814105a0ffcf36f9233
title: 什么是字体族，它们如何工作？
challengeType: 19
dashedName: what-are-font-families
---

# --interactive--

字体族是一组共享通用设计的字体。 属于同一字体族的所有字体均基于同一核心字型，但它们的风格、粗细和宽度会有所变化。 你可以将它们视为具有相似特征但也有差异的同胞。 例如，Arial 是一个字体族，包括 Arial Bold 和 Arial Italic 等变体。

在 CSS 中，你可以使用 `font-family` 属性设置字体系。下面是将段落字体设置为 `Arial` 的示例：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="arial-font">Example paragraph using Arial font.</p>
<p>Paragraph not using Arial font.</p>
```

```css
#arial-font {
  font-family: Arial;
}
```

:::

但如果未找到该字体族怎么办？ 你可以通过逗号分隔，按优先级从高到低指定多个字体族。 这些备用字体将作为回退选项。 在此示例中，Arial 是首选字体，Lato 是备用字体：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="specified-font">Example paragraph using specified fonts.</p>
<p>Paragraph not using specified fonts.</p>
```

```css
#specified-font {
  font-family: Arial, Lato;
}
```

:::

如果设备上存在该字体，浏览器会优先渲染第一个字体（Arial）。 如果不存在，则会渲染第二个字体（Lato）。

你还需要了解这一过程背后的工作原理。 即使第一个字体可用，字体选择过程也不会停止。 字体族是逐个字符选择的，因此，如果某个字体缺少特定字符，浏览器会在优先级较低的字体中查找该字符。 很有趣，对吧？

在网页开发中，你还会遇到通用字体族。 它们非常重要。 通用字体族是当更高优先级的字体不可用时，网络浏览器将渲染的默认字体。 为确保内容仍可读，浏览器会根据指定的通用字体族，用能找到的最合适的字体替换原始字体。 一些最常用的通用字体族包括： 

- 衬线
- 无衬线字体
- 等宽字体
- 草书
- 奇幻

但还有更多选项可供选择。 在以下代码中，我们有三个备用字体族：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="specified-font">Example paragraph using specified fonts.</p>
<p>Paragraph not using specified fonts.</p>
```

```css
#specified-font {
  font-family: Arial, Lato, sans-serif;
}
```

:::

Arial 具有最高优先级。 如果未找到 Arial，浏览器将尝试渲染 Lato。 如果两者均未找到，浏览器将使用通用 sans-serif 字体族，从用户系统上安装的字体中选择一个具有这些特征的字体。

你应始终在字体族列表末尾包含一个通用字体族。 请记住，通用字体可能与你在设计中最初设想的字体外观不同，因此检查这些回退字体在不同浏览器上的显示效果总是很有帮助的。

为了尽可能保持用户体验一致，还建议使用网页安全字体。 这些字体族通常安装在大多数设备上，因此对于大多数用户而言，它们很可能会被找到并正确渲染。  You'll learn more about them in the next lessons.

字体族在网页设计领域至关重要。 通过使用 font-family 属性，你可以立即改变文本的外观和感觉。

# --questions--

## --text--

`font-family` CSS 属性的主要用途是什么？

## --answers--

设置文本颜色。

### --feedback--

想想文本的视觉外观。

---

控制文本字体大小。

### --feedback--

想想文本的视觉外观。

---

定义文本使用的字型。

---

调整文本行间距。

### --feedback--

想想文本的视觉外观。

## --video-solution--

3

## --text--

以下哪项是常见的通用字体族？

## --answers--

`Helvetica`

### --feedback--

想想广义的字体类别。

---

`Times New Roman`

### --feedback--

想想广义的字体类别。

---

`Sans-Serif`

---

`Arial`

### --feedback--

想想广义的字体类别。

## --video-solution--

3

## --text--

如何在 CSS 中指定多个备用字体族？

## --answers--

通过逗号分隔它们。

---

通过使用嵌套的 `font-family` 声明。

### --feedback--

思考如何在首选字体不可用时提供备用字体。

---

通过使用 `&` 字符。

### --feedback--

思考如何在首选字体不可用时提供备用字体。

---

通过使用 `|` 字符。

### --feedback--

思考如何在首选字体不可用时提供备用字体。

## --video-solution--

1
