---
id: 66f1ad049d7a6ac0886cc2ba
title: Cuestionario sobre Formularios de React, Obtención de Datos y Enrutamiento
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

Para aprobar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Cuál de los siguientes es un ejemplo de una entrada controlada?

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

¿Cuál de los siguientes es un ejemplo de una entrada no controlada?

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

¿Cuál de los siguientes NO es una forma común de obtener datos en React?

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

¿Cuál de los siguientes hooks está comúnmente asociado con la obtención de datos en React?

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

¿Qué es la memorización?

#### --distractors--

Un algoritmo comúnmente usado para encontrar el camino más corto entre dos vértices en un gráfico.

---

Una técnica especial usada para optimizar el rendimiento de todas las entradas controladas y no controladas.

---

Un algoritmo de dividir y conquistar usado para ordenar recursivamente sub-arreglos y fusionarlos juntos.

#### --answer--

Una técnica de optimización en la que el resultado de llamadas a funciones costosas se almacena (recuerda) basado en argumentos específicos.

### --question--

#### --text--

¿Cuál de los siguientes hooks se usa para memorizar valores computados?

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

¿Cuál de los siguientes usa el hook correcto para memorizar referencias de funciones?

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

¿Qué es una dependencia?

#### --distractors--

Una dependencia es una biblioteca de pruebas comúnmente usada en aplicaciones React.

---

Una dependencia es un tipo de componente React que solo se ejecuta en el servidor.

---

Una dependencia es un compilador especial usado en aplicaciones React para compilar el código React a JavaScript. 

#### --answer--

Una dependencia es cuando un componente o módulo en una aplicación depende de otro para funcionar correctamente.

### --question--

#### --text--

¿Qué es un gestor de paquetes?

#### --distractors--

Una herramienta usada para actualizar paquetes de terceros instalados en aplicaciones.

---

Una herramienta usada para gestionar paquetes creados únicamente dentro de un proyecto.

---

Una herramienta usada para mover dependencias fuera de la carpeta `node_modules` y hacia una carpeta de configuración especial.

#### --answer--

Una herramienta usada para la instalación, actualizaciones y eliminación de dependencias.

### --question--

#### --text--

¿Cuál de los siguientes NO es un gestor de paquetes comúnmente usado en el ecosistema de JavaScript?

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

¿Cuál de los siguientes es un archivo de configuración clave que contiene metadatos sobre su proyecto?

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

¿Cuál de los siguientes es un marco de React comúnmente usado?

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

¿Cuál de los siguientes es la forma correcta de agregar rutas a su aplicación React usando React Router?

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

¿Cuál de los siguientes es la forma correcta de crear una ruta dinámica en React Router?

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

¿Cuál de los siguientes hooks se usa para acceder a los parámetros dinámicos de una ruta URL?

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

¿Cuál de los siguientes es la manera correcta de crear un manejador de solicitudes personalizado en Next.js?

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

¿Qué es prop drilling?

#### --distractors--

El proceso de crear un reductor que especifica cómo debe cambiar el estado en respuesta a propiedades particulares usadas.

---

El proceso de pasar propiedades de un componente hijo hasta el componente padre. 

---

El proceso de crear un proveedor de contexto para una aplicación React y usar eso para pasar propiedades de componentes padre a hijo.

#### --answer--

El proceso de pasar propiedades de un componente padre a componentes hijos profundamente anidados, incluso cuando algunos de los componentes hijos no necesitan las propiedades.

### --question--

#### --text--

¿Cuál de las siguientes NO es una solución de gestión de estado comúnmente utilizada para aplicaciones React?

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

¿Para qué se utilizan las Herramientas del Desarrollador React?

#### --distractors--

Estas herramientas se usan solo para verificar problemas de accesibilidad en aplicaciones React.

---

Estas herramientas se usan para agregar pruebas adicionales para componentes React.

---

Estas herramientas se usan para memorizar todos los componentes React dentro de la aplicación.

#### --answer--

Estas herramientas se usan para inspeccionar componentes React e identificar problemas de rendimiento.

### --question--

#### --text--

¿Qué son los Componentes del Servidor de React?

#### --distractors--

Estos son componentes que gestionan el contexto exclusivamente en el servidor.

---

Estos son componentes memorizados que se renderizan exclusivamente en el servidor.

---

Estos son componentes que gestionan el estado exclusivamente en el servidor.

#### --answer--

Estos son componentes que se renderizan exclusivamente en el servidor.

