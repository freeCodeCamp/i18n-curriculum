---
id: 66edd3f9bef926f129990425
title: 局部的存储和 CRUD 测验
challengeType: 8
dashedName: quiz-local-storage-and-crud
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

在网页开发的上下文中，CRUD 代表什么？

#### --distractors--

创建、替换、撤销、删除。

---

连接、移除、更新、显示。

---

拷贝、检索、更新、删除。

#### --answer--

创建、读取、更新、删除。

### --question--

#### --text--

在 RESTful API 中，哪个超文本传输协议方法通常与创建操作关联？

#### --distractors--

`GET`

---

`PUT`

---

`DELETE`

#### --answer--

`POST`

### --question--

#### --text--

在修改数据库中现有记录时，通常执行以下哪种 CRUD 操作？

#### --distractors--

读取

---

删除

---

创建

#### --answer--

更新

### --question--

#### --text--

在 Web Storage API 中，`localStorage` 和 `sessionStorage` 的主要区别是什么？

#### --distractors--

`localStorage` 将数据保存在内存中，而 `sessionStorage` 将数据保存在盘上。

---

`sessionStorage` 在检索数据时比 `localStorage` 更快。

---

`localStorage` 数据仅在一个会话期间可用，而 `sessionStorage` 跨会话持久存在。

#### --answer--

localStorage 数据会一直保留，直到被显式删除，而 sessionStorage 数据会在页面会话结束时被清除。

### --question--

#### --text--

哪个方法用于将数据保存到 `localStorage`？

#### --distractors--

`localStorage.setData()`

---

`localStorage.saveItem()`

---

`localStorage.storeData()`

#### --answer--

`localStorage.setItem()`

### --question--

#### --text--

你如何从 `sessionStorage` 中移除单个项？

#### --distractors--

`sessionStorage.clearItem('key')`

---

`sessionStorage.remove('key')`

---

`sessionStorage.deleteItem('key')`

#### --answer--

`sessionStorage.removeItem('key')`

### --question--

#### --text--

`PUT` 和 `PATCH` 有什么区别？

#### --distractors--

`PUT` 修改特定字段，而 `PATCH` 替换整个资源。

---

`PUT` 仅添加新资源，而 `PATCH` 更新现有资源。

---

`PUT` 用于检索数据，而 `PATCH` 用于发送数据。

#### --answer--

`PUT` 替换整个资源，而 `PATCH` 更新特定字段。

### --question--

#### --text--

缓存存储用于什么？

#### --distractors--

缓存存储用于在浏览器中安全地永久保存用户凭据以进行认证。

---

缓存存储用于通过数据库系统在浏览器中保存大量结构化数据。

---

缓存存储用于通过存储可跨多个网站访问的键值点对来跟踪用户会话和偏好。

#### --answer--

缓存存储用于保存频繁请求的网络资源，减少装载时间并提升性能。

### --question--

#### --text--

在网页开发的上下文中，什么是 cookie？

#### --distractors--

一种超文本传输协议请求的类型。

---

一段在浏览器中运行的代码。

---

一种在服务器上保存大文件的方法。

#### --answer--

用户的网页浏览器保存的一小段数据。

### --question--

#### --text--

以下哪个方法用于在 JavaScript 中手动设置 cookie？

#### --distractors--

`document.setCookie`

---

`document.cookie.set`

---

`document.createCookie`

#### --answer--

`document.cookie`

### --question--

#### --text--

以下哪个代码截图能正确删除名为 `username` 的 cookie？

#### --distractors--

`document.cookie.delete("username");`

---

`document.removeCookie("username");`

---

`cookie.delete("username");`

#### --answer--

`document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";`

### --question--

#### --text--

以下关于 cookies 的哪条语句是错误的？

#### --distractors--

会话 cookie 在浏览器关闭时过期。

---

HttpOnly cookie 无法被 JavaScript 访问。

---

安全的 cookie 仅通过 `HTTPS` 传输。

#### --answer--

Cookies 只能由服务器设置。

### --question--

#### --text--

什么是浏览器指纹识别？

#### --distractors--

一种基于用户 `IP address` 进行识别的技术。

---

一种数据加密的形式。

---

一种将数据存储在浏览器缓存中的方法。

#### --answer--

一种通过浏览器和设备属性跟踪用户的方法。

### --question--

#### --text--

将密码存储在局部存储中关联的主要风险是什么？

#### --distractors--

局部的存储数据在你关闭浏览器标签（页）后被删除。

---

一旦你删除 cookies，局部的存储数据就会丢失。

---

局部存储在离线时无法使用。

#### --answer--

局部存储未加密且可以轻松访问。

### --question--

#### --text--

以下关于客户端存储的语句中，哪一条是正确的？

#### --distractors--

客户端保存使用对象而不是字串来保存数据。

---

客户端存储使用云服务在多个设备之间同步用户数据。

---

客户端存储比服务器端存储更安全。

#### --answer--

客户端保存允许网站将数据存储在用户设备上。

### --question--

#### --text--

使用 IndexedDB 相对于 localStorage 的主要好处是什么？

#### --distractors--

IndexedDB 不需要用户同意即可存储数据。

---

IndexedDB 有更简单的 API。

---

所有网页浏览器都支持 IndexedDB。

#### --answer--

`IndexedDB` 可以处理更大量的 `data` 和更复杂的查询。

### --question--

#### --text--

以下哪种方式是从 IndexedDB 中的对象保存中检索数据的有效方法？

#### --distractors--

在对象保存上调用 `retrieve()`。

---

使用 `query()` 来访问特定记录。

---

使用 `fetch()` 方法。

#### --answer--

在 store 引用上使用 `get()`。

### --question--

#### --text--

IndexedDB 支持哪种类型的 `transaction`？

#### --distractors--

仅读取事务。

---

仅限异步的事务。

---

仅限同步的事务。

#### --answer--

读取和写入事务。

### --question--

#### --text--

以下关于 Service Worker 的生命周期，哪项是正确的？

#### --distractors--

Service Workers 在网页应用的主线程中运行。

---

Service Workers 可以在安装之前被激活。

---

Service Workers 只能在浏览器中打开网页应用时运行。

#### --answer--

Service Worker 在被激活后可以拦截网络请求。

### --question--

#### --text--

哪个语句最能描述渐进式网页应用（PWAs）的一个关键特色？

#### --distractors--

PWA 只能安装在移动设备上。

---

PWA 需要有一个后端服务器。

---

PWA 只能通过特定浏览器访问。

#### --answer--

PWA 通过 Service Workers 提供离线功能性。

