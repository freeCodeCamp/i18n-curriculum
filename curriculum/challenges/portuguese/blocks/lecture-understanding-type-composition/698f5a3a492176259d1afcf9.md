---
id: 698f5a3a492176259d1afcf9
title: O que são Interfaces e Como Elas Funcionam?
challengeType: 19
dashedName: what-are-interfaces-and-how-do-they-work
---

# --description--

Em uma lição anterior, você aprendeu como trabalhar com aliases de tipo e viu exemplos de como nomear tipos de objeto. 

Como você se lembra, uma maneira de nomear um tipo de objeto seria usar a palavra-chave `type` assim:

```ts
type ID = number | string;

type User = {
  id: ID;
  name: string;
};
```

Outra forma de nomear um tipo de objeto seria usar uma declaração de `interface` assim:

```ts
type ID = number | string;

interface User {
  id: ID;
  name: string;
}
```

`type` e `interface` são semelhantes mas uma diferença chave é que interfaces usam a palavra-chave `extends` para construir sobre outras interfaces, enquanto aliases de `type` usam tipos de interseção (`&`) para combinar tipos.

Vamos ver alguns exemplos:

```ts
interface User {
  id: number | string;
  name: string;
}

interface Admin extends User {
  role: string;
}

const adminUser: Admin = {
  id: 101,
  name: "Alice",
  role: "superadmin"
};
```

Neste exemplo, `User` começa com `id` e `name`. A interface `Admin` estende `User`, significando que inclui `id` e `name` e adiciona uma nova propriedade chamada `role`.

Se você quiser estender um `type`, você usaria um tipo de interseção (`&`) assim:

```ts
type User = {
  id: number | string;
  name: string;
};

type Admin = User & {
  role: string;
};

const adminUser: Admin = {
  id: 101,
  name: "Alice",
  role: "superadmin"
};
```

`Admin` combina o tipo `User` com um novo tipo de objeto que inclui `role`. O resultado tem a mesma forma do exemplo da interface que é um objeto com `id`, `name` e `role`.

Tanto `type` quanto `interface` podem descrever a forma de um objeto. Se você não tem certeza de qual usar, lembre-se que `interface` é especificamente projetado para formas de objetos, enquanto `type` é mais flexível e também pode representar tipos união, primitivos e combinações de tipos mais complexas.

# --questions--

## --text--

Qual recurso do TypeScript permite criar um novo tipo combinando vários tipos existentes usando `&`?

## --answers--

`interface`

### --feedback--

Consulte novamente o meio da lição onde os tipos de interseção foram discutidos.

---

`type`

---

`class`

### --feedback--

Consulte novamente o meio da lição onde os tipos de interseção foram discutidos.

---

`enum`

### --feedback--

Consulte novamente o meio da lição onde os tipos de interseção foram discutidos.

## --video-solution--

2

## --text--

Qual das seguintes é a maneira correta de criar um `interface`?

## --answers--

```ts
type ID = number | string;

interface User {
  id: ID;
  name: string;
}
```

---

```ts
type ID = number | string;

interface User == {
  id: ID;
  name: string;
}
```

### --feedback--

Consulte o início da lição.

---

```ts
type ID = number | string;

interface > User {
  id: ID;
  name: string;
}
```

### --feedback--

Consulte o início da lição.

---

```ts
type ID = number | string;

interface User >> {
  id: ID;
  name: string;
}
```

### --feedback--

Consulte o início da lição.

## --video-solution--

1

## --text--

Qual das seguintes afirmações é verdadeira sobre aliases de tipo em TypeScript?

## --answers--

Eles podem representar uniões e interseções.

---

Eles só podem descrever formas de objeto.

### --feedback--

Consulte o final da lição.

---

Eles devem usar `extends` para construir sobre outros tipos.

### --feedback--

Consulte o final da lição.

---

Eles não podem ser atribuídos a variáveis.

### --feedback--

Consulte o final da lição.

## --video-solution--

1
