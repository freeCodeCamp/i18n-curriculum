---
id: 672acc100d59d24da7b4e09c
title: Які існують різні типи CSS-комбінаторів?
challengeType: 19
dashedName: what-are-the-different-types-of-css-combinators
---

# --interactive--

CSS-комбінатори використовуються для визначення зв’язку між селекторами в CSS. Вони допомагають вибирати елементи на основі їхнього зв’язку з іншими елементами, що дозволяє створювати більш точне та ефективне стилізування.

Ми розглянемо деякі основні CSS-комбінатори та їхні випадки використання, починаючи з комбінатора нащадка.

Комбінатор нащадка використовується для вибору елементів, які відповідають другому селектору, якщо вони вкладені всередині батьківського елемента, що відповідає першому селектору. Батьківський елемент може бути безпосереднім батьком або батьком батька.

Щоб краще зрозуміти, як це працює, давайте розглянемо приклад.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<figure>
  <img src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/relaxing-cat.jpg" alt="Relaxing Cat">
  <figcaption>A relaxing cat with a border</figcaption>
</figure>
```

```css
figure img {
  border: 4px solid red;
}
```

:::

У наведеному прикладі ми використовуємо комбінатор нащадка, щоб вибрати всі елементи зображень всередині `figure` елементів і застосувати `solid red` кордон з усіх чотирьох сторін.

Зверніть увагу, що між батьківським і дочірнім селекторами використовується `space`.

У цьому випадку `figure` буде батьківським елементом, а `img` — дочірнім.

Якщо у вас було б кілька зображень всередині `figure` елемента, використання комбінатора нащадка було б хорошим способом додати суцільний чорний кордон навколо кожного з цих зображень.

Іншим типом комбінатора є комбінатор дочірнього елемента.

Комбінатор дочірнього елемента (`>`) у CSS використовується для вибору елементів, які є безпосередніми дітьми вказаного батьківського елемента.

Цей комбінатор націлений лише на елементи з певним батьком, роблячи ваші CSS-правила точнішими та запобігаючи небажаному стилізуванню глибше вкладених елементів.

Давайте розглянемо наступний приклад HTML:

:::interactive_editor

```html
<div class="container">
  <p>First</p>
  <div>
    <p>Second</p>
  </div>
  <div>
    <p>Third</p>
  </div>
</div>
```

:::

У наведеній HTML-структурі клас `container` застосований до `div` елемента.

Всередині цього контейнера є безпосередній дочірній `p` елемент ("First"), за яким слідують два додаткові `div` елементи, кожен з яких містить `p` елемент ("Second" і "Third").

Перший `p` елемент є безпосереднім дочірнім елементом `.container`, тоді як інші два `p` елементи вкладені всередину інших `div` елементів, що робить їх глибшими нащадками.

Щоб застосувати стилі лише до безпосереднього дочірнього елемента класу `container`, ви можете використати комбінатор дочірнього елемента так:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <p>First</p>
  <div>
    <p>Second</p>
  </div>
  <div>
    <p>Third</p>
  </div>
</div>
```

```css
.container > p {
  color: blue;
}
```

:::

У наведеному прикладі ми націлені лише на безпосереднього дочірнього елемента класу `container`. Це надасть цьому дочірньому елементу текстовий колір `blue`.

Оскільки інші два абзаци вкладені всередину `div` елементів, вони не вважаються безпосередніми дітьми класу `container` і не отримають синій колір тексту.

Ще одним поширеним комбінатором є комбінатор наступного братського елемента.

Комбінатор наступного братського елемента (`+`) у CSS вибирає елемент, який безпосередньо слідує за вказаним братським елементом. Цей комбінатор корисний, коли потрібно застосувати стилі до елемента, що йде безпосередньо після іншого, дозволяючи цілеспрямоване стилізування залежно від позиції елемента відносно його братів.

Розглянемо наступний приклад HTML:

:::interactive_editor

```html
<figure>
  <img
    src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/relaxing-cat.jpg"
    alt="A cute orange cat lying on its back."
  />
  <figcaption>A cute orange cat lying on its back.</figcaption>
</figure>
```

:::

Тут ми маємо `figure` елемент, який містить `img` елемент, за яким слідує `figcaption` елемент. `figcaption` є безпосереднім братським елементом `img`.

Якщо ви хочете додати чорний кордон навколо `figcaption` елемента, ви можете використати комбінатор наступного братського елемента так:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<figure>
  <img
    src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/relaxing-cat.jpg"
    alt="A cute orange cat lying on its back."
  />
  <figcaption>A cute orange cat lying on its back.</figcaption>
</figure>
```

```css
img + figcaption {
  border: 4px solid black;
}
```

:::

У цьому прикладі комбінатор наступного братського елемента (`+`) вибирає `figcaption` елемент, який йде безпосередньо після `img` елемента. Застосоване CSS-правило додає `4px solid black border` навколо `figcaption`.

Ще одним поширеним комбінатором є комбінатор наступних братських елементів.

Комбінатор наступних братських елементів (`~`) у CSS вибирає всі братські елементи вказаного елемента, які йдуть після нього.

На відміну від комбінатора наступного братського елемента, який націлений лише на безпосереднього наступного брата, комбінатор наступних братських елементів (`~`) може вибирати будь-яких братів, що йдуть після вказаного елемента, забезпечуючи більшу гнучкість у стилізуванні.

Розглянемо наступний приклад HTML:

:::interactive_editor

```html
<div class="container">
  <h2>Subheading</h2>
  <p>First paragraph.</p>
  <p>Second paragraph.</p>
  <p>Third paragraph.</p>
  <p>Another paragraph element</p>
</div>
```

:::

У цій HTML-структурі ми маємо `h2` елемент, за яким слідують чотири абзаци. Абзаци є братськими елементами `h2`.

Якщо ви хочете стилізувати всі абзаци, що йдуть після `h2` елемента, ви можете використати комбінатор наступних братських елементів так:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <h2>Subheading</h2>
  <p>First paragraph.</p>
  <p>Second paragraph.</p>
  <p>Third paragraph.</p>
  <p>Another paragraph element</p>
</div>
```

```css
h2 ~ p {
  color: green;
}
```

:::

У цьому прикладі всі абзаци, що йдуть після `h2` елемента, отримають зелений колір тексту.

Комбінатор наступних братських елементів (`~`) націлений на всі братські абзаци, які з’являються після `h2` елемента, незалежно від того, чи є вони безпосередніми братами.

На завершення, розуміння та використання різних CSS-комбінаторів дозволяє застосовувати точні стилі до ваших HTML-елементів, підвищуючи контроль і гнучкість дизайну.

Опанувавши ці селектори, ви зможете створювати більш складні та цілеспрямовані правила стилізування, що покращують як вигляд, так і функціональність ваших вебсторінок.

# --questions--

## --text--

Яке CSS-правило стилізує всі `span` елементи, що знаходяться всередині `div` елементів, незалежно від глибини їхнього вкладення?

## --answers--

```css
div > span {
  color: red;
}
```

### --feedback--

Подумайте про комбінатор, який застосовується до елементів на будь-якому рівні вкладення всередині вказаного батька.

---

```css
div + span {
  color: red;
}
```

### --feedback--

Подумайте про комбінатор, який застосовується до елементів на будь-якому рівні вкладення всередині вказаного батька.

---

```css
div ~ span {
  color: red;
}
```

### --feedback--

Подумайте про комбінатор, який застосовується до елементів на будь-якому рівні вкладення всередині вказаного батька.

---

```css
div span {
  color: red;
}
```

## --video-solution--

4

## --text--

Яке CSS-правило стилізує лише безпосередні дочірні `li` елементи `ul` елементів?

## --answers--

```css
ul > li {
  font-weight: bold;
}
```

---

```css
ul li {
  font-weight: bold;
}
```

### --feedback--

Подумайте про комбінатор, який націлений на елементи, що є безпосередніми дітьми вказаного батька.

---

```css
ul + li {
  font-weight: bold;
}
```

### --feedback--

Подумайте про комбінатор, який націлений на елементи, що є безпосередніми дітьми вказаного батька.

---

```css
ul ~ li {
  font-weight: bold;
}
```

### --feedback--

Подумайте про комбінатор, який націлений на елементи, що є безпосередніми дітьми вказаного батька.

## --video-solution--

1

## --text--

Яке CSS-правило стилізує перший `p` елемент, що йде безпосередньо після будь-якого `h1` елемента?

## --answers--

```css
h1 > p {
  margin-top: 0;
}
```

### --feedback--

Подумайте про комбінатор, який застосовується до елемента, що йде безпосередньо після вказаного братського елемента.

---

```css
h1 + p {
  margin-top: 0;
}
```

---

```css
h1 ~ p {
  margin-top: 0;
}
```

### --feedback--

Подумайте про комбінатор, який застосовується до елемента, що йде безпосередньо після вказаного братського елемента.

---

```css
h1 p {
  margin-top: 0;
}
```

### --feedback--

Подумайте про комбінатор, який застосовується до елемента, що йде безпосередньо після вказаного братського елемента.

## --video-solution--

2
