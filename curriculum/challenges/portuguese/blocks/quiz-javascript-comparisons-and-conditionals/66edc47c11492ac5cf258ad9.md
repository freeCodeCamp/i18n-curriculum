---
id: 66edc47c11492ac5cf258ad9
title: Questionário sobre condicionais e comparações em JavaScript
challengeType: 8
dashedName: quiz-javascript-comparisons-and-conditionals
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 9 das 10 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual é o resultado da expressão `undefined > 0`?

#### --distractors--

`true`

---

`undefined`

---

`null`

#### --answer--

`false`

### --question--

#### --text--

Qual operação lógica `||` representa?

#### --distractors--

XOR

---

E

---

NÃO

#### --answer--

OU

### --question--

#### --text--

Qual é a saída do seguinte código?

```js
console.log(5 === 2 + 3 || 4 == 2);
```

#### --distractors--

`undefined`

---

`false`

---

Um erro é gerado.

#### --answer--

`true`

### --question--

#### --text--

O que é um valor truthy e falsy?

#### --distractors--

Um valor que é tanto `true` e `false`.

---

Um valor que muda dependendo do contexto.

---

Um valor que funciona de forma diferente do valor `true` ou `false`.

#### --answer--

Um valor que é considerado `true` ou `false` quando encontrado em um contexto Booleano.

### --question--

#### --text--

O que é registrado no console a partir do código abaixo?

```js
if (1) {
  console.log("True!");
}
```

#### --distractors--

`false`

---

Nada é impresso no console.

---

Um erro é gerado.

#### --answer--

`True!`

### --question--

#### --text--

Qual é a diferença entre `undefined` e `null`?

#### --distractors--

`null` e `undefined` apontam para uma localização de memória fora do intervalo e são inacessíveis.

---

`null` é o valor implícito atribuído a variáveis e não pode ser alterado, enquanto `undefined` é o valor explícito atribuído a variáveis.

---

`null` é uma propriedade global, `undefined` não é.

#### --answer--

Variáveis sem valor são `undefined`, enquanto `null` representa uma ausência intencional de valor de objeto.

### --question--

#### --text--

O que acontece quando você não inclui `break` ao implementar uma instrução `switch`?

#### --distractors--

A instrução `switch` irá parar abruptamente.

---

A declaração `switch` lançará um erro após a declaração anterior.

---

A instrução `switch` sairá após a primeira correspondência.

#### --answer--

O código continua a avaliar as seguintes declarações `case`, mesmo após encontrar uma correspondência.

### --question--

#### --text--

O que é impresso no console a partir do código abaixo?

```js
const a = 2;
if (1 == "1") {
  let b = 3;
  console.log(a + b);
}
console.log(b);
```

#### --distractors--

Um erro é gerado.

---

```js
5
3
```

---

```js
5
undefined
```

#### --answer--

`5` e então um erro é gerado.

### --question--

#### --text--

Qual será a saída do seguinte código JavaScript?

```js
let vehicle = "car";

switch (vehicle) {
  case "bike":
    console.log("Bikes are two-wheelers.");
    break;
  case "car":
    console.log("Some cars are 4x4.");
  case "truck":
    console.log("Trucks can carry heavy loads.");
    break;
  default:
    console.log("Unknown vehicle.");
}
```

#### --distractors--

```md
Some cars are 4x4.
```

---

```md
Some cars are 4x4.
Trucks can carry heavy loads.
Unknown vehicle.
```

---

```md
Unknown vehicle.
```

#### --answer--

```md
Some cars are 4x4.
Trucks can carry heavy loads.
```

### --question--

#### --text--

O que é impresso no console com o código abaixo?  

```js
let x = 5;
if (x > 1 && x < 10) {
  console.log("x is between 1 and 10");
} else {
  console.log("x is not between 1 and 10");
}
```

#### --distractors--

Um erro é gerado.

---

Nada é impresso.

---

`x is not between 1 and 10`

#### --answer--

`x is between 1 and 10`

