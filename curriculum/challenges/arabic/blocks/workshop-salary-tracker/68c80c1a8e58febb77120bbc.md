---
id: 68c80c1a8e58febb77120bbc
title: الخطوة 13
challengeType: 20
dashedName: step-13
---

# --description--

احذف آخر استدعاء لـ `print` من شفرتك.

# --hints--

يجب حذف `print(repr(charlie_brown))` من شفرتك.

```js
({ test: () => assert(runPython(`not _Node(_code).has_stmt("print(repr(charlie_brown))")`)) })
```

# --seed--

## --seed-contents--

```py
class Employee:
    def __init__(self, name, level):
        self._name = name
        self._level = level

    def __str__(self):
        return f'{self.name}: {self.level}'

    def __repr__(self):
        return f"Employee('{self.name}', '{self.level}')"

    @property
    def name(self):
        return self._name

    @property
    def level(self):
        return self._level

charlie_brown = Employee('Charlie Brown', 'trainee')
print(charlie_brown)
--fcc-editable-region--
print(repr(charlie_brown))
--fcc-editable-region--

```
