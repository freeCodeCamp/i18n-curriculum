---
id: 66f1ae758b77cfc3e4da6151
title: Questionário sobre TypeScript
challengeType: 8
dashedName: quiz-typescript
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

O que é Typescript?

#### --distractors--

Um framework usado para programação de sistemas.

---

Uma ferramenta de gerenciamento de banco de dados.

---

Uma linguagem de programação usada para estilizar sites.

#### --answer--

Um superconjunto de JavaScript que adiciona tipagem estática.

### --question--

#### --text--

Por que o TypeScript deve ser usado em um projeto?

#### --distractors--

Ele elimina a necessidade de testes em aplicações.

---

É mais rápido do que todas as outras linguagens de programação.

---

Isso facilita a estilização de sites.

#### --answer--

Ele melhora a manutenibilidade adicionando definições de tipo ao JavaScript.

### --question--

#### --text--

Para qual linguagem o TypeScript é compilado?

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

Qual seria a saída do seguinte trecho de código?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

Qual é o propósito principal de uma interface em TypeScript?

#### --distractors--

Para compilar código TypeScript em JavaScript.

---

Para criar um novo tipo de dado.

---

Para criar um novo JavaScript Object.

#### --answer--

Para definir a estrutura de um objeto.

### --question--

#### --text--

Qual dos seguintes NÃO é um primitivo básico em TypeScript?

#### --distractors--

string

---

boolean

---

número

#### --answer--

array

### --question--

#### --text--

Qual das seguintes opções define corretamente um tipo literal em TypeScript?

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

Qual é o propósito principal da palavra-chave `type` no TypeScript?

#### --distractors--

Para declarar variáveis.

---

Para criar classes.

---

Para implementar interfaces.

#### --answer--

Para definir tipos personalizados.

### --question--

#### --text--

Qual das seguintes opções define corretamente um tipo union?

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

Qual das seguintes capacidades é única para `interface` e NÃO PODE ser alcançada usando `type` em TypeScript?

#### --distractors--

Definindo formatos de objetos.

---

Criando tipos união.

---

Definindo tipos que incluem apenas valores específicos.

#### --answer--

Declarando implementações de classe.

### --question--

#### --text--

Qual é o propósito principal de usar generics em TypeScript?

#### --distractors--

Para implementar herança entre classes.

---

Para definir a estrutura de um objeto.

---

Para criar um novo tipo de dado.

#### --answer--

Para escrever código reutilizável que possa funcionar com múltiplos tipos.

### --question--

#### --text--

Qual dos seguintes é um **exemplo adequado** de uma função genérica em TypeScript?

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

O que é type narrowing em TypeScript?

#### --distractors--

Reduzindo o tamanho do JavaScript compilado.

---

Limitando o número de tipos em uma união.

---

Removendo tipos não utilizados de um projeto TypeScript.

#### --answer--

Para escrever código que restrinja um tipo união para um tipo mais específico.

### --question--

#### --text--

Qual arquivo é tipicamente usado para configurar as opções do compilador TypeScript?

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

O que a opção `rootDir` no arquivo `tsconfig.json` especifica?

#### --distractors--

A versão do TypeScript a ser usada durante a compilação.

---

O ambiente de runtime alvo (por exemplo, Node.js ou browser) para o código TypeScript.

---

O sistema de módulo específico a ser usado para `imports` e `exports`.

#### --answer--

O diretório que contém seus arquivos fonte.

### --question--

#### --text--

Qual exemplo usa `instanceof` corretamente para restringir o tipo e acessar a propriedade `value` com segurança?

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
O que a configuração `"strict": true` em `tsconfig.json` faz no TypeScript?

#### --distractors--

Habilita a verificação estática de tipos para que você possa detectar os erros antes de executar seu código.

---

Exige que você sempre use a sintaxe ES6.

---

Exige que você compile o código antes de executá-lo.

#### --answer--

Habilita um conjunto de opções de verificação de tipo, como exigir o tratamento de `null` ou `undefined` e impedir `any` implícito.

### --question--

#### --text--

Qual é o propósito do tipo `never` no TypeScript?

#### --distractors--

Para representar null ou undefined.

---

Para representar qualquer valor possível.

---

Para representar um tipo que pode ser qualquer coisa exceto null.

#### --answer--

Para representar o tipo de valores que nunca ocorrem.

### --question--

#### --text--

Qual das seguintes chamadas da função `getRandomValue` será compilada com sucesso em TypeScript?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

Como você pode corrigir o erro do compilador TypeScript `'email' is possibly 'null'` no código a seguir usando encadeamento opcional?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`

