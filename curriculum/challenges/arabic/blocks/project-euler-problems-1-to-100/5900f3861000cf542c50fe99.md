---
id: 5900f3861000cf542c50fe99
title: "المشكلة 26: الدورات العكسية"
challengeType: 1
forumTopicId: 301908
dashedName: problem-26-reciprocal-cycles
---

# --description--

الكسر الواحد يحتوي على 1 في البسط. يُعطى التمثيل العشري للكسور الواحدة ذات المقامات من 2 إلى 10:

<div style='padding-left: 4em; display: inline-grid; grid-template-rows: auto; row-gap: 7px;'><div><sup>1</sup>/<sub>2</sub> = 0.5</div><div><sup>1</sup>/<sub>3</sub> = 0.(3)</div><div><sup>1</sup>/<sub>4</sub> = 0.25</div><div><sup>1</sup>/<sub>5</sub> = 0.2</div><div><sup>1</sup>/<sub>6</sub> = 0.1(6)</div><div><sup>1</sup>/<sub>7</sub> = 0.(142857)</div><div><sup>1</sup>/<sub>8</sub> = 0.125</div><div><sup>1</sup>/<sub>9</sub> = 0.(1)</div><div><sup>1</sup>/<sub>10</sub> = 0.1</div></div>

حيث 0.1(6) تعني 0.166666...، ولها دورة متكررة مكونة من رقم واحد. يمكن ملاحظة أن <sup>1</sup>/<sub>7</sub> لها دورة متكررة مكونة من 6 أرقام.

ابحث عن قيمة `d` التي هي أقل من `n` بحيث يحتوي <sup>1</sup>/<sub>d</sub> على أطول دورة متكررة في جزء الكسر العشري.

# --hints--

يجب أن تُرجع `reciprocalCycles(700)` رقمًا.

```js
assert(typeof reciprocalCycles(700) === 'number');
```

يجب أن تُرجع `reciprocalCycles(700)` القيمة 659.

```js
assert(reciprocalCycles(700) == 659);
```

يجب أن تُرجع `reciprocalCycles(800)` القيمة 743.

```js
assert(reciprocalCycles(800) == 743);
```

يجب أن تُرجع `reciprocalCycles(900)` القيمة 887.

```js
assert(reciprocalCycles(900) == 887);
```

يجب أن تُرجع `reciprocalCycles(1000)` القيمة 983.

```js
assert(reciprocalCycles(1000) == 983);
```

# --seed--

## --seed-contents--

```js
function reciprocalCycles(n) {

  return n;
}

reciprocalCycles(1000);
```

# --solutions--

```js
// solution required
```
