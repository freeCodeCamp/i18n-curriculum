---
id: 69a98755ee5508d58e91dccc
title: NPM 測驗
challengeType: 8
dashedName: quiz-npm
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

npm 為開發者提供什麼？

#### --distractors--

一個精選的平台，只提供像是字型和圖示等設計資產。

---

一個自動將你的專案部署到雲端的託管服務。

---

一個管理你應用程式使用者記錄的資料庫解決方案。

#### --answer--

一個預建軟體套件的登錄檔，解決常見問題。

### --question--

#### --text--

以下哪一項不是 npm 取用的三個零件之一？

#### --distractors--

網站 npmjs.com。

---

儲存函式庫的軟體包登錄檔。

---

用於安裝和管理軟體包的命令列工具。

#### --answer--

Node.js 執行期。

### --question--

#### --text--

`npm install` 指令的作用是什麼？

#### --distractors--

它從一個模板搭建一個全新的 Node.js 專案。

---

它會在區域的開發伺服端上執行你的 Node.js 應用程式。

---

它會移除你專案中不再需要的軟體包。

#### --answer--

它為你的專案添加了用於常見任務的預建軟體包。

### --question--

#### --text--

哪個檔案儲存 Node.js 專案的詮釋資料，例如專案名稱、版本、作者和相依性？

#### --distractors--

`README.md`。

---

`node_modules/package.json`。

---

`package-lock.json`。

#### --answer--

`package.json`。

### --question--

#### --text--

你如何互動式地創建一個 `package.json` 檔案？

#### --distractors--

執行 `npm install` 時不指定軟體包名稱。

---

從 npm 登錄檔首頁下載模板。

---

從另一個專案複製檔案並重新命名。

#### --answer--

執行 `npm init` 指令。

### --question--

#### --text--

如何在不回答提示的情況下，使用預設值創建 `package.json` 檔案？

#### --distractors--

執行 `npm init --interactive`。

---

創建一個名為 `package.json` 的空白檔案並保持其為空。

---

使用 `npm init` 並在開啟編輯器後刪除所有提示。

#### --answer--

執行 `npm init -y`（或 `npm init --yes`）以自動接受所有預設值。

### --question--

#### --text--

npm 將只在開發時需要的軟體包（例如 linter 或測試執行器）列在哪裡？

#### --distractors--

在 `"dependencies"` 區段內。

---

在訂製指令下的 `"scripts"` 區段內。

---

在 `node_modules` 資料夾本身內。

#### --answer--

在 `package.json` 的 `"devDependencies"` 區段內。

### --question--

#### --text--

哪個指令會將 `nodemon` 安裝為僅用於開發的相依性？

#### --distractors--

`npm uninstall nodemon`。

---

`npm save nodemon --dev`。

---

`npm install nodemon`。

#### --answer--

`npm install nodemon -D`。

### --question--

#### --text--

安裝相依性後，哪個資料夾和檔案會被添加到你的專案根目錄？

#### --distractors--

`public` 資料夾和 `index.html`。

---

`dist` 資料夾和 `webpack.config.js`。

---

`src` 資料夾和 `README.md`。

#### --answer--

`node_modules` 資料夾和 `package-lock.json` 檔案。

### --question--

#### --text--

`package-lock.json` 如何幫助保持建置的一致性？

#### --distractors--

在每次安裝之前刪除未使用的依賴項。

---

只儲存軟體包的主要版本號。

---

透過自動將軟體包升級到最新版本。

#### --answer--

透過凍結安裝的每個相依性的精確版本。

### --question--

#### --text--

為什麼你應該將 `package-lock.json` 提交到你的 Git 儲存庫？

#### --distractors--

讓 npm 在開發者的機器上執行得更快。

---

自動更新過期的相依性。

---

為了減少儲存庫大小。

#### --answer--

因此整個團隊安裝相同版本的相依性和設定。

### --question--

#### --text--

你如何安裝特定版本的軟體包，例如 `express@4.21.2`？

#### --distractors--

`npm update express@4.21.2`。

---

`npm get express@4.21.2`。

---

`npm use express@4.21.2`。

#### --answer--

`npm install express@4.21.2`。

### --question--

#### --text--

指定相依性版本時，插入符號 (`^`) 允許什麼？

#### --distractors--

它將相依性鎖定到精確的修補版本。

---

它只允許更新主要版本。

---

它排除所有低於指定版本的版本。

#### --answer--

它允許較新的次要版本和修補版本，但阻擋新的主要版本。

### --question--

#### --text--

在像 `~1.2.3` 這樣的版本範圍中，波浪號（`~`）符號代表什麼意思？

#### --distractors--

它允許更新主要版本和次要版本。

---

它會安裝最新的主要版本，無論 `range` 為何。

---

它防止 npm 安裝任何更新。

#### --answer--

它只允許更新修補版本，同時保持次版本不變。

### --question--

#### --text--

什麼樣的變更會觸發語意版本控制中的主要版本升級？

#### --distractors--

對註解和格式進行細微的風格調整。

---

不改變行為的小錯誤修正。

---

不會破壞現有程式碼的選用特性。

#### --answer--

可能導致現有程式碼停止運作的重大變更。

### --question--

#### --text--

哪個指令會列出有較新發佈版本的相依性？

#### --distractors--

`npm check`。

---

`npm list`。

---

`npm refresh`。

#### --answer--

`npm outdated`。

### --question--

#### --text--

Yarn、PNPM 和 Bun 被提及為 `npm` 的替代方案。它們是什麼類型的工具？

#### --distractors--

你發佈或下載 npm 軟體包的登錄網站。

---

儲存 npm 軟體包使用者資料的資料庫服務。

---

自動部署 npm 專案的雲端託管平台。

#### --answer--

替代軟體包管理工具。

### --question--

#### --text--

訂製的 npm 腳本在哪裡於專案中宣告？

#### --distractors--

在 `.npmrc` 檔案內。

---

在 `package.json` 的 `"dependencies"` 區段內。

---

在根目錄的獨立 `scripts.json` 檔案中。

#### --answer--

在 `package.json` 的 `"scripts"` 物件內。

### --question--

#### --text--

你如何執行名為 `build` 的訂製 npm 腳本？

#### --distractors--

`build npm`

---

`node build`。

---

`run build`

#### --answer--

`npm run build`。

### --question--

#### --text--

當執行 `npm run start -- 8000` 時，雙破折號 (`--`) 有什麼作用？

#### --distractors--

它會靜音腳本輸出。

---

它強制 npm 平行執行該腳本。

---

它在執行腳本之前請求管理員權限。

#### --answer--

它告訴 npm 將 `--` 之後的所有內容作為引數轉發給底層指令。
