---
id: 6882902bc844b1723da7eb13
title: 如何安装和设置 Git？
challengeType: 19
dashedName: how-do-you-install-and-set-up-git
---

# --description--

在你真正开始使用版本控制和像 GitHub 这样的网站之前，你需要先安装并准备好使用 Git。

要查看 Git 是否已安装在你的机器上，你可以在终端中运行以下 `Command`：

```sh
git --version
```

如果你看到版本号，说明已安装 Git。如果没有，则需要安装它。

对于 Linux 系统，Git 通常随大多数发行版预装。如果你没有预装 Git，你应该能够使用你的包管理器命令安装它，例如 `sudo apt-get install git` 或 `sudo pacman -S git`。

对于 Mac 用户，你可以通过 Homebrew 使用 `brew install git` 来安装 Git，或者你可以从 Git 的网站下载可执行安装程序。

对于 Windows，你可以从 Git 的网站下载可执行安装程序。或者，如果你已经设置了 Chocolatey，可以在 PowerShell 中运行 `choco install git.install`。请注意，在 Windows 上，你可能还想下载 GitBash，以便拥有类似 Unix 的 shell 环境。

为了确保安装成功，请在终端中再次运行 `git --version` 命令。

安装好 Git 后，你需要对配置做一些更改。你只需在你的计算机上进行这些更改一次，升级时无需反复更改。

`git config` 用于设置负责 Git 在你的机器上如何运行的配置变量。要查看你当前的设置变量以及它们在系统中的存储位置，你可以运行以下命令：

```sh
git config --list --show-origin
```

现在，如果你是第一次安装 Git，你应该只看到系统级别的配置设置。如果你在本课之前已经安装过 Git，那么你可能会看到用户名称和电子邮件的用户配置。

要设置你的用户名，你可以运行以下命令：

```sh
git config --global user.name "Jane Doe"
```

这里使用 `--global` 标记来设置系统中所有使用 Git 的项目的用户名。如果你需要为特定项目覆写用户名，则可以在该特定项目目录中运行命令而不使用 `--global` 标记。

要设置用户电子邮件地址，你可以运行以下命令：

```sh
git config --global user.email janedoe@example.com
```

配置你的用户名和电子邮件很重要，因为 Git 会在你对一个项目进行的每次提交中使用这些信息。你将在后续课程中了解更多关于提交的内容。

你还可以设置的另一个配置是你希望 Git 使用的首选编辑器。下面是如何将你的首选编辑器设置为 Emacs 的示例：

```sh
git config --global core.editor emacs
```

其他选项包括 Vim、Nano、VS Code 等。例如，如果你使用 VS Code 作为你的编辑器，那么你可以这样配置 Git 设置：

```sh
git config --global core.editor "code --wait"
```

如果你选择不设置首选编辑器，那么 Git 将使用你系统的默认编辑器。

如果你使用的是 Windows 机器并且想要更改你的编辑器，你需要像这样提供可执行文件的完整路径：

```sh
git config --global core.editor "'C:/Program Files/Notepad++/notepad++.exe' -multiInst -notabbar -nosession -noPlugin"
```

要查看你的配置列表，你可以运行以下命令：

```sh
git config --list
```

现在你已经安装并设置了 Git，就可以开始为你的项目添加版本控制。

# --questions--

## --text--

以下 `Command` 会执行什么操作？

```sh
git config --list --show-origin
```

## --answers--

显示仅可用于 Git 的编辑器列表及其在你的系统上的保存位置。

### --feedback--

请回到讨论 Git `configuration` 的部分查找答案。

---

显示你 Git 配置中仅包含颜色选项的列表。

### --feedback--

请回到讨论 Git `configuration` 的部分查找答案。

---

显示你当前的设置变量及其在系统中的保存位置。

---

显示系统中所有使用 Git 的项目列表。

### --feedback--

请回到讨论 Git `configuration` 的部分查找答案。

## --video-solution--

3

## --text--

使用 `--global` 标记配置你的用户名的目的是什么？

## --answers--

`--global` 标记用于为系统中使用 Git 的某些项目设置用户名称。

### --feedback--

考虑这个标记如何影响所有使用 Git 的项目。

---

`--global` 标记用于为系统上所有使用 Git 的项目设置用户名称。

---

`--global` 标记用于设置系统中所有使用 Git 的项目都不使用的用户名。

### --feedback--

考虑这个标记如何影响所有使用 Git 的项目。

---

`--global` 标记用于仅为你系统中超过 5 年且使用 Git 的项目设置用户名称。

### --feedback--

考虑这个标记如何影响所有使用 Git 的项目。

## --video-solution--

2

## --text--

以下哪项不是设置你首选编辑器时 Git 中提到的有效选项？

## --answers--

Vim

### --feedback--

请参考脚本末尾设置 Git 配置和编辑器的部分。

---

Emacs

### --feedback--

请参考脚本末尾设置 Git 配置和编辑器的部分。

---

纳米

### --feedback--

请参考脚本末尾设置 Git 配置和编辑器的部分。

---

ESLint

## --video-solution--

4
