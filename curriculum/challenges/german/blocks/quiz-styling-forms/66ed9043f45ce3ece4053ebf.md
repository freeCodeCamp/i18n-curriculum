---
id: 66ed9043f45ce3ece4053ebf
title: Quiz zum Stylen von Formularen
challengeType: 8
dashedName: quiz-styling-forms
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 9 der 10 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist ein häufiges Problem beim Stylen des `datetime-local` Eingabetyp?

#### --distractors--

Es funktioniert nicht mit der `float` Eigenschaft.

---

Es ist unmöglich, es responsiv zu machen.

---

Es funktioniert nicht mit `rem` Einheiten.

#### --answer--

Das Eingabeformat ist in verschiedenen Browsern unterschiedlich.

### --question--

#### --text--

Welche der folgenden Optionen gilt als bewährte Vorgehensweise für das Stylen von Texteingaben?

#### --distractors--

Die Schriftgrößen von Texteingaben müssen mit `em` Einheiten gesetzt werden, und die Farben sollten ein helles Grau sein.

---

Die Größen von Texteingaben sollten nur dunkle grüne Rahmen bei Fokus verwenden, und die Schriftfarbe sollte dunkelgrau sein.

---

Die Schriftfarben von Texteingaben müssen helle Farben sein, und die Größe sollte nur mit der `px` Einheit gesetzt werden.

#### --answer--

Die Schriftarten von Texteingaben müssen angemessen groß sein, und die Farbe muss einen ausreichenden Kontrast zum Hintergrund haben.

### --question--

#### --text--

Wie sollte sich das Styling des Fehlerzustands zum Styling des Fokuszustands verhalten?

#### --distractors--

Sie sollten für Konsistenz identisch sein.

---

Der Fehlerzustand sollte weniger sichtbar sein, um Benutzer nicht zu beunruhigen.

---

Der Fehlerzustand sollte animiert sein, während der Fokuszustand statisch sein sollte.

#### --answer--

Der Fehlerzustand sollte sich deutlich vom Fokuszustand unterscheiden.

### --question--

#### --text--

Was ist eine bewährte Vorgehensweise für das Stylen von Eingaben im Fokuszustand?

#### --distractors--

Es ist wichtig, nur hellgraue Rahmen für fokussierte Eingaben zu verwenden.

---

Es ist wichtig, nur dunkelrote Rahmen für fokussierte Eingaben zu verwenden.

---

Es ist wichtig, jeglichen auffälligen Indikator zu entfernen.

#### --answer--

Es ist wichtig, einen auffälligen Indikator beizubehalten.

### --question--

#### --text--

Was ist eine bewährte Vorgehensweise für das Stylen von Eingaben im Fehlerzustand?

#### --distractors--

Erhöhen Sie die Schriftgröße für den Eingabetext auf `3rem`.

---

Setzen Sie die Eingabe auf `display: none;`, sodass sie von der Seite entfernt wird.

---

Setzen Sie die Eingabe auf deaktiviert, sodass Benutzer nicht mehr damit interagieren können.

#### --answer--

Geben Sie einen visuellen Indikator mit einer Nachricht an, damit Benutzer wissen, dass etwas falsch ist und behoben werden muss.

### --question--

#### --text--

Warum ist es wichtig, beschreibenden Platzhaltertext in Formulareingaben zu verwenden?

#### --distractors--

Er hilft, das Formular für den Benutzer vorab auszufüllen.

---

Er validiert die Eingabe automatisch, während der Benutzer tippt.

---

Er verbirgt die Bezeichnung der Eingabe und reduziert so visuelles Durcheinander.

#### --answer--

Er gibt den Benutzern Hinweise zum erwarteten Eingabeformat.

### --question--

#### --text--

Was ist WebKit?

#### --distractors--

Es ist eine Browser-Engine, die sicherstellt, dass es keine Validierungsfehler in Ihrem CSS gibt.

---

Es ist eine spezielle CSS-Eigenschaft, die im CSS Grid verwendet wird.

---

Es ist eine spezielle CSS-Eigenschaft, die im CSS Flexbox verwendet wird.

#### --answer--

Es ist eine Browser-Engine, die Webseiten darstellt.

### --question--

#### --text--

Wann ist es angemessen, `appearance: none;` für Formularelemente zu verwenden?

#### --distractors--

Um Formularelemente vollständig aus dem DOM und der Seite zu entfernen.

---

Um Formularelemente vor Personen zu verbergen, die unterstützende Technologien wie Screenreader verwenden.

---

Um nur die Standardfarben zu entfernen, die auf die Formularelemente angewendet werden, und eigene zu verwenden.

#### --answer--

Um einige der Standardstile zu entfernen, die auf die Formularelemente angewendet werden.

### --question--

#### --text--

Was sind einige Überlegungen beim Arbeiten mit `appearance: none;` an Formularelementen?

#### --distractors--

Es ist wichtig, Hover- und Absende-Indikatoren auf interaktiven Elementen beizubehalten.

---

Es ist wichtig, `appearance: none;` nur zu verwenden, wenn Sie unterschiedliche Stile für den Safari-Browser anwenden möchten.

---

Es ist wichtig, `appearance: none;` zu verwenden, wenn Sie ein Formularelement vollständig von der Seite entfernen müssen.

#### --answer--

Es ist wichtig, Fokus- und Fehlerindikatoren auf interaktiven Elementen beizubehalten.

### --question--

#### --text--

Welche Art von Formularelementen wird häufig mit `appearance: none;` gestylt, um ihr Standardaussehen zu entfernen?

#### --distractors--

`label` Elemente.

---

`div` Elemente.

---

`span` Elemente.

#### --answer--

`input` Elemente.
