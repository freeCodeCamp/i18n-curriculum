---
id: 672bd853c985cdfdeb32f4f9
title: ما هي خاصية text-shadow، وكيف تعمل؟
challengeType: 19
dashedName: what-is-the-text-shadow-property
---

# --interactive--

لا يطبق CSS أي ظلال على النص افتراضيًا. هذا مثال على فقرة بدون أي ظلال. لكن إذا كنت بحاجة لإضافة ظلال، يمكنك استخدام خاصية `text-shadow`.

```css
text-shadow: /* Values */
```

في CSS، يمكنك وصف الظل من خلال مزيج من إزاحة `X`، وإزاحة `Y`، ونصف قطر التمويه، واللون. أولًا، تحتاج إلى تحديد إزاحة `X` و`Y`، التي تمثل المسافة الأفقية والعمودية للظل من النص على التوالي. هذه القيم مطلوبة.

إليك مثالًا على كيفية تطبيق إزاحة `X` و`Y` على الظل. نطبق خاصية `text-shadow` مع إزاحة x مقدارها `3px` وإزاحة y مقدارها `2px`:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px;
}
```

:::

في المتصفح، سيبدو النص والظل متشابهين لكن يمكننا أيضًا تخصيص لون الظل بتحديد القيمة قبل أو بعد الإزاحة. لنحدد لون الظل. سنستخدم لونًا سداسيًا عشريًا هنا لكن يمكنك استخدام أي صيغة لون صالحة.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px #00ffc3;
}
```

:::

نكتب اللون بعد الإزاحة لكن يمكنك أيضًا كتابة اللون قبل الإزاحة إذا فضلت. النتيجة متساوية:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: #00ffc3 3px 2px;
}
```

:::

في المتصفح، سيكون للظل لون محدد، لذا يمكننا تمييزه بسهولة عن النص. الآن بعد أن يمكننا التمييز بين الظل والنص، من المهم أيضًا معرفة كيف تؤثر القيم الموجبة والسالبة على إزاحة الظل. القيم الموجبة لإزاحة `X` و`Y` تحرك الظل إلى اليمين والأسفل على التوالي، بينما القيم السالبة تحرك الظل إلى اليسار والأعلى. إليك مثالًا يستخدم القيم السالبة بدلاً من ذلك:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: -3px -2px #00ffc3;
}
```

:::

الآن سيتحرك الظل إلى اليسار والأعلى بالنسبة للنص. رائع. لكن الظل لا يبدو جيدًا جدًا، لأنه يشبه النص الأصلي تمامًا لكنه موضوع خلفه. لجعله يبدو أجمل، نحتاج إلى إضافة قيمة ثالثة، وهي نصف قطر التمويه. هذه القيمة اختيارية لكنها تجعل الظل يبدو أكثر نعومة ودقة. القيمة الافتراضية لنصف قطر التمويه هي صفر. كلما زادت القيمة، زاد التمويه، مما يعني أن الظل يصبح أخف.

هنا، نحدد نصف قطر التمويه إلى `3px` ونعود إلى القيم الموجبة للإزاحة:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px 3px #00ffc3;
}
```

:::

الآن بدأ الظل يبدو كظل حقيقي. أصبح أكثر تمويهًا ودقة، لذا يمكننا التركيز على النص الرئيسي بينما يضيف الظل بعض الأناقة في الخلفية. من هنا، يمكننا تعديل القيم قليلاً حتى نجد مزيجًا من الإزاحة واللون ونصف قطر التمويه يناسب احتياجاتنا.

من المفيد أيضًا معرفة أن النص يمكن أن يحتوي على أكثر من ظل واحد. فقط اكتبهم في نفس خاصية `text-shadow` مفصولين بفواصل. سيتم تطبيقهم على طبقات، من الأمام إلى الخلف، مع الظل الأول في الأعلى.

إليك مثالًا على فقرة بها ثلاثة ظلال مطبقة على طبقات. لاحظ أن كل هذه الظلال معرفة في نفس خاصية `text-shadow` ومفصولة بفواصل:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 
    3px 2px 3px #00ffc3, 
    -3px -2px 3px #0077ff, 
    5px 4px 3px #dee7e5;
}
```

:::

خاصية `text-shadow` هي خاصية CSS قوية لجعل نصك يبرز. يمكنها أيضًا تحسين التصميم العام لتطبيق الويب الخاص بك. باختيار الإزاحة والتمويه واللون، يمكنك إنشاء مجموعة واسعة من تأثيرات الظل.

# --questions--

## --text--

ما هو الغرض الأساسي من خاصية CSS `text-shadow`؟

## --answers--

تعديل حجم خط النص.

### --feedback--

فكر في كيفية تأثير خاصية `text-shadow` على المظهر البصري للنص.

---

تغيير لون النص.

### --feedback--

فكر في كيفية تأثير خاصية `text-shadow` على المظهر البصري للنص.

---

إنشاء تأثير ظل للنص.

---

التحكم في التباعد بين أسطر النص.

### --feedback--

فكر في كيفية تأثير خاصية `text-shadow` على المظهر البصري للنص.

## --video-solution--

3

## --text--

كيف يمكنك إنشاء ظل نص أسود مع إزاحة أفقية إلى اليمين ونصف قطر تمويه 5 بكسل؟

## --answers--

`text-shadow: 5px 0px 0px black;`

### --feedback--

فكر في ترتيب القيم في خاصية text-shadow.

---

`text-shadow: 5px 0px 5px black;`

---

`text-shadow: 5px 5px 0px black;`

### --feedback--

فكر في ترتيب القيم في خاصية text-shadow.

---

`text-shadow: 0px 5px 0px black;`

### --feedback--

فكر في ترتيب القيم في خاصية text-shadow.

## --video-solution--

2

## --text--

كيف يمكنك إنشاء تأثير ظل نص متعدد الطبقات؟

## --answers--

بتعريف ظلال متعددة في خاصية text-shadow.

---

عن طريق تسكين العناصر داخل بعضها البعض.

### --feedback--

فكر في كيفية دمج تأثيرات الظل المتعددة.

---

باستخدام خاصية `filter`.

### --feedback--

فكر في كيفية دمج تأثيرات الظل المتعددة.

---

بتطبيق صورة خلفية.

### --feedback--

فكر في كيفية دمج تأثيرات الظل المتعددة.

## --video-solution--

1
