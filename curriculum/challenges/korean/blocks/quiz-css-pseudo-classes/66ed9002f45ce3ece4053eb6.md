---
id: 66ed9002f45ce3ece4053eb6
title: CSS 가상 클래스 퀴즈
challengeType: 8
dashedName: quiz-css-pseudo-classes
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

가상 클래스란 무엇인가요?

#### --distractors--

필요할 때 요소 내에 추가 콘텐츠를 생성하는 CSS 규칙입니다.

---

요소의 크기나 레이아웃을 조정하는 속성입니다.

---

상호작용 중에 요소에 움직임 효과를 추가하는 선택자입니다.

#### --answer--

선택자에 추가되어 요소의 상태에 따라 스타일을 지정하는 키워드입니다.

### --question--

#### --text--

포인팅 장치가 요소 위에 위치할 때 적용되는 가상 클래스는 무엇인가요?

#### --distractors--

`:focus`

---

`:active`

---

`:checked`

#### --answer--

`:hover`

### --question--

#### --text--

단락의 첫 글자를 스타일링할 수 있는 가상 요소는 무엇인가요?

#### --distractors--

`:first-letter`

---

`:first-child`

---

`:last-of-type`

#### --answer--

`::first-letter`

### --question--

#### --text--

클릭하는 동안 요소의 스타일을 변경하는 가상 클래스는 무엇인가요?

#### --distractors--

`:focus`

---

`:hover`

---

`:checked`

#### --answer--

`:active`

### --question--

#### --text--

텍스트 필드처럼 사용자가 입력할 준비가 되었을 때 스타일링하는 가상 클래스는 무엇인가요?

#### --distractors--

`::focus`

---

`:active`

---

`:visited`

#### --answer--

`:focus`

### --question--

#### --text--

다음 중 클래스가 `Note:`인 각 단락 요소 앞에 텍스트 `note`를 올바르게 추가하는 CSS 규칙은 무엇인가요?

#### --distractors--

```css
.note::before {
  content: "Note:";
}
```

---

```css
p.note::after {
  content: "Note:";
}
```

---

```css
p::before {
  content: "Note:";
}
```

#### --answer--

```css
p.note::before {
  content: "Note:";
}
```

### --question--

#### --text--

선택되거나 토글된 입력 필드에 적용되는 가상 클래스는 무엇인가요?

#### --distractors--

`:required`

---

`:disabled`

---

`:optional`

#### --answer--

`:checked`

### --question--

#### --text--

목록의 마지막 자식을 스타일링하는 올바른 구문은 무엇인가요?

#### --distractors--

```css
li:nth-child(last) {
  color: blue;
}
```

---

```css
#li:last-child {
  color: blue;
}
```

---

```css
.li:last-child() {
  color: blue;
}
```

#### --answer--

```css
li:last-child {
  color: blue;
}
```

### --question--

#### --text--

필수 입력이 아닌 입력 필드를 대상으로 하는 가상 클래스는 무엇인가요?

#### --distractors--

`:required`

---

`:enabled`

---

`::optional`

#### --answer--

`:optional`

### --question--

#### --text--

`:disabled` 가상 클래스는 무엇을 하나요?

#### --distractors--

선택된 입력을 스타일링합니다.

---

호버된 요소를 스타일링합니다.

---

주어진 선택자와 일치하지 않는 요소를 선택합니다.

#### --answer--

사용자 상호작용이 불가능한 요소를 스타일링합니다.

### --question--

#### --text--

폼 입력이 유효성 검사 기준을 충족할 때 적용되는 가상 클래스는 무엇인가요?

#### --distractors--

`:checked`

---

`:required`

---

`:disabled`

#### --answer--

`:valid`

### --question--

#### --text--

다음 중 위치 가상 클래스가 아닌 것은 무엇인가요?

#### --distractors--

`:visited`

---

`:any-link`

---

`:link`

#### --answer--

`:current`

### --question--

#### --text--

다음 중 세 번째 목록 항목을 선택하는 것은 무엇인가요?

#### --distractors--

```css
li:child(3) {
  color: red;
}
```

---

```css
li:last-child(3) {
  color: red;
}
```

---

```css
li:nth-child(three) {
  color: red;
}
```

#### --answer--

```css
li:nth-child(3) {
  color: red;
}
```

### --question--

#### --text--

다음 CSS로 `color` 요소의 `blue` 색상이 적용되는 요소는 무엇인가요?

```css
p:is(.blue, .highlight) {
  color: blue;
}
```

#### --distractors--

```html
<p class="class">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

---

```html
<div class="blue">Paragraph 1</div>
<div class="highlight">Paragraph 2</div>
```

---

```html
<p>Paragraph 1</p>
<span class="highlight">Paragraph 2</span>
```

#### --answer--

```html
<p class="blue">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

### --question--

#### --text--

`:not()` 가상 클래스는 무엇을 하나요?

#### --distractors--

모든 요소에 스타일을 추가합니다.

---

부모의 모든 자식 요소를 선택합니다.

---

주어진 선택자와 일치하는 요소를 선택합니다.

#### --answer--

주어진 선택자와 일치하지 않는 요소를 선택합니다.

### --question--

#### --text--

다음 CSS 규칙은 무엇을 하나요?

```css
p:first-of-type {
  font-style: italic;
}
```

#### --distractors--

문서 내 첫 번째 `p` 요소를 선택합니다.

---

문서 내 모든 `p` 요소를 선택합니다.

---

모든 `p` 요소의 첫 번째 자식을 선택합니다.

#### --answer--

부모 컨테이너 내 첫 번째 `p` 요소를 선택합니다.

### --question--

#### --text--

`:last-of-type` 가상 클래스는 무엇을 하나요?

#### --distractors--

부모 내 특정 유형의 첫 번째 자식 요소를 선택합니다.

---

부모 내 특정 유형의 중간 자식 요소를 선택합니다.

---

부모 내 특정 유형의 모든 자식 요소를 선택합니다.

#### --answer--

부모 내 특정 유형의 마지막 자식 요소를 선택합니다.

### --question--

#### --text--

목록에서 두 번째 항목을 선택하는 가상 클래스는 무엇인가요?

#### --distractors--

`:first-child`

---

`:required`

---

`:is()`

#### --answer--

`:nth-child(2)`

### --question--

#### --text--

다음 중 함수형 가상 클래스는 무엇인가요?

#### --distractors--

`:first-child`

---

`:match()`

---

`:checked`

#### --answer--

`:is()`

### --question--

#### --text--

다음 중 함수형 가상 클래스가 아닌 것은 무엇인가요?

#### --distractors--

`:has()`

---

`:not()`

---

`:where()`

#### --answer--

`:contains()`

## --quiz--

### --question--

#### --text--

활성화된 폼 요소를 대상으로 하는 가상 클래스는 무엇인가요?

#### --distractors--

`:disabled`

---

`:active`

---

`:focus`

#### --answer--

`:enabled`

### --question--

#### --text--

끝에서부터 요소를 선택할 수 있게 하는 가상 클래스는 무엇인가요?

#### --distractors--

`:nth-child(n)`

---

`:last-child`

---

`:last-of-type`

#### --answer--

`:nth-last-child(n)`

### --question--

#### --text--

특정 자식 요소를 포함하는 요소를 선택할 수 있는 것은 무엇인가요?

#### --distractors--

`:is()`

---

`:where()`

---

`:in-range`

#### --answer--

`:has()`

### --question--

#### --text--

내용이나 자식 요소가 전혀 없는 요소를 선택하는 것은 무엇인가요?

#### --distractors--

`:only-child`

---

`:last-child`

---

`:not()`

#### --answer--

`:empty`

### --question--

#### --text--

이 CSS 선택자는 무엇을 대상으로 하나요?

```css
input:invalid {
  background-color: red;
}
```

#### --distractors--

모든 입력 요소입니다.

---

허용된 범위 내 값을 가진 모든 입력 요소입니다.

---

유효성 검사를 통과한 모든 입력 요소입니다.

#### --answer--

유효성 검사를 통과하지 못한 모든 입력 요소입니다.

### --question--

#### --text--

브라우저가 자동으로 값을 채운 입력 필드를 선택하는 가상 클래스는 무엇인가요?

#### --distractors--

`:visited`

---

`:valid`

---

`:where()`

#### --answer--

`:autofill`

### --question--

#### --text--

자신이나 자손 요소 중 하나가 포커스된 요소를 선택하는 가상 클래스는 무엇인가요?

#### --distractors--

`:focus`

---

`:in-range`

---

`:only-child`

#### --answer--

`:focus-within`

### --question--

#### --text--

같은 문서를 가리키는 링크를 나타내는 가상 클래스는 무엇인가요?

#### --distractors--

`:target`

---

`:visited`

---

`:link`

#### --answer--

`:local-link`

### --question--

#### --text--

URL 조각의 대상이 될 때 `p` 요소를 스타일링하는 것은 무엇인가요?

#### --distractors--

```css
p:empty {
  background-color: gold;
}
```

---

```css
p:not(.targeted) {
  background-color: gold;
}
```

---

```css
p:is(.target) {
  background-color: gold;
}
```

#### --answer--

```css
p:target {
  background-color: gold;
}
```

### --question--

#### --text--

요소가 URL 조각의 대상일 때 사용하는 가상 클래스는 무엇인가요?

#### --distractors--

`:focus-within`

---

`:hover`

---

`:visited`

#### --answer--

`:target`

### --question--

#### --text--

`:only-child` 가상 클래스는 무엇을 선택하나요?

#### --distractors--

자식이 하나뿐인 부모 요소를 선택합니다.

---

부모 요소 내 모든 자식 요소를 선택합니다.

---

하나의 유형 자식 요소만 포함하는 부모 요소를 선택합니다.

#### --answer--

부모 요소 내 형제가 없는 요소를 선택합니다.

### --question--

#### --text--

부모 내에서 해당 유형이 유일한 요소일 때 선택하는 가상 클래스는 무엇인가요?

#### --distractors--

`:only-child`

---

`:nth-of-type(n)`

---

`:first-of-type`

#### --answer--

`:only-of-type`

### --question--

#### --text--

다음 HTML에서 두 번째 `color` 요소에 `yellow`가 `p`로 적용되는 CSS 규칙은 무엇인가요?

```html
<div>
  <h1>Courses</h1>
  <p>HTML</p>
  <p>CSS</p>
  <p>JavaScript</p>
</div>
```

#### --distractors--

```css
p:nth-child(2) {
  color: yellow;
}
```

---

```css
p:first-of-type {
  color: yellow;
}
```

---

```css
p:last-of-type {
  color: yellow;
}
```

#### --answer--

```css
p:nth-of-type(2) {
  color: yellow;
}
```

### --question--

#### --text--

목록 항목의 마커를 선택해 스타일링할 수 있는 가상 요소는 무엇인가요?

#### --distractors--

`::before`

---

`::after`

---

`:root`

#### --answer--

`::marker`

### --question--

#### --text--

문서에서 가장 상위 요소, 보통 `html` 요소를 대상으로 하는 가상 클래스는 무엇인가요?

#### --distractors--

`:first-child`

---

`:in-range`

---

`:target`

#### --answer--

`:root`

### --question--

#### --text--

특이성이 0인 CSS 가상 클래스로, 다른 특정 스타일 규칙에 영향을 주지 않는 것은 무엇인가요?

#### --distractors--

`:is()`

---

`:not()`

---

`:focus`

#### --answer--

`:where()`

### --question--

#### --text--

다음 중 트리 구조 가상 클래스는 무엇인가요?

#### --distractors--

`:where()`

---

`:valid`

---

`:link`

#### --answer--

`:root`

### --question--

#### --text--

다음 요소의 값이 지정된 범위를 벗어날 때 `background-color`를 `red`로 설정하는 CSS 규칙은 무엇인가요?

```html
<input type="number" min="10" max="25"/>
```

#### --distractors--

```css
input:in-range {
  background-color: red;
}
```

---

```css
input {
  background-color: red;
}
```

---

```css
input:valid {
  background-color: red;
}
```

#### --answer--

```css
input:out-of-range {
  background-color: red;
}
```

### --question--

#### --text--

요소 뒤에 콘텐츠를 삽입하는 데 `content` 속성을 사용하는 가상 요소는 무엇인가요?

#### --distractors--

`::before`

---

`::marker`

---

`::first-letter`

#### --answer--

`::after`

### --question--

#### --text--

값이 지정된 범위 내에 있을 때 요소에 스타일을 적용하는 가상 클래스는 무엇인가요?

#### --distractors--

`:out-of-range`

---

`:enabled`

---

`:checked`

#### --answer--

`:in-range`
