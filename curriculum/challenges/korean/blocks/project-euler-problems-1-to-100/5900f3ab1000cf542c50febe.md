---
id: 5900f3ab1000cf542c50febe
title: '문제 63: 강력한 자리수 개수'
challengeType: 1
forumTopicId: 302175
dashedName: problem-63-powerful-digit-counts
---

# --description--

5자리 숫자 16807 = 7<sup>5</sup>는 다섯 번째 거듭제곱이기도 합니다. 마찬가지로 9자리 숫자 134217728 = 8<sup>9</sup>는 아홉 번째 거듭제곱입니다.

함수를 완성하여 길이가 `n`이고 `n`번째 거듭제곱인 양의 정수가 몇 개인지 반환하세요.

# --hints--

`powerfulDigitCounts(1)`는 숫자를 반환해야 합니다.

```js
assert(typeof powerfulDigitCounts(1) === 'number');
```

`powerfulDigitCounts(1)`는 `9`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(1), 9);
```

`powerfulDigitCounts(2)`는 `6`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(2), 6);
```

`powerfulDigitCounts(3)`는 `5`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(3), 5);
```

`powerfulDigitCounts(4)`는 `4`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(4), 4);
```

`powerfulDigitCounts(5)`는 `3`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(5), 3);
```

`powerfulDigitCounts(6)`는 `3`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(6), 3);
```

`powerfulDigitCounts(7)`는 `2`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(7), 2);
```

`powerfulDigitCounts(8)`는 `2`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(8), 2);
```

`powerfulDigitCounts(10)`는 `2`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(10), 2);
```

`powerfulDigitCounts(21)`는 `1`를 반환해야 합니다.

```js
assert.strictEqual(powerfulDigitCounts(21), 1);
```

# --seed--

## --seed-contents--

```js
function powerfulDigitCounts(n) {

  return true;
}

powerfulDigitCounts(1);
```

# --solutions--

```js
function powerfulDigitCounts(n) {
  function countDigits(num) {
    let counter = 0;
    while (num > 0) {
      num = Math.floor(num / 10);
      counter++;
    }
    return counter;
  }

  let numbersCount = 0;

  let curNum = 1;
  while (curNum < 10) {
    let power = n;
    if (power === countDigits(curNum ** power)) {
      numbersCount++;
    }
    curNum++;
  }

  return numbersCount;
}
```
