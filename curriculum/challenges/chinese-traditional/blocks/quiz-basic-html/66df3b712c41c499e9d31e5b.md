---
id: 66df3b712c41c499e9d31e5b
title: 基本 HTML 測驗
challengeType: 8
dashedName: quiz-basic-html
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

`audio` 元素內的 `loop` 屬性如何運作？

#### --distractors--

它允許 `audio` 元素與網頁上的其他多媒體元素同步播放。

---

它會調整音訊的音高和音調。

---

它會觸發 `audio` 元素停止並以隨機順序重新開始播放。

#### --answer--

完成後會重新開始音訊。

### --question--

#### --text--

HTML 中的 void 元素是什麼？

#### --distractors--

用於在螢幕上嵌入影片的元素。

---

用於顯示列表的元素。

---

用於將聲音嵌入到文件中的元素。

#### --answer--

沒有任何內容或結束標籤的元素。

### --question--

#### --text--

以下哪一項是 `id` 屬性的正確值？

#### --distractors--

`id="main@content"`

---

`id="main content"`

---

`id="main~content"`

#### --answer--

`id="main-content"`

### --question--

#### --text--

`target="_parent"` 如何運作？

#### --distractors--

它會在新的瀏覽背景關係中開啟一個聯結。

---

它指定在點擊聯結後，瀏覽器應捲動到聯結文件中的位置。

---

它會將使用者重新導向到 `href` 屬性中指定的不同網站。

#### --answer--

它會在目前背景關係的父層中開啟一個 `link`。

### --question--

#### --text--

布林值和一般屬性之間有什麼差別？

#### --distractors--

布林值屬性只能用於核取方塊，而一般屬性則用於圓鈕。

---

一般屬性對於影像總是必須的，而布林值屬性則是選用的。

---

布林值屬性必須始終指定值，而一般屬性則不需要指定值。

#### --answer--

布林值屬性可以存在或不存在，而一般屬性則總是有指定的值。

### --question--

#### --text--

哪個元素用來將腳本連結到你的 HTML 檔案？

#### --distractors--

`img`

---

`ul`

---

`h2`

#### --answer--

`script`

### --question--

#### --text--

什麼是 HTML boilerplate？

#### --distractors--

一個由網頁開發者用來為文件添加浮水印的特殊工具。

---

一種標記語言的型別。

---

一個會自動在頁面上產生「lorem ipsum」文字的外掛程式。

#### --answer--

包含 HTML `document` 所需所有基本資訊的 `template`。

### --question--

#### --text--

哪一個屬性會套用唯一識別字到 HTML 元素？

#### --distractors--

`class`

---

`href`

---

`action`

#### --answer--

`id`

### --question--

#### --text--

什麼是 HTML 物體（字元引用）？

#### --distractors--

HTML 中字元的圖形表述。

---

HTML 中使用的一種特殊格式化標記。

---

用於在網頁設計中表述字元的影像。

#### --answer--

一組用來表述 HTML 中保留字元的字元。

### --question--

#### --text--

`audio` 元素的用途是什麼？

#### --distractors--

它用於允許使用者直接在網頁上錄製他們的聲音。

---

它用於為頁面上的 `audio` 和 `video` 元素設計樣式。

---

它用於在點擊時使網頁上的文字可聽見。

#### --answer--

它用於將音訊添加到 HTML 文件中。

### --question--

#### --text--

以下哪一項不是 HTML 元素的範例？

#### --distractors--

`img`

---

`h1`

---

`link`

#### --answer--

`byte`

### --question--

#### --text--

`target` 屬性在錨點元素中的角色是什麼？

#### --distractors--

它定義了當使用者暫留在聯結上時的顏色。

---

它指定在點擊聯結後，瀏覽器應捲動到聯結文件中的位置。

---

它決定 `link` 元素內錨點文字的字型大小。

#### --answer--

它用於指定當使用者點擊聯結時，聯結文件將在哪裡開啟。

### --question--

#### --text--

什麼是絕對路徑？

#### --distractors--

相對於目前 `file` 的路徑。

---

包含變數的路徑。

---

包含萬用字元的路徑。

#### --answer--

從根目錄開始的資源完整路徑。

### --question--

#### --text--

`link` 元素在 HTML 中的角色是什麼？

#### --distractors--

它用於在同一個網頁內創建超連結。

---

它用於定義網頁的版面配置和結構。

---

它用於將音訊或影片檔案嵌入到網頁中。

#### --answer--

它用於連結到外部資源，例如樣式表和網站圖示。

### --question--

#### --text--

下列哪一項是用於操作 open graph 屬性的有效值？

#### --distractors--

`"og:socialMedia"`

---

`"og:mediaOG"`

---

`"og:openGraph"`

#### --answer--

`"og:title"`

### --question--

#### --text--

以下哪一項是布林值屬性的範例？

#### --distractors--

`href`

---

`src`

---

`width`

#### --answer--

`checked`

### --question--

#### --text--

以下哪一項是 `img` 元素的正確語法？

#### --distractors--

`<img src="image.jpg" alt="Description of the image"></img>`

---

`<img source="image.jpg" desc="Description of the image"></img>`

---

`<img src="image.jpg" desc="Description of the image">`

#### --answer--

`<img src="image.jpg" alt="Description of the image">`

### --question--

#### --text--

`iframe` 元素用於什麼？

#### --distractors--

它用於為影像添加標題。

---

它用於為媒體添加版權資訊。

---

它用於在同一個網頁內創建超連結。

#### --answer--

它用於在當前的 HTML 文件中嵌入另一個文件。

### --question--

#### --text--

以下哪一項不是錨點元素的有效 link 狀態？

#### --distractors--

`hover`

---

`visited`

---

`active`

#### --answer--

`href`

### --question--

#### --text--

`title` 元素的角色是什麼？

#### --distractors--

一個允許使用者編輯網頁 `source code` 的元素。

---

它定義了網頁中元素的佈局和定位。

---

它用於將影片和多媒體內容直接嵌入到 HTML 文件中。

#### --answer--

它指定文件的標題，並顯示在瀏覽器分頁或視窗中。

## --quiz--

### --question--

#### --text--

下列哪一個 `target` 屬性會在目前背景關係的父層中開啟連結？

#### --distractors--

`_blank`

---

`_self`

---

`_unfencedTop`

#### --answer--

`_parent`

### --question--

#### --text--

哪個屬性用於設定 HTML 文件的 `UTF-8` 字元編碼？

#### --distractors--

`accept`

---

`capture`

---

`enctype`

#### --answer--

`charset`

### --question--

#### --text--

HTML 在網路上的角色是什麼？

#### --distractors--

HTML 用於樣式和版面配置。

---

HTML 用於為頁面添加互動性。

---

HTML 是軟體包的登錄檔。

#### --answer--

HTML 表述網頁的內容和結構。

### --question--

#### --text--

哪一種影像授權適用於公共領域？

#### --distractors--

JPG

---

創用 CC。

---

BSD

#### --answer--

創用 CC 0。

### --question--

#### --text--

你如何將布林值屬性設為 `false`？

#### --distractors--

透過將屬性值設為 `0`。

---

在屬性名稱前使用 `not` 運算子。

---

透過將屬性設定為空字串（`""`）。

#### --answer--

透過省略元素中的屬性。

### --question--

#### --text--

`script` 元素用於什麼？

#### --distractors--

它用於將 CSS 嵌入到 HTML 文件中或連結到外部 CSS 檔案。

---

它用於將 Pascal 程式碼嵌入到 HTML 文件中。

---

它用於將 C# 程式碼嵌入到 HTML 文件中。

#### --answer--

它用於將 JavaScript 嵌入到 HTML 文件中或連結到外部 JavaScript 檔案。

### --question--

#### --text--

以下哪個元素用於設定 HTML `document` 的標題？

#### --distractors--

`footer`

---

`section`

---

`figcaption`

#### --answer--

`title`

### --question--

#### --text--

`class` 屬性通常用於什麼？

#### --distractors--

它用於嵌入關於該元素的詮釋資料。

---

它用於直接在 HTML 頁籤中定義行內樣式。

---

它用於指定 JavaScript 函式的唯一識別符。

#### --answer--

它用於將一組樣式套用到多個元素。

### --question--

#### --text--

以下哪一項不是常用 HTML 物體的範例？

#### --distractors--

`&quot;`（雙引號）。

---

`&copy;`（版權符號）。

---

`&gt;`（大於符號）。

#### --answer--

`&div;`（div 元素）。

### --question--

#### --text--

哪個元素用於為你的 HTML 文件添加音訊？

#### --distractors--

`media`

---

`video`

---

`hr`

#### --answer--

`audio`

### --question--

#### --text--

在 HTML 文件中，頁面上表述內容的位置在哪裡？

#### --distractors--

在 `head` 和 `body` 元素之間。

---

在 `repr` 元素內。

---

在 `script` 元素內。

#### --answer--

在 `body` 元素內。

### --question--

#### --text--

哪個屬性用於在音訊結束後重新開始播放？

#### --distractors--

`multiple`

---

`inputmode`

---

`enctype`

#### --answer--

`loop`

### --question--

#### --text--

以下哪一項是 `div` 元素的正確語法？

#### --distractors--

`<<div>>block container<</div>>`

---

`>>div>>block container>>div>>`

---

`[div]block container[/div]`

#### --answer--

`<div>block container</div>`

### --question--

#### --text--

`target="_self"` 如何運作？

#### --distractors--

它指定在點擊聯結後，瀏覽器應捲動到聯結文件中的位置。

---

它會自動將聯結的文件下載到使用者的電腦。

---

它會將使用者重新導向到 `href` 屬性中指定的不同網站。

#### --answer--

它會在目前的瀏覽背景關係中開啟一個聯結。

### --question--

#### --text--

什麼是相對路徑？

#### --distractors--

它指定了從檔案系統根目錄開始的檔案或目錄的確切位置。

---

它包含從網域名稱到特定檔案或目錄的完整 URL。

---

它總是以斜線（`/`）開頭，表示網站或檔案系統的根目錄。

#### --answer--

它指定相對於目前工作目錄的檔案或目錄位置。

### --question--

#### --text--

以下哪一個元素是 `link` 元素的正確語法？

#### --distractors--

`<link rel="stylesheet" href="./styles.css"></link>`

---

`</link rel="stylesheet" href="./styles.css"></link>`

---

`<<link rel="stylesheet" href="./styles.css"></link>>`

#### --answer--

`<link rel="stylesheet" href="./styles.css" />`

### --question--

#### --text--

以下哪一項不是用於操作 open graph 屬性的有效值？

#### --distractors--

`"og:title"`

---

`"og:type"`

---

`"og:image"`

#### --answer--

`"og:socialMedia"`

### --question--

#### --text--

下列哪一項是段落元素的正確語法？

#### --distractors--

`>p>paragraph element>/p>`

---

`<<p>>paragraph element<</p>>`

---

`<p>`

#### --answer--

`<p>paragraph element</p>`

### --question--

#### --text--

`iframe` 元素的名稱代表什麼？

#### --distractors--

行內框架

---

內部影格

---

行內 影片

#### --answer--

行內影格

### --question--

#### --text--

作用中狀態對於錨點元素意味著什麼？

#### --distractors--

該聯結已被停用，無法點擊。

---

滑鼠游標位於聯結上。

---

該聯結已被使用者造訪。

#### --answer--

使用者正在作用中點擊它。

## --quiz--

### --question--

#### --text--

以下哪一項會在新的瀏覽器分頁中開啟 `link`？

#### --distractors--

```html
<a target="_parent" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_top" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_self" href="https://freecatphotoapp.com">cat photos</a>
```

#### --answer--

```html
<a target="_blank" href="https://freecatphotoapp.com">cat photos</a>
```

### --question--

#### --text--

以下哪一項是空元素？

#### --distractors--

`div`

---

`section`

---

`p`

#### --answer--

`img`

### --question--

#### --text--

以下哪一項是使用 HTML 實體的正確方式？

#### --distractors--

`<p>This is an &lt&img /&gt& element</p>`

---

`<p>This is an ;lt&img /;gt& element</p>`

---

`<p>This is an ;lt;img /;gt; element</p>`

#### --answer--

`<p>This is an &lt;img /&gt; element</p>`

### --question--

#### --text--

HTML 中的 `ul` 元素用於什麼？

#### --distractors--

它用於僅將影像列表分組。

---

它用於僅將一個聯結列表分組。

---

它用於將有序的項目列表分組。

#### --answer--

它用於將項目符號列表的項目分組。

### --question--

#### --text--

以下哪一種方式是將多個 `class` 套用到 HTML 元素的正確方法？

#### --distractors--

```html
<div class="box>>red>>box"></div>
``` 

---

```html
<div class="box<<red<<box"></div>
``` 

---

```html
<div class="box-red-box"></div>
``` 

#### --answer--

```html
<div class="box red-box"></div>
``` 

### --question--

#### --text--

以下哪一個元素用於表述有序的項目列表？

#### --distractors--

`span`

---

`div`

---

`ul`

#### --answer--

`ol` 

### --question--

#### --text--

Open graph 屬性用於什麼？

#### --distractors--

這些屬性用於將互動式多媒體內容直接嵌入到網頁中。

---

這些屬性用於在網站上產生動態彈出廣告。

---

這些屬性用於加密在網頁伺服器和使用者瀏覽器之間傳輸的敏感資料。

#### --answer--

這些屬性用於設定網站內容在不同社交媒體平台上的顯示方式。 

### --question--

#### --text--

以下哪一項用於設定網頁的簡短描述？

#### --distractors--

```html
<meta
  add="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  set="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  description="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

#### --answer--

```html
<meta
  name="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

### --question--

#### --text--

SVG 代表什麼？

#### --distractors--

可擴充的向量圖形

---

可擴充向量圖形

---

網站向量圖形

#### --answer--

可縮放向量圖形 

### --question--

#### --text--

`footer` 元素的角色是什麼？

#### --distractors--

此元素用於將相關的標題和影像、圖片在網頁上進行群組。 

---

此元素用於在頁面段落之間創建水平線。

---

此元素用於在頁面底部創建有序項目列表。 

#### --answer--

此元素用於在頁面底部群組版權內容或相關文件。 

### --question--

#### --text--

什麼是 `UTF-8` 字元編碼？

#### --distractors--

它是一種標準化的字元編碼，用於設定網頁的標題。

---

它是一組用於設定網頁頁面描述的字元。

---

它是一組用於元素中以提升媒體效率的特殊字元。 

#### --answer--

它是一種在網路上廣泛使用的標準化字元編碼。  

### --question--

#### --text--

以下哪一項是取代元素的範例？

#### --distractors--

`slot`

---

`div`

---

`p`

#### --answer--

`iframe` 

### --question--

#### --text--

以下哪一項被視為網頁上最重要的標題？

#### --distractors--

`h6`

---

`h3`

---

`h5`

#### --answer--

`h1` 

### --question--

#### --text--

下列哪一個元素用於連結到外部資源，例如樣式表和網站圖示？

#### --distractors--

`a`

---

`p`

---

`div`

#### --answer--

`link` 

### --question--

#### --text--

以下哪一個元素用於傳達緊迫感、嚴重性或強烈的重要性？

#### --distractors--

`important`

---

`heavy`

---

`strength`

#### --answer--

`strong`

### --question--

#### --text--

`figcaption` 元素的角色是什麼？

#### --distractors--

它為 `span` 元素內的內容提供標題。 

---

它為 `slot` 元素內的內容提供標題。 

---

它為 `div` 元素內的內容提供標題。 

#### --answer--

它為 `figure` 元素內的內容提供標題。 

### --question--

#### --text--

以下哪一種方式是為 HTML `DOCTYPE` 宣告設定正確的方法？

#### --distractors--

`<DOCTYPE>`

---

`<*DOCTYPE html>`

---

`<<DOCTYPE html>>`

#### --answer--

`<!DOCTYPE html>`

### --question--

#### --text--

HTML 文件中 `head` 元素的用途是什麼？

#### --distractors--

它是 HTML 文件中錨點元素的容器。 

---

它是 HTML 文件中影像的容器。 

---

它是 HTML 文件中標題元素的容器。 

#### --answer--

它是 HTML 文件的元資料容器。 

### --question--

#### --text--

以下哪一項是將 HTML 文件語言設為英文的正確方法？

#### --distractors--

`<html lang-set="en">` 

---

`<html set-lang="en">` 

---

`<html language="en">` 

#### --answer--

`<html lang="en">` 

### --question--

#### --text--

下列哪些元素不應該放在 `head` 裡？

#### --distractors--

`link`

---

`meta`

---

`title`

#### --answer--

`img` 

## --quiz--

### --question--

#### --text--

下列哪一項不是關於絕對路徑和相對路徑的正確述語？

#### --distractors--

絕對路徑從根目錄開始，而相對路徑則取決於目前檔案的位置。

---

相對路徑較短且在網站內聯結時更具彈性，而絕對路徑則包含完整的 URL。

---

絕對路徑和相對路徑皆可用於連結同一網站內的檔案。

#### --answer--

相對路徑不能使用 `..` 向上移動目錄，但絕對路徑可以。

### --question--

#### --text--

以下哪一項不是 `a` 頁籤中有效的 target 屬性值？

#### --distractors--

`_self`

---

`_parent`

---

`_top`

#### --answer--

`_main`

### --question--

#### --text--

`audio` 元素中 `controls` 屬性的用途是什麼？

#### --distractors--

它定義瀏覽器要使用的音訊檔案格式。

---

頁面載入時會自動開始音訊。

---

它指定音訊檔案的來源 URL。

#### --answer--

它添加了內建的播放控制元件，例如播放、暫停和音量調整。

### --question--

#### --text--

以下哪個元素透過提供網頁的簡短描述來提升 SEO？

#### --distractors--

```html
<meta
 description="describe"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 name="desc"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 description="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

#### --answer--

```html
<meta
 name="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

### --question--

#### --text--

`<meta charset="utf-8" />` 有什麼作用？

#### --distractors--

將網頁語言設定為英文。

---

將外部 CSS 樣式表聯結起來。

---

指定網頁的標題。

#### --answer--

定義字元編碼以確保正確的文字顯示。

### --question--

#### --text--

HTML 中的 `link` 和 `script` 元素有什麼差異？

#### --distractors--

`link` 用於 JavaScript 檔案，`script` 用於 CSS 檔案。

---

`link` 和 `script` 都用於為網頁設計樣式。

---

`script` 用於添加樣式，`link` 用於執行 JavaScript。

#### --answer--

`link` 用於從外部 CSS 檔案添加樣式，而 `script` 用於執行 JavaScript 程式碼。

### --question--

#### --text--

以下關於 HTML 類別和 ID 的述語中，哪一項是不正確的？

#### --distractors--

一個類別名稱可以在整個 HTML 文件中多次使用。

---

`rel` 屬性定義了連結資源與 HTML 文件之間的關係。

---

`og:image` 屬性用於指定在社群媒體貼文中顯示的影像。

#### --answer--

ID 名稱不能包含底線，且必須唯一。

### --question--

#### --text--

以下關於 HTML 標題元素的述語中，哪一項是正確的？

#### --distractors--

文字大小從 `h1` 增加到 `h6`。

---

所有標題元素的文字大小保持不變。

---

`h3` 比 `h2` 大。

#### --answer--

文字大小從 `h1` 到 `h6` 逐漸減小。

### --question--

#### --text--

關於 HTML 元素，哪個述語是不正確的？

#### --distractors--

HTML 元素是網頁的基本建置區塊。

---

某些程式碼庫在 void 元素內包含正斜線（/）。

---

布林值屬性是指只要在頁籤中存在或不存在即可生效的屬性。

#### --answer--

Void 元素包含內容且只有開始標籤。

### --question--

#### --text--

以下關於 HTML 中 void 元素的述語，哪一項是正確的？

#### --distractors--

空元素必須在結尾前始終包含一個斜線 /。

---

空元素絕不應包含斜線 /。

---

在所有版本的 HTML 中，自我關閉的空元素必須包含正斜線 /。

#### --answer--

在現代 HTML 中，`<img>` 和 `<img/>` 都是可接受的。

### --question--

#### --text--

程式設計中註解的主要目的是什麼？

#### --distractors--

為了讓程式碼執行得更快。

---

要更改瀏覽器顯示內容的方式。

---

在程式碼中儲存臨時資料。

#### --answer--

在程式碼中為你自己和其他開發者留下註解。

### --question--

#### --text--

哪一個標的屬性會在與目前相同的瀏覽背景關係中開啟連結？

#### --distractors--

`_blank`

---

`_top`

---

`_parent`

#### --answer--

`_self`

### --question--

#### --text--

HTML 中 `em` 元素的用途是什麼？

#### --distractors--

使文字變大。

---

要更改文字顏色。

---

為了強調而為字、單字加底線。

#### --answer--

透過斜體字來凸顯標示重要的文字。

### --question--

#### --text--

`strong` 元素在 HTML 中的作用是什麼？

#### --distractors--

增加字型大小。

---

變更文字顏色。

---

為文字加底線。

#### --answer--

將文字加粗以顯示重要性。

### --question--

#### --text--

`figure` 和 `figcaption` 元素在 HTML 中的主要差異是什麼？

#### --distractors--

`figure` 用於標題，而 `figcaption` 用於群組相關內容，如影像或圖表。

---

`figcaption` 必須始終放置在 `figure` 元素之外。

---

沒有差別；兩者用於相同的目的。

#### --answer--

`figure` 將相關內容如影像或圖表群組起來，而 `figcaption` 則為該內容提供標題。

### --question--

#### --text--

為什麼要使用 HTML 實體？

#### --distractors--

為文字添加樣式。

---

在網頁中創建超連結。

---

要增加特殊字元的字型大小。

#### --answer--

在 HTML 中表述保留字元如 `<` 和 `&`。

### --question--

#### --text--

以下關於 Open Graph 頁籤的述語中，哪一項是正確的？

#### --distractors--

Open Graph 頁籤用於使用 CSS 來設計網頁。

---

Open Graph 頁籤僅用於 SEO，對社群媒體沒有影響。

---

Open Graph 頁籤必須放置在 HTML 的 `body` 區段內。

#### --answer--

`og:title` 屬性設定內容在社群媒體分享時顯示的標題。

### --question--

#### --text--

以下關於影像格式的敘述中，哪一項是不正確的？

#### --distractors--

WEBP 和 AVIF 對現代影像優化更為有效。

---

SVG 影像可以縮放而不會損失品質。

---

壓縮有助於減少影像檔案大小。

#### --answer--

PNG 和 JPG 是目前最佳化影像的最佳格式。

### --question--

#### --text--

`audio` 元素中的 `loop` 屬性有什麼作用？

#### --distractors--

播放該音訊一次。

---

在播放一次後停止音訊。

---

減慢音訊播放速度。

#### --answer--

持續重複該音訊。

### --question--

#### --text--

以下哪一項最能描述 HTML 範本？

#### --distractors--

一個由網頁開發者用來為文件添加浮水印的特殊工具。

---

一個加速網頁載入的瀏覽器外掛程式。

---

一個專門用於 HTML 動畫的程式設計框架。

#### --answer--

包含網頁開發所需基本 HTML 結構的預定義模板。
