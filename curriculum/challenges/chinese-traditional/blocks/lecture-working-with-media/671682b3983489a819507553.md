---
id: 671682b3983489a819507553
title: 如何使用內聯框架元素在頁面上嵌入視頻？
challengeType: 19
dashedName: how-do-you-embed-videos-onto-your-page-using-the-iframe-element
---

# --interactive--

In a prior lesson, you were first introduced to the `iframe` element. In this lesson, you will learn more about how to work with the `iframe` element. This element stands for inline frame. 它是一種內聯元素，用於在 HTML 頁面中直接嵌入其他 HTML 內容。 這些 HTML 內容可以是視頻、地圖、其他 HTML 元素，甚至是其他網頁。

Here's an example of embedding a popular freeCodeCamp course from YouTube. To see a different video, change the `src` value to a video of your choosing.

:::interactive_editor

```html
<iframe
  width="400"
  height="400"
  src="https://www.youtube.com/embed/PkZNo7MFNFg?si=-UBVIUNM3csdeiWF"
  title="Learn JavaScript - Full Course for Beginners (YouTube video)"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
  referrerpolicy="strict-origin-when-cross-origin"
  allowfullscreen
></iframe>
```

:::

`src` 屬性指定了要嵌入的頁面的 URL。 `width` 屬性指定 `iframe` 的寬度。 `height` 屬性指定 `iframe` 的高度。 `allowfullscreen` 屬性允許用戶以全屏模式顯示 `iframe` 。 爲 `iframe` 指定 `title` 屬性也是一種好的做法，因爲它對可訪問性非常重要。

注意，視頻可以來自任何地方。 它不一定要來自 YouTube 和 Vimeo 等視頻服務。

別忘了，你還可以在 `iframe` 元素中嵌入地圖、其他網頁或直接嵌入 HTML。 Here is an example of an embedded map.

Try interacting with the map by zooming in and out.

:::interactive_editor

```html
<h1>A Map from Openstreetmap.org Embedded with the iframe Element</h1>

<iframe
  width="425"
  height="350"
  src="https://www.openstreetmap.org/export/embed.html?bbox=3.006134033203125%2C6.150112578753815%2C3.6357879638671875%2C6.749850810550778&amp;layer=mapnik"
  title="Map of Lagos area, Nigeria"
  style="border: 1px solid black"
>
</iframe>
<br />
<small>
  <a href="https://www.openstreetmap.org/#map=11/6.4501/3.3210">
    View Larger Map
  </a>
</small>
```

:::

如果你想在 `iframe` 元素中直接嵌入 HTML，則必須使用 `srcdoc` 屬性而不是 `src`。

# --questions--

## --text--

iframe 是什麼意思？

## --answers--

國際框架。

### --feedback--

Review the beginning of the lesson to obtain the answer.

---

內聯表單。

### --feedback--

Review the beginning of the lesson to obtain the answer.

---

內聯框架。

---

內聯表單元素。

### --feedback--

Review the beginning of the lesson to obtain the answer.

## --video-solution--

3

## --text--

`iframe` 元素的哪個屬性指定了要嵌入內容的位置？

## --answers--

`src`

---

`url`

### --feedback--

想想表示 "源 "的屬性。

---

`frameborder`

### --feedback--

Think about the attribute that means "source".

---

`cross-origin`

### --feedback--

Think about the attribute that means "source".

## --video-solution--

1

## --text--

如果要直接嵌入 HTML，應使用 `iframe` 元素的哪個屬性而不是 `src`？

## --answers--

`html`

### --feedback--

找出與 src 相關的屬性。

---

`document`

### --feedback--

Look out for the attribute that is related to src.

---

`srcweb`

### --feedback--

Look out for the attribute that is related to src.

---

`srcdoc`

## --video-solution--

4
