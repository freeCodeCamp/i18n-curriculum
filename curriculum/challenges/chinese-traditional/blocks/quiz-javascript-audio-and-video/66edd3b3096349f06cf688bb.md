---
id: 66edd3b3096349f06cf688bb
title: JavaScript 音訊與影片測驗
challengeType: 8
dashedName: quiz-javascript-audio-and-video
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript 中 `Audio()` 建構式的用途是什麼？

#### --distractors--

創建一個新的音訊檔案。

---

播放現有的音訊檔案。

---

修改音訊檔案。

#### --answer--

創建一個新的 `audio` 元素。

### --question--

#### --text--

哪一個方法用於開始播放 `audio` 或 `video` 元素？

#### --distractors--

`start()`

---

`begin()`

---

`resume()`

#### --answer--

`play()`

### --question--

#### --text--

`pause()` 和 `play()` 方法在媒體元素上有什麼差異？

#### --distractors--

`pause()` 傳回一個 Promise，而 `play()` 則不會。

---

`pause()` 無法恢復，而 `play()` 可以從任何位置開始。

---

`pause()` 會自動重設到開頭，而 `play()` 則從音訊暫停的地方開始。

#### --answer--

`pause()` 停止播放但維護當前位置，而 `play()` 則開始播放。

### --question--

#### --text--

哪種音訊格式擁有最廣泛的瀏覽器支援？

#### --distractors--

`FLAC`

---

`OGG`

---

`WAV`

#### --answer--

`MP3`

### --question--

#### --text--

`audio` 或 `video` 元素中的 `source` 元素的用途是什麼？

#### --distractors--

指定媒體的音量。

---

為你的媒體添加字幕。

---

連結到外部音訊處理程序。

#### --answer--

為不同格式指定多個媒體 `source`。

### --question--

#### --text--

MP4 格式可能有哪兩種 MIME 型別？

#### --distractors--

`mp4/audio` 和 `mp4/video`

---

`audio/mpeg4` 和 `video/mpeg4`

---

`mp4/mpeg` 和 `mp4/stream`

#### --answer--

`audio/mp4` 和 `video/mp4`

### --question--

#### --text--

在音訊和影片的背景關係中，`codec` 的作用是什麼？

#### --distractors--

增加媒體的音量。

---

變更媒體的檔案格式。

---

加密媒體以確保安全傳輸。

#### --answer--

在類比和數位格式之間轉換媒體。

### --question--

#### --text--

哪一個 API 提供在網頁應用程式中創建、處理程序和分析音訊的方法？

#### --distractors--

音訊處理 API。

---

聲音操作 API。

---

媒體資料流 API。

#### --answer--

Web Audio API。

### --question--

#### --text--

`getUserMedia()` 方法的目的為何？

#### --distractors--

使用媒體服務驗證使用者。

---

取用使用者對媒體播放的偏好設定。

---

將媒體檔案下載到使用者的裝置。

#### --answer--

存取使用者的相機和／或麥克風。

### --question--

#### --text--

`HTMLMediaElement` 介面的哪個屬性表述目前的播放時間？

#### --distractors--

`time`

---

`playbackPosition`

---

`currentPosition`

#### --answer--

`currentTime`

### --question--

#### --text--

以下程式碼片段的作用是什麼？

```javascript
const audio = new Audio('song.mp3');
audio.loop = true;
```

#### --distractors--

播放該音訊檔案一次。

---

增加音訊的音量。

---

在一次迴圈後暫停音訊。

#### --answer--

將音訊設定為無限重複。

### --question--

#### --text--

MIME 的完整意義是什麼？

#### --distractors--

多重網際網路訊息擴充套件

---

郵件網際網路多媒體擴充套件

---

多媒體網際網路郵件延伸功能

#### --answer--

多用途網際網路郵件擴充標準

### --question--

#### --text--

`canPlayType()` 方法幫助你做什麼？ 

#### --distractors--

檢查瀏覽器中是否正在以特定格式播放音訊。

---

驗證媒體檔案是否存在於瀏覽器的快取中且可供播放存取。

---

測試 `audio` 元素是否啟用了 `controls`，以及使用者是否具有與之互動的適當權限。

#### --answer--

判斷瀏覽器是否可能能播放你的特定 `audio` 格式。

### --question--

#### --text--

以下哪一項不是 `HTMLMediaElement` API 的有效事件？

#### --distractors--

`waiting`

---

`canplay`

---

`ended`

#### --answer--

`stopped`

### --question--

#### --text--

當因資料緩衝而暫停播放時，會觸發哪個 `HTMLMediaElement` 事件？

#### --distractors--

`stopped`

---

`pause`

---

`buffering`

#### --answer--

`waiting`

### --question--

#### --text--

哪一個 Web Audio API 介面用來表述音訊處理程序圖？

#### --distractors--

`AudioNode`

---

`AudioBuffer`

---

`AudioListener`

#### --answer--

`AudioContext`

### --question--

#### --text--

Media Source Extensions API 的目的為何？

#### --distractors--

為你的影片添加視覺效果。

---

在不同媒體格式之間轉換。

---

啟用 JavaScript 以同步多個音訊軌道。

#### --answer--

它讓你直接將使用者的網路攝影機串流傳遞到 `video` 元素。

### --question--

#### --text--

`ended` 事件何時觸發？

#### --distractors--

當媒體原始碼被更改時。

---

當呼叫 `pause()` 方法時。

---

當媒體被手動停止時。

#### --answer--

當媒體結束時。

### --question--

#### --text--

螢幕擷取 API 如何被暴露？

#### --distractors--

透過 `window.screenCapture` 介面

---

透過使用 `navigator.capture` 屬性

---

透過使用 `captureScreen()` 方法

#### --answer--

透過呼叫 `getDisplayMedia()` 方法。

### --question--

#### --text--

WMV 格式與什麼相關？

#### --distractors--

寬媒體影片

---

世界媒體影片

---

網頁媒體播放器

#### --answer--

Windows Media Player
