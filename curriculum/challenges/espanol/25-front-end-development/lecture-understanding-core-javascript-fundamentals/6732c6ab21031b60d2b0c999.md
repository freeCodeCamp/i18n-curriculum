---
id: 6732c6ab21031b60d2b0c999
title: '¿Cuáles son algunas prácticas comunes para nombrar variables y funciones?'
challengeType: 11
videoId: wheRZCpE724
dashedName: what-are-some-common-practices-for-naming-variables-and-functions
---

# --description--

Mira el video o lee la transcripción y responde las preguntas a continuación.

# --transcript--

¿Cuáles son algunas prácticas comunes para nombrar variables y funciones?

Nombrar variables y funciones es un aspecto importante de escribir código limpio, legible y mantenible. Las buenas prácticas de nomenclatura hacen que tu código se autocomente, reduciendo la necesidad de comentarios extensos y facilitando que otros desarrolladores, incluido tu futuro yo, entiendan tu código.

Comencemos con las convenciones generales de nomenclatura en JavaScript. En videos de conferencias previos, aprendiste sobre el uso de Camel Case para nombres de variables. Para las variables booleanas, es una práctica común usar prefijos como `is`, `has` o `can`. Esto le indica inmediatamente al lector que la variable es un booleano:

```js
let isLoading = true;
let hasPermission = false;
let canEdit = true;
```

Para las funciones, el nombre debe indicar claramente lo que hace la función. A menudo es útil comenzar con un verbo:

```js
function getUserData(){
  /* ... */
}

function calculateTotal(){
  /* ... */
}

function validateInput(){
  /* ... */
}
```

Para las funciones que devuelven un booleano, a menudo llamadas predicados, puedes usar los mismos prefijos `is`, `has` o `can`:

```js
function isValidEmail(email) {
  /* ... */
}

function hasRequiredFields(form) {
  /* ... */
}
```

Cuando tienes funciones que obtienen datos, es común comenzar con la palabra `get`:

```js
function getProductDetails(productId) {
  /* ... */
}

function getUserProfile(userId) {
  /* ... */
}
```

Cuando tienes funciones que asignan datos, es común comenzar con la palabra `set`:

```js
function setUserPreferences(preferences) {
  /* ... */
}

function setPageTitle(title) {
  /* ... */
}
```

Para las funciones manejadoras de eventos, podrías utilizar el prefijo `handle` o el sufijo `handler`:

```js
function handleClick(){
  /* ... */
}

function onSubmit(){
  /* ... */
}

function keyPressHandler(){
  /* ... */
}
```

Un manejador de eventos es una acción que ocurre después de que ha sucedido un evento, como un clic de botón. Aprenderás sobre eso en futuros videos de conferencias.

Al nombrar variables iteradoras y bucles, es común usar letras individuales como `i`, `j` o `k`, pero para bucles anidados o iteraciones más complejas, nombres más descriptivos pueden ser útiles:

```js
for (let i = 0; i < array.length; i++) {
  /* ... */
}

for (let studentIndex = 0; studentIndex < students.length; studentIndex++) {
  /* ... */
}
```

Para nombres de arreglos, considera usar sustantivos en plural para indicar que la variable contiene múltiples elementos:

```js
const colors = ['red', 'green', 'blue'];
const userNames = ['Alice', 'Bob', 'Charlie'];
```

Recuerda que el objetivo es hacer que tu código sea lo más autoexplicativo posible. Una buena regla general es que si necesitas agregar un comentario para explicar qué hace una variable o función, podrías considerar renombrarla a algo más descriptivo.

Por último, sé consistente con tu base de código o equipo. Si tu equipo ha establecido convenciones de nomenclatura, adhiérete a ellas. La consistencia hace que el código sea más legible y mantenible para todos los involucrados.

# --questions--

## --text--

¿Cuál de las siguientes es un buen nombre para una variable booleana que verifica si un usuario ha iniciado sesión?

## --answers--

`user_logged`

### --feedback--

Piensa en las convenciones que discutimos para las variables booleanas.

---

`checkLogin`

### --feedback--

Piensa en las convenciones que discutimos para las variables booleanas.

---

`isLoggedIn`

---

`login`

### --feedback--

Piensa en las convenciones que discutimos para las variables booleanas.

## --video-solution--

3

## --text--

¿Cuál es una buena convención de nomenclatura para una función que recupera información del producto?

## --answers--

`productInformation`

### --feedback--

Recuerda el prefijo que típicamente usamos para las funciones que obtienen datos.

---

`getProductInfo`

---

`retrieveProduct`

### --feedback--

Recuerda el prefijo que típicamente usamos para las funciones que obtienen datos.

---

`prod_info`

### --feedback--

Recuerda el prefijo que típicamente usamos para las funciones que obtienen datos.

## --video-solution--

2

## --text--

¿Cuál de las siguientes acciones deberías realizar si estás trabajando en una base de código establecida y/o con un equipo?

## --answers--

Usa la función de busca y reemplaza de tu editor de texto para actualizar los nombres de funciones o variables que encuentres que podrían mejorarse, incluso si no está relacionado con la tarea en la que estás trabajando.

### --feedback--

Recuerda que esta conferencia cubre algunas convenciones de nombres, y que cada base de código podría tener sus propias convenciones establecidas.

---

Adhiérete a las convenciones de nombres para variables y funciones que ya han sido establecidas.

---

Crea tus propias convenciones de nombres para cualquier nuevo código que escribas.

### --feedback--

Recuerda que esta conferencia cubre algunas convenciones de nombres, y que cada base de código podría tener sus propias convenciones establecidas.

---

Usa estrictamente las convenciones de nombres cubiertas en esta conferencia.

### --feedback--

Recuerda que esta conferencia cubre algunas convenciones de nombres, y que cada base de código podría tener sus propias convenciones establecidas.

## --video-solution--

2
