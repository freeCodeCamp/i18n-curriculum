---
id: 692487767b6b728454f53f29
title: Quiz dos Módulos Core do NodeJS
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual método `fs` adiciona conteúdo a um arquivo existente sem sobrescrevê-lo?

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

O que acontece quando você usa métodos síncronos do módulo `fs`?

#### --distractors--

Eles deletam arquivos existentes automaticamente.

---

Eles executam mais rápido do que os assíncronos em todos os casos.

---

Eles retornam Promises por padrão.

#### --answer--

Eles bloqueiam o programa até que a operação termine.

### --question--

#### --text--

O que o parâmetro `utf8` faz em `fs.readFile()`?

#### --distractors--

Ele previne que erros sejam lançados.

---

Ele altera a extensão do arquivo.

---

Ele comprime arquivos grandes.

#### --answer--

Ele instrui o Node a retornar uma string legível em vez de um buffer.

### --question--

#### --text--

Qual método `Buffer` cria um novo buffer preenchido com zeros?

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

O que `Buffer.toString()` faz?

#### --distractors--

Converte um buffer em código binário.

---

Limpa o conteúdo de um buffer.

---

Mostra o comprimento em bytes do buffer.

#### --answer--

Converte o conteúdo do buffer em uma string legível.

### --question--

#### --text--

Qual recurso `crypto` requer uma chave privada para gerar uma assinatura?

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

Por que você deve evitar escrever seus próprios algoritmos de criptografia diretamente?

#### --distractors--

É ilegal em aplicações Node.js.

---

Node.js não suporta criptografia.

---

JavaScript não pode calcular hashes.

#### --answer--

Pode ser inseguro sem bibliotecas bem testadas.

### --question--

#### --text--

Qual método `crypto` é recomendado para gerar tokens aleatórios seguros em vez de `Math.random()`?

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

Qual método do módulo OS retorna a arquitetura da CPU?

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

O que `os.userInfo()` retorna?

#### --distractors--

Apenas o nome de usuário.

---

Apenas o endereço IP do dispositivo.

---

Apenas o ID do usuário logado.

#### --answer--

Informações sobre o usuário atual do sistema.

### --question--

#### --text--

Qual método `path` junta segmentos e normaliza barras extras?

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

Qual método `path` produz um caminho absoluto baseado no diretório de trabalho atual?

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

O que a variável global `__dirname` representa?

#### --distractors--

O nome do usuário que está executando o Node.

---

A extensão do arquivo atual.

---

Um caminho de pasta relativo começando com `./`.

#### --answer--

O caminho absoluto do diretório que contém o arquivo atual.

### --question--

#### --text--

Qual propriedade do processo contém variáveis de ambiente como `NODE_ENV`?

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

O que o evento `process.on("exit")` faz?

#### --distractors--

Ele reinicia a aplicação Node.

---

Ele envia um sinal `exit` para os processos filhos.

---

Ele limpa a fila do event loop.

#### --answer--

Executa o código logo antes do processo Node terminar.

### --question--

#### --text--

Qual tipo de stream pode tanto ler quanto escrever dados mas não os transforma?

#### --distractors--

Stream legível

---

Transformar stream

---

Stream gravável

#### --answer--

Fluxo duplex

### --question--

#### --text--

O que é necessário para implementar a leitura e escrita eficiente de arquivos grandes?

#### --distractors--

Usando apenas `Buffer.alloc()`

---

Importando o módulo `crypto`

---

Salvando o arquivo inteiro na memória primeiro

#### --answer--

Usando streams para processar dados em pedaços

### --question--

#### --text--

Qual método conecta um stream legível a um stream gravável?

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

Qual evento de stream indica que não há mais dados para escrever?

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

O que é registrado se você imprimir um chunk de um readable stream sem `toString()`?

#### --distractors--

Conteúdo de texto simples

---

O PID do processo

---

Nome do sistema operacional

#### --answer--

Um objeto buffer com dados binários
