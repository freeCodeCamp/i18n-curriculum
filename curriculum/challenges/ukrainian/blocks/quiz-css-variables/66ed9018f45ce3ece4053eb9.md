---
id: 66ed9018f45ce3ece4053eb9
title: Тест з CSS-змінних
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

Щоб пройти тест, потрібно правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який із наведених описів НЕ є дійсним у правилі `@property`?

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

У наведеному коді, яке значення матиме `color`, якщо `--main-color` не визначено?

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

Колір не буде застосовано.

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

Який із наведених способів є дійсним для оголошення кастомної властивості в CSS?

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

Яка основна мета використання кастомних властивостей у CSS?

#### --distractors--

Змінити структуру DOM.

---

Створити динамічні селектори.

---

Покращити доступність.

#### --answer--

Визначити повторно використовувані стилі.

### --question--

#### --text--

Що представляє селектор `:root`?

#### --distractors--

Перший дочірній елемент `body`.

---

Батьківський елемент усіх `header` елементів.

---

Перший дочірній елемент тегу `html`.

#### --answer--

Елемент найвищого рівня в дереві DOM.

### --question--

#### --text--

Як застосувати кастомну властивість `--foreground` як `color` у CSS?

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

Яка мета правила `@property` у CSS?

#### --distractors--

Визначити медіазапит.

---

Керувати CSS-анімаціями та їхнім часом.

---

Групувати CSS-властивості в одному правилі.

#### --answer--

Визначити, як поводяться кастомні властивості.

### --question--

#### --text--

При оголошенні кастомної `@property`, яка мета `syntax` у її визначенні?

#### --distractors--

Вказати значення за замовчуванням для кастомної властивості.

---

Визначити, чи може властивість наслідуватися дочірніми елементами.

---

Визначити, чи застосовується властивість до всіх елементів.

#### --answer--

Забезпечити конкретний тип даних або шаблон значення для кастомної властивості.

### --question--

#### --text--

На що слід звертати увагу при використанні кастомних властивостей?

#### --distractors--

Кастомні властивості автоматично конвертуються в одиниці `px`.

---

Кастомні властивості завжди заміщують рядкові стилі.

---

Кастомні властивості не можна використовувати для встановлення значень шрифтів.

#### --answer--

Кастомні властивості можуть не підтримуватися в старих браузерах.

### --question--

#### --text--

Яка мета надання запасного значення у функції `var()` для кастомних властивостей CSS?

#### --distractors--

Зменшити обсяг CSS-коду.

---

Покращити продуктивність у сучасних браузерах.

---

Оптимізувати час відтворення на повільних мережах.

#### --answer--

Гарантувати застосування дійсного значення, якщо кастомна властивість не визначена.

### --question--

#### --text--

Що відбувається з кастомною властивістю `--foreground`, визначеною всередині медіазапиту, коли медіазапит більше не відповідає поточному вікну перегляду?

#### --distractors--

Кастомна властивість зберігається і продовжує застосовуватися.

---

Кастомна властивість повертається до свого ініціального значення.

---

Кастомна властивість перераховується залежно від вікна перегляду.

#### --answer--

Кастомна властивість більше недоступна, оскільки вона обмежена областю медіазапиту.

### --question--

#### --text--

Як поводитиметься наступне оголошення?

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

Застосує `--main-color`, навіть якщо воно не визначене, використовуючи це значення за замовчуванням.

---

Застосує `--main-color` і `--fallback-color` одночасно, створюючи змішування двох кольорів.

---

Завжди використовуватиме `#000`, незалежно від того, чи визначені `--main-color` або `--fallback-color`.

#### --answer--

Застосує `--main-color`, якщо визначене; інакше перевірить `--fallback-color;`, а якщо обидва не визначені, використає `#000`.

### --question--

#### --text--

Яка перевага використання `@property` у CSS?

#### --distractors--

Покращує продуктивність за рахунок попереднього обчислення значень кастомних властивостей.

---

Автоматично додає префікси кастомним властивостям для кращої підтримки браузерами.

---

Обмежує використання кастомних властивостей певними елементами.

#### --answer--

Дозволяє анімацію кастомних властивостей.

### --question--

#### --text--

Що контролює властивість `inherits` у визначенні кастомної `@property`?

#### --distractors--

Чи матиме кастомна властивість ініціальне значення.

---

Чи може властивість мати скорочену версію.

---

Чи приймає властивість запасні значення.

#### --answer--

Чи передається значення кастомної властивості дочірнім елементам.

### --question--

#### --text--

Що вказує `@property` у визначенні кастомної `initial-value`?

#### --distractors--

Допустимі значення, які може приймати властивість.

---

Пріоритет властивості в каскаді.

---

Тип значення, яке має мати властивість.

#### --answer--

Запасне значення для властивості.

### --question--

#### --text--

Враховуючи наведений HTML і CSS, яке значення матиме властивість `color` для елемента `.box`?

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

До якої властивості слід застосовувати CSS-градієнт?

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

Яка мета зупинок кольору у CSS-градієнтах?

#### --distractors--

Визначити рівень прозорості градієнта.

---

Вказати напрямок градієнта.

---

Повторювати градієнт за фіксованим шаблоном.

#### --answer--

Визначити конкретні точки, де кольори змінюються у градієнті.

### --question--

#### --text--

Що відбувається, якщо в CSS-лінійному градієнті не вказано кут або напрямок?

#### --distractors--

Градієнт за замовчуванням спрямований по діагоналі.

---

Градієнт за замовчуванням рухається знизу вгору.

---

Градієнт за замовчуванням рухається зліва направо.

#### --answer--

Градієнт за замовчуванням рухається зверху вниз.

### --question--

#### --text--

Яка CSS-функція градієнта дозволяє створити градієнт, що розходиться від центральної точки?

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

Який із наведених способів є правильним для оголошення кастомної властивості CSS?

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

Який селектор зазвичай використовується для визначення глобальних кастомних властивостей CSS?

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

Чому рекомендується включати запасне значення при використанні `var()`?

#### --distractors--

Це гарантує правильну анімацію змінної.

---

Це уникає завантаження зовнішніх таблиць стилів.

---

Це запобігає перерахунку браузера.

#### --answer--

Це гарантує дійсне значення, якщо кастомна властивість не визначена.

### --question--

#### --text--

Який правильний синтаксис для застосування кастомної властивості як кольору фону?

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

Яке CSS-правило дозволяє розробникам визначати кастомні властивості з більшим контролем над їхньою поведінкою?

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

Що контролює поле `inherits` у визначенні `@property`?

#### --distractors--

Чи використовується властивість у JavaScript.

---

Чи викликає властивість перефарбування.

---

Чи може властивість містити функції.

#### --answer--

Чи передається значення властивості дочірнім елементам.

### --question--

#### --text--

При визначенні анімованого кута градієнта за допомогою `@property`, який синтаксис слід використовувати?

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

Чим відрізняється кастомна властивість CSS від стандартної у способі її визначення?

#### --distractors--

Стандартні CSS-властивості визначаються одним дефісом, а кастомні — двома.

---

Стандартні CSS-властивості використовують `var()` для визначення, кастомні — ні.

---

Стандартні CSS-властивості визначаються за правилом `@property`, кастомні — ні.

#### --answer--

Кастомні властивості мають починатися з двох дефісів (`--`), стандартні — ні.

### --question--

#### --text--

Яка перевага використання кастомних властивостей CSS?

#### --distractors--

Вони зменшують потребу в коментарях CSS.

---

Вони автоматично оптимізують зображення.

---

Вони роблять змінні JavaScript зайвими.

#### --answer--

Вони дозволяють повторно використовувати стилі та централізовано їх підтримувати.

### --question--

#### --text--

Яка CSS-властивість може динамічно змінюватися залежно від медіазапитів за допомогою кастомних властивостей?

#### --distractors--

Тільки `background-color`

---

Тільки `font-family`

---

Тільки `z-index`

#### --answer--

Будь-яка властивість, що приймає значення.

### --question--

#### --text--

У контексті CSS-змінних, яку роль відіграє `initial-value` у правилі `@property`?

#### --distractors--

Встановлює мінімальне значення властивості.

---

Визначає максимальне значення для анімацій.

---

Змінює пріоритет селектора.

#### --answer--

Призначає значення за замовчуванням для властивості, якщо воно не встановлене.

### --question--

#### --text--

Розгляньте наведені HTML і CSS. Який колір фону буде застосовано до елемента `.card`?

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

Що станеться, якщо користувач спробує призначити недійсне значення `--padding` (наприклад, колір замість довжини) у наведеному CSS?

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

Браузер прийме значення, але ігноруватиме його під час макету.

---

Браузер викине помилку виконання.

---

Браузер автоматично конвертує значення у дійсну довжину.

#### --answer--

Браузер повернеться до ініціального значення властивості.

### --question--

#### --text--

Яка мета кастомної властивості `--gradient-angle` у цьому прикладі?

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

Визначає швидкість переходу градієнта.

---

Вказує розмір градієнта.

---

Встановлює режим змішування кольорів.

#### --answer--

Керує напрямком градієнта.

### --question--

#### --text--

Який сценарій найкраще демонструє перевагу використання кастомних властивостей для тем?

#### --distractors--

Використання їх лише для розмірів шрифтів.

---

Застосування їх лише до `:hover`.

---

Обмеження їх одним CSS-класом.

#### --answer--

Зміна значень через клас.

### --question--

#### --text--

Чому розробники можуть віддавати перевагу використанню `:root` для визначення CSS-змінних?

#### --distractors--

Тому що `:root` покращує швидкість завантаження сторінки.

---

Тому що `:root` вимикає специфічність.

---

Тому що `:root` потрібен для JavaScript.

#### --answer--

Тому що `:root` дозволяє властивостям мати глобальну область видимості.

### --question--

#### --text--

Що зробить наведений CSS, якщо `--secondary-color` не визначено?

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

Застосує `black`.

---

Ігноруватиме властивість `color`.

---

Застосує `--secondary-color`.

#### --answer--

Застосує `orange`.

### --question--

#### --text--

Які типи значень представляє `<color>` у полі синтаксису `@property`?

#### --distractors--

Кути, як `90deg`.

---

Довжини, як `10px`.

---

Відсотки, як `50%`.

#### --answer--

Кольорові значення, як `#ff0000` або `red`.

### --question--

#### --text--

Яка основна перевага використання кастомних властивостей у поєднанні з `@property`?

#### --distractors--

Вони запобігають необхідності запасних значень.

---

Вони примушують статичний макет.

---

Вони зменшують потребу в класах.

#### --answer--

Вони дозволяють анімацію властивостей.

### --question--

#### --text--

Що робить `var(--undefined-property, fallback)`, коли кастомна властивість не визначена?

#### --distractors--

Викидає помилку і припиняє застосування стилів.

---

Ігнорує все CSS-правило.

---

Застосовує ім’я змінної як є.

#### --answer--

Застосовує замість цього запасне значення.
