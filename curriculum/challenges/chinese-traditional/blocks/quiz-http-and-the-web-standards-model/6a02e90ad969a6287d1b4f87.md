---
id: 6a02e90ad969a6287d1b4f87
title: HTTP 與網頁標準模型測驗
challengeType: 8
dashedName: quiz-http-and-the-web-standards-model
---

# --description--

要通過此測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

DNS 代表什麼？它的主要功能是什麼？

#### --distractors--

資料命名系統，它會在網路傳送前加密網域名稱。

---

網域節點伺服器，它儲存網站的檔案，讓瀏覽器能下載。

---

動態網路服務，當裝置連線時會指派 IP 位址。

#### --answer--

網域名稱系統，它將人類可讀的網域名稱轉換成 IP 位址。

### --question--

#### --text--

在 DNS 決議過程中，哪個組件會透過根伺服器、頂級網域伺服器和權威名稱伺服器進行反覆查詢？

#### --distractors--

權威名稱伺服器

---

頂級網域伺服器

---

根伺服器

#### --answer--

遞迴解析器

### --question--

#### --text--

TCP 做了什麼是網際網路協定（IP）本身無法做到的？

#### --distractors--

它使用 IP 位址將封包從來源路由到目的地。

---

它在網路傳送前加密資料。

---

它將網域名稱指派給 IP 位址。

#### --answer--

它透過編號分段並要求重傳遺失封包，確保可靠且有序的傳送。

### --question--

#### --text--

應該使用哪個 HTTP 方法來部分更新伺服器上的既有資源？

#### --distractors--

`PUT`

---

`POST`

---

`DELETE`

#### --answer--

`PATCH`

### --question--

#### --text--

用於回應 JSON 的正確 `Content-Type` 標頭是什麼？

#### --distractors--

`text/json`

---

`text/plain`

---

`application/xml`

#### --answer--

`application/json`

### --question--

#### --text--

哪個 HTTP 狀態碼表示請求的資源已永久移動到新 URL？

#### --distractors--

`302`

---

`304`

---

`404`

#### --answer--

`301`

### --question--

#### --text--

`401 Unauthorized` 與 `403 Forbidden` 回應有什麼差別？

#### --distractors--

`401` 表示伺服器當機，而 `403` 表示資源不存在。

---

`401` 是用戶端錯誤，而 `403` 是伺服器錯誤。

---

`401` 表示資源永久移動，而 `403` 表示暫時移動。

#### --answer--

`401` 表示需要認證，而 `403` 表示用戶端有有效認證但無權限存取資源。

### --question--

#### --text--

HTTP 請求的哪一部分包含方法、路徑和 HTTP 版本？

#### --distractors--

回應主體

---

請求主體

---

請求標頭

#### --answer--

請求列

### --question--

#### --text--

`304 Not Modified` 狀態碼告訴用戶端什麼？

#### --distractors--

資源已從伺服器刪除。

---

請求格式錯誤，應重新送出。

---

伺服器暫時無法使用。

#### --answer--

資源自上次請求後未改變，用戶端應使用快取版本。

### --question--

#### --text--

在 HTTP 請求-回應週期中，伺服器處理請求後緊接著會發生什麼？

#### --distractors--

用戶端發送第二個請求以確認收到。

---

DNS 解析器快取回應。

---

用戶端啟動新的 TCP 連線。

#### --answer--

伺服器準備包含狀態碼、標頭和選用主體的回應。

### --question--

#### --text--

哪個 HTML 屬性指定表單提交時資料送往何處？

#### --distractors--

`method`

---

`name`

---

`type`

#### --answer--

`action`

### --question--

#### --text--

即使已經有用戶端驗證，為什麼仍必須執行伺服器端驗證？

#### --distractors--

現代瀏覽器不支援用戶端驗證。

---

伺服器端驗證只用於檔案上傳。

---

用戶端驗證比伺服器端驗證快。

#### --answer--

用戶端驗證可以被繞過，因此為了安全必須執行伺服器端驗證。

### --question--

#### --text--

哪個標準組織維護 HTML 和 DOM，並持續更新為活文件標準？

#### --distractors--

W3C

---

ECMA International

---

Khronos Group

#### --answer--

WHATWG

### --question--

#### --text--

TC39 是什麼？它在網頁標準生態系中扮演什麼角色？

#### --distractors--

W3C 的工作群組，負責 CSS 規格。

---

瀏覽器廠商聯盟，決定 Chrome 中的功能。

---

Khronos Group 委員會，管理 WebGL 標準。

#### --answer--

ECMA International 內的技術委員會，透過五階段提案流程推動 JavaScript 語言功能。

### --question--

#### --text--

在網頁標準生命週期的哪個階段，功能會透過公開討論、收集使用案例和競爭設計方案進行壓力測試？

#### --distractors--

規格測試

---

最終測試

---

相容性測試

#### --answer--

孵化階段

### --question--

#### --text--

W3C 網頁平台測試套件在標準制定過程中的目的是什麼？

#### --distractors--

為行銷目的排名瀏覽器效能。

---

取代書面規格的需求。

---

讓開發者提交新功能提案。

#### --answer--

驗證每個瀏覽器是否以相同方式實作功能。

### --question--

#### --text--

哪個網頁標準原則確保依規格建置的頁面在 Chrome、Firefox 和 Safari 中行為一致？

#### --distractors--

向下相容性

---

漸進增強

---

開放性

#### --answer--

相容性

### --question--

#### --text--

漸進增強原則在設計網頁功能時要求什麼？

#### --distractors--

功能必須只針對最新瀏覽器版本。

---

功能必須先用 JavaScript 實作，再用 CSS 或 HTML。

---

功能必須完成定案後才可由任何瀏覽器實作。

#### --answer--

功能必須先對所有人可用，然後為擁有更強瀏覽器或裝置的使用者提供增強。

### --question--

#### --text--

為什麼網頁在引入新標準時幾乎完全維持向下相容？

#### --distractors--

新標準總是取代舊標準，因此舊頁面會自動更新。

---

瀏覽器廠商依法必須支援所有已發佈頁面。

---

舊頁面被封存並從獨立網路提供。

#### --answer--

讓多年以前建置的頁面在現代瀏覽器中仍能正常運作。

### --question--

#### --text--

哪個 HTTP 狀態碼類別用於伺服器端錯誤？

#### --distractors--

`2xx`

---

`3xx`

---

`4xx`

#### --answer--

`5xx`
