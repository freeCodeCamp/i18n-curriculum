---
id: 67358ac128957c865dcf3ddf
title: JavaScript 类测验
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript 中类的目的是什么？

#### --distractors--

仅定义属性，不定义方法。

---

仅定义静态变量。

---

仅组织原语数据。

#### --answer--

创建可重用的对象蓝图。

### --question--

#### --text--

你如何在 JavaScript 中定义一个类？

#### --distractors--

使用 `className` 关键字。

---

使用 `object` 关键字。

---

仅使用 `constructor` 关键字。

#### --answer--

使用 `class` 关键字。

### --question--

#### --text--

在一个类中，使用哪个方法来初始化属性？

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

`class` 构造函数内部的 `this` 表现什么？

#### --distractors--

全局的类对象。

---

函数本身。

---

一个未定义的引用。

#### --answer--

类的一个实例。

### --question--

#### --text--

以下哪项描述了 `this` 在类方法内部的箭头函数中的行为？

#### --distractors--

它从不同作用域中最近的函数继承 `this` 的值。

---

它指的是该类的新建实例。

---

它在严格模式下从全局的对象继承 `this` 的值。

#### --answer--

它继承了定义它的封闭作用域中 `this` 的值。

### --question--

#### --text--

`extends` 关键字的主要用途是什么？

#### --distractors--

在父类构造函数中定义静态方法。

---

在一个类中创建一个方法。

---

初始化父类中的默认属性。

#### --answer--

创建一个继承自父类的子类。

### --question--

#### --text--

以下哪项是如何创建一个继承自 `Vehicle` 类的 `Car` 类的示例？

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

下面代码的输出结果是什么？

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

出现错误。

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

子类中 `super` 关键字的作用是什么？

#### --distractors--

定义父类的一个新实例。

---

从父类中删除方法和属性。

---

仅访问父类的静态方法。

#### --answer--

调用父类的构造函数或方法。

### --question--

#### --text--

以下哪项描述了静态方法？

#### --distractors--

一个在类内部无法访问的方法。

---

仅在箭头函数中使用的方法。

---

每个类实例独有的方法。

#### --answer--

属于类本身而不是任何实例的方法。

### --question--

#### --text--

下面的代码会输出什么？

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

出现错误。

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

你如何访问 JavaScript 类中的静态属性？

#### --distractors--

通过在方法中使用 `this.propertyName`。

---

通过在创建实例后使用 `instance.propertyName`。

---

通过在子类中使用 `super.propertyName`。

#### --answer--

通过使用 `ClassName.propertyName`。

### --question--

#### --text--

在什么情况下你会使用静态 `method` 而不是实例 `method`？

#### --distractors--

当你需要访问和修改实例的属性时。

---

当方法需要在对象实例上调用时。

---

当该方法不依赖于实例特定的数据且操作类级别的数据时。

#### --answer--

当该方法操作由该类所有实例共享的数据时。

### --question--

#### --text--

下面的代码会输出什么？

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

会报错。

### --question--

#### --text--

继承另一个类的属性和方法使用什么关键字？

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

以下代码打印了什么？

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

出现错误。

#### --answer--

`Human`

### --question--

#### --text--

以下哪项最能描述继承？

#### --distractors--

它允许一个方法返回一个新建对象。

---

它允许一个实例定义它自己的属性。

---

它允许将静态属性转换为非静态属性。

#### --answer--

它允许一个类从另一个类继承属性和方法。

### --question--

#### --text--

如果子类中没有定义构造函数，会发生什么？

#### --distractors--

将抛出一个误差。

---

子类将继承代码中任何类的构造函数。

---

子类无法创建新实例。

#### --answer--

默认情况下将使用基类的构造函数。

### --question--

#### --text--

`this` 在以下代码中表现什么？

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

全局的 `object`。

---

`Animal` 类本身。

---

一个未定义的变量。

#### --answer--

`Animal` 的实例。

### --question--

#### --text--

静态属性的主要用途是什么？

#### --distractors--

为每个类实例定义默认属性。

---

在子类中覆写方法。

---

定义实例特定的数据。

#### --answer--

为跨实例共享的类级数据保存。
