---
id: 672990ecf71a852804ababe7
title: 什麼是表現型與語意型 HTML 之間的差異？
challengeType: 19
dashedName: what-is-the-difference-between-presentational-and-semantic-html
---

# --interactive--

呈現型 HTML 著重於內容的外觀和樣式。在 HTML 的早期，開發者會使用像是 `center`、`big` 和 `font` 元素。但在現代網頁開發中，你不應該使用這些類型的元素，因為它們的限制以及對可存取性和維護性的負面影響。

許多呈現用的 HTML 元素已被棄用，這表示它們已過時且不再建議使用。有更好的方法可以達成相同的結果。然而，知道它們的存在是有幫助的，所以我們將看看一些範例。

`font` 元素是一個已棄用的元素，用於設定文字的字型大小和顏色。以下是一個 `font` 元素的範例。

啟用互動式編輯器並將大小從 5 改為 7，以查看字型增大。

:::interactive_editor

```html
<font size="5" color="blue">This text is blue and large.</font>
```

:::

此範例將文字的顏色設定為 `blue`，並將大小設定為 `5`。`size` 屬性的範圍是從 `1` 到 `7`，其中 `1` 是最小值，`7` 是最大值。如果你沒有明確設定值，預設值為 `3`。

雖然此元素仍然有效，但你不應該使用它，因為字型大小和顏色應該始終在 CSS 中設定，而不是在 HTML 中設定。 

`center` 元素是另一個已棄用的元素，用於將內容在其容器中水平置中。以下是一個包含文字和段落元素的 `center` 元素範例。

啟用互動式編輯器，並在 `<p>Another example text.</p>` 周圍添加 `center` 頁籤，以便在頁面上置中顯示。

:::interactive_editor

```html
<center>
  This text is centered.
  <p>HTML is awesome.</p>
</center>

<p>Another example text.</p>
```

:::

接著，我們有 `big` 元素。這是另一個已棄用的呈現型 HTML 元素，會使包圍的文字比其周圍的文字大一層級。這裡有一個範例，定義了一個包含兩個零件的段落。

啟用互動式編輯器，並在 `Some other text` 周圍添加 `big` 頁籤，然後在預覽視窗中查看變更。 

:::interactive_editor

```html
<p>
  This text has a normal font size.
  <big>This text is larger.</big>
  Some other text.
</p>
```

:::

請記住，字型大小應該始終使用 CSS 設定，因此你不應該在現代 HTML 中使用此元素。

這些是呈現用的 HTML 元素範例。但它們全部都已被淘汰，不再建議使用。那麼你應該改用什麼呢？讓我們來看看。

語意化 HTML 現在是推薦的做法。它描述元素的內容，因此更容易閱讀、理解和維護。

當你使用語意化的 HTML 時，搜尋引擎可以輕鬆理解你的網站。這對於無障礙存取也很有幫助，因為螢幕閱讀器需要語意化的資訊來描述網頁上的內容。

語意化 HTML 元素的範例包括：

- 用於定義文件或區段標頭的 `header` 元素。
- 用於具有導覽聯結區段的導覽區段元素，`nav`。
- 用於群組相關資訊的 `section` 元素。
- 用於插圖和圖表的 `figure` 元素。

這是一個包含導覽區段元素的 `header` 元素範例。

啟用互動式編輯器，並在 `nav` 中添加一個 `<a href="#">Products</a>`，即可在預覽視窗中看到變更。

:::interactive_editor

```html
<header>
  <nav>
    <a href="#">Home</a>
    <a href="#">About</a>
    <!--Add the products link here-->

    <a href="#">Contact</a>
  </nav>
</header>
```

:::

語意元素清楚地顯示它們在 HTML 結構中的用途。 有許多不同的語意 HTML 元素。 你一定會找到一個符合你專案需求的元素。

做得很好。現在你知道了表現型和語意型 HTML 之間的差異：語意型 HTML 描述內容，而表現型 HTML 著重於外觀。

# --questions--

## --text--

下列哪一項最能描述表現型與語意型 HTML 之間的差異？

## --answers--

呈現型 HTML 著重於內容結構，而語意型 HTML 著重於外觀。

### --feedback--

思考「presentation」和「語意」的意義，以及它們與 HTML 元素的關係。

---

語意化 HTML 著重於內容結構，而呈現型 HTML 著重於外觀。

---

呈現型與語意型 HTML 之間沒有差異。

### --feedback--

思考「presentation」和「語意」的意義，以及它們與 HTML 元素的關係。

---

兩者都專注於樣式，但呈現用的 HTML 較為現代。

### --feedback--

思考「presentation」和「語意」的意義，以及它們與 HTML 元素的關係。

## --video-solution--

2

## --text--

以下哪一項是呈現型 HTML 的範例？

## --answers--

使用 `header` 元素來定義標頭。

### --feedback--

思考哪個元素會直接控制內容的外觀。

---

使用 `nav` 元素來定義導覽區段。

### --feedback--

思考哪個元素會直接控制內容的外觀。

---

使用 `article` 元素來定義獨立內容。

### --feedback--

思考哪個元素會直接控制內容的外觀。

---

使用 `center` 元素置中內容。

## --video-solution--

4

## --text--

以下哪一項是語意化 HTML 的範例？

## --answers--

使用 `<font color="red">` 使文字變成紅色。

### --feedback--

思考哪個元素描述內容的意義。

---

使用 `center` 元素置中內容。

### --feedback--

思考哪個元素描述內容的意義。

---

使用 `nav` 元素來表述導覽區段

---

使用 `big` 元素使文字變大。

### --feedback--

思考哪個元素描述內容的意義。

## --video-solution--

3
