---
id: 6733d83630e76f08ff49e6dc
title: 什么是 `HTMLMediaElement` API 及其工作原理？
challengeType: 19
dashedName: what-is-the-htmlmediaelement-api-and-how-does-it-work
---

# --description--

`HTMLMediaElement` API 是一个强大的工具，用于控件页面上音频和视频元素的行为。它扩展了基础的 `HTMLElement` 接口，因此你可以访问基础属性以及这些有用的方法。

你已经在之前关于 `Audio` 元素的课程中探索过一些方法，比如 `play()` 和 `pause()`。这些相同的方法也适用于视频元素。

还有一些其他有帮助的方法，例如 `addTextTrack()` 方法。此方法允许你为媒体元素指定一个文本轨道，这对于为视频添加字幕尤其有帮助。

或者 `fastSeek()` 方法，它允许你将播放位置移动到媒体中的特定时间。

`HTMLMediaElement` API 还提供了标准元素所没有的一些新建事件的访问。

当媒体开始和停止播放时，会触发 `play` 和 `pause` 事件。当媒体播放结束时，会触发 `ended` 事件。

当由于数据缓冲区而自动暂停播放时，会触发 `waiting` 事件。

当媒体可以部分播放或完全播放时，会触发 `canplay` 和 `canplaythrough` 事件。

`HTMLMediaElement` 为你的交互式媒体提供了大量的控件，而我们只是触及了表面。我鼓励你探索并尝试该 API，以找到将其集成到你的应用中的最佳方法。

# --questions--

## --text--

`addTextTrack()` 方法的作用是什么？

## --answers--

为媒体添加背景音乐。

### --feedback--

`addTextTrack()` 方法可用于为视频添加字幕。

---

允许你为媒体元素指定一个文本轨道。

---

创建一个新的音频轨道。

### --feedback--

`addTextTrack()` 方法可用于为视频添加字幕。

---

从 `video` 中移除现有字幕。

### --feedback--

`addTextTrack()` 方法可用于为视频添加字幕。

## --video-solution--

2

## --text--

当媒体可以完整播放且不会中止时，哪个事件会触发？

## --answers--

`play`

### --feedback--

考虑哪个 `event` 表示整个 `video` 可以完整播放。

---

`canplay`

### --feedback--

考虑哪个 `event` 表示整个 `video` 可以完整播放。

---

`canplaythrough`

---

`ended`

### --feedback--

考虑哪个 `event` 表示整个 `video` 可以完整播放。

## --video-solution--

3

## --text--

当由于数据缓冲区而自动暂停播放时，哪个事件会触发？

## --answers--

`pause`

### --feedback--

该事件与视频装载时你所执行的操作同名。

---

`waiting`

---

`ended`

### --feedback--

该事件与视频装载时你所执行的操作同名。

---

`canplay`

### --feedback--

该事件与视频装载时你所执行的操作同名。

## --video-solution--

2
