---
id: 67f4138c34cd9c4ac783ceef
title: 图和树测验
challengeType: 8
dashedName: quiz-graphs-and-trees
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是图？

#### --distractors--

一种遵循后进先出原则的线性数据结构。 

---

一种数据结构，用于保存内存中连续的相同类型的多值。 

---

一种遵循先进先出原则的线性数据结构。 

#### --answer--

一种用于表现对象或实体之间连接或关系的数据结构。

### --question--

#### --text--

以下哪项不是可以用图表现的典型场景？

#### --distractors--

社交网络

---

运输网络

---

通信网络

#### --answer--

虚拟专用网络

### --question--

#### --text--

以下哪个术语描述了图中所示网络的一部分的对象或实体？

#### --distractors--

Cookie

---

端点

---

CPU

#### --answer--

节点

### --question--

#### --text--

以下哪个术语描述了图中节点之间的连接？

#### --distractors--

闸门

---

连接器

---

顶点

#### --answer--

边缘

### --question--

#### --text--

什么是无向图？

#### --distractors--

一个节点是无向的但边是有向的 `Graph`。

---

一张边具有特定方向的图。

---

一个节点和边在中点连接的图。

#### --answer--

一张边没有特定方向的图。

### --question--

#### --text--

以下哪项指的是每个节点除了其数据外，还关联有标签或标识符的图？

#### --distractors--

版本标记图

---

虚拟标记图

---

垂直带标签的图

#### --answer--

带顶点标签的图

### --question--

#### --text--

以下哪项指的是至少包含一个周期的有向图？

#### --distractors--

骑行图

---

旋转图

---

圆形图

#### --answer--

循环图

### --question--

#### --text--

什么是带权图？

#### --distractors--

一种特定类型的带标签边的图，仅显示节点之间连接的方向。

---

一种特定类型的边标记图，赋值类别而非数值给边。

---

一种特定类型的边标记图，将所有边的权值视为相等，无论标签如何。

#### --answer--

一种特定类型的边标记图，其中边上的标签表现为可比较且可用于执行算术操作的值。

### --question--

#### --text--

什么是有向无环图？

#### --distractors--

一个带有一个周期的有向图。

---

一个具有三个周期的有向图。

---

一个带有两个周期的有向图。

#### --answer--

一个没有周期的有向图。

### --question--

#### --text--

以下哪项是指具有两个或更多节点组且这些组之间没有任何边连接的 `graph`？

#### --distractors--

挂起来图

---

断开图

---

断开连接图

#### --answer--

不连通图

### --question--

#### --text--

什么是广度优先查找算法？

#### --distractors--

一种算法，在图中沿一个方向访问两个节点后，再转向另一个方向。

---

一种算法，在图中沿一个方向访问所有节点，然后再沿另一个方向移动。

---

一种算法，每次访问四个节点，然后再移动到图中的下一层。

#### --answer--

一种算法，在移动到图中的下一层之前访问所有相邻节点。

### --question--

#### --text--

以下哪种算法会在回溯之前尽可能深入地遍历每个分支？

#### --distractors--

数据优先查找

---

Dock-First 查找

---

守护进程优先查找

#### --answer--

深度优先搜索

### --question--

#### --text--

以下哪个术语表现了一个二维列表，其中行和列表现图的顶点？

#### --distractors--

数组矩阵

---

深度矩阵

---

顶点矩阵

#### --answer--

邻接矩阵

### --question--

#### --text--

以下哪个术语表现为保存每个节点所有邻居的数组或字典？

#### --distractors--

循环列表

---

元组列表

---

广度列表

#### --answer--

邻接列表

### --question--

#### --text--

一个图被归类为树必须满足什么条件？

#### --distractors--

它必须至少有一个循环或周期。

---

它必须至少有两个循环或周期。

---

它必须有三个循环或周期。

#### --answer--

它不能有循环或周期。

### --question--

#### --text--

以下哪项是每个节点最多可以有两个子节点的树的类型？

#### --distractors--

深度树

---

空间树

---

线性树

#### --answer--

二叉树

### --question--

#### --text--

什么是 Trie？

#### --distractors--

用于保存一组数字的树数据结构。

---

用于保存一组字典的树数据结构。

---

用于保存一组列表的树形数据结构。

#### --answer--

用于保存一组字串的树数据结构。

### --question--

#### --text--

优先级队列数据结构与普通队列数据结构有何不同？

#### --distractors--

优先级队列会在插入前自动移除副本。

---

优先级队列总是按照元素被添加的顺序处理元素。

---

优先级队列只保存相同数据类型的元素。

#### --answer--

优先级队列根据元素的优先级处理元素。

### --question--

#### --text--

堆 有哪 两种 类型？

#### --distractors--

宽堆和高堆

---

水平堆 和 垂直堆

---

广度堆和深度堆

#### --answer--

最大堆和最小堆

### --question--

#### --text--

从堆中插入和提取最小值或最大值的平均和最坏分支时间复杂度是什么？

#### --distractors--

`O(n log n)`

---

`O(1)`

---

`O(n)`

#### --answer--

`O(log n)`

