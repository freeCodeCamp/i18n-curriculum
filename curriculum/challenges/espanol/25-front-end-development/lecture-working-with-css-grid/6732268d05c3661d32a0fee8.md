---
id: 6732268d05c3661d32a0fee8
title: '¿Cómo Puedes Repetir Listados de Pistas en un Diseño de Cuadrícula?'
challengeType: 19
dashedName: how-can-you-repeat-track-listings-in-a-grid-layout
---

# --description--

In the previous lectures, we have been working with the `grid-template-columns` property and setting the value to a few fractional units.

```css
.grid-container {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr 1fr;
  column-gap: 10px;
}
```

Aunque el siguiente código es completamente válido, hay una forma más sencilla de repetir una sección o todos los listados de tus pistas.

La función `repeat()` se usa para repetir una sección o todos los elementos de las columnas o filas. Esta función toma una cuenta de repetición y los elementos que deseas repetir.

Aquí tienes una versión revisada del ejemplo anterior usando la función de repetición:

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  column-gap: 10px;
}
```

No habrá ningún cambio en los estilos en el navegador, pero esta es una manera más concisa de escribir valores repetidos para las columnas.

La función `repeat()` aceptará cualquier patrón válido que puedas usar para filas o columnas.

En este ejemplo, tenemos marcado HTML para un contenedor de cuadrícula:

```html
<div class="grid-container">
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
</div>
```

Luego estamos usando la función `repeat()` para establecer la primera y tercera columnas a 20 píxeles y la segunda y cuarta columnas a una unidad fraccionaria.

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(2, 20px 1fr);
  column-gap: 10px;
}

.grid-container div {
  height: 100px;
  background-color: darkblue;
}
```

A veces, puedes optar por escribir cada valor individual en lugar de usar la función `repeat()`. Pero hay ocasiones en las que esta función resulta útil, especialmente cuando deseas repetir un patrón particular para un listado de pistas.

# --questions--

## --text--

¿Cuál de las siguientes es la sintaxis correcta para repetir listados de pistas?

## --answers--

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(1fr times 4);
  column-gap: 10px;
}
```

### --feedback--

Review the beginning of the lecture where this was discussed.

---

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  column-gap: 10px;
}
```

---

```css
.grid-container {
  display: grid;
  grid-template-columns: multiply(4, 1fr);
  column-gap: 10px;
}
```

### --feedback--

Review the beginning of the lecture where this was discussed.

---

```css
.grid-container {
  display: grid;
  grid-template-columns: repeatSections(4, 1fr);
  column-gap: 10px;
}
```

### --feedback--

Review the beginning of the lecture where this was discussed.

## --video-solution--

2

## --text--

¿Cuál es el beneficio principal de usar la función `repeat()` en la propiedad `grid-template-columns`?

## --answers--

Permite definir columnas repetidas de manera más concisa y reduce la redundancia.

---

Hace que el código se ejecute más rápido.


### --feedback--

Piensa en cómo esta propiedad reduce la repetición.

---

Conduce a un código libre de errores.

### --feedback--

Piensa en cómo esta propiedad reduce la repetición.

---

Hace que la página web sea más receptiva.

### --feedback--

Piensa en cómo esta propiedad reduce la repetición.

## --video-solution--

1

## --text--

¿Cuál es el resultado de establecer `grid-template-columns` en `repeat(2, 20px 1fr);`?

## --answers--

Crea cuatro columnas todas establecidas con un ancho de `1fr`.

### --feedback--

Review the end of the lecture where this was discussed.

---

Crea dos columnas donde cada una está establecida con un ancho de `20px`.

### --feedback--

Review the end of the lecture where this was discussed.

---

Crea cuatro columnas donde la primera y la tercera tienen un ancho de `20px`, y la segunda y cuarta tienen un ancho de `1fr`.

---

Crea tres columnas donde la primera y segunda tienen un ancho de `20px`, y la tercera tiene un ancho de `fr`.

### --feedback--

Review the end of the lecture where this was discussed.

## --video-solution--

3
