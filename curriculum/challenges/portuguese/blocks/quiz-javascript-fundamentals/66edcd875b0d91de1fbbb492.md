---
id: 66edcd875b0d91de1fbbb492
title: Questionário sobre o básico em JavaScript
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual destes criará um objeto String?

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

Como você pode converter um objeto String em um literal de string?

#### --distractors--

Com o construtor `String()`.

---

Com o método `.stringify()`.

---

Coloque o objeto `String` entre aspas duplas.

#### --answer--

Com o método `.toString()`.

### --question--

#### --text--

Por que você pode usar métodos como `.length`, `.repeat()`, `.concat()` e `.slice()` em um primitivo string no JavaScript?

#### --distractors--

Esses métodos existem nativamente em primitivas de string.

---

Primitivos de string são armazenados internamente como objetos.

---

JavaScript converte temporariamente o primitivo string em um array.

#### --answer--

JavaScript envolve temporariamente o primitivo string em um objeto String.

### --question--

#### --text--

O que o seguinte código irá registrar no console?

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

Como você pode converter um literal de string em um objeto Number?

#### --distractors--

Com o construtor `Object()`.

---

Com o método `.toNumber()`.

---

Com o método `.parseInt()`.

#### --answer--

Com o construtor `Number()`.

### --question--

#### --text--

O que `Number(true)` e `Number(false)` retornarão, respectivamente?

#### --distractors--

`undefined` e `undefined`

---

`NaN` e `NaN`

---

`true` e `false`

#### --answer--

`1` e `0`

### --question--

#### --text--

O que `Number(undefined)` e `Number(null)` retornarão, respectivamente?

#### --distractors--

`NaN` e `NaN`

---

`0` e `0`

---

`undefined` e `null`

#### --answer--

`NaN` e `0`

### --question--

#### --text--

Qual dos seguintes é o melhor nome para uma variável booleana que verifica se um usuário tem permissão para fazer algo no seu site?

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

Qual dos seguintes é o melhor nome para uma função que é executada quando um usuário envia um formulário?

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

O que é um sparse array?

#### --distractors--

Um array onde um ou mais índices têm um valor de `null`.

---

Um array onde um ou mais índices têm um valor de `undefined`.

---

Um array onde um ou mais índices têm um valor de um literal de objeto vazio.

#### --answer--

Um array onde um ou mais índices não têm um valor atribuído.

### --question--

#### --text--

Em qual dos seguintes exemplos `arr` NÃO será um array esparso?

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

Qual é o propósito principal de um linter?

#### --distractors--

Para formatar o código automaticamente.

---

Para otimizar o código para desempenho.

---

Para criar documentação para código.

#### --answer--

Para detectar erros e problemas potenciais no código.

### --question--

#### --text--

Qual é um benefício de usar um formatter em uma codebase?

#### --distractors--

Eles permitem que os desenvolvedores compilem código de forma mais eficiente.

---

Eles convertem código para uma linguagem de programação específica.

---

Eles detectam erros lógicos.

#### --answer--

Eles garantem um estilo de código consistente.

### --question--

#### --text--

Em JavaScript, quem ou o que é tipicamente responsável por alocar memória para variáveis?

#### --distractors--

O desenvolvedor.

---

O sistema operacional.

---

O servidor.

#### --answer--

O motor de runtime.

### --question--

#### --text--

O que será registrado no console no seguinte código?

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

O que é hoisting em JavaScript?

#### --distractors--

Elevando erros para o topo do código.

---

O processo de criar novas variáveis.

---

Movendo automaticamente as declarações `var` para o topo do bloco.

#### --answer--

O processo de mover declarações de variáveis para o topo do seu escopo.

### --question--

#### --text--

Por que usar a palavra-chave `var` é considerado uma má prática no JavaScript moderno?

#### --distractors--

Eles são mais lentos que `let` e `const`.

---

Eles causam erros de sintaxe.

---

Eles não permitem verificação de tipo.

#### --answer--

Eles são acessíveis fora do seu bloco.

### --question--

#### --text--

Qual é o propósito dos módulos em JavaScript?

#### --distractors--

Para melhorar o desempenho do código.

---

Para tornar o código mais difícil de entender.

---

Para criar novas linguagens de programação.

#### --answer--

Para organizar o código em unidades reutilizáveis.

### --question--

#### --text--

Como você adiciona um script `app.js` como um módulo em um documento HTML?

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

Qual dos exemplos a seguir exporta a função `calculateSum` de um módulo `utils.js` e a importa e utiliza corretamente em um arquivo `app.js`?

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
