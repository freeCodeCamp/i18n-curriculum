---
id: 66f1ae758b77cfc3e4da6151
title: اختبار TypeScript
challengeType: 8
dashedName: quiz-typescript
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هو TypeScript؟

#### --distractors--

إطار عمل يُستخدم لبرمجة الأنظمة.

---

أداة لإدارة قواعد البيانات.

---

لغة برمجة تُستخدم لتنسيق مواقع الويب.

#### --answer--

امتداد لـ JavaScript يضيف التحقق من الأنواع الثابتة.

### --question--

#### --text--

لماذا يجب استخدام TypeScript في مشروع؟

#### --distractors--

يلغي الحاجة للاختبار في التطبيقات.

---

هو أسرع من جميع لغات البرمجة الأخرى.

---

يجعل تنسيق مواقع الويب أسهل.

#### --answer--

يحسن الصيانة بإضافة تعريفات الأنواع إلى JavaScript.

### --question--

#### --text--

إلى أي لغة يُترجم TypeScript؟

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

ما هو ناتج مقتطف الشفرة التالي؟

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

ما هو الغرض الأساسي من الواجهة (interface) في TypeScript؟

#### --distractors--

لترجمة شفرة TypeScript إلى JavaScript.

---

لإنشاء نوع بيانات جديد.

---

لإنشاء كائن جديد في JavaScript.

#### --answer--

لتعريف هيكل كائن.

### --question--

#### --text--

أي مما يلي ليس نوعًا بدائيًا أساسيًا في TypeScript؟

#### --distractors--

سلسلة نصية

---

قيمة منطقية

---

عدد

#### --answer--

مصفوفة

### --question--

#### --text--

أي مما يلي يعرّف نوعًا حرفيًا بشكل صحيح في TypeScript؟

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

ما هو الغرض الأساسي من الكلمة المفتاحية `type` في TypeScript؟

#### --distractors--

لإعلان المتغيرات.

---

لإنشاء الأصناف.

---

لتنفيذ الواجهات.

#### --answer--

لتعريف أنواع مخصصة.

### --question--

#### --text--

أي مما يلي يعرّف نوع اتحاد (union) بشكل صحيح؟

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

أي من القدرات التالية فريدة لـ `interface` ولا يمكن تحقيقها باستخدام `type` في TypeScript؟

#### --distractors--

تعريف أشكال الكائنات.

---

إنشاء أنواع اتحاد.

---

تعريف أنواع تشمل قيمًا محددة فقط.

#### --answer--

إعلان تنفيذات الأصناف.

### --question--

#### --text--

ما هو الغرض الأساسي من استخدام الأنواع العامة (generics) في TypeScript؟

#### --distractors--

لتنفيذ الوراثة بين الأصناف.

---

لتعريف هيكل كائن.

---

لإنشاء نوع بيانات جديد.

#### --answer--

لكتابة شفرة قابلة لإعادة الاستخدام تعمل مع أنواع متعددة.

### --question--

#### --text--

أي مما يلي هو **مثال صحيح** لدالة عامة في TypeScript؟

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

ما هو تضييق النوع (type narrowing) في TypeScript؟

#### --distractors--

تقليل حجم JavaScript المترجمة.

---

تحديد عدد الأنواع في اتحاد.

---

إزالة الأنواع غير المستخدمة من مشروع TypeScript.

#### --answer--

كتابة شفرة تضيق نوع الاتحاد إلى نوع أكثر تحديدًا.

### --question--

#### --text--

أي ملف يُستخدم عادةً لتكوين خيارات مترجم TypeScript؟

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

ماذا يحدد الخيار `rootDir` في ملف `tsconfig.json`؟

#### --distractors--

إصدار TypeScript المستخدم أثناء الترجمة.

---

بيئة التشغيل المستهدفة (مثل Node.js أو المتصفح) لشفرة TypeScript.

---

نظام الوحدة المحدد لاستخدامه في الاستيراد والتصدير.

#### --answer--

الدليل الذي يحتوي على ملفات المصدر الخاصة بك.

### --question--

#### --text--

أي مثال يستخدم `instanceof` بشكل صحيح لتضييق النوع والوصول الآمن إلى خاصية `value`؟

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
ماذا يفعل تعيين `"strict": true` في `tsconfig.json` في TypeScript؟

#### --distractors--

يُفعّل التحقق من النوع الثابت لتتمكن من اكتشاف الأخطاء قبل تشغيل الشفرة.

---

يجبرك على استخدام صيغة ES6 دائمًا.

---

يجبرك على ترجمة الشفرة قبل تشغيلها.

#### --answer--

يُفعّل مجموعة من خيارات التحقق من النوع مثل اشتراط التعامل مع `null` أو `undefined` ومنع النوع الضمني `any`.

### --question--

#### --text--

ما هو الغرض من النوع `never` في TypeScript؟

#### --distractors--

لتمثيل null أو undefined.

---

لتمثيل أي قيمة ممكنة.

---

لتمثيل نوع يمكن أن يكون أي شيء ما عدا null.

#### --answer--

لتمثيل نوع القيم التي لا تحدث أبدًا.

### --question--

#### --text--

أي من استدعاءات دالة `getRandomValue` التالية ستُترجم بنجاح في TypeScript؟

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

كيف تصلح خطأ مترجم TypeScript `'email' is possibly 'null'` في الشفرة التالية باستخدام التسلسل الاختياري؟

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`

