---
id: 66ed9009f45ce3ece4053eb7
title: CSS 상대 단위와 절대 단위 퀴즈
challengeType: 8
dashedName: quiz-css-relative-and-absolute-units
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답하세요.

# --quizzes--

## --quiz--

### --question--

#### --text--

절대 단위란 무엇인가요?

#### --distractors--

화면 크기에 따라 동적으로 조정되는 단위입니다.

---

부모 요소의 폰트 크기에 따라 달라지는 단위입니다.

---

뷰포트의 백분율로 측정되는 단위입니다.

#### --answer--

다른 어떤 것에도 상대적이지 않은 고정 길이 단위입니다.

### --question--

#### --text--

`100vw`는 화면 너비 측면에서 무엇을 나타내나요?

#### --distractors--

화면 너비의 `100%`입니다.

---

뷰포트 높이의 `100%`입니다.

---

부모 너비의 `100%`입니다.

#### --answer--

뷰포트 너비의 `100%`입니다.

### --question--

#### --text--

다음 중 상대 단위는 무엇인가요?

#### --distractors--

`pt`

---

`cm`

---

`mm`

#### --answer--

`em`

### --question--

#### --text--

루트 폰트 크기가 `20px`일 때, `1rem`은 무엇과 같나요?

#### --distractors--

`18px`

---

`16px`

---

`10px`

#### --answer--

`20px`

### --question--

#### --text--

CSS에서 `calc()` 함수는 무엇을 할 수 있나요?

#### --distractors--

색상을 동적으로 수정합니다.

---

고정 너비를 설정합니다.

---

백분율에만 작동합니다.

#### --answer--

서로 다른 단위를 더하고 빼고 곱하고 나눌 수 있습니다.

### --question--

#### --text--

CSS에서 가장 일반적으로 사용되는 절대 단위는 무엇인가요?

#### --distractors--

`inch`

---

`rem`

---

`%`

#### --answer--

`px`

### --question--

#### --text--

뷰포트 높이가 `800px`일 때, `50vh`은 무엇일까요?

#### --distractors--

`50px`

---

`200px`

---

`100px`

#### --answer--

`400px`

### --question--

#### --text--

요소의 너비를 `100%`로 설정한다는 것은 무엇을 의미하나요?

#### --distractors--

문서의 전체 너비를 차지합니다.

---

화면의 전체 너비를 차지합니다.

---

`100px`만큼 차지합니다.

#### --answer--

부모의 전체 너비를 차지합니다.

### --question--

#### --text--

CSS에서 `calc(10px + 20%)`은 어떻게 작동하나요?

#### --distractors--

문서 너비의 `10px`에 `10%`를 더합니다.

---

항상 `30px`와 같습니다.

---

화면 너비의 `10px`에 `20%`를 더합니다.

#### --answer--

부모 너비의 `10px`에 `20%`를 더합니다.

### --question--

#### --text--

루트 요소의 폰트 크기에 상대적인 단위는 무엇인가요?

#### --distractors--

`px`

---

`pt`

---

`%`

#### --answer--

`rem`

### --question--

#### --text--

루트 폰트 크기가 `16px`일 때, `10rem`은 얼마나 큰가요?

#### --distractors--

`140px`

---

`10px`

---

`100px`

#### --answer--

`160px`

### --question--

#### --text--

디자인에서 `point`를 `pixels` 대신 사용하는 이유는 무엇인가요?

#### --distractors--

포인트는 확장 가능합니다.

---

포인트는 반응형 레이아웃에 더 적합합니다.

---

포인트는 화면에서 관리하기 쉽습니다.

#### --answer--

포인트는 인쇄 디자인용입니다.

### --question--

#### --text--

타이포그래피에서 `rem` 단위가 `pixels`보다 선호되는 이유는 무엇인가요?

#### --distractors--

화면 해상도와 상관없이 고정 크기를 제공하기 때문입니다.

---

구형 브라우저와의 호환성을 보장하기 때문입니다.

---

반응형 디자인 구현이 더 쉽기 때문입니다.

#### --answer--

사용자의 브라우저 설정에 비례해 크기가 조절되기 때문입니다.

### --question--

#### --text--

`80vw`는 화면 공간을 얼마나 차지하나요?

#### --distractors--

부모 너비의 `80%`입니다.

---

뷰포트 높이의 `100%`입니다.

---

`80px`

#### --answer--

뷰포트 너비의 `80%`입니다.

### --question--

#### --text--

화면의 백분율로 요소의 너비나 높이를 지정하려면 어떤 단위를 사용해야 하나요?

#### --distractors--

`pt`와 `px`

---

`rem`와 `em`

---

`pt`와 `pc`

#### --answer--

`vw`와 `vh`

### --question--

#### --text--

`vw`는 무엇의 약자인가요?

#### --distractors--

세로 너비.

---

가상 너비.

---

가변 너비.

#### --answer--

뷰포트 너비.

### --question--

#### --text--

CSS에서 `rem`와 `em`의 주요 차이점은 무엇인가요?

#### --distractors--

`em`는 루트 요소에 상대적이고 `rem`는 부모 요소에 상대적입니다.

---

`rem`는 데스크톱 너비에 상대적이고 `em`는 모바일 너비에 상대적입니다.

---

`rem`는 사용자 정의 너비에 상대적이고 `em`는 브라우저 너비에 상대적입니다.

#### --answer--

`em`는 부모 요소의 폰트 크기에 상대적이고 `rem`는 루트 요소에 상대적입니다.

### --question--

#### --text--

`vh` 단위란 무엇인가요?

#### --distractors--

뷰포트 너비를 기준으로 한 단위입니다.

---

요소 높이를 기준으로 한 단위입니다.

---

폰트 크기에 따라 조정되는 단위입니다.

#### --answer--

뷰포트 높이의 1%와 같은 단위입니다.

### --question--

#### --text--

CSS에서 요소의 너비를 `auto`로 설정하면 어떻게 되나요?

#### --distractors--

기본적으로 부모 요소 너비의 `100%`로 설정됩니다.

---

다른 스타일이 재정의하지 않으면 뷰포트 너비의 `50%`로 설정됩니다.

---

내용이나 패딩이 없으면 `0px`로 설정됩니다.

#### --answer--

브라우저가 내용과 컨테이너에 따라 너비를 결정하도록 허용합니다.

### --question--

#### --text--

`calc()` 함수의 주요 장점은 무엇인가요?

#### --distractors--

더 나은 성능을 위해 CSS 파일을 자동으로 축소합니다.

---

전처리기 없이 CSS 내에서 변수를 사용할 수 있게 합니다.

---

CSS 규칙 내에 JavaScript 표현식을 직접 임베드할 수 있습니다.

#### --answer--

애플리케이션이나 뷰포트의 다양한 측면에 따라 CSS 속성 값을 동적으로 결정할 수 있게 합니다.
