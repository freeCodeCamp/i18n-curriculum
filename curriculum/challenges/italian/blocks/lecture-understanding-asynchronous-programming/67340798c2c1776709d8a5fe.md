---
id: 67340798c2c1776709d8a5fe
title: "Come funziona l'attributo async negli elementi script e in cosa si differenzia dall'attributo defer?"
challengeType: 19
dashedName: how-does-the-async-attribute-work-inside-script-elements-and-how-does-it-differ-from-the-defer-attribute
---

# --description--

Gli attributi `async` e `defer` negli elementi `script` HTML giocano un ruolo cruciale nel modo in cui i file JavaScript vengono caricati ed eseguiti nelle pagine web. Capirli può migliorare le prestazioni del tuo sito e l'esperienza utente. Quando includi un `script` nel tuo file HTML, appare così:

```js
<script src="example.js"></script>
```

Quando il browser trova questo tag `script`, interrompe l'analisi dell'HTML, scarica lo script, lo esegue e poi continua ad analizzare l'HTML. Questo può rallentare il caricamento della tua pagina web, soprattutto se hai script di grandi dimensioni.

Qui entrano in gioco gli attributi `async` e `defer`. Offrono modi per caricare gli script in modo più efficiente.

Iniziamo con l'attributo `async`:

```js
<script src="example.js" async></script>
```

Aggiungendo l'attributo `async` a un tag `script`, il browser continuerà ad analizzare l'HTML mentre lo script viene scaricato. Una volta che lo script è completamente scaricato, il browser metterà in pausa l'analisi dell'HTML, eseguirà lo script e poi riprenderà l'analisi dell'HTML. Questo può velocizzare notevolmente il caricamento della pagina.

È importante notare che gli script async vengono eseguiti non appena sono scaricati, il che significa che potrebbero non essere eseguiti nell'ordine corretto che desideriamo. Qui entra in gioco l'attributo `defer`. Vediamo come appare l'attributo `defer`:

```js
<script src="example.js" defer></script>
```

L'attributo `defer` è simile all'attributo `async`. Tuttavia, gli script defer non vengono eseguiti immediatamente dopo il loro download. Invece, aspettano che l'analisi dell'HTML sia completata. Inoltre, gli script defer vengono eseguiti nell'ordine in cui appaiono nel codice HTML.

In breve, usa `async` per gli script in cui l'ordine di esecuzione non è importante, e usa `defer` quando devi assicurarti che gli script vengano eseguiti nell'ordine corretto. Entrambi gli attributi possono migliorare significativamente i tempi di caricamento della pagina permettendo al browser di continuare ad analizzare l'HTML mentre gli script vengono scaricati in background.

# --questions--

## --text--

Qual è il vantaggio di usare l'attributo `defer` in un tag `<script>`?

## --answers--

Aiuta a differenziare lo script dagli script `async`.

### --feedback--

Ricorda lo scopo e le differenze degli attributi `defer` e `async`.

---

Riduce l'uso di memoria durante il caricamento della pagina.

### --feedback--

L'attributo `defer` non ha nulla a che fare con la gestione della memoria.

---

Può migliorare le prestazioni complessive del caricamento della pagina.

---

Permette agli sviluppatori di impostare un ritardo personalizzato prima che gli script vengano eseguiti.

### --feedback--

L'attributo `defer` non esegue gli script con un timer personalizzato.

## --video-solution--

3

## --text--

In quale ordine verranno eseguiti i seguenti script?

```html
<script src="1.js" defer></script>
<script src="2.js" async></script>
<script src="3.js" defer></script>
```

## --answers--

`1.js`, `2.js`, `3.js`.

### --feedback--

Ricorda come `async` e `defer` influenzano l'ordine di esecuzione degli script.

---

`2.js`, `1.js`, `3.js`.

### --feedback--

Ricorda come `async` e `defer` influenzano l'ordine di esecuzione degli script.

---

L'ordine è imprevedibile.

---

`1.js`, `3.js`, `2.js`.

### --feedback--

Ricorda come `async` e `defer` influenzano l'ordine di esecuzione degli script.

## --video-solution--

3

## --text--

Quale affermazione su `async` e `defer` è vera?

## --answers--

Gli script `async` vengono eseguiti nell'ordine in cui appaiono nell'HTML.

### --feedback--

Pensa a quando gli script `async` e `defer` vengono eseguiti in relazione all'analisi dell'HTML.

---

Gli script `defer` vengono eseguiti non appena sono scaricati.

### --feedback--

Pensa a quando gli script `async` e `defer` vengono eseguiti in relazione all'analisi dell'HTML.

---

Gli script `async` possono essere eseguiti prima che l'HTML sia completamente analizzato.

---

Gli script `defer` vengono eseguiti prima di qualsiasi script `async`.

### --feedback--

Pensa a quando gli script `async` e `defer` vengono eseguiti in relazione all'analisi dell'HTML.

## --video-solution--

3
