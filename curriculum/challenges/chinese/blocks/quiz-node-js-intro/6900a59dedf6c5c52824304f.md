---
id: 6900a59dedf6c5c52824304f
title: NodeJS 入门测验
challengeType: 8
dashedName: quiz-node-js-intro
---

# --description--

要通过测验，你必须正确回答以下 10 道题中的至少 9 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

为什么客户端 JavaScript 对局部的文件系统的访问受到限制？

#### --distractors--

因为 JavaScript 是一种运行缓慢的编程语言。

---

因为浏览器没有足够的内存来处理文件。

---

因为 DOM API 阻止文件访问。

#### --answer--

因为它被设计为在网络浏览器中运行，在那里不受限制的文件访问会带来安全性风险。

### --question--

#### --text--

Node.js 是“跨平台”的意思是什么？

#### --distractors--

它可以同时运行多种编程语言。

---

它可以将不同的数据库连接在一起。

---

每个操作系统需要不同的安装方法。

#### --answer--

它适用于任何操作系统，包括 Windows、macOS 和 Linux。

### --question--

#### --text--

哪个 API 可以在浏览器环境中访问，但不能在 Node.js 环境中访问？

#### --distractors--

文件系统 API

---

网络 API

---

操作系统 API

#### --answer--

DOM API

### --question--

#### --text--

在 Node.js 的上下文中，什么是 `npm`？

#### --distractors--

一个用于创建用户界面的 JavaScript 框架。

---

为 Node.js 应用优化的网页浏览器。

---

一个用于 Node.js 应用的调试工具。

#### --answer--

一个用于 Node.js 的包管理器，允许你安装和管理包和模块。

### --question--

#### --text--

运行 `nvm install --lts` 时，`--lts` 标记表示什么？

#### --distractors--

它安装最新的测试屏幕截图版本。

---

它安装了可用的最轻量和最小版本。

---

它安装具有最多特色的版本。

#### --answer--

它安装了优先考虑稳定性、可靠性和安全性的长期支持版本。

### --question--

#### --text--

在 Node.js 异步的编程上下文中，什么是 `callback`？

#### --distractors--

一种重新启动失败操作的方法。

---

一个保存同步的操作结果的变量。

---

一个监控应用性能的工具。

#### --answer--

一个定义异步操作完成时发生内容的函数。

### --question--

#### --text--

`npm init` 创建的 `package.json` 文件的目的是什么？

#### --distractors--

为应用保存环境变量。

---

配置网络服务器的设置。

---

定义应用的超文本标记语言结构。

#### --answer--

跟踪项目的详细信息和依赖关系。

### --question--

#### --text--

为什么 CPU 密集型任务在 Node.js 中可能会有问题？

#### --distractors--

Node.js 无法执行数学操作。

---

Node.js 不支持加密或图像处理。

---

事件循环防止 CPU 操作运行。

#### --answer--

由于 Node.js 在单线程上运行，它们可能会阻塞主线程，导致性能问题。

### --question--

#### --text--

使用什么命令来运行名为 `server.js` 的 JavaScript 文件，使用 Node.js？

#### --distractors--

`npm run server.js`

---

`nvm execute server.js`

---

`javascript server.js`

#### --answer--

`node server.js`

### --question--

#### --text--

开发者在从 npm 选择包之前应该查看什么？

#### --distractors--

该包是否至少有 1000 次下载。

---

该包是否最近被创建。

---

该包是否需要付款。

#### --answer--

该包是否被维护并遵循质量和安全性最佳实践。
