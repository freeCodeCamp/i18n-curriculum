---
id: 5f332b23c2045fb843337579
title: Step 3
challengeType: 0
dashedName: step-3
---

# --description--

To let visitors know the cafe was founded in `2020`, add a `p` element below the `h1` element with the text `Est.  </p>

<h1 spaces-before="0">--hints--</h1>

<p spaces-before="0">Du solltest ein öffnendes <code><p>`-Tag haben.

```js
assert.match(code, /<p>/i);
```

Du solltest ein schließendes `</p>` Tag haben.

```js
assert.match(code, /<\/p>/i);
```

Du solltest dein vorhandenes `h1`-Element nicht ändern. Stelle sicher, dass du das abschließende Tag nicht gelöscht hast.

```js
assert.lengthOf(document.querySelectorAll('h1'),1);
```

Dein `p`-Element sollte unter deinem `h1`-Element stehen.

```js
assert.equal(document.querySelector('p')?.previousElementSibling?.tagName, 'H1');
```

Dein `p`-Element sollte den Text `Est. 2020` haben.

```js
assert.equal(document.querySelector("p")?.innerText.trim(), "Est. 2020");
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Cafe Menu</title>
  </head>
  <body>
    <main>
      <h1>CAMPER CAFE</h1>
--fcc-editable-region--

--fcc-editable-region--
    </main>
  </body>
</html>
```

