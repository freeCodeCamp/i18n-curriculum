---
id: 6a226d36b5d21265afeadc82
title: Тест з WebSockets
challengeType: 8
dashedName: quiz-websockets
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Чому Node.js потребує зовнішньої бібліотеки для серверів WebSocket?

#### --distractors--

Node.js має вбудований сервер WebSocket, але для його увімкнення потрібна додаткова конфігурація.

---

Node.js підтримує WebSockets лише при використанні фреймворку Express.

---

Node.js взагалі не підтримує WebSockets, незалежно від зовнішніх пакетів.

#### --answer--

Node.js не має вбудованого сервера WebSocket.

### --question--

#### --text--

Які три основні частини архітектури Pub/Sub?

#### --distractors--

Відправники, черги та отримувачі.

---

Виробники, канали та споживачі.

---

Клієнти, сервери та бази даних.

#### --answer--

Видавці, теми та підписники.

### --question--

#### --text--

Яку модель комунікації використовує HTTP?

#### --distractors--

Постійна двонапрямна модель.

---

Модель трансляції.

---

Модель pub/sub.

#### --answer--

Модель запит-відповідь.

### --question--

#### --text--

Яка подія WebSocket спрацьовує, коли новий клієнт підключається до сервера?

#### --distractors--

`connect`

---

`open`

---

`join`

#### --answer--

`connection`

### --question--

#### --text--

Який головний недолік прямої комунікації застосунок-застосунок у порівнянні з Pub/Sub?

#### --distractors--

Пряма комунікація завжди повільніша за Pub/Sub.

---

Пряма комунікація забороняє застосункам надсилати більше одного повідомлення.

---

Пряма комунікація завжди вимагає стороннього сервера автентифікації.

#### --answer--

Вона створює непотрібну залежність між застосунками; зміна або збій в одному застосунку може порушити інший.

### --question--

#### --text--

З якого протоколу починається WebSocket-з’єднання перед оновленням?

#### --distractors--

WebSocket-з’єднання починаються з виділеного протоколу WebSocket з самого початку.

---

WebSocket-з’єднання починаються з TCP, повністю обходячи HTTP.

---

Кожне WebSocket-з’єднання починається з FTP, а потім оновлюється до WebSocket.

#### --answer--

Кожне WebSocket-з’єднання починається з HTTP.

### --question--

#### --text--

Який метод ви викликаєте, щоб надіслати дані через WebSocket-з’єднання клієнту?

#### --distractors--

`socket.emit()`

---

`socket.write()`

---

`socket.push()`

#### --answer--

`socket.send()`

### --question--

#### --text--

Яку роль відіграє тема в системі Pub/Sub?

#### --distractors--

Вона зберігає повідомлення назавжди для подальшого отримання.

---

Вона автентифікує видавців перед тим, як вони можуть надсилати повідомлення.

---

Вона безпосередньо з’єднує видавців із конкретними підписниками.

#### --answer--

Вона виступає посередником між видавцями та підписниками.

### --question--

#### --text--

Чим відрізняється комунікація HTTP від WebSocket?

#### --distractors--

HTTP двонапрямний, а WebSocket — однонапрямний.

---

HTTP підтримує постійне з’єднання, а WebSocket закривається після кожного обміну.

---

HTTP і WebSocket обидва слідують однаковому шаблону запит-відповідь.

#### --answer--

HTTP слідує `Client --> Request --> Server --> Response`, а WebSocket — `Client <--> Server`.

### --question--

#### --text--

Чому слід перевіряти `client.readyState === WebSocket.OPEN` перед трансляцією повідомлення підключеним клієнтам?

#### --distractors--

Щоб зашифрувати повідомлення перед відправленням кожному клієнту.

---

Щоб обмежити кількість повідомлень, що надсилаються кожному клієнту за секунду.

---

Щоб перевірити, чи має клієнт дозвіл отримувати повідомлення.

#### --answer--

Щоб уникнути помилок при відправленні клієнтам, які відключилися або ще підключаються.

### --question--

#### --text--

Який HTTP-статус код сервер надсилає, коли приймає запит на оновлення WebSocket?

#### --distractors--

`200 OK`

---

`301 Moved Permanently`

---

`426 Upgrade Required`

#### --answer--

`101 Switching Protocols`

### --question--

#### --text--

Який із наведених варіантів є реальним прикладом використання Pub/Sub?

#### --distractors--

Статична вебсторінка, що завантажує HTML із сервера.

---

Інструмент командного рядка, який обробляє файли по одному.

---

Реляційна база даних, що зберігає та виконує запити до структурованих даних.

#### --answer--

Система сповіщень, яка доставляє оповіщення в реальному часі всім своїм підписникам.

### --question--

#### --text--

Як створити сервер WebSocket на порті `3000` за допомогою бібліотеки `ws`?

#### --distractors--

```js
const server = new WebSocket({ port: 3000 });
```

---

```js
const server = WebSocket.createServer(3000);
```

---

```js
const server = ws.listen(3000);
```

#### --answer--

```js
const server = new WebSocket.Server({ port: 3000 });
```

### --question--

#### --text--

Яка подія WebSocket спрацьовує, коли з’єднання закривається?

#### --distractors--

`disconnect`

---

`end`

---

`terminate`

#### --answer--

`close`

### --question--

#### --text--

Який потік повідомлень у Pub/Sub?

#### --distractors--

Видавець надсилає підписнику --> підписник пересилає темі --> тема зберігає повідомлення.

---

Тема надсилає повідомлення --> видавці його отримують --> видавці пересилають підписникам.

---

Підписник підключається до видавця --> видавець надсилає дані --> підписник підтверджує отримання.

#### --answer--

Підписник підписується на тему --> видавець надсилає повідомлення цій темі --> система доставки повідомлень доставляє його всім підписникам цієї теми.

### --question--

#### --text--

Яка аналогія найкраще описує різницю між HTTP і WebSockets?

#### --distractors--

HTTP — це як телефонний дзвінок, а WebSockets — як обмін листами.

---

HTTP — це як чат, а WebSockets — як газета.

---

HTTP — це як пряма трансляція, а WebSockets — як записане відео.

#### --answer--

HTTP — це як обмін листами, а WebSockets — як телефонний дзвінок.

### --question--

#### --text--

Яка подія WebSocket спрацьовує, коли підключений клієнт надсилає дані серверу?

#### --distractors--

`data`

---

`receive`

---

`incoming`

#### --answer--

`message`

### --question--

#### --text--

Який із наведених варіантів правильно описує потік з’єднання WebSocket?

#### --distractors--

Клієнт підключається через TCP --> сервер надсилає дані --> клієнт отримує --> з’єднання закривається.

---

Клієнт надсилає дані --> сервер запитує оновлення --> з’єднання відкривається --> клієнт підтверджує.

---

Сервер ініціює з’єднання --> клієнт приймає --> обидві сторони обмінюються даними --> з’єднання закривається.

#### --answer--

Клієнт підключається через HTTP --> сервер відповідає `101 Switching Protocols` --> з’єднання залишається відкритим --> обидві сторони можуть обмінюватися даними в будь-який час.

### --question--

#### --text--

Чим WebSockets і Pub/Sub відрізняються в реальному застосунку?

#### --distractors--

WebSockets відповідають за автентифікацію, а Pub/Sub — за зберігання даних.

---

WebSockets призначені для сервер-сервер комунікації, а Pub/Sub — для клієнт-сервер.

---

Це одна й та сама технологія з різними назвами.

#### --answer--

WebSockets забезпечують канал комунікації, а Pub/Sub — шаблон обміну повідомленнями.

### --question--

#### --text--

Який із наведених варіантів є реальним прикладом використання WebSockets?

#### --distractors--

Статична блог-сторінка з публікаціями.

---

Система відтворення файлів на сервері.

---

База даних, що зберігає записи користувачів.

#### --answer--

Чат-застосунок.
