---
id: 5900f3711000cf542c50fe84
title: 'Zoezi la 5: Nambari ndogo zaidi inayogawika'
challengeType: 1
forumTopicId: 302160
dashedName: problem-5-smallest-multiple
---

# --description--

2520 ni nambari ndogo zaidi inayoweza kugawika kwa kila nambari kuanzia 1 hadi 10 bila mabaki yoyote.

Nambari ndogo zaidi chanya inayogawika sawasawa na nambari zote kuanzia 1 hadi `n` ni ipi?

# --hints--

`smallestMult(5)` inapaswa kurudisha nambari.

```js
assert.isNumber(smallestMult(5));
```

`smallestMult(5)` inapaswa kurudisha 60.

```js
assert.strictEqual(smallestMult(5), 60);
```

`smallestMult(7)` inapaswa kurudisha 420.

```js
assert.strictEqual(smallestMult(7), 420);
```

`smallestMult(10)` inapaswa kurudisha 2520.

```js
assert.strictEqual(smallestMult(10), 2520);
```

`smallestMult(13)` inapaswa kurudisha 360360.

```js
assert.strictEqual(smallestMult(13), 360360);
```

`smallestMult(20)` inapaswa kurudisha 232792560.

```js
assert.strictEqual(smallestMult(20), 232792560);
```

# --seed--

## --seed-contents--

```js
function smallestMult(n) {

  return true;
}

smallestMult(20);
```

# --solutions--

```js
function smallestMult(n){
  function gcd(a, b) {
    return b === 0 ? a : gcd(b, a%b); // Euclidean algorithm
  }

  function lcm(a, b) {
    return a * b / gcd(a, b);
  }
  var result = 1;
  for(var i = 2; i <= n; i++) {
    result = lcm(result, i);
  }
  return result;
}
```
