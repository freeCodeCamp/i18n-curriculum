---
id: 69a98755ee5508d58e91dccc
title: NPM-Quiz
challengeType: 8
dashedName: quiz-npm
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 Fragen unten richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was bietet npm Entwicklern?

#### --distractors--

Eine kuratierte Plattform, die nur Design-Assets wie Schriftarten und Icons hostet.

---

Einen Hosting-Dienst, der Ihr Projekt automatisch in der Cloud bereitstellt.

---

Eine Datenbanklösung, die Benutzeraufzeichnungen für Ihre Anwendungen verwaltet.

#### --answer--

Ein Register vorgefertigter Softwarepakete, das häufige Probleme löst.

### --question--

#### --text--

Welches der folgenden ist nicht einer der drei Teile, auf die sich npm bezieht?

#### --distractors--

Die Website npmjs.com.

---

Das Paket-Register, in dem Bibliotheken gespeichert sind.

---

Das Kommandozeilen-Tool zum Installieren und Verwalten von Paketen.

#### --answer--

Die Node.js-Laufzeit.

### --question--

#### --text--

Was macht der `npm install`-Befehl?

#### --distractors--

Er erstellt ein brandneues Node.js-Projekt aus einer Vorlage.

---

Er führt Ihre Node.js-Anwendung auf einem lokalen Entwicklungsserver aus.

---

Er entfernt Pakete, die in Ihrem Projekt nicht mehr benötigt werden.

#### --answer--

Er fügt vorgefertigte Pakete zu Ihrem Projekt für häufige Aufgaben hinzu.

### --question--

#### --text--

Welche Datei speichert Metadaten wie Projektname, Version, Autor und Abhängigkeiten für ein Node.js-Projekt?

#### --distractors--

`README.md`.

---

`node_modules/package.json`.

---

`package-lock.json`.

#### --answer--

`package.json`.

### --question--

#### --text--

Wie können Sie interaktiv eine `package.json`-Datei erstellen?

#### --distractors--

Führen Sie `npm install` aus, ohne einen Paketnamen anzugeben.

---

Laden Sie eine Vorlage von der Startseite des npm-Registers herunter.

---

Kopieren Sie die Datei aus einem anderen Projekt und benennen Sie sie um.

#### --answer--

Führen Sie den `npm init`-Befehl aus.

### --question--

#### --text--

Wie erstellen Sie eine `package.json`-Datei mit Standardwerten, ohne Eingabeaufforderungen zu beantworten?

#### --distractors--

Führen Sie `npm init --interactive` aus.

---

Erstellen Sie eine leere Datei namens `package.json` und lassen Sie sie leer.

---

Verwenden Sie `npm init` und löschen Sie jede Eingabeaufforderung nach dem Öffnen des Editors.

#### --answer--

Führen Sie `npm init -y` (oder `npm init --yes`) aus, um alle Standardwerte automatisch zu akzeptieren.

### --question--

#### --text--

Wo listet npm Pakete auf, die nur während der Entwicklung benötigt werden, wie Linter oder Test-Runner?

#### --distractors--

Innerhalb des `"dependencies"`-Abschnitts.

---

Innerhalb des `"scripts"`-Abschnitts unter benutzerdefinierten Befehlen.

---

Innerhalb des `node_modules`-Ordners selbst.

#### --answer--

Innerhalb des `"devDependencies"`-Abschnitts von `package.json`.

### --question--

#### --text--

Welcher Befehl installiert `nodemon` als Entwicklungsabhängigkeit?

#### --distractors--

`npm uninstall nodemon`.

---

`npm save nodemon --dev`.

---

`npm install nodemon`.

#### --answer--

`npm install nodemon -D`.

### --question--

#### --text--

Welcher Ordner und welche Datei werden nach der Installation einer Abhängigkeit im Projektstamm hinzugefügt?

#### --distractors--

`public`-Ordner und `index.html`.

---

`dist`-Ordner und `webpack.config.js`.

---

`src`-Ordner und `README.md`.

#### --answer--

`node_modules`-Ordner und `package-lock.json`-Datei.

### --question--

#### --text--

Wie hilft `package-lock.json`, Builds konsistent zu halten?

#### --distractors--

Indem es vor jeder Installation ungenutzte Abhängigkeiten löscht.

---

Indem es nur die Hauptversionsnummern der Pakete speichert.

---

Indem es Pakete automatisch auf ihre neuesten Versionen aktualisiert.

#### --answer--

Indem es die genauen Versionen jeder installierten Abhängigkeit einfriert.

### --question--

#### --text--

Warum sollten Sie `package-lock.json` in Ihr Git-Repository einchecken?

#### --distractors--

Um npm auf der Maschine eines Entwicklers schneller laufen zu lassen.

---

Um veraltete Abhängigkeiten automatisch zu aktualisieren.

---

Um die Repository-Größe zu reduzieren.

#### --answer--

Damit das gesamte Team dieselben Abhängigkeitsversionen und Setups installiert.

### --question--

#### --text--

Wie installieren Sie eine bestimmte Version eines Pakets, wie `express@4.21.2`?

#### --distractors--

`npm update express@4.21.2`.

---

`npm get express@4.21.2`.

---

`npm use express@4.21.2`.

#### --answer--

`npm install express@4.21.2`.

### --question--

#### --text--

Was erlaubt das Caret-Zeichen (`^`), wenn Sie Abhängigkeitsversionen angeben?

#### --distractors--

Es sperrt die Abhängigkeit auf eine genaue Patch-Version.

---

Es erlaubt nur Updates der Hauptversion.

---

Es schließt alle Versionen unterhalb der angegebenen aus.

#### --answer--

Es erlaubt neuere Neben- und Patch-Versionen, blockiert aber neue Hauptversionen.

### --question--

#### --text--

Was bedeutet das Tilde-Zeichen (`~`) in einem Versionsbereich wie `~1.2.3`?

#### --distractors--

Es erlaubt Updates sowohl der Haupt- als auch der Nebenversion.

---

Es installiert die neueste Hauptversion unabhängig vom Bereich.

---

Es verhindert, dass npm überhaupt Updates installiert.

#### --answer--

Es erlaubt nur Updates der Patch-Version, während die Nebenversion fest bleibt.

### --question--

#### --text--

Welche Art von Änderungen löst bei der semantischen Versionierung eine Hauptversions-Erhöhung aus?

#### --distractors--

Kleine stilistische Anpassungen an Kommentaren und Formatierung.

---

Kleine Bugfixes, die das Verhalten nicht ändern.

---

Optionale Funktionen, die bestehenden Code nicht brechen.

#### --answer--

Breaking Changes, die dazu führen können, dass bestehender Code nicht mehr funktioniert.

### --question--

#### --text--

Welcher Befehl listet Abhängigkeiten auf, für die neuere Versionen veröffentlicht wurden?

#### --distractors--

`npm check`.

---

`npm list`.

---

`npm refresh`.

#### --answer--

`npm outdated`.

### --question--

#### --text--

Yarn, PNPM und Bun werden als Alternativen zu npm genannt. Was für Werkzeuge sind sie?

#### --distractors--

Registry-Websites, auf denen Sie npm-Pakete veröffentlichen oder herunterladen.

---

Datenbankdienste, die Benutzerdaten für npm-Pakete speichern.

---

Cloud-Hosting-Plattformen, die npm-Projekte automatisch bereitstellen.

#### --answer--

Alternative Paketmanager.

### --question--

#### --text--

Wo werden benutzerdefinierte npm-Skripte in einem Projekt deklariert?

#### --distractors--

Innerhalb der `.npmrc`-Datei.

---

Innerhalb des `"dependencies"`-Abschnitts von `package.json`.

---

In einer separaten `scripts.json`-Datei im Stammverzeichnis.

#### --answer--

Innerhalb des `"scripts"`-Objekts von `package.json`.

### --question--

#### --text--

Wie führen Sie ein benutzerdefiniertes npm-Skript namens `build` aus?

#### --distractors--

`build npm`.

---

`node build`.

---

`run build`.

#### --answer--

`npm run build`.

### --question--

#### --text--

Was bewirkt der Doppelstrich (`--`), wenn Sie `npm run start -- 8000` ausführen?

#### --distractors--

Er unterdrückt die Skriptausgabe.

---

Er zwingt npm, das Skript parallel auszuführen.

---

Er fordert vor der Ausführung des Skripts Administratorrechte an.

#### --answer--

Er sagt npm, alles nach `--` als Argumente an den zugrunde liegenden Befehl weiterzuleiten.
