---
id: 66f1a2009e65c9a40a26d51e
title: "Основи React: тест"
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

Щоб пройти тест, потрібно правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який із наведених варіантів не є бібліотекою або фреймворком JavaScript?

#### --distractors--

Angular

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

Який із наведених способів НЕ є методом умовного відтворення в React?

#### --distractors--

Тернарні оператори.

---

Логічний `&&` оператор.

---

`if` інструкції.

#### --answer--

`setTimeout`

### --question--

#### --text--

Яка основна мета додавання пропса `key` під час відтворення списку в React?

#### --distractors--

Щоб застосувати унікальний CSS-стиль до кожного елемента списку.

---

Щоб слугувати текстовим вмістом для елемента.

---

Щоб додати слухача подій до кожного конкретного елемента списку.

#### --answer--

Щоб допомогти React ідентифікувати елементи для ефективного відтворення.

### --question--

#### --text--

Де встановлюються пакети та бібліотеки проєкту?

#### --distractors--

У директорії користувача вашого комп’ютера.

---

У файлі `package.json`.

---

У папці `src`.

#### --answer--

У папці `node_modules`.

### --question--

#### --text--

Що з наведеного є ПРАВДОЮ про JSX?

#### --distractors--

JSX завжди має писатися у окремих файлах від JavaScript.

---

JSX не може використовувати JavaScript-вирази.

---

JSX не рекомендується для React-застосунків.

#### --answer--

JSX дозволяє писати HTML всередині JavaScript-коду.

### --question--

#### --text--

Яка з наведених проблем є поширеною для SPA?

#### --distractors--

Ресурси сервера використовуються інтенсивніше через постійне відтворення сторінки.

---

Кешування браузера стає ненадійним через часті оновлення DOM.

---

Багаторазове завантаження сторінок створює непослідовне керування станом.

#### --answer--

Читачам екрана важко працювати з динамічно оновлюваним вмістом.

### --question--

#### --text--

Як зазвичай оновлює вигляд односторінковий застосунок?

#### --distractors--

Повним перезавантаженням сервера.

---

Перезавантаженням усього HTML-документа.

---

Використанням лише серверного відтворення.

#### --answer--

Оновленням лише необхідних частин сторінки.

### --question--

#### --text--

Яка з наведених команд запускає React-проєкт у режимі розробки?

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

Який із наведених варіантів не є дійсним JSX-елементом?

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

Який метод масиву JavaScript зазвичай використовується для перетворення масиву даних у масив JSX-елементів?

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

Яка найкраща практика для передачі пропса `key` у React?

#### --distractors--

Використовувати індекси масиву у всіх випадках.

---

Випадково генерувати ключі при кожному відтворенні.

---

Повністю опускати пропс `key`.

#### --answer--

Використовувати унікальні та стабільні ідентифікатори з даних.

### --question--

#### --text--

Яка команда встановлює залежності, перелічені у `package.json`?

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

Як передати пропс `name` дочірньому компоненту?

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

Який правильний спосіб передати кілька пропсів за допомогою оператора розширення в React?

#### --distractors--

`<Child person={name..., age..., city...} />`.

---

`<Child spreader={...person} />`.

---

`<Child spread={person} />`.

#### --answer--

`<Child {...person} />`.

### --question--

#### --text--

Що є будівельними блоками React?

#### --distractors--

Функції та модулі.

---

DOM-елементи та події.

---

Класи та об’єкти.

#### --answer--

Повторно використовувані компоненти.

### --question--

#### --text--

Який правильний спосіб додати рядковий стиль у React?

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

Який правильний спосіб умовно відтворити React-компонент?

#### --distractors--

Використання `if` інструкцій всередині JSX.

---

Оголошення нового компонента всередині методу `render()`.

---

Використання `while` циклів всередині JSX.

#### --answer--

Використання тернарних операторів або логічних `&&` операторів у JSX.

### --question--

#### --text--

Яке ключове слово використовується для імпорту компонента в інший файл?

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

Яке повне значення абревіатури SPA?

#### --distractors--

Server Page Architectures.

---

Selective Page Applications.

---

Static Progressive Apps.

#### --answer--

Single-page Applications.

### --question--

#### --text--

Яке розширення синтаксису JavaScript використовується для опису інтерфейсу користувача в React?

#### --distractors--

TSC

---

HTML+

---

ReactML

#### --answer--

JSX
