---
id: 66edd3011f18f4ee1bd9d28b
title: JavaScript-Quiz zu regulären Ausdrücken
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 Fragen unten richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche der folgenden Optionen ist KEIN gültiger regulärer Ausdruck?

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

Welcher `RegExp` enthält dasselbe Muster wie der reguläre Ausdruck `/f[o0]{2} b[a4@]r/i`?

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

Welchen Rückgabetyp hat die reguläre Ausdrucks-Methode `test`?

#### --distractors--

Ein Array von Strings, die dem regulären Ausdruck entsprechen.

---

Ein Objekt mit Informationen über den (Teil-)String, der vom regulären Ausdruck erfasst wurde.

---

`null`, die Methode `test` prüft nur, ob der angegebene reguläre Ausdruck gültig ist.

#### --answer--

Ein Boolean, der angibt, ob der String dem regulären Ausdruck entspricht.

### --question--

#### --text--

Was macht die `match`-Methode, wenn ihr ein regulärer Ausdruck ohne Flags übergeben wird?

#### --distractors--

Sie sucht nach allen Strings, die dem regulären Ausdruck entsprechen, und gibt ALLE Treffer als Array zurück.

---

Sie sucht nach dem ersten vollständigen Treffer und gibt den Startindex dieses Treffers zurück.

---

Sie sucht nach dem ersten vollständigen Treffer und gibt einen Boolean zurück, der angibt, ob ein Treffer gefunden wurde.

#### --answer--

Sie sucht nach dem ersten vollständigen Treffer und gibt ein Array zurück, das diesen ersten Treffer enthält.

### --question--

#### --text--

Für welchen Anwendungsfall ist `test` am besten geeignet?

#### --distractors--

Zur Überprüfung eines gegebenen regulären Ausdrucks.

---

Um detaillierte Informationen über einen String-Treffer zu erhalten, wie z. B. die Länge des erfassten Strings.

---

Um alle Treffer aus einem gegebenen String zu extrahieren.

#### --answer--

Um zu prüfen, ob ein gegebener String dem regulären Ausdruck entspricht.

### --question--

#### --text--

Was ist der Zweck der Verwendung von `replace`?

#### --distractors--

Um den aktuellen regulären Ausdruck durch einen neuen zu ersetzen.

---

Wenn ein Index und ein String angegeben sind, wird das Zeichen am angegebenen Index durch den bereitgestellten String ersetzt.

---

Um alle Instanzen eines erfassten Teilstrings aus einem String zu entfernen.

#### --answer--

Um einen erfassten String durch einen angegebenen Ersatzstring zu ersetzen.

### --question--

#### --text--

Was ist der Unterschied zwischen `match` und `matchAll`?

#### --distractors--

`match` gibt immer einen String zurück, während `matchAll` immer ein Array von Treffern zurückgibt.

---

`match` gibt einen Boolean zurück, der angibt, ob ein Treffer gefunden wurde, während `matchAll` eine Zahl zurückgibt, die die Anzahl der gefundenen Treffer darstellt.

---

`match` gibt eine Zahl zurück, die den Startindex des ersten gefundenen Treffers angibt, während `matchAll` ein Array von Zahlen mit Indizes zurückgibt, an denen Treffer gefunden wurden.

#### --answer--

`match` gibt ein Array von Strings zurück, die übereinstimmen, während `matchAll` ein Iterator-Objekt aller gefundenen Treffer zurückgibt.

### --question--

#### --text--

Welche der folgenden Verwendungen von `replaceAll` ist korrekt?

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

Welche der folgenden Zeichenklassen entspricht dem regulären Ausdruck `/[a-zA-Z0-9_]/`?

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

Welche der folgenden Zeichenklassen ist am besten geeignet, um Ziffern aus einer Telefonnummer zu extrahieren?

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

Was passiert, wenn in einem regulären Ausdruck eine Vorausschau `(?=)` verwendet wird?

#### --distractors--

Die Vorausschau prüft links vom String, ob das Teilmuster VORHANDEN ist.

---

Eine Vorausschau ist ein alternativer regulärer Ausdruck, der verwendet wird, wenn das Hauptmuster nicht passt.

---

Die Vorausschau prüft rechts vom String, ob das Teilmuster NICHT vorhanden ist.

#### --answer--

Eine Vorausschau prüft rechts vom String, ob das Teilmuster VORHANDEN ist.

### --question--

#### --text--

Wo sollte eine Rückwärtssuche `(?<=)` in einem regulären Ausdruck platziert werden?

#### --distractors--

Die Rückwärtssuche kann überall im regulären Ausdruck platziert werden.

---

Die Rückwärtssuche sollte rechts vom Hauptmuster des regulären Ausdrucks platziert werden.

---

Die Rückwärtssuche sollte das Hauptmuster des regulären Ausdrucks einschließen.

#### --answer--

Die Rückwärtssuche sollte links vom Hauptmuster des regulären Ausdrucks platziert werden.

### --question--

#### --text--

Welcher Quantifizierer entspricht dem vorhergehenden Element null oder einmal?

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

Welcher der folgenden regulären Ausdrücke erlaubt nur Zahlen zwischen 0 und 999.999?

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

Welche der folgenden Aussagen trifft auf die benutzerdefinierte Zeichenklasse (`[]`) zu?

#### --distractors--

Es ist eine Menge von Zeichen, die aus dem Treffer entfernt werden sollen.

---

Sie kann eine Menge von Zeichen definieren, die ohne Escape von Sonderzeichen übereinstimmen.

---

Sie stellt eine Menge von Zeichen in Unicode-Form dar.

#### --answer--

Sie kann eine benutzerdefinierte Menge von Zeichen definieren, die übereinstimmen sollen.

### --question--

#### --text--

Welche der folgenden Zeichenklassen entspricht korrekt dem Großbuchstaben-Alphabet?

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

Was passiert, wenn in einem regulären Ausdruck eine Capturing-Gruppe `(...)` verwendet wird?

#### --distractors--

Die Capturing-Gruppe wird sofort ausgewertet, egal wo sie im regulären Ausdruck steht.

---

Die Capturing-Gruppe versucht mit dem angegebenen Teilmuster zu matchen und fährt fort, ohne das Ergebnis zu speichern.

---

Die Capturing-Gruppe ist ein priorisiertes Teilmuster, das sofort zurückkehrt, wenn ein Treffer gefunden wird.

#### --answer--

Die Capturing-Gruppe versucht mit dem angegebenen Teilmuster zu matchen und fährt mit dem Ergebnis im Speicher fort.

### --question--

#### --text--

Was passiert, wenn in einem regulären Ausdruck eine Non-Capturing-Gruppe `(?:...)` verwendet wird?

#### --distractors--

Die Non-Capturing-Gruppe versucht mit dem angegebenen Teilmuster zu matchen und fährt mit dem Ergebnis im Speicher fort.

---

Die Non-Capturing-Gruppe beendet sofort, wenn sie einen Treffer für das angegebene Teilmuster findet.

---

Die Non-Capturing-Gruppe gilt als optionaler Treffer, sodass nur ein erfolgreicher Treffer zum Ergebnis hinzugefügt wird.

#### --answer--

Die Non-Capturing-Gruppe versucht mit dem angegebenen Teilmuster zu matchen und fährt fort, ohne das Ergebnis im Speicher zu behalten.

### --question--

#### --text--

Welcher der folgenden Tests gibt bei folgendem regulären Ausdruck `true` zurück?

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

Was bewirkt die Wildcard-Zeichenklasse (`.`) in einem regulären Ausdruck?

#### --distractors--

Sie entspricht nur Leerraumzeichen.

---

Sie entspricht dem Anfang eines Strings.

---

Sie entspricht einer einzelnen Ziffer.

#### --answer--

Sie entspricht jedem einzelnen Zeichen außer einem Zeilenumbruch.
