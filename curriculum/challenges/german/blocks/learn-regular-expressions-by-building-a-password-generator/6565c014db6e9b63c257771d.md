---
id: 6565c014db6e9b63c257771d
title: Schritt 58
challengeType: 20
dashedName: step-58
---

# --description--

Direkt vor Ihrer `for`-Schleife deklarieren Sie eine `count`-Variable und weisen dieser Variable den Wert null zu.

# --hints--

Sie sollten eine `count`-Variable deklarieren und dieser Variable den Wert `0` zuweisen.

```js
({ test: () => assert.match(code, /count\s*=\s*0/) })
```

# --seed--

## --seed-contents--

```py
import re
import secrets
import string


def generate_password(length, nums, special_chars, uppercase, lowercase):
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    while True:
        password = ''
        # Generate password
        for _ in range(length):
            password += secrets.choice(all_characters)
       
        constraints = [
            (nums, r'\d'),
            (lowercase, r'[a-z]'),
            (uppercase, r'[A-Z]'),            
            (special_chars, fr'[{symbols}]')            
        ]
--fcc-editable-region--         
        # Check constraints
        
        for constraint, pattern in constraints:
            constraint <= len(re.findall(pattern, password))
--fcc-editable-region--
    return password

# new_password = generate_password(8)
# print(new_password)
```
