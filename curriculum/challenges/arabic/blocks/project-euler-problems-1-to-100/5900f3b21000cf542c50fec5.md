---
id: 5900f3b21000cf542c50fec5
title: "المشكلة 70: تبديل دالة أويلر"
challengeType: 1
forumTopicId: 302183
dashedName: problem-70-totient-permutation
---

# --description--

تُستخدم دالة أويلر ${\phi}(n)$ (تُسمى أحيانًا دالة في) لتحديد عدد الأعداد الموجبة التي تقل عن أو تساوي `n` والتي تكون أولية بالنسبة لـ `n`. على سبيل المثال، الأعداد 1، 2، 4، 5، 7، و8 كلها أقل من تسعة وأولية بالنسبة لتسعة، إذن ${\phi}(9) = 6$. يُعتبر العدد 1 أوليًا بالنسبة لكل عدد موجب، لذلك ${\phi}(1) = 1$.

من المثير للاهتمام أن ${\phi}(87109) = 79180$، ويمكن ملاحظة أن 87109 هو تبديل للعدد 79180.

ابحث عن قيمة `n` حيث 1 &lt; `n` &lt; `limit`، بحيث تكون ${\phi}(n)$ تبديلًا لـ `n` ونسبة $\displaystyle\frac{n}{{\phi}(n)}$ تحقق الحد الأدنى.

# --hints--

يجب أن تُرجع الدالة `totientPermutation(10000)` عددًا.

```js
assert(typeof totientPermutation(10000) === 'number');
```

يجب أن تُرجع الدالة `totientPermutation(10000)` القيمة `4435`.

```js
assert.strictEqual(totientPermutation(10000), 4435);
```

يجب أن تُرجع الدالة `totientPermutation(100000)` القيمة `75841`.

```js
assert.strictEqual(totientPermutation(100000), 75841);
```

يجب أن تُرجع الدالة `totientPermutation(500000)` القيمة `474883`.

```js
assert.strictEqual(totientPermutation(500000), 474883);
```

يجب أن تُرجع الدالة `totientPermutation(10000000)` القيمة `8319823`.

```js
assert.strictEqual(totientPermutation(10000000), 8319823);
```

# --seed--

## --seed-contents--

```js
function totientPermutation(limit) {

  return true;
}

totientPermutation(10000);
```

# --solutions--

```js
function totientPermutation(limit) {
  function getSievePrimes(max) {
    const primes = [];
    const primesMap = new Array(max).fill(true);
    primesMap[0] = false;
    primesMap[1] = false;

    for (let i = 2; i < max; i += 2) {
      if (primesMap[i]) {
        primes.push(i);
        for (let j = i * i; j < max; j += i) {
          primesMap[j] = false;
        }
      }
      if (i === 2) {
        i = 1;
      }
    }
    return primes;
  }

  function sortDigits(number) {
    return number.toString().split('').sort().join('');
  }

  function isPermutation(numberA, numberB) {
    return sortDigits(numberA) === sortDigits(numberB);
  }

  const MAX_PRIME = 4000;
  const primes = getSievePrimes(MAX_PRIME);

  let nValue = 1;
  let minRatio = Infinity;

  for (let i = 1; i < primes.length; i++) {
    for (let j = i + 1; j < primes.length; j++) {
      const num = primes[i] * primes[j];
      if (num > limit) {
        break;
      }

      const phi = (primes[i] - 1) * (primes[j] - 1);
      const ratio = num / phi;

      if (minRatio > ratio && isPermutation(num, phi)) {
        nValue = num;
        minRatio = ratio;
      }
    }
  }
  return nValue;
}
```
