---
id: 66edd10913f078e7669eca81
title: JavaScript 偵錯測驗
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

當變數在定義之前被使用時，會發生什麼型別的錯誤？

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

哪一個 JavaScript 述語可以用來故意暫停你的程式碼執行以進行偵錯？

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

哪一個主控台方法可以用來將資料顯示為表格？

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

你如何在 Chrome 開發者工具中檢查網路請求？

#### --distractors--

透過使用「Network Links」分頁。

---

透過使用「主控台」分頁。

---

透過使用「Sources」標籤。

#### --answer--

透過使用「Network」分頁。

### --question--

#### --text--

在 JavaScript 中使用 `try...catch` 的正確語法是什麼？

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

以下哪一項是 `SyntaxError` 的範例？

#### --distractors--

在宣告 `variable` 之前使用它。

---

在相同範圍內使用同一個 `variable` 兩次。

---

使用超出範圍的 `variable`。

#### --answer--

在陣列元素之間遺漏逗號。

### --question--

#### --text--

在 JavaScript 偵錯中，中斷點的目的為何？

#### --distractors--

自動修正程式碼錯誤。

---

防止程式碼被執行。

---

將訊息記錄到主控台。

#### --answer--

在特定點暫停執行。

### --question--

#### --text--

當執行 `throw` 述語時會發生什麼？

#### --distractors--

程式立即終止，忽略所有剩餘的程式碼。

---

`throw` 述語會將錯誤記錄到主控台，但不會停止執行。

---

它會暫停程式碼執行，直到透過停用中斷點恢復。

#### --answer--

當發出例外時，會執行 `catch` 區塊（如果存在）。

### --question--

#### --text--

在開發者工具中偵錯時，你會使用哪種方法來檢查變數或表達式？

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

執行以下程式碼時會發生什麼型別的錯誤？

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

堆疊上限溢位(相對於 underflow)錯誤

#### --answer--

`ReferenceError`

### --question--

#### --text--

在偵錯期間，哪個工具通常用來逐行執行 `code`？

#### --distractors--

`console.log()`

---

`alert()`

---

`throw` 述語。

#### --answer--

開發者工具中的中斷點。

### --question--

#### --text--

當嘗試存取 `undefined` 的屬性時會發生什麼型別的錯誤？

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

`try...catch` 述語中的 `finally` 區塊作用是什麼？

#### --distractors--

只有在發生錯誤時才執行。

---

如果觸發 `catch`，則跳過執行。

---

立即離開程式碼區塊。

#### --answer--

無論是否發生錯誤，皆會執行。

### --question--

#### --text--

執行以下程式碼會得到什麼結果？

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

程式碼將會丟出 `SyntaxError`。

---

程式碼將在主控台記錄一個空的陣列。

---

程式碼將會丟出記憶體不足錯誤。

#### --answer--

程式碼會丟出 `RangeError`。

### --question--

#### --text--

以下哪一個關於 JavaScript 中 `debugger` 述語的答案是正確的？

#### --distractors--

瀏覽器總是忽略 `debugger` 述語。

---

`debugger` 述語會將錯誤報告傳送到伺服器。

---

`debugger` 述語總是重新載入頁面並清除主控台。

#### --answer--

當開發者工具未開啟時，會忽略 `debugger` 述語。

### --question--

#### --text--

JavaScript 中「profiling」的主要目的是什麼？

#### --distractors--

為 JavaScript 函式（程式）撰寫單元測試並建立程式碼執行的評測。

---

暫停程式碼執行並釋放所捕獲的資源，如記憶體和 CPU。

---

將 JavaScript 程式碼轉換成機器碼以加快執行速度。

#### --answer--

透過記錄 CPU 使用率、函式呼叫和執行時間來識別效率瓶頸。

### --question--

#### --text--

在偵錯的背景關係中，什麼是「watcher」？

#### --distractors--

一個用於暫停程式碼執行的工具。

---

一個用於記錄錯誤的內建函式（程式）。

---

用於處理例外的 JavaScript 方法。

#### --answer--

用於監控變數值的工具。

### --question--

#### --text--

你應該在什麼時候使用 `console.error()` 而不是 `console.log()`？

#### --distractors--

要顯示表格資料。

---

僅在偵錯期間記錄訊息。

---

記錄關於瀏覽器的資訊。

#### --answer--

記錄錯誤訊息。

### --question--

#### --text--

`Error()` 建構式在 JavaScript 中的作用是什麼？

#### --distractors--

將錯誤訊息記錄到主控台。

---

在沙盒中執行程式碼。

---

終止程式。

#### --answer--

創建一個可以被丟出的新錯誤物件。

### --question--

#### --text--

`console.dir()` 方法在 JavaScript 中的用途是什麼？

#### --distractors--

為個別錯誤型別創建一個獨立的日誌檔案目錄。

---

為你的主控台輸出產生視覺表現並將其存檔到 pdf 檔案中。

---

停止程式執行並顯示與記憶體和 CPU 使用率相關的錯誤訊息。

#### --answer--

輸出指定 JavaScript 物件的階層式、互動式屬性列表。

