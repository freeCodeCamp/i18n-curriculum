---
id: 67f41242431cbf3db8ca79c7
title: Quiz sulle basi di Python
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale delle seguenti funzioni si usa per verificare se una variabile corrisponde a un tipo di dato specifico?

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

Quale delle seguenti NON è una forma di concatenazione di stringhe?

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

Quale delle seguenti funzioni si usa per restituire il numero di caratteri in una stringa?

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

Cosa sarà `result` in questo esempio?

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

Cosa succede quando sommi un float e un intero?

#### --distractors--

Il risultato sarà un messaggio di errore.

---

Il risultato sarà `None`.

---

Il risultato sarà un intero.

#### --answer--

Il risultato sarà un float.

### --question--

#### --text--

Qual è il modo corretto per definire una funzione?

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

Cosa verrà stampato sulla console?

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

Quale delle seguenti affermazioni è falsa riguardo alla denominazione delle variabili?

#### --distractors--

I nomi delle variabili possono iniziare solo con una lettera o un underscore (_), non con un numero.

---

I nomi delle variabili possono contenere solo caratteri alfanumerici (a-z, A-Z, 0-9) e underscore (_).

---

I nomi delle variabili non possono essere una delle parole chiave riservate di Python come `if`, `class` o `def`.

#### --answer--

I nomi delle variabili devono avere una lunghezza massima di 10 caratteri, altrimenti Python lancerà un errore.

### --question--

#### --text--

Quale dei seguenti NON è un tipo di dato supportato in Python?

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

Quale delle seguenti restituirà una nuova stringa con tutti i caratteri convertiti in maiuscolo?

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

Quale delle seguenti funzioni si usa per ottenere un input dall’utente?

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

Quale sarà il risultato del seguente codice?

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

Cosa fa il metodo `split()`?

#### --distractors--

Questo metodo serve a dividere una tupla in una lista di sottostringhe.

---

Questo metodo serve a dividere un float in una lista di sottostringhe.

---

Questo metodo serve a dividere un dizionario in una lista di sottostringhe.

#### --answer--

Questo metodo serve a dividere una stringa in una lista di sottostringhe.

### --question--

#### --text--

Cosa stamperà sulla console il seguente codice?

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

Quale delle seguenti funzioni si usa per creare una tabella di mappature da 1 a 1 carattere per la traduzione?

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

Quale sarà il risultato del seguente codice?

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

Cosa restituisce il metodo `find()` se non trova occorrenze della sottostringa in una stringa?

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

Quale delle seguenti funzioni si usa per contare quante volte una sottostringa appare in una stringa?

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

Cosa fa la divisione intera in Python?

#### --distractors--

Questo operatore serve a moltiplicare due numeri e arrotondare il risultato per eccesso al numero intero più vicino.

---

Questo operatore serve a convertire un float in un intero.

---

Questo operatore serve a elevare un numero alla potenza di un altro.

#### --answer--

Questo operatore serve a dividere due numeri e arrotondare il risultato per difetto al numero intero più vicino.

### --question--

#### --text--

Quale delle seguenti funzioni si usa per arrotondare un numero all’intero più vicino?

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`
