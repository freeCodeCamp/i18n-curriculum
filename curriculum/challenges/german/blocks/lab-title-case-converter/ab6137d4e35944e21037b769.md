---
id: ab6137d4e35944e21037b769
title: Erstellen Sie einen Titel-Case-Konverter
challengeType: 26
dashedName: build-a-title-case-converter
---

# --description--

In diesem Labor erstellen Sie eine Funktion, die einen String in Titel-Case umwandelt. Titel-Case bedeutet, dass der erste Buchstabe jedes Wortes großgeschrieben wird und der Rest des Wortes in Kleinbuchstaben steht.

`"Web Development Is Awesome"` ist ein Beispiel für einen String im Titel-Case.

**Ziel**: Erfüllen Sie die untenstehenden User Stories und bestehen Sie alle Tests, um das Labor abzuschließen.

**User Stories:**

1. Sie sollten eine `titleCase`-Funktion haben, die einen String als Argument entgegennimmt.
1. Die `titleCase`-Funktion sollte einen String zurückgeben, bei dem der erste Buchstabe jedes Wortes großgeschrieben ist und der Rest des Wortes in Kleinbuchstaben.
1. `titleCase("I like to code")` sollte `"I Like To Code"` zurückgeben.
1. `titleCase("javaScript is fun")` sollte `"Javascript Is Fun"` zurückgeben.

# --hints--

Sie sollten eine Funktion mit dem Namen `titleCase` erstellen.

```js
assert.isFunction(titleCase);
```

`titleCase` sollte ein einzelnes Argument entgegennehmen.

```js
assert.lengthOf(titleCase, 1);
```


`titleCase("I'm a little tea pot")` sollte einen String zurückgeben.

```js
assert.isString(titleCase("I'm a little tea pot"));
```

`titleCase("I'm a little tea pot")` sollte den String `I'm A Little Tea Pot` zurückgeben.

```js
assert.strictEqual(titleCase("I'm a little tea pot"), "I'm A Little Tea Pot");
```

`titleCase("sHoRt AnD sToUt")` sollte den String `Short And Stout` zurückgeben.

```js
assert.strictEqual(titleCase('sHoRt AnD sToUt'), 'Short And Stout');
```

`titleCase("HERE IS MY HANDLE HERE IS MY SPOUT")` sollte den String `Here Is My Handle Here Is My Spout` zurückgeben.

```js
assert.strictEqual(
  titleCase('HERE IS MY HANDLE HERE IS MY SPOUT'),
  'Here Is My Handle Here Is My Spout'
);
```

# --seed--

## --seed-contents--

```js

```

# --solutions--

```js
function titleCase(str) {
  return str
    .split(' ')
    .map(word => word.charAt(0).toUpperCase() + word.substring(1).toLowerCase())
    .join(' ');
}
```
