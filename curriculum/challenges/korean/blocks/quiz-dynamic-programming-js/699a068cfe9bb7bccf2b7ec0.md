---
id: 699a068cfe9bb7bccf2b7ec0
title: 동적 프로그래밍 퀴즈
challengeType: 8
dashedName: quiz-dynamic-programming-js
---

# --description--

퀴즈를 통과하려면 아래 10문제 중 최소 9문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

동적 프로그래밍이 효과적인 해결 방법이 되기 위해 문제에 반드시 존재해야 하는 두 가지 필수 속성은 무엇인가요?

#### --distractors--

빠른 실행 시간과 최소한의 메모리 사용

---

재귀 기능과 반복 루프

---

순차 처리와 병렬 계산

#### --answer--

중복되는 부분 문제와 최적 부분 구조

### --question--

#### --text--

동적 프로그래밍에서 메모이제이션과 테이블화 접근법의 주요 차이점은 무엇인가요?

#### --distractors--

메모이제이션은 해시 테이블을 사용하고 테이블화는 배열을 사용하여 더 효율적이다.

---

메모이제이션은 더 빠르지만 테이블화보다 더 많은 메모리와 CPU 사이클을 사용한다.

---

메모이제이션은 테이블화보다 더 단순한 문제만 해결할 수 있다.

#### --answer--

메모이제이션은 재귀를 사용하는 상향식 접근법이고, 테이블화는 반복을 사용하는 하향식 접근법이다.

### --question--

#### --text--

동적 프로그래밍 문제에 대한 순진한 재귀적 해결책이 일반적으로 지수 시간 복잡도를 가지는 이유는 무엇인가요?

#### --distractors--

변수를 저장하기 위해 지수적인 양의 메모리를 사용하기 때문이다.

---

데이터를 지수 시간 내에 정렬해야 하기 때문이다.

---

입력의 모든 가능한 순열을 확인해야 하기 때문이다.

#### --answer--

각 재귀 호출이 여러 번 분기하여 동일한 부분 문제를 반복해서 다시 계산하기 때문이다.

### --question--

#### --text--

동적 프로그래밍에서 최적 부분 구조란 무엇을 의미하나요?

#### --distractors--

알고리즘은 가장 효율적인 자료 구조를 사용해야 한다.

---

해결책은 시간과 공간 복잡도를 동시에 최소화해야 한다.

---

문제는 유일하고 단일한 최적 해를 가져야 한다.

#### --answer--

최적 해는 그 부분 문제들의 최적 해로부터 구성할 수 있다.

### --question--

#### --text--

메모이제이션을 구현할 때, 이미 계산된 인수로 함수가 호출되면 어떻게 되나요?

#### --distractors--

정확성을 위해 함수가 결과를 다시 계산한다.

---

더 나은 정밀도를 위해 이전 결과와 새 결과를 평균 낸다.

---

중복 계산이 허용되지 않아 오류가 발생한다.

#### --answer--

캐시된 결과가 즉시 재계산 없이 반환된다.

### --question--

#### --text--

메모이제이션 대신 테이블화를 사용할 때의 주요 장점은 무엇인가요?

#### --distractors--

테이블화는 항상 메모리를 덜 사용한다.

---

테이블화는 더 넓은 범위의 문제를 해결할 수 있다.

---

테이블화는 항상 구현과 이해가 더 쉽다.

#### --answer--

테이블화는 재귀 오버헤드를 피하고 예측 가능한 순차 실행을 제공한다.

### --question--

#### --text--

하향식 동적 프로그래밍 해결책에서 기본 사례를 먼저 초기화하는 이유는 무엇인가요?

#### --distractors--

자료 구조에 메모리를 효율적으로 할당하기 위해서이다.

---

알고리즘에서 무한 루프를 방지하기 위해서이다.

---

알고리즘의 시간 복잡도를 개선하기 위해서이다.

#### --answer--

더 큰 모든 부분 문제가 기반할 수 있는 기초 값을 제공하기 위해서이다.

### --question--

#### --text--

동적 프로그래밍은 중복되는 부분 문제가 있는 문제의 시간 복잡도를 어떻게 변환하나요?

#### --distractors--

문제를 효율적으로 나누어 다항식에서 로그로 변환한다.

---

루프 구조를 최적화하여 이차에서 선형으로 변환한다.

---

해시 테이블을 사용하여 선형에서 상수로 변환한다.

#### --answer--

부분 문제 해를 저장하고 재사용하여 지수에서 다항식으로 변환한다.

### --question--

#### --text--

동적 프로그래밍이 더 나은 시간 복잡도를 달성하기 위해 일반적으로 하는 트레이드오프는 무엇인가요?

#### --distractors--

더 빠른 실행을 위해 코드 가독성을 희생한다.

---

유지보수가 어려운 더 복잡한 알고리즘을 요구한다.

---

해결할 수 있는 문제 크기를 제한한다.

#### --answer--

중간 결과를 저장하기 위해 추가 공간을 사용한다.

### --question--

#### --text--

어떤 상황에서 동적 프로그래밍이 적절한 알고리즘적 접근법이 아닐까요?

#### --distractors--

문제가 최적 해를 찾아야 할 때

---

문제를 더 작은 부분 문제로 나눌 수 있을 때

---

공간 복잡도를 최소화해야 할 때

#### --answer--

부분 문제가 독립적이고 중복되지 않을 때
