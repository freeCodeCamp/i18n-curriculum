---
id: 6733692ffe1da034469f7917
title: Wie manipulieren Sie Stile mit Element.style und Element.classList?
challengeType: 19
dashedName: how-do-you-manipulate-styles-using-elementstyle-and-elementclasslist
---

# --interactive--

Es wird Situationen geben, in denen Sie die Stile eines HTML-Elements innerhalb Ihrer JavaScript-Datei manipulieren müssen. Ein Beispiel dafür ist, wenn Sie ein Menü ausblenden oder anzeigen möchten, wenn ein Benutzer auf eine Schaltfläche klickt.

In dieser Lektion lernen Sie, wie Sie Stile mit den Eigenschaften `Element.style` und `Element.classList` manipulieren.

Die Eigenschaft `Element.style` ist eine schreibgeschützte Eigenschaft, die den inline-Stil eines Elements darstellt. Sie können diese Eigenschaft verwenden, um den Stil eines Elements abzurufen oder festzulegen.

Hier ist ein Beispiel für ein Absatz-Element mit der ID `para`:

```html
<p id="para">This is a paragraph</p>
```

Sie können die Eigenschaft `style` verwenden, um die Farbe des Absatz-Elements auf Rot zu ändern:

:::interactive_editor

```html
<p id="para">This is a paragraph</p>
<script src="index.js"></script>
```

```js
const paraEl = document.getElementById("para");
paraEl.style.color = "red";
```

:::

Die Eigenschaft `style` kann verwendet werden, um viele CSS-Eigenschaften festzulegen, wie `color`, `background-color`, `font-size`, `font-weight` und so weiter.

Eine weitere Möglichkeit, Stile zu manipulieren, ist die Verwendung der Eigenschaft `Element.classList`. Die Eigenschaft `classList` ist eine schreibgeschützte Eigenschaft, mit der Sie Klassen zu einem Element hinzufügen, entfernen oder umschalten können. Schauen wir uns ein paar Beispiele an.

In diesem Beispiel fügen Sie einem Absatz-Element mit der ID `highlight` eine Klasse namens `para` hinzu. Im CSS setzen wir die Klasse `highlight`, um die Hintergrundfarbe des Absatz-Elements auf Gelb zu ändern. Im JavaScript fügen wir dem Absatz-Element mit der Methode `highlight` die Klasse `classList.add()` hinzu.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css" />
<p id="para">This is a paragraph</p>
<script src="index.js"></script>
```

```css
.highlight {
  background-color: yellow;
}
```

```js
const paraEl = document.getElementById("para");
paraEl.classList.add("highlight");
```

:::

Sie können auch mehrere Klassen zu einem Element hinzufügen, indem Sie sie als Argumente an die Methode `classList.add()` übergeben:

```js
classList.add("class1", "class2", "class3");
```

Wenn Sie eine Klasse von einem Element entfernen müssen, können Sie die Methode `classList.remove()` wie folgt verwenden:

```js
classList.remove("highlight");
```

Um eine Klasse bei einem Element umzuschalten, können Sie die Methode `classList.toggle()` verwenden. In diesem Beispiel haben wir ein `button`-Element mit der ID `toggle-btn` und ein `nav`-Element mit der ID `menu`. Im CSS haben wir einige Stile für das Menü und eine Klasse namens `show` hinzugefügt, die die Eigenschaft `display` des `.menu` auf `block` ändert. Im JavaScript greifen wir mit der Methode `button` auf das Menü-Element und das `getElementById()`-Element zu. Dann fügen wir dem `button`-Element einen Ereignis-Listener hinzu, der die Klasse `show` beim `.menu`-Element umschaltet, wenn die Schaltfläche angeklickt wird:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css" />
<button id="toggle-btn">Toggle Menu</button>

<nav id="menu" class="menu">
  <ul>
    <li>Home</li>
    <li>About</li>
    <li>Products</li>
  </ul>
</nav>
<script src="index.js"></script>
```

```css
.menu {
  display: none;
  background-color: lightgray;
  width: 50%;
  padding: 10px;
}

.menu.show {
  display: block;
}
```

```js
const menu = document.getElementById("menu");
const toggleBtn = document.getElementById("toggle-btn");

toggleBtn.addEventListener("click", () => menu.classList.toggle("show"));
```

:::

Wenn ein Benutzer auf die Schaltfläche klickt, wird die Klasse `show` zum `.menu`-Element hinzugefügt, und das Menü wird angezeigt. Wenn der Benutzer erneut auf die Schaltfläche klickt, wird die Klasse `show` vom `.menu`-Element entfernt, und das Menü wird ausgeblendet.

Durch die Verwendung von Methoden und Eigenschaften wie `style`, `add()`, `remove()` und `toggle()` können Sie die Stile eines Elements mit JavaScript einfach manipulieren und so dynamische und interaktive Webseiten erstellen.

# --questions--

## --text--

Welche Eigenschaft würden Sie verwenden, um die Farbe des Texts eines Elements direkt mit JavaScript zu ändern?

## --answers--

`Element.classList`

### --feedback--

Diese Eigenschaft ermöglicht es Ihnen, inline CSS-Stile direkt festzulegen.

---

`Element.style`

---

`Element.innerHTML`

### --feedback--

Diese Eigenschaft ermöglicht es Ihnen, inline CSS-Stile direkt festzulegen.

---

`Element.getAttribute`

### --feedback--

Diese Eigenschaft ermöglicht es Ihnen, inline CSS-Stile direkt festzulegen.

## --video-solution--

2

## --text--

Wie würden Sie mehrere Klassen mit der Eigenschaft `Element.classList` zu einem Element hinzufügen?

## --answers--

`classList.add("class1 class2 class3")`

### --feedback--

Überprüfen Sie den Abschnitt der Lektion, der das Hinzufügen mehrerer Klassen zu einem Element behandelt.

---

`classList.add("class1", "class2", "class3")`

---

`classList.append("class1", "class2", "class3")`

### --feedback--

Überprüfen Sie den Abschnitt der Lektion, der das Hinzufügen mehrerer Klassen zu einem Element behandelt.

---

`classList.insert("class1", "class2", "class3")`

### --feedback--

Überprüfen Sie den Abschnitt der Lektion, der das Hinzufügen mehrerer Klassen zu einem Element behandelt.

## --video-solution--

2

## --text--

Was passiert, wenn Sie die Schaltfläche im bereitgestellten Beispiel mehrmals anklicken?

## --answers--

Das Menü bleibt dauerhaft sichtbar.

### --feedback--

Der Ereignis-Listener der Schaltfläche verwendet eine Methode, die die Klasse im Menü abwechselt.

---

Das Menü bleibt dauerhaft ausgeblendet.

### --feedback--

Der Ereignis-Listener der Schaltfläche verwendet eine Methode, die die Klasse im Menü abwechselt.

---

Das Menü wechselt jedes Mal zwischen sichtbar und ausgeblendet, wenn die Schaltfläche angeklickt wird.

---

Das Menü zeigt eine Fehlermeldung an.

### --feedback--

Der Ereignis-Listener der Schaltfläche verwendet eine Methode, die die Klasse im Menü abwechselt.

## --video-solution--

3
