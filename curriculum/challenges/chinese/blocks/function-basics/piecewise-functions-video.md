---
id: 699e0353138be64fa313138f
title: 分段函数
challengeType: 11
videoId: C9hCmH9nj4o
dashedName: piecewise-functions
---

# --description--

在这个 `video` 中，你将学习分段 `function`。

# --questions--

## --text--

什么是分段函数？

## --answers--

一个由 y 轴上的多个子函数定义的 `function`。

---

一个由单一公式为所有 `x` 值定义的 `function`。

---

由一组点的 x 和 y 值定义的 `function`。

---

一个由多个子函数定义的 `function`。

## --video-solution--

4
