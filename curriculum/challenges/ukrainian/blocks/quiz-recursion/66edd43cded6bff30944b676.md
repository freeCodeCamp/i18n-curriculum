---
id: 66edd43cded6bff30944b676
title: Тест з рекурсії
challengeType: 8
dashedName: quiz-recursion
---

# --description--

Щоб пройти тест, потрібно правильно відповісти щонайменше на 9 із 10 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке рекурсія в програмуванні?

#### --distractors--

Метод сортування масивів.

---

Цикл, який ніколи не закінчується.

---

Функція, яка повертає `undefined`.

#### --answer--

Процес, у якому функція викликає сама себе.

### --question--

#### --text--

Який із наведених прикладів є рекурсією?

#### --distractors--

```js
function factorial(n) {
  let result = 1;

  while (n > 0) {
    result = result * n;
    n--;
  }

  return result;
}
```

---

```js
function factorial(n) {
  const arr = Array(n).fill().map((_, i) => i + 1);
  return arr.reduce((acc, curr) => acc * curr, 1);
}
```

---

```js
function factorial(n) {
  let result = 1;

  for (let i = n; i > 0; i--) {
    result *= i;
  }

  return result;
}
```

#### --answer--

```js
function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  }
  return n * factorial(n - 1);
}
```

### --question--

#### --text--

Що поверне наступна функція?

```js
function sum(n) {
  if (n === 0) return 0;
  return n + sum(n - 1);
}
sum(3);
```

#### --distractors--

3

---

0

---

1

#### --answer--

6

### --question--

#### --text--

Скільки разів буде викликана функція `mystery`?

```javascript
function mystery(n) {
  if (n <= 1) return 1;
  return mystery(n - 2);
}
mystery(5);
```

#### --distractors--

2

---

5

---

4

#### --answer--

3

### --question--

#### --text--

Що з наведеного є правильним щодо рекурсії?

#### --distractors--

Вона ніколи не повинна мати базовий випадок.

---

Її слід використовувати лише для послідовності Фібоначчі.

---

Її слід використовувати лише для факторіалів.

#### --answer--

Вона завжди повинна мати базовий випадок.

### --question--

#### --text--

Який із варіантів був би доречним базовим випадком для наведеного прикладу?

```js
function countDownToZero(number) {
  // Base case goes here.

  console.log(number);
  countDownToZero(number - 1);
}
```

#### --distractors--

```js
if (number > 0) {
  return;
}
```

---

```js
if (number !== 0) {
  return;
}
```

---

```js
if (number === 0) {
  return;
}
```

#### --answer--

```js
if (number < 0) {
  return;
}
```

### --question--

#### --text--

Чому рекурсія має мати базовий випадок?

#### --distractors--

Щоб гарантувати, що функція завжди має повернене значення.

---

Щоб зменшити кількість викликів функції.

---

Щоб гарантувати, що всі рекурсивні виклики виконуються у правильному порядку і не виникає `RecursionError`.

#### --answer--

Щоб надати функції спосіб вийти з рекурсивних викликів і запобігти нескінченним циклам.

### --question--

#### --text--

Що зробить ця рекурсивна функція?

```js
function repeatString(str) {
  return str + repeatString(str);
}
```

#### --distractors--

Поверне рядок двічі.

---

Поверне `undefined`.

---

Створить порожній рядок.

#### --answer--

Спричинить переповнення стеку.

### --question--

#### --text--

Що таке стек викликів?

#### --distractors--

Список викликів функцій, які були виконані.

---

Список значень, які були повернені функцією.

---

Спеціальна функція, яка викликає функцію рівно тричі.

#### --answer--

Структура даних, яка відстежує виклики функцій і порядок їх виконання.

### --question--

#### --text--

Що робить ця рекурсивна функція?

```js
function changeString(str) {
  if (str === "") return "";
  return changeString(str.slice(1)) + str[0];
}
```

#### --distractors--

Дублює рядок.

---

Видаляє голосні.

---

Видаляє пробіли.

#### --answer--

Реверсує рядок.
