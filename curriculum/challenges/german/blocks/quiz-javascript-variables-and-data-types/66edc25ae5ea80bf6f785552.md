---
id: 66edc25ae5ea80bf6f785552
title: JavaScript-Variablen und Datentypen Quiz
challengeType: 8
dashedName: quiz-javascript-variables-and-data-types
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche der folgenden Beschreibungen trifft am besten auf JavaScript zu?

#### --distractors--

Eine Auszeichnungssprache, die zum Gestalten von Websites verwendet wird.

---

Eine serverseitige Programmiersprache.

---

Ein Datenbankverwaltungssystem.

#### --answer--

Eine Programmiersprache, die für die Webentwicklung verwendet wird.

### --question--

#### --text--

Welcher der folgenden Punkte ist KEIN JavaScript-Datentyp?

#### --distractors--

`Number`

---

`Undefined`

---

`Object`

#### --answer--

`Double`

### --question--

#### --text--

Welche der folgenden Deklarationen ist KEINE gültige JavaScript-Variablendeklaration?

#### --distractors--

`const x = 0;`

---

`let x;`

---

`var x;`

#### --answer--

`public Int x;`

### --question--

#### --text--

Welche der folgenden ist eine gängige Benennungskonvention für Variablen in JavaScript?

#### --distractors--

JavaScript case.

---

Lightning case.

---

Giraffe case.

#### --answer--

Camel case.

### --question--

#### --text--

Was ist der Unterschied zwischen `let` und `const` Variablendeklarationen in JavaScript?

#### --distractors--

`const` kann weder neu zugewiesen noch neu deklariert werden. `let` kann neu zugewiesen und neu deklariert werden.

---

`const` kann neu zugewiesen und neu deklariert werden. `let` kann weder neu zugewiesen noch neu deklariert werden.

---

`const` kann neu zugewiesen, aber nicht neu deklariert werden. `let` kann neu deklariert, aber nicht neu zugewiesen werden.

#### --answer--

`const` kann weder neu zugewiesen noch neu deklariert werden. `let` kann neu zugewiesen, aber nicht neu deklariert werden.

### --question--

#### --text--

Warum gelten Strings in JavaScript als unveränderlich?

#### --distractors--

Sie können keine Strings mit Variablen erstellen.

---

Strings können nur mit Literalen erstellt werden.

---

Sie können Strings ändern, aber nur über globale Variablen.

#### --answer--

Sobald ein String erstellt wurde, können Sie seine Zeichen nicht direkt ändern.

### --question--

#### --text--

Wie würden Sie den String `Hello, world!` in der Konsole in JavaScript ausgeben?

#### --distractors--

`print("Hello, world!")`

---

`alert("Hello, world");`

---

`System.out.println("Hello, world!");`

#### --answer--

`console.log("Hello, world!");`

### --question--

#### --text--

Was bewirkt der folgende Code?

```js
let hello = "Hello";
hello += " world";
console.log(hello);
```

#### --distractors--

Weist `" world"` der Variablen `hello` neu zu und gibt sie in der Konsole aus.

---

Gibt die Anzahl der Zeichen in `Hello world` in der Konsole aus.

---

Verbindet `"Hello"` und `" world"` zu einem neuen String und gibt ihn aus, aber der Wert von `hello` bleibt `"Hello"`.

#### --answer--

Verbindet den aktuellen Wert von `hello` mit dem String `" world"`, weist ihn `hello` neu zu und gibt ihn in der Konsole aus.

### --question--

#### --text--

Welches JavaScript-Zeichen wird verwendet, um das Ende einer Anweisung zu markieren?

#### --distractors--

`:`

---

`,`

---

`.`

#### --answer--

`;`

### --question--

#### --text--

Welche der folgenden ist eine gültige Syntax für einen einzeiligen Kommentar in JavaScript?

#### --distractors--

`# This is a single line comment`

---

`-- This is a single line comment`

---

`/* This is a single line comment */`

#### --answer--

`// This is a single line comment`

### --question--

#### --text--

Dynamische Typisierung in JavaScript bedeutet:

#### --distractors--

Sie müssen den Datentyp einer Variable deklarieren, bevor Sie sie verwenden.

---

Sie können einer Variable eines anderen Typs keinen Wert zuweisen.

---

JavaScript führt vor der Ausführung eine Typprüfung durch, sodass typbezogene Fehler vor dem Ausführen des Codes geworfen werden.

#### --answer--

JavaScript leitet den Typ basierend auf dem zugewiesenen Wert der Variable ab.

### --question--

#### --text--

Welche der folgenden Anweisungen gibt den Typ der Variablen `x` in der Konsole aus?

#### --distractors--

`console.log(x.type);`

---

`console.log(typeOf(x));`

---

`console.log(isNaN(x));`

#### --answer--

`console.log(typeof x);`

### --question--

#### --text--

Was wird die Ausgabe von `console.log(typeof null);` sein?

#### --distractors--

`"undefined"`

---

`"null"`

---

`"NaN"`

#### --answer--

`"object"`

### --question--

#### --text--

Welche der folgenden ist ein gültiger mehrzeiliger Kommentar in JavaScript?

#### --distractors--

```js
// This is a
// multi-line
// comment
```

---

```py
'''
This is a
multi-line
comment
'''
```

---

```html
<!--
This is a
multi-line
comment
-->
```

#### --answer--

```js
/*
This is a
multi-line
comment
*/
```

### --question--

#### --text--

Welche der folgenden Deklarationen definiert eine Variable in JavaScript, die nicht geändert werden kann?

#### --distractors--

`var x = 0;`

---

`let x = 0;`

---

`final x = 0;`

#### --answer--

`const x = 0;`

### --question--

#### --text--

Welcher der folgenden ist ein gültiger Variablenname in JavaScript?

#### --distractors--

`tart@n`

---

`4cats`

---

`!!dogs`

#### --answer--

`$number`

### --question--

#### --text--

Welche der folgenden Methoden ist KEINE gültige String-Verkettung in JavaScript?

#### --distractors--

`console.log(string1 + " " + string2);`

---

`console.log("string1 " + string2);`

---

`console.log(string1.concat(" ", string2));`

#### --answer--

`console.log(string1 string2);`

### --question--

#### --text--

Welcher der folgenden Variablennamen verwendet camel case korrekt?

#### --distractors--

`a_long_variable_name`

---

`alongvariableName`

---

`ALongVariableName`

#### --answer--

`aLongVariableName`

### --question--

#### --text--

Warum ist es vorteilhaft, Semikolons explizit zu verwenden, obwohl JavaScript die automatische Semikolon-Einfügung hat?

#### --distractors--

Um die Ausführungsgeschwindigkeit des Codes zu erhöhen.

---

Um die Konsistenz mit anderen Programmiersprachen zu wahren.

---

Um besseres Debuggen und Fehlerverfolgung zu ermöglichen.

#### --answer--

Um die Lesbarkeit und Zuverlässigkeit des Codes zu verbessern.

### --question--

#### --text--

Welcher der folgenden Punkte ist KEIN JavaScript-Datentyp?

#### --distractors--

`Symbol`

---

`Boolean`

---

`Null`

#### --answer--

`Float`
