---
id: 69c5f3d787b1725d5f00c8b8
title: "Zoezi la 254: Maneno yasiyo na nambari mbili"
challengeType: 28
dashedName: challenge-254
---

# --description--

Ukipata mfuatano wa herufi wa maneno, rudisha maneno yenye idadi ya herufi isiyo na nambari mbili.

- Maneno katika mfuatano uliotolewa yataachwa na nafasi moja tu.
- Rudisha maneno yakiwa yameachwa na nafasi moja tu.

# --hints--

`getOddWords("This is a super good test")` inapaswa kurudisha `"a super"`.

```js
assert.equal(getOddWords("This is a super good test"), "a super");
```

`getOddWords("one two three four")` inapaswa kurudisha `"one two three"`.

```js
assert.equal(getOddWords("one two three four"), "one two three");
```

`getOddWords("banana split sundae with rainbow sprinkles on top")` inapaswa kurudisha `"split rainbow sprinkles top"`.

```js
assert.equal(getOddWords("banana split sundae with rainbow sprinkles on top"), "split rainbow sprinkles top");
```

`getOddWords("The quick brown fox jumped over the lazy river")` inapaswa kurudisha `"The quick brown fox the river"`.

```js
assert.equal(getOddWords("The quick brown fox jumped over the lazy river"), "The quick brown fox the river");
```

# --seed--

## --seed-contents--

```js
function getOddWords(str) {

  return str;
}
```

# --solutions--

```js
function getOddWords(str) {
  return str
    .split(" ")
    .filter(word => word.length % 2 === 1)
    .join(" ");
}
```
