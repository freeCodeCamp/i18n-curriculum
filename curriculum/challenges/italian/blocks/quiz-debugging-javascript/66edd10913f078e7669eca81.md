---
id: 66edd10913f078e7669eca81
title: Quiz sul debugging di JavaScript
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Che tipo di errore si verifica quando una variabile viene usata prima di essere definita?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

Quale istruzione JavaScript può essere usata per mettere intenzionalmente in pausa l'esecuzione del codice a scopo di debugging?

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

Quale metodo della console può essere usato per mostrare dati come tabella?

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

Come puoi ispezionare le richieste di rete negli strumenti per sviluppatori di Chrome?

#### --distractors--

Usando la scheda "Network Links".

---

Usando la scheda "Console".

---

Usando la scheda "Sources".

#### --answer--

Usando la scheda "Network".

### --question--

#### --text--

Qual è la sintassi corretta per usare `try...catch` in JavaScript?

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

Quale dei seguenti è un esempio di `SyntaxError`?

#### --distractors--

Usare una variabile prima che sia dichiarata.

---

Usare una variabile due volte nello stesso ambito.

---

Usare una variabile che è fuori ambito.

#### --answer--

Mancanza di una virgola tra gli elementi di un array.

### --question--

#### --text--

Qual è lo scopo dei breakpoint nel debugging di JavaScript?

#### --distractors--

Correggere automaticamente gli errori nel codice.

---

Impedire che il codice venga eseguito.

---

Registrare messaggi nella console.

#### --answer--

Mettere in pausa l'esecuzione in un punto specifico.

### --question--

#### --text--

Cosa succede quando viene eseguita l'istruzione `throw`?

#### --distractors--

Il programma termina immediatamente, ignorando tutto il codice rimanente.

---

L'istruzione throw registra un errore nella console ma non ferma l'esecuzione.

---

Mette in pausa l'esecuzione del codice finché non si disabilitano i breakpoint.

#### --answer--

Viene lanciata un'eccezione e viene eseguito il blocco `catch` (se presente).

### --question--

#### --text--

Quale metodo useresti per ispezionare variabili o espressioni durante il debugging negli strumenti per sviluppatori?

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

Che tipo di errore si verificherà quando verrà eseguito il seguente codice?

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

Errore di overflow dello stack

#### --answer--

`ReferenceError`

### --question--

#### --text--

Quale strumento è comunemente usato per eseguire il codice riga per riga durante il debugging?

#### --distractors--

`console.log()`

---

`alert()`

---

Istruzione `throw`.

#### --answer--

Breakpoint negli strumenti per sviluppatori.

### --question--

#### --text--

Che tipo di errore si verifica quando si tenta di consultare una proprietà di `undefined`?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

Cosa fa il blocco `finally` in un'istruzione `try...catch`?

#### --distractors--

Si esegue solo se c'è un errore.

---

Salta l'esecuzione se viene attivato `catch`.

---

Esce immediatamente dal blocco di codice.

#### --answer--

Si esegue sempre, indipendentemente dagli errori.

### --question--

#### --text--

Quale sarebbe il risultato dell'esecuzione del seguente codice?

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

Il codice lancerà un `SyntaxError`.

---

Il codice registrerà un array vuoto nella console.

---

Il codice lancerà un errore di memoria esaurita.

#### --answer--

Il codice lancerà un `RangeError`.

### --question--

#### --text--

Quale delle seguenti risposte è vera riguardo all'istruzione `debugger` in JavaScript?

#### --distractors--

L'istruzione `debugger` è sempre ignorata dal browser.

---

L'istruzione `debugger` invia un rapporto di errore al server.

---

L'istruzione `debugger` ricarica sempre la pagina e cancella la console.

#### --answer--

L'istruzione `debugger` è ignorata quando gli strumenti per sviluppatori non sono aperti.

### --question--

#### --text--

Qual è lo scopo principale del "profiling" in JavaScript?

#### --distractors--

Scrivere test unitari per le funzioni JavaScript e creare profili di esecuzione del codice.

---

Mettere in pausa l'esecuzione del codice e liberare le risorse catturate come memoria e CPU.

---

Convertire il codice JavaScript in codice macchina per un'esecuzione più veloce.

#### --answer--

Individuare i colli di bottiglia delle prestazioni registrando l'utilizzo della CPU, le chiamate alle funzioni e il tempo di esecuzione.

### --question--

#### --text--

Cos'è un "watcher" nel contesto del debugging?

#### --distractors--

Uno strumento per mettere in pausa l'esecuzione del codice.

---

Una funzione integrata per registrare errori.

---

Un metodo JavaScript per gestire le eccezioni.

#### --answer--

Uno strumento per monitorare il valore delle variabili.

### --question--

#### --text--

Quando dovresti usare `console.error()` invece di `console.log()`?

#### --distractors--

Per mostrare dati in tabella.

---

Per registrare messaggi solo durante il debugging.

---

Per registrare informazioni sul browser.

#### --answer--

Per registrare messaggi di errore.

### --question--

#### --text--

Cosa fa il costruttore `Error()` in JavaScript?

#### --distractors--

Registra messaggi di errore nella console.

---

Esegue codice in una sandbox.

---

Termina il programma.

#### --answer--

Crea un nuovo oggetto errore che può essere lanciato.

### --question--

#### --text--

Qual è lo scopo del metodo `console.dir()` in JavaScript?

#### --distractors--

Creare una directory separata per i file di log per ogni tipo di errore.

---

Generare una rappresentazione visiva dell'output della console e salvarla in un file pdf.

---

Fermare l'esecuzione del programma e mostrare un messaggio di errore relativo all'uso di memoria e CPU.

#### --answer--

Mostrare una lista gerarchica e interattiva delle proprietà di un oggetto JavaScript specificato.
