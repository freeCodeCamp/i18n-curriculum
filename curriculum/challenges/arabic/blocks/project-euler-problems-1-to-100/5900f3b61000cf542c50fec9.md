---
id: 5900f3b61000cf542c50fec9
title: "المشكلة 74: سلاسل مضروب الأرقام"
challengeType: 1
forumTopicId: 302187
dashedName: problem-74-digit-factorial-chains
---

# --description--

الرقم 145 معروف بخاصية أن مجموع مضروب أرقامه يساوي 145:

$$1! + 4! + 5! = 1 + 24 + 120 = 145$$

ربما الرقم 169 أقل شهرة، إذ يُنتج أطول سلسلة من الأرقام التي تعود إلى 169؛ ويتضح أن هناك ثلاث حلقات فقط من هذا النوع:

$$\begin{align}
&169 → 363601 → 1454 → 169\\\\
&871 → 45361 → 871\\\\
&872 → 45362 → 872\\\\
\end{align}$$

ليس من الصعب إثبات أن كل رقم بداية سينتهي به المطاف في حلقة. على سبيل المثال،

$$\begin{align}
&69 → 363600 → 1454 → 169 → 363601\\ (→ 1454)\\\\
&78 → 45360 → 871 → 45361\\ (→ 871)\\\\
&540 → 145\\ (→ 145)\\\\
\end{align}$$

البداية بالرقم 69 تُنتج سلسلة من خمسة مصطلحات غير متكررة، لكن أطول سلسلة غير متكررة تبدأ برقم أقل من مليون تحتوي على ستون مصطلحًا.

كم عدد السلاسل التي تبدأ برقم أقل من `n` وتحتوي بالضبط على ستون مصطلحًا غير متكرر؟

# --hints--

يجب أن تُرجع الدالة `digitFactorialChains(2000)` رقمًا.

```js
assert(typeof digitFactorialChains(2000) === 'number');
```

يجب أن تُرجع الدالة `digitFactorialChains(2000)` القيمة `6`.

```js
assert.strictEqual(digitFactorialChains(2000), 6);
```

يجب أن تُرجع الدالة `digitFactorialChains(100000)` القيمة `42`.

```js
assert.strictEqual(digitFactorialChains(100000), 42);
```

يجب أن تُرجع الدالة `digitFactorialChains(500000)` القيمة `282`.

```js
assert.strictEqual(digitFactorialChains(500000), 282);
```

يجب أن تُرجع الدالة `digitFactorialChains(1000000)` القيمة `402`.

```js
assert.strictEqual(digitFactorialChains(1000000), 402);
```

# --seed--

## --seed-contents--

```js
function digitFactorialChains(n) {

  return true;
}

digitFactorialChains(2000);
```

# --solutions--

```js
function digitFactorialChains(n) {
  function sumDigitsFactorials(number) {
    let sum = 0;
    while (number > 0) {
      sum += factorials[number % 10];
      number = Math.floor(number / 10);
    }
    return sum;
  }

  const factorials = [1];
  for (let i = 1; i < 10; i++) {
    factorials.push(factorials[factorials.length - 1] * i);
  }

  const sequences = {
    169: 3,
    871: 2,
    872: 2,
    1454: 3,
    45362: 2,
    45461: 2,
    3693601: 3
  };
  let result = 0;

  for (let i = 2; i < n; i++) {
    let curNum = i;
    let chainLength = 0;
    const curSequence = [];
    while (curSequence.indexOf(curNum) === -1) {
      curSequence.push(curNum);
      curNum = sumDigitsFactorials(curNum);
      chainLength++;
      if (sequences.hasOwnProperty(curNum) > 0) {
        chainLength += sequences[curNum];
        break;
      }
    }
    if (chainLength === 60) {
      result++;
    }
    for (let j = 1; j < curSequence.length; j++) {
      sequences[curSequence[j]] = chainLength - j;
    }
  }
  return result;
}
```
