---
id: 5900f3b31000cf542c50fec6
title: "المشكلة 71: الكسور المرتبة"
challengeType: 1
forumTopicId: 302184
dashedName: problem-71-ordered-fractions
---

# --description--

اعتبر الكسر، $\frac{n}{d}$، حيث `n` و `d` أعداد صحيحة موجبة. إذا كان `n` &lt; `d` وأكبر قاسم مشترك، ${{HCF}(n, d)} = 1$، يُسمى الكسر الفعلي المختزل / الكسر الفعلي المبسط.

إذا سردنا مجموعة الكسور الفعلية المختزلة لـ `d` ≤ 8 بترتيب تصاعدي للحجم، نحصل على:

$$\frac{1}{8}, \frac{1}{7}, \frac{1}{6}, \frac{1}{5}, \frac{1}{4}, \frac{2}{7}, \frac{1}{3}, \frac{3}{8}, \frac{\textbf2}{\textbf5}, \frac{3}{7}, \frac{1}{2}, \frac{4}{7}, \frac{3}{5}, \frac{5}{8}, \frac{2}{3}, \frac{5}{7}, \frac{3}{4}, \frac{4}{5}, \frac{5}{6}, \frac{6}{7}, \frac{7}{8}$$

يمكن ملاحظة أن $\frac{2}{5}$ هو الكسر الذي يقع مباشرة إلى يسار $\frac{3}{7}$.

عن طريق سرد مجموعة الكسور الفعلية المختزلة لـ `d` ≤ `limit` بترتيب تصاعدي للحجم، جد بسط الكسر الذي يقع مباشرة إلى يسار $\frac{3}{7}$.

# --hints--

`orderedFractions(8)` يجب أن تُرجع رقمًا.

```js
assert(typeof orderedFractions(8) === 'number');
```

`orderedFractions(8)` يجب أن تُرجع `2`.

```js
assert.strictEqual(orderedFractions(8), 2);
```

`orderedFractions(10)` يجب أن تُرجع `2`.

```js
assert.strictEqual(orderedFractions(10), 2);
```

`orderedFractions(9994)` يجب أن تُرجع `4283`.

```js
assert.strictEqual(orderedFractions(9994), 4283);
```

`orderedFractions(500000)` يجب أن تُرجع `214283`.

```js
assert.strictEqual(orderedFractions(500000), 214283);
```

`orderedFractions(1000000)` يجب أن تُرجع `428570`.

```js
assert.strictEqual(orderedFractions(1000000), 428570);
```

# --seed--

## --seed-contents--

```js
function orderedFractions(limit) {

  return true;
}

orderedFractions(8);
```

# --solutions--

```js
function orderedFractions(limit) {
  const fractions = [];
  const fractionValues = {};
  const highBoundary = 3 / 7;
  let lowBoundary = 2 / 7;

  for (let denominator = limit; denominator > 2; denominator--) {
    let numerator = Math.floor((3 * denominator - 1) / 7);
    let value = numerator / denominator;
    if (value > highBoundary || value < lowBoundary) {
      continue;
    }
    fractionValues[value] = [numerator, denominator];
    fractions.push(value);
    lowBoundary = value;
  }

  fractions.sort();
  return fractionValues[fractions[fractions.length - 1]][0];
}
```
