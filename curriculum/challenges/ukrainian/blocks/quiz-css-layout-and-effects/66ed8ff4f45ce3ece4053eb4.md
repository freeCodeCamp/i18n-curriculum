---
id: 66ed8ff4f45ce3ece4053eb4
title: Тест з макетів і ефектів CSS
challengeType: 8
dashedName: quiz-css-layout-and-effects
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який термін у CSS використовується для опису ситуації, коли вміст елемента перевищує розмір його контейнера?

#### --distractors--

Underflow

---

Compression

---

Padding

#### --answer--

Overflow

### --question--

#### --text--

Яка поведінка CSS за замовчуванням, коли вміст виходить за межі елемента?

#### --distractors--

Приховування вмісту.

---

Обрізання вмісту.

---

Зміна розміру вмісту.

#### --answer--

Збереження видимості вмісту.

### --question--

#### --text--

Чому CSS зазвичай уникає приховування вмісту при переповненні?

#### --distractors--

Щоб зберегти оригінальний макет і дизайн.

---

Щоб запобігти проблемам з продуктивністю в браузері.

---

Щоб покращити оптимізацію для пошукових систем.

#### --answer--

Щоб забезпечити доступність усього вмісту для користувачів.

### --question--

#### --text--

Як у CSS можна обробити вертикальне переповнення, приховуючи вміст або відображаючи смуги прокрутки?

#### --distractors--

Use `overflow-y: visible` to hide content and `overflow-y: auto` to always display scrollbars.

---

Use `overflow-y: collapse` to hide content and `overflow-y: expand` to display scrollbars.

---

Use `overflow-y: none` to hide content and `overflow-y: show` to display scrollbars.

#### --answer--

Use `overflow-y: hidden` to hide content and `overflow-y: scroll` to always display scrollbars.

### --question--

#### --text--

Як у CSS можна задати різну поведінку горизонтальної та вертикальної прокрутки?

#### --distractors--

By using the `scroll-x` and `scroll-y` properties.

---

By specifying the same value for `overflow-x` and `overflow-y`.

---

By specifying two values for the `overflow` property, where the first value applies to `overflow-y` and the second value to `overflow-x`.

#### --answer--

By specifying two values for the `overflow` property, where the first value applies to `overflow-x` and the second value to `overflow-y`.

### --question--

#### --text--

Що дозволяє робити CSS-властивість `transform`?

#### --distractors--

Змінювати колір і стиль шрифту елемента.

---

Регулювати поле і відступ елемента.

---

Міняти видимість і відображення елемента.

#### --answer--

Обертати, масштабувати, нахиляти і переміщувати елемент.

### --question--

#### --text--

Як властивість `transform: translate(15px, 200px)` змінює елемент?

#### --distractors--

Вона переміщує елемент на 15 пікселів вліво і на 200 пікселів вниз від початкової позиції.

---

Вона обертає елемент на 15 градусів за годинниковою стрілкою.

---

Вона переміщує елемент на 15 пікселів вправо і на 200 пікселів вгору від початкової позиції.

#### --answer--

Вона переміщує елемент на 15 пікселів вправо і на 200 пікселів вниз від початкової позиції.

### --question--

#### --text--

Яке призначення CSS-властивостей `overflow-x` і `overflow-y`?

#### --distractors--

Вони регулюють відступ і поле елемента.

---

Вони задають колір фону і стиль шрифту елемента.

---

Вони визначають видимість і відображення елемента.

#### --answer--

Вони контролюють горизонтальне і вертикальне переповнення вмісту елемента.

### --question--

#### --text--

Яке призначення властивості `margin` у CSS?

#### --distractors--

Вона визначає простір між вмістом і кордоном елемента.

---

Вона визначає простір між кордоном і відступом елемента.

---

Вона визначає ширину і висоту елемента.

#### --answer--

Вона визначає простір навколо елемента, поза межами його кордону.

### --question--

#### --text--

Що представляє область вмісту елемента у CSS?

#### --distractors--

Вона визначає поля і відступи навколо елемента.

---

Вона визначає товщину кордону елемента.

---

Вона контролює видимість елемента на сторінці.

#### --answer--

Вона містить фактичний вміст елемента, такий як текст, зображення і відео.

### --question--

#### --text--

У чому різниця між `content-box` і `border-box` у CSS?

#### --distractors--

`content-box` включає відступ і кордон, а `border-box` включає лише розміри вмісту.

---

Обидва `content-box` і `border-box` включають лише розміри кордону.

---

`content-box` використовується для 2D елементів, а `border-box` — для 3D елементів.

#### --answer--

`content-box` включає лише розміри вмісту, а `border-box` включає в розміри вміст, відступ і кордон.

### --question--

#### --text--

Яке призначення CSS-скидань стилів (resets)?

#### --distractors--

Застосовувати стилі за замовчуванням до всіх елементів для швидшого завантаження.

---

Мінімізувати розмір CSS-файлів, видаляючи невикористані стилі.

---

Оптимізувати зображення і шрифти для кращої продуктивності.

#### --answer--

Видалити стилі браузера за замовчуванням і забезпечити послідовне оформлення в різних браузерах.

### --question--

#### --text--

Який із наведених є загальним прикладом CSS-скидання стилів?

#### --distractors--

bootstrap.css

---

undo.css

---

restore.css

#### --answer--

normalize.css

### --question--

#### --text--

Яка CSS-властивість використовується для застосування змін, таких як обертання, масштабування і трансляція елементів?

#### --distractors--

`box-shadow`

---

`opacity`

---

`z-index`

#### --answer--

`transform`

### --question--

#### --text--

Як обчислюється загальна ширина елемента в CSS при використанні значення `border-box` для властивості `box-sizing`?

#### --distractors--

Загальна ширина = ширина вмісту + кордон + поле

---

Загальна ширина = ширина вмісту + відступ + кордон + поле

---

Загальна ширина = ширина вмісту

#### --answer--

Загальна ширина = ширина вмісту + відступ + кордон

### --question--

#### --text--

Яке призначення властивості `filter` у CSS?

#### --distractors--

Зміна масштабу і обертання елемента.

---

Зміна кольору фону і текстури елемента.

---

Зміна розміру і позиції елемента.

#### --answer--

Зміна візуального вигляду елемента за допомогою різних графічних ефектів.

### --question--

#### --text--

Що робить CSS-властивість `filter: grayscale(50%);` з елементом?

#### --distractors--

Вона збільшує яскравість елемента на 50%.

---

Вона знижує контрастність елемента на 50%.

---

Вона застосовує сепію до елемента.

#### --answer--

Вона застосовує 50% ефект відтінків сірого до елемента.

### --question--

#### --text--

Яке призначення функції `contrast()` у CSS?

#### --distractors--

Вона регулює яскравість зображення, де `0%` робить його чорним, а `100%` — білим.

---

Вона регулює контрастність зображення, де `100%` робить його сірим, `0%` не має ефекту, а значення понад `100%` збільшують контраст.

---

Вона регулює прозорість зображення, де `0%` робить його прозорим, а `100%` — непрозорим.

#### --answer--

Вона регулює контрастність зображення, де `0%` робить його сірим, `100%` не має ефекту, а значення понад `100%` збільшують контраст.

### --question--

#### --text--

Яке призначення функції `hue-rotate()` у CSS?

#### --distractors--

Вона змінює яскравість зображення.

---

Вона обертає зображення за годинниковою стрілкою на певну кількість градусів.

---

Вона регулює насиченість і контрастність зображення.

#### --answer--

Вона застосовує зсув кольору, де значення `angle` визначає кількість градусів обертання навколо кола кольорів.

### --question--

#### --text--

Що таке злиття полів (margin collapsing) у CSS?

#### --distractors--

Коли поле елемента встановлено від’ємним числом і це викликає проблеми в макеті.

---

Коли поле елемента зменшується на відступ і це змінює розміри елемента.

---

Коли браузер ігнорує поле елемента, тому воно не видно.

#### --answer--

Коли вертикальні поля суміжних елементів накладаються одне на одне і об’єднуються в одне поле.

## --quiz--

### --question--

#### --text--

Що з наведеного є ПРАВДИВОЮ характеристикою функції `translate()`?

#### --distractors--

Вона використовується для масштабування розміру елемента.

---

Вона використовується для трансляції тексту елемента.

---

Вона використовується для зміни форми або розміру елемента.

#### --answer--

Вона використовується для переміщення елемента з його поточної позиції.

### --question--

#### --text--

Що з наведеного є компонентом моделі блоку CSS?

#### --distractors--

Ширина

---

Висота

---

Розмір

#### --answer--

Відступ

### --question--

#### --text--

Коли у елемента властивість `box-sizing` встановлена в `content-box`, до якої частини моделі блоку застосовуються `width` і `height`?

#### --distractors--

Вміст, відступ, кордон і поле

---

Вміст і відступ

---

Вміст, відступ і кордон

#### --answer--

Вміст

### --question--

#### --text--

Коли у елемента властивість `box-sizing` встановлена в `border-box`, до яких частин моделі блоку CSS НЕ застосовуються `width` і `height`?

#### --distractors--

Область вмісту.

---

Відступ

---

Кордон

#### --answer--

Поле

### --question--

#### --text--

Яке значення функції `brightness()` робить елемент повністю чорним?

#### --distractors--

`100%`

---

`0px`

---

`100px`

#### --answer--

`0%`

### --question--

#### --text--

Які значення використовує функція `hue-rotate()`?

#### --distractors--

Пікселі

---

Радіани

---

Відсотки

#### --answer--

Градуси

### --question--

#### --text--

Що робить CSS-властивість `filter: contrast(0%);` з елементом?

#### --distractors--

Вона зробить елемент повністю білим.

---

Вона зробить елемент повністю чорним.

---

Вона не матиме жодного ефекту на елемент.

#### --answer--

Вона зробить елемент повністю сірим.

### --question--

#### --text--

Яке базове значення функції `brightness()` у CSS, від якого починається збільшення яскравості?

#### --distractors--

`100px`

---

`50px`

---

`50%`

#### --answer--

`100%`

### --question--

#### --text--

Які значення використовує функція `grayscale()`?

#### --distractors--

Пікселі

---

Градуси

---

Радіани

#### --answer--

Відсотки

### --question--

#### --text--

Які значення використовує функція `contrast()`?

#### --distractors--

Пікселі

---

Градуси

---

Радіани

#### --answer--

Відсотки

### --question--

#### --text--

Що контролює властивість `overflow-x` у CSS?

#### --distractors--

Вона визначає горизонтальне переповнення по осі y.

---

Вона визначає вертикальне переповнення по осі y.

---

Вона визначає вертикальне переповнення по осі x.

#### --answer--

Вона визначає горизонтальне переповнення по осі x.

### --question--

#### --text--

Що контролює властивість `overflow-y` у CSS?

#### --distractors--

Вона визначає горизонтальне переповнення по осі x.

---

Вона визначає вертикальне переповнення по осі x.

---

Вона визначає горизонтальне переповнення по осі y.

#### --answer--

Вона визначає вертикальне переповнення по осі y.

### --question--

#### --text--

Який приклад стороннього варіанту скидання стилів CSS?

#### --distractors--

clean.css

---

restore.css

---

reset.css

#### --answer--

sanitize.css

### --question--

#### --text--

Скільки вимірів має властивість overflow у CSS?

#### --distractors--

Один

---

Три

---

Чотири

#### --answer--

Два

### --question--

#### --text--

Якщо скороченій властивості `overflow` задано два значення, яка властивість використовує перше значення?

#### --distractors--

`overflow-y`

---

`overflow-w`

---

`overflow-z`

#### --answer--

`overflow-x`

### --question--

#### --text--

Який тип розмиття застосовує функція `blur()` до елемента у CSS?

#### --distractors--

Giliani

---

Galilei

---

Galileo

#### --answer--

Gaussian

### --question--

#### --text--

Які значення використовує функція `sepia()`?

#### --distractors--

Пікселі

---

Радіани

---

Градуси

#### --answer--

Відсотки

### --question--

#### --text--

Якщо скороченій властивості `overflow` задано два значення, яка властивість використовує друге значення?

#### --distractors--

`overflow-w`

---

`overflow-x`

---

`overflow-z`

#### --answer--

`overflow-y`

### --question--

#### --text--

Що робить скидання стилів CSS?

#### --distractors--

Воно змушує браузери за замовчуванням використовувати темний режим.

---

Воно видаляє рядкові стилі з HTML-елементів.

---

Воно вимикає розширення браузера, які включають CSS.

#### --answer--

Воно видаляє стилі браузера за замовчуванням.

### --question--

#### --text--

Що робить CSS-властивість `filter: grayscale(100%);` з елементом?

#### --distractors--

Вона не матиме жодного ефекту на елемент.

---

Вона зробить елемент повністю чорним.

---

Вона зробить елемент повністю білим.

#### --answer--

Вона видаляє насиченість кольору, зберігаючи тональну структуру елемента.
