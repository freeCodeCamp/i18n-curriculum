---
id: 66ed8fe7f45ce3ece4053eb2
title: Тест з CSS flexbox
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке flexbox?

#### --distractors--

Двовимірна модель для макету.

---

Тривимірна модель для макету.

---

Багатовимірна модель для макету.

#### --answer--

Одновимірна модель для макету.

### --question--

#### --text--

Яку CSS-властивість потрібно встановити, щоб увімкнути макет flexbox для елемента `div`?

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

Яка CSS-властивість дозволяє гнучким елементам переноситися на новий рядок, коли поточний рядок заповнений?

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

Що контролює властивість flexbox `justify-content`?

#### --distractors--

Вона контролює напрямок дочірніх елементів гнучкого контейнера.

---

Вона контролює стиль тексту дочірніх елементів гнучкого контейнера.

---

Вона контролює вирівнювання дочірніх елементів всередині гнучкого контейнера.

#### --answer--

Вона контролює розподіл дочірніх елементів по головній осі.

### --question--

#### --text--

Яка властивість контролює напрямок елементів всередині гнучкого контейнера?

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

Які CSS-властивості можна використати, щоб гнучкі елементи розташовувалися зліва направо, а новий гнучкий елемент з’являвся зверху попереднього, якщо ширина поточного рядка перевищена?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

Яка властивість контролює позиціонування елементів по поперечній осі?

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

Яке значення не є допустимим для `justify-content`?

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

Які CSS-властивості ви б використали, щоб горизонтально і вертикально центровано розмістити елемент у гнучкому контейнері?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

Яке значення `flex-direction` змушує гнучкі елементи розташовуватися справа наліво?

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

Яке значення не є допустимим для властивості `flex-wrap`?

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

Що робить значення `stretch` у властивості `align-items`?

#### --distractors--

Елементи щільно прилягають один до одного до початку контейнера.

---

Елементи вирівнюються так, щоб їх базові лінії гнучкого контейнера співпадали.

---

Елементи щільно прилягають один до одного до кінця контейнера.

#### --answer--

Автоматично розмірює елементи, щоб вони були однакового розміру і заповнювали контейнер.

### --question--

#### --text--

Яке значення `justify-content` розміщує перший елемент на початку, останній — в кінці, а інші рівномірно розподіляє між ними вздовж головної осі?

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

Яке значення `justify-content` розподіляє елементи так, що проміжки між кожною парою сусідніх елементів і краями контейнера однакові?

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

Яке значення `justify-content` розподіляє елементи рівномірно вздовж головної осі, залишаючи половину цього простору на краях контейнера?

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

Яке з наведених значень призводить до вирівнювання елементів на початку поперечної осі?

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

Яке з наведених значень призводить до вирівнювання елементів в кінці поперечної осі?

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

Яке значення для `flex-direction` змусить елементи вирівнюватися вздовж головної осі зліва направо?

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

Яке значення для `flex-direction` змусить елементи вирівнюватися вздовж поперечної осі зверху вниз?

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

Яке з наведених застосувань не підходить для flexbox?

#### --distractors--

Центрування елемента в контейнері.

---

Вертикальне вирівнювання елементів у рядку.

---

Динамічне регулювання розподілу і вирівнювання елементів для різних вікон перегляду.

#### --answer--

Фіксований розмір і простір між елементами, як у сітці.

## --quiz--

### --question--

#### --text--

Яка мета одновимірної моделі макету, як-от Flexbox?

#### --distractors--

Обробляти макет у двох вимірах, одночасно рядки і стовпці.

---

Керувати порядком накладання позиційованих елементів за допомогою `z-index`.

---

Створювати 3D-трансформації та складні анімації на елементах сторінки.

#### --answer--

Розташовувати елементи вздовж однієї лінії або осі, як рядок або стовпець.

### --question--

#### --text--

Яка CSS-властивість вирівнює всі гнучкі елементи праворуч у елементі `.container`?

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Які дві осі визначають напрямок розташування елементів у Flex-моделі?

#### --distractors--

Горизонтальна вісь і вертикальна вісь.

---

Вісь x і вісь y.

---

Основна вісь і вторинна вісь.

#### --answer--

Головна вісь і поперечна вісь.

### --question--

#### --text--

Яке з наведених CSS-правил розташовує елементи у вертикальному стовпці і центрує їх горизонтально в контейнері?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

Яка основна мета властивості `flex-wrap`?

#### --distractors--

Вона встановлює напрямок розташування гнучких елементів у гнучкому контейнері.

---

Вона вирівнює гнучкі елементи вздовж головної осі контейнера.

---

Вона вирівнює гнучкі елементи вздовж поперечної осі контейнера.

#### --answer--

Вона визначає, чи повинні гнучкі елементи переноситися на нові рядки, щоб поміститися у доступний простір.

### --question--

#### --text--

Яка поведінка за замовчуванням оголошення `flex-wrap: nowrap;`?

#### --distractors--

Воно змушує гнучкі елементи переноситися на новий рядок, якщо вони занадто широкі для контейнера.

---

Воно переносить елементи на новий рядок, але у зворотному порядку.

---

Воно забороняє гнучкому контейнеру бути більшим за батьківський елемент.

#### --answer--

Воно змушує гнучкі елементи залишатися в одному рядку, навіть якщо вони виходять за межі контейнера.

### --question--

#### --text--

Яка CSS-властивість є скороченням для одночасного встановлення властивостей `flex-direction` і `flex-wrap`?

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

Який ефект має встановлення `flex-direction: column-reverse;` на гнучкий контейнер?

#### --distractors--

Воно розташовує елементи горизонтально справа наліво.

---

Воно змінює порядок елементів у рядку, але залишає їх в одному рядку.

---

Воно розташовує елементи вертикально зверху вниз.

#### --answer--

Воно розташовує елементи вертикально у зворотному порядку знизу вгору.

### --question--

#### --text--

Який CSS-фрагмент правильно розташовує гнучкі елементи у вертикальному стовпці і дозволяє їм переноситися у новий стовпець, якщо вони виходять за висоту контейнера?

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

Яке значення `justify-content` вирівнює всі гнучкі елементи в кінці головної осі контейнера?

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Який ефект має наступне CSS-правило на гнучкі елементи?

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

Елементи розташовані у стовпець і будуть переноситися у новий стовпець.

---

Елементи розташовані у рядок і будуть переноситися у новий рядок.

---

Елементи розташовані у стовпець і переповнюватимуть контейнер, якщо не помістяться.

#### --answer--

Елементи розташовані у рядок і переповнюватимуть контейнер, якщо не помістяться.

### --question--

#### --text--

Як `justify-content: space-evenly;` вирівнює гнучкі елементи?

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

Елементи рівномірно розподілені, без пробілів на початку чи в кінці.

---

Елементи щільно упаковані в центрі контейнера.

---

Елементи притиснуті до правого краю контейнера з рівними проміжками між ними.

#### --answer--

Між елементами і на обох кінцях контейнера однакові проміжки.

### --question--

#### --text--

Яку властивість ви б використали, щоб вертикально центровано розташувати елементи, коли встановлено `flex-direction: row;`?

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

Що робить значення `flex-start` для властивості `justify-content`?

#### --distractors--

Вирівнює елементи на початку поперечної осі.

---

Розтягує елементи, щоб заповнити головну вісь контейнера.

---

Центрує всі елементи вздовж головної осі.

#### --answer--

Вирівнює елементи на початку головної осі контейнера.

### --question--

#### --text--

Як налаштувати гнучкий контейнер, щоб розташувати його елементи справа наліво в одному рядку і при цьому розмістити їх у верхній частині контейнера?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

Яке з наведених оголошень є дійсним для властивості `flex-flow`?

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

Яке з наведених CSS-правил призведе до того, що гнучкі елементи будуть центровані горизонтально і розтягнуті вертикально, щоб заповнити висоту контейнера?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

Який ефект має `align-items: stretch;`, коли `flex-direction` встановлено в `column`?

#### --distractors--

Розтягує елементи вертикально, щоб заповнити висоту контейнера.

---

Не має ефекту, коли напрямок `column`.

---

Розтягує елементи вертикально, щоб відповідати найвищому елементу.

#### --answer--

Розтягує елементи горизонтально, щоб заповнити ширину контейнера.

### --question--

#### --text--

Якщо ви хочете змінити порядок елементів у горизонтальному рядку на зворотний, яке оголошення слід використати?

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

Яке значення `align-items` розміщує елементи у верхній частині контейнера, якщо `flex-direction` встановлено в `row`?

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
