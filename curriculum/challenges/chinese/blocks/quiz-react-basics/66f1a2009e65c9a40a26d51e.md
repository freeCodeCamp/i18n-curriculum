---
id: 66f1a2009e65c9a40a26d51e
title: React 基础测验
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项不是 JavaScript 库或框架？

#### --distractors--

角度

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

以下哪项不是 React 中用于条件式渲染的方法？

#### --distractors--

三元操作符。

---

逻辑 `&&` 操作符。

---

`if` 语句。

#### --answer--

`setTimeout`

### --question--

#### --text--

在 React 中渲染列表时，添加 `key` 属性的主要目的是为了什么？

#### --distractors--

为每个列表项应用唯一的 CSS 样式。

---

作为元素的文本内容。

---

为列表中的每个特定项添加事件监听器。

#### --answer--

帮助 React 识别项以实现高效渲染。

### --question--

#### --text--

项目的包和库安装在哪里？

#### --distractors--

你计算机的用户目录。

---

`package.json` 文件。

---

`src` 文件夹。

#### --answer--

`node_modules` 文件夹。

### --question--

#### --text--

以下关于 JSX 哪项是正确的？

#### --distractors--

JSX 必须始终写在与 JavaScript 分开的文件中。

---

JSX 不能使用 JavaScript 表达式。

---

不推荐在 React 应用中使用 JSX。

#### --answer--

JSX 允许在 JavaScript 代码中编写 HTML。

### --question--

#### --text--

以下哪项是 SPA 的常见问题？

#### --distractors--

由于页面的常量渲染，服务器资源的使用更加频繁。

---

由于频繁的 DOM 更新，浏览器缓存变得不可靠。

---

多次页面装载会创建不一致的状态管理。

#### --answer--

屏幕阅读器难以处理动态更新的内容。

### --question--

#### --text--

单页应用通常如何更新视图？

#### --distractors--

通过进行完整的服务器装载。

---

通过重新装载整个超文本标记语言文档。

---

仅使用服务器端渲染。

#### --answer--

仅更新页面中必要的部分。

### --question--

#### --text--

以下哪个命令以开发模式启动 React 项目？

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

以下哪项不是有效的 JSX 元素？

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

哪个 JavaScript 数组方法常用于将数据数组转换为 JSX 元素数组？

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

以下哪项是传递 React 中 `key` 属性的最佳实践？

#### --distractors--

在所有分支中使用数组索引。

---

在每次 `Render` 时随机生成密钥。

---

完全省略 `key` 属性。

#### --answer--

使用来自数据的唯一且稳定的 ID。

### --question--

#### --text--

哪个命令安装 `package.json` 中列出的依赖？

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

你如何将 `name` 属性传递给子组件？

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

在 React 中，使用展开操作符传递多个 props 的正确方法是什么？

#### --distractors--

`<Child person={name..., age..., city...} />`。

---

`<Child spreader={...person} />`。

---

`<Child spread={person} />`。

#### --answer--

`<Child {...person} />`。

### --question--

#### --text--

React 的创建块是什么？

#### --distractors--

函数和模块。

---

DOM 元素和事件。

---

类和对象。

#### --answer--

可重用组件。

### --question--

#### --text--

在 React 中添加内联样式的正确方法是什么？

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

条件式渲染 React 组件的正确方法是什么？

#### --distractors--

在 JSX 中使用 `if` 语句。

---

在 `render()` 方法内声明一个新建组件。

---

在 JSX 中使用 `while` 循环。

#### --answer--

在 JSX 中使用三元操作符或逻辑 `&&` 操作符。

### --question--

#### --text--

哪个关键字用于将组件引入另一个文件？

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

SPAs 的完整含义是什么？

#### --distractors--

服务器页面架构。

---

选择性页面应用。

---

静态渐进式应用。

#### --answer--

单页应用。

### --question--

#### --text--

React 中用于描述 UI 的 JavaScript 语法扩展名是哪一个？

#### --distractors--

TSC

---

超文本标记语言+

---

ReactML

#### --answer--

JSX
