---
id: 66ed902df45ce3ece4053ebc
title: HTML 표와 폼 퀴즈
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 맞히세요.

# --quizzes--

## --quiz--

### --question--

#### --text--

폼 데이터가 전송될 URL을 지정하는 데 사용되는 속성은 무엇인가요?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

표에서 행과 행 헤더를 지정하는 데 사용되는 두 요소는 무엇인가요?

#### --distractors--

`r` 요소는 행에, `thead` 요소는 헤더에 사용됩니다.

---

`row` 요소는 행에, `th` 요소는 헤더에 사용됩니다.

---

`tr` 요소는 행에, `head` 요소는 헤더에 사용됩니다.

#### --answer--

`tr` 요소는 행에, `th` 요소는 헤더에 사용됩니다.

### --question--

#### --text--

`td` 요소는 무엇을 위해 사용되나요?

#### --distractors--

표 데이터 유형을 정의하는 데 사용됩니다.

---

두 열을 병합하는 데 사용됩니다.

---

두 행을 병합하는 데 사용됩니다.

#### --answer--

표 셀을 정의하는 데 사용됩니다.

### --question--

#### --text--

`colspan` 속성의 기능은 무엇인가요?

#### --distractors--

표에서 열의 길이를 정의합니다.

---

열 내 텍스트 사이의 줄 바꿈을 제거합니다.

---

열의 개수를 정의하는 데 사용됩니다.

#### --answer--

표 셀이 차지할 열의 수를 정의합니다.

### --question--

#### --text--

`placeholder` 속성의 역할은 무엇인가요?

#### --distractors--

폼이 제출될 때 데이터를 서버로 전송하는 데 사용됩니다.

---

`label` 요소와 `input` 요소를 연결하는 데 사용됩니다.

---

입력 필드를 사용자에게 읽기 전용으로 지정하는 데 사용됩니다.

#### --answer--

입력 필드에 대한 힌트를 제공하는 데 사용됩니다.

### --question--

#### --text--

`thead` 요소의 목적은 무엇인가요?

#### --distractors--

HTML 표에서 본문 내용을 그룹화하는 데 사용됩니다.

---

HTML 표의 설명을 그룹화하는 데 사용됩니다.

---

HTML 표의 푸터 내용을 그룹화하는 데 사용됩니다.

#### --answer--

HTML 표에서 헤더 내용을 그룹화하는 데 사용됩니다.

### --question--

#### --text--

레이블과 입력을 명시적으로 연결하는 예시는 무엇인가요?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

HTML 유효성 검사기는 무엇을 위해 사용되나요?

#### --distractors--

HTML 코드를 자동으로 포맷하는 도구입니다.

---

HTML에 스타일을 적용하는 도구입니다.

---

HTML 코드를 더 빠르게 실행하게 하는 도구입니다.

#### --answer--

HTML 코드의 구문을 검사하여 유효한지 확인하는 도구입니다.

### --question--

#### --text--

표에서 셀을 정의하는 데 사용되는 요소는 무엇인가요?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

웹 페이지의 HTML 구조를 검사하고 수정할 수 있는 도구는 무엇인가요?

#### --distractors--

DOM 제거.

---

DOM 테스터.

---

DOM 유효성 검사.

#### --answer--

DOM 검사기.

### --question--

#### --text--

관련된 입력 필드를 그룹화하는 올바른 방법은 무엇인가요?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

입력 필드를 읽기 전용으로 지정하는 데 사용되는 것은 무엇인가요?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

폼 제출 전에 입력 필드를 반드시 채워야 함을 지정하는 속성은 무엇인가요?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

개발자 도구(Devtools)는 무엇을 위해 사용되나요?

#### --distractors--

브라우저에 내장되어 코드의 린팅 문제를 찾는 데 사용됩니다.

---

브라우저에 내장되어 코드를 자동으로 포맷하는 데 사용됩니다.

---

브라우저에 내장되어 코드의 100% 테스트 커버리지를 보장하는 데 사용됩니다.

#### --answer--

브라우저에 내장되어 웹 페이지를 디버그, 프로파일링, 분석하는 데 사용됩니다.

### --question--

#### --text--

입력을 비활성화하는 올바른 방법은 무엇인가요?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

`type` 속성에 유효한 값은 무엇인가요?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

`size` 속성의 올바른 사용법은 무엇인가요?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

입력 필드에서 요구되는 최소 및 최대 문자 수를 지정하는 속성은 무엇인가요?

#### --distractors--

`minlen` 와 `maxlen`

---

`min` 와 `max`

---

`minimumLen` 와 `maximumLen`

#### --answer--

`minlength` 와 `maxlength`

### --question--

#### --text--

다음 버튼 예시 중 `type` 속성에 올바르지 않은 값을 사용하는 것은 무엇인가요?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

버튼의 값을 지정하는 데 사용되는 속성은 무엇인가요?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

`action` 태그의 시작 부분에 있는 `form` 속성의 역할은 무엇인가요?

#### --distractors--

입력 필드를 읽기 전용으로 지정하는 데 사용됩니다.

---

입력 시 사용자에게 보이는 문자 수를 정의하는 데 사용됩니다.

---

입력 필드에 무엇을 입력할지 사용자에게 힌트를 보여주는 데 사용됩니다.

#### --answer--

폼 데이터가 전송될 URL을 지정하는 데 사용됩니다.

### --question--

#### --text--

폼 데이터를 전송할 때 사용할 HTTP 메서드를 지정하는 속성은 무엇인가요?

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

폼 작업 시 자주 사용하는 메서드는 무엇인가요?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

`type` 속성에 유효하지 않은 값은 무엇인가요?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

폼에서 버튼을 만드는 올바른 방법은 무엇인가요?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

암묵적 폼 연결이란 무엇인가요?

#### --distractors--

`action` 요소에 `for` 와 `label` 속성을 사용해 입력과 레이블을 연결하는 방식입니다.

---

`required` 요소에 `for` 와 `label` 속성을 사용해 입력과 레이블을 연결하는 방식입니다.

---

`for` 요소에 `label` 속성을 사용해 입력과 레이블을 연결하는 방식입니다.

#### --answer--

입력 필드를 `label` 요소 안에 감싸서 입력과 레이블을 연결하는 방식입니다.

### --question--

#### --text--

`fieldset` 요소의 역할은 무엇인가요?

#### --distractors--

HTML 표에서 헤더 내용을 그룹화하는 데 사용됩니다.

---

입력 필드에 레이블을 만드는 데 사용됩니다.

---

입력 그룹을 설명하는 캡션을 추가하는 데 사용됩니다.

#### --answer--

관련된 입력들을 함께 그룹화하는 데 사용됩니다.

### --question--

#### --text--

폼에서 체크박스를 만드는 데 사용되는 요소는 무엇인가요?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

HTML 표에 제목을 추가하는 데 사용되는 요소는 무엇인가요?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

`legend` 요소의 역할은 무엇인가요?

#### --distractors--

HTML 표에서 본문 내용을 그룹화하는 데 사용됩니다.

---

표의 셀을 설명하는 캡션을 추가하는 데 사용됩니다.

---

관련된 입력들을 함께 그룹화하는 데 사용됩니다.

#### --answer--

입력 그룹을 설명하는 캡션을 추가하는 데 사용됩니다.

### --question--

#### --text--

HTML 표에서 헤더 내용을 그룹화하는 데 사용되는 요소는 무엇인가요?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

표에 행과 데이터 셀을 추가하는 올바른 방법은 무엇인가요?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

DOM 검사기란 무엇인가요?

#### --distractors--

DOM에서 모든 요소를 제거하는 도구입니다.

---

브라우저에 내장되어 웹 페이지를 디버그, 프로파일링, 분석하는 도구 모음입니다.

---

HTML 코드의 구문을 검사하여 유효한지 확인하는 도구입니다.

#### --answer--

웹 페이지의 HTML 구조를 검사하고 수정할 수 있는 도구입니다.

### --question--

#### --text--

HTML 코드의 구문을 검사하여 유효한지 확인하는 도구는 무엇인가요?

#### --distractors--

HTML validate

---

HTML isValid

---

HTML valid

#### --answer--

HTML validator

### --question--

#### --text--

표에서 데이터 셀의 열 수를 지정하는 올바른 방법은 무엇인가요?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

입력 필드의 포커스 상태란 무엇인가요?

#### --distractors--

사용자가 포커스를 제거했을 때의 상태입니다.

---

사용자가 읽기 전용으로 설정했을 때의 상태입니다.

---

사용자가 비활성화했을 때의 상태입니다.

#### --answer--

사용자가 선택한 상태입니다.

### --question--

#### --text--

라디오 버튼을 만드는 올바른 방법은 무엇인가요?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

`for` 속성의 역할은 무엇인가요?

#### --distractors--

사용자에게 힌트를 보여주는 데 사용됩니다.

---

입력 값의 값을 지정하는 데 사용됩니다.

---

입력 필드에 레이블을 만드는 데 사용됩니다.

#### --answer--

레이블이 어떤 입력 필드에 해당하는지 지정하는 데 사용됩니다.

### --question--

#### --text--

입력 시 사용자에게 보이는 문자 수를 정의하는 속성은 무엇인가요?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

입력 필드에 무엇을 입력할지 사용자에게 힌트를 보여주는 속성은 무엇인가요?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
