---
id: 67fe859e9d3b3635197781c8
title: 你如何處理整數和浮點數？
challengeType: 19
dashedName: how-do-you-work-with-integers-and-floating-point-numbers
---

# --description--

整數和浮點數是 Python 中主要的數值資料型別。使用它們，你可以儲存數值資料並執行數學運算。

讓我們來看看整數和浮點數是什麼、如何使用它們進行算術運算，以及 Python 提供的幾種用於處理兩者的行為。

整數是沒有小數點的整數，可以是正數或負數：

```python
my_int_1 = 56
my_int_2 = -4

print(type(my_int_1)) # <class 'int'>
print(type(my_int_2)) # <class 'int'>
```

以下是如何對整數執行加法操作的方法：

```python
my_int_1 = 56
my_int_2 = 12

sum_ints = my_int_1 + my_int_2
print('Integer Addition:', sum_ints) # Integer Addition: 68
```

以下是如何對整數執行減法的方法：

```python
my_int_1 = 56
my_int_2 = 12

# Subtraction
diff_ints = my_int_1 - my_int_2
print('Integer Subtraction:', diff_ints) # Integer Subtraction: 44
```

以下是如何對整數執行乘法操作的方法：

```python
my_int_1 = 12
my_int_2 = 4

# Multiplication
product_ints = my_int_1 * my_int_2
print('Integer Multiplication:', product_ints) # Integer Multiplication: 48
```

以下是如何對整數執行除法操作的方法：

```python
my_int_1 = 56
my_int_2 = 12

# Division
div_ints = my_int_1 / my_int_2
print('Integer Division:', div_ints) # Integer Division: 4.666666666666667
```

浮點數是帶有小數點的正數或負數，例如 `3.14`、`-0.5` 或 `0.0`。

```python
my_float_1 = -12.0
my_float_2 = 4.9

print(type(my_float_1)) # <class 'float'>
print(type(my_float_2)) # <class 'float'>
```

這是一個使用浮點數的加法操作。

```python
my_float_1 = 5.4
my_float_2 = 12.0

float_addition = my_float_1 + my_float_2
print('Float Addition:', float_addition) # Float Addition: 17.4
```

這裡有一個使用浮點數的減法操作。

```python
my_float_1 = 5.4
my_float_2 = 12.0

float_subtraction = my_float_2 - my_float_1
print('Float Subtraction:', float_subtraction) # Float Subtraction: 6.6
```

這裡有一個使用浮點數的乘法操作。

```python
my_float_1 = 5.4
my_float_2 = 12.0

float_multiplication = my_float_2 * my_float_1
print('Float Multiplication:', float_multiplication) # Float Multiplication: 64.80000000000001
```

這裡有一個帶有浮點數的除法操作：

```python
my_float_1 = 5.4
my_float_2 = 12.0

float_division = my_float_2 / my_float_1
print('Float Division:', float_division) # Float Division: 2.222222222222222
```

如果你將整數和浮點數相加，結果會自動轉換為浮點數：

```python
my_int = 56
my_float = 5.4

sum_int_and_float = my_int + my_float

print(sum_int_and_float) # 61.4
print(type(sum_int_and_float)) # <class 'float'>
```

這對其他基本算術操作也適用，例如減法、乘法和除法。如果你混合使用整數和浮點數，Python 將傳回浮點數作為結果。

你也可以執行更複雜的算術運算，例如使用取模運算子取得兩個數字的餘數、地板除法，以及對整數和浮點數進行冪次方運算。

取模運算子（`%`）會傳回左邊的值除以右邊的值後的餘數：

```python
my_int_1 = 56
my_int_2 = 12

my_float_1 = 5.4
my_float_2 = 12.0

mod_ints = my_int_1 % my_int_2
mod_floats = my_float_2 % my_float_1

print('Integer Modulus:', mod_ints) # Integer Modulus: 8
print('Float Modulus:', mod_floats) # Float Modulus: 1.1999999999999993
```

地板除法將兩個數字相除並傳回小於或相等於結果的最大整數。這是使用雙斜線運算子（`//`）完成的：

```python
my_int_1 = 56
my_int_2 = 12

my_float_1 = 5.4
my_float_2 = 12.0

floor_div_ints = my_int_1 // my_int_2
floor_div_floats = my_float_2 // my_float_1

print('Integer Floor Division:', floor_div_ints) # Integer Floor Division: 4
print('Float Floor Division:', floor_div_floats) # Float Floor Division: 2.0
```

指數運算會將一個數字提升到另一個數字的次方，並使用雙星號運算子（`**`）來完成：

```python
my_int_1 = 56
my_int_2 = 12

my_float_1 = 5.4
my_float_2 = 12.0

exp_ints = my_int_1 ** my_int_2
exp_floats = my_float_1 ** my_float_2

print('Integer Exponentiation:', exp_ints) # Integer Exponentiation: 951166013805414055936
print('Float Exponentiation:',  exp_floats) # Float Exponentiation: 614787626.1765089
```

Python 也提供內建函式，將數值資料或字串轉換成整數或浮點數。 

`float()` 函式（程式）傳回由給定數字構成的浮點數：

```python
my_int_1 = 56
my_float_1 = float(my_int_1)

print(my_float_1)  # 56.0
print(type(my_float_1))  # <class 'float'>
```

`int()` 函式（程式）傳回由給定數字構成的整數：

```python
my_float = 12.92563
my_int = int(my_float)

print(my_int)  # 12
print(type(my_int))  # <class 'int'>
```

此外，你可以使用相同的內建函式將字串轉換成浮點數或整數：

```python
my_str_int = '45'
my_str_float = '7.8'

converted_int = int(my_str_int)
converted_float = float(my_str_float)

print(converted_int, type(converted_int))  # 45 <class 'int'>
print(converted_float, type(converted_float))  # 7.8 <class 'float'>
```

這裡是 Python 提供的其他用於處理整數和浮點數的行為。

- `round()`：將數字四捨五入到指定的小數位數。預設情況下，此函式會四捨五入到最接近的整數，並傳回沒有小數位的整數：
    

```python
my_int_1 = 4.798
my_int_2 = 4.253

rounded_int_1 = round(my_int_1)
rounded_int_2 = round(my_int_2, 1)

print(rounded_int_1) # 5
print(rounded_int_2) # 4.3
```

- `abs()`：傳回數字的絕對值，
    

```python
num = -15

absolute_value = abs(num)
print(absolute_value) # 15
```

- `pow()`：將一個數字引發到另一個數字的次方或執行模指數運算。
    

```python
result_1 = pow(2, 3)  # Equivalent to 2 ** 3
print(result_1)  # 8

result_2 = pow(2, 3, 5)  # (2 ** 3) % 5
print(result_2)  # 3
```

# --questions--

## --text--

Python 中的雙斜線（`//`）運算子是做什麼用的？

## --answers--

它將兩個數字相除並傳回小於或相等於結果的最大整數。

---

 它執行一般除法並傳回浮點數。

### --feedback--

這個運算子用於一種特殊的整數除法。

---

它表述 Python 中的註解。

### --feedback--

這個運算子用於一種特殊的整數除法。

---

它會將一個數字引發到另一個數字的次方。

### --feedback--

這個運算子用於一種特殊的整數除法。

## --video-solution--

1

## --text--

哪個述語正確描述 Python 中的整數？

## --answers--

整數只能儲存正整數。

### --feedback--

思考整數表述什麼，例如整數值，而非帶有小數點的數字。

---

整數是不帶小數點的數字。

### --feedback--

思考整數表述什麼，例如整數值，而非帶有小數點的數字。

---

整數是沒有小數點的整數，可以是正數或負數。

---

整數在執行除法時會自動轉換為浮點數。

### --feedback--

思考整數表述什麼，例如整數值，而非帶有小數點的數字。

## --video-solution--

3

## --text--

哪一個方法傳回數字的絕對值？

## --answers--

`abs()`

---

`round()`

### --feedback--

思考那個總是給出非負結果的方法。

---

`sum()`

### --feedback--

思考那個總是給出非負結果的方法。

---

`max()`

### --feedback--

思考那個總是給出非負結果的方法。

## --video-solution--

1
