---
id: 6733c5e54e3a154c8078ed48
title: ما هي المحددات الكمية في التعبيرات النمطية وكيف تعمل؟
challengeType: 19
dashedName: what-are-regex-quantifiers-and-how-do-they-work
---

# --interactive--

دعنا نتعلم عن المحددات الكمية في التعبيرات النمطية.

تخيل سيناريو تريد فيه مطابقة رمز تعريف مكون من أربعة أرقام. تعرف أنه يمكنك استخدام صنف الحرف `\d`، لذا قد تكتبه أربع مرات. ولتجنب أي أحرف زائدة، ستضيف وسم الرابط للبداية ووسم الرابط للنهاية:

```js
const regex = /^\d\d\d\d$/;
```

وهذا يعمل: سيطابق أربعة أحرف رقمية. لكن بدلًا من كتابة نفس الصنف عدة مرات، يمكنك إعطاؤه محددًا كميًا.

تُعرف المحددات الكمية بأقواس متعرجة تحتوي على رقم أو رقمين. دعنا نستخدم محددًا كميًا في نمطنا:

```js
const regex = /^\d{4}$/;
```

لاحظ أن محددنا الكمي يحتوي فقط على الرقم `4`. هذه الصيغة تعني "مطابقة الحرف السابق بالضبط أربع مرات". لنرَ كيف يتصرف هذا:

:::محرر_تفاعلي

```js
const regex = /^\d{4}$/;
console.log(regex.test("123")); // false
console.log(regex.test("1234")); // true
console.log(regex.test("12345")); // false
console.log(regex.test("123456")); // false
console.log(regex.test("1234567")); // false
```

:::

النمط يطابق فقط السلسلة التي تحتوي على أربعة أرقام بالضبط، لأننا استخدمنا وسم الرابط ووسم الرابط للنهاية، ومحددنا الكمي يسمح بأربعة أرقام فقط بالضبط. لكن ربما رمز التعريف يحتاج فقط إلى حد أدنى من أربعة أرقام.

للسماح بأربعة أرقام أو أكثر، أضف فاصلة بعد الرقم في المحدد الكمي:

```js
const regex = /^\d{4,}$/;
```

الآن، تسمح صياغتنا للنمط بمطابقة أربعة أرقام أو أكثر. دعنا نختبر ذلك:

:::محرر_تفاعلي

```js
const regex = /^\d{4,}$/;
console.log(regex.test("123")); // false
console.log(regex.test("1234")); // true
console.log(regex.test("12345")); // true
console.log(regex.test("123456")); // true
console.log(regex.test("1234567")); // true
```

:::

رمز تعريف مكون من سبعة أرقام طويل جدًا. يجب أن يكون لهذه الرموز حد أقصى 6 أرقام وحد أدنى 4 أرقام. لتحقيق ذلك، يمكنك إضافة رقم ثاني إلى المحدد الكمي بعد الفاصلة:

```js
const regex = /^\d{4,6}$/;
```

والآن نمطنا لم يعد يطابق رمز التعريف المكون من سبعة أرقام، لأنه أكبر من الحد الأقصى المسموح به وهو ستة أرقام.

:::محرر_تفاعلي

```js
const regex = /^\d{4,6}$/;
console.log(regex.test("123")); // false
console.log(regex.test("1234")); // true
console.log(regex.test("12345")); // true
console.log(regex.test("123456")); // true
console.log(regex.test("1234567")); // false
```

:::

لاحظ أنه لا يمكنك استخدام هذه الصيغة لتحديد الحد الأقصى فقط: يجب دائمًا تحديد الحد الأدنى. لكن إذا حددت الحد الأدنى بـ `1`، يمكنك تحقيق نفس النتيجة فعليًا.

تلقينا متطلبات محدثة من المستخدمين. يمكن الآن أن تبدأ الرموز بحرف اختياريًا. نعرف بالفعل صنف الحرف لذلك، فلنضيفه إلى التعبير النمطي:

```js
const regex = /^[a-zA-Z]\d{4,6}$/;
```

لكن الآن نلزم وجود حرف. كيف نجعله اختياريًا؟

يمكنك استخدام صيغة المحدد الكمي مع `0` كحد أدنى و`1` كحد أقصى:

```js
const regex = /^[a-zA-Z]{0,1}\d{4,6}$/;
```

لكن هناك في الواقع محدد كمي مختصر خاص لحرف اختياري واحد، وهو علامة الاستفهام (`?`). لنستبدل محددنا الكمي بعلامة الاستفهام:

```js
const regex = /^[a-zA-Z]?\d{4,6}$/;
```

يجب أن نتحقق من النتيجة:

:::محرر_تفاعلي

```js
const regex = /^[a-zA-Z]?\d{4,6}$/;
console.log(regex.test("123")); // false
console.log(regex.test("a1234")); // true
console.log(regex.test("12345")); // true
console.log(regex.test("az12345")); // false
console.log(regex.test("X123456")); // true
console.log(regex.test("1234567")); // false
```

:::

نمطنا الآن يسمح بحرف اختياري واحد، يليه من أربعة إلى ستة أرقام.

للأسف، أدركنا للتو أننا قرأنا المتطلبات بشكل خاطئ. نحتاج للسماح بأي عدد من الحروف قبل الأرقام. يمكننا استخدام محددنا الكمي مع حد أدنى `0` وبدون حد أقصى محدد:

```js
const regex = /^[a-zA-Z]{0,}\d{4,6}$/;
```

لكن نمطنا بدأ يطول مجددًا. لحسن الحظ، هناك اختصار آخر لـ "مطابقة الحرف السابق صفر أو أكثر من المرات"، وهو رمز النجمة (`*`). لنستبدل محددنا الكمي بهذا في النمط، ونختبره:

:::محرر_تفاعلي

```js
const regex = /^[a-zA-Z]*\d{4,6}$/;
console.log(regex.test("123")); // false
console.log(regex.test("a1234")); // true
console.log(regex.test("12345")); // true
console.log(regex.test("az12345")); // true
console.log(regex.test("X123456")); // true
console.log(regex.test("1234567")); // false
```

:::

الآن نطابق بنجاح أي معرف يحتوي على صفر أو أكثر من الحروف متبوعة بأربعة إلى ستة أرقام. لكن تبين أن هذا يتسبب في تعطل نظامنا: يجب علينا فعليًا أن نطلب وجود حرف واحد على الأقل.

مرة أخرى، يمكننا استخدام محدد كمي بحد أدنى واحد وبدون حد أقصى محدد، أو يمكننا استخدام صيغة خاصة أخرى، وهي رمز الزائد (`+`):

:::محرر_تفاعلي

```js
const regex = /^[a-zA-Z]+\d{4,6}$/;
console.log(regex.test("123")); // false
console.log(regex.test("a1234")); // true
console.log(regex.test("12345")); // false
console.log(regex.test("az12345")); // true
console.log(regex.test("X123456")); // true
console.log(regex.test("1234567")); // false
```

:::

الآن تفشل الرموز التي لا تبدأ بحرف واحد على الأقل، بغض النظر عن عدد الأرقام.

يمكنك استخدام المحددات الكمية لتعزيز إيجاز ووضوح التعبيرات النمطية بشكل كبير.

# --questions--

## --text--

ماذا يعني المحدد الكمي `{4,6}` في التعبير النمطي؟

## --answers--

مطابقة أربع أو ست مرات بالضبط.

### --feedback--

فكر في الحد الأدنى والحد الأقصى المحددين في المحدد الكمي.

---

مطابقة على الأقل أربع مرات، ولكن لا تزيد عن ست مرات.

---

مطابقة من أربع إلى ستة أحرف إجمالًا.

### --feedback--

فكر في الحد الأدنى والحد الأقصى المحددين في المحدد الكمي.

---

مطابقة إما أربع أو ست أرقام.

### --feedback--

فكر في الحد الأدنى والحد الأقصى المحددين في المحدد الكمي.

## --video-solution--

2

## --text--

أي رمز محدد كمي يعادل `{0,1}` في التعبيرات النمطية؟

## --answers--

`*`

### --feedback--

الدرس يذكر "محدد كمي مختصر خاص لحرف اختياري واحد".

---

`+`

### --feedback--

الدرس يذكر "محدد كمي مختصر خاص لحرف اختياري واحد".

---

`?`

---

`^`

### --feedback--

الدرس يذكر "محدد كمي مختصر خاص لحرف اختياري واحد".

## --video-solution--

3

## --text--

ماذا يعني المحدد الكمي `+` في التعبير النمطي؟

## --answers--

مطابقة صفر أو أكثر مرات.

### --feedback--

الدرس يصف هذا المحدد الكمي بأنه يتطلب "واحدًا على الأقل" من العنصر السابق.

---

مطابقة مرة واحدة بالضبط.

### --feedback--

الدرس يصف هذا المحدد الكمي بأنه يتطلب "واحدًا على الأقل" من العنصر السابق.

---

مطابقة مرة واحدة أو أكثر.

---

مطابقة صفر أو مرة واحدة.

### --feedback--

الدرس يصف هذا المحدد الكمي بأنه يتطلب "واحدًا على الأقل" من العنصر السابق.

## --video-solution--

3
