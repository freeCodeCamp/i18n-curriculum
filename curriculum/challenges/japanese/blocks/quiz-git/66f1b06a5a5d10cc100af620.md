---
id: 66f1b06a5a5d10cc100af620
title: Git クイズ
challengeType: 8
dashedName: quiz-git
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

`git`とは何ですか？

#### --distractors--

人気のあるアクセシビリティツールの一種。

---

ソフトウェアプロジェクトでよく使われるリンター。

---

特別な種類のJavaScriptライブラリ。

#### --answer--

バージョン管理システムは、プロジェクトの変更を追跡・管理することを可能にします。

### --question--

#### --text--

次のうち、人気のあるクラウドベースのバージョン管理プロバイダーはどれですか？

#### --distractors--

Spring Boot

---

Laravel

---

Remix

#### --answer--

GitHub

### --question--

#### --text--

`git config`コマンドは何をしますか？

#### --distractors--

変更のリベースに関する設定変数を設定します。

---

GitLab上のリモートリポジトリのみを管理する設定を行います。

---

マージコンフリクトの解決に関する設定を行います。

#### --answer--

Gitがマシン上でどのように動作するかを決める設定変数を設定します。

### --question--

#### --text--

マシンにインストールされているGitのバージョンを表示するコマンドはどれですか？

#### --distractors--

`git --versions`

---

`git --setVersion`

---

`git --set`

#### --answer--

`git --version`

### --question--

#### --text--

コマンドラインを離れずにGitHub固有のタスクを行うためのツールはどれですか？

#### --distractors--

GitHub CI/CD

---

GitHub Actions

---

GitHub Pages

#### --answer--

GitHub CLI

### --question--

#### --text--

GitHubリポジトリ内でコードのビルド、テスト、デプロイなどのワークフローを自動化できる機能はどれですか？

#### --distractors--

GitHub Pipelines

---

GitHub Cloud

---

GitHub API

#### --answer--

GitHub Actions

### --question--

#### --text--

空の`git`リポジトリを初期化するコマンドはどれですか？

#### --distractors--

`git status`

---

`git set`

---

`git log`

#### --answer--

`git init`

### --question--

#### --text--

`git push`コマンドは何をしますか？

#### --distractors--

Gitの全履歴をGitHubにプッシュするコマンドです。

---

リモートリポジトリからローカルマシンに変更をプッシュするコマンドです。

---

コードをGitHubにプッシュし、自動的に`main`ブランチにマージするコマンドです。

#### --answer--

ローカルのコミットをリモートリポジトリにプッシュするコマンドです。

### --question--

#### --text--

リポジトリをクローンするコマンドはどれですか？

#### --distractors--

`git cloning`

---

`git cloned`

---

`git clones`

#### --answer--

`git clone`

### --question--

#### --text--

作者、コミット日時、コミットメッセージ、コミットハッシュなどの情報を含む過去のすべてのコミットを一覧表示するコマンドはどれですか？

#### --distractors--

`git rebase`

---

`git status`

---

`git commit`

#### --answer--

`git log`

### --question--

#### --text--

ローカルGitリポジトリに関連付けられているすべてのリモートリポジトリを一覧表示するコマンドはどれですか？

#### --distractors--

`git rebase -v`

---

`git pull -v`

---

`git checkout -v`

#### --answer--

`git remote -v`

### --question--

#### --text--

`git reset`コマンドは何をしますか？

#### --distractors--

マージコンフリクトの解決の試みをすべてリセットします。

---

`git rebase`の試みをすべてリセットします。

---

リポジトリのすべてのコミットをリセットします。

#### --answer--

ブランチの現在の状態をリセットします。

### --question--

#### --text--

プルリクエストとは何ですか？

#### --distractors--

あるブランチから別のブランチへコミットのシーケンスを結合するリクエスト。

---

すべてのマージコンフリクトを解決するリクエスト。

---

リポジトリからすべてのコミットを削除するリクエスト。

#### --answer--

自分のブランチの変更をターゲットブランチに取り込むリクエストです。

### --question--

#### --text--

プロジェクト内で`main`ブランチは何を表しますか？

#### --distractors--

アプリケーションの主要なドキュメント用ブランチを表します。

---

アプリケーションのホットフィックス用ブランチを表します。

---

アプリケーションの主要な機能ブランチを表します。

#### --answer--

アプリケーションの主要または本番用ブランチを表します。

### --question--

#### --text--

GitHub Pagesは何に最適ですか？

#### --distractors--

C#とUnityゲームのデプロイに最適です。

---

Python専用アプリケーションのデプロイに最適です。

---

バックエンド重視のウェブサイトのデプロイに最適です。

#### --answer--

静的サイトのデプロイに最適です。

### --question--

#### --text--

作業ディレクトリの現在の状態を表示するコマンドはどれですか？

#### --distractors--

`git show`

---

`git check`

---

`git update`

#### --answer--

`git status`

### --question--

#### --text--

リモートリポジトリから変更を取得するコマンドはどれですか？

#### --distractors--

`git get`

---

`git sync`

---

`git update`

#### --answer--

`git fetch`

### --question--

#### --text--

`.gitignore`ファイルとは何ですか？

#### --distractors--

プロジェクトの全`git`履歴を表示する特別なファイル。

---

変更の差分を表示する特別なファイル。

---

プロジェクトのコンフリクトを解決するための特別なファイル。

#### --answer--

プロジェクト内の特定のファイルやフォルダーを`git`に追跡させないように指示する特別なファイル。

### --question--

#### --text--

ローカルのすべてのブランチを一覧表示するコマンドはどれですか？

#### --distractors--

`git show`

---

`git log`

---

`git list`

#### --answer--

`git branch`

### --question--

#### --text--

あるブランチから別のブランチへコミットのシーケンスを移動または結合するコマンドはどれですか？

#### --distractors--

`git remote`

---

`git merge`

---

`git switch`

#### --answer--

`git rebase`
