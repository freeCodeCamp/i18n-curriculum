---
id: 6630e7b497466d6d8bb5b863
title: 任務 10
challengeType: 19
dashedName: task-10
lang: en-US
---

# --description--

在程式設計中，`string` 是用於儲存文字的資料型別。`string` 可以包含字母、數字和符號。它通常透過將文字包圍在引號中來表述。

例如，`"Hello, world!"` 是一個 `string`。

# --questions--

## --text--

在程式設計的背景關係中，什麼是 `string`？

## --answers--

一段文字

---

一種數字型別

### --feedback--

雖然 `string` 可以包含數字，但它主要用於文字，而不僅僅是數值。

---

一個程式設計工具

### --feedback--

`string` 是一種用於程式設計的資料型別，專門用於文字，而非工具。

---

啟動程式的指令

### --feedback--

`String` 是用於文字的資料型別，而不是執行程式的指令。

## --video-solution--

1
