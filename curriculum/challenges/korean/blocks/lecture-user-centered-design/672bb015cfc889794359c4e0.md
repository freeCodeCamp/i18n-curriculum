---
id: 672bb015cfc889794359c4e0
title: 모달 대화상자 디자인 모범 사례란 무엇인가요?
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**참고**: 일부 인터랙티브 예제는 아직 배우지 않은 CSS와 JavaScript를 사용할 수 있습니다. 모든 코드를 이해하려고 걱정하지 마세요. 예제의 목적은 이러한 디자인 개념을 미리 보여줘서 작동 방식을 더 잘 이해하도록 돕는 것입니다. 미리보기를 보려면 인터랙티브 편집기를 활성화하세요.

모달이란 무엇인가요? 웹사이트가 콘텐츠 위에 표시할 수 있는 팝업 유형입니다. HTML에는 모달을 만들 때 사용할 수 있는 `dialog` 요소가 있습니다.

모달 뒤의 콘텐츠는 보통 어둡게 처리됩니다. 이는 사용자가 상호작용해야 할 영역, 즉 이 경우 모달에 시각적으로 집중하도록 돕습니다.

사용자가 모달 외부를 클릭해서 모달을 닫을 수 있도록 하는 것이 항상 좋은 방법입니다.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

모달에는 매우 눈에 띄는 버튼이 자주 있습니다. 이를 CTA, 즉 call-to-action이라고 합니다. 모달로 사용자의 흐름을 방해하는 목적이 특정 행동을 유도하는 것이므로, 이 버튼들은 쉽게 식별할 수 있어야 합니다.

모달에는 닫기 버튼도 있어야 합니다. 사용자가 CTA를 클릭하기를 바라더라도, 모달에서 벗어나 이전에 하던 작업을 계속할 수 있는 선택권을 주는 것이 중요합니다.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

물론 모달에는 포커스 관리 같은 접근성 문제도 있습니다. 하지만 이러한 일반적인 모범 사례를 출발점으로 삼으면 탄탄한 기반 위에 구축할 수 있습니다.

# --questions--

## --text--

모달 대화상자 뒤의 콘텐츠를 어둡게 처리하는 목적은 무엇인가요?

## --answers--

모바일 기기의 배터리 수명을 절약하기 위해서.

### --feedback--

수업에서 이 시각적 효과에 대한 구체적인 이유를 언급합니다.

---

사용자가 상호작용해야 할 영역을 시각적으로 식별하도록 돕기 위해서.

---

웹사이트가 더 빨리 로드되도록 하기 위해서.

### --feedback--

수업에서 이 시각적 효과에 대한 구체적인 이유를 언급합니다.

---

사용자가 민감한 정보를 보지 못하게 하기 위해서.

### --feedback--

수업에서 이 시각적 효과에 대한 구체적인 이유를 언급합니다.

## --video-solution--

2

## --text--

모달 디자인에서 CTA는 무엇의 약자인가요?

## --answers--

Click To Activate.

### --feedback--

수업에서 모달의 눈에 띄는 버튼을 설명할 때 이 약어의 전체 용어를 제공합니다.

---

Complete The Action.

### --feedback--

수업에서 모달의 눈에 띄는 버튼을 설명할 때 이 약어의 전체 용어를 제공합니다.

---

Call-to-Action.

---

Close The Application.

### --feedback--

수업에서 모달의 눈에 띄는 버튼을 설명할 때 이 약어의 전체 용어를 제공합니다.

## --video-solution--

3

## --text--

모달 대화상자에 닫기 버튼을 포함하는 것이 왜 중요한가요?

## --answers--

일부 국가에서는 법적으로 요구되기 때문입니다.

### --feedback--

수업에서 사용자가 모달에서 벗어날 수 있도록 하는 중요성을 강조합니다.

---

모달이 브라우저를 충돌시키지 않도록 하기 위해서입니다.

### --feedback--

수업에서 사용자가 모달에서 벗어날 수 있도록 하는 중요성을 강조합니다.

---

사용자가 모달을 종료하고 이전 활동을 계속할 수 있는 선택권을 주기 위해서입니다.

---

모달이 더 전문적으로 보이도록 하기 위해서입니다.

### --feedback--

수업에서 사용자가 모달에서 벗어날 수 있도록 하는 중요성을 강조합니다.

## --video-solution--

3
