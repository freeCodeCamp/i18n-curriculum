---
id: 69f42d414b7626e21a922794
title: ما هي برمجيات وسيطة لمعالجة الأخطاء؟
challengeType: 19
dashedName: what-is-error-handling-middleware
---

# --description--

برمجيات وسيطة لمعالجة الأخطاء في Express هي نوع خاص من البرمجيات الوسيطة تُستخدم لمعالجة الأخطاء التي تحدث أثناء دورة الطلب والاستجابة. هي ضرورية لالتقاط الأخطاء غير المتوقعة، وتسجيلها، وتوفير استجابات مناسبة للعميل.

كما رأيت في درس سابق تحت معالجة الأخطاء، تحتوي برمجيات وسيطة معالجة الأخطاء في Express على أربع معلمات: `err`، `req`، `res`، و `next`. تُستدعى هذه البرمجيات الوسيطة عندما يُمرر خطأ إلى الدالة `next()`.

إليك تذكيرًا بصيغة النحو مرة أخرى:

```js
app.use((err, req, res, next) => {
  console.error(err.message);
  res.status(500).send("Internal Server Error");
});
```

وهذا ما يبدو عليه ذلك في تطبيق Express أساسي:

```js
const express = require('express')
const app = express()

// A simple route that throws an error
app.get('/', (req, res) => {
  throw new Error('Something went wrong!')
})

// Error-handling middleware
app.use((err, req, res, next) => {
  console.error(err.stack); // Logs the error stack
  res.status(500).send('Something went wrong!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* معالِج المسار: يرمي معالِج المسار الخطأ عمدًا لمحاكاة مشكلة.
* برمجيات وسيطة لمعالجة الأخطاء: تُسجل هذه البرمجيات الوسيطة باستخدام `app.use()`. تلتقط الأخطاء التي تُمرر عبر `next(err)` وترسل استجابة HTTP برمز 500 إلى العميل مع رسالة خطأ عامة.
* `err`: هذا هو كائن الخطأ الذي يُمرر إلى البرمجيات الوسيطة.

تضمن برمجيات وسيطة معالجة الأخطاء أنه عندما يحدث خطأ في تطبيقك، يحصل المستخدمون على استجابة مناسبة. من المهم دائمًا تضمين هذه البرمجيات الوسيطة في نهاية مكدس البرمجيات الوسيطة لالتقاط أي أخطاء.

# --questions--

## --text--

ماذا تفعل برمجيات وسيطة معالجة الأخطاء؟

## --answers--

تعالج الطلبات وترسل الاستجابات.

### --feedback--

ركز على ما يحدث عندما يحدث خطأ أثناء الطلب.

---

تعالج الأخطاء التي تحدث أثناء دورة الطلب والاستجابة.

---

تخدم الملفات الثابتة.

### --feedback--

ركز على ما يحدث عندما يحدث خطأ أثناء الطلب.

---

تعالج منطق التوجيه.

### --feedback--

ركز على ما يحدث عندما يحدث خطأ أثناء الطلب.

## --video-solution--

2

## --text--

أي من المعلمات التالية مطلوبة في برمجيات وسيطة معالجة الأخطاء؟

## --answers--

`err`, `req`, `res`, `next`.

---

`req` و `res`.

### --feedback--

فكر في المعلمات التي يستخدمها Express للتعرف على معالجي الأخطاء.

---

`err` و `next`.

### --feedback--

فكر في المعلمات التي يستخدمها Express للتعرف على معالجي الأخطاء.

---

`err` و `req`.

### --feedback--

فكر في المعلمات التي يستخدمها Express للتعرف على معالجي الأخطاء.

## --video-solution--

1

## --text--

أين يجب وضع برمجيات وسيطة معالجة الأخطاء في مكدس البرمجيات الوسيطة؟

## --answers--

في الأعلى، قبل البرمجيات الوسيطة الأخرى.

### --feedback--

فكر في مكان وضع معالجي الأخطاء في الشفرة التي قد تفشل.

---

في منتصف المكدس.

### --feedback--

فكر في مكان وضع معالجي الأخطاء في الشفرة التي قد تفشل.

---

في الأسفل، بعد البرمجيات الوسيطة الأخرى.

---

لا يوجد مكان محدد لها.

### --feedback--

فكر في مكان وضع معالجي الأخطاء في الشفرة التي قد تفشل.

## --video-solution--

3
