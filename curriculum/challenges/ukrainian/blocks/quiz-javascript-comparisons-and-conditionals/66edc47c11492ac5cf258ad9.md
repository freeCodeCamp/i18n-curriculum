---
id: 66edc47c11492ac5cf258ad9
title: Тест з порівнянь і умовних операторів JavaScript
challengeType: 8
dashedName: quiz-javascript-comparisons-and-conditionals
---

# --description--

Щоб пройти тест, ви маєте правильно відповісти щонайменше на 9 із 10 наведених нижче питань.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який результат виразу `undefined > 0`?

#### --distractors--

`true`

---

`undefined`

---

`null`

#### --answer--

`false`

### --question--

#### --text--

Яку логічну операцію представляє `||`?

#### --distractors--

XOR

---

AND

---

NOT

#### --answer--

OR

### --question--

#### --text--

Який вихідний результат наступного коду?

```js
console.log(5 === 2 + 3 || 4 == 2);
```

#### --distractors--

`undefined`

---

`false`

---

Виникає помилка.

#### --answer--

`true`

### --question--

#### --text--

Що таке істинне (truthy) і хибне (falsy) значення?

#### --distractors--

Значення, яке одночасно є `true` і `false`.

---

Значення, яке змінюється залежно від контексту.

---

Значення, яке функціонує інакше, ніж `true` або `false` значення.

#### --answer--

Значення, яке вважається `true` або `false` при зустрічі в булевому контексті.

### --question--

#### --text--

Що виводиться в консоль із наведеного нижче коду?

```js
if (1) {
  console.log("True!");
}
```

#### --distractors--

`false`

---

У консоль нічого не виводиться.

---

Виникає помилка.

#### --answer--

`True!`

### --question--

#### --text--

У чому різниця між `undefined` і `null`?

#### --distractors--

`null` і `undefined` вказують на пам’ять поза діапазоном і недоступні.

---

`null` — це неявне значення, призначене змінним, яке не можна змінити, тоді як `undefined` — це явне значення, призначене змінним.

---

`null` — глобальна властивість, `undefined` — ні.

#### --answer--

Змінні без значення є `undefined`, тоді як `null` представляє навмисну відсутність значення об’єкта.

### --question--

#### --text--

Що відбувається, якщо не включити `break` під час реалізації інструкції `switch`?

#### --distractors--

Інструкція `switch` раптово припиниться.

---

Інструкція `switch` викличе помилку після попередньої інструкції.

---

Інструкція `switch` вийде після першого збігу.

#### --answer--

Код продовжує виконувати наступні інструкції `case`, навіть після знаходження збігу.

### --question--

#### --text--

Що виводиться в консоль із наведеного нижче коду?

```js
const a = 2;
if (1 == "1") {
  let b = 3;
  console.log(a + b);
}
console.log(b);
```

#### --distractors--

Виникає помилка.

---

```js
5
3
```

---

```js
5
undefined
```

#### --answer--

`5`, а потім виникає помилка.

### --question--

#### --text--

Який буде вихідний результат наступного коду JavaScript?

```js
let vehicle = "car";

switch (vehicle) {
  case "bike":
    console.log("Bikes are two-wheelers.");
    break;
  case "car":
    console.log("Some cars are 4x4.");
  case "truck":
    console.log("Trucks can carry heavy loads.");
    break;
  default:
    console.log("Unknown vehicle.");
}
```

#### --distractors--

```md
Some cars are 4x4.
```

---

```md
Some cars are 4x4.
Trucks can carry heavy loads.
Unknown vehicle.
```

---

```md
Unknown vehicle.
```

#### --answer--

```md
Some cars are 4x4.
Trucks can carry heavy loads.
```

### --question--

#### --text--

Що виводиться в консоль із наведеного коду?  

```js
let x = 5;
if (x > 1 && x < 10) {
  console.log("x is between 1 and 10");
} else {
  console.log("x is not between 1 and 10");
}
```

#### --distractors--

Виникає помилка.

---

Нічого не виводиться.

---

`x is not between 1 and 10`

#### --answer--

`x is between 1 and 10`
