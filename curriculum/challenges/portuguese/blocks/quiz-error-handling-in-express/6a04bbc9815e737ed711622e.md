---
id: 6a04bbc9815e737ed711622e
title: Quiz sobre tratamento de erros no Express
challengeType: 8
dashedName: quiz-error-handling-in-express
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

O que significa o código de status HTTP 201?

#### --distractors--

A requisição foi bem-sucedida sem corpo de resposta.

---

O cliente não está autorizado a acessar o recurso.

---

O recurso solicitado não existe.

#### --answer--

Um novo recurso foi criado com sucesso.

### --question--

#### --text--

Qual código de status indica que a requisição foi bem-sucedida mas não há corpo de resposta?

#### --distractors--

`200`

---

`201`

---

`301`

#### --answer--

`204`

### --question--

#### --text--

O que o código de status `304 Not Modified` informa ao cliente?

#### --distractors--

O recurso foi movido para uma nova URL permanente.

---

O servidor está temporariamente indisponível para manutenção.

---

É necessária autenticação para acessar o recurso.

#### --answer--

Nada mudou, então o cliente pode usar sua versão em cache.

### --question--

#### --text--

Qual a diferença entre os códigos de status `401` e `403`?

#### --distractors--

`401` significa que o recurso não foi encontrado enquanto `403` significa que o servidor está fora do ar.

---

`401` significa que a requisição estava malformada enquanto `403` significa que o recurso foi movido.

---

`401` significa que o servidor travou enquanto `403` significa que a requisição expirou.

#### --answer--

`401` significa que é necessária autenticação enquanto `403` significa autenticado mas sem permissão.

### --question--

#### --text--

Qual código de status indica que o servidor está temporariamente sobrecarregado ou fora do ar para manutenção?

#### --distractors--

`500`

---

`502`

---

`301`

#### --answer--

`503`

### --question--

#### --text--

O que `res.status(200).json({ message: "Success" })` faz no Express?

#### --distractors--

Envia uma resposta `200` com uma mensagem em texto simples.

---

Envia uma resposta `200` sem corpo.

---

Redireciona o cliente para a página de sucesso.

#### --answer--

Envia uma resposta `200` com um corpo JSON.

### --question--

#### --text--

O que faz o Express reconhecer uma função middleware como um manipulador de erros?

#### --distractors--

Ela é colocada no topo da pilha de middleware.

---

Ela usa `res.error()` em vez de `res.send()`.

---

Ela é definida dentro de um manipulador de rota.

#### --answer--

Ela tem os parâmetros `err`, `req`, `res` e `next`.

### --question--

#### --text--

Onde o middleware de tratamento de erros deve ser colocado em um app Express?

#### --distractors--

Antes de todas as definições de rota.

---

No topo do arquivo, antes de qualquer importação.

---

Dentro do primeiro manipulador de rota que pode lançar um erro.

#### --answer--

No final da pilha de middleware, depois de todas as rotas.

### --question--

#### --text--

No Express 4, o que você deve fazer para tratar erros assíncronos em um manipulador de rota?

#### --distractors--

Lançar o erro diretamente do manipulador de rota e deixar o Node.js tratar no nível do processo.

---

Retornar o objeto de erro como corpo JSON com código de status 500.

---

Usar `process.on('uncaughtException')` para capturar globalmente todos os erros assíncronos não tratados.

#### --answer--

Envolver a lógica assíncrona em `try/catch` e chamar `next(err)` para encaminhar o erro ao manipulador de erros.

### --question--

#### --text--

Qual é o propósito de uma função wrapper `asyncHandler` no Express 4?

#### --distractors--

Converte automaticamente manipuladores de rota síncronos em assíncronos.

---

Reinicia automaticamente o servidor e limpa o estado quando ocorre um erro.

---

Substitui o middleware de tratamento de erros colocado no final da pilha de middleware.

#### --answer--

Evita repetir `try/catch` em cada rota encaminhando promises rejeitadas para `next`.

### --question--

#### --text--

Como o Express 5 trata erros assíncronos de forma diferente do Express 4?

#### --distractors--

Express 5 exige adicionar um listener global `process.on('unhandledRejection')` para capturar erros assíncronos.

---

Express 5 ignora silenciosamente erros assíncronos e apenas os registra no console sem parar o app.

---

Express 5 exige que cada rota assíncrona defina seu próprio middleware dedicado para tratamento de erros.

#### --answer--

Express 5 encaminha automaticamente erros assíncronos e promises rejeitadas para o manipulador de erros sem precisar de `next(err)`.

### --question--

#### --text--

Onde um middleware catch-all `404` deve ser colocado em um app Express?

#### --distractors--

Antes de todas as outras rotas, no topo do arquivo.

---

Dentro do middleware de tratamento de erros, como uma verificação fallback.

---

No final, depois do middleware de tratamento de erros.

#### --answer--

Depois de todas as rotas mas antes do middleware de tratamento de erros.

### --question--

#### --text--

Qual das opções a seguir é uma melhor prática para tratamento de erros em apps Express de produção?

#### --distractors--

Sempre enviar o rastreamento completo da pilha para o cliente para facilitar a depuração.

---

Colocar o middleware de tratamento de erros antes das rotas para processamento mais rápido.

---

Tratar todos os erros, incluindo 404s, dentro de um único middleware.

#### --answer--

Registrar detalhes do erro internamente e retornar uma mensagem amigável ao usuário em vez de enviar rastreamentos para os usuários.

### --question--

#### --text--

Como você habilita todas as mensagens de depuração embutidas do Express ao iniciar seu app?

#### --distractors--

Passar `{ debug: true }` para o construtor `express()`.

---

Chamar `app.enable('debug')` antes de definir as rotas.

---

Adicionar `require('express-debug')` no topo do arquivo de entrada.

#### --answer--

Definir a variável de ambiente `DEBUG` para `express:*` ao executar o app.

### --question--

#### --text--

O que `DEBUG=express:router node index.js` faz?

#### --distractors--

Habilita toda a saída de depuração do Express, incluindo middleware, roteamento e eventos de aplicação.

---

Desabilita toda a saída de depuração completamente e executa o app em modo silencioso de produção.

---

Registra apenas detalhes das requisições HTTP recebidas como método e URL no terminal.

#### --answer--

Mostra apenas a saída de depuração relacionada ao roteador, filtrando outros namespaces do Express.

### --question--

#### --text--

Para que serve `morgan` em uma aplicação Express?

#### --distractors--

Capturar e encaminhar automaticamente erros assíncronos em manipuladores de rota Express para `next`.

---

Servir arquivos estáticos de um diretório especificado sem definir rotas individuais.

---

Conectar o Express a um banco de dados relacional e registrar todas as consultas executadas.

#### --answer--

Registrar requisições HTTP com detalhes como método, URL, código de status e tempo de resposta.

### --question--

#### --text--

Qual é o propósito de uma rota de health check em um app Express?

#### --distractors--

Reiniciar automaticamente o servidor e redirecionar o tráfego quando o volume de requisições for muito alto.

---

Registrar e encaminhar todas as requisições HTTP recebidas para um serviço externo de monitoramento ou análise.

---

Inspecionar e bloquear requisições de endereços IP que não estejam em uma lista aprovada.

#### --answer--

Informar se o app está funcionando corretamente para que balanceadores de carga e orquestradores decidam se o serviço está saudável.

### --question--

#### --text--

O que o sinal `SIGTERM` indica para um processo Node.js?

#### --distractors--

Reiniciar imediatamente, limpar todo o estado em memória e reinicializar todas as conexões ativas.

---

Gerar um snapshot completo da memória no disco para análise e depuração posterior.

---

Forçar o encerramento de todas as conexões ativas com banco de dados e fechar todos os descritores de arquivo abertos imediatamente.

#### --answer--

Começar o desligamento, dando ao app a chance de terminar o trabalho existente antes de sair.

### --question--

#### --text--

O que acontece ao chamar `server.close()` durante um desligamento gracioso?

#### --distractors--

Termina imediatamente todas as conexões ativas e encerra o processo.

---

Envia uma resposta 500 para todos os clientes conectados no momento.

---

Exclui todos os dados de sessão em memória e reinicia o servidor.

#### --answer--

Para de aceitar novas conexões enquanto permite que as existentes terminem.

### --question--

#### --text--

Por que você retornaria um status 503 da sua rota de health check durante um desligamento gracioso?

#### --distractors--

Para registrar o evento de desligamento e o timestamp em um painel externo de monitoramento para fins de auditoria.

---

Para forçar o orquestrador a iniciar imediatamente uma nova instância substituta antes do término do desligamento.

---

Para notificar todos os clientes conectados que suas sessões ativas foram salvas com sucesso.

#### --answer--

Para sinalizar ao balanceador de carga que o app está indisponível para que ele pare de redirecionar novo tráfego para ele.
