---
id: 5900f4081000cf542c50ff1a
title: '問題 155：計算電容器電路'
challengeType: 1
forumTopicId: 301786
dashedName: problem-155-counting-capacitor-circuits
---

# --description--

一個電路僅使用相同電容值 C 的相同電容器。

電容器可以串聯或平行連接以形成子單元，然後這些子單元可以再與其他電容器或其他子單元串聯或平行連接，以形成更大的子單元，如此類推直到最終電路。

使用這個簡單的程序和最多 n 個相同的電容器，我們可以製作具有不同總電容值範圍的電路。 例如，使用最多 $n = 3$ 個每個為 $60 μF$ 的電容器，我們可以得到以下 7 個不同的總電容值：

<img alt="example circuits having up to three capacitors, each of 60 μF" src="https://cdn.freecodecamp.org/curriculum/project-euler/counting-capacitor-circuits.gif" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

如果我們用 $D(n)$ 表示在使用最多 n 個相等電容值的電容器以及上述簡單程序時，可以獲得的不同總電容值的數量，我們有：$D(1) = 1, D(2) = 3, D(3)=7, \ldots$

尋找 $D(18)$。

提醒：當以平行方式連接電容器 $C_1$、$C_2$ 等時，總電容為 $C_T = C_1 + C_2 + \cdots$，而當以串聯方式連接時，整體電容則為：$\frac{1}{C_T} = \frac{1}{C_1} + \frac{1}{C_2} + \cdots$。

# --hints--

`capacitanceValues()` 應該傳回 `3857447`。

```js
assert.strictEqual(capacitanceValues(), 3857447);
```

# --seed--

## --seed-contents--

```js
function capacitanceValues() {

  return true;
}

capacitanceValues();
```

# --solutions--

```js
// solution required
```
