---
id: 6a298d88d1606c0f1ef322a9
title: 認證與授權小測驗
challengeType: 8
dashedName: quiz-authentication-and-authorization
---

# --description--

要通過此測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

JSON Web Token 的三個部分是什麼？

#### --distractors--

標頭、主體和頁腳

---

令牌、有效載荷和雜湊

---

ID、聲明和憑證

#### --answer--

標頭、有效載荷和簽名

### --question--

#### --text--

以下哪一項是作為認證因素的「你擁有的東西」的範例？

#### --distractors--

指紋掃描。

---

你記住的密碼。

---

安全問題的答案。

#### --answer--

傳送到你智慧型手機的一次性代碼。

### --question--

#### --text--

以下哪一項最能描述授權？

#### --distractors--

驗證使用者的憑證是否與儲存的記錄相符。

---

使用者成功登入後產生會話令牌。

---

在將使用者密碼儲存到資料庫前加密。

#### --answer--

決定已認證的使用者被允許執行哪些動作。

### --question--

#### --text--

什麼是 Passport.js Strategy？

#### --distractors--

將應用程式路由對應到認證規則的設定檔。

---

內建函式，用來安全地雜湊並儲存使用者密碼。

---

使用者成功完成登入流程後產生的令牌。

#### --answer--

定義如何使用特定方法或提供者來認證使用者的外掛程式。

### --question--

#### --text--

Helmet.js 預設對 X-Powered-By 標頭做什麼？

#### --distractors--

用通用的佔位符值取代該標頭。

---

在傳送給客戶端前加密該標頭值。

---

將標頭資訊移到 HTTP-only cookie 中。

#### --answer--

完全移除 X-Powered-By 標頭。

### --question--

#### --text--

將 cookie 的 `SameSite` 屬性設為 `Strict` 會怎麼做？

#### --distractors--

在瀏覽器儲存前加密該 cookie。

---

確保該 cookie 僅在 HTTPS 連線時傳送。

---

阻止伺服器在每個回應中設定多於一個 cookie。

#### --answer--

阻止瀏覽器在任何跨站請求中傳送該 cookie。

### --question--

#### --text--

在 JWT 中，使用者的聲明資料（例如使用者 ID 或角色）儲存在何處？

#### --distractors--

在簽名區段。

---

在標頭區段。

---

在令牌外部的加密容器中。

#### --answer--

在有效載荷區段。

### --question--

#### --text--

Helmet 設定的哪個 HTTP 標頭可防止你的網站被嵌入其他網域的 iframe？

#### --distractors--

內容安全政策（Content-Security-Policy）

---

X-Powered-By

---

嚴格傳輸安全（Strict-Transport-Security）

#### --answer--

X-Frame-Options

### --question--

#### --text--

CSRF 攻擊如何利用瀏覽器？

#### --distractors--

它注入 JavaScript 到受害者瀏覽器以直接竊取 cookie。

---

它攔截受害者瀏覽器與可信伺服器間的流量。

---

它傳送釣魚連結，將受害者導向假登入頁面。

#### --answer--

它誘使瀏覽器自動傳送包含受害者會話 cookie 的請求。

### --question--

#### --text--

在 Express 應用程式中，`app.use(passport.initialize())` 做什麼？

#### --distractors--

向應用程式註冊新的認證 Strategy。

---

為每個訪問應用程式的使用者建立會話 cookie。

---

自動安裝所有可用的 Passport 策略。

#### --answer--

將 Passport 設為中介層，處理每個進來請求的認證。

### --question--

#### --text--

在基於角色的授權系統中，哪個角色通常擁有系統設定和使用者管理的完整存取權？

#### --distractors--

編輯者

---

版主

---

一般使用者

#### --answer--

管理員

### --question--

#### --text--

哪個指令安裝 Passport.js 核心套件和本地策略？

#### --distractors--

`npm install passport.js passport-local`

---

`npm install passport passport-auth`

---

`npm install passportjs local-strategy`

#### --answer--

`npm install passport passport-local`

### --question--

#### --text--

為什麼絕不應該在 JWT 有效載荷中儲存密碼？

#### --distractors--

有效載荷有大小限制，無法儲存密碼長度的字串。

---

在有效載荷中儲存密碼會自動使令牌簽名失效。

---

JWT 令牌過快過期，裡面儲存的密碼沒用。

#### --answer--

有效載荷是 Base64 編碼，不是加密，任何持有令牌的人都能解碼。

### --question--

#### --text--

CSRF 令牌的目的是什麼？

#### --distractors--

在使用者的會話 cookie 儲存到瀏覽器前加密它。

---

驗證每個送到伺服器的 API 請求中的使用者身份。

---

用短期認證取代會話 cookie。

#### --answer--

確認請求來自合法前端，惡意網站無法讀取。

### --question--

#### --text--

Helmet 設定哪個 HTTP 回應標頭來防禦跨站指令碼攻擊（XSS）？

#### --distractors--

X-Frame-Options

---

X-Powered-By

---

嚴格傳輸安全（Strict-Transport-Security）

#### --answer--

內容安全政策（Content-Security-Policy）

### --question--

#### --text--

當使用者嘗試存取受保護資源時，哪個程序應該先發生？

#### --distractors--

授權

---

角色指派

---

會話建立

#### --answer--

認證

### --question--

#### --text--

使用者成功認證後，Passport.js 會附加什麼到請求物件？

#### --distractors--

作用中的會話令牌，作為 `req.sessionToken`。

---

用於登入的策略名稱，作為 `req.strategy`。

---

使用者的雜湊密碼，作為 `req.credentials`。

#### --answer--

已認證的使用者物件，作為 `req.user`。

### --question--

#### --text--

當伺服器收到有效載荷被竄改的 JWT 時會發生什麼？

#### --distractors--

伺服器解碼有效載荷並用新資料更新使用者記錄。

---

伺服器默默忽略竄改資料，使用原始令牌聲明。

---

伺服器刪除令牌並將使用者送回註冊頁面。

#### --answer--

簽名不匹配，伺服器拒絕該請求。

### --question--

#### --text--

與 `SameSite=Strict` 相比，`SameSite=Lax` 允許什麼？

#### --distractors--

允許所有跨站請求的 cookie，包括表單提交。

---

阻擋所有來自不同網域的請求的 cookie。

---

要求使用者在任何跨站導覽時重新登入。

#### --answer--

允許頂層導覽時傳送 cookie，但仍阻擋跨站表單提交和 API 請求。

### --question--

#### --text--

Helmet 設定的 Strict-Transport-Security 標頭強制執行什麼？

#### --distractors--

強制伺服器拒絕所有非 HTTPS 請求並回傳錯誤。

---

在傳送給瀏覽器前加密整個 HTTP 回應主體。

---

要求所有 cookie 設定 Secure 和 HttpOnly 旗標。

#### --answer--

強制瀏覽器僅透過 HTTPS 與伺服器通訊。
