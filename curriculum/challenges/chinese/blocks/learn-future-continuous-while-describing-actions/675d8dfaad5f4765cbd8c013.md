---
id: 675d8dfaad5f4765cbd8c013
title: 任务 64
challengeType: 19
dashedName: task-64
lang: en-US
---

# --instructions--

本任务无音频。 阅读下面的问题，选择正确的答案。

# --questions--

## --text--

哪个句子在标签问句中正确使用了 `don't`？

## --answers--

`She likes coffee, don't she?`

### --feedback--

主语 `she` 需要 `doesn't`，而不是 `don't`。

---

`We need to resolve this quickly, don't we?`

---

`They know the answer, don't it?`

### --feedback--

主语 `they` 需要 `don't they`，而不是 `don't it`。

---

`The system is working fine, don't it?`

### --feedback--

主语 `it` 需要 `doesn't`，而不是 `don't`。

## --video-solution--

2

# --explanation--

标签疑问句必须与主句的主语和辅助动词匹配。
