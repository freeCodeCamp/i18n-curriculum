---
id: 69f42d414b7626e21a922794
title: Qué es el middleware para manejo de errores
challengeType: 19
dashedName: what-is-error-handling-middleware
---

# --description--

El middleware para manejo de errores en Express es un tipo especial de middleware que se usa para gestionar los errores que ocurren durante el ciclo de solicitud-respuesta. Es esencial para capturar errores inesperados, registrarlos y proporcionar respuestas adecuadas al cliente.

Como viste en una lección anterior sobre manejo de errores, el middleware para manejo de errores de Express tiene cuatro parámetros: `err`, `req`, `res` y `next`. Este middleware se invoca cuando se pasa un error a la función `next()`.

Aquí tienes un recordatorio de la sintaxis una vez más:

```js
app.use((err, req, res, next) => {
  console.error(err.message);
  res.status(500).send("Internal Server Error");
});
```

Y así es como se ve eso en una app básica de Express:

```js
const express = require('express')
const app = express()

// A simple route that throws an error
app.get('/', (req, res) => {
  throw new Error('Something went wrong!')
})

// Error-handling middleware
app.use((err, req, res, next) => {
  console.error(err.stack); // Logs the error stack
  res.status(500).send('Something went wrong!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* Manejador de ruta: El manejador de ruta lanza intencionalmente un error para simular un problema.
* Middleware para manejo de errores: Este middleware se registra con `app.use()`. Captura errores pasados mediante `next(err)` y envía una respuesta HTTP 500 al cliente con un mensaje genérico de error.
* `err`: Este es el objeto error que se pasa al middleware.

El middleware para manejo de errores asegura que cuando algo sale mal en tu aplicación, los usuarios aún reciban una respuesta adecuada. Es importante incluir siempre este middleware al final de tu pila de middleware para capturar cualquier error.

# --questions--

## --text--

¿Qué hace el middleware para manejo de errores?

## --answers--

Procesa solicitudes y envía respuestas.

### --feedback--

Concéntrate en lo que sucede cuando algo sale mal durante una solicitud.

---

Gestiona los errores que ocurren durante el ciclo de solicitud-respuesta.

---

Sirve archivos estáticos.

### --feedback--

Concéntrate en lo que sucede cuando algo sale mal durante una solicitud.

---

Gestiona la lógica de rutas.

### --feedback--

Concéntrate en lo que sucede cuando algo sale mal durante una solicitud.

## --video-solution--

2

## --text--

¿Cuáles de los siguientes parámetros son obligatorios en el middleware para manejo de errores?

## --answers--

`err`, `req`, `res`, `next`.

---

`req` y `res`.

### --feedback--

Piensa en los parámetros que Express usa para reconocer manejadores de errores.

---

`err` y `next`.

### --feedback--

Piensa en los parámetros que Express usa para reconocer manejadores de errores.

---

`err` y `req`.

### --feedback--

Piensa en los parámetros que Express usa para reconocer manejadores de errores.

## --video-solution--

1

## --text--

¿Dónde debe colocarse el middleware para manejo de errores en la pila de middleware?

## --answers--

En la parte superior, antes de otros middleware.

### --feedback--

Piensa en dónde se colocan los manejadores de errores en código que puede fallar.

---

En medio de la pila.

### --feedback--

Piensa en dónde se colocan los manejadores de errores en código que puede fallar.

---

En la parte inferior, después de otros middleware.

---

No hay un lugar específico para ello.

### --feedback--

Piensa en dónde se colocan los manejadores de errores en código que puede fallar.

## --video-solution--

3
