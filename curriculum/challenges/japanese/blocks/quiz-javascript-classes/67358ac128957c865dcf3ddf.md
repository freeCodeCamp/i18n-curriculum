---
id: 67358ac128957c865dcf3ddf
title: JavaScript クラス クイズ
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScriptにおけるクラスの目的は何ですか？

#### --distractors--

メソッドではなくプロパティのみを定義すること。

---

静的変数のみを定義すること。

---

プリミティブデータのみを整理すること。

#### --answer--

オブジェクトの再利用可能な設計図を作成すること。

### --question--

#### --text--

JavaScriptでクラスを定義するにはどうしますか？

#### --distractors--

`className` キーワードを使う。

---

`object` キーワードを使う。

---

`constructor` キーワードのみを使う。

#### --answer--

`class` キーワードを使う。

### --question--

#### --text--

クラス内でプロパティを初期化するために使われるメソッドはどれですか？

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

クラスのコンストラクタ内で`this`は何を表しますか？

#### --distractors--

グローバルクラスオブジェクト。

---

関数自体。

---

未定義の参照。

#### --answer--

クラスのインスタンス。

### --question--

#### --text--

クラスメソッド内のアロー関数で`this`がどのように振る舞うか説明してください。

#### --distractors--

異なるスコープの最も近い関数から`this`の値を継承する。

---

新しく作成されたクラスのインスタンスを指す。

---

厳格モードのグローバルオブジェクトから`this`の値を継承する。

#### --answer--

定義されている囲むスコープから`this`の値を継承する。

### --question--

#### --text--

`extends` キーワードの主な目的は何ですか？

#### --distractors--

親クラスのコンストラクタで静的メソッドを定義すること。

---

クラス内でメソッドを作成すること。

---

親クラスのデフォルトプロパティを初期化すること。

#### --answer--

親クラスから継承するサブクラスを作成すること。

### --question--

#### --text--

`Car` クラスが`Vehicle` クラスから継承する例として正しいものはどれですか？

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

次のコードの出力は何ですか？

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

エラーが発生する。

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

サブクラス内で`super` キーワードの役割は何ですか？

#### --distractors--

親クラスの新しいインスタンスを定義すること。

---

親クラスのメソッドやプロパティを削除すること。

---

親クラスの静的メソッドのみをアクセスすること。

#### --answer--

親クラスのコンストラクタやメソッドを呼び出すこと。

### --question--

#### --text--

静的メソッドとはどのようなものですか？

#### --distractors--

クラス内でアクセスできないメソッド。

---

アロー関数でのみ使われるメソッド。

---

クラスの各インスタンスに固有のメソッド。

#### --answer--

クラス自体に属し、インスタンスには属さないメソッド。

### --question--

#### --text--

次のコードは何を出力しますか？

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

エラーが発生する。

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

JavaScriptクラスで静的プロパティにアクセスするにはどうしますか？

#### --distractors--

メソッド内で`this.propertyName`を使う。

---

インスタンスを作成した後に`instance.propertyName`を使う。

---

サブクラス内で`super.propertyName`を使う。

#### --answer--

`ClassName.propertyName`を使う。

### --question--

#### --text--

インスタンスメソッドより静的メソッドを使うのはどんな場合ですか？

#### --distractors--

インスタンスのプロパティにアクセスして変更する必要がある場合。

---

メソッドをオブジェクトインスタンスで呼び出す必要がある場合。

---

メソッドがインスタンス固有のデータに依存せず、クラスレベルのデータを操作する場合。

#### --answer--

メソッドがクラスのすべてのインスタンスで共有されるデータを操作する場合。

### --question--

#### --text--

次のコードは何を出力しますか？

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

エラーをスローする。

### --question--

#### --text--

別のクラスからプロパティやメソッドを継承するために使うキーワードは何ですか？

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

次のコードは何を出力しますか？

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

エラーが発生する。

#### --answer--

`Human`

### --question--

#### --text--

継承を最もよく表す説明はどれですか？

#### --distractors--

メソッドが新しいオブジェクトを返すことを可能にする。

---

インスタンスが独自のプロパティを定義できるようにする。

---

静的プロパティを非静的プロパティに変換できるようにする。

#### --answer--

クラスが別のクラスからプロパティやメソッドを取得できるようにする。

### --question--

#### --text--

サブクラスにコンストラクタが定義されていない場合はどうなりますか？

#### --distractors--

エラーがスローされる。

---

サブクラスはコード内のどのクラスからでもコンストラクタを継承する。

---

サブクラスは新しいインスタンスを作成できない。

#### --answer--

スーパークラスのコンストラクタがデフォルトで使われる。

### --question--

#### --text--

次のコードで`this`は何を表しますか？

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

グローバルオブジェクト。

---

`Animal` クラス自体。

---

未定義の変数。

#### --answer--

`Animal` のインスタンス。

### --question--

#### --text--

静的プロパティの主な用途は何ですか？

#### --distractors--

各クラスインスタンスのデフォルトプロパティを定義すること。

---

サブクラスでメソッドをオーバーライドすること。

---

インスタンス固有のデータを定義すること。

#### --answer--

インスタンス間で共有されるクラス全体のデータを格納すること。
