---
id: 66edd07682767adff3a6231e
title: JavaScriptによるDOM操作とクリックイベントのクイズ
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

Document Object Modelとは何ですか？

#### --distractors--

HTMLページとやり取りするために使われるプログラミング言語。

---

浮動小数点数のみを含むオブジェクトの集合。

---

HTML要素を表すJavaScriptのクラス群。

#### --answer--

HTMLドキュメントとやり取りできるプログラミングインターフェース。

### --question--

#### --text--

APIは何の略ですか？

#### --distractors--

Additional Programming Interface

---

Adding Programming Interface

---

Another Programming Interface

#### --answer--

Application Programming Interface

### --question--

#### --text--

JavaScriptを使って、リンクが欠落しているすべてのHTMLアンカーをどのように見つけますか？

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

`textContent` プロパティは何をしますか？

#### --distractors--

すべてのHTML要素からテキストコンテンツを削除します。

---

JavaScriptファイルからテキストコンテンツを削除します。

---

JavaScriptファイル内のすべての配列とオブジェクトにテキストを追加します。

#### --answer--

要素のプレーンテキストコンテンツを返します。

### --question--

#### --text--

`innerHTML` プロパティは何をしますか？

#### --distractors--

HTML要素のすべてのテキストコンテンツを取得するために使われる`Element`のプロパティです。

---

HTMLに注釈を追加するために使われる`Element`のプロパティです。

---

HTMLでイベントを発火させるために使われる`Element`のプロパティです。

#### --answer--

HTMLマークアップの一部を設定または更新するために使われる`Element`のプロパティです。

### --question--

#### --text--

次のうち有効なイベントリスナーを追加するものはどれですか？

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

次のうち`document`オブジェクトの有効なメソッドでないものはどれですか？

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()`

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

次のうち`class`属性を`my-class`に設定するものはどれですか？

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

`Event`オブジェクトとは何ですか？

#### --distractors--

JavaScriptプロジェクトからイベントを削除するための特別なオブジェクトです。

---

JavaScriptプロジェクトに自動的にイベントを追加するために使われます。

---

配列を扱うときに使われる特別なオブジェクトです。

#### --answer--

ユーザーがウェブページと何らかの形でやり取りしたときに発火するペイロードです。

### --question--

#### --text--

`addEventListener()`メソッドを使って要素に追加したイベントリスナーを削除するために使うメソッドはどれですか？

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

インラインイベントハンドラとは何ですか？

#### --distractors--

すべてのイベントをキャンセルするために使われるHTML要素の特別な属性。

---

JavaScriptコード内でフォーム送信をトリガーするためだけに使われるHTML要素の特別な属性。

---

イベントを削除するために使われるHTML要素の特別な属性。

#### --answer--

イベントが発生したときにJavaScriptコードを実行するために使われるHTML要素の特別な属性。

### --question--

#### --text--

ウェブAPIの主な2つのカテゴリは何ですか？

#### --distractors--

Java APIとサードパーティAPI。

---

ブラウザAPIとRust API。

---

システムAPIとサードパーティAPI。

#### --answer--

ブラウザAPIとサードパーティAPI。

### --question--

#### --text--

HTMLページのすべての要素がブラウザによって解析されたときに発火するイベントはどれですか？

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

次のうち要素のインラインスタイルを表すプロパティはどれですか？

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

インラインイベントハンドラを使う正しい方法はどれですか？

#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--

指定した時間だけ処理を遅延させるために使うメソッドはどれですか？

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

JavaScript内で直接アニメーションを作成・制御するために使えるのはどれですか？

#### --distractors--

Allow Animations API

---

Control Animations API

---

Create Animations API

#### --answer--

Web Animations API

### --question--

#### --text--

Canvas APIは何をしますか？

#### --distractors--

CSSファイル内のグラフィックをアニメーション化するために使われます。

---

JavaScriptファイルからグラフィックを削除するためだけに使われます。

---

HTMLファイルからグラフィックを削除するために使われます。

#### --answer--

JavaScriptファイルを通じてグラフィックを操作するために使われます。

### --question--

#### --text--

イベントデリゲーションとは何ですか？

#### --distractors--

イベントがキャンセルされたときにイベントを監視するプロセス。

---

クリックイベントが発生したときにイベントを監視するプロセス。

---

変更イベントが発生したときにイベントを監視するプロセス。

#### --answer--

バブリングして親に届いたイベントを監視するプロセス。

### --question--

#### --text--

ダイアログやモーダルを扱うときに使えないメソッドはどれですか？

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`
