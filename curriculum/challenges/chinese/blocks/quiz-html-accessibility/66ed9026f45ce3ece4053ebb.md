---
id: 66ed9026f45ce3ece4053ebb
title: HTML 无障碍测验
challengeType: 8
dashedName: quiz-html-accessibility
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是无障碍？

#### --distractors--

无障碍是一套标准化实践，可确保你的代码不存在任何安全风险。

---

无障碍是一套标准化实践，可提高网络应用程序的速度和性能。

---

无障碍是一套标准化实践，可确保代码满足 100% 的测试覆盖率。

#### --answer--

无障碍是一套标准化实践，用于确保每个人（包括残障人士）都能使用你的网络应用程序。


### --question--

#### --text--

以下哪项是正确的标题层次结构的良好范例？

#### --distractors--

```html
<h3>Heading 3</h3>
<h3>Heading 3</h3>
<h3>Heading 3</h3>

<h1>Heading 1</h1>
<h2>Heading 2</h2>
<h4>Heading 4</h4>
```

---

```html
<h6>Heading 6</h6>

<h1>Heading 1</h1>
<h1>Heading 1</h1>

<h5>Heading 5</h5>

<h2>Heading 2</h2>
<h2>Heading 2</h2>
```

---

```html
<h6>Heading 6</h6>
<h5>Heading 5</h5>
<h4>Heading 4</h4>
<h3>Heading 3</h3>
<h2>Heading 2</h2>
<h1>Heading 1</h1>
```

#### --answer--

```html
<h1>Heading 1</h1>
<h2>Heading 2</h2>
<h3>Heading 3</h3>
<h4>Heading 4</h4>
<h5>Heading 5</h5>
<h6>Heading 6</h6>
```

### --question--

#### --text--

ARIA 角色有哪六大类？

#### --distractors--

文档结构、地标、窗口、显式、组件和页脚。

---

地标、房屋、实时区域、应用、窗口和扩展。

---

窗口、警报对话框、文章、单元格、横幅和按钮。

#### --answer--

文档结构、地标、窗口、抽象、组件和实时区域。

### --question--

#### --text--

向 `img` 元素添加 `alt` 属性的正确方法是什么？

#### --distractors--

```html
alt="cat running"<img src="cat.jpg">
```

---

```html
alt=""<img src="cat.jpg">
```

---

```html
<img src="cat.jpg"> alt="cat running"
```

#### --answer--

```html
<img src="cat.jpg" alt="cat running">
```

### --question--

#### --text--

WAI-ARIA 代表什么？

#### --distractors--

Website Accessible Initiative - Accessible Rust Internet Applications。

---

Web Anchor Initiative - Anchor Rich Internet Applications。

---

Web Accessibility Initial - Accessible Ready Internet Applications。

#### --answer--

Web Accessibility Initiative - Accessible Rich Internet Applications。

### --question--

#### --text--

`aria-hidden` 属性的作用是什么？

#### --distractors--

该属性用于仅向有视觉障碍的用户隐藏元素。

---

该属性用于隐藏和添加元素的标签文本。

---

该属性用于仅向键盘用户隐藏元素。

#### --answer--

该属性用于向辅助技术隐藏元素。

### --question--

#### --text--

哪个属性可使元素成为焦点并定义使用键盘导航元素的相对顺序？

#### --distractors--

`keyboardindex`

---

`tabbingindex`

---

`indextab`

#### --answer--

`tabindex`

### --question--

#### --text--

为什么在 HTML 表格中包含 `caption` 元素是一种良好做法？

#### --distractors--

它通过增加一个装饰性的表头来提升表格的外观。

---

它允许用户轻松对表格数据进行排序和过滤。

---

它有助于提高表格在移动设备上的响应速度。


#### --answer--

这有助于用户快速了解表格的目的和内容。


### --question--

#### --text--

`aria-label` 和 `aria-labelledby` 属性的区别是什么？

#### --distractors--

`aria-label` 和 `aria-labelledby` 属性的作用相同。

---

`aria-label` 属性用于为标签赋予颜色，而 `aria-labelledby` 属性则用于赋予标签高度。

---

`aria-label` 和 `aria-labelledby` 属性保持标签未定义。

#### --answer--

`aria-labelledby` 属性允许你通过引用页面上的现有元素，为元素赋予一个可访问的名称，而 `aria-label` 属性允许你在属性本身中定义名称。

### --question--

#### --text--

以下哪个属性能通过引用页面上的现有内容为屏幕阅读器用户提供有关元素的附加信息？

#### --distractors--

`aria-described`

---

`aria-set`

---

`aria-sets`

#### --answer--

`aria-describedby`

### --question--

#### --text--

以下哪个属性用于定义元素的键盘快捷键？

#### --distractors--

`provideaccesskey`

---

`tabindexkey`

---

`accessingkey`

#### --answer--

`accesskey`

### --question--

#### --text--

以下哪个示例是将 `label` 与 `input` 元素关联的正确方法？

#### --distractors--

```html
<form>
  <label accessible="name">Your Name</label>
  <input type="text" id="name" />
</form>
```

---

```html
<form>
  <label for="name">Your Name</label>
  <input type="text" id="first-name" />
</form>
```

---

```html
<form>
  <label associate="name">Your Name</label>
  <input type="text" id="name" />
</form>
```

#### --answer--

```html
<form>
  <label for="name">Your Name</label>
  <input type="text" id="name" />
</form>
```

### --question--

#### --text--

屏幕放大镜如何帮助视障用户浏览网页？

#### --distractors--

屏幕放大镜可将图像放大至两倍大小，以便视障用户更好地查看。

---

屏幕放大镜可将文字转换为盲文，方便触觉阅读。

---

屏幕放大镜可自动调整网页的色彩对比度。

#### --answer--

屏幕放大镜可以放大和缩小，帮助视障用户浏览网页。

### --question--

#### --text--

以下哪项是正确使用 `aria-describedby` 来提供有关按钮操作的更多上下文？

#### --distractors--

```html
<button id="delete-message" aria-describedby="delete-message">Delete</button>

<p aria-describedby="delete-message" id="delete-message">Warning! All deletions are permanent.</p>
```

---

```html
<button id="delete-btn" aria-describedby="delete-btn">Delete</button>

<p aria-describedby="delete-message" id="delete-message">Warning! All deletions are permanent.</p>
```

---

```html
<button id="delete-message">Delete</button>

<p aria-describedby="delete-message">Warning! All deletions are permanent.</p>
```

#### --answer--

```html
<button aria-describedby="delete-message">Delete</button>

<p id="delete-message">Warning! All deletions are permanent.</p>
```

### --question--

#### --text--

WCAG 和 WAI-ARIA 有什么区别？

#### --distractors--

WCAG 提供了移动设备无障碍的一般指导原则，而 WAI-ARIA 则提供了使辅助技术用户能够无障碍访问动态和交互式内容的具体规则。

---

WAI-ARIA 提供了移动设备无障碍的一般指导原则，而 WCAG 则提供了使辅助技术用户能够无障碍访问动态和交互式内容的具体规则。

---

WAI-ARIA 提供了网络无障碍的一般指导原则，而 WCAG 则提供了使辅助技术用户能够无障碍访问动态和交互式内容的具体规则。

#### --answer--

WCAG 提供了网络无障碍的一般指导原则，而 WAI-ARIA 则提供了使辅助技术用户能够无障碍访问动态和交互式内容的具体规则。

### --question--

#### --text--

以下哪项是使用 `aria-hidden` 属性的常见用例？


#### --distractors--

不包含重要信息的表单和表格。

---

不包含重要信息的段落和标题。

---

只有装饰作用的链接和图像。

#### --answer--

只有装饰作用的图标和图像。

### --question--

#### --text--

`role` 属性的作用是什么？

#### --distractors--

使用 CSS 动画为元素设计样式，让辅助技术更好地理解元素的解释和交互方式。

---

定义 HTML 元素的视觉外观，使辅助技术能够更好地理解元素的解释和交互方式。

---

为表单元素添加标签，使辅助技术能更好地理解元素的解释和交互方式。

#### --answer--

指定元素的类型和目的，使辅助技术能更好地理解元素的解释和交互方式。

### --question--

#### --text--

为什么在链接中使用描述性链接文本很重要？

#### --distractors--

使链接文本从页面其他内容中脱颖而出。

---

以确保点击链接时加载速度更快。

---

确保搜索引擎始终将你的网站列为搜索结果的第一位。

#### --answer--

确保包括辅助技术用户在内的所有人都了解链接的目的。

### --question--

#### --text--

以下哪项是使音频和视频内容无障碍的最佳实践？

#### --distractors--

提供与音频和视频内容相配套的高质量图形，以便视力障碍者可以使用。

---

使用鲜艳的颜色和大号文字，使内容更吸引人，让视觉障碍者也能阅读。

---

限制音频和视频内容的长度，使有听力障碍的人也能观看。

#### --answer--

为音频和视频内容提供说明和文稿，使有听力障碍的人也能观看。

### --question--

#### --text--

《网页内容可访问性指南》的四项原则是什么？

#### --distractors--

可感知、可操作、单元和健壮性。

---

可感知、操作性、可理解和健壮性。

---

持久性、可操作、可理解性和健壮性。

#### --answer--

可感知、可操作、可理解和健壮性。

