---
id: 67f412ac59601c4151b763da
title: 字典與集合測驗
challengeType: 8
dashedName: quiz-dictionaries-and-sets
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是字典？

#### --distractors--

一個只包含字串和列表的資料結構。

---

一種資料結構，是只接受大數字的鍵值配對群集。

---

一種只包含元組群集的資料結構。

#### --answer--

一種由鍵值配對組成的資料結構。

### --question--

#### --text--

以下哪一項是字典的正確語法？

#### --distractors--

```py
dictionary = {
    <key1: value1>,
    <key2: value2>
}
```

---

```py
dictionary = {
    (key1: value1),
    (key2: value2)
}
```

---

```py
dictionary = {
    [key1: value1],
    [key2: value2]
}
```

#### --answer--

```py
dictionary = {
    key1: value1,
    key2: value2
}
```

### --question--

#### --text--

以下關於字典的敘述何者正確？

#### --distractors--

鍵必須至少有兩個字元長。

---

鍵必須包含特殊符號。

---

金鑰必須至少包含一個數字。

#### --answer--

鍵必須是唯一的。

### --question--

#### --text--

下列哪一個建構式可以用來創建字典？

#### --distractors--

`diction()`

---

`dic()`

---

`dictionary()`

#### --answer--

`dict()`

### --question--

#### --text--

以下哪一種方式是從字典中正確存取值的方法？

#### --distractors--

`dictionary<key>`

---

`dictionary/key/`

---

`dictionary{key}`

#### --answer--

`dictionary[key]`

### --question--

#### --text--

以下哪一種方式是更新字典中值的正確方法？

#### --distractors--

`pizza['name'] >> 'Margherita'`

---

`pizza['name'] << 'Margherita'`

---

`pizza['name'] == 'Margherita'`

#### --answer--

`pizza['name'] = 'Margherita'`

### --question--

#### --text--

下列哪一種方法可以用來取得與鍵相關的值？

#### --distractors--

`access()`

---

`set()`

---

`retrieve()`

#### --answer--

`get()`

### --question--

#### --text--

什麼是視圖物件？

#### --distractors--

一個用來將字串轉換成字典的特殊物件。

---

一種只有在字典具有兩個或以上的鍵值配對時，才查看字典內容的方法。

---

一個用於將列表轉換成字典的特殊物件。

#### --answer--

一種在不建立資料獨立副本的情況下查看字典內容的方法。

### --question--

#### --text--

下列哪一個方法會傳回包含字典中所有鍵值對的檢視物件？

#### --distractors--

`lists()`

---

`dictionaries()`

---

`collections()`

#### --answer--

`items()`

### --question--

#### --text--

以下哪一個方法會移除字典中所有的鍵值對組？

#### --distractors--

`empty()`

---

`replace()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

以下 `code` 的輸出結果會是什麼？

```py
products = {
    'Laptop': 990,
    'Smartphone': 600,
    'Tablet': 250,
    'Headphones': 70,
}

for product in products.items():
    print(product)
```

#### --distractors--

```py
<'Laptop', 990>
<'Smartphone', 600>
<'Tablet', 250>
<'Headphones', 70>
```

---

```py
'Laptop', 990
'Smartphone', 600
'Tablet', 250
'Headphones', 70
```

---

```py
['Laptop', 990]
['Smartphone', 600]
['Tablet', 250]
['Headphones', 70]
```

#### --answer--

```py
('Laptop', 990)
('Smartphone', 600)
('Tablet', 250)
('Headphones', 70)
```

### --question--

#### --text--

以下關於集合的敘述何者正確？

#### --distractors--

集合是可變的，且順序為反向排列。

---

集合是可變的且有序的。

---

集合是不可變的且無序的。

#### --answer--

集合是可變的且無序的。

### --question--

#### --text--

以下哪一種是正確的建立集合的方法？

#### --distractors--

`my_set = (1, 2, 3, 4, 5)`

---

`my_set = <1, 2, 3, 4, 5>`

---

`my_set = [1, 2, 3, 4, 5]`

#### --answer--

`my_set = {1, 2, 3, 4, 5}`

### --question--

#### --text--

以下哪一個函式（程式）用於創建一個空集合？

#### --distractors--

`create_set()`

---

`empty_set()`

---

`sets()`

#### --answer--

`set()`

### --question--

#### --text--

將會輸出到終端機的是什麼？

```py
my_set = {1, 2, 3, 4, 5, 6}
my_set.add(5)

print(my_set)
```

#### --distractors--

`SyntaxError`

---

`RangeError`

---

`{1, 2, 3, 4, 5, 5, 6}`

#### --answer--

`{1, 2, 3, 4, 5, 6}`

### --question--

#### --text--

以下哪一個方法會檢查集合是否為子集合？ 

#### --distractors--

`issub()`

---

`isset()`

---

`subset()`

#### --answer--

`issubset()`

### --question--

#### --text--

以下哪一個方法會檢查兩個集合是否不相交？

#### --distractors--

`joint()`

---

`isjoint()`

---

`disjoint()`

#### --answer--

`isdisjoint()`

### --question--

#### --text--

對稱差運算子(`^`)的作用是什麼？

#### --distractors--

它傳回一個只包含集合共同元素的新集合。

---

它會找出集合之間的差異，並使用該結果更新第一個集合。

---

它傳回一個新集合，該集合包含第一個集合中不在另一個集合內的元素。 

#### --answer--

它傳回一個新的集合，該集合包含只存在於第一個集合或第二個集合中，但不在兩者中同時存在的元素。

### --question--

#### --text--

以下哪一個內建模組用於產生隨機數？

#### --distractors--

`set_random`

---

`get_random`

---

`rand`

#### --answer--

`random`

### --question--

#### --text--

以下哪一個模組用於處理正規表達式？

#### --distractors--

`regex`

---

`reg`

---

`r`

#### --answer--

`re`

