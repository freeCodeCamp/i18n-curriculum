---
id: 65e18d61500d930ce8ed90a5
title: 変数と演算子を学ぼう レッスンB
challengeType: 15
dashedName: learn-variables-and-operators-lesson-b
---
# --description--

変数はコード内のデータを入れておく「ストレージコンテナ」のようなものだと考えてください。

<img src="https://cdn.freecodecamp.org/curriculum/odin-project/variables-and-operators/top-learn-variables-and-operators.png" alt='the concept of variables in coding as boxes labeled "birthday" that store and provide access to data, specifically a date "16-09-2003"'>

変数とはデータの「名前付きストア」です。変数を使ってお菓子や訪問者、その他のデータを保存できます。

JavaScriptで変数を作るには、`let` キーワードを使います。

以下の文は「message」という名前の変数を作成（つまり宣言）しています：

```js
let message;
```

次に、代入演算子 `=` を使ってデータを入れられます：

```js
let message;

// store the string 'Hello' in the variable named message
message = 'Hello'; 
```

簡潔にするために、変数の宣言と代入を1行でまとめることもできます：

```js
let message = 'Hello';
```


# --questions--

## --text--

JavaScriptで`let` キーワードを使う目的は何ですか？

## --answers--

数学的な計算を行うため。

---

変更できない定数の値を宣言するため。

---

データを保存でき、代入や変更が可能な変数を宣言するため。

---

関数を作成するため。


## --video-solution--

3
