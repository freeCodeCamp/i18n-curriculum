---
id: 68376949899fcbad3c9cac23
title: JavaScript とアクセシビリティのクイズ
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

このクイズに合格するには、以下の10問中少なくとも9問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

コントロールが展開されているか折りたたまれているかを示すために、アクセシビリティ目的で使用される属性はどれですか？

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

`aria-expanded` と組み合わせて、制御要素と制御される要素の間にプログラム的な接続を確立するために使える2つの属性はどれですか？

#### --distractors--

`aria-labelledBy` and `aria-owns`

---

`aria-controls` and `aria-label`

---

`aria-live` and `aria-owns`

#### --answer--

`aria-controls` and `aria-owns`

### --question--

#### --text--

ページ上にライブリージョンを作成し、ライブリージョン内の動的コンテンツの変化をスクリーンリーダーユーザーに通知するために使われる属性はどれですか？

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

次のうち、スクリーンリーダーなどの支援技術を使う人にのみ無効化される `div` 要素はどれですか？

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

`aria-expanded` 属性の有効な使い方はどれですか？

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

制御要素とページ上で影響を与える要素の間にプログラム的な関係を作るために使われる属性はどれですか？

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

ライブリージョンの一般的な使用例ではないものはどれですか？

#### --distractors--

操作後に表示されるメッセージ（エラーメッセージや確認メッセージなど）。

---

定期的に更新されるコンテンツ（ティッカー、マーキー、カウントダウンタイマーなど）。

---

一般的なステータスメッセージ（プロセスの更新など）。

#### --answer--

操作前に表示されるメッセージ（確認ダイアログなど）。

### --question--

#### --text--

現在選択されているタブを示す正しい方法は次のうちどれですか？

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

`aria-live` 属性の有効な値ではないものはどれですか？

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

次の `aria-live` 属性値のうち、更新が非常に重要で最高の優先度を持つことを意味するものはどれですか？

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`
