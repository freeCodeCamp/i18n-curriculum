---
id: 5a23c84252665b21eecc8016
title: الفرز باستخدام دالة مقارنة مخصصة
challengeType: 1
forumTopicId: 302309
dashedName: sort-using-a-custom-comparator
---

# --description--

اكتب دالة لفرز مصفوفة (أو قائمة) من السلاسل النصية حسب الطول تنازليًا، وبالترتيب المعجمي تصاعديًا للسلاسل النصية ذات الطول المتساوي.

# --hints--

يجب أن تكون `lengthSorter` دالة.

```js
assert(typeof lengthSorter == 'function');
```

يجب أن تُرجع `lengthSorter(["Here", "are", "some", "sample", "strings", "to", "be", "sorted"])` مصفوفة.

```js
assert(
  Array.isArray(
    lengthSorter([
      'Here',
      'are',
      'some',
      'sample',
      'strings',
      'to',
      'be',
      'sorted'
    ])
  )
);
```

يجب أن تُرجع `lengthSorter(["Here", "are", "some", "sample", "strings", "to", "be", "sorted"])` القيمة `["strings", "sample", "sorted", "Here", "some", "are", "be", "to"]`.

```js
assert.deepEqual(
  lengthSorter([
    'Here',
    'are',
    'some',
    'sample',
    'strings',
    'to',
    'be',
    'sorted'
  ]),
  ['strings', 'sample', 'sorted', 'Here', 'some', 'are', 'be', 'to']
);
```

يجب أن تُرجع `lengthSorter(["I", "hope", "your", "day", "is", "going", "good", "?"])` القيمة `["going", "good", "hope", "your", "day", "is", "?","I"]`.

```js
assert.deepEqual(
  lengthSorter(['I', 'hope', 'your', 'day', 'is', 'going', 'good', '?']),
  ['going', 'good', 'hope', 'your', 'day', 'is', '?', 'I']
);
```

يجب أن تُرجع `lengthSorter(["Mine", "is", "going", "great"])` القيمة `["going", "great", "Mine", "is"]`.

```js
assert.deepEqual(lengthSorter(['Mine', 'is', 'going', 'great']), [
  'going',
  'great',
  'Mine',
  'is'
]);
```

يجب أن تُرجع `lengthSorter(["Have", "fun", "sorting", "!!"])` القيمة `["sorting", "Have", "fun", "!!"]`.

```js
assert.deepEqual(lengthSorter(['Have', 'fun', 'sorting', '!!']), [
  'sorting',
  'Have',
  'fun',
  '!!'
]);
```

يجب أن تُرجع `lengthSorter(["Everything", "is", "good", "!!"])` القيمة `["Everything", "good", "!!", "is"]`.

```js
assert.deepEqual(lengthSorter(['Everything', 'is', 'good', '!!']), [
  'Everything',
  'good',
  '!!',
  'is'
]);
```

# --seed--

## --seed-contents--

```js
function lengthSorter(arr) {

}
```

# --solutions--

```js
function lengthSorter(arr) {
  arr.sort(function(a, b) {
    var result = b.length - a.length;
    if (result == 0) result = a.localeCompare(b);
    return result;
  });
  return arr;
}
```
