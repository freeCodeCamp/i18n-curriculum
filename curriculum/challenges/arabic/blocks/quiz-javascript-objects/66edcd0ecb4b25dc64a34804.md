---
id: 66edcd0ecb4b25dc64a34804
title: اختبار كائنات JavaScript
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي مما يلي لا يصف كائنًا في JavaScript؟

#### --distractors--

مجموعة من أزواج المفتاح-القيمة.

---

حاوية لخصائص وطرق.

---

نوع بيانات غير بدائي.

#### --answer--

نوع بيانات بدائي.

### --question--

#### --text--

أي من تعريفات الكائن التالية غير صحيحة؟

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

كيف يمكنك الوصول إلى الخاصية `job` في هذا الكائن `person`؟

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

كيف يمكنك الوصول إلى الخاصية `street` في كائن `person` أدناه؟

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

أي من مفاتيح الخصائص هذه يمكن الوصول إليها فقط باستخدام ترميز الأقواس؟

#### --distractors--

مفتاح خاصية معروف وثابت.

---

مفتاح خاصية مسكن ضمن عدة مستويات من كائن.

---

مفتاح خاصية قد لا يوجد في الكائن.

#### --answer--

مفتاح خاصية ديناميكي.

### --question--

#### --text--

أي من التالي يستخدم التفكيك بشكل صحيح للوصول إلى الخاصية `name` في كائن `person` أدناه؟

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

أي من التالي يستخدم التفكيك بشكل صحيح للوصول إلى الخاصية `flour` في كائن `recipe` أدناه؟

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

كيف يمكنك تعيين قيمة افتراضية لمفتاح خاصية عند استخدام التفكيك في JavaScript؟

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

كيف يمكنك التحقق مما إذا كان لكائن `car` خاصية `year`؟

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

ما هي طريقة الكائن؟

#### --distractors--

سلسلة نصية مرتبطة بكائن.

---

مصفوفة مرتبطة بكائن.

---

رقم مرتبط بكائن.

#### --answer--

دالة مرتبطة بكائن.

### --question--

#### --text--

كيف يمكنك استدعاء طريقة `add` في كائن `calculator` هذا لجمع الرقمين `10` و `7`؟

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

أي عامل يُزيل خاصية من كائن؟

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

كيف يمكنك التحقق مما إذا كانت خاصية `score` موجودة في كائن `player`؟

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

كيف يمكنك إنشاء كائن فارغ جديد في JavaScript؟

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

ما هي الصيغة النحوية الصحيحة لاستخدام التسلسل الاختياري في JavaScript؟

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

ما هو استخدام التسلسل الاختياري في JavaScript؟

#### --distractors--

يُستخدم لتعريف متغيرات بقيم افتراضية.

---

يُستخدم لتعريف طرق اختيارية داخل كائن قد تُستدعى أو لا تُستدعى.

---

يُستخدم لكتابة شفرة أكثر إيجازًا بتسلسل استدعاءات طرق متعددة في بيان واحد.

#### --answer--

يُستخدم للوصول الآمن إلى خصائص كائن قد تكون قيمتها `null` أو `undefined`.

### --question--

#### --text--

ما هي الطريقة المستخدمة لتحويل كائن JavaScript إلى سلسلة JSON؟

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

ما هي الطريقة التي تحول سلسلة JSON مرة أخرى إلى كائن JavaScript؟

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

أي من هذه الخيارات يصل إلى الخاصية `color` في كائن `toy` باستخدام ترميز الأقواس؟

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

كيف يمكنك الوصول إلى هاتف `home` في كائن `learner` هذا باستخدام ترميز النقطة في JavaScript؟

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`

