---
id: 61967e74a8e3690ab6292daa
title: 步骤2
challengeType: 0
dashedName: step-2
---

# --description--

针对 `body` 元素，将其 `background` 设置为顺时针 45 度的线性渐变，从 `rgb(118, 201, 255)` 开始到 `rgb(247, 255, 222)` 结束。

# --hints--

你应该使用 `body` 元素选择器。

```js
assert.exists(new __helpers.CSSHelp(document).getStyle('body'));
```

你应该在 `body` 选择器内设置 `background` 属性。

```js
assert.isTrue(new __helpers.CSSHelp(document).isPropertyUsed('background'));
```

你应该将 `background` 设置为 `linear-gradient(45deg, rgb(118, 201, 255), rgb(247, 255, 222))`。

```js
assert.include(['linear-gradient(45deg,rgb(118,201,255),rgb(247,255,222))', 'rgba(0,0,0,0)linear-gradient(45deg,rgb(118,201,255),rgb(247,255,222))repeatscroll0%0%'], new __helpers.CSSHelp(document).getStyle('body')?.getPropVal('background', true));
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Penguin</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>

  <body>
  </body>
</html>
```

```css
--fcc-editable-region--

--fcc-editable-region--
```
