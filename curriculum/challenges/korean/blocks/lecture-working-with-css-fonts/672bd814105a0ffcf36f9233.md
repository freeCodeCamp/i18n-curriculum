---
id: 672bd814105a0ffcf36f9233
title: 폰트 패밀리란 무엇이며 어떻게 작동할까요?
challengeType: 19
dashedName: what-are-font-families
---

# --interactive--

폰트 패밀리는 공통된 디자인을 공유하는 폰트 그룹입니다. 같은 패밀리에 속한 모든 폰트는 동일한 기본 서체를 기반으로 하지만, 스타일, 굵기, 너비에 변형이 있습니다. 이들을 비슷한 특징을 공유하지만 약간씩 다른 형제자매로 생각할 수 있습니다. 예를 들어, Arial은 Arial Bold와 Arial Italic 같은 변형을 포함하는 폰트 패밀리입니다.

CSS에서는 `font-family` 속성으로 폰트 패밀리를 설정할 수 있습니다. 다음은 단락의 폰트를 `Arial`로 설정하는 예입니다:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="arial-font">Example paragraph using Arial font.</p>
<p>Paragraph not using Arial font.</p>
```

```css
#arial-font {
  font-family: Arial;
}
```

:::

그런데 폰트 패밀리를 찾지 못하면 어떻게 될까요? 우선순위가 높은 것부터 낮은 순서로 여러 폰트 패밀리를 쉼표로 구분해 지정할 수 있습니다. 이 대체 폰트들은 대체 수단 역할을 합니다. 이 예에서는 Arial이 기본 폰트이고 Lato가 대체 폰트입니다:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="specified-font">Example paragraph using specified fonts.</p>
<p>Paragraph not using specified fonts.</p>
```

```css
#specified-font {
  font-family: Arial, Lato;
}
```

:::

브라우저는 기기에 Arial 폰트가 있으면 이를 렌더링합니다. 없으면 두 번째 폰트인 Lato를 렌더링합니다.

이 과정이 어떻게 작동하는지도 알아야 합니다. 첫 번째 폰트가 있으면 선택 과정이 멈추지 않습니다. 폰트 패밀리는 문자 하나씩 선택되므로, 특정 문자가 없으면 브라우저가 우선순위가 낮은 폰트에서 찾아 렌더링합니다. 흥미롭죠?

웹 개발 맥락에서는 일반 폰트 패밀리도 자주 사용합니다. 매우 중요합니다. 일반 폰트 패밀리는 우선순위가 높은 폰트가 없을 때 웹 브라우저가 기본으로 렌더링하는 폰트입니다. 콘텐츠가 읽기 쉽도록 브라우저가 지정된 일반 폰트 패밀리를 기준으로 가장 적합한 폰트로 원래 폰트를 대체합니다. 가장 흔히 쓰이는 일반 폰트 패밀리는 다음과 같습니다:

- serif
- sans-serif
- monospace
- cursive
- fantasy

하지만 선택할 수 있는 옵션은 더 많습니다. 아래 코드에는 세 가지 대체 폰트 패밀리가 있습니다:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="specified-font">Example paragraph using specified fonts.</p>
<p>Paragraph not using specified fonts.</p>
```

```css
#specified-font {
  font-family: Arial, Lato, sans-serif;
}
```

:::

Arial이 가장 우선순위가 높습니다. Arial이 없으면 브라우저가 Lato를 렌더링하려 시도합니다. 둘 다 없으면 브라우저가 일반 sans-serif 폰트 패밀리를 사용해 사용자의 시스템에 설치된 특성에 맞는 폰트를 선택합니다.

폰트 패밀리 목록 끝에는 항상 일반 폰트 패밀리를 포함해야 합니다. 일반 폰트는 원래 디자인에서 의도한 폰트와 다르게 보일 수 있으니, 다양한 브라우저에서 대체 폰트가 어떻게 보이는지 확인하는 것이 좋습니다.

사용자 경험을 최대한 일관되게 하려면 웹 안전 폰트를 사용하는 것도 권장합니다. 이 폰트 패밀리는 대부분 기기에 설치되어 있어 대부분 사용자에게 올바르게 렌더링될 가능성이 높습니다. 다음 수업에서 더 자세히 배울 것입니다.

폰트 패밀리는 웹 디자인에서 필수적입니다. font-family 속성을 사용하면 텍스트의 모양과 느낌을 즉시 바꿀 수 있습니다.

# --questions--

## --text--

`font-family` CSS 속성의 주요 목적은 무엇인가요?

## --answers--

텍스트의 색상을 설정하기 위해서입니다.

### --feedback--

텍스트의 시각적 모양을 생각해 보세요.

---

텍스트의 글자 크기를 조절하기 위해서입니다.

### --feedback--

텍스트의 시각적 모양을 생각해 보세요.

---

텍스트에 사용할 서체를 정의하기 위해서입니다.

---

텍스트의 줄 간격을 조절하기 위해서입니다.

### --feedback--

텍스트의 시각적 모양을 생각해 보세요.

## --video-solution--

3

## --text--

다음 중 일반 폰트 패밀리에 해당하는 것은 무엇인가요?

## --answers--

`Helvetica`

### --feedback--

폰트의 넓은 범주를 생각해 보세요.

---

`Times New Roman`

### --feedback--

폰트의 넓은 범주를 생각해 보세요.

---

`Sans-Serif`

---

`Arial`

### --feedback--

폰트의 넓은 범주를 생각해 보세요.

## --video-solution--

3

## --text--

CSS에서 여러 폰트 패밀리를 대체 수단으로 지정하려면 어떻게 해야 하나요?

## --answers--

쉼표로 구분해서 지정합니다.

---

중첩된 `font-family` 선언을 사용합니다.

### --feedback--

기본 폰트를 사용할 수 없을 때 대체 폰트를 제공하는 방법을 생각해 보세요.

---

`&` 문자를 사용합니다.

### --feedback--

기본 폰트를 사용할 수 없을 때 대체 폰트를 제공하는 방법을 생각해 보세요.

---

`|` 문자를 사용합니다.

### --feedback--

기본 폰트를 사용할 수 없을 때 대체 폰트를 제공하는 방법을 생각해 보세요.

## --video-solution--

1
