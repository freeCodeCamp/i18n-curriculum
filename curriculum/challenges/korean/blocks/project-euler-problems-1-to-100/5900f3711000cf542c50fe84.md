---
id: 5900f3711000cf542c50fe84
title: '문제 5: 가장 작은 배수'
challengeType: 1
forumTopicId: 302160
dashedName: problem-5-smallest-multiple
---

# --description--

2520은 1부터 10까지의 모든 수로 나누었을 때 나머지가 전혀 없는 가장 작은 수입니다.

1부터 `n`까지의 모든 수로 나누어떨어지는 가장 작은 양수는 무엇인가요?

# --hints--

`smallestMult(5)`은 숫자를 반환해야 합니다.

```js
assert.isNumber(smallestMult(5));
```

`smallestMult(5)`는 60을 반환해야 합니다.

```js
assert.strictEqual(smallestMult(5), 60);
```

`smallestMult(7)`는 420을 반환해야 합니다.

```js
assert.strictEqual(smallestMult(7), 420);
```

`smallestMult(10)`는 2520을 반환해야 합니다.

```js
assert.strictEqual(smallestMult(10), 2520);
```

`smallestMult(13)`는 360360을 반환해야 합니다.

```js
assert.strictEqual(smallestMult(13), 360360);
```

`smallestMult(20)`는 232792560을 반환해야 합니다.

```js
assert.strictEqual(smallestMult(20), 232792560);
```

# --seed--

## --seed-contents--

```js
function smallestMult(n) {

  return true;
}

smallestMult(20);
```

# --solutions--

```js
function smallestMult(n){
  function gcd(a, b) {
    return b === 0 ? a : gcd(b, a%b); // Euclidean algorithm
  }

  function lcm(a, b) {
    return a * b / gcd(a, b);
  }
  var result = 1;
  for(var i = 2; i <= n; i++) {
    result = lcm(result, i);
  }
  return result;
}
```
