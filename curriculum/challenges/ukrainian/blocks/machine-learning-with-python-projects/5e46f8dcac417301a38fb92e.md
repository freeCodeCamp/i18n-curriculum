---
id: 5e46f8dcac417301a38fb92e
title: Класифікатор зображень котів і собак
challengeType: 10
forumTopicId: 462377
dashedName: cat-and-dog-image-classifier
---

# --description--

Ви будете <a href="https://colab.research.google.com/github/freeCodeCamp/boilerplate-cat-and-dog-image-classifier/blob/master/fcc_cat_dog.ipynb" target="_blank" rel="noopener noreferrer nofollow">працювати над цим проєктом у Google Colaboratory</a>.

Перейдіть за цим посиланням і створіть копію блокнота у своєму акаунті або локально. Після завершення проєкту і проходження тесту (який є за цим посиланням) надішліть посилання на свій проєкт нижче. Якщо ви надсилаєте посилання Google Colaboratory, переконайтеся, що увімкнули спільний доступ за посиланням для «будь-кого, у кого є посилання».

Ми ще розробляємо інтерактивний навчальний вміст для навчальної програми з машинного навчання. Наразі ви можете проходити відеозавдання цієї сертифікації. Також можливо доведеться шукати додаткові навчальні ресурси, як це роблять при роботі над реальним проєктом.

# --instructions--

Для цього завдання ви маєте дописати код, щоб класифікувати зображення собак і котів. Ви будете використовувати TensorFlow 2.0 і Keras для створення згорткової нейронної мережі, яка правильно класифікуватиме зображення котів і собак принаймні з 63% точністю. (Додаткові бали, якщо вдасться досягти 70% точності!)

Частина коду вже надана, але деякі частини коду потрібно дописати, щоб завершити це завдання. Читайте інструкції в кожній текстовій клітинці, щоб знати, що потрібно зробити в кожній клітинці з кодом.

Перша клітинка з кодом імпортує необхідні бібліотеки. Друга клітинка завантажує дані і встановлює ключові змінні. Третя клітинка — це перше місце, де ви будете писати власний код.

Структура файлів датасету, які завантажуються, виглядає так (ви помітите, що в директорії test немає підпапок, і зображення не мають міток):

```py
cats_and_dogs
|__ train:
    |______ cats: [cat.0.jpg, cat.1.jpg ...]
    |______ dogs: [dog.0.jpg, dog.1.jpg ...]
|__ validation:
    |______ cats: [cat.2000.jpg, cat.2001.jpg ...]
    |______ dogs: [dog.2000.jpg, dog.2001.jpg ...]
|__ test: [1.jpg, 2.jpg ...]
```

Ви можете змінювати кількість епох і розмір батчу, якщо хочете, але це не обов’язково.

Наступні інструкції відповідають конкретним номерам клітинок, які позначені коментарем у верхній частині клітинки (наприклад, `# 3`).

## Cell 3

Тепер ваша черга! Встановіть кожну зі змінних у цій клітинці правильно. (Вони більше не повинні дорівнювати `None`.)

Створіть генератори зображень для кожного з трьох наборів даних (train, validation, test). Використовуйте `ImageDataGenerator` для читання/декодування зображень і перетворення їх у тензори з плаваючою точкою. Використовуйте аргумент `rescale` (і поки що жодних інших аргументів), щоб масштабувати тензори зі значень від 0 до 255 до значень від 0 до 1.

Для змінних `*_data_gen` використовуйте метод `flow_from_directory`. Передайте розмір батчу, директорію, цільовий розмір (`(IMG_HEIGHT, IMG_WIDTH)`), режим класу та все інше, що потрібно. `test_data_gen` буде найскладнішим. Для `test_data_gen` обов’язково передайте `shuffle=False` у метод `flow_from_directory`. Це гарантує, що фінальні передбачення залишаться в порядку, який очікує наш тест. Для `test_data_gen` також буде корисно врахувати структуру директорії.

Після запуску коду вихідні дані мають виглядати так:

```py
Found 2000 images belonging to 2 classes.
Found 1000 images belonging to 2 classes.
Found 50 images belonging to 1 class.
```

## Cell 4

Функція `plotImages` буде використовуватися кілька разів для побудови зображень на графіку. Вона приймає масив зображень і список ймовірностей, хоча список ймовірностей необов’язковий. Цей код наданий вам. Якщо ви правильно створили змінну `train_data_gen`, то запуск цієї клітинки побудує п’ять випадкових зображень з тренувального набору.

## Cell 5

Перестворіть `train_image_generator` за допомогою `ImageDataGenerator`.

Оскільки прикладів для тренування небагато, існує ризик перенавчання. Один зі способів вирішити цю проблему — створити більше тренувальних даних із наявних прикладів, використовуючи випадкові трансформації.

Додайте 4-6 випадкових трансформацій як аргументи до `ImageDataGenerator`. Переконайтеся, що масштабування таке саме, як і раніше.

## Cell 6

У цій клітинці нічого робити не потрібно. `train_data_gen` створюється так само, як і раніше, але з новим `train_image_generator`. Потім одне зображення п’ять разів відображається з різними варіаціями.

## Cell 7

У цій клітинці створіть модель нейронної мережі, яка видає ймовірності класів. Вона має використовувати послідовну модель Keras. Ймовірно, це буде стек шарів Conv2D і MaxPooling2D, а потім повнозв’язний шар зверху з активацією ReLU.

Скомпілюйте модель, передавши аргументи для встановлення оптимізатора і функції втрат. Також передайте `metrics=['accuracy']`, щоб бачити точність тренування і валідації на кожній епосі.

## Cell 8

Використайте метод `fit` на вашій `model`, щоб навчити мережу. Обов’язково передайте аргументи для `x`, `steps_per_epoch`, `epochs`, `validation_data` і `validation_steps`.

## Cell 9

Запустіть цю клітинку, щоб візуалізувати точність і втрати моделі.

## Cell 10

Тепер час використати вашу модель, щоб передбачити, чи є нове зображення котом чи собакою.

У цій клітинці отримайте ймовірність того, що кожне тестове зображення (з `test_data_gen`) — це собака або кіт. `probabilities` має бути списком цілих чисел.

Викличте функцію `plotImages` і передайте тестові зображення та ймовірності, що відповідають кожному тестовому зображенню.

Після запуску клітинки ви побачите всі 50 тестових зображень з міткою, що показує відсоток впевненості, що зображення — кіт або собака. Точність відповідатиме точності, показаній на графіку вище (після запуску попередньої клітинки). Більше тренувальних зображень може призвести до вищої точності.

## Cell 11

Запустіть цю фінальну клітинку, щоб дізнатися, чи пройшли ви завдання, чи потрібно продовжувати спроби.

# --hints--

Вона має пройти всі тести Python.

```js

```

# --solutions--

```py
  # Python challenges don't need solutions,
  # because they would need to be tested against a full working project.
  # Please check our contributing guidelines to learn more.
```
