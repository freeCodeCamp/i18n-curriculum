---
id: 692487767b6b728454f53f29
title: Тест на основні модулі NodeJS
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

Щоб пройти тест, потрібно правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який метод `fs` додає вміст до існуючого файлу, не перезаписуючи його?

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

Що відбувається, коли ви використовуєте синхронні методи з модуля `fs`?

#### --distractors--

Вони автоматично видаляють існуючі файли.

---

Вони працюють швидше за асинхронні у всіх випадках.

---

Вони за замовчуванням повертають проміси.

#### --answer--

Вони блокують програму, доки операція не завершиться.

### --question--

#### --text--

Що робить параметр `utf8` у `fs.readFile()`?

#### --distractors--

Він запобігає виникненню помилок.

---

Він змінює розширення файлу.

---

Він стискає великі файли.

#### --answer--

Він вказує Node повертати читабельний рядок замість буфера.

### --question--

#### --text--

Який метод `Buffer` створює новий буфер, заповнений нулями?

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

Що робить `Buffer.toString()`?

#### --distractors--

Перетворює буфер у двійковий код.

---

Очищає вміст буфера.

---

Показує довжину буфера в байтах.

#### --answer--

Перетворює вміст буфера у читабельний рядок.

### --question--

#### --text--

Яка функція `crypto` вимагає приватного ключа для створення підпису?

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

Чому слід уникати написання власних криптоалгоритмів безпосередньо?

#### --distractors--

Це незаконно у застосунках Node.js.

---

Node.js не підтримує шифрування.

---

JavaScript не може обчислювати хеші.

#### --answer--

Це може бути небезпечно без добре протестованих бібліотек.

### --question--

#### --text--

Який метод `crypto` рекомендують для генерації безпечних випадкових токенів замість `Math.random()`?

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

Який метод модуля OS повертає архітектуру центрального процесора?

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

Що повертає `os.userInfo()`?

#### --distractors--

Тільки ім’я користувача.

---

Тільки IP-адресу пристрою.

---

Тільки ідентифікатор увійшовшого користувача.

#### --answer--

Інформацію про поточного системного користувача.

### --question--

#### --text--

Який метод `path` об’єднує сегменти та нормалізує зайві слеші?

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

Який метод `path` створює абсолютний шлях на основі поточної робочої директорії?

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

Що представляє глобальна змінна `__dirname`?

#### --distractors--

Ім’я користувача, що запускає Node.

---

Розширення поточного файлу.

---

Відносний шлях папки, що починається з `./`.

#### --answer--

Абсолютний шлях директорії, що містить поточний файл.

### --question--

#### --text--

Яка властивість process містить змінні середовища, як `NODE_ENV`?

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

Що робить подія `process.on("exit")`?

#### --distractors--

Вона перезапускає застосунок Node.

---

Вона надсилає сигнал `exit` дочірнім процесам.

---

Вона очищає чергу циклу подій.

#### --answer--

Виконує код безпосередньо перед завершенням процесу Node.

### --question--

#### --text--

Який тип потоку може і читати, і записувати дані, але не трансформує їх?

#### --distractors--

Readable stream

---

Transform stream

---

Writable stream

#### --answer--

Duplex stream

### --question--

#### --text--

Що потрібно для ефективного читання і запису великих файлів?

#### --distractors--

Використовувати лише `Buffer.alloc()`

---

Імпортувати модуль `crypto`

---

Спочатку зберегти весь файл у пам’яті

#### --answer--

Використовувати потоки для обробки даних частинами

### --question--

#### --text--

Який метод з’єднує readable stream з writable stream?

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

Яка подія потоку вказує, що більше немає даних для запису?

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

Що буде виведено, якщо надрукувати частину readable stream без `toString()`?

#### --distractors--

Звичайний текстовий вміст

---

PID процесу

---

Назва операційної системи

#### --answer--

Об’єкт буфера з двійковими даними
