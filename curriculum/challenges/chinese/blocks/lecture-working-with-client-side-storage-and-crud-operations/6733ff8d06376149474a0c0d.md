---
id: 6733ff8d06376149474a0c0d
title: 什么是 `Cache API`，它是如何工作的？
challengeType: 19
dashedName: what-is-the-cache-api-and-how-does-that-work
---

# --description--

`Cache` API 用于保存网络请求和响应，使网页应用运行更高效，甚至可以离线工作。它是更广泛的 Service Worker API 的一部分，对于创建能够在不可靠或缓慢网络条件下运行的渐进式网页应用（PWA）至关重要。 

在继续使用 `Cache` API 之前，我们首先需要了解 service workers、PWAs 和网络请求的基本工作原理。 

网络请求是由网页浏览器或应用向服务器发出的请求，用于通过互联网检索数据或资源。例如，当你访问一个网站时，你的浏览器会向网络服务器发送网络请求，以获取显示页面所需的文件（例如 `HTML`、图像或视频）。

服务工作者是一种特殊类型的 JavaScript `file`，在网页 `application` 的后台运行，独立于主浏览器 `thread`。它充当网页和 `network` 之间的中间人，允许开发者拦截 `network` `request`、缓存资源，并处理推送通知或后台同步等功能。

PWA，或渐进式网页应用，是一种使用现代网页技术在网页上提供类似本地应用体验的网页应用类型。PWA 设计为能够在任何设备上可靠运行，无论网络状况如何，都可以通过浏览器访问或像传统应用一样安装到用户设备上。 

现在我们对 service workers、PWAs 和网络请求有了更深入的了解，我们可以更深入地探讨 Cache API。

`Cache` API 是一种存储机制，用于保存 `Request` 和 `Response` 对象。当向服务器发出请求时，应用可以储存响应，之后从缓存中检索它，而不是发起新的网络请求。这减少了装载时间，节省了带宽，并提升了整体用户体验。

浏览器提供了一个称为 `CacheStorage` 的存储区域，开发者可以在其中储存网络请求及其对应响应的键值对。

通过 `Cache-Control` 头部，开发者可以指定缓存资源应保存多长时间，以及是否应重新验证或直接从缓存中提供。 

通过使用 `Cache` API，开发者可以构建离线优先的网页应用，例如允许 PWA 在用户断开网络连接时提供缓存的资源。

# --questions--

## --text--

Cache API 在网页应用中的主要目的是什么？

## --answers--

处理用户认证和会话。

### --feedback--

考虑 Cache API 在网页性能、离线功能性和缓存网络请求方面的作用。

---

为离线访问保存和管理网络请求和响应。

---

启用推送通知和后台同步。

### --feedback--

考虑 Cache API 在网页性能、离线功能性和缓存网络请求方面的作用。

---

创建和管理 service workers。

### --feedback--

考虑 Cache API 在网页性能、离线功能性和缓存网络请求方面的作用。

## --video-solution--

2

## --text--

以下哪项最能描述服务工作者在网页应用中的作用？

## --answers--

一个处理 UI `Render` 的后台 `Script`。

### --feedback--

考虑 service workers 如何拦截 `network requests` 并启用网页应用的离线行为。

---

一个用于管理用户认证和会话的工具。

### --feedback--

考虑 service workers 如何拦截 `network requests` 并启用网页应用的离线行为。

---

一个特殊的 JavaScript 文件，在后台运行，管理网络请求和缓存资源。

---

一个用于实现与服务器离线数据同步的库。

### --feedback--

考虑 service workers 如何拦截 `network requests` 并启用网页应用的离线行为。

## --video-solution--

3

## --text--

在网页应用的上下文中，什么是网络请求？

## --answers--

由网页浏览器发出的请求，用于从局部存储数据库中检索文件。

### --feedback--

考虑当一个用户访问网站时会发生什么，以及图像、脚本和数据等资源如何装载到浏览器中。

---

由网页浏览器或应用向服务器发出的请求，用于通过互联网检索数据或资源。

---

网络服务器向用户设备发出的推送更新的请求。

### --feedback--

考虑当一个用户访问网站时会发生什么，以及图像、脚本和数据等资源如何装载到浏览器中。

---

服务器发出的用于验证用户认证的请求。

### --feedback--

考虑当一个用户访问网站时会发生什么，以及图像、脚本和数据等资源如何装载到浏览器中。

## --video-solution--

2
