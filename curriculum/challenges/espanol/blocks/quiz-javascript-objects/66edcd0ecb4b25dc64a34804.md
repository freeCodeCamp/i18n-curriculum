---
id: 66edcd0ecb4b25dc64a34804
title: Cuestionario de Objetos en JavaScript
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

Para aprobar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Cuál de las siguientes opciones NO describe un objeto en JavaScript?

#### --distractors--

Una colección de pares clave-valor.

---

Un contenedor para propiedades y métodos.

---

Un tipo de dato no primitivo.

#### --answer--

Un tipo de dato primitivo.

### --question--

#### --text--

¿Cuál de las siguientes definiciones de objetos es inválida?

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

¿Cómo puede acceder a la propiedad `job` de este objeto `person`?

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

¿Cómo puede acceder a la propiedad `street` del objeto `person` a continuación?

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

¿Cuáles de estas claves de propiedad solo pueden accederse utilizando la notación de corchetes?

#### --distractors--

Una clave de propiedad que es conocida y estática.

---

Una clave de propiedad que está anidada dentro de múltiples niveles de un objeto.

---

Una clave de propiedad que podría no existir en el objeto.

#### --answer--

Una clave de propiedad que es dinámica.

### --question--

#### --text--

¿Cuál de las siguientes opciones usa correctamente asignación por destructuración para acceder a la propiedad `name` del objeto `person` a continuación?

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

¿Cuál de las siguientes opciones usa correctamente asignación por destructuración para acceder a la propiedad `flour` del objeto `recipe` a continuación?

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

¿Cómo puede asignar un valor predeterminado a una clave de propiedad al utilizar destructuración en JavaScript?

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

¿Cómo puede verificar si un objeto `car` tiene una propiedad `year`?

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

¿Qué es un método de objeto?

#### --distractors--

Una cadena asociada con un objeto.

---

Un array asociado con un objeto.

---

Un número asociado con un objeto.

#### --answer--

Una función asociada con un objeto.

### --question--

#### --text--

¿Cómo puede llamar al método `add` de este objeto `calculator` para sumar los números `10` y `7`?

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

¿Qué operador elimina una propiedad de un objeto?

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

¿Cómo puede verificar si una propiedad `score` existe en un objeto `player`?

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

¿Cómo puede crear un nuevo objeto vacío en JavaScript?

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

¿Cuál es la sintaxis correcta para usar el encadenamiento opcional en JavaScript?

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

¿Para qué se utiliza el encadenamiento opcional en JavaScript?

#### --distractors--

Utilizado para definir variables con valores por defecto.

---

Utilizado para definir métodos opcionales dentro de un objeto que pueden o no ser llamados.

---

Utilizado para escribir código más conciso encadenando múltiples llamadas de métodos en una sola declaración.

#### --answer--

Utilizado para acceder de manera segura a las propiedades de un objeto que podrían ser `null` o `undefined`.

### --question--

#### --text--

¿Qué método se utiliza para convertir un objeto JavaScript en una cadena JSON?

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

¿Qué método convierte una cadena JSON de nuevo en un objeto JavaScript?

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

¿Cuál de estas opciones accede a la propiedad `color` de un objeto `toy` usando la notación de corchetes?

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

¿Cómo puede acceder al teléfono `home` de este objeto `learner` en JavaScript usando la notación de punto?

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`

