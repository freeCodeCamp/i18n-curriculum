---
id: 6a298d88d1606c0f1ef322a9
title: Quiz de autenticação e autorização
challengeType: 8
dashedName: quiz-authentication-and-authorization
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quais são as três partes de um JSON Web Token?

#### --distractors--

Cabeçalho, corpo e rodapé

---

Token, payload e hash

---

ID, claims e certificado

#### --answer--

Cabeçalho, payload e assinatura

### --question--

#### --text--

Qual dos seguintes é um exemplo de "algo que você tem" como fator de autenticação?

#### --distractors--

Uma digital.

---

Uma senha que você memorizou.

---

A resposta a uma pergunta de segurança.

#### --answer--

Um código único enviado para seu smartphone.

### --question--

#### --text--

Qual das opções a seguir melhor descreve autorização?

#### --distractors--

Verificar se as credenciais do usuário correspondem aos registros armazenados.

---

Gerar um token de sessão após o usuário fazer login com sucesso.

---

Criptografar a senha do usuário antes de salvá-la no banco de dados.

#### --answer--

Decidir quais ações um usuário autenticado tem permissão para realizar.

### --question--

#### --text--

O que é uma Strategy do Passport.js?

#### --distractors--

Um arquivo de configuração que mapeia rotas da aplicação para regras de autenticação.

---

Uma função embutida que faz hash e armazena senhas de usuários com segurança.

---

Um token gerado após o usuário completar com sucesso o fluxo de login.

#### --answer--

Um plugin que define como autenticar usuários com um método ou provedor específico.

### --question--

#### --text--

O que o Helmet.js faz com o cabeçalho X-Powered-By por padrão?

#### --distractors--

Substitui o cabeçalho por um valor genérico de marcador de posição.

---

Criptografa o valor do cabeçalho antes de enviá-lo ao cliente.

---

Move a informação do cabeçalho para um cookie HTTP-only.

#### --answer--

Remove completamente o cabeçalho X-Powered-By.

### --question--

#### --text--

O que acontece ao definir o atributo `SameSite` de um cookie como `Strict`?

#### --distractors--

Criptografa o cookie antes de armazená-lo no navegador.

---

Garante que o cookie seja enviado apenas em conexões HTTPS.

---

Impede que o servidor defina mais de um cookie por resposta.

#### --answer--

Impede que o navegador envie o cookie em qualquer requisição cross-site.

### --question--

#### --text--

Em um JWT, onde os dados de claims do usuário, como id ou papel, são armazenados?

#### --distractors--

Na seção de assinatura.

---

Na seção de cabeçalho.

---

Em um container criptografado fora do token.

#### --answer--

Na seção de payload.

### --question--

#### --text--

Qual cabeçalho HTTP definido pelo Helmet impede que seu site seja incorporado dentro de um iframe em outro domínio?

#### --distractors--

Content-Security-Policy

---

X-Powered-By

---

Strict-Transport-Security

#### --answer--

X-Frame-Options

### --question--

#### --text--

Como um ataque CSRF explora o navegador?

#### --distractors--

Injeta JavaScript no navegador da vítima para roubar cookies diretamente.

---

Intercepta o tráfego entre o navegador da vítima e o servidor confiável.

---

Envia um link de phishing que redireciona a vítima para uma página falsa de login.

#### --answer--

Engana o navegador para enviar uma requisição que inclui automaticamente o cookie de sessão da vítima.

### --question--

#### --text--

O que `app.use(passport.initialize())` faz em um app Express?

#### --distractors--

Registra uma nova Strategy de autenticação na aplicação.

---

Cria um cookie de sessão para cada usuário que visita o app.

---

Instala automaticamente todas as strategies disponíveis do Passport.

#### --answer--

Configura o Passport como middleware para lidar com autenticação em cada requisição recebida.

### --question--

#### --text--

Qual papel em um sistema de autorização baseado em papéis geralmente tem acesso total às configurações do sistema e gerenciamento de usuários?

#### --distractors--

Editor

---

Moderador

---

Usuário padrão

#### --answer--

Admin

### --question--

#### --text--

Qual comando instala o pacote core do Passport.js e a strategy local?

#### --distractors--

`npm install passport.js passport-local`

---

`npm install passport passport-auth`

---

`npm install passportjs local-strategy`

#### --answer--

`npm install passport passport-local`

### --question--

#### --text--

Por que você nunca deve armazenar uma senha no payload de um JWT?

#### --distractors--

O payload tem um limite de tamanho que torna impossível armazenar strings do comprimento de uma senha.

---

Armazenar uma senha no payload invalida automaticamente a assinatura do token.

---

Tokens JWT expiram rápido demais para que senhas armazenadas neles sejam úteis.

#### --answer--

O payload é codificado em Base64, não criptografado, então qualquer um com o token pode decodificá-lo.

### --question--

#### --text--

Qual é o propósito de um token CSRF?

#### --distractors--

Criptografar o cookie de sessão do usuário antes de ser armazenado no navegador.

---

Verificar a identidade do usuário em cada requisição API enviada ao servidor.

---

Substituir o cookie de sessão por uma credencial de autenticação de curta duração.

#### --answer--

Confirmar que uma requisição veio do frontend legítimo, que um site malicioso não pode ler.

### --question--

#### --text--

Qual cabeçalho de resposta HTTP o Helmet define para defender contra Cross-Site Scripting (XSS)?

#### --distractors--

X-Frame-Options

---

X-Powered-By

---

Strict-Transport-Security

#### --answer--

Política de segurança de conteúdo

### --question--

#### --text--

Quando um usuário tenta acessar um recurso protegido, qual processo deve acontecer primeiro?

#### --distractors--

Autorização

---

Atribuição de papel

---

Criação de sessão

#### --answer--

Autenticação

### --question--

#### --text--

O que o Passport.js anexa ao objeto de requisição após um usuário autenticar com sucesso?

#### --distractors--

O token de sessão ativo como `req.sessionToken`.

---

O nome da strategy usada para login como `req.strategy`.

---

A senha hash do usuário como `req.credentials`.

#### --answer--

O objeto do usuário autenticado como `req.user`.

### --question--

#### --text--

O que acontece quando um servidor recebe um JWT cujo payload foi adulterado?

#### --distractors--

O servidor decodifica o payload e atualiza o registro do usuário com os novos dados.

---

O servidor ignora silenciosamente os dados adulterados e usa os claims originais do token.

---

O servidor exclui o token e envia o usuário de volta para a página de registro.

#### --answer--

A assinatura não vai coincidir e o servidor rejeita a requisição.

### --question--

#### --text--

Comparado a `SameSite=Strict`, o que `SameSite=Lax` permite?

#### --distractors--

Permite o cookie em todas as requisições cross-site, incluindo envios de formulários.

---

Bloqueia o cookie em todas as requisições que se originam fora do mesmo domínio.

---

Exige que o usuário faça login novamente em qualquer navegação cross-site.

#### --answer--

Permite o cookie na navegação de nível superior mas ainda bloqueia em envios de formulários e requisições API cross-site.

### --question--

#### --text--

O que o cabeçalho Strict-Transport-Security impõe quando definido pelo Helmet?

#### --distractors--

Força o servidor a rejeitar todas as requisições não HTTPS com uma resposta de erro.

---

Criptografa todo o corpo da resposta HTTP antes de enviá-lo ao navegador.

---

Exige que todos os cookies tenham as flags Secure e HttpOnly definidas.

#### --answer--

Força o navegador a se comunicar com o servidor apenas via HTTPS.
