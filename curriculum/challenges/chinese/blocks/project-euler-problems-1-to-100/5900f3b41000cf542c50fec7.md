---
id: 5900f3b41000cf542c50fec7
title: '关卡 72：分数计数'
challengeType: 1
forumTopicId: 302185
dashedName: problem-72-counting-fractions
---

# --description--

考虑分数 $\frac{n}{d}$，其中 `n` 和 `d` 是正整数。如果 `n` &lt; `d` 且最大公因数 ${HCF}(n, d) = 1`，则称其为最简真分数。

如果我们按大小升序列出 `d` ≤ 8 的约简真分数集合，我们得到：

$$\frac{1}{8}, \frac{1}{7}, \frac{1}{6}, \frac{1}{5}, \frac{1}{4}, \frac{2}{7}, \frac{1}{3}, \frac{3}{8}, \frac{2}{5}, \frac{3}{7}, \frac{1}{2}, \frac{4}{7}, \frac{3}{5}, \frac{5}{8}, \frac{2}{3}, \frac{5}{7}, \frac{3}{4}, \frac{4}{5}, \frac{5}{6}, \frac{6}{7}, \frac{7}{8}$$

可以看到该集合中有 `21` 个元素。

对于 `d` ≤ `limit`，约简真分数集合中将包含多少个元素？

# --hints--

`countingFractions(8)` 应该返回一个数字。

```js
assert(typeof countingFractions(8) === 'number');
```

`countingFractions(8)` 应该返回 `21`。

```js
assert.strictEqual(countingFractions(8), 21);
```

`countingFractions(20000)` 应该返回 `121590395`。

```js
assert.strictEqual(countingFractions(20000), 121590395);
```

`countingFractions(500000)` 应该返回 `75991039675`。

```js
assert.strictEqual(countingFractions(500000), 75991039675);
```

`countingFractions(1000000)` 应该返回 `303963552391`。

```js
assert.strictEqual(countingFractions(1000000), 303963552391);
```

# --seed--

## --seed-contents--

```js
function countingFractions(limit) {

  return true;
}

countingFractions(8);
```

# --solutions--

```js
function countingFractions(limit) {
  const phi = {};
  let count = 0;

  for (let i = 2; i <= limit; i++) {
    if (!phi[i]) {
      phi[i] = i;
    }
    if (phi[i] === i) {
      for (let j = i; j <= limit; j += i) {
        if (!phi[j]) {
          phi[j] = j;
        }
        phi[j] = (phi[j] / i) * (i - 1);
      }
    }
    count += phi[i];
  }

  return count;
}
```
