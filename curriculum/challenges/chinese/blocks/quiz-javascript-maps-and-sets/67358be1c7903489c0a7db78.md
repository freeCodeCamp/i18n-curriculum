---
id: 67358be1c7903489c0a7db78
title: JavaScript Maps 和 Sets 测验
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript 中的 Set 是什么？

#### --distractors--

允许重复值的数据结构。

---

一种只保存字串和数字的数据结构。

---

一个不允许删除其元素的集合。

#### --answer--

一个值只能出现一次的集合。

### --question--

#### --text--

以下哪个 `method` 在 `Maps` 中可用但在 `Sets` 中不可用？

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

使用什么方法将元素添加到 `Set`？

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

你如何查看 `Set` 是否包含某个值？

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

以下哪项是使用 Sets 的绝佳分支？

#### --distractors--

管理数据库中的键值点对。

---

创建有序的项列表。

---

在浏览器局部存储中存储分层数据结构。

#### --answer--

从数组中消除副本。

### --question--

#### --text--

JavaScript 中的 WeakSet 是什么？

#### --distractors--

一个类似集合的集合，只允许原语数据类型。

---

一个类似集合的集合，允许重复值。

---

一个类似集合的集合，防止值被移除。

#### --answer--

一个类似集合的集合，只保存对象。

### --question--

#### --text--

Set 和 WeakSet 之间的一个区别是什么？

#### --distractors--

WeakSet 可以保存任何类型的值，包括数字。

---

Set 不允许重复值，但 WeakSet 允许。

---

集合允许垃圾回收，而 WeakSet 不允许。

#### --answer--

Set 可以保存任何类型的值，而 WeakSet 只保存对象。

### --question--

#### --text--

JavaScript 中的 Map 是什么？

#### --distractors--

只允许唯一值的集合。

---

只能使用字串作为密钥的集合。

---

一个不允许删除其项的集合。

#### --answer--

任何类型的键值点对集合。

### --question--

#### --text--

将密钥-值点对插入到 `Map` 中使用了什么方法？

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

下面的代码会输出什么？

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

在 JavaScript 中，`Map` 与 `对象` 有何不同？

#### --distractors--

Map 只允许数字作为密钥。

---

对象允许任何类型作为密钥，而 Map 只允许字串。

---

映射不支持迭代，而对象支持。

#### --answer--

Maps 允许任何类型的密钥，但 Objects 只允许字串和符号。

### --question--

#### --text--

你如何从 `Map` 中移除一个密钥-值点对？

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

`Map` 的 `size` 属性返回什么？

#### --distractors--

地图中的索引数量。

---

Map 中唯一值的数量。

---

Map 使用的总内存。

#### --answer--

Map 中的密钥-值点对数量。

### --question--

#### --text--

JavaScript 中的 WeakMap 是什么？

#### --distractors--

一种类似 Map 的集合，允许使用原语值作为密钥。

---

一个类似 Map 的集合，阻止删除项。

---

一个类似 Map 的集合，会自动排序其密钥。

#### --answer--

一个类似 Map 的集合，对密钥使用弱引用。

### --question--

#### --text--

Map 和 WeakMap 之间的一个区别是什么？

#### --distractors--

Map 只允许原生类型作为密钥，而 WeakMap 只允许对象。

---

Map 允许垃圾回收，而 WeakMap 不允许。

---

WeakMap 允许副本密钥，而 Map 不允许。

#### --answer--

Map 是可迭代的，而 WeakMap 不是。

### --question--

#### --text--

以下哪个方法不存在于 `WeakMap` 上？

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

下面代码的输出是什么？

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

你如何迭代 `Set` 中的值？

#### --distractors--

通过使用 `.map()` 方法。

---

通过使用 `.reduce()` 方法。

---

通过使用 `.filter()` 方法。

#### --answer--

通过使用 `.forEach()` 方法。

### --question--

#### --text--

哪两个 Set 的方法返回包含某个 Set 值的 `SetIterator`？

#### --distractors--

`entries()` 和 `values()`

---

`forEach()` 和 `values()`

---

`has()` 和 `add()`

#### --answer--

`keys()` 和 `values()`

### --question--

#### --text--

下面代码的输出是什么？

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
