---
id: 5900f3731000cf542c50fe86
title: 'Changamoto 7: namba msingi ya 10001'
challengeType: 1
forumTopicId: 302182
dashedName: problem-7-10001st-prime
---

# --description--

Kwa kuorodhesha nambari msingi sita za kwanza: 2, 3, 5, 7, 11, na 13, tunaweza kuona kwamba namba msingi ya sita ni 13.

Namba msingi ya `n` ni ipi?

# --hints--

`nthPrime(6)` inapaswa kurudisha nambari.

```js
assert.isNumber(nthPrime(6));
```

`nthPrime(6)` inapaswa kurudisha 13.

```js
assert.strictEqual(nthPrime(6), 13);
```

`nthPrime(10)` inapaswa kurudisha 29.

```js
assert.strictEqual(nthPrime(10), 29);
```

`nthPrime(100)` inapaswa kurudisha 541.

```js
assert.strictEqual(nthPrime(100), 541);
```

`nthPrime(1000)` inapaswa kurudisha 7919.

```js
assert.strictEqual(nthPrime(1000), 7919);
```

`nthPrime(10001)` inapaswa kurudisha 104743.

```js
assert.strictEqual(nthPrime(10001), 104743);
```

# --seed--

## --seed-contents--

```js
function nthPrime(n) {

  return true;
}

nthPrime(10001);
```

# --solutions--

```js
const nthPrime = n => {
  let pN = 2;
  let step = 0;
  while (step < n) {
    let isPrime = true;
    let rootN = Math.sqrt(pN);
    for (let i = 2; i <= rootN; i++) {
      if (!(pN % i)) {
        isPrime = false;
        break;
      }
    }
    isPrime ? step++ : '';
    pN++;
  }
  return pN - 1;
}
```
