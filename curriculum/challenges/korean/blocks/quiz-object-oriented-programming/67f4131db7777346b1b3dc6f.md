---
id: 67f4131db7777346b1b3dc6f
title: 객체 지향 프로그래밍 퀴즈
challengeType: 8
dashedName: quiz-object-oriented-programming
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

객체를 생성하기 위한 청사진 역할을 하는 것은 무엇인가요?

#### --distractors--

함수

---

변수

---

반복문

#### --answer--

클래스

### --question--

#### --text--

Python에서 추상 클래스를 구현하는 데 사용하는 모듈은 무엇인가요?

#### --distractors--

`Time`

---

`xyz`

---

`Random`

#### --answer--

`abc`

### --question--

#### --text--

객체의 데이터와 동작을 정의하는 것은 무엇인가요?

#### --distractors--

함수와 클래스.

---

변수와 반복문.

---

시더와 데이터베이스.

#### --answer--

속성과 메서드.

### --question--

#### --text--

단일 상속이란 무엇인가요?

#### --distractors--

하나의 함수로부터 속성과 메서드를 상속받는 자식 클래스.

---

여러 함수로부터 속성과 메서드를 상속받는 자식 클래스.

---

여러 클래스로부터 속성과 메서드를 상속받는 자식 클래스.

#### --answer--

하나의 클래스로부터 속성과 메서드를 상속받는 자식 클래스.

### --question--

#### --text--

다음 중 객체 지향 프로그래밍의 핵심 원칙이 아닌 것은 무엇인가요?

#### --distractors--

캡슐화

---

상속

---

추상화

#### --answer--

반복하지 마세요(DRY)

### --question--

#### --text--

속성과 메서드에 접두사로 단일 밑줄과 이중 밑줄을 붙이는 것의 차이는 무엇인가요?

#### --distractors--

단일 밑줄은 속성을 완전히 숨기고, 이중 밑줄은 메서드 내부에서만 읽을 수 있게 한다.

---

단일 밑줄은 private, 이중 밑줄은 protected를 의미한다.

---

단일 밑줄은 public, 이중 밑줄은 static으로 만든다.

#### --answer--

단일 밑줄은 내부 사용을 위한 관례일 뿐이고, 이중 밑줄은 이름 맹글링을 유발해 속성과 메서드가 실수로 덮어쓰이는 것을 방지한다.

### --question--

#### --text--

복잡한 구현 세부사항을 숨기고 객체나 시스템의 필수 기능만 보여주는 객체 지향 개념은 무엇인가요?

#### --distractors--

상속

---

다형성

---

캡슐화

#### --answer--

추상화

### --question--

#### --text--

setter를 만들 때 사용하는 데코레이터는 무엇인가요?

#### --distractors--

`@<property_name>.creator`

---

`@<method_name>.creater`

---

`@<method_name>.creator`

#### --answer--

`@<property_name>.setter`

### --question--

#### --text--

객체의 내부 상태를 공개 메서드와 속성 집합 뒤에 숨기는 객체 지향 개념은 무엇인가요?

#### --distractors--

다형성

---

추상화

---

단일 상속

#### --answer--

캡슐화

### --question--

#### --text--

getter란 무엇인가요?

#### --distractors--

속성의 값을 업데이트하거나 변경하는 메서드.

---

객체에서 속성을 삭제하는 메서드.

---

객체에 새 속성을 만드는 메서드.

#### --answer--

속성의 값을 가져오거나 반환하는 메서드.

### --question--

#### --text--

setter란 무엇인가요?

#### --distractors--

속성의 값을 가져오는 메서드.

---

속성의 값을 삭제하는 메서드.

---

객체에서 속성을 삭제하는 메서드.

#### --answer--

속성의 값을 설정하거나 업데이트하는 메서드.

### --question--

#### --text--

setter와 getter로 설정하고 가져온 값을 삭제할 수 있게 하는 것은 무엇인가요?

#### --distractors--

Remover

---

Eraser

---

Delayer

#### --answer--

Deleter

### --question--

#### --text--

코드 재사용을 촉진하고 명확한 계층 구조를 제공하며 모든 것을 다시 작성하지 않고 동작을 맞춤화할 수 있게 하는 것은 무엇인가요?

#### --distractors--

DRY

---

WET

---

추상화

#### --answer--

상속

### --question--

#### --text--

다음 중 상속의 올바른 구문은 무엇인가요?

#### --distractors--

```py
class Parent:
    pass
class Child(InheritParent):
    pass
```

---

```py
class Parent:
    pass
class Child(Inheriter):
    pass
```

---

```py
class Child:
   pass
class Parent(Parent):
   pass
```

#### --answer--

```py
class Parent:
   pass
class Child(Parent):
   pass
```

### --question--

#### --text--

setter와 getter를 연결하는 것은 무엇인가요?

#### --distractors--

메서드

---

함수

---

클래스

#### --answer--

프로퍼티

### --question--

#### --text--

Python이 이중 밑줄이 붙은 속성 이름을 내부적으로 클래스 이름과 밑줄을 접두사로 붙여 바꾸는 과정을 무엇이라 하나요?

#### --distractors--

Name Mingling

---

다형성

---

추상화

#### --answer--

Name Mangling

### --question--

#### --text--

자식 클래스 내부에서 부모의 메서드를 호출할 수 있게 하는 함수는 무엇인가요?

#### --distractors--

`constructor()`

---

`__init__()`

---

`property()`

#### --answer--

`super()`

### --question--

#### --text--

다음 중 객체 지향 프로그래밍에서 다형성을 가장 잘 설명하는 문장은 무엇인가요?

#### --distractors--

클래스가 서로 다른 속성과 메서드를 가진 여러 생성자를 가질 수 있게 한다.

---

클래스가 다른 클래스로부터 속성과 메서드를 상속받을 수 있게 한다.

---

`class` 키워드로 객체를 생성할 수 있게 한다.

#### --answer--

서로 다른 클래스가 같은 메서드 이름을 사용하지만 호출될 때 서로 다른 동작을 수행할 수 있게 한다.

### --question--

#### --text--

Python이 이중 밑줄이 붙은 속성 이름을 맹글링하는 이유는 무엇인가요?

#### --distractors--

속성을 완전히 private으로 만들어 클래스 외부에서 접근할 수 없게 하기 위해서.

---

속성을 더 효율적으로 저장해 성능을 향상시키기 위해서.

---

속성을 자동으로 읽기 전용 프로퍼티로 변환하기 위해서.

#### --answer--

상속을 사용할 때 자식 클래스에서 속성과 메서드가 실수로 덮어쓰이는 것을 방지하기 위해서.

### --question--

#### --text--

getter와 setter에 메서드 대신 프로퍼티를 사용하는 이유는 무엇인가요?

#### --distractors--

코드를 더 길고 명확하게 만들기 위해서.

---

값에 대한 접근을 완전히 차단하기 위해서.

---

모든 값 변경을 자동으로 기록하기 위해서.

#### --answer--

점 표기법으로 직접 속성처럼 접근할 수 있게 해 가독성을 높이기 위해서.
