---
id: 5dc23991f86c76b9248c6eb8
title: Passaggio 6
challengeType: 0
dashedName: step-6
---

# --description--

Nel passaggio precedente, hai messo gli elementi `h1`, `h2`, commento e `p` all'interno dell'elemento `main`. Questo si chiama *annidamento*. Gli elementi annidati dovrebbero essere posizionati due spazi più a destra rispetto all'elemento in cui sono annidati. Questo spazio si chiama indentazione ed è usato per rendere l'HTML più facile da leggere.

Ecco un esempio di annidamento e indentazione:

```html
<main>
  <h1>Most important content of the document</h1>
  <p>Some more important content...</p>
</main>
```

L'elemento `h1`, l'elemento `h2` e il commento sono indentati di due spazi in più rispetto all'elemento `main` nel codice qui sotto. Usa la barra spaziatrice sulla tastiera per aggiungere due spazi davanti all'elemento `p` in modo che anche lui sia indentato correttamente.

# --hints--

Il tuo codice dovrebbe avere un elemento `h2` con il testo `Cat Photos`. Potresti averlo cancellato per errore, manca sia il tag di apertura sia il tag di chiusura, oppure il testo è cambiato.

```js
assert.exists(document.querySelector('h2'));
assert.match(code, /<\/h2\>/);
assert.equal(document.querySelector('h2')?.innerText?.trim().replace(/\s+/g, ' ').toLowerCase(),'cat photos')
```

Non dovresti aggiungere gli elementi `ul` o `li` dell'esempio.

```js
assert.notExists(document.querySelector('ul'));
assert.notExists(document.querySelector('li'));
```

Non dovresti modificare l'indentazione sulla riga con il tuo elemento `h2`. Il suo tag di apertura dovrebbe iniziare a 6 spazi dall'inizio della riga. Puoi riavviare il passaggio per ripristinare l'indentazione originale.

```js
assert.match(code.toLowerCase(), /<\/h1\>\s*\n\s{6}<h2>/);
```

Il tuo codice dovrebbe avere un commento. Hai rimosso il commento da un passaggio precedente.

```js
assert.match(code, /<!--.*-->/);
```

Il testo del commento dovrebbe essere `TODO: Add link to cat photos`. Non modificare il testo o gli spazi del commento.

```js
assert.match(code, /<!--\s*todo:\s+add\s+link\s+to\s+cat\s+photos\.?\s*-->/i);
```

Non dovresti modificare l'indentazione sulla riga con il tuo elemento commento. Il suo tag di apertura dovrebbe iniziare a 6 spazi dall'inizio della riga. Puoi riavviare il passaggio per ripristinare l'indentazione originale.

```js
assert.match(
  code
    .toLowerCase(),
    /<\/h2>\s*\n\s{6}<!--\s*todo:\s+add\s+link\s+to\s+cat\s+photos\s*-->/
);
```

Il tuo codice dovrebbe avere un elemento `p`. Hai rimosso l'elemento `p` da un passaggio precedente.

```js
assert.exists(document.querySelector('p'));
```

Il testo dell'elemento `p` dovrebbe essere `Everyone loves cute cats online!`. Non modificare il testo, gli spazi o la punteggiatura dell'elemento `p`.

```js
assert.match(
  document
    .querySelector('p')
    ?.innerText?.trim().toLowerCase(),
    /^everyone\s+loves\s+cute\s+cats\s+online!$/
);
```

Il tag di apertura `p` dovrebbe avere un'indentazione che corrisponde a quella degli elementi `h2` e commento. Il suo tag di apertura dovrebbe iniziare a 6 spazi dall'inizio della riga.

```js
assert.match(code.toLowerCase(), /-->\s*\n\s{6}<p>/);
```

# --seed--

## --seed-contents--

```html
<html>
  <body>
    <main>
      <h1>CatPhotoApp</h1>
      <h2>Cat Photos</h2>
      <!-- TODO: Add link to cat photos -->
--fcc-editable-region--
    <p>Everyone loves cute cats online!</p>
--fcc-editable-region--
    </main>
  </body>
</html>
```
