---
id: 67d3018062fe6ba92b7d8299
title: Come funzionano i tipi primitivi in TypeScript?
challengeType: 19
dashedName: how-do-primitive-types-work-in-typescript
---

# --description--

In questa lezione imparerai come lavorare con i tipi primitivi di base e come funzionano le annotazioni di tipo per le variabili.

Per i tipi di dati primitivi `string`, `null`, `undefined`, `number`, `boolean` e `bigint`, TypeScript offre parole chiave di tipo corrispondenti.

Per questa lezione, ci concentreremo sui tipi primitivi più comuni e non entreremo troppo nei dettagli per `bigint`. Il tipo di dato `bigint` è un tipo numerico in JavaScript usato per rappresentare numeri più grandi del tipo intero standard.

Diamo un’occhiata a ciascuno di questi tipi primitivi a partire da `string`. Ecco come puoi tipizzare una stringa in TypeScript:

```ts
let firstName: string = "Angie";
```

A differenza di JavaScript puro, la variabile `firstName` si aspetta un tipo `string`. Quindi se provi a riassegnarla con un valore numerico, otterrai un errore come questo:

```ts
let firstName: string = "Angie";
firstName = 9 // Type 'number' is not assignable to type 'string'.
```

Il prossimo tipo che vedremo è il tipo `number`. Ecco un esempio:

```ts
let age: number = 16;
```

Puoi assegnare interi e numeri decimali al tipo `number`. Ma se provassi a riassegnare un valore di tipo `bigint`, otterresti un errore come questo:

```ts
let age: number = 16;

// Type 'bigint' is not assignable to type 'number'.
age = 123n;
```

Il prossimo tipo che vedremo è il tipo `boolean`. Ecco alcuni esempi:

```ts
let isLoggedIn: boolean = true;
let isAdmin: boolean = false;
```

Gli ultimi due tipi sono i tipi `null` e `undefined`.

Come ricorderai dalle lezioni precedenti di JavaScript, `null` rappresenta un’assenza intenzionale di valore. `undefined` significa che una variabile è stata dichiarata ma non le è stato assegnato un valore.

Vedrai esempi di come lavorare con questi tipi più avanti, quando imparerai i tipi di funzione e i tipi unione.

Ora che hai visto come funzionano le annotazioni di tipo con le variabili, sorge la domanda se sia sempre necessario specificare esplicitamente il tipo delle variabili.

Se hai un’annotazione di tipo semplice come questa, potresti non aver bisogno di usare un tipo esplicito.

```ts
let firstName: string = "Angie";
```

Il motivo è che TypeScript cercherà di inferire automaticamente i tipi in base al valore. Quindi se scrivi questo, TypeScript capirà che si tratta di una stringa.

```js
let firstName = "Angie";
```

Dovresti tipizzare esplicitamente le tue variabili quando TypeScript non può inferire chiaramente il tipo o quando vuoi imporre un tipo specifico in situazioni come i parametri di funzione o oggetti complessi.

# --questions--

## --text--

Qual è il modo corretto per aggiungere esplicitamente un’annotazione di tipo a una variabile di tipo numero?

## --answers--

```ts
let age: number = 16;
```

---

```ts
let age<> number = 16;
```

### --feedback--

Riferisciti all’inizio della lezione per i tipi numerici.

---

```ts
let age>> number = 16;
```

### --feedback--

Riferisciti all’inizio della lezione per i tipi numerici.

---

```ts
let age == number = 16;
```

### --feedback--

Riferisciti all’inizio della lezione per i tipi numerici.

## --video-solution--

1

## --text--

Quale tra i seguenti NON è un tipo di dato primitivo?

## --answers--

`string`

### --feedback--

Riferisciti all’inizio della lezione.

---

`boolean`

### --feedback--

Riferisciti all’inizio della lezione.

---

`object`

---

`number`

### --feedback--

Riferisciti all’inizio della lezione.

## --video-solution--

3

## --text--

Quando dovresti tipizzare esplicitamente le tue variabili in TypeScript?

## --answers--

Quando lavori con tipi `number` e `bigint`.

### --feedback--

Riferisciti alla fine della lezione.

---

Quando lavori con tipi `null` e `undefined`.

### --feedback--

Riferisciti alla fine della lezione.

---

Quando TypeScript non può inferire chiaramente il tipo.

---

Dovresti sempre avere tipi espliciti nel tuo codice.

### --feedback--

Riferisciti alla fine della lezione.

## --video-solution--

3
