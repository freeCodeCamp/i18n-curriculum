---
id: 66edd3711bb9f7efa73aef91
title: JavaScript 날짜 퀴즈
challengeType: 8
dashedName: quiz-javascript-dates
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 맞혀야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript `Date` 객체는 무엇에 사용되나요?

#### --distractors--

날짜만 다룹니다.

---

시간만 다룹니다.

---

윤년만 다룹니다.

#### --answer--

날짜와 시간을 모두 다룹니다.

### --question--

#### --text--

어떤 코드가 새로운 `Date` 객체 인스턴스를 생성하나요?

#### --distractors--

`Date.new()`

---

`Date.fetch()`

---

`new.Date()`

#### --answer--

`new Date()`

### --question--

#### --text--

`Date.now()`는 어떻게 작동하나요?

#### --distractors--

1776년 7월 4일부터 현재까지의 시간을 나노초 단위로 반환합니다.

---

1974년 1월 1일부터 이전 시간을 밀리초 단위로 반환합니다.

---

1970년 1월 1일부터 현재 날짜를 분 단위로 반환합니다.

#### --answer--

1970년 1월 1일부터 현재까지의 시간을 밀리초 단위로 반환합니다.

### --question--

#### --text--

현재 연도를 반환하는 메서드는 무엇인가요?

#### --distractors--

`getNewYear()`

---

`getLastYear()`

---

`getLeapYear()`

#### --answer--

`getFullYear()`

### --question--

#### --text--

0부터 시작하는 정수로 현재 월을 반환하는 메서드는 무엇인가요?

#### --distractors--

`getCalendar()`

---

`fetchMonth()`

---

`get.Month()`

#### --answer--

`getMonth()`

### --question--

#### --text--

날짜를 문자열로 포맷하는 메서드는 무엇인가요?

#### --distractors--

`getMonth()`

---

`formatDate()`

---

`toStingy()`

#### --answer--

`toString()`

### --question--

#### --text--

만약 2022년 6월 12일이라면 `console.log(new Date().getFullYear());`의 결과는 무엇일까요?

#### --distractors--

`"June 12th 2022"`

---

`"2023"`

---

`"06/12/2022"`

#### --answer--

`"2022"`

### --question--

#### --text--

`fr-FR`의 `date.toLocaleDateString("fr-FR")`는 무엇을 나타내나요?

#### --distractors--

프랑스-캐나다 지역 설정입니다.

---

프랑스-이탈리아 지역 설정입니다.

---

프랑스-핀란드 지역 설정입니다.

#### --answer--

프랑스 지역 설정입니다.

### --question--

#### --text--

만약 지역 설정에서 시간이 `HH:MM:SS AM/PM` 형식으로 포맷된다면, 현재 시간을 올바르게 표시하는 코드는 어느 줄인가요?

#### --distractors--

`console.log(new Date().toLocaleDateString());`

---

`console.log(new Date().toString());`

---

`console.log(new Date().toCityDateString());`

#### --answer--

`console.log(new Date().toLocaleTimeString());`

### --question--

#### --text--

`toISOString()` 메서드는 어떤 형식으로 날짜를 반환하나요?

#### --distractors--

USA-863 문자열 형식입니다.

---

MP3 문자열 형식입니다.

---

UTC 문자열 형식입니다.

#### --answer--

ISO-8601 문자열 형식입니다.

### --question--

#### --text--

ISO-8601 날짜 형식은 어떻게 생겼나요?

#### --distractors--

DD-MM-YYYY

---

DD-MM-YYTHH

---

YYYY-MM-DDTHH:mm:ssZ

#### --answer--

YYYY-MM-DDTHH:mm:ss.sssZ

### --question--

#### --text--

`console.log(new Date(2003, 6, 27).getMonth());`에 해당하는 월은 무엇인가요?

#### --distractors--

6월

---

4월

---

1월

#### --answer--

7월

### --question--

#### --text--

날짜를 지역 설정에 맞는 문자열이나 더 읽기 쉬운 형식으로 포맷하려면 어떻게 해야 하나요?

#### --distractors--

`.toLocaleDate()`

---

`.toLocaleString()`

---

`.toLocaleTimeString()`

#### --answer--

`.toLocaleDateString()`

### --question--

#### --text--

`toLocaleDateString()` 메서드에 `locales` 매개변수가 제공되지 않으면 기본적으로 어떤 지역 설정을 사용하나요?

#### --distractors--

영국 영어입니다.

---

미국에 가장 가까운 지역 설정입니다.

---

프랑스어(프랑스)입니다.

#### --answer--

사용자 시스템의 지역 설정입니다.

### --question--

#### --text--

아래 코드에서 `now`에 할당되는 값은 무엇인가요?

```js
const now = new Date();
```

#### --distractors--

1990년 1월 1일부터 현재까지의 시간을 밀리초 단위로 나타낸 값입니다.

---

Unix epoch를 뺀 현재 날짜의 밀리초 값입니다.

---

1990년 1월 1일부터 현재까지의 시간을 나노초 단위로 나타낸 값입니다.

#### --answer--

컴퓨터 시스템 시계를 기준으로 한 현재 날짜와 시간입니다.

### --question--

#### --text--

`console.log(new Date(2003, 6, 27).getFullYear());`의 출력 결과는 무엇인가요?

#### --distractors--

`"27/6/2003"`

---

`27`

---

`6`

#### --answer--

`2003`

### --question--

#### --text--

날짜가 유효하지 않을 때 `getDate`는 무엇을 반환하나요?

#### --distractors--

`0`

---

`null`

---

`undefined`

#### --answer--

`NaN`

### --question--

#### --text--

현재가 10월이라면 `console.log(new Date().getMonth());`는 무엇을 출력하나요?

#### --distractors--

`1`

---

`3`

---

`10`

#### --answer--

`9`

### --question--

#### --text--

다음 객체에 대해 `2021`를 출력하는 옵션은 무엇인가요?

```js
const d = new Date("2021-12-25");
```

#### --distractors--

`console.log(d.getTime())`

---

`console.log(d.toUTCString())`

---

`console.log(d.getDate())`

#### --answer--

`console.log(d.getFullYear())`

### --question--

#### --text--

다음 객체에 대해 `"2021-12-25T00:00:00.000Z"`를 출력하는 옵션은 무엇인가요?

```js
const d = new Date("2021-12-25");
```

#### --distractors--

`console.log(d.getFullYear())`

---

`console.log(d.toUTCString())`

---

`console.log(d.getTime())`

#### --answer--

`console.log(d.toISOString())`
