---
id: 66f1affc0ef4fcca423d4688
title: Bash と SQL のクイズ
challengeType: 8
dashedName: quiz-bash-and-sql
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

SQLは何の略ですか？

#### --distractors--

Standard Query Language.

---

Simple Quantum Language.

---

Sophisticated Query Language.

#### --answer--

Structured Query Language.

### --question--

#### --text--

新しいデータベースを作成するために使うSQLコマンドはどれですか？

#### --distractors--

`BUILD DATABASE`

---

`GENERATE DATABASE`

---

`NEW DATABASE`

#### --answer--

`CREATE DATABASE`

### --question--

#### --text--

第一正規形（1NF）とは何ですか？

#### --distractors--

冗長なデータがないこと。

---

部分的な依存関係がないこと。

---

推移的な依存関係がないこと。

#### --answer--

カラムの値が原子値であること。

### --question--

#### --text--

テーブルからすべてのカラムを選択するにはどうしますか？

#### --distractors--

`SELECT []`

---

`SELECT ALL`

---

`SELECT COLUMNS`

#### --answer--

`SELECT *`

### --question--

#### --text--

`FOREIGN KEY` は何を確立しますか？

#### --distractors--

一意の識別子キー。

---

主キー。

---

NULL制約。

#### --answer--

テーブル間のリレーションシップ。

### --question--

#### --text--

SQLにおける正規化の目的は何ですか？

#### --distractors--

クエリの高速化。

---

ストレージ容量の増加。

---

インデックスの追加。

#### --answer--

データの冗長性を減らすこと。

### --question--

#### --text--

SQLインジェクションに対する最善の防御策は何ですか？

#### --distractors--

すべてのユーザー入力を削除すること。

---

SQLアクセスを無効にすること。

---

データベースアクセスを削除すること。

#### --answer--

プリペアドステートメントを使うこと。

### --question--

#### --text--

既存のテーブルデータを変更するコマンドはどれですか？

#### --distractors--

`MODIFY`

---

`CHANGE`

---

`ALTER`

#### --answer--

`UPDATE`

### --question--

#### --text--

ジャンクションテーブルには通常何を含めますか？

#### --distractors--

外部キーのみ。

---

タイムスタンプのみ。

---

主キーのみ。

#### --answer--

外部キーと追加データ。

### --question--

#### --text--

テーブルに新しいデータを追加するコマンドはどれですか？

#### --distractors--

`ADD`

---

`PUT`

---

`NEW DATA`

#### --answer--

`INSERT`

### --question--

#### --text--

どのキーが重複した値を持つことができますか？

#### --distractors--

主キー。

---

ユニークキー。

---

複合キー。

#### --answer--

外部キー。

### --question--

#### --text--

第三正規形（3NF）は何を排除しますか？

#### --distractors--

多値依存。

---

部分依存。

---

すべての依存関係。

#### --answer--

推移的依存。

### --question--

#### --text--

SQLの結果をフィルターするにはどうしますか？

#### --distractors--

`FILTER`

---

`SORT`

---

`TAGS`

#### --answer--

`WHERE`

### --question--

#### --text--

SQLにおけるN+1問題の主な特徴は何ですか？

#### --distractors--

1つの大きく複雑なクエリを実行してサーバーに返すこと。

---

一度に大量のデータを返し、アルファベット順に並べ替えるクエリ。

---

1つのクエリでJOINを多用し、過剰なデータを返すこと。

#### --answer--

最初に1つのクエリを実行し、その後関連データのためにN回の追加クエリを実行すること。

### --question--

#### --text--

スーパキーと候補キーの主な違いは何ですか？

#### --distractors--

スーパキーは複数の属性やカラムを含められず、候補キーは含められる。

---

候補キーは単一カラムでなければならず複合キーにはできないが、スーパキーは複合キーにできる。

---

スーパキーはデータベース管理者が選び、一意である必要はない。

#### --answer--

スーパキーは一意識別に必須でない追加属性を含められるが、候補キーは含められない。

### --question--

#### --text--

正規化の際、複合キーの一部にのみ依存するデータを何と呼びますか？

#### --distractors--

推移的依存。

---

多値依存。

---

関数的依存。

#### --answer--

部分依存。

### --question--

#### --text--

Bashで`>`演算子の主な目的は何ですか？

#### --distractors--

出力をファイルに追記し、`tmp`にコピーを作成すること。

---

ファイルから入力を読み込み、エンコードすること。

---

2つのファイルを比較して差分を出力すること。

#### --answer--

標準出力をファイルにリダイレクトし、ファイルが存在すれば上書きすること。

### --question--

#### --text--

`wc -l`コマンドは何をしますか？

#### --distractors--

ファイル内の単語数を数える。

---

ファイル内の文字数を数える。

---

カレントディレクトリのすべてのファイルを一覧表示する。

#### --answer--

ファイル内の行数を数える。

### --question--

#### --text--

Bashスクリプトで、渡された最初の引数にどうアクセスしますか？

#### --distractors--

`$ARG1`

---

`$0`

---

`$#`

#### --answer--

`$1`

### --question--

#### --text--

学生とコースの多対多の関係において、ジャンクションテーブルには何が含まれますか？

#### --distractors--

学生IDのみ。

---

コースIDのみ。

---

学生とコースの詳細情報すべて。

#### --answer--

両方のIDと登録日。
