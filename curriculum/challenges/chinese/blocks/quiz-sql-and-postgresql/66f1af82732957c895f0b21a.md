---
id: 66f1af82732957c895f0b21a
title: SQL 和 PostgreSQL 测验
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是关系数据库？

#### --distractors--

一个结构化的数据库，用于识别存储项之间的非关系。

---

一个保存非结构化 `data` 的 `database`。

---

基于平面 `file` 的系统。

#### --answer--

一个结构化的数据库，用于识别和管理表格中数据之间的关系。

### --question--

#### --text--

非关系数据库与关系数据库有何不同？

#### --distractors--

非关系数据库使用表格，而关系数据库不使用。

---

关系数据库可以保存非结构化数据。

---

非关系数据库比关系数据库更慢。

#### --answer--

非关系数据库可以保存非结构化数据。

### --question--

#### --text--

在 PostgreSQL 交互式终端（`psql`）中执行 `\l` 命令会做什么？

#### --distractors--

列出所有架构。

---

列出所有表格。

---

连接到数据库。

#### --answer--

列出所有数据库。

### --question--

#### --text--

以下哪个命令在 SQL 中创建一个新建表格？

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

关系数据库中 `foreign key` 的目的是什么？

#### --distractors--

允许数据重复。

---

唯一标识 `table` 中的一行。

---

为保存大量文本数据。

#### --answer--

在两个表格之间创建关系。

### --question--

#### --text--

哪个命令会为现有的 `table` 添加一列？

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

当表格 A 中的一个记录对应表格 B 中的一个或多个记录时，存在什么类型的关系？

#### --distractors--

一对一 (1-1)

---

多对多 (m-m)

---

多对一(m-1)

#### --answer--

一对多 (1-m)

### --question--

#### --text--

什么是 `query`？

#### --distractors--

一个删除数据库中所有数据的请求。

---

数据库中唯一值的特殊行。

---

数据库中用于唯一值的特殊列。

#### --answer--

对数据库中特定数据的请求。

### --question--

#### --text--

如何在 SQL 中从表格删除一列？

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

SQL 中 `PRIMARY KEY` 约束的作用是什么？

#### --distractors--

将两个 `table` 连接起来。

---

为 `column` 定义默认值。

---

允许该列为空值。

#### --answer--

为了确保该列包含唯一值。

### --question--

#### --text--

在 PostgreSQL 交互式终端（`psql`）中，命令 `\c` 有什么作用？

#### --distractors--

列出当前数据库中的所有表格。

---

显示当前数据库连接详情。

---

退出 `psql` shell。

#### --answer--

连接到指定的数据库。

### --question--

#### --text--

关系数据库中的复合密钥是什么？

#### --distractors--

从另一个密钥派生的密钥。

---

一个基于数据更新频繁变化的密钥。

---

专门用于外键关系的密钥。

#### --answer--

由多个列组成以唯一标识一条记录的密钥。

### --question--

#### --text--

哪个命令将数据插入到表格中？

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

如何在 SQL 中重命名一个列？

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

哪个查询将删除 employees `table` 中的所有 `records`？

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

哪个 SQL 约束专门确保一列只能包含唯一值，同时允许空值？

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

以下哪项不是行业中常用的关系数据库示例？

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQL查询

### --question--

#### --text--

一对一（1-1）关系在关系数据库中表现什么？

#### --distractors--

表格 A 中的每个记录对应表格 B 中的一个或多个记录。

---

表格 A 中的每条记录对应表格 B 中没有记录。

---

表格 A 中的每个记录对应表格 B 中的多个记录，反之亦然。

#### --answer--

表格 A 中的每条记录对应表格 B 中的唯一一条记录。

### --question--

#### --text--

哪个命令会列出当前 PostgreSQL 数据库中的所有表格？

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

以下哪项允许你将一个 `table` 与其自身连接？

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`


