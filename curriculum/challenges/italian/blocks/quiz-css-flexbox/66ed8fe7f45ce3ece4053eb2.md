---
id: 66ed8fe7f45ce3ece4053eb2
title: Quiz su css flexbox
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos’è flexbox?

#### --distractors--

Un modello bidimensionale per il layout.

---

Un modello tridimensionale per il layout.

---

Un modello multidimensionale per il layout.

#### --answer--

Un modello monodimensionale per il layout.

### --question--

#### --text--

Quale proprietà CSS si imposta per abilitare il layout flexbox per l’elemento `div`?

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

Quale proprietà CSS permette agli elementi flex di andare a capo su una nuova riga quando la riga corrente è piena?

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

Cosa controlla la proprietà flexbox `justify-content`?

#### --distractors--

Controlla la direzione degli elementi figli del contenitore flex.

---

Controlla lo stile del testo degli elementi figli del contenitore flex.

---

Controlla l’allineamento degli elementi figli all’interno del contenitore flex.

#### --answer--

Controlla la distribuzione degli elementi figli sull’asse principale.

### --question--

#### --text--

Quale proprietà controlla la direzione degli elementi all’interno del contenitore flex?

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

Quali proprietà CSS si possono usare per far scorrere gli elementi flex da sinistra a destra e far apparire il nuovo elemento flex sopra quello precedente se la larghezza della riga corrente viene superata?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

Quale proprietà controlla il posizionamento degli elementi sull’asse trasversale?

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

Quale non è un valore per `justify-content`?

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

Quali proprietà CSS useresti per centrare un elemento orizzontalmente e verticalmente all’interno di un contenitore flex?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

Quale valore di `flex-direction` fa sì che gli elementi flex vadano da destra a sinistra?  

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

Quale non è un valore della proprietà `flex-wrap`?

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

Cosa fa il valore `stretch` in `align-items`?

#### --distractors--

Gli elementi sono impacchettati direttamente l’uno contro l’altro verso il bordo iniziale del contenitore.

---

Gli elementi sono allineati in modo che le loro linee di base del contenitore flex coincidano.

---

Gli elementi sono impacchettati direttamente l’uno contro l’altro verso il bordo finale del contenitore.

#### --answer--

Gli elementi si ridimensionano automaticamente per essere ugualmente grandi e riempire il contenitore.

### --question--

#### --text--

Quale valore `justify-content` posiziona il primo elemento all’inizio, l’ultimo alla fine e distribuisce uniformemente gli altri elementi tra di loro lungo l’asse principale?

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

Quale valore `justify-content` distribuisce gli elementi in modo che gli spazi tra ogni coppia di elementi adiacenti e i bordi del contenitore siano tutti uguali?

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

Quale valore `justify-content` distribuisce gli elementi in modo uguale lungo l’asse principale lasciando però metà di quello spazio ai bordi del contenitore?

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

Quale delle seguenti opzioni fa sì che gli elementi siano allineati all’inizio dell’asse trasversale?

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

Quale delle seguenti opzioni fa sì che gli elementi siano allineati alla fine dell’asse trasversale?

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

Quale valore per `flex-direction` fa sì che gli elementi siano allineati lungo l’asse principale da sinistra a destra?

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

Quale valore per `flex-direction` fa sì che gli elementi siano allineati lungo l’asse trasversale dall’alto verso il basso?

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

Quale dei seguenti casi d’uso non è adatto per flexbox?

#### --distractors--

Centrare un elemento in un contenitore.

---

Allineamento verticale degli elementi in una riga.

---

Regolare dinamicamente la distribuzione e l’allineamento degli elementi per viewport diversi.

#### --answer--

Dimensionamento fisso e spaziatura tra elementi come in una griglia.

## --quiz--

### --question--

#### --text--

Qual è lo scopo di un modello di layout monodimensionale come Flexbox?

#### --distractors--

Gestire il layout in due dimensioni, sia righe che colonne contemporaneamente.

---

Controllare l’ordine di sovrapposizione degli elementi posizionati usando `z-index`.

---

Creare trasformazioni 3D e animazioni complesse sugli elementi della pagina.

#### --answer--

Disporre gli elementi lungo una singola linea o asse, sia come riga sia come colonna.

### --question--

#### --text--

Quale proprietà CSS allinea tutti gli elementi flex sul lato destro dell’elemento `.container`?

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Quali sono i due assi che definiscono la direzione del layout degli elementi nel modello Flex?

#### --distractors--

L’asse orizzontale e l’asse verticale.

---

L’asse x e l’asse y.

---

L’asse primario e l’asse secondario.

#### --answer--

L’asse principale e l’asse trasversale.

### --question--

#### --text--

Quale delle seguenti regole CSS dispone gli elementi in una colonna verticale e li centra orizzontalmente all’interno del loro contenitore?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

Qual è lo scopo principale della proprietà `flex-wrap`?

#### --distractors--

Imposta la direzione in cui gli elementi flex sono posizionati nel contenitore flex.

---

Allinea gli elementi flex lungo l’asse principale del contenitore.

---

Allinea gli elementi flex lungo l’asse trasversale del contenitore.

#### --answer--

Determina se gli elementi flex devono andare a capo su nuove righe per adattarsi allo spazio disponibile.

### --question--

#### --text--

Qual è il comportamento predefinito della dichiarazione `flex-wrap: nowrap;`?

#### --distractors--

Costringe gli elementi flex ad andare a capo su una nuova riga se sono troppo larghi per il contenitore.

---

Fa andare gli elementi a capo su una nuova riga, ma in ordine inverso.

---

Impedisce che il contenitore flex sia più grande del suo elemento genitore.

#### --answer--

Costringe gli elementi flex a rimanere su una singola riga, anche se escono dal contenitore.

### --question--

#### --text--

Quale proprietà CSS è una scorciatoia per impostare contemporaneamente le proprietà `flex-direction` e `flex-wrap`?

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

Qual è l’effetto di impostare `flex-direction: column-reverse;` su un contenitore flex?

#### --distractors--

Dispone gli elementi orizzontalmente da destra a sinistra.

---

Inverte gli elementi in una riga, ma li mantiene su una singola linea.

---

Dispone gli elementi verticalmente dall’alto verso il basso.

#### --answer--

Dispone gli elementi verticalmente in ordine inverso dal basso verso l’alto.

### --question--

#### --text--

Quale frammento CSS dispone correttamente gli elementi flex in una colonna verticale e permette loro di andare a capo su una nuova colonna se superano l’altezza del contenitore?

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

Quale valore `justify-content` allinea tutti gli elementi flex alla fine dell’asse principale del contenitore?

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Qual è l’effetto della seguente regola CSS sugli elementi flex?

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

Gli elementi sono disposti in colonna e andranno a capo su una nuova colonna.

---

Gli elementi sono disposti in riga e andranno a capo su una nuova riga.

---

Gli elementi sono disposti in colonna e usciranno dal contenitore se non ci stanno.

#### --answer--

Gli elementi sono disposti in riga e usciranno dal contenitore se non ci stanno.

### --question--

#### --text--

Come allinea gli elementi flex `justify-content: space-evenly;`?

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

Gli elementi sono distribuiti uniformemente, senza spazio all’inizio o alla fine.

---

Gli elementi sono raggruppati al centro del contenitore.

---

Gli elementi sono spinti sul lato destro del contenitore, con spazi uguali tra di loro.

#### --answer--

Gli elementi hanno spazi uguali tra di loro e ad entrambe le estremità del contenitore.

### --question--

#### --text--

Quale proprietà useresti per centrare verticalmente gli elementi quando `flex-direction: row;` è impostato?

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

Cosa fa il valore `flex-start` per la proprietà `justify-content`?

#### --distractors--

Allinea gli elementi all’inizio dell’asse trasversale.

---

Allunga gli elementi per riempire l’asse principale del contenitore.

---

Centra tutti gli elementi lungo l’asse principale.

#### --answer--

Allinea gli elementi all’inizio dell’asse principale del contenitore.

### --question--

#### --text--

Come imposteresti un contenitore flex per disporre i suoi elementi da destra a sinistra su una singola riga e posizionarli anche in cima al contenitore?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

Quale delle seguenti è una dichiarazione valida per la proprietà `flex-flow`?

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

Quale delle seguenti regole CSS fa sì che gli elementi flex siano centrati orizzontalmente e allungati verticalmente per riempire l’altezza del contenitore?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

Che effetto ha `align-items: stretch;` quando `flex-direction` è impostato su `column`?

#### --distractors--

Allunga gli elementi verticalmente per riempire l’altezza del contenitore.

---

Non ha effetto quando la direzione è `column`.

---

Allunga gli elementi verticalmente per corrispondere all’elemento più alto.

#### --answer--

Allunga gli elementi orizzontalmente per riempire la larghezza del contenitore.

### --question--

#### --text--

Se vuoi invertire l’ordine degli elementi in una riga orizzontale, quale dichiarazione dovresti usare?

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

Quale valore di `align-items` posizionerà gli elementi in cima a un contenitore il cui `flex-direction` è `row`?

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
