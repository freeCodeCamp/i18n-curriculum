---
id: 66f1af82732957c895f0b21a
title: SQL과 PostgreSQL 퀴즈
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

관계형 데이터베이스란 무엇인가요?

#### --distractors--

저장된 항목 간의 비관계를 인식하도록 구조화된 데이터베이스입니다.

---

비구조화된 데이터를 저장하는 데이터베이스입니다.

---

평면 파일 기반 시스템입니다.

#### --answer--

표에 있는 데이터 간의 관계를 인식하고 관리하도록 구조화된 데이터베이스입니다.

### --question--

#### --text--

비관계형 데이터베이스는 관계형 데이터베이스와 어떻게 다른가요?

#### --distractors--

비관계형 데이터베이스는 표를 사용하지만 관계형 데이터베이스는 사용하지 않습니다.

---

관계형 데이터베이스는 비구조화된 데이터를 저장할 수 있습니다.

---

비관계형 데이터베이스가 관계형 데이터베이스보다 느립니다.

#### --answer--

비관계형 데이터베이스는 비구조화된 데이터를 저장할 수 있습니다.

### --question--

#### --text--

PostgreSQL 대화형 터미널(`\l`)에서 `psql` 명령을 실행하면 무엇을 하나요?

#### --distractors--

모든 스키마를 나열합니다.

---

모든 표를 나열합니다.

---

데이터베이스에 연결합니다.

#### --answer--

모든 데이터베이스를 나열합니다.

### --question--

#### --text--

다음 중 SQL에서 새 표를 생성하는 명령은 무엇인가요?

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

관계형 데이터베이스에서 외래 키의 목적은 무엇인가요?

#### --distractors--

데이터 중복을 허용하기 위해서입니다.

---

표의 행을 고유하게 식별하기 위해서입니다.

---

대량의 텍스트 데이터를 저장하기 위해서입니다.

#### --answer--

두 표 간의 관계를 생성하기 위해서입니다.

### --question--

#### --text--

기존 표에 새 열을 추가하는 명령은 무엇인가요?

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

표 A의 한 레코드가 표 B의 하나 이상의 레코드에 대응할 때 존재하는 관계 유형은 무엇인가요?

#### --distractors--

일대일 (1-1)

---

다대다 (m-m)

---

다대일 (m-1)

#### --answer--

일대다 (1-m)

### --question--

#### --text--

쿼리란 무엇인가요?

#### --distractors--

데이터베이스에서 모든 데이터를 삭제하는 요청입니다.

---

데이터베이스에서 고유 값을 위한 특별한 행입니다.

---

데이터베이스에서 고유 값을 위한 특별한 열입니다.

#### --answer--

데이터베이스에서 특정 데이터를 요청하는 것입니다.

### --question--

#### --text--

SQL에서 표에서 열을 삭제하려면 어떻게 하나요?

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

SQL에서 `PRIMARY KEY` 제약 조건의 기능은 무엇인가요?

#### --distractors--

두 표를 연결하기 위해서입니다.

---

열의 기본값을 정의하기 위해서입니다.

---

열에 null 값을 허용하기 위해서입니다.

#### --answer--

열에 고유한 값만 포함되도록 보장하기 위해서입니다.

### --question--

#### --text--

PostgreSQL 대화형 터미널(`\c`)에서 `psql` 명령은 무엇을 하나요?

#### --distractors--

현재 데이터베이스의 모든 표를 나열합니다.

---

현재 데이터베이스 연결 세부 정보를 표시합니다.

---

`psql` 셸을 종료합니다.

#### --answer--

지정된 데이터베이스에 연결합니다.

### --question--

#### --text--

관계형 데이터베이스에서 복합 키란 무엇인가요?

#### --distractors--

다른 키에서 파생된 키입니다.

---

데이터 업데이트에 따라 자주 변경되는 키입니다.

---

외래 키 관계에만 배타적으로 사용되는 키입니다.

#### --answer--

여러 열로 구성되어 레코드를 고유하게 식별하는 키입니다.

### --question--

#### --text--

데이터를 표에 삽입하는 명령은 무엇인가요?

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

SQL에서 열 이름을 바꾸려면 어떻게 하나요?

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

employees 표에서 모든 레코드를 삭제하는 쿼리는 무엇인가요?

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

열에 고유한 값만 포함하도록 하면서 null 값도 허용하는 SQL 제약 조건은 무엇인가요?

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

다음 중 업계에서 일반적으로 사용되지 않는 관계형 데이터베이스는 무엇인가요?

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQLQuery

### --question--

#### --text--

관계형 데이터베이스에서 일대일(1-1) 관계는 무엇을 나타내나요?

#### --distractors--

표 A의 각 레코드는 표 B의 하나 이상의 레코드에 대응합니다.

---

표 A의 각 레코드는 표 B의 어떤 레코드에도 대응하지 않습니다.

---

표 A의 각 레코드는 표 B의 여러 레코드에 대응하며 그 반대도 마찬가지입니다.

#### --answer--

표 A의 각 레코드는 표 B의 정확히 한 레코드에 대응합니다.

### --question--

#### --text--

현재 PostgreSQL 데이터베이스의 모든 표를 나열하는 명령은 무엇인가요?

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

다음 중 표를 자기 자신과 조인할 수 있게 하는 것은 무엇인가요?

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`
