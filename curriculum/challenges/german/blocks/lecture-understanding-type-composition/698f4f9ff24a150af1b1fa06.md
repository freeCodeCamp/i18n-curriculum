---
id: 698f4f9ff24a150af1b1fa06
title: "Wie funktionieren die Typen any, never, unknown und void?"
challengeType: 19
dashedName: how-do-the-any-never-unknown-and-void-types-work
---

# --description--

In vorherigen Lektionen haben Sie gelernt, wie man mit primitiven Typen sowie Union-Typen und Interfaces arbeitet.

TypeScript hat jedoch einige spezielle Typen, die Sie kennen sollten. Beginnen wir mit dem `any`-Typ.

Der `any`-Typ wird verwendet, um jeden beliebigen Werttyp darzustellen.

```ts
let randomValue: any;

randomValue = 42;           

randomValue = "Hello";     
 
randomValue = true;        

randomValue = { name: "Alice" }; 
```

Bei diesem Typ müssen Sie vorsichtig sein, da er leicht übermäßig verwendet und missbraucht werden kann. Sie wollen nicht alles mit dem `any`-Typ typisieren, nur um TypeScript-Fehlermeldungen zu unterdrücken. Das würde den Zweck von TypeScript und der Hinzufügung von Typensicherheit zunichtemachen.

Ein sichereres Gegenstück zum `any`-Typ wäre der `unknown`-Typ. `unknown` ist ähnlich wie `any`, aber beim `unknown`-Typ müssen Sie vor der Verwendung eine Typprüfung für die Variable durchführen.

Hier ist ein Beispiel:

```ts
function doubleValue(value: unknown) {
  if (typeof value === "number") {
    console.log(value * 2);
  } else if (typeof value === "string") {
    console.log(value + value);
  }
}

doubleValue(10);    
doubleValue("Hi "); 
doubleValue(true);   
```

In diesem Beispiel stellen die Typprüfungen sicher, dass Operationen nur auf Werten des korrekten Typs ausgeführt werden. Werte, die nicht den erwarteten Typen entsprechen, werden ignoriert.

Der nächste Typ, den wir betrachten, ist der `never`-Typ. Dieser Typ stellt etwas dar, das niemals eintreten wird.

In den meisten Situationen werden Sie keine Typannotationen mit `never` schreiben. Stattdessen werden Sie höchstwahrscheinlich den `never`-Typ in Fehlermeldungen wie dieser sehen:

```ts
function processValue(value: string | number) {
  if (typeof value === "string") {
    console.log("String value:", value.toUpperCase());
  } else if (typeof value === "number") {
    console.log("Number value:", value * 2);
  } else {
    console.log(value); 
  }
}
```

Innerhalb der `else`-Klausel des obigen Beispiels hat der `value` den `never`-Typ. Das liegt daran, dass der `else`-Zweig jetzt unmöglich zu erreichen ist und kein verbleibender Typ mehr übrig ist. Wenn Sie versuchen würden, die Funktion mit einem Wert außer `string` oder `number` aufzurufen, würde eine Fehlermeldung wie diese angezeigt:

```ts
function processValue(value: string | number) {
  if (typeof value === "string") {
    console.log("String value:", value.toUpperCase());
  } else if (typeof value === "number") {
    console.log("Number value:", value * 2);
  } else {
    console.log(value); 
  }
}

// Argument of type 'boolean' is not assignable to parameter of type 'string | number'.
processValue(true); 
```

Der letzte Typ, den wir betrachten, ist der `void`-Typ. Wenn Sie eine Funktion haben, die keinen Wert zurückgibt, wäre das ein `void`-Typ.

Hier ist ein Beispiel:

```ts
type Status = "loading" | "success" | "error";

type Handler = {
  status: Status;
  onChange: (newStatus: Status) => void; 
};
```

In diesem Beispiel gibt die `onChange`-Eigenschaft für den `Handler`-Typ nichts zurück, weshalb `void` verwendet wird.

# --questions--

## --text--

Welcher TypeScript-Typ erlaubt es Ihnen, jeden beliebigen Wert zu speichern, sollte aber vorsichtig verwendet werden, um den Verlust der Typensicherheit zu vermeiden?

## --answers--

`void`

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

---

`any`

---

`unknown`

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

---

`null`

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

## --video-solution--

2

## --text--

Was ist der Zweck des `never`-Typs in TypeScript?

## --answers--

Er stellt einen Wert dar, der jeden Typ haben kann.

### --feedback--

Sehen Sie sich die Mitte der Lektion noch einmal an, wo der `never`-Typ erklärt wurde.

---

Er stellt einen optionalen Rückgabetyp einer Funktion dar.

### --feedback--

Sehen Sie sich die Mitte der Lektion noch einmal an, wo der `never`-Typ erklärt wurde.

---

Er stellt etwas dar, das niemals eintreten wird.

---

Er stellt eine Variable dar, die jeden Typ enthalten kann.

### --feedback--

Sehen Sie sich die Mitte der Lektion noch einmal an, wo der `never`-Typ erklärt wurde.

## --video-solution--

3

## --text--

Welchen Typ würden Sie für eine Funktion verwenden, die keinen Wert zurückgibt?

## --answers--

`null`

### --feedback--

Sehen Sie sich das Ende der Lektion noch einmal an.

---

`void`

---

`any`

### --feedback--

Sehen Sie sich das Ende der Lektion noch einmal an.

---

`unknown`

### --feedback--

Sehen Sie sich das Ende der Lektion noch einmal an.

## --video-solution--

2
