---
id: 66edd3711bb9f7efa73aef91
title: JavaScriptの日付に関するクイズ
challengeType: 8
dashedName: quiz-javascript-dates
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScriptの`Date`オブジェクトは何に使われますか？

#### --distractors--

日付だけを扱う。

---

時間だけを扱う。

---

うるう年だけを扱う。

#### --answer--

日付と時間の両方を扱う。

### --question--

#### --text--

どのコードが新しい`Date`オブジェクトのインスタンスを作成しますか？

#### --distractors--

`Date.new()`

---

`Date.fetch()`

---

`new.Date()`

#### --answer--

`new Date()`

### --question--

#### --text--

`Date.now()`はどのように動作しますか？

#### --distractors--

1776年7月4日からのナノ秒単位の現在時刻を返す。

---

1974年1月1日からのミリ秒単位の前の時刻を返す。

---

1970年1月1日からの分単位の現在日付を返す。

#### --answer--

1970年1月1日からのミリ秒単位の現在時刻を返す。

### --question--

#### --text--

現在の年を返すメソッドはどれですか？

#### --distractors--

`getNewYear()`

---

`getLastYear()`

---

`getLeapYear()`

#### --answer--

`getFullYear()`

### --question--

#### --text--

現在の月をゼロから始まる整数で返すメソッドはどれですか？

#### --distractors--

`getCalendar()`

---

`fetchMonth()`

---

`get.Month()`

#### --answer--

`getMonth()`

### --question--

#### --text--

日付を文字列としてフォーマットするメソッドはどれですか？

#### --distractors--

`getMonth()`

---

`formatDate()`

---

`toStingy()`

#### --answer--

`toString()`

### --question--

#### --text--

2022年6月12日の場合、`console.log(new Date().getFullYear());`の結果はどうなりますか？

#### --distractors--

`"June 12th 2022"`

---

`"2023"`

---

`"06/12/2022"`

#### --answer--

`"2022"`

### --question--

#### --text--

`fr-FR`の`date.toLocaleDateString("fr-FR")`は何を表していますか？

#### --distractors--

フランス系カナダのロケール。

---

フランス系イタリアのロケール。

---

フランス系フィンランドのロケール。

#### --answer--

フランスのロケール。

### --question--

#### --text--

ロケールの時間が`HH:MM:SS AM/PM`の形式で表示される場合、現在時刻を正しく表示するコードはどれですか？

#### --distractors--

`console.log(new Date().toLocaleDateString());`

---

`console.log(new Date().toString());`

---

`console.log(new Date().toCityDateString());`

#### --answer--

`console.log(new Date().toLocaleTimeString());`

### --question--

#### --text--

`toISOString()`メソッドはどの形式で日付を返しますか？

#### --distractors--

USA-863の文字列形式。

---

MP3の文字列形式。

---

UTCの文字列形式。

#### --answer--

ISO-8601の文字列形式。

### --question--

#### --text--

ISO-8601の日付形式はどのような形ですか？

#### --distractors--

DD-MM-YYYY

---

DD-MM-YYTHH

---

YYYY-MM-DDTHH:mm:ssZ

#### --answer--

YYYY-MM-DDTHH:mm:ss.sssZ

### --question--

#### --text--

`console.log(new Date(2003, 6, 27).getMonth());`に対応する月は何ですか？

#### --distractors--

6月

---

4月

---

1月

#### --answer--

7月

### --question--

#### --text--

日付をロケール固有の文字列や読みやすい形式にフォーマットするにはどうしますか？

#### --distractors--

`.toLocaleDate()`

---

`.toLocaleString()`

---

`.toLocaleTimeString()`

#### --answer--

`.toLocaleDateString()`

### --question--

#### --text--

`toLocaleDateString()`パラメータが指定されていない場合、`locales`メソッドが使うデフォルトのロケールは何ですか？

#### --distractors--

イギリス英語。

---

アメリカに最も近いロケール。

---

フランス語（フランス）。

#### --answer--

ユーザーのシステムロケール。

### --question--

#### --text--

以下のコードで`now`に代入されるのは何ですか？

```js
const now = new Date();
```

#### --distractors--

1990年1月1日からのミリ秒単位の現在時刻。

---

Unixエポックからのミリ秒単位の現在日付。

---

1990年1月1日からのナノ秒単位の現在時刻。

#### --answer--

コンピューターのシステム時計に基づく現在の日付と時刻。

### --question--

#### --text--

`console.log(new Date(2003, 6, 27).getFullYear());`の出力は何ですか？

#### --distractors--

`"27/6/2003"`

---

`27`

---

`6`

#### --answer--

`2003`

### --question--

#### --text--

日付が無効な場合、`getDate`は何を返しますか？

#### --distractors--

`0`

---

`null`

---

`undefined`

#### --answer--

`NaN`

### --question--

#### --text--

10月の場合、`console.log(new Date().getMonth());`は何を出力しますか？

#### --distractors--

`1`

---

`3`

---

`10`

#### --answer--

`9`

### --question--

#### --text--

次のオブジェクトに対して、`2021`を出力する選択肢はどれですか？

```js
const d = new Date("2021-12-25");
```

#### --distractors--

`console.log(d.getTime())`

---

`console.log(d.toUTCString())`

---

`console.log(d.getDate())`

#### --answer--

`console.log(d.getFullYear())`

### --question--

#### --text--

次のオブジェクトに対して、`"2021-12-25T00:00:00.000Z"`を出力する選択肢はどれですか？

```js
const d = new Date("2021-12-25");
```

#### --distractors--

`console.log(d.getFullYear())`

---

`console.log(d.toUTCString())`

---

`console.log(d.getTime())`

#### --answer--

`console.log(d.toISOString())`
