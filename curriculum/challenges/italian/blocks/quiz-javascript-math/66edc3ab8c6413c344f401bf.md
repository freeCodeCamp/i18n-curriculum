---
id: 66edc3ab8c6413c344f401bf
title: Quiz di matematica in JavaScript
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cosa rappresenta `NaN`?

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

Quale dei seguenti NON è un operatore aritmetico in JavaScript?

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

Cosa succede se provi a dividere per zero in JavaScript?

#### --distractors--

JavaScript mostrerà 0.

---

Non succederà nulla.

---

JavaScript lancerà un errore.

#### --answer--

JavaScript mostrerà `Infinity`

### --question--

#### --text--

Qual è l'uso corretto dell'operatore di esponenziazione?

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

Quale operatore dovresti usare per decrementare una variabile in JavaScript?

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

Quale dei seguenti NON è un operatore bit a bit?

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

Qual è l'operatore composto corretto per ottenere il resto?

#### --distractors--

`//=`

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

Qual è la differenza tra gli operatori `==` e `===`?

#### --distractors--

Non c'è differenza. Il risultato del confronto sarà sempre lo stesso.

---

L'operatore `==` serve per verificare se il tipo di dato è lo stesso mentre `===` serve per verificare se il valore è uguale.

---

L'operatore `==` può essere usato solo con tipi primitivi mentre l'operatore `===` è usato solo per oggetti.

#### --answer--

L'operatore `==` confronta solo i valori mentre l'operatore `===` confronta valori e tipi.

### --question--

#### --text--

A cosa serve l'operatore unario più (`+`)?

#### --distractors--

Serve per decrementare un valore di uno.

---

Serve per ottenere il resto di un'operazione.

---

Serve per invertire la rappresentazione binaria di un numero.

#### --answer--

Serve per convertire il suo operando in un numero.

### --question--

#### --text--

Quale dei seguenti metodi `Math` arrotonda un valore all'intero più vicino?

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

Cosa fa il metodo `Math.sqrt()`?

#### --distractors--

Questo metodo restituisce un numero casuale in virgola mobile.

---

Questo metodo restituisce il valore assoluto di un numero.

---

Questo metodo restituisce il cubo di un numero.

#### --answer--

Questo metodo restituisce la radice quadrata di un numero.

### --question--

#### --text--

Quale dei seguenti NON è un metodo valido per l'oggetto `Math`?

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

Quale dei seguenti restituirà `false`?

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

Cosa fa il metodo `parseFloat()`?

#### --distractors--

Analizza un argomento stringa ed esegue un'operazione bit a bit su di esso.

---

Analizza un argomento stringa e restituisce `false`.

---

Analizza un argomento stringa e restituisce un intero.

#### --answer--

Analizza un argomento stringa e restituisce un numero in virgola mobile.

### --question--

#### --text--

Cosa verrà mostrato nella console?

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

Quale sarà l'output del seguente codice?

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

Cos'è la precedenza degli operatori?

#### --distractors--

La precedenza degli operatori serve per verificare se il valore a sinistra è minore di quello a destra.

---

La precedenza degli operatori serve per elevare una variabile alla potenza specificata e riassegnare il risultato alla variabile.

---

La precedenza degli operatori serve per aumentare il valore di uno.

#### --answer--

La precedenza degli operatori determina l'ordine in cui le operazioni vengono valutate in un'espressione.

### --question--

#### --text--

Quale dei seguenti NON è un operatore di confronto?

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

Cosa fa l'operatore unario di negazione (`-`)?

#### --distractors--

Inverte il valore booleano del suo operando.

---

Decrementa il valore di uno.

---

Incrementa il valore di uno.

#### --answer--

Nega l'operando.

### --question--

#### --text--

Quale dei seguenti è un esempio di utilizzo dell'operatore di coalescenza nullish?

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```
