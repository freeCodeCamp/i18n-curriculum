---
id: 67d2ff4231359d9f21d1437a
title: 什么是端到端测试？
challengeType: 19
dashedName: what-is-end-to-end-testing
---

# --description--

端到端测试，简称 E2E，从用户的角度测试真实场景。端到端测试场景的示例包括测试注册和登录进程或测试电子商务网站的结账。

为了更好地理解端到端测试的工作原理，让我们来看一个来自 freeCodeCamp 代码库的真实示例。该示例使用 Playwright，这是微软开发的一个流行的端到端测试框架。

在 freeCodeCamp 代码库中，有一个名为 `e2e` 的目录，里面包含捐赠、认证、考试等的测试。在该目录中，有一个 `donate-page-donor.spec.ts` 文件，其中包含经过身份验证的捐赠者支持者页面的测试。

第一步是以已经是支持者的演示用户身份导航到捐赠页面：

```js
test.beforeEach(async ({ page }) => {
  execSync("node ./tools/scripts/seed/seed-demo-user --set-true isDonating");
  await page.goto("/donate");
});
```

由于此 `file` 中有多个测试，`beforeEach` 钩子将在每个测试之前运行。

一旦用户进入捐赠页面，他们将看到一条感谢消息，并附有如何进一步支持 freeCodeCamp 的建议。以下是一些用于查看捐赠页面上部分文本的测试示例：

```js
test("should render the donate page correctly", async ({ page }) => {
  await expect(
    page.getByText("Thank you for your continued support")
  ).toBeVisible();

  await expect(
    page.getByText(
      "Your contributions are crucial in creating resources that empower millions of people to learn new skills and support their families."
    )
  ).toBeVisible();

  ...
});
```

还有测试用来查看捐赠者在菜单栏中是否有支持者链接，以及他们的头像周围是否有特殊样式的边框以表示他们是支持者：

```js
test("The menu should have a supporters link", async ({ page }) => {
  const menuButton = page.getByTestId("header-menu-button");
  const menu = page.getByTestId("header-menu");

  await expect(menuButton).toBeVisible();
  await menuButton.click();

  await expect(menu).toBeVisible();

  await expect(page.getByRole("link", { name: "Supporters" })).toBeVisible();
});

test("The Avatar should have a special border for donors", async ({ page }) => {
  const container = page.locator(".avatar-container");
  await expect(container).toHaveClass("avatar-container gold-border");
});
```

通过进行这些类型的详细测试，你可以将你的应用的测试覆盖率提升到不仅仅是单元测试的水平。端到端测试还有助于确保你的应用行为正确，并且对用户来说是可预测的。其他端到端测试工具的示例包括 Cypress、Selenium 和 Puppeteer。

虽然端到端测试很重要，但由于设置、设计和维护耗时，因此成本较高。端到端测试非常适合关键用户流程，而单元测试非常适合测试应用中的小单元。

# --questions--

## --text--

以下哪项不是常用的 E2E 测试工具示例？

## --answers--

Cypress

### --feedback--

参考课程结尾处对此的讨论。

---

Selenium

### --feedback--

参考课程结尾处对此的讨论。

---

敏捷

---

Puppeteer

### --feedback--

参考课程结尾处对此的讨论。

## --video-solution--

3

## --text--

以下哪一个是微软开发的流行端到端测试框架？

## --answers--

Playwright

---

Java

### --feedback--

参考课程开始时讨论的内容。

---

角度

### --feedback--

参考课程开始时讨论的内容。

---

JUnit

### --feedback--

参考课程开始时讨论的内容。

## --video-solution--

1

## --text--

以下哪项是端到端测试中常用的测试场景？

## --answers--

测试 CSS 代码的有效性。

### --feedback--

参考课程开始时讨论的内容。

---

测试一个应用的性能。

### --feedback--

参考课程开始时讨论的内容。

---

测试 `HTML` `code` 的有效性。

### --feedback--

参考课程开始时讨论的内容。

---

测试用户登录进程。

## --video-solution--

4
