---
id: 66edcc779993c0da6906dbb9
title: اختبار دوال JavaScript
challengeType: 8
dashedName: quiz-javascript-functions
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي من الطرق التالية هي الطريقة الصحيحة لإعلان دالة في JavaScript؟

#### --distractors--

```js
func greet() {
  console.log("Hello there!");
}
```

---

```js
defineFunction greet() {
  console.log("Hello there!");
}
```

---

```js
def greet() {
  console.log("Hello there!");
}
```

#### --answer--

```js
function greet() {
  console.log("Hello there!");
}
```

### --question--

#### --text--

ماذا يحدث عند استدعاء (أو تنفيذ) دالة؟

#### --distractors--

ستُنفذ الشفرة داخل الدالة وسيُعاد دائمًا `null`.

---

لن يحدث شيء عند استدعاء دالة.

---

سيتم رمي `TypeError` وسيتعطل البرنامج.

#### --answer--

ستُنفذ الشفرة داخل الدالة وسيُعاد قيمة.

### --question--

#### --text--

أي من الطرق التالية هي الطريقة الصحيحة لاستدعاء دالة؟

#### --distractors--

```js
exampleFunction;
```

---

```js
exampleFunction>>;
```

---

```js
()exampleFunction();
```

#### --answer--

```js
exampleFunction();
```

### --question--

#### --text--

ما هو دور الكلمة المفتاحية `return`؟

#### --distractors--

الكلمة المفتاحية `return` ستنفذ الدالة.

---

الكلمة المفتاحية `return` ستُرمي خطأ نوع.

---

الكلمة المفتاحية `return` لا تؤدي أي دور في الدوال.

#### --answer--

الكلمة المفتاحية `return` تُعيد قيمة من الدالة.

### --question--

#### --text--

أي من التعبيرات التالية هو تعبير دالة صالح؟

#### --distractors--

```js
expression function getSum(x, y) {
    return x + y;
};
```

---

```js
getSum: function(x, y) {
    return x + y;
};
```

---

```js
function = getSum(x, y) {
    return x + y;
};
```

#### --answer--

```js
const getSum = function(x, y) {
    return x + y;
};
```

### --question--

#### --text--

ما هي معلمات الدالة؟

#### --distractors--

قيم تمثل غياب القيمة.

---

قيم خاصة تعمل بشكل مشابه للدوال المجهولة.

---

قيم عنصر نائب تُستخدم داخل الدالة.

#### --answer--

قيم حقيقية تُمرر إلى الدالة عند استدعائها.

### --question--

#### --text--

ما هي معلمات الدالة؟

#### --distractors--

قيم فاصلة عائمة تُستخدم داخل الدالة.

---

قيم تُمرر إلى الدالة عند استدعائها.

---

قيم تمثل الغياب المقصود للقيمة.

#### --answer--

قيم مدرجة داخل تعريف الدالة.

### --question--

#### --text--

ماذا سيُسجل في وحدة التحكم؟

```js
const calculateTotal = (amount, taxRate = 0.05) => {
  return amount + (amount * taxRate);
};

console.log(calculateTotal(100));
```

#### --distractors--

`10`

---

`undefined`

---

`1`

#### --answer--

`105`

### --question--

#### --text--

أي من الصيغ التالية هي الصيغة الصحيحة للدالة السهمية؟

#### --distractors--

```js
const sum <<>> (num1, num2) => num1 + num2
```

---

```js
const sum === (num1, num2) === num1 + num2
```

---

```js
const sum >=> (num1, num2) => num1 + num2
```

#### --answer--

```js
const sum = (num1, num2) => num1 + num2
```

### --question--

#### --text--

ما هي النتيجة للكود التالي؟

```js
function greet() {
  const developer = "Jessica";
  console.log("Hello there!");
}

console.log(developer);
```

#### --distractors--

لن يحدث شيء وسيعمل الكود بشكل طبيعي.

---

سيُسجل في وحدة التحكم السلسلة النصية `"Hello there!"`.

---

سيُسجل في وحدة التحكم السلسلة النصية `"Hello!"`.

#### --answer--

ستحصل على خطأ مرجعي لأن `developer` غير معرف عالميًا.

### --question--

#### --text--

متى يمكنك حذف مجموعة الأقواس العادية حول قائمة المعلمات في الدالة السهمية؟

#### --distractors--

يجب دائمًا حذف الأقواس العادية حول قائمة المعلمات.

---

لا يُفترض أبدًا حذف الأقواس العادية للدالة السهمية.

---

يجب حذف الأقواس فقط عند العمل مع معلمات متعددة.

#### --answer--

يجب حذف الأقواس فقط عندما يكون لديك معلمة واحدة فقط.

### --question--

#### --text--

متى يمكنك حذف الأقواس المتعرجة والكلمة المفتاحية `return` في الدالة السهمية؟

#### --distractors--

يجب دائمًا حذف الأقواس المتعرجة والكلمة المفتاحية `return`.

---

يجب ألا تحذف الأقواس المتعرجة والكلمة المفتاحية `return` أبدًا.

---

عندما يحتوي جسم الدالة على عدة أسطر من الشفرة.

#### --answer--

عندما يتكون جسم الدالة من تعبير واحد فقط.

### --question--

#### --text--

ماذا سترجع الدالة التالية؟

```js
function exampleFunction() {
  return "Hello";
  return "World!";
};
```

#### --distractors--

`"World!"`

---

`"Hello World!"`

---

`undefined`

#### --answer--

`"Hello"`

### --question--

#### --text--

أي من الطرق التالية هي الطريقة الصحيحة لاستخدام المعلمات الافتراضية؟

#### --distractors--

```js
function sum(num1, num2 <<>> 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 === 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 >> 12) {
  return num1 + num2
}
```

#### --answer--

```js
function sum(num1, num2 = 12) {
  return num1 + num2
}
```

### --question--

#### --text--

ما هي النتيجة للكود التالي؟

```js
const developer = "Jessica";

function greet() {
  console.log("Hello, " + developer)
}

greet();
```

#### --distractors--

سيُسجل في وحدة التحكم السلسلة النصية `"Hello, developer"`.

---

ستظهر رسالة خطأ في وحدة التحكم.

---

لن يظهر شيء في وحدة التحكم.

#### --answer--

سيُسجل في وحدة التحكم السلسلة النصية `"Hello, Jessica"`.

### --question--

#### --text--

ماذا يحدث إذا حاولت حذف الأقواس المتعرجة لدالة عادية؟

#### --distractors--

ستظهر نافذة تنبيه على الصفحة مع رسالة خطأ.

---

سيتعطل البرنامج ولن يبدأ مجددًا.

---

لن يحدث شيء.

#### --answer--

ستحصل على خطأ نحوي.

### --question--

#### --text--

ما هي النتيجة للكود التالي؟

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + 10);
```

#### --distractors--

ستظهر رسالة خطأ في وحدة التحكم.

---

سيُسجل في وحدة التحكم الرقم `0`.

---

لن يُسجل شيء في وحدة التحكم.

#### --answer--

سيُسجل في وحدة التحكم الرقم `10`.

### --question--

#### --text--

ما هو الناتج للكود التالي؟

```js
const exampleFunction = (param1, param2) => param1 + param2;
console.log(exampleFunction(3, "Something"));
```

#### --distractors--

`null`

---

`undefined`

---

`3 + "Something"`

#### --answer--

`"3Something"`

### --question--

#### --text--

ما هي النتيجة للكود التالي؟

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + num2);
```

#### --distractors--

رسالة خطأ تقول إن `num1` غير معرف.

---

سيُعرض الرقم `0` في وحدة التحكم.

---

سيُعرض `undefined` في وحدة التحكم.

#### --answer--

رسالة خطأ تقول إن `num2` غير معرف.

### --question--

#### --text--

ما هو الناتج للكود التالي؟

```js
const divideTwoNumbers = (num1, num2) => num1 / num2;
console.log(divideTwoNumbers(3, 0));
```

#### --distractors--

`Error`

---

`0`

---

`null`

#### --answer--

`Infinity`

