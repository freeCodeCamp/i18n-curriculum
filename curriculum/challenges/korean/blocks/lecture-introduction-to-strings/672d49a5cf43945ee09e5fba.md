---
id: 672d49a5cf43945ee09e5fba
title: JavaScript에서 문자열이란 무엇이며 문자열 불변성이란 무엇인가요?
challengeType: 19
dashedName: what-is-a-string
---

# --interactive--

JavaScript에서 문자열은 텍스트 데이터를 나타내는 데 사용되는 문자들의 수열입니다. 문자열은 숫자, 불리언, `null`, `undefined`과 함께 언어의 원시 데이터 유형 중 하나입니다.

JavaScript에서 문자열을 만들려면 작은따옴표(`'`)나 큰따옴표(`"`)를 사용할 수 있습니다.

다음은 문자열 값을 담는 두 변수를 만드는 예시입니다:

:::interactive_editor

```js
let singleQuotes = 'This is a string';
console.log(singleQuotes);
let doubleQuotes = "This is also a string";
console.log(doubleQuotes);
```

:::

문자열을 만들 때 작은따옴표나 큰따옴표를 사용할 수 있지만, 일관성을 유지하는 것이 중요합니다. 문자열이 작은따옴표로 시작하면 반드시 작은따옴표로 끝나야 합니다.

큰따옴표도 마찬가지입니다. 다음 예시는 큰따옴표로 시작해서 작은따옴표로 끝나기 때문에 오류가 발생합니다:

```js
const improperStr = "Do not do this';
```

또 한 가지 고려할 점은 문자열이 불변이라는 것입니다. 프로그래밍에서 불변성은 한 번 생성된 것은 변경할 수 없다는 뜻입니다. 따라서 문자열을 만들면 그 문자를 직접 변경할 수 없습니다. 대신 변경하려면 새 문자열을 만들어야 합니다.

다음은 `developer` 변수에 새 문자열을 할당하는 예시입니다:

:::interactive_editor

```js
let developer = "Jessica";
console.log(developer);
developer = "Quincy";
console.log(developer);
```

:::

문자열이 불변이기 때문에 첫 번째 문자열을 직접 업데이트할 수 없습니다. 그래서 `developer` 변수에 새 문자열을 할당하는 것입니다.

문자열은 프로그래밍에서 중요한 부분이며, 앞으로의 수업에서 문자열을 조작하고 동적이며 인터랙티브한 애플리케이션을 만들기 위해 문자열의 잠재력을 최대한 활용하는 고급 기법을 배우게 될 것입니다.

# --questions--

## --text--

JavaScript에서 문자열을 만드는 올바른 구문은 무엇인가요?

## --answers--

`const str = <this is a string>`

### --feedback--

수업에서 제시된 예시를 검토하세요.

---

`const str = [this is a string]`

### --feedback--

수업에서 제시된 예시를 검토하세요.

---

`const str = "this is a string"`

---

`const str = //this is a string//`

### --feedback--

수업에서 제시된 예시를 검토하세요.

## --video-solution--

3

## --text--

문자열이 작은따옴표로 시작해서 큰따옴표로 끝나면 어떻게 되나요?

## --answers--

유효한 문자열이 만들어집니다.

### --feedback--

따옴표가 일치해야 한다는 조건을 생각하세요.

---

구문 오류가 발생합니다.

---

두 따옴표가 모두 포함된 문자열이 만들어집니다.

### --feedback--

따옴표가 일치해야 한다는 조건을 생각하세요.

---

JavaScript 인터프리터가 무시합니다.

### --feedback--

따옴표가 일치해야 한다는 조건을 생각하세요.

## --video-solution--

2

## --text--

왜 JavaScript에서 문자열이 불변이라고 하나요?

## --answers--

변수를 사용해 문자열을 만들 수 없기 때문입니다.

### --feedback--

프로그래밍에서 "불변"이 의미하는 바를 생각하세요.

---

문자열이 생성된 후에는 문자를 직접 변경할 수 없기 때문입니다.

---

문자열은 리터럴로만 만들 수 있기 때문입니다.

### --feedback--

프로그래밍에서 "불변"이 의미하는 바를 생각하세요.

---

문자열은 변경할 수 있지만 전역 변수를 통해서만 가능합니다.

### --feedback--

프로그래밍에서 "불변"이 의미하는 바를 생각하세요.

## --video-solution--

2
