---
id: 69f42d414b7626e21a922795
title: 내장 미들웨어와 서드파티 미들웨어의 예시는 무엇인가요?
challengeType: 19
dashedName: what-are-examples-of-built-in-and-3rd-party-middleware
---

# --description--

Express는 정적 파일 제공, JSON 요청 본문 파싱 등 일반적인 작업을 수행하는 데 도움이 되는 다양한 내장 미들웨어 함수를 제공합니다. 또한 기능을 추가하기 위해 서드파티 미들웨어도 사용할 수 있습니다.

**다음은 내장 미들웨어의 몇 가지 예시입니다**:

1. `express.json()`: 들어오는 JSON 페이로드를 파싱하는 내장 미들웨어입니다.

```js
app.use(express.json())
```

1. `express.static()`: 이미지, CSS, JavaScript 파일과 같은 정적 파일을 제공하는 미들웨어입니다.

```js
app.use(express.static('public'))
```

1. `express.urlencoded()`: HTML 폼에서 주로 보내는 URL 인코딩된 페이로드를 파싱하는 미들웨어입니다. 파싱된 데이터는 `req.body`에서 사용할 수 있습니다.

```js
app.use(express.urlencoded({ extended: true }))
```

**이제 서드파티 미들웨어의 몇 가지 예시를 살펴보겠습니다:**

1. `cors`: 교차 출처 리소스 공유(CORS)를 활성화하는 인기 있는 서드파티 미들웨어입니다.

```bash
npm install cors
```

```js
const cors = require('cors')
app.use(cors())
```

1. `morgan`: HTTP 요청을 기록하는 로깅 미들웨어입니다.

```bash
npm install morgan
```

```js
const morgan = require('morgan')
app.use(morgan('tiny'))
```

이제 내장 미들웨어와 서드파티 미들웨어를 함께 사용하는 예시를 살펴보겠습니다:

```js
const express = require('express')
const cors = require('cors')
const morgan = require('morgan')
const app = express()

app.use(express.json()) // Built-in middleware to parse JSON
app.use(cors())       // Third-party middleware for CORS
app.use(morgan('tiny')) // Third-party middleware for logging

app.get('/', (req, res) => {
  res.send('Hello, World!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* 내장 미들웨어 `express.json()`는 들어오는 JSON 페이로드를 파싱합니다.
* 서드파티 미들웨어 `cors`는 교차 출처 요청을 허용하며, `morgan`는 들어오는 HTTP 요청을 기록합니다.

미들웨어 함수는 `app.use()`로 호출된 순서대로 추가된다는 점이 중요합니다.

Express는 JSON 파싱과 정적 파일 제공 같은 일반 작업을 처리하는 데 필수적인 내장 미들웨어를 제공합니다. 또한 서드파티 미들웨어로 기능을 확장할 수 있습니다.

# --questions--

## --text--

다음 중 Express의 내장 미들웨어는 무엇인가요?

## --answers--

`express.json()`

---

`cors`

### --feedback--

별도의 설치 없이 Express에 기본으로 포함된 미들웨어가 무엇인지 생각해 보세요.

---

`morgan`

### --feedback--

별도의 설치 없이 Express에 기본으로 포함된 미들웨어가 무엇인지 생각해 보세요.

---

`helmet`

### --feedback--

별도의 설치 없이 Express에 기본으로 포함된 미들웨어가 무엇인지 생각해 보세요.

## --video-solution--

1

## --text--

`express.static()` 미들웨어는 무엇을 하나요?

## --answers--

들어오는 JSON 데이터를 파싱합니다.

### --feedback--

이미지, CSS, JavaScript 같은 파일을 직접 제공하는 기능을 생각해 보세요.

---

HTTP 요청을 기록합니다.

### --feedback--

이미지, CSS, JavaScript 같은 파일을 직접 제공하는 기능을 생각해 보세요.

---

정적 파일을 제공합니다.

---

교차 출처 요청을 허용합니다.

### --feedback--

이미지, CSS, JavaScript 같은 파일을 직접 제공하는 기능을 생각해 보세요.

## --video-solution--

3

## --text--

다음 중 서드파티 미들웨어는 무엇인가요?

## --answers--

`express.json()`

### --feedback--

외부 패키지에서 설치한 미들웨어를 생각해 보세요.

---

`express.static()`

### --feedback--

외부 패키지에서 설치한 미들웨어를 생각해 보세요.

---

`cors`

---

`express.Router()`

### --feedback--

외부 패키지에서 설치한 미들웨어를 생각해 보세요.

## --video-solution--

3
