---
id: 66f1af82732957c895f0b21a
title: SQL と PostgreSQL クイズ
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

リレーショナルデータベースとは何ですか？

#### --distractors--

保存された項目間の非関係を認識するように構成されたデータベース。

---

非構造化データを保存するデータベース。

---

フラットファイルベースのシステム。

#### --answer--

テーブル内のデータ間の関係を認識し管理するように構成されたデータベース。

### --question--

#### --text--

非リレーショナルデータベースはリレーショナルデータベースとどのように異なりますか？

#### --distractors--

非リレーショナルデータベースはテーブルを使用し、リレーショナルデータベースは使用しない。

---

リレーショナルデータベースは非構造化データを保存できる。

---

非リレーショナルデータベースはリレーショナルデータベースより遅い。

#### --answer--

非リレーショナルデータベースは非構造化データを保存できます。

### --question--

#### --text--

PostgreSQLの対話型端末（`\l`）で`psql`コマンドを実行すると何が起こりますか？

#### --distractors--

すべてのスキーマを一覧表示する。

---

すべてのテーブルを一覧表示する。

---

データベースに接続する。

#### --answer--

すべてのデータベースを一覧表示する。

### --question--

#### --text--

次のうち、SQLで新しいテーブルを作成するコマンドはどれですか？

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

リレーショナルデータベースにおける外部キーの目的は何ですか？

#### --distractors--

データの重複を許可するため。

---

テーブル内の行を一意に識別するため。

---

大量のテキストデータを保存するため。

#### --answer--

2つのテーブル間に関係を作成するため。

### --question--

#### --text--

既存のテーブルに新しい列を追加するコマンドはどれですか？

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

テーブルAの1つのレコードがテーブルBの1つ以上のレコードに対応する関係は何ですか？

#### --distractors--

1対1 (1-1)

---

多対多 (m-m)

---

多対1 (m-1)

#### --answer--

1対多 (1-m)

### --question--

#### --text--

クエリとは何ですか？

#### --distractors--

データベースからすべてのデータを削除するリクエスト。

---

データベース内の一意の値のための特別な行。

---

データベース内の一意の値のための特別な列。

#### --answer--

データベースから特定のデータを取得するリクエスト。

### --question--

#### --text--

SQLでテーブルから列を削除するにはどうしますか？

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

SQLの`PRIMARY KEY`制約の機能は何ですか？

#### --distractors--

2つのテーブルをリンクするため。

---

列のデフォルト値を定義するため。

---

列にNULL値を許可するため。

#### --answer--

列に一意の値が含まれることを保証するため。

### --question--

#### --text--

PostgreSQLの対話型端末（`\c`）で`psql`コマンドは何をしますか？

#### --distractors--

現在のデータベース内のすべてのテーブルを一覧表示する。

---

現在のデータベース接続の詳細を表示する。

---

`psql`シェルを終了する。

#### --answer--

指定されたデータベースに接続する。

### --question--

#### --text--

リレーショナルデータベースにおける複合キーとは何ですか？

#### --distractors--

別のキーから派生したキー。

---

データ更新により頻繁に変わるキー。

---

外部キー関係専用のキー。

#### --answer--

複数の列で構成され、レコードを一意に識別するキー。

### --question--

#### --text--

テーブルにデータを挿入するコマンドはどれですか？

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

SQLで列の名前を変更するにはどうしますか？

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

employeesテーブルのすべてのレコードを削除するクエリはどれですか？

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

列に一意の値のみを含み、NULLを許可するSQL制約はどれですか？

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

次のうち、業界で一般的に使用されているリレーショナルデータベースの例ではないものはどれですか？

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQLQuery

### --question--

#### --text--

リレーショナルデータベースにおける1対1（1-1）関係は何を表しますか？

#### --distractors--

テーブルAの各レコードがテーブルBの1つ以上のレコードに対応する。

---

テーブルAの各レコードがテーブルBのレコードに対応しない。

---

テーブルAの各レコードがテーブルBの複数のレコードに対応し、その逆も同様。

#### --answer--

テーブルAの各レコードがテーブルBのちょうど1つのレコードに対応する。

### --question--

#### --text--

現在のPostgreSQLデータベース内のすべてのテーブルを一覧表示するコマンドはどれですか？

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

次のうち、テーブルを自身と結合できるものはどれですか？

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`
