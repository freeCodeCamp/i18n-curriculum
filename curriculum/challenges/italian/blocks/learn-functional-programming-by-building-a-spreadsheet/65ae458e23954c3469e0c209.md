---
id: 65ae458e23954c3469e0c209
title: Passaggio 18
challengeType: 0
dashedName: step-18
---


# --description--

Nei progetti precedenti hai imparato il metodo `setAttribute`. Un altro modo per aggiornare un attributo in JavaScript è usare la seguente sintassi:

```js
el.attribute = value;
```

I nomi delle proprietà per i valori degli attributi HTML scritti con trattino, come `aria-label`, seguono la camel case, diventando `ariaLabel`.

```js
el.ariaLabel = "Aria Label Value";
```

Imposta l'attributo `aria-label` per l'elemento `input` allo stesso valore dell'attributo `id`.


# --hints--

Dovresti avere un `input.ariaLabel`.

```js
assert.match(code, /input\.ariaLabel\s*=/);
```


Dovresti dare a `input.ariaLabel` lo stesso valore di `input.id`.

```js
assert.match(code, /input\.ariaLabel\s*=\s*((letter\s*\+\s*number)|(input.id))/);
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

```css
#container {
  display: grid;
  grid-template-columns: 50px repeat(10, 200px);
  grid-template-rows: repeat(11, 30px);
}

.label {
  background-color: lightgray;
  text-align: center;
  vertical-align: middle;
  line-height: 30px;
}
```

```js
const range = (start, end) => Array(end - start + 1).fill(start).map((element, index) => element + index);
const charRange = (start, end) => range(start.charCodeAt(0), end.charCodeAt(0)).map(code => String.fromCharCode(code));

window.onload = () => {
  const container = document.getElementById("container");
  const createLabel = (name) => {
    const label = document.createElement("div");
    label.className = "label";
    label.textContent = name;
    container.appendChild(label);
  }
  const letters = charRange("A", "J");
  letters.forEach(createLabel);
  range(1, 99).forEach(number => {
     createLabel(number);
--fcc-editable-region--
    letters.forEach(letter => {
     const input = document.createElement("input");
     input.type = "text";
     input.id = letter + number;
 
    })   
--fcc-editable-region--
  })
}
```
