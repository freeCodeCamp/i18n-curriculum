---
id: 68eac57fadcc662ce917e75e
title: 步骤2
challengeType: 0
dashedName: step-2
---

# --description--

接下来，为你的页面添加一个 `button` 元素，其 `type` 属性设置为值 `button`。

`button` 元素的文本应为 `Play`。

# --hints--

你应该有一个 `button` 元素。

```js
assert.exists(document.querySelector('button'));
```

你的 `button` 元素应具有 `type` 属性。

```js
const button = document.querySelector('button');
assert.isTrue(button?.hasAttribute('type'));
```

你的 `button` 元素应将 `type` 属性设置为 `button`。

```js
const button = document.querySelector('button');
assert.strictEqual(button?.getAttribute('type'), 'button');
```

你的 `button` 元素应包含 `Play` 文本。

```js
const button = document.querySelector('button');
assert.strictEqual(button?.innerText.trim(), 'Play');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Accessible Controls</title>
  </head>
  <body>

    <h1>Audio Controls</h1>
--fcc-editable-region--
    
--fcc-editable-region--
  </body>
</html>
```
