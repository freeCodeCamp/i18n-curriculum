---
id: 67358be1c7903489c0a7db78
title: JavaScript Maps 和 Sets 測驗
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript 中的 Set 是什麼？

#### --distractors--

允許重複值的資料結構。

---

一種只儲存字串和數字的資料結構。

---

不允許刪除其元素的群集。

#### --answer--

一個值只能出現一次的群集。

### --question--

#### --text--

以下哪一個方法在 `Maps` 中可用，但在 `Sets` 中不可用？

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

用來為 `Set` 添加元素的方法是什麼？

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

你如何檢查一個 Set 是否包含某個值？

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

以下哪一項是使用 Sets 的絕佳案例？

#### --distractors--

在資料庫中管理鍵值配對。

---

建立有序的項目列表。

---

在瀏覽器區域的儲存中儲存階層式資料結構。

#### --answer--

從陣列中消除重複項目。

### --question--

#### --text--

什麼是 JavaScript 中的 WeakSet？

#### --distractors--

一種類似 Set 的群集，只允許原始資料型別。

---

一種類似 Set 的群集，允許重複值。

---

一種類似 Set 的群集，可防止值被移除。

#### --answer--

一種類似 Set 的群集，只儲存物件。

### --question--

#### --text--

Set 和 WeakSet 之間的一個差異是什麼？

#### --distractors--

`WeakSet` 可以儲存任何型別的值，包括數字。

---

Set 不允許重複值，但 WeakSet 允許。

---

Set 允許垃圾回收，而 WeakSet 則不允許。

#### --answer--

Set 可以儲存任何型別的值，而 WeakSet 僅能儲存物件。

### --question--

#### --text--

JavaScript 中的 Map 是什麼？

#### --distractors--

只允許唯一值的群集。

---

只能使用字串作為鍵的群集。

---

不允許刪除其項目的群集。

#### --answer--

任意型別的鍵值對組成的群集。

### --question--

#### --text--

用來將鍵值對組插入到 `Map` 的方法是什麼？

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

以下 `code` 會輸出什麼？

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

在 JavaScript 中，`Map` 與物件有何不同？

#### --distractors--

Map 只允許數字作為鍵。

---

物件允許任何型別作為鍵，而 Maps 僅允許字串。

---

Maps 不支援迭代，而物件支援。

#### --answer--

Maps 允許任何型別的鍵，但物件只允許字串和符號。

### --question--

#### --text--

你如何從 `Map` 中移除一個鍵值配對？

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

`size` 屬性在 Map 中傳回什麼？

#### --distractors--

Map 中的索引數量。

---

Map 中唯一值的數量。

---

Map 使用的總記憶體。

#### --answer--

Map 中的鍵值配對數量。

### --question--

#### --text--

什麼是 JavaScript 中的 WeakMap？

#### --distractors--

一種類似 Map 的群集，允許使用原始值作為鍵。

---

一種類似 Map 的群集，會阻止刪除項目。

---

一個類似 Map 的群集，會自動排序其鍵。

#### --answer--

一個類似 Map 的群集，對鍵使用弱引用。

### --question--

#### --text--

Map 和 WeakMap 之間的一個差異是什麼？

#### --distractors--

Map 只允許原生類型作為鍵，而 WeakMap 只允許物件。

---

Map 允許垃圾回收，而 WeakMap 則不允許。

---

`WeakMap` 允許重複鍵，而 `Map` 則不允許。

#### --answer--

Map 是可迭代的，而 WeakMap 則不是。

### --question--

#### --text--

以下哪一個方法不存在於 `WeakMap`？

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

以下 `code` 的輸出結果會是什麼？

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

你如何對 Set 中的值進行迭代？

#### --distractors--

透過使用 `.map()` 方法。

---

透過使用 `.reduce()` 方法。

---

透過使用 `.filter()` 方法。

#### --answer--

透過使用 `.forEach()` 方法。

### --question--

#### --text--

哪兩個 Set 的方法會傳回包含某個 Set 值的 `SetIterator`？

#### --distractors--

`entries()` 和 `values()`

---

`forEach()` 和 `values()`

---

`has()` 和 `add()`

#### --answer--

`keys()` 和 `values()`

### --question--

#### --text--

以下 `code` 的輸出結果會是什麼？

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
