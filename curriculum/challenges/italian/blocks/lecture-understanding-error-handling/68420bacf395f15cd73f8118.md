---
id: 68420bacf395f15cd73f8118
title: Quali sono alcuni messaggi di errore comuni in Python?
challengeType: 19
dashedName: what-are-some-common-error-messages-in-python
---

# --description--

Quando scrivi codice Python, è comune incontrare errori. Capire questi errori è fondamentale per eseguire il debug del codice in modo rapido ed efficiente. Questi messaggi ti dicono esattamente cosa è andato storto, se sai come leggerli.

Gli errori comuni in Python includono `SyntaxError`, `NameError`, `TypeError`, `IndexError` e `AttributeError`. Questi si verificano quando Python non capisce il tuo codice o quando la tua logica non corrisponde ai dati con cui stai lavorando.

Ecco un esempio di `SyntaxError`:

```py
print("Hello, world!"
# SyntaxError: unexpected EOF while parsing
```

A questa riga manca una parentesi di chiusura. Python genera un `SyntaxError` perché il codice non segue le regole di sintassi corrette.

Ecco un esempio di `NameError`:

```py
print(name)
# NameError: name 'name' is not defined
```

Stai cercando di stampare una variabile che non è stata ancora definita. Python genera un `NameError` quando non riesce a trovare una variabile con quel nome.

Ecco un esempio di `TypeError`:

```py
5 + "5"
# TypeError: unsupported operand type(s) for +: 'int' and 'str'
```

Non puoi sommare un intero e una stringa insieme. Python genera un `TypeError` quando provi a eseguire un’operazione su tipi di dati incompatibili.

Ecco un esempio di `IndexError`:

```py
my_list = [1, 2, 3]
print(my_list[5])
# IndexError: list index out of range
```

Stai cercando di consultare un indice che non esiste nella lista. Python genera un `IndexError` quando esci dai limiti.

Ecco un esempio di `AttributeError`:

```py
num = 42
num.append(5)
# AttributeError: 'int' object has no attribute 'append'
```

L’oggetto `int` non ha un metodo `append()`. Python genera un `AttributeError` quando provi a usare un metodo o una proprietà che non esiste per quel tipo di dato.

Riconoscere i messaggi di errore comuni in Python ti aiuta a risolvere i problemi più velocemente. Invece di indovinare, leggi attentamente il messaggio di errore, spesso ti dice esattamente cosa è andato storto e dove cercare.


# --questions--

## --text--

Cosa significa un `NameError` in Python?

## --answers--

Una variabile è scritta male.

### --feedback--

Riferisciti alla sezione della lezione su `NameError`.

---

Una variabile non è definita.

---

Un indice è fuori intervallo.

### --feedback--

Riferisciti alla sezione della lezione su `NameError`.

---

Un tipo di dato è errato.

### --feedback--

Riferisciti alla sezione della lezione su `NameError`.

## --video-solution--

2

## --text--

Quale errore si verifica quando si prova ad aggiungere un intero a una stringa?

## --answers--

`NameError`

### --feedback--

Riferisciti agli errori di tipo nella lezione.

---

`SyntaxError`

### --feedback--

Riferisciti agli errori di tipo nella lezione.

---

`TypeError`

---

`AttributeError`

### --feedback--

Riferisciti agli errori di tipo nella lezione.

## --video-solution--

3

## --text--

Cosa causa un `IndexError`?

## --answers--

Manca una funzione.

### --feedback--

Riferisciti alla sezione su `IndexError` nella lezione.

---

Una stringa non è formattata correttamente.

### --feedback--

Riferisciti alla sezione su `IndexError` nella lezione.

---

Consultare un elemento fuori dai limiti di un iterabile.

---

Usare una variabile non definita.

### --feedback--

Riferisciti alla sezione su `IndexError` nella lezione.

## --video-solution--

3
