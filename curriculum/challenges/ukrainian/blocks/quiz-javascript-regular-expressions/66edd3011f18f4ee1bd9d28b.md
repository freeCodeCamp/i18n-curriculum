---
id: 66edd3011f18f4ee1bd9d28b
title: Тест з регулярних виразів JavaScript
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який із наведених варіантів НЕ є дійсним регулярним виразом?

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

Який `RegExp` містить той самий шаблон, що й регулярний вираз `/f[o0]{2} b[a4@]r/i`?

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

Який тип повертає метод регулярного виразу `test`?

#### --distractors--

Масив рядків, які співпали з регулярним виразом.

---

Об’єкт, що містить інформацію про (під)рядок, який співпав із регулярним виразом.

---

`null`, метод `test` лише перевіряє, чи є регулярний вираз дійсним.

#### --answer--

Булевий тип, що вказує, чи рядок співпадає з регулярним виразом.

### --question--

#### --text--

Що робить метод `match`, якщо йому передати регулярний вираз без прапорців?

#### --distractors--

Він шукає всі рядки, що співпадають із заданим регулярним виразом, і повертає ВСІ співпадіння у вигляді масиву.

---

Він шукає перше повне співпадіння і повертає початковий індекс цього співпадіння.

---

Він шукає перше повне співпадіння і повертає булевий тип, що вказує, чи знайдено співпадіння.

#### --answer--

Він шукає перше повне співпадіння і повертає масив, що містить це перше співпадіння.

### --question--

#### --text--

Яке найкраще застосування для `test`?

#### --distractors--

Для перевірки дійсності заданого регулярного виразу.

---

Для отримання детальної інформації про співпадіння рядка, наприклад, довжини співпавшого рядка.

---

Щоб витягти всі співпадіння з заданого рядка.

#### --answer--

Щоб перевірити, чи співпадає заданий рядок із регулярним виразом.

### --question--

#### --text--

Яка мета використання `replace`?

#### --distractors--

Щоб замінити поточний регулярний вираз на новий регулярний вираз.

---

Якщо задано індекс і рядок, замінити символ за вказаним індексом на наданий рядок.

---

Щоб видалити всі входження співпавшого підрядка з рядка.

#### --answer--

Щоб замінити співпавший рядок на заданий рядок-замінник.

### --question--

#### --text--

У чому різниця між `match` і `matchAll`?

#### --distractors--

`match` завжди повертає рядок, тоді як `matchAll` завжди повертає масив співпадінь.

---

`match` повертає булевий тип, що вказує, чи знайдено співпадіння, а `matchAll` повертає число, що представляє кількість знайдених співпадінь.

---

`match` повертає число, що вказує початковий індекс першого знайденого співпадіння, а `matchAll` повертає масив чисел з індексами, де знайдено співпадіння.

#### --answer--

`match` повертає масив рядків, що співпадають, а `matchAll` повертає ітератор об’єкт усіх знайдених співпадінь.

### --question--

#### --text--

Яке з наведених правильне використання `replaceAll`?

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

Який із наведених класів символів є еквівалентним регулярному виразу `/[a-zA-Z0-9_]/`?

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

Який із наведених класів символів найкраще підходить для вилучення цифр із номера телефону?

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

Що відбувається, коли у регулярному виразі використовується випередження `(?=)`?

#### --distractors--

Випередження перевіряє ліворуч від рядка, підтверджуючи, що підшаблон ПРИСУТНІЙ.

---

Випередження — це альтернативний шаблон регулярного виразу, який використовується, коли основний шаблон не співпадає.

---

Випередження перевіряє праворуч від рядка, підтверджуючи, що підшаблон ВІДСУТНІЙ.

#### --answer--

Випередження перевіряє праворуч від рядка, підтверджуючи, що підшаблон ПРИСУТНІЙ.

### --question--

#### --text--

Де слід розміщувати відставання `(?<=)` при створенні регулярного виразу?

#### --distractors--

Відставання можна розміщувати будь-де у регулярному виразі.

---

Відставання слід розміщувати праворуч від основного шаблону регулярного виразу.

---

Відставання має включати основний шаблон регулярного виразу.

#### --answer--

Відставання слід розміщувати ліворуч від основного шаблону регулярного виразу.

### --question--

#### --text--

Який квантор відповідає за співпадіння попереднього елемента нуль або один раз?

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

Який із наведених регулярних виразів дозволяє лише числа від 0 до 999999?

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

Що є правдою про кастомний клас символів (`[]`)?

#### --distractors--

Це множина символів, які потрібно видалити зі співпадіння.

---

Він може визначати множину символів для співпадіння без необхідності екранування спеціальних символів.

---

Він представляє множину символів у форматі Unicode.

#### --answer--

Він може визначати кастомну множину символів для співпадіння.

### --question--

#### --text--

Який із наведених класів символів правильно співпадає з алфавітом у верхньому регістрі?

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

Що відбувається, коли у регулярному виразі використовується захоплююча група `(...)`?

#### --distractors--

Захоплююча група негайно оцінюється, незалежно від того, де вона розташована у регулярному виразі.

---

Захоплююча група намагається співпасти за заданим підшаблоном і продовжує без запам’ятовування результату.

---

Захоплююча група — це пріоритетний підшаблон, який негайно повертає результат, коли знайдено співпадіння.

#### --answer--

Захоплююча група намагається співпасти за заданим підшаблоном і продовжує з результатом у пам’яті.

### --question--

#### --text--

Що відбувається, коли у регулярному виразі використовується незахоплююча група `(?:...)`?

#### --distractors--

Незахоплююча група намагається співпасти за заданим підшаблоном і продовжує з результатом у пам’яті.

---

Незахоплююча група негайно завершується, коли знаходить співпадіння для заданого підшаблону.

---

Незахоплююча група вважається необов’язковим співпадінням, тому до результату додається лише успішне співпадіння.

#### --answer--

Незахоплююча група намагається співпасти за заданим підшаблоном і продовжує без збереження результату в пам’яті.

### --question--

#### --text--

Для наведеного регулярного виразу, який із цих тестів поверне `true`?

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

Що робить клас символів wildcard (`.`) у регулярному виразі?

#### --distractors--

Він співпадає лише з пробільними символами.

---

Він співпадає з початком рядка.

---

Він співпадає з однією цифрою.

#### --answer--

Він співпадає з будь-яким одним символом, крім символу нового рядка.
