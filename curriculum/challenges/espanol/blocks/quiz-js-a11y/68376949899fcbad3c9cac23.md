---
id: 68376949899fcbad3c9cac23
title: Cuestionario de JavaScript y Accesibilidad
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

Para pasar el cuestionario, debe responder correctamente al menos 9 de las 10 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué atributo se usa para propósitos de accesibilidad para indicar si un control está expandido o colapsado?

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

¿Qué dos atributos se pueden usar en combinación con `aria-expanded` para establecer una conexión programática entre el elemento controlador y el elemento que controla?

#### --distractors--

`aria-labelledBy` y `aria-owns`

---

`aria-controls` y `aria-label`

---

`aria-live` y `aria-owns`

#### --answer--

`aria-controls` y `aria-owns`

### --question--

#### --text--

¿Cuál de los siguientes atributos crea una región activa en tu página que puede usarse para notificar a los usuarios de lectores de pantalla sobre cambios dinámicos en el contenido de la región activa a medida que ocurren?

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

¿Cuál de los siguientes elementos `div` estará deshabilitado solo para personas que usan tecnologías de asistencia, como lectores de pantalla?

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

¿Cuál de los siguientes es un uso válido del atributo `aria-expanded`?

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

¿Qué atributo se usa para crear una relación programática entre un elemento controlador y el elemento que afecta en la página?

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

¿Cuál de los siguientes NO es un caso de uso común para las regiones en vivo?

#### --distractors--

Mensajes que se muestran después de que se ha realizado una acción (como un mensaje de error o de confirmación).

---

Contenido que se actualiza periódicamente (como un ticker, marquee o countdown timer).

---

Mensajes generales de estado (como actualizaciones sobre un proceso).

#### --answer--

Mensajes que se muestran antes de que se haya realizado una acción (como un cuadro de diálogo de confirmación).

### --question--

#### --text--

¿Cuál de los siguientes ejemplos es la forma correcta de indicar qué pestaña está actualmente seleccionada?

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

¿Cuál de los siguientes NO es un valor válido para el atributo `aria-live`?

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

¿Cuál de los siguientes valores del atributo `aria-live` significa que la actualización es muy importante y tiene la máxima prioridad?

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`

