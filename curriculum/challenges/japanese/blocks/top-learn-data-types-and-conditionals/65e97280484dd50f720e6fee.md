---
id: 65e97280484dd50f720e6fee
title: データ型と条件分岐の学習 レッスンI
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-i
---
# --description--

前の例では、`if`か`else`の2つの選択肢がありました。しかし、もし2つ以上の選択肢が欲しい場合はどうでしょう？`else if`文を使ってコードに条件を追加できます。`else if`文は複数の条件をチェックし、最初の条件が真の場合にコードのブロックを実行します。最初の条件が偽の場合は次の条件をチェックし、以下同様に続きます。どの条件も真でない場合は、`else`ブロックが実行されます。

`else if`文の例を示します：

```javascript
let x = 5;

if (x > 10) {
  console.log("x is greater than 10");
} else if (x > 5) {
  console.log("x is greater than 5");
} else {
  console.log("x is less than or equal to 5");
}
```

この例では、`x`変数が`10`という数値と`>`演算子で比較されています。もし`x`が`10`より大きければ、最初のコードブロックが実行されます。そうでなければ、`else if`文が次の条件をチェックします。もし`x`が`5`より大きければ、2番目のコードブロックが実行されます。そうでなければ、`else`ブロックが実行されます。

# --questions--

## --text--

次のコードの出力は何になりますか？

```javascript
let x = 4;

if (x > 10) {
  console.log("x is greater than 10");
} else if (x > 5) {
  console.log("x is greater than 5");
} else {
  console.log("x is less than or equal to 5");
}
```

## --answers--

`x is less than or equal to 5`

---

`x is greater than 5`

---

`x is greater than 10`

## --video-solution--

1
