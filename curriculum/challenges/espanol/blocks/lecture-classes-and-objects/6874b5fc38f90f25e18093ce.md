---
id: 6874b5fc38f90f25e18093ce
title: ¿Qué son los métodos y atributos, y cómo funcionan?
challengeType: 19
dashedName: what-are-methods-and-attributes-and-how-do-they-work
---

# --description--

En la última lección, aprendiste sobre las clases y cómo actúan como planos para crear objetos.

Aquí, profundizaremos en atributos y métodos.

Primero, echemos un vistazo más de cerca a los atributos, luego a los métodos.

Los atributos son variables que pertenecen a un objeto, por lo que contienen datos. Hay dos tipos de atributos: atributos de instancia y atributos de clase.

Los atributos de instancia son únicos para cada objeto creado a partir de una clase, y normalmente los configuras con el método `__init__`. Los atributos de clase, en cambio, pertenecen a la clase misma y son compartidos por todas las instancias de esa clase.

Para acceder a un atributo, usa la notación de punto.

Aquí tienes ejemplos de atributos de instancia y de clase, y cómo acceder a ellos desde objetos:

```python
class Dog:
    species = "French Bulldog" # Class attribute

    def __init__(self, name):
        self.name = name # Instance attribute

print(Dog.species) # French Bulldog

dog1 = Dog("Jack")
print(dog1.name)    # Jack
print(dog1.species) # French Bulldog

dog2 = Dog("Tom")
print(dog2.name)    # Tom
print(dog2.species) # French Bulldog
```

Ten en cuenta que puedes acceder a los atributos de clase directamente desde la clase, pero necesitas crear un objeto y pasarle datos primero antes de poder acceder a los atributos de instancia.

Los coches son otro buen ejemplo, ya que todos los coches tienen un modelo y un color:

```python
class Car:
    def __init__(self, color, model):
        self.color = color
        self.model = model

car_1 = Car("red", "Toyota Corolla")
car_2 = Car("green", "Lamborghini Revuelto")

print(car_1.model) # Toyota Corolla
print(car_2.model) # Lamborghini Revuelto

print(car_1.color) # red
print(car_2.color) # green
```

Los métodos son funciones definidas dentro de una clase. Con ellos, cualquier objeto definido a partir de una clase puede realizar acciones que operan o modifican sus propios datos. También accedes a un método con notación de punto.

Por ejemplo, los perros pueden ladrar. Así que podemos tener un método `bark` en la clase `Dog` como viste en una lección anterior:

```python
class Dog:
   species = "French Bulldog"

   def __init__(self, name):
     self.name = name

   def bark(self):
       return f"{self.name} says woof woof!"

jack = Dog("Jack")
jill = Dog("Jill")

print(jack.bark()) # Jack says woof woof!
print(jill.bark()) # Jill says woof woof!
```

Una clase `Car` también puede tener un método `describe`:

```python
class Car:
    def __init__(self, color, model):
        self.color = color  # Instance attribute
        self.model = model  # Instance attribute

    def describe(self):
        return f"This car is a {self.color} {self.model}"

car_1 = Car("red", "Toyota Corolla")
car_2 = Car("green", "Lamborghini Revuelto")

print(car_1.describe()) # This car is a red Toyota Corolla
print(car_2.describe()) # This car is a green Lamborghini Revuelto
```

# --questions--

## --text--

¿Cuáles son los dos tipos de atributos en Python?

## --answers--

Atributos públicos y privados

### --feedback--

Piensa en las dos formas en que puedes definir atributos.

---

Atributos locales y globales

### --feedback--

Piensa en las dos formas en que puedes definir atributos.

---

Atributos de instancia y atributos de clase

---

Atributos mutables e inmutables

### --feedback--

Piensa en las dos formas en que puedes definir atributos.

## --video-solution--

3

## --text--

¿Qué se requiere para acceder a los atributos de una instancia?

## --answers--

El nombre de la clase solamente

### --feedback--

Piensa en cómo accedes a los atributos de clase versus cómo accedes a los atributos de instancia.

---

Decoradores

### --feedback--

Piensa en cómo accedes a los atributos de clase versus cómo accedes a los atributos de instancia.

---

Una instancia u objeto de la clase

---

Un método estático

### --feedback--

Piensa en cómo accedes a los atributos de clase versus cómo accedes a los atributos de instancia.

## --video-solution--

3

## --text--

¿Cómo defines y accedes a los métodos?

## --answers--

Como funciones independientes fuera de una clase, accesadas con corchetes

### --feedback--

Pertenecen a la clase pero operan sobre datos del objeto.

---

Como variables en una clase, accedidas como atributos

### --feedback--

Pertenecen a la clase pero operan sobre datos del objeto.

---

Con palabras clave especiales, llamadas automáticamente

### --feedback--

Pertenecen a la clase pero operan sobre datos del objeto.

---

Se definen dentro de una clase y se acceden con notación de punto

## --video-solution--

4
