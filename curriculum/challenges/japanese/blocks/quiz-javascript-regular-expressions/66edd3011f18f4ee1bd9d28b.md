---
id: 66edd3011f18f4ee1bd9d28b
title: JavaScript 正規表現クイズ
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

次のうち、正規表現として無効なものはどれですか？

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

`RegExp` の中で、正規表現 `/f[o0]{2} b[a4@]r/i` と同じパターンを含むものはどれですか？

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

正規表現のメソッド `test` の戻り値の型は何ですか？

#### --distractors--

正規表現にマッチした文字列の配列。

---

正規表現がマッチした（サブ）文字列に関する情報を含むオブジェクト。

---

`null`、メソッド `test` は与えられた正規表現が有効かどうかを検証するだけです。

#### --answer--

文字列が正規表現にマッチするかどうかを示すブール値。

### --question--

#### --text--

フラグなしの正規表現を渡した場合、`match` メソッドは何をしますか？

#### --distractors--

与えられた正規表現にマッチするすべての文字列を検索し、配列として返します。

---

最初の完全なマッチを検索し、そのマッチの開始インデックスを返します。

---

最初の完全なマッチを検索し、マッチが見つかったかどうかを示すブール値を返します。

#### --answer--

最初の完全なマッチを検索し、その最初のマッチを含む配列を返します。

### --question--

#### --text--

`test` の最適な使用例はどれですか？

#### --distractors--

与えられた正規表現の検証。

---

マッチした文字列の長さなど、文字列マッチに関する詳細情報の取得。

---

与えられた文字列からすべてのマッチを抽出すること。

#### --answer--

与えられた文字列が正規表現にマッチするかどうかを確認すること。

### --question--

#### --text--

`replace` を使う目的は何ですか？

#### --distractors--

現在の正規表現を新しい正規表現に置き換えること。

---

インデックスと文字列が与えられた場合、指定されたインデックスの文字をその文字列で置き換えること。

---

文字列からマッチした部分文字列のすべてのインスタンスを削除すること。

#### --answer--

マッチした文字列を指定した置換文字列に置き換えること。

### --question--

#### --text--

`match` と `matchAll` の違いは何ですか？

#### --distractors--

`match` は常に文字列を返し、`matchAll` は常にマッチの配列を返します。

---

`match` はマッチが見つかったかどうかを示すブール値を返し、`matchAll` は見つかったマッチの数を表す数値を返します。

---

`match` は最初のマッチの開始インデックスを示す数値を返し、`matchAll` はマッチが見つかったインデックスの配列を返します。

#### --answer--

`match` はマッチした文字列の配列を返し、`matchAll` は見つかったすべてのマッチのイテレーターオブジェクトを返します。

### --question--

#### --text--

`replaceAll` の正しい使い方はどれですか？

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

次のうち、正規表現 `/[a-zA-Z0-9_]/` と同等の文字クラスはどれですか？

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

電話番号から数字を抽出するのに最も適切な文字クラスはどれですか？

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

正規表現で先読み `(?=)` を使うと何が起こりますか？

#### --distractors--

先読みは文字列の左側でアサートし、サブパターンが存在することを検証します。

---

先読みはメインパターンがマッチしなかった場合に使う代替の正規表現パターンです。

---

先読みは文字列の右側でアサートし、サブパターンが存在しないことを検証します。

#### --answer--

先読みは文字列の右側でアサートし、サブパターンが存在することを検証します。

### --question--

#### --text--

正規表現を作成する際、先読みの後読み `(?<=)` はどこに置くべきですか？

#### --distractors--

後読みは正規表現のどこにでも置けます。

---

後読みは正規表現のメインパターンの右側に置くべきです。

---

後読みは正規表現のメインパターンを含むべきです。

#### --answer--

後読みは正規表現のメインパターンの左側に置くべきです。

### --question--

#### --text--

直前の要素に0回または1回マッチする量指定子はどれですか？

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

次のうち、0から999,999までの数字のみを許可する正規表現はどれですか？

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

カスタム文字クラス (`[]`) について正しい説明はどれですか？

#### --distractors--

マッチから除外する文字のセットです。

---

特殊文字をエスケープせずにマッチする文字のセットを定義できます。

---

Unicode形式の文字セットを表します。

#### --answer--

マッチするカスタムの文字セットを定義できます。

### --question--

#### --text--

次のうち、大文字アルファベットに正しくマッチする文字クラスはどれですか？

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

正規表現でキャプチャグループ `(...)` を使うと何が起こりますか？

#### --distractors--

キャプチャグループは正規表現のどこにあっても即座に評価されます。

---

キャプチャグループは与えられたサブパターンでマッチを試み、結果を記憶せずに続行します。

---

キャプチャグループは優先されるサブパターンで、マッチが見つかると即座に返します。

#### --answer--

キャプチャグループは与えられたサブパターンでマッチを試み、結果を記憶しながら続行します。

### --question--

#### --text--

正規表現で非キャプチャグループ `(?:...)` を使うと何が起こりますか？

#### --distractors--

非キャプチャグループは与えられたサブパターンでマッチを試み、結果を記憶しながら続行します。

---

非キャプチャグループは与えられたサブパターンでマッチが見つかると即座に終了します。

---

非キャプチャグループはオプションのマッチとみなされ、成功したマッチのみ結果に追加されます。

#### --answer--

非キャプチャグループは与えられたサブパターンでマッチを試み、結果を記憶せずに続行します。

### --question--

#### --text--

次の正規表現に対して、どのテストが `true` を返しますか？

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

正規表現でワイルドカード文字クラス (`.`) は何をしますか？

#### --distractors--

空白文字のみマッチします。

---

文字列の先頭にマッチします。

---

1つの数字にマッチします。

#### --answer--

改行を除く任意の1文字にマッチします。
