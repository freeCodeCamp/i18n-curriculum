---
id: 6733696567d2273540aa6033
title: ما هي واجهة برمجة تطبيقات تحريك الويب (Web Animations API)، وكيف ترتبط بخواص تحريك CSS؟
challengeType: 19
dashedName: what-is-the-web-animations-api-and-how-does-it-relate-to-css-animation-properties
---

# --interactive--

تسمح لك واجهة برمجة تطبيقات `Web Animations` (WAAPI) بإنشاء وتحكم في التحريكات مباشرة داخل JavaScript. مع WAAPI، يمكنك التعامل مع التحريكات بشكل أكثر ديناميكية، مما يسهل التلاعب بها.

لنلق نظرة على WAAPI بمزيد من التفصيل، حتى تبدأ في العمل مع التحريكات مباشرة داخل شفرة JavaScript الخاصة بك. في جوهر WAAPI توجد دالة البناء `Animation`، التي توفر عدة طرق وخصائص تسمح لك بتحريك العناصر بشكل ديناميكي.

طريقة مهمة في دالة البناء `Animation` هي `animate()`. تتيح لك إنشاء تحريك بتحديد الإطارات المفتاحية وخيارات مثل المدة، الاتجاه، التسهيل، وعدد التكرارات.

إليك الصيغة النحوية الأساسية لطريقة `animate()`:

```js
element.animate(keyframes, options);
```

لننظر إلى مثال.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css" />
<div class="square" id="square"></div>
<script src="index.js"></script>
```

```css
body {
  background: #f1f1f1;
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
}

.square {
  background: #1b1b32;
  width: 10rem;
  aspect-ratio: 1/1;
}
```

```js
const square = document.querySelector("#square");

const animation = square.animate(
  [{ transform: "translateX(0px)" }, { transform: "translateX(100px)" }],
  {
    duration: 2000, // makes the animation last 2 seconds
    iterations: Infinity, // loops indefinitely
    direction: "alternate", // moves back and forth
    easing: "ease-in-out" // smooth easing
  }
);
```

:::

النتيجة في المتصفح ستكون مربعًا أزرق يتحرك ذهابًا وإيابًا أفقيًا.

تشمل طرق دالة البناء `Animation` ما يلي:

```js
play()
pause()
reverse()
finish()
cancel()
```

وتشمل الخصائص ما يلي:

```md
playbackRate
currentTime
startTime
effect
timeline
playState
finished
onfinish
oncancel
```

دعنا نعدل المثال لاستخدام طريقتي `play()` و`pause()` والخاصية `onfinish`. إليك النسخة الجديدة:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css" />
<div class="square" id="square"></div>

<button id="playBtn">Play</button>
<button id="pauseBtn">Pause</button>
<script src="index.js"></script>
```

```css
body {
  background: #f1f1f1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
}
.square {
  background: #1b1b32;
  width: 10rem;
  aspect-ratio: 1/1;
  margin-bottom: 20px;
}
button {
  margin: 10px;
  padding: 10px 20px;
  font-size: 16px;
}
```

```js
const square = document.querySelector("#square");
const playBtn = document.querySelector("#playBtn");
const pauseBtn = document.querySelector("#pauseBtn");

const animation = square.animate(
  [{ transform: "translateX(0px)" }, { transform: "translateX(200px)" }],
  {
    duration: 5000, // Animation lasts 5 seconds
    // iterations: Infinity, // Loops indefinitely
    direction: "alternate", // Moves back and forth
    easing: "ease-in-out" // Smooth easing function
  }
);

// Set the onfinish property to log a message when the animation ends
animation.onfinish = () => {
  console.log("Animation finished!");
};

// Play the animation when the "Play" button is clicked
playBtn.addEventListener("click", () => {
  animation.play();
  console.log("You start the animation");
});

// Pause the animation when the "Pause" button is clicked
pauseBtn.addEventListener("click", () => {
  animation.pause();
  console.log("You pause the animation");
});
```

:::

النتيجة في المتصفح ستُظهر المربع الأزرق يتحرك من اليسار إلى اليمين عند النقر على زر `play`.

توسع WAAPI قدرات تحريكات CSS من خلال توفير تحكم أكثر ديناميكية بالتحريكات داخل JavaScript.

مع تحريكات CSS، تعرف التحريكات بشكل إعلاني باستخدام خصائص مثل `animation-name`، `animation-duration`، و`animation-timing-function`. كما يمكنك فعل الشيء نفسه باستخدام طريقة `animate()` في WAAPI.

الفرق هو أنك تستطيع التحكم في التحريكات التي تنشئها باستخدام طريقة `animate()` بشكل أكثر مباشرة وديناميكية، أما مع تحريكات CSS فتحتاج إلى تعريف أنماط مخصصة وتشغيلها داخل ملف JavaScript الخاص بك.

تحريك CSS مثالي للتحريكات البسيطة والإعلانية التي تعمل تلقائيًا، مثل تأثيرات المرور بالمؤشر، الانتقالات، أو التحريكات التي لا تحتاج إلى تفاعل كبير بعد تشغيلها. إذا كان تحريكك يحتاج إلى الاستجابة لتفاعلات المستخدم مثل النقرات والتمرير، أو تريد أن يتمكن المستخدم من الإيقاف المؤقت، العكس، أو تغيير السرعة بشكل ديناميكي، فإن WAAPI هو الخيار الأفضل.

يمكنك التفكير في دمج كل من WAAPI وتحريكات CSS عندما تريد بساطة CSS للإعداد الأساسي ولكن تحتاج إلى التحكم في التحريكات أثناء التشغيل.

# --questions--

## --text--

ما هي الطريقة الأساسية في واجهة برمجة تطبيقات تحريك الويب (WAAPI) لإنشاء التحريكات؟

## --answers--

`setInterval()`

### --feedback--

فكر في الطريقة التي تسمح لك بتحديد الإطارات المفتاحية وخيارات مثل المدة.

---

`setTimeout()`

### --feedback--

فكر في الطريقة التي تسمح لك بتحديد الإطارات المفتاحية وخيارات مثل المدة.

---

`animate()`

---

`requestAnimationFrame()`

### --feedback--

فكر في الطريقة التي تسمح لك بتحديد الإطارات المفتاحية وخيارات مثل المدة.

## --video-solution--

3

## --text--
أي من هذه ليست طريقة من طرق دالة البناء `Animation` في واجهة برمجة تطبيقات تحريك الويب (WAAPI)؟

## --answers--

`play()`

### --feedback--

فكر في الطرق المستخدمة للتحكم في التحريكات.

---

`pause()`

### --feedback--

فكر في الطرق المستخدمة للتحكم في التحريكات.

---

`reverse()`

### --feedback--

فكر في الطرق المستخدمة للتحكم في التحريكات.

---

`stop()`

## --video-solution--

4

## --text--

متى يجب استخدام WAAPI بدلًا من تحريكات CSS؟

## --answers--

لتأثيرات المرور بالمؤشر البسيطة والتلقائية.

### --feedback--

فكر في الحالات التي تحتاج فيها إلى تحكم أكبر في التحريك.

---

عندما تحتاج أن تستجيب التحريكات لتفاعلات المستخدم مثل النقرات، التمرير، أو تسمح بالتحكم الديناميكي مثل الإيقاف المؤقت أو العكس.

---

عندما تحتاج فقط إلى انتقالات بدون تفاعل.

### --feedback--

فكر في الحالات التي تحتاج فيها إلى تحكم أكبر في التحريك.

---

للتحريكات التي لا تتطلب تفاعلًا كبيرًا بعد تشغيلها.

### --feedback--

فكر في الحالات التي تحتاج فيها إلى تحكم أكبر في التحريك.

## --video-solution--

2
