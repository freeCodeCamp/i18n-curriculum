---
id: 6a0dcd03ee4e68698080ef6d
title: "Завдання 309: сортування чисел"
challengeType: 29
dashedName: challenge-309
---

# --description--

Дано рядок чисел, розділених комами. Поверніть масив чисел, відсортований від найменшого до найбільшого.

# --hints--

`sort_numbers("3,1,2")` має повернути `[1, 2, 3]`.

```js
({test: () => { runPython(`
from unittest import TestCase
TestCase().assertEqual(sort_numbers("3,1,2"), [1, 2, 3])`)
}})
```

`sort_numbers("5,3,8,1,9,2")` має повернути `[1, 2, 3, 5, 8, 9]`.

```js
({test: () => { runPython(`
from unittest import TestCase
TestCase().assertEqual(sort_numbers("5,3,8,1,9,2"), [1, 2, 3, 5, 8, 9])`)
}})
```

`sort_numbers("12,61,49,80,19,50,77,38")` має повернути `[12, 19, 38, 49, 50, 61, 77, 80]`.

```js
({test: () => { runPython(`
from unittest import TestCase
TestCase().assertEqual(sort_numbers("12,61,49,80,19,50,77,38"), [12, 19, 38, 49, 50, 61, 77, 80])`)
}})
```

`sort_numbers("0,6,-19,44,-2,7,0")` має повернути `[-19, -2, 0, 0, 6, 7, 44]`.

```js
({test: () => { runPython(`
from unittest import TestCase
TestCase().assertEqual(sort_numbers("0,6,-19,44,-2,7,0"), [-19, -2, 0, 0, 6, 7, 44])`)
}})
```

# --seed--

## --seed-contents--

```py
def sort_numbers(s):

    return s
```

# --solutions--

```py
def sort_numbers(s):
    return sorted(int(x) for x in s.split(","))
```
