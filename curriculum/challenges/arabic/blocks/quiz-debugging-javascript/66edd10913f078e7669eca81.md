---
id: 66edd10913f078e7669eca81
title: اختبار تصحيح أخطاء JavaScript
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما نوع الخطأ الذي يحدث عند استخدام متغير قبل تعريفه؟

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

أي عبارة في JavaScript يمكن استخدامها لإيقاف تنفيذ الشفرة عمدًا لأغراض تصحيح الأخطاء؟

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

أي طريقة في وحدة التحكم يمكن استخدامها لعرض البيانات كجدول؟

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

كيف يمكنك فحص طلبات الشبكة في أدوات مطوِّر Chrome؟

#### --distractors--

باستخدام تبويب "روابط الشبكة".

---

باستخدام تبويب "وحدة التحكم".

---

باستخدام تبويب "المصادر".

#### --answer--

باستخدام تبويب "الشبكة".

### --question--

#### --text--

ما الصيغة النحوية الصحيحة لاستخدام `try...catch` في JavaScript؟

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

أي مما يلي مثال على `SyntaxError`؟

#### --distractors--

استخدام متغير قبل إعلانه.

---

استخدام متغير مرتين في نفس النطاق.

---

استخدام متغير خارج النطاق.

#### --answer--

فقدان فاصلة بين عناصر المصفوفة.

### --question--

#### --text--

ما الغرض من نقاط التوقف في تصحيح أخطاء JavaScript؟

#### --distractors--

تصحيح أخطاء الشفرة تلقائيًا.

---

منع تنفيذ الشفرة.

---

تسجيل الرسائل في وحدة التحكم.

#### --answer--

إيقاف التنفيذ عند نقطة محددة.

### --question--

#### --text--

ماذا يحدث عند تنفيذ عبارة `throw`؟

#### --distractors--

ينهي البرنامج فورًا متجاهلًا كل الشفرة المتبقية.

---

تسجل عبارة throw خطأ في وحدة التحكم لكنها لا توقف التنفيذ.

---

توقف تنفيذ الشفرة حتى يتم استئنافه بإلغاء نقاط التوقف.

#### --answer--

يُرمى استثناء ويتم تنفيذ كتلة `catch` (إذا كانت موجودة).

### --question--

#### --text--

أي طريقة تستخدم لفحص المتغيرات أو التعبيرات أثناء التصحيح في أدوات المطوِّر؟

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

ما نوع الخطأ الذي سيحدث عند تنفيذ الشفرة التالية؟

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

خطأ تجاوز السعة في المكدس

#### --answer--

`ReferenceError`

### --question--

#### --text--

أي أداة تُستخدم عادةً للتنقل عبر الشفرة سطرًا بسطر أثناء التصحيح؟

#### --distractors--

`console.log()`

---

`alert()`

---

عبارة `throw`.

#### --answer--

نقاط التوقف في أدوات المطوِّر.

### --question--

#### --text--

ما نوع الخطأ الذي يحدث عند محاولة الوصول إلى خاصية من `undefined`؟

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

ماذا تفعل كتلة `finally` في عبارة `try...catch`؟

#### --distractors--

تنفذ فقط إذا حدث خطأ.

---

تتخطى التنفيذ إذا تم تفعيل `catch`.

---

تخرج من كتلة الشفرة فورًا.

#### --answer--

تنفذ دائمًا، بغض النظر عن وجود أخطاء.

### --question--

#### --text--

ما نتيجة تشغيل الشفرة التالية؟

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

ستُرمى `SyntaxError` في الشفرة.

---

ستسجل الشفرة مصفوفة فارغة في وحدة التحكم.

---

ستُرمى الشفرة خطأ نفاد الذاكرة.

#### --answer--

ستُرمى الشفرة `RangeError`.

### --question--

#### --text--

أي من الإجابات التالية صحيحة عن عبارة `debugger` في JavaScript؟

#### --distractors--

عبارة `debugger` تُتجاهل دائمًا من قبل المتصفح.

---

عبارة `debugger` ترسل تقرير خطأ إلى الخادم.

---

عبارة `debugger` تعيد تحميل الصفحة دائمًا وتمسح وحدة التحكم.

#### --answer--

تُتجاهل عبارة `debugger` عندما لا تكون أدوات المطوِّر مفتوحة.

### --question--

#### --text--

ما الغرض الأساسي من "الملف الشخصي" (profiling) في JavaScript؟

#### --distractors--

كتابة اختبارات الوحدة لدوال JavaScript وإنشاء ملفات تعريف لتنفيذ الشفرة.

---

إيقاف تنفيذ الشفرة وإطلاق الموارد المحتجزة مثل الذاكرة ووحدة المعالجة المركزية.

---

تحويل شفرة JavaScript إلى شفرة آلة لتنفيذ أسرع.

#### --answer--

تحديد عنق الزجاجة في الأداء بتسجيل استخدام وحدة المعالجة المركزية، واستدعاءات الدوال، ووقت التنفيذ.

### --question--

#### --text--

ما هو "المراقب" (watcher) في سياق تصحيح الأخطاء؟

#### --distractors--

أداة لإيقاف تنفيذ الشفرة.

---

دالة مدمجة لتسجيل الأخطاء.

---

طريقة JavaScript لمعالجة الاستثناءات.

#### --answer--

أداة لمراقبة قيمة المتغيرات.

### --question--

#### --text--

متى تستخدم `console.error()` بدلًا من `console.log()`؟

#### --distractors--

لعرض بيانات الجدول.

---

لتسجيل الرسائل فقط أثناء التصحيح.

---

لتسجيل معلومات عن المتصفح.

#### --answer--

لتسجيل رسائل الخطأ.

### --question--

#### --text--

ماذا تفعل دالة البناء `Error()` في JavaScript؟

#### --distractors--

تسجل رسائل الخطأ في وحدة التحكم.

---

تشغل الشفرة في بيئة معزولة.

---

تنهي البرنامج.

#### --answer--

تنشئ كائن خطأ جديد يمكن رميه.

### --question--

#### --text--

ما الغرض من طريقة `console.dir()` في JavaScript؟

#### --distractors--

إنشاء دليل منفصل لملفات السجل لكل نوع خطأ.

---

إنشاء تمثيل بصري لمخرجات وحدة التحكم وحفظه في ملف pdf.

---

إيقاف تنفيذ البرنامج وعرض رسالة خطأ تتعلق باستخدام الذاكرة ووحدة المعالجة المركزية.

#### --answer--

عرض قائمة تفاعلية هرمية بالخصائص لكائن JavaScript محدد.

