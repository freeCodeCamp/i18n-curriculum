---
id: 6900a59dedf6c5c52824304f
title: Quiz de Introdução ao NodeJS
challengeType: 8
dashedName: quiz-node-js-intro
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 9 das 10 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Por que o JavaScript do lado do cliente tem acesso restrito ao sistema de arquivos local?

#### --distractors--

Porque JavaScript é uma linguagem de programação lenta.

---

Porque os navegadores não têm memória suficiente para lidar com arquivos.

---

Porque a API do DOM impede o acesso a arquivos.

#### --answer--

Porque foi projetado para rodar em navegadores web onde o acesso irrestrito a arquivos representaria riscos de segurança.

### --question--

#### --text--

O que significa que o Node.js é "cross-platform"?

#### --distractors--

Ele pode executar várias linguagens de programação simultaneamente.

---

Ele pode conectar diferentes bancos de dados juntos.

---

Requer métodos de instalação diferentes para cada sistema operacional.

#### --answer--

Funciona em qualquer sistema operacional, incluindo Windows, macOS e Linux.

### --question--

#### --text--

Qual API é acessível no ambiente do navegador mas NÃO no ambiente Node.js?

#### --distractors--

A API do Sistema de Arquivos

---

A API de Networking

---

A API do Sistema Operacional

#### --answer--

A API do DOM

### --question--

#### --text--

O que é `npm` no contexto do Node.js?

#### --distractors--

Um framework JavaScript para construir interfaces de usuário.

---

Um navegador web otimizado para aplicações Node.js.

---

Uma ferramenta de depuração para aplicações Node.js.

#### --answer--

Um gerenciador de pacotes para Node.js que permite instalar e gerenciar pacotes e módulos.

### --question--

#### --text--

O que a flag `--lts` indica ao executar `nvm install --lts`?

#### --distractors--

Ele instala a versão mais recente do snapshot de teste.

---

Ele instala a versão mais leve e menor disponível.

---

Ele instala a versão com mais recursos.

#### --answer--

Ele instala a versão Long-Term Support que prioriza estabilidade, confiabilidade e segurança.

### --question--

#### --text--

O que é um "callback" no contexto da programação assíncrona em Node.js?

#### --distractors--

Um método que reinicia uma operação falhada.

---

Uma variável que armazena o resultado de operações síncronas.

---

Uma ferramenta que monitora o desempenho da aplicação.

#### --answer--

Uma função que define o que acontece quando operações assíncronas são concluídas.

### --question--

#### --text--

Qual é o propósito do arquivo `package.json` criado pelo `npm init`?

#### --distractors--

Para armazenar variáveis de ambiente para a aplicação.

---

Para configurar as configurações do servidor web.

---

Para definir a estrutura HTML da aplicação.

#### --answer--

Para rastrear os detalhes e dependências do projeto.

### --question--

#### --text--

Por que tarefas intensivas em CPU podem ser problemáticas no Node.js?

#### --distractors--

Node.js não pode realizar operações matemáticas.

---

Node.js não suporta criptografia e processamento de imagens.

---

O event loop impede que operações da CPU sejam executadas.

#### --answer--

Eles podem bloquear a thread principal já que o Node.js roda em uma única thread, resultando em problemas de desempenho.

### --question--

#### --text--

Qual comando você usaria para executar um arquivo JavaScript chamado `server.js` usando Node.js?

#### --distractors--

`npm run server.js`

---

`nvm execute server.js`

---

`javascript server.js`

#### --answer--

`node server.js`

### --question--

#### --text--

O que os desenvolvedores devem verificar antes de escolher pacotes do npm?

#### --distractors--

Se o pacote tem pelo menos 1000 downloads.

---

Se o pacote foi criado recentemente.

---

Se o pacote requer pagamento.

#### --answer--

Se o pacote é mantido e segue as melhores práticas de qualidade e segurança.
