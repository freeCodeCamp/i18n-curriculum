---
id: 66f1b06a5a5d10cc100af620
title: Git 測驗
challengeType: 8
dashedName: quiz-git
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是 `git`？

#### --distractors--

一種流行的無障礙工具類型。

---

軟體專案中常用的 linter。

---

一種特殊型別的 JavaScript 函式庫。

#### --answer--

版本控制系統允許你追蹤和管理你專案中的變更。

### --question--

#### --text--

以下哪一個是熱門的雲端版本控制提供者？

#### --distractors--

Spring Boot

---

Laravel

---

混音

#### --answer--

GitHub

### --question--

#### --text--

`git config` 指令的作用是什麼？

#### --distractors--

設定用於重新基底你變更的組態變數。

---

設定用於管理 GitLab 上僅遠端儲存庫的組態。

---

設定用於解決合併衝突的組態。

#### --answer--

設定負責 Git 在你的機器上如何運作的組態變數。

### --question--

#### --text--

哪個指令會顯示你機器上目前安裝的 Git 版本？

#### --distractors--

`git --versions`

---

`git --setVersion`

---

`git --set`

#### --answer--

`git --version`

### --question--

#### --text--

以下哪一項是用來在不離開命令列的情況下執行 GitHub 特定任務的工具？

#### --distractors--

GitHub 持續整合/持續部署

---

GitHub Actions

---

GitHub 頁面

#### --answer--

GitHub CLI

### --question--

#### --text--

以下哪一項是讓你能直接在你的 GitHub 儲存庫中自動化工作流程的特性，包括建置、測試和部署你的程式碼？

#### --distractors--

GitHub 工作流程

---

GitHub 雲端

---

GitHub API

#### --answer--

GitHub Actions

### --question--

#### --text--

以下哪個指令會初始化一個空的 `git` 倉庫？

#### --distractors--

`git status`

---

`git set`

---

`git log`

#### --answer--

`git init`

### --question--

#### --text--

`git push` 指令的作用是什麼？

#### --distractors--

此指令用於將整個 Git 歷史推送到 GitHub。

---

此指令用於將變更從遠端儲存庫推送到你的區域的機器。

---

此指令用於將你的程式碼推送到 GitHub 並自動合併到 `main` 分支。

#### --answer--

此指令用於將你的區域的提交推送到遠端儲存庫。

### --question--

#### --text--

以下哪個指令用於複製儲存庫？

#### --distractors--

`git cloning`

---

`git cloned`

---

`git clones`

#### --answer--

`git clone`

### --question--

#### --text--

以下哪個指令用於列出所有先前的提交，並包含作者、提交日期、提交訊息和提交雜湊等有用資訊？

#### --distractors--

`git rebase`

---

`git status`

---

`git commit`

#### --answer--

`git log`

### --question--

#### --text--

下列哪個指令會列出所有與你的區域 `Git` 倉庫相關的遠端倉庫？

#### --distractors--

`git rebase -v`

---

`git pull -v`

---

`git checkout -v`

#### --answer--

`git remote -v`

### --question--

#### --text--

`git reset` 指令的作用是什麼？

#### --distractors--

此指令允許你重設所有解決合併衝突的嘗試。

---

此指令允許你重設所有對 `git rebase` 的嘗試。

---

此指令允許你重設該儲存庫的所有提交。

#### --answer--

此指令允許你重設分支的當前狀態。

### --question--

#### --text--

什麼是 pull request？

#### --distractors--

將一連串提交從一個分支合併到另一個分支的請求。

---

請求決議所有合併衝突。

---

請求從儲存庫中移除所有提交。

#### --answer--

從你的分支拉取變更到標的分支的請求。

### --question--

#### --text--

`main` 分支在專案中表述什麼？

#### --distractors--

它表述應用程式的主要文件分支。

---

它表述應用程式的 hotfix 分支。

---

它表述應用程式的主要特性分支。

#### --answer--

它表述應用程式的主要或生產分支。

### --question--

#### --text--

GitHub Pages 最適合用來做什麼？

#### --distractors--

最佳部署 C# 和 Unity 遊戲。

---

它最適合部署僅限 Python 的應用程式。

---

它最適合部署後端負載較重的網站。

#### --answer--

它最適合部署靜態網站。

### --question--

#### --text--

下列哪一個指令用於顯示你工作目錄的當前狀態？

#### --distractors--

`git show`

---

`git check`

---

`git update`

#### --answer--

`git status`

### --question--

#### --text--

你使用什麼指令從遠端儲存庫擷取變更？

#### --distractors--

`git get`

---

`git sync`

---

`git update`

#### --answer--

`git fetch`

### --question--

#### --text--

什麼是 `.gitignore` 檔案？

#### --distractors--

一種用來顯示你整個 `git` 歷史的特殊檔案型別。

---

一種用於顯示你變更差異的特殊檔案型別。

---

用於為專案決議衝突的特殊型別檔案。

#### --answer--

一種特殊型別的檔案，用來告訴 `git` 不要追蹤專案中某些檔案和資料夾。

### --question--

#### --text--

以下哪個指令用於列出你所有的區域的分支？

#### --distractors--

`git show`

---

`git log`

---

`git list`

#### --answer--

`git branch`

### --question--

#### --text--

以下哪個指令用於將一連串的提交從一個分支移動或合併到另一個分支？

#### --distractors--

`git remote`

---

`git merge`

---

`git switch`

#### --answer--

`git rebase`

