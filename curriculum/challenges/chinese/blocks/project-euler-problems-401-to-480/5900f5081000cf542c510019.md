---
id: 5900f5081000cf542c510019
title: '问题 411：上坡路径'
challengeType: 1
forumTopicId: 302080
dashedName: problem-411-uphill-paths
---

# --description--

设 $n$ 为正整数。假设在坐标 $(x, y) = (2^i\bmod n, 3^i\bmod n)$ 处有车站，其中 $0 ≤ i ≤ 2n$。我们将坐标相同的车站视为同一车站。

我们希望从 (0, 0) 形成一条 `path` 到 ($n$, $n$)，使得 $x$ 和 $y$ 坐标永不减少。

设 $S(n)$ 为这样的路径可以经过的最大车站数。

例如，如果 $n = 22$，则有 11 个不同的站点，有效的路径最多可以经过 5 个站点。因此，$S(22) = 5$。该分支如下所示，并附有一个最优路径的示例：

<img alt="valid path passing through 5 stations, for n = 22, with 11 distinct stations" src="https://cdn.freecodecamp.org/curriculum/project-euler/uphill-paths.png" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

还可以验证 $S(123) = 14$ 和 $S(10\\,000) = 48$。

查找 $\sum S(k^5)$，其中 $1 ≤ k ≤ 30$。

# --hints--

`uphillPaths()` 应该返回 `9936352`。

```js
assert.strictEqual(uphillPaths(), 9936352);
```

# --seed--

## --seed-contents--

```js
function uphillPaths() {

  return true;
}

uphillPaths();
```

# --solutions--

```js
// solution required
```
