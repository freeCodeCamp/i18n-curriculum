---
id: 66ed902df45ce3ece4053ebc
title: Тест «Таблиці та форми в HTML»
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

Вам потрібно правильно відповісти на принаймні 18 запитань із 20, щоб скласти тест.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який атрибут вказує посилання, на яке буде надіслано дані форми?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

Які два елементи використовують, щоб визначити рядок і його заголовок в таблиці?

#### --distractors--

Елемент `r` використовують для рядків таблиці, а `thead` — для заголовка.

---

Елемент `row` використовують для рядків таблиці, а `th` — для заголовка.

---

Елемент `tr` використовують для рядків таблиці, а `head` — для заголовка.

#### --answer--

Елемент `tr` використовують для рядків таблиці, а `th` — для заголовка.

### --question--

#### --text--

Для чого використовують елемент `td`?

#### --distractors--

Щоб визначити тип даних в таблиці.

---

Щоб об’єднати два стовпці.

---

Щоб об’єднати два рядки.

#### --answer--

Щоб визначити комірку в таблиці.

### --question--

#### --text--

Яке завдання атрибута `colspan`?

#### --distractors--

Визначати довжину стовпця в таблиці.

---

Видаляти розриви між текстом у стовпці.

---

Визначати кількість стовпців.

#### --answer--

Визначати кількість стовпців, які має займати комірка.

### --question--

#### --text--

Яке завдання атрибута `placeholder`?

#### --distractors--

Відправляти дані на сервер, якщо форма надіслана.

---

З’єднувати елемент `label` з елементом `input`.

---

Вказувати, що текстове поле доступне для читання лише користувачу.

#### --answer--

Надавати підказки в текстовому полі.

### --question--

#### --text--

Яке головне завдання елемента `thead`?

#### --distractors--

Групувати вміст тіла HTML-таблиці.

---

Групувати опис HTML-таблиці.

---

Групувати підсумкову частину HTML-таблиці.

#### --answer--

Групувати заголовки HTML-таблиці.

### --question--

#### --text--

Що є прикладом явного зв’язку між міткою і текстовим полем?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

Яке визначення найкраще описує валідатор HTML?

#### --distractors--

Валідатор — це інструмент, який автоматично форматує HTML.

---

Валідатор — це інструмент, який застосовує стилі до HTML.

---

Валідатор — це інструмент, який пришвидшує роботу HTML.

#### --answer--

Валідатор — це інструмент, який перевіряє синтаксис HTML, щоб переконатися в його валідності.

### --question--

#### --text--

Який елемент визначає комірку в таблиці?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

Який з наведених інструментів дозволяє переглядати та змінювати HTML-структуру вебсторінки?

#### --distractors--

DOM removal.

---

DOM tester.

---

DOM validation.

#### --answer--

DOM inspector.

### --question--

#### --text--

Як правильно згрупувати пов’язані текстові поля?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

Який атрибут вказує, що текстове поле доступне лише для читання?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

Який атрибут вказує, що поле обов’язково потрібно заповнити перед надсиланням форми?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

Яке визначення найкраще описує інструменти розробника?

#### --distractors--

Інструменти, які вбудовані безпосередньо в браузер та застосовуються для виявлення проблем у коді.

---

Інструменти, які вбудовані безпосередньо в браузер та застосовуються для автоматичного форматування коду.

---

Інструменти, які вбудовані безпосередньо в браузер та застосовуються для 100% тестування коду.

#### --answer--

Інструменти, які вбудовані безпосередньо в браузер та застосовуються для налагодження, профілювання і аналізу вебсторінок.

### --question--

#### --text--

Як зробити текстове поле неактивним?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

Яке зі значень є дійсним для атрибута `type`?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

Де правильно використано атрибут `size`?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

Які атрибути вказують мінімальну і максимальну кількість символів, що можна ввести в текстовому полі?

#### --distractors--

`minlen` та `maxlen`

---

`min` та `max`

---

`minimumLen` та `maximumLen`

#### --answer--

`minlength` та `maxlength`

### --question--

#### --text--

У якому з прикладів використано **неправильне** значення атрибута `type`?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

Який атрибут вказує значення кнопки?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

Яке завдання атрибута `action` у початковому тегу `form`?

#### --distractors--

Вказувати, що текстове поле доступне лише для читання.

---

Вказувати кількість символів, які мають бути видимими під час введення даних.

---

Підказувати користувачу інформацію, яку потрібно ввести в текстовому полі.

#### --answer--

Вказувати посилання, на яке буде надіслано дані форми.

### --question--

#### --text--

Який атрибут вказує HTTP-метод, що потрібно використати під час надсилання даних форми?

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

Який з методів часто використовується при роботі з формами?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

Яке зі значень НЕ є дійсним для атрибута `type`?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

Як правильно створити кнопку у формі?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

Яке визначення описує неявний зв’язок елементів форми?

#### --distractors--

Текстове поле пов’язане з міткою, оскільки до елемента `label` застосовано атрибути `action` та `for`.

---

Текстове поле пов’язане з міткою, оскільки до елемента `label` застосовано атрибути `required` та `for`.

---

Текстове поле пов’язане з міткою, оскільки до елемента `label` застосовано атрибут `for`.

#### --answer--

Текстове поле пов’язане з міткою, оскільки текстове поле вкладено в елементі `label`.

### --question--

#### --text--

Яке головне завдання елемента `fieldset`?

#### --distractors--

Групувати заголовки HTML-таблиці.

---

Створювати мітку для текстового поля.

---

Додавати заголовок, який описує групу елементів форми.

#### --answer--

Групувати пов’язані текстові поля.

### --question--

#### --text--

Як правильно створити прапорець у формі?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

Який з наведених елементів використовують, щоб додати заголовок HTML-таблиці?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

Яке головне завдання елемента `legend`?

#### --distractors--

Групувати вміст тіла HTML-таблиці.

---

Додавати заголовок, який описує комірки таблиці.

---

Групувати пов’язані текстові поля.

#### --answer--

Додавати заголовок, який описує групу елементів форми.

### --question--

#### --text--

Який елемент групує заголовки HTML-таблиці?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

Як правильно додати рядки і комірки з даними до таблиці?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

Що таке інспектор DOM?

#### --distractors--

Інструмент, який використовується для вилучення всіх елементів з DOM.

---

Набір інструментів, вбудованих безпосередньо в браузер, що допомагає налагоджувати, профілювати і аналізувати вебсторінки.

---

Інструмент, який перевіряє синтаксис HTML-коду, щоб переконатися в його валідності.

#### --answer--

Інструмент, який дозволяє переглядати та змінювати HTML-структуру вебсторінки.

### --question--

#### --text--

Який інструмент перевіряє синтаксис HTML-коду, щоб переконатися в його правильності?

#### --distractors--

HTML validate

---

HTML isValid

---

HTML valid

#### --answer--

HTML validator

### --question--

#### --text--

Як правильно вказати кількість стовпців комірки в таблиці?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

Що таке сфокусований стан текстового поля?

#### --distractors--

Стан текстового поля, коли фокус видаляється користувачем.

---

Стан текстового поля, коли користувач робить його доступним лише для читання.

---

Стан текстового поля, коли користувач робить його неактивним.

#### --answer--

Стан текстового поля, коли користувач вибирає його.

### --question--

#### --text--

Як правильно створити радіокнопку?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

Яке завдання атрибута `for`?

#### --distractors--

Надавати підказку користувачу.

---

Вказувати значення вхідних даних.

---

Створювати мітку для текстового поля.

#### --answer--

Вказувати, якого поля стосується мітка.

### --question--

#### --text--

Який атрибут вказує кількість символів, які мають бути видимими під час введення даних в текстовому полі?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

Який атрибут підказує користувачу інформацію, яку потрібно ввести в текстовому полі?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
