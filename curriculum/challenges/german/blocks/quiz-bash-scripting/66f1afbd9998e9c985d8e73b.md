---
id: 66f1afbd9998e9c985d8e73b
title: Bash Scripting Quiz
challengeType: 8
dashedName: quiz-bash-scripting
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist eine korrekte Shebang-Zeile in einem Bash-Skript?

#### --distractors--

`!#/bin/bash`

---

`/#!bin/bash`

---

`/#!/bin/bash`

#### --answer--

`#!/bin/bash`

### --question--

#### --text--

Welcher Befehl würde `MY_VAR` definieren und in der Konsole ausgeben?

#### --distractors--

`MY_VAR=10; echo MY_VAR`

---

`$MY_VAR=10; echo MY_VAR`

---

`$MY_VAR=10; echo $MY_VAR`

#### --answer--

`MY_VAR=10; echo $MY_VAR`

### --question--

#### --text--

Welcher Befehl wird verwendet, um alle Variablen in der Shell anzuzeigen?

#### --distractors--

`declare -n env`

---

`declare -p env`

---

`declare -x bashrc`

#### --answer--

`declare -p`

### --question--

#### --text--

Welcher Befehl würde eine `script.sh`-Datei ausführen?

#### --distractors--

`script.sh bash`

---

`zsh ./script.sh`

---

`./sh script.sh`

#### --answer--

`./script.sh`

### --question--

#### --text--

Wie zeigt man das Handbuch für den `less`-Befehl in Bash an?

#### --distractors--

`less man`

---

`less --man`

---

`man --less`

#### --answer--

`man less`

### --question--

#### --text--

Wie würden Sie die `print_arguments`-Funktion aufrufen, um `a` und `b` im Terminal auszugeben?

```bash
print_arguments() {
  echo "Argument 1: $1"
  echo "Argument 2: $2"
}

```

#### --distractors--

`print_arguments("a", "b")`

---

`print_arguments(a, b)`

---

`print_arguments{a, b}`

#### --answer--

`print_arguments "a" "b"`

### --question--

#### --text--

Wie initialisieren Sie ein Array in Bash?

#### --distractors--

`ARR=["item1" "item2" "item3"]`

---

`ARR=[["item1" "item2" "item3"]]`

---

`ARR={"item1" "item2" "item3"}`

#### --answer--

`ARR=("item1" "item2" "item3")`

### --question--

#### --text--

Wie geben Sie den dritten Wert eines Arrays in Bash aus?

#### --distractors--

`echo ${ARR[3]}`

---

`echo $ARR[2]`

---

`echo $ARR[3]`

#### --answer--

`echo ${ARR[2]}`

### --question--

#### --text--

Wie weisen Sie Benutzereingaben einer Variablen in Bash zu?

#### --distractors--

`input MY_VAR`

---

`cin MY_VAR`

---

`get MY_VAR`

#### --answer--

`read MY_VAR`

### --question--

#### --text--

Welche Berechtigungsflags hat die `1.txt`-Datei nach Ausführung des Befehls `chmod 765 1.txt`?

#### --distractors--

`rwxrwxrwx`

---

`rwxr-xrw-`

---

`r-xrw-rwx`

#### --answer--

`rwxrw-r-x`

### --question--

#### --text--

Mit welchem Schlüsselwort endet die Anweisung `if ... then ...`?

#### --distractors--

`end`

---

`done`

---

`stop`

#### --answer--

`fi`

### --question--

#### --text--

Mit welchem Schlüsselwort endet die Anweisung `for ... do ...`?

#### --distractors--

`end`

---

`od`

---

`stop`

#### --answer--

`done`

### --question--

#### --text--

Wie erstellen Sie einen Kommentar in Bash?

#### --distractors--

`// comment`

---

`!comment`

---

`/* comment`

#### --answer--

`# comment`

### --question--

#### --text--

Welche Zeile sollten Sie am Anfang dieses Ausschnitts hinzufügen, um eine `for`-Schleife zu erstellen?

```bash

do
    echo $i
done
```

#### --distractors--

`for ( int i = 0; i < 10; i++ )`

---

`for (( int i = 0; i < 10; i++ ))`

---

`for (( let i = 0; i < 10; i++ ))`

#### --answer--

`for (( i = 0; i < 10; i++ ))`

### --question--

#### --text--

Wie würden Sie das Ergebnis der Summe der Variablen `A` und `B` der Variablen `C` zuweisen?

#### --distractors--

`C=$A + $B`

---

`C=$( A + B )`

---

`$C=$(( $A + $B ))`

#### --answer--

`C=$(( A + B ))`

### --question--

#### --text--

Welcher Befehl wird verwendet, um eine neue Datei zu erstellen?

#### --distractors--

`newfile`

---

`echo new_file`

---

`fileargs`

#### --answer--

`touch`

### --question--

#### --text--

Was bewirkt der Befehl `echo $?`?

#### --distractors--

Gibt den Wert der zuletzt verwendeten Variablen aus.

---

Gibt den Wert der zuletzt verwendeten arithmetischen Operation aus.

---

Gibt den Wert der zuletzt ausgeführten Bedingung aus.

#### --answer--

Gibt den Exit-Code des zuletzt ausgeführten Befehls aus.

### --question--

#### --text--

Welchen Vorteil bietet Bash beim Testen von Teilen eines Skripts?

#### --distractors--

Bash-Skripte müssen vor dem Testen kompiliert werden.

---

Sie können Skripte nur mit einer IDE testen.

---

Bash generiert automatisch Testfälle.

#### --answer--

Skriptabschnitte können direkt in das Terminal eingefügt werden, um sie zu testen.

### --question--

#### --text--

Welche Arten von Schleifen gibt es in Bash?

#### --distractors--

`for`

---

`for`, `while`

---

`for`, `while`, `until`, `forEach`

#### --answer--

`for`, `while`, `until`

### --question--

#### --text--

Wie geben Sie alle Werte eines Arrays in Bash aus?

#### --distractors--

`echo $ARR[@]`

---

`echo {ARR[@]}`

---

`echo ${ARR[$@]}`

#### --answer--

`echo ${ARR[@]}`

