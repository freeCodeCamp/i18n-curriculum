---
id: 66ed9018f45ce3ece4053eb9
title: CSS 變數測驗
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一項不是 `@property` 規則中有效的描述符？

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

在以下程式碼中，如果未定義 `--main-color`，`color` 將會有什麼值？

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

不會套用任何顏色。

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

以下哪一項是在 CSS 中宣告訂製屬性的有效方式？

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

在 CSS 中使用訂製屬性的主要目的是什麼？

#### --distractors--

變更 DOM 結構。

---

為了創建動態選擇器。

---

為了改善存取性。

#### --answer--

定義可重複使用的樣式。

### --question--

#### --text--

`:root` 選擇器表述什麼？

#### --distractors--

`body` 的第一個子元素。

---

所有 `header` 元素的父元素。

---

`html` 頁籤的第一個子元素。

#### --answer--

DOM 樹中最高層級的元素。

### --question--

#### --text--

你如何將 `--foreground` 訂製屬性套用為 CSS 中的 `color`？

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

`@property` 規則在 CSS 中的目的為何？

#### --distractors--

定義媒體查詢。

---

控制 CSS 動畫及其時間。

---

將 CSS 屬性群組到一個規則中。

#### --answer--

定義訂製屬性的行為方式。

### --question--

#### --text--

當宣告訂製的 `@property` 時，`syntax` 在其定義中的目的是什麼？

#### --distractors--

它指定自定義屬性的預設值。

---

它決定該屬性是否可以被子元素繼承。

---

它定義該屬性是否套用到所有元素。

#### --answer--

它強制訂製屬性符合特定的資料型別或值樣式。

### --question--

#### --text--

使用訂製屬性時你應該注意什麼？

#### --distractors--

訂製屬性會自動轉換為 `px` 單位。

---

訂製屬性總是會覆寫行內樣式。

---

訂製屬性無法用來設定字型值。

#### --answer--

舊版瀏覽器可能不支援訂製屬性。

### --question--

#### --text--

在 CSS 訂製屬性的 `var()` 函式中提供備用值的目的為何？

#### --distractors--

為了減少 CSS 程式碼的量。

---

為了提升現代瀏覽器中的效率。

---

為了在慢速網路上最佳化呈現時間。

#### --answer--

為確保在訂製屬性未定義時套用有效值。

### --question--

#### --text--

當在媒體查詢中定義變數 `--foreground`，且媒體查詢不再符合目前的視窗大小時，會發生什麼情況？

#### --distractors--

訂製屬性會被保留並繼續套用。

---

訂製屬性會還原為其初始值。

---

訂製屬性是根據視窗重新計算的。

#### --answer--

訂製屬性不再可用，因為它的作用域限定於媒體查詢。

### --question--

#### --text--

以下的宣告會如何運作？

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

即使未定義，也會套用 `--main-color`，並使用該預設值。

---

它將同時套用 `--main-color` 和 `--fallback-color`，產生兩種顏色的混合效果。

---

無論是否定義了 `--main-color` 或 `--fallback-color`，它都會始終預設為 `#000`。

#### --answer--

如果定義了 `--main-color`，則使用它；否則，檢查 `--fallback-color`；如果兩者皆未定義，則使用 `#000`。

### --question--

#### --text--

使用 `@property` 在 CSS 中的其中一個利益是什麼？

#### --distractors--

它透過預先計算訂製屬性值來提升效率。

---

它會自動為訂製屬性加上前置式，以提升瀏覽器支援度。

---

它限制了訂製屬性只能用於特定元素。

#### --answer--

它允許對訂製屬性進行動畫處理。

### --question--

#### --text--

`inherits` 屬性在訂製 `@property` 宣告中控制什麼？

#### --distractors--

訂製屬性是否會有初始值。

---

該屬性是否可以有縮寫版本。

---

該屬性是否接受備用值。

#### --answer--

是否將訂製屬性的值傳遞給子元素。

### --question--

#### --text--

在訂製 `@property` 的宣告中，`initial-value` 指定了什麼？

#### --distractors--

該屬性可接受的值。

---

該屬性在層疊中的優先權。

---

該屬性必須具有的值的型別。

#### --answer--

該屬性的備用值。

### --question--

#### --text--

給定以下 HTML 和 CSS 程式碼，`.box` 元素的 `color` 屬性值會是什麼？

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

CSS 漸層應該套用到哪個屬性？

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

CSS 漸層中顏色停點的目的是什麼？

#### --distractors--

定義漸層的透明度層次。

---

指定漸層的方向。

---

以固定樣式重複漸層。

#### --answer--

定義漸層中顏色變化的具體點。

### --question--

#### --text--

如果在 CSS 線性漸層中未指定角度或方向，會發生什麼情況？

#### --distractors--

漸層的預設方向為對角線。

---

漸層的預設值是從下方移動到上方。

---

漸層預設值為從左到右移動。

#### --answer--

漸層預設值為從上到下移動。

### --question--

#### --text--

哪一個 CSS 漸層函式（程式）允許你創建從中心點向外輻射的漸層？

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

以下哪一項是宣告 CSS 訂製屬性的正確方式？

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

通常使用什麼選擇器來定義全域的 CSS 訂製屬性？

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

當使用 `var()` 時，為什麼建議包含備用值？

#### --distractors--

它確保變數會正確地動畫化。

---

它避免載入外部樣式表。

---

它防止瀏覽器重新排版。

#### --answer--

如果訂製屬性未定義，它保證有一個有效值。

### --question--

#### --text--

將訂製屬性套用為背景顏色的正確語法是什麼？

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

哪一個 CSS 規則允許開發者以更高的控制元件定義訂製屬性？

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

`@property` 定義中的 `inherits` 欄控制什麼？

#### --distractors--

該屬性是否在 JavaScript 中使用。

---

該屬性是否會觸發重繪。

---

該屬性是否可以包含函式（程式）。

#### --answer--

該屬性的值是否傳遞給子元素。

### --question--

#### --text--

當使用 `@property` 定義動畫漸層角度時，應該使用哪種語法？

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

CSS 訂製屬性與標準 CSS 屬性在定義方式上有何不同？

#### --distractors--

標準 CSS 屬性使用單一連字號定義，而訂製屬性使用兩個連字號。

---

標準 CSS 屬性使用 `var()` 來定義，而訂製屬性則不使用。

---

標準 CSS 屬性是使用 `@property` 規則定義的，而訂製屬性則不是。

#### --answer--

訂製屬性必須以兩個破折號（`--`）開頭，而標準 CSS 屬性則不需要。

### --question--

#### --text--

使用 CSS 訂製屬性的利益是什麼？

#### --distractors--

它們減少了對 CSS 註解的需求。

---

它們會自動最佳化影像。

---

它們使 JavaScript 變數過時。

#### --answer--

它們允許樣式被重複使用並集中維護。

### --question--

#### --text--

哪一個 CSS 屬性可以根據媒體查詢使用訂製屬性動態變化？

#### --distractors--

只有 `background-color`

---

只有 `font-family`

---

只有 `z-index`

#### --answer--

任何接受值的屬性。

### --question--

#### --text--

在 CSS 變數的背景關係中，`@property` 規則中 `initial-value` 的角色是什麼？

#### --distractors--

它設定該屬性的最小值。

---

它定義動畫的最大值。

---

它會改變選擇器的優先權。

#### --answer--

如果沒有設值，則為該屬性指定預設值。

### --question--

#### --text--

請考慮以下的 HTML 和 CSS。將會套用到 `.card` 元素的背景顏色是什麼？

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

在以下的 CSS 中，如果使用者嘗試為 `--padding` 指定無效的值（例如，顏色而非長度），會發生什麼情況？

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

瀏覽器接受該值但在佈局期間忽略它。

---

瀏覽器發出執行期錯誤。

---

瀏覽器會自動將該值轉換為有效長度。

#### --answer--

瀏覽器會回退到該屬性的初始值。

### --question--

#### --text--

在此範例中，`--gradient-angle` 訂製屬性的用途是什麼？

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

它定義了漸層過渡的速度。

---

它指定漸層的大小。

---

它設定顏色混合模式。

#### --answer--

它控制漸層的方向。

### --question--

#### --text--

哪種情境最能顯示使用訂製屬性於主題的優勢？

#### --distractors--

僅將它們用於字型大小。

---

只在 `:hover` 上套用它們。

---

將它們限制為一個 CSS 類別。

#### --answer--

透過類別變更值。

### --question--

#### --text--

為什麼開發者可能偏好使用 `:root` 來定義 CSS 變數？

#### --distractors--

因為 `:root` 改善了頁面載入速度。

---

因為 `:root` 會停用特異性。

---

因為 `:root` 是 JavaScript 所必須的。

#### --answer--

因為 `:root` 允許屬性具有全域的範圍。

### --question--

#### --text--

如果 `--secondary-color` 未定義，以下 CSS 會做什麼？

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

它將套用 `black`。

---

它會忽略 `color` 屬性。

---

它將套用 `--secondary-color`。

#### --answer--

它將套用 `orange`。

### --question--

#### --text--

`@property` 語法欄中的 `<color>` 表述什麼型別的值？

#### --distractors--

像 `90deg` 這樣的角度。

---

像 `10px` 這樣的長度。

---

百分比如 `50%`。

#### --answer--

像 `#ff0000` 或 `red` 這樣的顏色值。

### --question--

#### --text--

使用 `@property` 結合訂製屬性的主要利益是什麼？

#### --distractors--

它們避免了需要備用值。

---

它們強制靜態佈局。

---

它們減少了對類別的需求。

#### --answer--

它們允許屬性動畫。

### --question--

#### --text--

當訂製屬性未定義時，`var(--undefined-property, fallback)` 會做什麼？

#### --distractors--

發出錯誤並停止套用樣式。

---

忽略整個 CSS 規則。

---

套用變數名稱原樣。

#### --answer--

改用備用值。
