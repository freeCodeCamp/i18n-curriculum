---
id: 67358be1c7903489c0a7db78
title: Cuestionario sobre Maps y Sets de JavaScript
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

Para aprobar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué es un Set en JavaScript?

#### --distractors--

Una estructura de datos que permite valores duplicados.

---

Una estructura de datos que solo almacena cadenas y números.

---

Una colección que no permite la eliminación de sus elementos.

#### --answer--

Una colección en la que un valor solo puede ocurrir una vez.

### --question--

#### --text--

¿Cuál de los siguientes métodos está disponible en Maps pero no en Sets?

#### --distractors--

`has()`.

---

`clear()`.

---

`values()`.

#### --answer--

`set()`.

### --question--

#### --text--

¿Qué método se usa para agregar elementos a un Set?

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

¿Cómo se verifica si un Set contiene un determinado valor?

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

¿Cuál de estos es un gran caso de uso para los Sets?

#### --distractors--

Gestionar pares clave-valor en una base de datos.

---

Crear listas ordenadas de ítems.

---

Almacenar estructuras de datos jerárquicas en el almacenamiento local del navegador.

#### --answer--

Eliminar duplicados de un array.

### --question--

#### --text--

¿Qué es un WeakSet en JavaScript?

#### --distractors--

Una colección similar a Set que solo permite tipos de datos primitivos.

---

Una colección similar a Set que permite valores duplicados.

---

Una colección similar a Set que evita que se eliminen los valores.

#### --answer--

Una colección similar a Set que solo almacena objetos.

### --question--

#### --text--

¿Cuál es una diferencia entre un Set y un WeakSet?

#### --distractors--

Un WeakSet puede almacenar cualquier tipo de valor, incluidos números.

---

Un Set no permite valores duplicados, pero un WeakSet sí.

---

Un Set permite la recolección de basura, mientras que un WeakSet no.

#### --answer--

Un Set puede contener cualquier tipo de valor, mientras que un WeakSet solo objetos.

### --question--

#### --text--

¿Qué es un Map en JavaScript?

#### --distractors--

Una colección que solo permite valores únicos.

---

Una colección que solo puede usar cadenas como claves.

---

Una colección que no permite la eliminación de sus ítems.

#### --answer--

Una colección de pares clave-valor de cualquier tipo.

### --question--

#### --text--

¿Qué método se usa para insertar pares clave-valor en un Map?

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

¿Cuál será la salida del siguiente código?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

¿Cómo difiere un Map de un Objeto en JavaScript?

#### --distractors--

Un Map solo permite números como claves.

---

Los objetos permiten cualquier tipo como claves, mientras que los Maps solo permiten cadenas.

---

Los mapas no admiten iteración, mientras que los objetos sí.

#### --answer--

Los mapas permiten cualquier tipo de clave, pero los objetos solo permiten cadenas y símbolos.

### --question--

#### --text--

¿Cómo puede eliminar un par clave-valor de un Map?

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

¿Qué devuelve la propiedad `size` de un Map?

#### --distractors--

El número de índices en el Map.

---

El número de valores únicos en el Map.

---

La memoria total utilizada por el Map.

#### --answer--

El número de pares clave-valor en el Map.

### --question--

#### --text--

¿Qué es un WeakMap en JavaScript?

#### --distractors--

Una colección similar a Map que permite valores primitivos como claves.

---

Una colección similar a Map que bloquea la eliminación de elementos.

---

Una colección similar a Map que ordena automáticamente sus claves.

#### --answer--

Una colección similar a Map con referencias débiles a las claves.

### --question--

#### --text--

¿Cuál es una diferencia entre un Map y un WeakMap?

#### --distractors--

Un Map solo permite tipos primitivos como claves, mientras que un WeakMap solo permite objetos.

---

Un Map permite la recolección de basura, mientras que un WeakMap no.

---

Un WeakMap permite claves duplicadas, mientras que un Map no.

#### --answer--

Un Map es iterable, mientras que un WeakMap no lo es.

### --question--

#### --text--

¿Cuál de los siguientes métodos NO existe en un WeakMap?

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

¿Cuál será la salida del siguiente código?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

¿Cómo se puede iterar sobre los valores en un Set?

#### --distractors--

Usando el método `.map()`.

---

Usando el método `.reduce()`.

---

Usando el método `.filter()`.

#### --answer--

Usando el método `.forEach()`.

### --question--

#### --text--

¿Qué dos métodos de Sets devuelven un `SetIterator` que contiene los valores de un determinado Set?

#### --distractors--

`entries()` y `values()`.

---

`forEach()` y `values()`.

---

`has()` y `add()`.

#### --answer--

`keys()` y `values()`.

### --question--

#### --text--

¿Cuál será la salida del siguiente código?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
