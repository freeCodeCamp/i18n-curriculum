---
id: 66edcd0ecb4b25dc64a34804
title: Тест з об’єктів JavaScript
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що з наведеного НЕ описує об’єкт у JavaScript?

#### --distractors--

Колекція пар ключ-значення.

---

Контейнер для властивостей і методів.

---

Непримітивний тип даних.

#### --answer--

Примітивний тип даних.

### --question--

#### --text--

Яке з наведених визначень об’єкта є недійсним?

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

Як можна отримати доступ до властивості `job` цього об’єкта `person`?

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

Як можна отримати доступ до властивості `street` об’єкта `person` нижче?

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

До яких ключів властивостей можна отримати доступ лише за допомогою дужкової нотації?

#### --distractors--

Ключ властивості, який відомий і статичний.

---

Ключ властивості, який вкладений у кілька рівнів об’єкта.

---

Ключ властивості, який може не існувати в об’єкті.

#### --answer--

Ключ властивості, який є динамічним.

### --question--

#### --text--

Яке з наведених правильно використовує деструктуризацію для доступу до властивості `name` об’єкта `person` нижче?

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

Яке з наведених правильно використовує деструктуризацію для доступу до властивості `flour` об’єкта `recipe` нижче?

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

Як можна призначити значення за замовчуванням для ключа властивості під час деструктуризації в JavaScript?

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

Як перевірити, чи має об’єкт `car` властивість `year`?

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

Що таке метод об’єкта?

#### --distractors--

Рядок, пов’язаний з об’єктом.

---

Масив, пов’язаний з об’єктом.

---

Число, пов’язане з об’єктом.

#### --answer--

Функція, пов’язана з об’єктом.

### --question--

#### --text--

Як викликати метод `add` цього об’єкта `calculator`, щоб додати числа `10` і `7`?

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

Який оператор видаляє властивість з об’єкта?

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

Як перевірити, чи існує властивість `score` в об’єкті `player`?

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

Як створити новий порожній об’єкт у JavaScript?

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

Який правильний синтаксис для використання опціонального ланцюжка в JavaScript?

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

Для чого використовується опціональний ланцюжок у JavaScript?

#### --distractors--

Використовується для визначення змінних зі значеннями за замовчуванням.

---

Використовується для визначення опціональних методів у об’єкті, які можуть бути викликані або ні.

---

Використовується для написання більш лаконічного коду шляхом ланцюжка кількох викликів методів в одній інструкції.

#### --answer--

Використовується для безпечного доступу до властивостей об’єкта, які можуть бути `null` або `undefined`.

### --question--

#### --text--

Який метод використовується для перетворення об’єкта JavaScript у рядок JSON?

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

Який метод перетворює рядок JSON назад в об’єкт JavaScript?

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

Який із варіантів отримує доступ до властивості `color` об’єкта `toy` за допомогою дужкової нотації?

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

Як можна отримати доступ до телефону `home` цього об’єкта `learner` у JavaScript, використовуючи точкову нотацію?

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`
