---
id: 66f1ae758b77cfc3e4da6151
title: TypeScript-Quiz
challengeType: 8
dashedName: quiz-typescript
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 folgenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist TypeScript?

#### --distractors--

Ein Framework, das für Systemprogrammierung verwendet wird.

---

Ein Werkzeug zur Verwaltung von Datenbanken.

---

Eine Programmiersprache, die zum Stylen von Websites verwendet wird.

#### --answer--

Eine Obermenge von JavaScript, die statische Typisierung hinzufügt.

### --question--

#### --text--

Warum sollte TypeScript in einem Projekt verwendet werden?

#### --distractors--

Es macht Tests in Anwendungen überflüssig.

---

Es ist schneller als alle anderen Programmiersprachen.

---

Es erleichtert das Stylen von Websites.

#### --answer--

Es verbessert die Wartbarkeit, indem es Typdefinitionen zu JavaScript hinzufügt.

### --question--

#### --text--

In welche Sprache wird TypeScript kompiliert?

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

Was wäre die Ausgabe des folgenden Code-Ausschnitts?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

Was ist der Hauptzweck eines Interfaces in TypeScript?

#### --distractors--

TypeScript-Code in JavaScript zu kompilieren.

---

Einen neuen Datentyp zu erstellen.

---

Ein neues JavaScript-Objekt zu erstellen.

#### --answer--

Die Struktur eines Objekts zu definieren.

### --question--

#### --text--

Welche der folgenden Optionen ist KEIN grundlegender primitiver Typ in TypeScript?

#### --distractors--

string

---

boolean

---

number

#### --answer--

array

### --question--

#### --text--

Welche der folgenden Definitionen beschreibt korrekt einen Literaltyp in TypeScript?

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

Was ist der Hauptzweck des Schlüsselworts `type` in TypeScript?

#### --distractors--

Variablen zu deklarieren.

---

Klassen zu erstellen.

---

Interfaces zu implementieren.

#### --answer--

Benutzerdefinierte Typen zu definieren.

### --question--

#### --text--

Welche der folgenden Definitionen beschreibt korrekt einen Union-Typ?

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

Welche der folgenden Fähigkeiten ist einzigartig für `interface` und KANN NICHT mit `type` in TypeScript erreicht werden?

#### --distractors--

Objektformen zu definieren.

---

Union-Typen zu erstellen.

---

Typen zu definieren, die nur bestimmte Werte enthalten.

#### --answer--

Klassenimplementierungen zu deklarieren.

### --question--

#### --text--

Was ist der Hauptzweck der Verwendung von Generics in TypeScript?

#### --distractors--

Vererbung zwischen Klassen zu implementieren.

---

Die Struktur eines Objekts zu definieren.

---

Einen neuen Datentyp zu erstellen.

#### --answer--

Wiederverwendbaren Code zu schreiben, der mit mehreren Typen arbeiten kann.

### --question--

#### --text--

Welches der folgenden Beispiele ist ein **richtiges Beispiel** für eine generische Funktion in TypeScript?

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

Was versteht man unter Typverengung (type narrowing) in TypeScript?

#### --distractors--

Die Größe des kompilierten JavaScript zu reduzieren.

---

Die Anzahl der Typen in einer Union zu begrenzen.

---

Unbenutzte Typen aus einem TypeScript-Projekt zu entfernen.

#### --answer--

Code zu schreiben, der einen Union-Typ auf einen spezifischeren Typ eingrenzt.

### --question--

#### --text--

Welche Datei wird typischerweise verwendet, um die Compiler-Optionen von TypeScript zu konfigurieren?

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

Was gibt die Option `rootDir` in der Datei `tsconfig.json` an?

#### --distractors--

Die Version von TypeScript, die während der Kompilierung verwendet wird.

---

Die Ziel-Laufzeitumgebung (z. B. Node.js oder Browser) für den TypeScript-Code.

---

Das spezifische Modulsystem, das für Importe und Exporte verwendet wird.

#### --answer--

Das Verzeichnis, das Ihre Quelldateien enthält.

### --question--

#### --text--

Welches Beispiel verwendet `instanceof` korrekt, um den Typ einzugrenzen und sicher auf die Eigenschaft `value` zuzugreifen?

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
Was bewirkt das Setzen von `"strict": true` in `tsconfig.json` in TypeScript?

#### --distractors--

Es aktiviert die statische Typprüfung, sodass Sie Fehler erkennen können, bevor Sie den Code ausführen.

---

Es zwingt Sie, immer die ES6-Syntax zu verwenden.

---

Es zwingt Sie, den Code vor der Ausführung zu kompilieren.

#### --answer--

Es aktiviert eine Reihe von Typprüfungsoptionen, wie das Erfordernis der Behandlung von `null` oder `undefined` und verhindert implizite `any`.

### --question--

#### --text--

Was ist der Zweck des Typs `never` in TypeScript?

#### --distractors--

Um null oder undefined darzustellen.

---

Um jeden möglichen Wert darzustellen.

---

Um einen Typ darzustellen, der alles außer null sein kann.

#### --answer--

Um den Typ von Werten darzustellen, die niemals auftreten.

### --question--

#### --text--

Welche der folgenden `getRandomValue`-Funktionsaufrufe wird in TypeScript erfolgreich kompiliert?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

Wie können Sie den TypeScript-Compilerfehler `'email' is possibly 'null'` im folgenden Code mit optionaler Verkettung beheben?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`

