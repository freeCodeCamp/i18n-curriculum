---
id: 66edd07682767adff3a6231e
title: اختبار التلاعب بنموذج كائن المستند (DOM) وأحداث النقر باستخدام JavaScript
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

لنجاح الاختبار القصير، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هو نموذج كائن المستند (DOM)؟

#### --distractors--

لغة برمجة تُستخدم للتفاعل مع صفحة HTML.

---

مجموعة من الكائنات التي تحتوي فقط على أعداد عشرية عائمة.

---

مجموعة من أصناف JavaScript التي تمثل عناصر HTML.

#### --answer--

واجهة برمجة تتيح لك التفاعل مع مستندات HTML.

### --question--

#### --text--

ماذا تعني اختصار API؟

#### --distractors--

واجهة برمجة إضافية

---

إضافة واجهة برمجة

---

واجهة برمجة أخرى

#### --answer--

واجهة برمجة التطبيقات (Application Programming Interface)

### --question--

#### --text--

كيف يمكنك استخدام JavaScript للعثور على جميع وسوم الرابط التي تفتقد إلى روابط تشعبية؟

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

ماذا تفعل الخاصية `textContent`؟

#### --distractors--

تحذف محتوى النص من جميع عناصر HTML.

---

تحذف محتوى النص من ملف JavaScript الخاص بك.

---

تضيف نصًا إلى جميع المصفوفات والكائنات في ملف JavaScript الخاص بك.

#### --answer--

تعيد محتوى النص العادي لعنصر ما.

### --question--

#### --text--

ماذا تفعل الخاصية `innerHTML`؟

#### --distractors--

هي خاصية من `Element` تُستخدم للحصول على كل محتوى النص لعنصر HTML.

---

هي خاصية من `Element` تُستخدم لإضافة تعليقات توضيحية إلى HTML الخاص بك.

---

هي خاصية من `Element` تُستخدم لتشغيل أحداث في HTML الخاص بك.

#### --answer--

هي خاصية من `Element` تُستخدم لتعيين أو تحديث أجزاء من ترميز HTML.

### --question--

#### --text--

أي مما يلي سيضيف مستمع حدث صالح؟

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

أي مما يلي ليس طريقة صالحة لكائن `document`؟

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()` 

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

أي مما يلي سيُعيّن الخاصية `class` إلى `my-class`؟

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

ما هو كائن `Event`؟

#### --distractors--

هو كائن خاص يُستخدم لإزالة الأحداث من مشروع JavaScript الخاص بك.

---

يُستخدم لإضافة الأحداث تلقائيًا إلى مشاريع JavaScript الخاصة بك.

---

هو كائن خاص يُستخدم عند العمل مع المصفوفات.

#### --answer--

هو حمولة تُشغّل عند تفاعل المستخدم مع صفحة الويب الخاصة بك بطريقة ما.

### --question--

#### --text--

أي مما يلي هو طريقة تُستخدم لإزالة مستمع حدث تمت إضافته سابقًا إلى عنصر باستخدام طريقة `addEventListener()`؟

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

ما هي معالجات الأحداث المضمّنة؟

#### --distractors--

خصائص خاصة على عنصر HTML تُستخدم لإلغاء جميع الأحداث.

---

خصائص خاصة على عنصر HTML تُستخدم فقط لتشغيل إرسال النماذج في شفرة JavaScript الخاصة بك.

---

خصائص خاصة على عنصر HTML تُستخدم لإزالة الأحداث.

#### --answer--

خصائص خاصة على عنصر HTML تُستخدم لتنفيذ شفرة JavaScript عند حدوث حدث.

### --question--

#### --text--

ما هما الفئتان الرئيسيتان لواجهات برمجة تطبيقات الويب؟

#### --distractors--

واجهات برمجة تطبيقات Java وواجهات برمجة تطبيقات الطرف الثالث.

---

واجهات برمجة تطبيقات المتصفح وواجهات برمجة تطبيقات Rust.

---

واجهات برمجة تطبيقات النظام وواجهات برمجة تطبيقات الطرف الثالث.

#### --answer--

واجهات برمجة تطبيقات المتصفح وواجهات برمجة تطبيقات الطرف الثالث.

### --question--

#### --text--

أي حدث يُشغّل عندما يتم تحليل جميع عناصر صفحة HTML بواسطة المتصفح؟

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

أي مما يلي خاصية تمثل النمط المضمّن لعنصر؟

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

أي مما يلي هو الطريقة الصحيحة لاستخدام معالج حدث مضمّن؟

#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--

أي من الطرق التالية تُستخدم لتأخير إجراء لفترة زمنية محددة؟

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

أي مما يلي يمكن استخدامه لإنشاء والتحكم في الرسوم المتحركة مباشرة داخل JavaScript؟

#### --distractors--

واجهة برمجة تطبيقات السماح بالرسوم المتحركة

---

واجهة برمجة تطبيقات التحكم في الرسوم المتحركة

---

واجهة برمجة تطبيقات إنشاء الرسوم المتحركة

#### --answer--

واجهة برمجة تطبيقات الرسوم المتحركة على الويب

### --question--

#### --text--

ماذا تفعل واجهة برمجة تطبيقات اللوحة (Canvas API)؟

#### --distractors--

تُستخدم لتحريك الرسوم في ملف CSS الخاص بك.

---

تُستخدم فقط لإزالة الرسوم من ملف JavaScript الخاص بك.

---

تُستخدم لإزالة الرسوم من ملف HTML الخاص بك.

#### --answer--

تُستخدم للتلاعب بالرسوم عبر ملف JavaScript الخاص بك.

### --question--

#### --text--

ما هو تفويض الأحداث؟

#### --distractors--

عملية الاستماع إلى الأحداث عندما يتم إلغاء حدث ما.

---

عملية الاستماع إلى الأحداث عند حدوث حدث نقر.

---

عملية الاستماع إلى الأحداث عند حدوث حدث تغيير.

#### --answer--

عملية الاستماع إلى الأحداث التي ارتفعت إلى عنصر أبوي.

### --question--

#### --text--

أي مما يلي ليس طريقة يمكنك استخدامها عند العمل مع الحوارات والنوافذ المنبثقة؟

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`

