---
id: 67f412ac59601c4151b763da
title: 辞書とセットのクイズ
challengeType: 8
dashedName: quiz-dictionaries-and-sets
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

辞書とは何ですか？

#### --distractors--

文字列とリストのみを保持するデータ構造。

---

大きな数値のみを受け入れるキーと値のペアのコレクションであるデータ構造。

---

タプルのコレクションのみを保持するデータ構造。

#### --answer--

キーと値のペアのコレクションであるデータ構造。

### --question--

#### --text--

辞書の正しい構文はどれですか？

#### --distractors--

```py
dictionary = {
    <key1: value1>,
    <key2: value2>
}
```

---

```py
dictionary = {
    (key1: value1),
    (key2: value2)
}
```

---

```py
dictionary = {
    [key1: value1],
    [key2: value2]
}
```

#### --answer--

```py
dictionary = {
    key1: value1,
    key2: value2
}
```

### --question--

#### --text--

辞書について正しいのはどれですか？

#### --distractors--

キーは少なくとも2文字でなければなりません。

---

キーには特殊記号を含める必要があります。

---

キーには少なくとも1つの数字を含める必要があります。

#### --answer--

キーは一意でなければなりません。

### --question--

#### --text--

辞書を作成するために使えるコンストラクタはどれですか？

#### --distractors--

`diction()`

---

`dic()`

---

`dictionary()`

#### --answer--

`dict()`

### --question--

#### --text--

辞書から値にアクセスする正しい方法はどれですか？

#### --distractors--

`dictionary<key>`

---

`dictionary/key/`

---

`dictionary{key}`

#### --answer--

`dictionary[key]`

### --question--

#### --text--

辞書の値を更新する正しい方法はどれですか？

#### --distractors--

`pizza['name'] >> 'Margherita'`

---

`pizza['name'] << 'Margherita'`

---

`pizza['name'] == 'Margherita'`

#### --answer--

`pizza['name'] = 'Margherita'`

### --question--

#### --text--

キーに関連付けられた値を取得するために使えるメソッドはどれですか？

#### --distractors--

`access()`

---

`set()`

---

`retrieve()`

#### --answer--

`get()`

### --question--

#### --text--

ビューオブジェクトとは何ですか？

#### --distractors--

文字列を辞書に変換するために使われる特別なオブジェクト。

---

辞書に2つ以上のキーと値のペアがある場合のみ内容を表示する方法。

---

リストを辞書に変換するために使われる特別なオブジェクト。

#### --answer--

データの別コピーを作成せずに辞書の内容を見る方法。

### --question--

#### --text--

辞書内のすべてのキーと値のペアを含むビューオブジェクトを返すメソッドはどれですか？

#### --distractors--

`lists()`

---

`dictionaries()`

---

`collections()`

#### --answer--

`items()`

### --question--

#### --text--

辞書からすべてのキーと値のペアを削除するメソッドはどれですか？

#### --distractors--

`empty()`

---

`replace()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

次のコードの出力は何になりますか？

```py
products = {
    'Laptop': 990,
    'Smartphone': 600,
    'Tablet': 250,
    'Headphones': 70,
}

for product in products.items():
    print(product)
```

#### --distractors--

```py
<'Laptop', 990>
<'Smartphone', 600>
<'Tablet', 250>
<'Headphones', 70>
```

---

```py
'Laptop', 990
'Smartphone', 600
'Tablet', 250
'Headphones', 70
```

---

```py
['Laptop', 990]
['Smartphone', 600]
['Tablet', 250]
['Headphones', 70]
```

#### --answer--

```py
('Laptop', 990)
('Smartphone', 600)
('Tablet', 250)
('Headphones', 70)
```

### --question--

#### --text--

セットについて正しいのはどれですか？

#### --distractors--

セットは変更可能で逆順に並んでいます。

---

セットは変更可能で順序付きです。

---

セットは変更不可能で順序なしです。

#### --answer--

セットは変更可能で順序なしです。

### --question--

#### --text--

セットを作成する正しい方法はどれですか？

#### --distractors--

`my_set = (1, 2, 3, 4, 5)`

---

`my_set = <1, 2, 3, 4, 5>`

---

`my_set = [1, 2, 3, 4, 5]`

#### --answer--

`my_set = {1, 2, 3, 4, 5}`

### --question--

#### --text--

空のセットを作成するために使う関数はどれですか？

#### --distractors--

`create_set()`

---

`empty_set()`

---

`sets()`

#### --answer--

`set()`

### --question--

#### --text--

ターミナルに出力されるのは何ですか？

```py
my_set = {1, 2, 3, 4, 5, 6}
my_set.add(5)

print(my_set)
```

#### --distractors--

`SyntaxError`

---

`RangeError`

---

`{1, 2, 3, 4, 5, 5, 6}`

#### --answer--

`{1, 2, 3, 4, 5, 6}`

### --question--

#### --text--

セットが部分集合かどうかをチェックするメソッドはどれですか？

#### --distractors--

`issub()`

---

`isset()`

---

`subset()`

#### --answer--

`issubset()`

### --question--

#### --text--

2つのセットが互いに素かどうかをチェックするメソッドはどれですか？

#### --distractors--

`joint()`

---

`isjoint()`

---

`disjoint()`

#### --answer--

`isdisjoint()`

### --question--

#### --text--

対称差演算子(`^`)は何をしますか？

#### --distractors--

セットに共通する要素のみを含む新しいセットを返します。

---

セットの差を見つけ、その結果で最初のセットを更新します。

---

最初のセットにあってもう一方のセットにない要素のみを含む新しいセットを返します。

#### --answer--

最初か2番目のセットのどちらか一方にだけある要素を含む新しいセットを返します。

### --question--

#### --text--

乱数を生成するために使われる組み込みモジュールはどれですか？

#### --distractors--

`set_random`

---

`get_random`

---

`rand`

#### --answer--

`random`

### --question--

#### --text--

正規表現を扱うために使われるモジュールはどれですか？

#### --distractors--

`regex`

---

`reg`

---

`r`

#### --answer--

`re`
