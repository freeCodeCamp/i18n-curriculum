---
id: 66edc25ae5ea80bf6f785552
title: JavaScriptの変数とデータ型クイズ
challengeType: 8
dashedName: quiz-javascript-variables-and-data-types
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

次のうち、JavaScriptを最もよく表しているものはどれですか？

#### --distractors--

ウェブサイトのデザインに使われるマークアップ言語。

---

サーバーサイドのプログラミング言語。

---

データベース管理システム。

#### --answer--

ウェブ開発に使われるプログラミング言語。

### --question--

#### --text--

次のうち、JavaScriptのデータ型ではないものはどれですか？

#### --distractors--

`Number`

---

`Undefined`

---

`Object`

#### --answer--

`Double`

### --question--

#### --text--

次のうち、JavaScriptの有効な変数宣言ではないものはどれですか？

#### --distractors--

`const x = 0;`

---

`let x;`

---

`var x;`

#### --answer--

`public Int x;`

### --question--

#### --text--

JavaScriptで変数名に使われる一般的な命名慣例はどれですか？

#### --distractors--

JavaScriptケース。

---

ライトニングケース。

---

ジラフケース。

#### --answer--

キャメルケース。

### --question--

#### --text--

JavaScriptの`let`と`const`の変数宣言の違いは何ですか？

#### --distractors--

`const`は再代入も再宣言もできません。`let`は再代入も再宣言もできます。

---

`const`は再代入も再宣言もできます。`let`は再代入も再宣言もできません。

---

`const`は再代入できますが再宣言はできません。`let`は再宣言できますが再代入はできません。

#### --answer--

`const`は再代入も再宣言もできません。`let`は再代入できますが再宣言はできません。

### --question--

#### --text--

なぜJavaScriptの文字列はイミュータブル（不変）と考えられているのですか？

#### --distractors--

変数を使って文字列を作成できません。

---

文字列はリテラルでしか作成できません。

---

文字列は変更できますが、グローバル変数を通じてのみです。

#### --answer--

一度文字列が作成されると、その文字を直接変更できないからです。

### --question--

#### --text--

JavaScriptで文字列`Hello, world!`をコンソールに出力するにはどうしますか？

#### --distractors--

`print("Hello, world!")`

---

`alert("Hello, world");`

---

`System.out.println("Hello, world!");`

#### --answer--

`console.log("Hello, world!");`

### --question--

#### --text--

以下のコードは何をしますか？

```js
let hello = "Hello";
hello += " world";
console.log(hello);
```

#### --distractors--

`" world"`を変数`hello`に再代入し、コンソールに出力します。

---

`Hello world`の文字数をコンソールに出力します。

---

`"Hello"`と`" world"`を結合して新しい文字列を作り出力しますが、`hello`の値は`"Hello"`のままです。

#### --answer--

現在の`hello`の値に文字列`" world"`を結合し、それを`hello`に再代入してコンソールに出力します。

### --question--

#### --text--

JavaScriptで文の終わりを示す文字はどれですか？

#### --distractors--

`:`

---

`,`

---

`.`

#### --answer--

`;`

### --question--

#### --text--

JavaScriptで有効な一行コメントの構文はどれですか？

#### --distractors--

`# This is a single line comment`

---

`-- This is a single line comment`

---

`/* This is a single line comment */`

#### --answer--

`// This is a single line comment`

### --question--

#### --text--

JavaScriptの動的型付けとは何を意味しますか？

#### --distractors--

変数を使う前にデータ型を宣言する必要がある。

---

異なる型の変数に別の型の値を代入できない。

---

JavaScriptは実行前に型チェックを行い、型に関するエラーはコード実行前にスローされる。

#### --answer--

JavaScriptは代入された値に基づいて型を推測します。

### --question--

#### --text--

次のうち、変数`x`の型をコンソールに出力するコードはどれですか？

#### --distractors--

`console.log(x.type);`

---

`console.log(typeOf(x));`

---

`console.log(isNaN(x));`

#### --answer--

`console.log(typeof x);`

### --question--

#### --text--

`console.log(typeof null);`の出力は何になりますか？

#### --distractors--

`"undefined"`

---

`"null"`

---

`"NaN"`

#### --answer--

`"object"`

### --question--

#### --text--

JavaScriptで有効な複数行コメントはどれですか？

#### --distractors--

```js
// This is a
// multi-line
// comment
```

---

```py
'''
This is a
multi-line
comment
'''
```

---

```html
<!--
This is a
multi-line
comment
-->
```

#### --answer--

```js
/*
This is a
multi-line
comment
*/
```

### --question--

#### --text--

変更できない変数をJavaScriptで宣言するにはどれを使いますか？

#### --distractors--

`var x = 0;`

---

`let x = 0;`

---

`final x = 0;`

#### --answer--

`const x = 0;`

### --question--

#### --text--

JavaScriptで有効な変数名はどれですか？

#### --distractors--

`tart@n`

---

`4cats`

---

`!!dogs`

#### --answer--

`$number`

### --question--

#### --text--

JavaScriptで有効でない文字列連結の方法はどれですか？

#### --distractors--

`console.log(string1 + " " + string2);`

---

`console.log("string1 " + string2);`

---

`console.log(string1.concat(" ", string2));`

#### --answer--

`console.log(string1 string2);`

### --question--

#### --text--

次のうち、キャメルケースを正しく使っている変数名はどれですか？

#### --distractors--

`a_long_variable_name`

---

`alongvariableName`

---

`ALongVariableName`

#### --answer--

`aLongVariableName`

### --question--

#### --text--

JavaScriptには自動セミコロン挿入機能がありますが、なぜ明示的にセミコロンを使うことが有益ですか？

#### --distractors--

コードの実行速度を上げるため。

---

他のプログラミング言語との一貫性を保つため。

---

デバッグやエラー追跡をしやすくするため。

#### --answer--

コードの可読性と信頼性を高めるため。

### --question--

#### --text--

次のうち、JavaScriptのデータ型ではないものはどれですか？

#### --distractors--

`Symbol`

---

`Boolean`

---

`Null`

#### --answer--

`Float`
