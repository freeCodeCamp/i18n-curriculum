---
id: 66edc3ab8c6413c344f401bf
title: JavaScript 數學測驗
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

`NaN` 代表什麼？

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

以下哪一項不是 JavaScript 中的算術運算子？

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

如果你嘗試在 JavaScript 中除以零會發生什麼？

#### --distractors--

JavaScript 會輸出 0。

---

不會發生任何事情。

---

JavaScript 會丟出錯誤。

#### --answer--

JavaScript 會輸出 `Infinity`

### --question--

#### --text--

以下哪一項是指數運算子的正確用法？

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

在 JavaScript 中遞減變數時，你應該使用哪個運算子？

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

以下哪一項不是以 bit 為單元逐一處理的運算子？

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

下列哪一個是取得餘數的正確複合運算子？

#### --distractors--

`//=`

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

`==` 和 `===` 運算子之間有什麼差別？

#### --distractors--

沒有差異。比較結果將永遠相同。

---

`==` 運算子用於檢查資料型別是否相同，而 `===` 用於檢查值是否相等。

---

`==` 運算子只能用於原生型別，而 `===` 運算子只能用於物件。

#### --answer--

`==` 運算子只比較值，而 `===` 運算子比較值和型別。

### --question--

#### --text--

一元加號運算子（`+`）的用途是什麼？

#### --distractors--

它用於將值減少 1。

---

它用於取得操作的餘數。

---

它用於反轉數字的二進位表述。

#### --answer--

它用於將其運算元轉換為數字。

### --question--

#### --text--

以下哪一個 `Math` 方法會將數值四捨五入到最接近的整數？

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

`Math.sqrt()` 方法做什麼？

#### --distractors--

此方法傳回一個隨機浮點數。

---

此方法傳回數字的絕對值。

---

此方法傳回一個數字的立方。

#### --answer--

此方法傳回一個數字的平方根。

### --question--

#### --text--

以下哪一項不是 `Math` 物件的有效方法？

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

以下哪一項會傳回 `false`？

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

`parseFloat()` 方法做什麼？

#### --distractors--

它會解析字串引數並對其執行以 bit 為單元逐一操作。

---

它會解析字串引數並傳回 `false`。

---

它會解析字串引數並傳回整數。

#### --answer--

它解析字串引數並傳回浮點數。

### --question--

#### --text--

什麼會被記錄到主控台？

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

以下程式碼的輸出會是什麼？

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

什麼是運算子優先？

#### --distractors--

運算子優先用來檢查左邊的值是否小於右邊的值。

---

運算子優先用於將變數設值為指定數字的次方，並將結果重新指定給該變數。

---

運算子優先用於將值增加 1。

#### --answer--

運算子優先決定運算式中操作的評估順序。

### --question--

#### --text--

以下哪一項不是比較運算子？

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

一元負號運算子（`-`）的作用是什麼？

#### --distractors--

它會翻轉其運算元的布林值。

---

它將值減少 1。

---

它將值增加 1。

#### --answer--

它會對運算元取反。

### --question--

#### --text--

以下哪一項是使用 nullish coalescing 運算子的範例？

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```

