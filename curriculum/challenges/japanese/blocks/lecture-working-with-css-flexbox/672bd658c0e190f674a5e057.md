---
id: 672bd658c0e190f674a5e057
title: よく使われるフレックスプロパティとは何で、どのように機能するのか？
challengeType: 19
dashedName: what-are-some-common-flex-properties
---

# --interactive--

フレックスプロパティは、フレックスコンテナに適用して子要素の配置を決定するためのプロパティです。ここでは、最もよく使われる `flex-wrap`、`justify-content`、`align-items` を紹介します。

まずは `flex-wrap` から始めましょう。このプロパティは、フレックスアイテムがフレックスコンテナ内で利用可能なスペースに合わせてどのように折り返されるかを決定します。`flex-wrap` は `nowrap`、`wrap`、`wrap-reverse` の3つの値を取ることができます。`nowrap` はデフォルト値で、フレックスアイテムは幅がコンテナの幅を超えても新しい行に折り返されません。

以下のコードでは、3つの `div` 要素があります。`width` に注目しましょう。赤いボーダーの `main` コンテナは `width` が `200px` に設定されており、その3つの子 `div` 要素の合計は `width` が `240px`（それぞれ `80px`）です。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::


`div` 要素の幅はコンテナの幅を超えていますが、デフォルトでは利用可能なスペースに合わせて縮小されます。もしコンテナの幅を超えたときに折り返したい場合は、フレックスコンテナに `flex-wrap: wrap` を設定してください。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  flex-wrap: wrap;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

`div` 要素はコンテナの幅を超えると行に並び替えられます。`flex-wrap: wrap-reverse` を使うと逆順で折り返すこともできます。`flex-flow` プロパティは `flex-direction` と `flex-wrap` の省略形プロパティです。この例では `flex-direction` を `column` に、`flex-wrap` を `wrap-reverse` に設定しています。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  flex-flow: column wrap-reverse;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

次に `justify-content` について説明します。`justify-content` はフレックスコンテナの主軸に沿って子要素を整列させます。`flex-start` に `justify-content` の値を代入すると、フレックスアイテムは主軸の開始位置に整列します。主軸は水平または垂直の場合があります。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: flex-start;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

`justify-content: flex-end` を使うと、フレックスアイテムは主軸の終端に整列します。水平でも垂直でも同様です。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: flex-end;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

主軸に沿ってフレックスアイテムを中央に配置したい場合は `justify-content: center` を使います。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: center;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

主軸に沿って要素を均等に配置したい場合は `justify-content: space-between` を使います。必要に応じてフレックスアイテム間にスペースが追加されます。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-between;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

`justify-content: space-around` は主軸内でフレックスアイテムを均等に配置し、最初のアイテムの前と最後のアイテムの後にもスペースを追加します。この追加スペースは隣接するアイテム間のスペースの半分です。アイテムが1つだけの場合は中央に配置されます。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-around;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::


最後に `justify-content: space-evenly` があります。これは主軸に沿ってアイテムを均等に配置し、アイテム間のスペースと最初と最後の要素の前後のスペースがすべて同じになります。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-evenly;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

これで主軸に沿ったフレックスアイテムの配置方法がわかりましたね。しかし、交差軸に沿っても配置したい場合があります。交差軸は主軸に対して垂直な軸です。`align-items` プロパティでこれができます。交差軸に沿ってアイテムを中央に配置するには、フレックスコンテナに `align-items: center` を追加してください。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: center;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

この例では、フレックスアイテムはデフォルトで垂直の交差軸に沿って中央に配置されています。交差軸が水平の場合は水平方向に中央配置されます。対して `align-items: flex-start` は交差軸の開始位置にアイテムを整列させます。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: flex-start;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

反対に `align-items: flex-end` は交差軸の終端にフレックスアイテムを整列させます。垂直でも水平でも同様です。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: flex-end;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

交差軸に沿ってフレックスアイテムを伸ばしたい場合は `align-items: stretch` を使います。これは `width` や `height` が設定されていない自動サイズの要素や、最小 `width` や `height` のみ設定されている要素にも適用されます。フレックスアイテムは交差軸の方向にコンテナいっぱいに伸びます。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: stretch; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;           
  border: 1px solid #888;
  background-color: lightblue;
}
```

:::

最後に、個別のフレックスアイテムに対して交差軸の異なる整列を割り当てるには `align-self` プロパティを使います。例えば `align-self: stretch` で伸ばすことができます。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: stretch; 
  background-color: lightcoral;
}
```

:::

`align-self: center` で中央に配置することもできます。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: center; 
  background-color: lightcoral;
}
```

:::

`align-self: flex-start` で交差軸の開始位置に整列させることも可能です。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: flex-start; 
  background-color: lightcoral;
}
```

:::

また `align-self: flex-end` で交差軸の終端に整列させることもできます。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: flex-end; 
  background-color: lightcoral;
}
```

:::

他にもレスポンシブなレイアウトを作るために選べるフレックスプロパティや値はありますが、これらが最もよく使われるものです。これらのCSSフレックスプロパティとCSSフレックスモデルの新しい知識を使って、デバイス間でスムーズで包括的なユーザー体験を作るレスポンシブレイアウトを作成し始めましょう。

# --questions--

## --text--

フレックスコンテナ内で子要素を水平方向（デフォルト）に整列させるために使うCSSプロパティはどれですか？

## --answers--

`flex-direction`

### --feedback--

フレックスコンテナ内で要素が水平方向にどのように整列されるかを考えてみてください。

---

`justify-content`

---

`align-items`

### --feedback--

フレックスコンテナ内で要素が水平方向にどのように整列されるかを考えてみてください。

---

`flex-wrap`

### --feedback--

フレックスコンテナ内で要素が水平方向にどのように整列されるかを考えてみてください。

## --video-solution--

2

## --text--

フレックスコンテナ内で子要素を垂直方向（デフォルト）に整列させるために使うCSSプロパティはどれですか？

## --answers--

`flex-direction`

### --feedback--

フレックスコンテナ内で要素が垂直方向にどのように整列されるかを考えてみてください。

---

`justify-content`

### --feedback--

フレックスコンテナ内で要素が垂直方向にどのように整列されるかを考えてみてください。

---

`align-items`

---

`flex-wrap`

### --feedback--

フレックスコンテナ内で要素が垂直方向にどのように整列されるかを考えてみてください。

## --video-solution--

3

## --text--

次のうち、`flex-flow` プロパティにまとめられるプロパティはどれですか？

## --answers--

`flex-direction` と `justify-content`。

### --feedback--

フレックスアイテムの配置方法の2つの主要な側面を考えてみてください。

---

`flex-direction` と `align-items`。

### --feedback--

フレックスアイテムの配置方法の2つの主要な側面を考えてみてください。

---

`flex-direction` と `align-content`。

### --feedback--

フレックスアイテムの配置方法の2つの主要な側面を考えてみてください。

---

`flex-direction` と `flex-wrap`。

## --video-solution--

4
