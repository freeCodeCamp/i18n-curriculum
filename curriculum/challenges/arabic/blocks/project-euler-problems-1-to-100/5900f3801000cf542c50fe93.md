---
id: 5900f3801000cf542c50fe93
title: "المشكلة 20: مجموع أرقام المضروب"
challengeType: 1
forumTopicId: 301839
dashedName: problem-20-factorial-digit-sum
---

# --description--

`n`! تعني `n` × (`n` − 1) × ... × 3 × 2 × 1

على سبيل المثال، 10! = 10 × 9 × ... × 3 × 2 × 1 = 3628800،  
ومجموع الأرقام في العدد 10! هو 3 + 6 + 2 + 8 + 8 + 0 + 0 = 27.

احسب مجموع أرقام `n`!

# --hints--

يجب أن تُرجع الدالة `sumFactorialDigits(10)` رقمًا.

```js
assert.isNumber(sumFactorialDigits(10));
```

يجب أن تُرجع الدالة `sumFactorialDigits(10)` القيمة 27.

```js
assert.strictEqual(sumFactorialDigits(10), 27);
```

يجب أن تُرجع الدالة `sumFactorialDigits(25)` القيمة 72.

```js
assert.strictEqual(sumFactorialDigits(25), 72);
```

يجب أن تُرجع الدالة `sumFactorialDigits(50)` القيمة 216.

```js
assert.strictEqual(sumFactorialDigits(50), 216);
```

يجب أن تُرجع الدالة `sumFactorialDigits(75)` القيمة 432.

```js
assert.strictEqual(sumFactorialDigits(75), 432);
```

يجب أن تُرجع الدالة `sumFactorialDigits(100)` القيمة 648.

```js
assert.strictEqual(sumFactorialDigits(100), 648);
```

# --seed--

## --seed-contents--

```js
function sumFactorialDigits(n) {

  return n;
}

sumFactorialDigits(100);
```

# --solutions--

```js
let factorial = (n) => n <= 1 ? BigInt(n) : BigInt(n) * BigInt(factorial(--n));

let sumDigits = n => n.toString().split('').map(x => parseInt(x)).reduce((a,b) => a + b);

function sumFactorialDigits(n) {
  return sumDigits(factorial(n));
}
```
