---
id: 68910fc037a90c285107af04
title: ما هو تقسيم المشكلة وغزوها، وكيف تعمل خوارزمية دمج الفرز؟
challengeType: 19
dashedName: what-is-divide-and-conquer-and-how-does-merge-sort-work
---

# --description--

نموذج تقسيم المشكلة وغزوها في علوم الحاسوب هو تقنية لتقسيم المشكلات بشكل متكرر إلى مشكلات فرعية أصغر. أحد الجوانب الأساسية لهذه التقنية هو العودية، التي تحدث عندما تستدعي الدالة نفسها مرارًا حتى الوصول إلى حالة أساسية. في هذا الدرس، سنلقي نظرة على خوارزمية دمج الفرز لفهم أفضل لكيفية عمل تقنية تقسيم المشكلة وغزوها.

لنفترض أن لدينا هذه القائمة من الأرقام:

```md
42 37 53 17
```

الهدف هو فرز تلك القائمة من الأصغر إلى الأكبر باستخدام خوارزمية دمج الفرز. الخطوة الأولى هي تقسيم تلك القائمة إلى نصفين:

```md
42 37 | 53 17
```

بعدها نحتاج إلى النظر إلى الجانب الأيسر من القائمة:

```md
42 37
```

نأخذ تلك القائمة الفرعية ونقسمها إلى نصفين مرة أخرى حتى تحتوي كل قائمة فرعية على عنصر واحد فقط:

```md
42 | 37
```

القائمة التي تحتوي على عنصر واحد فقط تكون مرتبة افتراضيًا. بعد ذلك نحتاج إلى دمج كل تلك القوائم الفرعية ذات العنصر الواحد في قائمة مرتبة:

```md
37 42
```

ثم نتبع نفس العملية للجانب الأيمن من القائمة الأصلية:

```py
# right side of original list
53 17

# divide the list in half
53 | 17

# merge the lists in sorted order
17 53
```

الآن بعد أن تم فرز كلا نصفي القائمة الأصلية، ندمج هذين النصفيْن معًا ونرتب العناصر:

```markdown
17 37 42 53
```

إليك كيف تبدو الخوارزمية في الشفرة:

```py
def merge_sort(arr):
    if len(arr) <= 1:
        return arr

    mid = len(arr) // 2
    left = merge_sort(arr[:mid])
    right = merge_sort(arr[mid:])

    sorted_list = []
    i = 0
    j = 0

    while i < len(left) and j < len(right):
        if left[i] <= right[j]:
            sorted_list.append(left[i])
            i += 1
        else:
            sorted_list.append(right[j])
            j += 1

    sorted_list.extend(left[i:])
    sorted_list.extend(right[j:])

    return sorted_list
```

تعقيد الزمن لخوارزمية دمج الفرز هو `O(n log n)` لأن القائمة تُقسم باستمرار إلى نصفين (`log n`) ثم تُدمج معًا (`O(n)`). على عكس خوارزميات الفرز الأخرى مثل فرز الفقاعات، لا يتم الفرز في مكانه في دمج الفرز وله تعقيد مساحة `O(n)`.

# --questions--

## --text--

ما هو نموذج تقسيم المشكلة وغزوها في علوم الحاسوب؟

## --answers--

تقنية لاكتشاف دورة في تكرارات قيم الدالة باستخدام مؤشرين فقط.

### --feedback--

راجع بداية الدرس.

---

خوارزمية لمقارنة عنصرين وتبديلهما من الأصغر إلى الأكبر إذا لزم الأمر.

### --feedback--

راجع بداية الدرس.

---

تقنية لتحليل المشكلات بشكل متكرر إلى مشكلات فرعية أصغر.

---

خوارزمية لحساب أقصر شبكة ربط للنقاط في مستوى.

### --feedback--

راجع بداية الدرس.

## --video-solution--

3

## --text--

ما هو تعقيد الزمن لخوارزمية دمج الفرز؟

## --answers--

`O(n log n)`

---

`O(log n²)`

### --feedback--

راجع نهاية الدرس.

---

`O(n³ log n)`

### --feedback--

راجع نهاية الدرس.

---

`O(log n³)`

### --feedback--

راجع نهاية الدرس.

## --video-solution--

1

## --text--

ما هو تعقيد المساحة لخوارزمية دمج الفرز؟

## --answers--

`O(n²)`

### --feedback--

راجع نهاية الدرس.

---

`O(1)`

### --feedback--

راجع نهاية الدرس.

---

`O(n log n)`

### --feedback--

راجع نهاية الدرس.

---

`O(n)`

## --video-solution--

4
