---
id: 5900f3c31000cf542c50fed5
title: "المشكلة 86: مسار متوازي مستطيلات"
challengeType: 1
forumTopicId: 302200
dashedName: problem-86-cuboid-route
---

# --description--

تجلس عنكبوتة، S، في زاوية واحدة من غرفة متوازي مستطيلات أبعادها 6 في 5 في 3، وتجلس ذبابة، F، في الزاوية المقابلة. بالسفر على أسطح الغرفة، أقصر مسافة "خط مستقيم" من S إلى F تساوي 10، والمسار موضح في الرسم البياني.

<img alt="a diagram of a spider and fly's path from one corner of a cuboid room to the opposite corner" src="https://cdn-media-1.freecodecamp.org/project-euler/cuboid-route.png" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

مع ذلك، هناك حتى ثلاثة مرشحين لمسار "الأقصر" لأي متوازي مستطيلات معين، ولا يكون المسار الأقصر دائمًا بطول عدد صحيح.

يمكن إثبات وجود بالضبط `2060` متوازي مستطيلات مميزة، متجاهلين التدوير، بأبعاد صحيحة، حتى حجم أقصى M في M في M، حيث يكون المسار الأقصر بطول عدد صحيح عندما تكون M = 100. هذه هي أقل قيمة لـ M يتجاوز فيها عدد الحلول لأول مرة ألفي حل؛ وعدد الحلول عندما تكون M = 99 هو `1975`.

اعثر على أقل قيمة لـ M بحيث يتجاوز عدد الحلول لأول مرة `n`.

# --hints--

يجب أن تُرجع الدالة `cuboidRoute(2000)` رقمًا.

```js
assert(typeof cuboidRoute(2000) === 'number');
```

يجب أن تُرجع الدالة `cuboidRoute(2000)` القيمة `100`.

```js
assert.strictEqual(cuboidRoute(2000), 100);
```

يجب أن تُرجع الدالة `cuboidRoute(25000)` القيمة `320`.

```js
assert.strictEqual(cuboidRoute(25000), 320);
```

يجب أن تُرجع الدالة `cuboidRoute(500000)` القيمة `1309`.

```js
assert.strictEqual(cuboidRoute(500000), 1309);
```

يجب أن تُرجع الدالة `cuboidRoute(1000000)` القيمة `1818`.

```js
assert.strictEqual(cuboidRoute(1000000), 1818);
```

# --seed--

## --seed-contents--

```js
function cuboidRoute(n) {

  return true;
}

cuboidRoute(2000);
```

# --solutions--

```js
function cuboidRoute(n) {
  // Based on https://www.mathblog.dk/project-euler-86-shortest-path-cuboid/
  function getLength(a, b) {
    return Math.sqrt(a ** 2 + b ** 2);
  }

  let M = 2;
  let counter = 0;

  while (counter < n) {
    M++;
    for (let baseHeightWidth = 3; baseHeightWidth <= 2 * M; baseHeightWidth++) {
      const pathLength = getLength(M, baseHeightWidth);
      if (Number.isInteger(pathLength)) {
        if (baseHeightWidth <= M) {
          counter += Math.floor(baseHeightWidth / 2);
        } else {
          counter += 1 + M - Math.floor((baseHeightWidth + 1) / 2);
        }
      }
    }
  }

  return M;
}
```
