---
id: 66edcd875b0d91de1fbbb492
title: Quiz sui fondamenti di JavaScript
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale di questi creerà un oggetto String?

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

Come puoi convertire un oggetto String in una stringa letterale?

#### --distractors--

Con il costruttore `String()`.

---

Con il metodo `.stringify()`.

---

Racchiudi l’oggetto `String` tra virgolette doppie.

#### --answer--

Con il metodo `.toString()`.

### --question--

#### --text--

Perché puoi usare metodi come `.length`, `.repeat()`, `.concat()` e `.slice()` su una stringa primitiva in JavaScript?

#### --distractors--

Quei metodi esistono nativamente sulle stringhe primitive.

---

Le stringhe primitive sono archiviate internamente come oggetti.

---

JavaScript converte temporaneamente la stringa primitiva in un array.

#### --answer--

JavaScript avvolge temporaneamente la stringa primitiva in un oggetto String.

### --question--

#### --text--

Cosa stamperà il seguente codice nella console?

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

Quale delle seguenti restituisce un oggetto `Number` dalla stringa `"123"`?

#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--

Cosa restituiranno rispettivamente `Number(true)` e `Number(false)`?

#### --distractors--

`undefined` e `undefined`

---

`NaN` e `NaN`

---

`true` e `false`

#### --answer--

`1` e `0`

### --question--

#### --text--

Cosa restituiranno rispettivamente `Number(undefined)` e `Number(null)`?

#### --distractors--

`NaN` e `NaN`

---

`0` e `0`

---

`undefined` e `null`

#### --answer--

`NaN` e `0`

### --question--

#### --text--

Qual è il nome migliore per una variabile booleana che verifica se un utente è autorizzato a fare qualcosa sul tuo sito web?

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

Qual è il nome migliore per una funzione che viene eseguita quando un utente invia un modulo?

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

Cos’è un array sparso?

#### --distractors--

Un array in cui uno o più indici hanno un valore `null`.

---

Un array in cui uno o più indici hanno un valore `undefined`.

---

Un array in cui uno o più indici hanno un valore di un oggetto letterale vuoto.

#### --answer--

Un array in cui uno o più indici non sono assegnati a un valore.

### --question--

#### --text--

In quale dei seguenti esempi `arr` NON sarà un array sparso?

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

Qual è lo scopo principale di un linter?

#### --distractors--

Formattare automaticamente il codice.

---

Ottimizzare il codice per le prestazioni.

---

Creare documentazione per il codice.

#### --answer--

Individuare potenziali errori e problemi nel codice.

### --question--

#### --text--

Qual è un vantaggio dell’uso di un formatter in un codice base?

#### --distractors--

Permettono agli sviluppatori di compilare il codice più efficientemente.

---

Convertono il codice in un linguaggio di programmazione specifico.

---

Individuano errori logici.

#### --answer--

Garantiscono uno stile di codice coerente.

### --question--

#### --text--

In JavaScript, chi o cosa è tipicamente responsabile di allocare la memoria alle variabili?

#### --distractors--

Lo sviluppatore.

---

Il sistema operativo.

---

Il server.

#### --answer--

Il motore di runtime.

### --question--

#### --text--

Cosa verrà stampato nella console nel seguente codice?

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

Cos’è l’hoisting in JavaScript?

#### --distractors--

Spostare gli errori in cima al codice.

---

Il processo di creare nuove variabili.

---

Spostare automaticamente le dichiarazioni `var` in cima al blocco.

#### --answer--

Il processo di spostare le dichiarazioni di variabili in cima al loro ambito.

### --question--

#### --text--

Perché l’uso della parola chiave `var` è considerato una cattiva pratica nel JavaScript moderno?

#### --distractors--

Sono più lenti di `let` e `const`.

---

Causano errori di sintassi.

---

Non permettono il controllo dei tipi.

#### --answer--

Sono accessibili al di fuori del loro blocco.

### --question--

#### --text--

Qual è lo scopo dei moduli in JavaScript?

#### --distractors--

Migliorare le prestazioni del codice.

---

Rendere il codice più difficile da capire.

---

Creare nuovi linguaggi di programmazione.

#### --answer--

Organizzare il codice in unità riutilizzabili.

### --question--

#### --text--

Come aggiungi uno script `app.js` come modulo in un documento HTML?

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

Quale dei seguenti esempi esporta la funzione `calculateSum` da un modulo `utils.js` e la importa e usa correttamente in un file `app.js`?

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
