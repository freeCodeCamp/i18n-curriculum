---
id: 66edd07682767adff3a6231e
title: 使用 JavaScript 进行 DOM 操作和点击事件测验
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是文档对象模型？

#### --distractors--

一种用于与 HTML 页面交互的编程语言。

---

一组只包含浮点数的对象。

---

一组表现超文本标记语言元素的 JavaScript 类。

#### --answer--

一个允许你与 `HTML` 文档交互的编程接口。

### --question--

#### --text--

API 的全称是什么？

#### --distractors--

附加编程接口

---

添加编程接口

---

另一个编程接口

#### --answer--

应用程序编程接口

### --question--

#### --text--

你如何使用 JavaScript 查找所有缺少超链接的超文本标记语言锚点？

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

`textContent` 属性的作用是什么？

#### --distractors--

它会从所有超文本标记语言元素中移除文本内容。

---

它从你的 JavaScript 文件中移除文本内容。

---

它将文本添加到你的 JavaScript 文件中的所有数组和对象。

#### --answer--

它返回元素的纯文本内容。

### --question--

#### --text--

`innerHTML` 属性的作用是什么？

#### --distractors--

它是 `Element` 的一个属性，用于获取超文本标记语言元素的所有文本内容。

---

它是 `Element` 的一个属性，用于为你的 HTML 添加注解。

---

它是 `Element` 的一个属性，用于触发你超文本标记语言中的事件。 

#### --answer--

它是 `Element` 的一个属性，用于设置或更新超文本标记语言标记的部分内容。

### --question--

#### --text--

以下哪项将添加有效的事件监听器？

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

以下哪项不是 `document` 对象的有效方法？

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()` 

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

以下哪项会将 `class` 属性设置为 `my-class`？

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

什么是 `Event` 对象？

#### --distractors--

它是一个用于从你的 JavaScript 项目中移除事件的特殊对象。

---

它用于自动将事件添加到你的 JavaScript 项目中。

---

它是在处理数组时使用的特殊对象。

#### --answer--

这是一个在用户以某种方式与您的网页交互时触发的负载。 

### --question--

#### --text--

以下哪项是用于移除之前通过 `addEventListener()` 方法为元素添加的事件监听器的方法？

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

什么是内联事件处理器？

#### --distractors--

用于取消所有 `event` 的超文本标记语言 `Element` 上的特殊 `attribute`。

---

HTML 元素上的特殊属性，仅用于触发你 JavaScript 代码中的表单提交。

---

用于移除 `event` 的超文本标记语言 `Element` 上的特殊 `attribute`。

#### --answer--

HTML 元素上的特殊属性，用于在事件发生时执行 JavaScript 代码。

### --question--

#### --text--

Web API 的两个主要类别是什么？

#### --distractors--

Java API 和第三方 API。

---

浏览器 API 和 Rust API。

---

系统 API 和第三方 API。

#### --answer--

浏览器 API 和第三方 API。

### --question--

#### --text--

当浏览器解析完所有超文本标记语言页面的元素时，会触发哪个事件？

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

以下哪项是表现元素内联样式的属性？

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

以下哪种方式是使用内联事件处理器的正确方法？

#### --distractors--

```html
<button eventclick="validate()">Validate</button>
```

---

```html
<button click="validate()">Validate</button>
```

---

```html
<button clickevent="validate()">Validate</button>
```

#### --answer--

```html
<button onclick="validate()">Validate</button>
```

### --question--

#### --text--

以下哪个方法用于延迟指定时间的操作？

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

以下哪项可用于直接在 JavaScript 内创建和控制动画？

#### --distractors--

允许动画 API

---

控件动画 API 

---

创建 Animations API

#### --answer--

Web 动画 API 

### --question--

#### --text--

Canvas API 是做什么的？

#### --distractors--

它用于在你的 CSS 文件中动画图形。

---

它仅用于从你的 JavaScript 文件中移除图形。

---

它用于从你的 HTML 文件中移除图形。

#### --answer--

它用于通过你的 JavaScript `file` 操作图形。

### --question--

#### --text--

什么是事件委托？

#### --distractors--

监听事件在事件被取消时的过程。

---

当发生点击 `event` 时监听 `event` 的进程。

---

当发生 change 事件时监听事件的进程。

#### --answer--

监听已冒泡到父级的事件的过程。

### --question--

#### --text--

以下哪项不是你在处理对话框和模态时可以使用的方法？

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`

