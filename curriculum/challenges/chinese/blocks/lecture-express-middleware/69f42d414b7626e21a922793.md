---
id: 69f42d414b7626e21a922793
title: 什么是路由级中间件？
challengeType: 19
dashedName: what-is-router-level-middleware
---

# --description--

路由级中间件类似于应用级中间件，但它绑定到 `express.Router()` 的一个实例，而不是主应用实例。它可以通过创建独立的路由模块来模块化你的 Express 应用。

路由级中间件的工作方式与应用级中间件相同，但它只应用于特定的路由器实例。这使得只对一组路由应用中间件变得更容易，而不是全局应用。

```js
const express = require('express')
const app = express()
const router = express.Router()

// Router-level middleware to log requests to '/menu' routes
router.use((req, res, next) => {
  console.log('Request made to /menu route')
  next() // Pass control to the next handler
})

// Define routes
router.get('/drinks', (req, res) => {
  res.send('Welcome to the drinks menu!')
})

app.use('/menu', router) // Apply the router

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* `router.use()`：这是路由级中间件，只应用于由路由器处理的路由。
* `app.use('/menu', router)`：该中间件只应用于以 `/menu` 开头的路由（例如 `/menu/drinks`）。

路由级中间件允许你将应用拆分为模块化组件，并选择性地将中间件应用于一组路由。这有助于组织和代码清晰。

# --questions--

## --text--

Express 中的路由级中间件是什么？

## --answers--

全局应用于整个应用的中间件。

### --feedback--

关注中间件附加的位置，而不是它的功能。

---

绑定到特定路由器实例的中间件。

---

仅用于记录请求的中间件。

### --feedback--

关注中间件附加的位置，而不是它的功能。

---

用于静态文件的中间件。

### --feedback--

关注中间件附加的位置，而不是它的功能。

## --video-solution--

2

## --text--

如何应用路由级中间件？

## --answers--

通过使用 `app.use()`。

### --feedback--

关注用于将中间件附加到路由器的方法。

---

通过使用 `router.use()`。

---

通过使用 `app.listen()`。

### --feedback--

关注用于将中间件附加到路由器的方法。

---

通过使用 `app.get()`。

### --feedback--

关注用于将中间件附加到路由器的方法。

## --video-solution--

2

## --text--

你在哪里应用路由级中间件？

## --answers--

应用于应用实例。

### --feedback--

考虑路由中间件附加的位置。

---

应用于特定路由器实例。

---

应用于静态文件。

### --feedback--

考虑路由中间件附加的位置。

---

应用于单个路由处理程序。

### --feedback--

考虑路由中间件附加的位置。

## --video-solution--

2
