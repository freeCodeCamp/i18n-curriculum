---
id: 692487767b6b728454f53f29
title: Quiz zu NodeJS-Core-Modulen
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche `fs`-Methode fügt einer bestehenden Datei Inhalt hinzu, ohne sie zu überschreiben?

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

Was passiert, wenn Sie synchrone Methoden aus dem `fs`-Modul verwenden?

#### --distractors--

Sie löschen automatisch vorhandene Dateien.

---

Sie laufen in allen Fällen schneller als asynchrone Methoden.

---

Sie geben standardmäßig Promises zurück.

#### --answer--

Sie blockieren das Programm, bis die Operation abgeschlossen ist.

### --question--

#### --text--

Was bewirkt der `utf8`-Parameter in `fs.readFile()`?

#### --distractors--

Er verhindert, dass Fehler geworfen werden.

---

Er ändert die Dateiendung.

---

Er komprimiert große Dateien.

#### --answer--

Er weist Node an, einen lesbaren String statt eines Puffers zurückzugeben.

### --question--

#### --text--

Welche `Buffer`-Methode erstellt einen neuen Puffer, der mit Nullen gefüllt ist?

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

Was macht `Buffer.toString()`?

#### --distractors--

Konvertiert einen Puffer in Binärcode.

---

Löscht den Inhalt eines Puffers.

---

Zeigt die Byte-Länge eines Puffers an.

#### --answer--

Konvertiert den Pufferinhalt in einen lesbaren String.

### --question--

#### --text--

Welche `crypto`-Funktion erfordert einen privaten Schlüssel, um eine Signatur zu erzeugen?

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

Warum sollten Sie vermeiden, eigene Krypto-Algorithmen direkt zu schreiben?

#### --distractors--

Es ist in Node.js-Anwendungen illegal.

---

Node.js unterstützt keine Verschlüsselung.

---

JavaScript kann keine Hashes berechnen.

#### --answer--

Es kann unsicher sein ohne gut getestete Bibliotheken.

### --question--

#### --text--

Welche `crypto`-Methode wird empfohlen, um sichere zufällige Tokens zu erzeugen, anstelle von `Math.random()`?

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

Welche OS-Modul-Methode gibt die CPU-Architektur zurück?

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

Was gibt `os.userInfo()` zurück?

#### --distractors--

Nur den Benutzernamen.

---

Nur die IP-Adresse des Geräts.

---

Nur die ID des angemeldeten Benutzers.

#### --answer--

Informationen über den aktuellen Systembenutzer.

### --question--

#### --text--

Welche `path`-Methode verbindet Segmente und normalisiert überflüssige Schrägstriche?

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

Welche `path`-Methode erzeugt einen absoluten Pfad basierend auf dem aktuellen Arbeitsverzeichnis?

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

Was stellt die globale Variable `__dirname` dar?

#### --distractors--

Den Namen des Benutzers, der Node ausführt.

---

Die Dateiendung der aktuellen Datei.

---

Einen relativen Ordnerpfad, der mit `./` beginnt.

#### --answer--

Den absoluten Pfad des Verzeichnisses, das die aktuelle Datei enthält.

### --question--

#### --text--

Welche Prozess-Eigenschaft enthält Umgebungsvariablen wie `NODE_ENV`?

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

Was bewirkt das `process.on("exit")`-Ereignis?

#### --distractors--

Es startet die Node-Anwendung neu.

---

Es sendet ein `exit`-Signal an Kindprozesse.

---

Es leert die Ereignisschleifen-Warteschlange.

#### --answer--

Führt Code direkt aus, bevor der Node-Prozess beendet wird.

### --question--

#### --text--

Welcher Stream-Typ kann sowohl Daten lesen als auch schreiben, transformiert sie aber nicht?

#### --distractors--

Readable stream

---

Transform stream

---

Writable stream

#### --answer--

Duplex stream

### --question--

#### --text--

Was ist erforderlich, um das effiziente Lesen und Schreiben großer Dateien zu implementieren?

#### --distractors--

Nur `Buffer.alloc()` verwenden

---

Das `crypto`-Modul importieren

---

Die gesamte Datei zuerst im Speicher speichern

#### --answer--

Streams verwenden, um Daten in Stücken zu verarbeiten

### --question--

#### --text--

Welche Methode verbindet einen lesbaren Stream mit einem beschreibbaren Stream?

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

Welches Stream-Ereignis zeigt an, dass keine Daten mehr geschrieben werden können?

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

Was wird protokolliert, wenn Sie ein Chunk aus einem lesbaren Stream ohne `toString()` ausgeben?

#### --distractors--

Klartextinhalt

---

Die Prozess-PID

---

Name des Betriebssystems

#### --answer--

Ein Pufferobjekt mit Binärdaten
