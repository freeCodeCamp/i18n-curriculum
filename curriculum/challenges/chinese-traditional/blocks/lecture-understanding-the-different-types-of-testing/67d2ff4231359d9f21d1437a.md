---
id: 67d2ff4231359d9f21d1437a
title: 什麼是端對端測試？
challengeType: 19
dashedName: what-is-end-to-end-testing
---

# --description--

端對端測試，簡稱 E2E，從使用者的角度測試真實世界的情境。E2E 測試情境的範例包括測試註冊和登入流程，或測試電子商務網站的結帳。

為了更好地理解端對端測試的運作方式，讓我們來看看 freeCodeCamp 程式碼庫中的一個實際範例。這個範例使用了 Playwright，一個由微軟開發的流行端對端測試框架。

在 freeCodeCamp 程式碼庫中，有一個名為 `e2e` 的目錄，裡面包含捐款、認證、考試等測試。在該目錄中，有一個 `donate-page-donor.spec.ts` 檔案，裡面有針對已驗證捐款者支援者頁面的測試。

第一步是以已經是支持者的示範使用者身份前往捐款頁面：

```js
test.beforeEach(async ({ page }) => {
  execSync("node ./tools/scripts/seed/seed-demo-user --set-true isDonating");
  await page.goto("/donate");
});
```

由於這個檔案中有多個測試，`beforeEach` 掛鉤會在每個測試之前執行。

一旦使用者進入捐款頁面，他們將會看到一則感謝訊息，並附有如何進一步支援 freeCodeCamp 的建議。以下是一些用來檢查捐款頁面上部分文字的測試範例：

```js
test("should render the donate page correctly", async ({ page }) => {
  await expect(
    page.getByText("Thank you for your continued support")
  ).toBeVisible();

  await expect(
    page.getByText(
      "Your contributions are crucial in creating resources that empower millions of people to learn new skills and support their families."
    )
  ).toBeVisible();

  ...
});
```

還有測試會檢查捐贈者在選單列中是否有支持者聯結，以及他們的頭像周圍是否有特殊樣式的邊框以顯示他們是支持者：

```js
test("The menu should have a supporters link", async ({ page }) => {
  const menuButton = page.getByTestId("header-menu-button");
  const menu = page.getByTestId("header-menu");

  await expect(menuButton).toBeVisible();
  await menuButton.click();

  await expect(menu).toBeVisible();

  await expect(page.getByRole("link", { name: "Supporters" })).toBeVisible();
});

test("The Avatar should have a special border for donors", async ({ page }) => {
  const container = page.locator(".avatar-container");
  await expect(container).toHaveClass("avatar-container gold-border");
});
```

透過這些詳細的測試型別，你可以將測試涵蓋率提升到超越單元測試的層級。端對端測試也有助於確保你的應用程式行為正確，並且對使用者來說是可預測的。其他端對端測試工具的範例包括 Cypress、Selenium 和 Puppeteer。

雖然端對端測試很重要，但因為設定、設計和維護都很耗時，所以成本很高。E2E 測試非常適合關鍵的使用者流程，而單元測試則非常適合測試應用程式中的小單元。

# --questions--

## --text--

以下哪一項不是常用的 E2E 測試工具範例？

## --answers--

Cypress

### --feedback--

請參考本課程結尾處討論的內容。

---

Selenium

### --feedback--

請參考本課程結尾處討論的內容。

---

敏捷

---

Puppeteer

### --feedback--

請參考本課程結尾處討論的內容。

## --video-solution--

3

## --text--

以下哪一個是由 Microsoft 開發的熱門端對端測試框架？

## --answers--

Playwright

---

Java

### --feedback--

請參考課程開始時討論的內容。

---

Angular

### --feedback--

請參考課程開始時討論的內容。

---

JUnit

### --feedback--

請參考課程開始時討論的內容。

## --video-solution--

1

## --text--

以下哪一項是端對端測試中常用的測試情境？

## --answers--

測試 CSS 程式碼的有效性。

### --feedback--

請參考課程開始時討論的內容。

---

測試應用程式的效率、性能。

### --feedback--

請參考課程開始時討論的內容。

---

測試 HTML 程式碼的有效性。

### --feedback--

請參考課程開始時討論的內容。

---

測試使用者登入處理程序。

## --video-solution--

4
