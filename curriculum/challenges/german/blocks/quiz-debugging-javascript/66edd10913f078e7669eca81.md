---
id: 66edd10913f078e7669eca81
title: Quiz zum Debuggen von JavaScript
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche Art von Fehler tritt auf, wenn eine Variable verwendet wird, bevor sie definiert ist?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

Welche JavaScript-Anweisung kann verwendet werden, um die Ausführung Ihres Codes absichtlich für Debugging-Zwecke zu pausieren?

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

Welche Konsolenmethode kann verwendet werden, um Daten als Tabelle anzuzeigen?

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

Wie können Sie Netzwerk-Anfragen in den Chrome-Entwicklertools inspizieren?

#### --distractors--

Indem Sie den Tab „Network Links“ verwenden.

---

Indem Sie den Tab „Console“ verwenden.

---

Indem Sie den Tab „Sources“ verwenden.

#### --answer--

Indem Sie den Tab „Network“ verwenden.

### --question--

#### --text--

Wie lautet die korrekte Syntax für die Verwendung von `try...catch` in JavaScript?

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

Welches der folgenden Beispiele ist ein `SyntaxError`?

#### --distractors--

Eine Variable verwenden, bevor sie deklariert wurde.

---

Eine Variable zweimal im gleichen Gültigkeitsbereich verwenden.

---

Eine Variable verwenden, die außerhalb des Gültigkeitsbereichs liegt.

#### --answer--

Ein fehlendes Komma zwischen Elementen eines Arrays.

### --question--

#### --text--

Was ist der Zweck von Haltepunkten beim Debuggen von JavaScript?

#### --distractors--

Automatisches Korrigieren von Codefehlern.

---

Verhindern, dass Code ausgeführt wird.

---

Nachrichten in der Konsole protokollieren.

#### --answer--

Die Ausführung an einem bestimmten Punkt pausieren.

### --question--

#### --text--

Was passiert, wenn die `throw`-Anweisung ausgeführt wird?

#### --distractors--

Das Programm wird sofort beendet und ignoriert den restlichen Code.

---

Die throw-Anweisung protokolliert einen Fehler in der Konsole, stoppt aber die Ausführung nicht.

---

Die Codeausführung wird pausiert, bis Haltepunkte deaktiviert werden.

#### --answer--

Eine Ausnahme wird geworfen und der `catch`-Block (falls vorhanden) wird ausgeführt.

### --question--

#### --text--

Welche Methode würden Sie verwenden, um Variablen oder Ausdrücke während des Debuggens in den Entwicklertools zu inspizieren?

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

Welche Art von Fehler tritt auf, wenn der folgende Code ausgeführt wird?

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

Stack-Überlauf-Fehler

#### --answer--

`ReferenceError`

### --question--

#### --text--

Welches Werkzeug wird üblicherweise verwendet, um Code beim Debuggen Zeile für Zeile durchzugehen?

#### --distractors--

`console.log()`

---

`alert()`

---

`throw`-Anweisung.

#### --answer--

Haltepunkte in den Entwicklertools.

### --question--

#### --text--

Welche Art von Fehler tritt auf, wenn versucht wird, auf eine Eigenschaft von `undefined` zuzugreifen?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

Was bewirkt der `finally`-Block in einer `try...catch`-Anweisung?

#### --distractors--

Wird nur ausgeführt, wenn ein Fehler auftritt.

---

Überspringt die Ausführung, wenn `catch` ausgelöst wird.

---

Beendet den Codeblock sofort.

#### --answer--

Wird immer ausgeführt, unabhängig von Fehlern.

### --question--

#### --text--

Was wäre das Ergebnis, wenn der folgende Code ausgeführt wird?

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

Der Code wirft einen `SyntaxError`.

---

Der Code protokolliert ein leeres Array in der Konsole.

---

Der Code wirft einen Speicherüberlauf-Fehler.

#### --answer--

Der Code wirft einen `RangeError`.

### --question--

#### --text--

Welche der folgenden Aussagen ist wahr über die `debugger`-Anweisung in JavaScript?

#### --distractors--

Die `debugger`-Anweisung wird vom Browser immer ignoriert.

---

Die `debugger`-Anweisung sendet einen Fehlerbericht an den Server.

---

Die `debugger`-Anweisung lädt die Seite immer neu und löscht die Konsole.

#### --answer--

Die `debugger`-Anweisung wird ignoriert, wenn die Entwicklertools nicht geöffnet sind.

### --question--

#### --text--

Was ist der Hauptzweck des „Profilings“ in JavaScript?

#### --distractors--

Unit-Tests für JavaScript-Funktionen schreiben und Profile der Codeausführung erstellen.

---

Die Codeausführung pausieren und die erfassten Ressourcen wie Speicher und CPU freigeben.

---

JavaScript-Code in Maschinencode für schnellere Ausführung umwandeln.

#### --answer--

Leistungsengpässe identifizieren, indem CPU-Auslastung, Funktionsaufrufe und Ausführungszeit aufgezeichnet werden.

### --question--

#### --text--

Was ist ein „Watcher“ im Kontext des Debuggens?

#### --distractors--

Ein Werkzeug zum Pausieren der Codeausführung.

---

Eine eingebaute Funktion zum Protokollieren von Fehlern.

---

Eine JavaScript-Methode zum Umgang mit Ausnahmen.

#### --answer--

Ein Werkzeug zur Überwachung des Werts von Variablen.

### --question--

#### --text--

Wann sollten Sie `console.error()` anstelle von `console.log()` verwenden?

#### --distractors--

Um Tabellendaten anzuzeigen.

---

Um Nachrichten nur während des Debuggens zu protokollieren.

---

Um Informationen über den Browser zu protokollieren.

#### --answer--

Um Fehlermeldungen zu protokollieren.

### --question--

#### --text--

Was macht der `Error()`-Konstruktor in JavaScript?

#### --distractors--

Protokolliert Fehlermeldungen in der Konsole.

---

Führt Code in einer Sandbox aus.

---

Beendet das Programm.

#### --answer--

Erstellt ein neues Fehlerobjekt, das geworfen werden kann.

### --question--

#### --text--

Was ist der Zweck der `console.dir()`-Methode in JavaScript?

#### --distractors--

Ein separates Verzeichnis für Protokolldateien für einzelne Fehlertypen erstellen.

---

Eine visuelle Darstellung der Konsolenausgabe erzeugen und als PDF-Datei speichern.

---

Die Programmausführung stoppen und eine Fehlermeldung bezüglich Speicher- und CPU-Auslastung anzeigen.

#### --answer--

Eine hierarchische, interaktive Liste von Eigenschaften für ein angegebenes JavaScript-Objekt ausgeben.

