---
id: 68376949899fcbad3c9cac23
title: Тест з JavaScript і доступності
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 9 із 10 наведених нижче питань.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який атрибут використовується для забезпечення доступності, щоб вказати, чи елемент керування розгорнутий чи згорнутий?

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

Які два атрибути можна використовувати разом із `aria-expanded` для встановлення програмного зв’язку між елементом керування та елементом, яким він керує?

#### --distractors--

`aria-labelledBy` and `aria-owns`

---

`aria-controls` and `aria-label`

---

`aria-live` and `aria-owns`

#### --answer--

`aria-controls` and `aria-owns`

### --question--

#### --text--

Який із наведених атрибутів створює живу область на вашій сторінці, яку можна використовувати для повідомлення користувачів читачів екрана про динамічні зміни вмісту в цій живій області в момент їх виникнення?

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

Який із наведених `div` елементів буде вимкнений лише для людей, які використовують допоміжні технології, такі як читачі екрана?

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

Яке з наведених є дійсним використанням атрибута `aria-expanded`?

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

Який атрибут використовується для створення програмного зв’язку між елементом керування та елементом, на який він впливає на сторінці?

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

Яке з наведених НЕ є загальним випадком використання живих областей?

#### --distractors--

Повідомлення, які відображаються після виконання дії (наприклад, повідомлення про помилку або підтвердження).

---

Вміст, який оновлюється періодично (наприклад, бігунок, рядок новин або зворотний відлік).

---

Загальні повідомлення про стан (наприклад, оновлення про процес).

#### --answer--

Повідомлення, які відображаються перед виконанням дії (наприклад, діалог підтвердження).

### --question--

#### --text--

Який із наведених прикладів є правильним способом вказати, яка вкладка наразі вибрана?

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

Яке з наведених НЕ є дійсним значенням атрибута `aria-live`?

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

Яке з наведених значень атрибута `aria-live` означає, що оновлення дуже важливе і має найвищий пріоритет?

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`
