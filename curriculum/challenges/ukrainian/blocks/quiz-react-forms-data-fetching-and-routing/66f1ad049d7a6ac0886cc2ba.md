---
id: 66f1ad049d7a6ac0886cc2ba
title: "Тест на форми React, отримання даних і маршрутизацію"
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який із наведених прикладів є прикладом контрольованого вхідного елемента?

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

Який із наведених прикладів є прикладом неконтрольованого вхідного елемента?

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

Який із наведених способів НЕ є загальновживаним для отримання даних у React?

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

Який із наведених хуків зазвичай асоціюється з отриманням даних у React?

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

Що таке мемоізація?

#### --distractors--

Загальний алгоритм, який використовується для пошуку найкоротшого шляху між двома вершинами в графі.

---

Особлива техніка, що використовується для оптимізації продуктивності всіх контрольованих і неконтрольованих вхідних елементів.

---

Алгоритм "розділяй і володарюй", який рекурсивно сортує підмасиви та об’єднує їх разом.

#### --answer--

Техніка оптимізації, при якій результат дорогих викликів функцій кешується (запам’ятовується) на основі конкретних аргументів.

### --question--

#### --text--

Який із наведених хуків використовується для мемоізації обчислених значень?

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

Який із наведених прикладів використовує правильний хук для мемоізації референсів функцій?

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

Що таке залежність?

#### --distractors--

Залежність — це загальновживана бібліотека для тестування, що використовується у застосунках React.

---

Залежність — це тип компонента React, який виконується лише на сервері.

---

Залежність — це спеціальний компілятор, який використовується у застосунках React для компіляції коду React у JavaScript.

#### --answer--

Залежність — це коли один компонент або модуль у застосунку покладається на інший для правильного функціонування.

### --question--

#### --text--

Що таке менеджер пакетів?

#### --distractors--

Інструмент, який використовується для оновлення сторонніх пакетів, встановлених у застосунках.

---

Інструмент, який використовується для керування пакетами, створеними виключно в межах проєкту.

---

Інструмент, який використовується для переміщення залежностей з папки `node_modules` у спеціальну конфігураційну папку.

#### --answer--

Інструмент, який використовується для встановлення, оновлення та видалення залежностей.

### --question--

#### --text--

Який із наведених менеджерів пакетів НЕ є загальновживаним у екосистемі JavaScript?

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

Який із наведених є ключовим конфігураційним файлом, що містить метадані про ваш проєкт?

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

Який із наведених є загальновживаним фреймворком React?

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

Який із наведених способів є правильним для додавання маршрутів у ваш React-застосунок за допомогою React Router?

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

Який із наведених способів є правильним для створення динамічного маршруту в React Router?

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

Який із наведених хуків використовується для доступу до динамічних параметрів із шляху URL?

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

Який із наведених способів є правильним для створення кастомного обробника запитів у Next.js?

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

Що таке prop drilling?

#### --distractors--

Процес створення редюсера, який визначає, як має змінюватися стан у відповідь на конкретні пропси.

---

Процес передачі пропсів від дочірнього компонента до батьківського.

---

Процес створення провайдера контексту для React-застосунку та використання його для передачі пропсів від батьківських до дочірніх компонентів.

#### --answer--

Процес передачі пропсів від батьківського компонента до глибоко вкладених дочірніх компонентів, навіть якщо деякі дочірні компоненти не потребують цих пропсів.

### --question--

#### --text--

Яке з наведених рішень НЕ є загальновживаним для керування станом у React-застосунках?

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

Для чого використовують React Developer Tools?

#### --distractors--

Ці інструменти використовують лише для перевірки проблем доступності у React-застосунках.

---

Ці інструменти використовують для додавання додаткових тестів для React-компонентів.

---

Ці інструменти використовують для мемоізації всіх React-компонентів у застосунку.

#### --answer--

Ці інструменти використовують для інспектування React-компонентів і виявлення проблем із продуктивністю.

### --question--

#### --text--

Що таке React Server Components?

#### --distractors--

Це компоненти, які керують контекстом виключно на сервері.

---

Це мемоізовані компоненти, які відтворюються виключно на сервері.

---

Це компоненти, які керують станом виключно на сервері.

#### --answer--

Це компоненти, які відтворюються виключно на сервері.
