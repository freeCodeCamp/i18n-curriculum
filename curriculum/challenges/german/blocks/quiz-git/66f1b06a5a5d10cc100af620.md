---
id: 66f1b06a5a5d10cc100af620
title: Git-Quiz
challengeType: 8
dashedName: quiz-git
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist `git`?

#### --distractors--

Ein beliebter Typ eines Barrierefreiheits-Tools.

---

Ein häufig verwendeter Linter in Softwareprojekten.

---

Eine spezielle Art von JavaScript-Bibliothek.

#### --answer--

Ein Versionskontrollsystem ermöglicht es Ihnen, Änderungen in Ihrem Projekt zu verfolgen und zu verwalten.

### --question--

#### --text--

Welcher der folgenden ist ein beliebter cloudbasierter Anbieter für Versionskontrolle?

#### --distractors--

Spring Boot

---

Laravel

---

Remix

#### --answer--

GitHub

### --question--

#### --text--

Was bewirkt der Befehl `git config`?

#### --distractors--

Setzt Konfigurationsvariablen für das Rebasen Ihrer Änderungen.

---

Setzt Konfigurationen zur Verwaltung nur von Remote-Repositories auf GitLab.

---

Setzt Konfigurationen zur Lösung von Merge-Konflikten.

#### --answer--

Setzt Konfigurationsvariablen, die dafür verantwortlich sind, wie Git auf Ihrem Rechner arbeitet.

### --question--

#### --text--

Welcher Befehl zeigt die aktuell installierte Git-Version auf Ihrem Rechner an?

#### --distractors--

`git --versions`

---

`git --setVersion`

---

`git --set`

#### --answer--

`git --version`

### --question--

#### --text--

Welches der folgenden Tools wird verwendet, um GitHub-spezifische Aufgaben direkt in der Befehlszeile auszuführen?

#### --distractors--

GitHub CI/CD

---

GitHub Actions

---

GitHub Pages

#### --answer--

GitHub CLI

### --question--

#### --text--

Welche der folgenden Funktionen ermöglicht es Ihnen, Workflows direkt in Ihrem GitHub-Repository zu automatisieren, einschließlich Build, Test und Deployment Ihres Codes?

#### --distractors--

GitHub Pipelines

---

GitHub Cloud

---

GitHub API

#### --answer--

GitHub Actions

### --question--

#### --text--

Welcher der folgenden Befehle initialisiert ein leeres `git`-Repository?

#### --distractors--

`git status`

---

`git set`

---

`git log`

#### --answer--

`git init`

### --question--

#### --text--

Was bewirkt der Befehl `git push`?

#### --distractors--

Dieser Befehl wird verwendet, um die gesamte Git-Historie zu GitHub hochzuladen.

---

Dieser Befehl wird verwendet, um Änderungen vom Remote-Repository auf Ihre lokale Maschine zu holen.

---

Dieser Befehl wird verwendet, um Ihren Code zu GitHub hochzuladen und automatisch in den `main`-Branch zusammenzuführen.

#### --answer--

Dieser Befehl wird verwendet, um Ihre lokalen Commits in ein Remote-Repository zu pushen.

### --question--

#### --text--

Welcher der folgenden Befehle wird verwendet, um ein Repository zu klonen?

#### --distractors--

`git cloning`

---

`git cloned`

---

`git clones`

#### --answer--

`git clone`

### --question--

#### --text--

Welcher der folgenden Befehle listet alle vorherigen Commits mit hilfreichen Informationen wie Autor, Commit-Datum, Commit-Nachricht und Commit-Hash auf?

#### --distractors--

`git rebase`

---

`git status`

---

`git commit`

#### --answer--

`git log`

### --question--

#### --text--

Welcher der folgenden Befehle listet alle Remote-Repositories auf, die mit Ihrem lokalen Git-Repository verbunden sind?

#### --distractors--

`git rebase -v`

---

`git pull -v`

---

`git checkout -v`

#### --answer--

`git remote -v`

### --question--

#### --text--

Was bewirkt der Befehl `git reset`?

#### --distractors--

Dieser Befehl erlaubt es Ihnen, alle Versuche zur Lösung von Merge-Konflikten zurückzusetzen.

---

Dieser Befehl erlaubt es Ihnen, alle Versuche eines `git rebase` zurückzusetzen.

---

Dieser Befehl erlaubt es Ihnen, alle Commits für das Repository zurückzusetzen.

#### --answer--

Dieser Befehl erlaubt es Ihnen, den aktuellen Zustand eines Branches zurückzusetzen.

### --question--

#### --text--

Was ist ein Pull Request?

#### --distractors--

Eine Anfrage, eine Folge von Commits von einem Branch in einen anderen zusammenzuführen.

---

Eine Anfrage, alle Merge-Konflikte zu lösen.

---

Eine Anfrage, alle Commits aus einem Repository zu entfernen.

#### --answer--

Eine Anfrage, Änderungen aus Ihrem Branch in den Zielbranch zu übernehmen.

### --question--

#### --text--

Was stellt der `main`-Branch in einem Projekt dar?

#### --distractors--

Er stellt den primären Dokumentationsbranch für eine Anwendung dar.

---

Er stellt den Hotfix-Branch für eine Anwendung dar.

---

Er stellt den Hauptfeature-Branch für eine Anwendung dar.

#### --answer--

Er stellt den primären oder Produktionsbranch für eine Anwendung dar.

### --question--

#### --text--

Wofür wird GitHub Pages am besten verwendet?

#### --distractors--

Es ist am besten zum Bereitstellen von C#- und Unity-Spielen geeignet.

---

Es ist am besten zum Bereitstellen von reinen Python-Anwendungen geeignet.

---

Es ist am besten zum Bereitstellen von back-end-lastigen Websites geeignet.

#### --answer--

Es ist am besten zum Bereitstellen statischer Websites geeignet.

### --question--

#### --text--

Welcher der folgenden Befehle zeigt den aktuellen Zustand Ihres Arbeitsverzeichnisses an?

#### --distractors--

`git show`

---

`git check`

---

`git update`

#### --answer--

`git status`

### --question--

#### --text--

Welchen Befehl verwenden Sie, um Änderungen vom Remote-Repository abzurufen?

#### --distractors--

`git get`

---

`git sync`

---

`git update`

#### --answer--

`git fetch`

### --question--

#### --text--

Was ist eine `.gitignore`-Datei?

#### --distractors--

Eine spezielle Art von Datei, die Ihre gesamte `git`-Historie anzeigt.

---

Eine spezielle Art von Datei, die Unterschiede in Ihren Änderungen anzeigt.

---

Eine spezielle Art von Datei, die Konflikte für ein Projekt löst.

#### --answer--

Eine spezielle Art von Datei, die `git` mitteilt, bestimmte Dateien und Ordner in einem Projekt nicht zu verfolgen.

### --question--

#### --text--

Welcher der folgenden Befehle listet alle Ihre lokalen Branches auf?

#### --distractors--

`git show`

---

`git log`

---

`git list`

#### --answer--

`git branch`

### --question--

#### --text--

Welcher der folgenden Befehle wird verwendet, um eine Folge von Commits von einem Branch in einen anderen zu verschieben oder zusammenzuführen?

#### --distractors--

`git remote`

---

`git merge`

---

`git switch`

#### --answer--

`git rebase`

