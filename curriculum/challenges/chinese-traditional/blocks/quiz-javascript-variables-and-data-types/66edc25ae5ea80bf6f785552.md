---
id: 66edc25ae5ea80bf6f785552
title: JavaScript 變數和資料型別測驗
challengeType: 8
dashedName: quiz-javascript-variables-and-data-types
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一項最能描述 JavaScript？

#### --distractors--

用於設計網站的標記語言。

---

伺服端程式設計語言。

---

一個資料庫管理系統。

#### --answer--

用於網頁開發的程式語言。

### --question--

#### --text--

以下哪一項不是 JavaScript 資料型別？

#### --distractors--

`Number`

---

`Undefined`

---

`Object`

#### --answer--

`Double`

### --question--

#### --text--

以下哪一項不是有效的 JavaScript 變數宣告？

#### --distractors--

`const x = 0;`

---

`let x;`

---

`var x;`

#### --answer--

`public Int x;`

### --question--

#### --text--

以下哪一項是 JavaScript 中變數的常見命名慣例？

#### --distractors--

JavaScript 範例。

---

閃電盒。

---

長頸鹿式大小寫。

#### --answer--

駝峰式大小寫。

### --question--

#### --text--

在 JavaScript 中，`let` 和 `const` 變數宣告有什麼差異？

#### --distractors--

`const` 無法重新指定或重新宣告。`let` 可以重新指定和重新宣告。

---

`const` 可以被重新指定和重新宣告。`let` 不能被重新指定也不能被重新宣告。

---

`const` 可以重新指定、設值但不能重新宣告。`let` 可以重新宣告，但不能重新指定、設值。

#### --answer--

`const` 無法重新指定或重新宣告。`let` 可以重新指定，但無法重新宣告。

### --question--

#### --text--

為什麼在 JavaScript 中 `string` 被視為不可變的？

#### --distractors--

你不能使用變數來創建字串。

---

字串只能使用字面值創建。

---

你可以更改字串，但只能透過全域變數。

#### --answer--

一旦字串被創建，你就無法直接更改其字元。

### --question--

#### --text--

你如何在 JavaScript 的主控台中列印字串 `Hello, world!`？

#### --distractors--

`print("Hello, world!")`

---

`alert("Hello, world");`

---

`System.out.println("Hello, world!");`

#### --answer--

`console.log("Hello, world!");`

### --question--

#### --text--

下面的程式碼做了什麼？

```js
let hello = "Hello";
hello += " world";
console.log(hello);
```

#### --distractors--

將 `" world"` 重新指定給變數 `hello`，並在主控台中列印出來。

---

在主控台中列印出 `Hello world` 的字元數量。

---

將 `"Hello"` 和 `" world"` 組合成一個新的字串並列印，但 `hello` 的值仍然是 `"Hello"`。

#### --answer--

將 `hello` 的目前值與字串 `" world"` 結合，重新指定給 `hello`，並將其列印到主控台。

### --question--

#### --text--

哪一個 JavaScript 字元用來標示述語的結尾？

#### --distractors--

`:`

---

`,`

---

`.`

#### --answer--

`;`

### --question--

#### --text--

以下哪一項是 JavaScript 中有效的單行註解語法？

#### --distractors--

`# This is a single line comment`

---

`-- This is a single line comment`

---

`/* This is a single line comment */`

#### --answer--

`// This is a single line comment`

### --question--

#### --text--

JavaScript 中的動態類型意味著：

#### --distractors--

你需要在使用 `variable` 之前宣告其資料型別。

---

你不能將一種資料型別的值指定給不同型別的變數。

---

JavaScript 在執行前會進行型別檢查，這表示型別相關的錯誤會在程式碼執行前被發出。

#### --answer--

JavaScript 根據指定給變數的值來推斷型別。

### --question--

#### --text--

以下哪一項會在主控台列印出變數 `x` 的型別？

#### --distractors--

`console.log(x.type);`

---

`console.log(typeOf(x));`

---

`console.log(isNaN(x));`

#### --answer--

`console.log(typeof x);`

### --question--

#### --text--

`console.log(typeof null);` 的輸出會是什麼？

#### --distractors--

`"undefined"`

---

`"null"`

---

`"NaN"`

#### --answer--

`"object"`

### --question--

#### --text--

以下哪一項是 JavaScript 中有效的多行註解？

#### --distractors--

```js
// This is a
// multi-line
// comment
```

---

```py
'''
This is a
multi-line
comment
'''
```

---

```html
<!--
This is a
multi-line
comment
-->
```

#### --answer--

```js
/*
This is a
multi-line
comment
*/
```

### --question--

#### --text--

以下哪一項宣告了在 JavaScript 中無法更改的變數？

#### --distractors--

`var x = 0;`

---

`let x = 0;`

---

`final x = 0;`

#### --answer--

`const x = 0;`

### --question--

#### --text--

以下哪一個是 JavaScript 中有效的變數名稱？

#### --distractors--

`tart@n`

---

`4cats`

---

`!!dogs`

#### --answer--

`$number`

### --question--

#### --text--

以下哪一項不是 JavaScript 中有效的字串串接方法？

#### --distractors--

`console.log(string1 + " " + string2);`

---

`console.log("string1 " + string2);`

---

`console.log(string1.concat(" ", string2));`

#### --answer--

`console.log(string1 string2);`

### --question--

#### --text--

以下哪一個變數名稱正確使用了駝峰式大小寫？

#### --distractors--

`a_long_variable_name`

---

`alongvariableName`

---

`ALongVariableName`

#### --answer--

`aLongVariableName`

### --question--

#### --text--

即使 JavaScript 有自動分號插入，為什麼明確使用分號仍然有益？

#### --distractors--

為了提升程式碼的執行速度。

---

為了與其他程式設計語言保持一致性。

---

為了允許更好的偵錯和錯誤追蹤。

#### --answer--

為了提升程式碼的可讀性和可靠性。

### --question--

#### --text--

以下哪一項不是 JavaScript 資料型別？

#### --distractors--

`Symbol`

---

`Boolean`

---

`Null`

#### --answer--

`Float`
