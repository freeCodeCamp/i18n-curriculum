---
id: 672bd853c985cdfdeb32f4f9
title: 什麼是 `text-shadow` 屬性，以及它如何運作？
challengeType: 19
dashedName: what-is-the-text-shadow-property
---

# --interactive--

CSS 預設不會對文字套用任何陰影。這是一個沒有任何陰影的段落範例。但如果你確實需要添加陰影，你可以使用 `text-shadow` 屬性。

```css
text-shadow: /* Values */
```

在 CSS 中，你可以透過 `X` 偏移量、`Y` 偏移量、模糊半徑和顏色的組合來表述陰影。首先，你需要指定 `X` 和 `Y` 偏移量，分別表現陰影與文字的水平和垂直距離。這些數值是必填的。

以下是如何在陰影上套用 `X` 和 `Y` 偏移量的範例。我們使用 `text-shadow` 屬性，x 偏移量為 `3px`，y 偏移量為 `2px`：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px;
}
```

:::

在瀏覽器中，文字和陰影看起來會相似，但我們也可以透過在偏移量之前或之後指定值來自訂陰影的顏色。讓我們設定陰影顏色。我們將使用十六進位顏色，但你可以使用任何有效的顏色格式。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px #00ffc3;
}
```

:::

我們是在偏移量之後寫顏色，但如果你比較喜歡，也可以在偏移量之前寫顏色。這是等價的：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: #00ffc3 3px 2px;
}
```

:::

在瀏覽器中，陰影會有特定的顏色，因此我們可以非常輕鬆地將它與文字區分開來。既然我們可以區分陰影和文字，了解正值和負值如何影響陰影偏移也很重要。`X` 偏移和 `Y` 偏移的正值會分別將陰影向右和向下移動，而負值則會將陰影向左和向上移動。以下是一個使用負值的範例：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: -3px -2px #00ffc3;
}
```

:::

現在陰影將會相對於文字向左上移動。很好。但陰影看起來並不太好，因為它看起來完全像原始文字，只是放在後面。為了讓它看起來更好，我們需要添加第三個值，也就是模糊半徑。這個值是選用的，但會讓陰影看起來更平滑且更細緻。模糊半徑的預設值是零。數值越高，模糊越大，這表示陰影會變得越淡。

這裡，我們將模糊半徑設定為 `3px`，並且偏移量回到正值：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px 3px #00ffc3;
}
```

:::

現在它開始看起來像陰影了。它更模糊且細緻，所以我們可以專注於主要文字，同時陰影在背景中增添一些風格。從這裡開始，我們可以稍微調整數值，直到找到符合我們需求的偏移量、顏色和模糊半徑組合。

知道 `text` 可以有多個陰影也很有幫助。你只需要在同一個 `text-shadow` 屬性中用逗號分隔它們。它們會依層次從前到後套用，第一個陰影會在最上層。

這是一個範例段落，套用了三層陰影。請注意，所有這些陰影都定義在同一個 `text-shadow` 屬性中，並以逗號分隔：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 
    3px 2px 3px #00ffc3, 
    -3px -2px 3px #0077ff, 
    5px 4px 3px #dee7e5;
}
```

:::

`text-shadow` 是一個強大的 CSS 屬性，用於讓你的文字更突出。它也能增強你的網頁應用程式的整體設計。透過選擇偏移量、模糊度和顏色，你可以創建多種陰影效果。

# --questions--

## --text--

`text-shadow` CSS 屬性的主要用途是什麼？

## --answers--

調整文字的字型大小。

### --feedback--

思考 `text-shadow` 屬性如何影響文字的視覺外觀。

---

要更改文字的顏色。

### --feedback--

思考 `text-shadow` 屬性如何影響文字的視覺外觀。

---

為文字創建陰影效果。

---

控制文字行間距。

### --feedback--

思考 `text-shadow` 屬性如何影響文字的視覺外觀。

## --video-solution--

3

## --text--

你如何創建一個黑色文字陰影，並且水平偏移向右，模糊半徑為 5 像素？

## --answers--

`text-shadow: 5px 0px 0px black;`

### --feedback--

請考慮 `text-shadow` 屬性中值的順序。

---

`text-shadow: 5px 0px 5px black;`

---

`text-shadow: 5px 5px 0px black;`

### --feedback--

請考慮 `text-shadow` 屬性中值的順序。

---

`text-shadow: 0px 5px 0px black;`

### --feedback--

請考慮 `text-shadow` 屬性中值的順序。

## --video-solution--

2

## --text--

你如何創建多層文字陰影效果？

## --answers--

透過在 `text-shadow` 屬性中定義多個陰影。

---

透過將元素彼此巢狀排列。

### --feedback--

思考如何結合多個陰影效果。

---

透過使用 `filter` 屬性。

### --feedback--

思考如何結合多個陰影效果。

---

透過套用背景影像。

### --feedback--

思考如何結合多個陰影效果。

## --video-solution--

1
