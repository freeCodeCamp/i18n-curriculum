---
id: 6923642b185cf68eed95dc46
title: 任務 10
challengeType: 19
dashedName: task-10
lang: es
---

<!-- NO AUDIO -->

# --instructions--

此任務沒有音訊。

閱讀以下問題並選擇正確答案。

# --questions--

## --text--

選擇寫得正確的數字。

## --answers--

`Veintidós`

---

`Quinze`

### --feedback--

數字 15 使用字母 `c`，而非 `z`。

---

`Veintiseis`

### --feedback--

數字 26 的最後一個 `e` 上必須有重音符號。

---

`Catorze`

### --feedback--

數字 14 使用字母 `c`，而非 `z`。

## --video-solution--

1

# --explanation--

正確答案是 `veintidós`，它對應數字 22。請記得它在字母 `o` 上有重音符號。

`Quinze` 是錯誤的。數字 15 是 `quince`。

`Veintiseis` 是錯誤的。數字 26 是 `veintiséis`（第二個 `e` 上有重音符號）。

`Catorze` 是錯誤的。數字 14 是 `catorce`。
