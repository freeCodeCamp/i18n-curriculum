---
id: 67358ac128957c865dcf3ddf
title: JavaScript-Klassen-Quiz
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist der Zweck einer Klasse in JavaScript?

#### --distractors--

Nur Eigenschaften zu definieren, keine Methoden.

---

Nur statische Variablen zu definieren.

---

Nur primitive Daten zu organisieren.

#### --answer--

Wiederverwendbare Vorlagen für Objekte zu erstellen.

### --question--

#### --text--

Wie definieren Sie eine Klasse in JavaScript?

#### --distractors--

Mit dem Schlüsselwort `className`.

---

Mit dem Schlüsselwort `object`.

---

Nur mit dem Schlüsselwort `constructor`.

#### --answer--

Mit dem Schlüsselwort `class`.

### --question--

#### --text--

Welche Methode wird in einer Klasse verwendet, um Eigenschaften zu initialisieren?

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

Was stellt `this` im Konstruktor einer Klasse dar?

#### --distractors--

Das globale Klassenobjekt.

---

Die Funktion selbst.

---

Eine undefinierte Referenz.

#### --answer--

Eine Instanz der Klasse.

### --question--

#### --text--

Welche der folgenden Aussagen beschreibt, wie sich `this` in einer Pfeilfunktion innerhalb einer Klassenmethode verhält?

#### --distractors--

Es erbt den Wert von `this` von der nächstgelegenen Funktion in einem anderen Gültigkeitsbereich.

---

Es bezieht sich auf eine neu erstellte Instanz der Klasse.

---

Es erbt den Wert von `this` vom globalen Objekt im Strict-Modus.

#### --answer--

Es erbt den Wert von `this` aus dem umgebenden Gültigkeitsbereich, in dem es definiert ist.

### --question--

#### --text--

Was ist der Hauptzweck des Schlüsselworts `extends`?

#### --distractors--

Statische Methoden im Konstruktor der Elternklasse zu definieren.

---

Eine Methode in einer Klasse zu erstellen.

---

Standard-Eigenschaften in der Elternklasse zu initialisieren.

#### --answer--

Eine Unterklasse zu erstellen, die von einer Elternklasse erbt.

### --question--

#### --text--

Welches der folgenden Beispiele zeigt, wie man eine `Car`-Klasse erstellt, die von einer `Vehicle`-Klasse erbt?

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

Was ist die Ausgabe des folgenden Codes?

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

Es wird ein Fehler ausgelöst.

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

Welche Funktion hat das Schlüsselwort `super` in einer Unterklasse?

#### --distractors--

Eine neue Instanz der Elternklasse zu definieren.

---

Methoden und Eigenschaften der Elternklasse zu löschen.

---

Nur auf die statischen Methoden der Elternklasse zuzugreifen.

#### --answer--

Den Konstruktor oder Methoden der Elternklasse aufzurufen.

### --question--

#### --text--

Welche der folgenden Aussagen beschreibt eine statische Methode?

#### --distractors--

Eine Methode, die innerhalb der Klasse selbst nicht zugänglich ist.

---

Eine Methode, die nur in Pfeilfunktionen verwendet wird.

---

Eine Methode, die für jede Instanz einer Klasse einzigartig ist.

#### --answer--

Eine Methode, die zur Klasse selbst gehört, nicht zu einer Instanz.

### --question--

#### --text--

Was wird der folgende Code ausgeben?

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

Es wird ein Fehler ausgelöst.

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

Wie greifen Sie auf eine statische Eigenschaft in einer JavaScript-Klasse zu?

#### --distractors--

Indem Sie `this.propertyName` innerhalb einer Methode verwenden.

---

Indem Sie `instance.propertyName` nach der Erstellung einer Instanz verwenden.

---

Indem Sie `super.propertyName` innerhalb einer Unterklasse verwenden.

#### --answer--

Indem Sie `ClassName.propertyName` verwenden.

### --question--

#### --text--

In welchem Szenario würden Sie eine statische Methode einer Instanzmethode vorziehen?

#### --distractors--

Wenn Sie auf die Eigenschaften der Instanz zugreifen und diese ändern müssen.

---

Wenn die Methode auf einer Objektinstanz aufgerufen werden muss.

---

Wenn die Methode nicht auf instanzspezifische Daten angewiesen ist und auf klassenweite Daten arbeitet.

#### --answer--

Wenn die Methode auf Daten arbeitet, die von allen Instanzen der Klasse geteilt werden.

### --question--

#### --text--

Was wird der folgende Code ausgeben?

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

Es wird ein Fehler ausgelöst.

### --question--

#### --text--

Welches Schlüsselwort wird verwendet, um Eigenschaften und Methoden von einer anderen Klasse zu erben?

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

Was wird durch den folgenden Code ausgegeben?

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

Es wird ein Fehler ausgelöst.

#### --answer--

`Human`

### --question--

#### --text--

Welche der folgenden Aussagen beschreibt Vererbung am besten?

#### --distractors--

Sie erlaubt einer Methode, ein neues Objekt zurückzugeben.

---

Sie erlaubt einer Instanz, eigene Eigenschaften zu definieren.

---

Sie erlaubt, eine statische Eigenschaft in eine nicht-statische Eigenschaft umzuwandeln.

#### --answer--

Sie erlaubt einer Klasse, Eigenschaften und Methoden von einer anderen Klasse zu übernehmen.

### --question--

#### --text--

Was passiert, wenn in einer Unterklasse kein Konstruktor definiert ist?

#### --distractors--

Es wird ein Fehler ausgelöst.

---

Die Unterklasse erbt den Konstruktor von irgendeiner Klasse im Code.

---

Die Unterklasse kann keine neuen Instanzen erstellen.

#### --answer--

Standardmäßig wird der Konstruktor der Superklasse verwendet.

### --question--

#### --text--

Was stellt `this` im folgenden Code dar?

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

Das globale Objekt.

---

Die `Animal`-Klasse selbst.

---

Eine undefinierte Variable.

#### --answer--

Die Instanz von `Animal`.

### --question--

#### --text--

Was ist der Hauptzweck statischer Eigenschaften?

#### --distractors--

Standard-Eigenschaften für jede Klasseninstanz zu definieren.

---

Methoden in einer Unterklasse zu überschreiben.

---

Instanzspezifische Daten zu definieren.

#### --answer--

Klassenweite Daten zu speichern, die von allen Instanzen geteilt werden.
