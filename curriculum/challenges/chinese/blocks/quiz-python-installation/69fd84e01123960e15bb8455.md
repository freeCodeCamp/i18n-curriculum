---
id: 69fd84e01123960e15bb8455
title: Python 安装测验
challengeType: 8
dashedName: quiz-python-installation
---

# --description--

要通过测验，你必须正确回答下面 10 个问题中的至少 9 个。

# --quizzes--

## --quiz--

### --question--

#### --text--

在哪里下载适用于 Windows、Mac 和 Linux 的 Python 最合适？

#### --distractors--

从 GitHub 仓库下载

---

从 npm 包管理器下载

---

从 pip 管理器下载

#### --answer--

从官方 Python 网站下载

### --question--

#### --text--

在编程上下文中，终端是什么？

#### --distractors--

一个用于设计应用的图形工具。

---

一个用于代码格式检查和格式化的工具。

---

一个带自动补全功能的 Python 代码编辑器。

#### --answer--

一个用于运行命令的基于文本的接口。

### --question--

#### --text--

IDE 代表什么？

#### --distractors--

互联网开发引擎

---

集成 DevOps 环境

---

内部调试执行器

#### --answer--

集成开发环境

### --question--

#### --text--

以下哪些是流行的 Python IDE 或代码编辑器？

#### --distractors--

JetBrains、CLion、WindSurf

---

Photoshop、Illustrator、Figma

---

Clang、Pip、Deno

#### --answer--

VS Code、PyCharm、Spyder

### --question--

#### --text--

通常如何从终端运行名为 `main.py` 的 Python 文件？

#### --distractors--

`run main.py`

---

`pip main.py`

---

`execute main.py python`

#### --answer--

`python main.py`

### --question--

#### --text--

什么是交互式 shell？

#### --distractors--

一个审计代码错误的程序。

---

一个对代码运行测试的程序。

---

一个用于代码格式检查的程序。

#### --answer--

一个让你一次输入一条命令并查看结果的程序。

### --question--

#### --text--

如何从终端启动 Python 交互式 shell？

#### --distractors--

输入 `node python` 并按 `Enter`。

---

输入 `run python` 并按 `Enter`。

---

输入 `start python shell` 并按 `Enter`。

#### --answer--

输入 `python` 并按 `Enter`。

### --question--

#### --text--

Python 交互式 shell 中 `>>>` 符号表示什么？

#### --distractors--

Python 已停止运行。

---

Python 正在测试你的代码。

---

Python 正在安装包。

#### --answer--

Python 正在等待用户输入文本。

### --question--

#### --text--

REPL 代表什么？

#### --distractors--

运行 执行 进程 循环

---

读取 评估 打印 列表

---

运行期 执行 编程语言

#### --answer--

读取 评估 打印 循环

### --question--

#### --text--

在 Python 交互式 shell 中运行 `print("Hello, world!")` 会发生什么？

#### --distractors--

它会循环打印 `"Hello, world!"`，直到用户停止。

---

它会创建一个文件并在终端打印 `"Hello, world!"`。

---

它会抛出错误，因为那不是有效的 Python 代码。

#### --answer--

它会在终端打印 `"Hello, world!"`。
