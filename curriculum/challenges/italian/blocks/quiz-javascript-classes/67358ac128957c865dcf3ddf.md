---
id: 67358ac128957c865dcf3ddf
title: Quiz sulle classi JavaScript
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual è lo scopo di una classe in JavaScript?

#### --distractors--

Definire solo proprietà, non metodi.

---

Definire solo variabili statiche.

---

Organizzare solo dati primitivi.

#### --answer--

Creare modelli riutilizzabili per oggetti.

### --question--

#### --text--

Come si definisce una classe in JavaScript?

#### --distractors--

Usando la parola chiave `className`.

---

Usando la parola chiave `object`.

---

Usando solo la parola chiave `constructor`.

#### --answer--

Usando la parola chiave `class`.

### --question--

#### --text--

Quale metodo si usa in una classe per inizializzare le proprietà?

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

Cosa rappresenta `this` all’interno di un costruttore di classe?

#### --distractors--

L’oggetto globale della classe.

---

La funzione stessa.

---

Un riferimento indefinito.

#### --answer--

Un’istanza della classe.

### --question--

#### --text--

Quale delle seguenti descrive come si comporta `this` in una funzione freccia all’interno di un metodo di classe?

#### --distractors--

Eredita il valore di `this` dalla funzione più vicina in un ambito diverso.

---

Si riferisce a una nuova istanza creata della classe.

---

Eredita il valore di `this` dall’oggetto globale in modalità strict.

#### --answer--

Eredita il valore di `this` dall’ambito che la racchiude dove è definita.

### --question--

#### --text--

Qual è lo scopo principale della parola chiave `extends`?

#### --distractors--

Definire metodi statici nel costruttore della classe genitore.

---

Creare un metodo in una classe.

---

Inizializzare proprietà predefinite nella classe genitore.

#### --answer--

Creare una sottoclasse che eredita da una classe genitore.

### --question--

#### --text--

Quale dei seguenti è un esempio di come creare una classe `Car` che eredita da una classe `Vehicle`?

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

Qual è l’output del seguente codice?

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

Viene generato un errore.

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

Qual è la funzione della parola chiave `super` in una sottoclasse?

#### --distractors--

Definire una nuova istanza della classe genitore.

---

Eliminare metodi e proprietà dalla classe genitore.

---

Consultare solo i metodi statici della classe genitore.

#### --answer--

Chiamare il costruttore o i metodi della classe genitore.

### --question--

#### --text--

Quale delle seguenti descrive un metodo statico?

#### --distractors--

Un metodo inaccessibile all’interno della classe stessa.

---

Un metodo usato solo in funzioni freccia.

---

Un metodo unico per ogni istanza di una classe.

#### --answer--

Un metodo che appartiene alla classe stessa, non a nessuna istanza.

### --question--

#### --text--

Quale sarà l’output del seguente codice?

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

Viene generato un errore.

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

Come accederesti a una proprietà statica in una classe JavaScript?

#### --distractors--

Usando `this.propertyName` all’interno di un metodo.

---

Usando `instance.propertyName` dopo aver creato un’istanza.

---

Usando `super.propertyName` all’interno di una sottoclasse.

#### --answer--

Usando `ClassName.propertyName`.

### --question--

#### --text--

In quale scenario useresti un metodo statico invece di un metodo di istanza?

#### --distractors--

Quando devi consultare e modificare le proprietà dell’istanza.

---

Quando il metodo deve essere chiamato su un’istanza di oggetto.

---

Quando il metodo non si basa su dati specifici dell’istanza e opera su dati a livello di classe.

#### --answer--

Quando il metodo opera su dati condivisi da tutte le istanze della classe.

### --question--

#### --text--

Quale sarà l’output del seguente codice?

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

Genera un errore.

### --question--

#### --text--

Quale parola chiave si usa per ereditare proprietà e metodi da un’altra classe?

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

Cosa viene stampato dal seguente codice?

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

Viene generato un errore.

#### --answer--

`Human`

### --question--

#### --text--

Quale delle seguenti descrive meglio l’ereditarietà?

#### --distractors--

Permette a un metodo di restituire un nuovo oggetto.

---

Permette a un’istanza di definire le proprie proprietà.

---

Permette a una proprietà statica di essere convertita in non statica.

#### --answer--

Permette a una classe di acquisire proprietà e metodi da un’altra classe.

### --question--

#### --text--

Cosa succede se un costruttore non è definito in una sottoclasse?

#### --distractors--

Viene generato un errore.

---

La sottoclasse eredita il costruttore da qualsiasi classe nel codice.

---

La sottoclasse non può creare nuove istanze.

#### --answer--

Viene usato per impostazione predefinita il costruttore della superclasse.

### --question--

#### --text--

Cosa rappresenta `this` nel seguente codice?

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

L’oggetto globale.

---

La classe `Animal` stessa.

---

Una variabile indefinita.

#### --answer--

L’istanza di `Animal`.

### --question--

#### --text--

Qual è l’uso principale delle proprietà statiche?

#### --distractors--

Definire proprietà predefinite per ogni istanza della classe.

---

Sovrascrivere metodi in una sottoclasse.

---

Definire dati specifici dell’istanza.

#### --answer--

Archiviare dati condivisi a livello di classe tra le istanze.
