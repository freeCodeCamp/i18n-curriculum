---
id: 67f41268a129e63f8e071736
title: Quiz su loop e sequenze
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual è la sintassi corretta per una lista?

#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--

Cosa verrà stampato sulla console?

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--

Che cos’è un iterabile?

#### --distractors--

È un tipo speciale di stringa che può contenere una grande quantità di caratteri.

---

È un tipo speciale di numero usato per rappresentare valori molto grandi.

---

È un tipo di dato speciale usato principalmente nelle classi e nelle funzioni speciali.

#### --answer--

È un tipo speciale di oggetto su cui si può iterare un elemento alla volta.

### --question--

#### --text--

Cosa stamperà sulla console il seguente codice?

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--

Quale dei seguenti metodi si usa per ottenere il numero di elementi in una lista?

#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--

Cosa succede se provi a passare un indice (positivo o negativo) che è fuori dai limiti di una lista?

#### --distractors--

Riceverai un `SyntaxError`.

---

Riceverai un `RangeError`.

---

Riceverai un `TypeError`.

#### --answer--

Riceverai un `IndexError`.

### --question--

#### --text--

Cosa verrà stampato sulla console?

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--

Quale dei seguenti metodi si usa per aggiungere un elemento alla fine di una lista?

#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--

Cosa verrà stampato sulla console?

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--

Quale dei seguenti metodi si usa per inserire un elemento in un indice specifico di una lista?

#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--

Cosa verrà stampato sulla console?

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--

Quale dei seguenti metodi si usa per svuotare una lista?

#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

Che cos’è una tupla?

#### --distractors--

È un tipo di dato mutabile di una sequenza casuale di valori.

---

È un tipo di dato speciale usato per rappresentare numeri grandi.

---

È un metodo usato per aggiungere elementi a una lista.

#### --answer--

È una sequenza ordinata e immutabile di valori.

### --question--

#### --text--

Qual è il modo corretto per estrarre gli elementi da una tupla?

#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--

Quale sarà il risultato del seguente codice?

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--

Produrrà un `RangeError`.

---

Non succederà nulla.

---

Creerà un loop infinito.

#### --answer--

Produrrà un `TypeError`.

### --question--

#### --text--

Quale sarà il risultato del seguente codice?

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--

Quale delle seguenti funzioni si usa per creare una nuova lista con i valori ordinati?

#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--

Qual è il modo corretto di usare un loop `for`?

#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--

Quale delle seguenti funzioni si usa per generare una sequenza di numeri interi?

#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--

Quale delle seguenti funzioni si usa per iterare su più iterabili in parallelo?

#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`
