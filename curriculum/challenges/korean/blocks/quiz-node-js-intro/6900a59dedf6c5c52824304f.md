---
id: 6900a59dedf6c5c52824304f
title: NodeJS 소개 퀴즈
challengeType: 8
dashedName: quiz-node-js-intro
---

# --description--

퀴즈를 통과하려면 아래 10문제 중 최소 9문제에 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

클라이언트 측 JavaScript가 로컬 파일 시스템에 제한된 접근 권한을 가지는 이유는 무엇인가요?

#### --distractors--

JavaScript가 느린 프로그래밍 언어이기 때문입니다.

---

브라우저가 파일을 처리할 충분한 메모리를 가지고 있지 않기 때문입니다.

---

DOM API가 파일 접근을 막기 때문입니다.

#### --answer--

웹 브라우저에서 실행되도록 설계되어 무제한 파일 접근이 보안 위험을 초래할 수 있기 때문입니다.

### --question--

#### --text--

Node.js가 "크로스 플랫폼"이라는 것은 무슨 뜻인가요?

#### --distractors--

여러 프로그래밍 언어를 동시에 실행할 수 있습니다.

---

서로 다른 데이터베이스를 연결할 수 있습니다.

---

운영체제마다 다른 설치 방법이 필요합니다.

#### --answer--

Windows, macOS, Linux 등 모든 운영체제에서 작동합니다.

### --question--

#### --text--

브라우저 환경에서는 접근 가능하지만 Node.js 환경에서는 접근할 수 없는 API는 무엇인가요?

#### --distractors--

파일 시스템 API

---

네트워킹 API

---

운영체제 API

#### --answer--

DOM API

### --question--

#### --text--

Node.js 맥락에서 `npm`는 무엇인가요?

#### --distractors--

사용자 인터페이스를 구축하는 JavaScript 프레임워크입니다.

---

Node.js 애플리케이션에 최적화된 웹 브라우저입니다.

---

Node.js 애플리케이션용 디버깅 도구입니다.

#### --answer--

패키지와 모듈을 설치하고 관리할 수 있는 Node.js용 패키지 관리자입니다.

### --question--

#### --text--

`--lts`를 실행할 때 `nvm install --lts` 플래그는 무엇을 의미하나요?

#### --distractors--

최신 테스트 스냅샷 버전을 설치합니다.

---

가장 가볍고 작은 버전을 설치합니다.

---

가장 많은 기능을 가진 버전을 설치합니다.

#### --answer--

안정성, 신뢰성, 보안을 우선하는 장기 지원 버전을 설치합니다.

### --question--

#### --text--

Node.js 비동기 프로그래밍에서 "콜백"이란 무엇인가요?

#### --distractors--

실패한 작업을 재시작하는 메서드입니다.

---

동기 작업 결과를 저장하는 변수입니다.

---

애플리케이션 성능을 모니터링하는 도구입니다.

#### --answer--

비동기 작업이 완료되었을 때 실행할 내용을 정의하는 함수입니다.

### --question--

#### --text--

`package.json`가 생성하는 `npm init` 파일의 목적은 무엇인가요?

#### --distractors--

애플리케이션의 환경 변수를 저장합니다.

---

웹 서버 설정을 구성합니다.

---

애플리케이션의 HTML 구조를 정의합니다.

#### --answer--

프로젝트의 세부 정보와 의존성을 추적합니다.

### --question--

#### --text--

Node.js에서 CPU 집약적인 작업이 문제가 될 수 있는 이유는 무엇인가요?

#### --distractors--

Node.js는 수학 연산을 수행할 수 없습니다.

---

Node.js는 암호화나 이미지 처리를 지원하지 않습니다.

---

이벤트 루프가 CPU 작업 실행을 막습니다.

#### --answer--

Node.js가 단일 스레드로 실행되어 메인 스레드를 차단할 수 있어 성능 문제를 일으킬 수 있습니다.

### --question--

#### --text--

Node.js로 `server.js`라는 JavaScript 파일을 실행하려면 어떤 명령어를 사용해야 하나요?

#### --distractors--

`npm run server.js`

---

`nvm execute server.js`

---

`javascript server.js`

#### --answer--

`node server.js`

### --question--

#### --text--

개발자가 npm에서 패키지를 선택하기 전에 무엇을 확인해야 하나요?

#### --distractors--

패키지 다운로드 수가 최소 1000건인지 여부

---

패키지가 최근에 만들어졌는지 여부

---

패키지가 유료인지 여부

#### --answer--

패키지가 유지 관리되고 품질과 보안 모범 사례를 따르는지 여부
