---
id: 66edd3f9bef926f129990425
title: ローカルストレージとCRUDクイズ
challengeType: 8
dashedName: quiz-local-storage-and-crud
---

# --description--

クイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

ウェブ開発の文脈でCRUDは何の略ですか？

#### --distractors--

Create, Replace, Undo, Delete.

---

Connect, Remove, Update, Display.

---

Copy, Retrieve, Update, Delete.

#### --answer--

Create, Read, Update, Delete.

### --question--

#### --text--

RESTful APIにおけるCreate操作に一般的に関連付けられているHTTPメソッドはどれですか？

#### --distractors--

`GET`

---

`PUT`

---

`DELETE`

#### --answer--

`POST`

### --question--

#### --text--

データベースの既存レコードを変更するときに通常行われるCRUD操作はどれですか？

#### --distractors--

Read

---

Delete

---

Create

#### --answer--

Update

### --question--

#### --text--

Web Storage APIにおけるlocalStorageとsessionStorageの主な違いは何ですか？

#### --distractors--

localStorageはメモリにデータを保存し、sessionStorageはディスクにデータを保存します。

---

sessionStorageはlocalStorageよりもデータ取得が速いです。

---

localStorageのデータはセッション中のみ利用可能で、sessionStorageはセッションをまたいで持続します。

#### --answer--

localStorageのデータは明示的に削除されるまで持続し、sessionStorageのデータはページセッション終了時にクリアされます。

### --question--

#### --text--

localStorageにデータを保存するために使われるメソッドはどれですか？

#### --distractors--

`localStorage.setData()`

---

`localStorage.saveItem()`

---

`localStorage.storeData()`

#### --answer--

`localStorage.setItem()`

### --question--

#### --text--

sessionStorageから単一のアイテムを削除するにはどうすればよいですか？

#### --distractors--

`sessionStorage.clearItem('key')`

---

`sessionStorage.remove('key')`

---

`sessionStorage.deleteItem('key')`

#### --answer--

`sessionStorage.removeItem('key')`

### --question--

#### --text--

`PUT`と`PATCH`の違いは何ですか？

#### --distractors--

`PUT`は特定のフィールドを変更し、`PATCH`はリソース全体を置き換えます。

---

`PUT`は新しいリソースのみ追加し、`PATCH`は既存のものを更新します。

---

`PUT`はデータ取得に使われ、`PATCH`はデータ送信に使われます。

#### --answer--

`PUT`はリソース全体を置き換え、`PATCH`は特定のフィールドを更新します。

### --question--

#### --text--

キャッシュストレージは何のために使われますか？

#### --distractors--

キャッシュストレージは認証目的でブラウザにユーザー資格情報を安全に永久保存するために使われます。

---

キャッシュストレージはブラウザ内でデータベースシステムを使い大量の構造化データを保存するために使われます。

---

キャッシュストレージは複数のウェブサイトでアクセス可能なキーと値のペアを保存し、ユーザーセッションや設定を追跡するために使われます。

#### --answer--

キャッシュストレージは頻繁にリクエストされるネットワークリソースを保存し、読み込み時間を短縮しパフォーマンスを向上させるために使われます。

### --question--

#### --text--

ウェブ開発の文脈でクッキーとは何ですか？

#### --distractors--

HTTPリクエストの一種。

---

ブラウザで実行されるコードの一部。

---

サーバー上に大きなファイルを保存する方法。

#### --answer--

ユーザーのウェブブラウザに保存される小さなデータの断片。

### --question--

#### --text--

JavaScriptでクッキーを手動で設定するために使われるメソッドはどれですか？

#### --distractors--

`document.setCookie`

---

`document.cookie.set`

---

`document.createCookie`

#### --answer--

`document.cookie`

### --question--

#### --text--

名前が`username`のクッキーを正しく削除するコードスニペットはどれですか？

#### --distractors--

`document.cookie.delete("username");`

---

`document.removeCookie("username");`

---

`cookie.delete("username");`

#### --answer--

`document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";`

### --question--

#### --text--

クッキーに関する次のうち誤っている記述はどれですか？

#### --distractors--

セッションクッキーはブラウザを閉じると期限切れになります。

---

HttpOnlyクッキーはJavaScriptからアクセスできません。

---

セキュアクッキーはHTTPS経由でのみ送信されます。

#### --answer--

クッキーはサーバーだけが設定できます。

### --question--

#### --text--

ブラウザフィンガープリンティングとは何ですか？

#### --distractors--

IPアドレスに基づいてユーザーを識別する手法。

---

データ暗号化の一種。

---

ブラウザのキャッシュにデータを保存する方法。

#### --answer--

ブラウザやデバイスの属性を通じてユーザーを追跡する方法。

### --question--

#### --text--

ローカルストレージにパスワードを保存することに伴う主なリスクは何ですか？

#### --distractors--

ローカルストレージのデータはブラウザタブを閉じると削除されます。

---

ローカルストレージのデータはクッキーを削除すると失われます。

---

ローカルストレージはオフラインでは動作しません。

#### --answer--

ローカルストレージは暗号化されておらず簡単にアクセスされる可能性があります。

### --question--

#### --text--

クライアント側ストレージについて正しい記述はどれですか？

#### --distractors--

クライアント側ストレージはデータ保存に文字列ではなくオブジェクトを使います。

---

クライアント側ストレージは複数デバイス間でユーザーデータを同期するクラウドサービスを使います。

---

クライアント側ストレージはサーバー側ストレージよりも安全です。

#### --answer--

クライアント側ストレージはウェブサイトがユーザーのデバイスにデータを保存できるようにします。

### --question--

#### --text--

localStorageよりIndexedDBを使う主な利点は何ですか？

#### --distractors--

IndexedDBはデータ保存にユーザーの同意を必要としません。

---

IndexedDBはよりシンプルなAPIを持っています。

---

IndexedDBはすべてのウェブブラウザでサポートされています。

#### --answer--

IndexedDBはより大量のデータや複雑なクエリを扱うことができます。

### --question--

#### --text--

IndexedDBのオブジェクトストアからデータを取得する有効な方法はどれですか？

#### --distractors--

オブジェクトストアで`retrieve()`を呼び出す。

---

特定のレコードにアクセスするために`query()`を使う。

---

`fetch()`メソッドを使う。

#### --answer--

ストア参照で`get()`を使う。

### --question--

#### --text--

IndexedDBはどのタイプのトランザクションをサポートしていますか？

#### --distractors--

読み取りトランザクションのみ。

---

非同期トランザクションのみ。

---

同期トランザクションのみ。

#### --answer--

読み取りおよび書き込みトランザクション。

### --question--

#### --text--

Service Workerのライフサイクルについて正しい記述はどれですか？

#### --distractors--

Service Workerはウェブアプリのメインスレッドで動作します。

---

Service Workerはインストール前にアクティベートできます。

---

Service Workerはウェブアプリがブラウザで開いているときのみ動作します。

#### --answer--

Service Workerはアクティベート後にネットワークリクエストをインターセプトできます。

### --question--

#### --text--

プログレッシブウェブアプリ（PWA）の主要な特徴を最もよく表す記述はどれですか？

#### --distractors--

PWAはモバイルデバイスにのみインストール可能です。

---

PWAはバックエンドサーバーが必須です。

---

PWAは特定のブラウザでのみアクセス可能です。

#### --answer--

PWAはService Workerを通じてオフライン機能を提供します。
