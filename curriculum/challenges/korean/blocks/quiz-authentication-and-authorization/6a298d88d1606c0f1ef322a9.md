---
id: 6a298d88d1606c0f1ef322a9
title: 인증 및 권한 부여 퀴즈
challengeType: 8
dashedName: quiz-authentication-and-authorization
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답하세요.

# --quizzes--

## --quiz--

### --question--

#### --text--

JSON Web Token의 세 부분은 무엇인가요?

#### --distractors--

헤더, 본문, 푸터

---

토큰, 페이로드, 해시

---

ID, 클레임, 인증서

#### --answer--

헤더, 페이로드, 서명

### --question--

#### --text--

다음 중 인증 요소로서 "소유한 것"의 예는 무엇인가요?

#### --distractors--

지문 스캔

---

기억한 비밀번호

---

보안 질문 답변

#### --answer--

스마트폰으로 전송된 일회용 코드

### --question--

#### --text--

다음 중 권한 부여를 가장 잘 설명한 것은 무엇인가요?

#### --distractors--

사용자의 자격 증명이 저장된 기록과 일치하는지 확인하는 것

---

사용자가 성공적으로 로그인한 후 세션 토큰을 생성하는 것

---

사용자의 비밀번호를 데이터베이스에 저장하기 전에 암호화하는 것

#### --answer--

인증된 사용자가 수행할 수 있는 작업을 결정하는 것

### --question--

#### --text--

Passport.js 전략이란 무엇인가요?

#### --distractors--

애플리케이션 라우트를 인증 규칙에 매핑하는 구성 파일

---

사용자 비밀번호를 안전하게 해시하고 저장하는 내장 함수

---

사용자가 로그인 절차를 성공적으로 완료한 후 생성되는 토큰

#### --answer--

특정 방법이나 제공자로 사용자를 인증하는 방식을 정의하는 플러그인

### --question--

#### --text--

Helmet.js가 기본적으로 X-Powered-By 헤더에 대해 하는 일은 무엇인가요?

#### --distractors--

헤더를 일반 자리 표시자 값으로 교체한다

---

헤더 값을 클라이언트에 보내기 전에 암호화한다

---

헤더 정보를 HTTP 전용 쿠키로 이동한다

#### --answer--

X-Powered-By 헤더를 완전히 제거한다

### --question--

#### --text--

쿠키의 `SameSite` 속성을 `Strict`로 설정하면 어떤 일이 발생하나요?

#### --distractors--

쿠키를 브라우저에 저장하기 전에 암호화한다

---

쿠키가 HTTPS 연결에서만 전송되도록 보장한다

---

서버가 응답당 하나 이상의 쿠키를 설정하지 못하게 차단한다

#### --answer--

브라우저가 모든 교차 사이트 요청에 쿠키를 보내지 못하게 한다

### --question--

#### --text--

JWT에서 사용자 ID나 역할 같은 사용자 클레임 데이터는 어디에 저장되나요?

#### --distractors--

서명 섹션에

---

헤더 섹션에

---

토큰 외부의 암호화된 컨테이너에

#### --answer--

페이로드 섹션에

### --question--

#### --text--

Helmet이 설정하는 HTTP 헤더 중 다른 도메인의 iframe에 사이트가 임베드되는 것을 막는 것은 무엇인가요?

#### --distractors--

Content-Security-Policy

---

X-Powered-By

---

Strict-Transport-Security

#### --answer--

X-Frame-Options

### --question--

#### --text--

CSRF 공격은 브라우저를 어떻게 악용하나요?

#### --distractors--

피해자 브라우저에 자바스크립트를 주입해 쿠키를 직접 훔친다

---

피해자 브라우저와 신뢰된 서버 간 트래픽을 가로챈다

---

피해자를 가짜 로그인 페이지로 리다이렉트하는 피싱 링크를 보낸다

#### --answer--

브라우저가 피해자의 세션 쿠키를 자동으로 포함한 요청을 보내도록 속인다

### --question--

#### --text--

Express 앱에서 `app.use(passport.initialize())`는 무엇을 하나요?

#### --distractors--

애플리케이션에 새로운 인증 전략을 등록한다

---

앱을 방문하는 모든 사용자에게 세션 쿠키를 생성한다

---

사용 가능한 모든 Passport 전략을 자동으로 설치한다

#### --answer--

들어오는 각 요청에 대해 인증을 처리하도록 Passport를 미들웨어로 설정한다

### --question--

#### --text--

역할 기반 권한 부여 시스템에서 시스템 설정과 사용자 관리를 완전히 접근할 수 있는 역할은 무엇인가요?

#### --distractors--

편집자

---

중재자

---

일반 사용자

#### --answer--

관리자

### --question--

#### --text--

Passport.js 핵심 패키지와 로컬 전략을 설치하는 명령어는 무엇인가요?

#### --distractors--

`npm install passport.js passport-local`

---

`npm install passport passport-auth`

---

`npm install passportjs local-strategy`

#### --answer--

`npm install passport passport-local`

### --question--

#### --text--

왜 JWT 페이로드에 비밀번호를 절대 저장하면 안 되나요?

#### --distractors--

페이로드 크기 제한 때문에 비밀번호 길이 문자열 저장이 불가능하다

---

페이로드에 비밀번호를 저장하면 토큰 서명이 자동으로 무효화된다

---

JWT 토큰이 너무 빨리 만료되어 내부 비밀번호가 쓸모없다

#### --answer--

페이로드는 암호화된 것이 아니라 Base64로 인코딩되어 토큰을 가진 누구나 해독할 수 있다

### --question--

#### --text--

CSRF 토큰의 목적은 무엇인가요?

#### --distractors--

사용자의 세션 쿠키가 브라우저에 저장되기 전에 암호화하기 위해

---

서버에 보내는 모든 API 요청에서 사용자의 신원을 확인하기 위해

---

세션 쿠키를 단기 인증 자격 증명으로 대체하기 위해

#### --answer--

악성 사이트가 읽을 수 없는 정당한 프런트엔드에서 요청이 왔음을 확인하기 위해

### --question--

#### --text--

Helmet이 교차 사이트 스크립팅(XSS)을 방어하기 위해 설정하는 HTTP 응답 헤더는 무엇인가요?

#### --distractors--

X-Frame-Options

---

X-Powered-By

---

Strict-Transport-Security

#### --answer--

Content-Security-Policy

### --question--

#### --text--

사용자가 보호된 리소스에 접근하려 할 때 가장 먼저 수행되어야 하는 과정은 무엇인가요?

#### --distractors--

권한 부여

---

역할 할당

---

세션 생성

#### --answer--

인증

### --question--

#### --text--

사용자가 성공적으로 인증한 후 Passport.js가 요청 객체에 첨부하는 것은 무엇인가요?

#### --distractors--

활성 세션 토큰을 `req.sessionToken`으로

---

로그인에 사용된 전략 이름을 `req.strategy`로

---

사용자의 해시된 비밀번호를 `req.credentials`로

#### --answer--

인증된 사용자 객체를 `req.user`로

### --question--

#### --text--

서버가 페이로드가 변조된 JWT를 받으면 어떻게 되나요?

#### --distractors--

서버가 페이로드를 디코딩하고 사용자 기록을 새 데이터로 업데이트한다

---

서버가 변조된 데이터를 무시하고 원래 토큰 클레임을 사용한다

---

서버가 토큰을 삭제하고 사용자를 등록 페이지로 보낸다

#### --answer--

서명이 일치하지 않아 서버가 요청을 거부한다

### --question--

#### --text--

`SameSite=Strict`와 비교해 `SameSite=Lax`는 무엇을 허용하나요?

#### --distractors--

폼 제출을 포함한 모든 교차 사이트 요청에서 쿠키를 허용한다

---

같은 도메인 외부에서 시작된 모든 요청에서 쿠키를 차단한다

---

교차 사이트 탐색 시 사용자가 다시 로그인해야 한다

#### --answer--

최상위 탐색에서는 쿠키를 허용하지만 교차 사이트 폼 제출과 API 요청에서는 차단한다

### --question--

#### --text--

Helmet이 설정하는 Strict-Transport-Security 헤더는 무엇을 강제하나요?

#### --distractors--

서버가 모든 비 HTTPS 요청을 오류 응답으로 거부하도록 강제한다

---

HTTP 응답 본문 전체를 암호화해 브라우저로 보낸다

---

모든 쿠키에 Secure와 HttpOnly 플래그가 설정되도록 요구한다

#### --answer--

브라우저가 서버와 HTTPS로만 통신하도록 강제한다
