---
id: 673226a62eb2121da41a3d68
title: Cos’è la funzione minmax() e come funziona?
challengeType: 19
dashedName: what-is-the-minmax-function-and-how-does-it-work
---

# --interactive--

La funzione `minmax()` definisce l’intervallo per la dimensione di una traccia della griglia, specificando quanto spazio può occupare una riga o una colonna.

Ricorda che puoi impostare la dimensione della traccia con unità come `px` (pixel), `rem` o anche `em`, e con unità frazionarie (`fr`).

La funzione `minmax()` va oltre permettendoti di impostare una dimensione minima e una dimensione massima per la traccia della griglia.

Ecco la sintassi della funzione `minmax()`:

```css
minmax(min, max)
```

`min` è la dimensione minima della traccia della griglia, che può essere impostata usando pixel, percentuale o `auto`. E `max` è la dimensione massima della traccia della griglia che puoi impostare con le stesse unità.

I due valori lavorano insieme in questo modo:

- Il valore `min` assicura che la traccia della griglia non si riduca mai sotto una dimensione impostata.

- Il valore `max` limita quanto grande la traccia della griglia può diventare.

La dimensione della traccia della griglia si adatta dinamicamente tra i valori `min` e `max` in base al contenuto e alla dimensione del contenitore.

Vediamo un esempio pratico:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="grid-container">
  <div>
    <h2>Item 1</h2>
  </div>
  <div>
    <h2>Item 2</h2>
  </div>
</div>
```

```css
.grid-container {
  display: grid;
  grid-template-columns: minmax(150px, 300px) 1fr;
  gap: 20px;
}

.grid-container > div {
  background: crimson;
  padding: 20px;
  text-align: center;
}
```

:::

Cosa succede qui? 

La prima colonna, `minmax(150px, 300px)`, sarà sempre almeno `150px` e al massimo `300px`, a seconda dello spazio disponibile.

D’altra parte, la seconda colonna, `1fr`, occuperà tutto lo spazio rimanente disponibile nel contenitore griglia dato che non ci sono altre colonne con cui condividere lo spazio.

Il vantaggio della funzione `minmax()` rispetto a dimensioni fisse e persino alle unità `fr` è che è più flessibile, rendendola ideale per adattabilità e responsività.

# --questions--

## --text--

Quale funzione puoi usare per definire l’intervallo per la dimensione di una traccia della griglia?

## --answers--

`clamp()`

### --feedback--

Questa funzione serve a limitare un valore tra un minimo e un massimo ma non definisce intervalli per tracce della griglia.

---

`minmax()`

---

`max-width()`

### --feedback--

Questa proprietà imposta la larghezza massima di un elemento, non un intervallo per tracce della griglia.

---

`calc()`

### --feedback--

Questa funzione esegue calcoli ma non definisce intervalli per tracce della griglia.

## --video-solution--

2

## --text--

Quale delle seguenti descrive meglio come si comporta la dimensione della traccia della griglia quando si usa la funzione `minmax()`?

## --answers--

La dimensione della traccia della griglia è fissa tra i valori minimo e massimo.

### --feedback--

La dimensione della traccia della griglia si adatta dinamicamente, non è fissa, tra i valori minimo e massimo dati.

---

La dimensione della traccia della griglia si adatta dinamicamente tra i valori minimo e massimo in base al contenuto e alla dimensione del contenitore.

---

La dimensione della traccia della griglia sarà sempre impostata al valore massimo, indipendentemente dal contenuto.

### --feedback--

La dimensione della traccia della griglia si adatta sia al valore minimo che a quello massimo, non solo al massimo.

---

La dimensione della traccia della griglia risponde solo al contenuto, non alla dimensione del contenitore.

### --feedback--

La dimensione si adatta dinamicamente sia in base al contenuto che alla dimensione del contenitore.

## --video-solution--

2

## --text--

Cosa controllano i valori min e max nella funzione `minmax()`?

## --answers--

Il valore min permette la riduzione, e il valore max imposta una dimensione fissa.

### --feedback--

Il valore min impedisce che la traccia si riduca sotto la dimensione specificata, e il valore max limita la crescita.

---

Il valore min assicura che la traccia della griglia non si riduca sotto una dimensione impostata, e il valore max limita quanto può crescere.

---

Il valore min limita la crescita, e il valore max determina la dimensione più piccola.

### --feedback--

Il valore max limita la crescita, e il valore min assicura una dimensione minima, non il contrario.

---

Entrambi i valori impostano dimensioni fisse per la traccia della griglia.

### --feedback--

I valori min e max definiscono un intervallo, non dimensioni fisse.

## --video-solution--

2
