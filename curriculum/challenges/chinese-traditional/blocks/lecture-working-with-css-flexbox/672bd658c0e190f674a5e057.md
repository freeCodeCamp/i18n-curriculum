---
id: 672bd658c0e190f674a5e057
title: 常見的 Flex 屬性有哪些，它們如何運作？
challengeType: 19
dashedName: what-are-some-common-flex-properties
---

# --interactive--

Flex 屬性是你可以套用到彈性容器的屬性，用來決定子元素的分佈。我們將涵蓋一些最常用的屬性：`flex-wrap`、`justify-content` 和 `align-items`。

讓我們從 `flex-wrap` 開始。這個屬性決定了彈性項目如何在彈性容器內換行以適應可用空間。`flex-wrap` 可以有三種可能的值：`nowrap`、`wrap` 和 `wrap-reverse`。`nowrap` 是預設值——即使彈性項目的寬度超過容器的寬度，也不會換行到新的一行。

在下面的程式碼中，我們有三個 `div` 元素。讓我們專注於 `width`。以紅色邊框標示的 `main` 容器的 `width` 為 `200px`，而其三個子 `div` 元素合計的 `width` 為 `240px`（每個 `80px`）：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::


`div` 元素的寬度超過它們的容器寬度，但預設會縮小以適應可用空間。如果你確實想在它們超過容器寬度時換行，可以在彈性容器上設定 `flex-wrap: wrap`：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  flex-wrap: wrap;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

當 `div` 元素超過其容器的寬度時，將會以列的方式重新排列。你可以使用 `flex-wrap: wrap-reverse` 反向換行彈性項目。`flex-flow` 屬性是 `flex-direction` 和 `flex-wrap` 的縮寫屬性。在此範例中，我們將 `flex-direction` 設為 `column`，並將 `flex-wrap` 設為 `wrap-reverse`：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  flex-flow: column wrap-reverse;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

很好。現在讓我們來談談 `justify-content`。`justify-content` 將子元素沿著彈性容器的主軸對齊。如果你為 `justify-content` 指定值為 `flex-start`，彈性項目將會對齊到主軸的起點。這可以是水平或垂直方向：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: flex-start;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

使用 `justify-content: flex-end` 時，彈性項目會沿主軸的末端對齊，無論是水平還是垂直方向。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: flex-end;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

若要沿主軸置中 `flex` 項目，請使用 `justify-content: center`。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: center;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

要沿主軸均勻分配元素，你可以使用 `justify-content: space-between`。如果需要，這會在彈性項目之間添加一些間距。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-between;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

`justify-content: space-around` 在主軸內均勻分配彈性項目，並在第一個項目前和最後一個項目後添加空間。這個額外的空間是每對相鄰項目之間空間的一半。如果只有一個項目要分配，則會置中。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-around;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::


最後但同樣重要的是，我們有 `justify-content: space-evenly`，它會沿主軸平均分配項目。項目之間的間距以及第一個和最後一個元素之前和之後的間距完全相同：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-evenly;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

太好了。現在你知道如何沿主軸分配彈性項目。但你可能也想沿交叉軸分配它們。請記住，交叉軸與主軸垂直。你可以使用 `align-items` 屬性來做到這一點。要將項目沿交叉軸置中，只需為彈性容器添加 `align-items: center`： 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: center;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

在此範例中，彈性項目沿著交叉軸置中，交叉軸預設為垂直方向。如果交叉軸為水平方向，則它們會改為水平置中。相反地，`align-items: flex-start` 將項目對齊到交叉軸的起點：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: flex-start;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

相反的是 `align-items: flex-end`。這會將彈性項目對齊到交叉軸的末端，垂直或水平。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: flex-end;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

要沿著交叉軸拉伸彈性項目，你可以使用 `align-items: stretch`。這也適用於自動調整大小的元素，例如沒有設定 `width` 或 `height`，或只有最小 `width` 或 `height` 的元素。彈性項目將會在交叉軸方向上拉伸以填滿容器。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: stretch; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;           
  border: 1px solid #888;
  background-color: lightblue;
}
```

:::

最後，你可以使用 `align-self` 屬性為單一彈性項目指定交叉軸上的不同對齊方式。例如，你可以使用 `align-self: stretch` 將它拉伸。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: stretch; 
  background-color: lightcoral;
}
```

:::

你可以用 `align-self: center` 將它置中。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: center; 
  background-color: lightcoral;
}
```

:::

你可以使用 `align-self: flex-start` 將其對齊到交叉軸的起點。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: flex-start; 
  background-color: lightcoral;
}
```

:::

或者你可以使用 `align-self: flex-end` 將其對齊到交叉軸的末端。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: flex-end; 
  background-color: lightcoral;
}
```

:::

還有其他的 flex 屬性和值，你可以選擇用來創建你所想像的響應式佈局，但這些是最常用的。利用這些 CSS flex 屬性和你對 CSS flex 模型的新認識，你可以開始創建響應式佈局，為不同裝置打造流暢且包容的使用者體驗。

# --questions--

## --text--

哪一個 CSS 屬性用於在 flex 容器中水平對齊子元素（預設情況下）？

## --answers--

`flex-direction`

### --feedback--

思考元素在彈性容器中如何水平對齊。

---

`justify-content`

---

`align-items`

### --feedback--

思考元素在彈性容器中如何水平對齊。

---

`flex-wrap`

### --feedback--

思考元素在彈性容器中如何水平對齊。

## --video-solution--

2

## --text--

哪一個 CSS 屬性用於在 flex 容器內垂直對齊子元素（預設情況下）？

## --answers--

`flex-direction`

### --feedback--

思考元素在彈性容器中如何垂直對齊。

---

`justify-content`

### --feedback--

思考元素在彈性容器中如何垂直對齊。

---

`align-items`

---

`flex-wrap`

### --feedback--

思考元素在彈性容器中如何垂直對齊。

## --video-solution--

3

## --text--

以下哪些屬性可以合併到 `flex-flow` 屬性中？

## --answers--

`flex-direction` 和 `justify-content`。

### --feedback--

思考 `flex` 項目佈局的兩個主要面向。

---

`flex-direction` 和 `align-items`。

### --feedback--

思考 `flex` 項目佈局的兩個主要面向。

---

`flex-direction` 和 `align-content`。

### --feedback--

思考 `flex` 項目佈局的兩個主要面向。

---

`flex-direction` 和 `flex-wrap`。

## --video-solution--

4
