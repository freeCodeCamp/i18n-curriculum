---
id: 66edc3ab8c6413c344f401bf
title: JavaScript-Mathematik-Quiz
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Wofür steht `NaN`?

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

Welcher der folgenden ist KEIN arithmetischer Operator in JavaScript?

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

Was passiert, wenn Sie in JavaScript versuchen, durch Null zu dividieren?

#### --distractors--

JavaScript gibt 0 aus.

---

Es passiert nichts.

---

JavaScript wirft einen Fehler.

#### --answer--

JavaScript gibt `Infinity` aus.

### --question--

#### --text--

Welche der folgenden Verwendungen des Exponentiation-Operators ist korrekt?

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

Welchen Operator sollten Sie verwenden, um eine Variable in JavaScript zu dekrementieren?

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

Welcher der folgenden ist KEIN bitweiser Operator?

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

Welcher der folgenden zusammengesetzten Operatoren ist korrekt, um den Rest zu erhalten?

#### --distractors--

`//=`

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

Was ist der Unterschied zwischen den `==`- und `===`-Operatoren?

#### --distractors--

Es gibt keinen Unterschied. Das Vergleichsergebnis ist immer dasselbe.

---

Der `==`-Operator wird verwendet, um zu prüfen, ob der Datentyp derselbe ist, während der `===` prüft, ob der Wert gleich ist.

---

Der `==`-Operator kann nur mit primitiven Typen verwendet werden, während der `===`-Operator nur für Objekte verwendet wird.

#### --answer--

Der `==`-Operator vergleicht nur Werte, während der `===`-Operator Werte und Typen vergleicht.

### --question--

#### --text--

Wofür wird der unäre Plus-Operator (`+`) verwendet?

#### --distractors--

Er wird verwendet, um einen Wert um eins zu verringern.

---

Er wird verwendet, um den Rest einer Operation zu erhalten.

---

Er wird verwendet, um die binäre Darstellung einer Zahl zu invertieren.

#### --answer--

Er wird verwendet, um seinen Operanden in eine Zahl umzuwandeln.

### --question--

#### --text--

Welche der folgenden `Math`-Methoden rundet einen Wert auf die nächste ganze Zahl?

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

Was macht die `Math.sqrt()`-Methode?

#### --distractors--

Diese Methode gibt eine zufällige Fließkommazahl zurück.

---

Diese Methode gibt den Absolutwert einer Zahl zurück.

---

Diese Methode gibt die Kubikzahl einer Zahl zurück.

#### --answer--

Diese Methode gibt die Quadratwurzel einer Zahl zurück.

### --question--

#### --text--

Welche der folgenden ist KEINE gültige Methode für das `Math`-Objekt?

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

Welche der folgenden gibt `false` zurück?

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

Was macht die `parseFloat()`-Methode?

#### --distractors--

Sie parst ein String-Argument und führt eine bitweise Operation darauf aus.

---

Sie parst ein String-Argument und gibt `false` zurück.

---

Sie parst ein String-Argument und gibt eine Ganzzahl zurück.

#### --answer--

Sie parst ein String-Argument und gibt eine Fließkommazahl zurück.

### --question--

#### --text--

Was wird in der Konsole ausgegeben?

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

Was wäre die Ausgabe für den folgenden Code?

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

Was ist Operator-Priorität?

#### --distractors--

Operator-Priorität wird verwendet, um zu prüfen, ob der Wert links kleiner ist als der rechts.

---

Operator-Priorität wird verwendet, um eine Variable auf die angegebene Potenz zu erhöhen und das Ergebnis der Variable zuzuweisen.

---

Operator-Priorität wird verwendet, um den Wert um eins zu erhöhen.

#### --answer--

Operator-Priorität bestimmt die Reihenfolge, in der Operationen in einem Ausdruck ausgewertet werden.

### --question--

#### --text--

Welcher der folgenden ist KEIN Vergleichsoperator?

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

Was macht der unäre Negationsoperator (`-`)?

#### --distractors--

Er kehrt den Boolean-Wert seines Operanden um.

---

Er verringert den Wert um eins.

---

Er erhöht den Wert um eins.

#### --answer--

Er negiert den Operanden.

### --question--

#### --text--

Welches der folgenden ist ein Beispiel für die Verwendung des nullish coalescing Operators?

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```

