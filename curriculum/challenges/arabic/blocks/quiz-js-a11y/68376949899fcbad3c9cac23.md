---
id: 68376949899fcbad3c9cac23
title: اختبار JavaScript وإمكانية الوصول
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

لتجاوز الاختبار، يجب أن تجيب بشكل صحيح على 9 أسئلة على الأقل من أصل 10 أسئلة أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي خاصية تُستخدم لأغراض إمكانية الوصول لتحديد ما إذا كان عنصر التحكم موسعًا أو مطويًا؟

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

ما الخاصيتان اللتان يمكن استخدامهما مع `aria-expanded` لإنشاء ارتباط برمجي بين عنصر التحكم والعنصر الذي يتحكم به؟

#### --distractors--

`aria-labelledBy` و `aria-owns`

---

`aria-controls` و `aria-label`

---

`aria-live` و `aria-owns`

#### --answer--

`aria-controls` و `aria-owns`

### --question--

#### --text--

أي من الخصائص التالية تنشئ منطقة حية على صفحتك يمكن استخدامها لإخطار مستخدمي قارئ الشاشة بالتغييرات الديناميكية في المنطقة الحية عند حدوثها؟

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

أي من عناصر `div` التالية سيتم تعطيلها فقط للأشخاص الذين يستخدمون تقنيات المساعدة، مثل قارئات الشاشة؟

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

أي من الاستخدامات التالية لخاصية `aria-expanded` صحيحة؟

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

أي خاصية تُستخدم لإنشاء علاقة برمجية بين عنصر تحكم والعنصر الذي يؤثر عليه في الصفحة؟

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

أي مما يلي ليس حالة استخدام شائعة للمناطق الحية؟

#### --distractors--

الرسائل التي تُعرض بعد اتخاذ إجراء (مثل رسالة خطأ أو تأكيد).

---

المحتوى الذي يتحدث بشكل دوري (مثل شريط الأخبار، أو شريط التمرير، أو عداد تنازلي).

---

رسائل الحالة العامة (مثل التحديثات حول عملية ما).

#### --answer--

الرسائل التي تُعرض قبل اتخاذ إجراء (مثل مربع حوار التأكيد).

### --question--

#### --text--

أي من الأمثلة التالية هي الطريقة الصحيحة للإشارة إلى التبويب المحدد حاليًا؟

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

أي من القيم التالية ليست قيمة صحيحة لخاصية `aria-live`؟

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

أي من قيم خاصية `aria-live` التالية تعني أن التحديث مهم جدًا وله أعلى أولوية؟

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`

