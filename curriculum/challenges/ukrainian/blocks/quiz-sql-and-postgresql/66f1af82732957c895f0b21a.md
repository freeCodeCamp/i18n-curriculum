---
id: 66f1af82732957c895f0b21a
title: Тест з SQL і PostgreSQL
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке реляційна база даних?

#### --distractors--

База даних, структурована для розпізнавання відсутності зв’язку між збереженими елементами.

---

База даних, що зберігає неструктуровані дані.

---

Плоска система на основі файлів.

#### --answer--

База даних, структурована для розпізнавання та керування зв’язками між даними в таблицях.

### --question--

#### --text--

Чим не реляційна база даних відрізняється від реляційної?

#### --distractors--

Не реляційні бази даних використовують таблиці, а реляційні — ні.

---

Реляційні бази даних можуть зберігати неструктуровані дані.

---

Не реляційні бази даних повільніші за реляційні.

#### --answer--

Не реляційні бази даних можуть зберігати неструктуровані дані.

### --question--

#### --text--

Що робить команда `\l`, коли її виконують у інтерактивному терміналі PostgreSQL (`psql`)?

#### --distractors--

Показує всі схеми.

---

Показує всі таблиці.

---

Підключається до бази даних.

#### --answer--

Показує всі бази даних.

### --question--

#### --text--

Яка з наведених команд створює нову таблицю в SQL?

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

Яке призначення зовнішнього ключа в реляційній базі даних?

#### --distractors--

Дозволити дублювання даних.

---

Унікально ідентифікувати рядок у таблиці.

---

Зберігати великі обсяги текстових даних.

#### --answer--

Створити зв’язок між двома таблицями.

### --question--

#### --text--

Яка команда додасть новий стовпець до існуючої таблиці?

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

Який тип зв’язку існує, коли один запис у Таблиці А відповідає одному або кільком записам у Таблиці Б?

#### --distractors--

Один-до-одного (1-1)

---

Багато-до-багатьох (m-m)

---

Багато-до-одного (m-1)

#### --answer--

Один-до-багатьох (1-m)

### --question--

#### --text--

Що таке запит?

#### --distractors--

Запит на видалення всіх даних із бази даних.

---

Спеціальний рядок для унікальних значень у базі даних.

---

Спеціальний стовпець для унікальних значень у базі даних.

#### --answer--

Запит на отримання конкретних даних із бази даних.

### --question--

#### --text--

Як видалити стовпець із таблиці в SQL?

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

Яка функція обмеження `PRIMARY KEY` у SQL?

#### --distractors--

Зв’язати дві таблиці.

---

Визначити значення за замовчуванням для стовпця.

---

Дозволити null-значення у стовпці.

#### --answer--

Гарантувати, що стовпець містить унікальні значення.

### --question--

#### --text--

Що робить команда `\c` у інтерактивному терміналі PostgreSQL (`psql`)?

#### --distractors--

Показує всі таблиці в поточній базі даних.

---

Відображає деталі поточного підключення до бази даних.

---

Виходить із оболонки `psql`.

#### --answer--

Підключається до вказаної бази даних.

### --question--

#### --text--

Що таке складений ключ у реляційній базі даних?

#### --distractors--

Ключ, який походить від іншого ключа.

---

Ключ, який часто змінюється залежно від оновлень даних.

---

Ключ, який використовується виключно для зовнішніх ключів.

#### --answer--

Ключ, що складається з кількох стовпців для унікальної ідентифікації запису.

### --question--

#### --text--

Яка команда вставляє дані в таблицю?

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

Як перейменувати стовпець у SQL?

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

Який запит видалить усі записи з таблиці employees?

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

Яке обмеження SQL гарантує, що стовпець може містити лише унікальні значення, допускаючи null?

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

Яке з наведених не є прикладом загальновживаної реляційної бази даних у галузі?

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQLQuery

### --question--

#### --text--

Що означає зв’язок один-до-одного (1-1) у реляційній базі даних?

#### --distractors--

Кожен запис у Таблиці А відповідає одному або кільком записам у Таблиці Б.

---

Кожен запис у Таблиці А не відповідає жодному запису в Таблиці Б.

---

Кожен запис у Таблиці А відповідає кільком записам у Таблиці Б і навпаки.

#### --answer--

Кожен запис у Таблиці А відповідає точно одному запису в Таблиці Б.

### --question--

#### --text--

Яка команда покаже всі таблиці в поточній базі даних PostgreSQL?

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

Що з наведеного дозволяє приєднати таблицю до самої себе?

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`
