---
id: 5900f3971000cf542c50feaa
title: 'Zoezi la 43: Ugawaji wa mfuatano wa herufi ndogo'
challengeType: 1
forumTopicId: 302100
dashedName: problem-43-sub-string-divisibility
---

# --description--

Nambari 1406357289 ni nambari yenye tarakimu zote kutoka 0 hadi 9 kwa sababu inajumuisha kila tarakimu kutoka 0 hadi 9 kwa mpangilio fulani, lakini pia ina sifa ya kuvutia ya ugawaji wa mfuatano wa herufi ndogo.

Acha $d_1$ iwe tarakimu ya $1^{st}$, $d_2$ iwe tarakimu ya $2^{nd}$, na kadhalika. Kwa njia hii, tunabaini yafuatayo:

- ${d_2}{d_3}{d_4} = 406$ inagawika kwa 2
- ${d_3}{d_4}{d_5} = 063$ inagawika kwa 3
- ${d_4}{d_5}{d_6} = 635$ inagawika kwa 5
- ${d_5}{d_6}{d_7} = 357$ inagawika kwa 7
- ${d_6}{d_7}{d_8} = 572$ inagawika kwa 11
- ${d_7}{d_8}{d_9} = 728$ inagawika kwa 13
- ${d_8}{d_9}{d_{10}} = 289$ inagawika kwa 17

Tafuta jumla ya nambari zote zenye tarakimu zote kutoka 0 hadi `n` zenye mfuatano wa herufi ndogo unaokidhi `n - 2` ya sifa hizi za ugawaji.

**Kumbuka:** Nambari yenye tarakimu zote kuanzia na `0` zinapaswa kuzingatiwa katika matokeo.

# --hints--

`substringDivisibility(5)` inapaswa kurudisha nambari.

```js
assert(typeof substringDivisibility(5) === 'number');
```

`substringDivisibility(5)` inapaswa kurudisha `12444480`.

```js
assert.strictEqual(substringDivisibility(5), 12444480)
```

`substringDivisibility(7)` inapaswa kurudisha `1099210170`.

```js
assert.strictEqual(substringDivisibility(7), 1099210170)
```

`substringDivisibility(8)` inapaswa kurudisha `1113342912`.

```js
assert.strictEqual(substringDivisibility(8), 1113342912)
```

`substringDivisibility(9)` inapaswa kurudisha `16695334890`.

```js
assert.strictEqual(substringDivisibility(9), 16695334890)
```

# --seed--

## --seed-contents--

```js
function substringDivisibility(n) {

  return true;
}

substringDivisibility(5);
```

# --solutions--

```js
function substringDivisibility(n) {
  function isSubDivisible(digits) {
    const factors = [2, 3, 5, 7, 11, 13, 17];

    for (let i = 1; i < digits.length - 2; i++) {
      const subNumber = digits[i] * 100 + digits[i + 1] * 10 + digits[i + 2];
      if (subNumber % factors[i - 1] !== 0) {
        return false;
      }
    }
    return true;
  }

  function heapsPermutations(k, digits, conditionCheck, results) {
    if (k === 1) {
      if (conditionCheck(digits)) {
        const number = parseInt(digits.join(''), 10);
        results.push(number);
      }
      return;
    }

    heapsPermutations(k - 1, digits, conditionCheck, results);

    for (let i = 0; i < k - 1; i++) {
      if (k % 2 === 0) {
        [digits[i], digits[k - 1]] = [digits[k - 1], digits[i]];
      } else {
        [digits[0], digits[k - 1]] = [digits[k - 1], digits[0]];
      }
      heapsPermutations(k - 1, digits, conditionCheck, results);
    }
    return;
  }

  const allowedDigits = [...new Array(n + 1).keys()];
  const divisiblePandigitals = [];
  heapsPermutations(
    allowedDigits.length,
    allowedDigits,
    isSubDivisible,
    divisiblePandigitals
  );

  let sum = 0;
  for (let i = 0; i < divisiblePandigitals.length; i++) {
    sum += divisiblePandigitals[i];
  }

  return sum;
}
```
