---
id: 69ae7684b4a814be6a4f2be9
title: MCP 检查器
challengeType: 11
videoId: OIr0TIWc0Bs
dashedName: mcp-inspector
---

# --description--

在这个 `video` 中，你将学习 MCP Inspector。

# --questions--

## --text--

MCP Inspector 的主要函数是什么？

## --answers--

一个用于保存数据集和嵌入以供检索的组件。

---

一个自动为 AI `applications` 生成 `prompts` 的特色。

---

一个用于在 MCP `server` 上直接训练新建 `LLM` 的工具。

---

用于检查和调试 MCP `server` 和 `client` 的工具。

## --video-solution--

4

