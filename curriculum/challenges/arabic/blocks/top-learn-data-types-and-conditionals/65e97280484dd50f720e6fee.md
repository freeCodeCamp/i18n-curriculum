---
id: 65e97280484dd50f720e6fee
title: تعلّم أنواع البيانات والشرطيات الدرس الأول
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-i
---
# --description--

في المثال السابق، كان لديك خياران `if` أو `else`. لكن ماذا لو أردت أكثر من خيارين؟ يمكنك استخدام جملة `else if` لإضافة شروط أكثر إلى شفرتنا. تسمح جملة `else if` بفحص شروط متعددة وتنفيذ كتلة شفرة عندما يكون الشرط الأول صحيحًا. إذا كان الشرط الأول خاطئًا، ستفحص الشرط التالي وهكذا. إذا لم يكن أي من الشروط صحيحًا، ستُنفذ كتلة `else`.

إليك مثالًا على جملة `else if`:

```javascript
let x = 5;

if (x > 10) {
  console.log("x is greater than 10");
} else if (x > 5) {
  console.log("x is greater than 5");
} else {
  console.log("x is less than or equal to 5");
}
```

في هذا المثال، تُقارن المتغير `x` بالرقم `10` باستخدام العامل `>`. إذا كان `x` أكبر من `10`، ستُنفذ كتلة الشفرة الأولى. وإلا، ستفحص جملة `else if` الشرط التالي. إذا كان `x` أكبر من `5`، ستُنفذ كتلة الشفرة الثانية. وإلا، ستُنفذ كتلة `else`.

# --questions--

## --text--

ما الناتج المتوقع من الشفرة التالية؟

```javascript
let x = 4;

if (x > 10) {
  console.log("x is greater than 10");
} else if (x > 5) {
  console.log("x is greater than 5");
} else {
  console.log("x is less than or equal to 5");
}
```

## --answers--

`x is less than or equal to 5`

---

`x is greater than 5`

---

`x is greater than 10`

## --video-solution--

1
