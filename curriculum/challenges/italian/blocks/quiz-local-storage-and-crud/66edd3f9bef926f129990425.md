---
id: 66edd3f9bef926f129990425
title: Quiz su local storage e CRUD
challengeType: 8
dashedName: quiz-local-storage-and-crud
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cosa significa CRUD nel contesto dello sviluppo web?

#### --distractors--

Create, Replace, Undo, Delete.

---

Connect, Remove, Update, Display.

---

Copy, Retrieve, Update, Delete.

#### --answer--

Create, Read, Update, Delete.

### --question--

#### --text--

Quale metodo HTTP è comunemente associato all'operazione Create in un'API RESTful?

#### --distractors--

`GET`

---

`PUT`

---

`DELETE`

#### --answer--

`POST`

### --question--

#### --text--

Quale delle seguenti operazioni CRUD viene tipicamente eseguita quando si modifica un record esistente in un database?

#### --distractors--

Read

---

Delete

---

Create

#### --answer--

Update

### --question--

#### --text--

Qual è la principale differenza tra localStorage e sessionStorage nell'API Web Storage?

#### --distractors--

localStorage memorizza i dati in memoria, mentre sessionStorage memorizza i dati su disco.

---

sessionStorage è più veloce di localStorage per recuperare i dati.

---

I dati di localStorage sono disponibili solo durante una sessione, mentre sessionStorage persiste tra le sessioni.

#### --answer--

I dati di localStorage persistono fino a quando non vengono esplicitamente cancellati, mentre i dati di sessionStorage vengono cancellati quando termina la sessione della pagina.

### --question--

#### --text--

Quale metodo si usa per memorizzare dati in localStorage?

#### --distractors--

`localStorage.setData()`

---

`localStorage.saveItem()`

---

`localStorage.storeData()`

#### --answer--

`localStorage.setItem()`

### --question--

#### --text--

Come puoi rimuovere un singolo elemento da sessionStorage?

#### --distractors--

`sessionStorage.clearItem('key')`

---

`sessionStorage.remove('key')`

---

`sessionStorage.deleteItem('key')`

#### --answer--

`sessionStorage.removeItem('key')`

### --question--

#### --text--

Qual è la differenza tra `PUT` e `PATCH`?

#### --distractors--

`PUT` modifica campi specifici, mentre `PATCH` sostituisce l'intera risorsa.

---

`PUT` aggiunge solo nuove risorse, mentre `PATCH` aggiorna quelle esistenti.

---

`PUT` si usa per recuperare dati, mentre `PATCH` si usa per inviare dati.

#### --answer--

`PUT` sostituisce l'intera risorsa, mentre `PATCH` aggiorna campi specifici.

### --question--

#### --text--

A cosa serve la cache storage?

#### --distractors--

La cache storage serve a memorizzare permanentemente in modo sicuro le credenziali utente nel browser per scopi di autenticazione.

---

La cache storage serve a memorizzare grandi volumi di dati strutturati nel browser usando un sistema di database.

---

La cache storage serve a tracciare le sessioni e le preferenze degli utenti memorizzando coppie chiave-valore accessibili da più siti web.

#### --answer--

La cache storage serve a memorizzare risorse di rete richieste frequentemente, riducendo i tempi di caricamento e migliorando le prestazioni.

### --question--

#### --text--

Cos’è un cookie nel contesto dello sviluppo web?

#### --distractors--

Un tipo di richiesta HTTP.

---

Un pezzo di codice che viene eseguito nel browser.

---

Un metodo per memorizzare file di grandi dimensioni su un server.

#### --answer--

Un piccolo pezzo di dati memorizzato dal browser web dell’utente.

### --question--

#### --text--

Quale dei seguenti metodi si usa per impostare manualmente un cookie in JavaScript?

#### --distractors--

`document.setCookie`

---

`document.cookie.set`

---

`document.createCookie`

#### --answer--

`document.cookie`

### --question--

#### --text--

Quale dei seguenti frammenti di codice elimina correttamente un cookie chiamato `username`?

#### --distractors--

`document.cookie.delete("username");`

---

`document.removeCookie("username");`

---

`cookie.delete("username");`

#### --answer--

`document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";`

### --question--

#### --text--

Quale delle seguenti affermazioni sui cookie è falsa?

#### --distractors--

I cookie di sessione scadono quando il browser viene chiuso.

---

I cookie HttpOnly non possono essere consultati da JavaScript.

---

I cookie Secure vengono trasmessi solo tramite HTTPS.

#### --answer--

I cookie possono essere impostati solo dal server.

### --question--

#### --text--

Cos’è il browser fingerprinting?

#### --distractors--

Una tecnica usata per identificare gli utenti basandosi sul loro indirizzo IP.

---

Una forma di crittografia dei dati.

---

Un metodo per memorizzare dati nella cache del browser.

#### --answer--

Un modo per tracciare gli utenti attraverso attributi del browser e del dispositivo.

### --question--

#### --text--

Qual è il rischio principale associato alla memorizzazione delle password nel local storage?

#### --distractors--

I dati nel local storage vengono cancellati una volta chiusa la scheda del browser.

---

I dati nel local storage si perdono una volta eliminati i cookie.

---

Il local storage non funziona offline.

#### --answer--

Il local storage non è criptato e può essere facilmente consultato.

### --question--

#### --text--

Quale delle seguenti affermazioni è vera riguardo l’archiviazione lato client?

#### --distractors--

L’archiviazione lato client usa oggetti invece di stringhe per memorizzare i dati.

---

L’archiviazione lato client usa servizi cloud per sincronizzare i dati utente su più dispositivi.

---

L’archiviazione lato client è più sicura di quella lato server.

#### --answer--

L’archiviazione lato client permette ai siti web di memorizzare dati sul dispositivo dell’utente.

### --question--

#### --text--

Qual è il vantaggio principale di usare IndexedDB rispetto a localStorage?

#### --distractors--

IndexedDB non richiede il consenso dell’utente per la memorizzazione dei dati.

---

IndexedDB ha un’API più semplice.

---

IndexedDB è supportato in tutti i browser web.

#### --answer--

IndexedDB può gestire quantità maggiori di dati e query più complesse.

### --question--

#### --text--

Quale dei seguenti è un modo valido per recuperare dati da un object store in IndexedDB?

#### --distractors--

Chiamare `retrieve()` sull’object store.

---

Usare `query()` per accedere a record specifici.

---

Usare il metodo `fetch()`.

#### --answer--

Usare `get()` sul riferimento allo store.

### --question--

#### --text--

Che tipo di transazioni supporta IndexedDB?

#### --distractors--

Solo transazioni di lettura.

---

Solo transazioni asincrone.

---

Solo transazioni sincrone.

#### --answer--

Transazioni di lettura e scrittura.

### --question--

#### --text--

Quale delle seguenti affermazioni è vera riguardo il ciclo di vita di un Service Worker?

#### --distractors--

I Service Worker girano nel thread principale dell’applicazione web.

---

I Service Worker possono essere attivati prima di essere installati.

---

I Service Worker possono girare solo quando l’applicazione web è aperta nel browser.

#### --answer--

Un Service Worker può intercettare le richieste di rete dopo essere stato attivato.

### --question--

#### --text--

Quale affermazione descrive meglio una funzionalità chiave delle Progressive Web Apps (PWA)?

#### --distractors--

Le PWA possono essere installate solo su dispositivi mobili.

---

Le PWA devono avere un server back-end.

---

Le PWA possono essere consultate solo tramite browser specifici.

#### --answer--

Le PWA offrono funzionalità offline tramite i Service Worker.
