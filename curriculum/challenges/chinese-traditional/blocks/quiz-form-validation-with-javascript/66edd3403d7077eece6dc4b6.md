---
id: 66edd3403d7077eece6dc4b6
title: 使用 JavaScript 的表單驗證測驗
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

要通過此測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

哪一個方法用來檢查 `input` 元素的值是否符合驗證規則？

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

如果 `checkValidity()` 方法發現 `input` 無效，會發生什麼？

#### --distractors--

表單會自動提交。

---

頁面會自動重新整理。

---

`input` 值已被清除。

#### --answer--

它傳回 `false`，且不會向使用者顯示任何錯誤訊息。

### --question--

#### --text--

哪一個方法用於自訂驗證失敗時顯示的訊息？

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

當表單提交時，必填欄的預設驗證行為是什麼？

#### --distractors--

即使必填欄位為空，表單仍會被提交。

---

表單提交但顯示警告。

---

表單會重設 `input` 值。

#### --answer--

如果必填欄位為空，瀏覽器會停止表單提交。

### --question--

#### --text--

哪一個動作不會導致 HTML 表單被檢查錯誤？

#### --distractors--

變更 `input` 值。

---

提交表單。

---

呼叫 `reportValidity()`。

#### --answer--

點擊重設按鈕。

### --question--

#### --text--

當所有表單輸入有效時，`checkValidity()` 傳回什麼？

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

`patternMismatch` 為 `true` 是什麼意思？

#### --distractors--

該欄位是空的。

---

該值太長。

---

該欄位被停用。

#### --answer--

該值不符合 `pattern` 屬性。

### --question--

#### --text--

如果呼叫 `reportValidity()`，但有欄位驗證失敗，會發生什麼事？

#### --distractors--

表單提交時沒有錯誤。

---

`input` 值已被清除。

---

表單會立即關閉。

#### --answer--

無效的欄位會被凸顯標示並顯示訊息。

### --question--

#### --text--

哪一個動作會觸發瀏覽器內建的表單驗證？

#### --distractors--

重設表單。

---

變更 `input` 值。

---

點擊錨點頁籤。

#### --answer--

手動提交表單。

### --question--

#### --text--

在提交表單之前呼叫 `checkValidity()` 的目的為何？

#### --distractors--

自動提交表單。

---

重新載入頁面。

---

清除所有 `input` 欄位。

#### --answer--

檢查所有 `input` 元素是否符合其指定的條件。

### --question--

#### --text--

`e.preventDefault()` 的主要用途是什麼？

#### --distractors--

重新載入表單。

---

清除表單資料。

---

重設表單值。

#### --answer--

停止事件的預設行為。

### --question--

#### --text--

當用於表單的提交事件時，`e.preventDefault()` 會做什麼？

#### --distractors--

它會提交表單。

---

它會清除所有表單欄位。

---

它會暫時停用該表單。

#### --answer--

它防止表單被提交。

### --question--

#### --text--

`e.preventDefault()` 在哪種情境下最有用？

#### --distractors--

當處理提交表單的按鈕點擊時。

---

關閉模式對話時。

---

當開啟新頁面時。

#### --answer--

在提交表單並防止頁面重新載入時。

### --question--

#### --text--

如果你忘記在表單提交事件上呼叫 `e.preventDefault()`，會發生什麼事？

#### --distractors--

所有輸入欄位皆設定為其預設值。

---

表單無法提交。

---

表單資料遺失。

#### --answer--

表單提交並重新整理頁面。

### --question--

#### --text--

可以使用 `e.preventDefault()` 防止哪種預設行為？

#### --distractors--

編輯表單。

---

呈現表單。

---

重新載入表單。

#### --answer--

提交表單所引起的預設行為。

### --question--

#### --text--

是什麼觸發了表單的提交事件？

#### --distractors--

當輸入欄位變更時。

---

當表單被重設時。

---

當表單的 action 被清除時。

#### --answer--

當點擊表單的提交按鈕或按下 Enter 鍵時。

### --question--

#### --text--

你通常在哪裡處理 JavaScript 的提交事件？

#### --distractors--

在文字輸入元素上。

---

在表單中的任何欄位。

---

在表單外的按鈕元素上。

#### --answer--

在表單元素本身。

### --question--

#### --text--

提交事件預設會做什麼？

#### --distractors--

它會驗證表單但不會提交。

---

它會清除表單欄位。

---

它會取消表單提交。

#### --answer--

它會將表單的資料傳送到表單的 action 中指定的伺服器。

### --question--

#### --text--

在 JavaScript 中，當表單提交時，你應該監聽哪個事件來控制表單資料？

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

HTML 表單中 `method` 屬性的預設值是什麼？

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
