---
id: 6a042b8be919e91c35460341
title: Express 简介测验
challengeType: 8
dashedName: quiz-introduction-to-express
---

# --description--

要通过测验，你必须正确回答下面 20 个问题中的至少 18 个。

# --quizzes--

## --quiz--

### --question--

#### --text--

API 和网络服务之间有什么关系？

#### --distractors--

每个 API 都是网络服务，但不是每个网络服务都是 API。

---

API 和网络服务是完全相同的概念。

---

网络服务只支持 REST，而 API 支持多种协议。

#### --answer--

每个网络服务都是 API，但不是每个 API 都是网络服务。

### --question--

#### --text--

客户使用哪个 HTTP 头部告诉服务器它想要哪种响应格式？

#### --distractors--

`Content-Type`

---

`Authorization`

---

`X-Requested-With`

#### --answer--

`Accept`

### --question--

#### --text--

在 REST 架构中，无状态性是什么意思？

#### --distractors--

服务器在请求之间保存客户的会话数据。

---

客户在请求之间不保留任何状态。

---

REST 限制所有请求只能是只读操作。

#### --answer--

服务器不保存之前请求的任何记忆。

### --question--

#### --text--

哪个 REST HTTP 方法用于删除资源？

#### --distractors--

`GET`

---

`POST`

---

`PUT`

#### --answer--

`DELETE`

### --question--

#### --text--

微服务相比单体架构的一个关键优势是什么？

#### --distractors--

更简单的调试和服务间更少的网络调用。

---

所有团队都贡献到一个共享的代码库。

---

跨所有服务更容易的基础设施管理。

#### --answer--

更快的开发、更好的故障隔离，以及每个服务可以使用不同语言。

### --question--

#### --text--

调用 `express()` 会返回什么？

#### --distractors--

一个新的 HTTP 服务器。

---

一个路由对象。

---

一个中间件函数。

#### --answer--

一个新的 Express 应用实例。

### --question--

#### --text--

Express 中的中间件是什么？

#### --distractors--

一个用于启动 Express 服务器的内置方法。

---

REST API 中使用的一种特殊 HTTP 方法。

---

一个用于从目录提供静态文件的内置工具。

#### --answer--

在接收请求和发送响应之间运行的代码。

### --question--

#### --text--

`app.listen(port, callback)` 在 Express 中做什么？

#### --distractors--

定义传入请求的路由。

---

加载 Express 库。

---

创建一个新的中间件函数。

#### --answer--

启动服务器并开始监听传入的 HTTP 请求。

### --question--

#### --text--

Express 中的路由参数是什么？

#### --distractors--

像 `/home` 这样的固定 URL 路径。

---

附加到 URL 的查询字符串。

---

提供静态文件的方法。

#### --answer--

URL 中以 `:` 为前缀的占位符，用于捕捉动态值。

### --question--

#### --text--

如何访问在 Express 路由 `/post/42` 中从 URL `/post/:postId` 捕捉到的动态值？

#### --distractors--

`req.query.postId`

---

`req.body.postId`

---

`req.url.postId`

#### --answer--

`req.params.postId`

### --question--

#### --text--

`res.json()` 在 Express 中做什么？

#### --distractors--

发送纯文本响应。

---

重定向客户到不同的 URL。

---

渲染模板引擎视图并发送生成的 HTML。

#### --answer--

发送 JSON 响应并自动转换 JavaScript 对象。

### --question--

#### --text--

`res.redirect(url)` 在 Express 中做什么？

#### --distractors--

设置 HTTP 状态码。

---

发送 JSON 响应。

---

使用模板引擎渲染视图。

#### --answer--

重定向客户到不同的 URL。

### --question--

#### --text--

`app.route()` 在 Express 中的作用是什么？

#### --distractors--

创建 Express 应用的新实例。

---

从指定目录提供静态文件。

---

为不同 URL 路径定义独立路由。

#### --answer--

在一个块中为同一路径链接多个 HTTP 方法处理程序。

### --question--

#### --text--

`express.Router()` 创建什么？

#### --distractors--

一个新的 Express 应用实例，替代 `app` 处理所有服务器逻辑。

---

一个专门捕捉和处理路由级错误的中间件函数。

---

一个内置的静态文件服务器，自动将文件映射到 URL 路径。

#### --answer--

一个路由对象，类似一个迷你 Express 应用，你可以在上面定义路由并挂载到主应用。

### --question--

#### --text--

如何在主 Express 应用中挂载路由器？

#### --distractors--

通过调用 `router.listen()`。

---

通过调用 `router.start()`。

---

直接在路由器内调用 `app.get()`。

#### --answer--

通过调用 `app.use(path, router)`。

### --question--

#### --text--

哪个内置 Express 中间件用于提供静态文件？

#### --distractors--

`express.json()`

---

`express.urlencoded()`

---

`express.raw()`

#### --answer--

`express.static()`

### --question--

#### --text--

当用户请求一个在 `express.static()` 配置的目录中不存在的静态文件时，会发生什么？

#### --distractors--

Express 抛出错误并崩溃服务器。

---

Express 将用户重定向到主页。

---

Express 在请求路径创建一个空文件。

#### --answer--

Express 自动响应 404。

### --question--

#### --text--

与 REST 相比，关于 SOAP 哪项是正确的？

#### --distractors--

SOAP 使用 JSON，比 REST 更灵活，适合现代应用。

---

SOAP 是目前最广泛使用的网络服务类型，已取代大多数系统中的 REST。

---

SOAP 支持多种响应格式，包括纯文本、JSON 和二进制数据。

#### --answer--

SOAP 只使用 XML，常见于需要更严格标准和安全性的企业系统。

### --question--

#### --text--

Express 中的路由处理程序是什么？

#### --distractors--

一个配置对象，将所有可用路由路径映射到它们的 HTTP 方法。

---

一个自动拦截并提供文件夹中静态文件的中间件函数。

---

一个处理路由相关数据库查询的数据库连接器。

#### --answer--

当请求匹配路由时运行的回调函数，可以访问 `req` 和 `res`。

### --question--

#### --text--

使用 `express.Router()` 而不是直接在 `app.js` 中定义所有路由的好处是什么？

#### --distractors--

它允许服务器同时处理更多并发 HTTP 请求。

---

它根据路由文件名自动生成并注册 REST API 端点。

---

它让服务器启动和运行时无需指定端口号。

#### --answer--

它通过将相关路由移入各自模块，分离关注点并保持主文件整洁。
