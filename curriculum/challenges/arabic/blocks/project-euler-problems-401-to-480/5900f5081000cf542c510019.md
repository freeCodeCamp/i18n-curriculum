---
id: 5900f5081000cf542c510019
title: 'المشكلة 411: المسارات الصاعدة'
challengeType: 1
forumTopicId: 302080
dashedName: problem-411-uphill-paths
---

# --description--

ليكن $n$ عددًا صحيحًا موجبًا. افترض وجود محطات عند الإحداثيات $(x, y) = (2^i\bmod n, 3^i\bmod n)$ حيث $0 ≤ i ≤ 2n$. سنعتبر المحطات التي لها نفس الإحداثيات هي نفس المحطة.

نرغب في تكوين مسار من (0, 0) إلى ($n$, $n$) بحيث لا تنخفض إحداثيات $x$ و $y$ أبدًا.

ليكن $S(n)$ هو أقصى عدد من المحطات التي يمكن لمسار كهذا أن يمر بها.

على سبيل المثال، إذا كان $n = 22$، فهناك 11 محطة مميزة، ويمكن لمسار صالح أن يمر عبر 5 محطات كحد أقصى. لذلك، $S(22) = 5$. الحالة موضحة أدناه، مع مثال لمسار أمثل:

<img alt="valid path passing through 5 stations, for n = 22, with 11 distinct stations" src="https://cdn.freecodecamp.org/curriculum/project-euler/uphill-paths.png" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

يمكن أيضًا التحقق من أن $S(123) = 14$ و $S(10\\,000) = 48$.

احسب $\sum S(k^5)$ حيث $1 ≤ k ≤ 30$.

# --hints--

`uphillPaths()` يجب أن تُرجع `9936352`.

```js
assert.strictEqual(uphillPaths(), 9936352);
```

# --seed--

## --seed-contents--

```js
function uphillPaths() {

  return true;
}

uphillPaths();
```

# --solutions--

```js
// solution required
```
