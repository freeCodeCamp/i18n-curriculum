---
id: 5900f3b81000cf542c50fecb
title: "المشكلة 76: عد المجاميع"
challengeType: 1
forumTopicId: 302189
dashedName: problem-76-counting-summations
---

# --description--

يمكن كتابة العدد خمسة كمجموع بعدة طرق مختلفة بالضبط ست مرات:

<div style='margin-left: 4em;'>
  4 + 1<br>
  3 + 2<br>
  3 + 1 + 1<br>
  2 + 2 + 1<br>
  2 + 1 + 1 + 1<br>
  1 + 1 + 1 + 1 + 1<br><br>
</div>

كم عدد الطرق المختلفة التي يمكن كتابة `n` كمجموع على الأقل لعددين صحيحين موجبين؟

# --hints--

يجب أن تُرجع الدالة `countingSummations(5)` رقمًا.

```js
assert(typeof countingSummations(5) === 'number');
```

يجب أن تُرجع الدالة `countingSummations(5)` القيمة `6`.

```js
assert.strictEqual(countingSummations(5), 6);
```

يجب أن تُرجع الدالة `countingSummations(20)` القيمة `626`.

```js
assert.strictEqual(countingSummations(20), 626);
```

يجب أن تُرجع الدالة `countingSummations(50)` القيمة `204225`.

```js
assert.strictEqual(countingSummations(50), 204225);
```

يجب أن تُرجع الدالة `countingSummations(100)` القيمة `190569291`.

```js
assert.strictEqual(countingSummations(100), 190569291);
```

# --seed--

## --seed-contents--

```js
function countingSummations(n) {

  return true;
}

countingSummations(5);
```

# --solutions--

```js
function countingSummations(n) {
  const combinations = new Array(n + 1).fill(0);
  combinations[0] = 1;

  for (let i = 1; i < n; i++) {
    for (let j = i; j < n + 1; j++) {
      combinations[j] += combinations[j - i];
    }
  }
  return combinations[n];
}
```
