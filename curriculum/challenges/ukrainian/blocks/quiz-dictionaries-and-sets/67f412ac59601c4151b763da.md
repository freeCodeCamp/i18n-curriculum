---
id: 67f412ac59601c4151b763da
title: Тест на словники та множини
challengeType: 8
dashedName: quiz-dictionaries-and-sets
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке словник?

#### --distractors--

Структура даних, яка містить лише рядки та списки.

---

Структура даних, що є колекцією пар ключ-значення, які приймають лише великі числа.

---

Структура даних, яка містить лише колекцію кортежів.

#### --answer--

Структура даних, що є колекцією пар ключ-значення.

### --question--

#### --text--

Який із наведених синтаксисів є правильним для словника?

#### --distractors--

```py
dictionary = {
    <key1: value1>,
    <key2: value2>
}
```

---

```py
dictionary = {
    (key1: value1),
    (key2: value2)
}
```

---

```py
dictionary = {
    [key1: value1],
    [key2: value2]
}
```

#### --answer--

```py
dictionary = {
    key1: value1,
    key2: value2
}
```

### --question--

#### --text--

Що з наведеного є правдою про словники?

#### --distractors--

Ключі повинні бути довжиною щонайменше два символи.

---

Ключі повинні містити спеціальний символ.

---

Ключі повинні містити щонайменше одне число.

#### --answer--

Ключі повинні бути унікальними.

### --question--

#### --text--

Який із наведених конструкторів можна використати для створення словника?

#### --distractors--

`diction()`

---

`dic()`

---

`dictionary()`

#### --answer--

`dict()`

### --question--

#### --text--

Який із наведених способів є правильним для доступу до значення у словнику?

#### --distractors--

`dictionary<key>`

---

`dictionary/key/`

---

`dictionary{key}`

#### --answer--

`dictionary[key]`

### --question--

#### --text--

Який із наведених способів є правильним для оновлення значення у словнику?

#### --distractors--

`pizza['name'] >> 'Margherita'`

---

`pizza['name'] << 'Margherita'`

---

`pizza['name'] == 'Margherita'`

#### --answer--

`pizza['name'] = 'Margherita'`

### --question--

#### --text--

Який із наведених методів можна використати, щоб отримати значення, пов’язане з ключем?

#### --distractors--

`access()`

---

`set()`

---

`retrieve()`

#### --answer--

`get()`

### --question--

#### --text--

Що таке об’єкт view?

#### --distractors--

Спеціальний об’єкт, який використовується для перетворення рядків у словники.

---

Спосіб перегляду вмісту словника лише якщо словник має дві або більше пар ключ-значення.

---

Спеціальний об’єкт, який використовується для перетворення списків у словники.

#### --answer--

Спосіб побачити вміст словника без створення окремої копії даних.

### --question--

#### --text--

Який із наведених методів повертає об’єкт view з усіма парами ключ-значення у словнику?

#### --distractors--

`lists()`

---

`dictionaries()`

---

`collections()`

#### --answer--

`items()`

### --question--

#### --text--

Який із наведених методів видаляє всі пари ключ-значення зі словника?

#### --distractors--

`empty()`

---

`replace()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

Який буде вихідний результат для наступного коду?

```py
products = {
    'Laptop': 990,
    'Smartphone': 600,
    'Tablet': 250,
    'Headphones': 70,
}

for product in products.items():
    print(product)
```

#### --distractors--

```py
<'Laptop', 990>
<'Smartphone', 600>
<'Tablet', 250>
<'Headphones', 70>
```

---

```py
'Laptop', 990
'Smartphone', 600
'Tablet', 250
'Headphones', 70
```

---

```py
['Laptop', 990]
['Smartphone', 600]
['Tablet', 250]
['Headphones', 70]
```

#### --answer--

```py
('Laptop', 990)
('Smartphone', 600)
('Tablet', 250)
('Headphones', 70)
```

### --question--

#### --text--

Що з наведеного є правдою про множини?

#### --distractors--

Множини змінні і впорядковані у зворотному порядку.

---

Множини змінні і впорядковані.

---

Множини незмінні і невпорядковані.

#### --answer--

Множини змінні і невпорядковані.

### --question--

#### --text--

Який із наведених способів є правильним для створення множини?

#### --distractors--

`my_set = (1, 2, 3, 4, 5)`

---

`my_set = <1, 2, 3, 4, 5>`

---

`my_set = [1, 2, 3, 4, 5]`

#### --answer--

`my_set = {1, 2, 3, 4, 5}`

### --question--

#### --text--

Яка з наведених функцій використовується для створення порожньої множини?

#### --distractors--

`create_set()`

---

`empty_set()`

---

`sets()`

#### --answer--

`set()`

### --question--

#### --text--

Що буде виведено у термінал?

```py
my_set = {1, 2, 3, 4, 5, 6}
my_set.add(5)

print(my_set)
```

#### --distractors--

`SyntaxError`

---

`RangeError`

---

`{1, 2, 3, 4, 5, 5, 6}`

#### --answer--

`{1, 2, 3, 4, 5, 6}`

### --question--

#### --text--

Який із наведених методів перевіряє, чи є множина підмножиною?

#### --distractors--

`issub()`

---

`isset()`

---

`subset()`

#### --answer--

`issubset()`

### --question--

#### --text--

Який із наведених методів перевіряє, чи є дві множини диз’юктними?

#### --distractors--

`joint()`

---

`isjoint()`

---

`disjoint()`

#### --answer--

`isdisjoint()`

### --question--

#### --text--

Що робить оператор симетричної різниці (`^`)?

#### --distractors--

Він повертає нову множину лише з елементами, які є спільними для множин.

---

Він знаходить різницю між множинами і оновлює першу множину цим результатом.

---

Він повертає нову множину з елементами першої множини, яких немає в іншій множині.

#### --answer--

Він повертає нову множину з елементами, які є або в першій, або в другій множині, але не в обох одночасно.

### --question--

#### --text--

Який із наведених вбудованих модулів використовується для генерації випадкових чисел?

#### --distractors--

`set_random`

---

`get_random`

---

`rand`

#### --answer--

`random`

### --question--

#### --text--

Який із наведених модулів використовується для роботи з регулярними виразами?

#### --distractors--

`regex`

---

`reg`

---

`r`

#### --answer--

`re`
