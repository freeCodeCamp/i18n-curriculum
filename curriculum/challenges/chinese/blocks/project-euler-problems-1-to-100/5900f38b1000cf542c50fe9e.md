---
id: 5900f38b1000cf542c50fe9e
title: '问题31：硬币总和'
challengeType: 1
forumTopicId: 301965
dashedName: problem-31-coin-sums
---

# --description--

在英格兰，货币由英镑，英镑和便士p组成，并且有八种普通硬币流通：

<div style='margin-left: 4em;'>1p, 2p, 5p, 10p, 20p, 50p, £1 (100p) and £2 (200p).</div>

1p，2p，5p，10p，20p，50p，£1（100p）和£2（200p）。

<div style='margin-left: 4em;'>1×£1 + 1×50p + 2×20p + 1×5p + 1×2p + 3×1p</div>

用任意数量的硬币，可以用多少种不同的方法组成 `n` 便士？

# --hints--

`coinSums(50)` 应该返回一个数字。

```js
assert(typeof coinSums(50) === 'number');
```

`coinSums(50)` 应该返回 451。

```js
assert(coinSums(50) == 451);
```

`coinSums(100)` 应该返回 4563。

```js
assert(coinSums(100) == 4563);
```

`coinSums(150)` 应该返回 21873。

```js
assert(coinSums(150) == 21873);
```

`coinSums(200)` 应该返回 73682。

```js
assert(coinSums(200) == 73682);
```

# --seed--

## --seed-contents--

```js
function coinSums(n) {

  return n;
}

coinSums(200);
```

# --solutions--

```js
const coinSums = (n) => {
  const getWays = (n, m=8, c=[1, 2, 5, 10, 20, 50, 100, 200]) => {
    if (n === 0) return 1;
    if (m === 0 || n < 0) return 0;
    return getWays(n - c[m - 1], m, c) + getWays(n, m - 1, c);
  };
  return getWays(n);
};
```
