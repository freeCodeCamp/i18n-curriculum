---
id: 5900f46e1000cf542c50ff80
title: '问题 257：角平分线'
challengeType: 1
forumTopicId: 301905
dashedName: problem-257-angular-bisectors
---

# --description--

给定一个整数边长的三角形 $ABC$，其边长满足 $a ≤ b ≤ c$（$AB = c$，$BC = a$ 和 $AC = b$）。

三角形的角平分线在边上交于点 $E$、$F$ 和 $G$（见下图）。

<img alt="triangle ABC, with angular bisectors intersecting sides at the points E, F and G" src="https://cdn.freecodecamp.org/curriculum/project-euler/angular-bisectors.gif" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

线段 $EF$、$EG$ 和 $FG$ 将三角形 $ABC$ 分区为四个更小的三角形：$AEG$、$BFE$、$CGF$ 和 $EFG$。可以证明，对于这四个三角形中的每一个，比例 $\frac{\text{area}(ABC)}{\text{area}(\text{subtriangle})}$ 是有理数。然而，存在一些三角形，其某些或全部这些比例是整数。

存在多少个周长 $≤ 100\,000\,000$ 的三角形 $ABC$，使得比值 $\frac{\text{area}(ABC)}{\text{area}(AEG)}$ 是整数？

# --hints--

`angularBisectors()` 应该返回 `139012411`。

```js
assert.strictEqual(angularBisectors(), 139012411);
```

# --seed--

## --seed-contents--

```js
function angularBisectors() {

  return true;
}

angularBisectors();
```

# --solutions--

```js
// solution required
```
