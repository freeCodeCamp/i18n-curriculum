---
id: 662efc55d552e72591518e31
title: 任務 43
challengeType: 19
dashedName: task-43
lang: en-US
---

# --description--

`function` 是一組執行特定任務的指令。`Functions` 可以讓程式碼更有組織性、可重複使用且更易於閱讀。<dfn>例如</dfn>，一個 `function` 可能會計算兩個數字的總和或顯示訊息給使用者。

將 `function` 想像成工廠中的一台小機器；你給它一些輸入，它會對該輸入進行處理，然後給你輸出。

# --questions--

## --text--

在程式設計的背景關係中，`function` 是什麼意思？

## --answers--

這是程式的標題。

### --feedback--

`function` 不是標題，而是設計用來執行特定操作的程式碼部分。

---

它是一個可重複使用的程式碼區塊，執行特定任務。

---

它取用一種特殊型別的變數。

### --feedback--

雖然 `functions` 可以與變數互動，但它們本身不是變數，而是一組指令。

---

它是在電腦上執行的主要軟體。

### --feedback--

`function` 是軟體的組件，而非整個軟體系統。

## --video-solution--

2
