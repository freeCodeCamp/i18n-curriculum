---
id: 68829061f03543726ea6f318
title: リポジトリとは何か、そしてどうやって作成するのか？
challengeType: 19
dashedName: what-is-a-repository-and-how-do-you-create-one
---

# --description--

GitとGitHubについて少し話しましたので、ここからそれらを組み合わせていきましょう。そのためにはリポジトリが必要です。でもリポジトリとは何でしょうか？リポジトリはプロジェクトのコンテナのようなものだと考えてください。アプリを作っている場合、そのアプリのファイルをまとめてリポジトリに保管します。

リポジトリはコンピューターのローカルにある場合もあれば、GitHubのようなサービス上のリモートにある場合もあります。リポジトリを作成するには、ローカルかリモートのどちらかから始められます。結果は同じなので、自分のワークフローに合った方法を選んでください。リモートリポジトリから始めるワークフローの場合は、GitHubのウェブサイトから直接リポジトリを作成できます。以下の手順に従ってください。

<a href="https://github.com" target="_blank">GitHubのウェブサイト</a>にアクセスし、ログインしていることを確認したら、ナビゲーションバーの右上にあるプラスアイコンをクリックします。いくつかのオプションが表示されますが、関心があるのは「New Repository」です。これを選択すると、新しいリポジトリを作成するためのUIが開きます。

最初に表示されるオプションはテンプレートの選択です。まだ設定していないかもしれませんが、テンプレートは新しいリポジトリの出発点として使える特別な種類のリポジトリです。これにより、すべてのプロジェクトにドキュメントやイシューテンプレートを含めるのに役立ちます。

次にオーナーを選択できます。自分のユーザーアカウントか、所有またはアクセス権のある組織を選べます。そしてリポジトリ名を付けます。一般的には、作成するプロジェクトを簡潔に表す名前を使うのがよいです。ベストプラクティスとして、リポジトリ名にスペースを使わないことをお勧めします。GitHubはスペースを自動的にハイフンに置き換えます。

次に、リポジトリのホームページに表示される説明を任意で追加できます。

リポジトリを公開にするか非公開にするかも選べます。公開リポジトリは誰でも閲覧・ダウンロードできます。非公開リポジトリは自分と明示的にアクセス権を与えた人だけがアクセスできます。

最後に、テンプレートを選択しなかった場合は、リポジトリ内に自動的にファイルを生成するオプションが表示されます。READMEファイルはリポジトリのホームページに表示され、プロジェクトのドキュメントや説明を提供するのに最適です。`.gitignore` ファイルはGitに「無視」させるファイルを指定でき、パッケージの依存関係や環境ファイルなど、保存したくないものに便利です。GitHubは指定した言語やランタイムに共通する項目を除外するテンプレートを提供します。そしてライセンスは、誰がソフトウェアを使えるか、何ができるかを法的に定めた文書です。

設定がすべて完了したら、「Create Repository」ボタンをクリックして作成を完了してください。新しいリポジトリのランディングページに自動的にリダイレクトされます！

ここから「Code」ボタンをクリックすると、リポジトリをコンピューターにクローンするためのオプションが表示されます。

このレッスンでは「Codespaces」や「Copilot」タブは無視してください。代わりに「Local」タブの3つのオプションに注目します。最初の2つ、HTTPSとSSHは同じように使います。`git clone` コマンドを、ボックスに表示されたURLとともに使ってリモートリポジトリをコンピューターにクローンします。違いは接続方法と認証方法です。

公開リポジトリをクローンする場合は認証は不要です。しかし非公開リポジトリをクローンしたり、公開・非公開リポジトリにプッシュしたりする場合は認証が必要です。HTTPSのリモートURLはGitHubのユーザー名とパスワードで認証しますが、セキュリティ上の理由から現在はサポートされておらず、代わりにユーザー名とアクセストークンを使う必要があります。それでもSSHよりは安全性が低いです。SSHは秘密鍵と公開鍵を使い、自動的にサインインできる利点があります。SSHの仕組みについては別のレッスンで詳しく扱いますが、GitHubのドキュメントでも設定方法を読むことができます。

リポジトリをクローンするには、ターミナルで以下を実行してください。

```sh
git clone git@github.com:your-username/your-repository-name
```

これにより、コンピューターにリポジトリ名のフォルダーが作成され、その中にリポジトリのすべてのファイルが入ります。そこから`cd`（ディレクトリを移動）してクローンしたリポジトリに移動できます。

```sh
cd name-of-cloned-repo
```

さらに、ローカルのコピーにはGitHub上のコピーを指すリモートが設定されています。リモートを確認するには以下のコマンドを実行します。

```sh
git remote -v
```

もう一つのクローン方法はGitHub CLIを使うことです。このツールはコマンドラインを離れずにGitHub固有の作業を行えます。インストールしていない場合はGitHubのドキュメントで手順を確認してください。多くのシステムのパッケージマネージャーで利用可能です。

CLIツールでリポジトリをクローンするには、以下を使います。

```sh
gh repo clone your-username/your-repository-name
```

これらのコマンドの結果は同じで、ローカルにリポジトリのコピーができます。

GitHub CLIはリポジトリの作成にも使えます。

```sh
gh repo create
```

引数を渡さない場合、CLIは対話式ウィザードを表示し、リポジトリの設定を行えます。このウィザードはウェブUIと同じオプションを提供します。このツールでは新しいリポジトリを作成後すぐにクローンするオプションもあり、先ほど説明した手動の手順を省けます。

これでリポジトリの基本的な仕組みと、作成して作業のバージョン管理を始める方法がわかりました。今後のレッスンでは、既存のローカルリポジトリをGitHubにプッシュする方法や、リモートリポジトリに変更をプッシュする方法を学びます。

# --questions--

## --text--

Gitにおいてリポジトリは主に何の役割を果たしますか？

## --answers--

コードを自動的にフォーマットするツール。

### --feedback--

バージョン管理におけるリポジトリの基本的な目的を考えてみてください。

---

プロジェクトとそのファイルをまとめるコンテナ。

---

コンピューターのバックアップシステム。

### --feedback--

バージョン管理におけるリポジトリの基本的な目的を考えてみてください。

---

チームミーティングのためのコラボレーションプラットフォーム。

### --feedback--

バージョン管理におけるリポジトリの基本的な目的を考えてみてください。

## --video-solution--

2

## --text--

GitHubでリポジトリを作成するとき、自動ファイル生成オプションに含まれないものはどれですか？

## --answers--

READMEファイル。

### --feedback--

レッスンで説明された自動ファイル生成オプションを見直してください。

---

`.gitignore` ファイル。

### --feedback--

レッスンで説明された自動ファイル生成オプションを見直してください。

---

ライセンスファイル。

### --feedback--

レッスンで説明された自動ファイル生成オプションを見直してください。

---

`package.json` ファイル。

## --video-solution--

4

## --text--

リモートリポジトリをローカルコンピューターにクローンするために使うGitコマンドはどれですか？

## --answers--

`git cloned`

### --feedback--

レッスンの最後の部分を見直してください。

---

`git clones`

### --feedback--

レッスンの最後の部分を見直してください。

---

`git clone`

---

`git cloning`

### --feedback--

レッスンの最後の部分を見直してください。

## --video-solution--

3
