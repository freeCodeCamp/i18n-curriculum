---
id: 66f1a417757b6ca4eecd89d6
title: React-State- und Hooks-Quiz
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 Fragen unten richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um in React ein Klick-Ereignis zu einem Button hinzuzufügen?

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

Was ist ein React Synthetic Event?

#### --distractors--

Ein Typ von JSX-Element nur für `inputs` und `submit` Ereignisse.

---

Ein Typ von JSX-Element nur für `submit` Ereignisse.

---

Eine Möglichkeit, die Bubbling-Phase der Ereignisweiterleitung benutzerfreundlicher zu gestalten.

#### --answer--

Eine Hülle um native Ereignisse wie `click` und `submit` Ereignisse.

### --question--

#### --text--

Welches der folgenden Präfixe wird häufig für die Benennung von Event-Handler-Funktionen verwendet?

#### --distractors--

isHandling

---

hasHandled

---

handling

#### --answer--

handle

### --question--

#### --text--

Was sind die drei Hauptphasen im Rendering von React?

#### --distractors--

Handshake, Laden und Hydration.

---

Abrufen, Dekodieren und Ausführen.

---

Mounting, Updating und Unmounting.

#### --answer--

Triggern, Rendern und Committen.

### --question--

#### --text--

Welcher der folgenden Wege ist der korrekte Umgang mit dem `useState` Hook?

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

Welches der folgenden Attribute ist KEIN gültiges Attribut zum Handhaben von React-Ereignissen?

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

Welcher der folgenden Wege ist der korrekte Weg, um den Array-State zu aktualisieren?

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--

Was ist an dieser Funktion falsch?

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

Das Aktualisieren einer `useState` Variablen ist innerhalb einer verschachtelten Funktion nicht erlaubt.

---

`previousState` muss in Klammern gesetzt werden.

---

Pfeilfunktionen sollten nicht innerhalb einer State-Setter-Funktion verwendet werden.

#### --answer--

`previousState` muss vor `name` gespreadet werden, um sicherzustellen, dass keine anderen Eigenschaften überschrieben werden.

### --question--

#### --text--

Welcher der folgenden Wege ist der korrekte Weg, um den State zu aktualisieren, um Elemente aus einem Array zu entfernen?

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

Wofür wird das `ref` Attribut typischerweise in React verwendet?

#### --distractors--

Es wird verwendet, um Performance-Probleme in React-Komponenten zu prüfen.

---

Es wird verwendet, um Barrierefreiheitsprobleme in React-Komponenten zu prüfen.

---

Es wird verwendet, um DOM-Knoten zu entfernen.

#### --answer--

Es wird verwendet, um auf einen DOM-Knoten zuzugreifen.

### --question--

#### --text--

Welcher der folgenden Wege ist der korrekte Weg, um auf den aktuellen Wert eines `ref` zuzugreifen?

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

Wie oft wird diese Nachricht in der Konsole protokolliert?

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

Dreimal.

---

Zweimal.

---

Gar nicht, da das Dependency-Array leer ist.

#### --answer--

Nur einmal beim initialen Rendern.

### --question--

#### --text--

Wofür werden React Custom Hooks typischerweise verwendet?

#### --distractors--

Sie werden verwendet, um die komplexeste Funktionalität einer App auszuführen.

---

Sie werden normalerweise nicht verwendet, eingebaute Hooks werden bevorzugt.

---

Sie ersetzen Lifecycle-Methoden aus früheren React-Versionen.

#### --answer--

Sie sind hilfreich, um zustandsbehaftete Logik über Komponenten hinweg wiederzuverwenden.

### --question--

#### --text--

Wie oft wird diese Nachricht in der Konsole protokolliert?

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

Sie wird protokolliert, wann immer ein Formular abgesendet oder ein Klick-Ereignis ausgelöst wird.

---

Sie wird zweimal protokolliert.

---

Sie wird nur einmal protokolliert.

#### --answer--

Sie wird jedes Mal protokolliert, wenn die Komponente gerendert wird.

### --question--

#### --text--

Welcher der folgenden Wege ist der korrekte Weg, um ein Browser-Refresh bei einem `onSubmit` Ereignis zu verhindern?

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

Welcher der folgenden Hooks wird typischerweise verwendet, um Daten abzurufen?

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

Welches der folgenden ist KEINE gültige Eigenschaft des nativen Event-Objekts?

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

Welche der folgenden Rendering-Phasen tritt ein, wenn React erkennt, dass sich etwas geändert hat und die Benutzeroberfläche (UI) möglicherweise aktualisiert werden muss?

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

Welche der folgenden Phasen bezeichnet den Moment, in dem React die vorbereiteten Änderungen aus dem virtuellen DOM nimmt und auf das reale DOM anwendet?

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

Was ist der Zweck des `dependencies` in einem `useEffect`?

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

Das optionale `dependencies` Argument hat keinen Einfluss auf etwas, das mit dem `useEffect` zu tun hat.

---

Das optionale `dependencies` Argument lässt den Effekt in einer Endlosschleife laufen.

---

Das optionale `dependencies` Argument wird verwendet, um zu verhindern, dass der Effekt ausgeführt wird.

#### --answer--

Das optionale `dependencies` Argument steuert, wann der Effekt ausgeführt wird.

