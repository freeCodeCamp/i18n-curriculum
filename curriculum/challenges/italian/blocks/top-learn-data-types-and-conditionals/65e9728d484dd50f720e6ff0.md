---
id: 65e9728d484dd50f720e6ff0
title: Impara i tipi di dati e le istruzioni condizionali lezione K
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-k
---
# --description--

L'istruzione `switch` viene usata per eseguire azioni diverse in base a condizioni diverse. È simile all'istruzione `if-else`, ma è più leggibile e facile da capire quando ci sono più condizioni da verificare. L'istruzione `switch` serve a selezionare uno tra molti blocchi di codice da eseguire.

L'istruzione `switch` valuta un'espressione e la confronta con i valori di ogni caso. Se c'è una corrispondenza, viene eseguito il blocco di codice associato. Se non c'è corrispondenza, viene eseguito il blocco di codice predefinito.

```javascript
let day = "Monday";

switch (day) {
  case "Monday":
    console.log("Today is Monday");
    break;
  case "Tuesday":
    console.log("Today is Tuesday");
    break;
  case "Wednesday":
    console.log("Today is Wednesday");
    break;
  case "Thursday":
    console.log("Today is Thursday");
    break;
  case "Friday":
    console.log("Today is Friday");
    break;
  case "Saturday":
    console.log("Today is Saturday");
    break;
  case "Sunday":
    console.log("Today is Sunday");
    break;
  default:
    console.log("Invalid day");
}
```

Nell'esempio sopra, il valore della variabile `day` viene confrontato con i valori di ogni caso. Se c'è una corrispondenza, viene eseguito il blocco di codice associato. Se non c'è corrispondenza, viene eseguito il blocco di codice predefinito.

# --questions--

## --text--

Dato il frammento di codice JavaScript qui sotto, che modifica la variabile `activity` in base al giorno della settimana, quale sarà il valore della variabile `activity` se il valore della variabile `day` è `"Tuesday"`?

```javascript
let day = "Tuesday";
let activity;

switch (day) {
  case "Monday":
    activity = "Go to the gym";
    break;
  case "Tuesday":
    activity = "Attend coding meetup";
    break;
  case "Wednesday":
    activity = "Watch a movie";
    break;
  case "Thursday":
    activity = "Visit a museum";
    break;
  case "Friday":
    activity = "Dinner with friends";
    break;
  case "Saturday":
    activity = "Hiking in the mountains";
    break;
  case "Sunday":
    activity = "Rest at home";
    break;
  default:
    activity = "Undefined day";
}

```

## --answers--

Il valore della variabile `activity` sarà `"Attend coding meetup"`.

---

Il valore della variabile `activity` sarà `"Watch a movie"`.

---

Il valore della variabile `activity` sarà `"Undefined day"`.

---

Il valore della variabile `activity` sarà `"Go to the gym"`.

## --video-solution--

1
