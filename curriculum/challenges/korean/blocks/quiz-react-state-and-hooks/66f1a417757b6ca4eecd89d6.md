---
id: 66f1a417757b6ca4eecd89d6
title: React 상태와 훅 퀴즈
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

React에서 버튼에 클릭 이벤트를 추가하는 올바른 방법은 무엇인가요?

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

React Synthetic Event란 무엇인가요?

#### --distractors--

`inputs`와 `submit` 이벤트에만 해당하는 JSX 요소 유형입니다.

---

`submit` 이벤트에만 해당하는 JSX 요소 유형입니다.

---

이벤트 전파의 버블링 단계를 사용자 친화적으로 변경하는 방법입니다.

#### --answer--

`click`와 `submit` 이벤트 같은 네이티브 이벤트를 감싸는 래퍼입니다.

### --question--

#### --text--

이벤트 핸들러 함수를 이름 지을 때 흔히 사용하는 접두사는 무엇인가요?

#### --distractors--

isHandling

---

hasHandled

---

handling

#### --answer--

handle

### --question--

#### --text--

React의 렌더링에서 세 가지 주요 단계는 무엇인가요?

#### --distractors--

핸드셰이크, 로딩, 하이드레이션.

---

가져오기, 디코딩, 실행.

---

마운팅, 업데이트, 언마운팅.

#### --answer--

트리거, 렌더링, 커밋.

### --question--

#### --text--

다음 중 `useState` 훅을 사용하는 올바른 방법은 무엇인가요?

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

다음 중 React 이벤트를 처리할 때 사용하는 유효하지 않은 속성은 무엇인가요?

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

배열 상태를 업데이트하는 올바른 방법은 무엇인가요?

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--

이 함수에서 잘못된 점은 무엇인가요?

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

중첩 함수 안에서 `useState` 변수를 업데이트하는 것은 허용되지 않습니다.

---

`previousState`은 괄호로 감싸야 합니다.

---

상태 설정 함수 안에서는 화살표 함수를 사용하면 안 됩니다.

#### --answer--

다른 속성이 덮어쓰이지 않도록 `previousState`를 `name` 전에 펼쳐야 합니다.

### --question--

#### --text--

배열에서 항목을 제거하기 위해 상태를 업데이트하는 올바른 방법은 무엇인가요?

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

React에서 `ref` 속성은 일반적으로 무엇에 사용되나요?

#### --distractors--

React 컴포넌트의 성능 문제를 감사하는 데 사용됩니다.

---

React 컴포넌트의 접근성 문제를 감사하는 데 사용됩니다.

---

DOM 노드를 제거하는 데 사용됩니다.

#### --answer--

DOM 노드에 접근하는 데 사용됩니다.

### --question--

#### --text--

`ref`의 현재 값을 접근하는 올바른 방법은 무엇인가요?

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

이 메시지는 콘솔에 몇 번 기록될까요?

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

세 번.

---

두 번.

---

의존성 배열이 비어 있어 기록되지 않습니다.

#### --answer--

초기 렌더링 시 한 번만 기록됩니다.

### --question--

#### --text--

React 커스텀 훅은 일반적으로 무엇에 사용되나요?

#### --distractors--

앱의 가장 복잡한 기능을 실행하는 데 사용됩니다.

---

일반적으로 사용하지 않으며 내장 훅이 선호됩니다.

---

이전 React 버전의 생명주기 메서드를 대체합니다.

#### --answer--

컴포넌트 간에 상태 유지 로직을 재사용하는 데 유용합니다.

### --question--

#### --text--

이 메시지는 콘솔에 몇 번 기록될까요?

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

폼이 제출되거나 클릭 이벤트가 발생할 때마다 기록됩니다.

---

두 번 기록됩니다.

---

한 번만 기록됩니다.

#### --answer--

컴포넌트가 렌더링될 때마다 기록됩니다.

### --question--

#### --text--

`onSubmit` 이벤트에 대해 브라우저 새로 고침을 방지하는 올바른 방법은 무엇인가요?

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--

```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

데이터를 가져오는 데 일반적으로 사용되는 훅은 무엇인가요?

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

네이티브 Event 객체에서 유효하지 않은 속성은 무엇인가요?

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

React가 변경 사항을 감지하고 사용자 인터페이스(UI)를 업데이트해야 할 때 발생하는 렌더링 단계는 무엇인가요?

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

React가 가상 DOM에서 준비된 변경 사항을 실제 DOM에 적용하는 단계를 무엇이라 하나요?

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

`dependencies`에서 `useEffect`의 목적은 무엇인가요?

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

선택적 `dependencies` 인수는 `useEffect`과 관련된 어떤 것도 영향을 주지 않습니다.

---

선택적 `dependencies` 인수는 효과가 무한 루프로 실행되게 합니다.

---

선택적 `dependencies` 인수는 효과 실행을 방지하는 데 사용됩니다.

#### --answer--

선택적 `dependencies` 인수는 효과가 실행되는 시점을 제어합니다.
