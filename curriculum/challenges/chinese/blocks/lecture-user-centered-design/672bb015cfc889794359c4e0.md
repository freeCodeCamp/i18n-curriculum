---
id: 672bb015cfc889794359c4e0
title: 设计模态对话框的最佳实践是什么？
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**注意**：某些交互示例可能使用你尚未学习的 CSS 和 JavaScript。不要担心尝试理解所有的代码。示例的目的是向你展示这些设计概念的预览，以便你更好地理解事物的工作原理。要查看预览，你需要启用交互式编辑器。

什么是模态？它是一种网站可能会在其内容之上显示给你的弹出窗口类型。超文本标记语言 有一个 `dialog` 元素，你可以用它来创建模态。

模态框背后的内容通常会变暗。这有助于用户视觉上聚焦于你希望他们交互的区域——在本例中即模态框。

允许用户点击模态框外部以关闭它总是一个好主意。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

你经常会在模态窗口上看到非常显眼的按钮。这些按钮称为 CTA，或称为 call-to-action。你希望这些按钮易于识别，因为中止用户流程以显示模态窗口的目的是提示他们采取特定的操作。

模态框也应该有一个关闭按钮。虽然你可能非常希望用户点击你的 `CTA`，但重要的是给他们一个选项，可以退出模态框并继续他们之前正在做的事情。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

当然，模态框存在无障碍问题，例如正确管理元素的焦点。然而，如果你以这些通用的做法作为起点，你将拥有一个坚实的基础。

# --questions--

## --text--

模态对话框后面内容变暗的目的是什么？

## --answers--

为了节省移动设备的电池寿命。

### --feedback--

课程提到了这种视觉效果的具体原因。

---

帮助用户直观识别他们应交互的区域。

---

为了使网站装载更快。

### --feedback--

课程提到了这种视觉效果的具体原因。

---

防止用户看到敏感信息。

### --feedback--

课程提到了这种视觉效果的具体原因。

## --video-solution--

2

## --text--

在模态设计的上下文中，CTA 代表什么？

## --answers--

点击激活。

### --feedback--

本课在讨论模态框中的突出按钮时提供了该缩写的完整术语。

---

完成该操作。

### --feedback--

本课在讨论模态框中的突出按钮时提供了该缩写的完整术语。

---

号召性用语。

---

关闭该应用。

### --feedback--

本课在讨论模态框中的突出按钮时提供了该缩写的完整术语。

## --video-solution--

3

## --text--

为什么在模态对话框中包含关闭按钮很重要？

## --answers--

某些国家法律要求。

### --feedback--

本课强调允许用户退出模态框的重要性。

---

为了防止模态导致浏览器崩溃。

### --feedback--

本课强调允许用户退出模态框的重要性。

---

为用户提供一个退出模态框并恢复其先前活动的选项。

---

使模态框看起来更专业。

### --feedback--

本课强调允许用户退出模态框的重要性。

## --video-solution--

3
