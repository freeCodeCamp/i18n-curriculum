---
id: 672bb015cfc889794359c4e0
title: 設計模式對話窗的最佳實務是什麼？
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**注意**：有些互動範例可能會使用你還沒學過的 CSS 和 JavaScript。不要擔心試著理解所有的程式碼。這些範例的目標是為了展示這些設計概念的預覽，讓你更了解事物的運作方式。要看到預覽，你需要啟用互動式編輯器。

什麼是 modal？它是一種網站可能會在其內容上方顯示給你的彈出視窗。HTML 有一個 `dialog` 元素，你可以用來創建 modal。

模態視窗背後的內容通常會被調暗。這有助於使用者視覺上專注於你希望他們互動的區域－在此情況下，即為模態視窗。

允許使用者點擊模態框外部以關閉它，總是個好主意。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

你經常會在模態視窗上看到非常顯眼的按鈕。這些按鈕稱為 CTA，或稱為 call-to-action。你希望這些按鈕容易辨識，因為使用模態視窗中斷使用者流程的目的是要促使他們採取特定的行動。

模態視窗也應該有一個關閉按鈕。雖然你可能真的希望使用者點擊你的 CTA，但重要的是要給他們一個選項，可選方案，以退出模態視窗並繼續他們之前正在做的事情。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

當然，模態視窗存在存取性問題，例如正確管理元素的焦點。然而，如果你以這些一般做法作為起點，你將擁有穩固的基礎來建置。

# --questions--

## --text--

將背景內容調暗以突顯模態對話窗的目的為何？

## --answers--

為了節省行動裝置的電池壽命。

### --feedback--

課程提到此視覺效果的特定原因。

---

為了幫助使用者視覺上識別他們應該互動的區域。

---

為了讓網站載入更快。

### --feedback--

課程提到此視覺效果的特定原因。

---

為了防止使用者看到敏感資訊。

### --feedback--

課程提到此視覺效果的特定原因。

## --video-solution--

2

## --text--

在模態設計的背景關係中，CTA 代表什麼？

## --answers--

點擊以作用。

### --feedback--

當討論模態視窗中的顯著按鈕時，本課程會提供此縮寫的完整術語。

---

完成該操作。

### --feedback--

當討論模態視窗中的顯著按鈕時，本課程會提供此縮寫的完整術語。

---

行動呼籲。

---

關閉應用程式。

### --feedback--

當討論模態視窗中的顯著按鈕時，本課程會提供此縮寫的完整術語。

## --video-solution--

3

## --text--

為什麼在模態對話窗中包含關閉按鈕很重要？

## --answers--

某些國家法律規定必須如此。

### --feedback--

本課程強調允許使用者退出模態視窗的重要性。

---

為了防止模式視窗使瀏覽器當機。

### --feedback--

本課程強調允許使用者退出模態視窗的重要性。

---

為使用者提供一個離開彈出視窗並恢復先前活動的選項。

---

為了讓 modal 看起來更專業。

### --feedback--

本課程強調允許使用者退出模態視窗的重要性。

## --video-solution--

3
