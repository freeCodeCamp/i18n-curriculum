---
id: 66f1a417757b6ca4eecd89d6
title: React 狀態與掛鉤測驗
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一種方式是在 React 中為按鈕添加點擊事件的正確方法？

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

什麼是 React Synthetic Event？

#### --distractors--

僅適用於 `inputs` 和 `submit` 事件的 JSX 元素型別。

---

僅適用於 `submit` 事件的 JSX 元素型別。

---

一種改變 `event` 傳播的冒泡階段，使其對使用者更友善的方法。

#### --answer--

包裝像 `click` 和 `submit` 事件這類原生的事件。

### --question--

#### --text--

以下哪一項是用於命名事件處理常式函式的常見前置式？

#### --distractors--

正在處理

---

已處理

---

處理

#### --answer--

控制代碼

### --question--

#### --text--

React 呈現的三個主要階段是什麼？

#### --distractors--

握手、載入和補水。

---

擷取、解碼與執行。

---

掛載、更新和卸載。

#### --answer--

觸發、呈現和提交。

### --question--

#### --text--

以下哪一種方式是使用 `useState` 掛鉤的正確方法？

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

以下哪一項不是用於處理 React 事件的有效屬性？

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

以下哪一種是更新陣列狀態的正確方法？

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Frontend");
```

---

```js
setCertificates.push("Frontend");
```

---

```js
setCertificates().append("Frontend");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Frontend"]);
```

### --question--

#### --text--

這個函式（程式）有什麼問題？

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

不允許在巢狀函式中更新 `useState` 變數。

---

`previousState` 需要用括號括起來。

---

不應在狀態設值函式內使用箭頭函式。

#### --answer--

必須在 `name` 之前展開 `previousState`，以確保不會覆寫任何其他屬性。

### --question--

#### --text--

以下哪一種方式是更新狀態以從陣列中移除條款的正確方法？

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

在 React 中，`ref` 屬性通常用於什麼？

#### --distractors--

它用於審核 react 組件中的效率問題。

---

它用於審核 react 組件中的存取性問題。

---

它用於移除 DOM 節點。

#### --answer--

它用於存取 DOM 節點。

### --question--

#### --text--

以下哪一種方式是正確的用來存取 `ref` 的當前值？

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

這則訊息會被記錄到主控台多少次？

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

三次。

---

兩次。

---

沒有，因為相依性陣列是空的。

#### --answer--

只在初始呈現時執行一次。

### --question--

#### --text--

React 訂製掛鉤通常用來做什麼？

#### --distractors--

它們用於執行應用程式中最複雜的功能。

---

它們通常不會被使用，建議優先使用內建掛鉤。

---

它們取代了先前版本 React 的生命週期方法。

#### --answer--

它們有助於在組件之間重複使用有狀態的邏輯。

### --question--

#### --text--

這則訊息會被記錄到主控台多少次？

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

只要表單被提交或點擊事件發生，就會被記錄。

---

它將被記錄兩次。

---

它只會被記錄一次。

#### --answer--

每次組件呈現時都會被記錄。

### --question--

#### --text--

以下哪一種方法是防止瀏覽器對 `onSubmit` 事件進行重新整理的正確方式？

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

以下哪一個掛鉤通常用於擷取資料？

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

以下哪一項不是原生 `Event` 物件上的有效屬性？

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

以下哪一個呈現階段會在 React 偵測到某些東西已改變且使用者介面（UI）可能需要更新時發生？

#### --distractors--

設值函式

---

提交

---

迴圈

#### --answer--

觸發

### --question--

#### --text--

以下哪個階段是指 React 從虛擬 DOM 取用已準備好的變更並將它們套用到真實 DOM？

#### --distractors--

觸發

---

呈現、算繪

---

設值函式

#### --answer--

提交

### --question--

#### --text--

`useEffect` 中的 `dependencies` 目的是什麼？

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

選用的 `dependencies` 引數不會影響任何與 `useEffect` 有關的內容。

---

選用的 `dependencies` 引數會讓效果在無窮迴圈中執行。

---

選用的 `dependencies` 引數用於防止 effect 被執行。

#### --answer--

選用的 `dependencies` 引數控制效果何時執行。

