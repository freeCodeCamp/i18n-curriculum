---
id: 5900f5081000cf542c510019
title: '문제 411: 오르막 경로'
challengeType: 1
forumTopicId: 302080
dashedName: problem-411-uphill-paths
---

# --description--

$n$을 양의 정수라고 합시다. 좌표가 $(x, y) = (2^i\bmod n, 3^i\bmod n)$인 역들이 $0 ≤ i ≤ 2n$에 대해 있다고 가정합니다. 좌표가 같은 역들은 같은 역으로 간주합니다.

$(0, 0)$에서 $(n, n)$까지 $x$와 $y$ 좌표가 절대 감소하지 않는 경로를 만들고자 합니다.

$S(n)$을 그런 경로가 지날 수 있는 역의 최대 개수라고 합시다.

예를 들어, $n = 22$일 때, 서로 다른 역은 11개이고, 유효한 경로는 최대 5개의 역을 지날 수 있습니다. 따라서 $S(22) = 5$입니다. 아래 그림은 최적 경로 예시와 함께 이 경우를 보여줍니다:

<img alt="valid path passing through 5 stations, for n = 22, with 11 distinct stations" src="https://cdn.freecodecamp.org/curriculum/project-euler/uphill-paths.png" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

또한 $S(123) = 14$이고 $S(10\,000) = 48$임을 확인할 수 있습니다.

$1 ≤ k ≤ 30$에 대해 $\sum S(k^5)$를 구하세요.

# --hints--

`uphillPaths()` should return `9936352`.

```js
assert.strictEqual(uphillPaths(), 9936352);
```

# --seed--

## --seed-contents--

```js
function uphillPaths() {

  return true;
}

uphillPaths();
```

# --solutions--

```js
// solution required
```
