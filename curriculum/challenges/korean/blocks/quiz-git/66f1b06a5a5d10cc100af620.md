---
id: 66f1b06a5a5d10cc100af620
title: Git 퀴즈
challengeType: 8
dashedName: quiz-git
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답하세요.

# --quizzes--

## --quiz--

### --question--

#### --text--

`git`이란 무엇인가요?

#### --distractors--

인기 있는 접근성 도구 유형입니다.

---

소프트웨어 프로젝트에서 자주 사용하는 린터입니다.

---

특별한 유형의 JavaScript 라이브러리입니다.

#### --answer--

버전 관리 시스템은 프로젝트의 변경 사항을 추적하고 관리할 수 있게 해줍니다.

### --question--

#### --text--

다음 중 인기 있는 클라우드 기반 버전 관리 제공자는 무엇인가요?

#### --distractors--

Spring Boot

---

Laravel

---

Remix

#### --answer--

GitHub

### --question--

#### --text--

`git config` 명령어는 무엇을 하나요?

#### --distractors--

변경 사항 리베이스를 위한 구성 변수를 설정합니다.

---

GitLab에서 원격 저장소만 관리하는 구성을 설정합니다.

---

병합 충돌 해결 구성을 설정합니다.

#### --answer--

Git이 컴퓨터에서 어떻게 작동하는지에 관한 구성 변수를 설정합니다.

### --question--

#### --text--

현재 컴퓨터에 설치된 Git 버전을 보여주는 명령어는 무엇인가요?

#### --distractors--

`git --versions`

---

`git --setVersion`

---

`git --set`

#### --answer--

`git --version`

### --question--

#### --text--

명령줄을 벗어나지 않고 GitHub 관련 작업을 할 수 있는 도구는 무엇인가요?

#### --distractors--

GitHub CI/CD

---

GitHub Actions

---

GitHub Pages

#### --answer--

GitHub CLI

### --question--

#### --text--

GitHub 저장소에서 코드 빌드, 테스트, 배포 등 워크플로우를 자동화할 수 있는 기능은 무엇인가요?

#### --distractors--

GitHub Pipelines

---

GitHub Cloud

---

GitHub API

#### --answer--

GitHub Actions

### --question--

#### --text--

다음 명령어 중 빈 `git` 저장소를 초기화하는 명령어는 무엇인가요?

#### --distractors--

`git status`

---

`git set`

---

`git log`

#### --answer--

`git init`

### --question--

#### --text--

`git push` 명령어는 무엇을 하나요?

#### --distractors--

이 명령어는 전체 Git 기록을 GitHub에 푸시하는 데 사용됩니다.

---

이 명령어는 원격 저장소에서 변경 사항을 로컬 컴퓨터로 가져오는 데 사용됩니다.

---

이 명령어는 코드를 GitHub에 푸시하고 `main` 브랜치에 자동 병합합니다.

#### --answer--

이 명령어는 로컬 커밋을 원격 저장소로 푸시하는 데 사용됩니다.

### --question--

#### --text--

저장소를 복제하는 데 사용하는 명령어는 무엇인가요?

#### --distractors--

`git cloning`

---

`git cloned`

---

`git clones`

#### --answer--

`git clone`

### --question--

#### --text--

작성자, 커밋 날짜, 메시지, 해시 등 유용한 정보를 포함해 이전 커밋을 모두 나열하는 명령어는 무엇인가요?

#### --distractors--

`git rebase`

---

`git status`

---

`git commit`

#### --answer--

`git log`

### --question--

#### --text--

로컬 Git 저장소와 연결된 모든 원격 저장소를 나열하는 명령어는 무엇인가요?

#### --distractors--

`git rebase -v`

---

`git pull -v`

---

`git checkout -v`

#### --answer--

`git remote -v`

### --question--

#### --text--

`git reset` 명령어는 무엇을 하나요?

#### --distractors--

이 명령어는 병합 충돌 해결 시도 모두를 초기화합니다.

---

이 명령어는 `git rebase` 시도 모두를 초기화합니다.

---

이 명령어는 저장소의 모든 커밋을 초기화합니다.

#### --answer--

이 명령어는 브랜치의 현재 상태를 초기화합니다.

### --question--

#### --text--

풀 리퀘스트란 무엇인가요?

#### --distractors--

한 브랜치에서 다른 브랜치로 커밋 수열을 병합해 달라는 요청입니다.

---

모든 병합 충돌을 해결해 달라는 요청입니다.

---

저장소에서 모든 커밋을 제거해 달라는 요청입니다.

#### --answer--

자신의 브랜치 변경 사항을 대상 브랜치로 가져오도록 요청하는 것입니다.

### --question--

#### --text--

프로젝트에서 `main` 브랜치는 무엇을 나타내나요?

#### --distractors--

애플리케이션의 주요 문서 브랜치를 나타냅니다.

---

애플리케이션의 핫픽스 브랜치를 나타냅니다.

---

애플리케이션의 주요 기능 브랜치를 나타냅니다.

#### --answer--

애플리케이션의 주요 또는 프로덕션 브랜치를 나타냅니다.

### --question--

#### --text--

GitHub Pages는 무엇에 가장 적합한가요?

#### --distractors--

C#과 Unity 게임 배포에 가장 적합합니다.

---

Python 전용 애플리케이션 배포에 가장 적합합니다.

---

백엔드 중심 웹사이트 배포에 가장 적합합니다.

#### --answer--

정적 사이트 배포에 가장 적합합니다.

### --question--

#### --text--

작업 디렉터리의 현재 상태를 보여주는 명령어는 무엇인가요?

#### --distractors--

`git show`

---

`git check`

---

`git update`

#### --answer--

`git status`

### --question--

#### --text--

원격 저장소에서 변경 사항을 가져오는 명령어는 무엇인가요?

#### --distractors--

`git get`

---

`git sync`

---

`git update`

#### --answer--

`git fetch`

### --question--

#### --text--

`.gitignore` 파일이란 무엇인가요?

#### --distractors--

프로젝트의 전체 `git` 기록을 보여주는 특별한 유형의 파일입니다.

---

변경 사항의 차이를 보여주는 특별한 유형의 파일입니다.

---

프로젝트 충돌을 해결하는 데 사용하는 특별한 유형의 파일입니다.

#### --answer--

프로젝트에서 특정 파일과 폴더를 `git`가 추적하지 않도록 알려주는 특별한 유형의 파일입니다.

### --question--

#### --text--

로컬 브랜치를 모두 나열하는 명령어는 무엇인가요?

#### --distractors--

`git show`

---

`git log`

---

`git list`

#### --answer--

`git branch`

### --question--

#### --text--

한 브랜치에서 다른 브랜치로 커밋 수열을 이동하거나 병합하는 데 사용하는 명령어는 무엇인가요?

#### --distractors--

`git remote`

---

`git merge`

---

`git switch`

#### --answer--

`git rebase`
