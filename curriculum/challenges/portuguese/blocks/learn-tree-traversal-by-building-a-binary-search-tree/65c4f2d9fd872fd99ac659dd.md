---
id: 65c4f2d9fd872fd99ac659dd
title: Passo 13
challengeType: 20
dashedName: step-13
---

# --description--

Adicione uma declaração condicional `elif` que verifica se `key` > `node.key`.

# --hints--

Você deve adicionar uma declaração condicional `elif` para verificar se `key > node.key`. Lembre-se de usar a palavra-chave `pass`.

```js
({ test: () => assert(runPython(`_Node(_code).find_class("BinarySearchTree").find_function("_insert").find_ifs()[1].find_conditions()[1].is_equivalent("key > node.key")`))})
```

# --seed--

## --seed-contents--

```py
class TreeNode:

    def __init__(self, key):
        self.key = key
        self.left = None
        self.right = None


class BinarySearchTree:

    def __init__(self):
        self.root = None

--fcc-editable-region--    
    def _insert(self, node, key):
        if node is None:
            return TreeNode(key)

        if key < node.key:
            node.left = self._insert(node.left, key)

--fcc-editable-region--    

```

