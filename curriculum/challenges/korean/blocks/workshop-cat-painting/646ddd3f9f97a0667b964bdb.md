---
id: 646ddd3f9f97a0667b964bdb
title: Step 40
challengeType: 0
dashedName: step-40
---

# --description--

`.cat-left-eye` 요소 안에 `cat-left-inner-eye` 클래스를 가진 `div` 요소 하나와 `.cat-right-eye` 요소 안에 `cat-right-inner-eye` 클래스를 가진 `div` 요소 하나를 만드세요.

# --hints--

기존 `div` 요소와 `cat-left-eye` 클래스를 변경하지 마세요.

```js
assert.lengthOf(document.querySelectorAll('div.cat-left-eye'), 1);
```

기존 `div` 요소와 `cat-right-eye` 클래스를 변경하지 마세요.

```js
assert.lengthOf(document.querySelectorAll('div.cat-right-eye'), 1);
```

`.cat-left-eye` 요소 안에 `div` 요소가 하나 있어야 합니다.

```js
assert.lengthOf(document.querySelectorAll('.cat-left-eye div'), 1);
```

`.cat-right-eye` 요소 안에 `div` 요소가 하나 있어야 합니다.

```js
assert.lengthOf(document.querySelectorAll('.cat-right-eye div'), 1);
```

`.cat-left-eye` 안에 새로 만든 `div` 요소는 `cat-left-inner-eye` 클래스를 가져야 합니다.

```js
assert.isTrue(document.querySelectorAll('.cat-left-eye div')[0]?.classList.contains('cat-left-inner-eye'));
```

`.cat-right-eye` 안에 새로 만든 `div` 요소는 `cat-right-inner-eye` 클래스를 가져야 합니다.

```js
assert.isTrue(document.querySelectorAll('.cat-right-eye div')[0]?.classList.contains('cat-right-inner-eye'));
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>fCC Cat Painting</title>
    <link rel="stylesheet" href="./styles.css">
</head>
<body>
    <main>
      <div class="cat-head">
        <div class="cat-ears">
          <div class="cat-left-ear">
            <div class="cat-left-inner-ear"></div>
          </div>
          <div class="cat-right-ear">
            <div class="cat-right-inner-ear"></div>
          </div>
        </div>

        <div class="cat-eyes">
          --fcc-editable-region--
          <div class="cat-left-eye">
            
          </div>
          <div class="cat-right-eye">
            
          </div>
          --fcc-editable-region--
        </div>        
      </div>
    </main>
</body>
</html>
```

```css
* {
  box-sizing: border-box;
}

body {
  background-color: #c9d2fc;
}

.cat-head {
  position: absolute;
  right: 0;
  left: 0;
  top: 0;
  bottom: 0;
  margin: auto;
  background: linear-gradient(#5e5e5e 85%, #45454f 100%);
  width: 205px;
  height: 180px;
  border: 1px solid #000;
  border-radius: 46%;
}

.cat-left-ear {
  position: absolute;
  top: -26px;
  left: -31px;
  z-index: 1;
  border-top-left-radius: 90px;
  border-top-right-radius: 10px;
  transform: rotate(-45deg);
  border-left: 35px solid transparent;
  border-right: 35px solid transparent;
  border-bottom: 70px solid #5e5e5e;
}

.cat-right-ear {
  position: absolute;
  top: -26px;
  left: 163px;
  z-index: 1;
  transform: rotate(45deg);
  border-top-left-radius: 90px;
  border-top-right-radius: 10px;
  border-left: 35px solid transparent;
  border-right: 35px solid transparent;
  border-bottom: 70px solid #5e5e5e;
}

.cat-left-inner-ear {
  position: absolute;
  top: 22px;
  left: -20px;
  border-top-left-radius: 90px;
  border-top-right-radius: 10px;
  border-bottom-right-radius: 40%;
  border-bottom-left-radius: 40%;
  border-left: 20px solid transparent;
  border-right: 20px solid transparent;
  border-bottom: 40px solid #3b3b4f;
}

.cat-right-inner-ear {
  position: absolute;
  top: 22px;
  left: -20px;
  border-top-left-radius: 90px;
  border-top-right-radius: 10px;
  border-bottom-right-radius: 40%;
  border-bottom-left-radius: 40%;
  border-left: 20px solid transparent;
  border-right: 20px solid transparent;
  border-bottom: 40px solid #3b3b4f;
}
```
