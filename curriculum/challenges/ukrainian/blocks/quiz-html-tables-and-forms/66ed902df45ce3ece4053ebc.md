---
id: 66ed902df45ce3ece4053ebc
title: Тест з таблиць і форм HTML
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який із наведених атрибутів використовується для вказівки URL, куди мають бути надіслані дані форми?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

Які два елементи використовуються для вказівки рядка та заголовка рядка в таблиці?

#### --distractors--

Елемент `r` використовується для рядків, а елемент `thead` — для заголовка.

---

Елемент `row` використовується для рядків, а елемент `th` — для заголовка.

---

Елемент `tr` використовується для рядків, а елемент `head` — для заголовка.

#### --answer--

Елемент `tr` використовується для рядків, а елемент `th` — для заголовка.

### --question--

#### --text--

Для чого використовується елемент `td`?

#### --distractors--

Він використовується для визначення типу даних таблиці.

---

Він використовується для об’єднання двох стовпців.

---

Він використовується для об’єднання двох рядків.

#### --answer--

Він використовується для визначення клітинки таблиці.

### --question--

#### --text--

Яка функція атрибута `colspan`?

#### --distractors--

Визначає довжину стовпця в таблиці.

---

Видаляє розрив рядка між текстом у стовпці.

---

Використовується для визначення кількості стовпців.

#### --answer--

Визначає кількість стовпців, які має охоплювати клітинка таблиці.

### --question--

#### --text--

Яка роль атрибута `placeholder`?

#### --distractors--

Він використовується для надсилання даних на сервер при відправленні форми.

---

Він використовується для зв’язування елемента `label` з елементом `input`.

---

Він використовується для вказівки, що поле введення є лише для читання користувачем.

#### --answer--

Він використовується для надання підказки для поля введення.

### --question--

#### --text--

Яка мета елемента `thead`?

#### --distractors--

Він використовується для групування вмісту тіла в HTML-таблиці.

---

Він використовується для групування опису HTML-таблиці.

---

Він використовується для групування вмісту нижнього колонтитула в HTML-таблиці.

#### --answer--

Він використовується для групування вмісту заголовка в HTML-таблиці.

### --question--

#### --text--

Який із наведених прикладів є прикладом явного зв’язку форми між мітками та полями введення?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

Для чого використовується валідатор HTML?

#### --distractors--

Валідатор — це інструмент, який автоматично форматуватиме ваш HTML-код.

---

Валідатор — це інструмент, який застосовує стилі до вашого HTML.

---

Валідатор — це інструмент, який робить ваш HTML-код швидшим.

#### --answer--

Валідатор — це інструмент, який перевіряє синтаксис HTML-коду, щоб переконатися, що він є дійсним.

### --question--

#### --text--

Який елемент використовується для визначення клітинки в таблиці?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

Який із наведених інструментів дозволяє переглядати та змінювати структуру HTML вебсторінки?

#### --distractors--

Видалення DOM.

---

Тестер DOM.

---

Валідація DOM.

#### --answer--

Інспектор DOM.

### --question--

#### --text--

Який із наведених способів є правильним для групування пов’язаних полів введення?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

Який із наведених способів використовується, щоб вказати, що поле введення є лише для читання?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

Який атрибут вказує, що поле введення має бути заповнене перед відправленням форми?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

Для чого використовують інструменти розробника (Devtools)?

#### --distractors--

Ці інструменти вбудовані безпосередньо в браузер і використовуються для виявлення будь-яких проблем із лінтингом у вашому коді.

---

Ці інструменти вбудовані безпосередньо в браузер і використовуються для автоматичного форматування вашого коду.

---

Ці інструменти вбудовані безпосередньо в браузер і допомагають забезпечити 100% покриття тестами вашого коду.

#### --answer--

Ці інструменти вбудовані безпосередньо в браузер і використовуються для налагодження, профілювання та аналізу вебсторінок.

### --question--

#### --text--

Який із наведених способів є правильним для відключення поля введення?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

Яке з наведених значень є дійсним для атрибута `type`?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

Яке з наведених правильне використання атрибута `size`?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

Які атрибути використовуються для вказівки мінімальної та максимальної кількості символів, необхідних у полі введення?

#### --distractors--

`minlen` і `maxlen`

---

`min` і `max`

---

`minimumLen` і `maximumLen`

#### --answer--

`minlength` і `maxlength`

### --question--

#### --text--

Який із наведених прикладів кнопок **НЕ** використовує правильне значення для атрибута `type`?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

Який атрибут використовується для вказівки значення кнопки?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

Яка роль атрибута `action` всередині відкриваючого тегу `form`?

#### --distractors--

Він використовується, щоб вказати, що поле введення є лише для читання.

---

Він використовується для визначення кількості символів, які мають бути видимі під час введення в поле.

---

Він використовується, щоб показати підказку користувачу, що вводити в поле введення.

#### --answer--

Він використовується для вказівки URL, куди мають бути надіслані дані форми.

### --question--

#### --text--

Який із наведених атрибутів використовується для вказівки HTTP-методу, який слід застосувати при надсиланні даних форми?

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

Який із наведених методів є загальноприйнятим при роботі з формами?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

Яке з наведених значень **НЕ** є дійсним для атрибута `type`?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

Який із наведених способів є правильним для створення кнопки у формі?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

Що таке неявне зв’язування форми?

#### --distractors--

Це коли поля введення можна зв’язати з мітками, використовуючи атрибути `action` і `for` на елементі `label`.

---

Це коли поля введення можна зв’язати з мітками, використовуючи атрибути `required` і `for` на елементі `label`.

---

Це коли поля введення можна зв’язати з мітками, використовуючи атрибут `for` на елементі `label`.

#### --answer--

Це коли поле введення обгортається всередині елемента `label`.

### --question--

#### --text--

Яка роль елемента `fieldset`?

#### --distractors--

Він використовується для групування вмісту заголовка в HTML-таблиці.

---

Він використовується для створення мітки для поля введення.

---

Він використовується для додавання підпису, що описує групу полів введення.

#### --answer--

Він використовується для групування пов’язаних полів введення разом.

### --question--

#### --text--

Який із наведених елементів використовується для створення прапорця у формі?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

Який із наведених елементів використовується для додавання заголовка до HTML-таблиці?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

Яка роль елемента `legend`?

#### --distractors--

Він використовується для групування вмісту тіла в HTML-таблиці.

---

Він використовується для додавання підпису, що описує клітинки таблиці.

---

Він використовується для групування пов’язаних полів введення разом.

#### --answer--

Він використовується для додавання підпису, що описує групу полів введення.

### --question--

#### --text--

Який із наведених елементів використовується для групування вмісту заголовка в HTML-таблиці?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

Який із наведених способів є правильним для додавання рядків і клітинок даних до таблиці?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

Що таке інспектор DOM?

#### --distractors--

Інструмент, який використовується для видалення всіх елементів із DOM.

---

Набір інструментів, вбудованих безпосередньо в браузер, який допомагає налагоджувати, профілювати та аналізувати вебсторінки.

---

Інструмент, який перевіряє синтаксис HTML-коду, щоб переконатися, що він є дійсним.

#### --answer--

Інструмент, який дозволяє переглядати та змінювати структуру HTML вебсторінки.

### --question--

#### --text--

Який із наведених інструментів перевіряє синтаксис HTML-коду, щоб переконатися, що він є дійсним?

#### --distractors--

HTML validate

---

HTML isValid

---

HTML valid

#### --answer--

HTML validator

### --question--

#### --text--

Який із наведених способів є правильним для вказівки кількості стовпців для клітинки даних у таблиці?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

Що таке фокусований стан для поля введення?

#### --distractors--

Це стан поля введення, коли користувач знімає фокус.

---

Це стан поля введення, коли воно встановлене як лише для читання користувачем.

---

Це стан поля введення, коли воно відключене користувачем.

#### --answer--

Це стан поля введення, коли воно вибране користувачем.

### --question--

#### --text--

Який із наведених способів є правильним для створення радіокнопки?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

Яка роль атрибута `for`?

#### --distractors--

Він використовується, щоб показати підказку користувачу.

---

Він використовується для вказівки значення поля введення.

---

Він використовується для створення мітки для поля введення.

#### --answer--

Він використовується для вказівки, для якого поля введення призначена мітка.

### --question--

#### --text--

Який із наведених атрибутів використовується для визначення кількості символів, які мають бути видимі під час введення в поле?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

Який із наведених атрибутів використовується, щоб показати підказку користувачу, що вводити в поле введення?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
