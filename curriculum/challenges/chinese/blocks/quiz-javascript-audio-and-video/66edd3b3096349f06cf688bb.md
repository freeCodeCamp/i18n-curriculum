---
id: 66edd3b3096349f06cf688bb
title: JavaScript 音频和视频测验
challengeType: 8
dashedName: quiz-javascript-audio-and-video
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript 中 `Audio()` 构造函数的用途是什么？

#### --distractors--

创建一个新的音频文件。

---

播放现有的音频文件。

---

修改音频文件。

#### --answer--

创建一个新的 `audio` 元素。

### --question--

#### --text--

哪个方法用于开始播放 `audio` 或 `video` 元素？

#### --distractors--

`start()`

---

`begin()`

---

`resume()`

#### --answer--

`play()`

### --question--

#### --text--

`pause()` 和 `play()` 方法对于媒体元素有什么区别？

#### --distractors--

`pause()` 返回一个 Promise，而 `play()` 不返回。

---

`pause()` 无法恢复，而 `play()` 可以从任何位置开始。

---

`pause()` 会自动重置到开头，而 `play()` 则从音频暂停的位置开始。

#### --answer--

`pause()` 停止播放但维护当前位置，而 `play()` 开始播放。

### --question--

#### --text--

哪种音频形式具有最广泛的浏览器支持？

#### --distractors--

`FLAC`

---

`OGG`

---

`WAV`

#### --answer--

`MP3`

### --question--

#### --text--

`audio` 或 `video` 元素中的 `source` 元素的作用是什么？

#### --distractors--

指定媒体的卷。

---

为媒体添加字幕。

---

链接到外部音频处理器。

#### --answer--

为不同的形式指定多个媒体源。

### --question--

#### --text--

MP4 形式可以有哪两种可能的 MIME 类型？

#### --distractors--

`mp4/audio` 和 `mp4/video`

---

`audio/mpeg4` 和 `video/mpeg4`

---

`mp4/mpeg` 和 `mp4/stream`

#### --answer--

`audio/mp4` 和 `video/mp4`

### --question--

#### --text--

在音频和视频的上下文中，`codec` 做什么？

#### --distractors--

增加媒体的卷。

---

更改媒体的文件形式。

---

加密媒体以实现安全传输。

#### --answer--

在模拟和数字的形式之间转换媒体。

### --question--

#### --text--

哪个 API 提供了一种在网页应用中创建、处理和分析音频的方法？

#### --distractors--

音频处理 API。

---

声音操作 API。

---

媒体流 API。

#### --answer--

Web Audio API。

### --question--

#### --text--

`getUserMedia()` 方法的目的是什么？

#### --distractors--

使用媒体服务对用户进行身份验证。

---

检索媒体播放的用户偏好。

---

将媒体文件下载到用户的设备。

#### --answer--

访问用户的摄像头和/或麦克风。

### --question--

#### --text--

`HTMLMediaElement` 接口的哪个属性表现当前的播放时间？

#### --distractors--

`time`

---

`playbackPosition`

---

`currentPosition`

#### --answer--

`currentTime`

### --question--

#### --text--

以下代码截图的作用是什么？

```javascript
const audio = new Audio('song.mp3');
audio.loop = true;
```

#### --distractors--

播放音频文件一次。

---

增加音频的卷。

---

在一次循环后暂停音频。

#### --answer--

将音频设置为无限循环。

### --question--

#### --text--

MIME 的完整含义是什么？

#### --distractors--

多个互联网消息扩展名

---

邮件互联网多媒体扩展名

---

多媒体互联网邮件扩展名

#### --answer--

多用途互联网邮件扩展名

### --question--

#### --text--

`canPlayType()` 方法帮助你做什么？ 

#### --distractors--

查看浏览器中是否正在以特定形式播放音频。

---

验证媒体文件是否存在于浏览器的缓存中并且可供播放。

---

测试音频元素是否启用了控件以及用户是否具有与之交互的适当权限。

#### --answer--

确定浏览器是否可能能够播放你的特定音频形式。

### --question--

#### --text--

以下哪项不是 `HTMLMediaElement` API 的有效事件？

#### --distractors--

`waiting`

---

`canplay`

---

`ended`

#### --answer--

`stopped`

### --question--

#### --text--

当由于数据缓冲区而暂停播放时，哪个 `HTMLMediaElement` 事件会触发？

#### --distractors--

`stopped`

---

`pause`

---

`buffering`

#### --answer--

`waiting`

### --question--

#### --text--

哪个 Web Audio API 接口用于表现音频处理图？

#### --distractors--

`AudioNode`

---

`AudioBuffer`

---

`AudioListener`

#### --answer--

`AudioContext`

### --question--

#### --text--

Media Source Extensions API 的目的是什么？

#### --distractors--

为视频添加视觉效果。

---

在不同媒体格式之间转换。

---

启用 JavaScript 以同步多个音频轨道。

#### --answer--

它允许你将用户的摄像头视频直接传递给 `video` 元素

### --question--

#### --text--

`ended` 事件何时触发？

#### --distractors--

当媒体源更改时。

---

当调用 `pause()` 方法时。

---

当媒体被手动停止时。

#### --answer--

当媒体播放结束时。

### --question--

#### --text--

屏幕捕捉 API 如何暴露？

#### --distractors--

通过 `window.screenCapture` 接口

---

通过使用 `navigator.capture` 属性

---

通过使用 `captureScreen()` 方法

#### --answer--

通过调用 `getDisplayMedia()` 方法。

### --question--

#### --text--

WMV 格式关联的是什么？

#### --distractors--

宽媒体视频

---

世界媒体视频

---

网页媒体播放器

#### --answer--

Windows Media Player
