---
id: 66edcd0ecb4b25dc64a34804
title: JavaScript 物件小測驗
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一項不描述 JavaScript 中的物件？

#### --distractors--

一個鍵值配對的群集。

---

一個用於屬性和方法的容器。

---

非原始資料型別。

#### --answer--

一種原始資料型別。

### --question--

#### --text--

以下哪一個物件定義是無效的？

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

你如何存取這個 `person` 物件的 `job` 屬性？

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

你如何存取下方 `person` 物件的 `street` 屬性？

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

這些屬性鍵中，哪些只能使用括號表示法存取？

#### --distractors--

已知且靜態的屬性鍵。

---

巢狀於物件多個層次中的屬性鍵。

---

可能不存在於該物件中的屬性鍵。

#### --answer--

一個動態的屬性鍵。

### --question--

#### --text--

以下哪一項正確使用解構賦值來存取下方 `person` 物件的 `name` 屬性？

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

下列哪一項正確使用解構賦值來存取下方 `recipe` 物件的 `flour` 屬性？

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

在 JavaScript 中使用解構賦值時，如何為屬性鍵指定預設值？

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

你如何檢查 `car` 物件是否有 `year` 屬性？

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

什麼是物件方法？

#### --distractors--

與物件相關的字串。

---

與物件相關的陣列。

---

與物件相關的數字。

#### --answer--

與物件相關的函式（程式）。

### --question--

#### --text--

你如何呼叫這個 `calculator` 物件的 `add` 方法來加上數字 `10` 和 `7`？

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

哪一個運算子會從物件中移除屬性？

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

你如何檢查 `player` 物件中是否存在 `score` 屬性？

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

你如何在 JavaScript 中創建一個新的空物件？

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

在 JavaScript 中使用選用串鏈的正確語法是什麼？

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

JavaScript 中的選用串鏈用來做什麼？

#### --distractors--

用於定義具有預設值的變數。

---

用於定義物件中可能會或不會被呼叫的選用方法。

---

用於透過在單一述語中串接多個方法呼叫來撰寫更簡潔的程式碼。

#### --answer--

用於安全地存取可能為 `null` 或 `undefined` 的物件屬性。

### --question--

#### --text--

將 JavaScript 物件轉換成 JSON 字串使用了什麼方法？

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

哪一個方法會將 JSON 字串轉換回 JavaScript 物件？

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

以下哪一個選項使用括號表示法存取 `toy` 物件的 `color` 屬性？

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

你如何使用點記法在 JavaScript 中存取這個 `learner` 物件的 `home` 電話？

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`

