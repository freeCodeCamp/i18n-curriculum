---
id: 65c4f013851cefd1a4fe4c96
title: Крок 3
challengeType: 20
dashedName: step-3
---

# --description--

Метод `__init__` приймає два параметри: `self` (який представляє екземпляр класу, що створюється) та `key` (значення, яке потрібно зберегти у вузлі). Додайте ці два параметри до методу `__init__()`.

# --hints--

Ви повинні додати параметр `self` до методу.

```js
({ test: () => assert.match(code, /def\s+__init__\s*\(\s*self\s*/) });
```

Ви повинні додати параметр `key` до методу.

```js
({
  test: () => assert.match(code, /def\s+__init__\s*\(\s*self\s*,\s*key\s*\)/)
});
```

# --seed--

## --seed-contents--

```py
--fcc-editable-region--
class TreeNode:
    def __init__():
        pass
--fcc-editable-region--
```
