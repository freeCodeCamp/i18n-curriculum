---
id: 66edd07682767adff3a6231e
title: JavaScript를 이용한 DOM 조작과 클릭 이벤트 퀴즈
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

문서 객체 모델(Document Object Model)이란 무엇인가요?

#### --distractors--

HTML 페이지와 상호작용하기 위해 사용되는 프로그래밍 언어입니다.

---

부동 소수점 숫자만 포함하는 객체 집합입니다.

---

HTML 요소를 나타내는 JavaScript 클래스 집합입니다.

#### --answer--

HTML 문서와 상호작용할 수 있게 해주는 프로그래밍 인터페이스입니다.

### --question--

#### --text--

API는 무엇의 약자인가요?

#### --distractors--

추가 프로그래밍 인터페이스

---

프로그램 인터페이스 추가

---

또 다른 프로그래밍 인터페이스

#### --answer--

애플리케이션 프로그래밍 인터페이스

### --question--

#### --text--

JavaScript를 사용해 하이퍼링크가 없는 모든 HTML 앵커를 어떻게 찾을 수 있나요?

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

`textContent` 속성은 무엇을 하나요?

#### --distractors--

모든 HTML 요소에서 텍스트 내용을 제거합니다.

---

JavaScript 파일에서 텍스트 내용을 제거합니다.

---

JavaScript 파일의 모든 배열과 객체에 텍스트를 추가합니다.

#### --answer--

요소의 일반 텍스트 내용을 반환합니다.

### --question--

#### --text--

`innerHTML` 속성은 무엇을 하나요?

#### --distractors--

HTML 요소의 모든 텍스트 내용을 가져오는 데 사용되는 `Element`의 속성입니다.

---

HTML에 주석을 추가하는 데 사용되는 `Element`의 속성입니다.

---

HTML에서 이벤트를 발생시키는 데 사용되는 `Element`의 속성입니다.

#### --answer--

HTML 마크업의 일부를 설정하거나 업데이트하는 데 사용되는 `Element`의 속성입니다.

### --question--

#### --text--

다음 중 유효한 이벤트 리스너를 추가하는 방법은 무엇인가요?

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

다음 중 `document` 객체의 유효하지 않은 메서드는 무엇인가요?

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()`

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

다음 중 `class` 속성을 `my-class`로 설정하는 방법은 무엇인가요?

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

`Event` 객체란 무엇인가요?

#### --distractors--

JavaScript 프로젝트에서 이벤트를 제거하는 데 사용되는 특별한 객체입니다.

---

JavaScript 프로젝트에 이벤트를 자동으로 추가하는 데 사용됩니다.

---

배열 작업 시 사용되는 특별한 객체입니다.

#### --answer--

사용자가 웹 페이지와 어떤 방식으로 상호작용할 때 발생하는 페이로드입니다.

### --question--

#### --text--

이전에 `addEventListener()` 메서드를 사용해 요소에 추가한 이벤트 리스너를 제거하는 데 사용하는 메서드는 무엇인가요?

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

인라인 이벤트 핸들러란 무엇인가요?

#### --distractors--

모든 이벤트를 취소하는 데 사용되는 HTML 요소의 특별한 속성입니다.

---

JavaScript 코드에서 폼 제출만 트리거하는 데 사용되는 HTML 요소의 특별한 속성입니다.

---

이벤트를 제거하는 데 사용되는 HTML 요소의 특별한 속성입니다.

#### --answer--

이벤트가 발생할 때 JavaScript 코드를 실행하는 데 사용되는 HTML 요소의 특별한 속성입니다.

### --question--

#### --text--

웹 API의 두 가지 주요 분류는 무엇인가요?

#### --distractors--

Java API와 서드파티 API.

---

브라우저 API와 Rust API.

---

시스템 API와 서드파티 API.

#### --answer--

브라우저 API와 서드파티 API.

### --question--

#### --text--

브라우저가 HTML 페이지의 모든 요소를 파싱했을 때 발생하는 이벤트는 무엇인가요?

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

다음 중 요소의 인라인 스타일을 나타내는 속성은 무엇인가요?

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

인라인 이벤트 핸들러를 사용하는 올바른 방법은 무엇인가요?

#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--

지정한 시간 동안 동작을 지연시키는 데 사용하는 메서드는 무엇인가요?

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

JavaScript 내에서 직접 애니메이션을 생성하고 제어하는 데 사용할 수 있는 것은 무엇인가요?

#### --distractors--

Allow Animations API

---

Control Animations API

---

Create Animations API

#### --answer--

Web Animations API

### --question--

#### --text--

Canvas API는 무엇을 하나요?

#### --distractors--

CSS 파일에서 그래픽을 애니메이션하는 데 사용됩니다.

---

JavaScript 파일에서 그래픽을 제거하는 데만 사용됩니다.

---

HTML 파일에서 그래픽을 제거하는 데 사용됩니다.

#### --answer--

JavaScript 파일을 통해 그래픽을 조작하는 데 사용됩니다.

### --question--

#### --text--

이벤트 위임이란 무엇인가요?

#### --distractors--

이벤트가 취소될 때 이벤트를 듣는 과정입니다.

---

클릭 이벤트가 발생할 때 이벤트를 듣는 과정입니다.

---

변경 이벤트가 발생할 때 이벤트를 듣는 과정입니다.

#### --answer--

버블링되어 상위 요소에 도달한 이벤트를 듣는 과정입니다.

### --question--

#### --text--

다음 중 대화상자와 모달 작업 시 사용할 수 없는 메서드는 무엇인가요?

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`
