---
id: 66f1ad049d7a6ac0886cc2ba
title: React フォーム、データ取得、ルーティング クイズ
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

クイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

次のうち、制御された入力の例はどれですか？

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

次のうち、制御されていない入力の例はどれですか？

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

次のうち、Reactでデータを取得する一般的な方法ではないものはどれですか？

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

Reactでデータ取得に一般的に使われるフックはどれですか？

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

メモ化とは何ですか？

#### --distractors--

グラフの2つの頂点間の最短経路を見つけるために使われる一般的なアルゴリズム。

---

すべての制御された入力と制御されていない入力のパフォーマンスを最適化するための特別な手法。

---

部分配列を再帰的にソートしてマージする分割統治アルゴリズム。

#### --answer--

高コストな関数呼び出しの結果を特定の引数に基づいてキャッシュ（記憶）する最適化手法。

### --question--

#### --text--

計算された値をメモ化するために使われるフックはどれですか？

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

関数の参照をメモ化するために正しいフックを使っているのはどれですか？

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

依存関係とは何ですか？

#### --distractors--

依存関係とは、Reactアプリケーションでよく使われるテストライブラリのこと。

---

依存関係とは、サーバー上でのみ動作するReactコンポーネントの種類。

---

依存関係とは、ReactコードをJavaScriptにコンパイルするための特別なコンパイラ。

#### --answer--

依存関係とは、アプリケーション内のあるコンポーネントやモジュールが、正しく動作するために別のものに頼っていること。

### --question--

#### --text--

パッケージマネージャーとは何ですか？

#### --distractors--

アプリケーションにインストールされたサードパーティのパッケージを更新するためのツール。

---

プロジェクト内で作成されたパッケージのみを管理するためのツール。

---

依存関係を`node_modules`フォルダから特別な設定フォルダに移動するためのツール。

#### --answer--

依存関係のインストール、更新、削除に使われるツール。

### --question--

#### --text--

次のうち、JavaScriptエコシステムで一般的に使われないパッケージマネージャーはどれですか？

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

次のうち、プロジェクトのメタデータを含む重要な設定ファイルはどれですか？

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

次のうち、よく使われるReactフレームワークはどれですか？

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

React Routerを使ってReactアプリケーションにルートを追加する正しい方法はどれですか？

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

React Routerで動的ルートを作成する正しい方法はどれですか？

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

URLパスから動的パラメータにアクセスするために使われるフックはどれですか？

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

Next.jsでカスタムリクエストハンドラを作成する正しい方法はどれですか？

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

プロップドリリングとは何ですか？

#### --distractors--

特定のプロップスに応じて状態がどう変わるかを指定するリデューサーを作成するプロセス。

---

子コンポーネントから親コンポーネントへプロップスを渡すプロセス。

---

Reactアプリケーションでコンテキストプロバイダーを作成し、親から子へプロップスを送るプロセス。

#### --answer--

親コンポーネントから深くネストされた子コンポーネントに、子の一部がプロップスを必要としなくてもプロップスを渡すプロセス。

### --question--

#### --text--

次のうち、Reactアプリケーションで一般的に使われない状態管理ソリューションはどれですか？

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

React Developer Toolsは何のために使われますか？

#### --distractors--

これらのツールはReactアプリケーションのアクセシビリティ問題をチェックするためだけに使われます。

---

これらのツールはReactコンポーネントの追加テストを行うために使われます。

---

これらのツールはアプリケーション内のすべてのReactコンポーネントをメモ化するために使われます。

#### --answer--

これらのツールはReactコンポーネントを検査し、パフォーマンス問題を特定するために使われます。

### --question--

#### --text--

React Server Componentsとは何ですか？

#### --distractors--

サーバー上でのみコンテキストを管理するコンポーネント。

---

サーバー上でのみレンダリングされるメモ化されたコンポーネント。

---

サーバー上でのみ状態を管理するコンポーネント。

#### --answer--

サーバー上でのみレンダリングされるコンポーネント。
