---
id: 66edd630f7666cfa54b404d0
title: 비동기 JavaScript 퀴즈
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제에 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

비동기 프로그래밍이란 무엇인가요?

#### --distractors--

코드를 순차적으로 실행하는 방법입니다.

---

코드에서 오류를 처리하는 기법입니다.

---

서버에서 데이터를 가져오거나 보내기 위해 네트워크 요청을 하는 방법입니다.

#### --answer--

메인 스레드를 차단하지 않고 코드를 동시에 실행하는 방법입니다.

### --question--

#### --text--

스레드란 무엇인가요?

#### --distractors--

서버에서 데이터를 가져오는 데 사용하는 함수입니다.

---

웹 브라우저에서 JavaScript 코드를 실행하는 프로그램입니다.

---

다른 함수에 인수로 전달되는 특별한 유형의 함수입니다.

#### --answer--

메인 프로그램 흐름과 독립적으로 실행할 수 있는 명령어 수열입니다.

### --question--

#### --text--

JavaScript에서 Fetch API의 목적은 무엇인가요?

#### --distractors--

비동기 콜백을 관리하기 위해서입니다.

---

쿠키를 생성하고 관리하기 위해서입니다.

---

동적 HTML 콘텐츠를 렌더링하기 위해서입니다.

#### --answer--

서버에 네트워크 요청을 하기 위해서입니다.

### --question--

#### --text--

서버로 데이터를 보내 처리하도록 하는 `HTTP` 메서드는 무엇인가요?

#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--

API에서 데이터를 가져오는 올바른 방법은 무엇인가요?

#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--

JavaScript에서 프로미스란 무엇인가요?

#### --distractors--

Fetch API를 사용할 때 여러 작업을 동기화하는 방법입니다.

---

모든 고차 함수에서 반드시 사용해야 하는 특별한 유형의 콜백 함수입니다.

---

Fetch API를 사용할 때 오류를 처리하는 기법입니다.

#### --answer--

비동기 작업의 최종 완료(또는 실패)를 나타내는 객체입니다.

### --question--

#### --text--

프로미스 체이닝을 사용하면 무엇을 할 수 있나요?

#### --distractors--

여러 동기 작업을 병렬로 실행할 수 있습니다.

---

동기 작업을 더 효율적으로 처리할 수 있습니다.

---

실패한 작업을 자동으로 재시도할 수 있습니다.

#### --answer--

비동기 작업을 순차적으로 하나씩 수행할 수 있습니다.

### --question--

#### --text--

프로미스가 성공적으로 완료되었을 때 처리하는 메서드는 무엇인가요?

#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--

프로미스 체인에서 오류를 처리하는 메서드는 무엇인가요?

#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--

다음 중 유효하지 않은 HTTP 메서드는 무엇인가요?

#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--

비동기 함수를 생성하는 올바른 구문은 무엇인가요?

#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--

비동기 함수에서 `await` 키워드는 무엇을 하나요?

#### --distractors--

새로운 프로미스를 생성합니다.

---

기다리지 않고 즉시 코드를 실행하며 객체를 반환합니다.

---

프로미스 결과를 로그로 출력하고 `null`를 반환합니다.

#### --answer--

프로미스가 해결될 때까지 함수 실행을 일시 중지합니다.

### --question--

#### --text--

웹사이트가 사용자의 위치를 요청할 수 있도록 제공하는 API는 무엇인가요?

#### --distractors--

Locator API

---

Geo API

---

Locate API

#### --answer--

Geolocation API

### --question--

#### --text--

스크립트를 비동기로 다운로드하지만 HTML 문서가 완전히 파싱된 후에 스크립트를 실행하는 속성은 무엇인가요?

#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--

다음 코드는 무엇을 반환하나요?

```js
fetch('https://api.example.com/data')
```

#### --distractors--

데이터가 손상되었다는 오류입니다.

---

데이터를 가져올 수 없다는 오류입니다.

---

아무것도 반환하지 않고 JavaScript가 스크립트의 다음 함수로 넘어갑니다.

#### --answer--

`Promise`가 `Response` 객체로 해결됩니다.

### --question--

#### --text--

Fetch API의 기본 HTTP 메서드는 무엇인가요?

#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--

서버에서 리소스를 삭제하는 데 사용하는 메서드는 무엇인가요?

#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--

프로미스를 생성하는 올바른 방법은 무엇인가요?

#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--

브라우저가 HTML 문서 파싱을 계속하는 동안 스크립트 파일을 비동기로 다운로드하도록 지시하는 속성은 무엇인가요?

#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--

Geolocation API에서 장치의 현재 위치를 가져오는 메서드는 무엇인가요?

#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`
