---
id: 65e9725a484dd50f720e6fe9
title: 学习数据类型和条件式 第 D 课
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-d
---

# --description--

看下面的例子，你想知道字串中单词 `The` 最后一次出现的索引。

```javascript
let sentence = "freeCodeCamp loves The Odin Project! The Odin Project is great!";
```

<a href="https://www.freecodecamp.org/news/javascript-string-handbook" target="_blank"> 指南链接 </a>

# --questions--

## --text--

应该使用哪个方法来查找字串中最后一次出现单词 `The` 的索引？

## --answers--

应使用 `includes()` 方法查找字串中单词 `The` 最后一次出现的索引。

---

应使用 `lastIndexOf()` 方法查找字串中单词 `The` 最后一次出现的索引。

---

应使用 `indexOf()` 方法查找字串中单词 `The` 最后一次出现的索引。

---

应使用 `search()` 方法来查找字串中单词 `The` 最后一次出现的索引。

## --video-solution--

2
