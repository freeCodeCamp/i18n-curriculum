---
id: 66f1af82732957c895f0b21a
title: اختبار SQL وPostgreSQL
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هي قاعدة البيانات العلائقية؟

#### --distractors--

قاعدة بيانات مُنظمة للتعرف على عدم وجود علاقة بين العناصر المخزنة.

---

قاعدة بيانات تخزن بيانات غير منظمة.

---

نظام قائم على ملفات مسطحة.

#### --answer--

قاعدة بيانات مُنظمة للتعرف على العلاقات بين البيانات في الجداول وإدارتها.

### --question--

#### --text--

كيف تختلف قاعدة البيانات غير العلائقية عن قاعدة البيانات العلائقية؟

#### --distractors--

تستخدم قواعد البيانات غير العلائقية الجداول، بينما لا تستخدمها قواعد البيانات العلائقية.

---

يمكن لقواعد البيانات العلائقية تخزين بيانات غير منظمة.

---

قواعد البيانات غير العلائقية أبطأ من قواعد البيانات العلائقية.

#### --answer--

يمكن لقواعد البيانات غير العلائقية تخزين بيانات غير منظمة.

### --question--

#### --text--

ماذا يفعل الأمر `\l` عند تنفيذه في الطرفية التفاعلية لـ PostgreSQL (`psql`)?

#### --distractors--

يعرض جميع المخططات.

---

يعرض جميع الجداول.

---

يتصل بقاعدة بيانات.

#### --answer--

يعرض جميع قواعد البيانات.

### --question--

#### --text--

أي من الأوامر التالية ينشئ جدولًا جديدًا في SQL؟

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

ما هو الغرض من المفتاح الأجنبي في قاعدة بيانات علائقية؟

#### --distractors--

للسماح بتكرار البيانات.

---

لتحديد صف بشكل فريد في الجدول.

---

لتخزين كميات كبيرة من بيانات النص.

#### --answer--

لإنشاء علاقة بين جدولين.

### --question--

#### --text--

أي أمر سيضيف عمودًا جديدًا إلى جدول موجود؟

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

ما نوع العلاقة التي توجد عندما يتطابق سجل واحد في الجدول A مع سجل واحد أو أكثر في الجدول B؟

#### --distractors--

واحد إلى واحد (1-1)

---

عديد إلى عديد (m-m)

---

عديد إلى واحد (m-1)

#### --answer--

واحد إلى عديد (1-m)

### --question--

#### --text--

ما هو الاستعلام؟

#### --distractors--

طلب لحذف جميع البيانات من قاعدة البيانات.

---

صف خاص للقيم الفريدة في قاعدة البيانات.

---

عمود خاص للقيم الفريدة في قاعدة البيانات.

#### --answer--

طلب بيانات محددة من قاعدة البيانات.

### --question--

#### --text--

كيف تحذف عمودًا من جدول في SQL؟

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

ما وظيفة قيد `PRIMARY KEY` في SQL؟

#### --distractors--

لربط جدولين معًا.

---

لتحديد القيمة الافتراضية لعمود.

---

للسماح بقيم فارغة في العمود.

#### --answer--

لضمان احتواء العمود على قيم فريدة.

### --question--

#### --text--

ماذا يفعل الأمر `\c` في الطرفية التفاعلية لـ PostgreSQL (`psql`)?

#### --distractors--

يعرض جميع الجداول في قاعدة البيانات الحالية.

---

يعرض تفاصيل اتصال قاعدة البيانات الحالية.

---

يخرج من واجهة `psql`.

#### --answer--

يتصل بقاعدة البيانات المحددة.

### --question--

#### --text--

ما هو المفتاح المركب في قاعدة بيانات علائقية؟

#### --distractors--

مفتاح مشتق من مفتاح آخر.

---

مفتاح يتغير بشكل متكرر بناءً على تحديثات البيانات.

---

مفتاح يستخدم حصريًا لعلاقات المفتاح الأجنبي.

#### --answer--

مفتاح يتكون من عدة أعمدة لتحديد سجل بشكل فريد.

### --question--

#### --text--

أي أمر يُدخل بيانات في جدول؟

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

كيف تعيد تسمية عمود في SQL؟

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

أي استعلام سيحذف جميع السجلات من جدول الموظفين؟

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

أي قيد في SQL يضمن أن يحتوي العمود على قيم فريدة فقط، مع السماح بالقيم الفارغة؟

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

أي مما يلي ليس مثالًا على قاعدة بيانات علائقية مستخدمة بشكل شائع في الصناعة؟

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQLQuery

### --question--

#### --text--

ماذا تمثل علاقة واحد إلى واحد (1-1) في قاعدة بيانات علائقية؟

#### --distractors--

كل سجل في الجدول A يتطابق مع سجل واحد أو أكثر في الجدول B.

---

كل سجل في الجدول A لا يتطابق مع أي سجل في الجدول B.

---

كل سجل في الجدول A يتطابق مع سجلات متعددة في الجدول B والعكس صحيح.

#### --answer--

كل سجل في الجدول A يتطابق مع سجل واحد بالضبط في الجدول B.

### --question--

#### --text--

أي أمر يعرض جميع الجداول في قاعدة بيانات PostgreSQL الحالية؟

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

أي مما يلي يسمح لك بضم جدول إلى نفسه؟

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`


