---
id: 69a98755ee5508d58e91dccc
title: NPM Quiz
challengeType: 8
dashedName: quiz-npm
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

O que o npm oferece para desenvolvedores?

#### --distractors--

Uma plataforma selecionada que hospeda apenas design assets como fontes e ícones.

---

Um serviço de hospedagem que implanta automaticamente seu projeto na nuvem.

---

Uma solução de banco de dados que gerencia registros de usuários para seus apps.

#### --answer--

Um registro de pacotes de software pré-construídos que resolve problemas comuns.

### --question--

#### --text--

Qual dos seguintes não é uma das três partes a que o npm se refere?

#### --distractors--

O site npmjs.com.

---

O registro de pacotes onde as bibliotecas são armazenadas.

---

A ferramenta de linha de comando para instalar e gerenciar pacotes.

#### --answer--

O runtime do Node.js.

### --question--

#### --text--

O que o comando `npm install` faz?

#### --distractors--

Ele cria a estrutura de um novo `Node.js project` a partir de um template.

---

Ele executa sua aplicação Node.js em um servidor de desenvolvimento local.

---

Ele remove pacotes que não são mais necessários no seu projeto.

#### --answer--

Ele adiciona pacotes pré-construídos ao seu `project` para tarefas comuns.

### --question--

#### --text--

Qual `file` armazena os `metadata` como nome do `project`, `version`, autor e dependências para um `Node.js project`?

#### --distractors--

`README.md`.

---

`node_modules/package.json`

---

`package-lock.json`.

#### --answer--

`package.json`.

### --question--

#### --text--

Como você pode criar interativamente um `package.json` arquivo?

#### --distractors--

Execute `npm install` sem especificar o nome do pacote.

---

Baixe um template da homepage do registro npm.

---

Copie o `file` de outro `project` e renomeie-o.

#### --answer--

Execute o comando `npm init`.

### --question--

#### --text--

Como você cria um `package.json` arquivo com valores padrão sem responder aos prompts?

#### --distractors--

Execute `npm init --interactive`.

---

Crie um arquivo em branco chamado `package.json` e deixe-o vazio.

---

Use `npm init` e exclua todas as solicitações após abrir o editor.

#### --answer--

Execute `npm init -y` (ou `npm init --yes`) para aceitar todos os padrões automaticamente.

### --question--

#### --text--

Onde o npm lista pacotes que são necessários apenas durante o desenvolvimento, como linters ou test runners?

#### --distractors--

Dentro da seção `"dependencies"`.

---

Dentro da seção `"scripts"` sob comandos personalizados.

---

Dentro da própria pasta `node_modules`.

#### --answer--

Dentro da seção `"devDependencies"` de `package.json`.

### --question--

#### --text--

Qual comando instala `nodemon` como uma dependência apenas de desenvolvimento?

#### --distractors--

`npm uninstall nodemon`.

---

`npm save nodemon --dev`.

---

`npm install nodemon`.

#### --answer--

`npm install nodemon -D`.

### --question--

#### --text--

Qual pasta e arquivo são adicionados à raiz do seu projeto após instalar uma dependência?

#### --distractors--

`public` pasta e `index.html`.

---

`dist` pasta e `webpack.config.js`.

---

`src` pasta e `README.md`.

#### --answer--

Pasta `node_modules` e arquivo `package-lock.json`.

### --question--

#### --text--

Como o `package-lock.json` ajuda a manter as compilações consistentes?

#### --distractors--

Ao deletar dependências não usadas antes de cada instalação.

---

Armazenando apenas os números da versão majoritária dos pacotes.

---

Atualizando automaticamente os pacotes para suas versões mais recentes.

#### --answer--

Congelando as versões exatas de cada dependência que foram instaladas.

### --question--

#### --text--

Por que você deve commitar `package-lock.json` no seu repositório Git?

#### --distractors--

Para fazer o `npm` executar mais rápido na máquina de um desenvolvedor.

---

Para atualizar automaticamente dependências desatualizadas.

---

Para reduzir o tamanho do repositório.

#### --answer--

Então toda a equipe instala as mesmas versões de dependência e configuração.

### --question--

#### --text--

Como você instala uma versão específica de um pacote, como `express@4.21.2`?

#### --distractors--

`npm update express@4.21.2`.

---

`npm get express@4.21.2`.

---

`npm use express@4.21.2`.

#### --answer--

`npm install express@4.21.2`.

### --question--

#### --text--

O que o símbolo caret (`^`) permite ao especificar versões de dependência?

#### --distractors--

Ele bloqueia a dependência para uma versão de patch exata.

---

Ele permite atualizações apenas para a versão major.

---

Ele exclui todas as versões abaixo da especificada.

#### --answer--

Permite versões menores e patch mais recentes mas bloqueia novos lançamentos principais.

### --question--

#### --text--

O que o símbolo til (`~`) significa em um intervalo de versão como `~1.2.3`?

#### --distractors--

Ele permite atualizar tanto versões principais quanto secundárias.

---

Ele instala a versão major mais recente independentemente do intervalo.

---

Ele impede que o npm instale qualquer atualização.

#### --answer--

Ele permite atualizar apenas a patch `version` enquanto mantém a `minor version` fixa.

### --question--

#### --text--

Que tipo de mudanças acionam um aumento de major version no versionamento semântico?

#### --distractors--

Pequenas alterações estilísticas em comentários e formatação.

---

Pequenas correções de bug que não alteram o comportamento.

---

Recursos opcionais que não quebram o código existente.

#### --answer--

Mudanças críticas que podem fazer o código existente parar de funcionar.

### --question--

#### --text--

Qual comando lista as dependências que têm versões publicadas mais recentes?

#### --distractors--

`npm check`.

---

`npm list`.

---

`npm refresh`

#### --answer--

`npm outdated`.

### --question--

#### --text--

Yarn, PNPM e Bun são mencionados como alternativas ao npm. Que tipo de ferramentas são elas?

#### --distractors--

Sites de registro onde você publica ou baixa pacotes npm.

---

Serviços de banco de dados que armazenam dados do usuário para pacotes npm.

---

Plataformas de hospedagem na nuvem que fazem o deploy automático de `npm` `projects`.

#### --answer--

Gerenciadores de pacotes alternativos.

### --question--

#### --text--

Onde os scripts npm personalizados são declarados em um projeto?

#### --distractors--

Dentro do arquivo `.npmrc`.

---

Dentro da seção `"dependencies"` de `package.json`.

---

Em um `scripts.json` separado na raiz.

#### --answer--

Dentro do objeto `"scripts"` do `package.json`.

### --question--

#### --text--

Como você executa um script npm personalizado chamado `build`?

#### --distractors--

`build npm`

---

`node build`

---

`run build`.

#### --answer--

`npm run build`.

### --question--

#### --text--

O que o hífen duplo (`--`) faz ao executar `npm run start -- 8000`?

#### --distractors--

Ele silencia a saída do script.

---

Ele força o npm a executar o script em paralelo.

---

Ele solicita privilégios administrativos antes de executar o script.

#### --answer--

Ele diz ao npm para encaminhar tudo que vem depois de `--` como argumentos para o comando subjacente.
