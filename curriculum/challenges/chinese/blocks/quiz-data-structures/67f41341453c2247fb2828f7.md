---
id: 67f41341453c2247fb2828f7
title: 数据结构测验
challengeType: 8
dashedName: quiz-data-structures
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

在算法分析中，Big O 符号描述了什么？

#### --distractors--

特定计算机的精确运行期秒数。

---

运行期间执行的代码行百分比。

---

代码对其他开发者的可读性。

#### --answer--

时间或空间相对于 `input` 大小的增长（一个上边界）。

### --question--

#### --text--

开始一个算法挑战时，最好的第一步是什么？

#### --distractors--

立即开始编码以获得动力。

---

在你理解问题之前，先优化性能。

---

只有在完成 `solution` 后才编写单元测试。

#### --answer--

通过示例和边界情况明确问题和约束。

### --question--

#### --text--

动态数组和静态数组之间的关键区别是什么？

#### --distractors--

动态数组保存不同类型的值；静态数组不能。

---

静态数组允许重复值；动态数组不允许。

---

动态数组在每个 `operation` 上都比静态数组更快。

#### --answer--

动态数组可以通过调整大小来增长或缩小；静态数组具有固定大小。

### --question--

#### --text--

将一个元素追加到动态数组末尾的摊销时间复杂度是多少？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` 摊销。

### --question--

#### --text--

为什么通过索引访问单链表中的第 k 个元素需要 `O(n)` 时间？

#### --distractors--

必须在任何 `access` 之前调整 `list` 的大小。

---

索引经过哈希处理后在表格中查找。

---

节点是连续保存的，因此需要移动。

#### --answer--

你必须从头节点逐个遍历到第 k 个节点。

### --question--

#### --text--

双向链表具有单向链表没有的哪个特色？

#### --distractors--

对任意索引的随机访问在 `O(1)` 时间内完成。

---

用于更快迭代的内置数组缓冲区。

---

列表长度作为常量的自动维护。

#### --answer--

指向下一个和上一个节点的指针，使得能够向后遍历。

### --question--

#### --text--

以下哪项最能描述一个栈？

#### --distractors--

先进先出（`FIFO`），从前端移除。

---

一种结构，其中任何元素都可以在 `O(1)` 时间内被移除。

---

一个具有常量时间随机访问的循环缓冲区。

#### --answer--

后进先出（`LIFO`），在顶部使用 `push` 和 `pop`。

### --question--

#### --text--

哪个操作移除队列前端的元素？

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

在哈希映射中通过密钥查找值的典型平均分支时间复杂度是多少？

#### --distractors--

`O(n)` 因为所有密钥必须循序扫描。

---

由于桶内的二分查找，时间复杂度为 `O(log n)`。

---

`O(n log n)` 因为密钥在插入过程中被排序。

#### --answer--

平均情况下，使用良好的哈希函数和低负载因子时为 `O(1)`。

### --question--

#### --text--

集合数据结构提供了什么保证？

#### --distractors--

元素默认以排序顺序保存。

---

允许重复值并将它们保留在一起。

---

元素按其插入位置进行索引。

#### --answer--

它只保存唯一的元素（无副本）。

### --question--

#### --text--

在动态数组中，在索引 i（不是末尾）插入一个元素的最坏分支时间复杂度是多少？

#### --distractors--

`O(1)`

---

`O(log n)`

---

摊销 `O(1)`

#### --answer--

`O(n)`

### --question--

#### --text--

在单向链表头部插入一个新节点的时间复杂度是多少？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

哪个操作返回栈顶元素而不将其移除？

#### --distractors--

`push`

---

`pop`

---

插入到底部。

#### --answer--

`peek`

### --question--

#### --text--

以下哪项最能描述一个队列？

#### --distractors--

后进先出（`LIFO`），从顶部移除。

---

对任意索引的随机访问在 `O(1)` 时间内完成。

---

元素始终自动保持排序顺序。

#### --answer--

先进先出（`FIFO`），在队列尾部进行 `enqueue`，在队列头部进行 `dequeue`。

### --question--

#### --text--

哈希映射中的哈希冲突是什么？

#### --distractors--

当一个密钥按设计映射到多个不同的值时。

---

当两个一致的密钥保存在不同的桶中。

---

当地图的内存不足且必须调整大小时。

#### --answer--

当两个不同的密钥产生相同的哈希索引时。

### --question--

#### --text--

哈希映射为什么会在增长时调整大小（重新哈希）？

#### --distractors--

将密钥按升序排序以加快迭代速度。

---

压缩数值并减少内存片段化。

---

为了避免触发该语言的垃圾回收器。

#### --answer--

为了保持装载因子低，使平均操作保持为 `O(1)`。

### --question--

#### --text--

关于集合，哪个语句是正确的？

#### --distractors--

集合根据定义保持插入顺序。

---

集合允许副本元素并保持计数。

---

集合成员测试平均为 `O(n log n)`。

#### --answer--

成员测试平均通常是 `O(1)`。

### --question--

#### --text--

随着 n 变大，哪种时间复杂度增长速度比 `O(n log n)` 更快？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

在实现暴力解决方案后，接下来的一个好步骤是什么？

#### --distractors--

在测量之前对常量因子进行微优化。

---

放弃测试并从头重写方案。

---

避免考虑边缘分支以保持代码简单。

#### --answer--

分析其时间/空间复杂度并优化识别出的瓶颈。

### --question--

#### --text--

空间复杂度衡量什么？

#### --distractors--

程序使用多少个 CPU 核心。

---

程序的代码行数长度。

---

程序编译所需的时间。

#### --answer--

内存使用量相对于输入大小的增长情况。

