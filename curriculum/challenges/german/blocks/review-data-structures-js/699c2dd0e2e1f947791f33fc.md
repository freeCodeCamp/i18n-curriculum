---
id: 699c2dd0e2e1f947791f33fc
title: Überprüfung der Datenstrukturen
challengeType: 31
dashedName: review-data-structures-js
---

# --interactive--
 
## Algorithmen und Big-O-Notation

- **Algorithmen**: Eine Menge eindeutiger Anweisungen zur Lösung eines Problems oder zur Ausführung einer Aufgabe. Algorithmen müssen in einer endlichen Anzahl von Schritten enden, und jeder Schritt muss präzise und eindeutig sein.

- **Big-O-Notation**: Beschreibt die schlechteste Laufzeit oder das Wachstum eines Algorithmus, wenn die Eingabegröße zunimmt. Sie konzentriert sich darauf, wie der Ressourcenverbrauch mit der Eingabegröße wächst, und ignoriert konstante Faktoren und Terme niedrigerer Ordnung.

### Häufige Zeitkomplexitäten

- **O(1) – konstante Zeit**: Der Algorithmus benötigt unabhängig von der Eingabegröße dieselbe Zeit.

:::interactive_editor

```js
function checkEvenOrOdd(number) {
  if (number % 2 === 0) {
    return "Even";
  } else {
    return "Odd";
  }
}

console.log(checkEvenOrOdd(4)) // "Even"
console.log(checkEvenOrOdd(5)) // "Odd"
```

:::

- **O(log n) – logarithmische Zeit**: Die Zeit wächst langsam mit der Eingabegröße. Häufig bei Algorithmen, die die Problemgröße wiederholt um einen Bruchteil reduzieren (wie die binäre Suche).

- **O(n) – lineare Zeit**: Die Laufzeit wächst proportional zur Eingabegröße.

```js
for (const grade of grades) { // grades is an array
  console.log(grade);
}
```

- **O(n log n) – log-lineare Zeit**: Häufige Zeitkomplexität effizienter Sortieralgorithmen wie Merge Sort und Quick Sort.

- **O(n²) – quadratische Zeit**: Die Laufzeit wächst quadratisch. Oft bei verschachtelten Schleifen.

:::interactive_editor

```js
const n = 3;

for (let i = 0; i < n; i++) {
  for (let j = 0; j < n; j++) {
    console.log("Hello, World!");
  }
}
```

:::

### Speicherkomplexität

- **O(1) – konstanter Speicher**: Der Algorithmus verwendet unabhängig von der Eingabegröße denselben Speicher.
- **O(n) – linearer Speicher**: Der Speicherverbrauch wächst proportional zur Eingabegröße.
- **O(n²) – quadratischer Speicher**: Der Speicherverbrauch wächst quadratisch mit der Eingabegröße.

## Problemlösungstechniken

- **Problem verstehen**: Lesen Sie die Problemstellung mehrmals. Identifizieren Sie die Eingabe, die erwartete Ausgabe und wie die Eingabe in die Ausgabe transformiert wird.

- **Pseudocode**: Hochwertige Beschreibung der Algorithmuslogik, die unabhängig von der Programmiersprache ist. Verwendet eine Mischung aus gebräuchlicher Schriftsprache und Programmierkonstrukten wie `IF`, `ELSE`, `FOR`, `WHILE`.

```md
GET original_string
SET reversed_string = ""
FOR EACH character IN original_string:
  ADD character TO THE BEGINNING OF reversed_string
DISPLAY reversed_string
```

- **Randfälle**: Spezifische, gültige Eingaben, die an den Grenzen dessen liegen, was ein Algorithmus verarbeiten sollte. Berücksichtigen und testen Sie immer Randfälle.

## Arrays

- **Statische Arrays**: Haben eine feste Größe, die bei der Initialisierung bestimmt wird. Elemente werden an benachbarten Speicherstellen gespeichert. Die Größe kann während der Programmausführung nicht geändert werden.

- **Dynamische Arrays**: Können während der Programmausführung automatisch wachsen oder schrumpfen. Die Größenänderung erfolgt durch automatisches Kopieren in größere Arrays bei Bedarf.

### Implementierung dynamischer Arrays in JavaScript

:::interactive_editor

```js
let numbers = [3, 4, 5, 6];
// Access elements
console.log(numbers[0])  // 3

// Update elements
numbers[2] = 16
console.log([...numbers]);  // [3, 4, 16, 6]

// Add elements
numbers.push(7)
console.log([...numbers]);  // [3, 4, 16, 6, 7] 

// Add at a specific index
numbers.splice(3, 0, 15);
console.log([...numbers]);  // [3, 4, 16, 15, 6, 7]

// Remove elements
numbers.splice(2, 1);  // Remove at specific index
console.log([...numbers]);  // [3, 4, 15, 6, 7]

numbers.pop()   // Remove last element
console.log([...numbers]); // [3, 4, 15, 6]
```

:::

### Zeitkomplexitäten für dynamische Arrays

- **Zugriff**: O(1)
- **Einfügen am Ende**: O(1) im Durchschnitt, O(n) bei Größenänderung
- **Einfügen in der Mitte**: O(n)
- **Löschen**: O(n) in der Mitte, O(1) am Ende

## Stacks

- **Stacks**: Last-In, First-Out (LIFO) Datenstruktur. Elemente werden nur oben hinzugefügt und entfernt.

- **Push-Operation**: Ein Element oben auf den Stack legen. Zeitkomplexität: O(1).

- **Pop-Operation**: Ein Element oben vom Stack entfernen. Zeitkomplexität: O(1).

:::interactive_editor

```js
// Using JavaScript array as a stack

let stack = [];

// Push operations
stack.push(1);
stack.push(2);
stack.push(3);
console.log([...stack]);  // [1, 2, 3]

// Pop operations
let topElement = stack.pop();
console.log(topElement);  // 3
console.log([...stack]);  // [1, 2]
```

:::

## Queues

- **Queues**: First-In, First-Out (FIFO) Datenstruktur. Elemente werden hinten hinzugefügt und vorne entfernt.

- **Enqueue-Operation**: Ein Element hinten in die Queue einfügen. Zeitkomplexität: O(1).

- **Dequeue-Operation**: Ein Element vorne aus der Queue entfernen. Zeitkomplexität: O(1).

:::interactive_editor

```js
// Using JavaScript array as a queue
let queue = [];

// Enqueue operations
queue.push(1);
queue.push(2);
queue.push(3);
console.log([...queue]);  // [1, 2, 3]

// Dequeue operations
let firstElement = queue.shift();
console.log(firstElement);  // 1
console.log([...queue]);  // [2, 3]
```

:::

## Verkettete Listen

- **Verkettete Listen**: Lineare Datenstruktur, bei der jeder Knoten Daten und eine Referenz auf den nächsten Knoten enthält. Die Knoten sind wie eine Kette verbunden.

### Einfach verkettete Listen

- **Struktur**: Jeder Knoten hat Daten und eine Referenz auf den nächsten Knoten.
- **Durchlaufen**: Kann nur vorwärts vom Kopf bis zum Ende gehen.
- **Kopf-Knoten**: Erster Knoten in der Liste, normalerweise der einzige direkt zugängliche Knoten.
- **End-Knoten**: Letzter Knoten in der Liste, zeigt auf `null`.

### Operationen und Zeitkomplexitäten

- **Einfügen am Anfang**: O(1)
- **Einfügen am Ende**: O(n) – muss bis zum Ende durchlaufen
- **Einfügen in der Mitte**: O(n) – muss bis zur Position durchlaufen
- **Löschen am Anfang**: O(1)
- **Löschen am Ende**: O(n) – muss vorherigen Knoten finden
- **Löschen in der Mitte**: O(n) – muss Knoten finden

### Doppelt verkettete Listen

- **Struktur**: Jeder Knoten hat Daten und zwei Referenzen: auf den nächsten und den vorherigen Knoten.
- **Durchlaufen**: Kann in beide Richtungen gehen.
- **Speicher**: Benötigt mehr Speicher als einfach verkettete Listen wegen der zusätzlichen Referenz.

## Wann welche Datenstruktur verwenden

- **Arrays (dynamische Arrays)**: Wenn Sie geordneten, indizierten Zugriff benötigen und die Größe nicht im Voraus kennen
- **Stacks**: Für LIFO-Operationen (Rückgängig-Funktion, Ausdrucksauswertung, Backtracking)
- **Queues**: Für FIFO-Operationen (Aufgabenplanung, Breitensuche)
- **Verkettete Listen**: Wenn häufiges Einfügen/Löschen am Anfang, unbekannte Größe, kein direkter Zugriff benötigt wird

# --assignment--

Überprüfen Sie die Themen und Konzepte zu Datenstrukturen.
