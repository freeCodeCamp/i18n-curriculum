---
id: 695b9093a0c45b36932dcbcb
title: D3 `library` 用于什么？
challengeType: 19
dashedName: what-is-the-d3-library-used-for
---

# --description--

在之前的课程中，你已经了解了数据可视化，它是创建清晰、可视形式以表现复杂数据的进程。在本课中，你将学习 `D3` 库。

D3 库是一个免费且开源的库，你可以用它在 JavaScript 中创建引人注目且有效的数据可视化。

<img src="https://cdn.freecodecamp.org/curriculum/lecture-transcripts/what-is-the-d3-library-used-for-1.png" alt="A graph with four nodes labeled A, B, C, and D. Node A connects to B, C, and D. Node B connects to A and D. Node C connects to A. Node D connects to A and B. It's the same image as before.">


名称 D3 是数据驱动文档（data-driven documents）的缩略语，指的是它如何动态地将数据绑定到文档对象模型（DOM）中的元素。这个库非常灵活，且与网页自然兼容。你无需学习全新的图形表现方式，就可以使用 `svg` 和 `canvas` 元素构建数据可视化。

例如，如果你想使用 D3 创建一个条形图，你可以在一个 JS 文件中完成以下所有操作：

* 声明表现图表维度和边距的变量
    
* 声明水平位置刻度
    
* 声明垂直位置刻度
    
* 创建带有条形和标签的 SVG 容器
    
<img src="https://cdn.freecodecamp.org/curriculum/lecture-transcripts/what-is-the-d3-library-used-for-2.png" alt="A graph with four nodes labeled A, B, C, and D. Node A connects to B, C, and D. Node B connects to A and D. Node C connects to A. Node D connects to A and B. It's the same image as before.">

那么，除了 Chart.js、Plotly 或 Tableau 之外，还有哪些其他理由学习 D3 呢？

与其他工具不同，D3 为你提供了大量底层控件，你可以仅用原生的 HTML、CSS 和 JavaScript 构建可自定义且交互式的可视化效果。此外，该库是用 JavaScript 编写的，所以你无需担心学习一门全新的语言。

在即将到来的研讨会和实验中，你将学习 D3 库的基础知识，如选择、形状和比例。你将通过创建散点图、条形图、仪表盘应用程序等来学习这些关键概念。

# --questions--

## --text--

什么是 D3 库？

## --answers--

 一个用于创建编译器的免费开源库。

### --feedback--

回到课程开始处以获取答案。

---

一个用于创建交互式 `forms` 和 `tables` 的免费开源 `library`。

### --feedback--

回到课程开始处以获取答案。

---

一个用于在 JavaScript 中创建数据可视化的免费开源库。

---

一个用于创建微服务的免费开源 `library`。

### --feedback--

回到课程开始处以获取答案。

## --video-solution--

3

## --text--

使用 D3 相较于 Chart.js 或 Tableau 的一个优势是什么？

## --answers--

 D3 自动以最少的配置生成所有图表类型，类似于拖放工具。

### --feedback--

考虑 D3 为你提供的控件级别。

---

D3 为你提供低级控件，以便你构建可自定义和交互式的可视化。

---

D3 包含一个内置的 GUI（图形用户界面），用于在不编写代码的情况下创建仪表盘。

### --feedback--

考虑 D3 为你提供的控件级别。

---

D3 使用了一种专为数据可视化设计的全新编程语言。

### --feedback--

考虑 D3 为你提供的控件级别。

## --video-solution--

2

## --text--

你将在此数据可视化模块中构建的项目类型是什么？

## --answers--

电子商务网站

### --feedback--

请参考课程结尾处的答案。

---

express 服务器

### --feedback--

请参考课程结尾处的答案。

---

discord 机器人

### --feedback--

请参考课程结尾处的答案。

---

散点图

## --video-solution--

4
