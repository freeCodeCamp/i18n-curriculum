---
id: 5900f37b1000cf542c50fe8e
title: '문제 15: 격자 경로'
challengeType: 1
forumTopicId: 301780
dashedName: problem-15-lattice-paths
---

# --description--

2×2 격자의 왼쪽 위 모서리에서 시작해 오른쪽과 아래 방향으로만 이동할 때, 오른쪽 아래 모서리까지 정확히 6개의 경로가 있습니다.

<img alt="오른쪽 아래 모서리까지의 모든 경로를 보여주는 6개의 2x2 격자 다이어그램" src="https://cdn-media-1.freecodecamp.org/project-euler/1Atixoj.gif" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

주어진 `gridSize`을 통과하는 경로는 몇 개일까요?

# --hints--

`latticePaths(4)`은 숫자를 반환해야 합니다.

```js
assert.isNumber(latticePaths(4));
```

`latticePaths(4)`는 70을 반환해야 합니다.

```js
assert.strictEqual(latticePaths(4), 70);
```

`latticePaths(9)`은 48620을 반환해야 합니다.

```js
assert.strictEqual(latticePaths(9), 48620);
```

`latticePaths(20)`는 137846528820를 반환해야 합니다.

```js
assert.strictEqual(latticePaths(20), 137846528820);
```

# --seed--

## --seed-contents--

```js
function latticePaths(gridSize) {

  return true;
}

latticePaths(4);
```

# --solutions--

```js
function latticePaths(gridSize) {
  let paths = 1;

  for (let i = 0; i < gridSize; i++) {
    paths *= (2 * gridSize) - i;
    paths /= i + 1;
  }
  return paths;
}
```
