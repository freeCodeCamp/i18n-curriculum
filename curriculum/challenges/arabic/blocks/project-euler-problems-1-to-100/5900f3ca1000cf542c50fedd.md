---
id: 5900f3ca1000cf542c50fedd
title: "المشكلة 94: المثلثات شبه المتساوية الأضلاع"
challengeType: 1
forumTopicId: 302211
dashedName: problem-94-almost-equilateral-triangles
---

# --description--

يثبت بسهولة أنه لا يوجد مثلث متساوي الأضلاع بأضلاع طولها أعداد صحيحة ومساحة صحيحة. مع ذلك، المثلث شبه المتساوي الأضلاع 5-5-6 له مساحة 12 وحدة مربعة.

سنعرّف <dfn>المثلث شبه المتساوي الأضلاع</dfn> بأنه مثلث له ضلعان متساويان والضلع الثالث يختلف عنهما بوحدة واحدة على الأكثر.

احسب مجموع محيطات جميع المثلثات شبه المتساوية الأضلاع التي لها أطوال أضلاع ومساحة صحيحة والتي لا يتجاوز محيطها `limit`.

# --hints--

يجب أن تُرجع الدالة `almostEquilateralTriangles(50)` رقمًا.

```js
assert(typeof almostEquilateralTriangles(50) === 'number');
```

يجب أن تُرجع الدالة `almostEquilateralTriangles(50)` القيمة `66`.

```js
assert.strictEqual(almostEquilateralTriangles(50), 66);
```

يجب أن تُرجع الدالة `almostEquilateralTriangles(10000)` القيمة `3688`.

```js
assert.strictEqual(almostEquilateralTriangles(10000), 3688);
```

يجب أن تُرجع الدالة `almostEquilateralTriangles(10000000)` القيمة `9973078`.

```js
assert.strictEqual(almostEquilateralTriangles(10000000), 9973078);
```

يجب أن تُرجع الدالة `almostEquilateralTriangles(1000000000)` القيمة `518408346`.

```js
assert.strictEqual(almostEquilateralTriangles(1000000000), 518408346);
```

# --seed--

## --seed-contents--

```js
function almostEquilateralTriangles(limit) {

  return true;
}

almostEquilateralTriangles(50);
```

# --solutions--

```js
function almostEquilateralTriangles(limit) {
  // Based on https://blog.dreamshire.com/project-euler-94-solution/
  let perimetersSum = 0;

  let sidesAB = 1;
  let sideC = 1;
  let perimeter = 0;
  let perimeterOffset = 1;

  while (perimeter <= limit) {
    [sidesAB, sideC] = [4 * sidesAB - sideC + 2 * perimeterOffset, sidesAB];
    perimeterOffset = -perimeterOffset;

    perimetersSum += perimeter;
    perimeter = 3 * sidesAB - perimeterOffset;
  }

  return perimetersSum;
}
```
