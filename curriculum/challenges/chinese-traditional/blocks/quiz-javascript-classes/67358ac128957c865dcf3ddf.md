---
id: 67358ac128957c865dcf3ddf
title: JavaScript 類別測驗
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript 中類別的目的為何？

#### --distractors--

只定義屬性，而非方法。

---

僅定義靜態變數。

---

只組織原始資料。

#### --answer--

為物件創建可重複使用的藍圖。

### --question--

#### --text--

你如何在 JavaScript 中定義一個類別？

#### --distractors--

使用 `className` 關鍵字。

---

使用 `object` 關鍵字。

---

僅使用 `constructor` 關鍵字。

#### --answer--

使用 `class` 關鍵字。

### --question--

#### --text--

在類別中使用哪個方法來初始化屬性？

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

`this` 在類別建構式內表述什麼？

#### --distractors--

全域的類別物件。

---

函式（程式）本身。

---

未定義的引用。

#### --answer--

一個類別的實體。

### --question--

#### --text--

以下哪一項描述了 `this` 在類別方法內的箭頭函式中的行為？

#### --distractors--

它從不同作用域中最近的函式繼承 `this` 的值。

---

它取用該類別中新創建的實體。

---

在嚴格模式中，它從全域物件繼承 `this` 的值。

#### --answer--

它繼承定義時所處的包覆範圍中的 `this` 值。

### --question--

#### --text--

`extends` 關鍵字的主要用途是什麼？

#### --distractors--

在父類別的建構式中定義靜態方法。

---

在類別中創建方法。

---

在父類別中初始化預設屬性。

#### --answer--

要創建一個繼承自父類別的子類別。

### --question--

#### --text--

以下哪一項是如何創建繼承自 `Vehicle` 類別的 `Car` 類別的範例？

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

以下 `code` 的輸出是什麼？

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

引發錯誤。

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

子類別中 `super` 關鍵字的作用是什麼？

#### --distractors--

定義父類別的新實體。

---

從父類別刪除方法和屬性。

---

僅存取父類別的靜態方法。

#### --answer--

呼叫父類別的建構式或方法。

### --question--

#### --text--

以下哪一項描述靜態方法？

#### --distractors--

在類別內部無法存取的方法。

---

只用於箭頭函式的方法。

---

每個類別實體獨有的方法。

#### --answer--

屬於類別本身而非任何實體的方法。

### --question--

#### --text--

以下 `code` 會輸出什麼？

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

引發錯誤。

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

你如何存取 JavaScript 類別中的靜態屬性？

#### --distractors--

在方法中使用 `this.propertyName`。

---

在建立實體後使用 `instance.propertyName`。

---

在子類別中使用 `super.propertyName`。

#### --answer--

透過使用 `ClassName.propertyName`。

### --question--

#### --text--

在什麼情況下你會使用靜態方法而非實體方法？

#### --distractors--

當你需要存取並修改該實體的屬性時。

---

當需要在物件實體上呼叫該方法時。

---

當該方法不依賴於實體特定的資料且操作類別層級的資料時。

#### --answer--

當該方法操作由該類別所有實體共用的資料時。

### --question--

#### --text--

以下 `code` 會輸出什麼？

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

它會丟出錯誤。

### --question--

#### --text--

繼承另一個類別的屬性和方法時使用什麼關鍵字？

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

以下程式碼列印了什麼？

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

引發錯誤。

#### --answer--

`Human`

### --question--

#### --text--

下列哪一項最能描述繼承？

#### --distractors--

它允許一個方法傳回一個新的物件。

---

它允許一個實體定義自己的屬性。

---

它允許將靜態屬性轉換為非靜態屬性。

#### --answer--

它允許一個類別從另一個類別繼承屬性和方法。

### --question--

#### --text--

如果子類別中沒有定義建構式，會發生什麼情況？

#### --distractors--

會發出錯誤。

---

子類別將繼承程式碼中任何類別的建構式。

---

子類別無法創建新的實體。

#### --answer--

預設會使用父類別的建構式。

### --question--

#### --text--

在以下程式碼中，`this` 表現為什麼？

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

全域物件。

---

`Animal` 類別本身。

---

未定義的變數。

#### --answer--

`Animal` 的實體。

### --question--

#### --text--

靜態屬性的主要用途是什麼？

#### --distractors--

為每個類別實體定義預設屬性。

---

在子類別中改寫方法。

---

定義實體特定的資料。

#### --answer--

用於儲存跨實體共享的類別範圍資料。
