---
id: 66edd3011f18f4ee1bd9d28b
title: JavaScript 正規表達式測驗
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一項不是有效的正規表達式？

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

哪一個 `RegExp` 包含與正規表達式 `/f[o0]{2} b[a4@]r/i` 相同的樣式？

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

`test` 正規表達式方法的返回類型是什麼？

#### --distractors--

符合正規表達式的字串陣列。

---

包含關於正規表達式所匹配的（子）字串的資訊的物件。

---

`null`，`test` 方法僅驗證所給的正規表達式是否有效。

#### --answer--

一個布林值，表示字串是否符合正規表達式。

### --question--

#### --text--

當傳入不帶任何旗標的正規表達式時，`match` 方法會做什麼？

#### --distractors--

它會搜尋任何符合給定正規表達式的字串，並以陣列傳回所有符合項目。

---

它會搜尋第一個完整符合項，並傳回該符合項的起始索引。

---

它會搜尋第一個完整符合項目，並傳回一個布林值以表示是否找到符合項目。

#### --answer--

它會搜尋第一個完整符合項目，並傳回包含該第一個符合項目的陣列。

### --question--

#### --text--

哪一種是 `test` 的最佳使用案例？

#### --distractors--

用於驗證給定的正規表達式。

---

用於取得有關字串匹配的詳細資訊，例如匹配字串的長度。

---

從給定的字串中擷取所有符合項目。

#### --answer--

檢查給定的字串是否符合正規表達式。

### --question--

#### --text--

使用 `replace` 的目的為何？

#### --distractors--

將目前的正規表達式取代為新的正規表達式。

---

當給定一個索引和一個字串時，將指定索引處的字元替換為提供的字串。

---

從字串中移除所有匹配子字串的實體。

#### --answer--

將符合的字串取代為指定的取代字串。

### --question--

#### --text--

`match` 和 `matchAll` 有什麼差別？

#### --distractors--

`match` 總是傳回字串，而 `matchAll` 總是傳回符合項目的陣列。

---

`match` 傳回一個布林值，表示是否找到匹配，而 `matchAll` 傳回一個數字，表述找到的匹配數量。

---

`match` 傳回一個數字，表示找到的第一個符合項目的起始索引，而 `matchAll` 傳回一個包含找到符合項目索引的數字陣列。

#### --answer--

`match` 傳回符合的字串陣列，而 `matchAll` 傳回所有找到符合項目的迭代器物件。

### --question--

#### --text--

以下哪一項是 `replaceAll` 的正確用法？

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

下列哪一個字元類別等價於正規表達式 `/[a-zA-Z0-9_]/`？

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

下列哪一個字元類別最適合用來從電話號碼中擷取數字？

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

當在正規表達式中使用前瞻 `(?=)` 時會發生什麼？

#### --distractors--

前瞻會在字串的左側斷言，驗證子樣式是否存在。

---

前瞻是一種替代的正規表達式樣式，當主要樣式無法匹配時使用。

---

前瞻會在字串的右側斷言，驗證子樣式不存在。

#### --answer--

前瞻斷言位於字串的右側，驗證子樣式是否存在。

### --question--

#### --text--

在建立正規表達式時，`(?<=)` 的反向先行條件應該放在哪裡？

#### --distractors--

反向先行條件可以放在正規表達式中的任何位置。

---

反向先行應該放在正規表達式主要樣式的右側。

---

後顧應該包含正規表達式的主要樣式。

#### --answer--

前瞻應該放在正規表達式主要樣式的左側。

### --question--

#### --text--

哪一個量詞匹配前一個元素零次或一次？

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

以下哪一個正規表達式只允許介於 0 和 999,999 之間的數字？

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

以下哪一個述語關於訂製字元類別（`[]`）是正確的？

#### --distractors--

這是一組要從比對中移除的字元。

---

它可以定義一組字元來匹配，而無需跳脫任何特殊字元。

---

它以 Unicode 形式表述一組字元。

#### --answer--

它可以定義一組訂製的字元來匹配。

### --question--

#### --text--

以下哪一個字元類別正確匹配大寫字母表？

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

當在正規表達式中使用捕獲群組 `(...)` 時會發生什麼？

#### --distractors--

捕獲群組會立即被評估，無論它位於正規表達式的何處。

---

捕獲群組嘗試使用給定的子樣式進行匹配，並繼續而不記憶結果。

---

捕獲群組是一個優先的子樣式，當找到子樣式的匹配時會立即傳回。

#### --answer--

捕獲群組嘗試使用給定的子樣式進行比對，並將結果保存在記憶體中繼續。

### --question--

#### --text--

當在正規表達式中使用非捕獲群組 `(?:...)` 時會發生什麼？

#### --distractors--

非捕獲群組嘗試使用給定的子樣式進行匹配，並將結果繼續保存在記憶體中。

---

非捕獲群組在找到給定子樣式的匹配時會立即終止。

---

非捕獲群組被視為選用的比對，因此只有成功的比對才會被加入結果中。

#### --answer--

非捕獲群組嘗試使用給定的子樣式進行比對，並繼續而不將結果保存在記憶體中。

### --question--

#### --text--

給定以下的正規表達式，哪一個測試會傳回 `true`？

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

萬用字元類別（`.`）在正規表達式中有什麼作用？

#### --distractors--

它只匹配空白字元。

---

它符合字串的開頭。

---

它匹配單一數字。

#### --answer--

它匹配除換行字元以外的任何單一字元。
