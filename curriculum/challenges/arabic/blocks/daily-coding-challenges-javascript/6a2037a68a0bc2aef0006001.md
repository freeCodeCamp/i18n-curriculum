---
id: 6a2037a68a0bc2aef0006001
title: "تحدٍّ 339: تقسيم المصفوفة إلى أجزاء"
challengeType: 28
dashedName: challenge-339
---

# --description--

أعطِ مصفوفة وحجم جزء، أرجع المصفوفة مقسمة إلى مصفوفات فرعية بهذا الحجم.

- قد يكون الجزء الأخير أصغر إذا لم تنقسم المصفوفة بالتساوي.

# --hints--

`chunkArray([1, 2, 3, 4, 5, 6], 3)` يجب أن تُرجع `[[1, 2, 3], [4, 5, 6]]`.

```js
assert.deepEqual(chunkArray([1, 2, 3, 4, 5, 6], 3), [[1, 2, 3], [4, 5, 6]]);
```

`chunkArray([1, "two", 3, "four", 5, "six", 7, "eight"], 2)` يجب أن تُرجع `[[1, "two"], [3, "four"], [5, "six"], [7, "eight"]]`.

```js
assert.deepEqual(chunkArray([1, "two", 3, "four", 5, "six", 7, "eight"], 2), [[1, "two"], [3, "four"], [5, "six"], [7, "eight"]]);
```

`chunkArray([1, 2, 3, 4, 5], 3)` يجب أن تُرجع `[[1, 2, 3], [4, 5]]`.

```js
assert.deepEqual(chunkArray([1, 2, 3, 4, 5], 3), [[1, 2, 3], [4, 5]]);
```

`chunkArray(["a", "b", "c", "d", "e"], 1)` يجب أن تُرجع `[["a"], ["b"], ["c"], ["d"], ["e"]]`.

```js
assert.deepEqual(chunkArray(["a", "b", "c", "d", "e"], 1), [["a"], ["b"], ["c"], ["d"], ["e"]]);
```

`chunkArray([1, 2, 3], 5)` يجب أن تُرجع `[[1, 2, 3]]`.

```js
assert.deepEqual(chunkArray([1, 2, 3], 5), [[1, 2, 3]]);
```

# --seed--

## --seed-contents--

```js
function chunkArray(arr, size) {

  return arr;
}
```

# --solutions--

```js
function chunkArray(arr, size) {
  const result = [];
  for (let i = 0; i < arr.length; i += size) {
    result.push(arr.slice(i, i + size));
  }
  return result;
}
```
