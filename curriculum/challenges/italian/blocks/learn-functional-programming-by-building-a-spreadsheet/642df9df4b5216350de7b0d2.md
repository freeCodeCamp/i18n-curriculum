---
id: 642df9df4b5216350de7b0d2
title: Passaggio 6
challengeType: 0
dashedName: step-6
---

# --description--

Ti servirà una funzione per generare un intervallo di numeri.

Dichiara una funzione `range` vuota che prende un parametro `start` e un parametro `end`. Usa il costruttore `Array()` e restituisci implicitamente un array vuoto.

# --hints--

Dovresti dichiarare una variabile `range`.

```js
assert.match(code, /(?:let|var|const)\s+range/);
```

Dovresti usare `const` per dichiarare la tua variabile `range`.

```js
assert.match(code, /const\s+range/);
```

La tua variabile `range` dovrebbe essere una funzione.

```js
assert.isFunction(range);
```

La tua funzione `range` dovrebbe usare la sintassi arrow.

```js
assert.match(code, /const\s+range\s*=\s*\(.*\)\s*=>/);
```

La tua funzione `range` dovrebbe prendere prima un parametro `start`.

```js
assert.match(code, /const\s+range\s*=\s*\(\s*start/)
```

La tua funzione `range` dovrebbe prendere un parametro `end` come secondo.

```js
assert.match(code, /const\s+range\s*=\s*\(\s*start\s*,\s*end\s*\)/);
```

La tua funzione `range` dovrebbe usare un ritorno implicito. Ricorda che questo significa che non userai parentesi graffe.

```js
assert.notMatch(code, /const\s+range\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*{/);
```

La tua funzione `range` dovrebbe usare il costruttore `Array()`. I costruttori primitivi non hanno bisogno della parola chiave `new`.

```js
assert.match(code, /const\s+range\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*Array\(/);
```

Non dovresti passare nulla al costruttore `Array()`.

```js
assert.match(code, /const\s+range\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*Array\(\s*\)/);
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

```css
#container {
  display: grid;
  grid-template-columns: 50px repeat(10, 200px);
  grid-template-rows: repeat(11, 30px);
}

.label {
  background-color: lightgray;
  text-align: center;
  vertical-align: middle;
  line-height: 30px;
}
```

```js
--fcc-editable-region--

--fcc-editable-region--

window.onload = () => {
  const container = document.getElementById("container");
  const createLabel = (name) => {
    const label = document.createElement("div");
    label.className = "label";
    label.textContent = name;
    container.appendChild(label);
  }
}
```
