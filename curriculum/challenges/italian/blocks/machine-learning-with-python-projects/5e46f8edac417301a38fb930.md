---
id: 5e46f8edac417301a38fb930
title: Calcolatore dei costi sanitari con regressione lineare
challengeType: 10
forumTopicId: 462379
dashedName: linear-regression-health-costs-calculator
---

# --description--

Lavorerai su questo progetto con <a href="https://colab.research.google.com/github/freeCodeCamp/boilerplate-linear-regression-health-costs-calculator/blob/master/fcc_predict_health_costs_with_regression.ipynb" target="_blank" rel="noopener noreferrer nofollow">Google Colaboratory</a>.

Dopo aver aperto quel collegamento, crea una copia del notebook nel tuo account o localmente. Una volta completato il progetto e superati i test (inclusi in quel collegamento), invia qui sotto il link al tuo progetto. Se invii un link di Google Colaboratory, assicurati di attivare la condivisione del link per "chiunque abbia il link".

Stiamo ancora sviluppando i contenuti didattici interattivi per il curriculum di machine learning. Per ora, puoi seguire le sfide video di questa certificazione. Potrebbe anche servirti cercare risorse di apprendimento aggiuntive, come faresti lavorando su un progetto reale.

# --instructions--

In questa sfida prevedrai i costi sanitari usando un algoritmo di regressione.

Hai a disposizione un dataset che contiene informazioni su diverse persone, inclusi i loro costi sanitari. Usa i dati per prevedere i costi sanitari basandoti su nuovi dati.

Le prime due celle di questo notebook importano le librerie e i dati.

Assicurati di convertire i dati categorici in numeri. Usa l'80% dei dati come `train_dataset` e il 20% dei dati come `test_dataset`.

`pop` la colonna "expenses" da questi dataset per creare nuovi dataset chiamati `train_labels` e `test_labels`. Usa queste etichette per addestrare il modello.

Crea un modello e addestralo con `train_dataset`. Esegui l'ultima cella di questo notebook per verificare il modello. L'ultima cella userà `test_dataset` mai visto prima per controllare quanto bene il modello generalizza.

Per superare la sfida, `model.evaluate` deve restituire un errore assoluto medio inferiore a 3500. Questo significa che prevede correttamente i costi sanitari entro 3500 dollari.

L'ultima cella prevede anche le spese usando `test_dataset` e mostra un grafico dei risultati.

# --hints--

Dovrebbe superare tutti i test Python.

```js

```

# --solutions--

```py
  # Python challenges don't need solutions,
  # because they would need to be tested against a full working project.
  # Please check our contributing guidelines to learn more.
```
