---
id: 66ed902df45ce3ece4053ebc
title: HTML-Tabellen- und Formular-Quiz
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welches der folgenden Attribute wird verwendet, um die URL anzugeben, an die die Formulardaten gesendet werden sollen?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

Welche zwei Elemente werden verwendet, um eine Zeile und eine Zeilenüberschrift in einer Tabelle anzugeben?

#### --distractors--

Das `r`-Element wird für Zeilen verwendet und das `thead`-Element für die Kopfzeile.

---

Das `row`-Element wird für Zeilen verwendet und das `th`-Element für die Kopfzeile.

---

Das `tr`-Element wird für Zeilen verwendet und das `head`-Element für die Kopfzeile.

#### --answer--

Das `tr`-Element wird für Zeilen verwendet und das `th`-Element für die Kopfzeile.

### --question--

#### --text--

Wofür wird das `td`-Element verwendet?

#### --distractors--

Es wird verwendet, um einen Tabellendatentyp zu definieren.

---

Es wird verwendet, um zwei Spalten zusammenzuführen.

---

Es wird verwendet, um zwei Zeilen zusammenzuführen.

#### --answer--

Es wird verwendet, um eine Tabellenzelle zu definieren.

### --question--

#### --text--

Was ist die Funktion des `colspan`-Attributs?

#### --distractors--

Es definiert die Länge einer Spalte in einer Tabelle.

---

Es entfernt den Zeilenumbruch zwischen Text in einer Spalte.

---

Es wird verwendet, um die Anzahl der Spalten zu definieren.

#### --answer--

Es definiert die Anzahl der Spalten, die eine Tabellenzelle überspannen soll.

### --question--

#### --text--

Welche Rolle hat das `placeholder`-Attribut?

#### --distractors--

Es wird verwendet, um Daten an den Server zu senden, wenn das Formular abgesendet wird.

---

Es wird verwendet, um ein `label`-Element mit einem `input`-Element zu verknüpfen.

---

Es wird verwendet, um anzugeben, dass ein Eingabefeld für den Benutzer schreibgeschützt ist.

#### --answer--

Es wird verwendet, um einen Hinweis für ein Eingabefeld bereitzustellen.

### --question--

#### --text--

Wozu dient das `thead`-Element?

#### --distractors--

Es wird verwendet, um den Körperinhalt in einer HTML-Tabelle zu gruppieren.

---

Es wird verwendet, um die Beschreibung einer HTML-Tabelle zu gruppieren.

---

Es wird verwendet, um den Fußzeileninhalt in einer HTML-Tabelle zu gruppieren.

#### --answer--

Es wird verwendet, um den Kopfzeileninhalt in einer HTML-Tabelle zu gruppieren.

### --question--

#### --text--

Welches der folgenden Beispiele zeigt eine explizite Formularzuordnung zwischen Bezeichnungen und Eingaben?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

Wozu wird ein HTML-Validator verwendet?

#### --distractors--

Ein Validator ist ein Werkzeug, das Ihren HTML-Code automatisch formatiert.

---

Ein Validator ist ein Werkzeug, das Ihrem HTML Stil verleiht.

---

Ein Validator ist ein Werkzeug, das Ihren HTML-Code schneller ausführt.

#### --answer--

Ein Validator ist ein Werkzeug, das die Syntax von HTML-Code überprüft, um sicherzustellen, dass er gültig ist.

### --question--

#### --text--

Welches Element wird verwendet, um eine Zelle in einer Tabelle zu definieren?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

Welches der folgenden Werkzeuge ermöglicht es Ihnen, die HTML-Struktur einer Webseite zu inspizieren und zu ändern?

#### --distractors--

DOM-Entfernung.

---

DOM-Tester.

---

DOM-Validierung.

#### --answer--

DOM-Inspektor.

### --question--

#### --text--

Welches der folgenden ist die korrekte Methode, um zusammengehörige Eingabefelder zu gruppieren?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

Welches der folgenden wird verwendet, um anzugeben, dass ein Eingabefeld schreibgeschützt ist?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

Welches Attribut gibt an, dass ein Eingabefeld ausgefüllt werden muss, bevor das Formular abgesendet wird?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

Wozu werden Entwicklerwerkzeuge (Devtools) verwendet?

#### --distractors--

Diese Werkzeuge sind direkt im Browser eingebaut und werden verwendet, um Linting-Probleme in Ihrem Code zu erkennen.

---

Diese Werkzeuge sind direkt im Browser eingebaut und werden verwendet, um Ihren Code automatisch zu formatieren.

---

Diese Werkzeuge sind direkt im Browser eingebaut und helfen Ihnen, eine 100%ige Testabdeckung für Ihren Code sicherzustellen.

#### --answer--

Diese Werkzeuge sind direkt im Browser eingebaut und helfen Ihnen, Webseiten zu debuggen, zu profilieren und zu analysieren.

### --question--

#### --text--

Welches der folgenden ist die korrekte Methode, um eine Eingabe zu deaktivieren?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

Welches der folgenden ist ein gültiger Wert für das `type`-Attribut?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

Welches der folgenden ist die korrekte Verwendung des `size`-Attributs?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

Welche der folgenden Attribute werden verwendet, um die minimale und maximale Anzahl von Zeichen anzugeben, die in einem Eingabefeld erforderlich sind?

#### --distractors--

`minlen` und `maxlen`

---

`min` und `max`

---

`minimumLen` und `maximumLen`

#### --answer--

`minlength` und `maxlength`

### --question--

#### --text--

Welches der folgenden Button-Beispiele verwendet **NICHT** einen korrekten Wert für das `type`-Attribut?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

Welches der folgenden Attribute wird verwendet, um den Wert für einen Button anzugeben?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

Welche Rolle hat das `action`-Attribut innerhalb des öffnenden `form`-Tags?

#### --distractors--

Es wird verwendet, um anzugeben, dass ein Eingabefeld schreibgeschützt ist.

---

Es wird verwendet, um die Anzahl der Zeichen zu definieren, die sichtbar sein sollen, während der Benutzer in das Eingabefeld tippt.

---

Es wird verwendet, um dem Benutzer einen Hinweis zu geben, was er in das Eingabefeld eingeben soll.

#### --answer--

Es wird verwendet, um die URL anzugeben, an die die Formulardaten gesendet werden sollen.

### --question--

#### --text--

Welches der folgenden Attribute wird verwendet, um die HTTP-Methode anzugeben, die beim Senden der Formulardaten verwendet werden soll?

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

Welche der folgenden Methoden wird häufig bei der Arbeit mit Formularen verwendet?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

Welcher der folgenden Werte ist KEIN gültiger Wert für das `type`-Attribut?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

Wie erstellt man korrekt einen Button in einem Formular?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

Was ist eine implizite Formularzuordnung?

#### --distractors--

Dabei können Eingaben mit Bezeichnungen verknüpft werden, indem die `action`- und `for`-Attribute im `label`-Element verwendet werden.

---

Dabei können Eingaben mit Bezeichnungen verknüpft werden, indem die `required`- und `for`-Attribute im `label`-Element verwendet werden.

---

Dabei können Eingaben mit Bezeichnungen verknüpft werden, indem das `for`-Attribut im `label`-Element verwendet wird.

#### --answer--

Dabei können Eingabefelder mit Bezeichnungen verknüpft werden, indem das Eingabefeld innerhalb des `label`-Elements umschlossen wird.

### --question--

#### --text--

Welche Rolle hat das `fieldset`-Element?

#### --distractors--

Es wird verwendet, um den Kopfzeileninhalt in einer HTML-Tabelle zu gruppieren.

---

Es wird verwendet, um eine Bezeichnung für ein Eingabefeld zu erstellen.

---

Es wird verwendet, um eine Bildunterschrift hinzuzufügen, die die Gruppe von Eingaben beschreibt.

#### --answer--

Es wird verwendet, um zusammengehörige Eingaben zu gruppieren.

### --question--

#### --text--

Welches der folgenden wird verwendet, um ein Kontrollkästchen in einem Formular zu erstellen?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um einen Titel für eine HTML-Tabelle hinzuzufügen?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

Welche Rolle hat das `legend`-Element?

#### --distractors--

Es wird verwendet, um den Körperinhalt in einer HTML-Tabelle zu gruppieren.

---

Es wird verwendet, um eine Bildunterschrift hinzuzufügen, die die Zellen einer Tabelle beschreibt.

---

Es wird verwendet, um zusammengehörige Eingaben zu gruppieren.

#### --answer--

Es wird verwendet, um eine Bildunterschrift hinzuzufügen, die die Gruppe von Eingaben beschreibt.

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um den Kopfzeileninhalt in einer HTML-Tabelle zu gruppieren?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

Wie fügt man korrekt Zeilen und Datenzellen zu einer Tabelle hinzu?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

Was ist der DOM-Inspektor?

#### --distractors--

Ein Werkzeug, das verwendet wird, um alle Elemente aus dem DOM zu entfernen.

---

Ein Satz von Werkzeugen, die direkt im Browser eingebaut sind und Ihnen helfen, Webseiten zu debuggen, zu profilieren und zu analysieren.

---

Ein Werkzeug, das die Syntax von HTML-Code überprüft, um sicherzustellen, dass er gültig ist.

#### --answer--

Ein Werkzeug, das es Ihnen ermöglicht, die HTML-Struktur einer Webseite zu inspizieren und zu ändern.

### --question--

#### --text--

Welches der folgenden Werkzeuge überprüft die Syntax von HTML-Code, um sicherzustellen, dass er gültig ist?

#### --distractors--

HTML validate

---

HTML isValid

---

HTML valid

#### --answer--

HTML validator

### --question--

#### --text--

Wie gibt man korrekt die Anzahl der Spalten für eine Datenzelle in einer Tabelle an?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

Was ist ein fokussierter Zustand für ein Eingabefeld?

#### --distractors--

Das ist der Zustand eines Eingabefelds, wenn der Fokus vom Benutzer entfernt wird.

---

Das ist der Zustand eines Eingabefelds, wenn es vom Benutzer auf readonly gesetzt wird.

---

Das ist der Zustand eines Eingabefelds, wenn es vom Benutzer deaktiviert wird.

#### --answer--

Das ist der Zustand eines Eingabefelds, wenn es vom Benutzer ausgewählt wird.

### --question--

#### --text--

Wie erstellt man korrekt eine Optionsschaltfläche?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

Welche Rolle hat das `for`-Attribut?

#### --distractors--

Es wird verwendet, um dem Benutzer einen Hinweis zu geben.

---

Es wird verwendet, um den Wert der Eingabe anzugeben.

---

Es wird verwendet, um eine Bezeichnung für ein Eingabefeld zu erstellen.

#### --answer--

Es wird verwendet, um anzugeben, für welches Eingabefeld die Bezeichnung gilt.

### --question--

#### --text--

Welches der folgenden Attribute wird verwendet, um die Anzahl der Zeichen anzugeben, die sichtbar sein sollen, während der Benutzer in das Eingabefeld tippt?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

Welches der folgenden Attribute wird verwendet, um dem Benutzer einen Hinweis zu geben, was er in das Eingabefeld eingeben soll?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
