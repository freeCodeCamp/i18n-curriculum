---
id: 6732719e2e3ad4d947410b65
title: Wie funktioniert die Operatorpriorität?
challengeType: 19
dashedName: how-does-operator-precedence-work
---

# --interactive--

Wenn Sie in JavaScript einen Ausdruck mit mehreren Operatoren schreiben, wie entscheidet JavaScript dann, welcher zuerst ausgewertet wird? Hier kommt die Operatorpriorität ins Spiel. Lassen Sie uns die Operatorpriorität mit Codebeispielen genauer ansehen und auch, was passiert, wenn Operatoren dieselbe Priorität haben.

Die Operatorpriorität bestimmt die Reihenfolge, in der Operationen in einem Ausdruck ausgewertet werden. Operatoren mit höherer Priorität werden vor denen mit niedrigerer Priorität ausgewertet. Denken Sie an die Operatorpriorität wie in der Mathematik, wo Division und Multiplikation vor Addition und Subtraktion stattfinden.

Ohne diese Regel würden einfache Gleichungen falsche Ergebnisse liefern. JavaScript funktioniert genauso. Es hat eigene Regeln, welche Operatoren zuerst, als zweites und so weiter kommen. Schauen Sie sich zum Beispiel den folgenden Ausdruck an:

:::interactive_editor

```js
const result = 2 + 3 * 4;

console.log(result); // 14
```

:::

Wenn JavaScript diesen Ausdruck von links nach rechts auswerten würde, würden Sie `2 + 3 = 5` erwarten, dann `5 * 4 = 20`. Aber da Multiplikation eine höhere Priorität als Addition hat, wertet JavaScript zuerst den `3 * 4`-Teil aus, was zu `2 + 12 = 14` führt.

Manchmal möchten Sie, dass bestimmte Teile Ihres Ausdrucks zuerst ausgeführt werden, unabhängig von den Prioritätsregeln. Dafür können Sie runde Klammern, `()`, verwenden. Alles, was in runden Klammern steht, wird zuerst ausgewertet, egal was. Lassen Sie uns den `2 + 3`-Teil des vorherigen Beispiels zuerst auswerten:

:::interactive_editor

```js
const result = (2 + 3) * 4;

console.log(result); // 20
```

:::

Im obigen Beispiel zwingen die runden Klammern JavaScript dazu, zuerst `2 + 3` auszuwerten und dann das Ergebnis mit `4` zu multiplizieren. Das ergibt `20` statt `14`.

Der Divisionsoperator hat ebenfalls eine höhere Priorität als Addition oder Subtraktion:

:::interactive_editor

```js
const result = 2 + 6 / 3;

console.log(result); // 4
```

:::

Wenn JavaScript diesen Ausdruck von links nach rechts auswerten würde, könnten Sie `2 + 6 = 8` erwarten, dann `8 / 3 = 2.67`. Aber da Division eine höhere Priorität als Addition hat, wertet JavaScript zuerst die Division aus: `6 / 3 = 2`, und addiert dann `2 + 2`, was das Ergebnis `4` ergibt.

Sowohl bei Multiplikation als auch bei Division finden diese Operationen also immer vor Addition und Subtraktion statt, es sei denn, Sie verwenden runde Klammern, um die Reihenfolge zu ändern. Aber was passiert, wenn die Operatoren dieselbe Priorität haben? JavaScript verwendet die Assoziativität, um die Reihenfolge der Auswertung zu bestimmen.

Die Assoziativität sagt JavaScript, ob Operatoren von links nach rechts oder von rechts nach links ausgewertet werden. Für die meisten Operatoren wie Addition und Multiplikation ist die Assoziativität von links nach rechts. JavaScript verarbeitet diese also von der linken Seite des Ausdrucks zur rechten:

:::interactive_editor

```js
const result = 10 - 2 + 3;

console.log(result); // 11
```

:::

Zuerst `10 - 2 = 8`, dann `8 + 3 = 11`. JavaScript geht hier von links nach rechts vor. Einige Operatoren, wie die Zuweisung (`=`), sind von rechts nach links assoziativ. Das bedeutet, dass die rechte Seite des Ausdrucks zuerst ausgewertet wird:

:::interactive_editor

```js
let a, b;
a = b = 5;

console.log(a); // 5
console.log(b); // 5
console.log(a + b); // 10
```

:::

Im obigen Beispiel weist JavaScript zuerst `5` `b` zu, dann weist es `b` (das jetzt `5` ist) `a` zu.

Der Exponentenoperator ist ebenfalls von rechts nach links assoziativ:

:::interactive_editor

```js
const result = 2 ** 3 ** 2;

console.log(result); // 512
```

:::

Zuerst wertet JavaScript `3 ** 2` aus, was `9` ergibt, dann wertet es `2 ** 9` aus, was `512` ergibt. Wenn der Exponentenoperator von links nach rechts assoziativ wäre, hätte JavaScript zuerst `2 ** 3` berechnet, um `8` zu erhalten, und dann `8 ** 2`, um `64` zu erhalten.

# --questions--

## --text--

Wie unterscheidet sich die Operatorpriorität in JavaScript von der in der Mathematik?

## --answers--

Addition findet vor Multiplikation und Division statt.

### --feedback--

Denken Sie an die Reihenfolge der Operationen, die Sie bei mathematischen Gleichungen befolgen.

---

Division und Multiplikation finden vor Addition und Subtraktion statt, genau wie in der Mathematik.

---

Alle Operationen werden in der Reihenfolge ausgeführt, in der sie erscheinen.

### --feedback--

Denken Sie an die Reihenfolge der Operationen, die Sie bei mathematischen Gleichungen befolgen.

---

Subtraktion hat Vorrang vor allen anderen Operationen.

### --feedback--

Denken Sie an die Reihenfolge der Operationen, die Sie bei mathematischen Gleichungen befolgen.

## --video-solution--

2

## --text--

Wie können Sie die Operatorpriorität in JavaScript überschreiben?

## --answers--

Indem Sie den Additionsoperator (`+`) verwenden, um die Reihenfolge der Operationen zu ändern.

### --feedback--

Denken Sie daran, wie Sie die Reihenfolge der Operationen bei mathematischen Gleichungen ändern.

---

Indem Sie runde Klammern verwenden, um bestimmte Teile des Ausdrucks zuerst auswerten zu lassen.

---

Indem Sie die Operatoren im Ausdruck umkehren.

### --feedback--

Denken Sie daran, wie Sie die Reihenfolge der Operationen bei mathematischen Gleichungen ändern.

---

Indem Sie alle Operationen von links nach rechts ausführen, unabhängig von der Priorität.

### --feedback--

Denken Sie daran, wie Sie die Reihenfolge der Operationen bei mathematischen Gleichungen ändern.

## --video-solution--

2

## --text--

Wie wertet JavaScript Ausdrücke mit dem Exponentenoperator (`**`) aus?

## --answers--

Von links nach rechts.

### --feedback--

Denken Sie an die Reihenfolge, in der die Potenzierung im Ausdruck erfolgt.

---

Von rechts nach links, das heißt, es wird zuerst die Potenzierung ganz rechts ausgewertet.

---

Indem die Basis so oft mit sich selbst multipliziert wird, wie angegeben.

### --feedback--

Denken Sie an die Reihenfolge, in der die Potenzierung im Ausdruck erfolgt.

---

Indem zuerst die Exponenten addiert werden und dann das Ergebnis berechnet wird.

### --feedback--

Denken Sie an die Reihenfolge, in der die Potenzierung im Ausdruck erfolgt.

## --video-solution--

2
