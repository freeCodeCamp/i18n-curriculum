---
id: 6874b5fc38f90f25e18093ce
title: 什麼是方法和屬性，以及它們如何運作？
challengeType: 19
dashedName: what-are-methods-and-attributes-and-how-do-they-work
---

# --description--

在上一課中，你學習了類別以及它們如何作為建立物件的藍圖。

在這裡，我們將更深入探討屬性和方法。

我們先仔細看看屬性，然後是方法。

屬性是屬於物件的變數，因此它們保存資料。屬性有兩種：實體屬性和類別屬性。

實體屬性是每個由類別創建的物件所獨有的，通常你會使用 `__init__` 方法來設定它們。另一方面，類別屬性屬於類別本身，並且由該類別的所有實體共享。

要存取一個屬性，你使用點記法。

以下是實體屬性和類別屬性的範例，以及如何從物件中存取它們：

```python
class Dog:
    species = "French Bulldog" # Class attribute

    def __init__(self, name):
        self.name = name # Instance attribute

print(Dog.species) # French Bulldog

dog1 = Dog("Jack")
print(dog1.name)    # Jack
print(dog1.species) # French Bulldog

dog2 = Dog("Tom")
print(dog2.name)    # Tom
print(dog2.species) # French Bulldog
```

請注意，你可以直接從類別本身存取類別屬性，但你需要先創建一個物件並傳遞資料給它，才能存取實體屬性。

汽車是另一個很好的例子，因為所有汽車都有型號和顏色：

```python
class Car:
    def __init__(self, color, model):
        self.color = color
        self.model = model

car_1 = Car("red", "Toyota Corolla")
car_2 = Car("green", "Lamborghini Revuelto")

print(car_1.model) # Toyota Corolla
print(car_2.model) # Lamborghini Revuelto

print(car_1.color) # red
print(car_2.color) # green
```

方法是定義在類別內的函式。透過它們，任何從類別定義的物件都可以執行作用於或修改其自身資料的動作。你也可以使用點記法來存取方法。

例如，狗會叫。因此，我們可以在 `Dog` 類別中有一個 `bark` 方法，就像你在之前的課程中看到的那樣：

```python
class Dog:
   species = "French Bulldog"

   def __init__(self, name):
     self.name = name

   def bark(self):
       return f"{self.name} says woof woof!"

jack = Dog("Jack")
jill = Dog("Jill")

print(jack.bark()) # Jack says woof woof!
print(jill.bark()) # Jill says woof woof!
```

`Car` 類別也可以有一個 `describe` 方法：

```python
class Car:
    def __init__(self, color, model):
        self.color = color  # Instance attribute
        self.model = model  # Instance attribute

    def describe(self):
        return f"This car is a {self.color} {self.model}"

car_1 = Car("red", "Toyota Corolla")
car_2 = Car("green", "Lamborghini Revuelto")

print(car_1.describe()) # This car is a red Toyota Corolla
print(car_2.describe()) # This car is a green Lamborghini Revuelto
```

# --questions--

## --text--

Python 中有哪兩種屬性型別？

## --answers--

公開和私有屬性

### --feedback--

思考你可以定義屬性的兩種方式。

---

區域的和全域的屬性

### --feedback--

思考你可以定義屬性的兩種方式。

---

實體屬性和類別屬性

---

可變的和不可變的屬性

### --feedback--

思考你可以定義屬性的兩種方式。

## --video-solution--

3

## --text--

存取實體屬性需要什麼？

## --answers--

類別名稱而已

### --feedback--

思考你如何存取類別屬性與如何存取實體屬性。

---

裝飾器

### --feedback--

思考你如何存取類別屬性與如何存取實體屬性。

---

類別的實體或物件

---

靜態方法

### --feedback--

思考你如何存取類別屬性與如何存取實體屬性。

## --video-solution--

3

## --text--

你如何定義和存取方法？

## --answers--

作為類別外的獨立函式（程式），以括號存取

### --feedback--

它們屬於該類別，但操作物件資料。

---

作為類別中的變數，像屬性一樣被存取

### --feedback--

它們屬於該類別，但操作物件資料。

---

使用特殊關鍵字，自動呼叫

### --feedback--

它們屬於該類別，但操作物件資料。

---

它們定義在類別內，並以點記法存取。

## --video-solution--

4
