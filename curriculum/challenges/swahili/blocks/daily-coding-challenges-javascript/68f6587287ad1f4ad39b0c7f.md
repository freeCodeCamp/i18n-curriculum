---
id: 68f6587287ad1f4ad39b0c7f
title: "Zoezi la 93: Vowels na Consonants"
challengeType: 28
dashedName: challenge-93
---

# --description--

Ukipata mfuatano wa herufi, rudisha safu ya data yenye idadi ya vowels na idadi ya consonants katika mfuatano huo.

- Vowels ni `a`, `e`, `i`, `o`, `u` katika hali yoyote.
- Consonants ni herufi zote nyingine katika hali yoyote.
- Puuza herufi zisizo za alfabeti.

Kwa mfano, ukipata `"Hello World"`, rudisha `[3, 7]`.

# --hints--

`count("Hello World")` inapaswa kurudisha `[3, 7]`.

```js
assert.deepEqual(count("Hello World"), [3, 7]);
```

`count("JavaScript")` inapaswa kurudisha `[3, 7]`.

```js
assert.deepEqual(count("JavaScript"), [3, 7]);
```

`count("Python")` inapaswa kurudisha `[1, 5]`.

```js
assert.deepEqual(count("Python"), [1, 5]);
```

`count("freeCodeCamp")` inapaswa kurudisha `[5, 7]`.

```js
assert.deepEqual(count("freeCodeCamp"), [5, 7]);
```

`count("Hello, World!")` inapaswa kurudisha `[3, 7]`.

```js
assert.deepEqual(count("Hello, World!"), [3, 7]);
```

`count("The quick brown fox jumps over the lazy dog.")` inapaswa kurudisha `[11, 24]`.

```js
assert.deepEqual(count("The quick brown fox jumps over the lazy dog."), [11, 24]);
```

# --seed--

## --seed-contents--

```js
function count(str) {

  return str;
}
```

# --solutions--

```js
function count(str) {
  const vowels = 'aeiou';
  const consonants = 'bcdfghjklmnpqrstvwxyz';
  let v = 0, c = 0;

  for (let i=0; i<str.length; i++) {
    if (vowels.includes(str[i].toLowerCase())) {
      v++;
    }
    if (consonants.includes(str[i].toLowerCase())) {
      c++;
    }
  }

  return [v, c];
}
```
