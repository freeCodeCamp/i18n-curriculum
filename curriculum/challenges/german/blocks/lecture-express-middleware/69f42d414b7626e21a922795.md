---
id: 69f42d414b7626e21a922795
title: Was sind Beispiele für eingebaute und Drittanbieter-Middleware?
challengeType: 19
dashedName: what-are-examples-of-built-in-and-3rd-party-middleware
---

# --description--

Express stellt eine Reihe von eingebauten Middleware-Funktionen bereit, die bei häufigen Aufgaben helfen, wie dem Bereitstellen statischer Dateien, dem Parsen von JSON-Anfrageinhalten und mehr. Sie können auch Drittanbieter-Middleware verwenden, um die Funktionalität zu erweitern.

**Hier sind einige Beispiele für eingebaute Middleware**:

1. `express.json()`: Eine eingebaute Middleware, die eingehende JSON-Nutzlasten parst.

```js
app.use(express.json())
```

1. `express.static()`: Eine Middleware zum Bereitstellen statischer Dateien wie Bilder, CSS- und JavaScript-Dateien.

```js
app.use(express.static('public'))
```

1. `express.urlencoded()`: Eine Middleware, die eingehende Anfragen mit URL-kodierten Nutzlasten parst, das Format, das typischerweise von HTML-Formularen gesendet wird. Sie macht die geparsten Daten auf `req.body` verfügbar.

```js
app.use(express.urlencoded({ extended: true }))
```

**Nun, hier sind einige Beispiele für Drittanbieter-Middleware:**

1. `cors`: Eine beliebte Drittanbieter-Middleware, um Cross-Origin Resource Sharing zu ermöglichen.

```bash
npm install cors
```

```js
const cors = require('cors')
app.use(cors())
```

1. `morgan`: Eine Logging-Middleware für HTTP-Anfragen.

```bash
npm install morgan
```

```js
const morgan = require('morgan')
app.use(morgan('tiny'))
```

Schauen wir uns nun ein Beispiel an, in dem sowohl eingebaute als auch Drittanbieter-Middleware verwendet werden:

```js
const express = require('express')
const cors = require('cors')
const morgan = require('morgan')
const app = express()

app.use(express.json()) // Built-in middleware to parse JSON
app.use(cors())       // Third-party middleware for CORS
app.use(morgan('tiny')) // Third-party middleware for logging

app.get('/', (req, res) => {
  res.send('Hello, World!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* Die eingebaute Middleware `express.json()` parst eingehende JSON-Nutzlasten.
* Die Drittanbieter-Middleware `cors` erlaubt Cross-Origin-Anfragen, und `morgan` protokolliert eingehende HTTP-Anfragen.

Es ist wichtig zu beachten, dass die Middleware-Funktionen in der Reihenfolge hinzugefügt werden, in der sie mit `app.use()` aufgerufen werden.

Express stellt wichtige eingebaute Middleware für die Behandlung häufiger Aufgaben wie JSON-Parsing und das Bereitstellen statischer Dateien bereit. Sie können die Funktionalität auch mit Drittanbieter-Middleware für zusätzliche Fähigkeiten erweitern.

# --questions--

## --text--

Welche der folgenden Middleware ist eine eingebaute Middleware in Express?

## --answers--

`express.json()`

---

`cors`

### --feedback--

Überlegen Sie, welche Middleware mit Express gebündelt ist, ohne dass eine separate Installation erforderlich ist.

---

`morgan`

### --feedback--

Überlegen Sie, welche Middleware mit Express gebündelt ist, ohne dass eine separate Installation erforderlich ist.

---

`helmet`

### --feedback--

Überlegen Sie, welche Middleware mit Express gebündelt ist, ohne dass eine separate Installation erforderlich ist.

## --video-solution--

1

## --text--

Was macht die Middleware `express.static()`?

## --answers--

Parst eingehende JSON-Daten.

### --feedback--

Achten Sie darauf, was es Ihnen ermöglicht, Dateien wie Bilder, CSS und JavaScript direkt bereitzustellen.

---

Protokolliert HTTP-Anfragen.

### --feedback--

Achten Sie darauf, was es Ihnen ermöglicht, Dateien wie Bilder, CSS und JavaScript direkt bereitzustellen.

---

Stellt statische Dateien bereit.

---

Ermöglicht Cross-Origin-Anfragen.

### --feedback--

Achten Sie darauf, was es Ihnen ermöglicht, Dateien wie Bilder, CSS und JavaScript direkt bereitzustellen.

## --video-solution--

3

## --text--

Welche der folgenden Middleware ist eine Drittanbieter-Middleware?

## --answers--

`express.json()`

### --feedback--

Denken Sie an Middleware, die aus einem externen Paket installiert wird.

---

`express.static()`

### --feedback--

Denken Sie an Middleware, die aus einem externen Paket installiert wird.

---

`cors`

---

`express.Router()`

### --feedback--

Denken Sie an Middleware, die aus einem externen Paket installiert wird.

## --video-solution--

3
