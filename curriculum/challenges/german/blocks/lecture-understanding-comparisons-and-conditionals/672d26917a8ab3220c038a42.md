---
id: 672d26917a8ab3220c038a42
title: Wie funktionieren Vergleiche mit den Datentypen null und undefined?
challengeType: 19
dashedName: how-do-comparisons-work-with-null-and-undefined-data-types
---

# --interactive--

In JavaScript sind `null` und `undefined` zwei unterschiedliche Datentypen, die das Fehlen eines Werts darstellen, sich aber bei Vergleichen unterschiedlich verhalten. Zu verstehen, wie diese Typen in verschiedenen Vergleichsszenarien interagieren, ist entscheidend, um robusten und fehlerfreien Code zu schreiben.

Beginnen wir mit dem `undefined`-Typ. Eine Variable ist `undefined`, wenn sie deklariert wurde, aber noch kein Wert zugewiesen wurde. Es ist der Standardwert von nicht initialisierten Variablen und Funktionsparametern, denen kein Argument übergeben wurde.

Der `null`-Typ hingegen ist ein zugewiesener Wert, der einen bewussten Nicht-Wert darstellt. Er wird oft verwendet, um anzuzeigen, dass eine Variable absichtlich keinen Wert hat.

Beim Vergleich von `null` und `undefined` mit dem Gleichheitsoperator (`==`) führt JavaScript eine Typumwandlung durch. Das bedeutet, dass versucht wird, die Operanden vor dem Vergleich in denselben Typ zu konvertieren. In diesem Fall werden `null` und `undefined` als gleich betrachtet:

:::interactive_editor

```js
console.log(null == undefined); // true
```

:::

Wenn jedoch der strikte Gleichheitsoperator (`===`) verwendet wird, der sowohl Wert als auch Typ überprüft, ohne eine Typumwandlung durchzuführen, sind `null` und `undefined` nicht gleich:

:::interactive_editor

```js
console.log(null === undefined); // false
```

:::

Dieser Unterschied ist wichtig, wenn Sie bedingte Anweisungen schreiben oder Gleichheitsprüfungen in Ihrem Code durchführen. Beim Vergleich von `null` oder `undefined` mit anderen Werten unter Verwendung des Gleichheitsoperators (`==`) kann das Verhalten unerwartet sein. Zum Beispiel:

:::interactive_editor

```js
console.log(null == 0);  // false
console.log(null == ''); // false
console.log(undefined == 0); // false
console.log(undefined == ''); // false
```

:::

Diese Vergleiche ergeben `false`, weil `null` und `undefined` nur zueinander (und zu sich selbst) gleich sind, wenn der Gleichheitsoperator verwendet wird. Das Verhalten von `null` bei anderen Vergleichen ist besonders knifflig:

:::interactive_editor

```js
console.log(null > 0);  // false
console.log(null == 0); // false
console.log(null >= 0); // true
```

:::

`undefined` hingegen wird in numerischen Kontexten immer in `NaN` umgewandelt, was dazu führt, dass alle numerischen Vergleiche mit `undefined` `false` zurückgeben:

:::interactive_editor

```js
console.log(undefined > 0);  // false
console.log(undefined < 0);  // false
console.log(undefined == 0); // false
```

:::

Angesichts dieser Feinheiten wird allgemein empfohlen, beim Vergleichen von Werten den strikten Gleichheitsoperator zu verwenden, insbesondere wenn es um `null` und `undefined` geht. Dieser Ansatz hilft, unerwartete Typumwandlungen zu vermeiden und macht das Verhalten Ihres Codes vorhersehbarer.

Zusammenfassend lässt sich sagen, dass `null` und `undefined` zwar beide verwendet werden, um das Fehlen eines Werts darzustellen, sich aber bei Vergleichen unterschiedlich verhalten. Das Verständnis dieser Unterschiede ist der Schlüssel zum Schreiben klaren und fehlerfreien JavaScript-Codes.

# --questions--

## --text--

Was ist das Ergebnis des Ausdrucks: `null === undefined`?

## --answers--

`true`

### --feedback--

Denken Sie daran, was der strikte Gleichheitsoperator (`===`) überprüft.

---

`false`

---

`undefined`

### --feedback--

Denken Sie daran, was der strikte Gleichheitsoperator (`===`) überprüft.

---

`null`

### --feedback--

Denken Sie daran, was der strikte Gleichheitsoperator (`===`) überprüft.

## --video-solution--

2

## --text--

Was ist in JavaScript das Ergebnis des Vergleichs: `null >= 0`?

## --answers--

`true`

---

`false`

### --feedback--

Beachten Sie, wie sich `null` bei numerischen Vergleichen verhält.

---

`undefined`

### --feedback--

Beachten Sie, wie sich `null` bei numerischen Vergleichen verhält.

---

`Error`

### --feedback--

Beachten Sie, wie sich `null` bei numerischen Vergleichen verhält.

## --video-solution--

1

## --text--

Welche der folgenden Aussagen über `undefined` ist korrekt?

## --answers--

`undefined == null` ergibt `false`.

### --feedback--

Denken Sie daran, wie sich `undefined` bei verschiedenen Arten von Vergleichen verhält.

---

`undefined === null` ergibt `true`.

### --feedback--

Denken Sie daran, wie sich `undefined` bei verschiedenen Arten von Vergleichen verhält.

---

`undefined < 0` ergibt `true`.

### --feedback--

Denken Sie daran, wie sich `undefined` bei verschiedenen Arten von Vergleichen verhält.

---

`undefined == 0` ergibt `false`.

## --video-solution--

4
