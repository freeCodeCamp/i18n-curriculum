---
id: 66df3b712c41c499e9d31e5b
title: Quiz base su HTML
challengeType: 8
dashedName: quiz-basic-html
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Come funziona l'attributo `loop` all'interno dell'elemento `audio`?

#### --distractors--

Permette all'elemento `audio` di sincronizzare la riproduzione con altri elementi multimediali nella pagina web.

---

Regola l'intonazione e il tono dell'audio.

---

Fa sì che l'elemento `audio` si fermi e riavvii la riproduzione in sequenza casuale.

#### --answer--

Fa ripartire l'audio una volta terminato.

### --question--

#### --text--

Cos'è un elemento void in HTML?

#### --distractors--

Un elemento usato per incorporare video sullo schermo.

---

Un elemento per visualizzare liste.

---

Un elemento usato per incorporare suoni nel documento.

#### --answer--

Un elemento senza contenuto o tag di chiusura.

### --question--

#### --text--

Quale dei seguenti è un valore corretto per l'attributo `id`?

#### --distractors--

`id="main@content"`

---

`id="main content"`

---

`id="main~content"`

#### --answer--

`id="main-content"`

### --question--

#### --text--

Come funziona `target="_parent"`?

#### --distractors--

Apre un collegamento in un nuovo contesto di navigazione.

---

Specifica la posizione all'interno del documento collegato dove il browser deve scorrere dopo il clic sul collegamento.

---

Reindirizza l'utente a un sito web diverso specificato nell'attributo `href`.

#### --answer--

Apre un collegamento nel genitore del contesto corrente.

### --question--

#### --text--

Qual è la differenza tra un attributo booleano e un attributo normale?

#### --distractors--

Gli attributi booleani possono essere usati solo con caselle di spunta, mentre gli attributi normali sono usati con i pulsanti di opzione.

---

Gli attributi normali sono sempre richiesti per le immagini, mentre gli attributi booleani sono opzionali.

---

Gli attributi booleani devono sempre avere un valore assegnato, mentre gli attributi normali non necessitano di valori assegnati.

#### --answer--

Un attributo booleano può essere presente o assente, mentre un attributo normale ha sempre un valore specificato.

### --question--

#### --text--

Quale elemento si usa per collegare script al tuo file HTML?

#### --distractors--

`img`

---

`ul`

---

`h2`

#### --answer--

`script`

### --question--

#### --text--

Cos'è un boilerplate HTML?

#### --distractors--

Uno strumento speciale usato dagli sviluppatori web per aggiungere filigrane ai documenti.

---

Un tipo di linguaggio di markup.

---

Un plugin che genera automaticamente testo "lorem ipsum" sulla pagina.

#### --answer--

Un modello che include tutte le informazioni essenziali necessarie per un documento HTML.

### --question--

#### --text--

Quale attributo applica un identificatore univoco a un elemento HTML?

#### --distractors--

`class`

---

`href`

---

`action`

#### --answer--

`id`

### --question--

#### --text--

Cos'è un'entità HTML (riferimento carattere)?

#### --distractors--

Una rappresentazione grafica di un carattere in HTML.

---

Un tag di formattazione speciale usato in HTML.

---

Un'immagine usata per rappresentare un carattere nel design web.

#### --answer--

Un insieme di caratteri usati per rappresentare un carattere riservato in HTML.

### --question--

#### --text--

Qual è lo scopo dell'elemento `audio`?

#### --distractors--

Serve a permettere agli utenti di registrare la propria voce direttamente su una pagina web.

---

Serve a definire lo stile degli elementi `audio` e `video` sulla pagina.

---

Serve a rendere il testo su una pagina web udibile al clic.

#### --answer--

Serve ad aggiungere audio al documento HTML.

### --question--

#### --text--

Quale dei seguenti NON è un esempio di elemento HTML?

#### --distractors--

`img`

---

`h1`

---

`link`

#### --answer--

`byte`

### --question--

#### --text--

Qual è il ruolo dell'attributo `target` all'interno degli elementi àncora?

#### --distractors--

Definisce il colore del collegamento quando l'utente ci passa sopra con il mouse.

---

Specifica la posizione all'interno del documento collegato dove il browser deve scorrere dopo il clic sul collegamento.

---

Determina la dimensione del carattere del testo dell'àncora all'interno dell'elemento `link`.

#### --answer--

Serve a specificare dove si aprirà il documento collegato quando l'utente clicca sul collegamento.

### --question--

#### --text--

Cos'è un percorso assoluto?

#### --distractors--

Un percorso relativo al file corrente.

---

Un percorso che include variabili.

---

Un percorso che include caratteri jolly.

#### --answer--

Un percorso completo a una risorsa, che parte dalla directory radice.

### --question--

#### --text--

Qual è il ruolo dell'elemento `link` in HTML?

#### --distractors--

Serve a creare collegamenti ipertestuali all'interno della stessa pagina web.

---

Serve a definire il layout e la struttura di una pagina web.

---

Serve a incorporare file audio o video in una pagina web.

#### --answer--

Serve a collegare risorse esterne come fogli di stile e icone del sito.

### --question--

#### --text--

Quale dei seguenti è un valore valido per lavorare con le proprietà open graph?

#### --distractors--

`"og:socialMedia"`

---

`"og:mediaOG"`

---

`"og:openGraph"`

#### --answer--

`"og:title"`

### --question--

#### --text--

Quale dei seguenti è un esempio di attributo booleano?

#### --distractors--

`href`

---

`src`

---

`width`

#### --answer--

`checked`

### --question--

#### --text--

Qual è la sintassi corretta per l'elemento `img`?

#### --distractors--

`<img src="image.jpg" alt="Description of the image"></img>`

---

`<img source="image.jpg" desc="Description of the image"></img>`

---

`<img src="image.jpg" desc="Description of the image">`

#### --answer--

`<img src="image.jpg" alt="Description of the image">`

### --question--

#### --text--

A cosa serve l'elemento `iframe`?

#### --distractors--

Serve ad aggiungere sottotitoli alle immagini.

---

Serve ad aggiungere informazioni sul copyright per i media.

---

Serve a creare collegamenti ipertestuali all'interno della stessa pagina web.

#### --answer--

Serve a incorporare un altro documento all'interno del documento HTML corrente.

### --question--

#### --text--

Quale dei seguenti NON è uno stato valido per i collegamenti negli elementi àncora?

#### --distractors--

`hover`

---

`visited`

---

`active`

#### --answer--

`href`

### --question--

#### --text--

Qual è il ruolo dell'elemento `title`?

#### --distractors--

Un elemento che permette agli utenti di modificare il codice sorgente di una pagina web.

---

Definisce il layout e il posizionamento degli elementi all'interno di una pagina web.

---

Serve a incorporare video e contenuti multimediali direttamente in un documento HTML.

#### --answer--

Specifica il titolo di un documento e appare nella scheda o nella finestra del browser.

## --quiz--

### --question--

#### --text--

Quale dei seguenti attributi `target` apre un collegamento nel genitore del contesto corrente?

#### --distractors--

`_blank`

---

`_self`

---

`_unfencedTop`

#### --answer--

`_parent`

### --question--

#### --text--

Quale attributo si usa per impostare la codifica dei caratteri `UTF-8` per un documento HTML?

#### --distractors--

`accept`

---

`capture`

---

`enctype`

#### --answer--

`charset`

### --question--

#### --text--

Qual è il ruolo di HTML sul web?

#### --distractors--

HTML serve per lo stile e il layout.

---

HTML serve per aggiungere interattività alla pagina.

---

HTML è un registro per pacchetti software.

#### --answer--

HTML rappresenta il contenuto e la struttura di una pagina web.

### --question--

#### --text--

Quale licenza per immagini funziona per il pubblico dominio?

#### --distractors--

JPG

---

Creative Commons.

---

BSD

#### --answer--

Creative Commons 0.

### --question--

#### --text--

Come si imposta un attributo booleano su `false`?

#### --distractors--

Impostando il valore dell'attributo su `0`.

---

Usando l'operatore `not` prima del nome dell'attributo.

---

Impostando l'attributo a una stringa vuota (`""`).

#### --answer--

Omettendo l'attributo dall'elemento.

### --question--

#### --text--

A cosa serve l'elemento `script`?

#### --distractors--

Serve a incorporare CSS nel documento HTML o collegare un file CSS esterno.

---

Serve a incorporare codice Pascal nel documento HTML.

---

Serve a incorporare codice C# nel documento HTML.

#### --answer--

Serve a incorporare JavaScript nel documento HTML o collegare un file JavaScript esterno.

### --question--

#### --text--

Quale dei seguenti elementi si usa per impostare il titolo di un documento HTML?

#### --distractors--

`footer`

---

`section`

---

`figcaption`

#### --answer--

`title`

### --question--

#### --text--

A cosa serve tipicamente l'attributo `class`?

#### --distractors--

Serve a incorporare metadati sull'elemento.

---

Serve a definire stili in linea direttamente nel tag HTML.

---

Serve a specificare identificatori univoci per funzioni JavaScript.

#### --answer--

Serve ad applicare un insieme di stili a più elementi.

### --question--

#### --text--

Quale dei seguenti NON è un esempio di entità HTML comunemente usata?

#### --distractors--

`&quot;` (virgolette doppie).

---

`&copy;` (simbolo copyright).

---

`&gt;` (simbolo maggiore di).

#### --answer--

`&div;` (elemento div).

### --question--

#### --text--

Quale elemento si usa per aggiungere audio al documento HTML?

#### --distractors--

`media`

---

`video`

---

`hr`

#### --answer--

`audio`

### --question--

#### --text--

Dove si colloca il contenuto rappresentato nella pagina in un documento HTML?

#### --distractors--

Tra gli elementi `head` e `body`.

---

All'interno dell'elemento `repr`.

---

All'interno dell'elemento `script`.

#### --answer--

All'interno dell'elemento `body`.

### --question--

#### --text--

Quale attributo si usa per far ripartire l'audio una volta terminato?

#### --distractors--

`multiple`

---

`inputmode`

---

`enctype`

#### --answer--

`loop`

### --question--

#### --text--

Qual è la sintassi corretta per un elemento `div`?

#### --distractors--

`<<div>>block container<</div>>`

---

`>>div>>block container>>div>>`

---

`[div]block container[/div]`

#### --answer--

`<div>block container</div>`

### --question--

#### --text--

Come funziona `target="_self"`?

#### --distractors--

Specifica la posizione all'interno del documento collegato dove il browser deve scorrere dopo il clic sul collegamento.

---

Scarica automaticamente il documento collegato sul computer dell'utente.

---

Reindirizza l'utente a un sito web diverso specificato nell'attributo `href`.

#### --answer--

Apre un collegamento nel contesto di navigazione corrente.

### --question--

#### --text--

Cos'è un percorso relativo?

#### --distractors--

Specifica la posizione esatta di un file o directory dalla directory radice del file system.

---

Include l'URL completo a partire dal nome di dominio fino al file o directory specifico.

---

Inizia sempre con una barra (`/`) che indica la directory radice del sito o file system.

#### --answer--

Specifica la posizione di un file o directory relativa alla directory di lavoro corrente.

### --question--

#### --text--

Quale dei seguenti elementi è la sintassi corretta per un elemento `link`?

#### --distractors--

`<link rel="stylesheet" href="./styles.css"></link>`

---

`</link rel="stylesheet" href="./styles.css"></link>`

---

`<<link rel="stylesheet" href="./styles.css"></link>>`

#### --answer--

`<link rel="stylesheet" href="./styles.css" />`

### --question--

#### --text--

Quale dei seguenti NON è un valore valido per lavorare con le proprietà open graph?

#### --distractors--

`"og:title"`

---

`"og:type"`

---

`"og:image"`

#### --answer--

`"og:socialMedia"`

### --question--

#### --text--

Qual è la sintassi corretta per un elemento paragrafo?

#### --distractors--

`>p>paragraph element>/p>`

---

`<<p>>paragraph element<</p>>`

---

`<p>`

#### --answer--

`<p>paragraph element</p>`

### --question--

#### --text--

Cosa indica il nome dell'elemento `iframe`?

#### --distractors--

Inline Framing

---

Inner Frame

---

Inline video

#### --answer--

Inline Frame

### --question--

#### --text--

Cosa significa lo stato attivo per gli elementi àncora?

#### --distractors--

Il collegamento è disabilitato e non può essere cliccato.

---

Il cursore del mouse è sopra il collegamento.

---

Il collegamento è stato visitato dall'utente.

#### --answer--

Un utente sta cliccando attivamente su di esso.

## --quiz--

### --question--

#### --text--

Quale dei seguenti aprirà il collegamento in una nuova scheda del browser?

#### --distractors--

```html
<a target="_parent" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_top" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_self" href="https://freecatphotoapp.com">cat photos</a>
```

#### --answer--

```html
<a target="_blank" href="https://freecatphotoapp.com">cat photos</a>
```

### --question--

#### --text--

Quale dei seguenti è un elemento void?

#### --distractors--

`div`

---

`section`

---

`p`

#### --answer--

`img`

### --question--

#### --text--

Qual è il modo corretto di usare le entità HTML?

#### --distractors--

`<p>This is an &lt&img /&gt& element</p>`

---

`<p>This is an ;lt&img /;gt& element</p>`

---

`<p>This is an ;lt;img /;gt; element</p>`

#### --answer--

`<p>This is an &lt;img /&gt; element</p>`

### --question--

#### --text--

A cosa serve l'elemento `ul` in HTML?

#### --distractors--

Serve a raggruppare solo una lista di immagini.

---

Serve a raggruppare solo una lista di collegamenti.

---

Serve a raggruppare una lista ordinata di elementi.

#### --answer--

Serve a raggruppare una lista puntata di elementi.

### --question--

#### --text--

Qual è il modo corretto di applicare più classi a un elemento HTML?

#### --distractors--

```html
<div class="box>>red>>box"></div>
``` 

---

```html
<div class="box<<red<<box"></div>
``` 

---

```html
<div class="box-red-box"></div>
``` 

#### --answer--

```html
<div class="box red-box"></div>
``` 

### --question--

#### --text--

Quale dei seguenti elementi si usa per rappresentare una lista ordinata di elementi?

#### --distractors--

`span`

---

`div`

---

`ul`

#### --answer--

`ol` 

### --question--

#### --text--

A cosa servono le proprietà open graph?

#### --distractors--

Queste proprietà servono a incorporare contenuti multimediali interattivi direttamente nelle pagine web.

---

Queste proprietà servono a generare pubblicità pop-up dinamiche sui siti web.

---

Queste proprietà servono a criptare dati sensibili trasmessi tra server web e browser degli utenti.

#### --answer--

Queste proprietà servono a impostare come il contenuto di un sito sarà visto su diverse piattaforme social.

### --question--

#### --text--

Quale dei seguenti si usa per impostare una breve descrizione per una pagina web?

#### --distractors--

```html
<meta
  add="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  set="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  description="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

#### --answer--

```html
<meta
  name="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

### --question--

#### --text--

Cosa significa SVG?

#### --distractors--

Scalable Vector Graph

---

Scalable Vistor Graphics

---

Site Vector Graphics

#### --answer--

Scalable Vector Graphics 

### --question--

#### --text--

Qual è il ruolo dell'elemento `footer`?

#### --distractors--

Questo elemento serve a raggruppare sottotitoli e immagini correlate su una pagina web. 

---

Questo elemento serve a creare linee orizzontali tra paragrafi sulla pagina.

---

Questo elemento serve a creare una lista di elementi ordinati in fondo alla pagina. 

#### --answer--

Questo elemento serve a raggruppare contenuti di copyright o documenti correlati in fondo alla pagina. 

### --question--

#### --text--

Cos'è la codifica dei caratteri `UTF-8`?

#### --distractors--

È una codifica dei caratteri standardizzata usata per impostare il titolo di una pagina web.

---

È un insieme di caratteri usato per impostare la descrizione della pagina web.

---

È un insieme speciale di caratteri usato negli elementi per migliorare le prestazioni dei media. 

#### --answer--

È una codifica dei caratteri standardizzata ampiamente usata sul web.  

### --question--

#### --text--

Quale dei seguenti è un esempio di elemento sostituito?

#### --distractors--

`slot`

---

`div`

---

`p`

#### --answer--

`iframe` 

### --question--

#### --text--

Quale intestazione è considerata la più importante su una pagina web?

#### --distractors--

`h6`

---

`h3`

---

`h5`

#### --answer--

`h1` 

### --question--

#### --text--

Quale elemento si usa per collegare risorse esterne come fogli di stile e icone del sito?

#### --distractors--

`a`

---

`p`

---

`div`

#### --answer--

`link` 

### --question--

#### --text--

Quale elemento si usa per trasmettere un senso di urgenza, serietà o forte importanza?

#### --distractors--

`important`

---

`heavy`

---

`strength`

#### --answer--

`strong`

### --question--

#### --text--

Qual è il ruolo dell'elemento `figcaption`?

#### --distractors--

Fornisce sottotitoli per il contenuto all'interno di un elemento `span`. 

---

Fornisce sottotitoli per il contenuto all'interno di un elemento `slot`. 

---

Fornisce sottotitoli per il contenuto all'interno di un elemento `div`. 

#### --answer--

Fornisce sottotitoli per il contenuto all'interno di un elemento `figure`. 

### --question--

#### --text--

Qual è il modo corretto di impostare la dichiarazione `DOCTYPE` per un documento HTML?

#### --distractors--

`<DOCTYPE>`

---

`<*DOCTYPE html>`

---

`<<DOCTYPE html>>`

#### --answer--

`<!DOCTYPE html>`

### --question--

#### --text--

Qual è lo scopo dell'elemento `head` in un documento HTML?

#### --distractors--

È un contenitore di elementi àncora per il documento HTML. 

---

È un contenitore di immagini per il documento HTML. 

---

È un contenitore di elementi intestazione per il documento HTML. 

#### --answer--

È un contenitore di metadati per il documento HTML. 

### --question--

#### --text--

Qual è il modo corretto di impostare la lingua del documento HTML in inglese?

#### --distractors--

`<html lang-set="en">` 

---

`<html set-lang="en">` 

---

`<html language="en">` 

#### --answer--

`<html lang="en">` 

### --question--

#### --text--

Quale dei seguenti elementi NON dovrebbe essere inserito all'interno di `head`?

#### --distractors--

`link`

---

`meta`

---

`title`

#### --answer--

`img` 

## --quiz--

### --question--

#### --text--

Quale delle seguenti affermazioni NON è corretta riguardo ai percorsi assoluti e relativi?

#### --distractors--

I percorsi assoluti partono dalla directory radice, mentre i percorsi relativi dipendono dalla posizione del file corrente.

---

I percorsi relativi sono più corti e flessibili per i collegamenti all'interno di un sito, mentre i percorsi assoluti includono l'URL completo.

---

Sia i percorsi assoluti che relativi possono essere usati per collegare file all'interno dello stesso sito.

#### --answer--

I percorsi relativi non possono usare `..` per risalire le directory, mentre i percorsi assoluti sì.

### --question--

#### --text--

Quale dei seguenti NON è un valore valido per l'attributo target in un tag `a`?

#### --distractors--

`_self`

---

`_parent`

---

`_top`

#### --answer--

`_main`

### --question--

#### --text--

Qual è lo scopo dell'attributo `controls` nell'elemento `audio`?

#### --distractors--

Definisce il formato del file audio da usare nel browser.

---

Fa partire automaticamente l'audio quando la pagina si carica.

---

Specifica l'URL della sorgente del file audio.

#### --answer--

Aggiunge controlli integrati per la riproduzione come play, pausa e regolazione del volume.

### --question--

#### --text--

Quale elemento migliora la SEO fornendo una breve descrizione della pagina web?

#### --distractors--

```html
<meta
 description="describe"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 name="desc"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 description="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

#### --answer--

```html
<meta
 name="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

### --question--

#### --text--

Cosa fa `<meta charset="utf-8" />`?

#### --distractors--

Imposta la lingua della pagina web in inglese.

---

Collega un foglio di stile CSS esterno.

---

Specifica il titolo della pagina web.

#### --answer--

Definisce la codifica dei caratteri per garantire la corretta visualizzazione del testo.

### --question--

#### --text--

Qual è la differenza tra gli elementi `link` e `script` in HTML?

#### --distractors--

`link` è per file JavaScript, mentre `script` è per file CSS.

---

Sia `link` che `script` sono usati per stilizzare le pagine web.

---

`script` serve per aggiungere stili, mentre `link` serve per eseguire JavaScript.

#### --answer--

`link` si usa per aggiungere stili da un file CSS esterno, mentre `script` si usa per eseguire codice JavaScript.

### --question--

#### --text--

Quale delle seguenti affermazioni su classi e ID HTML è errata?

#### --distractors--

Un nome di classe può essere usato più volte in un documento HTML.

---

L'attributo `rel` definisce la relazione tra la risorsa collegata e il documento HTML.

---

La proprietà `og:image` si usa per specificare l'immagine mostrata nei post sui social media.

#### --answer--

I nomi ID non possono contenere underscore e devono essere unici.

### --question--

#### --text--

Quale delle seguenti affermazioni sugli elementi intestazione HTML è vera?

#### --distractors--

La dimensione del testo aumenta da `h1` a `h6`.

---

La dimensione del testo rimane uguale per tutti gli elementi intestazione.

---

`h3` è più grande di `h2`.

#### --answer--

La dimensione del testo diminuisce da `h1` a `h6`.

### --question--

#### --text--

Quale affermazione è errata sugli elementi HTML?

#### --distractors--

Gli elementi HTML sono i blocchi fondamentali di una pagina web.

---

Alcune basi di codice includono una barra (/) all'interno degli elementi void.

---

Un attributo booleano funziona semplicemente essendo presente o assente in un tag.

#### --answer--

Gli elementi void contengono contenuto e hanno solo un tag di apertura.

### --question--

#### --text--

Quale delle seguenti affermazioni sugli elementi void in HTML è vera?

#### --distractors--

Un elemento void deve sempre includere una barra / prima della chiusura.

---

Un elemento void non dovrebbe mai includere una barra /.

---

La barra / è obbligatoria per gli elementi void autochiudenti in tutte le versioni di HTML.

#### --answer--

Sia `<img>` che `<img/>` sono accettabili nell'HTML moderno.

### --question--

#### --text--

Qual è lo scopo principale dei commenti nella programmazione?

#### --distractors--

Far eseguire il codice più velocemente.

---

Cambiare come il browser mostra il contenuto.

---

Archiviare dati temporanei nel codice.

#### --answer--

Lasciare note per te stesso e altri sviluppatori nel codice.

### --question--

#### --text--

Quale attributo target apre un collegamento nello stesso contesto di navigazione corrente?

#### --distractors--

`_blank`

---

`_top`

---

`_parent`

#### --answer--

`_self`

### --question--

#### --text--

Qual è lo scopo dell'elemento `em` in HTML?

#### --distractors--

Ingrandire il testo.

---

Cambiare il colore del testo.

---

Sottolineare parole per enfasi.

#### --answer--

Evidenziare testo importante rendendolo in corsivo.

### --question--

#### --text--

Cosa fa l'elemento `strong` in HTML?

#### --distractors--

Aumenta la dimensione del carattere.

---

Cambia il colore del testo.

---

Sottolinea il testo.

#### --answer--

Rende il testo in grassetto per mostrare importanza.

### --question--

#### --text--

Qual è la differenza principale tra gli elementi `figure` e `figcaption` in HTML?

#### --distractors--

`figure` si usa per sottotitoli, mentre `figcaption` si usa per raggruppare contenuti correlati come immagini o diagrammi.

---

`figcaption` deve sempre essere posizionato fuori dall'elemento `figure`.

---

Non c'è differenza; entrambi si usano per lo stesso scopo.

#### --answer--

`figure` raggruppa contenuti correlati come immagini o diagrammi, mentre `figcaption` fornisce un sottotitolo per quel contenuto.

### --question--

#### --text--

Perché si usano le entità HTML?

#### --distractors--

Per aggiungere stile al testo.

---

Per creare collegamenti ipertestuali in una pagina web.

---

Per aumentare la dimensione del carattere dei caratteri speciali.

#### --answer--

Per rappresentare caratteri riservati come `<` e `&` in HTML.

### --question--

#### --text--

Quale delle seguenti affermazioni sui tag Open Graph è corretta?

#### --distractors--

I tag Open Graph si usano per stilizzare una pagina web con CSS.

---

I tag Open Graph si usano solo per la SEO e non hanno effetto sui social media.

---

I tag Open Graph devono essere inseriti nella sezione `body` dell'HTML.

#### --answer--

La proprietà `og:title` imposta il titolo che appare quando il contenuto è condiviso sui social media.

### --question--

#### --text--

Quale delle seguenti affermazioni sui formati immagine è errata?

#### --distractors--

WEBP e AVIF sono migliori per l'ottimizzazione moderna delle immagini.

---

Le immagini SVG possono scalare senza perdere qualità.

---

La compressione aiuta a ridurre le dimensioni dei file immagine.

#### --answer--

PNG e JPG sono i migliori formati per immagini ottimizzate oggi.

### --question--

#### --text--

Cosa fa l'attributo `loop` nell'elemento `audio`?

#### --distractors--

Riproduce l'audio una sola volta.

---

Ferma l'audio dopo una riproduzione.

---

Rallenta la riproduzione audio.

#### --answer--

Ripete l'audio in modo continuo.

### --question--

#### --text--

Qual è la descrizione migliore di un boilerplate HTML?

#### --distractors--

Uno strumento speciale usato dagli sviluppatori web per aggiungere filigrane ai documenti.

---

Un plugin del browser che velocizza il caricamento delle pagine web.

---

Un framework di codifica esclusivamente per animazioni in HTML.

#### --answer--

Un modello predefinito contenente la struttura HTML essenziale per lo sviluppo web.
