---
id: 67f41341453c2247fb2828f7
title: Тест зі структур даних
challengeType: 8
dashedName: quiz-data-structures
---

# --description--

Щоб пройти тест, потрібно правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що описує нотація Big O в аналізі алгоритмів?

#### --distractors--

Точний час виконання в секундах для конкретного комп’ютера.

---

Відсоток рядків коду, виконаних під час запуску.

---

Наскільки код зрозумілий іншим розробникам.

#### --answer--

Як зростає час або обсяг пам’яті відносно розміру вхідних даних (верхня межа).

### --question--

#### --text--

Який найкращий перший крок при початку алгоритмічного завдання?

#### --distractors--

Одразу почати писати код, щоб набрати темп.

---

Оптимізувати продуктивність до розуміння проблеми.

---

Писати юніт-тести лише після завершення рішення.

#### --answer--

Уточнити проблему та обмеження за допомогою прикладів і крайніх випадків.

### --question--

#### --text--

У чому ключова різниця між динамічними та статичними масивами?

#### --distractors--

Динамічні масиви зберігають значення різних типів; статичні — ні.

---

Статичні масиви дозволяють дублікати; динамічні — ні.

---

Динамічні масиви швидші за статичні у всіх операціях.

#### --answer--

Динамічні масиви можуть змінювати розмір (збільшуватись або зменшуватись); статичні мають фіксований розмір.

### --question--

#### --text--

Яка амортизована складність часу додавання елемента в кінець динамічного масиву?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` амортизовано.

### --question--

#### --text--

Чому доступ до k-го елемента за індексом у однозв’язному списку займає `O(n)` часу?

#### --distractors--

Список потрібно змінити за розміром перед будь-яким доступом.

---

Індекс хешується і шукається в таблиці.

---

Вузли зберігаються послідовно, тому потрібне зсування.

#### --answer--

Потрібно послідовно пройти від головного вузла до k-го.

### --question--

#### --text--

Яку функцію має двозв’язний список, якої немає у однозв’язного?

#### --distractors--

Випадковий доступ до будь-якого індексу за `O(1)` часу.

---

Вбудований буфер масиву для швидшої ітерації.

---

Автоматичне підтримання довжини списку як константи.

#### --answer--

Вказівники як на наступний, так і на попередній вузол, що дозволяє рухатися назад.

### --question--

#### --text--

Що найкраще описує стек?

#### --distractors--

Перший прийшов — перший вийшов (`FIFO`) з видаленнями спереду.

---

Структура, де будь-який елемент можна видалити за `O(1)` часу.

---

Кільцевий буфер із константним випадковим доступом.

#### --answer--

Останній прийшов — перший вийшов (`LIFO`) з `push` та `pop` зверху.

### --question--

#### --text--

Яка операція видаляє елемент із початку черги?

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

Яка типова середня складність часу пошуку значення за ключем у хеш-мапі?

#### --distractors--

`O(n)` через послідовне сканування всіх ключів.

---

`O(log n)` через бінарний пошук у бакетах.

---

`O(n log n)` через сортування ключів під час вставки.

#### --answer--

`O(1)` в середньому при хорошій хеш-функції та низькому коефіцієнті навантаження.

### --question--

#### --text--

Яку гарантію надає структура даних множина?

#### --distractors--

Елементи зберігаються за замовчуванням у відсортованому порядку.

---

Дозволяє дублікати і зберігає їх разом.

---

Елементи індексуються за позицією вставки.

#### --answer--

Зберігає лише унікальні елементи (без дублікатів).

### --question--

#### --text--

Яка найгірша складність часу вставки елемента на індекс i (не в кінець) у динамічному масиві?

#### --distractors--

`O(1)`

---

`O(log n)`

---

`O(1)` амортизовано

#### --answer--

`O(n)`

### --question--

#### --text--

Яка складність часу вставки нового вузла на початок однозв’язного списку?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

Яка операція використовується для видалення елемента зі стеку?

#### --distractors--

`push`

---

`dequeue`

---

Вставка внизу.

#### --answer--

`pop`

### --question--

#### --text--

Що найкраще описує чергу?

#### --distractors--

Останній прийшов — перший вийшов (`LIFO`) з видаленнями зверху.

---

Випадковий доступ до будь-якого індексу за `O(1)` часу.

---

Елементи завжди автоматично зберігаються у відсортованому порядку.

#### --answer--

Перший прийшов — перший вийшов (`FIFO`) з `enqueue` ззаду і `dequeue` спереду.

### --question--

#### --text--

Що таке хеш-колізія у хеш-мапі?

#### --distractors--

Коли ключ відображається на кілька різних значень за задумом.

---

Коли два ідентичні ключі зберігаються в різних бакетах.

---

Коли карта вичерпує пам’ять і її потрібно змінити за розміром.

#### --answer--

Коли два різні ключі дають однаковий хеш-індекс.

### --question--

#### --text--

Чому хеш-мапи змінюють розмір (рехешують) під час зростання?

#### --distractors--

Щоб відсортувати ключі за зростанням для швидшої ітерації.

---

Щоб стиснути значення і зменшити фрагментацію пам’яті.

---

Щоб уникнути запуску збирача сміття мови.

#### --answer--

Щоб підтримувати низький коефіцієнт навантаження і зберігати середню складність операцій `O(1)`.

### --question--

#### --text--

Яке твердження про множини є правильним?

#### --distractors--

Множини зберігають порядок вставки за визначенням.

---

Множини дозволяють дублікати і рахує їх.

---

Перевірки належності до множини мають `O(n log n)` в середньому.

#### --answer--

Перевірки належності зазвичай мають `O(1)` в середньому.

### --question--

#### --text--

Яка складність часу зростає швидше за `O(n log n)` при великому n?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

Що робити після реалізації рішення методом грубої сили?

#### --distractors--

Мікрооптимізувати константні множники до вимірювань.

---

Відмовитись від тестів і переписати рішення з нуля.

---

Уникати крайніх випадків, щоб код був простішим.

#### --answer--

Проаналізувати складність часу/пам’яті і оптимізувати виявлені вузькі місця.

### --question--

#### --text--

Що вимірює складність простору?

#### --distractors--

Скільки ядер центрального процесора використовує програма.

---

Довжину програми в рядках коду.

---

Скільки часу програма компілюється.

#### --answer--

Як зростає використання пам’яті відносно розміру вхідних даних.
