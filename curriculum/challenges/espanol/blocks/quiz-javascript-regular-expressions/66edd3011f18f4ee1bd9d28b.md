---
id: 66edd3011f18f4ee1bd9d28b
title: Cuestionario de Expresiones Regulares de JavaScript
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

Para aprobar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Cuál de las siguientes NO es una expresión regular válida?

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

¿Cuál `RegExp` contiene el mismo patrón que la expresión regular `/f[o0]{2} b[a4@]r/i`?

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

¿Cuál es el tipo de retorno del método de expresión regular `test`?

#### --distractors--

Un arreglo de cadenas que coincidieron con la expresión regular.

---

Un objeto que contiene información sobre la cadena (sub) que coincidió con la expresión regular.

---

`null`, el método `test` solo valida si la expresión regular dada es válida.

#### --answer--

Un booleano que indica si la cadena coincide con la expresión regular.

### --question--

#### --text--

¿Qué hace el método `match` cuando se le pasa una expresión regular sin banderas?

#### --distractors--

Busca cualquier cadena que coincida con la expresión regular dada, y devuelve TODAS las coincidencias como un arreglo.

---

Busca la primera coincidencia completa, y devuelve el índice de inicio de esa coincidencia.

---

Busca la primera coincidencia completa, y devuelve un booleano indicando si se encontró una coincidencia o no.

#### --answer--

Busca la primera coincidencia completa, y devuelve un arreglo que contiene esa primera coincidencia.

### --question--

#### --text--

¿Cuál es el mejor caso de uso para `test`?

#### --distractors--

Para verificar una expresión regular dada.

---

Para obtener información detallada sobre una coincidencia de cadena, como la longitud de la cadena coincidente.

---

Para extraer todas las coincidencias de una cadena dada.

#### --answer--

Para verificar si una cadena dada coincide con la expresión regular.

### --question--

#### --text--

¿Cuál es el propósito de usar `replace`?

#### --distractors--

Para reemplazar la expresión regular actual con una nueva expresión regular.

---

Al proporcionar un índice y una cadena, para reemplazar el carácter en el índice especificado con la cadena proporcionada.

---

Para eliminar todas las instancias de una subcadena coincidente de una cadena.

#### --answer--

Para reemplazar una cadena coincidente con una cadena de reemplazo dada.

### --question--

#### --text--

¿Cuál es la diferencia entre `match` y `matchAll`?

#### --distractors--

`match` siempre devuelve una cadena, mientras que `matchAll` siempre devuelve un arreglo de coincidencias.

---

`match` devuelve un booleano indicando si se encuentra una coincidencia, mientras que `matchAll` devuelve un número que representa la cantidad de coincidencias encontradas.

---

`match` devuelve un número que indica el índice de inicio de la primera coincidencia encontrada, mientras que `matchAll` devuelve un arreglo de números que contienen los índices donde se encontró una coincidencia.

#### --answer--

`match` devuelve un arreglo de cadenas que coinciden, mientras que `matchAll` devuelve un objeto iterador de todas las coincidencias encontradas.

### --question--

#### --text--

¿Cuál de las siguientes es la correcta utilización de `replaceAll`?

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

¿Cuál de las siguientes clases de caracteres es equivalente a la expresión regular `/[a-zA-Z0-9_]/`?

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

¿Cuál de las siguientes clases de caracteres es más apropiada para extraer dígitos de un número de teléfono?

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

¿Qué ocurre cuando se utiliza un lookahead `(?=)` en una expresión regular?

#### --distractors--

El lookahead verificará a la izquierda de la cadena, asegurando que el subpatrón ESTÉ presente.

---

Un lookahead es un patrón alternativo de expresión regular usado cuando el patrón principal no coincide.

---

El lookahead verificará a la derecha de la cadena, asegurando que el subpatrón NO esté presente.

#### --answer--

Un lookahead verifica a la derecha de la cadena, asegurando que el subpatrón ESTÉ presente.

### --question--

#### --text--

Al crear una expresión regular, ¿dónde debe colocarse un lookbehind `(?<=)`?

#### --distractors--

El lookbehind puede colocarse en cualquier parte de la expresión regular.

---

El lookbehind debe colocarse a la derecha del patrón principal de la expresión regular.

---

El lookbehind debe incluir el patrón principal de la expresión regular.

#### --answer--

El lookbehind debe colocarse a la izquierda del patrón principal de la expresión regular.

### --question--

#### --text--

¿Qué cuantificador coincide con el elemento anterior cero o una vez?

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

¿Cuál de las siguientes expresiones regulares sólo permite números entre 0 y 999,999?

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

¿Cuál de las siguientes afirmaciones es verdadera sobre la clase de carácter personalizada (`[]`)?

#### --distractors--

Es un conjunto de caracteres a eliminar de la coincidencia.

---

Puede definir un conjunto de caracteres para coincidir sin necesidad de escapar caracteres especiales.

---

Representa un conjunto de caracteres en forma Unicode.

#### --answer--

Puede definir un conjunto personalizado de caracteres para coincidir.

### --question--

#### --text--

¿Cuál de las siguientes clases de caracteres coincide correctamente con el alfabeto en mayúsculas?

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

¿Qué ocurre cuando se utiliza un grupo de captura `(...)` en una expresión regular?

#### --distractors--

El grupo de captura se evalúa de inmediato, sin importar donde se ubique en la expresión regular.

---

El grupo de captura intenta coincidir con el subpatrón dado, y continúa sin memorizar el resultado.

---

El grupo de captura es un subpatrón prioritario, que retornará inmediatamente cuando se encuentre una coincidencia para el subpatrón.

#### --answer--

El grupo de captura intenta coincidir con el subpatrón dado, y continúa con el resultado en memoria.

### --question--

#### --text--

¿Qué ocurre cuando se utiliza un grupo no capturante `(?:...)` en una expresión regular?

#### --distractors--

El grupo no capturante intenta coincidir con el subpatrón dado, y continúa con el resultado en memoria.

---

El grupo no capturante termina inmediatamente cuando encuentra una coincidencia para el subpatrón dado.

---

El grupo no capturante se considera una coincidencia opcional, así que solo se añade una coincidencia exitosa al resultado.

#### --answer--

El grupo no capturante intenta coincidir con el subpatrón dado, y continúa sin mantener el resultado en memoria.

### --question--

#### --text--

Dada la siguiente expresión regular, ¿cuál de estas pruebas devolverá `true`?

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

¿Qué hace la clase de carácter comodín (`.`) en una expresión regular?

#### --distractors--

Coincide solo con los caracteres de espacio en blanco.

---

Coincide con el inicio de una cadena.

---

Coincide con un solo dígito.

#### --answer--

Coincide con cualquier carácter individual excepto una nueva línea.
