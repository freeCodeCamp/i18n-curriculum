---
id: 5900f37d1000cf542c50fe90
title: '문제 17: 숫자 글자 수 세기'
challengeType: 1
forumTopicId: 301804
dashedName: problem-17-number-letter-counts
---

# --description--

숫자 1부터 5까지를 단어로 쓰면: one, two, three, four, five, 총 3 + 3 + 5 + 4 + 4 = 19글자가 사용됩니다.

1부터 주어진 `limit`까지 모든 숫자를 단어로 썼을 때, 총 몇 글자가 사용될까요?

**참고:** 공백이나 하이픈은 세지 마세요. 예를 들어, 342 (three hundred and forty-two)는 23글자이고 115 (one hundred and fifteen)는 20글자입니다. 숫자를 쓸 때 "and"를 사용하는 것은 영국식 표기에 따릅니다.

# --hints--

`numberLetterCounts(5)`은 숫자를 반환해야 합니다.

```js
assert.isNumber(numberLetterCounts(5));
```

`numberLetterCounts(5)`는 19를 반환해야 합니다.

```js
assert.strictEqual(numberLetterCounts(5), 19);
```

`numberLetterCounts(150)`은 1903을 반환해야 합니다.

```js
assert.strictEqual(numberLetterCounts(150), 1903);
```

`numberLetterCounts(1000)`는 21124를 반환해야 합니다.

```js
assert.strictEqual(numberLetterCounts(1000), 21124);
```

# --seed--

## --seed-contents--

```js
function numberLetterCounts(limit) {

  return true;
}

numberLetterCounts(5);
```

# --solutions--

```js
function numberLetterCounts(limit) {
  const dictionary = {
    0: '',
    1: 'one',
    2: 'two',
    3: 'three',
    4: 'four',
    5: 'five',
    6: 'six',
    7: 'seven',
    8: 'eight',
    9: 'nine',
    10: 'ten',
    11: 'eleven',
    12: 'twelve',
    13: 'thirteen',
    14: 'fourteen',
    15: 'fifteen',
    16: 'sixteen',
    17: 'seventeen',
    18: 'eighteen',
    19: 'nineteen',
    20: 'twenty',
    30: 'thirty',
    40: 'forty',
    50: 'fifty',
    60: 'sixty',
    70: 'seventy',
    80: 'eighty',
    90: 'ninety',
    1000: 'onethousand'
  };

  let numString = '';

  function convertToString(num) {
    // check dictionary for number
    if (dictionary[num]) {
      return dictionary[num];
    } else {
      const hundreds = Math.floor(num / 100);
      const tens =  Math.floor((num / 10) % 10) * 10;
      const remainder = num % 10;

      let tempStr = '';

      if (hundreds === 0) {
        tempStr += dictionary[tens] + dictionary[remainder];
      } else {
        tempStr += dictionary[hundreds] + 'hundred';

        if (tens !== 0 || remainder !== 0) {
          tempStr += 'and';
        }

        if (tens < 20) {
          const lessThanTwenty = tens + remainder;
          tempStr += dictionary[lessThanTwenty];
        } else {
          tempStr += dictionary[tens] + dictionary[remainder];
        }
      }
      return tempStr;
    }
  }

  for (let i = 1; i <= limit; i++) {
    numString += convertToString(i);
  }
  return numString.length;
}
```
