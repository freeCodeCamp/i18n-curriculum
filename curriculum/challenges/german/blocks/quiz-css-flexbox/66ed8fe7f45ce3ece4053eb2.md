---
id: 66ed8fe7f45ce3ece4053eb2
title: CSS Flexbox Quiz
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist Flexbox?

#### --distractors--

Ein zweidimensionales Modell für Layout.

---

Ein dreidimensionales Modell für Layout.

---

Ein multidimensionales Modell für Layout.

#### --answer--

Ein eindimensionales Modell für Layout.

### --question--

#### --text--

Welche CSS-Eigenschaft wird gesetzt, um das Flexbox-Layout für das `div`-Element zu aktivieren?

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

Welche CSS-Eigenschaft erlaubt es, dass Flex-Elemente in eine neue Zeile umbrechen, wenn die aktuelle Zeile voll ist?

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

Was steuert die Flexbox-Eigenschaft `justify-content`?

#### --distractors--

Sie steuert die Richtung der Kindelemente des Flex-Containers.

---

Sie steuert den Textstil der Kindelemente des Flex-Containers.

---

Sie steuert die Ausrichtung der Kindelemente innerhalb des Flex-Containers.

#### --answer--

Sie steuert die Verteilung der Kindelemente auf der Hauptachse.

### --question--

#### --text--

Welche Eigenschaft steuert die Richtung der Elemente innerhalb des Flex-Containers?

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

Welche CSS-Eigenschaften können verwendet werden, um Flex-Elemente von links nach rechts fließen zu lassen und das neue Flex-Element über dem vorherigen erscheinen zu lassen, wenn die Breite der aktuellen Zeile überschritten wird?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

Welche Eigenschaft steuert die Positionierung der Elemente auf der Querachse?

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

Welcher Wert ist kein Wert für `justify-content`?

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

Welche CSS-Eigenschaften würden Sie verwenden, um ein Element innerhalb eines Flex-Containers horizontal und vertikal zu zentrieren?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

Welcher Wert von `flex-direction` lässt die Flex-Elemente von rechts nach links fließen?

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

Welcher Wert ist kein Wert der Eigenschaft `flex-wrap`?

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

Was bewirkt der Wert `stretch` in `align-items`?

#### --distractors--

Elemente werden direkt aneinander am Startrand des Containers gepackt.

---

Elemente werden so ausgerichtet, dass ihre Flex-Container-Baselines übereinstimmen.

---

Elemente werden direkt aneinander am Endrand des Containers gepackt.

#### --answer--

Elemente werden automatisch so groß gemacht, dass sie den Container gleichmäßig ausfüllen.

### --question--

#### --text--

Welcher Wert von `justify-content` platziert das erste Element am Anfang, das letzte am Ende und verteilt die anderen Elemente gleichmäßig dazwischen entlang der Hauptachse?

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

Welcher Wert von `justify-content` verteilt die Elemente so, dass die Abstände zwischen jedem Paar benachbarter Elemente und den Container-Rändern alle gleich sind?

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

Welcher Wert von `justify-content` verteilt die Elemente gleichmäßig entlang der Hauptachse, lässt aber an den Container-Rändern jeweils die Hälfte dieses Abstands?

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

Welcher der folgenden Werte führt dazu, dass die Elemente am Anfang der Querachse ausgerichtet werden?

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

Welcher der folgenden Werte führt dazu, dass die Elemente am Ende der Querachse ausgerichtet werden?

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

Welcher Wert für `flex-direction` sorgt dafür, dass Elemente entlang der Hauptachse von links nach rechts ausgerichtet werden?

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

Welcher Wert für `flex-direction` sorgt dafür, dass Elemente entlang der Querachse von oben nach unten ausgerichtet werden?

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

Welcher der folgenden Anwendungsfälle ist für Flexbox nicht geeignet?

#### --distractors--

Ein Element in einem Container zentrieren.

---

Vertikale Ausrichtung von Elementen in einer Reihe.

---

Dynamische Anpassung der Verteilung und Ausrichtung von Elementen für verschiedene Viewports.

#### --answer--

Feste Größen und Abstände zwischen Elementen wie in einem Grid.

## --quiz--

### --question--

#### --text--

Was ist der Zweck eines eindimensionalen Layoutmodells wie Flexbox?

#### --distractors--

Das Layout in zwei Dimensionen gleichzeitig zu handhaben, sowohl Reihen als auch Spalten.

---

Die Stapelreihenfolge positionierter Elemente mit `z-index` zu steuern.

---

3D-Transformationen und komplexe Animationen auf Seitenelementen zu erstellen.

#### --answer--

Elemente entlang einer einzigen Linie oder Achse anzuordnen, entweder als Reihe oder als Spalte.

### --question--

#### --text--

Welche CSS-Eigenschaft richtet alle Flex-Elemente rechts im `.container`-Element aus?

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Welche zwei Achsen definieren die Richtung der Elementanordnung im Flex-Modell?

#### --distractors--

Die horizontale Achse und die vertikale Achse.

---

Die x-Achse und die y-Achse.

---

Die primäre Achse und die sekundäre Achse.

#### --answer--

Die Hauptachse und die Querachse.

### --question--

#### --text--

Welche der folgenden CSS-Regeln ordnet Elemente in einer vertikalen Spalte an und zentriert sie horizontal innerhalb ihres Containers?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

Was ist der Hauptzweck der Eigenschaft `flex-wrap`?

#### --distractors--

Sie legt die Richtung fest, in der Flex-Elemente im Flex-Container platziert werden.

---

Sie richtet Flex-Elemente entlang der Hauptachse des Containers aus.

---

Sie richtet Flex-Elemente entlang der Querachse des Containers aus.

#### --answer--

Sie bestimmt, ob Flex-Elemente auf neue Zeilen umbrechen sollen, um den verfügbaren Platz zu nutzen.

### --question--

#### --text--

Wie verhält sich die Deklaration `flex-wrap: nowrap;` standardmäßig?

#### --distractors--

Sie zwingt Flex-Elemente, auf eine neue Zeile umzubrechen, wenn sie zu breit für den Container sind.

---

Sie bricht Elemente auf eine neue Zeile um, aber in umgekehrter Reihenfolge.

---

Sie verhindert, dass der Flex-Container größer als sein Elternelement wird.

#### --answer--

Sie zwingt Flex-Elemente, auf einer einzigen Zeile zu bleiben, auch wenn sie den Container überlaufen.

### --question--

#### --text--

Welche CSS-Eigenschaft ist eine Kurzschreibweise, um sowohl `flex-direction` als auch `flex-wrap` gleichzeitig zu setzen?

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

Was bewirkt das Setzen von `flex-direction: column-reverse;` auf einem Flex-Container?

#### --distractors--

Es ordnet Elemente horizontal von rechts nach links an.

---

Es kehrt die Reihenfolge der Elemente in einer Reihe um, hält sie aber auf einer einzigen Zeile.

---

Es ordnet Elemente vertikal von oben nach unten an.

#### --answer--

Es ordnet Elemente vertikal in umgekehrter Reihenfolge von unten nach oben an.

### --question--

#### --text--

Welcher CSS-Code ordnet Flex-Elemente korrekt in einer vertikalen Spalte an und erlaubt ihnen, auf eine neue Spalte umzubrechen, wenn sie die Containerhöhe überschreiten?

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

Welcher Wert von `justify-content` richtet alle Flex-Elemente am Ende der Hauptachse des Containers aus?

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Welche Wirkung hat die folgende CSS-Regel auf Flex-Elemente?

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

Elemente werden in einer Spalte angeordnet und umbrechen in eine neue Spalte.

---

Elemente werden in einer Reihe angeordnet und umbrechen in eine neue Reihe.

---

Elemente werden in einer Spalte angeordnet und überlaufen, wenn sie nicht passen.

#### --answer--

Elemente werden in einer Reihe angeordnet und überlaufen den Container, wenn sie nicht passen.

### --question--

#### --text--

Wie richtet `justify-content: space-evenly;` Flex-Elemente aus?

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

Elemente sind gleichmäßig verteilt, ohne Abstand am Anfang oder Ende.

---

Elemente sind in der Mitte des Containers zusammengepackt.

---

Elemente werden an die rechte Seite des Containers gedrückt, mit gleichem Abstand zwischen ihnen.

#### --answer--

Elemente haben gleichen Abstand zueinander und an beiden Enden des Containers.

### --question--

#### --text--

Welche Eigenschaft würden Sie verwenden, um Elemente vertikal zu zentrieren, wenn `flex-direction: row;` gesetzt ist?

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

Was bewirkt der Wert `flex-start` für die Eigenschaft `justify-content`?

#### --distractors--

Er richtet Elemente am Anfang der Querachse aus.

---

Er streckt Elemente, um die Hauptachse des Containers auszufüllen.

---

Er zentriert alle Elemente entlang der Hauptachse.

#### --answer--

Er richtet Elemente am Anfang der Hauptachse des Containers aus.

### --question--

#### --text--

Wie richten Sie einen Flex-Container ein, damit seine Elemente von rechts nach links in einer einzigen Zeile angeordnet sind und außerdem oben im Container positioniert werden?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

Welche der folgenden Deklarationen ist gültig für die Eigenschaft `flex-flow`?

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

Welche der folgenden CSS-Regeln bewirkt, dass Flex-Elemente horizontal zentriert und vertikal gestreckt werden, um die Höhe des Containers auszufüllen?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

Welche Wirkung hat `align-items: stretch;`, wenn `flex-direction` auf `column` gesetzt ist?

#### --distractors--

Es streckt Elemente vertikal, um die Containerhöhe auszufüllen.

---

Es hat keine Wirkung, wenn die Richtung `column` ist.

---

Es streckt Elemente vertikal, um sich an das höchste Element anzupassen.

#### --answer--

Es streckt Elemente horizontal, um die Containerbreite auszufüllen.

### --question--

#### --text--

Wenn Sie die Reihenfolge der Elemente in einer horizontalen Reihe umkehren möchten, welche Deklaration sollten Sie verwenden?

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

Welcher Wert von `align-items` positioniert Elemente oben in einem Container, dessen `flex-direction` `row` ist?

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
