---
id: 6570447ee7b02272663824e2
title: 學習區塊和行內課程 D
challengeType: 15
dashedName: learn-block-and-inline-lesson-d
---

# --description--

既然你已經基本了解為 `inline` 元素添加寬度和高度的運作方式，當你為 `inline` 元素添加上邊距時會發生什麼？

# --questions--    

## --text--

`margin-top` 如何影響 `inline` 元素？

## --answers--

`margin-top` 對 `inline` 元素的版面配置沒有影響

---

`margin-top` 會增加 `inline` 元素的高度。

---

`margin-top` 為 `inline` 元素上方添加空間。

---

`margin-top` 僅影響 `inline` 元素的水平間距。

## --video-solution--

1
