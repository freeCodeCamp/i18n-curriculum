---
id: 66ed9043f45ce3ece4053ebf
title: 폼 스타일링 퀴즈
challengeType: 8
dashedName: quiz-styling-forms
---

# --description--

퀴즈를 통과하려면 아래 10문제 중 최소 9문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

`datetime-local` 입력 유형을 스타일링할 때 흔히 발생하는 문제는 무엇인가요?

#### --distractors--

`float` 속성과 함께 작동하지 않습니다.

---

반응형으로 만드는 것이 불가능합니다.

---

`rem` 단위와 함께 작동하지 않습니다.

#### --answer--

브라우저마다 입력 형식이 다릅니다.

### --question--

#### --text--

텍스트 입력 스타일링에 대한 모범 사례로 간주되는 것은 무엇인가요?

#### --distractors--

텍스트 입력 폰트 크기는 `em` 단위로 설정해야 하며 색상은 연한 회색이어야 합니다.

---

텍스트 입력 크기는 포커스 시 어두운 녹색 테두리만 사용하고 텍스트 색상은 어두운 회색이어야 합니다.

---

텍스트 입력 폰트 색상은 밝은 색이어야 하며 크기는 `px` 단위로만 설정해야 합니다.

#### --answer--

텍스트 입력 폰트는 적절한 크기로 설정하고, 색상은 배경과 충분한 대비를 가져야 합니다.

### --question--

#### --text--

오류 상태 스타일링은 포커스 상태 스타일링과 어떻게 관련되어야 하나요?

#### --distractors--

일관성을 위해 동일해야 합니다.

---

사용자를 놀라게 하지 않도록 오류 상태는 덜 눈에 띄어야 합니다.

---

오류 상태는 애니메이션 효과가 있어야 하고 포커스 상태는 정적이어야 합니다.

#### --answer--

오류 상태는 포커스 상태와 구별되어야 합니다.

### --question--

#### --text--

포커스 상태의 입력 스타일링에 대한 모범 사례는 무엇인가요?

#### --distractors--

포커스된 입력에는 연한 회색 테두리만 사용하는 것이 중요합니다.

---

포커스된 입력에는 진한 빨간색 테두리만 사용하는 것이 중요합니다.

---

눈에 띄는 표시기를 모두 제거하는 것이 중요합니다.

#### --answer--

눈에 띄는 표시기를 유지하는 것이 중요합니다.

### --question--

#### --text--

오류 상태의 입력 스타일링에 대한 모범 사례는 무엇인가요?

#### --distractors--

입력 텍스트의 폰트 크기를 `3rem`로 키우세요.

---

입력을 `display: none;`로 설정하여 페이지에서 제거하세요.

---

입력을 비활성화하여 사용자가 더 이상 상호작용하지 못하게 하세요.

#### --answer--

사용자가 잘못된 점을 알고 수정해야 한다는 메시지와 함께 시각적 표시기를 제공하세요.

### --question--

#### --text--

폼 입력에서 설명적인 자리 표시자 텍스트를 사용하는 것이 왜 중요한가요?

#### --distractors--

사용자를 위해 폼을 미리 채우는 데 도움이 됩니다.

---

사용자가 입력할 때 자동으로 유효성 검사를 합니다.

---

입력 레이블을 숨겨 시각적 혼란을 줄입니다.

#### --answer--

사용자에게 예상 입력 형식에 대한 안내를 제공합니다.

### --question--

#### --text--

WebKit이란 무엇인가요?

#### --distractors--

CSS에서 유효성 검사 오류가 없도록 보장하는 브라우저 엔진입니다.

---

CSS Grid에서 사용하는 특별한 CSS 속성입니다.

---

CSS flexbox에서 사용하는 특별한 CSS 속성입니다.

#### --answer--

웹페이지를 표시하는 브라우저 엔진입니다.

### --question--

#### --text--

폼 요소에 `appearance: none;`를 사용하는 것이 적절한 경우는 언제인가요?

#### --distractors--

폼 요소를 DOM과 페이지에서 완전히 제거할 때.

---

화면 읽기 프로그램 같은 보조 기술 사용자를 위해 폼 요소를 숨길 때.

---

폼 요소에 적용된 기본 색상만 제거하고 직접 스타일을 적용할 때.

#### --answer--

폼 요소에 적용된 일부 기본 스타일을 제거할 때.

### --question--

#### --text--

폼 요소에서 `appearance: none;`를 사용할 때 고려해야 할 점은 무엇인가요?

#### --distractors--

인터랙티브 요소에서 호버 및 제출 표시기를 유지하는 것이 중요합니다.

---

Safari 브라우저에 다른 스타일을 적용할 때만 `appearance: none;`를 사용하는 것이 중요합니다.

---

페이지에서 폼 요소를 완전히 제거해야 할 때 `appearance: none;`를 사용하는 것이 중요합니다.

#### --answer--

인터랙티브 요소에서 포커스와 오류 표시기를 유지하는 것이 중요합니다.

### --question--

#### --text--

기본 모양을 제거하기 위해 `appearance: none;`를 사용해 스타일링하는 폼 요소 유형은 무엇인가요?

#### --distractors--

`label` 요소입니다.

---

`div` 요소입니다.

---

`span` 요소입니다.

#### --answer--

`input` 요소입니다.
