---
id: 672bb015cfc889794359c4e0
title: Які найкращі практики для проєктування модальних діалогів?
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**ПРИМІТКА**: Деякі інтерактивні приклади можуть використовувати CSS і JavaScript, які ви ще не вивчали. Не хвилюйтеся, якщо не розумієте весь код. Мета прикладів — показати вам попередній перегляд цих концепцій дизайну, щоб ви краще розуміли, як це працює. Щоб побачити попередній перегляд, потрібно увімкнути інтерактивний редактор.

Що таке модальне вікно? Це тип спливаючого вікна, яке сайт може показати поверх свого вмісту. HTML має `dialog` елемент, який можна використати для створення модальних вікон.

Вміст позаду модального вікна зазвичай затемнюють. Це допомагає користувачу візуально сфокусуватися на області, з якою ви хочете, щоб він взаємодіяв — у цьому випадку, на модальному вікні.

Завжди добре дозволяти користувачу натиснути поза межами модального вікна, щоб його закрити.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

Ви часто побачите дуже помітні кнопки на модальних вікнах. Їх називають CTA, або call-to-action. Ви хочете, щоб їх легко впізнавали, адже мета переривати потік користувача модальним вікном — спонукати його виконати конкретну дію.

Модальні вікна також повинні мати кнопку закриття. Хоча ви й хочете, щоб користувач натискав на ваші CTA, важливо дати йому можливість вийти з модального вікна і продовжити те, що він робив раніше.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

Звісно, існують питання доступності з модальними вікнами, наприклад, правильне керування фокусом на елементах. Проте, якщо ви використаєте ці загальні практики як відправну точку, у вас буде міцна основа для подальшої роботи.

# --questions--

## --text--

Яка мета затемнення вмісту позаду модального діалогу?

## --answers--

Щоб заощадити заряд батареї на мобільних пристроях.

### --feedback--

Урок згадує конкретну причину цього візуального ефекту.

---

Щоб допомогти користувачам візуально визначити область, з якою слід взаємодіяти.

---

Щоб сайт завантажувався швидше.

### --feedback--

Урок згадує конкретну причину цього візуального ефекту.

---

Щоб запобігти перегляду користувачами конфіденційної інформації.

### --feedback--

Урок згадує конкретну причину цього візуального ефекту.

## --video-solution--

2

## --text--

Що означає CTA у контексті дизайну модальних вікон?

## --answers--

Click To Activate.

### --feedback--

Урок надає повну назву цієї абревіатури, обговорюючи помітні кнопки в модальних вікнах.

---

Complete The Action.

### --feedback--

Урок надає повну назву цієї абревіатури, обговорюючи помітні кнопки в модальних вікнах.

---

Call-to-Action.

---

Close The Application.

### --feedback--

Урок надає повну назву цієї абревіатури, обговорюючи помітні кнопки в модальних вікнах.

## --video-solution--

3

## --text--

Чому важливо включати кнопку закриття в модальний діалог?

## --answers--

Це вимагається законом у деяких країнах.

### --feedback--

Урок підкреслює важливість надання користувачам можливості вийти з модального вікна.

---

Щоб запобігти аварійному завершенню браузера через модальне вікно.

### --feedback--

Урок підкреслює важливість надання користувачам можливості вийти з модального вікна.

---

Щоб дати користувачам можливість вийти з модального вікна і продовжити попередню діяльність.

---

Щоб модальне вікно виглядало професійніше.

### --feedback--

Урок підкреслює важливість надання користувачам можливості вийти з модального вікна.

## --video-solution--

3
