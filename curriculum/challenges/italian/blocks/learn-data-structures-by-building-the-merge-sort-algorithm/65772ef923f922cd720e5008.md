---
id: 65772ef923f922cd720e5008
title: Passaggio 7
challengeType: 20
dashedName: step-7
---

# --description--

Passa `left_part` come argomento alla chiamata `merge_sort()` che hai aggiunto nel passaggio precedente.

# --hints--

Dovresti passare `left_part` come argomento alla tua chiamata `merge_sort()`.

```js
({
  test: () => {
    const transformedCode = code.replace(/\r/g, "");
    const merge_sort = __helpers.python.getDef("\n" + transformedCode, "merge_sort");
    const { function_body } = merge_sort;

    assert.match(function_body, /merge_sort\s*\(\s*left_part\s*\)/);
  }
})
```

# --seed--

## --seed-contents--

```py
--fcc-editable-region--
def merge_sort(array):
    
    middle_point = len(array) // 2
    left_part = array[:middle_point]
    right_part = array[middle_point:]
    
    merge_sort()
--fcc-editable-region--
```
