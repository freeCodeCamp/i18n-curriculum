---
id: 66ed902df45ce3ece4053ebc
title: HTML 表格和表單測驗
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

下列哪一個屬性用來指定表單資料應該送往的 URL？

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

哪兩個元素用於指定表格中的列和列標題？

#### --distractors--

`r` 元素用於列，`thead` 元素用於標頭。

---

`row` 元素用於列，`th` 元素用於標題。

---

`tr` 元素用於列，`head` 元素用於標頭。

#### --answer--

`tr` 元素用於列，`th` 元素用於標頭。

### --question--

#### --text--

`td` 元素用於什麼？

#### --distractors--

它用於定義一個表格資料型別。

---

它用於合併兩個行。

---

它用於合併兩個列。

#### --answer--

它用於定義一個表格儲存格。

### --question--

#### --text--

`colspan` 屬性的作用是什麼？

#### --distractors--

它定義了表格中一個行的長度。

---

它會移除行中文字之間的換行符。

---

它用於定義行數。

#### --answer--

它定義了表格儲存格應該跨越的行數。

### --question--

#### --text--

`placeholder` 屬性的作用是什麼？

#### --distractors--

當表單提交時，它用於將資料傳送到伺服端。

---

它用於將 `label` 元素與 `input` 元素關聯。

---

它用於指定輸入欄位對使用者為唯讀。

#### --answer--

它用於為輸入欄提供提示。

### --question--

#### --text--

`thead` 元素的用途是什麼？

#### --distractors--

它用於將 HTML 表格中的主體內容分組。

---

它用於將 HTML 表格的描述分組。

---

它用於將頁腳內容分組在 HTML 表格中。

#### --answer--

它用於將標頭內容分組在 HTML 表格中。

### --question--

#### --text--

以下哪一項是標籤與輸入之間明確的表單關聯範例？

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

HTML 驗證器的用途是什麼？

#### --distractors--

驗證器是一種自動格式化你的 HTML 程式碼的工具。

---

驗證器是一種將樣式套用到你的 HTML 的工具。

---

驗證器是一種讓你的 HTML 程式碼執行更快的工具。

#### --answer--

驗證器是一種工具，用來檢查 HTML 程式碼的語法，以確保其有效。

### --question--

#### --text--

哪個元素用來定義表格中的儲存格？

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

以下哪一項是允許你檢查和修改網頁 HTML 結構的工具？

#### --distractors--

移除 DOM。

---

DOM 測試工具。

---

DOM 驗證。

#### --answer--

DOM 檢查器。

### --question--

#### --text--

以下哪一項是將相關輸入欄位分組的正確方式？

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

下列哪一項用於指定輸入欄為唯讀？

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

哪個屬性指定在提交表單之前必須填寫輸入欄位？

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

Devtools 用來做什麼？

#### --distractors--

這些工具是直接內建於瀏覽器中，用來偵測你程式碼中的任何 linting 問題。

---

這些工具是直接內建於瀏覽器中，用於自動格式化你的程式碼。

---

這些工具是直接內建於瀏覽器中，用來幫助你確保你的程式碼有 100％ 的測試涵蓋率。

#### --answer--

這些工具直接內建於瀏覽器中，用於幫助你偵錯、評測和分析網頁。

### --question--

#### --text--

以下哪一種方法是停用輸入的正確方式？

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

以下哪一項是 `type` 屬性的有效值？

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

以下哪一項是 `size` 屬性的正確用法？

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

下列哪些屬性用於指定輸入欄位中所需的最小和最大字元數？

#### --distractors--

`minlen` 和 `maxlen`

---

`min` 和 `max`

---

`minimumLen` 和 `maximumLen`

#### --answer--

`minlength` 和 `maxlength`

### --question--

#### --text--

以下哪一個按鈕範例**沒有**為 `type` 屬性使用正確的值？

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

下列哪一個屬性用來指定按鈕的值？

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

開啟的 `form` 頁籤內 `action` 屬性的角色是什麼？

#### --distractors--

它用於指定輸入欄位為唯讀。

---

它用於定義使用者在輸入時應該可見的字元數量。

---

它用於向使用者顯示提示，告訴他們在輸入欄位中應輸入什麼。

#### --answer--

它用於指定表單資料應該發送到的 URL。

### --question--

#### --text--

下列哪一個屬性用來指定在傳送表單資料時所使用的 HTTP 方法？

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

以下哪一項是處理表單時常用的方法？

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

以下哪一項不是 `type` 屬性的有效值？

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

以下哪一種是在表單中創建按鈕的正確方式？

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

什麼是隱喻的表單關聯？

#### --distractors--

這是透過在 `label` 元素上使用 `action` 和 `for` 屬性，將輸入與標籤相關聯的地方。

---

這是透過在 `label` 元素上使用 `required` 和 `for` 屬性，將輸入與標籤相關聯的地方。

---

這是透過在 `label` 元素上使用 `for` 屬性，將輸入與標籤相關聯的地方。

#### --answer--

這是將輸入欄位包裹在 `label` 元素內，以便將輸入與標籤相關聯的地方。

### --question--

#### --text--

`fieldset` 元素的角色是什麼？

#### --distractors--

它用於將標題內容分組在 HTML 表格中。

---

它用於為輸入欄位創建標籤。

---

它用於為輸入的群組添加說明文字。

#### --answer--

它用於將相關的輸入組合在一起。

### --question--

#### --text--

以下哪一項用於在表單中創建核取方塊？

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

下列哪一個元素用於為 HTML 表格添加標題？

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

`legend` 元素的角色是什麼？

#### --distractors--

它用於將 HTML 表格中的主體內容分組。

---

它用於為表格的儲存格添加說明文字。

---

它用於將相關的輸入組合在一起。

#### --answer--

它用於為輸入的群組添加說明文字。

### --question--

#### --text--

下列哪一個元素用於將 HTML 表格中的標頭內容分組？

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

以下哪一種方式是將列和資料儲存格添加到表格的正確方法？

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

什麼是 DOM 檢查器？

#### --distractors--

一個用於從 DOM 中移除所有元素的工具。

---

一組直接內建於瀏覽器中的工具，幫助你偵錯、評測及分析網頁。

---

一個檢查 HTML `code` 語法以確保其有效的工具。

#### --answer--

一個允許你檢查和修改網頁 HTML 結構的工具。

### --question--

#### --text--

以下哪一項是用來檢查 HTML 程式碼語法以確保其有效的工具？

#### --distractors--

HTML 驗證

---

HTML isValid

---

HTML 有效

#### --answer--

HTML 驗證器

### --question--

#### --text--

下列哪一項是指定表格中資料儲存格行數的正確方式？

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

輸入欄的聚焦狀態是什麼？

#### --distractors--

這是使用者移除焦點時輸入欄的狀態。

---

這是當使用者將輸入欄設定為唯讀時的狀態。

---

這是輸入欄位被使用者停用時的狀態。

#### --answer--

這是使用者選取輸入欄時的狀態。

### --question--

#### --text--

以下哪一種方式是正確的圓鈕創建方法？

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

`for` 屬性的作用是什麼？ 

#### --distractors--

它用於向使用者顯示提示。

---

它用於指定輸入的值

---

它用於為輸入欄位創建標籤。

#### --answer--

它用於指定標籤所對應的輸入欄。

### --question--

#### --text--

以下哪一個屬性用來定義使用者在輸入時應該可見的字元數量？

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

以下哪一個屬性用來向使用者顯示提示，告訴他們在輸入欄中應輸入什麼？

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
