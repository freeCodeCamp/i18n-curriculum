---
id: 66edcc779993c0da6906dbb9
title: JavaScript 函数测验
challengeType: 8
dashedName: quiz-javascript-functions
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪种方法是在 JavaScript 中声明函数的正确方法？

#### --distractors--

```js
func greet() {
  console.log("Hello there!");
}
```

---

```js
defineFunction greet() {
  console.log("Hello there!");
}
```

---

```js
def greet() {
  console.log("Hello there!");
}
```

#### --answer--

```js
function greet() {
  console.log("Hello there!");
}
```

### --question--

#### --text--

调用（或执行）函数时会发生什么？

#### --distractors--

函数内部的代码将运行，且始终返回 `null`。

---

调用函数时不会发生任何事情。

---

将抛出 `TypeError` 并导致程序崩溃。

#### --answer--

函数内部的代码将运行，并返回一个值。

### --question--

#### --text--

以下哪项是调用 minmax() 函数的正确方法？

#### --distractors--

```js
exampleFunction;
```

---

```js
exampleFunction>>;
```

---

```js
()exampleFunction();
```

#### --answer--

```js
exampleFunction();
```

### --question--

#### --text--

`return` 关键字的作用是什么？

#### --distractors--

`return` 关键字将执行函数。

---

`return` 关键字将抛出类型错误。

---

`return` 关键字在函数中没有任何作用。

#### --answer--

`return` 关键字从函数返回一个值。

### --question--

#### --text--

以下哪项是有效的函数表达式？

#### --distractors--

```js
expression function getSum(x, y) {
    return x + y;
};
```

---

```js
getSum: function(x, y) {
    return x + y;
};
```

---

```js
function = getSum(x, y) {
    return x + y;
};
```

#### --answer--

```js
const getSum = function(x, y) {
    return x + y;
};
```

### --question--

#### --text--

什么是函数实参？

#### --distractors--

表示没有数值的值。

---

与匿名函数类似的特殊值。

---

函数内部使用的占位符值。

#### --answer--

调用函数时传递给函数的实际值。

### --question--

#### --text--

什么是函数形参？

#### --distractors--

函数内部使用的浮点数值。

---

调用函数时传递给函数的值。

---

代表故意不存在的值。

#### --answer--

函数定义中列出的值。

### --question--

#### --text--

控制台将记录什么内容？

```js
const calculateTotal = (amount, taxRate = 0.05) => {
  return amount + (amount * taxRate);
};

console.log(calculateTotal(100));
```

#### --distractors--

`10`

---

`undefined`

---

`1`

#### --answer--

`105`

### --question--

#### --text--

以下哪种语法是箭头函数的正确语法？

#### --distractors--

```js
const sum <<>> (num1, num2) => num1 + num2
```

---

```js
const sum === (num1, num2) === num1 + num2
```

---

```js
const sum >=> (num1, num2) => num1 + num2
```

#### --answer--

```js
const sum = (num1, num2) => num1 + num2
```

### --question--

#### --text--

下面代码的结果是什么？

```js
function greet() {
  const developer = "Jessica";
  console.log("Hello there!");
}

console.log(developer);
```

#### --distractors--

什么也不会发生，代码将正常运行。

---

字符串 `"Hello there!"` 将被记录到控制台。

---

字符串 `"Hello!"` 将被记录到控制台。

#### --answer--

由于 `developer` 并非全局定义，因此将发生引用错误。

### --question--

#### --text--

什么时候可以省略箭头函数参数列表周围的括号？

#### --distractors--

应始终省略参数列表周围的括号。

---

箭头函数的括号绝对不能省略。

---

只有在处理多个参数时，才可以省略括号。

#### --answer--

只有在只有一个参数时，才可以省略括号。

### --question--

#### --text--

何时可以省略箭头函数的大括号和 `return` 关键字？

#### --distractors--

应始终省略大括号和 `return` 关键字。

---

切勿省略大括号和 `return` 关键字。

---

当函数体有多行代码时。

#### --answer--

当函数体由单个表达式组成时。

### --question--

#### --text--

以下函数将返回什么？

```js
function exampleFunction() {
  return "Hello";
  return "World!";
};
```

#### --distractors--

`"World!"`

---

`"Hello World!"`

---

`undefined`

#### --answer--

`"Hello"`

### --question--

#### --text--

以下哪项是使用默认参数的正确方法？

#### --distractors--

```js
function sum(num1, num2 <<>> 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 === 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 >> 12) {
  return num1 + num2
}
```

#### --answer--

```js
function sum(num1, num2 = 12) {
  return num1 + num2
}
```

### --question--

#### --text--

下面代码的结果是什么？

```js
const developer = "Jessica";

function greet() {
  console.log("Hello, " + developer)
}

greet();
```

#### --distractors--

字符串 `"Hello, developer"` 将被记录到控制台。

---

控制台中将显示错误信息。

---

控制台中不会显示任何内容。

#### --answer--

字符串 `"Hello, Jessica"` 将被记录到控制台。

### --question--

#### --text--

如果尝试去掉普通函数的大括号，会发生什么情况？

#### --distractors--

页面上将显示一个提示错误的提示框。

---

程序会崩溃，无法再次启动。

---

什么都不会发生。

#### --answer--

会出现语法错误。

### --question--

#### --text--

下面代码的结果是什么？

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + 10);
```

#### --distractors--

控制台中将显示错误信息。

---

数字 `0` 将被记录到控制台。

---

控制台不会记录任何内容。

#### --answer--

数字 `10` 将被记录到控制台。

### --question--

#### --text--

下面代码的输出是什么？

```js
const exampleFunction = (param1, param2) => param1 + param2;
console.log(exampleFunction(3, "Something"));
```

#### --distractors--

`null`

---

`undefined`

---

`3 + "Something"`

#### --answer--

`"3Something"`

### --question--

#### --text--

下面代码的结果是什么？

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + num2);
```

#### --distractors--

错误信息提示 `num1` 未定义。

---

控制台中将显示数字 `0` 。

---

控制台中将显示 `undefined`。

#### --answer--

错误信息提示 `num2` 未定义。

### --question--

#### --text--

下面代码的输出是什么？

```js
const divideTwoNumbers = (num1, num2) => num1 / num2;
console.log(divideTwoNumbers(3, 0));
```

#### --distractors--

`Error`

---

`0`

---

`null`

#### --answer--

`Infinity`

