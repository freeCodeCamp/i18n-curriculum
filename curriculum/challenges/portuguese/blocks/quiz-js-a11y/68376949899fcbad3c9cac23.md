---
id: 68376949899fcbad3c9cac23
title: Quiz de JavaScript e Acessibilidade
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 9 das 10 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual atributo é usado para fins de acessibilidade para indicar se um controle está expandido ou recolhido?

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

Quais dois atributos podem ser usados em combinação com `aria-expanded` para estabelecer uma conexão programática entre o elemento controlador e o elemento que ele controla?

#### --distractors--

`aria-labelledBy` e `aria-owns`

---

`aria-controls` e `aria-label`

---

`aria-live` e `aria-owns`

#### --answer--

`aria-controls` e `aria-owns`

### --question--

#### --text--

Qual dos seguintes atributos cria uma região ao vivo na sua página que pode ser usada para notificar usuários de leitor de tela sobre mudanças dinâmicas no conteúdo da região ao vivo conforme elas ocorrem?

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

Qual dos seguintes elementos `div` será desabilitado apenas para pessoas que usam tecnologias assistivas, como leitores de tela?

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

Qual das seguintes opções é um uso válido do atributo `aria-expanded`?

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

Qual atributo é usado para criar uma relação programática entre um elemento controlador e o elemento que ele afeta na página?

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

Qual dos seguintes NÃO é um caso de uso comum para live regions?

#### --distractors--

Mensagens que são exibidas após uma ação ter sido realizada (como uma mensagem de erro ou confirmação).

---

Conteúdo que é atualizado periodicamente (como um `ticker`, `marquee` ou `countdown timer`).

---

Mensagens gerais de status (como atualizações sobre um processo).

#### --answer--

Mensagens que são exibidas antes que uma ação tenha sido realizada (como uma caixa de diálogo de confirmação).

### --question--

#### --text--

Qual dos exemplos a seguir é a maneira correta de indicar qual aba está atualmente selecionada?

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

Qual dos seguintes NÃO é um valor válido para o atributo `aria-live`?

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

Qual dos seguintes valores do atributo `aria-live` significa que a atualização é muito importante e tem a maior prioridade?

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`

