---
id: 67f41242431cbf3db8ca79c7
title: Quiz Básico de Python
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual das seguintes funções é usada para verificar se uma variável corresponde a um tipo de dado específico?

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

Qual das seguintes opções NÃO é uma forma de concatenação de strings?

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

Qual das seguintes funções é usada para retornar o número de caracteres na string?

#### --distractors--

```py
counting()
```

---

```py
length()
```

---

```py
iscount()
```

#### --answer--

```py
len()
```

### --question--

#### --text--

Qual será o valor de `result` neste exemplo?

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

O que acontece quando você adiciona um float e um integer?

#### --distractors--

O resultado será uma mensagem de erro.

---

O resultado será `None`.

---

O resultado será um inteiro.

#### --answer--

O resultado será um float.

### --question--

#### --text--

Qual das seguintes é a maneira correta de definir uma função?

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

O que será impresso no console?

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

Qual das seguintes afirmações é falsa ao nomear variáveis?

#### --distractors--

Nomes de variáveis só podem começar com uma letra ou um underscore (_), não com um número.

---

Nomes de variáveis só podem conter caracteres alfanuméricos (a-z, A-Z, 0-9) e underscores (_).

---

Nomes de variáveis não podem ser uma das palavras reservadas do Python como `if`, `class` ou `def`.

#### --answer--

Nomes de variáveis devem ter um comprimento máximo de 10 caracteres, caso contrário o Python gerará um erro.

### --question--

#### --text--

Qual dos seguintes NÃO é um tipo de dado suportado em Python?

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

Qual das seguintes opções retornará uma nova string com todos os caracteres convertidos para maiúsculas?

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

Qual das seguintes funções é usada para obter entrada de um usuário?

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

Qual será o resultado para o seguinte código?

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

O que o método `split()` faz?

#### --distractors--

Este método é usado para dividir uma tupla em uma lista de substrings.

---

Este método é usado para dividir um float em uma lista de substrings.

---

Este método é usado para dividir um dicionário em uma lista de substrings.

#### --answer--

Este método é usado para dividir uma string em uma lista de substrings.

### --question--

#### --text--

O que o seguinte irá imprimir no console?

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

Qual das seguintes funções é usada para criar uma tabela de mapeamentos de 1 para 1 de caracteres para tradução?

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

Qual será o resultado para o seguinte código?

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

O que será retornado pelo método `find()` se nenhuma ocorrência da substring for encontrada em uma string?

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

Qual das seguintes funções é usada para contar quantas vezes uma substring aparece em uma string?

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

O que a divisão inteira faz em Python?

#### --distractors--

Este operador é usado para multiplicar dois números e arredondar o resultado para o número inteiro mais próximo para cima.

---

Este operador é usado para converter um float em um inteiro.

---

Este operador é usado para elevar um número à potência de outro.

#### --answer--

Este operador é usado para dividir dois números e arredondar para baixo o resultado para o número inteiro mais próximo.

### --question--

#### --text--

Qual das seguintes funções é usada para arredondar um número para o inteiro mais próximo?

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`

