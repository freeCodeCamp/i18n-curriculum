---
id: 6a04bbc9815e737ed711622e
title: Quiz sobre manejo de errores en Express
challengeType: 8
dashedName: quiz-error-handling-in-express
---

# --description--

Para aprobar el quiz, debes responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué significa el código de estado HTTP 201?

#### --distractors--

La solicitud tuvo éxito sin cuerpo de respuesta.

---

El cliente no está autorizado para acceder al recurso.

---

El recurso solicitado no existe.

#### --answer--

Se creó un recurso nuevo con éxito.

### --question--

#### --text--

¿Qué código de estado indica que la solicitud tuvo éxito pero no hay cuerpo de respuesta?

#### --distractors--

`200`

---

`201`

---

`301`

#### --answer--

`204`

### --question--

#### --text--

¿Qué le indica al cliente el código de estado `304 Not Modified`?

#### --distractors--

El recurso se ha movido a una URL permanente nueva.

---

El servidor está temporalmente no disponible por mantenimiento.

---

Se requiere autenticación para acceder al recurso.

#### --answer--

Nada ha cambiado, por lo que el cliente puede usar su versión en caché.

### --question--

#### --text--

¿Cuál es la diferencia entre los códigos de estado `401` y `403`?

#### --distractors--

`401` significa que el recurso no se encontró mientras `403` significa que el servidor está caído.

---

`401` significa que la solicitud estaba mal formada mientras `403` significa que el recurso se movió.

---

`401` significa que el servidor se cayó mientras `403` significa que la solicitud agotó el tiempo.

#### --answer--

`401` significa que se requiere autenticación mientras `403` significa autenticado pero sin permiso.

### --question--

#### --text--

¿Qué código de estado indica que el servidor está temporalmente sobrecargado o en mantenimiento?

#### --distractors--

`500`

---

`502`

---

`301`

#### --answer--

`503`

### --question--

#### --text--

¿Qué hace `res.status(200).json({ message: "Success" })` en Express?

#### --distractors--

Envía una respuesta `200` con un mensaje en texto plano.

---

Envía una respuesta `200` sin cuerpo.

---

Redirecciona al cliente a la página de éxito.

#### --answer--

Envía una respuesta `200` con un cuerpo JSON.

### --question--

#### --text--

¿Qué hace que Express reconozca una función middleware como manejador de errores?

#### --distractors--

Está colocada al inicio de la pila de middleware.

---

Usa `res.error()` en lugar de `res.send()`.

---

Está definida dentro de un manejador de ruta.

#### --answer--

Tiene los parámetros `err`, `req`, `res` y `next`.

### --question--

#### --text--

¿Dónde debe colocarse el middleware para manejo de errores en una app Express?

#### --distractors--

Antes de todas las definiciones de ruta.

---

En la parte superior del archivo, antes de cualquier importación.

---

Dentro del primer manejador de ruta que pueda lanzar un error.

#### --answer--

Al final de la pila de middleware, después de todas las rutas.

### --question--

#### --text--

En Express 4, ¿qué debes hacer para manejar errores asíncronos en un manejador de ruta?

#### --distractors--

Lanzar el error directamente desde el manejador de ruta y dejar que Node.js lo maneje a nivel de proceso.

---

Devolver el objeto error como cuerpo JSON con código de estado 500.

---

Usar `process.on('uncaughtException')` para capturar globalmente todos los errores asíncronos no manejados.

#### --answer--

Encapsular la lógica asíncrona en `try/catch` y llamar a `next(err)` para pasar el error al manejador de errores.

### --question--

#### --text--

¿Cuál es el propósito de una función wrapper `asyncHandler` en Express 4?

#### --distractors--

Convierte automáticamente manejadores de ruta síncronos en asíncronos.

---

Reinicia automáticamente el servidor y limpia el estado cuando ocurre un error.

---

Reemplaza el middleware para manejo de errores colocado al final de la pila de middleware.

#### --answer--

Evita repetir `try/catch` en cada ruta al pasar promesas rechazadas al `next`.

### --question--

#### --text--

¿Cómo maneja Express 5 los errores asíncronos de forma diferente a Express 4?

#### --distractors--

Express 5 requiere agregar un listener global `process.on('unhandledRejection')` para capturar errores asíncronos.

---

Express 5 ignora silenciosamente los errores asíncronos y solo los registra en consola sin detener la app.

---

Express 5 requiere que cada ruta asíncrona defina su propio middleware dedicado para manejo de errores.

#### --answer--

Express 5 pasa automáticamente errores asíncronos y promesas rechazadas al manejador de errores sin necesitar `next(err)`.

### --question--

#### --text--

¿Dónde debe colocarse un middleware catch-all `404` en una app Express?

#### --distractors--

Antes de todas las demás rutas, al inicio del archivo.

---

Dentro del middleware para manejo de errores, como respaldo.

---

Al final, después del middleware para manejo de errores.

#### --answer--

Después de todas las rutas pero antes del middleware para manejo de errores.

### --question--

#### --text--

¿Cuál de las siguientes es una buena práctica para el manejo de errores en apps Express en producción?

#### --distractors--

Siempre enviar la traza completa de la pila al cliente para facilitar la depuración.

---

Colocar el middleware para manejo de errores antes de las rutas para un procesamiento más rápido.

---

Manejar todos los errores, incluyendo 404, dentro de un solo middleware.

#### --answer--

Registrar detalles del error internamente y devolver un mensaje amigable en lugar de enviar trazas a los usuarios.

### --question--

#### --text--

¿Cómo habilitas todos los mensajes integrados de depuración de Express al iniciar tu app?

#### --distractors--

Pasar `{ debug: true }` al constructor `express()`.

---

Llamar a `app.enable('debug')` antes de definir rutas.

---

Agregar `require('express-debug')` al inicio de tu archivo principal.

#### --answer--

Configurar la variable de entorno `DEBUG` a `express:*` al ejecutar la app.

### --question--

#### --text--

¿Qué hace `DEBUG=express:router node index.js`?

#### --distractors--

Habilita toda la salida de depuración de Express, incluyendo middleware, enrutamiento y eventos a nivel de aplicación.

---

Deshabilita toda la salida de depuración y ejecuta la app en modo producción silencioso.

---

Registra solo detalles de solicitudes HTTP entrantes como método y URL en la terminal.

#### --answer--

Muestra solo la salida de depuración relacionada con el enrutador, filtrando otros espacios de nombres de Express.

### --question--

#### --text--

¿Para qué se usa `morgan` en una aplicación Express?

#### --distractors--

Capturar y pasar automáticamente errores asíncronos en manejadores de ruta Express a `next`.

---

Servir archivos estáticos desde un directorio especificado sin definir rutas individuales.

---

Conectar Express a una base de datos relacional y registrar todas las consultas ejecutadas.

#### --answer--

Registrar solicitudes HTTP con detalles como método, URL, código de estado y tiempo de respuesta.

### --question--

#### --text--

¿Cuál es el propósito de una ruta de verificación de estado en una app Express?

#### --distractors--

Reiniciar automáticamente el servidor y redirigir tráfico cuando el volumen de solicitudes entrantes es muy alto.

---

Registrar y pasar todas las solicitudes HTTP entrantes a un servicio externo de monitoreo o análisis.

---

Inspeccionar y bloquear solicitudes de direcciones IP que no están en una lista blanca aprobada.

#### --answer--

Informar si la app está funcionando correctamente para que balanceadores de carga y orquestadores decidan si el servicio está saludable.

### --question--

#### --text--

¿Qué indica la señal `SIGTERM` a un proceso Node.js?

#### --distractors--

Reiniciar inmediatamente, limpiar todo el estado en memoria y re-inicializar todas las conexiones activas.

---

Volcar una instantánea completa de memoria en disco para análisis y depuración posteriores.

---

Forzar la terminación de todas las conexiones activas a la base de datos y cerrar todos los descriptores de archivo abiertos de inmediato.

#### --answer--

Comenzar el apagado, dando a la app la oportunidad de terminar el trabajo existente antes de salir.

### --question--

#### --text--

¿Qué hace llamar a `server.close()` durante un apagado ordenado?

#### --distractors--

Termina inmediatamente todas las conexiones activas y sale del proceso.

---

Envía una respuesta 500 a cualquier cliente conectado actualmente.

---

Elimina todos los datos de sesión en memoria y reinicia el servidor.

#### --answer--

Deja de aceptar nuevas conexiones mientras permite que las existentes terminen.

### --question--

#### --text--

¿Por qué devolverías un estado 503 desde tu ruta de verificación de estado durante un apagado ordenado?

#### --distractors--

Para registrar el evento de apagado y la marca de tiempo en un panel externo de monitoreo para auditoría.

---

Para forzar al orquestador a iniciar inmediatamente una nueva instancia de reemplazo antes de que termine el apagado.

---

Para notificar a todos los clientes conectados que sus sesiones activas se guardaron correctamente.

#### --answer--

Para indicar al balanceador de carga que la app no está disponible y que deje de redirigirle tráfico nuevo.
