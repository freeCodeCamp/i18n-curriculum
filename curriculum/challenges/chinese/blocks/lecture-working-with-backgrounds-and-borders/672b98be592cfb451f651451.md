---
id: 672b98be592cfb451f651451
title: 什么是背景渐变，它是如何工作的？
challengeType: 19
dashedName: what-is-a-background-gradient
---

# --interactive--

CSS 中的背景渐变是在元素背景上应用的两种或多种颜色之间的平滑过渡。渐变允许你创建视觉上吸引人的背景，而无需使用图像。

CSS 中有两种主要的渐变 `type` ：线性渐变和径向渐变。

线性渐变沿直线过渡颜色。你可以定义方向和涉及的颜色。

这是基本语法：

```css
background: linear-gradient(direction, color-stop1, color-stop2, ...);
```

在此示例中，我们使用了 `background` CSS 属性，其值为 `linear gradient`。

方向指定渐变的方向。它可以是角度（例如 `45deg`）、关键字（例如 `to right`、`to bottom`）或边/角。

`color-stop` 指定渐变过渡发生的颜色和位置。

为了更好地理解线性渐变的工作原理，让我们来看以下示例：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="linear-gradient"></div>
```

```css
.linear-gradient{
  background: linear-gradient(to right, red, yellow);
  height: 40vh;
}
```

:::

此 CSS 创建了一个线性渐变，从 `left` 方向的 `red` 过渡到 `right` 方向的 `yellow`。该渐变应用于一个高度为视口高度 `40%` 的元素。你将在后续课程中学习更多关于 `vh` 单位的内容。

`to right` 方向表示渐变从左向右水平运行。

另一种类型的渐变是 `radial` 渐变。

径向渐变从一个起点（通常是中心）向外以圆形或椭圆形的形状辐射颜色过渡。

这是基本语法：

```css
background: radial-gradient(shape size at position, color-stop1, color-stop2, ...)
```

在语法中，`shape` 指定渐变的形状，可以是 `circle` 或 `ellipse`。

`size` 决定渐变结束形状的大小，该形状可以是 `closest-side`、`closest-corner`、`farthest-side` 或 `farthest-corner`。

`position` 决定渐变中心的位置，可以使用关键字（例如 `center`、`top left`、`bottom right`）或精确数值（例如 `50% 50%`、`10px 20px`）来指定。

最后，颜色停靠点是渐变过渡的颜色列表。每个颜色停靠点可以可选地包含一个位置值（百分比或长度），指示颜色应放置的位置。

一个示例是：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="radial-gradient"></div>
```

```css
.radial-gradient{
  background: radial-gradient(circle closest-side at center, red, yellow 50%, green);
  height: 60vh;
}
```

:::

此 CSS 创建一个以元素中心为圆心的径向渐变。它从中心的 `red` 开始，过渡到半径的 `50%` 处的 `yellow`，并以 `green` 结束。

`closest-side` 关键字使渐变的结束形状适合元素的最近一侧。渐变应用于高度为视口高度 `60%` 的元素。

理解如何使用 CSS 渐变可以显著提升你的设计，通过提供视觉上吸引人的背景而无需使用图像。

通过线性渐变实现平滑过渡和径向渐变实现圆形效果等选项，它们在网页设计中提供了灵活性和创造力。

# --questions--

## --text--

`radial-gradient()` 函数中的 `circle` 关键字指定什么？

## --answers--

梯度将具有平方形状。

### --feedback--

考虑使用 `circle` 关键字时渐变的形状。

---

渐变将沿对角线方向延伸。

### --feedback--

考虑使用 `circle` 关键字时渐变的形状。

---

梯度将具有圆形。

---

渐变将应用到文本。

### --feedback--

考虑使用 `circle` 关键字时渐变的形状。

## --video-solution--

3

## --text--

`radial-gradient()` 函数的 size 参数中的 `farthest-corner` 关键字有什么作用？

## --answers--

它使渐变延伸到元素的最近角落。

### --feedback--

考虑渐变的大小如何影响其相对于元素的覆盖范围。

---

它使渐变覆盖整个元素的 `width`。

### --feedback--

考虑渐变的大小如何影响其相对于元素的覆盖范围。

---

它使渐变延伸到元素的最远角落。

---

它使渐变仅出现在元素的 `padding` 内。

### --feedback--

考虑渐变的大小如何影响其相对于元素的覆盖范围。

## --video-solution--

3

## --text--

在 `radial-gradient(circle at center, red, yellow 50%, green)` 函数中，渐变的中心位于哪里？

## --answers--

在元素的右上角。

### --feedback--

考虑函数中 at center 部分指定的位置。

---

在元素的左下角。

### --feedback--

考虑函数中 at center 部分指定的位置。

---

在元素的中心。

---

在元素的外缘。

### --feedback--

考虑函数中 at center 部分指定的位置。

## --video-solution--

3
