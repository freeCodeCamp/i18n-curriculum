---
id: 66ed9009f45ce3ece4053eb7
title: Quiz sulle unità relative e assolute in CSS
challengeType: 8
dashedName: quiz-css-relative-and-absolute-units
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos’è un’unità assoluta?

#### --distractors--

Unità che si adattano dinamicamente in base alla dimensione dello schermo.

---

Unità che dipendono dalla dimensione del carattere tipografico del genitore.

---

Unità misurate in percentuale del viewport.

#### --answer--

Unità a lunghezza fissa che non sono relative a nient’altro.

### --question--

#### --text--

Cosa copre `100vw` in termini di larghezza dello schermo?

#### --distractors--

`100%` della larghezza dello schermo.

---

`100%` dell’altezza del viewport.

---

`100%` della larghezza del genitore.

#### --answer--

`100%` della larghezza del viewport.

### --question--

#### --text--

Quale delle seguenti è un’unità relativa?

#### --distractors--

`pt`

---

`cm`

---

`mm`

#### --answer--

`em`

### --question--

#### --text--

Se la dimensione del carattere tipografico radice è `20px`, a cosa è uguale `1rem`?

#### --distractors--

`18px`

---

`16px`

---

`10px`

#### --answer--

`20px`

### --question--

#### --text--

Cosa può fare la funzione `calc()` in CSS?

#### --distractors--

Modificare i colori dinamicamente.

---

Impostare una larghezza fissa.

---

Funzionare solo con percentuali.

#### --answer--

Aggiungere, sottrarre, moltiplicare o dividere unità diverse.

### --question--

#### --text--

Qual è l’unità assoluta più comunemente usata in CSS?

#### --distractors--

`inch`

---

`rem`

---

`%`

#### --answer--

`px`

### --question--

#### --text--

Se l’altezza del viewport è `800px`, cosa sarebbe `50vh`?

#### --distractors--

`50px`

---

`200px`

---

`100px`

#### --answer--

`400px`

### --question--

#### --text--

Cosa significa impostare la larghezza di un elemento a `100%`?

#### --distractors--

Occupa tutta la larghezza del documento.

---

Occupa tutta la larghezza dello schermo.

---

Occupa `100px`.

#### --answer--

Occupa tutta la larghezza del suo genitore.

### --question--

#### --text--

Cosa succede con `calc(10px + 20%)` in CSS?

#### --distractors--

Aggiunge `10px` a `10%` della larghezza del documento.

---

È sempre uguale a `30px`.

---

Aggiunge `10px` a `20%` della larghezza dello schermo.

#### --answer--

Aggiunge `10px` a `20%` della larghezza del genitore.

### --question--

#### --text--

Quale unità è relativa alla dimensione del carattere tipografico dell’elemento radice?

#### --distractors--

`px`

---

`pt`

---

`%`

#### --answer--

`rem`

### --question--

#### --text--

Con una dimensione del carattere radice di `16px`, quanto è grande `10rem`?

#### --distractors--

`140px`

---

`10px`

---

`100px`

#### --answer--

`160px`

### --question--

#### --text--

Perché potresti usare `point` invece di `pixels` nel design?

#### --distractors--

I punti sono scalabili.

---

I punti sono migliori per layout responsivi.

---

I punti sono più facili da gestire sugli schermi.

#### --answer--

I punti sono per il design di stampa.

### --question--

#### --text--

Perché le unità `rem` sono preferite rispetto a `pixels` nella tipografia?

#### --distractors--

Perché forniscono dimensioni fisse indipendentemente dalla risoluzione dello schermo.

---

Perché garantiscono compatibilità con browser più vecchi.

---

Perché sono più facili da implementare in design responsivi.

#### --answer--

Perché si scalano proporzionalmente alle impostazioni del browser dell’utente.

### --question--

#### --text--

Quanto spazio sullo schermo occupa `80vw`?

#### --distractors--

`80%` della larghezza del genitore.

---

`100%` dell’altezza del viewport.

---

`80px`

#### --answer--

`80%` della larghezza del viewport.

### --question--

#### --text--

Quali unità dovresti usare se vuoi far occupare a un elemento una certa larghezza o altezza in percentuale dello schermo?

#### --distractors--

`pt` e `px`

---

`rem` e `em`

---

`pt` e `pc`

#### --answer--

`vw` e `vh`

### --question--

#### --text--

Cosa significa `vw`?

#### --distractors--

Larghezza verticale.

---

Larghezza virtuale.

---

Larghezza variabile.

#### --answer--

Larghezza del viewport.

### --question--

#### --text--

Qual è la differenza chiave tra `rem` e `em` in CSS?

#### --distractors--

`em` è relativo all’elemento radice, mentre `rem` è relativo all’elemento genitore.

---

`rem` è relativo alla larghezza desktop, mentre `em` è relativo alla larghezza mobile.

---

`rem` è relativo alla larghezza definita dall’utente, mentre `em` è relativo alla larghezza del browser.

#### --answer--

`em` è relativo alla dimensione del carattere tipografico del genitore, mentre `rem` è relativo all’elemento radice.

### --question--

#### --text--

Cos’è un’unità `vh`?

#### --distractors--

Un’unità basata sulla larghezza del viewport.

---

Un’unità basata sull’altezza dell’elemento.

---

Un’unità che si adatta in base alla dimensione del carattere.

#### --answer--

Un’unità uguale all’1% dell’altezza del viewport.

### --question--

#### --text--

Cosa succede impostando la larghezza di un elemento a `auto` in CSS?

#### --distractors--

Imposta la larghezza a `100%` dell’elemento genitore per impostazione predefinita.

---

Imposta la larghezza a `50%` del viewport a meno che altri stili non la sovrascrivano.

---

Imposta la larghezza a `0px` se non è presente contenuto o spaziatura interna.

#### --answer--

Permette al browser di determinare la larghezza in base al contenuto e al contenitore.

### --question--

#### --text--

Qual è un vantaggio chiave della funzione `calc()`?

#### --distractors--

Minifica automaticamente i file CSS per migliorare le prestazioni.

---

Permette di usare variabili all’interno del CSS senza preprocessori.

---

Permette di incorporare espressioni JavaScript direttamente nelle regole CSS.

#### --answer--

Permette di determinare dinamicamente il valore di una proprietà CSS in base a diversi aspetti dell’applicazione o del viewport.
