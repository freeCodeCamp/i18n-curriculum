---
id: 66edd10913f078e7669eca81
title: JavaScriptのデバッグクイズ
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

変数が定義される前に使用された場合、どのタイプのエラーが発生しますか？

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

デバッグ目的でコードの実行を意図的に一時停止するために使えるJavaScriptの文はどれですか？

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

データをテーブル形式で表示するために使えるコンソールのメソッドはどれですか？

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

Chromeの開発者ツールでネットワークリクエストを調査するにはどうすればよいですか？

#### --distractors--

「Network Links」タブを使う。

---

「Console」タブを使う。

---

「Sources」タブを使う。

#### --answer--

「Network」タブを使う。

### --question--

#### --text--

JavaScriptで`try...catch`を使う正しい構文はどれですか？

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

次のうち`SyntaxError`の例はどれですか？

#### --distractors--

変数を宣言する前に使う。

---

同じスコープ内で変数を2回使う。

---

スコープ外の変数を使う。

#### --answer--

配列の要素間でカンマが抜けている。

### --question--

#### --text--

JavaScriptのデバッグでブレークポイントの目的は何ですか？

#### --distractors--

コードのミスを自動的に修正する。

---

コードの実行を防ぐ。

---

コンソールにメッセージを記録する。

#### --answer--

特定のポイントで実行を一時停止する。

### --question--

#### --text--

`throw`文が実行されると何が起こりますか？

#### --distractors--

プログラムはすぐに終了し、残りのコードは無視される。

---

throw文はエラーをコンソールに記録するが、実行は止まらない。

---

ブレークポイントを無効にするまでコードの実行が一時停止する。

#### --answer--

例外がスローされ、`catch`ブロック（存在する場合）が実行される。

### --question--

#### --text--

開発者ツールでデバッグ中に変数や式を調査するために使うメソッドはどれですか？

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

次のコードを実行するとどのタイプのエラーが発生しますか？

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

スタックオーバーフローエラー

#### --answer--

`ReferenceError`

### --question--

#### --text--

デバッグ中にコードを1行ずつ実行するためによく使われるツールはどれですか？

#### --distractors--

`console.log()`

---

`alert()`

---

`throw`文。

#### --answer--

開発者ツールのブレークポイント。

### --question--

#### --text--

`undefined`のプロパティにアクセスしようとするとどのタイプのエラーが発生しますか？

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

`finally`文の`try...catch`ブロックは何をしますか？

#### --distractors--

エラーがある場合のみ実行される。

---

`catch`が発生した場合は実行をスキップする。

---

コードブロックをすぐに終了する。

#### --answer--

エラーの有無にかかわらず常に実行される。

### --question--

#### --text--

次のコードを実行した結果はどうなりますか？

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

コードは`SyntaxError`をスローする。

---

コードは空の配列をコンソールに記録する。

---

コードはメモリ不足エラーをスローする。

#### --answer--

コードは`RangeError`をスローする。

### --question--

#### --text--

JavaScriptの`debugger`文について正しいのはどれですか？

#### --distractors--

`debugger`文は常にブラウザに無視される。

---

`debugger`文はエラーレポートをサーバーに送信する。

---

`debugger`文は常にページをリロードし、コンソールをクリアする。

#### --answer--

`debugger`文は開発者ツールが開いていないと無視される。

### --question--

#### --text--

JavaScriptにおける「プロファイリング」の主な目的は何ですか？

#### --distractors--

JavaScript関数の単体テストを書き、コード実行のプロファイルを作成すること。

---

コードの実行を一時停止し、メモリやCPUなどのリソースを解放すること。

---

JavaScriptコードを機械語に変換して高速化すること。

#### --answer--

CPU使用率、関数呼び出し、実行時間を記録してパフォーマンスのボトルネックを特定すること。

### --question--

#### --text--

デバッグの文脈で「ウォッチャー」とは何ですか？

#### --distractors--

コードの実行を一時停止するツール。

---

エラーを記録する組み込み関数。

---

例外処理を行うJavaScriptメソッド。

#### --answer--

変数の値を監視するツール。

### --question--

#### --text--

`console.error()`を`console.log()`の代わりに使うのはどんな場合ですか？

#### --distractors--

テーブルデータを表示するため。

---

デバッグ中のみメッセージを記録するため。

---

ブラウザに関する情報を記録するため。

#### --answer--

エラーメッセージを記録するため。

### --question--

#### --text--

JavaScriptの`Error()`コンストラクタは何をしますか？

#### --distractors--

エラーメッセージをコンソールに記録する。

---

サンドボックス内でコードを実行する。

---

プログラムを終了する。

#### --answer--

スロー可能な新しいエラーオブジェクトを作成する。

### --question--

#### --text--

JavaScriptの`console.dir()`メソッドの目的は何ですか？

#### --distractors--

個別のエラータイプごとにログファイル用の別ディレクトリを作成する。

---

コンソール出力の視覚的表現を生成し、pdfファイルに保存する。

---

プログラムの実行を停止し、メモリやCPU使用に関するエラーメッセージを表示する。

#### --answer--

指定したJavaScriptオブジェクトのプロパティを階層的かつインタラクティブに一覧表示する。
