---
id: 5900f52c1000cf542c51003e
title: "المشكلة 447: الانسحابات C"
challengeType: 1
forumTopicId: 302119
dashedName: problem-447-retractions-c
---

# --description--

لكل عدد صحيح $n > 1$، تُعرّف عائلة الدوال $f_{n, a, b}$ كما يلي:

$f_{n, a, b}(x) ≡ ax + b\bmod n$ حيث $a, b, x$ أعداد صحيحة و $0 \lt a \lt n$، $0 \le b \lt n$، $0 \le x \lt n$.

نسمي $f_{n, a, b}$ انسحابًا إذا تحقق $f_{n, a, b}(f_{n, a, b}(x)) \equiv f_{n, a, b}(x)\bmod n$ لكل $0 \le x \lt n$.

لتكن $R(n)$ عدد الانسحابات لـ $n$.

$F(N) = \displaystyle\sum_{n = 2}^N R(n)$.

$F({10}^7) ≡ 638\\,042\\,271\bmod 1\\,000\\,000\\,007$.

أوجد $F({10}^{14})$. أعطِ جوابك باقي القسمة على $1\\,000\\,000\\,007$.

# --hints--

`retractionsC()` يجب أن تُعيد `530553372`.

```js
assert.strictEqual(retractionsC(), 530553372);
```

# --seed--

## --seed-contents--

```js
function retractionsC() {

  return true;
}

retractionsC();
```

# --solutions--

```js
// solution required
```
