---
id: 66edcd49e73385dd4df54ac7
title: اختبار حلقات JavaScript
challengeType: 8
dashedName: quiz-javascript-loops
---

# --description--

لتجاوز الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي مما يلي يصف التكرار بشكل أفضل؟

#### --distractors--

هي عملية كتابة الشفرة بشكل متكرر حتى تعمل بشكل صحيح.

---

هي تقنية لتخزين البيانات في مواقع ذاكرة متتالية.

---

هي عملية إرسال إشارات بين المكوّنات لإنشاء اتصال.

#### --answer--

هي عملية تكرار مجموعة من التعليمات عدة مرات.

### --question--

#### --text--

ما التقنية المستخدمة للتكرار في البرمجة؟

#### --distractors--

الاستدعاء الذاتي

---

الشروط

---

الترجمة

#### --answer--

التكرار

### --question--

#### --text--

أي مما يلي هو كائن قابل للتكرار؟

#### --distractors--

غير معرف

---

رقم

---

قيمة منطقية

#### --answer--

سلسلة نصية

### --question--

#### --text--

أي مما يلي ليس نوعًا من الحلقات المدعومة في JavaScript؟

#### --distractors--

حلقة `for`.

---

حلقة `while`.

---

حلقة `for...in`.

#### --answer--

حلقة `if...else`.

### --question--

#### --text--

ما هو الترتيب الصحيح لإعلان حلقة `for`؟

#### --distractors--

```javascript
for (condition; increment/decrement; initialization) {
  statement;
}
```

---

```javascript
for (increment/decrement; condition; initialization) {
  statement;
}
```

---

```javascript
for (initialization; increment/decrement; condition) {
  statement;
}
```

#### --answer--

```javascript
for (initialization; condition; increment/decrement) {
  statement;
}
```

### --question--

#### --text--

أي حلقة تنفذ كتلة الشفرة مرة واحدة، قبل التحقق مما إذا كان الشرط صحيحًا، وتستمر في العمل طالما ظل الشرط صحيحًا؟

#### --distractors--

حلقة `while`.

---

حلقة `for...of`.

---

حلقة `for...in`.

#### --answer--

حلقة `do...while`.

### --question--

#### --text--

أي مما يلي سيكرر القيم لكائن قابل للتكرار؟

#### --distractors--

حلقة `for...in`.

---

حلقة `for`.

---

حلقة `do...while`.

#### --answer--

حلقة `for...of`.

### --question--

#### --text--

أي مما يلي يعرّف الحلقة اللانهائية بشكل أفضل؟

#### --distractors--

حلقة تعمل حتى يصبح الشرط خاطئًا.

---

حلقة تعمل مرة واحدة ثم تُنهى.

---

حلقة تتوقف تلقائيًا بعد عدد ثابت من التكرارات.

#### --answer--

حلقة لا يتحقق فيها شرط الإنهاء أبدًا أو يكون غائبًا.

### --question--

#### --text--

كم مرة ستعمل الحلقة التالية؟

```js
for (let i = 2; i < 10; i+=2) {  
  console.log(i);  
}
```

#### --distractors--

5

---

9

---

10

#### --answer--

4

### --question--

#### --text--

أي حلقة هي الأفضل لتكرار الكائنات؟

#### --distractors--

حلقة `for...of`.

---

حلقة `do...while`.

---

حلقة `for`.

#### --answer--

حلقة `for...in`.

### --question--

#### --text--

ما الفرق بين حلقة `for...in` وحلقة `for...of`؟

#### --distractors--

حلقة `for...in` تكرر أسماء الخصائص (المفاتيح)، بينما حلقة `for...of` تكرر قيم الخصائص.

---

حلقات `for...in` تنشئ خصائص جديدة، بينما حلقات `for...of` تعدل الخصائص الموجودة.

---

حلقات `for...in` يمكن استخدامها فقط مع السلاسل النصية، بينما حلقات `for...of` يمكن استخدامها مع السلاسل النصية والأرقام.

#### --answer--

حلقة `for...in` تكرر أسماء الخصائص (المفاتيح)، بينما حلقة `for...of` تكرر قيم الخصائص.

### --question--

#### --text--

أي مما يلي قد يسبب حلقة لانهائية؟

#### --distractors--

تهيئة المتغير.

---

شرط حلقة ينتهي بإرجاع false في النهاية.

---

منطق زيادة/نقصان.

#### --answer--

شرط حلقة يعيد true دائمًا.

### --question--

#### --text--

أي عبارة يمكن استخدامها للخروج تمامًا من حلقة؟

#### --distractors--

عبارة `continue`.

---

عبارة `label`.

---

عبارة `skip`.

#### --answer--

عبارة `break`.

### --question--

#### --text--

أي عبارة يمكن استخدامها لتخطي التكرار الحالي والانتقال إلى التكرار التالي؟

#### --distractors--

عبارة `break`.

---

عبارة `label`.

---

عبارة `return`.

#### --answer--

عبارة `continue`.

### --question--

#### --text--

ماذا يُطبع في وحدة التحكم مع الشفرة التالية؟

```js
for (let i = 0; i < 25; i += 2) {
  if (i % 5 === 0) {
    continue;
  }
  if (i % 13 === 0) {
    break;
  }
  console.log(i);
}
```

#### --distractors--

`0`, `2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

---

`0`, `2`, `4`, `6`, `8`, `12`

---

`2`, `4`, `6`, `8`, `12`

#### --answer--

`2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

### --question--

#### --text--

ماذا ستطبع الشفرة التالية في وحدة التحكم؟

```javascript
for (let i = 1; i < 6; i++) {
  if (i === 4) break;
  console.log(i);
}
```

#### --distractors--

`1`, `2`, `3`, `4`, `5`, `6`

---

`1`, `2`, `3`, `4`, `5`

---

`1`, `2`, `3`, `4`

#### --answer--

`1`, `2`, `3`

### --question--

#### --text--

ماذا سيُطبع في وحدة التحكم مع الشفرة أدناه؟

```javascript
const shoppingList = { tomatoes: 4, apples: 10 };
for (const item in shoppingList) {
  console.log(item);
}
```

#### --distractors--

`tomatoes - 4`, `apples - 10`

---

`4`, `10`

---

`tomatoes:4`, `apples:10`

#### --answer--

`tomatoes`, `apples`

### --question--

#### --text--

ما هو ناتج وحدة التحكم للشفرة أدناه؟

```javascript
for (let i = 2; i <= 13; i++) {
  if (i % 2 === 0) continue;
  console.log(i);
}
```

#### --distractors--

`2`, `4`, `6`, `8`, `10`, `12`

---

`1`, `3`, `5`, `7`, `9`, `11`, `13`

---

`1`, `3`, `5`, `7`, `9`, `11`

#### --answer--

`3`, `5`, `7`, `9`, `11`, `13`

### --question--

#### --text--

ماذا سيُطبع في وحدة التحكم مع الشفرة أدناه؟

```javascript
const fruits = ["Mango", "Pineapple", "Oranges"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

#### --distractors--

`0`, `1`, `2`

---

`Pineapple`, `Oranges`, `Mango`

---

`Oranges`, `Pineapple`, `Mango`

#### --answer--

`Mango`, `Pineapple`, `Oranges`

### --question--

#### --text--

كم مرة ستطبع الشفرة أدناه السلسلة النصية `in the loop`؟

```javascript
let x = 0
while (x < 5) {
  console.log("in the loop");
}
```

#### --distractors--

3 مرات

---

4 مرات

---

5 مرات

#### --answer--

عدد لا نهائي من المرات

