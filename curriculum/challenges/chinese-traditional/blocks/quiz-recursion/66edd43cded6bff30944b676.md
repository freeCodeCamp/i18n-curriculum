---
id: 66edd43cded6bff30944b676
title: 遞迴小測驗
challengeType: 8
dashedName: quiz-recursion
---

# --description--

要通過測驗，你必須正確回答以下 10 題中的至少 9 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是程式設計中的遞迴？

#### --distractors--

一種排序陣列的方法。

---

一個永不結束的迴圈。

---

一個傳回 `undefined` 的函式（程式）。

#### --answer--

一個函式呼叫自身的處理程序。

### --question--

#### --text--

以下哪一項是遞迴的範例？

#### --distractors--

```js
function factorial(n) {
  let result = 1;

  while (n > 0) {
    result = result * n;
    n--;
  }

  return result;
}
```

---

```js
function factorial(n) {
  const arr = Array(n).fill().map((_, i) => i + 1);
  return arr.reduce((acc, curr) => acc * curr, 1);
}
```

---

```js
function factorial(n) {
  let result = 1;

  for (let i = n; i > 0; i--) {
    result *= i;
  }

  return result;
}
```

#### --answer--

```js
function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  }
  return n * factorial(n - 1);
}
```

### --question--

#### --text--

以下函式會傳回什麼？

```js
function sum(n) {
  if (n === 0) return 0;
  return n + sum(n - 1);
}
sum(3);
```

#### --distractors--

3

---

0

---

1

#### --answer--

6

### --question--

#### --text--

`mystery` 函式（程式）會被呼叫多少次？

```javascript
function mystery(n) {
  if (n <= 1) return 1;
  return mystery(n - 2);
}
mystery(5);
```

#### --distractors--

2

---

5

---

4

#### --answer--

3

### --question--

#### --text--

以下哪一項關於遞迴是正確的？

#### --distractors--

它絕不應該有基底案例。

---

它應該只用於 Fibonacci 數列。 

---

它應該只用於階乘。

#### --answer--

它應該始終有一個基本情況。

### --question--

#### --text--

以下哪一個選項，可作為給定範例的適當基底案例？

```js
function countDownToZero(number) {
  // Base case goes here.

  console.log(number);
  countDownToZero(number - 1);
}
```

#### --distractors--

```js
if (number > 0) {
  return;
}
```

---

```js
if (number !== 0) {
  return;
}
```

---

```js
if (number === 0) {
  return;
}
```

#### --answer--

```js
if (number < 0) {
  return;
}
```

### --question--

#### --text--

為什麼遞迴必須有基底情況？

#### --distractors--

為了確保函式（程式）總是有返回值。

---

為了減少函式（程式）呼叫的次數。

---

為了確保所有遞迴呼叫都依正確順序執行，且不會發出 `RecursionError`。

#### --answer--

提供一種方式讓函式跳出其遞迴呼叫並防止無窮迴圈。

### --question--

#### --text--

這個遞迴函式（程式）會做什麼？

```js
function repeatString(str) {
  return str + repeatString(str);
}
```

#### --distractors--

傳回該字串兩次。

---

傳回 `undefined`。

---

創建一個空字串。

#### --answer--

造成堆疊上限溢位(相對於 underflow)。

### --question--

#### --text--

什麼是呼叫堆疊？

#### --distractors--

已執行的函式呼叫列表。

---

由函式傳回的值的列表。

---

一個用來精確呼叫函式三次的特殊函式。

#### --answer--

一個追蹤函式（程式）呼叫及其執行順序的資料結構。

### --question--

#### --text--

這個遞迴函式（程式）做什麼？

```js
function changeString(str) {
  if (str === "") return "";
  return changeString(str.slice(1)) + str[0];
}
```

#### --distractors--

複製字串。

---

移除母音。

---

移除空格。

#### --answer--

反轉字串。

