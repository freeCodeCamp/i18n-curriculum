---
id: 672bb015cfc889794359c4e0
title: モーダルダイアログをデザインする際のベストプラクティスとは？
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**注意**: いくつかのインタラクティブな例では、まだ学んでいないCSSやJavaScriptが使われている場合があります。すべてのコードを理解しようと心配しないでください。これらの例の目的は、デザインの概念をプレビューで示し、仕組みをよりよく理解してもらうことです。プレビューを見るには、インタラクティブエディターを有効にしてください。

モーダルとは何でしょうか？ウェブサイトがコンテンツの上に表示するポップアップの一種です。HTMLにはモーダルを作成するために使える`dialog`要素があります。

モーダルの背後のコンテンツは通常、薄暗くなっています。これはユーザーが操作してほしいエリア、つまりこの場合はモーダルに視覚的にフォーカスできるようにするためです。

モーダルの外側をクリックして閉じられるようにするのは常に良いアイデアです。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

モーダルには非常に目立つボタンがよくあります。これらはCTA（コールトゥアクション）と呼ばれます。モーダルでユーザーの流れを中断する目的は特定の行動を促すことなので、これらのボタンは簡単に識別できるようにしたいです。

モーダルには閉じるボタンも必要です。ユーザーにCTAをクリックしてほしい気持ちはあっても、モーダルから抜けて以前の作業を再開できる選択肢を与えることが重要です。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

もちろん、モーダルにはアクセシビリティの問題もあります。例えば、要素のフォーカスを正しく管理することなどです。しかし、これらの一般的なプラクティスを出発点にすれば、しっかりした基盤を築けます。

# --questions--

## --text--

モーダルダイアログの背後のコンテンツを薄暗くする目的は何ですか？

## --answers--

モバイルデバイスのバッテリー寿命を節約するため。

### --feedback--

レッスンではこの視覚効果の具体的な理由が述べられています。

---

ユーザーが操作すべきエリアを視覚的に識別しやすくするため。

---

ウェブサイトの読み込みを速くするため。

### --feedback--

レッスンではこの視覚効果の具体的な理由が述べられています。

---

ユーザーが機密情報を見ないようにするため。

### --feedback--

レッスンではこの視覚効果の具体的な理由が述べられています。

## --video-solution--

2

## --text--

モーダルデザインにおけるCTAとは何の略ですか？

## --answers--

Click To Activate.

### --feedback--

レッスンではモーダルの目立つボタンについてこの略語の正式名称が示されています。

---

Complete The Action.

### --feedback--

レッスンではモーダルの目立つボタンについてこの略語の正式名称が示されています。

---

Call-to-Action.

---

Close The Application.

### --feedback--

レッスンではモーダルの目立つボタンについてこの略語の正式名称が示されています。

## --video-solution--

3

## --text--

モーダルダイアログに閉じるボタンを含めることが重要な理由は何ですか？

## --answers--

一部の国では法律で義務付けられているため。

### --feedback--

レッスンではユーザーがモーダルから抜けられることの重要性が強調されています。

---

モーダルがブラウザをクラッシュさせるのを防ぐため。

### --feedback--

レッスンではユーザーがモーダルから抜けられることの重要性が強調されています。

---

ユーザーにモーダルを終了して以前の作業を再開する選択肢を与えるため。

---

モーダルをよりプロフェッショナルに見せるため。

### --feedback--

レッスンではユーザーがモーダルから抜けられることの重要性が強調されています。

## --video-solution--

3
