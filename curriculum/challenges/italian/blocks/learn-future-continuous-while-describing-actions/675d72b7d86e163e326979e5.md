---
id: 675d72b7d86e163e326979e5
title: Attività 56
challengeType: 22
dashedName: task-56
lang: en-US
---

<!-- (audio) Mark: Sure. We can do some tests on the staging server, can't we? -->

# --instructions--

Ascolta l’audio e completa la frase qui sotto.

# --fillInTheBlank--

## --sentence--

`Sure. We can do some tests on the BLANK, BLANK we?`

## --blanks--

`staging server`

### --feedback--

Questa frase di due parole indica un ambiente separato usato per testare le modifiche prima che vengano messe in produzione. Rispecchia il server di produzione per assicurarsi che gli aggiornamenti funzionino come previsto.

---

`can't`

### --feedback--

Fa parte di una domanda tag, usata qui per confermare o verificare l’accordo dopo un’affermazione positiva principale che usa `can`.

# --explanation--

`Staging server` indica un ambiente di test dove si provano le modifiche a un sistema o a un’applicazione prima di implementarle sul server di produzione attivo. Per esempio:

`We uploaded the new website features to the staging server for testing.` – In questa frase, `staging server` è lo spazio di test usato per assicurarsi che le nuove funzionalità del sito web funzionino correttamente prima di andare in produzione.

`Can't` in questo contesto fa parte di una domanda tag. Segue un’affermazione positiva per confermare o verificare l’accordo in modo cortese o colloquiale. Per esempio:

`We can finish the task today, can't we?` – In questa frase, l’affermazione positiva (`We can finish the task today`) è seguita dalla domanda tag negativa (`can't we?`) per verificare l’accordo o la conferma.

# --scene--

```json
{
  "setup": {
    "background": "company2-center.png",
    "characters": [
      {
        "character": "Mark",
        "position": {
          "x": 50,
          "y": 0,
          "z": 1.4
        },
        "opacity": 0
      }
    ],
    "audio": {
      "filename": "B1_6-2.mp3",
      "startTime": 1,
      "startTimestamp": 36.44,
      "finishTimestamp": 39.76
    }
  },
  "commands": [
    {
      "character": "Mark",
      "opacity": 1,
      "startTime": 0
    },
    {
      "character": "Mark",
      "startTime": 1,
      "finishTime": 4.32,
      "dialogue": {
        "text": "Sure. We can do some tests on the staging server, can't we?",
        "align": "center"
      }
    },
    {
      "character": "Mark",
      "opacity": 0,
      "startTime": 4.82
    }
  ]
}
```
