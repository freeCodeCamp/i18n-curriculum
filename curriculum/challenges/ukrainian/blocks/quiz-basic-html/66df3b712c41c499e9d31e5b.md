---
id: 66df3b712c41c499e9d31e5b
title: Базовий тест з HTML
challengeType: 8
dashedName: quiz-basic-html
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Як працює атрибут `loop` всередині елемента `audio`?

#### --distractors--

Він дозволяє елементу `audio` синхронізувати відтворення з іншими мультимедійними елементами на вебсторінці.

---

Він регулює висоту та тон аудіо.

---

Він запускає елемент `audio` зупинити та перезапустити відтворення у випадковій послідовності.

#### --answer--

Він запускає аудіо знову після його завершення.

### --question--

#### --text--

Що таке void-елемент у HTML?

#### --distractors--

Елемент, який використовується для вставки відео на екран.

---

Елемент для відображення списків.

---

Елемент, який використовується для вставки звуку в документ.

#### --answer--

Елемент без вмісту або закриваючого тегу.

### --question--

#### --text--

Яке з наведених є правильним значенням для атрибута `id`?

#### --distractors--

`id="main@content"`

---

`id="main content"`

---

`id="main~content"`

#### --answer--

`id="main-content"`

### --question--

#### --text--

Як працює `target="_parent"`?

#### --distractors--

Відкриває посилання у новому контексті перегляду.

---

Визначає позицію в пов’язаному документі, куди браузер має прокрутити після натискання посилання.

---

Перенаправляє користувача на інший вебсайт, вказаний у атрибуті `href`.

#### --answer--

Відкриває посилання у батьківському контексті поточного.

### --question--

#### --text--

У чому різниця між булевим і звичайним атрибутом?

#### --distractors--

Булеві атрибути можна використовувати лише з прапорцями, а звичайні — з радіокнопками.

---

Звичайні атрибути завжди потрібні для зображень, а булеві — опціональні.

---

Булеві атрибути завжди повинні мати призначене значення, а звичайні — ні.

#### --answer--

Булевий атрибут може бути присутнім або відсутнім, а звичайний атрибут завжди має вказане значення.

### --question--

#### --text--

Який елемент використовується для підключення скриптів до вашого HTML-файлу?

#### --distractors--

`img`

---

`ul`

---

`h2`

#### --answer--

`script`

### --question--

#### --text--

Що таке шаблонний код HTML?

#### --distractors--

Спеціальний інструмент, який використовують веброзробники для додавання водяних знаків до документів.

---

Тип мови розмітки.

---

Плагін, який автоматично генерує текст "lorem ipsum" на сторінці.

#### --answer--

Шаблон, що містить усю необхідну інформацію для HTML-документа.

### --question--

#### --text--

Який атрибут застосовує унікальний ідентифікатор до HTML-елемента?

#### --distractors--

`class`

---

`href`

---

`action`

#### --answer--

`id`

### --question--

#### --text--

Що таке HTML-сутність (посилання на символ)?

#### --distractors--

Графічне представлення символу в HTML.

---

Спеціальний тег форматування, що використовується в HTML.

---

Зображення, яке використовується для представлення символу у вебдизайні.

#### --answer--

Набір символів, який використовується для представлення зарезервованого символу в HTML.

### --question--

#### --text--

Яка мета елемента `audio`?

#### --distractors--

Він використовується, щоб дозволити користувачам записувати свій голос безпосередньо на вебсторінці.

---

Він використовується для стилізації елементів `audio` та `video` на сторінці.

---

Він використовується, щоб зробити текст на вебсторінці чутним при натисканні.

#### --answer--

Він використовується для додавання аудіо до HTML-документа.

### --question--

#### --text--

Який із наведених НЕ є прикладом HTML-елемента?

#### --distractors--

`img`

---

`h1`

---

`link`

#### --answer--

`byte`

### --question--

#### --text--

Яка роль атрибута `target` всередині елементів посилання?

#### --distractors--

Визначає колір посилання при наведенні курсора користувача.

---

Визначає позицію в пов’язаному документі, куди браузер має прокрутити після натискання посилання.

---

Визначає розмір шрифту тексту посилання всередині елемента `link`.

#### --answer--

Використовується, щоб вказати, де буде відкрито пов’язаний документ при натисканні на посилання.

### --question--

#### --text--

Що таке абсолютний шлях?

#### --distractors--

Шлях, який є відносним до поточного файлу.

---

Шлях, що містить змінні.

---

Шлях, що містить спеціальні символи.

#### --answer--

Повний шлях до ресурсу, починаючи з кореневого каталогу.

### --question--

#### --text--

Яка роль елемента `link` у HTML?

#### --distractors--

Він використовується для створення гіперпосилань у межах однієї вебсторінки.

---

Він використовується для визначення макету та структури вебсторінки.

---

Він використовується для вставки аудіо- або відеофайлів на вебсторінку.

#### --answer--

Він використовується для підключення зовнішніх ресурсів, таких як таблиці стилів і іконки сайту.

### --question--

#### --text--

Яке з наведених є дійсним значенням для роботи з властивостями open graph?

#### --distractors--

`"og:socialMedia"`

---

`"og:mediaOG"`

---

`"og:openGraph"`

#### --answer--

`"og:title"`

### --question--

#### --text--

Який із наведених є прикладом булевого атрибута?

#### --distractors--

`href`

---

`src`

---

`width`

#### --answer--

`checked`

### --question--

#### --text--

Який із наведених є правильною синтаксисом для елемента `img`?

#### --distractors--

`<img src="image.jpg" alt="Description of the image"></img>`

---

`<img source="image.jpg" desc="Description of the image"></img>`

---

`<img src="image.jpg" desc="Description of the image">`

#### --answer--

`<img src="image.jpg" alt="Description of the image">`

### --question--

#### --text--

Для чого використовується елемент `iframe`?

#### --distractors--

Він використовується для додавання підписів до зображень.

---

Він використовується для додавання інформації про авторські права для медіа.

---

Він використовується для створення гіперпосилань у межах однієї вебсторінки.

#### --answer--

Він використовується для вставки іншого документа всередину поточного HTML-документа.

### --question--

#### --text--

Який із наведених НЕ є дійсним станом посилання для елементів посилання?

#### --distractors--

`hover`

---

`visited`

---

`active`

#### --answer--

`href`

### --question--

#### --text--

Яка роль елемента `title`?

#### --distractors--

Елемент, який дозволяє користувачам редагувати вихідний код вебсторінки.

---

Визначає макет і позиціювання елементів на вебсторінці.

---

Використовується для вставки відео та мультимедійного контенту безпосередньо в HTML-документ.

#### --answer--

Визначає заголовок документа і відображається у вкладці або вікні браузера.

## --quiz--

### --question--

#### --text--

Який із наведених атрибутів `target` відкриває посилання у батьківському контексті поточного?

#### --distractors--

`_blank`

---

`_self`

---

`_unfencedTop`

#### --answer--

`_parent`

### --question--

#### --text--

Який атрибут використовується для встановлення кодування символів `UTF-8` для HTML-документа?

#### --distractors--

`accept`

---

`capture`

---

`enctype`

#### --answer--

`charset`

### --question--

#### --text--

Яка роль HTML у вебі?

#### --distractors--

HTML використовується для стилю та макету.

---

HTML використовується для додавання інтерактивності на сторінку.

---

HTML — це реєстр програмних пакетів.

#### --answer--

HTML представляє вміст і структуру вебсторінки.

### --question--

#### --text--

Яка ліцензія для зображень підходить для суспільного надбання?

#### --distractors--

JPG

---

Creative Commons.

---

BSD

#### --answer--

Creative Commons 0.

### --question--

#### --text--

Як встановити булевий атрибут у значення `false`?

#### --distractors--

Встановити значення атрибута в `0`.

---

Використати оператор `not` перед ім’ям атрибута.

---

Встановити атрибут у порожній рядок (`""`).

#### --answer--

Пропустити атрибут у елементі.

### --question--

#### --text--

Для чого використовується елемент `script`?

#### --distractors--

Він використовується для вставки CSS у HTML-документ або підключення зовнішнього CSS-файлу.

---

Він використовується для вставки коду Pascal у HTML-документ.

---

Він використовується для вставки коду C# у HTML-документ.

#### --answer--

Він використовується для вставки JavaScript у HTML-документ або підключення зовнішнього JavaScript-файлу.

### --question--

#### --text--

Який із наведених елементів використовується для встановлення заголовка HTML-документа?

#### --distractors--

`footer`

---

`section`

---

`figcaption`

#### --answer--

`title`

### --question--

#### --text--

Для чого зазвичай використовується атрибут `class`?

#### --distractors--

Він використовується для вставки метаданих про елемент.

---

Він використовується для визначення рядкових стилів безпосередньо в HTML-тезі.

---

Він використовується для вказівки унікальних ідентифікаторів для JavaScript-функцій.

#### --answer--

Він використовується для застосування набору стилів до кількох елементів.

### --question--

#### --text--

Який із наведених НЕ є прикладом поширеної HTML-сутності?

#### --distractors--

`&quot;` (подвійна лапка).

---

`&copy;` (символ авторського права).

---

`&gt;` (символ більше).

#### --answer--

`&div;` (елемент div).

### --question--

#### --text--

Який елемент використовується для додавання аудіо до вашого HTML-документа?

#### --distractors--

`media`

---

`video`

---

`hr`

#### --answer--

`audio`

### --question--

#### --text--

Де розміщується вміст, представлений на сторінці, в HTML-документі?

#### --distractors--

Між елементами `head` та `body`.

---

Всередині елемента `repr`.

---

Всередині елемента `script`.

#### --answer--

Всередині елемента `body`.

### --question--

#### --text--

Який атрибут використовується, щоб знову запустити аудіо після його завершення?

#### --distractors--

`multiple`

---

`inputmode`

---

`enctype`

#### --answer--

`loop`

### --question--

#### --text--

Який із наведених є правильним синтаксисом для елемента `div`?

#### --distractors--

`<<div>>block container<</div>>`

---

`>>div>>block container>>div>>`

---

`[div]block container[/div]`

#### --answer--

`<div>block container</div>`

### --question--

#### --text--

Як працює `target="_self"`?

#### --distractors--

Визначає позицію в пов’язаному документі, куди браузер має прокрутити після натискання посилання.

---

Автоматично завантажує пов’язаний документ на комп’ютер користувача.

---

Перенаправляє користувача на інший вебсайт, вказаний у атрибуті `href`.

#### --answer--

Відкриває посилання у поточному контексті перегляду.

### --question--

#### --text--

Що таке відносний шлях?

#### --distractors--

Визначає точне розташування файлу або каталогу від кореневого каталогу файлової системи.

---

Містить повний URL, починаючи з доменного імені до конкретного файлу або каталогу.

---

Завжди починається з косої риски (`/`), що вказує на кореневий каталог сайту або файлової системи.

#### --answer--

Визначає розташування файлу або каталогу відносно поточного робочого каталогу.

### --question--

#### --text--

Який із наведених є правильним синтаксисом для елемента `link`?

#### --distractors--

`<link rel="stylesheet" href="./styles.css"></link>`

---

`</link rel="stylesheet" href="./styles.css"></link>`

---

`<<link rel="stylesheet" href="./styles.css"></link>>`

#### --answer--

`<link rel="stylesheet" href="./styles.css" />`

### --question--

#### --text--

Яке з наведених НЕ є дійсним значенням для роботи з властивостями open graph?

#### --distractors--

`"og:title"`

---

`"og:type"`

---

`"og:image"`

#### --answer--

`"og:socialMedia"`

### --question--

#### --text--

Який із наведених є правильним синтаксисом для елемента абзацу?

#### --distractors--

`>p>paragraph element>/p>`

---

`<<p>>paragraph element<</p>>`

---

`<p>`

#### --answer--

`<p>paragraph element</p>`

### --question--

#### --text--

Що означає назва елемента `iframe`?

#### --distractors--

Inline Framing

---

Inner Frame

---

Inline video

#### --answer--

Inline Frame

### --question--

#### --text--

Що означає активний стан для елементів посилання?

#### --distractors--

Посилання вимкнене і на нього не можна натиснути.

---

Курсор миші знаходиться над посиланням.

---

Посилання було відвідане користувачем.

#### --answer--

Користувач активно натискає на нього.

## --quiz--

### --question--

#### --text--

Який із наведених відкриє посилання у новій вкладці браузера?

#### --distractors--

```html
<a target="_parent" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_top" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_self" href="https://freecatphotoapp.com">cat photos</a>
```

#### --answer--

```html
<a target="_blank" href="https://freecatphotoapp.com">cat photos</a>
```

### --question--

#### --text--

Який із наведених є void-елементом?

#### --distractors--

`div`

---

`section`

---

`p`

#### --answer--

`img`

### --question--

#### --text--

Який із наведених є правильним способом використання HTML-сутностей?

#### --distractors--

`<p>This is an &lt&img /&gt& element</p>`

---

`<p>This is an ;lt&img /;gt& element</p>`

---

`<p>This is an ;lt;img /;gt; element</p>`

#### --answer--

`<p>This is an &lt;img /&gt; element</p>`

### --question--

#### --text--

Для чого використовується елемент `ul` у HTML?

#### --distractors--

Він використовується для групування лише списку зображень.

---

Він використовується для групування лише списку посилань.

---

Він використовується для групування впорядкованого списку елементів.

#### --answer--

Він використовується для групування маркованого списку елементів.

### --question--

#### --text--

Який із наведених є правильним способом застосування кількох класів до HTML-елемента?

#### --distractors--

```html
<div class="box>>red>>box"></div>
``` 

---

```html
<div class="box<<red<<box"></div>
``` 

---

```html
<div class="box-red-box"></div>
``` 

#### --answer--

```html
<div class="box red-box"></div>
``` 

### --question--

#### --text--

Який із наведених елементів використовується для представлення впорядкованого списку елементів?

#### --distractors--

`span`

---

`div`

---

`ul`

#### --answer--

`ol` 

### --question--

#### --text--

Для чого використовують властивості open graph?

#### --distractors--

Ці властивості використовують для вставки інтерактивного мультимедійного контенту безпосередньо на вебсторінки.

---

Ці властивості використовують для генерації динамічних спливаючих реклам на сайтах.

---

Ці властивості використовують для шифрування конфіденційних даних, що передаються між вебсервером і браузером користувача.

#### --answer--

Ці властивості використовують, щоб визначити, як вміст сайту буде відображатися на різних соціальних платформах.

### --question--

#### --text--

Що використовується для встановлення короткого опису вебсторінки?

#### --distractors--

```html
<meta
  add="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  set="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  description="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

#### --answer--

```html
<meta
  name="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

### --question--

#### --text--

Що означає SVG?

#### --distractors--

Scalable Vector Graph

---

Scalable Vistor Graphics

---

Site Vector Graphics

#### --answer--

Scalable Vector Graphics 

### --question--

#### --text--

Яка роль елемента `footer`?

#### --distractors--

Цей елемент використовується для групування пов’язаних підписів і зображень на вебсторінці.

---

Цей елемент використовується для створення горизонтальних ліній між абзацами на сторінці.

---

Цей елемент використовується для створення списку впорядкованих елементів у нижній частині сторінки.

#### --answer--

Цей елемент використовується для групування контенту з авторськими правами або пов’язаних документів у нижній частині сторінки.

### --question--

#### --text--

Що таке кодування символів `UTF-8`?

#### --distractors--

Це стандартизоване кодування символів, яке використовується для встановлення заголовка вебсторінки.

---

Це набір символів, який використовується для встановлення опису сторінки вебсторінки.

---

Це спеціальний набір символів, який використовується в елементах для покращення продуктивності медіа.

#### --answer--

Це стандартизоване кодування символів, широко використовуване в інтернеті.

### --question--

#### --text--

Який із наведених є прикладом заміненого елемента?

#### --distractors--

`slot`

---

`div`

---

`p`

#### --answer--

`iframe` 

### --question--

#### --text--

Який із наведених вважається найважливішим заголовком на вебсторінці?

#### --distractors--

`h6`

---

`h3`

---

`h5`

#### --answer--

`h1` 

### --question--

#### --text--

Який із наведених елементів використовується для підключення зовнішніх ресурсів, таких як таблиці стилів і іконки сайту?

#### --distractors--

`a`

---

`p`

---

`div`

#### --answer--

`link` 

### --question--

#### --text--

Який із наведених елементів використовується для передачі відчуття терміновості, серйозності або сильної важливості?

#### --distractors--

`important`

---

`heavy`

---

`strength`

#### --answer--

`strong`

### --question--

#### --text--

Яка роль елемента `figcaption`?

#### --distractors--

Він надає підписи для вмісту всередині елемента `span`.

---

Він надає підписи для вмісту всередині елемента `slot`.

---

Він надає підписи для вмісту всередині елемента `div`.

#### --answer--

Він надає підписи для вмісту всередині елемента `figure`.

### --question--

#### --text--

Який із наведених є правильним способом встановлення оголошення `DOCTYPE` для HTML-документа?

#### --distractors--

`<DOCTYPE>`

---

`<*DOCTYPE html>`

---

`<<DOCTYPE html>>`

#### --answer--

`<!DOCTYPE html>`

### --question--

#### --text--

Яка мета елемента `head` в HTML-документі?

#### --distractors--

Він є контейнером для елементів посилання в HTML-документі.

---

Він є контейнером для зображень в HTML-документі.

---

Він є контейнером для заголовків в HTML-документі.

#### --answer--

Він є контейнером для метаданих в HTML-документі.

### --question--

#### --text--

Який із наведених є правильним способом встановлення мови HTML-документа на англійську?

#### --distractors--

`<html lang-set="en">` 

---

`<html set-lang="en">` 

---

`<html language="en">` 

#### --answer--

`<html lang="en">` 

### --question--

#### --text--

Які елементи НЕ слід розміщувати всередині `head`?

#### --distractors--

`link`

---

`meta`

---

`title`

#### --answer--

`img` 

## --quiz--

### --question--

#### --text--

Яке з наведених тверджень НЕ є правильним про абсолютні та відносні шляхи?

#### --distractors--

Абсолютні шляхи починаються з кореневого каталогу, а відносні залежать від розташування поточного файлу.

---

Відносні шляхи коротші і гнучкіші для посилань у межах сайту, а абсолютні містять повний URL.

---

І абсолютні, і відносні шляхи можна використовувати для посилань на файли в межах одного сайту.

#### --answer--

Відносні шляхи не можуть використовувати `..` для переходу вгору по каталогах, а абсолютні можуть.

### --question--

#### --text--

Яке з наведених НЕ є дійсним значенням атрибута target у тегу `a`?

#### --distractors--

`_self`

---

`_parent`

---

`_top`

#### --answer--

`_main`

### --question--

#### --text--

Яка мета атрибута `controls` у елементі `audio`?

#### --distractors--

Визначає формат аудіофайлу, який має використовувати браузер.

---

Автоматично запускає аудіо при завантаженні сторінки.

---

Визначає URL джерела аудіофайлу.

#### --answer--

Додає вбудовані елементи керування відтворенням, такі як відтворення, пауза та регулювання гучності.

### --question--

#### --text--

Який із наведених елементів покращує SEO, надаючи короткий опис вебсторінки?

#### --distractors--

```html
<meta
 description="describe"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 name="desc"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 description="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

#### --answer--

```html
<meta
 name="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

### --question--

#### --text--

Що робить `<meta charset="utf-8" />`?

#### --distractors--

Встановлює мову вебсторінки на англійську.

---

Підключає зовнішній CSS-файл.

---

Визначає заголовок вебсторінки.

#### --answer--

Визначає кодування символів для правильного відображення тексту.

### --question--

#### --text--

У чому різниця між елементами `link` і `script` у HTML?

#### --distractors--

`link` використовується для JavaScript-файлів, а `script` — для CSS-файлів.

---

Обидва `link` і `script` використовуються для стилізації вебсторінок.

---

`script` використовується для додавання стилів, а `link` — для запуску JavaScript.

#### --answer--

`link` використовується для додавання стилів із зовнішнього CSS-файлу, а `script` — для запуску JavaScript-коду.

### --question--

#### --text--

Яке з наведених тверджень про HTML-класи та ідентифікатори є неправильним?

#### --distractors--

Ім’я класу можна використовувати кілька разів у HTML-документі.

---

Атрибут `rel` визначає зв’язок між пов’язаним ресурсом і HTML-документом.

---

Властивість `og:image` використовується для вказівки зображення, що відображається у публікаціях у соцмережах.

#### --answer--

Імена ідентифікаторів не можуть містити підкреслення і мають бути унікальними.

### --question--

#### --text--

Яке з наведених тверджень про HTML-заголовки є правильним?

#### --distractors--

Розмір тексту збільшується від `h1` до `h6`.

---

Розмір тексту однаковий для всіх заголовків.

---

`h3` більший за `h2`.

#### --answer--

Розмір тексту зменшується від `h1` до `h6`.

### --question--

#### --text--

Яке твердження є неправильним про HTML-елементи?

#### --distractors--

HTML-елементи є базовими будівельними блоками вебсторінки.

---

Деякі кодові бази містять косу риску (/) всередині void-елементів.

---

Булевий атрибут працює просто через свою присутність або відсутність у тегу.

#### --answer--

Void-елементи містять вміст і мають лише початковий тег.

### --question--

#### --text--

Яке з наведених тверджень про void-елементи в HTML є правильним?

#### --distractors--

Void-елемент завжди повинен містити косу риску / перед закриттям.

---

Void-елемент ніколи не повинен містити косу риску /.

---

Коса риска / обов’язкова для самозакриваючих void-елементів у всіх версіях HTML.

#### --answer--

І `<img>`, і `<img/>` є прийнятними у сучасному HTML.

### --question--

#### --text--

Яка основна мета коментарів у програмуванні?

#### --distractors--

Щоб код працював швидше.

---

Щоб змінити спосіб відображення вмісту браузером.

---

Щоб зберігати тимчасові дані в коді.

#### --answer--

Щоб залишати нотатки для себе та інших розробників у коді.

### --question--

#### --text--

Який атрибут target відкриває посилання в тому самому контексті перегляду, що й поточний?

#### --distractors--

`_blank`

---

`_top`

---

`_parent`

#### --answer--

`_self`

### --question--

#### --text--

Яка мета елемента `em` у HTML?

#### --distractors--

Зробити текст більшим.

---

Змінити колір тексту.

---

Підкреслити слова для наголосу.

#### --answer--

Виділити важливий текст курсивом.

### --question--

#### --text--

Що робить елемент `strong` у HTML?

#### --distractors--

Збільшує розмір шрифту.

---

Змінює колір тексту.

---

Підкреслює текст.

#### --answer--

Робить текст жирним, щоб показати важливість.

### --question--

#### --text--

У чому основна різниця між елементами `figure` і `figcaption` у HTML?

#### --distractors--

`figure` використовується для підписів, а `figcaption` — для групування пов’язаного контенту, як зображення чи діаграми.

---

`figcaption` завжди має бути поза елементом `figure`.

---

Немає різниці; обидва використовуються для однієї мети.

#### --answer--

`figure` групує пов’язаний контент, як зображення чи діаграми, а `figcaption` надає підпис для цього контенту.

### --question--

#### --text--

Для чого використовують HTML-сутності?

#### --distractors--

Щоб додати стилі до тексту.

---

Щоб створити гіперпосилання на вебсторінці.

---

Щоб збільшити розмір шрифту спеціальних символів.

#### --answer--

Щоб представити зарезервовані символи, як `<` і `&`, у HTML.

### --question--

#### --text--

Яке з наведених тверджень про теги Open Graph є правильним?

#### --distractors--

Теги Open Graph використовують для стилізації вебсторінки за допомогою CSS.

---

Теги Open Graph використовують лише для SEO і вони не впливають на соціальні мережі.

---

Теги Open Graph мають розміщуватися всередині розділу `body` у HTML.

#### --answer--

Властивість `og:title` встановлює заголовок, який відображається при поширенні контенту у соцмережах.

### --question--

#### --text--

Яке з наведених тверджень про формати зображень є неправильним?

#### --distractors--

WEBP і AVIF краще підходять для сучасної оптимізації зображень.

---

SVG-зображення можна масштабувати без втрати якості.

---

Стиснення допомагає зменшити розмір файлів зображень.

#### --answer--

PNG і JPG — найкращі формати для оптимізованих зображень сьогодні.

### --question--

#### --text--

Що робить атрибут `loop` у елементі `audio`?

#### --distractors--

Відтворює аудіо один раз.

---

Зупиняє аудіо після одного відтворення.

---

Сповільнює відтворення аудіо.

#### --answer--

Повторює аудіо безперервно.

### --question--

#### --text--

Як найкраще описати шаблонний код HTML?

#### --distractors--

Спеціальний інструмент, який використовують веброзробники для додавання водяних знаків до документів.

---

Плагін браузера, який прискорює завантаження вебсторінок.

---

Фреймворк для кодування, призначений виключно для анімацій у HTML.

#### --answer--

Попередньо визначений шаблон, що містить основну структуру HTML для веброзробки.
