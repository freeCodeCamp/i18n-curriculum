---
id: 66edd3b3096349f06cf688bb
title: Quiz su audio e video in JavaScript
challengeType: 8
dashedName: quiz-javascript-audio-and-video
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual è lo scopo del costruttore `Audio()` in JavaScript?

#### --distractors--

Creare un nuovo file audio.

---

Riprodurre un file audio esistente.

---

Modificare file audio.

#### --answer--

Creare un nuovo elemento `audio`.

### --question--

#### --text--

Quale metodo si usa per iniziare la riproduzione di un elemento `audio` o `video`?

#### --distractors--

`start()`

---

`begin()`

---

`resume()`

#### --answer--

`play()`

### --question--

#### --text--

Qual è la differenza tra i metodi `pause()` e `play()` per gli elementi multimediali?

#### --distractors--

`pause()` restituisce una Promise mentre `play()` no.

---

`pause()` non può essere ripreso mentre `play()` può partire da qualsiasi posizione.

---

`pause()` si resetta automaticamente all'inizio mentre `play()` riparte da dove l'audio è stato messo in pausa.

#### --answer--

`pause()` ferma la riproduzione ma mantiene la posizione corrente mentre `play()` avvia la riproduzione.

### --question--

#### --text--

Quale formato audio ha il supporto più ampio nei browser?

#### --distractors--

`FLAC`

---

`OGG`

---

`WAV`

#### --answer--

`MP3`

### --question--

#### --text--

Qual è lo scopo dell'elemento `source` all'interno di un elemento `audio` o `video`?

#### --distractors--

Specificare il volume del media.

---

Aggiungere sottotitoli al media.

---

Collegare a un processore audio esterno.

#### --answer--

Specificare più sorgenti media per formati diversi.

### --question--

#### --text--

Quali sono i due possibili tipi MIME che un formato MP4 può avere?

#### --distractors--

`mp4/audio` e `mp4/video`

---

`audio/mpeg4` e `video/mpeg4`

---

`mp4/mpeg` e `mp4/stream`

#### --answer--

`audio/mp4` e `video/mp4`

### --question--

#### --text--

Cosa fa un codec nel contesto di audio e video?

#### --distractors--

Aumenta il volume del media.

---

Cambia il formato del file del media.

---

Cripta il media per una trasmissione sicura.

#### --answer--

Converte il media tra formati analogici e digitali.

### --question--

#### --text--

Quale API fornisce un modo per creare, processare e analizzare audio nelle applicazioni web?

#### --distractors--

Audio Processing API.

---

Sound Manipulation API.

---

Media Stream API.

#### --answer--

Web Audio API.

### --question--

#### --text--

Qual è lo scopo del metodo `getUserMedia()`?

#### --distractors--

Autenticare gli utenti con un servizio media.

---

Recuperare le preferenze utente per la riproduzione media.

---

Scaricare file media sul dispositivo dell'utente.

#### --answer--

Consultare la fotocamera e/o il microfono dell'utente.

### --question--

#### --text--

Quale proprietà dell'interfaccia `HTMLMediaElement` rappresenta il tempo corrente di riproduzione?

#### --distractors--

`time`

---

`playbackPosition`

---

`currentPosition`

#### --answer--

`currentTime`

### --question--

#### --text--

Cosa fa il seguente frammento di codice?

```javascript
const audio = new Audio('song.mp3');
audio.loop = true;
```

#### --distractors--

Riproduce il file audio una volta.

---

Aumenta il volume dell'audio.

---

Mette in pausa l'audio dopo un loop.

#### --answer--

Imposta l'audio per ripetere indefinitamente.

### --question--

#### --text--

Qual è il significato completo di MIME?

#### --distractors--

Multiple Internet Message Extensions

---

Mail Internet Multimedia Extensions

---

Multimedia Internet Mail Extensions

#### --answer--

Multipurpose Internet Mail Extensions

### --question--

#### --text--

A cosa serve il metodo `canPlayType()`?

#### --distractors--

Verificare se un audio è attualmente in riproduzione nel browser in un formato specifico.

---

Verificare se un file media esiste nella cache del browser ed è accessibile per la riproduzione.

---

Testare se un elemento audio ha i controlli abilitati e se l'utente ha i permessi appropriati per interagire con esso.

#### --answer--

Determinare se un browser probabilmente può riprodurre il tuo formato audio specifico.

### --question--

#### --text--

Quale tra i seguenti NON è un evento valido dell'API `HTMLMediaElement`?

#### --distractors--

`waiting`

---

`canplay`

---

`ended`

#### --answer--

`stopped`

### --question--

#### --text--

Quale evento `HTMLMediaElement` si attiva quando la riproduzione è messa in pausa a causa del buffering dei dati?

#### --distractors--

`stopped`

---

`pause`

---

`buffering`

#### --answer--

`waiting`

### --question--

#### --text--

Quale interfaccia della Web Audio API si usa per rappresentare un grafo di elaborazione audio?

#### --distractors--

`AudioNode`

---

`AudioBuffer`

---

`AudioListener`

#### --answer--

`AudioContext`

### --question--

#### --text--

Qual è lo scopo dell'API Media Source Extensions?

#### --distractors--

Aggiungere effetti visivi al video.

---

Convertire tra diversi formati media.

---

Permettere a JavaScript di sincronizzare più tracce audio.

#### --answer--

Permette di passare direttamente il flusso della webcam dell'utente a un elemento video.

### --question--

#### --text--

Quando si attiva l'evento `ended`?

#### --distractors--

Quando la sorgente media viene cambiata.

---

Quando viene chiamato il metodo `pause()`.

---

Quando il media viene fermato manualmente.

#### --answer--

Quando si raggiunge la fine del media.

### --question--

#### --text--

Come viene esposta la Screen Capture API?

#### --distractors--

Attraverso l'interfaccia `window.screenCapture`

---

Usando la proprietà `navigator.capture`

---

Usando il metodo `captureScreen()`

#### --answer--

Chiamando il metodo `getDisplayMedia()`.

### --question--

#### --text--

A cosa è associato il formato WMV?

#### --distractors--

Wide Media Video

---

World Media Video

---

Web Media Player

#### --answer--

Windows Media Player
