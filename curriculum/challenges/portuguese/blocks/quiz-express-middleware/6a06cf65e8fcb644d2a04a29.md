---
id: 6a06cf65e8fcb644d2a04a29
title: Quiz sobre middleware no Express
challengeType: 8
dashedName: quiz-express-middleware
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

O que é middleware no Express?

#### --distractors--

Uma função que inicia o servidor Express e o vincula a uma porta.

---

Um objeto de configuração que define rotas e seus manipuladores.

---

Um conector de banco de dados embutido para lidar com dados persistentes.

#### --answer--

Uma função que é executada durante o ciclo de vida de uma requisição ao servidor.

### --question--

#### --text--

A que três coisas uma função middleware tem acesso?

#### --distractors--

A conexão com o banco de dados, o armazenamento da sessão e a definição da rota.

---

A configuração do servidor, o número da porta e a instância do app.

---

As variáveis de ambiente, os cabeçalhos da requisição e o status da resposta.

#### --answer--

O objeto de requisição (`req`), o objeto de resposta (`res`) e a função `next`.

### --question--

#### --text--

O que acontece se uma função middleware não chamar `next()`?

#### --distractors--

O controle é passado automaticamente para o próximo middleware na pilha.

---

O Express lança um erro não tratado e o servidor trava.

---

A requisição é tentada novamente desde o início da pilha de middleware.

#### --answer--

O ciclo de requisição-resposta para.

### --question--

#### --text--

O que `app.use()` faz?

#### --distractors--

Define um manipulador de rota `GET` para um caminho específico.

---

Inicia o servidor e começa a escutar conexões recebidas.

---

Cria uma nova instância da aplicação Express.

#### --answer--

Adiciona middleware globalmente à aplicação.

### --question--

#### --text--

Em que ordem o middleware é executado em um app Express?

#### --distractors--

Em ordem inversa, começando pelo último middleware adicionado.

---

Em ordem alfabética, baseado nos nomes das funções middleware.

---

Aleatoriamente, dependendo de qual requisição chega primeiro.

#### --answer--

Na ordem em que é adicionado ao app.

### --question--

#### --text--

A que o middleware em nível de aplicação está vinculado?

#### --distractors--

A uma instância específica de rota criada com `express.Router()`.

---

Ao servidor HTTP do Node.js retornado por `app.listen()`.

---

A um manipulador de rota individual definido com `app.get()` ou `app.post()`.

#### --answer--

A uma instância do app Express.

### --question--

#### --text--

Qual dos seguintes pode ser usado para registrar middleware em nível de aplicação?

#### --distractors--

`app.listen()` ou métodos específicos de rota como `app.get()` e `app.delete()`

---

`app.route()` ou métodos específicos de rota como `app.patch()` e `app.options()`

---

`router.use()` ou métodos específicos de rota como `router.get()` e `router.post()`

#### --answer--

`app.use()` ou métodos específicos de rota como `app.get()` e `app.post()`

### --question--

#### --text--

A que o middleware em nível de roteador está vinculado?

#### --distractors--

À instância principal do app Express criada com `express()`.

---

Ao objeto servidor do módulo `http` do Node.js.

---

A uma função callback manipuladora de rota específica.

#### --answer--

A uma instância de `express.Router()`.

### --question--

#### --text--

Qual método é usado para aplicar middleware em nível de roteador?

#### --distractors--

`app.use()`

---

`express.use()`

---

`app.router()`

#### --answer--

`router.use()`

### --question--

#### --text--

Como você limita o middleware em nível de roteador a um caminho específico no seu app?

#### --distractors--

Chamando `router.scope('/path')` antes de definir as rotas.

---

Passando o caminho diretamente para `router.use()` no arquivo principal.

---

Configurando `router.basePath = '/path'` antes de montá-lo.

#### --answer--

Montando o roteador com `app.use('/path', router)`.

### --question--

#### --text--

O que torna o middleware de tratamento de erros diferente do middleware comum no Express?

#### --distractors--

Ele usa `res.error()` em vez de `res.send()` para responder ao cliente, ignorando o fluxo normal de resposta.

---

Deve ser definido antes de todas as outras rotas para interceptar erros antes que alcancem qualquer manipulador de rota.

---

É registrado com `app.error()` em vez de `app.use()`, o que sinaliza ao Express que deve tratar erros.

#### --answer--

Ele tem quatro parâmetros (`err`, `req`, `res` e `next`), e o parâmetro `err` indica ao Express que é um manipulador de erros.

### --question--

#### --text--

Quando o middleware de tratamento de erros é invocado?

#### --distractors--

Quando um manipulador de rota retorna uma resposta com código de status 4xx ou 5xx.

---

Quando o servidor recebe mais de uma requisição ao mesmo tempo.

---

Quando a requisição demora mais que o tempo limite padrão para completar.

#### --answer--

Quando um erro é passado para `next(err)`.

### --question--

#### --text--

Onde o middleware de tratamento de erros deve ser colocado na pilha de middleware?

#### --distractors--

No topo, antes de qualquer rota ser definida.

---

Imediatamente após a rota que tem maior probabilidade de lançar um erro.

---

No mesmo nível que outros middleware, em qualquer posição.

#### --answer--

No final, depois de todos os outros middleware e rotas.

### --question--

#### --text--

O que o middleware embutido `express.json()` faz?

#### --distractors--

Serve arquivos JSON armazenados no diretório público para o cliente.

---

Converte o objeto de resposta para JSON antes de enviá-lo ao cliente.

---

Valida que todas as requisições recebidas contenham um cabeçalho de tipo de conteúdo JSON válido.

#### --answer--

Analisa requisições recebidas com payloads JSON.

### --question--

#### --text--

O que `express.static()` faz?

#### --distractors--

Analisa requisições recebidas com dados de formulário codificados em URL.

---

Armazena em cache as respostas dos manipuladores de rota para melhorar o desempenho.

---

Registra caminhos de rota estáticos que não podem ser alterados em tempo de execução.

#### --answer--

Serve arquivos estáticos como imagens, CSS e JavaScript.

### --question--

#### --text--

O que `express.urlencoded()` analisa, e onde o resultado fica disponível?

#### --distractors--

Analisa payloads JSON e torna o resultado disponível em `req.json`.

---

Analisa strings de consulta da URL e as torna disponíveis em `req.query`.

---

Analisa dados de formulário multipart e torna os arquivos disponíveis em `req.files`.

#### --answer--

Analisa payloads codificados em URL de formulários HTML e torna os dados disponíveis em `req.body`.

### --question--

#### --text--

O que o middleware de terceiros `cors` faz?

#### --distractors--

Registra todas as requisições HTTP recebidas com método, URL e tempo de resposta.

---

Analisa cookies das requisições recebidas e os anexa a `req.cookies`.

---

Comprimi os corpos das respostas para reduzir o tamanho dos dados enviados ao cliente.

#### --answer--

Habilita o compartilhamento de recursos entre origens diferentes (CORS).

### --question--

#### --text--

Qual das opções a seguir classifica corretamente o middleware do Express?

#### --distractors--

`morgan` é embutido enquanto `express.static()` é de terceiros

---

`cors` é embutido enquanto `morgan` é de terceiros

---

`express.urlencoded()` é embutido enquanto `express.json()` é de terceiros

#### --answer--

`express.json()` é embutido enquanto `cors` é de terceiros

### --question--

#### --text--

O que o middleware de terceiros `morgan` faz?

#### --distractors--

Habilita o compartilhamento de recursos entre origens diferentes (CORS).

---

Analisa cookies das requisições recebidas e os anexa a `req.cookies`.

---

Comprimi os corpos das respostas para reduzir o tamanho dos dados enviados ao cliente.

#### --answer--

Registra requisições HTTP.

### --question--

#### --text--

O que acontece quando uma função middleware chama `next()`?

#### --distractors--

A requisição é imediatamente enviada de volta ao cliente com status 200.

---

A função middleware atual reinicia desde o começo.

---

O Express pula todos os middleware restantes e executa o manipulador de rota final.

#### --answer--

O controle passa para o próximo middleware ou manipulador de rota na pilha.
