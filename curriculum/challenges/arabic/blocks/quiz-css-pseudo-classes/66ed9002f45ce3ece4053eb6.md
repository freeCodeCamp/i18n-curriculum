---
id: 66ed9002f45ce3ece4053eb6
title: اختبار الفئات المزيفة في CSS
challengeType: 8
dashedName: quiz-css-pseudo-classes
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هي الفئات المزيفة؟

#### --distractors--

هي قواعد CSS تُنشئ محتوى إضافيًا داخل العنصر عند الحاجة.

---

هي خصائص تضبط حجم العنصر أو تخطيطه على الصفحة.

---

هي محددات تُستخدم لإضافة تأثيرات حركة على العنصر أثناء التفاعل.

#### --answer--

هي كلمات مفتاحية تُضاف إلى محدد لتنسيق العنصر بناءً على حالته.

### --question--

#### --text--

أي فئة مزيفة تُطبق عندما يتم وضع جهاز تأشير فوق عنصر؟

#### --distractors--

`:focus`

---

`:active`

---

`:checked`

#### --answer--

`:hover`

### --question--

#### --text--

أي عنصر مزيف يسمح لك بتنسيق الحرف الأول من فقرة؟

#### --distractors--

`:first-letter`

---

`:first-child`

---

`:last-of-type`

#### --answer--

`::first-letter`

### --question--

#### --text--

أي فئة مزيفة تغير نمط العنصر أثناء النقر عليه؟

#### --distractors--

`:focus`

---

`:hover`

---

`:checked`

#### --answer--

`:active`

### --question--

#### --text--

أي فئة مزيفة تُستخدم لتنسيق عنصر عندما يكون جاهزًا لاستقبال مُدخَل المستخدم، مثل حقل نص يتم النقر عليه أو التنقل إليه عبر التبويب؟

#### --distractors--

`::focus`

---

`:active`

---

`:visited`

#### --answer--

`:focus`

### --question--

#### --text--

أي من قواعد CSS التالية تضيف النص `Note:` بشكل صحيح أمام كل عنصر فقرة يحمل صنف `note`؟

#### --distractors--

```css
.note::before {
  content: "Note:";
}
```

---

```css
p.note::after {
  content: "Note:";
}
```

---

```css
p::before {
  content: "Note:";
}
```

#### --answer--

```css
p.note::before {
  content: "Note:";
}
```

### --question--

#### --text--

أي فئة مزيفة تُطبق على حقل الإدخال عندما يكون محددًا أو مفعّلًا؟

#### --distractors--

`:required`

---

`:disabled`

---

`:optional`

#### --answer--

`:checked`

### --question--

#### --text--

أي من الصيغ النحوية التالية صحيحة لتنسيق آخر عنصر تابع في قائمة؟

#### --distractors--

```css
li:nth-child(last) {
  color: blue;
}
```

---

```css
#li:last-child {
  color: blue;
}
```

---

```css
.li:last-child() {
  color: blue;
}
```

#### --answer--

```css
li:last-child {
  color: blue;
}
```

### --question--

#### --text--

أي فئة مزيفة تستهدف حقول الإدخال التي لا يُشترط ملؤها؟

#### --distractors--

`:required`

---

`:enabled`

---

`::optional`

#### --answer--

`:optional`

### --question--

#### --text--

ماذا تفعل الفئة المزيفة `:disabled`؟

#### --distractors--

تُنسق حقول الإدخال المحددة.

---

تُنسق العناصر التي يتم وضع المؤشر عليها.

---

تختار العناصر التي لا تطابق محددًا معينًا.

#### --answer--

تُنسق العناصر غير المتاحة للتفاعل مع المستخدم.

### --question--

#### --text--

أي فئة مزيفة تُطبق عندما يفي مُدخل النموذج بمعايير التحقق؟

#### --distractors--

`:checked`

---

`:required`

---

`:disabled`

#### --answer--

`:valid`

### --question--

#### --text--

أي من هذه ليست فئة مزيفة متعلقة بالموقع؟

#### --distractors--

`:visited`

---

`:any-link`

---

`:link`

#### --answer--

`:current`

### --question--

#### --text--

أي من التالي يختار العنصر الثالث في القائمة؟

#### --distractors--

```css
li:child(3) {
  color: red;
}
```

---

```css
li:last-child(3) {
  color: red;
}
```

---

```css
li:nth-child(three) {
  color: red;
}
```

#### --answer--

```css
li:nth-child(3) {
  color: red;
}
```

### --question--

#### --text--

أي العناصر سيكون لها `color` باللون `blue` مع CSS التالي؟

```css
p:is(.blue, .highlight) {
  color: blue;
}
```

#### --distractors--

```html
<p class="class">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

---

```html
<div class="blue">Paragraph 1</div>
<div class="highlight">Paragraph 2</div>
```

---

```html
<p>Paragraph 1</p>
<span class="highlight">Paragraph 2</span>
```

#### --answer--

```html
<p class="blue">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

### --question--

#### --text--

ماذا تفعل الفئة المزيفة `:not()`؟

#### --distractors--

تضيف أنماطًا لجميع العناصر.

---

تختار جميع العناصر التابعة لعنصر أب.

---

تختار العناصر التي تطابق محددًا معينًا.

#### --answer--

تختار العناصر التي لا تطابق محددًا معينًا.

### --question--

#### --text--

ماذا تفعل قاعدة CSS التالية؟

```css
p:first-of-type {
  font-style: italic;
}
```

#### --distractors--

تختار أول عنصر `p` في المستند.

---

تختار جميع عناصر `p` في المستند.

---

تختار أول عنصر تابع لكل عنصر `p`.

#### --answer--

تختار أول عنصر `p` داخل حاوية أب.

### --question--

#### --text--

ماذا تفعل الفئة المزيفة `:last-of-type`؟

#### --distractors--

تختار أول عنصر تابع من نوع معين داخل أبويه.

---

تختار العنصر الأوسط من نوع معين داخل أبويه.

---

تختار كل العناصر التابعة من نوع معين داخل أبويه.

#### --answer--

تختار آخر عنصر تابع من نوع معين داخل أبويه.

### --question--

#### --text--

أي فئة مزيفة تُستخدم لاختيار العنصر الثاني في قائمة؟

#### --distractors--

`:first-child`

---

`:required`

---

`:is()`

#### --answer--

`:nth-child(2)`

### --question--

#### --text--

أي من هذه فئة مزيفة وظيفية؟

#### --distractors--

`:first-child`

---

`:match()`

---

`:checked`

#### --answer--

`:is()`

### --question--

#### --text--

أي من هذه ليست فئة مزيفة وظيفية؟

#### --distractors--

`:has()`

---

`:not()`

---

`:where()`

#### --answer--

`:contains()`

## --quiz--

### --question--

#### --text--

أي فئة مزيفة تُستخدم لاستهداف عناصر النموذج المفعلة؟

#### --distractors--

`:disabled`

---

`:active`

---

`:focus`

#### --answer--

`:enabled`

### --question--

#### --text--

أي فئة مزيفة تسمح باختيار العناصر بالعد من النهاية؟

#### --distractors--

`:nth-child(n)`

---

`:last-child`

---

`:last-of-type`

#### --answer--

`:nth-last-child(n)`

### --question--

#### --text--

أي من التالي يسمح باختيار العناصر التي تحتوي على عناصر تابعة محددة؟

#### --distractors--

`:is()`

---

`:where()`

---

`:in-range`

#### --answer--

`:has()`

### --question--

#### --text--

أي من التالي يختار العناصر التي لا تحتوي على أي محتوى أو عناصر تابعة؟

#### --distractors--

`:only-child`

---

`:last-child`

---

`:not()`

#### --answer--

`:empty`

### --question--

#### --text--

ماذا يستهدف محدد CSS هذا؟

```css
input:invalid {
  background-color: red;
}
```

#### --distractors--

جميع عناصر الإدخال.

---

جميع عناصر الإدخال التي تحتوي على قيم ضمن النطاق المسموح.

---

جميع عناصر الإدخال التي تجتاز التحقق.

#### --answer--

جميع عناصر الإدخال التي تفشل في التحقق.

### --question--

#### --text--

أي فئة مزيفة تختار حقول الإدخال التي يملأها المتصفح تلقائيًا؟

#### --distractors--

`:visited`

---

`:valid`

---

`:where()`

#### --answer--

`:autofill`

### --question--

#### --text--

أي فئة مزيفة تختار عنصرًا إذا كان هو أو أي من عناصره التابعة في حالة تركيز؟

#### --distractors--

`:focus`

---

`:in-range`

---

`:only-child`

#### --answer--

`:focus-within`

### --question--

#### --text--

أي فئة مزيفة تمثل الروابط التي تشير إلى نفس المستند؟

#### --distractors--

`:target`

---

`:visited`

---

`:link`

#### --answer--

`:local-link`

### --question--

#### --text--

أي من التالي ينسق عنصر `p` عندما يكون هدف جزء من عنوان URL؟

#### --distractors--

```css
p:empty {
  background-color: gold;
}
```

---

```css
p:not(.targeted) {
  background-color: gold;
}
```

---

```css
p:is(.target) {
  background-color: gold;
}
```

#### --answer--

```css
p:target {
  background-color: gold;
}
```

### --question--

#### --text--

أي فئة مزيفة تُستخدم عندما يكون العنصر هدف جزء من عنوان URL؟

#### --distractors--

`:focus-within`

---

`:hover`

---

`:visited`

#### --answer--

`:target`

### --question--

#### --text--

ماذا تختار الفئة المزيفة `:only-child`؟

#### --distractors--

تختار عنصر الأب الذي له طفل واحد فقط.

---

تختار جميع العناصر التابعة داخل عنصر الأب.

---

تختار عنصر الأب الذي يحتوي على نوع واحد فقط من العناصر التابعة.

#### --answer--

تختار عنصرًا لا له أشقاء داخل عنصر الأب.

### --question--

#### --text--

أي فئة مزيفة تختار عنصرًا إذا كان الوحيد من نوعه داخل عنصر الأب؟

#### --distractors--

`:only-child`

---

`:nth-of-type(n)`

---

`:first-of-type`

#### --answer--

`:only-of-type`

### --question--

#### --text--

أي قاعدة CSS ستطبق `color` بقيمة `yellow` على العنصر الثاني `p` في HTML التالي؟

```html
<div>
  <h1>Courses</h1>
  <p>HTML</p>
  <p>CSS</p>
  <p>JavaScript</p>
</div>
```

#### --distractors--

```css
p:nth-child(2) {
  color: yellow;
}
```

---

```css
p:first-of-type {
  color: yellow;
}
```

---

```css
p:last-of-type {
  color: yellow;
}
```

#### --answer--

```css
p:nth-of-type(2) {
  color: yellow;
}
```

### --question--

#### --text--

أي عنصر مزيف يسمح باختيار علامة عناصر القائمة لتنسيقها؟

#### --distractors--

`::before`

---

`::after`

---

`:root`

#### --answer--

`::marker`

### --question--

#### --text--

أي فئة مزيفة تسمح باستهداف أعلى عنصر في المستند، عادة عنصر `html`؟

#### --distractors--

`:first-child`

---

`:in-range`

---

`:target`

#### --answer--

`:root`

### --question--

#### --text--

أي فئة مزيفة في CSS لها خصوصية صفر، مما يضمن عدم تعارضها مع قواعد التنسيق الأخرى؟

#### --distractors--

`:is()`

---

`:not()`

---

`:focus`

#### --answer--

`:where()`

### --question--

#### --text--

أي من التالي فئة مزيفة هيكلية شجرية؟

#### --distractors--

`:where()`

---

`:valid`

---

`:link`

#### --answer--

`:root`

### --question--

#### --text--

أي قاعدة CSS ستُعيّن خاصية `background-color` لهذا العنصر إلى اللون `red` إذا كانت قيمته خارج النطاق المحدد؟

```html
<input type="number" min="10" max="25"/>
```

#### --distractors--

```css
input:in-range {
  background-color: red;
}
```

---

```css
input {
  background-color: red;
}
```

---

```css
input:valid {
  background-color: red;
}
```

#### --answer--

```css
input:out-of-range {
  background-color: red;
}
```

### --question--

#### --text--

أي عنصر مزيف يستخدم خاصية `content` لإدخال محتوى بعد العنصر؟

#### --distractors--

`::before`

---

`::marker`

---

`::first-letter`

#### --answer--

`::after`

### --question--

#### --text--

أي فئة مزيفة تُطبق أنماطًا على عنصر إذا كانت قيمته ضمن النطاق المحدد؟

#### --distractors--

`:out-of-range`

---

`:enabled`

---

`:checked`

#### --answer--

`:in-range`
