---
id: 66df3b712c41c499e9d31e5b
title: 기본 HTML 퀴즈
challengeType: 8
dashedName: quiz-basic-html
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 맞히세요.

# --quizzes--

## --quiz--

### --question--

#### --text--

`loop` 요소 안에서 `audio` 속성은 어떻게 작동하나요?

#### --distractors--

웹페이지의 다른 멀티미디어 요소와 재생을 동기화할 수 있게 `audio` 요소를 허용합니다.

---

오디오의 음높이와 음색을 조정합니다.

---

`audio` 요소가 무작위 수열로 재생을 멈췄다가 다시 시작하게 합니다.

#### --answer--

오디오가 끝나면 다시 시작합니다.

### --question--

#### --text--

HTML에서 void 요소란 무엇인가요?

#### --distractors--

화면에 비디오를 임베드하는 데 사용하는 요소입니다.

---

목록을 표시하는 요소입니다.

---

문서에 소리를 임베드하는 데 사용하는 요소입니다.

#### --answer--

내용이나 종료 태그가 없는 요소입니다.

### --question--

#### --text--

다음 중 `id` 속성에 올바른 값은 무엇인가요?

#### --distractors--

`id="main@content"`

---

`id="main content"`

---

`id="main~content"`

#### --answer--

`id="main-content"`

### --question--

#### --text--

`target="_parent"`는 어떻게 작동하나요?

#### --distractors--

새로운 브라우징 컨텍스트에서 링크를 엽니다.

---

사용자가 링크를 클릭한 후 브라우저가 스크롤할 연결된 문서 내 위치를 지정합니다.

---

`href` 속성에 지정된 다른 웹사이트로 사용자를 리다이렉트합니다.

#### --answer--

현재 컨텍스트의 부모에서 링크를 엽니다.

### --question--

#### --text--

불리언 속성과 일반 속성의 차이는 무엇인가요?

#### --distractors--

불리언 속성은 체크박스에만 사용되고, 일반 속성은 라디오 버튼에 사용됩니다.

---

이미지에는 항상 일반 속성이 필요하고, 불리언 속성은 선택 사항입니다.

---

불리언 속성은 항상 값이 할당되어야 하고, 일반 속성은 값이 없어도 됩니다.

#### --answer--

불리언 속성은 존재하거나 없을 수 있지만, 일반 속성은 항상 지정된 값이 있습니다.

### --question--

#### --text--

스크립트를 HTML 파일에 링크하는 데 사용하는 요소는 무엇인가요?

#### --distractors--

`img`

---

`ul`

---

`h2`

#### --answer--

`script`

### --question--

#### --text--

HTML 보일러플레이트란 무엇인가요?

#### --distractors--

웹 개발자가 문서에 워터마크를 추가하는 데 사용하는 특별한 도구입니다.

---

마크업 언어의 한 종류입니다.

---

페이지에 자동으로 "lorem ipsum" 텍스트를 생성하는 플러그인입니다.

#### --answer--

HTML 문서에 필요한 모든 필수 정보를 포함한 템플릿입니다.

### --question--

#### --text--

HTML 요소에 고유 식별자를 적용하는 속성은 무엇인가요?

#### --distractors--

`class`

---

`href`

---

`action`

#### --answer--

`id`

### --question--

#### --text--

HTML 엔티티(문자 참조)란 무엇인가요?

#### --distractors--

HTML에서 문자를 그래픽으로 나타낸 것입니다.

---

HTML에서 사용하는 특별한 서식 태그입니다.

---

웹 디자인에서 문자를 나타내는 이미지입니다.

#### --answer--

HTML에서 예약된 문자를 나타내기 위해 사용하는 문자 집합입니다.

### --question--

#### --text--

`audio` 요소의 목적은 무엇인가요?

#### --distractors--

사용자가 웹페이지에서 직접 음성을 녹음할 수 있게 합니다.

---

페이지의 `audio` 및 `video` 요소를 스타일링하는 데 사용됩니다.

---

웹페이지에서 클릭 시 텍스트를 들을 수 있게 합니다.

#### --answer--

HTML 문서에 오디오를 추가하는 데 사용됩니다.

### --question--

#### --text--

다음 중 HTML 요소가 아닌 것은 무엇인가요?

#### --distractors--

`img`

---

`h1`

---

`link`

#### --answer--

`byte`

### --question--

#### --text--

앵커 요소 안에서 `target` 속성의 역할은 무엇인가요?

#### --distractors--

사용자가 링크 위에 마우스를 올렸을 때 링크 색상을 정의합니다.

---

사용자가 링크를 클릭한 후 브라우저가 스크롤할 연결된 문서 내 위치를 지정합니다.

---

`link` 요소 안의 앵커 텍스트 폰트 크기를 결정합니다.

#### --answer--

사용자가 링크를 클릭할 때 연결된 문서가 어디에서 열릴지 지정합니다.

### --question--

#### --text--

절대 경로란 무엇인가요?

#### --distractors--

현재 파일을 기준으로 한 상대 경로입니다.

---

변수를 포함하는 경로입니다.

---

와일드카드 문자를 포함하는 경로입니다.

#### --answer--

루트 디렉터리부터 시작하는 리소스에 대한 전체 경로입니다.

### --question--

#### --text--

HTML에서 `link` 요소의 역할은 무엇인가요?

#### --distractors--

같은 웹페이지 내에서 하이퍼링크를 생성하는 데 사용됩니다.

---

웹페이지의 레이아웃과 구조를 정의하는 데 사용됩니다.

---

오디오 또는 비디오 파일을 웹페이지에 임베드하는 데 사용됩니다.

#### --answer--

스타일시트와 사이트 아이콘 같은 외부 리소스에 링크하는 데 사용됩니다.

### --question--

#### --text--

오픈 그래프 프로퍼티 작업에 유효한 값은 무엇인가요?

#### --distractors--

`"og:socialMedia"`

---

`"og:mediaOG"`

---

`"og:openGraph"`

#### --answer--

`"og:title"`

### --question--

#### --text--

다음 중 불리언 속성의 예는 무엇인가요?

#### --distractors--

`href`

---

`src`

---

`width`

#### --answer--

`checked`

### --question--

#### --text--

`img` 요소의 올바른 구문은 무엇인가요?

#### --distractors--

`<img src="image.jpg" alt="Description of the image"></img>`

---

`<img source="image.jpg" desc="Description of the image"></img>`

---

`<img src="image.jpg" desc="Description of the image">`

#### --answer--

`<img src="image.jpg" alt="Description of the image">`

### --question--

#### --text--

`iframe` 요소는 무엇에 사용되나요?

#### --distractors--

이미지에 캡션을 추가하는 데 사용됩니다.

---

미디어의 저작권 정보를 추가하는 데 사용됩니다.

---

같은 웹페이지 내에서 하이퍼링크를 생성하는 데 사용됩니다.

#### --answer--

현재 HTML 문서 내에 다른 문서를 임베드하는 데 사용됩니다.

### --question--

#### --text--

앵커 요소에 대해 유효하지 않은 링크 상태는 무엇인가요?

#### --distractors--

`hover`

---

`visited`

---

`active`

#### --answer--

`href`

### --question--

#### --text--

`title` 요소의 역할은 무엇인가요?

#### --distractors--

사용자가 웹페이지의 소스 코드를 편집할 수 있게 하는 요소입니다.

---

웹페이지 내 요소의 레이아웃과 위치를 정의합니다.

---

HTML 문서에 비디오와 멀티미디어 콘텐츠를 직접 임베드하는 데 사용됩니다.

#### --answer--

문서의 제목을 지정하며 브라우저 탭이나 창에 표시됩니다.

## --quiz--

### --question--

#### --text--

다음 중 현재 컨텍스트의 부모에서 링크를 여는 `target` 속성은 무엇인가요?

#### --distractors--

`_blank`

---

`_self`

---

`_unfencedTop`

#### --answer--

`_parent`

### --question--

#### --text--

HTML 문서의 `UTF-8` 문자 인코딩을 설정하는 데 사용하는 속성은 무엇인가요?

#### --distractors--

`accept`

---

`capture`

---

`enctype`

#### --answer--

`charset`

### --question--

#### --text--

웹에서 HTML의 역할은 무엇인가요?

#### --distractors--

HTML은 스타일과 레이아웃에 사용됩니다.

---

HTML은 페이지에 상호작용을 추가하는 데 사용됩니다.

---

HTML은 소프트웨어 패키지 등록소입니다.

#### --answer--

HTML은 웹 페이지의 내용과 구조를 나타냅니다.

### --question--

#### --text--

퍼블릭 도메인에 해당하는 이미지 라이선스는 무엇인가요?

#### --distractors--

JPG

---

Creative Commons.

---

BSD

#### --answer--

Creative Commons 0.

### --question--

#### --text--

불리언 속성을 `false`로 설정하는 방법은 무엇인가요?

#### --distractors--

속성 값을 `0`로 설정합니다.

---

속성 이름 앞에 `not` 연산자를 사용합니다.

---

속성을 빈 문자열(`""`)로 설정합니다.

#### --answer--

요소에서 속성을 생략합니다.

### --question--

#### --text--

`script` 요소는 무엇에 사용되나요?

#### --distractors--

HTML 문서에 CSS를 임베드하거나 외부 CSS 파일에 링크하는 데 사용됩니다.

---

HTML 문서에 Pascal 코드를 임베드하는 데 사용됩니다.

---

HTML 문서에 C# 코드를 임베드하는 데 사용됩니다.

#### --answer--

HTML 문서에 JavaScript를 임베드하거나 외부 JavaScript 파일에 링크하는 데 사용됩니다.

### --question--

#### --text--

HTML 문서의 제목을 설정하는 데 사용하는 요소는 무엇인가요?

#### --distractors--

`footer`

---

`section`

---

`figcaption`

#### --answer--

`title`

### --question--

#### --text--

`class` 속성은 일반적으로 무엇에 사용되나요?

#### --distractors--

요소에 대한 메타데이터를 임베드하는 데 사용됩니다.

---

HTML 태그 내에서 인라인 스타일을 직접 정의하는 데 사용됩니다.

---

JavaScript 함수에 고유 식별자를 지정하는 데 사용됩니다.

#### --answer--

여러 요소에 스타일 집합을 적용하는 데 사용됩니다.

### --question--

#### --text--

다음 중 일반적으로 사용되는 HTML 엔티티가 아닌 것은 무엇인가요?

#### --distractors--

`&quot;` (큰따옴표).

---

`&copy;` (저작권 기호).

---

`&gt;` (보다 큼 기호).

#### --answer--

`&div;` (div 요소).

### --question--

#### --text--

HTML 문서에 오디오를 추가하는 데 사용하는 요소는 무엇인가요?

#### --distractors--

`media`

---

`video`

---

`hr`

#### --answer--

`audio`

### --question--

#### --text--

HTML 문서에서 페이지에 표시되는 내용은 어디에 배치되나요?

#### --distractors--

`head`와 `body` 요소 사이에 있습니다.

---

`repr` 요소 안에 있습니다.

---

`script` 요소 안에 있습니다.

#### --answer--

`body` 요소 안에 있습니다.

### --question--

#### --text--

오디오가 끝나면 다시 시작하게 하는 속성은 무엇인가요?

#### --distractors--

`multiple`

---

`inputmode`

---

`enctype`

#### --answer--

`loop`

### --question--

#### --text--

`div` 요소의 올바른 구문은 무엇인가요?

#### --distractors--

`<<div>>block container<</div>>`

---

`>>div>>block container>>div>>`

---

`[div]block container[/div]`

#### --answer--

`<div>block container</div>`

### --question--

#### --text--

`target="_self"`는 어떻게 작동하나요?

#### --distractors--

사용자가 링크를 클릭한 후 브라우저가 스크롤할 연결된 문서 내 위치를 지정합니다.

---

연결된 문서를 자동으로 사용자의 컴퓨터에 다운로드합니다.

---

`href` 속성에 지정된 다른 웹사이트로 사용자를 리다이렉트합니다.

#### --answer--

현재 브라우징 컨텍스트에서 링크를 엽니다.

### --question--

#### --text--

상대 경로란 무엇인가요?

#### --distractors--

파일 시스템의 루트 디렉터리부터 파일이나 디렉터리의 정확한 위치를 지정합니다.

---

도메인 이름부터 특정 파일이나 디렉터리까지 전체 URL을 포함합니다.

---

웹사이트나 파일 시스템의 루트 디렉터리를 나타내는 슬래시(`/`)로 항상 시작합니다.

#### --answer--

현재 작업 디렉터리를 기준으로 파일이나 디렉터리 위치를 지정합니다.

### --question--

#### --text--

`link` 요소의 올바른 구문은 무엇인가요?

#### --distractors--

`<link rel="stylesheet" href="./styles.css"></link>`

---

`</link rel="stylesheet" href="./styles.css"></link>`

---

`<<link rel="stylesheet" href="./styles.css"></link>>`

#### --answer--

`<link rel="stylesheet" href="./styles.css" />`

### --question--

#### --text--

오픈 그래프 프로퍼티 작업에 유효하지 않은 값은 무엇인가요?

#### --distractors--

`"og:title"`

---

`"og:type"`

---

`"og:image"`

#### --answer--

`"og:socialMedia"`

### --question--

#### --text--

단락 요소의 올바른 구문은 무엇인가요?

#### --distractors--

`>p>paragraph element>/p>`

---

`<<p>>paragraph element<</p>>`

---

`<p>`

#### --answer--

`<p>paragraph element</p>`

### --question--

#### --text--

`iframe` 요소 이름은 무엇을 의미하나요?

#### --distractors--

인라인 프레이밍

---

내부 프레임

---

인라인 비디오

#### --answer--

인라인 프레임

### --question--

#### --text--

앵커 요소의 활성 상태란 무엇을 의미하나요?

#### --distractors--

링크가 비활성화되어 클릭할 수 없습니다.

---

마우스 커서가 링크 위에 있습니다.

---

사용자가 링크를 방문한 상태입니다.

#### --answer--

사용자가 링크를 적극적으로 클릭하고 있습니다.

## --quiz--

### --question--

#### --text--

다음 중 링크를 새 브라우저 탭에서 여는 것은 무엇인가요?

#### --distractors--

```html
<a target="_parent" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_top" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_self" href="https://freecatphotoapp.com">cat photos</a>
```

#### --answer--

```html
<a target="_blank" href="https://freecatphotoapp.com">cat photos</a>
```

### --question--

#### --text--

다음 중 void 요소는 무엇인가요?

#### --distractors--

`div`

---

`section`

---

`p`

#### --answer--

`img`

### --question--

#### --text--

HTML 엔티티를 올바르게 사용하는 방법은 무엇인가요?

#### --distractors--

`<p>This is an &lt&img /&gt& element</p>`

---

`<p>This is an ;lt&img /;gt& element</p>`

---

`<p>This is an ;lt;img /;gt; element</p>`

#### --answer--

`<p>This is an &lt;img /&gt; element</p>`

### --question--

#### --text--

HTML에서 `ul` 요소는 무엇에 사용되나요?

#### --distractors--

이미지 목록을 그룹화하는 데만 사용됩니다.

---

링크 목록을 그룹화하는 데만 사용됩니다.

---

순서 있는 목록 항목을 그룹화하는 데 사용됩니다.

#### --answer--

글머리 기호 목록 항목을 그룹화하는 데 사용됩니다.

### --question--

#### --text--

HTML 요소에 여러 클래스를 적용하는 올바른 방법은 무엇인가요?

#### --distractors--

```html
<div class="box>>red>>box"></div>
``` 

---

```html
<div class="box<<red<<box"></div>
``` 

---

```html
<div class="box-red-box"></div>
``` 

#### --answer--

```html
<div class="box red-box"></div>
``` 

### --question--

#### --text--

순서 있는 목록 항목을 나타내는 요소는 무엇인가요?

#### --distractors--

`span`

---

`div`

---

`ul`

#### --answer--

`ol` 

### --question--

#### --text--

오픈 그래프 프로퍼티는 무엇에 사용되나요?

#### --distractors--

웹페이지에 대화형 멀티미디어 콘텐츠를 직접 임베드하는 데 사용됩니다.

---

웹사이트에 동적 팝업 광고를 생성하는 데 사용됩니다.

---

웹 서버와 사용자 브라우저 간에 전송되는 민감한 데이터를 암호화하는 데 사용됩니다.

#### --answer--

웹사이트 콘텐츠가 다양한 소셜 미디어 플랫폼에서 어떻게 보일지 설정하는 데 사용됩니다.

### --question--

#### --text--

웹 페이지에 대한 짧은 설명을 설정하는 데 사용하는 것은 무엇인가요?

#### --distractors--

```html
<meta
  add="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  set="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  description="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

#### --answer--

```html
<meta
  name="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

### --question--

#### --text--

SVG는 무엇의 약자인가요?

#### --distractors--

Scalable Vector Graph

---

Scalable Vistor Graphics

---

Site Vector Graphics

#### --answer--

Scalable Vector Graphics 

### --question--

#### --text--

`footer` 요소의 역할은 무엇인가요?

#### --distractors--

웹 페이지에서 관련 캡션과 이미지를 그룹화하는 데 사용됩니다.

---

페이지 내 단락 사이에 수평선을 만드는 데 사용됩니다.

---

페이지 하단에 순서 있는 항목 목록을 만드는 데 사용됩니다.

#### --answer--

페이지 하단에 저작권 콘텐츠나 관련 문서를 그룹화하는 데 사용됩니다.

### --question--

#### --text--

`UTF-8` 문자 인코딩이란 무엇인가요?

#### --distractors--

웹 페이지 제목을 설정하는 데 사용하는 표준화된 문자 인코딩입니다.

---

웹 페이지 설명을 설정하는 데 사용하는 문자 집합입니다.

---

미디어 성능 향상을 위해 요소에 사용하는 특별한 문자 집합입니다.

#### --answer--

웹에서 널리 사용하는 표준화된 문자 인코딩입니다.

### --question--

#### --text--

다음 중 교체된 요소의 예는 무엇인가요?

#### --distractors--

`slot`

---

`div`

---

`p`

#### --answer--

`iframe` 

### --question--

#### --text--

웹 페이지에서 가장 중요한 제목은 무엇인가요?

#### --distractors--

`h6`

---

`h3`

---

`h5`

#### --answer--

`h1` 

### --question--

#### --text--

스타일시트와 사이트 아이콘 같은 외부 리소스에 링크하는 데 사용하는 요소는 무엇인가요?

#### --distractors--

`a`

---

`p`

---

`div`

#### --answer--

`link` 

### --question--

#### --text--

긴급성, 심각성 또는 강한 중요성을 전달하는 데 사용하는 요소는 무엇인가요?

#### --distractors--

`important`

---

`heavy`

---

`strength`

#### --answer--

`strong`

### --question--

#### --text--

`figcaption` 요소의 역할은 무엇인가요?

#### --distractors--

`span` 요소 안의 콘텐츠에 캡션을 제공합니다.

---

`slot` 요소 안의 콘텐츠에 캡션을 제공합니다.

---

`div` 요소 안의 콘텐츠에 캡션을 제공합니다.

#### --answer--

`figure` 요소 안의 콘텐츠에 캡션을 제공합니다.

### --question--

#### --text--

HTML 문서에 `DOCTYPE` 선언을 설정하는 올바른 방법은 무엇인가요?

#### --distractors--

`<DOCTYPE>`

---

`<*DOCTYPE html>`

---

`<<DOCTYPE html>>`

#### --answer--

`<!DOCTYPE html>`

### --question--

#### --text--

HTML 문서에서 `head` 요소의 목적은 무엇인가요?

#### --distractors--

HTML 문서의 앵커 요소 컨테이너입니다.

---

HTML 문서의 이미지 컨테이너입니다.

---

HTML 문서의 제목 요소 컨테이너입니다.

#### --answer--

HTML 문서의 메타데이터 컨테이너입니다.

### --question--

#### --text--

HTML 문서의 언어를 영어로 설정하는 올바른 방법은 무엇인가요?

#### --distractors--

`<html lang-set="en">` 

---

`<html set-lang="en">` 

---

`<html language="en">` 

#### --answer--

`<html lang="en">` 

### --question--

#### --text--

`head` 안에 배치하면 안 되는 요소는 무엇인가요?

#### --distractors--

`link`

---

`meta`

---

`title`

#### --answer--

`img` 

## --quiz--

### --question--

#### --text--

절대 경로와 상대 경로에 대해 올바르지 않은 설명은 무엇인가요?

#### --distractors--

절대 경로는 루트 디렉터리부터 시작하고, 상대 경로는 현재 파일 위치에 따라 달라집니다.

---

상대 경로는 웹사이트 내 링크에 더 짧고 유연하며, 절대 경로는 전체 URL을 포함합니다.

---

절대 경로와 상대 경로 모두 같은 웹사이트 내 파일 링크에 사용할 수 있습니다.

#### --answer--

상대 경로는 상위 디렉터리로 이동하는 `..`를 사용할 수 없지만, 절대 경로는 사용할 수 있습니다.

### --question--

#### --text--

`a` 태그에서 유효하지 않은 target 속성 값은 무엇인가요?

#### --distractors--

`_self`

---

`_parent`

---

`_top`

#### --answer--

`_main`

### --question--

#### --text--

`controls` 요소의 `audio` 속성 목적은 무엇인가요?

#### --distractors--

브라우저가 사용할 오디오 파일 형식을 정의합니다.

---

페이지가 로드될 때 오디오를 자동으로 시작합니다.

---

오디오 파일의 소스 URL을 지정합니다.

#### --answer--

재생, 일시정지, 볼륨 조정 같은 내장 재생 컨트롤을 추가합니다.

### --question--

#### --text--

웹페이지에 대한 간단한 설명을 제공하여 SEO를 향상시키는 요소는 무엇인가요?

#### --distractors--

```html
<meta
 description="describe"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 name="desc"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 description="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

#### --answer--

```html
<meta
 name="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

### --question--

#### --text--

`<meta charset="utf-8" />`는 무엇을 하나요?

#### --distractors--

웹페이지의 언어를 영어로 설정합니다.

---

외부 CSS 스타일시트에 링크합니다.

---

웹페이지 제목을 지정합니다.

#### --answer--

텍스트가 올바르게 표시되도록 문자 인코딩을 정의합니다.

### --question--

#### --text--

HTML에서 `link`와 `script` 요소의 차이는 무엇인가요?

#### --distractors--

`link`는 JavaScript 파일용이고, `script`는 CSS 파일용입니다.

---

`link`와 `script` 모두 웹페이지 스타일링에 사용됩니다.

---

`script`는 스타일 추가용이고, `link`는 JavaScript 실행용입니다.

#### --answer--

`link`는 외부 CSS 파일에서 스타일을 추가하는 데 사용되고, `script`는 JavaScript 코드를 실행하는 데 사용됩니다.

### --question--

#### --text--

HTML 클래스와 ID에 관한 다음 설명 중 올바르지 않은 것은 무엇인가요?

#### --distractors--

클래스 이름은 HTML 문서 전체에서 여러 번 사용할 수 있습니다.

---

`rel` 속성은 연결된 리소스와 HTML 문서 간 관계를 정의합니다.

---

`og:image` 속성은 소셜 미디어 게시물에 표시되는 이미지를 지정합니다.

#### --answer--

ID 이름은 밑줄을 포함할 수 없으며 고유해야 합니다.

### --question--

#### --text--

HTML 제목 요소에 관한 다음 설명 중 올바른 것은 무엇인가요?

#### --distractors--

텍스트 크기는 `h1`에서 `h6`로 커집니다.

---

모든 제목 요소의 텍스트 크기는 동일합니다.

---

`h3`가 `h2`보다 큽니다.

#### --answer--

텍스트 크기는 `h1`에서 `h6`로 작아집니다.

### --question--

#### --text--

HTML 요소에 관한 잘못된 설명은 무엇인가요?

#### --distractors--

HTML 요소는 웹페이지의 기본 구성 요소입니다.

---

일부 코드베이스는 void 요소 안에 슬래시(/)를 포함합니다.

---

불리언 속성은 태그에 존재하거나 없으면 작동합니다.

#### --answer--

Void 요소는 내용을 포함하며 시작 태그만 있습니다.

### --question--

#### --text--

HTML에서 void 요소에 관한 올바른 설명은 무엇인가요?

#### --distractors--

void 요소는 항상 닫기 전에 슬래시 /를 포함해야 합니다.

---

void 요소는 절대 슬래시 /를 포함해서는 안 됩니다.

---

모든 HTML 버전에서 셀프 클로징 void 요소에 슬래시 /가 필수입니다.

#### --answer--

`<img>`와 `<img/>` 모두 현대 HTML에서 허용됩니다.

### --question--

#### --text--

프로그래밍에서 주석의 주요 목적은 무엇인가요?

#### --distractors--

코드를 더 빠르게 실행하게 합니다.

---

브라우저가 콘텐츠를 표시하는 방식을 변경합니다.

---

코드 내 임시 데이터를 저장합니다.

#### --answer--

자신과 다른 개발자를 위해 코드에 메모를 남깁니다.

### --question--

#### --text--

현재 브라우징 컨텍스트와 동일한 컨텍스트에서 링크를 여는 target 속성은 무엇인가요?

#### --distractors--

`_blank`

---

`_top`

---

`_parent`

#### --answer--

`_self`

### --question--

#### --text--

HTML에서 `em` 요소의 목적은 무엇인가요?

#### --distractors--

텍스트를 더 크게 만듭니다.

---

텍스트 색상을 변경합니다.

---

강조를 위해 단어에 밑줄을 긋습니다.

#### --answer--

기울임꼴로 만들어 중요한 텍스트를 강조합니다.

### --question--

#### --text--

HTML에서 `strong` 요소는 무엇을 하나요?

#### --distractors--

폰트 크기를 키웁니다.

---

텍스트 색상을 변경합니다.

---

텍스트에 밑줄을 긋습니다.

#### --answer--

중요성을 나타내기 위해 텍스트를 굵게 만듭니다.

### --question--

#### --text--

HTML에서 `figure`와 `figcaption` 요소의 주요 차이는 무엇인가요?

#### --distractors--

`figure`는 캡션에 사용되고, `figcaption`는 이미지나 도표 같은 관련 콘텐츠를 그룹화하는 데 사용됩니다.

---

`figcaption`는 항상 `figure` 요소 밖에 배치해야 합니다.

---

차이가 없으며 둘 다 같은 용도로 사용됩니다.

#### --answer--

`figure`는 이미지나 도표 같은 관련 콘텐츠를 그룹화하고, `figcaption`는 그 콘텐츠에 캡션을 제공합니다.

### --question--

#### --text--

HTML 엔티티를 사용하는 이유는 무엇인가요?

#### --distractors--

텍스트에 스타일을 추가하기 위해서입니다.

---

웹페이지에 하이퍼링크를 만들기 위해서입니다.

---

특수 문자의 폰트 크기를 키우기 위해서입니다.

#### --answer--

HTML에서 `<`와 `&` 같은 예약 문자를 나타내기 위해서입니다.

### --question--

#### --text--

오픈 그래프 태그에 관한 올바른 설명은 무엇인가요?

#### --distractors--

오픈 그래프 태그는 CSS로 웹페이지 스타일링에 사용됩니다.

---

오픈 그래프 태그는 SEO에만 사용되며 소셜 미디어에는 영향이 없습니다.

---

오픈 그래프 태그는 HTML의 `body` 섹션 안에 배치해야 합니다.

#### --answer--

`og:title` 속성은 콘텐츠가 소셜 미디어에서 공유될 때 표시되는 제목을 설정합니다.

### --question--

#### --text--

이미지 포맷에 관한 잘못된 설명은 무엇인가요?

#### --distractors--

WEBP와 AVIF는 현대 이미지 최적화에 더 좋습니다.

---

SVG 이미지는 품질 손실 없이 크기 조절이 가능합니다.

---

압축은 이미지 파일 크기를 줄이는 데 도움이 됩니다.

#### --answer--

PNG와 JPG가 오늘날 최적화된 이미지에 가장 좋은 포맷입니다.

### --question--

#### --text--

`loop` 요소의 `audio` 속성은 무엇을 하나요?

#### --distractors--

오디오를 한 번 재생합니다.

---

한 번 재생 후 오디오를 멈춥니다.

---

오디오 재생 속도를 늦춥니다.

#### --answer--

오디오를 계속 반복 재생합니다.

### --question--

#### --text--

HTML 보일러플레이트를 가장 잘 설명하는 것은 무엇인가요?

#### --distractors--

웹 개발자가 문서에 워터마크를 추가하는 특별한 도구입니다.

---

웹페이지 로딩 속도를 높이는 브라우저 플러그인입니다.

---

HTML에서 애니메이션 전용 코딩 프레임워크입니다.

#### --answer--

웹 개발에 필요한 필수 HTML 구조를 포함한 미리 정의된 템플릿입니다.
