---
id: 66edc25ae5ea80bf6f785552
title: اختبار قصير لمتغيرات وأنواع بيانات JavaScript
challengeType: 8
dashedName: quiz-javascript-variables-and-data-types
---

# --description--

لنجاح الاختبار القصير، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي مما يلي يصف JavaScript بشكل أفضل؟

#### --distractors--

لغة ترميز تُستخدم لتصميم مواقع الويب.

---

لغة برمجة من جانب الخادم.

---

نظام إدارة قواعد بيانات.

#### --answer--

لغة برمجة تُستخدم لتطوير الويب.

### --question--

#### --text--

أي مما يلي ليس نوع بيانات في JavaScript؟

#### --distractors--

`Number`

---

`Undefined`

---

`Object`

#### --answer--

`Double`

### --question--

#### --text--

أي مما يلي ليس إعلان متغير صالح في JavaScript؟

#### --distractors--

`const x = 0;`

---

`let x;`

---

`var x;`

#### --answer--

`public Int x;`

### --question--

#### --text--

أي مما يلي اصطلاح شائع لتسمية المتغيرات في JavaScript؟

#### --distractors--

حالة JavaScript.

---

حالة البرق.

---

حالة الزرافة.

#### --answer--

حالة الجمل.

### --question--

#### --text--

ما الفرق بين إعلانات المتغيرات `let` و `const` في JavaScript؟

#### --distractors--

`const` لا يمكن إعادة تعيينه أو إعادة إعلانه. `let` يمكن إعادة تعيينه وإعادة إعلانه.

---

`const` يمكن إعادة تعيينه وإعادة إعلانه. `let` لا يمكن إعادة تعيينه أو إعادة إعلانه.

---

`const` يمكن إعادة تعيينه ولكن لا يمكن إعادة إعلانه. `let` يمكن إعادة إعلانه، ولكن لا يمكن إعادة تعيينه.

#### --answer--

`const` لا يمكن إعادة تعيينه أو إعادة إعلانه. `let` يمكن إعادة تعيينه، ولكن لا يمكن إعادة إعلانه.

### --question--

#### --text--

لماذا تُعتبر السلاسل النصية غير قابلة للتغيير في JavaScript؟

#### --distractors--

لا يمكنك إنشاء سلاسل نصية باستخدام المتغيرات.

---

يمكن إنشاء السلاسل النصية فقط باستخدام القيم الحرفية.

---

يمكنك تغيير السلاسل النصية، ولكن فقط عبر المتغيرات العامة.

#### --answer--

بمجرد إنشاء سلسلة نصية، لا يمكنك تغيير حروفها مباشرة.

### --question--

#### --text--

كيف تطبع السلسلة النصية `Hello, world!` في وحدة التحكم في JavaScript؟

#### --distractors--

`print("Hello, world!")`

---

`alert("Hello, world");`

---

`System.out.println("Hello, world!");`

#### --answer--

`console.log("Hello, world!");`

### --question--

#### --text--

ماذا يفعل الكود أدناه؟

```js
let hello = "Hello";
hello += " world";
console.log(hello);
```

#### --distractors--

يعيد تعيين القيمة `" world"` للمتغير `hello`، ويطبعها في وحدة التحكم.

---

يطبع عدد الحروف في `Hello world` في وحدة التحكم.

---

يجمع بين `"Hello"` و `" world"` في سلسلة جديدة ويطبعها، لكن قيمة `hello` تبقى `"Hello"`.

#### --answer--

يجمع القيمة الحالية لـ `hello` مع السلسلة `" world"`، يعيد تعيينها إلى `hello`، ويطبعها في وحدة التحكم.

### --question--

#### --text--

أي حرف في JavaScript يُستخدم لتحديد نهاية العبارة؟

#### --distractors--

`:`

---

`,`

---

`.`

#### --answer--

`;`

### --question--

#### --text--

أي مما يلي صيغة صحيحة لتعليق سطر واحد في JavaScript؟

#### --distractors--

`# This is a single line comment`

---

`-- This is a single line comment`

---

`/* This is a single line comment */`

#### --answer--

`// This is a single line comment`

### --question--

#### --text--

الكتابة الديناميكية في JavaScript تعني:

#### --distractors--

تحتاج إلى إعلان نوع بيانات المتغير قبل استخدامه.

---

لا يمكنك تعيين قيمة من نوع بيانات إلى متغير من نوع مختلف.

---

تُجري JavaScript فحص النوع قبل التنفيذ، مما يعني أن أخطاء النوع تُرمي قبل تشغيل الشفرة.

#### --answer--

تستنتج JavaScript النوع بناءً على القيمة المعينة للمتغير.

### --question--

#### --text--

أي مما يلي يطبع نوع المتغير `x` في وحدة التحكم؟

#### --distractors--

`console.log(x.type);`

---

`console.log(typeOf(x));`

---

`console.log(isNaN(x));`

#### --answer--

`console.log(typeof x);`

### --question--

#### --text--

ما الناتج من `console.log(typeof null);`؟

#### --distractors--

`"undefined"`

---

`"null"`

---

`"NaN"`

#### --answer--

`"object"`

### --question--

#### --text--

أي مما يلي تعليق متعدد الأسطر صالح في JavaScript؟

#### --distractors--

```js
// This is a
// multi-line
// comment
```

---

```py
'''
This is a
multi-line
comment
'''
```

---

```html
<!--
This is a
multi-line
comment
-->
```

#### --answer--

```js
/*
This is a
multi-line
comment
*/
```

### --question--

#### --text--

أي مما يلي يعلن متغيرًا في JavaScript لا يمكن تغييره؟

#### --distractors--

`var x = 0;`

---

`let x = 0;`

---

`final x = 0;`

#### --answer--

`const x = 0;`

### --question--

#### --text--

أي مما يلي اسم متغير صالح في JavaScript؟

#### --distractors--

`tart@n`

---

`4cats`

---

`!!dogs`

#### --answer--

`$number`

### --question--

#### --text--

أي مما يلي ليس طريقة صحيحة لربط السلاسل النصية في JavaScript؟

#### --distractors--

`console.log(string1 + " " + string2);`

---

`console.log("string1 " + string2);`

---

`console.log(string1.concat(" ", string2));`

#### --answer--

`console.log(string1 string2);`

### --question--

#### --text--

أي من أسماء المتغيرات التالية يستخدم حالة الجمل بشكل صحيح؟

#### --distractors--

`a_long_variable_name`

---

`alongvariableName`

---

`ALongVariableName`

#### --answer--

`aLongVariableName`

### --question--

#### --text--

لماذا من المفيد استخدام الفواصل المنقوطة صراحة رغم وجود الإدراج التلقائي للفواصل في JavaScript؟

#### --distractors--

لزيادة سرعة تنفيذ الشفرة.

---

للحفاظ على التناسق مع لغات البرمجة الأخرى.

---

للسماح بتصحيح الأخطاء وتتبعها بشكل أفضل.

#### --answer--

لتحسين قابلية قراءة الشفرة وموثوقيتها.

### --question--

#### --text--

أي مما يلي ليس نوع بيانات في JavaScript؟

#### --distractors--

`Symbol`

---

`Boolean`

---

`Null`

#### --answer--

`Float`
