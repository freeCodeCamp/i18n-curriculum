---
id: 66edd3403d7077eece6dc4b6
title: Quiz sulla validazione dei moduli con JavaScript
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale metodo si usa per verificare se il valore di un elemento `input` soddisfa le regole di validazione?

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

Cosa succede se il metodo `checkValidity()` trova un `input` non valido?

#### --distractors--

Il modulo viene inviato automaticamente.

---

Non succede nulla.

---

Il valore `input` viene cancellato.

#### --answer--

Il browser mostra un messaggio di validazione.

### --question--

#### --text--

Quale metodo si usa per personalizzare il messaggio mostrato in caso di fallimento della validazione?

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

Qual è il comportamento di validazione predefinito per i campi obbligatori quando il modulo viene inviato?

#### --distractors--

Il modulo viene inviato anche se i campi obbligatori sono vuoti.

---

Il modulo si invia ma mostra un avviso.

---

Il modulo resetta i valori `input`.

#### --answer--

Il browser blocca l'invio del modulo se i campi obbligatori sono vuoti.

### --question--

#### --text--

Quale azione non fa controllare un modulo HTML per errori?

#### --distractors--

Cambiare il valore `input`.

---

Inviare il modulo.

---

Chiamare `reportValidity()`.

#### --answer--

Cliccare un pulsante di reset.

### --question--

#### --text--

Cosa restituisce `checkValidity()` quando tutti gli input del modulo sono validi?

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

Quando il metodo `checkValidity()` blocca l'invio del modulo?

#### --distractors--

Quando il modulo non ha un pulsante di invio.

---

Quando il `action` del modulo è vuoto.

---

Quando il metodo del modulo è POST.

#### --answer--

Quando il modulo ha almeno un `input` non valido (e il comportamento predefinito dell'invio del modulo è impedito).

### --question--

#### --text--

Cosa succede se si chiama `checkValidity()` ma un campo non supera la validazione?

#### --distractors--

Il modulo si invia senza errori.

---

I valori `input` vengono cancellati.

---

Il modulo si chiude immediatamente.

#### --answer--

Il campo non valido viene evidenziato e appare un messaggio.

### --question--

#### --text--

Quale azione fa eseguire `checkValidity()`?

#### --distractors--

Resettare il modulo.

---

Cambiare un valore `input`.

---

Cliccare un tag àncora.

#### --answer--

Inviare manualmente il modulo.

### --question--

#### --text--

Qual è lo scopo di chiamare `checkValidity()` prima di inviare un modulo?

#### --distractors--

Per inviare automaticamente il modulo.

---

Per ricaricare la pagina.

---

Per cancellare tutti i campi `input`.

#### --answer--

Per verificare se tutti gli elementi `input` rispettano i vincoli specificati.

### --question--

#### --text--

Qual è lo scopo principale di `e.preventDefault()`?

#### --distractors--

Ricaricare il modulo.

---

Cancellare i dati del modulo.

---

Resettare i valori del modulo.

#### --answer--

Bloccare l'azione predefinita di un evento.

### --question--

#### --text--

Quando usato sull'evento di invio di un modulo, cosa fa `e.preventDefault()`?

#### --distractors--

Invia il modulo.

---

Cancella tutti i campi del modulo.

---

Disabilita temporaneamente il modulo.

#### --answer--

Impedisce che il modulo venga inviato.

### --question--

#### --text--

In quale scenario `e.preventDefault()` è più utile?

#### --distractors--

Quando si gestisce il clic di un pulsante che invia un modulo.

---

Quando si chiude una finestra modale.

---

Quando si apre una nuova pagina.

#### --answer--

Quando si invia un modulo e si impedisce il ricaricamento della pagina.

### --question--

#### --text--

Cosa succede se dimentichi di chiamare `e.preventDefault()` sull'evento di invio di un modulo?

#### --distractors--

Tutti i campi di input vengono impostati ai valori predefiniti.

---

Il modulo non si invia.

---

I dati del modulo vengono persi.

#### --answer--

Il modulo si invia e la pagina si aggiorna.

### --question--

#### --text--

Quale comportamento predefinito si può impedire usando `e.preventDefault()`?

#### --distractors--

Modificare un modulo.

---

Renderizzare il modulo.

---

Ricaricare il modulo.

#### --answer--

Il comportamento predefinito causato dall'invio di un modulo.

### --question--

#### --text--

Cosa fa scattare l'evento di invio di un modulo?

#### --distractors--

Quando cambia un campo di input.

---

Quando il modulo viene resettato.

---

Quando l'azione del modulo viene cancellata.

#### --answer--

Quando si clicca il pulsante di invio del modulo o si preme Invio.

### --question--

#### --text--

Dove si gestisce solitamente l'evento di invio in JavaScript?

#### --distractors--

Su un elemento di input di testo.

---

Su qualsiasi campo del modulo.

---

Su un elemento pulsante esterno al modulo.

#### --answer--

Sull'elemento modulo stesso.

### --question--

#### --text--

Cosa fa l'evento di invio per impostazione predefinita?

#### --distractors--

Valida il modulo ma non lo invia.

---

Cancella i campi del modulo.

---

Annulla l'invio del modulo.

#### --answer--

Invia i dati del modulo al server specificato nell'azione del modulo.

### --question--

#### --text--

Quale evento devi ascoltare per gestire i dati del modulo quando viene inviato in JavaScript?

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

Qual è il valore predefinito dell'attributo `method` in un modulo HTML?

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
