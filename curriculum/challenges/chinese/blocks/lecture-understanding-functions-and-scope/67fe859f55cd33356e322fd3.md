---
id: 67fe859f55cd33356e322fd3
title: Python 中函数如何工作？
challengeType: 19
dashedName: how-do-functions-work-in-python
---

# --description--

函数是可重用的代码片段，当你调用它们时会运行。许多编程语言随附内置函数，使入门更容易。Python 也不例外，我们已经在之前的课程中介绍了一些内置函数，比如 `print()`。

另一个有用的内置函数是 `input()`，它允许你提示用户输入：

```python
name = input('What is your name?') # User types "Kolade" and presses Enter  
print('Hello', name) # Output: Hello Kolade
```

另一方面，`int()` 将数字、布尔值和数字字串转换为整数：

```python
print(int(3.14)) # 3
print(int('42')) # 42
print(int(True)) # 1
print(int(False)) # 0 
```

你也可以编写你自己的自定义函数。为此，你使用 `def` 关键字，后跟你想给函数命名的名称、一对括号和冒号。然后在新的一行，编写你的函数应运行的代码。函数运行的代码也称为函数的体。

这是一个名为 `hello` 的自定义函数示例，它将字串 `Hello World` 打印到终端：

```python
def hello():
    print('Hello World')
```

要运行该函数，你需要用它的名称调用它，后面跟一对括号：

```python
hello() # Hello World
```

注意 `print('Hello World')` 前的缩排。正如你可能从之前的课程中回忆的那样，Python 依赖缩排来确定哪些语句组属于同一组。这些语句组称为代码块。

这是另一个简单的函数，用于在终端打印两个数字的和：

```python
def calculate_sum(a, b):
    print(a + b)
```

你可以看到我们的函数 `calculate_sum` 在它的括号中有 `a` 和 `b`，它们由逗号分隔。这些被称为参数。将参数视为占位符变量，作为你调用函数时传入值的“槽”。

要使用参数，你必须传入“arguments”。arguments 是你在调用函数时传递给函数的值。

以下是如何调用 `calculate_sum` 函数来将数字 `3` 和 `1` 相加：

```python
calculate_sum(3, 1) # 4
```

如果你调用函数时参数数量不正确，你将得到一个 `TypeError`：

```python
calculate_sum()

# TypeError: calculate_sum() missing 2 required positional arguments: 'a' and 'b'
```

函数还使用特殊的 `return` 关键字来退出函数并返回一个值。如果你不显式使用 `return`，Python 默认会返回 `None`。

这是一个示例：

```python
def calculate_sum(a, b):
    print(a + b)

my_sum = calculate_sum(3, 1) # 4
print(my_sum) # None
```

你可以看到 `calculate_sum` 函数打印了 `a` 和 `b` 的和，但它没有显式返回任何内容。所以当我们将它的结果赋值给 `my_sum` 时，值实际上是 `None`。为了解决这个问题，你可以使用 `return` 关键字来返回结果：

```python
def calculate_sum(a, b):
    return a + b

my_sum = calculate_sum(3, 1)
print(my_sum) # 4
```

现在，`calculate_sum` 返回 `a` 和 `b` 的和，该和被保存到 `my_sum` 中。

# --questions--

## --text--

Python 中函数的默认返回值是什么？

## --answers--

`0`

### --feedback--

如果函数没有 `return` 语句，它会自动返回此值。

---

`None`

---

`""`（空字符串）

### --feedback--

如果函数没有 `return` 语句，它会自动返回此值。

---

`False`

### --feedback--

如果函数没有 `return` 语句，它会自动返回此值。

## --video-solution--

2

## --text--

函数中占位符变量的术语是什么？

## --answers--

参数

---

争论

### --feedback--

它是在函数声明中定义的，而不是在调用时定义的。

---

返回值

### --feedback--

它是在函数声明中定义的，而不是在调用时定义的。

---

装饰器

### --feedback--

它是在函数声明中定义的，而不是在调用时定义的。

## --video-solution--

1

## --text--

在 Python 中使用什么关键字来定义自定义函数？

## --answers--

`function`

### --feedback--

Python 使用特定关键字来开始一个函数定义。

---

`def`

---

`func`

### --feedback--

Python 使用特定关键字来开始一个函数定义。

---

`define`

### --feedback--

Python 使用特定关键字来开始一个函数定义。

## --video-solution--

2
