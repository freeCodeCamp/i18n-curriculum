---
id: 66edd43cded6bff30944b676
title: 递归测验
challengeType: 8
dashedName: quiz-recursion
---

# --description--

要通过测验，你必须正确回答以下 10 道题中的至少 9 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是编程中的递归？

#### --distractors--

一种排序数组的`方法`。

---

一个永不结束的循环。

---

一个返回 `undefined` 的函数。

#### --answer--

一个函数调用自身的进程。

### --question--

#### --text--

以下哪项是递归的示例？

#### --distractors--

```js
function factorial(n) {
  let result = 1;

  while (n > 0) {
    result = result * n;
    n--;
  }

  return result;
}
```

---

```js
function factorial(n) {
  const arr = Array(n).fill().map((_, i) => i + 1);
  return arr.reduce((acc, curr) => acc * curr, 1);
}
```

---

```js
function factorial(n) {
  let result = 1;

  for (let i = n; i > 0; i--) {
    result *= i;
  }

  return result;
}
```

#### --answer--

```js
function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  }
  return n * factorial(n - 1);
}
```

### --question--

#### --text--

以下函数将返回什么？

```js
function sum(n) {
  if (n === 0) return 0;
  return n + sum(n - 1);
}
sum(3);
```

#### --distractors--

3

---

0

---

1

#### --answer--

6

### --question--

#### --text--

`mystery` 函数将被调用多少次？

```javascript
function mystery(n) {
  if (n <= 1) return 1;
  return mystery(n - 2);
}
mystery(5);
```

#### --distractors--

2

---

5

---

4

#### --answer--

3

### --question--

#### --text--

以下关于递归哪项是正确的？

#### --distractors--

它永远不应该有基本情形。

---

它应仅用于 Fibonacci 序列。 

---

它应仅用于阶乘。

#### --answer--

它应始终有一个基本情形。

### --question--

#### --text--

以下哪个选项适合作为给定示例的基本情形？

```js
function countDownToZero(number) {
  // Base case goes here.

  console.log(number);
  countDownToZero(number - 1);
}
```

#### --distractors--

```js
if (number > 0) {
  return;
}
```

---

```js
if (number !== 0) {
  return;
}
```

---

```js
if (number === 0) {
  return;
}
```

#### --answer--

```js
if (number < 0) {
  return;
}
```

### --question--

#### --text--

为什么递归必须有基本情形？

#### --distractors--

确保函数始终有返回值。

---

为了减少函数调用的次数。

---

为了确保所有递归调用按正确顺序执行且不会抛出 `RecursionError`。

#### --answer--

为函数提供一种方法，以跳出其递归调用并防止无限循环。

### --question--

#### --text--

这个递归的函数会做什么？

```js
function repeatString(str) {
  return str + repeatString(str);
}
```

#### --distractors--

返回该字串两次。

---

返回 `undefined`。

---

创建一个空的字串。

#### --answer--

导致栈溢出。

### --question--

#### --text--

什么是调用栈？

#### --distractors--

已执行的函数调用列表。

---

一个由函数返回的值的列表。

---

一个用于精确调用一个函数三次的特殊函数。

#### --answer--

一种跟踪函数调用及其执行顺序的数据结构。

### --question--

#### --text--

这个递归的函数做什么？

```js
function changeString(str) {
  if (str === "") return "";
  return changeString(str.slice(1)) + str[0];
}
```

#### --distractors--

复制一个字串。

---

移除元音。

---

删除空格。

#### --answer--

反转一个字串。

