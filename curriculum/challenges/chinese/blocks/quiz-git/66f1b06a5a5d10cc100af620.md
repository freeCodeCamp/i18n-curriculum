---
id: 66f1b06a5a5d10cc100af620
title: Git 测验
challengeType: 8
dashedName: quiz-git
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是 `git`？

#### --distractors--

一种流行的无障碍工具类型。

---

软件项目中常用的 `linter`。

---

一种特殊类型的 JavaScript 库。

#### --answer--

版本控制系统允许你跟踪和管理你项目中的更改。

### --question--

#### --text--

以下哪一个是流行的基于云的版本控制提供商？

#### --distractors--

Spring Boot

---

Laravel

---

混音

#### --answer--

GitHub

### --question--

#### --text--

`git config` 命令的作用是什么？

#### --distractors--

设置用于变基你更改的配置变量。

---

设置仅管理 GitLab 上远程仓库的配置。

---

设置解决合并冲突的配置。

#### --answer--

设置负责 Git 在你的机器上如何运行的配置变量。

### --question--

#### --text--

哪个命令显示你机器上当前安装的 Git 版本？

#### --distractors--

`git --versions`

---

`git --setVersion`

---

`git --set`

#### --answer--

`git --version`

### --question--

#### --text--

以下哪项是用于在不离开命令行的情况下执行 GitHub 特定任务的工具？

#### --distractors--

GitHub CI/CD

---

GitHub Actions

---

GitHub 页面

#### --answer--

GitHub CLI

### --question--

#### --text--

以下哪项是允许你直接在你的 GitHub 仓库中自动化工作流的特色，包括创建、测试和部署你的代码？

#### --distractors--

GitHub 管道

---

GitHub 云端

---

GitHub API

#### --answer--

GitHub Actions

### --question--

#### --text--

以下哪个命令将初始化一个空的 `git` 仓库？

#### --distractors--

`git status`

---

`git set`

---

`git log`

#### --answer--

`git init`

### --question--

#### --text--

`git push` 命令的作用是什么？

#### --distractors--

此命令用于将整个 Git 历史推送到 GitHub。

---

此命令用于将远程仓库的更改推送到你的本地机器。

---

此命令用于将你的代码推送到 GitHub 并自动合并到 `main` 分支。

#### --answer--

此命令用于将你的局部的提交推送到远程仓库。

### --question--

#### --text--

以下哪个 `Command` 用于克隆仓库？

#### --distractors--

`git cloning`

---

`git cloned`

---

`git clones`

#### --answer--

`git clone`

### --question--

#### --text--

以下哪个命令用于列出所有之前的提交，并显示作者、提交日期、提交消息和提交哈希等有用信息？

#### --distractors--

`git rebase`

---

`git status`

---

`git commit`

#### --answer--

`git log`

### --question--

#### --text--

以下哪个命令将列出与你的局部 `Git` 仓库关联的所有远程仓库？

#### --distractors--

`git rebase -v`

---

`git pull -v`

---

`git checkout -v`

#### --answer--

`git remote -v`

### --question--

#### --text--

`git reset` 命令的作用是什么？

#### --distractors--

此命令允许你重置所有解决合并冲突的尝试。

---

此命令允许你重置所有对 `git rebase` 的尝试。

---

此命令允许你重置仓库的所有提交。

#### --answer--

此 `Command` 允许你重置分支的当前状态。

### --question--

#### --text--

什么是拉取请求？

#### --distractors--

将一个分支上的一系列提交合并到另一个分支的请求。

---

一个解析所有合并冲突的请求。

---

删除仓库中所有提交的请求。

#### --answer--

将你分支中的更改拉取到目标分支的请求。

### --question--

#### --text--

`main` 分支在一个项目中表现什么？

#### --distractors--

它表现为一个应用的主要文档分支。

---

它表现为一个应用的热修复分支。

---

它表现为一个应用的主要特色分支。

#### --answer--

它表现为应用的主分支或生产分支。

### --question--

#### --text--

GitHub Pages 最适合用于什么？

#### --distractors--

最好部署 C# 和 Unity 游戏。

---

它最适合部署仅包含 Python 的应用。

---

它最适合部署后端密集型网站。

#### --answer--

它最适合部署静态网站。

### --question--

#### --text--

以下哪个命令用于显示你工作目录的当前状态？

#### --distractors--

`git show`

---

`git check`

---

`git update`

#### --answer--

`git status`

### --question--

#### --text--

你使用什么 `Command` 来从远程仓库获取更改？

#### --distractors--

`git get`

---

`git sync`

---

`git update`

#### --answer--

`git fetch`

### --question--

#### --text--

什么是 `.gitignore` 文件？

#### --distractors--

一种用于显示你整个 `git` 历史记录的特殊类型文件。

---

一种用于显示你更改差异的特殊类型文件。

---

一种用于解析项目冲突的特殊类型文件。

#### --answer--

一种特殊类型的文件，用于告诉 `git` 不跟踪项目中的某些文件和文件夹。

### --question--

#### --text--

以下哪个命令用于列出你所有的局部的分支？

#### --distractors--

`git show`

---

`git log`

---

`git list`

#### --answer--

`git branch`

### --question--

#### --text--

以下哪个命令用于将一系列提交从一个分支移动或合并到另一个分支？

#### --distractors--

`git remote`

---

`git merge`

---

`git switch`

#### --answer--

`git rebase`

