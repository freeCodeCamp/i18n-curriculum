---
id: 66edd630f7666cfa54b404d0
title: اختبار جافاسكريبت غير المتزامن
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هو البرمجة غير المتزامنة؟

#### --distractors--

طريقة لتنفيذ الشفرة بشكل متسلسل.

---

تقنية لمعالجة الأخطاء في شفرتك.

---

طريقة لإجراء طلبات الشبكة، عادة لاسترجاع أو إرسال بيانات إلى الخادم.

#### --answer--

طريقة لتنفيذ الشفرة بشكل متزامن دون حجب الخيط الرئيسي.

### --question--

#### --text--

ما هو الخيط؟

#### --distractors--

دالة تُستخدم لاسترجاع البيانات من الخادم.

---

برنامج ينفذ شفرة جافاسكريبت في متصفح الويب.

---

نوع خاص من الدوال يُمرر كمعلمة إلى دالة أخرى.

#### --answer--

متتالية من التعليمات يمكن تنفيذها بشكل مستقل عن تدفق البرنامج الرئيسي.

### --question--

#### --text--

ما هو الغرض من واجهة برمجة التطبيقات Fetch في جافاسكريبت؟

#### --distractors--

لإدارة استدعاءات راجعة غير متزامنة.

---

لإنشاء وإدارة ملفات تعريف الارتباط.

---

لعرض محتوى HTML ديناميكي.

#### --answer--

لإجراء طلبات شبكة إلى الخوادم.

### --question--

#### --text--

أي طريقة HTTP تُستخدم لإرسال بيانات إلى الخادم للمعالجة؟

#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--

أي من الطرق التالية هي الطريقة الصحيحة لجلب البيانات من واجهة برمجة التطبيقات؟

#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--

ما هو الوعد في جافاسكريبت؟

#### --distractors--

طريقة لمزامنة عمليات متعددة عند العمل مع واجهة برمجة التطبيقات Fetch.

---

نوع خاص من دوال الاستدعاء الراجع التي يجب استخدامها في جميع الدوال عالية المستوى.

---

تقنية تُستخدم لمعالجة الأخطاء عند العمل مع واجهة برمجة التطبيقات Fetch.

#### --answer--

كائن يمثل إتمام عملية غير متزامنة في المستقبل (أو فشلها).

### --question--

#### --text--

ماذا يسمح لك تسلسل الوعود أن تفعل؟

#### --distractors--

يسمح لك بتنفيذ عمليات متزامنة متعددة بالتوازي.

---

يسمح لك بمعالجة العمليات المتزامنة بشكل أكثر كفاءة.

---

يسمح لك بإعادة محاولة العمليات الفاشلة تلقائيًا.

#### --answer--

يسمح لك بأداء متتالية من العمليات غير المتزامنة واحدة تلو الأخرى.

### --question--

#### --text--

أي طريقة تُستخدم لمعالجة إتمام الوعد بنجاح؟

#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--

أي طريقة تُستخدم لمعالجة الأخطاء في تسلسل الوعود؟

#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--

أي من الطرق التالية ليست طريقة HTTP صالحة؟

#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--

ما هي الصيغة الصحيحة لإنشاء دالة غير متزامنة؟

#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--

ماذا يفعل الكلمة المفتاحية `await` في دالة غير متزامنة؟

#### --distractors--

ينشئ وعدًا جديدًا.

---

ينفذ الشفرة فورًا دون انتظار ويُرجع كائنًا.

---

يسجل نتيجة الوعد ويُرجع `null`.

#### --answer--

يوقف تنفيذ الدالة حتى يُحل الوعد.

### --question--

#### --text--

أي من واجهات برمجة التطبيقات التالية تُستخدم لتوفير طريقة للمواقع لطلب موقع المستخدم؟

#### --distractors--

واجهة برمجة التطبيقات Locator

---

واجهة برمجة التطبيقات Geo

---

واجهة برمجة التطبيقات Locate

#### --answer--

واجهة برمجة التطبيقات Geolocation

### --question--

#### --text--

أي من الخصائص التالية تُحمّل السكريبت بشكل غير متزامن لكنها تنتظر حتى يُحلل مستند HTML بالكامل قبل تشغيل السكريبت؟

#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--

ماذا سترجع الشفرة التالية؟

```js
fetch('https://api.example.com/data')
```

#### --distractors--

خطأ يقول إن البيانات تالفة.

---

خطأ يقول إنه لا يمكن جلب البيانات.

---

لن يُرجع شيء وسيتابع جافاسكريبت إلى الدالة التالية في السكريبت.

#### --answer--

وعد `Promise` يُحل إلى كائن `Response`.

### --question--

#### --text--

ما هي طريقة HTTP الافتراضية لواجهة برمجة التطبيقات Fetch؟

#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--

أي من الطرق التالية تُستخدم لحذف الموارد على الخادم؟

#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--

أي من الطرق التالية هي الطريقة الصحيحة لإنشاء وعد؟

#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--

أي من الخصائص التالية تُخبر المتصفح بتحميل ملف السكريبت بشكل غير متزامن أثناء استمرار تحليل مستند HTML؟

#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--

أي طريقة من واجهة برمجة التطبيقات Geolocation تُستخدم للحصول على الموقع الحالي للجهاز؟

#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`

