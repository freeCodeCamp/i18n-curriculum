---
id: 67358be1c7903489c0a7db78
title: Quiz sobre Maps e Sets em JavaScript
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

O que é um Set em JavaScript?

#### --distractors--

Uma estrutura de dados que permite valores duplicados.

---

Uma estrutura de dados que armazena apenas strings e números.

---

Uma coleção que não permite a exclusão de seus elementos.

#### --answer--

Uma coleção na qual um valor pode ocorrer apenas uma vez.

### --question--

#### --text--

Qual dos seguintes métodos está disponível em Maps mas não em Sets?

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

Qual método é usado para adicionar elementos a um `Set`?

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

Como você verifica se um Set contém um determinado valor?

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

Qual destes é um ótimo caso de uso para Sets?

#### --distractors--

Gerenciando pares chave-valor em um banco de dados.

---

Criando listas ordenadas de itens.

---

Armazenando estruturas de dados hierárquicas no armazenamento local do navegador.

#### --answer--

Eliminando duplicatas de um array.

### --question--

#### --text--

O que é um WeakSet em JavaScript?

#### --distractors--

Uma coleção semelhante a Set que permite apenas tipos de dados primitivos.

---

Uma coleção semelhante a Set que permite valores duplicados.

---

Uma coleção semelhante a Set que impede que valores sejam removidos.

#### --answer--

Uma coleção semelhante a Set que armazena apenas objetos.

### --question--

#### --text--

Qual é uma diferença entre um Set e um WeakSet?

#### --distractors--

Um WeakSet pode armazenar qualquer tipo de valor, incluindo números.

---

Um Set não permite valores duplicados, mas um WeakSet permite.

---

Um Set permite coleta de lixo, enquanto um WeakSet não.

#### --answer--

Um Set pode conter qualquer tipo de valor, enquanto um WeakSet contém apenas objetos.

### --question--

#### --text--

O que é um Map em JavaScript?

#### --distractors--

Uma coleção que permite apenas valores únicos.

---

Uma coleção que pode usar apenas strings como chaves.

---

Uma coleção que não permite a exclusão de seus itens.

#### --answer--

Uma coleção de pares chave-valor de qualquer tipo.

### --question--

#### --text--

Qual método é usado para inserir pares chave-valor em um `Map`?

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

O que o seguinte código irá exibir?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

Como um Map difere de um Object em JavaScript?

#### --distractors--

Um Map permite apenas números como chaves.

---

Objetos permitem qualquer tipo como chaves, enquanto Maps permitem apenas strings.

---

Mapas não suportam iteração, enquanto Objetos suportam.

#### --answer--

Mapas permitem qualquer tipo de chave, mas Objetos permitem apenas strings e símbolos.

### --question--

#### --text--

Como você pode remover um par chave-valor de um `Map`?

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

O que a propriedade `size` de um Map retorna?

#### --distractors--

O número de índices no `Map`.

---

O número de valores únicos no `Map`.

---

A memória total usada pelo `Map`.

#### --answer--

O número de pares chave-valor no `Map`.

### --question--

#### --text--

O que é um WeakMap em JavaScript?

#### --distractors--

Uma coleção semelhante a Map que permite valores primitivos como chaves.

---

Uma coleção semelhante a Map que bloqueia a exclusão de itens.

---

Uma coleção semelhante a Map que ordena automaticamente suas chaves.

#### --answer--

Uma coleção semelhante a Map com referências fracas para as chaves.

### --question--

#### --text--

Qual é uma diferença entre um Map e um WeakMap?

#### --distractors--

Um Map permite apenas tipos primitivos como chaves, enquanto um WeakMap permite apenas objetos.

---

Um Map permite coleta de lixo, enquanto um WeakMap não.

---

Uma WeakMap permite chaves duplicadas, enquanto uma Map não permite.

#### --answer--

Um Map é iterável, enquanto um WeakMap não é.

### --question--

#### --text--

Qual dos seguintes métodos NÃO existe em um WeakMap?

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

Como será a saída do seguinte código?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

Como você pode iterar sobre os valores em um `Set`?

#### --distractors--

Ao usar o método `.map()`.

---

Ao usar o método `.reduce()`.

---

Ao usar o método `.filter()`.

#### --answer--

Ao usar o método `.forEach()`.

### --question--

#### --text--

Quais dois métodos de Sets retornam um `SetIterator` que contém os valores de um determinado Set?

#### --distractors--

`entries()` e `values()`

---

`forEach()` e `values()`

---

`has()` e `add()`

#### --answer--

`keys()` e `values()`

### --question--

#### --text--

Como será a saída do seguinte código?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
