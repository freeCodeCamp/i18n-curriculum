---
id: 66edd630f7666cfa54b404d0
title: 非同步 JavaScript 測驗
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是非同步程式設計？

#### --distractors--

一種依序執行程式碼的方法。

---

一種在你的程式碼中控制錯誤的技術。

---

一種進行網路請求的方法，通常用於從伺服器擷取或傳送資料。 

#### --answer--

一種在不阻塞主執行緒的情況下並行執行 `code` 的方法。

### --question--

#### --text--

什麼是執行緒？

#### --distractors--

用於從伺服器擷取資料的函式。 

---

一個在網頁瀏覽器中執行 JavaScript 程式碼的程式。

---

一種特殊型別的函式（程式），作為引數傳遞給另一個函式（程式）。

#### --answer--

一組可以獨立於主程式流程執行的指令序列。

### --question--

#### --text--

JavaScript 中 `Fetch API` 的用途是什麼？

#### --distractors--

管理非同步回呼。

---

創建和管理 cookies。

---

呈現動態 HTML 內容。

#### --answer--

向伺服端發出網路請求。

### --question--

#### --text--

哪一個 `HTTP` 方法用於將資料傳送到伺服端進行處理？

#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--

以下哪一種方式是從 API 取得資料的正確方法？

#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--

什麼是 JavaScript 中的 promise？

#### --distractors--

在使用 Fetch API 時，同步多個操作的一種方法。

---

一種特殊型別的回呼函式（程式），你必須在所有高階函式（程式）中使用。

---

一種用於處理你在使用 Fetch API 時錯誤的技術。

#### --answer--

一個表述非同步操作最終完成（或失敗）的物件。

### --question--

#### --text--

`promise chaining` 允許你做什麼？

#### --distractors--

允許你平行執行多個同步操作。

---

允許你更高效地控制同步操作行為。

---

允許你自動重試失敗的操作。

#### --answer--

允許你依序執行一連串非同步操作。

### --question--

#### --text--

哪一個方法用來控制代碼成功完成的承諾？

#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--

在串鏈中使用哪種方法來控制錯誤？

#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--

以下哪一項不是有效的 HTTP 方法？

#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--

以下哪一項是建立非同步函式的正確語法？

#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--

`await` 關鍵字在非同步函式中有什麼作用？

#### --distractors--

它創建一個新的 promise。

---

它會立即執行程式碼而不等待，並傳回一個物件。

---

它會記錄 promise 的結果並傳回 `null`。

#### --answer--

它會暫停函式（程式）的執行直到 `promise` 被決議。

### --question--

#### --text--

以下哪一個 API 用於提供網站請求使用者位置的方法？

#### --distractors--

定位器 API

---

地理 API

---

定位 API

#### --answer--

地理位置 API

### --question--

#### --text--

以下哪個屬性會非同步下載腳本，但會等到 HTML 文件完全解析後才執行腳本？

#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--

以下程式碼會傳回什麼？ 

```js
fetch('https://api.example.com/data')
```

#### --distractors--

錯誤訊息顯示資料已損毀。

---

出現錯誤，表示無法擷取資料。

---

不會有任何傳回值，JavaScript 將會繼續執行腳本中的下一個函式。

#### --answer--

一個會決議為 `Response` 物件的 `Promise`。

### --question--

#### --text--

Fetch API 的預設 HTTP 方法是什麼？

#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--

以下哪一種方法用於刪除伺服器上的資源？

#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--

以下哪一種方式是正確的 `promise` 創建方式？

#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--

下列哪一個屬性告訴瀏覽器在繼續解析 HTML 文件的同時非同步下載腳本檔案？

#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--

Geolocation API 的哪個方法用於取得裝置的當前位置？

#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`

