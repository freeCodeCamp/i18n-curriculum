---
id: 67f41242431cbf3db8ca79c7
title: Python基礎クイズ
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

変数が特定のデータ型に一致するかどうかを確認するために使われる関数はどれですか？

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

次のうち、文字列の連結の方法ではないものはどれですか？

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

文字列の文字数を返すために使われる関数はどれですか？

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

この例で`result`は何になりますか？

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

浮動小数点数と整数を足すとどうなりますか？

#### --distractors--

結果はエラーメッセージになります。

---

結果は`None`になります。

---

結果は整数になります。

#### --answer--

結果は浮動小数点数になります。

### --question--

#### --text--

関数を定義する正しい方法はどれですか？

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

コンソールに何が出力されますか？

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

変数名を付ける際に誤っている文はどれですか？

#### --distractors--

変数名は文字またはアンダースコア（_）で始まらなければならず、数字では始められません。

---

変数名は英数字（a-z、A-Z、0-9）とアンダースコア（_）のみを含めることができます。

---

変数名はPythonの予約キーワード（`if`、`class`、`def`など）を使うことはできません。

#### --answer--

変数名は最大10文字でなければならず、それを超えるとPythonはエラーをスローします。

### --question--

#### --text--

Pythonでサポートされていないデータ型はどれですか？

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

すべての文字を大文字に変換した新しい文字列を返す関数はどれですか？

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

ユーザーからの入力を取得するために使われる関数はどれですか？

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

次のコードの結果は何になりますか？

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

`split()`メソッドは何をしますか？

#### --distractors--

このメソッドはタプルを部分文字列のリストに分割します。

---

このメソッドは浮動小数点数を部分文字列のリストに分割します。

---

このメソッドは辞書を部分文字列のリストに分割します。

#### --answer--

このメソッドは文字列を部分文字列のリストに分割します。

### --question--

#### --text--

次のコードはコンソールに何を出力しますか？

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

1対1の文字マッピングのテーブルを作成するために使われる関数はどれですか？

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

次のコードの結果は何になりますか？

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

文字列内に部分文字列が見つからなかった場合、`find()`メソッドは何を返しますか？

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

部分文字列が文字列内に出現する回数を数えるために使われる関数はどれですか？

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

Pythonの床除算は何をしますか？

#### --distractors--

この演算子は2つの数を乗算し、結果を最も近い整数に切り上げます。

---

この演算子は浮動小数点数を整数に変換します。

---

この演算子は数を別の数のべき乗にします。

#### --answer--

この演算子は2つの数を除算し、結果を最も近い整数に切り捨てます。

### --question--

#### --text--

数値を最も近い整数に丸めるために使われる関数はどれですか？

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`
