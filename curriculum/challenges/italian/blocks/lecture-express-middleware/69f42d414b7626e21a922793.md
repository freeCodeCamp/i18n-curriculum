---
id: 69f42d414b7626e21a922793
title: "Cos'è il middleware a livello di router?"
challengeType: 19
dashedName: what-is-router-level-middleware
---

# --description--

Il middleware a livello di router è simile al middleware a livello di applicazione, ma è collegato a un'istanza di `express.Router()` invece che all'istanza principale dell'app. Può essere usato per modularizzare la tua applicazione Express creando moduli di rotta isolati.

Il middleware a livello di router funziona allo stesso modo del middleware a livello di applicazione, ma si applica a istanze specifiche di router. Questo rende più semplice applicare il middleware solo a un insieme di rotte invece che globalmente.

```js
const express = require('express')
const app = express()
const router = express.Router()

// Router-level middleware to log requests to '/menu' routes
router.use((req, res, next) => {
  console.log('Request made to /menu route')
  next() // Pass control to the next handler
})

// Define routes
router.get('/drinks', (req, res) => {
  res.send('Welcome to the drinks menu!')
})

app.use('/menu', router) // Apply the router

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* `router.use()`: Questo è middleware a livello di router, che si applica solo alle rotte gestite da router.
* `app.use('/menu', router)`: Il middleware si applica solo alle rotte che iniziano con `/menu` (ad esempio, `/menu/drinks`).

Il middleware a livello di router ti permette di suddividere la tua app in componenti modulari e applicare il middleware selettivamente a un gruppo di rotte. Questo aiuta con l'organizzazione e la chiarezza del codice.

# --questions--

## --text--

Cos'è il middleware a livello di router in Express?

## --answers--

Middleware applicato globalmente a tutta l'app.

### --feedback--

Concentrati su dove il middleware è allegato, non su cosa fa.

---

Middleware collegato a un'istanza specifica di router.

---

Middleware usato solo per registrare le richieste.

### --feedback--

Concentrati su dove il middleware è allegato, non su cosa fa.

---

Middleware usato per i file statici.

### --feedback--

Concentrati su dove il middleware è allegato, non su cosa fa.

## --video-solution--

2

## --text--

Come si può applicare il middleware a livello di router?

## --answers--

Usando `app.use()`.

### --feedback--

Concentrati sul metodo usato per allegare il middleware a un router.

---

Usando `router.use()`.

---

Usando `app.listen()`.

### --feedback--

Concentrati sul metodo usato per allegare il middleware a un router.

---

Usando `app.get()`.

### --feedback--

Concentrati sul metodo usato per allegare il middleware a un router.

## --video-solution--

2

## --text--

Dove si applica il middleware a livello di router?

## --answers--

All'istanza dell'app.

### --feedback--

Pensa a dove è allegato il middleware del router.

---

A istanze specifiche di router.

---

Ai file statici.

### --feedback--

Pensa a dove è allegato il middleware del router.

---

Ai singoli gestori di rotta.

### --feedback--

Pensa a dove è allegato il middleware del router.

## --video-solution--

2
