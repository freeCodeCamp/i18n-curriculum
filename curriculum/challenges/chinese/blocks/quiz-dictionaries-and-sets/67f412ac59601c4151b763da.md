---
id: 67f412ac59601c4151b763da
title: 字典和集合测验
challengeType: 8
dashedName: quiz-dictionaries-and-sets
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是字典？

#### --distractors--

一种只包含字串和列表的数据结构。

---

一种只接受大数字的键值点对集合的数据结构。

---

一种只保存元组集合的数据结构。

#### --answer--

一种由密钥-值点对组成的集合的数据结构。

### --question--

#### --text--

以下哪项是字典的正确语法？

#### --distractors--

```py
dictionary = {
    <key1: value1>,
    <key2: value2>
}
```

---

```py
dictionary = {
    (key1: value1),
    (key2: value2)
}
```

---

```py
dictionary = {
    [key1: value1],
    [key2: value2]
}
```

#### --answer--

```py
dictionary = {
    key1: value1,
    key2: value2
}
```

### --question--

#### --text--

以下关于字典，哪项是正确的？

#### --distractors--

`Key` 必须至少有两个字符长度。

---

密钥必须包含特殊符号。

---

密钥必须至少包含一个数字。

#### --answer--

密钥必须是唯一的。

### --question--

#### --text--

以下哪个构造函数可用于创建字典？

#### --distractors--

`diction()`

---

`dic()`

---

`dictionary()`

#### --answer--

`dict()`

### --question--

#### --text--

以下哪种方式是从字典中访问值的正确方法？

#### --distractors--

`dictionary<key>`

---

`dictionary/key/`

---

`dictionary{key}`

#### --answer--

`dictionary[key]`

### --question--

#### --text--

以下哪种方法是更新字典中值的正确方式？

#### --distractors--

`pizza['name'] >> 'Margherita'`

---

`pizza['name'] << 'Margherita'`

---

`pizza['name'] == 'Margherita'`

#### --answer--

`pizza['name'] = 'Margherita'`

### --question--

#### --text--

以下哪些方法可用于检索与密钥关联的值？

#### --distractors--

`access()`

---

`set()`

---

`retrieve()`

#### --answer--

`get()`

### --question--

#### --text--

什么是视图对象？

#### --distractors--

一种用于将字串转换为字典的特殊对象。

---

一种仅当字典具有两个或更多密钥-值点对时查看字典内容的方法。

---

一种用于将列表转换为字典的特殊对象。

#### --answer--

一种查看字典内容而不创建数据单独拷贝的方法。

### --question--

#### --text--

以下哪个 `method` 会 `return` 一个包含字典中所有密钥-值点对的视图 `object`？

#### --distractors--

`lists()`

---

`dictionaries()`

---

`collections()`

#### --answer--

`items()`

### --question--

#### --text--

以下哪个方法会从字典中移除所有的密钥-值点对？

#### --distractors--

`empty()`

---

`replace()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

下面代码的输出是什么？

```py
products = {
    'Laptop': 990,
    'Smartphone': 600,
    'Tablet': 250,
    'Headphones': 70,
}

for product in products.items():
    print(product)
```

#### --distractors--

```py
<'Laptop', 990>
<'Smartphone', 600>
<'Tablet', 250>
<'Headphones', 70>
```

---

```py
'Laptop', 990
'Smartphone', 600
'Tablet', 250
'Headphones', 70
```

---

```py
['Laptop', 990]
['Smartphone', 600]
['Tablet', 250]
['Headphones', 70]
```

#### --answer--

```py
('Laptop', 990)
('Smartphone', 600)
('Tablet', 250)
('Headphones', 70)
```

### --question--

#### --text--

以下关于集合的说法哪项是正确的？

#### --distractors--

集合是可变的，并且顺序是反向的。

---

集合是可变的且有序的。

---

集合是不可变的且无序的。

#### --answer--

集合是可变的且无序的。

### --question--

#### --text--

以下哪种方式是正确的 `create` 一个集合？

#### --distractors--

`my_set = (1, 2, 3, 4, 5)`

---

`my_set = <1, 2, 3, 4, 5>`

---

`my_set = [1, 2, 3, 4, 5]`

#### --answer--

`my_set = {1, 2, 3, 4, 5}`

### --question--

#### --text--

以下哪个函数用于创建一个空集合？

#### --distractors--

`create_set()`

---

`empty_set()`

---

`sets()`

#### --answer--

`set()`

### --question--

#### --text--

终端将输出什么？

```py
my_set = {1, 2, 3, 4, 5, 6}
my_set.add(5)

print(my_set)
```

#### --distractors--

`SyntaxError`

---

`RangeError`

---

`{1, 2, 3, 4, 5, 5, 6}`

#### --answer--

`{1, 2, 3, 4, 5, 6}`

### --question--

#### --text--

以下哪个方法用于查看一个集合是否是另一个集合的子集？ 

#### --distractors--

`issub()`

---

`isset()`

---

`subset()`

#### --answer--

`issubset()`

### --question--

#### --text--

以下哪种方法用于查看两个集合是否不相交？

#### --distractors--

`joint()`

---

`isjoint()`

---

`disjoint()`

#### --answer--

`isdisjoint()`

### --question--

#### --text--

对称差操作符(`^`)的作用是什么？

#### --distractors--

它返回一个只包含集合共有元素的新建集合。

---

它查找集合之间的差异，并用该结果更新第一个集合。

---

它返回一个新建集合，包含第一个集合中不在另一个集合内的元素。 

#### --answer--

它返回一个新建的集合，包含只存在于第一个集合或第二个集合中的元素，但不同时存在于两个集合中的元素。

### --question--

#### --text--

以下哪个内置模块用于生成随机数？

#### --distractors--

`set_random`

---

`get_random`

---

`rand`

#### --answer--

`random`

### --question--

#### --text--

以下哪个模块用于处理正则表达式？

#### --distractors--

`regex`

---

`reg`

---

`r`

#### --answer--

`re`

