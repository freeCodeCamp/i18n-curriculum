---
id: 672990ecf71a852804ababe7
title: 프레젠테이션 HTML과 시맨틱 HTML의 차이점은 무엇인가요?
challengeType: 19
dashedName: what-is-the-difference-between-presentational-and-semantic-html
---

# --interactive--

프레젠테이션 HTML은 콘텐츠의 외관과 스타일에 중점을 둡니다. HTML 초창기에는 개발자들이 `center`, `big`, `font` 같은 요소를 사용했습니다. 하지만 현대 웹 개발에서는 이러한 유형의 요소를 사용하지 않아야 합니다. 이들은 제한적이고 접근성과 유지보수에 부정적인 영향을 미치기 때문입니다.

많은 프레젠테이션 HTML 요소는 더 이상 권장되지 않는 폐기된 요소입니다. 같은 결과를 얻을 수 있는 더 나은 방법들이 있습니다. 하지만 이들이 존재한다는 것을 아는 것이 도움이 되므로 몇 가지 예를 살펴보겠습니다.

`font` 요소는 텍스트의 폰트 크기와 색상을 설정하는 데 사용되던 폐기된 요소입니다. 다음은 `font` 요소의 예입니다.

인터랙티브 편집기를 활성화하고 크기를 5에서 7로 변경하여 폰트가 커지는 것을 확인하세요.

:::interactive_editor

```html
<font size="5" color="blue">This text is blue and large.</font>
```

:::

이 예제는 텍스트 색상을 `blue`로, 크기를 `5` 값으로 설정합니다. `size` 속성의 범위는 `1`부터 `7`까지이며, `1`이 가장 작고 `7`이 가장 큽니다. 명시적으로 값을 설정하지 않으면 기본값은 `3`입니다.

이 요소는 여전히 작동하지만, 폰트 크기와 색상은 항상 HTML이 아닌 CSS에서 설정해야 하므로 사용하지 않아야 합니다.

`center` 요소는 컨테이너 내에서 콘텐츠를 수평으로 가운데 정렬하는 데 사용되던 또 다른 폐기된 요소입니다. 다음은 텍스트와 단락 요소를 포함하는 `center` 요소의 예입니다.

인터랙티브 편집기를 활성화하고 `center` 태그를 `<p>Another example text.</p>` 주위에 추가하여 페이지 중앙에 정렬되는 것을 확인하세요.

:::interactive_editor

```html
<center>
  This text is centered.
  <p>HTML is awesome.</p>
</center>

<p>Another example text.</p>
```

:::

다음은 `big` 요소입니다. 이것도 폐기된 프레젠테이션 HTML 요소로, 감싸진 텍스트를 주변 텍스트보다 한 단계 크게 만듭니다. 여기 두 부분으로 나뉜 단락 예제가 있습니다.

인터랙티브 편집기를 활성화하고 `big` 태그를 `Some other text` 주위에 추가하여 미리보기 창에서 변화를 확인하세요.

:::interactive_editor

```html
<p>
  This text has a normal font size.
  <big>This text is larger.</big>
  Some other text.
</p>
```

:::

폰트 크기는 항상 CSS로 설정해야 하므로 이 요소도 현대 HTML에서는 사용하지 않아야 합니다.

이들은 프레젠테이션 HTML 요소의 예시였습니다. 하지만 모두 폐기되어 더 이상 권장되지 않습니다. 그렇다면 대신 무엇을 사용해야 할까요? 살펴보겠습니다.

시맨틱 HTML이 이제 권장되는 방법입니다. 시맨틱 HTML은 요소의 콘텐츠를 설명하므로 읽고 이해하며 유지보수하기 훨씬 쉽습니다.

검색 엔진은 시맨틱 HTML을 사용하면 웹사이트를 쉽게 이해할 수 있습니다. 또한 화면 읽기 프로그램이 웹 페이지의 내용을 설명하는 데 시맨틱 정보가 필요하기 때문에 접근성 측면에서도 도움이 됩니다.

시맨틱 HTML 요소의 예는 다음과 같습니다:

- 문서나 섹션의 헤더를 정의하는 `header` 요소
- 내비게이션 링크가 있는 섹션을 위한 내비게이션 섹션 요소 `nav`
- 관련 정보를 그룹화하는 `section` 요소
- 삽화와 도표를 위한 `figure` 요소

다음은 내비게이션 섹션 요소를 포함하는 `header` 요소의 예입니다.

인터랙티브 편집기를 활성화하고 `<a href="#">Products</a>`를 `nav` 안에 추가하여 미리보기 창에서 변화를 확인하세요.

:::interactive_editor

```html
<header>
  <nav>
    <a href="#">Home</a>
    <a href="#">About</a>
    <!--Add the products link here-->

    <a href="#">Contact</a>
  </nav>
</header>
```

:::

시맨틱 요소는 HTML 구조 내에서 그 목적을 명확히 나타냅니다. 다양한 시맨틱 HTML 요소가 있으니 프로젝트에 맞는 요소를 꼭 찾을 수 있을 것입니다.

잘하셨습니다. 이제 프레젠테이션 HTML과 시맨틱 HTML의 차이를 알게 되었습니다: 시맨틱 HTML은 콘텐츠를 설명하고, 프레젠테이션 HTML은 외관에 중점을 둡니다.

# --questions--

## --text--

다음 중 프레젠테이션 HTML과 시맨틱 HTML의 차이를 가장 잘 설명한 것은 무엇인가요?

## --answers--

프레젠테이션 HTML은 콘텐츠 구조에 중점을 두고, 시맨틱 HTML은 외관에 중점을 둔다.

### --feedback--

"프레젠테이션"과 "시맨틱"이 의미하는 바와 이것들이 HTML 요소와 어떻게 관련되는지 생각해 보세요.

---

시맨틱 HTML은 콘텐츠 구조에 중점을 두고, 프레젠테이션 HTML은 외관에 중점을 둔다.

---

프레젠테이션 HTML과 시맨틱 HTML은 차이가 없다.

### --feedback--

"프레젠테이션"과 "시맨틱"이 의미하는 바와 이것들이 HTML 요소와 어떻게 관련되는지 생각해 보세요.

---

둘 다 스타일에 중점을 두지만, 프레젠테이션 HTML이 더 현대적이다.

### --feedback--

"프레젠테이션"과 "시맨틱"이 의미하는 바와 이것들이 HTML 요소와 어떻게 관련되는지 생각해 보세요.

## --video-solution--

2

## --text--

다음 중 프레젠테이션 HTML의 예는 무엇인가요?

## --answers--

헤더를 정의하기 위해 `header` 요소를 사용하는 것.

### --feedback--

어떤 요소가 콘텐츠의 외관을 직접 제어하는지 생각해 보세요.

---

내비게이션 섹션을 정의하기 위해 `nav` 요소를 사용하는 것.

### --feedback--

어떤 요소가 콘텐츠의 외관을 직접 제어하는지 생각해 보세요.

---

독립적인 콘텐츠를 정의하기 위해 `article` 요소를 사용하는 것.

### --feedback--

어떤 요소가 콘텐츠의 외관을 직접 제어하는지 생각해 보세요.

---

콘텐츠를 가운데 정렬하기 위해 `center` 요소를 사용하는 것.

## --video-solution--

4

## --text--

다음 중 시맨틱 HTML의 예는 무엇인가요?

## --answers--

텍스트를 빨간색으로 만들기 위해 `<font color="red">`를 사용하는 것.

### --feedback--

어떤 요소가 콘텐츠의 의미를 설명하는지 생각해 보세요.

---

콘텐츠를 가운데 정렬하기 위해 `center` 요소를 사용하는 것.

### --feedback--

어떤 요소가 콘텐츠의 의미를 설명하는지 생각해 보세요.

---

내비게이션 섹션을 나타내기 위해 `nav` 요소를 사용하는 것.

---

텍스트를 더 크게 만들기 위해 `big` 요소를 사용하는 것.

### --feedback--

어떤 요소가 콘텐츠의 의미를 설명하는지 생각해 보세요.

## --video-solution--

3
