---
id: 66f1a2009e65c9a40a26d51e
title: Quiz sulle basi di React
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale tra i seguenti non è una libreria o un framework JavaScript?

#### --distractors--

Angular

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

Quale tra i seguenti NON è un metodo per il rendering condizionale in React?

#### --distractors--

Operatori ternari.

---

Operatore logico `&&`.

---

Istruzioni `if`.

#### --answer--

`setTimeout`

### --question--

#### --text--

Qual è lo scopo principale di aggiungere una prop `key` quando si renderizza una lista in React?

#### --distractors--

Applicare uno stile CSS unico a ogni elemento della lista.

---

Servire come contenuto testuale per l'elemento.

---

Aggiungere un listener di eventi a ogni elemento specifico della lista.

#### --answer--

Aiutare React a identificare gli elementi per un rendering efficiente.

### --question--

#### --text--

Dove vengono installati i pacchetti e le librerie del progetto?

#### --distractors--

Nella directory utente del tuo computer.

---

Nel file `package.json`.

---

Nella cartella `src`.

#### --answer--

Nella cartella `node_modules`.

### --question--

#### --text--

Quale delle seguenti affermazioni è VERA riguardo JSX?

#### --distractors--

JSX deve sempre essere scritto in file separati dal JavaScript.

---

JSX non può usare espressioni JavaScript.

---

JSX non è raccomandato per le applicazioni React.

#### --answer--

JSX permette di scrivere HTML all'interno del codice JavaScript.

### --question--

#### --text--

Qual è un problema comune delle SPA?

#### --distractors--

Le risorse del server sono usate più intensamente a causa del rendering costante delle pagine.

---

La cache del browser diventa inaffidabile a causa degli aggiornamenti frequenti del DOM.

---

Caricamenti multipli di pagina creano una gestione incoerente dello stato.

#### --answer--

I lettori di schermo hanno difficoltà con contenuti aggiornati dinamicamente.

### --question--

#### --text--

Come aggiorna tipicamente la vista un'applicazione single-page?

#### --distractors--

Facendo un reload completo del server.

---

Ricaricando l'intero documento HTML.

---

Usando solo il rendering lato server.

#### --answer--

Aggiornando solo le parti necessarie della pagina.

### --question--

#### --text--

Quale dei seguenti comandi avvia un progetto React in modalità sviluppo?

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

Quale tra i seguenti non è un elemento JSX valido?

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

Quale metodo degli array JavaScript è comunemente usato per trasformare un array di dati in un array di elementi JSX?

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

Qual è la buona pratica per passare la prop `key` in React?

#### --distractors--

Usare sempre gli indici degli array.

---

Generare chiavi casuali a ogni render.

---

Omettere del tutto la prop `key`.

#### --answer--

Usare ID unici e stabili provenienti dai dati.

### --question--

#### --text--

Quale comando installa le dipendenze elencate in `package.json`?

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

Come passeresti una prop `name` a un componente figlio?

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

Qual è il modo corretto per passare props multiple usando l'operatore di espansione in React?

#### --distractors--

`<Child person={name..., age..., city...} />`.

---

`<Child spreader={...person} />`.

---

`<Child spread={person} />`.

#### --answer--

`<Child {...person} />`.

### --question--

#### --text--

Quali sono i mattoni fondamentali di React?

#### --distractors--

Funzioni e moduli.

---

Elementi DOM ed eventi.

---

Classi e oggetti.

#### --answer--

Componenti riutilizzabili.

### --question--

#### --text--

Qual è il modo corretto per aggiungere uno stile in linea in React?

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

Qual è il modo corretto per renderizzare condizionatamente un componente React?

#### --distractors--

Usare istruzioni `if` dentro JSX.

---

Dichiarare un nuovo componente dentro un metodo `render()`.

---

Usare loop `while` dentro JSX.

#### --answer--

Usare operatori ternari o operatori logici `&&` all'interno di JSX.

### --question--

#### --text--

Quale parola chiave si usa per importare un componente in un altro file?

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

Qual è il significato completo di SPA?

#### --distractors--

Server Page Architectures.

---

Selective Page Applications.

---

Static Progressive Apps.

#### --answer--

Single-page Applications.

### --question--

#### --text--

Quale estensione della sintassi JavaScript si usa per descrivere l'interfaccia utente in React?

#### --distractors--

TSC

---

HTML+

---

ReactML

#### --answer--

JSX
