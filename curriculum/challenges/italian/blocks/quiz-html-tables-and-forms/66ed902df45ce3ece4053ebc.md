---
id: 66ed902df45ce3ece4053ebc
title: Quiz su tabelle e moduli HTML
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale dei seguenti attributi si usa per specificare l’URL a cui devono essere inviati i dati del modulo?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

Quali due elementi si usano per specificare una riga e un’intestazione di riga in una tabella?

#### --distractors--

L’elemento `r` si usa per le righe e l’elemento `thead` si usa per l’intestazione.

---

L’elemento `row` si usa per le righe e l’elemento `th` si usa per l’intestazione.

---

L’elemento `tr` si usa per le righe e l’elemento `head` si usa per l’intestazione.

#### --answer--

L’elemento `tr` si usa per le righe e l’elemento `th` si usa per l’intestazione.

### --question--

#### --text--

A cosa serve l’elemento `td`?

#### --distractors--

Serve a definire un tipo di dato della tabella.

---

Serve a unire due colonne.

---

Serve a unire due righe.

#### --answer--

Serve a definire una cella della tabella.

### --question--

#### --text--

Qual è la funzione dell’attributo `colspan`?

#### --distractors--

Definisce la lunghezza di una colonna in una tabella.

---

Rimuove l’interruzione di riga tra il testo in una colonna.

---

Serve a definire il numero di colonne.

#### --answer--

Definisce il numero di colonne che una cella della tabella deve occupare.

### --question--

#### --text--

Qual è il ruolo dell’attributo `placeholder`?

#### --distractors--

Serve a inviare dati al server quando il modulo viene inviato.

---

Serve ad associare un elemento `label` con un elemento `input`.

---

Serve a specificare che un campo di input è di sola lettura per l’utente.

#### --answer--

Serve a fornire un suggerimento per un campo di input.

### --question--

#### --text--

Qual è lo scopo dell’elemento `thead`?

#### --distractors--

Serve a raggruppare il contenuto del corpo in una tabella HTML.

---

Serve a raggruppare la descrizione di una tabella HTML.

---

Serve a raggruppare il contenuto del piè di pagina in una tabella HTML.

#### --answer--

Serve a raggruppare il contenuto dell’intestazione in una tabella HTML.

### --question--

#### --text--

Quale dei seguenti è un esempio di associazione esplicita tra etichette e input in un modulo?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

A cosa serve un validatore HTML?

#### --distractors--

Un validatore è uno strumento che formatta automaticamente il tuo codice HTML.

---

Un validatore è uno strumento che applica stili al tuo HTML.

---

Un validatore è uno strumento che fa eseguire il codice HTML più velocemente.

#### --answer--

Un validatore è uno strumento che controlla la sintassi del codice HTML per assicurarsi che sia valido.

### --question--

#### --text--

Quale elemento si usa per definire una cella in una tabella?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

Quale dei seguenti è uno strumento che permette di ispezionare e modificare la struttura HTML di una pagina web?

#### --distractors--

Rimozione DOM.

---

Tester DOM.

---

Validazione DOM.

#### --answer--

Ispezione DOM.

### --question--

#### --text--

Qual è il modo corretto per raggruppare campi di input correlati?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

Quale dei seguenti si usa per specificare che un campo di input è di sola lettura?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

Quale attributo specifica che un input deve essere compilato prima di inviare il modulo?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

A cosa servono gli strumenti per sviluppatori (Devtools)?

#### --distractors--

Questi strumenti sono integrati direttamente nel browser e servono a individuare eventuali problemi di linting nel codice.

---

Questi strumenti sono integrati direttamente nel browser e servono a formattare automaticamente il codice.

---

Questi strumenti sono integrati direttamente nel browser e servono ad assicurare una copertura di test del 100% per il codice.

#### --answer--

Questi strumenti sono integrati direttamente nel browser e servono ad aiutarti a eseguire il debug, profilare e analizzare le pagine web.

### --question--

#### --text--

Qual è il modo corretto per disabilitare un input?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

Quale dei seguenti è un valore valido per l’attributo `type`?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

Qual è l’uso corretto dell’attributo `size`?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

Quali dei seguenti attributi si usano per specificare il numero minimo e massimo di caratteri richiesti in un campo di input?

#### --distractors--

`minlen` e `maxlen`

---

`min` e `max`

---

`minimumLen` e `maximumLen`

#### --answer--

`minlength` e `maxlength`

### --question--

#### --text--

Quale dei seguenti esempi di pulsanti **NON** usa un valore corretto per l’attributo `type`?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

Quale dei seguenti attributi si usa per specificare il valore di un pulsante?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

Qual è il ruolo dell’attributo `action` all’interno del tag di apertura `form`?

#### --distractors--

Serve a specificare che un campo di input è di sola lettura.

---

Serve a definire il numero di caratteri che devono essere visibili mentre l’utente digita nell’input.

---

Serve a mostrare un suggerimento all’utente per indicargli cosa inserire nel campo di input.

#### --answer--

Serve a specificare l’URL a cui devono essere inviati i dati del modulo.

### --question--

#### --text--

Quale dei seguenti attributi si usa per specificare il metodo HTTP da usare per inviare i dati del modulo?

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

Quale dei seguenti è un metodo comune usato quando si lavora con i moduli?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

Quale dei seguenti NON è un valore valido per l’attributo `type`?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

Qual è il modo corretto per creare un pulsante in un modulo?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

Cos’è l’associazione implicita in un modulo?

#### --distractors--

È quando gli input possono essere associati alle etichette usando gli attributi `action` e `for` sull’elemento `label`.

---

È quando gli input possono essere associati alle etichette usando gli attributi `required` e `for` sull’elemento `label`.

---

È quando gli input possono essere associati alle etichette usando l’attributo `for` sull’elemento `label`.

#### --answer--

È quando gli input possono essere associati alle etichette racchiudendo il campo di input all’interno dell’elemento `label`.

### --question--

#### --text--

Qual è il ruolo dell’elemento `fieldset`?

#### --distractors--

Serve a raggruppare il contenuto dell’intestazione in una tabella HTML.

---

Serve a creare un’etichetta per un campo di input.

---

Serve ad aggiungere un sottotitolo per descrivere il gruppo di input.

#### --answer--

Serve a raggruppare insieme input correlati.

### --question--

#### --text--

Quale dei seguenti si usa per creare una casella di spunta in un modulo?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

Quale dei seguenti elementi si usa per aggiungere un titolo a una tabella HTML?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

Qual è il ruolo dell’elemento `legend`?

#### --distractors--

Serve a raggruppare il contenuto del corpo in una tabella HTML.

---

Serve ad aggiungere un sottotitolo per descrivere le celle di una tabella.

---

Serve a raggruppare insieme input correlati.

#### --answer--

Serve ad aggiungere un sottotitolo per descrivere il gruppo di input.

### --question--

#### --text--

Quale dei seguenti elementi si usa per raggruppare il contenuto dell’intestazione in una tabella HTML?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

Qual è il modo corretto per aggiungere righe e celle dati a una tabella?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

Cos’è l’ispezione DOM?

#### --distractors--

Uno strumento che serve a rimuovere tutti gli elementi dal DOM.

---

Un insieme di strumenti integrati direttamente nel browser che ti aiutano a eseguire il debug, profilare e analizzare le pagine web.

---

Uno strumento che controlla la sintassi del codice HTML per assicurarsi che sia valido.

#### --answer--

Uno strumento che permette di ispezionare e modificare la struttura HTML di una pagina web.

### --question--

#### --text--

Quale dei seguenti è uno strumento che controlla la sintassi del codice HTML per assicurarsi che sia valido?

#### --distractors--

HTML validate

---

HTML isValid

---

HTML valid

#### --answer--

HTML validator

### --question--

#### --text--

Qual è il modo corretto per specificare il numero di colonne per una cella dati in una tabella?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

Cos’è uno stato di focus per un campo di input?

#### --distractors--

È lo stato di un campo di input quando il focus viene rimosso dall’utente.

---

È lo stato di un campo di input quando è impostato come readonly dall’utente.

---

È lo stato di un campo di input quando è disabilitato dall’utente.

#### --answer--

È lo stato di un campo di input quando è selezionato dall’utente.

### --question--

#### --text--

Qual è il modo corretto per creare un pulsante di opzione?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

Qual è il ruolo dell’attributo `for`?

#### --distractors--

Serve a mostrare un suggerimento all’utente.

---

Serve a specificare il valore dell’input.

---

Serve a creare un’etichetta per un campo di input.

#### --answer--

Serve a specificare a quale campo di input si riferisce l’etichetta.

### --question--

#### --text--

Quale dei seguenti attributi si usa per definire il numero di caratteri che devono essere visibili mentre l’utente digita nell’input?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

Quale dei seguenti attributi si usa per mostrare un suggerimento all’utente per indicargli cosa inserire nel campo di input?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
