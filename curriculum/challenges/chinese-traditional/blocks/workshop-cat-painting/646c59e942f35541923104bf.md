---
id: 646c59e942f35541923104bf
title: 步驟 4
challengeType: 0
dashedName: step-4
---

# --description--

回到你的 HTML 中，創建一個 `main` 元素。在該 `main` 元素內，為你的 `div` 元素添加類別為 `cat-head`。

# --hints--

你應該有一個 `main` 元素。

```js
assert.lengthOf(document.querySelectorAll('main'), 1);
```

你應該有一個 `div` 元素。

```js
assert.lengthOf(document.querySelectorAll('div'), 1);
```

你的 `div` 元素應該有 `cat-head` 類別。

```js
assert.equal(document.querySelector('div')?.getAttribute('class'), 'cat-head');
```

你的 `div` 元素應該在你的 `main` 頁籤內。

```js
assert.lengthOf(document.querySelectorAll('main div'), 1);
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>fCC Cat Painting</title>
    <link rel="stylesheet" href="./styles.css">
</head>
<body>
  --fcc-editable-region--

  --fcc-editable-region--
</body>
</html>
```

```css
* {
  box-sizing: border-box;
}

body {
  background-color: #c9d2fc;
}
```
