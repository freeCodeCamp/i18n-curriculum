---
id: 65e97251484dd50f720e6fe8
title: Datenarten und Bedingungen lernen Lektion C
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-c
---
# --description--

Im folgenden Beispiel möchten Sie sicherstellen, dass das Wort `loves` im String vorhanden ist.

```javascript
let sentence = "freeCodeCamp loves The Odin Project!";
```

<a href="https://www.freecodecamp.org/news/javascript-string-handbook" target="_blank"> Link zum Handbuch </a>

# --questions--

## --text--

Welche Methode sollte verwendet werden, um zu prüfen, ob das Wort `loves` im String vorhanden ist?

## --answers--

Die `includes()`-Methode sollte verwendet werden, um zu prüfen, ob das Wort `loves` im String vorhanden ist.

---

Die `indexOf()`-Methode sollte verwendet werden, um zu prüfen, ob das Wort `loves` im String vorhanden ist.

---

Die `search()`-Methode sollte verwendet werden, um zu prüfen, ob das Wort `loves` im String vorhanden ist.

---

Die `toUpperCase()`-Methode sollte verwendet werden, um zu prüfen, ob das Wort `loves` im String vorhanden ist.

## --video-solution--

1
