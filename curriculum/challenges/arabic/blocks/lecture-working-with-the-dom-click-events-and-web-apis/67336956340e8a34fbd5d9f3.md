---
id: 67336956340e8a34fbd5d9f3
title: ما هي واجهة برمجة التطبيقات requestAnimationFrame()، وكيف يمكن استخدامها لإعداد حلقة رسوم متحركة؟
challengeType: 19
dashedName: what-is-the-requestanimationframe-api-and-how-can-it-be-used-to-set-up-an-animation-loop
---

# --interactive--

إن إنشاء رسوم متحركة سلسة على صفحة ويب قد يكون صعبًا، خاصة إذا لم تكن متأكدًا من كيفية التعامل مع التوقيت بشكل صحيح. الخبر السار هو أن واجهة برمجة التطبيقات `requestAnimationFrame()` تُسهّل ذلك.

تُعد `requestAnimationFrame()` طريقة تسمح لك بجدولة الخطوة التالية من الرسوم المتحركة قبل إعادة رسم الشاشة التالية، مما ينتج عنه تجربة سلسة وجذابة بصريًا.

تشير إعادة رسم الشاشة التالية إلى اللحظة التي يُحدّث فيها المتصفح العرض البصري لصفحة الويب. يحدث هذا عدة مرات في الثانية، عادةً حوالي 60 مرة (أو 60 إطارًا في الثانية) على معظم الشاشات.

لاستخدام طريقة `requestAnimationFrame()`، كل ما عليك فعله هو استدعاؤها وتمرير دالة استدعاء راجع إليها:

```js
requestAnimationFrame(callback);
```

يجب أن يحدث استدعاء `requestAnimationFrame()` أولًا داخل دالة تتعامل مع الرسوم المتحركة، مثل `animate()`، مع دالة لتحديث الرسوم المتحركة، تُسمى تقليديًا `update()`:

```js
function animate() {
 // Update the animation...
 // for example, move an element, change a style, and more.
 update();
 // Request the next frame
 requestAnimationFrame(animate);
}
```

دالة `update()` هي المكان الذي يحدث فيه السحر. داخلها، يمكنك تغيير أي شيء تريد تحريكه. على سبيل المثال، تحديث نمط أو تغيير موقع عنصر:

```js
function update() {
 element.style.transform = `translateX(${position}px)`;
 position += 2;
}
```

ما يُشغّل الرسوم المتحركة في النهاية هو استدعاء `requestAnimationFrame()` وتمرير دالة `animate` إليها، هذه المرة خارج دالة `animate`:

```js
requestAnimationFrame(animate);
```

ستستمر الحلقة حتى توقفها.

الآن، لنلق نظرة على مثال آخر. يحتوي HTML لهذا المثال على عنصر `div` مع النص `freeCodeCamp is Awesome`. يجعل CSS العنصر `div` مستطيلاً ويخفي أي شيء يخرج من إطار العرض على اليسار أو اليمين. تحرك شفرة JavaScript المستطيل بمقدار `2px` إلى اليمين عند كل استدعاء لـ `requestAnimationFrame(animate)` مع دالة `animate` كدالة استدعاء راجع:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css" />
<div id="rect" class="rect">freeCodeCamp is Awesome</div>
<script src="index.js"></script>
```

```css
body {
  overflow-x: hidden;
}

.rect {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 400px;
  height: 250px;
  border-radius: 5px;
  background-color: #1b1b32;
  color: #f5f6f7;
  font-size: 2rem;
  position: absolute;
}
```

```js
const rect = document.getElementById("rect");

let position = 0;

function update() {
  // Move the rectangle 2px to the right
  rect.style.left = position + "px";
  position += 2;

  if (position > window.innerWidth) {
    // Move the rectangle just outside the left side of the screen
    position = -rect.offsetWidth;
  }
}

function animate() {
  update();

  //request the next frame
  requestAnimationFrame(animate);
}

// Start the animation
requestAnimationFrame(animate);
```

:::

النتيجة في المتصفح ستكون بطاقة عنوان متحركة تطفو عبر الشاشة.

# --questions--

## --text--

ماذا تفعل طريقة `requestAnimationFrame()`؟

## --answers--

توقف تشغيل الرسوم المتحركة.

### --feedback--

فكر في كيف تساعد هذه الطريقة على إنشاء رسوم متحركة سلسة.

---

تشغّل الرسوم المتحركة فورًا بدون تأخير.

### --feedback--

فكر في كيف تساعد هذه الطريقة على إنشاء رسوم متحركة سلسة.

---

تسرّع الرسوم المتحركة.

### --feedback--

فكر في كيف تساعد هذه الطريقة على إنشاء رسوم متحركة سلسة.

---

تجدول الخطوة التالية من الرسوم المتحركة قبل إعادة رسم الشاشة التالية للحصول على عرض أكثر سلاسة.

## --video-solution--

4

## --text--

ما الذي يُشغّل الرسوم المتحركة عند استخدام `requestAnimationFrame()`؟

## --answers--

استدعاء دالة `animate` داخل طريقة `requestAnimationFrame()`.

### --feedback--

فكر في كيف تُشغّل طريقة `requestAnimationFrame()` الرسوم المتحركة.

---

تعيين مهلة باستخدام `setTimeout()`.

### --feedback--

فكر في كيف تُشغّل طريقة `requestAnimationFrame()` الرسوم المتحركة.

---

استخدام `setInterval()` لتكرار الرسوم المتحركة.

### --feedback--

فكر في كيف تُشغّل طريقة `requestAnimationFrame()` الرسوم المتحركة.

---

استدعاء `requestAnimationFrame()` وتمرير دالة animate إليها خارج دالة animate.

## --video-solution--

4

## --text--

ماذا يعني "إعادة رسم الشاشة التالية"؟

## --answers--

اللحظة التي يُحدّث فيها المتصفح هيكل HTML.

### --feedback--

فكر في عدد مرات تحديث المتصفح لما تراه على الشاشة.

---

اللحظة التي يُحدّث فيها المتصفح العرض البصري لصفحة الويب، عادةً حوالي 60 مرة في الثانية.

---

اللحظة التي يُعيد فيها المتصفح تحميل الصفحة بالكامل.

### --feedback--

فكر في عدد مرات تحديث المتصفح لما تراه على الشاشة.

---

اللحظة التي يُحدّث فيها المتصفح أنماط CSS.

### --feedback--

فكر في عدد مرات تحديث المتصفح لما تراه على الشاشة.

## --video-solution--

2
