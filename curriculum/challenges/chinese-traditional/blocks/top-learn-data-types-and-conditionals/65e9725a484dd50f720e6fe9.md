---
id: 65e9725a484dd50f720e6fe9
title: 學習資料型別和條件語句 第 D 課
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-d
---

# --description--

參考以下範例，你想知道字串中 `The` 這個字最後一次出現的位置索引。

```javascript
let sentence = "freeCodeCamp loves The Odin Project! The Odin Project is great!";
```

<a href="https://www.freecodecamp.org/news/javascript-string-handbook" target="_blank"> 聯結至指南 </a>

# --questions--

## --text--

應該使用哪一個方法來尋找字串中 `The` 最後一次出現的索引？

## --answers--

應該使用 `includes()` 方法來尋找字串中最後一次出現字 `The` 的索引。

---

應該使用 `lastIndexOf()` 方法來尋找字串中 `The` 這個字最後一次出現的索引。

---

應該使用 `indexOf()` 方法來尋找字串中 `The` 這個字最後一次出現的索引。

---

應該使用 `search()` 方法來尋找字串中最後一次出現字 `The` 的索引。

## --video-solution--

2
