---
id: 672bc4ff5e7a4bbdee8ba013
title: 什么是 CSS 中的命名色？
challengeType: 19
dashedName: what-are-named-colors-in-css
---

# --interactive--

在 CSS 中，颜色在设计网页、提高可读性、营造氛围和改善用户体验方面起着至关重要的作用。 在 CSS 中定义颜色的最简单方法之一就是使用命名颜色。 命名颜色是浏览器识别的预定义颜色名称。 下面是一个为段落元素使用命名颜色的示例：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css" />
<p>This is a paragraph.</p>
```

```css
p {
  color: red;
}
```

:::

在本示例中，我们使用命名颜色 `red` 来样式化段落中的文本。

CSS 中的命名颜色是 140 种标准颜色名称的集合，如 `red`、`blue`、`yellow`、`aqua`、`fuchsia`、`black` 等。 这些名称简单易用，使代码更易读，而且具有自描述性。 

命名颜色对于快速原型设计、简单设计和提高代码可读性非常有用。 下面是另一个在 `h1` 选择器中使用命名颜色的示例：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css" />
<h1>This is a heading</h1>
```

```css
h1 {
  color: navy;
  background-color: lightgray;
}
```

:::

在本例中，标题文字将采用海军蓝，背景为浅灰色。 由于命名的颜色能让人立即直观地了解预期的样式，因此代码的可读性得到了提高。

CSS 中的命名颜色虽然方便，但数量有限，只有 140 个可用选项。 它们可能无法提供更多细节设计所需的精确色调。 

CSS 中的命名颜色是快速应用颜色并进行描述的好方法。 虽然它们对基本设计、原型设计和提高代码可读性很有用，但由于其范围有限，不太适合需要精确颜色控制的复杂设计。

通过了解命名颜色的优点和局限性，你可以判断何时最好使用它们，而不是使用更详细的颜色模型，如 `RGB` 或 `HSL`，你将在后续课程中学习这些内容。

# --questions--

## --text--

在 CSS 中使用命名颜色的主要好处是什么？

## --answers--

通过命名颜色，可以创建渐变效果。

### --feedback--

考虑一下命名颜色的简洁性和可读性。

---

命名颜色更易于书写，并使代码更具可读性。

---

与十六进制代码相比，命名颜色提供了更多的颜色选择。

### --feedback--

考虑一下命名颜色的简洁性和可读性。

---

命名颜色是网页设计中定义颜色最精确的方法。

### --feedback--

考虑一下命名颜色的简洁性和可读性。

## --video-solution--

2

## --text--

在什么情况下，命名颜色可能不是最佳选择？

## --answers--

当你需要快速制作设计原型时。

### --feedback--

思考在更复杂的设计中命名颜色的局限性。

---

当你的设计需要非常具体或细微的色调时。

---

当你的设计只涉及原色时。

### --feedback--

思考在更复杂的设计中命名颜色的局限性。

---

当与他人合作完成一个简单的项目时。

### --feedback--

思考在更复杂的设计中命名颜色的局限性。

## --video-solution--

2

## --text--

以下哪项是 CSS 中命名颜色的示例？

## --answers--

`#ff5733`

### --feedback--

命名颜色是描述性词语，而不是数字代码。

---

`rgb(255, 99, 71)`

### --feedback--

命名颜色是描述性词语，而不是数字代码。

---

`tomato`

---

`hsl(120, 100%, 50%)`

### --feedback--

命名颜色是描述性词语，而不是数字代码。

## --video-solution--

3
