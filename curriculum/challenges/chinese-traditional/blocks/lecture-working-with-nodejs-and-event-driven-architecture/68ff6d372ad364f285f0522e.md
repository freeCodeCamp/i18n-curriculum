---
id: 68ff6d372ad364f285f0522e
title: 在後端使用 Node 的優點和缺點是什麼？
challengeType: 19
dashedName: what-are-the-advantages-and-disadvantages-to-using-node-on-the-backend
---

# --description--

Node.js 的一大優點是你可以使用 JavaScript 實作網頁應用程式的前端和後端。這降低了初期的學習曲線，因為你和你的團隊不需要學習新的程式語言或在工作時切換語言，因此你可以更高效且更有生產力。

Node 的架構是另一個優勢。它具有非阻塞、事件驅動的架構，非常適合開發即時應用程式，在這些應用程式中，回應速度和效率對於打造良好的使用者體驗至關重要。

這就是「執行緒」這個概念變得非常重要的地方。執行緒是在一個處理程序內的執行路徑，就像一個正在執行的電腦程式。

Node 的架構依賴單一執行緒和事件迴圈，能有效控制大量同時的請求和輸入／輸出操作。這使它非常適合需要同時處理多個請求的應用程式。

它也擁有一個遍布全球的龐大開發者社群，他們不斷維護它並為它添加新的特性。因為它的普及度很高，所以有許多學習資源可供使用。如果你對 `Node.js` 有任何疑問，幾乎可以保證你會找到答案，這總是很有幫助。

當你使用 Node.js 時，你也可以存取 `npm`，這是 Node.js 的一個軟體包管理工具。`npm` 是一個強大的工具，允許你為你的專案安裝和管理軟體包與模組。

你會找到許多不同用途的軟體包，包括處理 HTTP 請求、操作資料庫、格式化日期和時間、測試程式碼等等。透過使用軟體包，你可以重複使用其他開發者已經撰寫、測試並分享的程式碼，讓你的工作流程更快速且高效。

最後，在成本方面，Node.js 有免費且開放原始碼的巨大優勢。它的軟體包、函式庫和相關工具也主要是免費的。這在考慮如何最佳化你的預算時，可能是一個重要的因素。

另一方面，Node.js 確實有某些缺點或限制，你應該注意。

如你所學，它一次只執行一個執行緒，這表示它一次只能控制代碼一個操作行為。雖然它的架構允許它高效地控制並行（同時）請求，但 CPU 密集型任務可能會阻塞主執行緒並導致效率問題。

CPU 密集型任務的範例包括複雜的數學運算、影像和影片處理，以及密碼學。有方法可以克服這些問題，但通常會增加應用程式的複雜度。

Node.js 也依賴非同步程式設計。在非同步程式設計中，會啟動一個可能需要很長時間才能執行完成的任務，但程式不會等待該任務完成，而是讓主程式繼續執行，同時非同步任務平行執行。當任務完成時，程式會控制代碼結果。

這個處理程序通常涉及我們所知的「回呼」，這些回呼是定義非同步操作完成後會發生什麼的函式（程式）。Node.js 的非同步特性可能會使程式碼更難閱讀、理解和偵錯。

最後，你在從 `npm`（Node 軟體包管理器）選擇軟體包時也應該小心，因為其中一些可能不會持續維護，因此可能會為你自己的應用程式引入漏洞。作為開發者，你應該仔細評估每個軟體包，並檢查它是否遵循品質和安全的最佳實務。

Node.js 目前是開發網頁應用程式最受歡迎的工具之一。了解它的優點和缺點將幫助你判斷它是否適合你的專案。

# --questions--

## --text--

以下哪一項是使用 Node.js 進行後端開發的優點？

## --answers--

由於其多執行緒架構，對於 CPU 密集型任務非常高效。

### --feedback--

思考 Node.js 使用的程式語言。

---

它允許開發者同時使用 JavaScript 進行前端和後端開發。

---

它避免了非同步程式設計，使程式碼更容易閱讀和維護。

### --feedback--

思考 Node.js 使用的程式語言。

---

它有一個小型且非作用中的社群，限制了可用的資源和支援。

### --feedback--

思考 Node.js 使用的程式語言。

## --video-solution--

2

---

## --text--

使用 Node.js 進行後端開發可能有哪些潛在缺點？

## --answers--

它的非阻塞架構使其在處理多個並行請求時效率低下。

### --feedback--

思考 Node.js 如何控制代碼任務。

---

龐大且作用中的社群可能會使尋找可靠的軟體包變得困難。

### --feedback--

思考 Node.js 如何控制代碼任務。

---

它的單一執行緒特性可能會降低 CPU 密集型操作的效率。

---

同時在前端和後端使用 JavaScript 可能導致不一致性。

### --feedback--

思考 Node.js 如何控制代碼任務。

## --video-solution--

3

---

## --text--

Node.js 的優勢使它特別適合以下哪一項？

## --answers--

具有複雜數學演算法的應用程式。

### --feedback--

思考 Node.js 是為了高效控制代碼而設計的。

---

需要處理高併發和即時更新的應用程式。

---

具有複雜關聯式資料庫互動的應用程式。

### --feedback--

思考 Node.js 是為了高效控制代碼而設計的。

---

開發速度不是主要因素的應用程式。

### --feedback--

思考 Node.js 是為了高效控制代碼而設計的。

## --video-solution--

2
