---
id: 68ff6d2f232c60f0fa68ef86
title: Що таке Node і які відмінності між браузером і середовищем виконання Node?
challengeType: 19
dashedName: what-is-node-and-what-are-some-differences-between-the-browser-and-node-runtime-environment
---

# --description--

До цього моменту ви писали код на JavaScript, який виконується на стороні клієнта вебзастосунку, зазвичай у веббраузері.

JavaScript на стороні клієнта може бути дуже потужним, але має певні обмеження для складніших завдань.

Наприклад:

- Він має дуже обмежений доступ до локальних файлів.
- Він не призначений для обробки складної логіки застосунку.
- І існують потенційні проблеми з безпекою. Наприклад, ви не хочете відкривати доступ до конфіденційної інформації, такої як облікові дані бази даних, у коді на стороні клієнта.
    
JavaScript на стороні клієнта має ці обмеження, бо спочатку був розроблений виключно для запуску у веббраузерах.

Веббраузери забезпечують середовище, необхідне для запуску JavaScript-коду, включно з JavaScript-двигуном. Вони також надають доступ до Document Object Model (DOM), щоб ви могли отримувати доступ до HTML-елементів у своєму коді.

Раніше ми не могли запускати JavaScript поза браузером, але тепер це можливо з Node.js.

Node.js — це середовище виконання JavaScript. Офіційно його визначають як «відкритий і кросплатформенний середовище виконання JavaScript».

Давайте розглянемо, що це означає:

- Node.js є відкритим кодом, бо його код публічно доступний і підтримується великою спільнотою розробників.
- Він кросплатформенний, бо працює на будь-якій операційній системі, включно з Windows, macOS і Linux.
- І це середовище виконання JavaScript, бо дозволяє запускати JavaScript-код поза браузером.
    
Node.js використовується у багатьох галузях. Розробники застосовують його для створення вебсерверів і API, які обробляють HTTP-запити, а також для веб- і мобільних застосунків.

Якщо ви використовуєте або розробляєте вебсайт чи вебзастосунок, який взаємодіє з базою даних або обробляє складні дані, велика ймовірність, що ви колись використаєте Node.js у бекенді.

Він ефективний, масштабований і має велику спільноту розробників і підтримувачів по всьому світу.

Тепер, коли ви знаєте, що таке Node.js, давайте розглянемо деякі відмінності між браузером і середовищем виконання Node.

Середовище браузера в основному призначене для фронтенд-розробки, тому воно виконує JavaScript на стороні клієнта. Натомість середовище виконання Node в основному призначене для бекенд-розробки, тому воно виконує JavaScript на стороні сервера. Це також визначає API, які вони пропонують.

Наприклад, з браузера ви можете отримати доступ до DOM API, але існують обмеження на доступ до локальної файлової системи. Натомість у середовищі виконання Node ви можете отримати доступ майже до всіх системних ресурсів, включно з файловою системою, але не до DOM.

Обидва середовища JavaScript надають глобальний об’єкт. У браузерному середовищі виконання JavaScript він називається `window`. Цей об’єкт `window` надає доступ до функціональностей, пов’язаних із браузером, таких як методи для маніпуляції DOM, керування куками та обробки подій браузера. У середовищі Node.js цей глобальний об’єкт називається `global`. Об’єкт `global` надає доступ до специфічних для Node.js функціональностей, таких як вбудовані модулі для роботи з локальними файлами, мережею та взаємодії з операційною системою.

Нарешті, ще одна важлива відмінність полягає в тому, що ви можете обирати версію Node.js, яку хочете використовувати на своєму сервері. Однак ви не маєте контролю над версією браузерного середовища, яке використовують ваші користувачі для відвідування вашого сайту.

Node.js став дуже важливим для розробників у всьому світі. Це потужний, масштабований і універсальний інструмент, який змінив веброзробку, дозволивши розробникам використовувати JavaScript для створення як фронтенду, так і бекенду повноцінних застосунків. Це зробило процес розробки ефективнішим, бо розробникам не потрібно вивчати нову мову програмування лише для бекенду.

# --questions--

## --text--

Який із наведених варіантів найкраще описує Node.js?

## --answers--

Бібліотека JavaScript, що використовується для фронтенд-розробки.

### --feedback--

Подумайте, де і як виконується код JavaScript при використанні Node.js.

---

Середовище виконання JavaScript, яке виконує JavaScript на стороні сервера.

---

Веббраузер, призначений для запуску JavaScript-застосунків.

### --feedback--

Подумайте, де і як виконується код JavaScript при використанні Node.js.

---

Фреймворк JavaScript для створення інтерфейсів користувача.

### --feedback--

Подумайте, де і як виконується код JavaScript при використанні Node.js.

## --video-solution--

2

---

## --text--

Яка з наведених відмінностей є ключовою між браузером і середовищем виконання Node?

## --answers--

Браузери мають прямий доступ до файлової системи, а середовище виконання Node — ні.

### --feedback--

Подумайте про ролі та призначення кожного середовища.

---

Середовище виконання Node використовує об’єкт window як свій глобальний об’єкт, а браузери — глобальний об’єкт.

### --feedback--

Подумайте про ролі та призначення кожного середовища.

---

Браузери в основному використовуються для фронтенд-розробки, а середовище виконання Node — для бекенд-розробки.

---

Браузери зазвичай взаємодіють з операційною системою, а середовище виконання Node — з DOM.

### --feedback--

Подумайте про ролі та призначення кожного середовища.

## --video-solution--

3

---

## --text--

Для якого з наведених завдань Node.js зазвичай використовується?

## --answers--

Маніпулювання HTML-структурою вебсторінки.

### --feedback--

Подумайте про можливості Node.js на стороні сервера.

---

Створення вебсервера для обробки HTTP-запитів.

---

Стилізація елементів вебсторінки за допомогою CSS.

### --feedback--

Подумайте про можливості Node.js на стороні сервера.

---

Створення інтерактивних анімацій у веббраузері.

### --feedback--

Подумайте про можливості Node.js на стороні сервера.

## --video-solution--

2
