---
id: 66edd630f7666cfa54b404d0
title: Asynchrones JavaScript-Quiz
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist asynchrones Programmieren?

#### --distractors--

Eine Methode, um Code sequenziell auszuführen.

---

Eine Technik, um Fehler in Ihrem Code zu behandeln.

---

Eine Möglichkeit, Netzwerk-Anfragen zu stellen, typischerweise um Daten vom Server abzurufen oder dorthin zu senden.

#### --answer--

Eine Methode, um Code gleichzeitig auszuführen, ohne den Hauptthread zu blockieren.

### --question--

#### --text--

Was ist ein Thread?

#### --distractors--

Eine Funktion, die verwendet wird, um Daten vom Server abzurufen.

---

Ein Programm, das JavaScript-Code in einem Webbrowser ausführt.

---

Eine spezielle Art von Funktion, die als Argument an eine andere Funktion übergeben wird.

#### --answer--

Eine Folge von Anweisungen, die unabhängig vom Hauptprogrammfluss ausgeführt werden kann.

### --question--

#### --text--

Was ist der Zweck der Fetch API in JavaScript?

#### --distractors--

Um asynchrone Callbacks zu verwalten.

---

Um Cookies zu erstellen und zu verwalten.

---

Um dynamische HTML-Inhalte zu rendern.

#### --answer--

Um Netzwerk-Anfragen an Server zu stellen.

### --question--

#### --text--

Welche HTTP-Methode wird verwendet, um Daten zur Verarbeitung an einen Server zu senden?

#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um Daten von einer API abzurufen?

#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--

Was ist ein Promise in JavaScript?

#### --distractors--

Eine Möglichkeit, mehrere Operationen zu synchronisieren, wenn man mit der Fetch API arbeitet.

---

Eine spezielle Art von Callback-Funktion, die Sie in allen Higher-Order-Funktionen verwenden müssen.

---

Eine Technik, um Fehler zu behandeln, wenn Sie mit der Fetch API arbeiten.

#### --answer--

Ein Objekt, das die endgültige Fertigstellung (oder das Scheitern) einer asynchronen Operation darstellt.

### --question--

#### --text--

Was ermöglicht Ihnen die Verkettung von Promises?

#### --distractors--

Sie können mehrere synchrone Operationen parallel ausführen.

---

Sie können synchrone Operationen effizienter handhaben.

---

Sie können fehlgeschlagene Operationen automatisch erneut versuchen.

#### --answer--

Sie können eine Folge von asynchronen Operationen nacheinander ausführen.

### --question--

#### --text--

Welche Methode wird verwendet, um den erfolgreichen Abschluss eines Promises zu behandeln?

#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--

Welche Methode wird verwendet, um Fehler in einer Promise-Kette zu behandeln?

#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--

Welche der folgenden HTTP-Methoden ist KEINE gültige Methode?

#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--

Welche der folgenden Syntaxen ist korrekt, um eine async-Funktion zu erstellen?

#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--

Was bewirkt das `await`-Schlüsselwort in einer async-Funktion?

#### --distractors--

Es erstellt ein neues Promise.

---

Es führt Code sofort aus, ohne zu warten, und gibt ein Objekt zurück.

---

Es protokolliert das Ergebnis des Promises und gibt `null` zurück.

#### --answer--

Es pausiert die Ausführung der Funktion, bis das Promise aufgelöst ist.

### --question--

#### --text--

Welche der folgenden APIs wird verwendet, um Websites eine Möglichkeit zu geben, den Standort des Nutzers anzufragen?

#### --distractors--

Locator API

---

Geo API

---

Locate API

#### --answer--

Geolocation API

### --question--

#### --text--

Welches der folgenden Attribute lädt das Skript asynchron herunter, wartet aber, bis das HTML-Dokument vollständig geparst ist, bevor das Skript ausgeführt wird?

#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--

Was wird der folgende Code zurückgeben?

```js
fetch('https://api.example.com/data')
```

#### --distractors--

Einen Fehler, der besagt, dass die Daten beschädigt sind.

---

Einen Fehler, der besagt, dass die Daten nicht abgerufen werden können.

---

Es wird nichts zurückgegeben und JavaScript fährt mit der nächsten Funktion im Skript fort.

#### --answer--

Ein `Promise`, das zu einem `Response`-Objekt aufgelöst wird.

### --question--

#### --text--

Was ist die Standard-HTTP-Methode für die Fetch API?

#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--

Welche der folgenden Methoden wird verwendet, um Ressourcen auf dem Server zu löschen?

#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um ein Promise zu erstellen?

#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--

Welches der folgenden Attribute weist den Browser an, die Skriptdatei asynchron herunterzuladen, während das HTML-Dokument weiterhin geparst wird?

#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--

Welche Methode der Geolocation API wird verwendet, um die aktuelle Position eines Geräts zu ermitteln?

#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`
