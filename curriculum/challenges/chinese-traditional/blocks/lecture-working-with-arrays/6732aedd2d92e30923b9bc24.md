---
id: 6732aedd2d92e30923b9bc24
title: 什麼是數組解構，它是如何工作的？
challengeType: 19
dashedName: what-is-array-destructuring-and-how-does-it-work
---

# --interactive--

數組解構是 JavaScript 中的一個特性，它允許你從數組中提取值，並將它們直接賦值給單獨的變量，從而讓代碼更簡潔、更易讀。 它提供了一種方便的語法，用於將數組元素拆包到獨立的變量中。

在處理數組和返回多個值的函數時，這種技術尤其有用。 下面是一個使用數組解構的示例：

:::interactive_editor

```js
let fruits = ["apple", "banana", "orange"];

let [first, second, third] = fruits;

console.log(first);  // "apple"
console.log(second); // "banana"
console.log(third);  // "orange"
```

:::

在這個示例中，我們有一個名爲 `fruits` 的數組，其中包含三個元素。 通過數組解構，我們把第一個元素賦值給變量 `first`，第二個賦給 `second`，第三個賦給 `third`。 這樣就可以很方便地訪問數組中的各個元素，而不需要寫索引。

如果不使用數組解構，而是通過索引訪問每個元素，結果會是這樣：

:::interactive_editor

```js
const fruits = ["apple", "banana", "orange"];

const first = fruits[0];
const second = fruits[1];
const third = fruits[2];

console.log(first); // "apple"
console.log(second); // "banana"
console.log(third); // "orange"
```

:::

數組解構還允許你跳過不需要的元素，只需用逗號隔開即可。 例如：

:::interactive_editor

```js
let colors = ["red", "green", "blue", "yellow"];
let [firstColor, , thirdColor] = colors;

console.log(firstColor); // "red"
console.log(thirdColor); // "blue"
```

:::

在這個示例中，我們使用了一個額外的逗號來跳過 `colors` 數組的第二個元素。 這將 `red` 賦值給 `firstColor`，將 `blue` 賦值給 `thirdColor`，有效忽略了 `green`。

數組解構的另一個強大功能是使用默認值。 如果數組中的元素少於要賦值的變量，則可以提供默認值：

:::interactive_editor

```js
let numbers = [1, 2];
let [a, b, c = 3] = numbers;

console.log(a); // 1
console.log(b); // 2
console.log(c); // 3
```

:::

這裏，因爲 `numbers` 數組沒有第三個元素，所以將默認值 `3` 賦值給 `c`。

現在，讓我們來討論剩餘語法，即用三個點表示的語法（`...`）。 它允許你將數組中尚未被解構的剩餘元素收集到一個新數組中。 具體操作如下：

:::interactive_editor

```js
let fruits = ["apple", "banana", "orange", "mango", "kiwi"];
let [first, second, ...rest] = fruits;

console.log(first);  // "apple"
console.log(second); // "banana"
console.log(rest);   // ["orange", "mango", "kiwi"]
```

:::

在這個例子中，`first` 和 `second` 分別得到前兩個元素，`rest` 則收集了 `fruits` 數組剩下的所有元素組成一個新數組。 剩餘語法必須放在解構模式的最後。

數組解構是一個強大的功能，可以讓你的代碼更加簡潔、易讀。 它在處理數組時非常有用，尤其是需要提取數組中的特定元素時。

# --questions--

## --text--

下面代碼的輸出是什麼？

```js
let numbers = [1, 2, 3, 4, 5];
let [a, , b, ...rest] = numbers;
console.log(a, b, rest);
```

## --answers--

`1 2 [3, 4, 5]`

### --feedback--

注意解構賦值中逗號的位置，並記住其剩餘法的作用。

---

`1 3 [4, 5]`

---

`1 2 [4, 5]`

### --feedback--

注意解構賦值中逗號的位置，並記住其剩餘法的作用。

---

`1 3 [2, 4, 5]`

### --feedback--

注意解構賦值中逗號的位置，並記住其剩餘法的作用。

## --video-solution--

2

## --text--

下面代碼的輸出是什麼？

```js
let colors = ["red", "green", "blue"];
let [primary, secondary, tertiary, quaternary = "yellow"] = colors;
console.log(quaternary);
```

## --answers--

`undefined`

### --feedback--

考慮一下當試圖解構的變量數量超過數組中的元素數量時會發生什麼，並記住默認值的概念。

---

`null`

### --feedback--

考慮一下當試圖解構的變量數量超過數組中的元素數量時會發生什麼，並記住默認值的概念。

---

`"yellow"`

---

會報錯。

### --feedback--

考慮一下當試圖解構的變量數量超過數組中的元素數量時會發生什麼，並記住默認值的概念。

## --video-solution--

3

## --text--

下面代碼的輸出是什麼？

```js
let fruits = ["apple", "banana", "orange", "grape"];
let [first, ...rest, last] = fruits;
console.log(first, rest, last);
```

## --answers--

`"apple" ["banana", "orange"] "grape"`

### --feedback--

考慮剩餘語法在解構賦值中的位置，並思考這是否是對剩餘語法的有效使用。

---

`"apple" ["banana", "orange", "grape"] undefined`

### --feedback--

考慮剩餘語法在解構賦值中的位置，並思考這是否是對剩餘語法的有效使用。

---

這將產生 `SyntaxError` 錯誤。

---

`"apple" [] "grape"`

### --feedback--

考慮剩餘語法在解構賦值中的位置，並思考這是否是對剩餘語法的有效使用。

## --video-solution--

3
