---
id: 66f1a417757b6ca4eecd89d6
title: Quiz sullo stato e gli hook di React
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual è il modo corretto per aggiungere un evento di clic a un pulsante in React?

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

Cos’è un React Synthetic Event?

#### --distractors--

Un tipo di elemento JSX per eventi `inputs` e `submit` soltanto.

---

Un tipo di elemento JSX per eventi `submit` soltanto.

---

Un modo per modificare la fase di bubbling della propagazione degli eventi per renderla più user friendly.

#### --answer--

Un involucro attorno agli eventi nativi come gli eventi `click` e `submit`.

### --question--

#### --text--

Quale dei seguenti è un prefisso comune usato per nominare le funzioni gestore di eventi?

#### --distractors--

isHandling

---

hasHandled

---

handling

#### --answer--

handle

### --question--

#### --text--

Quali sono le tre fasi principali nel rendering di React?

#### --distractors--

Handshake, caricamento e hydration.

---

Fetching, decoding e execution.

---

Mounting, updating e unmounting.

#### --answer--

Triggering, rendering e committing.

### --question--

#### --text--

Qual è il modo corretto per lavorare con l’hook `useState`?

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

Quale dei seguenti NON è un attributo valido usato per gestire eventi in React?

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

Qual è il modo corretto per aggiornare lo stato di un array?

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--

Cosa c’è di sbagliato in questa funzione?

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

Non è permesso aggiornare una variabile `useState` all’interno di una funzione annidata.

---

`previousState` deve essere racchiuso tra parentesi.

---

Le funzioni freccia non dovrebbero essere usate all’interno di una funzione setter di stato.

#### --answer--

`previousState` deve essere espanso prima di `name` per assicurarsi che nessun’altra proprietà venga sovrascritta.

### --question--

#### --text--

Qual è il modo corretto per aggiornare lo stato per rimuovere elementi da un array?

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

A cosa serve tipicamente l’attributo `ref` in React?

#### --distractors--

Serve per controllare problemi di performance nei componenti React.

---

Serve per controllare problemi di accessibilità nei componenti React.

---

Serve per rimuovere nodi dal DOM.

#### --answer--

Serve per consultare un nodo DOM.

### --question--

#### --text--

Qual è il modo corretto per consultare il valore corrente di un `ref`?

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

Quante volte questo messaggio verrà registrato nella console?

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

Tre volte.

---

Due volte.

---

Nessuna volta perché l’array delle dipendenze è vuoto.

#### --answer--

Solo una volta al render iniziale.

### --question--

#### --text--

A cosa servono tipicamente gli hook personalizzati di React?

#### --distractors--

Servono per eseguire le funzionalità più complesse di un’app.

---

Non sono usati tipicamente, si preferiscono gli hook integrati.

---

Sostituiscono i metodi del ciclo di vita delle versioni precedenti di React.

#### --answer--

Sono utili per riutilizzare la logica con stato tra componenti.

### --question--

#### --text--

Quante volte questo messaggio verrà registrato nella console?

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

Verrà registrato ogni volta che un modulo viene inviato o si verifica un evento di clic.

---

Verrà registrato due volte.

---

Verrà registrato solo una volta.

#### --answer--

Verrà registrato ogni volta che il componente viene renderizzato.

### --question--

#### --text--

Qual è il modo corretto per prevenire l’aggiornamento della pagina per un evento `onSubmit`?

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--

```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

Quale dei seguenti hook è tipicamente usato per recuperare dati?

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

Quale delle seguenti NON è una proprietà valida dell’oggetto Event nativo?

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

Quale fase del rendering si verifica quando React rileva che qualcosa è cambiato e l’interfaccia utente potrebbe dover essere aggiornata?

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

Quale fase si riferisce al momento in cui React prende le modifiche preparate dal DOM virtuale e le applica al DOM reale?

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

Qual è lo scopo di `dependencies` in un `useEffect`?

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

L’argomento opzionale `dependencies` non influisce su nulla riguardante `useEffect`.

---

L’argomento opzionale `dependencies` fa sì che l’effetto venga eseguito in un loop infinito.

---

L’argomento opzionale `dependencies` serve a impedire che l’effetto venga eseguito.

#### --answer--

L’argomento opzionale `dependencies` controlla quando l’effetto viene eseguito.
