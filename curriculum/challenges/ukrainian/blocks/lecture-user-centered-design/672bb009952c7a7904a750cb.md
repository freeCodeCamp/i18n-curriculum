---
id: 672bb009952c7a7904a750cb
title: Які найкращі практики для проєктування нескінченного скролінгу?
challengeType: 19
dashedName: what-are-best-practices-for-designing-infinite-scrolls
---

# --interactive--

**ПРИМІТКА**: Деякі інтерактивні приклади можуть використовувати CSS і JavaScript, які ви ще не вивчали. Не хвилюйтеся, якщо не розумієте весь код. Мета прикладів — показати вам попередній перегляд цих концепцій дизайну, щоб ви краще розуміли, як це працює. Щоб побачити попередній перегляд, потрібно увімкнути інтерактивний редактор.

Нескінченний скролінг — це шаблон дизайну, який завантажує більше вмісту, коли користувач прокручує сторінку вниз. Часто це використовується на соціальних мережах, як-от Twitter. Наприклад, якщо ви увійшли в систему і хочете побачити більше твітів, ви можете прокрутити вниз, і завантажаться нові твіти. Це приклад нескінченного скролінгу.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <div class="post">Post 4</div>
  <div class="post">Post 5</div>
  <div class="post">Post 6</div>
  <div class="post">Post 7</div>
  <div class="post">Post 8</div>
  <div class="post">Post 9</div>
  <div class="post">Post 10</div>
  <!-- More posts will load here as the user scrolls down -->
</div>

<script src="index.js"></script>
```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}
```

```js
// Simulate loading more content as the user scrolls
window.addEventListener('scroll', () => {
  if (window.innerHeight + window.scrollY >= document.body.offsetHeight) {
    loadMorePosts();
  }
});

function loadMorePosts() {
  const container = document.querySelector('.infinite-scroll');
  for (let i = 0; i < 3; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${container.children.length + 1}`;
    container.appendChild(post);
  }
}
```

:::

Нескінченний скролінг також використовується як заміна пагінації. Пагінація — це шаблон дизайну, який розбиває вміст на сторінки. Це часто застосовується, коли потрібно показати багато вмісту. Приклад пагінації — коли ви шукаєте щось у Google і бачите результати пошуку на кількох сторінках. З пагінацією потрібно натиснути на кнопку, щоб перейти до наступної сторінки. З нескінченним скролінгом ви просто продовжуєте прокручувати вниз, і завантажується більше вмісту.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="pagination">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <!-- More posts will be on the next pages -->
</div>
<div class="pagination-controls">
  <button class="prev" disabled>Previous</button>
  <button class="next">Next</button>
</div>
<script src="index.js"></script>
```

```css
.pagination {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.pagination-controls {
  margin-top: 12px;
}

.pagination-controls button {
  padding: 8px 16px;
  margin-right: 8px;
}
```

```js
let currentPage = 1;
const postsPerPage = 3;
const totalPosts = 9; 
const totalPages = Math.ceil(totalPosts / postsPerPage);
const container = document.querySelector('.pagination');
const prevButton = document.querySelector('.prev');
const nextButton = document.querySelector('.next'); 

function renderPosts() {
  container.innerHTML = '';
  const start = (currentPage - 1) * postsPerPage;
  const end = start + postsPerPage;
  for (let i = start; i < end && i < totalPosts; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${i + 1}`;
    container.appendChild(post);
  }
  prevButton.disabled = currentPage === 1;
  nextButton.disabled = currentPage === totalPages;
}

prevButton.addEventListener('click', () => {
  if (currentPage > 1) {
    currentPage--;
    renderPosts();
  }
});

nextButton.addEventListener('click', () => {
  if (currentPage < totalPages) {
    currentPage++;
    renderPosts();
  }
});

renderPosts();
```

:::

Коли ви впроваджуєте нескінченний скролінг у свій дизайн, варто пам’ятати кілька найкращих практик. Перше — надати кнопку «Завантажити ще», яка завантажує наступну множину результатів, коли користувач натискає на неї. Це хороший спосіб дати користувачу контроль над тим, коли він хоче побачити більше вмісту.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <!-- More posts will load when the user clicks "Load More" -->
</div>
<button class="load-more">Load More</button>
<script src="index.js"></script>
```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.load-more {
  margin-top: 12px;
  padding: 8px 16px;
}
```

```js
const loadMoreButton = document.querySelector('.load-more');
const container = document.querySelector('.infinite-scroll');   
loadMoreButton.addEventListener('click', () => {
  loadMorePosts();
});

function loadMorePosts() {
  for (let i = 0; i < 3; i++) {
    const post = document.createElement('div');
    post.className = 'post';
    post.textContent = `Post ${container.children.length + 1}`;
    container.appendChild(post);
  }
}
```

:::

Ще одна рекомендація — додати кнопку «Назад». Вона дає користувачам можливість повернутися на попередню сторінку, не прокручуючи всю сторінку вгору. Це покращує користувацький досвід і дає більше контролю над переглядом.

Іноді можна побачити дизайн із кнопкою «Повернутися до початку», яка веде користувачів назад до верхньої частини сторінки з результатами. Ще одна рекомендація — надати індикатор завантаження. Користувачі повинні чітко розуміти, що завантажується більше вмісту; інакше вони можуть подумати, що сторінка зламана.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="infinite-scroll">
  <div class="post">Post 1</div>
  <div class="post">Post 2</div>
  <div class="post">Post 3</div>
  <div class="post">Post 4</div>
  <div class="post">Post 5</div>
  <div class="post">Post 6</div>
  <div class="post">Post 7</div>
  <div class="post">Post 8</div>
  <div class="post">Post 9</div>
  <div class="post">Post 10</div>
  <!-- More posts will load here as the user scrolls down -->
</div>

<div class="loading-indicator" style="display: none;">Loading...</div>

<button id="back-to-top" class="back-to-top">↑ Back to Top</button>

<script src="index.js"></script>

```

```css
.infinite-scroll {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.post {
  padding: 12px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

.loading-indicator {
  margin-top: 12px;
  font-weight: bold;
}

.back-to-top {
  position: fixed;
  bottom: 30px;
  right: 30px;
  display: none;
  padding: 10px 15px;
  background-color: #007BFF;
  color: white;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2);
}

.back-to-top:hover {
  background-color: #0056b3;
}
```

```js
const container = document.querySelector('.infinite-scroll');
const loadingIndicator = document.querySelector('.loading-indicator');
const backToTopBtn = document.getElementById('back-to-top');

window.addEventListener('scroll', () => {
  if (window.scrollY > 400) {
    backToTopBtn.style.display = 'block';
  } else {
    backToTopBtn.style.display = 'none';
  }

  if (window.innerHeight + window.scrollY >= document.body.offsetHeight) {
    loadMorePosts();
  }
});

backToTopBtn.addEventListener('click', () => {
  window.scrollTo({ top: 0, behavior: 'smooth' });
});

function loadMorePosts() {
  loadingIndicator.style.display = 'block';
  setTimeout(() => {
    for (let i = 0; i < 3; i++) {
      const post = document.createElement('div');
      post.className = 'post';
      post.textContent = `Post ${container.children.length + 1}`;
      container.appendChild(post);
    }
    loadingIndicator.style.display = 'none';
  }, 1000);
}
``` 

:::

Останнє, про що варто подбати — це щоб нижній колонтитул був доступним для користувача. Якщо в нижньому колонтитулі є важлива інформація, він має бути доступним у будь-який момент.

Підсумовуючи, нескінченний скролінг — це чудовий спосіб показати вміст на вашому сайті. Проте варто пам’ятати про найкращі практики при проєктуванні нескінченного скролінгу, щоб забезпечити найкращий користувацький досвід.

# --questions--

## --text--

Що таке нескінченний скролінг?

## --answers--

Шаблон дизайну, який змінює смугу прокручування на сторінці.

### --feedback--

Перегляньте початок уроку, де вводиться це поняття.

---

Шаблон дизайну, який змінює розмір сторінки.

### --feedback--

Перегляньте початок уроку, де вводиться це поняття.

---

Шаблон дизайну, який змінює колір фону сторінки.

### --feedback--

Перегляньте початок уроку, де вводиться це поняття.

---

Шаблон дизайну, який завантажує більше вмісту, коли користувач прокручує сторінку вниз

## --video-solution--

4

## --text--

Яка роль кнопки «завантажити ще» в нескінченному скролінгу?

## --answers--

Вона перезавантажує сторінку.

### --feedback--

Назва підказує, яка її роль.

---

Щоб дати користувачам контроль над тим, коли вони хочуть побачити більше вмісту.

---

Вона дозволяє користувачам повернутися до раніше завантаженого вмісту.

### --feedback--

Назва підказує, яка її роль.

---

Вона обмежує кількість даних, які можна завантажити.

### --feedback--

Назва підказує, яка її роль.

## --video-solution--

2

## --text--

Чому важливо, щоб нижній колонтитул був доступним під час використання нескінченного скролінгу?

## --answers--

Він допомагає зробити кольори на сторінці яскравішими.

### --feedback--

Подумайте, до якої інформації користувач має мати доступ.

---

Він допомагає сторінці завантажуватися швидше.

### --feedback--

Подумайте, до якої інформації користувач має мати доступ.

---

Неважливо, щоб нижній колонтитул був доступним.

### --feedback--

Подумайте, до якої інформації користувач має мати доступ.

---

Щоб користувач міг у будь-який момент отримати доступ до посилань і важливої інформації з нижнього колонтитула.

## --video-solution--

4
