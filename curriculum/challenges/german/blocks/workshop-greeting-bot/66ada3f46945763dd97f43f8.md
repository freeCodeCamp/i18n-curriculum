---
id: 66ada3f46945763dd97f43f8
title: Schritt 7
challengeType: 1
dashedName: step-7
---

# --description--

Zuvor haben Sie die Variablen `bot` und `botLocation` erstellt. Jetzt verwenden Sie diese, um neue Nachrichten in der Konsole auszugeben.

In vorherigen Lektionen haben Sie gelernt, wie man mit String-Verkettung arbeitet, indem Sie den `+`-Operator verwenden, um Strings wie folgt zusammenzufügen:

```js
let firstName = "John";

console.log("Hello, my name is " + firstName + ".");
// result: "Hello, my name is John."
```

Denken Sie daran, beim Verketten von Strings mit Variablen auf Leerzeichen zu achten.

Erstellen Sie eine Variable namens `botIntroduction`.

Verwenden Sie dann die String-Verkettung mit dem `+`-Operator, um den String `"My name is "` gefolgt von der Variable `bot` und anschließend einem Punkt (`.`) zusammenzufügen.

Weisen Sie diesen Wert der Variable `botIntroduction` zu.

Geben Sie anschließend die Variable `botIntroduction` in der Konsole aus.

# --hints--

Sie sollten eine Variable namens `botIntroduction` haben.

```js
assert.isNotNull(botIntroduction);
```

Ihre Variable `botIntroduction` sollte den Wert eines Strings enthalten.

```js
assert.isString(botIntroduction);
```

Sie sollten die String-Verkettung mit dem `+`-Operator verwenden, um den String `"My name is "` mit der Variable `bot` gefolgt von einem Punkt (`.`) zusammenzufügen. Achten Sie auf Leerzeichen. Weisen Sie diesen verketteten String der Variable `botIntroduction` zu.

```js
assert.equal(botIntroduction, "My name is teacherBot.");
```

Ihr Code sollte eine vierte `console.log()`-Anweisung enthalten.

```js
assert.lengthOf(__helpers.removeJSComments(code).match(/console\.log(.*)/g), 4);
```

Ihre `console`-Anweisung sollte die Variable `botIntroduction` ausgeben.

```js
assert.match(__helpers.removeJSComments(code), /console\.log\s*\(\s*botIntroduction\s*\);?/);
```

# --seed--

## --seed-contents--

```js
console.log("Hi there!");
console.log("I am excited to talk to you.");
let bot;
bot = "teacherBot";

let botLocation = "the universe";

console.log("Allow me to introduce myself.");

--fcc-editable-region--

--fcc-editable-region--
```
