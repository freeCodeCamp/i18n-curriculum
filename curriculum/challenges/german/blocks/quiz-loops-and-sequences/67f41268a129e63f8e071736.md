---
id: 67f41268a129e63f8e071736
title: Quiz zu Schleifen und Folgen
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche der folgenden Optionen zeigt die korrekte Syntax für eine Liste?

#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--

Was wird in der Konsole ausgegeben?

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--

Was ist ein iterierbares Objekt?

#### --distractors--

Es ist ein spezieller Typ von String, der eine große Menge an Zeichen speichern kann.

---

Es ist ein spezieller Zahlentyp, der verwendet wird, um wirklich große Werte darzustellen.

---

Es ist ein spezieller Datentyp, der hauptsächlich in Klassen und speziellen Funktionen verwendet wird.

#### --answer--

Es ist ein spezieller Objekttyp, der Element für Element durchlaufen werden kann.

### --question--

#### --text--

Was wird Folgendes in der Konsole ausgeben?

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--

Welche der folgenden Methoden wird verwendet, um die Anzahl der Elemente in einer Liste zu erhalten?

#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--

Was passiert, wenn Sie versuchen, einen Index (entweder positiv oder negativ) zu übergeben, der außerhalb der Grenzen einer Liste liegt?

#### --distractors--

Sie erhalten eine `SyntaxError`.

---

Sie erhalten eine `RangeError`.

---

Sie erhalten eine `TypeError`.

#### --answer--

Sie erhalten eine `IndexError`.

### --question--

#### --text--

Was wird in der Konsole ausgegeben?

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--

Welche der folgenden Methoden wird verwendet, um ein Element am Ende einer Liste hinzuzufügen?

#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--

Was wird in der Konsole ausgegeben?

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--

Welche der folgenden Methoden wird verwendet, um ein Element an einem bestimmten Index in eine Liste einzufügen?

#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--

Was wird in der Konsole ausgegeben?

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--

Welche der folgenden Methoden wird verwendet, um eine Liste zu leeren?

#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

Was ist ein Tuple?

#### --distractors--

Es ist ein veränderbarer Datentyp einer zufälligen Folge von Werten.

---

Es ist ein spezieller Datentyp, der verwendet wird, um große Zahlen darzustellen.

---

Es ist eine Methode, um Elemente zu einer Liste hinzuzufügen.

#### --answer--

Es ist eine unveränderliche geordnete Folge von Werten.

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um Elemente aus einem Tuple zu entpacken?

#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--

Was wird das folgende Code-Ergebnis sein?

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--

Es wird eine `RangeError` erzeugen.

---

Es wird nichts passieren.

---

Es wird eine Endlosschleife erzeugen.

#### --answer--

Es wird eine `TypeError` erzeugen.

### --question--

#### --text--

Was wird das Ergebnis des folgenden Codes sein?

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um eine neue Liste mit den sortierten Werten zu erstellen?

#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um eine `for`-Schleife zu verwenden?

#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um eine Folge von ganzen Zahlen zu erzeugen?

#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um mehrere iterierbare Objekte parallel zu durchlaufen?

#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`

