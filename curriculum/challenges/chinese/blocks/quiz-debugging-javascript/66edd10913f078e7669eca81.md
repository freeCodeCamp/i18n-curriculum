---
id: 66edd10913f078e7669eca81
title: 调试 JavaScript 测验
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

当一个变量在被 `define` 之前使用时，会发生什么类型的误差？

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

哪个 JavaScript 语句可以用来有意暂停你的代码执行以进行调试？

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

哪个控制台方法可以用来以表格形式显示数据？

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

你如何在 Chrome 开发者工具中检查网络请求？

#### --distractors--

通过使用 `"Network Links"` 标签（页）。

---

通过使用 `"Console"` 标签（页）。

---

通过使用 "Sources" 标签（页）。

#### --answer--

通过使用 `"Network"` 标签（页）。

### --question--

#### --text--

在 JavaScript 中使用 `try...catch` 的正确语法是什么？

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

以下哪项是 `SyntaxError` 的示例？

#### --distractors--

在声明之前使用变量。

---

在同一作用域中使用一个变量两次。

---

使用超出作用域的 `variable`。

#### --answer--

数组元素之间缺少逗号。

### --question--

#### --text--

JavaScript 调试中断点的目的是什么？

#### --distractors--

自动纠正代码错误。

---

防止代码被执行。

---

将日志消息输出到控制台。

#### --answer--

在特定点暂停执行。

### --question--

#### --text--

当执行 `throw` 语句时会发生什么？

#### --distractors--

程序立即终止，忽略所有剩余的代码。

---

`throw` 语句将误差记录到控制台，但不会停止执行。

---

它会暂停代码执行，直到通过禁用断点恢复。

#### --answer--

抛出异常并执行 `catch` 块（如果存在）。

### --question--

#### --text--

在开发者工具中调试时，你会使用哪种 `method` 来检查 `variable` 或 `expression`？

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

执行以下代码时会发生什么类型的误差？

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

栈溢出误差

#### --answer--

`ReferenceError`

### --question--

#### --text--

调试时常用来逐行执行代码的工具是哪一个？

#### --distractors--

`console.log()`

---

`alert()`

---

`throw` 语句。

#### --answer--

开发者工具中的断点。

### --question--

#### --text--

尝试访问 `undefined` 的属性时会发生什么类型的误差？

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

`try...catch` 语句中的 `finally` 块有什么作用？

#### --distractors--

仅在存在误差时执行。

---

如果触发了 `catch`，则跳过执行。

---

立即退出代码块。

#### --answer--

无论是否有 `Error`，总是执行。

### --question--

#### --text--

运行以下代码的结果会是什么？

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

代码将抛出一个 `SyntaxError`。

---

代码将向控制台记录一个空数组。

---

代码将抛出内存不足误差。

#### --answer--

代码将抛出 `RangeError`。

### --question--

#### --text--

以下关于 JavaScript 中的 `debugger` 语句，哪个答案是正确的？

#### --distractors--

浏览器总是忽略 `debugger` 语句。

---

`debugger` 语句将错误报告发送到服务器。

---

`debugger` 语句总是重新装载页面并清除控制台。

#### --answer--

当开发者工具未打开时，`debugger` 语句会被忽略。

### --question--

#### --text--

JavaScript 中“profiling”的主要目的是什么？

#### --distractors--

为 JavaScript 函数编写单元测试并创建代码执行的评测。

---

暂停代码执行并释放捕捉的资源，如内存和 CPU。

---

将 JavaScript 代码转换为机器代码以实现更快的执行。

#### --answer--

通过记录 CPU 使用率、函数调用和执行时间来识别性能瓶颈。

### --question--

#### --text--

在调试的上下文中，什么是“watcher”？

#### --distractors--

一个用于暂停代码执行的工具。

---

一个用于记录误差的内置函数。

---

用于处理异常的 JavaScript 方法。

#### --answer--

一个用于监控变量值的工具。

### --question--

#### --text--

什么时候你应该使用 `console.error()` 而不是 `console.log()`？

#### --distractors--

显示表格数据。

---

仅在调试期间记录消息。

---

记录有关浏览器的信息。

#### --answer--

记录错误信息。

### --question--

#### --text--

JavaScript 中的 `Error()` 构造函数有什么作用？

#### --distractors--

将错误信息记录到控制台日志。

---

在沙箱中运行代码。

---

终止程序。

#### --answer--

创建一个可以被抛出的新建误差对象。

### --question--

#### --text--

JavaScript 中 `console.dir()` 方法的作用是什么？

#### --distractors--

为单个误差类型创建单独的日志文件目录。

---

生成控制台输出的可视化表现并将其储存在 pdf 文件中。

---

停止程序执行并显示与内存和 CPU 使用相关的错误信息。

#### --answer--

输出指定 JavaScript 对象的分层交互式属性列表。

