---
id: 60a3e3396c7b40068ad6996f
title: Passaggio 6
challengeType: 0
dashedName: step-6
---

# --description--

Aggiungi un elemento `div` nel `body`.

Imposta l'attributo `class` uguale a `canvas`.

Questo fungerà da canvas per il tuo dipinto.

# --hints--

Il tuo codice dovrebbe avere un elemento `div`.

```js
assert(document.querySelectorAll('div').length === 1)
```

L'elemento `div` dovrebbe avere un `class` con il valore `canvas`.

```js
assert(document.querySelector('div').className.split(' ').includes('canvas'))
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Rothko Painting</title>
  </head>
  <body>
--fcc-editable-region--

--fcc-editable-region--
  </body>
</html>
```
