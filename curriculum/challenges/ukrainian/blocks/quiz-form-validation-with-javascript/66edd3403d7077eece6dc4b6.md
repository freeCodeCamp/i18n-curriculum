---
id: 66edd3403d7077eece6dc4b6
title: Тест з валідації форм за допомогою JavaScript
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

Щоб пройти тест, потрібно правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який метод використовується, щоб перевірити, чи значення елемента `input` відповідає правилам валідації?

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

Що відбувається, якщо метод `checkValidity()` знаходить недійсне `input`?

#### --distractors--

Форма автоматично відправляється.

---

Нічого не відбувається.

---

Значення `input` очищується.

#### --answer--

Браузер відображає повідомлення про валідацію.

### --question--

#### --text--

Який метод використовується для налаштування повідомлення, що показується при невдачі валідації?

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

Яка поведінка валідації за замовчуванням для обов’язкових полів при відправці форми?

#### --distractors--

Форма відправляється навіть якщо обов’язкові поля порожні.

---

Форма відправляється, але показує попередження.

---

Форма скидає значення `input`.

#### --answer--

Браузер зупиняє відправку форми, якщо обов’язкові поля порожні.

### --question--

#### --text--

Яка дія не спричиняє перевірку HTML-форми на помилки?

#### --distractors--

Зміна значення `input`.

---

Відправка форми.

---

Виклик `reportValidity()`.

#### --answer--

Натиснення кнопки скидання.

### --question--

#### --text--

Що повертає `checkValidity()`, коли всі вхідні дані форми є дійсними?

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

Коли метод `checkValidity()` зупиняє відправку форми?

#### --distractors--

Коли у форми немає кнопки відправки.

---

Коли `action` форми порожній.

---

Коли метод форми — POST.

#### --answer--

Коли у форми є щонайменше один недійсний `input` (і стандартна поведінка відправки форми запобігається).

### --question--

#### --text--

Що відбувається, якщо викликати `checkValidity()`, але поле не проходить валідацію?

#### --distractors--

Форма відправляється без помилок.

---

Значення `input` очищуються.

---

Форма закривається одразу.

#### --answer--

Недійсне поле підсвічується і з’являється повідомлення.

### --question--

#### --text--

Яка дія спричинить запуск `checkValidity()`?

#### --distractors--

Скидання форми.

---

Зміна значення `input`.

---

Натиснення на тег посилання.

#### --answer--

Ручна відправка форми.

### --question--

#### --text--

Яка мета виклику `checkValidity()` перед відправкою форми?

#### --distractors--

Автоматично відправити форму.

---

Оновити сторінку.

---

Очистити всі поля `input`.

#### --answer--

Перевірити, чи всі елементи `input` відповідають заданим обмеженням.

### --question--

#### --text--

Яка основна мета `e.preventDefault()`?

#### --distractors--

Оновити форму.

---

Очистити дані форми.

---

Скинути значення форми.

#### --answer--

Зупинити стандартну дію події.

### --question--

#### --text--

Що робить `e.preventDefault()`, коли використовується на події відправки форми?

#### --distractors--

Відправляє форму.

---

Очищає всі поля форми.

---

Тимчасово відключає форму.

#### --answer--

Запобігає відправці форми.

### --question--

#### --text--

У якому випадку `e.preventDefault()` є найбільш корисним?

#### --distractors--

При обробці натискання кнопки, що відправляє форму.

---

При закритті модального вікна.

---

При відкритті нової сторінки.

#### --answer--

При відправці форми і запобіганні оновленню сторінки.

### --question--

#### --text--

Що станеться, якщо забути викликати `e.preventDefault()` на події відправки форми?

#### --distractors--

Всі поля введення встановляться у значення за замовчуванням.

---

Форма не відправиться.

---

Дані форми будуть втрачені.

#### --answer--

Форма відправиться і оновить сторінку.

### --question--

#### --text--

Яку поведінку за замовчуванням можна запобігти за допомогою `e.preventDefault()`?

#### --distractors--

Редагування форми.

---

Відтворення форми.

---

Оновлення форми.

#### --answer--

Стандартну поведінку, спричинену відправкою форми.

### --question--

#### --text--

Що спричиняє подію відправки форми?

#### --distractors--

Коли змінюється поле введення.

---

Коли форму скидають.

---

Коли очищується дія форми.

#### --answer--

Коли натискають кнопку відправки форми або клавішу Enter.

### --question--

#### --text--

Де зазвичай обробляють подію відправки у JavaScript?

#### --distractors--

На елементі текстового поля.

---

На будь-якому полі форми.

---

На кнопці поза формою.

#### --answer--

Безпосередньо на елементі форми.

### --question--

#### --text--

Що робить подія відправки за замовчуванням?

#### --distractors--

Валідує форму, але не відправляє її.

---

Очищає поля форми.

---

Скасовує відправку форми.

#### --answer--

Надсилає дані форми на сервер, вказаний у дії форми.

### --question--

#### --text--

За якою подією слід слухати, щоб обробити дані форми при її відправці у JavaScript?

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

Яке значення атрибута `method` за замовчуванням у HTML-формі?

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
