---
id: 66f1affc0ef4fcca423d4688
title: Bash와 SQL 퀴즈
challengeType: 8
dashedName: quiz-bash-and-sql
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

SQL은 무엇의 약자인가요?

#### --distractors--

표준 쿼리 언어.

---

간단한 양자 언어.

---

정교한 쿼리 언어.

#### --answer--

구조화된 쿼리 언어.

### --question--

#### --text--

새 데이터베이스를 생성하는 데 사용하는 SQL 명령어는 무엇인가요?

#### --distractors--

`BUILD DATABASE`

---

`GENERATE DATABASE`

---

`NEW DATABASE`

#### --answer--

`CREATE DATABASE`

### --question--

#### --text--

제1정규형(1NF)이란 무엇인가요?

#### --distractors--

중복 데이터가 없음.

---

부분적 종속성이 없음.

---

이행적 종속성이 없음.

#### --answer--

열에 원자값이 있음.

### --question--

#### --text--

테이블에서 모든 열을 선택하려면 어떻게 하나요?

#### --distractors--

`SELECT []`

---

`SELECT ALL`

---

`SELECT COLUMNS`

#### --answer--

`SELECT *`

### --question--

#### --text--

`FOREIGN KEY`은 무엇을 설정하나요?

#### --distractors--

고유 식별자 키.

---

기본 키.

---

널 제약 조건.

#### --answer--

테이블 간의 관계.

### --question--

#### --text--

SQL에서 정규화의 목적은 무엇인가요?

#### --distractors--

쿼리 속도 향상.

---

저장 공간 증가.

---

인덱스 추가.

#### --answer--

데이터 중복 감소.

### --question--

#### --text--

SQL 인젝션에 대한 최선의 방어책은 무엇인가요?

#### --distractors--

모든 사용자 입력 제거.

---

SQL 접근 비활성화.

---

데이터베이스 접근 제거.

#### --answer--

준비된 문장 사용.

### --question--

#### --text--

기존 테이블 데이터를 수정하는 명령어는 무엇인가요?

#### --distractors--

`MODIFY`

---

`CHANGE`

---

`ALTER`

#### --answer--

`UPDATE`

### --question--

#### --text--

조인 테이블에는 일반적으로 무엇을 포함하나요?

#### --distractors--

외래 키만.

---

타임스탬프만.

---

기본 키만.

#### --answer--

외래 키와 추가 데이터.

### --question--

#### --text--

테이블에 새 데이터를 추가하는 명령어는 무엇인가요?

#### --distractors--

`ADD`

---

`PUT`

---

`NEW DATA`

#### --answer--

`INSERT`

### --question--

#### --text--

어떤 키가 중복 값을 가질 수 있나요?

#### --distractors--

기본 키.

---

고유 키.

---

복합 키.

#### --answer--

외래 키.

### --question--

#### --text--

제3정규형(3NF)이 제거하는 것은 무엇인가요?

#### --distractors--

다중 값 종속성.

---

부분 종속성.

---

모든 종속성.

#### --answer--

이행적 종속성.

### --question--

#### --text--

SQL 결과를 필터링하려면 어떻게 하나요?

#### --distractors--

`FILTER`

---

`SORT`

---

`TAGS`

#### --answer--

`WHERE`

### --question--

#### --text--

SQL에서 N+1 문제의 주요 특징은 무엇인가요?

#### --distractors--

하나의 크고 복잡한 쿼리를 실행하고 서버로 다시 보내는 것.

---

한 번에 너무 많은 데이터를 반환하지만 알파벳순으로 정렬하는 쿼리.

---

하나의 쿼리에서 너무 많은 JOIN을 사용하고 너무 많은 데이터를 반환하는 것.

#### --answer--

하나의 초기 쿼리를 실행한 후 관련 데이터를 위해 N개의 추가 쿼리를 실행하는 것.

### --question--

#### --text--

슈퍼 키와 후보 키의 주요 차이점은 무엇인가요?

#### --distractors--

슈퍼 키는 여러 속성이나 열을 포함할 수 없지만 후보 키는 가능하다.

---

후보 키는 단일 열이어야 하며 복합 키일 수 없지만 슈퍼 키는 복합 키일 수 있다.

---

슈퍼 키는 데이터베이스 관리자가 선택하며 고유할 필요가 없다.

#### --answer--

슈퍼 키는 고유 식별에 필수적이지 않은 추가 속성을 포함할 수 있지만 후보 키는 포함할 수 없다.

### --question--

#### --text--

정규화할 때 복합 키의 일부에만 의존하는 데이터를 무엇이라고 하나요?

#### --distractors--

이행적 종속성.

---

다중 값 종속성.

---

함수적 종속성.

#### --answer--

부분 종속성.

### --question--

#### --text--

Bash에서 `>` 연산자의 주요 목적은 무엇인가요?

#### --distractors--

출력을 파일에 추가하고 `tmp`에 복사본을 생성.

---

파일에서 입력을 읽고 인코딩.

---

두 파일을 비교하여 차이점 출력.

#### --answer--

표준 출력을 파일로 리다이렉트하며, 파일이 있으면 덮어씀.

### --question--

#### --text--

`wc -l` 명령어는 무엇을 하나요?

#### --distractors--

파일 내 단어 수를 셈.

---

파일 내 문자 수를 셈.

---

현재 디렉터리의 모든 파일을 나열.

#### --answer--

파일 내 줄 수를 셈.

### --question--

#### --text--

Bash 스크립트에서 전달된 첫 번째 인수에 어떻게 접근하나요?

#### --distractors--

`$ARG1`

---

`$0`

---

`$#`

#### --answer--

`$1`

### --question--

#### --text--

학생과 강의 간 다대다 관계에서 조인 테이블에는 무엇이 포함되나요?

#### --distractors--

학생 ID만.

---

강의 ID만.

---

학생과 강의의 전체 세부 정보.

#### --answer--

두 ID와 등록 날짜.
