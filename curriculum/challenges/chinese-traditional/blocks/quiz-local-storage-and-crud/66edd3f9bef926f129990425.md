---
id: 66edd3f9bef926f129990425
title: 區域的儲存與 CRUD 測驗
challengeType: 8
dashedName: quiz-local-storage-and-crud
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

在網頁開發的背景關係中，CRUD 代表什麼？

#### --distractors--

創建、替換、復原、刪除。

---

連接、移除、更新、顯示。

---

複製、擷取、更新、刪除。

#### --answer--

創建、讀取、更新、刪除。

### --question--

#### --text--

在 RESTful API 中，哪一個 HTTP 方法通常與 Create 操作相關？

#### --distractors--

`GET`

---

`PUT`

---

`DELETE`

#### --answer--

`POST`

### --question--

#### --text--

在資料庫中修改現有記錄時，通常會執行下列哪一種 CRUD 操作？

#### --distractors--

閱讀

---

刪除

---

創建

#### --answer--

更新

### --question--

#### --text--

Web Storage API 中的 `localStorage` 和 `sessionStorage` 之間的主要差異是什麼？

#### --distractors--

`localStorage` 將資料儲存在記憶體中，而 `sessionStorage` 將資料儲存在碟中。

---

sessionStorage 在擷取資料時比 localStorage 更快。

---

`localStorage` 資料只在一個工作階段期間可用，而 `sessionStorage` 則可跨工作階段持續存在。

#### --answer--

`localStorage` 資料會持續存在直到被明確刪除，而 `sessionStorage` 資料則會在頁面會話結束時被清除。

### --question--

#### --text--

哪一種方法用於將資料儲存在 `localStorage`？

#### --distractors--

`localStorage.setData()`

---

`localStorage.saveItem()`

---

`localStorage.storeData()`

#### --answer--

`localStorage.setItem()`

### --question--

#### --text--

你如何從 `sessionStorage` 中移除單一項目？

#### --distractors--

`sessionStorage.clearItem('key')`

---

`sessionStorage.remove('key')`

---

`sessionStorage.deleteItem('key')`

#### --answer--

`sessionStorage.removeItem('key')`

### --question--

#### --text--

`PUT` 和 `PATCH` 有什麼差別？

#### --distractors--

`PUT` 修改特定欄位，而 `PATCH` 則取代整個資源。

---

`PUT` 只會新增資源，而 `PATCH` 則會更新現有資源。

---

`PUT` 用於擷取資料，而 `PATCH` 用於傳送資料。

#### --answer--

`PUT` 會取代整個資源，而 `PATCH` 則會更新特定欄位。

### --question--

#### --text--

快取記憶體用來做什麼？

#### --distractors--

快取儲存用於在瀏覽器中永久安全地儲存使用者憑證，以進行身份驗證。

---

快取儲存用於使用資料庫系統在瀏覽器中儲存大量結構化資料。

---

快取儲存用於透過儲存可跨多個網站存取的鍵值對來追蹤使用者會話和偏好設定。

#### --answer--

快取儲存用於儲存經常請求的網路資源，減少載入時間並提升效率。

### --question--

#### --text--

在網頁開發的背景關係中，什麼是 cookie？

#### --distractors--

一種 HTTP 請求的型別。

---

在瀏覽器中執行的一段程式碼。

---

一種將大型檔案儲存在伺服器上的方法。

#### --answer--

使用者的網頁瀏覽器儲存的一小段資料。

### --question--

#### --text--

以下哪一個方法用於在 JavaScript 中手動設定 cookie？

#### --distractors--

`document.setCookie`

---

`document.cookie.set`

---

`document.createCookie`

#### --answer--

`document.cookie`

### --question--

#### --text--

以下哪個程式碼片段正確刪除名為 `username` 的 cookie？

#### --distractors--

`document.cookie.delete("username");`

---

`document.removeCookie("username");`

---

`cookie.delete("username");`

#### --answer--

`document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";`

### --question--

#### --text--

以下關於 cookies 的敘述中，哪一項是錯誤的？

#### --distractors--

工作階段 Cookie 在瀏覽器關閉時過期。

---

HttpOnly cookie 無法被 JavaScript 存取。

---

安全的 cookie 僅會透過 HTTPS 傳輸。

#### --answer--

Cookie 只能由伺服器設定。

### --question--

#### --text--

什麼是瀏覽器指紋辨識？

#### --distractors--

一種根據使用者的 IP 位址進行識別的技術。

---

一種資料加密形式。

---

一種將資料儲存在瀏覽器快取中的方法。

#### --answer--

一種透過瀏覽器和裝置屬性追蹤使用者的方法。

### --question--

#### --text--

將密碼儲存在區域儲存中所帶來的主要風險是什麼？

#### --distractors--

區域的儲存資料在你關閉瀏覽器分頁後會被刪除。

---

一旦你刪除 cookie，區域的儲存資料就會遺失。

---

區域的儲存無法離線使用。

#### --answer--

區域的儲存沒有加密，且可以輕易存取。

### --question--

#### --text--

以下哪一項述語關於客戶端儲存是正確的？

#### --distractors--

客戶端儲存使用物件而非字串來儲存資料。

---

客戶端儲存使用雲端服務來同步使用者資料於多個裝置之間。

---

客戶端儲存比伺服端儲存更安全。

#### --answer--

客戶端儲存允許網站將資料儲存在使用者的裝置上。

### --question--

#### --text--

使用 `IndexedDB` 相較於 `localStorage` 的主要利益是什麼？

#### --distractors--

IndexedDB 不需要使用者同意即可儲存資料。

---

IndexedDB 有更簡單的 API。

---

所有網頁瀏覽器皆支援 `IndexedDB`。

#### --answer--

IndexedDB 可以控制代碼較大量的資料和更複雜的查詢。

### --question--

#### --text--

以下哪一項是從 IndexedDB 的物件存放區擷取資料的有效方法？

#### --distractors--

在物件儲存區上呼叫 `retrieve()`。

---

使用 `query()` 來存取特定記錄。

---

使用 `fetch()` 方法。

#### --answer--

在 store 引用上使用 `get()`。

### --question--

#### --text--

IndexedDB 支援哪種類型的交易？

#### --distractors--

僅讀取交易。

---

僅限非同步交易。

---

僅限同步交易。

#### --answer--

讀取和寫入交易。

### --question--

#### --text--

以下哪項關於 Service Worker 的生命週期是正確的？

#### --distractors--

Service Workers 在網頁應用程式的主執行緒中執行。

---

Service Workers 可以在安裝之前被作用。

---

Service Workers 只能在瀏覽器中開啟網頁應用程式時執行。

#### --answer--

Service Worker 在被作用後可以攔截網路請求。

### --question--

#### --text--

哪一個述語最能描述漸進式網頁應用程式（PWAs）的關鍵特性？

#### --distractors--

PWA 只能安裝在行動裝置上。

---

PWAs 需要有後端伺服器。

---

PWA 只能透過特定瀏覽器存取。

#### --answer--

PWA 透過 Service Workers 提供離線功能。

