---
id: 66ed9018f45ce3ece4053eb9
title: CSS-Variablen-Quiz
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welches der folgenden ist KEIN gültiger Deskriptor in der `@property`-Regel?

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

Welchen Wert wird `color` im folgenden Code haben, wenn `--main-color` nicht definiert ist?

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

Es wird keine Farbe angewendet.

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist eine gültige Art, eine benutzerdefinierte Eigenschaft in CSS zu deklarieren?

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

Was ist der Hauptzweck der Verwendung benutzerdefinierter Eigenschaften in CSS?

#### --distractors--

Um die DOM-Struktur zu ändern.

---

Um dynamische Selektoren zu erstellen.

---

Um die Barrierefreiheit zu verbessern.

#### --answer--

Um wiederverwendbare Styles zu definieren.

### --question--

#### --text--

Was stellt der `:root`-Selektor dar?

#### --distractors--

Das erste Kindelement des `body`.

---

Das Elternelement aller `header`-Elemente.

---

Das erste Kind des `html`-Tags.

#### --answer--

Das höchstrangige Element im DOM-Baum.

### --question--

#### --text--

Wie wenden Sie eine `--foreground`-benutzerdefinierte Eigenschaft als `color` in CSS an?

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

Was ist der Zweck der `@property`-Regel in CSS?

#### --distractors--

Eine Media Query definieren.

---

CSS-Animationen und deren Timing steuern.

---

CSS-Eigenschaften in einer Regel gruppieren.

#### --answer--

Definieren, wie benutzerdefinierte Eigenschaften sich verhalten.

### --question--

#### --text--

Wenn Sie eine benutzerdefinierte `@property` deklarieren, was ist der Zweck des `syntax` in ihrer Definition?

#### --distractors--

Es gibt den Standardwert der benutzerdefinierten Eigenschaft an.

---

Es bestimmt, ob die Eigenschaft von Kindelementen geerbt werden kann.

---

Es definiert, ob die Eigenschaft auf alle Elemente angewendet wird.

#### --answer--

Es erzwingt einen bestimmten Datentyp oder Wertebereich für die benutzerdefinierte Eigenschaft.

### --question--

#### --text--

Worauf sollten Sie achten, wenn Sie benutzerdefinierte Eigenschaften verwenden?

#### --distractors--

Benutzerdefinierte Eigenschaften werden automatisch in `px`-Einheiten umgewandelt.

---

Benutzerdefinierte Eigenschaften überschreiben immer inline-Styles.

---

Benutzerdefinierte Eigenschaften können nicht zum Setzen von Schriftwerten verwendet werden.

#### --answer--

Benutzerdefinierte Eigenschaften werden in älteren Browsern möglicherweise nicht unterstützt.

### --question--

#### --text--

Was ist der Zweck eines Fallback-Werts in der `var()`-Funktion für CSS-Benutzerdefinierte Eigenschaften?

#### --distractors--

Um die Menge des CSS-Codes zu reduzieren.

---

Um die Leistung in modernen Browsern zu verbessern.

---

Um die Renderzeit in langsamen Netzwerken zu optimieren.

#### --answer--

Um sicherzustellen, dass ein gültiger Wert angewendet wird, falls die benutzerdefinierte Eigenschaft nicht definiert ist.

### --question--

#### --text--

Was passiert, wenn Sie eine variable `--foreground` innerhalb einer Media Query definieren und die Media Query nicht mehr zum aktuellen Viewport passt?

#### --distractors--

Die benutzerdefinierte Eigenschaft bleibt erhalten und wird weiterhin angewendet.

---

Die benutzerdefinierte Eigenschaft wird auf ihren Initialwert zurückgesetzt.

---

Die benutzerdefinierte Eigenschaft wird basierend auf dem Viewport neu berechnet.

#### --answer--

Die benutzerdefinierte Eigenschaft ist nicht mehr verfügbar, da sie auf die Media Query beschränkt ist.

### --question--

#### --text--

Wie würde sich die folgende Deklaration verhalten?

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

Sie wendet `--main-color` an, auch wenn es nicht definiert ist, und verwendet diesen Wert als Standard.

---

Sie wendet `--main-color` und `--fallback-color` gleichzeitig an, was zu einer Mischung der beiden Farben führt.

---

Sie verwendet immer `#000` als Standard, unabhängig davon, ob `--main-color` oder `--fallback-color` definiert ist.

#### --answer--

Sie wendet `--main-color` an, wenn definiert; andernfalls prüft sie `--fallback-color;` und falls beide undefiniert sind, wird `#000` verwendet.

### --question--

#### --text--

Was ist ein Vorteil der Verwendung von `@property` in CSS?

#### --distractors--

Es verbessert die Leistung durch Vorberechnung von benutzerdefinierten Eigenschaftswerten.

---

Es fügt automatisch Präfixe für bessere Browserunterstützung hinzu.

---

Es beschränkt die Verwendung benutzerdefinierter Eigenschaften auf bestimmte Elemente.

#### --answer--

Es ermöglicht Animationen von benutzerdefinierten Eigenschaften.

### --question--

#### --text--

Was steuert die `inherits`-Eigenschaft in einer benutzerdefinierten `@property`-Deklaration?

#### --distractors--

Ob die benutzerdefinierte Eigenschaft einen Initialwert hat.

---

Ob die Eigenschaft eine Kurzschreibweise haben kann.

---

Ob die Eigenschaft Fallback-Werte akzeptiert.

#### --answer--

Ob der Wert der benutzerdefinierten Eigenschaft an Kindelemente weitergegeben wird.

### --question--

#### --text--

Was gibt das `@property` in der Deklaration einer benutzerdefinierten `initial-value` an?

#### --distractors--

Die zulässigen Werte, die die Eigenschaft annehmen kann.

---

Die Priorität der Eigenschaft in der Kaskade.

---

Den Typ des Werts, den die Eigenschaft haben muss.

#### --answer--

Den Fallback-Wert für die Eigenschaft.

### --question--

#### --text--

Angenommen, Sie haben folgenden HTML- und CSS-Code. Welchen Wert hat die `color`-Eigenschaft für das `.box`-Element?

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

Auf welche Eigenschaft sollte ein CSS-Verlauf angewendet werden?

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

Was ist der Zweck von Farb-Stopps in CSS-Verläufen?

#### --distractors--

Um den Transparenzgrad des Verlaufs zu definieren.

---

Um die Richtung des Verlaufs anzugeben.

---

Um den Verlauf in einem festen Muster zu wiederholen.

#### --answer--

Um die spezifischen Punkte zu definieren, an denen sich die Farben im Verlauf ändern.

### --question--

#### --text--

Was passiert, wenn in einem CSS-linear-gradient kein Winkel oder keine Richtung angegeben ist?

#### --distractors--

Der Verlauf wird diagonal dargestellt.

---

Der Verlauf bewegt sich von unten nach oben.

---

Der Verlauf bewegt sich von links nach rechts.

#### --answer--

Der Verlauf bewegt sich von oben nach unten.

### --question--

#### --text--

Welche CSS-Verlaufsfunktion ermöglicht es, einen Verlauf zu erstellen, der von einem zentralen Punkt nach außen strahlt?

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

Welche der folgenden ist eine korrekte Art, eine CSS-Benutzerdefinierte Eigenschaft zu deklarieren?

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

Welcher Selektor wird typischerweise verwendet, um globale CSS-Benutzerdefinierte Eigenschaften zu definieren?

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

Warum wird empfohlen, bei der Verwendung von `var()` einen Fallback-Wert anzugeben?

#### --distractors--

Damit die Variable korrekt animiert wird.

---

Um das Laden externer Stylesheets zu vermeiden.

---

Um Browser-Reflows zu verhindern.

#### --answer--

Um einen gültigen Wert zu garantieren, falls die benutzerdefinierte Eigenschaft nicht definiert ist.

### --question--

#### --text--

Was ist die korrekte Syntax, um eine benutzerdefinierte Eigenschaft als Hintergrundfarbe anzuwenden?

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

Welche CSS-Regel ermöglicht es Entwicklern, benutzerdefinierte Eigenschaften mit größerer Kontrolle über ihr Verhalten zu definieren?

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

Was steuert das `inherits`-Feld in einer `@property`-Definition?

#### --distractors--

Ob die Eigenschaft in JavaScript verwendet wird.

---

Ob die Eigenschaft Neuzeichnungen auslöst.

---

Ob die Eigenschaft Funktionen enthalten kann.

#### --answer--

Ob der Wert der Eigenschaft an Kindelemente weitergegeben wird.

### --question--

#### --text--

Welche Syntax sollte verwendet werden, um einen animierten Verlaufswinkel mit `@property` zu definieren?

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

Wodurch unterscheidet sich eine CSS-Benutzerdefinierte Eigenschaft von einer Standard-CSS-Eigenschaft hinsichtlich ihrer Definition?

#### --distractors--

Standard-CSS-Eigenschaften werden mit einem Bindestrich definiert, benutzerdefinierte Eigenschaften mit zwei Bindestrichen.

---

Standard-CSS-Eigenschaften verwenden `var()` zur Definition, benutzerdefinierte Eigenschaften nicht.

---

Standard-CSS-Eigenschaften werden mit der `@property`-Regel definiert, benutzerdefinierte Eigenschaften nicht.

#### --answer--

Benutzerdefinierte Eigenschaften müssen mit zwei Bindestrichen (`--`) beginnen, Standard-CSS-Eigenschaften nicht.

### --question--

#### --text--

Welcher Vorteil ergibt sich aus der Verwendung von CSS-Benutzerdefinierten Eigenschaften?

#### --distractors--

Sie reduzieren den Bedarf an CSS-Kommentaren.

---

Sie optimieren automatisch Bilder.

---

Sie machen JavaScript-Variablen überflüssig.

#### --answer--

Sie ermöglichen die Wiederverwendung und zentrale Pflege von Styles.

### --question--

#### --text--

Welche CSS-Eigenschaft kann sich dynamisch basierend auf Media Queries mit benutzerdefinierten Eigenschaften ändern?

#### --distractors--

Nur `background-color`

---

Nur `font-family`

---

Nur `z-index`

#### --answer--

Jede Eigenschaft, die einen Wert akzeptiert.

### --question--

#### --text--

Welche Rolle spielt `initial-value` in einer `@property`-Regel im Kontext von CSS-Variablen?

#### --distractors--

Es legt den Minimalwert der Eigenschaft fest.

---

Es definiert den Maximalwert für Animationen.

---

Es ändert die Priorität des Selektors.

#### --answer--

Es weist der Eigenschaft einen Standardwert zu, falls keiner gesetzt ist.

### --question--

#### --text--

Betrachten Sie folgenden HTML- und CSS-Code. Welche Hintergrundfarbe wird auf das `.card`-Element angewendet?

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

Was passiert im folgenden CSS, wenn ein Benutzer versucht, einen ungültigen Wert an `--padding` zuzuweisen (z. B. eine Farbe statt einer Länge)?

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

Der Browser akzeptiert den Wert, ignoriert ihn aber beim Layout.

---

Der Browser wirft einen Laufzeitfehler.

---

Der Browser konvertiert den Wert automatisch in eine gültige Länge.

#### --answer--

Der Browser fällt auf den Initialwert der Eigenschaft zurück.

### --question--

#### --text--

Was ist der Zweck der benutzerdefinierten Eigenschaft `--gradient-angle` in diesem Beispiel?

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

Sie definiert die Geschwindigkeit des Verlaufübergangs.

---

Sie gibt die Größe des Verlaufs an.

---

Sie legt den Farbüberblendmodus fest.

#### --answer--

Sie steuert die Richtung des Verlaufs.

### --question--

#### --text--

Welches Szenario zeigt am besten den Vorteil der Verwendung benutzerdefinierter Eigenschaften für Themes?

#### --distractors--

Sie nur für Schriftgrößen zu verwenden.

---

Sie nur auf `:hover` anzuwenden.

---

Sie auf eine CSS-Klasse zu beschränken.

#### --answer--

Werte über Klassen zu ändern.

### --question--

#### --text--

Warum bevorzugen Entwickler möglicherweise die Verwendung von `:root` zur Definition von CSS-Variablen?

#### --distractors--

Weil `:root` die Ladegeschwindigkeit der Seite verbessert.

---

Weil `:root` die Spezifität deaktiviert.

---

Weil `:root` für JavaScript erforderlich ist.

#### --answer--

Weil `:root` es ermöglicht, die Eigenschaften global zu definieren.

### --question--

#### --text--

Was würde das folgende CSS tun, wenn `--secondary-color` nicht definiert ist?

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

Es wendet `black` an.

---

Es ignoriert die `color`-Eigenschaft.

---

Es wendet `--secondary-color` an.

#### --answer--

Es wendet `orange` an.

### --question--

#### --text--

Welche Art von Werten stellt `<color>` in einem `@property`-Syntaxfeld dar?

#### --distractors--

Winkel wie `90deg`.

---

Längen wie `10px`.

---

Prozentwerte wie `50%`.

#### --answer--

Farbwerte wie `#ff0000` oder `red`.

### --question--

#### --text--

Was ist der Hauptvorteil der Verwendung benutzerdefinierter Eigenschaften in Kombination mit `@property`?

#### --distractors--

Sie verhindern die Notwendigkeit von Fallback-Werten.

---

Sie erzwingen ein statisches Layout.

---

Sie reduzieren den Bedarf an Klassen.

#### --answer--

Sie ermöglichen die Animation von Eigenschaften.

### --question--

#### --text--

Was bewirkt `var(--undefined-property, fallback)`, wenn die benutzerdefinierte Eigenschaft nicht definiert ist?

#### --distractors--

Es wirft einen Fehler und stoppt die Anwendung der Styles.

---

Es ignoriert die gesamte CSS-Regel.

---

Es wendet den Variablennamen unverändert an.

#### --answer--

Es verwendet stattdessen den Fallback-Wert.
