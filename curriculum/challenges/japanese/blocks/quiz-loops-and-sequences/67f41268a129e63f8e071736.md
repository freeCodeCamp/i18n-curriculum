---
id: 67f41268a129e63f8e071736
title: ループとシーケンスのクイズ
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

リストの正しい構文はどれですか？

#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--

コンソールに何が出力されますか？

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--

イテラブルとは何ですか？

#### --distractors--

大量の文字を保持できる特別な種類の文字列です。

---

非常に大きな値を表すために使われる特別な種類の数値です。

---

主にクラスや特別な関数で使われる特別なデータ型です。

#### --answer--

一度に1つずつ繰り返すことができる特別な種類のオブジェクトです。

### --question--

#### --text--

次のコードはコンソールに何を出力しますか？

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--

リストの要素数を取得するために使われるのはどれですか？

#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--

リストの範囲外のインデックス（正または負）を渡そうとするとどうなりますか？

#### --distractors--

`SyntaxError`が返されます。

---

`RangeError`が返されます。

---

`TypeError`が返されます。

#### --answer--

`IndexError`が返されます。

### --question--

#### --text--

コンソールに何が出力されますか？

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--

リストの末尾にアイテムを追加するために使われるメソッドはどれですか？

#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--

コンソールに何が出力されますか？

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--

リストの特定のインデックスに要素を挿入するために使われるメソッドはどれですか？

#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--

コンソールに何が出力されますか？

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--

リストを空にするために使われるメソッドはどれですか？

#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

タプルとは何ですか？

#### --distractors--

ランダムな値のシーケンスで変更可能なデータ型です。

---

大きな数値を表すために使われる特別なデータ型です。

---

リストに要素を追加するために使われるメソッドです。

#### --answer--

変更不可能な順序付きの値のシーケンスです。

### --question--

#### --text--

タプルからアイテムをアンパックする正しい方法はどれですか？

#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--

次のコードの結果はどうなりますか？

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--

`RangeError`が生成されます。

---

何も起こりません。

---

無限ループが作成されます。

#### --answer--

`TypeError`が生成されます。

### --question--

#### --text--

次のコードの結果はどうなりますか？

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--

ソートされた値の新しいリストを作成するために使われる関数はどれですか？

#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--

`for`ループを使う正しい方法はどれですか？

#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--

整数のシーケンスを生成するために使われる関数はどれですか？

#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--

複数のイテラブルを並行して繰り返すために使われる関数はどれですか？

#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`
