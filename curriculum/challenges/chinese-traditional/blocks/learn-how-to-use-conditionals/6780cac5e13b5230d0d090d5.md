---
id: 6780cac5e13b5230d0d090d5
title: 任務 68
challengeType: 19
dashedName: task-68
lang: en-US
---

# --instructions--

此任務沒有音訊。請閱讀以下問題並選擇正確答案。

# --questions--

## --text--

哪一句語法正確？

## --answers--

`If we lose critical data, it do set us back weeks.`

### --feedback--

這句話中錯誤地將字 `do` 用於 `could`。

---

`If we lose critical data, it could set us back weeks.`

---

`If we lose critical data, it could sets us back weeks.`

### --feedback--

動詞 `sets` 在這句話中形式不正確。

---

`If we losing critical data, it could set us back weeks.`

### --feedback--

這句話中錯誤地使用了字 `losing` 來代替 `lose`。

## --video-solution--

2

# --explanation--

正確的句子使用 `First Conditional` 結構，該結構由以下組成：

`if` + `Present Simple`：`If we lose critical data`。

情態動詞（`could`）＋原形動詞：`it could set us back weeks`。


`First Conditional` 用來描述可能發生的未來事件及其結果。
