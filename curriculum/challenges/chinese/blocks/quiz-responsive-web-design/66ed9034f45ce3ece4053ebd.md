---
id: 66ed9034f45ce3ece4053ebd
title: 响应式网页设计测验
challengeType: 8
dashedName: quiz-responsive-web-design
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是响应式网页设计中的断点？

#### --distractors--

设计中浮动元素与页面上其他元素重叠的特定点。

---

用于为网格或弹性布局设置列和行的特定点。

---

用于确定表格布局在页面上如何表现的具体要点。

#### --answer--

设计中调整布局和内容以适应不同屏幕尺寸的特定点。

### --question--

#### --text--

以下哪项不是用于较小设备的断点？

#### --distractors--

`600px`

---

`480px`

---

`320px`

#### --answer--

`960px`

### --question--

#### --text--

在响应式网页设计中使用媒体查询的主要目的是什么？

#### --distractors--

更改 Safari 浏览器的页面配色方案。

---

为表格布局创建动画。

---

使用 CSS flexbox 调整布局的外边距和内边距。

#### --answer--

根据屏幕尺寸或设备类型应用不同的样式。

### --question--

#### --text--

媒体查询中的哪个媒体特性会检查浏览器窗口的宽度？

#### --distractors--

`set-width`

---

`accept-width`

---

`allow-width`

#### --answer--

`min-width`

### --question--

#### --text--

下面的代码的作用是什么？

```css
@media screen and (min-width: 768px) {
  /* Styles go here */
}
```

#### --distractors--

这将忽略宽度为 `768px` 的屏幕的样式。

---

这将为宽度仅为 `768px` 的屏幕应用样式。

---

这将为小于 `768px` 的屏幕应用样式。

#### --answer--

这将为 `768px` 或更宽的屏幕应用样式。

### --question--

#### --text--

移动优先设计强调什么？

#### --distractors--

只为小型设备设计，而忽视其他设备。

---

只为苹果公司生产的移动设备设计。

---

首先为较小的安卓屏幕设计，而忽略苹果设备。

#### --answer--

首先为较小的屏幕设计，然后为较大的设备扩展。

### --question--

#### --text--

当设备宽度介于 600px 和 1200px 之间时，哪个媒体查询会应用样式？

#### --distractors--

`@media screen and (max-width: 1200px)`

---

`@media screen and (min-width: 600px)`

---

`@media screen and (width: 800px)`

#### --answer--

`@media screen and (min-width: 600px) and (max-width: 1200px)`

### --question--

#### --text--

以下哪项不是有效的媒体类型？

#### --distractors--

`all`

---

`print`

---

`screen`

#### --answer--

`poster`

### --question--

#### --text--

以下哪种媒体类型适用于在打印预览模式下在屏幕上查看的分页材料和文档？

#### --distractors--

`aspect-ratio`

---

`flex`

---

`screen`

#### --answer--

`print`

### --question--

#### --text--

媒体查询中的 `aspect-ratio` 的作用是什么？

#### --distractors--

它描述了弹性布局中弹性属性之间的比例。

---

它描述了网格容器 x 轴和 y 轴之间的比例。

---

它描述了表格布局的行列比例。

#### --answer--

它描述了视口宽度和高度之间的比例。

### --question--

#### --text--

以下哪项用于指示设备是横向还是纵向？

#### --distractors--

`apply-orientation`

---

`set-orientation`

---

`oriente`

#### --answer--

`orientation`

### --question--

#### --text--

以下哪项常用于针对桌面屏幕或更大的屏幕？

#### --distractors--

```css
@media screen and (min-width: 140000px) {
  /* Styles go here */
}
```

---

```css
@media screen and (min-width: 140px) {
  /* Styles go here */
}
```

---

```css
@media screen and (min-width: 14000px) {
  /* Styles go here */
}
```

#### --answer--

```css
@media screen and (min-width: 1400px) {
  /* Styles go here */
}
```

### --question--

#### --text--

以下哪项用于检测用户是否要求使用浅色或深色主题？

#### --distractors--

`allow-colors-scheme`

---

`apply-color-scheme`

---

`set-colors-scheme`

#### --answer--

`prefers-color-scheme`

### --question--

#### --text--

以下哪项用于测试主输入机制是否可以悬停在元素上？

#### --distractors--

```css
@media (set: hover) {
  /* Styles for devices that support hover */
}
```

---

```css
@media (apply: hover) {
  /* Styles for devices that support hover */
}
```

---

```css
@media (hover: apply) {
  /* Styles for devices that support hover */
}
```

#### --answer--

```css
@media (hover: hover) {
  /* Styles for devices that support hover */
}
```

### --question--

#### --text--

以下哪项不是可用于媒体查询的逻辑运算符类型？

#### --distractors--

`only`

---

`not`

---

`and`

#### --answer--

`accept`

### --question--

#### --text--

在响应式网页设计中，以下哪个断点通常用于平板电脑？

#### --distractors--

`480px`

---

`1920px`

---

`2560px`

#### --answer--

`768px`

### --question--

#### --text--

以下哪项是在媒体查询中应用 `aspect-ratio` 的正确方法？

#### --distractors--

```css
@media screen and (aspect-ratio: 16-9) {
  /* Styles for screens with a 16:9 aspect ratio */
}
```

---

```css
@media screen and (aspect-ratio: 16=9) {
  /* Styles for screens with a 16:9 aspect ratio */
}
```

---

```css
@media screen and (aspect-ratio: 16:9) {
  /* Styles for screens with a 16:9 aspect ratio */
}
```

#### --answer--

```css
@media screen and (aspect-ratio: 16/9) {
  /* Styles for screens with a 16:9 aspect ratio */
}
```

### --question--

#### --text--

哪个 CSS 属性用于创建无需媒体查询即可根据屏幕尺寸进行调整的布局？

#### --distractors--

`float`

---

`display: block;`

---

`width: 100%;`

#### --answer--

`flex`

### --question--

#### --text--

以下哪项可用于`768px`或更大的横向屏幕？

#### --distractors--

```css
@media screen and (min-width: 768px) and (landscape: orientation) {
  /* Styles go here */
}
```

---

```css
@media screen and (max-width: 768px) and (orientation: landscape) {
  /* Styles go here */
}
```

---

```css
@media screen and (min-width: 768px) and (landscape: set) {
  /* Styles go here */
}
```

#### --answer--

```css
@media screen and (min-width: 768px) and (orientation: landscape) {
  /* Styles go here */
}
```

### --question--

#### --text--

如果没有指定媒体类型，默认媒体类型是什么？

#### --distractors--

`screen`

---

`mobile`

---

`print`

#### --answer--

`all`

## --quiz--

### --question--

#### --text--

响应式设计中常用哪个 CSS 单位来创建流体布局？

#### --distractors--

`px`

---

`em`

---

`rem`

#### --answer--

`%`

### --question--

#### --text--

在响应式网页设计中使用媒体查询的主要目的是什么？

#### --distractors--

根据用户输入应用不同的样式

---

加快网站加载速度

---

自动更改网站配色方案

#### --answer--

根据屏幕尺寸调整布局和设计

### --question--

#### --text--

哪个 CSS `width` 值可使元素的宽度与其父容器的宽度成比例缩放，从而使其适用于响应式设计？

#### --distractors--

`auto`

---

`100vh`

---

`fit-content`

#### --answer--

`100%`

### --question--

#### --text--

以下哪项是可在媒体查询中使用的媒体特性示例？

#### --distractors--

`background-color`

---

`font-size`

---

`text-align`

#### --answer--

`min-width`

### --question--

#### --text--

`@media screen and (max-width: 768px)` 媒体查询适用于什么？

#### --distractors--

它将样式应用于宽度大于 768px 的屏幕。

---

它将样式应用于所有屏幕，无论屏幕大小如何。

---

它适用于分辨率大于 768dpi 的屏幕样式。

#### --answer--

它将样式应用于宽度小于 768px 的屏幕。

### --question--

#### --text--

以下哪种媒体查询特性针对屏幕分辨率至少为 300dpi 的设备？

#### --distractors--

`@media screen and (min-width: 300dpi)`

---

`@media screen and (resolution: 300dpi)`

---

`@media screen and (min-res: 300dpi)`

#### --answer--

`@media screen and (min-resolution: 300dpi)`

### --question--

#### --text--

当设备不支持悬停交互时，你会使用以下哪种媒体特性来应用样式？

#### --distractors--

`@media (hover: hover)`

---

`@media (hover: auto)`

---

`@media (no-hover: false)`

#### --answer--

`@media (hover: none)`

### --question--

#### --text--

以下哪项是使用媒体查询针对纵向设备的有效方法？

#### --distractors--

`@media screen and (orientation: landscape) { ... }`

---

`@media (orientation: portrait) { ... }`

---

`@media screen and (min-orientation: portrait) { ... }`

#### --answer--

`@media screen and (orientation: portrait) { ... }`

### --question--

#### --text--

哪种媒体查询特性用于根据设备的分辨率应用样式？

#### --distractors--

`@media (resolution: high) { ... }`

---

`@media screen and (resolution: 300dpi) { ... }`

---

`@media (min-resolution: 150) { ... }`

#### --answer--

`@media (min-resolution: 300dpi) { ... }`

### --question--

#### --text--

CSS 中的 `@media (hover: hover)` 查询的作用是什么？

#### --distractors--

它只在触摸设备上应用样式。

---

它可根据设备方向改变样式。

---

它能检测设备是否使用触摸屏。

#### --answer--

它会检查设备是否支持鼠标悬停。

### --question--

#### --text--

哪种媒体特性允许你仅在视网膜屏等高像素密度屏幕上应用样式？

#### --distractors--

`device-type`

---

`aspect-ratio`

---

`min-width`

#### --answer--

`min-resolution`

### --question--

#### --text--

媒体查询中可以使用哪个关键字来组合多个条件？

#### --distractors--

if

---

then

---

combine

#### --answer--

and

### --question--

#### --text--

哪种 CSS 技术通常与媒体查询一起使用，以便仅在特定条件下应用样式？

#### --distractors--

对所有元素使用内联样式

---

使用 JavaScript 检测屏幕尺寸并应用类

---

为手机和台式机链接不同的 HTML 文件

#### --answer--

在媒体查询中使用 `and` 等逻辑运算符组合媒体特性

### --question--

#### --text--

哪种 CSS 技术有助于确保图像在不同屏幕尺寸上按比例缩放？

#### --distractors--

使用固定像素宽度的图像

---

将图像设置为背景元素

---

应用具有精确尺寸的内联样式

#### --answer--

将图像宽度设置为百分比，如 `width: 100%`

### --question--

#### --text--

`@media screen and (min-resolution: 300dpi)` 查询的目的是什么？

#### --distractors--

为像素密度较低的屏幕应用样式。

---

根据屏幕宽度应用样式。

---

根据设备的像素分辨率应用样式。

#### --answer--

为高像素密度的屏幕应用样式。

### --question--

#### --text--

关于响应式设计中的移动优先方法，以下哪项是正确的？

#### --distractors--

它的重点是首先为大型桌面屏幕进行设计，然后再缩小。

---

它主要针对平板电脑屏幕进行设计，然后再进行缩小。

---

它只针对智能手机，而忽略了平板电脑或台式机。

#### --answer--

它的重点是首先为移动设备设计，然后再扩展到更大的屏幕。

### --question--

#### --text--

哪种媒体查询特性用于检测屏幕方向（横向或纵向）？

#### --distractors--

`aspect-ratio`

---

`device-width`

---

`max-width`

#### --answer--

`orientation`

### --question--

#### --text--

`prefers-color-scheme` 媒体特性检测什么？

#### --distractors--

`The type of device used for display`

---

`The screen resolution`

---

`The user's preferred font size`

#### --answer--

`Whether the user prefers a light or dark theme`

### --question--

#### --text--

`orientation` 媒体特性检测什么？

#### --distractors--

`The width of the screen`

---

`The resolution of the display`

---

`The type of device (tablet, phone, etc.)`

#### --answer--

`Whether the device is in landscape or portrait mode`

### --question--

#### --text--

为像素密度至少为 2 倍的设备应用样式的正确语法是什么？

#### --distractors--

`@media (resolution: 2x)`

---

`@media screen and (pixel-ratio: 2)`

---

`@media (dpi: 300)`

#### --answer--

`@media screen and (min-resolution: 192dpi)`
