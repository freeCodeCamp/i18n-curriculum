---
id: 66edcc779993c0da6906dbb9
title: Questionário sobre funções em JavaScript
challengeType: 8
dashedName: quiz-javascript-functions
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual das seguintes é a maneira correta de declarar uma função em JavaScript?

#### --distractors--

```js
func greet() {
  console.log("Hello there!");
}
```

---

```js
defineFunction greet() {
  console.log("Hello there!");
}
```

---

```js
def greet() {
  console.log("Hello there!");
}
```

#### --answer--

```js
function greet() {
  console.log("Hello there!");
}
```

### --question--

#### --text--

O que acontece quando você chama (ou executa) uma função?

#### --distractors--

O código dentro da função será executado e `null` sempre será retornado.

---

Nada acontecerá quando você chamar uma função.

---

Um `TypeError` será lançado e o programa irá travar.

#### --answer--

O código dentro da função será executado e um valor será retornado.

### --question--

#### --text--

Qual das seguintes é a maneira correta de chamar uma função?

#### --distractors--

```js
exampleFunction;
```

---

```js
exampleFunction>>;
```

---

```js
()exampleFunction();
```

#### --answer--

```js
exampleFunction();
```

### --question--

#### --text--

Qual é o papel da palavra-chave `return`?

#### --distractors--

A palavra-chave `return` executará a função.

---

A palavra-chave `return` lançará um erro de tipo.

---

A palavra-chave `return` não tem propósito em funções.

#### --answer--

A palavra-chave `return` retorna um valor da função.

### --question--

#### --text--

Qual das seguintes é uma expressão de função válida?

#### --distractors--

```js
expression function getSum(x, y) {
    return x + y;
};
```

---

```js
getSum: function(x, y) {
    return x + y;
};
```

---

```js
function = getSum(x, y) {
    return x + y;
};
```

#### --answer--

```js
const getSum = function(x, y) {
    return x + y;
};
```

### --question--

#### --text--

O que são argumentos de função?

#### --distractors--

Valores que representam a ausência de um valor.

---

Valores especiais que agem de forma semelhante a funções anônimas.

---

Valores de placeholder usados dentro da função.

#### --answer--

Valores reais passados para uma função quando ela é chamada.

### --question--

#### --text--

O que são parâmetros de função?

#### --distractors--

Valores de ponto flutuante usados dentro da função.

---

Valores passados para uma função quando ela é chamada.

---

Valores que representam a ausência intencional do valor.

#### --answer--

Valores listados dentro da definição da função.

### --question--

#### --text--

O que será registrado no console?

```js
const calculateTotal = (amount, taxRate = 0.05) => {
  return amount + (amount * taxRate);
};

console.log(calculateTotal(100));
```

#### --distractors--

`10`

---

`undefined`

---

`1`

#### --answer--

`105`

### --question--

#### --text--

Qual das seguintes opções é a sintaxe correta para uma arrow function?

#### --distractors--

```js
const sum <<>> (num1, num2) => num1 + num2
```

---

```js
const sum === (num1, num2) === num1 + num2
```

---

```js
const sum >=> (num1, num2) => num1 + num2
```

#### --answer--

```js
const sum = (num1, num2) => num1 + num2
```

### --question--

#### --text--

Qual será o resultado do seguinte código?

```js
function greet() {
  const developer = "Jessica";
  console.log("Hello there!");
}

console.log(developer);
```

#### --distractors--

Nada acontecerá e o código será executado normalmente.

---

A string `"Hello there!"` será registrada no console.

---

A string `"Hello!"` será registrada no console.

#### --answer--

Você receberá um erro de referência porque `developer` não está definido globalmente.

### --question--

#### --text--

Quando você pode omitir um conjunto de parênteses ao redor de uma lista de parâmetros para uma arrow function?

#### --distractors--

Você deve sempre omitir os parênteses ao redor de uma lista de parâmetros.

---

Você nunca deve omitir os parênteses para uma arrow function.

---

Você deve omitir os parênteses apenas quando estiver trabalhando com múltiplos parâmetros.

#### --answer--

Você deve omitir os parênteses apenas quando tiver um único parâmetro.

### --question--

#### --text--

Quando você pode omitir as chaves e a palavra-chave `return` em uma função arrow?

#### --distractors--

Você deve sempre omitir as chaves e a palavra-chave `return`.

---

Você nunca deve omitir as chaves e a palavra-chave `return`.

---

Quando o corpo da função tem múltiplas linhas de código.

#### --answer--

Quando o corpo da função consiste em uma única expressão.

### --question--

#### --text--

Qual será o resultado para o seguinte código?

```js
function exampleFunction() {
  return "Hello";
  return "World!";
};

exampleFunction();
```

#### --distractors--

`"World!"`

---

`"Hello World!"`

---

`undefined`

#### --answer--

`"Hello"`

### --question--

#### --text--

Qual das seguintes é a maneira correta de usar parâmetros padrão?

#### --distractors--

```js
function sum(num1, num2 <<>> 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 === 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 >> 12) {
  return num1 + num2
}
```

#### --answer--

```js
function sum(num1, num2 = 12) {
  return num1 + num2
}
```

### --question--

#### --text--

Qual será o resultado para o seguinte código?

```js
const developer = "Jessica";

function greet() {
  console.log("Hello, " + developer)
}

greet();
```

#### --distractors--

A string `"Hello, developer"` será registrada no console.

---

Uma mensagem de erro será exibida no console.

---

Nada será exibido no console.

#### --answer--

A string `"Hello, Jessica"` será registrada no console.

### --question--

#### --text--

O que acontece se você tentar remover as chaves para uma função regular?

#### --distractors--

Uma caixa de alerta será exibida na página com um erro.

---

O programa vai travar e não reiniciar.

---

Nada acontecerá.

#### --answer--

Você receberá um erro de sintaxe.

### --question--

#### --text--

Qual será o resultado para o seguinte código?

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + 10);
```

#### --distractors--

Uma mensagem de erro será exibida no console.

---

O número `0` será registrado no console.

---

Nada será registrado no console.

#### --answer--

O número `10` será registrado no console.

### --question--

#### --text--

Qual será a saída para o seguinte código?

```js
const exampleFunction = (param1, param2) => param1 + param2;
console.log(exampleFunction(3, "Something"));
```

#### --distractors--

`null`

---

`undefined`

---

`3 + "Something"`

#### --answer--

`"3Something"`

### --question--

#### --text--

Qual será o resultado para o seguinte código?

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + num2);
```

#### --distractors--

Uma mensagem de erro dizendo que `num1` não está definido.

---

O número `0` será exibido no console.

---

`undefined` será exibido no console.

#### --answer--

Uma mensagem de erro dizendo que `num2` não está definido.

### --question--

#### --text--

Qual será a saída para o seguinte código?

```js
const divideTwoNumbers = (num1, num2) => num1 / num2;
console.log(divideTwoNumbers(3, 0));
```

#### --distractors--

`Error`

---

`0`

---

`null`

#### --answer--

`Infinity`

