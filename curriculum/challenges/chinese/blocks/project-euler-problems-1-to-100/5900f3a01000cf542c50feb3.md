---
id: 5900f3a01000cf542c50feb3
title: '问题 52：排列倍数'
challengeType: 1
forumTopicId: 302163
dashedName: problem-52-permuted-multiples
---

# --description--

可以看出，数字125874及其双精度数251748包含完全相同的数字，但顺序不同。

查找最小的正整数，使其乘以整数 `$\\{2, 3, \ldots, n\\}$` 后，包含相同的数字。

# --hints--

`permutedMultiples(2)` 应该返回一个数字。

```js
assert(typeof permutedMultiples(2) === 'number');
```

`permutedMultiples(2)` 应该返回 `125874`。

```js
assert.strictEqual(permutedMultiples(2), 125874);
```

`permutedMultiples(6)` 应该返回 `142857`。

```js
assert.strictEqual(permutedMultiples(6), 142857);
```

# --seed--

## --seed-contents--

```js
function permutedMultiples(n) {

  return true;
}

permutedMultiples(2);
```

# --solutions--

```js
function permutedMultiples(n) {
    const isPermutation = (a, b) =>
        a.length !== b.length
            ? false
            : a.split('').sort().join() === b.split('').sort().join();


    let start = 1;
    let found = false;
    let result = 0;

    while (!found) {
        start *= 10;
        for (let i = start; i < start * 10 / n; i++) {
            found = true;
            for (let j = 2; j <= n; j++) {
                if (!isPermutation(i + '', j * i + '')) {
                    found = false;
                    break;
                }
            }
            if (found) {
                result = i;
                break;
            }
        }
    }

    return result;
}
```
