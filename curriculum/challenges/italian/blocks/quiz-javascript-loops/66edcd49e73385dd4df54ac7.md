---
id: 66edcd49e73385dd4df54ac7
title: Quiz sui loop in JavaScript
challengeType: 8
dashedName: quiz-javascript-loops
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale delle seguenti descrive meglio l'iterazione?

#### --distractors--

È il processo di scrivere codice ripetutamente finché non funziona correttamente.

---

È una tecnica per memorizzare dati in posizioni di memoria consecutive.

---

È il processo di inviare segnali tra componenti per stabilire una connessione.

#### --answer--

È il processo di ripetere un insieme di istruzioni più volte.

### --question--

#### --text--

Quale tecnica si usa per le iterazioni nella programmazione?

#### --distractors--

Ricorsione

---

Istruzioni condizionali

---

Compilazione

#### --answer--

Loop (iterazione)

### --question--

#### --text--

Quale dei seguenti è un oggetto iterabile?

#### --distractors--

Undefined

---

Number

---

Boolean

#### --answer--

Stringa

### --question--

#### --text--

Quale dei seguenti non è un tipo di loop supportato in JavaScript?

#### --distractors--

`for` loop.

---

`while` loop.

---

`for...in` loop.

#### --answer--

`if...else` loop.

### --question--

#### --text--

Qual è l'ordine corretto della dichiarazione del `for` loop?

#### --distractors--

```javascript
for (condition; increment/decrement; initialization) {
  statement;
}
```

---

```javascript
for (increment/decrement; condition; initialization) {
  statement;
}
```

---

```javascript
for (initialization; increment/decrement; condition) {
  statement;
}
```

#### --answer--

```javascript
for (initialization; condition; increment/decrement) {
  statement;
}
```

### --question--

#### --text--

Quale loop esegue il blocco di codice una volta, prima di verificare se la condizione è vera, e continua a funzionare finché la condizione rimane vera?

#### --distractors--

`while` loop.

---

`for...of` loop.

---

`for...in` loop.

#### --answer--

`do...while` loop.

### --question--

#### --text--

Quale dei seguenti itera sui valori di un oggetto iterabile?

#### --distractors--

`for...in` loop.

---

`for` loop.

---

`do...while` loop.

#### --answer--

`for...of` loop.

### --question--

#### --text--

Quale delle seguenti definisce meglio un loop infinito?

#### --distractors--

Un loop che gira finché la condizione diventa falsa.

---

Un loop che gira una volta e poi termina.

---

Un loop che si ferma automaticamente dopo un numero fisso di iterazioni.

#### --answer--

Un loop la cui condizione di terminazione non viene mai soddisfatta o è assente.

### --question--

#### --text--

Quante volte girerà il seguente loop?  

```js
for (let i = 2; i < 10; i+=2) {  
  console.log(i);  
}
```

#### --distractors--

5

---

9

---

10

#### --answer--

4

### --question--

#### --text--

Quale loop è il migliore per iterare sugli oggetti?

#### --distractors--

`for...of` loop.

---

`do...while` loop.

---

`for` loop.

#### --answer--

`for...in` loop.

### --question--

#### --text--

Qual è la differenza tra il `for...in` loop e il `for...of` loop?

#### --distractors--

Il `for...in` loop itera sui valori delle proprietà, mentre il `for...of` loop itera sui nomi delle proprietà (chiavi).

---

I loop `for...in` creano nuove proprietà, mentre i loop `for...of` modificano proprietà esistenti.

---

I loop `for...in` possono essere usati solo con stringhe, mentre i loop `for...of` possono essere usati sia con stringhe che con numeri.

#### --answer--

Il `for...in` loop itera sui nomi delle proprietà (chiavi), mentre il `for...of` loop itera sui valori delle proprietà.

### --question--

#### --text--

Quale di questi causerebbe un loop infinito?

#### --distractors--

Inizializzazione di una variabile.

---

Una condizione di loop che alla fine diventa falsa.

---

Logica di incremento/decremento.

#### --answer--

Una condizione di loop che restituisce sempre true.

### --question--

#### --text--

Quale istruzione può essere usata per uscire completamente da un loop?

#### --distractors--

L'istruzione `continue`.

---

L'istruzione `label`.

---

L'istruzione `skip`.

#### --answer--

L'istruzione `break`.

### --question--

#### --text--

Quale istruzione può essere usata per saltare l'iterazione corrente e passare a quella successiva?

#### --distractors--

L'istruzione `break`.

---

L'istruzione `label`.

---

L'istruzione `return`.

#### --answer--

L'istruzione `continue`.

### --question--

#### --text--

Cosa viene stampato nella console con il seguente codice?

```js
for (let i = 0; i < 25; i += 2) {
  if (i % 5 === 0) {
    continue;
  }
  if (i % 13 === 0) {
    break;
  }
  console.log(i);
}
```

#### --distractors--

`0`, `2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

---

`0`, `2`, `4`, `6`, `8`, `12`

---

`2`, `4`, `6`, `8`, `12`

#### --answer--

`2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

### --question--

#### --text--

Cosa stamperebbe il seguente codice nella console?

```javascript
for (let i = 1; i < 6; i++) {
  if (i === 4) break;
  console.log(i);
}
```

#### --distractors--

`1`, `2`, `3`, `4`, `5`, `6`

---

`1`, `2`, `3`, `4`, `5`

---

`1`, `2`, `3`, `4`

#### --answer--

`1`, `2`, `3`

### --question--

#### --text--

Cosa verrà stampato nella console con il codice qui sotto?

```javascript
const shoppingList = { tomatoes: 4, apples: 10 };
for (const item in shoppingList) {
  console.log(item);
}
```

#### --distractors--

`tomatoes - 4`, `apples - 10`

---

`4`, `10`

---

`tomatoes:4`, `apples:10`

#### --answer--

`tomatoes`, `apples`

### --question--

#### --text--

Quale sarà l'output della console del codice qui sotto?

```javascript
for (let i = 2; i <= 13; i++) {
  if (i % 2 === 0) continue;
  console.log(i);
}
```

#### --distractors--

`2`, `4`, `6`, `8`, `10`, `12`

---

`1`, `3`, `5`, `7`, `9`, `11`, `13`

---

`1`, `3`, `5`, `7`, `9`, `11`

#### --answer--

`3`, `5`, `7`, `9`, `11`, `13`

### --question--

#### --text--

Cosa verrebbe stampato nella console con il codice qui sotto?

```javascript
const fruits = ["Mango", "Pineapple", "Oranges"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

#### --distractors--

`0`, `1`, `2`

---

`Pineapple`, `Oranges`, `Mango`

---

`Oranges`, `Pineapple`, `Mango`

#### --answer--

`Mango`, `Pineapple`, `Oranges`

### --question--

#### --text--

Quante volte il codice qui sotto stamperà la stringa `in the loop`?

```javascript
let x = 0
while (x < 5) {
  console.log("in the loop");
}
```

#### --distractors--

3 volte

---

4 volte

---

5 volte

#### --answer--

Infinite volte
