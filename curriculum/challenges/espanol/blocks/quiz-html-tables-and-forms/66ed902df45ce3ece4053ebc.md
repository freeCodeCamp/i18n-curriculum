---
id: 66ed902df45ce3ece4053ebc
title: Cuestionario de tablas y formularios HTML
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

Para aprobar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Cuál de los siguientes atributos se utiliza para especificar la URL a donde se deben enviar los datos del formulario?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

¿Qué dos elementos se utilizan para especificar una fila y un encabezado de fila en una tabla?

#### --distractors--

El elemento `r` se usa para filas y el elemento `thead` se usa para el encabezado.

---

El elemento `row` se usa para filas y el elemento `th` se usa para el encabezado.

---

El elemento `tr` se usa para filas y el elemento `head` se usa para el encabezado.

#### --answer--

El elemento `tr` se usa para filas y el elemento `th` se usa para el encabezado.

### --question--

#### --text--

¿Para qué se utiliza el elemento `td`?

#### --distractors--

Se usa para definir un tipo de datos de tabla.

---

Se utiliza para fusionar dos columnas.

---

Se utiliza para fusionar dos filas.

#### --answer--

Se usa para definir una celda de tabla.

### --question--

#### --text--

¿Cuál es la función del atributo `colspan`?

#### --distractors--

Define el largo de una columna en una tabla.

---

Elimina el salto de línea entre el texto en una columna.

---

Se utiliza para definir el número de columnas.

#### --answer--

Define el número de columnas que una celda de tabla debe abarcar.

### --question--

#### --text--

¿Cuál es el papel del atributo `placeholder`?

#### --distractors--

Se utiliza para enviar datos al servidor cuando el formulario se envía.

---

Se utiliza para asociar un elemento `label` con un elemento `input`.

---

Se utiliza para especificar que un campo de entrada es de solo lectura para el usuario.

#### --answer--

Se utiliza para proporcionar una sugerencia para un campo de entrada.

### --question--

#### --text--

¿Cuál es el propósito del elemento `thead`?

#### --distractors--

Se utiliza para agrupar el contenido del cuerpo en una tabla HTML.

---

Se utiliza para agrupar la descripción de una tabla HTML.

---

Se utiliza para agrupar el contenido del pie de página en una tabla HTML.

#### --answer--

Se utiliza para agrupar el contenido del encabezado en una tabla HTML.

### --question--

#### --text--

¿Cuál de los siguientes es un ejemplo de asociación de formulario explícito entre etiquetas e inputs?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

¿Para qué se usa un validador de HTML?

#### --distractors--

Un validador es una herramienta que automáticamente formatea tu código HTML.

---

Un validador es una herramienta que aplica estilos a tu HTML.

---

Un validador es una herramienta que hace que tu código HTML corra más rápido.

#### --answer--

Un validador es una herramienta que verifica la sintaxis del código HTML para asegurar que es válido.

### --question--

#### --text--

¿Qué elemento se utiliza para definir una celda en una tabla?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

¿Cuál de las siguientes es una herramienta que te permite inspeccionar y modificar la estructura HTML de una página web?

#### --distractors--

Eliminación de DOM.

---

Tester de DOM.

---

Validación de DOM.

#### --answer--

Inspección DOM.

### --question--

#### --text--

¿Cuál de los siguientes es la manera correcta de agrupar campos de entrada relacionados?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

¿Cuál de los siguientes se usa para especificar que un campo de entrada es de solo lectura?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

¿Cuál atributo especifica que una entrada debe completarse antes de enviar el formulario?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

¿Para qué se utilizan las Devtools?

#### --distractors--

Estas herramientas están integradas directamente en el navegador y se utilizan para detectar cualquier problema de linting en tu código.

---

Estas herramientas están integradas directamente en el navegador y se utilizan para formatear automáticamente tu código.

---

Estas herramientas están integradas directamente en el navegador y se utilizan para ayudarte a asegurar el 100% de cobertura de pruebas para tu código.

#### --answer--

Estas herramientas están integradas directamente en el navegador y se utilizan para ayudarte a depurar, perfilar y analizar páginas web.

### --question--

#### --text--

¿Cuál de las siguientes es la manera correcta de desactivar una entrada?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

¿Cuál de los siguientes es un valor válido para el atributo `type`?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

¿Cuál de los siguientes es el uso correcto del atributo `size`?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

¿Cuáles de los siguientes atributos se utilizan para especificar el número mínimo y máximo de caracteres requeridos en un campo de entrada?

#### --distractors--

`minlen` y `maxlen`

---

`min` y `max`

---

`minimumLen` y `maximumLen`

#### --answer--

`minlength` y `maxlength`

### --question--

#### --text--

¿Cuál de los siguientes ejemplos de botón **NO** utiliza un valor correcto para el atributo `type`?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

¿Cuál de los siguientes atributos se utiliza para especificar el valor para un botón?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

¿Cuál es el papel del atributo `action` dentro de la etiqueta de apertura `form`?

#### --distractors--

Se utiliza para especificar que un campo de entrada es de solo lectura.

---

Se utiliza para definir el número de caracteres que deberían ser visibles cuando el usuario escribe en la entrada.

---

Se utiliza para mostrar una sugerencia al usuario para mostrarles qué ingresar en el campo de entrada.

#### --answer--

Se utiliza para especificar la URL donde deben enviarse los datos del formulario.

### --question--

#### --text--

¿Cuál de los siguientes atributos se utiliza para especificar el método HTTP a usar cuando se envían los datos del formulario?

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

¿Cuál de los siguientes es un método común utilizado al trabajar con formularios?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

¿Cuál de los siguientes NO es un valor válido para el atributo `type`?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

¿Cuál de los siguientes es la manera correcta de crear un botón en un formulario?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

¿Qué es la asociación de formulario implícita?

#### --distractors--

Es donde las entradas pueden asociarse con etiquetas utilizando los atributos `action` y `for` en el elemento `label`.

---

Es donde las entradas pueden asociarse con etiquetas utilizando los atributos `required` y `for` en el elemento `label`.

---

Es donde las entradas pueden asociarse con etiquetas utilizando el atributo `for` en el elemento `label`.

#### --answer--

Es donde las entradas pueden asociarse con etiquetas envolviendo el campo de entrada dentro del elemento `label`.

### --question--

#### --text--

¿Cuál es el papel del elemento `fieldset`?

#### --distractors--

Se utiliza para agrupar el contenido del encabezado en una tabla HTML.

---

Se utiliza para crear una etiqueta para un campo de entrada.

---

Se utiliza para agregar un pie al grupo de inputs.

#### --answer--

Se utiliza para agrupar inputs relacionados.

### --question--

#### --text--

¿Cuál de los siguientes se utiliza para crear una casilla de verificación en un formulario?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

¿Cuál de los siguientes elementos se utiliza para agregar un título a una tabla HTML?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

¿Cuál es el papel del elemento `legend`?

#### --distractors--

Se utiliza para agrupar el contenido del cuerpo en una tabla HTML.

---

Se utiliza para agregar un pie que describe las celdas de una tabla.

---

Se utiliza para agrupar inputs relacionados.

#### --answer--

Se utiliza para agregar un pie al grupo de inputs.

### --question--

#### --text--

¿Cuál de los siguientes elementos se utiliza para agrupar el contenido del encabezado en una tabla HTML?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

¿Cuál de las siguientes es la manera correcta de agregar filas y celdas de datos a una tabla?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

¿Qué es el inspector DOM?

#### --distractors--

Una herramienta que se usa para quitar todos los elementos del DOM.

---

Un conjunto de herramientas integrado directamente en el navegador que te ayuda a depurar, perfilar y analizar páginas web.

---

Una herramienta que verifica la sintaxis del código HTML para asegurar que es válido.

#### --answer--

Una herramienta que te permite inspeccionar y modificar la estructura HTML de una página web.

### --question--

#### --text--

¿Cuál de las siguientes es una herramienta que verifica la sintaxis del código HTML para asegurar que es válido?

#### --distractors--

Verificar HTML

---

HTML esValido

---

HTML válido

#### --answer--

Validador HTML

### --question--

#### --text--

¿Cuál de las siguientes es la forma correcta de especificar el número de columnas para la celda de datos en una tabla?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

¿Qué es el estado enfocado para un campo de entrada?

#### --distractors--

Este es el estado de un campo de entrada cuando el usuario retira el foco.

---

Este es el estado de un campo de entrada cuando se configura como solo lectura por el usuario.

---

Este es el estado de un campo de entrada cuando está deshabilitado por el usuario.

#### --answer--

Este es el estado de un campo de entrada cuando es seleccionado por el usuario.

### --question--

#### --text--

¿Cuál de los siguientes es la manera correcta de crear un botón de opción?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

¿Cuál es el papel del atributo `for`? 

#### --distractors--

Se utiliza para mostrar una sugerencia al usuario.

---

Se utiliza para especificar el valor de la entrada

---

Se utiliza para crear una etiqueta para un campo de entrada.

#### --answer--

Se utiliza para especificar para qué campo de entrada es la etiqueta.

### --question--

#### --text--

¿Cuál de los siguientes atributos se utiliza para definir el número de caracteres que deberían ser visibles cuando el usuario escribe en la entrada?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

¿Cuál de los siguientes atributos se utiliza para mostrar una sugerencia al usuario para mostrarle qué ingresar en el campo de entrada?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
