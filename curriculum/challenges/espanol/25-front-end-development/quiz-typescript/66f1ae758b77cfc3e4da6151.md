---
id: 66f1ae758b77cfc3e4da6151
title: Cuestionario de TypeScript
challengeType: 8
dashedName: quiz-typescript
---

# --description--

Para pasar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué es TypeScript?

#### --distractors--

Un marco utilizado para la programación de sistemas.

---

Una herramienta de gestión de bases de datos.

---

Un lenguaje de programación utilizado para estilizar sitios web.

#### --answer--

Un superconjunto de JavaScript que añade tipeado estático.

### --question--

#### --text--

¿Por qué debería usarse TypeScript en un proyecto?

#### --distractors--

Elimina la necesidad de pruebas en las aplicaciones.

---

Es más rápido que todos los demás lenguajes de programación.

---

Hace que el estilizado de sitios web sea más fácil.

#### --answer--

Mejora la mantenibilidad al añadir definiciones de tipo a JavaScript.

### --question--

#### --text--

¿A qué lenguaje se compila TypeScript?

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

¿Cuál sería la salida del siguiente fragmento de código?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

¿Cuál es el propósito principal de una interfaz en TypeScript?

#### --distractors--

Compilar el código de TypeScript en JavaScript.

---

Crear un nuevo tipo de dato.

---

Crear un nuevo objeto de JavaScript.

#### --answer--

Definir la estructura de un objeto.

### --question--

#### --text--

¿Cuál de los siguientes NO es un primitivo básico en TypeScript?

#### --distractors--

cadena

---

booleano

---

número

#### --answer--

arreglo

### --question--

#### --text--

¿Cuál de los siguientes define correctamente un tipo literal en TypeScript?

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

¿Cuál es el propósito principal de la palabra clave `type` en TypeScript?

#### --distractors--

Declarar variables.

---

Crear clases.

---

Implementar interfaces.

#### --answer--

Definir tipos personalizados.

### --question--

#### --text--

¿Cuál de los siguientes define correctamente un tipo de unión?

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

¿Cuál de las siguientes capacidades es única para `interface` y NO puede lograrse usando `type` en TypeScript?

#### --distractors--

Definir formas de objetos.

---

Crear tipos de unión.

---

Definir tipos que solo incluyan valores específicos.

#### --answer--

Declarar implementaciones de clases.

### --question--

#### --text--

¿Cuál es el propósito principal de usar genéricos en TypeScript?

#### --distractors--

Implementar herencia entre clases.

---

Definir la estructura de un objeto.

---

Crear un nuevo tipo de dato.

#### --answer--

Escribir código reutilizable que pueda trabajar con múltiples tipos.

### --question--

#### --text--

¿Cuál de los siguientes es un **ejemplo adecuado** de una función genérica en TypeScript?

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

¿Qué es el estrechamiento de tipo en TypeScript?

#### --distractors--

Reducir el tamaño del JavaScript compilado.

---

Limitar el número de tipos en una unión.

---

Eliminar tipos no utilizados de un proyecto TypeScript.

#### --answer--

Escribir código que reduzca un tipo de unión a un tipo más específico.

### --question--

#### --text--

¿Qué archivo se utiliza típicamente para configurar las opciones del compilador de TypeScript?

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

¿Qué especifica la opción `rootDir` en el archivo `tsconfig.json`?

#### --distractors--

La versión de TypeScript a utilizar durante la compilación.

---

El entorno de ejecución de destino (e.g., Node.js o navegador) para el código de TypeScript.

---

El sistema de módulos específico a utilizar para importaciones y exportaciones.

#### --answer--

El directorio que contiene sus archivos fuente.

### --question--

#### --text--

¿Cuál ejemplo utiliza `instanceof` correctamente para reducir el tipo para acceder de forma segura a la propiedad `valor`?

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
¿Qué hace la configuración `"estricto": verdadero` en `tsconfig.json` en TypeScript?

#### --distractors--

Habilita la comprobación de tipos estáticos para que pueda detectar errores antes de ejecutar su código.

---

Le obliga a utilizar siempre la sintaxis ES6.

---

Le obliga a compilar el código antes de ejecutarlo.

#### --answer--

Habilita un conjunto de opciones de verificación de tipos, como el manejo requerido de `null` o `indefinido` y evita `cualquier` implícito.

### --question--

#### --text--

¿Cuál es el propósito del tipo `nunca` en TypeScript?

#### --distractors--

Para representar nulo o indefinido.

---

Para representar cualquier valor posible.

---

Para representar un tipo que puede ser cualquier cosa excepto nulo.

#### --answer--

Para representar el tipo de valores que nunca ocurren.

### --question--

#### --text--

¿Cuál de las siguientes llamadas a la función `getRandomValue` se compilará con éxito en TypeScript?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

¿Cómo puede solucionarse el error del compilador de TypeScript `'email' es posiblemente 'nulo'` en el siguiente código usando el encadenamiento opcional?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`

