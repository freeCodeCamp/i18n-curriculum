---
id: 67f41268a129e63f8e071736
title: 반복문과 수열 퀴즈
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

다음 중 리스트의 올바른 구문은 무엇인가요?

#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--

콘솔에 무엇이 출력될까요?

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--

반복 가능한 객체란 무엇인가요?

#### --distractors--

많은 문자를 담을 수 있는 특별한 유형의 문자열입니다.

---

매우 큰 값을 나타내는 데 사용되는 특별한 유형의 숫자입니다.

---

주로 클래스와 특별한 함수에서 사용되는 특별한 데이터 유형입니다.

#### --answer--

한 번에 하나씩 순회할 수 있는 특별한 유형의 객체입니다.

### --question--

#### --text--

다음이 콘솔에 무엇을 출력할까요?

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--

다음 중 리스트의 요소 개수를 얻는 데 사용되는 것은 무엇인가요?

#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--

리스트의 범위를 벗어난 인덱스(양수 또는 음수)를 전달하면 어떻게 되나요?

#### --distractors--

`SyntaxError` 오류가 발생합니다.

---

`RangeError` 오류가 발생합니다.

---

`TypeError` 오류가 발생합니다.

#### --answer--

`IndexError` 오류가 발생합니다.

### --question--

#### --text--

콘솔에 무엇이 출력될까요?

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--

다음 중 리스트 끝에 항목을 추가하는 데 사용되는 메서드는 무엇인가요?

#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--

콘솔에 무엇이 출력될까요?

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--

다음 중 리스트의 특정 인덱스에 요소를 삽입하는 데 사용되는 메서드는 무엇인가요?

#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--

콘솔에 무엇이 출력될까요?

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--

다음 중 리스트를 비우는 데 사용되는 메서드는 무엇인가요?

#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

튜플이란 무엇인가요?

#### --distractors--

임의의 값 수열로 이루어진 변경 가능한 데이터 유형입니다.

---

큰 숫자를 나타내는 데 사용되는 특별한 데이터 유형입니다.

---

리스트에 요소를 추가하는 데 사용되는 메서드입니다.

#### --answer--

변경 불가능한 순서가 있는 값의 수열입니다.

### --question--

#### --text--

튜플에서 항목을 올바르게 풀어내는 방법은 무엇인가요?

#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--

다음 코드의 결과는 무엇일까요?

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--

`RangeError`이 생성됩니다.

---

아무 일도 일어나지 않습니다.

---

무한 루프가 생성됩니다.

#### --answer--

`TypeError`이 생성됩니다.

### --question--

#### --text--

다음 코드의 결과는 무엇일까요?

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--

다음 중 정렬된 값의 새 리스트를 만드는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--

다음 중 `for` 반복문을 올바르게 사용하는 방법은 무엇인가요?

#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--

다음 중 정수 수열을 생성하는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--

다음 중 여러 반복 가능한 객체를 병렬로 순회하는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`
