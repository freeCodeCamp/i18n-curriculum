---
id: 697a49e6ff50d756c9b69362
title: "도전 과제 185: 2026년 동계 경기 6일차: 피겨 스케이팅"
challengeType: 28
dashedName: challenge-185
---

# --description--

심판 점수 배열과 선택적 감점이 주어졌을 때, 피겨 스케이팅 연기의 최종 점수를 계산하세요.

첫 번째 인수는 0부터 10까지의 숫자로 이루어진 10개의 심판 점수 배열입니다. 가장 높은 점수와 가장 낮은 점수를 제거하고 나머지 8개의 점수를 합산하여 기본 점수를 구하세요.

함수에 전달되는 추가 인수들은 감점입니다. 모든 감점을 기본 점수에서 빼서 최종 점수를 구하세요.

# --hints--

`computeScore([10, 8, 9, 6, 9, 8, 8, 9, 7, 7], 1)`는 `64`을 반환해야 합니다.

```js
assert.equal(computeScore([10, 8, 9, 6, 9, 8, 8, 9, 7, 7], 1), 64);
```

`computeScore([10, 10, 10, 10, 10, 10, 10, 10, 10, 10])`는 `80`을 반환해야 합니다.

```js
assert.equal(computeScore([10, 10, 10, 10, 10, 10, 10, 10, 10, 10]), 80);
```

`computeScore([10, 8, 9, 10, 9, 8, 8, 9, 10, 7], 1, 2, 1)`는 `67`을 반환해야 합니다.

```js
assert.equal(computeScore([10, 8, 9, 10, 9, 8, 8, 9, 10, 7], 1, 2, 1), 67);
```

`computeScore([8.0, 8.5, 9.0, 8.5, 9.0, 8.0, 9.0, 8.5, 9.0, 8.5], 0.5, 1.0)`는 `67.5`을 반환해야 합니다.

```js
assert.equal(computeScore([8.0, 8.5, 9.0, 8.5, 9.0, 8.0, 9.0, 8.5, 9.0, 8.5], 0.5, 1.0), 67.5);
```

`computeScore([6.0, 8.5, 7.0, 9.0, 7.5, 8.0, 6.5, 9.5, 7.0, 8.0], 1.5, 0.5, 0.5)`는 `59`을 반환해야 합니다.

```js
assert.equal(computeScore([6.0, 8.5, 7.0, 9.0, 7.5, 8.0, 6.5, 9.5, 7.0, 8.0], 1.5, 0.5, 0.5), 59);
```

# --seed--

## --seed-contents--

```js
function computeScore(judgeScores, ...penalties) {

  return judgeScores;
}
```

# --solutions--

```js
function computeScore(judgeScores, ...penalties) {
  const sortedScores = [...judgeScores].sort((a, b) => a - b);
  const baseScore = sortedScores.slice(1, 9).reduce((sum, s) => sum + s, 0);
  const totalPenalty = penalties.reduce((sum, p) => sum + p, 0);

  return baseScore - totalPenalty;
}
```
