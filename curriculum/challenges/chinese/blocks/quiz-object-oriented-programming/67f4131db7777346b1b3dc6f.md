---
id: 67f4131db7777346b1b3dc6f
title: 面向对象编程测验
challengeType: 8
dashedName: quiz-object-oriented-programming
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么作为创建对象的蓝图？

#### --distractors--

函数

---

变量

---

循环

#### --answer--

课程

### --question--

#### --text--

Python 使用哪个模块来实现抽象的类？

#### --distractors--

`Time`

---

`xyz`

---

`Random`

#### --answer--

`abc`

### --question--

#### --text--

什么定义了一个对象的数据和行为？

#### --distractors--

函数和类。

---

变量和循环。

---

Seeder 和 数据库。

#### --answer--

属性和方法。

### --question--

#### --text--

什么是单继承？

#### --distractors--

一个子类继承自单个函数的属性和方法。

---

一个子类继承自多个函数的属性和方法。

---

一个从多个类继承属性和方法的子类。

#### --answer--

一个子类继承自单个类的属性和方法。

### --question--

#### --text--

以下哪项不是面向对象编程的关键原则之一？

#### --distractors--

封装

---

继承

---

抽象

#### --answer--

不要重复自己（DRY）

### --question--

#### --text--

将属性和方法前缀加双下划线与单下划线有什么区别？

#### --distractors--

单下划线会完全隐藏该属性，双下划线则使其只能在方法内部被读取。

---

单下划线表示私有，双下划线表示受保护。

---

单下划线使它们成为公共的，双下划线使它们成为静态的。

#### --answer--

单下划线只是内部使用的约定，双下划线触发名称重整以防止意外覆盖属性和方法。

### --question--

#### --text--

哪个面向对象的概念让你隐藏复杂的实现细节，只显示对象或系统的基本特色？

#### --distractors--

继承

---

多态

---

封装

#### --answer--

抽象

### --question--

#### --text--

你使用哪个装饰器来创建 setter？

#### --distractors--

`@<property_name>.creator`

---

`@<method_name>.creater`

---

`@<method_name>.creator`

#### --answer--

`@<property_name>.setter`

### --question--

#### --text--

哪个面向对象的概念允许你通过一组像门一样的公共方法和属性来隐藏对象的内部状态？

#### --distractors--

多态

---

抽象

---

单继承

#### --answer--

封装

### --question--

#### --text--

什么是 getter？

#### --distractors--

一种更新或更改属性值的方法。

---

一种从对象中删除属性的方法。

---

一种在对象中创建新属性的方法。

#### --answer--

一种检索或返回属性值的方法。

### --question--

#### --text--

什么是 setter？

#### --distractors--

一种获取属性值的方法。

---

一个删除属性值的方法。

---

一种从对象中删除属性的方法。

#### --answer--

一个设置或更新属性值的方法。

### --question--

#### --text--

什么让你删除用 setter 和 getter 设置和获取的值？

#### --distractors--

去除器

---

橡皮擦

---

延迟器

#### --answer--

删除者

### --question--

#### --text--

什么促进代码重用，提供清晰的层次结构，并允许在不重写所有内容的情况下自定义行为？

#### --distractors--

DRY

---

湿润

---

抽象

#### --answer--

继承

### --question--

#### --text--

以下哪一个是正确的 `inheritance` 语法？

#### --distractors--

```py
class Parent:
    pass
class Child(InheritParent):
    pass
```

---

```py
class Parent:
    pass
class Child(Inheriter):
    pass
```

---

```py
class Child:
   pass
class Parent(Parent):
   pass
```

#### --answer--

```py
class Parent:
   pass
class Child(Parent):
   pass
```

### --question--

#### --text--

什么连接了 setter 和 getter？

#### --distractors--

方法

---

函数

---

课程

#### --answer--

属性

### --question--

#### --text--

Python 通过什么进程将以双下划线开头的属性内部重命名为添加一个下划线和类名作为前缀？

#### --distractors--

名称混合

---

多态

---

抽象

#### --answer--

名称混淆

### --question--

#### --text--

哪个函数允许你在子类中调用父类的方法？

#### --distractors--

`constructor()`

---

`__init__()`

---

`property()`

#### --answer--

`super()`

### --question--

#### --text--

以下哪个语句最能描述面向对象编程中的多态？

#### --distractors--

它允许一个类拥有多个具有不同属性和方法的构造函数。

---

它允许一个类继承另一个类的属性和方法。

---

它允许使用 `class` 关键字创建对象。

#### --answer--

它允许不同的类使用相同的方法名，而在调用时执行不同的操作。

### --question--

#### --text--

为什么 Python 会对以双下划线开头的属性名称进行名称改写？

#### --distractors--

使该属性完全私有且无法从类外部访问。

---

通过更高效地存储属性来提升性能。

---

自动将属性转换为只读属性。

#### --answer--

为了防止在使用继承时子类中意外覆盖属性和方法。

### --question--

#### --text--

为什么使用属性而不是方法来实现 getter 和 setter？

#### --distractors--

使代码更长且更显式。

---

完全阻止访问该值。

---
自动记录每个值的变化。

#### --answer--

允许使用点符号进行类似属性的直接访问，以提高可读性。
