---
id: 672d45651d83b450801efb3a
title: Come creare un progetto ed eseguire il codice localmente in VS Code
challengeType: 19
dashedName: how-to-create-a-project-and-run-your-code-locally-in-vs-code
---

# --description--

Per iniziare, devi sapere che VS Code considera i progetti come "workspaces". Lo workspace è la directory che hai aperto in VS Code.

Per cominciare, dovrai usare un'interfaccia a riga di comando per creare una nuova directory per il tuo nuovo progetto. Una directory è un altro nome per cartella.

Le interfacce a riga di comando, o CLI in breve, ti permettono di interagire con il sistema operativo tramite comandi testuali. Imparerai di più su questi strumenti nelle prossime lezioni.

Se usi Windows, puoi usare il Prompt dei comandi o PowerShell. Se usi un Mac, puoi aprire l'app Terminal.

Una volta aperta la riga di comando, vai alla directory home digitando il comando `cd ~` e premendo `enter` sulla tastiera. Poi digita il comando `mkdir my-project` e premi `enter`. `mkdir`, o "make directory", è il comando usato per creare una nuova directory. Dovresti poi aprire la nuova directory con VS Code.

Ci sono anche altri modi grafici per creare una nuova directory.

A seconda del sistema operativo, potresti avere un menu contestuale (o opzione clic destro) per aprire VS Code direttamente dall'esplora file. Potresti anche scegliere di fare doppio clic sull'app VS Code. Se ti senti a tuo agio con la riga di comando, puoi eseguire il comando `code /path/to/folder`.

Una volta aperto, dovresti vedere un nuovo workspace vuoto.

Per creare un nuovo file, clicca sul menu `File` in alto a sinistra e seleziona `New File...`. Poi dai un nome al file `index.html` e clicca su `Save`.

Ora dovresti vedere il nuovo file nella scheda esplora. Prova ad aggiungere un po' di HTML al file `index.html`.

Ora potresti essere tentato di aprire il file HTML direttamente nel browser. Anche se può funzionare, può causare vari bug imprevisti, come il mancato caricamento corretto del CSS.

Invece, dovresti mettere il file HTML dietro un vero server web. Questo può sembrare un compito intimidatorio se sei nuovo alla programmazione, ma in realtà c’è un modo per farlo direttamente da VS Code.

Prima devi installare l’estensione _Live Server_, disponibile gratuitamente nel marketplace.

Clicca sulla scheda delle estensioni nell’angolo in basso a sinistra e digita `Live Server` nella barra di ricerca.

Una volta installata, dovresti vedere un pulsante `Go live` nella barra di stato. Assicurati di avere aperto il file HTML, poi cliccalo.

La tua nuova pagina dovrebbe aprirsi automaticamente in una nuova scheda del browser. Se non succede, puoi andare direttamente a `http://localhost:5500/`

Congratulazioni! Hai appena configurato il tuo progetto in VS Code e puoi vedere le modifiche con live server.

# --questions--

## --text--

Cosa considera Visual Studio Code come "workspace"?

## --answers--

L’intero disco rigido.

### --feedback--

La lezione spiega come VS Code definisce uno workspace in relazione alle directory.

---

La directory da cui hai aperto VS Code.

---

Tutti i file aperti in VS Code.

### --feedback--

La lezione spiega come VS Code definisce uno workspace in relazione alle directory.

---

La cartella dell’app VS Code.

### --feedback--

La lezione spiega come VS Code definisce uno workspace in relazione alle directory.

## --video-solution--

2

## --text--

Perché la lezione consiglia di usare un server web invece di aprire direttamente un file HTML nel browser?

## --answers--

Si carica più velocemente e rende più facile individuare problemi nel codice.

### --feedback--

La lezione menziona un problema specifico che può verificarsi aprendo direttamente file HTML.

---

Fornisce maggiore sicurezza per grandi applicazioni software.

### --feedback--

La lezione menziona un problema specifico che può verificarsi aprendo direttamente file HTML.

---

Evita bug imprevisti, come il mancato caricamento corretto del CSS.

---

Permette la collaborazione in tempo reale con altri sviluppatori del team.

### --feedback--

La lezione menziona un problema specifico che può verificarsi aprendo direttamente file HTML.

## --video-solution--

3

## --text--

Quale estensione di VS Code è consigliata per configurare facilmente un server web locale?

## --answers--

Web Server.

### --feedback--

La lezione menziona specificamente l’installazione di un’estensione dal marketplace per servire il file HTML.

---

Live Server.

---

Local Host.

### --feedback--

La lezione menziona specificamente l’installazione di un’estensione dal marketplace per servire il file HTML.

---

HTML Preview.

### --feedback--

La lezione menziona specificamente l’installazione di un’estensione dal marketplace per servire il file HTML.

## --video-solution--

2
