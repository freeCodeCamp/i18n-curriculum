---
id: 66ed8fe7f45ce3ece4053eb2
title: CSS 플렉스박스 퀴즈
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

플렉스박스란 무엇인가요?

#### --distractors--

레이아웃을 위한 2차원 모델입니다.

---

레이아웃을 위한 3차원 모델입니다.

---

다차원 모델입니다.

#### --answer--

1차원 레이아웃 모델입니다.

### --question--

#### --text--

`div` 요소에 플렉스박스 레이아웃을 활성화하려면 어떤 CSS 속성을 설정해야 하나요?

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

현재 행이 꽉 찼을 때 플렉스 항목이 새 행으로 감싸지도록 하는 CSS 속성은 무엇인가요?

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

플렉스박스 속성 `justify-content`는 무엇을 제어하나요?

#### --distractors--

플렉스 컨테이너의 자식 요소 방향을 제어합니다.

---

플렉스 컨테이너 자식 요소의 텍스트 스타일을 제어합니다.

---

플렉스 컨테이너 내 자식 요소의 정렬을 제어합니다.

#### --answer--

주축에서 자식 요소의 분포를 제어합니다.

### --question--

#### --text--

플렉스 컨테이너 내 요소들의 방향을 제어하는 속성은 무엇인가요?

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

플렉스 항목이 왼쪽에서 오른쪽으로 흐르며, 현재 행 너비를 초과하면 새 플렉스 항목이 이전 항목 위에 나타나도록 하는 CSS 속성은 무엇인가요?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

교차 축에서 요소들의 위치를 제어하는 속성은 무엇인가요?

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

`justify-content`의 값 중 올바르지 않은 것은 무엇인가요?

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

플렉스 컨테이너 내 요소를 수평과 수직으로 모두 가운데 정렬하려면 어떤 CSS 속성을 사용해야 하나요?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

`flex-direction`의 어떤 값이 플렉스 항목을 오른쪽에서 왼쪽으로 흐르게 하나요?

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

`flex-wrap` 속성의 값 중 올바르지 않은 것은 무엇인가요?

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

`stretch`에서 `align-items` 값은 어떤 동작을 하나요?

#### --distractors--

항목들이 컨테이너 시작 가장자리 쪽으로 서로 밀착됩니다.

---

항목들이 플렉스 컨테이너의 기준선에 맞춰 정렬됩니다.

---

항목들이 컨테이너 끝 가장자리 쪽으로 서로 밀착됩니다.

#### --answer--

컨테이너를 채우도록 항목 크기를 자동으로 동일하게 조정합니다.

### --question--

#### --text--

주축을 따라 첫 번째 항목은 시작에, 마지막 항목은 끝에 배치하고 나머지 항목은 균등 간격으로 배치하는 `justify-content` 값은 무엇인가요?

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

인접한 모든 항목 쌍과 컨테이너 가장자리 사이의 간격이 모두 동일하도록 항목을 배치하는 `justify-content` 값은 무엇인가요?

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

컨테이너 가장자리에서 절반 크기의 공간을 남기면서 주축을 따라 항목을 균등 간격으로 배치하는 `justify-content` 값은 무엇인가요?

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

다음 중 교차 축 시작에 항목들이 정렬되는 결과를 만드는 것은 무엇인가요?

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

다음 중 교차 축 끝에 항목들이 정렬되는 결과를 만드는 것은 무엇인가요?

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

`flex-direction`의 어떤 값이 항목들을 주축을 따라 왼쪽에서 오른쪽으로 정렬하게 하나요?

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

`flex-direction`의 어떤 값이 항목들을 교차 축을 따라 위에서 아래로 정렬하게 하나요?

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

다음 중 플렉스박스에 적합하지 않은 사용 사례는 무엇인가요?

#### --distractors--

컨테이너 내 항목을 가운데 정렬하기.

---

행 내 항목의 수직 정렬.

---

다양한 뷰포트에 맞춰 항목 분포와 정렬을 동적으로 조정하기.

#### --answer--

그리드처럼 고정 크기와 항목 간 간격을 유지하기.

## --quiz--

### --question--

#### --text--

Flexbox 같은 1차원 레이아웃 모델의 목적은 무엇인가요?

#### --distractors--

행과 열을 동시에 다루는 2차원 레이아웃 처리.

---

`z-index`를 사용해 위치 지정 요소의 쌓임 순서 제어.

---

페이지 요소에 3D 변환과 복잡한 애니메이션 생성.

#### --answer--

항목을 한 줄 또는 한 축을 따라 행 또는 열로 배치하기.

### --question--

#### --text--

`.container` 요소 내 모든 플렉스 항목을 오른쪽으로 정렬하는 CSS 속성은 무엇인가요?

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Flex 모델에서 항목 배치 방향을 정의하는 두 축은 무엇인가요?

#### --distractors--

수평 축과 수직 축.

---

x축과 y축.

---

주축과 보조 축.

#### --answer--

주축과 교차 축.

### --question--

#### --text--

다음 중 항목을 수직 열로 배치하고 컨테이너 내에서 수평으로 가운데 정렬하는 CSS 규칙은 무엇인가요?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

`flex-wrap` 속성의 주요 목적은 무엇인가요?

#### --distractors--

플렉스 항목이 플렉스 컨테이너 내에 배치되는 방향을 설정합니다.

---

플렉스 항목을 컨테이너 주축을 따라 정렬합니다.

---

플렉스 항목을 컨테이너 교차 축을 따라 정렬합니다.

#### --answer--

플렉스 항목이 사용 가능한 공간에 맞게 새 줄로 감쌀지 결정합니다.

### --question--

#### --text--

`flex-wrap: nowrap;` 선언의 기본 동작은 무엇인가요?

#### --distractors--

컨테이너에 너무 넓으면 플렉스 항목을 새 줄로 감쌉니다.

---

항목을 새 줄로 감싸지만 역순으로 배치합니다.

---

플렉스 컨테이너가 부모 요소보다 커지지 않도록 합니다.

#### --answer--

플렉스 항목이 오버플로우하더라도 한 줄에 유지되도록 강제합니다.

### --question--

#### --text--

`flex-direction`와 `flex-wrap` 속성을 한 번에 설정하는 축약형 CSS 속성은 무엇인가요?

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

플렉스 컨테이너에 `flex-direction: column-reverse;`을 설정하면 어떤 효과가 있나요?

#### --distractors--

항목을 오른쪽에서 왼쪽으로 수평 배치합니다.

---

한 줄에서 항목 순서만 역전시킵니다.

---

항목을 위에서 아래로 수직 배치합니다.

#### --answer--

항목을 아래에서 위로 역순 수직 배치합니다.

### --question--

#### --text--

플렉스 항목을 수직 열로 배치하고 컨테이너 높이를 초과하면 새 열로 감싸도록 하는 올바른 CSS 코드는 무엇인가요?

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

`justify-content` 값 중 컨테이너 주축 끝에 모든 플렉스 항목을 정렬하는 값은 무엇인가요?

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

다음 CSS 규칙이 플렉스 항목에 미치는 영향은 무엇인가요?

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

항목이 열로 배치되고 새 열로 감쌉니다.

---

항목이 행으로 배치되고 새 행으로 감쌉니다.

---

항목이 열로 배치되고 공간이 부족하면 오버플로우합니다.

#### --answer--

항목이 행으로 배치되고 공간이 부족하면 컨테이너를 오버플로우합니다.

### --question--

#### --text--

`justify-content: space-evenly;`은 플렉스 항목을 어떻게 정렬하나요?

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

시작과 끝에 공간 없이 균등 간격으로 배치합니다.

---

컨테이너 중앙에 밀집시킵니다.

---

컨테이너 오른쪽에 밀착시키고 항목 사이에 균등 간격을 둡니다.

#### --answer--

항목 사이와 컨테이너 양 끝에 균등한 공간을 둡니다.

### --question--

#### --text--

`flex-direction: row;`가 설정되었을 때 항목을 수직 가운데 정렬하려면 어떤 속성을 사용해야 하나요?

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

`flex-start` 속성의 `justify-content` 값은 어떤 동작을 하나요?

#### --distractors--

항목을 교차 축 시작에 정렬합니다.

---

항목을 컨테이너 주축에 맞게 늘립니다.

---

모든 항목을 주축 중앙에 정렬합니다.

#### --answer--

항목을 컨테이너 주축 시작에 정렬합니다.

### --question--

#### --text--

플렉스 컨테이너를 한 줄로 오른쪽에서 왼쪽으로 항목을 배치하고, 컨테이너 상단에 위치시키려면 어떻게 설정해야 하나요?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

`flex-flow` 속성에 대한 올바른 선언은 무엇인가요?

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

다음 CSS 규칙 중 플렉스 항목을 수평으로 가운데 정렬하고 수직으로 컨테이너 높이에 맞게 늘리는 것은 무엇인가요?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

`align-items: stretch;`가 `flex-direction`로 설정되었을 때 `column`이 미치는 영향은 무엇인가요?

#### --distractors--

항목을 수직으로 늘려 컨테이너 높이에 맞춥니다.

---

방향이 `column`일 때는 영향이 없습니다.

---

항목을 수직으로 늘려 가장 큰 항목과 맞춥니다.

#### --answer--

항목을 수평으로 늘려 컨테이너 너비에 맞춥니다.

### --question--

#### --text--

수평 행에서 항목 순서를 역전시키려면 어떤 선언을 사용해야 하나요?

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

`align-items`이 `flex-direction`인 컨테이너에서 항목을 상단에 배치하는 `row` 값은 무엇인가요?

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
