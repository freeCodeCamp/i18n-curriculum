---
id: 684d09cad4ec900e41db2e74
title: 第 11 步
challengeType: 0
dashedName: step-11
---

# --description--

目前 `div` 元素被放置在預覽螢幕的左上角。但為元素的頂部添加一些間距會更好。若要使用 Tailwind CSS 為元素套用 `margin`，你可以使用以下樣式：`m-number`，其中 `number` 表述，表現要套用到四個邊的固定像素。

如果你只想為一側套用邊距，可以使用以下任一樣式：`mt-number`（上邊距）、`mb-number`（下邊距）、`mr-number`（右邊距）和 `ml-number`（左邊距）。

以下是將 `margin-bottom` 套用到元素的範例：

```html
<p class="mb-8">This is margin.</p>
```

在你的開啟 `div` 頁籤中，使用正確的工具類別來套用 `margin-top` 為 `4px`。

# --hints--

你的開啟 `div` 頁籤應該有 `mt-4` 實用類別。

```js
const divEl = document.querySelector("div");
assert.isTrue(divEl.classList.contains("mt-4"));
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error Message Component</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    --fcc-editable-region--
    <div class="bg-red-100 border-2 border-red-300 rounded-md md:w-1/2 p-4">
    --fcc-editable-region--
      <p>
        <strong>Error!</strong> Something went wrong. Please try again.
      </p>
      <button aria-label="Dismiss">
        <span aria-hidden>&times;</span>
      </button>
    </div>
  </body>
</html>
```
