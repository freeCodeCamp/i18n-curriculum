---
id: 671a940c69cdee833d4cc312
title: CSS Flexbox 回顧
challengeType: 31
dashedName: review-css-flexbox
---

# --interactive--

## CSS Flexbox 與 Flex 模型簡介

- **定義**：CSS flexbox 是一種一維佈局模型，允許你在容器內以列和行排列元素。
- **Flex Model**：此模型定義了彈性項目在彈性容器內的排列方式。每個彈性容器都有兩個軸：主軸和交叉軸。

## `flex-direction` 屬性

- **定義**：此屬性設定主軸的方向。`flex-direction` 的預設值為 `row`，會將所有的彈性項目放在同一列，方向依照你的瀏覽器預設語言（由左至右或由右至左）。
- **`flex-direction: row-reverse;`**：這會反轉列中的項目。
- **`flex-direction: column;`**：這將使彈性項目垂直排列，而非水平排列。
- **`flex-direction: column-reverse;`**：這會垂直反轉彈性項目的順序。

:::interactive_editor

```html
<div class="container">
  <div class="box">1</div>
  <div class="box">2</div>
  <div class="box">3</div>
</div>
```

```css
.container {
  display: flex;
  flex-direction: row;
  gap: 10px;
}

.box {
  background-color: lightblue;
  padding: 20px;
  text-align: center;
}
```

:::

## `flex-wrap` 屬性

- **定義**：此屬性決定彈性項目如何在彈性容器內換行以適應可用空間。`flex-wrap` 可以採用三種可能的值：`nowrap`、`wrap` 和 `wrap-reverse`。
- **`flex-wrap: nowrap;`**：這是預設值。彈性項目不會換行，即使它們的寬度超過容器的寬度。
- **`flex-wrap: wrap;`**：當項目超過其容器的寬度時，這個屬性會將項目換行。
- **`flex-wrap: wrap-reverse;`**：此屬性會以反向順序換行彈性項目。
- **`flex-flow` 屬性**：此屬性是 `flex-direction` 和 `flex-wrap` 的縮寫屬性。

:::interactive_editor

```html
<div class="container">
  <div class="box">1</div>
  <div class="box">2</div>
  <div class="box">3</div>
  <div class="box">4</div>
  <div class="box">5</div>
</div>
```

```css
.container {
  display: flex;
  flex-wrap: wrap;
  width: 150px;
  background: #f0f0f0;
}

.box {
  width: 60px;
  padding: 10px;
  margin: 5px;
  background: skyblue;
  text-align: center;
}
```

:::

## `justify-content` 屬性

- **定義**：此屬性沿著彈性容器的主軸對齊子元素。
- **`justify-content: flex-start;`**：在這種情況下，彈性項目會對齊到主軸的起點。這可以是水平或垂直方向。
- **`justify-content: flex-end;`**：在這種情況下，彈性項目會沿主軸的末端對齊，無論是水平還是垂直方向。
- **`justify-content: center;`**：這會將彈性項目沿主軸置中。
- **`justify-content: space-between;`**：這會沿主軸均勻分配元素。
- **`justify-content: space-around;`**：這會在主軸內均勻分配彈性項目，並在第一個項目前及最後一個項目後添加空間。
- **`justify-content: space-evenly;`**：這會沿主軸平均分配項目。

:::interactive_editor

```html
<div class="container">
  <div class="box">A</div>
  <div class="box">B</div>
  <div class="box">C</div>
</div>
```

```css
.container {
  display: flex;
  justify-content: space-between;
  background: #eee;
}

.box {
  padding: 20px;
  background: salmon;
}
```

:::
  
## `align-items` 屬性

- **定義**：此屬性用於沿交叉軸分配項目。請記住交叉軸垂直於主軸。
- **`align-items: center;`**：這用於沿交叉軸置中項目。
- **`align-items: flex-start;`**：這會將項目對齊到交叉軸的起點。
- **`align-items: stretch;`**：這用於沿交叉軸拉伸彈性項目。

:::interactive_editor

```html
<div class="container">
  <div class="box tall">1</div>
  <div class="box">2</div>
  <div class="box">3</div>
</div>
```

```css
.container {
  display: flex;
  align-items: center;
  height: 150px;
  background: #ddd;
}

.box {
  background: lightgreen;
  padding: 10px;
}

.tall {
  height: 100px;
}
```

:::

# --assignment--

檢視 CSS Flexbox 主題和概念。
