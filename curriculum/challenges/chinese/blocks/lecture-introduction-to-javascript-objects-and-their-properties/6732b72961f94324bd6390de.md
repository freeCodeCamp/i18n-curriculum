---
id: 6732b72961f94324bd6390de
title: 如何查看对象是否具有某个属性？
challengeType: 19
dashedName: how-to-check-if-an-object-has-a-property
---

# --interactive--

在 JavaScript 中，有多种方法可以查看对象是否具有特定的属性。理解这些方法对于有效地使用对象非常重要，尤其是在处理来自外部来源的数据或需要确保某些属性存在后再使用它们时。 

我们将探讨一些常见的方法：`hasOwnProperty()` 方法、`Object.hasOwn()` 方法、`in` 操作符，以及与 `undefined` 的比较。

让我们从 `hasOwnProperty()` 方法开始。此方法返回一个布尔值，指示对象是否拥有指定的属性作为其自身属性。下面是一个示例：

:::interactive_editor

```js
const person = {
  name: "Alice",
  age: 30
};

console.log(person.hasOwnProperty("name")); // true
console.log(person.hasOwnProperty("job")); // false
```

:::

在这个例子中，我们有一个名为 `person` 的对象，它有两个属性：`name` 和 `age`。要查看 `name` 是否是 `person` 对象中的属性，我们使用 `hasOwnProperty()` 方法。由于 `name` 是一个属性，它将返回 `true`。但是当我们使用 `hasOwnProperty()` 来查看 `job` 是否是一个属性时，它将返回 `false`，因为该属性不存在于对象中。

`Object.hasOwn()` 是检查对象是否拥有自身（非继承）属性的现代推荐方法。可以将其视为 `hasOwnProperty()` 的升级版，更加安全。语法是 `Object.hasOwn(object, propertyName)` ——你将对象作为第一个参数，属性名作为第二个参数传入。

这是一个基本示例：

:::interactive_editor

```js
const person = {
  name: "Alice",
  age: 30
};

console.log(Object.hasOwn(person, "name")); // true
console.log(Object.hasOwn(person, "job")); // false
```

:::

在此示例中，`Object.hasOwn(person, "name")` 返回 `true`，因为 `name` 直接存在于 `person` 对象上。`Object.hasOwn(person, "job")` 返回 `false`，因为 `job` 从未被添加到该对象。

一个非常重要的事情是要理解 `Object.hasOwn()` 只检查属性是否**存在**——它不关心属性的值。这意味着即使值是 `0`、`false`、`null` 或 `undefined`，它仍然返回 `true`：

:::interactive_editor

```js
const user = {
  username: "coder123",
  score: 0,
  isActive: false,
  nickname: null
};

// Object.hasOwn() correctly reports these all exist
console.log(Object.hasOwn(user, "score"));    // true  (value is 0, but property exists)
console.log(Object.hasOwn(user, "isActive")); // true  (value is false, but property exists)
console.log(Object.hasOwn(user, "nickname")); // true  (value is null, but property exists)
console.log(Object.hasOwn(user, "email"));   // false (property was never added)

// Danger! Using if() directly gives wrong results for falsy values
if (user.score) {
  console.log("Has score"); // This will NOT print even though score exists!
}

// Safe! Object.hasOwn() gives correct result
if (Object.hasOwn(user, "score")) {
  console.log("Has score:", user.score); // Has score: 0
}
```

:::

另一种查看对象中属性是否存在的方法是使用 `in` 操作符。与 `hasOwnProperty()` 类似，如果属性存在于对象上，`in` 操作符将返回 `true`。以下是你如何使用它：

:::interactive_editor

```js
const person = {
  name: "Bob",
  age: 25
};
console.log("name" in person);  // true
```

:::

在此示例中，`"name" in person` 返回 `true`，因为 `name` 是 `person` 的一个属性。

第三种方法涉及查看属性是否为 `undefined`。这种方法可能有用，但它有一些限制。以下是一个示例：

:::interactive_editor

```js
const car = {
  brand: "Toyota",
  model: "Corolla",
  year: 2020
};

console.log(car.brand !== undefined); // true
console.log(car.color !== undefined); // false
```

:::

在这段代码中，我们查看 `car.brand` 和 `car.color` 是否不是 `undefined`。这是因为访问对象上不存在的属性会返回 `undefined`。但是，如果属性显式地具有值 `undefined`，此方法可能会产生假阴性。

在实际操作中，这些方法之间的选择通常取决于你的代码的具体需求。理解它们之间的差异将帮助你在不同场景中做出正确的选择。

# --questions--

## --text--

下面代码的输出是什么？

```js
let obj = {x: 0, y: null};
console.log(obj.hasOwnProperty("y"));
```

## --answers--

`true`

---

`false`

### --feedback--

`hasOwnProperty()` 检查该属性是否直接存在于对象上，无论其值如何。

---

`null`

### --feedback--

`hasOwnProperty()` 检查该属性是否直接存在于对象上，无论其值如何。

---

`undefined`

### --feedback--

`hasOwnProperty()` 检查该属性是否直接存在于对象上，无论其值如何。

## --video-solution--

1

## --text--

下面代码的输出是什么？

```js
let person = {
    name: "Alice",
    age: 30
};

console.log("name" in person);
```

## --answers--

`true`

---

`false`

### --feedback--

`in` 操作符查看属性是否存在于对象中。

---

`"Alice"`

### --feedback--

`in` 操作符查看属性是否存在于对象中。

---

`undefined`

### --feedback--

`in` 操作符查看属性是否存在于对象中。

## --video-solution--

1

## --text--

下面代码的输出是什么？

```js
let obj = {a: undefined};
console.log(obj.a !== undefined);
```

## --answers--

`true`

### --feedback--

考虑当一个属性存在但其值为 `undefined` 时会发生什么。

---

`false`

---

`undefined`

### --feedback--

考虑当一个属性存在但其值为 `undefined` 时会发生什么。

---

会报错。

### --feedback--

考虑当一个属性存在但其值为 `undefined` 时会发生什么。

## --video-solution--

2
