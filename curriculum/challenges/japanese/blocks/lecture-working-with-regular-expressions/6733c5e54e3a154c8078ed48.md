---
id: 6733c5e54e3a154c8078ed48
title: "正規表現の量指定子とは何か、そしてどのように機能するのか？"
challengeType: 19
dashedName: what-are-regex-quantifiers-and-how-do-they-work
---

# --interactive--

正規表現の量指定子について学びましょう。

4桁の識別コードにマッチさせたいとします。`\d` という文字クラスを使えることはわかっているので、それを4回書くかもしれません。そして余計な文字を含めないように、開始アンカーと終了アンカーの両方を含めます。

```js
const regex = /^\d\d\d\d$/;
```

これは確かに4つの数字にマッチします。しかし、同じクラスを何度も書く代わりに、量指定子を使うことができます。

量指定子は波括弧（中括弧）で囲まれた1つか2つの数字で定義されます。パターンに量指定子を使ってみましょう。

```js
const regex = /^\d{4}$/;
```

量指定子に`4` の数字だけが含まれていることに注目してください。この構文は「直前の文字に正確に4回マッチする」という意味です。どのように動作するか見てみましょう。

:::interactive_editor

```js
const regex = /^\d{4}$/;
console.log(regex.test("123")); // false
console.log(regex.test("1234")); // true
console.log(regex.test("12345")); // false
console.log(regex.test("123456")); // false
console.log(regex.test("1234567")); // false
```

:::

このパターンは、アンカーを使い量指定子が正確に4回の数字だけを許可しているため、ちょうど4桁の文字列にしかマッチしません。しかし、識別コードは4桁以上であればよい場合もあります。

4桁以上を許可するには、量指定子の数字の後にカンマを追加してください。

```js
const regex = /^\d{4,}$/;
```

これで、パターンは4桁以上の数字にマッチできるようになりました。試してみましょう。

:::interactive_editor

```js
const regex = /^\d{4,}$/;
console.log(regex.test("123")); // false
console.log(regex.test("1234")); // true
console.log(regex.test("12345")); // true
console.log(regex.test("123456")); // true
console.log(regex.test("1234567")); // true
```

:::

7桁の識別子はかなり長いです。これらの識別子は最大6桁、最小4桁であるべきです。これを実現するには、量指定子のカンマの後に2つ目の数字を追加します。

```js
const regex = /^\d{4,6}$/;
```

これで、7桁の識別子は最大6桁を超えているためマッチしなくなりました。

:::interactive_editor

```js
const regex = /^\d{4,6}$/;
console.log(regex.test("123")); // false
console.log(regex.test("1234")); // true
console.log(regex.test("12345")); // true
console.log(regex.test("123456")); // true
console.log(regex.test("1234567")); // false
```

:::

この構文では最大値だけを設定することはできません。必ず最小値を設定する必要があります。ただし、最小値を`1` に設定すれば、実質的に同じ結果を得られます。

ユーザーから更新された要件が届きました。識別子は任意で先頭に文字が付くことができます。すでにその文字クラスはわかっているので、正規表現に追加しましょう。

```js
const regex = /^[a-zA-Z]\d{4,6}$/;
```

しかし、これでは文字の存在が必須になってしまいます。どうすれば任意にできますか？

量指定子の構文で最小値を`0`、最大値を`1` にすることもできます。

```js
const regex = /^[a-zA-Z]{0,1}\d{4,6}$/;
```

しかし、実は1文字の任意の文字に対する特別な短縮量指定子があります。それが疑問符（`?`）です。量指定子を疑問符に置き換えましょう。

```js
const regex = /^[a-zA-Z]?\d{4,6}$/;
```

結果を検証しましょう。

:::interactive_editor

```js
const regex = /^[a-zA-Z]?\d{4,6}$/;
console.log(regex.test("123")); // false
console.log(regex.test("a1234")); // true
console.log(regex.test("12345")); // true
console.log(regex.test("az12345")); // false
console.log(regex.test("X123456")); // true
console.log(regex.test("1234567")); // false
```

:::

このパターンは、1文字の任意の文字が任意で続き、その後に4～6桁の数字が続くことを許可します。

残念ながら、要件を誤読していたことに気づきました。数字の前に任意の数の文字を許可する必要があります。量指定子で最小値を`0` にし、最大値を設定しないこともできます。

```js
const regex = /^[a-zA-Z]{0,}\d{4,6}$/;
```

しかし、パターンがまた長くなってきました。幸い、「直前の文字に0回以上マッチする」という別の短縮記法があります。それがアスタリスク（`*`）です。量指定子をこれに置き換えてテストしましょう。

:::interactive_editor

```js
const regex = /^[a-zA-Z]*\d{4,6}$/;
console.log(regex.test("123")); // false
console.log(regex.test("a1234")); // true
console.log(regex.test("12345")); // true
console.log(regex.test("az12345")); // true
console.log(regex.test("X123456")); // true
console.log(regex.test("1234567")); // false
```

:::

これで、0文字以上の文字に続き4～6桁の数字がある任意の識別子にマッチできました。しかし、これがシステムをクラッシュさせてしまいました。実際には少なくとも1文字は必要です。

再び、最小値1で最大値なしの量指定子を使うか、別の特別な構文であるプラス（`+`）を使うことができます。

:::interactive_editor

```js
const regex = /^[a-zA-Z]+\d{4,6}$/;
console.log(regex.test("123")); // false
console.log(regex.test("a1234")); // true
console.log(regex.test("12345")); // false
console.log(regex.test("az12345")); // true
console.log(regex.test("X123456")); // true
console.log(regex.test("1234567")); // false
```

:::

これで、少なくとも1文字で始まらない識別子は、数字の数に関わらずマッチしなくなりました。

量指定子を使うことで、正規表現の簡潔さと読みやすさを大幅に向上させることができます。

# --questions--

## --text--

正規表現における量指定子 `{4,6}` は何を意味しますか？

## --answers--

正確に4回または6回マッチする。

### --feedback--

量指定子で指定された最小値と最大値について考えてみてください。

---

少なくとも4回、しかし6回を超えない回数マッチする。

---

合計で4回から6回の文字にマッチする。

### --feedback--

量指定子で指定された最小値と最大値について考えてみてください。

---

4回または6回の数字のどちらかにマッチする。

### --feedback--

量指定子で指定された最小値と最大値について考えてみてください。

## --video-solution--

2

## --text--

正規表現で `{0,1}` と同等の量指定子記号はどれですか？

## --answers--

`*`

### --feedback--

レッスンでは「1文字の任意の文字に対する特別な短縮量指定子」と説明されています。

---

`+`

### --feedback--

レッスンでは「1文字の任意の文字に対する特別な短縮量指定子」と説明されています。

---

`?`

---

`^`

### --feedback--

レッスンでは「1文字の任意の文字に対する特別な短縮量指定子」と説明されています。

## --video-solution--

3

## --text--

正規表現における量指定子 `+` は何を意味しますか？

## --answers--

0回以上マッチする。

### --feedback--

レッスンではこの量指定子は「少なくとも1つ必要」と説明されています。

---

正確に1回マッチする。

### --feedback--

レッスンではこの量指定子は「少なくとも1つ必要」と説明されています。

---

1回以上マッチする。

---

0回または1回マッチする。

### --feedback--

レッスンではこの量指定子は「少なくとも1つ必要」と説明されています。

## --video-solution--

3
