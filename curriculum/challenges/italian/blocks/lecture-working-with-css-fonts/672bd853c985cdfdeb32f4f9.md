---
id: 672bd853c985cdfdeb32f4f9
title: Cos’è la proprietà text-shadow e come funziona?
challengeType: 19
dashedName: what-is-the-text-shadow-property
---

# --interactive--

Per impostazione predefinita, il CSS non applica ombre al testo. Questo è un esempio di un paragrafo senza ombre. Ma se hai bisogno di aggiungere ombre, puoi usare la proprietà `text-shadow`.

```css
text-shadow: /* Values */
```

In CSS, puoi descrivere un’ombra attraverso una combinazione del suo offset `X`, offset `Y`, raggio di sfocatura e colore. Prima devi specificare l’offset `X` e `Y`, che rappresentano rispettivamente la distanza orizzontale e verticale dell’ombra rispetto al testo. Questi valori sono obbligatori.

Ecco un esempio di come applicare un offset `X` e `Y` su un’ombra. Applichiamo la proprietà `text-shadow` con un offset x di `3px` e un offset y di `2px`:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px;
}
```

:::

Nel browser, il testo e l’ombra appariranno simili ma possiamo anche personalizzare il colore dell’ombra specificando il valore prima o dopo l’offset. Impostiamo il colore dell’ombra. Qui useremo un colore esadecimale ma puoi usare qualsiasi formato colore valido.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px #00ffc3;
}
```

:::

Stiamo scrivendo il colore dopo l’offset ma puoi anche scriverlo prima se preferisci. È equivalente:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: #00ffc3 3px 2px;
}
```

:::

Nel browser, l’ombra avrà un colore specifico, così possiamo distinguerla facilmente dal testo. Ora che possiamo differenziare l’ombra dal testo, è importante vedere anche come i valori positivi e negativi influenzano l’offset dell’ombra. I valori positivi dell’offset `X` e `Y` sposteranno l’ombra rispettivamente a destra e in basso, mentre i valori negativi la sposteranno a sinistra e in alto. Ecco un esempio che usa valori negativi:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: -3px -2px #00ffc3;
}
```

:::

Ora l’ombra si sposterà a sinistra e in alto rispetto al testo. Ottimo. Ma l’ombra non sembra molto bella, perché appare esattamente come il testo originale ma posizionata dietro di esso. Per farla sembrare migliore, dobbiamo aggiungere un terzo valore, il raggio di sfocatura. Questo valore è opzionale ma rende l’ombra molto più morbida e sottile. Il valore predefinito del raggio di sfocatura è zero. Più alto è il valore, maggiore è la sfocatura, il che significa che l’ombra diventa più chiara.

Qui stiamo impostando il raggio di sfocatura a `3px` e torniamo a valori positivi per l’offset:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px 3px #00ffc3;
}
```

:::

Ora inizia a sembrare un’ombra. È più sfocata e sottile, così possiamo concentrarci sul testo principale mentre l’ombra aggiunge un po’ di stile sullo sfondo. Da qui, possiamo modificare un po’ i valori finché non troviamo una combinazione di offset, colore e raggio di sfocatura che soddisfa le nostre esigenze.

È anche utile sapere che il testo può avere più di un’ombra. Devi solo scriverle nella stessa proprietà `text-shadow` separate da virgole. Verranno applicate a strati, dal primo in primo piano all’ultimo sullo sfondo, con la prima ombra in cima.

Ecco un esempio di un paragrafo con tre ombre applicate a strati. Nota che tutte queste ombre sono definite nella stessa proprietà `text-shadow` e separate da virgole:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 
    3px 2px 3px #00ffc3, 
    -3px -2px 3px #0077ff, 
    5px 4px 3px #dee7e5;
}
```

:::

`text-shadow` è una potente proprietà CSS per far risaltare il tuo testo. Può anche migliorare il design complessivo della tua applicazione web. Scegliendo offset, sfocatura e colore, puoi creare una vasta gamma di effetti ombra.

# --questions--

## --text--

Qual è lo scopo principale della proprietà CSS `text-shadow`?

## --answers--

Regolare la dimensione del carattere del testo.

### --feedback--

Pensa a come la proprietà `text-shadow` influenza l’aspetto visivo del testo.

---

Cambiare il colore del testo.

### --feedback--

Pensa a come la proprietà `text-shadow` influenza l’aspetto visivo del testo.

---

Creare un effetto ombra per il testo.

---

Controllare la spaziatura tra le righe di testo.

### --feedback--

Pensa a come la proprietà `text-shadow` influenza l’aspetto visivo del testo.

## --video-solution--

3

## --text--

Come puoi creare un’ombra nera per il testo con un offset orizzontale verso destra e un raggio di sfocatura di 5 pixel?

## --answers--

`text-shadow: 5px 0px 0px black;`

### --feedback--

Pensa all’ordine dei valori nella proprietà text-shadow.

---

`text-shadow: 5px 0px 5px black;`

---

`text-shadow: 5px 5px 0px black;`

### --feedback--

Pensa all’ordine dei valori nella proprietà text-shadow.

---

`text-shadow: 0px 5px 0px black;`

### --feedback--

Pensa all’ordine dei valori nella proprietà text-shadow.

## --video-solution--

2

## --text--

Come puoi creare un effetto ombra per il testo a più strati?

## --answers--

Definendo più ombre nella proprietà text-shadow.

---

Annidando elementi uno dentro l’altro.

### --feedback--

Pensa a come combinare più effetti ombra.

---

Usando la proprietà `filter`.

### --feedback--

Pensa a come combinare più effetti ombra.

---

Applicando un’immagine di sfondo.

### --feedback--

Pensa a come combinare più effetti ombra.

## --video-solution--

1
