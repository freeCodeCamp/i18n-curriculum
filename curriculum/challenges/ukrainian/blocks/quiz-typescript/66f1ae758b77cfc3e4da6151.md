---
id: 66f1ae758b77cfc3e4da6151
title: Тест з TypeScript
challengeType: 8
dashedName: quiz-typescript
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке TypeScript?

#### --distractors--

Фреймворк, який використовується для системного програмування.

---

Інструмент для керування базами даних.

---

Мова програмування, що використовується для стилізації вебсайтів.

#### --answer--

Надмножина JavaScript, яка додає статичну типізацію.

### --question--

#### --text--

Чому варто використовувати TypeScript у проєкті?

#### --distractors--

Він усуває потребу в тестуванні застосунків.

---

Він швидший за всі інші мови програмування.

---

Він полегшує стилізацію вебсайтів.

#### --answer--

Він покращує підтримуваність, додаючи визначення типів до JavaScript.

### --question--

#### --text--

У яку мову компілюється TypeScript?

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

Яким буде вихідні дані наступного фрагмента коду?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

Яка основна мета інтерфейсу в TypeScript?

#### --distractors--

Компілювати код TypeScript у JavaScript.

---

Створити новий тип даних.

---

Створити новий JavaScript-об’єкт.

#### --answer--

Визначити структуру об’єкта.

### --question--

#### --text--

Який із наведених типів НЕ є базовим примітивом у TypeScript?

#### --distractors--

рядок

---

булевий

---

число

#### --answer--

масив

### --question--

#### --text--

Яке з наведених визначень правильно описує літеральний тип у TypeScript?

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

Яка основна мета ключового слова `type` у TypeScript?

#### --distractors--

Оголошувати змінні.

---

Створювати класи.

---

Реалізовувати інтерфейси.

#### --answer--

Визначати кастомні типи.

### --question--

#### --text--

Яке з наведених визначень правильно описує об’єднаний тип?

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

Яка з наведених можливостей є унікальною для `interface` і НЕ МОЖЕ бути досягнута за допомогою `type` у TypeScript?

#### --distractors--

Визначення форм об’єктів.

---

Створення об’єднаних типів.

---

Визначення типів, що включають лише конкретні значення.

#### --answer--

Оголошення реалізацій класів.

### --question--

#### --text--

Яка основна мета використання дженериків у TypeScript?

#### --distractors--

Реалізувати успадкування між класами.

---

Визначити структуру об’єкта.

---

Створити новий тип даних.

#### --answer--

Писати повторно використовуваний код, який може працювати з різними типами.

### --question--

#### --text--

Який із наведених є **правильним прикладом** дженерик-функції у TypeScript?

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

Що таке звуження типів у TypeScript?

#### --distractors--

Зменшення розміру скомпільованого JavaScript.

---

Обмеження кількості типів в об’єднанні.

---

Видалення невикористовуваних типів із проєкту TypeScript.

#### --answer--

Написання коду, який звужує об’єднаний тип до більш конкретного типу.

### --question--

#### --text--

Який файл зазвичай використовується для налаштування параметрів компілятора TypeScript?

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

Що вказує опція `rootDir` у файлі `tsconfig.json`?

#### --distractors--

Версію TypeScript, що використовується під час компіляції.

---

Цільове середовище виконання (наприклад, Node.js або браузер) для коду TypeScript.

---

Конкретну систему модулів для імпортів і експортів.

#### --answer--

Каталог, у якому зберігаються ваші вихідні файли.

### --question--

#### --text--

Який приклад правильно використовує `instanceof` для безпечного звуження типу та доступу до властивості `value`?

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
Що робить встановлення `"strict": true` у `tsconfig.json` у TypeScript?

#### --distractors--

Увімкнює статичну перевірку типів, щоб ви могли виявляти помилки до запуску коду.

---

Змушує завжди використовувати синтаксис ES6.

---

Змушує компілювати код перед його запуском.

#### --answer--

Увімкнює набір опцій перевірки типів, таких як обов’язкова обробка `null` або `undefined` і запобігання неявному `any`.

### --question--

#### --text--

Яка мета типу `never` у TypeScript?

#### --distractors--

Представити null або undefined.

---

Представити будь-яке можливе значення.

---

Представити тип, який може бути будь-чим, крім null.

#### --answer--

Представити тип значень, які ніколи не трапляються.

### --question--

#### --text--

Який із наведених викликів функції `getRandomValue` скомпілюється успішно у TypeScript?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

Як виправити помилку компілятора TypeScript `'email' is possibly 'null'` у наведеному коді за допомогою опціонального ланцюжка?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`
