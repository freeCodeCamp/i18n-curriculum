---
id: 66edd07682767adff3a6231e
title: Quiz zu DOM-Manipulation und Klick-Ereignissen mit JavaScript
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist das Document Object Model?

#### --distractors--

Eine Programmiersprache, die verwendet wird, um mit einer HTML-Seite zu interagieren.

---

Eine Menge von Objekten, die nur Gleitkommazahlen enthalten.

---

Eine Menge von JavaScript-Klassen, die HTML-Elemente darstellen.

#### --answer--

Eine Programmierschnittstelle, die Ihnen erlaubt, mit HTML-Dokumenten zu interagieren.

### --question--

#### --text--

Wofür steht API?

#### --distractors--

Zusätzliches Programmierinterface

---

Hinzufügen eines Programmierinterfaces

---

Ein anderes Programmierinterface

#### --answer--

Programmierschnittstelle für Anwendungen

### --question--

#### --text--

Wie können Sie mit JavaScript alle HTML-Anker mit fehlenden Hyperlinks finden?

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

Was macht die `textContent`-Eigenschaft?

#### --distractors--

Sie entfernt den Textinhalt aus allen HTML-Elementen.

---

Sie entfernt den Textinhalt aus Ihrer JavaScript-Datei.

---

Sie fügt allen Arrays und Objekten in Ihrer JavaScript-Datei Text hinzu.

#### --answer--

Sie gibt den reinen Textinhalt eines Elements zurück.

### --question--

#### --text--

Was macht die `innerHTML`-Eigenschaft?

#### --distractors--

Sie ist eine Eigenschaft des `Element`, die verwendet wird, um den gesamten Textinhalt eines HTML-Elements zu erhalten.

---

Sie ist eine Eigenschaft des `Element`, die verwendet wird, um Anmerkungen zu Ihrem HTML hinzuzufügen.

---

Sie ist eine Eigenschaft des `Element`, die verwendet wird, um Ereignisse in Ihrem HTML auszulösen.

#### --answer--

Sie ist eine Eigenschaft des `Element`, die verwendet wird, um Teile der HTML-Auszeichnung zu setzen oder zu aktualisieren.

### --question--

#### --text--

Welche der folgenden Optionen fügt einen gültigen Ereignis-Listener hinzu?

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

Welche der folgenden Methoden ist KEINE gültige Methode des `document`-Objekts?

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()` 

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

Welche der folgenden Optionen setzt das `class`-Attribut auf `my-class`?

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

Was ist das `Event`-Objekt?

#### --distractors--

Es ist ein spezielles Objekt, das verwendet wird, um Ereignisse aus Ihrem JavaScript-Projekt zu entfernen.

---

Es wird verwendet, um automatisch Ereignisse zu Ihren JavaScript-Projekten hinzuzufügen.

---

Es ist ein spezielles Objekt, das beim Arbeiten mit Arrays verwendet wird.

#### --answer--

Es ist eine Nutzlast, die ausgelöst wird, wenn ein Benutzer auf irgendeine Weise mit Ihrer Webseite interagiert.

### --question--

#### --text--

Welche der folgenden Methoden wird verwendet, um einen zuvor mit der `addEventListener()`-Methode hinzugefügten Ereignis-Listener zu entfernen?

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

Was sind inline-Ereignis-Handler?

#### --distractors--

Spezielle Attribute an einem HTML-Element, die verwendet werden, um alle Ereignisse abzubrechen.

---

Spezielle Attribute an einem HTML-Element, die nur verwendet werden, um Formularübermittlungen in Ihrem JavaScript-Code auszulösen.

---

Spezielle Attribute an einem HTML-Element, die verwendet werden, um Ereignisse zu entfernen.

#### --answer--

Spezielle Attribute an einem HTML-Element, die verwendet werden, um JavaScript-Code auszuführen, wenn ein Ereignis eintritt.

### --question--

#### --text--

Was sind die zwei Hauptkategorien von Web-APIs?

#### --distractors--

Java-APIs und Drittanbieter-APIs.

---

Browser-APIs und Rust-APIs.

---

System-APIs und Drittanbieter-APIs.

#### --answer--

Browser-APIs und Drittanbieter-APIs.

### --question--

#### --text--

Welches Ereignis wird ausgelöst, wenn alle Elemente der HTML-Seite vom Browser geparst wurden?

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

Welche der folgenden Eigenschaften stellt den inline-Stil eines Elements dar?

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

Welche der folgenden Optionen ist die korrekte Verwendung eines inline-Ereignis-Handlers?

#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--

Welche der folgenden Methoden wird verwendet, um eine Aktion für eine bestimmte Zeit zu verzögern?

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

Welche der folgenden APIs kann verwendet werden, um Animationen direkt in JavaScript zu erstellen und zu steuern?

#### --distractors--

Allow Animations API

---

Control Animations API 

---

Create Animations API

#### --answer--

Web Animations API 

### --question--

#### --text--

Was macht die Canvas-API?

#### --distractors--

Sie wird verwendet, um Grafiken in Ihrer CSS-Datei zu animieren.

---

Sie wird nur verwendet, um Grafiken aus Ihrer JavaScript-Datei zu entfernen.

---

Sie wird verwendet, um Grafiken aus Ihrer HTML-Datei zu entfernen.

#### --answer--

Sie wird verwendet, um Grafiken über Ihre JavaScript-Datei zu manipulieren.

### --question--

#### --text--

Was ist Event Delegation?

#### --distractors--

Der Prozess, Ereignisse zu hören, wenn ein Ereignis abgebrochen wird.

---

Der Prozess, Ereignisse zu hören, wenn ein Klick-Ereignis eintritt.

---

Der Prozess, Ereignisse zu hören, wenn ein Änderungs-Ereignis eintritt.

#### --answer--

Der Prozess, Ereignisse zu hören, die zu einem übergeordneten Element hochgebubbelt sind.

### --question--

#### --text--

Welche der folgenden Methoden können Sie NICHT verwenden, wenn Sie mit Dialogen und Modals arbeiten?

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`

