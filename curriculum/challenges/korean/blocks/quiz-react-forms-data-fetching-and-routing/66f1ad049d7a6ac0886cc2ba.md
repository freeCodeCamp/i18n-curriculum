---
id: 66f1ad049d7a6ac0886cc2ba
title: "React 폼, 데이터 가져오기 및 라우팅 퀴즈"
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

다음 중 제어된 입력의 예는 무엇인가요?

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

다음 중 비제어 입력의 예는 무엇인가요?

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

다음 중 React에서 데이터를 가져오는 데 일반적으로 사용되지 않는 방법은 무엇인가요?

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

다음 중 React에서 데이터 가져오기와 일반적으로 관련된 훅은 무엇인가요?

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

메모이제이션이란 무엇인가요?

#### --distractors--

그래프에서 두 정점 사이의 최단 경로를 찾는 데 사용되는 일반적인 알고리즘입니다.

---

모든 제어 및 비제어 입력의 성능을 최적화하는 데 사용되는 특별한 기법입니다.

---

부분 배열을 재귀적으로 정렬하고 병합하는 분할 정복 알고리즘입니다.

#### --answer--

비싼 함수 호출 결과를 특정 인수에 따라 캐시(기억)하는 최적화 기법입니다.

### --question--

#### --text--

다음 중 계산된 값을 메모이제이션하는 데 사용되는 훅은 무엇인가요?

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

다음 중 함수 참조를 메모이제이션하는 데 올바른 훅을 사용하는 것은 무엇인가요?

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

의존성이란 무엇인가요?

#### --distractors--

의존성은 React 애플리케이션에서 일반적으로 사용되는 테스트 라이브러리입니다.

---

의존성은 서버에서만 실행되는 React 컴포넌트 유형입니다.

---

의존성은 React 코드를 JavaScript로 컴파일하는 데 사용되는 특별한 컴파일러입니다.

#### --answer--

의존성은 애플리케이션 내 한 컴포넌트나 모듈이 제대로 작동하려면 다른 컴포넌트나 모듈에 의존하는 경우를 말합니다.

### --question--

#### --text--

패키지 매니저란 무엇인가요?

#### --distractors--

애플리케이션에 설치된 서드파티 패키지를 업데이트하는 도구입니다.

---

프로젝트 내에서만 생성된 패키지를 관리하는 도구입니다.

---

의존성을 `node_modules` 폴더에서 특별한 설정 폴더로 이동하는 도구입니다.

#### --answer--

의존성 설치, 업데이트 및 제거에 사용하는 도구입니다.

### --question--

#### --text--

다음 중 JavaScript 생태계에서 일반적으로 사용되지 않는 패키지 매니저는 무엇인가요?

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

다음 중 프로젝트에 대한 메타데이터를 포함하는 주요 설정 파일은 무엇인가요?

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

다음 중 일반적으로 사용되는 React 프레임워크는 무엇인가요?

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

React Router를 사용하여 React 애플리케이션에 라우트를 추가하는 올바른 방법은 무엇인가요?

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

React Router에서 동적 라우트를 생성하는 올바른 방법은 무엇인가요?

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

URL 경로에서 동적 매개변수에 접근하는 데 사용하는 훅은 무엇인가요?

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

Next.js에서 커스텀 요청 핸들러를 만드는 올바른 방법은 무엇인가요?

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

프로프 드릴링이란 무엇인가요?

#### --distractors--

특정 프로프스에 반응하여 상태가 어떻게 변해야 하는지 지정하는 리듀서를 만드는 과정입니다.

---

자식 컴포넌트에서 부모 컴포넌트로 프로프스를 전달하는 과정입니다.

---

React 애플리케이션에서 컨텍스트 프로바이더를 생성하고 이를 사용해 부모에서 자식 컴포넌트로 프로프스를 전달하는 과정입니다.

#### --answer--

부모 컴포넌트에서 깊게 중첩된 자식 컴포넌트로 프로프스를 전달하는 과정으로, 일부 자식 컴포넌트는 프로프스를 필요로 하지 않아도 전달하는 경우입니다.

### --question--

#### --text--

다음 중 React 애플리케이션에서 일반적으로 사용되지 않는 상태 관리 솔루션은 무엇인가요?

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

React Developer Tools는 무엇을 위해 사용되나요?

#### --distractors--

이 도구들은 React 애플리케이션에서 접근성 문제만 확인하는 데 사용됩니다.

---

이 도구들은 React 컴포넌트에 추가 테스트를 추가하는 데 사용됩니다.

---

이 도구들은 애플리케이션 내 모든 React 컴포넌트를 메모이제이션하는 데 사용됩니다.

#### --answer--

이 도구들은 React 컴포넌트를 검사하고 성능 문제를 식별하는 데 사용됩니다.

### --question--

#### --text--

React 서버 컴포넌트란 무엇인가요?

#### --distractors--

서버에서만 컨텍스트를 관리하는 컴포넌트입니다.

---

서버에서만 렌더링되는 메모이제이션된 컴포넌트입니다.

---

서버에서만 상태를 관리하는 컴포넌트입니다.

#### --answer--

서버에서만 렌더링되는 컴포넌트입니다.
