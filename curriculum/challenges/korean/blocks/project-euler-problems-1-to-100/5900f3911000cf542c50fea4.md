---
id: 5900f3911000cf542c50fea4
title: '문제 37: 잘라내기 가능한 소수'
challengeType: 1
forumTopicId: 302031
dashedName: problem-37-truncatable-primes
---

# --description--

숫자 3797은 흥미로운 속성을 가지고 있습니다. 소수인 이 숫자는 왼쪽에서 오른쪽으로 연속해서 자릿수를 제거해도 각 단계마다 소수로 남을 수 있습니다: 3797, 797, 97, 그리고 7. 마찬가지로 오른쪽에서 왼쪽으로도 작업할 수 있습니다: 3797, 379, 37, 그리고 3.

왼쪽에서 오른쪽, 그리고 오른쪽에서 왼쪽으로 모두 잘라내기 가능한 `n` (8 ≤ `n` ≤ 11) 소수들의 합을 구하세요.

참고: 2, 3, 5, 그리고 7은 잘라내기 가능한 소수로 간주하지 않습니다.

# --hints--

`truncatablePrimes(8)`는 숫자를 반환해야 합니다.

```js
assert(typeof truncatablePrimes(8) === 'number');
```

`truncatablePrimes(8)`는 1986을 반환해야 합니다.

```js
assert(truncatablePrimes(8) == 1986);
```

`truncatablePrimes(9)`는 5123을 반환해야 합니다.

```js
assert(truncatablePrimes(9) == 5123);
```

`truncatablePrimes(10)`는 8920을 반환해야 합니다.

```js
assert(truncatablePrimes(10) == 8920);
```

`truncatablePrimes(11)`는 748317을 반환해야 합니다.

```js
assert(truncatablePrimes(11) == 748317);
```

# --seed--

## --seed-contents--

```js
function truncatablePrimes(n) {

  return n;
}

truncatablePrimes(11);
```

# --solutions--

```js
// solution required
```
