---
id: 68376949899fcbad3c9cac23
title: Quiz su JavaScript e accessibilità
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 9 delle 10 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale attributo si usa per scopi di accessibilità per indicare se un controllo è espanso o compresso?

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

Quali due attributi possono essere usati in combinazione con `aria-expanded` per stabilire una connessione programmata tra l'elemento di controllo e l'elemento che controlla?

#### --distractors--

`aria-labelledBy` e `aria-owns`

---

`aria-controls` e `aria-label`

---

`aria-live` e `aria-owns`

#### --answer--

`aria-controls` e `aria-owns`

### --question--

#### --text--

Quale dei seguenti attributi crea una regione live sulla tua pagina che può essere usata per notificare agli utenti di lettori di schermo i cambiamenti dinamici del contenuto nella regione live man mano che avvengono?

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

Quale dei seguenti elementi `div` sarà disabilitato solo per le persone che usano tecnologie assistive, come i lettori di schermo?

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

Quale dei seguenti è un uso valido dell'attributo `aria-expanded`?

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

Quale attributo si usa per creare una relazione programmata tra un elemento di controllo e l'elemento che influenza sulla pagina?

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

Quale dei seguenti NON è un caso d'uso comune per le regioni live?

#### --distractors--

Messaggi che vengono mostrati dopo che un'azione è stata eseguita (come un messaggio di errore o di conferma).

---

Contenuti che si aggiornano periodicamente (come un ticker, un marquee o un conto alla rovescia).

---

Messaggi di stato generali (come aggiornamenti su un processo).

#### --answer--

Messaggi che vengono mostrati prima che un'azione sia stata eseguita (come una finestra di dialogo di conferma).

### --question--

#### --text--

Quale dei seguenti esempi è il modo corretto per indicare quale scheda è attualmente selezionata?

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

Quale dei seguenti NON è un valore valido per l'attributo `aria-live`?

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

Quale dei seguenti valori dell'attributo `aria-live` significa che l'aggiornamento è molto importante e ha la priorità più alta?

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`
