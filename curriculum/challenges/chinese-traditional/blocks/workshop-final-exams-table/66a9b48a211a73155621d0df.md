---
id: 66a9b48a211a73155621d0df
title: 步驟 3
challengeType: 0
dashedName: step-3
---

# --description--

在前面的講座中，你學習瞭如何使用 `table` 元素來表示表格數據。

在 `body` 元素內，嵌套一個 `table` 元素。

# --hints--

你應該有一個 `table` 開始標籤。

```js
assert.match(code, /<table>/i);
```

你應該有一個 `table` 結束標籤。

```js
assert.match(code, /<\/table>/i);
```

你的 `table` 元素應該在 `body` 元素中。

```js
const tableEl = document.querySelector('table');
assert.strictEqual(tableEl?.parentElement?.tagName, 'BODY');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Calculus Final Exams Table</title>
    <meta charset="UTF-8" />
  </head>
  <body>
--fcc-editable-region--

--fcc-editable-region--
  </body>
</html>
```
