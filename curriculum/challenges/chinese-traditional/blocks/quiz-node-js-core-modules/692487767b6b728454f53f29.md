---
id: 692487767b6b728454f53f29
title: NodeJS 核心模組測驗
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

哪一個 `fs` 方法會將內容添加到現有的檔案而不覆蓋它？

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

當你使用來自 `fs` 模組的同步方法時會發生什麼？

#### --distractors--

它們會自動刪除現有的檔案。

---

它們在所有情況下的執行速度都比非同步的快。

---

它們預設會傳回 Promises。

#### --answer--

它們會阻塞程式直到操作結束。

### --question--

#### --text--

`fs.readFile()` 中的 `utf8` 參數有什麼作用？

#### --distractors--

它防止錯誤被丟出。

---

它會更改檔案的擴充套件。

---

它會壓縮大型檔案。

#### --answer--

它告訴 Node 傳回可讀取的字串，而不是緩衝區。

### --question--

#### --text--

哪一個 `Buffer` 方法會創建一個填滿零的新緩衝區？

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

`Buffer.toString()` 做什麼？

#### --distractors--

將緩衝區轉換成二進位程式碼。

---

清除緩衝區的內容。

---

顯示緩衝區位元組長度。

#### --answer--

將緩衝區內容轉換成可讀取的字串。

### --question--

#### --text--

哪一個 `crypto` 特性需要私鑰來產生標記？

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

為什麼你應該避免直接撰寫你自己的加密演算法？

#### --distractors--

在 Node.js 應用程式中是非法的。

---

Node.js 不支援加密。

---

JavaScript 無法計算雜湊值。

#### --answer--

如果沒有經過充分測試的函式庫，可能會不安全。

### --question--

#### --text--

建議使用哪個 `crypto` 方法來產生安全的隨機語彙單元，而非使用 `Math.random()`？

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

哪一個 `os` 模組方法會傳回 CPU 架構？

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

`os.userInfo()` 傳回什麼？

#### --distractors--

只有使用者名稱。

---

只有裝置 IP 位址。

---

只有已登入使用者的 ID。

#### --answer--

關於目前系統使用者的資訊。

### --question--

#### --text--

哪一個 `path` 方法會連接區段並標準化多餘的斜線？

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

哪一個 `path` 方法會根據目前的工作目錄產生絕對路徑？

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

全域變數 `__dirname` 表述什麼？

#### --distractors--

執行 Node 的使用者名稱。

---

目前檔案的擴充套件。

---

以 `./` 開頭的相對資料夾路徑。

#### --answer--

包含目前檔案的目錄的絕對路徑。

### --question--

#### --text--

哪個處理程序屬性包含像 `NODE_ENV` 這樣的環境變數？

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

`process.on("exit")` 事件的作用是什麼？

#### --distractors--

它會重新啟動 Node 應用程式。

---

它會向子處理程序發送 `exit` 信號。

---

它會清除事件迴圈佇列。

#### --answer--

在 Node 行程結束前執行程式碼。

### --question--

#### --text--

哪種資料流型別既能讀取又能寫入資料，但不會轉換它？

#### --distractors--

可讀資料流

---

轉換資料流

---

可寫入資料流

#### --answer--

雙工資料流

### --question--

#### --text--

實作高效讀寫大型檔案需要什麼？

#### --distractors--

僅使用 `Buffer.alloc()`

---

匯入 `crypto` 模組

---

先將整個檔案儲存在記憶體中

#### --answer--

使用資料流分塊處理資料

### --question--

#### --text--

哪一個方法將可讀取的 `stream` 聯結到可寫入的 `stream`？

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

哪個串流事件顯示沒有更多資料可寫入？

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

如果你從可讀取的資料流列印一個區塊而不使用 `toString()`，會記錄什麼？

#### --distractors--

純文字內容

---

處理程序 PID

---

作業系統名稱

#### --answer--

一個包含二進位資料的緩衝區物件
