---
id: 66edd630f7666cfa54b404d0
title: Quiz su JavaScript asincrono
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos'è la programmazione asincrona?

#### --distractors--

Un metodo per eseguire il codice in modo sequenziale.

---

Una tecnica per gestire gli errori nel tuo codice.

---

Un modo per effettuare richieste di rete, tipicamente per recuperare o inviare dati al server.

#### --answer--

Un metodo per eseguire il codice in modo concorrente senza bloccare il thread principale.

### --question--

#### --text--

Cos'è un thread?

#### --distractors--

Una funzione usata per recuperare dati dal server.

---

Un programma che esegue codice JavaScript in un browser web.

---

Un tipo speciale di funzione che viene passata come argomento a un'altra funzione.

#### --answer--

Una sequenza di istruzioni che può essere eseguita indipendentemente dal flusso principale del programma.

### --question--

#### --text--

Qual è lo scopo della Fetch API in JavaScript?

#### --distractors--

Gestire callback asincroni.

---

Creare e gestire cookie.

---

Renderizzare contenuti HTML dinamici.

#### --answer--

Effettuare richieste di rete ai server.

### --question--

#### --text--

Quale metodo `HTTP` viene usato per inviare dati a un server per l'elaborazione?

#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--

Qual è il modo corretto per recuperare dati da un'API?

#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--

Cos'è una promise in JavaScript?

#### --distractors--

Un modo per sincronizzare più operazioni quando si lavora con la Fetch API.

---

Un tipo speciale di funzione di callback che devi usare in tutte le funzioni di ordine superiore.

---

Una tecnica usata per gestire errori quando lavori con la Fetch API.

#### --answer--

Un oggetto che rappresenta il completamento eventuale (o il fallimento) di un'operazione asincrona.

### --question--

#### --text--

Cosa permette di fare il concatenamento di promise?

#### --distractors--

Permette di eseguire più operazioni sincrone in parallelo.

---

Permette di gestire operazioni sincrone in modo più efficiente.

---

Permette di riprovare automaticamente operazioni fallite.

#### --answer--

Permette di eseguire una sequenza di operazioni asincrone una dopo l'altra.

### --question--

#### --text--

Quale metodo si usa per gestire il completamento con successo di una promise?

#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--

Quale metodo si usa per gestire gli errori in una catena di promise?

#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--

Quale dei seguenti NON è un metodo HTTP valido?

#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--

Qual è la sintassi corretta per creare una funzione async?

#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--

Cosa fa la parola chiave `await` in una funzione async?

#### --distractors--

Crea una nuova promise.

---

Esegue il codice immediatamente senza aspettare e restituisce un oggetto.

---

Registra il risultato della promise e restituisce `null`.

#### --answer--

Pausa l'esecuzione della funzione finché la promise non viene risolta.

### --question--

#### --text--

Quale delle seguenti API fornisce un modo per i siti web di richiedere la posizione dell'utente?

#### --distractors--

Locator API

---

Geo API

---

Locate API

#### --answer--

Geolocation API

### --question--

#### --text--

Quale dei seguenti attributi scarica lo script in modo asincrono ma aspetta che il documento HTML sia completamente analizzato prima di eseguire lo script?

#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--

Cosa restituirà il seguente codice?

```js
fetch('https://api.example.com/data')
```

#### --distractors--

Un errore che dice che i dati sono corrotti.

---

Un errore che dice che i dati non possono essere recuperati.

---

Non verrà restituito nulla e JavaScript passerà alla funzione successiva nello script.

#### --answer--

Una `Promise` che si risolve in un oggetto `Response`.

### --question--

#### --text--

Qual è il metodo HTTP predefinito per la Fetch API?

#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--

Quale dei seguenti metodi si usa per eliminare risorse sul server?

#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--

Qual è il modo corretto per creare una promise?

#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--

Quale dei seguenti attributi dice al browser di scaricare il file script in modo asincrono continuando a parsare il documento HTML?

#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--

Quale metodo della Geolocation API si usa per ottenere la posizione attuale di un dispositivo?

#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`
