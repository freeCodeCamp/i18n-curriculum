---
id: 66f1afbd9998e9c985d8e73b
title: Bash 스크립팅 퀴즈
challengeType: 8
dashedName: quiz-bash-scripting
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

bash 스크립트에서 올바른 셰뱅 라인은 무엇인가요?

#### --distractors--

`!#/bin/bash`

---

`/#!bin/bash`

---

`/#!/bin/bash`

#### --answer--

`#!/bin/bash`

### --question--

#### --text--

어떤 명령어가 `MY_VAR`를 정의하고 콘솔에 출력하나요?

#### --distractors--

`MY_VAR=10; echo MY_VAR`

---

`$MY_VAR=10; echo MY_VAR`

---

`$MY_VAR=10; echo $MY_VAR`

#### --answer--

`MY_VAR=10; echo $MY_VAR`

### --question--

#### --text--

셸에서 모든 변수를 확인하는 명령어는 무엇인가요?

#### --distractors--

`declare -n env`

---

`declare -p env`

---

`declare -x bashrc`

#### --answer--

`declare -p`

### --question--

#### --text--

어떤 명령어가 `script.sh` 파일을 실행하나요?

#### --distractors--

`script.sh bash`

---

`zsh ./script.sh`

---

`./sh script.sh`

#### --answer--

`./script.sh`

### --question--

#### --text--

bash에서 `less` 명령어의 매뉴얼을 보는 방법은 무엇인가요?

#### --distractors--

`less man`

---

`less --man`

---

`man --less`

#### --answer--

`man less`

### --question--

#### --text--

`print_arguments` 함수를 호출하여 `a`와 `b`를 터미널에 출력하려면 어떻게 해야 하나요?

```bash
print_arguments() {
  echo "Argument 1: $1"
  echo "Argument 2: $2"
}

```

#### --distractors--

`print_arguments("a", "b")`

---

`print_arguments(a, b)`

---

`print_arguments{a, b}`

#### --answer--

`print_arguments "a" "b"`

### --question--

#### --text--

bash에서 배열을 초기화하는 방법은 무엇인가요?

#### --distractors--

`ARR=["item1" "item2" "item3"]`

---

`ARR=[["item1" "item2" "item3"]]`

---

`ARR={"item1" "item2" "item3"}`

#### --answer--

`ARR=("item1" "item2" "item3")`

### --question--

#### --text--

bash에서 배열의 세 번째 값을 출력하는 방법은 무엇인가요?

#### --distractors--

`echo ${ARR[3]}`

---

`echo $ARR[2]`

---

`echo $ARR[3]`

#### --answer--

`echo ${ARR[2]}`

### --question--

#### --text--

bash에서 사용자 입력을 변수에 할당하는 방법은 무엇인가요?

#### --distractors--

`input MY_VAR`

---

`cin MY_VAR`

---

`get MY_VAR`

#### --answer--

`read MY_VAR`

### --question--

#### --text--

명령어 `1.txt`를 실행한 후 `chmod 765 1.txt` 파일의 권한 플래그는 어떻게 되나요?

#### --distractors--

`rwxrwxrwx`

---

`rwxr-xrw-`

---

`r-xrw-rwx`

#### --answer--

`rwxrw-r-x`

### --question--

#### --text--

문장 `if ... then ...`는 어떤 키워드로 끝나나요?

#### --distractors--

`end`

---

`done`

---

`stop`

#### --answer--

`fi`

### --question--

#### --text--

문장 `for ... do ...`는 어떤 키워드로 끝나나요?

#### --distractors--

`end`

---

`od`

---

`stop`

#### --answer--

`done`

### --question--

#### --text--

bash에서 주석을 만드는 방법은 무엇인가요?

#### --distractors--

`// comment`

---

`!comment`

---

`/* comment`

#### --answer--

`# comment`

### --question--

#### --text--

이 코드 조각의 시작 부분에 어떤 줄을 추가해야 `for` 루프를 만들 수 있나요?

```bash

do
    echo $i
done
```

#### --distractors--

`for ( int i = 0; i < 10; i++ )`

---

`for (( int i = 0; i < 10; i++ ))`

---

`for (( let i = 0; i < 10; i++ ))`

#### --answer--

`for (( i = 0; i < 10; i++ ))`

### --question--

#### --text--

변수 `A`와 `B`의 합을 변수 `C`에 할당하려면 어떻게 해야 하나요?

#### --distractors--

`C=$A + $B`

---

`C=$( A + B )`

---

`$C=$(( $A + $B ))`

#### --answer--

`C=$(( A + B ))`

### --question--

#### --text--

새 파일을 만드는 데 사용하는 명령어는 무엇인가요?

#### --distractors--

`newfile`

---

`echo new_file`

---

`fileargs`

#### --answer--

`touch`

### --question--

#### --text--

명령어 `echo $?`는 무엇을 하나요?

#### --distractors--

마지막으로 사용한 변수의 값을 출력합니다.

---

마지막으로 사용한 산술 연산의 값을 출력합니다.

---

마지막으로 실행한 조건문의 값을 출력합니다.

#### --answer--

마지막으로 실행한 명령어의 종료 코드를 출력합니다.

### --question--

#### --text--

스크립트의 일부를 테스트할 때 Bash가 제공하는 장점은 무엇인가요?

#### --distractors--

Bash 스크립트는 테스트 전에 컴파일해야 합니다.

---

IDE를 사용해서만 스크립트를 테스트할 수 있습니다.

---

Bash가 자동으로 테스트 케이스를 생성합니다.

#### --answer--

스크립트 섹션을 터미널에 바로 붙여넣어 테스트할 수 있습니다.

### --question--

#### --text--

bash에는 어떤 종류의 루프가 있나요?

#### --distractors--

`for`

---

`for`, `while`

---

`for`, `while`, `until`, `forEach`

#### --answer--

`for`, `while`, `until`

### --question--

#### --text--

bash에서 배열의 모든 값을 출력하는 방법은 무엇인가요?

#### --distractors--

`echo $ARR[@]`

---

`echo {ARR[@]}`

---

`echo ${ARR[$@]}`

#### --answer--

`echo ${ARR[@]}`
