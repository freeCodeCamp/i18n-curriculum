---
id: 67f39e2119042d2f2ca926ff
title: 動的計画法のレビュー
challengeType: 31
dashedName: review-dynamic-programming
---

# --description--

## 動的計画法の紹介

- **定義**：動的計画法は、複雑な問題をより簡単な部分問題に分解し、その結果を保存して重複した計算を避けるアルゴリズムの手法です。 
- **重複部分問題**：大きな問題を解く際に同じ小さな問題が何度も現れます。これらの部分問題を繰り返し計算する代わりに、その解を保存します。
- **最適部分構造**：問題の最適解は、その部分問題の最適解を含みます。つまり、小さな部分の最適解を組み合わせて最良の解を構築できます。

## 動的計画法の解法

- **メモ化（トップダウンアプローチ）**：メモ化は高コストな関数呼び出しの結果を保存し、同じ入力が再度現れたときにキャッシュされた結果を返します。

```py
def climb_stairs_memo(n, memo={}):
    """Dynamic programming with memoization"""
    # Check if we've already calculated this value
    if n in memo:
        return memo[n]  # Return cached result - O(1) lookup!
    
    # Base cases
    if n <= 2:
        return n
    
    # Calculate once and store in memo for future use
    memo[n] = climb_stairs_memo(n-1, memo) + climb_stairs_memo(n-2, memo)
    return memo[n]
```

- **タビュレーション（ボトムアップアプローチ）**：タビュレーションは下から解を構築し、部分問題の解をテーブルに埋めていきます。

```py
def climb_stairs_tabulation(n):
    """Dynamic programming with tabulation"""
    if n <= 2:
        return n
    
    # Create array to store results for all steps from 0 to n
    dp = [0] * (n + 1)
    dp[1] = 1  # 1 way to reach step 1
    dp[2] = 2  # 2 ways to reach step 2
    
    # Build up the solution iteratively
    for i in range(3, n + 1):
        # Ways to reach step i = ways to reach (i-1) + ways to reach (i-2)
        dp[i] = dp[i-1] + dp[i-2]
    
    return dp[n]
```

## 動的計画法を使った実世界の応用

- **ルート最適化**：GPSシステムは動的計画法のアルゴリズムを使って、場所間の最短経路を見つけます。
- **テキスト処理**：スペルチェッカーやオートコンプリート機能は、単語間の編集距離を計算するために動的計画法をよく利用します。
- **金融モデリング**：投資戦略やポートフォリオの最適化には動的計画法の手法が頻繁に使われます。
- **資源配分**：ナップサック問題やその変種は、スケジューリング、予算編成、資源管理に現れます。

## 動的計画法を使うべき場合

次のような場合に動的計画法の利用を検討してください：

- 問題が重複する部分問題に分解できる。
- 問題が最適部分構造を持つ。
- 単純な再帰的解法では計算が繰り返される。
- 時間計算量を優先し、空間計算量を犠牲にしてもよい。

# --assignment--

動的計画法のトピックと概念をレビューしてください。
