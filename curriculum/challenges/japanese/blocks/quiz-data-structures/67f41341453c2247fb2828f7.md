---
id: 67f41341453c2247fb2828f7
title: データ構造クイズ
challengeType: 8
dashedName: quiz-data-structures
---

# --description--

クイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

アルゴリズム解析におけるビッグO記法は何を表していますか？

#### --distractors--

特定のコンピューターでの正確な実行時間（秒単位）。

---

実行中に実行されるコード行の割合。

---

他の開発者にとってのコードの読みやすさ。

#### --answer--

入力サイズに対して時間または空間がどのように増加するか（上限の目安）。

### --question--

#### --text--

アルゴリズムのチャレンジを始めるとき、最初に行うべき最善のステップは何ですか？

#### --distractors--

勢いをつけるためにすぐにコーディングを始める。

---

問題を理解する前にパフォーマンスを最適化する。

---

解決策を完成させてから単体テストを書く。

#### --answer--

例やエッジケースを使って問題と制約を明確にする。

### --question--

#### --text--

動的配列と静的配列の主な違いは何ですか？

#### --distractors--

動的配列は異なる型の値を格納できるが、静的配列はできない。

---

静的配列は重複値を許可するが、動的配列は許可しない。

---

動的配列はすべての操作で静的配列より高速である。

#### --answer--

動的配列はサイズ変更で増減できるが、静的配列は固定サイズである。

### --question--

#### --text--

動的配列の末尾に要素を追加する際の償却時間計算量は何ですか？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` の償却時間。

### --question--

#### --text--

単方向連結リストでインデックスkの要素にアクセスするのに`O(n)`時間かかるのはなぜですか？

#### --distractors--

アクセス前にリストのサイズ変更が必要だから。

---

インデックスはハッシュ化されテーブルで検索されるから。

---

ノードは連続して格納されているためシフトが必要だから。

#### --answer--

先頭ノードからk番目のノードまで一つずつたどる必要があるから。

### --question--

#### --text--

単方向連結リストにないが双方向連結リストにある機能は何ですか？

#### --distractors--

`O(1)`時間で任意のインデックスにランダムアクセスできる。

---

高速な反復のための組み込み配列バッファ。

---

リストの長さを自動的に一定に保つ機能。

#### --answer--

次のノードと前のノードの両方へのポインターがあり、逆方向のたどりも可能。

### --question--

#### --text--

スタックを最もよく表す説明はどれですか？

#### --distractors--

先入れ先出し（`FIFO`）で先頭から削除。

---

任意の要素を`O(1)`時間で削除可能な構造。

---

定数時間でランダムアクセスできる循環バッファ。

#### --answer--

後入れ先出し（`LIFO`）で、トップで`push`と`pop`を行う。

### --question--

#### --text--

キューの先頭の要素を削除する操作はどれですか？

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

ハッシュマップでキーによる値の検索の典型的な平均時間計算量は何ですか？

#### --distractors--

`O(n)`、すべてのキーを順番にスキャンするため。

---

`O(log n)`、バケット内での二分探索による。

---

`O(n log n)`、挿入時にキーがソートされるため。

#### --answer--

良いハッシュ関数と低い負荷率で平均`O(1)`。

### --question--

#### --text--

セットデータ構造が保証することは何ですか？

#### --distractors--

要素はデフォルトでソート順に格納される。

---

重複値を許可し、まとめて保持する。

---

要素は挿入位置でインデックス付けされる。

#### --answer--

重複しない一意の要素のみを格納する。

### --question--

#### --text--

動的配列で末尾以外のインデックスiに要素を挿入する最悪時間計算量は何ですか？

#### --distractors--

`O(1)`

---

`O(log n)`

---

`O(1)`の償却時間

#### --answer--

`O(n)`

### --question--

#### --text--

単方向連結リストの先頭に新しいノードを挿入する時間計算量は何ですか？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

スタックから要素を削除する操作はどれですか？

#### --distractors--

`push`

---

`dequeue`

---

底に挿入する。

#### --answer--

`pop`

### --question--

#### --text--

キューを最もよく表す説明はどれですか？

#### --distractors--

後入れ先出し（`LIFO`）でトップから削除。

---

`O(1)`時間で任意のインデックスにランダムアクセス。

---

要素は常に自動的にソート順に保持される。

#### --answer--

先入れ先出し（`FIFO`）で、後ろで`enqueue`、前で`dequeue`を行う。

### --question--

#### --text--

ハッシュマップにおけるハッシュ衝突とは何ですか？

#### --distractors--

キーが設計上複数の異なる値にマップされる場合。

---

同じキーが異なるバケットに格納される場合。

---

マップのメモリが不足しサイズ変更が必要な場合。

#### --answer--

異なる2つのキーが同じハッシュインデックスを生成する場合。

### --question--

#### --text--

ハッシュマップが成長するにつれてリサイズ（再ハッシュ）する理由は何ですか？

#### --distractors--

高速な反復のためにキーを昇順にソートするため。

---

値を圧縮してメモリの断片化を減らすため。

---

言語のガベージコレクターを起動させないため。

#### --answer--

負荷率を低く保ち、平均操作時間を`O(1)`に維持するため。

### --question--

#### --text--

セットについて正しい記述はどれですか？

#### --distractors--

セットは挿入順を保持する。

---

セットは重複要素を許可し、カウントを保持する。

---

セットのメンバーシップテストは平均で`O(n log n)`。

#### --answer--

メンバーシップテストは通常平均で`O(1)`。

### --question--

#### --text--

nが大きくなるにつれて`O(n log n)`よりも速く増加する時間計算量はどれですか？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

ブルートフォースの解決策を実装した後の良い次のステップは何ですか？

#### --distractors--

定数係数を測定前にミクロ最適化する。

---

テストを破棄して解決策を最初から書き直す。

---

コードをシンプルに保つためにエッジケースを考慮しない。

#### --answer--

時間・空間計算量を分析し、特定したボトルネックを最適化する。

### --question--

#### --text--

空間計算量は何を測定しますか？

#### --distractors--

プログラムが使用するCPUコア数。

---

コード行数としてのプログラムの長さ。

---

プログラムのコンパイルにかかる時間。

#### --answer--

入力サイズに対してメモリ使用量がどのように増加するか。
