---
id: 6a298d88d1606c0f1ef322a9
title: اختبار التوثيق والتفويض
challengeType: 8
dashedName: quiz-authentication-and-authorization
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هي الأجزاء الثلاثة لرمز JSON Web Token؟

#### --distractors--

القسم العلوي، الجسم، والتذييل

---

الرمز، الحمولة، والهاش

---

المعرف، المطالبات، والشهادة

#### --answer--

القسم العلوي، الحمولة، والتوقيع

### --question--

#### --text--

أي مما يلي يعد مثالًا على "شيء تملكه" كعامل توثيق؟

#### --distractors--

مسح بصمة الإصبع.

---

كلمة مرور حفظتها في ذاكرتك.

---

إجابة على سؤال أمني.

#### --answer--

رمز لمرة واحدة يُرسل إلى هاتفك الذكي.

### --question--

#### --text--

أي مما يلي يصف التفويض بشكل أفضل؟

#### --distractors--

التحقق من تطابق بيانات اعتماد المستخدم مع السجلات المخزنة.

---

توليد رمز جلسة بعد تسجيل دخول المستخدم بنجاح.

---

تشفير كلمة مرور المستخدم قبل حفظها في قاعدة البيانات.

#### --answer--

تحديد الإجراءات التي يُسمح للمستخدم المُوثَّق باتخاذها.

### --question--

#### --text--

ما هي استراتيجية Passport.js؟

#### --distractors--

ملف إعدادات يربط مسارات التطبيق بقواعد التوثيق.

---

دالة مدمجة تقوم بتجزئة كلمات مرور المستخدمين وتخزينها بأمان.

---

رمز يُولد بعد إكمال المستخدم لتدفق تسجيل الدخول بنجاح.

#### --answer--

ملحق يحدد كيفية توثيق المستخدمين بطريقة أو مزود معين.

### --question--

#### --text--

ماذا يفعل Helmet.js للخاصية X-Powered-By افتراضيًا؟

#### --distractors--

يستبدل الخاصية بقيمة عنصر نائب عامة.

---

يشفر قيمة الخاصية قبل إرسالها إلى العميل.

---

ينقل معلومات الخاصية إلى ملف تعريف ارتباط HTTP-only.

#### --answer--

يزيل الخاصية X-Powered-By بالكامل.

### --question--

#### --text--

ماذا يفعل تعيين خاصية `SameSite` لملف تعريف الارتباط إلى `Strict`؟

#### --distractors--

يشفر ملف تعريف الارتباط قبل تخزينه في المتصفح.

---

يضمن إرسال ملف تعريف الارتباط فقط عبر اتصالات HTTPS.

---

يمنع الخادم من تعيين أكثر من ملف تعريف ارتباط واحد لكل استجابة.

#### --answer--

يمنع المتصفح من إرسال ملف تعريف الارتباط في أي طلب عبر المواقع.

### --question--

#### --text--

في JWT، أين تُخزن بيانات مطالبة المستخدم مثل معرف المستخدم أو الدور؟

#### --distractors--

في قسم التوقيع.

---

في القسم العلوي.

---

في حاوية مشفرة خارج الرمز.

#### --answer--

في قسم الحمولة.

### --question--

#### --text--

أي خاصية HTTP التي يحددها Helmet تمنع تضمين موقعك داخل iframe على نطاق آخر؟

#### --distractors--

سياسة أمان المحتوى

---

X-Powered-By

---

الأمان الصارم للنقل (Strict-Transport-Security)

#### --answer--

X-Frame-Options

### --question--

#### --text--

كيف يستغل هجوم CSRF المتصفح؟

#### --distractors--

يحقن جافاسكريبت في متصفح الضحية لسرقة ملفات تعريف الارتباط مباشرة.

---

يعترض حركة المرور بين متصفح الضحية والخادم الموثوق.

---

يرسل رابط تصيد يعيد توجيه الضحية إلى صفحة تسجيل دخول مزيفة.

#### --answer--

يخدع المتصفح لإرسال طلب يتضمن ملف تعريف جلسة الضحية تلقائيًا.

### --question--

#### --text--

ماذا يفعل `app.use(passport.initialize())` في تطبيق Express؟

#### --distractors--

يسجل استراتيجية توثيق جديدة مع التطبيق.

---

ينشئ ملف تعريف ارتباط جلسة لكل مستخدم يزور التطبيق.

---

يثبت تلقائيًا جميع استراتيجيات Passport المتاحة.

#### --answer--

يُعد Passport كبرمجيات وسيطة للتعامل مع التوثيق في كل طلب وارد.

### --question--

#### --text--

أي دور في نظام التفويض القائم على الأدوار عادةً ما يمتلك وصولًا كاملاً لإعدادات النظام وإدارة المستخدمين؟

#### --distractors--

محرر

---

مشرف

---

مستخدم عادي

#### --answer--

مسؤول

### --question--

#### --text--

أي أمر يثبت الحزمة الأساسية لـ Passport.js والاستراتيجية المحلية؟

#### --distractors--

`npm install passport.js passport-local`

---

`npm install passport passport-auth`

---

`npm install passportjs local-strategy`

#### --answer--

`npm install passport passport-local`

### --question--

#### --text--

لماذا يجب ألا تخزن كلمة مرور في حمولة JWT؟

#### --distractors--

الحمولة لها حد حجم يجعل تخزين سلاسل بطول كلمة المرور مستحيلًا.

---

تخزين كلمة مرور في الحمولة يبطل توقيع الرمز تلقائيًا.

---

رموز JWT تنتهي صلاحيتها بسرعة كبيرة لجعل كلمات المرور المخزنة فيها مفيدة.

#### --answer--

الحمولة مشفرة بـ Base64 وليست مشفرة فعليًا، لذا يمكن لأي شخص يحمل الرمز فك تشفيرها.

### --question--

#### --text--

ما الغرض من رمز CSRF؟

#### --distractors--

لتشفير ملف تعريف جلسة المستخدم قبل تخزينه في المتصفح.

---

للتحقق من هوية المستخدم في كل طلب API يُرسل إلى الخادم.

---

لاستبدال ملف تعريف الجلسة باعتماد توثيق قصير الأمد.

#### --answer--

لتأكيد أن الطلب جاء من الواجهة الأمامية الشرعية التي لا يستطيع موقع خبيث قراءتها.

### --question--

#### --text--

أي خاصية استجابة HTTP يحددها Helmet للدفاع ضد هجمات البرمجة النصية عبر المواقع (XSS)؟

#### --distractors--

X-Frame-Options

---

X-Powered-By

---

الأمان الصارم للنقل

#### --answer--

سياسة أمان المحتوى

### --question--

#### --text--

عندما يحاول المستخدم الوصول إلى مورد محمي، أي عملية يجب أن تحدث أولًا؟

#### --distractors--

التفويض

---

تعيين الدور

---

إنشاء الجلسة

#### --answer--

التوثيق

### --question--

#### --text--

ماذا يربط Passport.js بكائن الطلب بعد توثيق المستخدم بنجاح؟

#### --distractors--

رمز الجلسة النشط كـ `req.sessionToken`.

---

اسم الاستراتيجية المستخدمة لتسجيل الدخول كـ `req.strategy`.

---

كلمة مرور المستخدم المجزأة كـ `req.credentials`.

#### --answer--

كائن المستخدم المُوثَّق كـ `req.user`.

### --question--

#### --text--

ماذا يحدث عندما يستقبل الخادم JWT تم التلاعب بحمولته؟

#### --distractors--

يقوم الخادم بفك تشفير الحمولة ويحدث سجل المستخدم بالبيانات الجديدة.

---

يتجاهل الخادم البيانات المتلاعب بها بصمت ويستخدم مطالبات الرمز الأصلية.

---

يحذف الخادم الرمز ويرسل المستخدم إلى صفحة التسجيل.

#### --answer--

التوقيع لن يتطابق ويرفض الخادم الطلب.

### --question--

#### --text--

مقارنة بـ `SameSite=Strict`، ماذا يسمح `SameSite=Lax`؟

#### --distractors--

يسمح بملف تعريف الارتباط في جميع الطلبات عبر المواقع، بما في ذلك إرسال النماذج.

---

يمنع ملف تعريف الارتباط في جميع الطلبات التي تنشأ من خارج نفس النطاق.

---

يتطلب من المستخدم تسجيل الدخول مرة أخرى عند أي تنقل عبر المواقع.

#### --answer--

يسمح بملف تعريف الارتباط في التنقل على المستوى الأعلى لكنه يمنعه في إرسال النماذج وطلبات API عبر المواقع.

### --question--

#### --text--

ماذا يفرض رأس Strict-Transport-Security عند تعيينه بواسطة Helmet؟

#### --distractors--

يجبر الخادم على رفض جميع الطلبات غير الآمنة (غير HTTPS) برد خطأ.

---

يشفر كامل جسم استجابة HTTP قبل إرساله إلى المتصفح.

---

يتطلب تعيين علامات Secure و HttpOnly لجميع ملفات تعريف الارتباط.

#### --answer--

يجبر المتصفح على التواصل مع الخادم عبر HTTPS فقط.
