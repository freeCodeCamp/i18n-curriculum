---
id: 5900f37f1000cf542c50fe92
title: '문제 19: 일요일 세기'
challengeType: 1
forumTopicId: 301827
dashedName: problem-19-counting-sundays
---

# --description--

다음 정보를 드리지만, 직접 조사해 보셔도 좋습니다.

<ul>
  <li>1900년 1월 1일은 월요일이었습니다.</li>
  <li>9월, 4월, 6월, 11월은 30일입니다.<br>나머지는 모두 31일이고,<br>2월만 예외로,<br>평년에는 28일,<br>윤년에는 29일입니다.</li>
  <li>윤년은 4로 나누어떨어지는 해이지만, 100년 단위는 제외하며, 단 400으로 나누어떨어지는 해는 윤년입니다.</li>
</ul>

20세기(1901년 1월 1일부터 2000년 12월 31일까지) 동안 매달 첫째 날이 일요일인 경우가 몇 번 있었나요?

# --hints--

`countingSundays(1943, 1946)`는 숫자를 반환해야 합니다.

```js
assert.isNumber(countingSundays(1943, 1946));
```

`countingSundays(1943, 1946)`는 6을 반환해야 합니다.

```js
assert.strictEqual(countingSundays(1943, 1946), 6);
```

`countingSundays(1995, 2000)`는 10을 반환해야 합니다.

```js
assert.strictEqual(countingSundays(1995, 2000), 10);
```

`countingSundays(1901, 2000)`는 171을 반환해야 합니다.

```js
assert.strictEqual(countingSundays(1901, 2000), 171);
```

# --seed--

## --seed-contents--

```js
function countingSundays(firstYear, lastYear) {

  return true;
}

countingSundays(1943, 1946);
```

# --solutions--

```js
function countingSundays(firstYear, lastYear) {
  let sundays = 0;

  for (let year = firstYear; year <= lastYear; year++) {
    for (let month = 0; month <= 11; month++) {
      const thisDate = new Date(year, month, 1);
      if (thisDate.getDay() === 0) {
        sundays++;
      }
    }
  }
  return sundays;
}
```
