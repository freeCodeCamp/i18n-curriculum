---
id: 69fd84e01123960e15bb8455
title: Python 安裝測驗
challengeType: 8
dashedName: quiz-python-installation
---

# --description--

要通過此測驗，你必須在以下 10 題中正確回答至少 9 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

在哪裡下載 Windows、Mac 和 Linux 版本的 Python 最合適？

#### --distractors--

從 GitHub 倉庫下載

---

從 npm 套件管理器下載

---

從 pip 管理器下載

#### --answer--

從官方 Python 網站下載

### --question--

#### --text--

在程式設計的背景下，什麼是終端機？

#### --distractors--

用於設計應用程式的圖形工具。

---

用於程式碼檢查和格式化的工具。

---

具有自動完成功能的 Python 程式碼編輯器。

#### --answer--

用於執行指令的文字介面。

### --question--

#### --text--

IDE 是什麼縮寫？

#### --distractors--

網際網路開發引擎

---

整合 DevOps 環境

---

內部偵錯執行器

#### --answer--

整合開發環境

### --question--

#### --text--

以下哪些是常見的 Python IDE 或程式碼編輯器？

#### --distractors--

JetBrains、CLion、WindSurf

---

Photoshop、Illustrator、Figma

---

Clang、Pip、Deno

#### --answer--

VS Code、PyCharm、Spyder

### --question--

#### --text--

通常如何從終端機執行名為 `main.py` 的 Python 檔案？

#### --distractors--

`run main.py`

---

`pip main.py`

---

`execute main.py python`

#### --answer--

`python main.py`

### --question--

#### --text--

什麼是互動式 shell？

#### --distractors--

一個用來審核程式碼錯誤的程式。

---

一個用來對程式碼執行測試的程式。

---

一個用來檢查程式碼格式的程式。

#### --answer--

一個讓你一次輸入一個指令並看到結果的程式。

### --question--

#### --text--

如何從終端機啟動 Python 互動式 shell？

#### --distractors--

輸入 `node python` 並按下 `Enter`。

---

輸入 `run python` 並按下 `Enter`。

---

輸入 `start python shell` 並按下 `Enter`。

#### --answer--

輸入 `python` 並按下 `Enter`。

### --question--

#### --text--

Python 互動式 shell 中的 `>>>` 符號代表什麼意思？

#### --distractors--

Python 已停止執行。

---

Python 正在測試你的程式碼。

---

Python 正在安裝套件。

#### --answer--

Python 正在等待使用者輸入文字。

### --question--

#### --text--

REPL 是什麼縮寫？

#### --distractors--

執行執行程序迴圈

---

讀取評估列印列表

---

執行期執行程式語言

#### --answer--

讀取評估列印迴圈

### --question--

#### --text--

當你在 Python 互動式 shell 執行 `print("Hello, world!")` 時會發生什麼？

#### --distractors--

它會持續列印 `"Hello, world!"`，直到使用者停止。

---

它會建立一個檔案並在終端機列印 `"Hello, world!"`。

---

它會丟出錯誤，因為那不是有效的 Python 程式碼。

#### --answer--

它會在終端機列印 `"Hello, world!"`。
