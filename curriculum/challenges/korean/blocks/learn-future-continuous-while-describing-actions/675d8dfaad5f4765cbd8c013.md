---
id: 675d8dfaad5f4765cbd8c013
title: 작업 64
challengeType: 19
dashedName: task-64
lang: en-US
---

# --instructions--

이 도전 과제에는 오디오가 없습니다. 아래 질문을 읽고 올바른 답을 선택하세요.

# --questions--

## --text--

어떤 문장이 `don't`를 태그 질문에서 올바르게 사용했나요?

## --answers--

`She likes coffee, don't she?`

### --feedback--

주어 `she`에는 `doesn't`가 필요하며 `don't`가 아닙니다.

---

`We need to resolve this quickly, don't we?`

---

`They know the answer, don't it?`

### --feedback--

주어 `they`에는 `don't they`가 필요하며 `don't it`가 아닙니다.

---

`The system is working fine, don't it?`

### --feedback--

주어 `it`에는 `doesn't`가 필요하며 `don't`가 아닙니다.

## --video-solution--

2

# --explanation--

태그 질문은 주절의 주어와 조동사에 맞아야 합니다.
