---
id: 68c3420cb0176439a95af1b8
title: 步骤 3
challengeType: 0
dashedName: step-3
---

# --description--

下一步是添加表格头。 

首先在 `caption` 元素下方添加一个 `thead` 元素。在 `thead` 内添加一个 `tr` 元素。 

# --hints--

你应该有一个 `thead` 元素。

```js
assert.exists(document.querySelector("table thead"));
```

你应该在 `thead` 内有一个 `tr` 元素。

```js
assert.exists(document.querySelector("table thead tr"));
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Tech Conference 2025 Schedule</title>
</head>
<body>
  <h1>Tech Conference 2025 Schedule</h1>

  <table>
    <caption>Schedule by Track and Time</caption>

    --fcc-editable-region--
        
    --fcc-editable-region--
  </table>
</body>
</html>
```
