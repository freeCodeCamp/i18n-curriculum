---
id: 66edc3ab8c6413c344f401bf
title: JavaScript 数学测验
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

`NaN` 代表什么？

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

以下哪项不是 JavaScript 中的算术运算符？

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

在 JavaScript 中尝试除以零会发生什么？

#### --distractors--

JavaScript 将输出 0。

---

什么都不会发生。

---

JavaScript 会报错。

#### --answer--

JavaScript 会输出 `Infinity`。

### --question--

#### --text--

以下哪项是指数运算符的正确用法？

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

在 JavaScript 中自减变量时应使用哪个运算符？

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

以下哪项不是位运算符？

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

下面哪项是正确的求余数的复合运算符？

#### --distractors--

`//=`

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

`==` 和 `===` 运算符的区别是什么？

#### --distractors--

没区别。 比较结果总是相同的。

---

`==` 运算符用于检查数据类型是否相同，而 `===` 则用于检查值是否相等。

---

`==` 运算符只能用于原始类型，而 `===` 运算符只用于对象。

#### --answer--

`==` 运算符只比较值，而 `===` 运算符则比较值和类型。

### --question--

#### --text--

一元正运算符（`+`）有什么用途？

#### --distractors--

用于将数值减 1。

---

用于获取运算的余数。

---

用于反转数字的二进制表示。

#### --answer--

用于将操作数转换成数字。

### --question--

#### --text--

以下哪个 `Math` 方法会将数值四舍五入为最接近的整数？

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

`Math.sqrt()` 方法的作用是什么？

#### --distractors--

返回一个随机浮点数。

---

返回一个数字的绝对值。

---

返回一个数字的立方。

#### --answer--

返回一个数字的平方根。

### --question--

#### --text--

以下哪项不是 `Math` 对象的有效方法？

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

以下哪项会返回 `false`？

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

`parseFloat()` 方法的作用是什么？

#### --distractors--

解析字符串参数并对其执行位运算。

---

解析字符串参数并返回 `false` 。

---

解析字符串参数并返回整数。

#### --answer--

解析字符串参数并返回浮点数。

### --question--

#### --text--

控制台将记录什么内容？

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

下面代码的输出是什么？

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

什么是运算符优先级？

#### --distractors--

用于检查左边的值是否小于右边的值。

---

用于将变量提升为指定数字的幂，并将结果重新分配给变量。

---

操作符优先级用于将值增加 1。

#### --answer--

决定表达式中运算的求值顺序。

### --question--

#### --text--

以下哪项不是比较运算符？

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

一元负运算符（`-`）有什么作用？

#### --distractors--

它会翻转操作数的布尔值。

---

将数值减一。

---

将数值加一。

#### --answer--

将操作数取负。

### --question--

#### --text--

以下哪项是使用空值合并运算符的示例？

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```

