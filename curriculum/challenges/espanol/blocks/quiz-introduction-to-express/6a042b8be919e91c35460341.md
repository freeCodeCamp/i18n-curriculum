---
id: 6a042b8be919e91c35460341
title: Introducción al quiz de Express
challengeType: 8
dashedName: quiz-introduction-to-express
---

# --description--

Para aprobar el quiz, debes responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Cuál es la relación entre una API y un servicio web?

#### --distractors--

Cada API es un servicio web, pero no todo servicio web es una API.

---

Las APIs y los servicios web son conceptos idénticos.

---

Los servicios web solo funcionan con REST, mientras que las APIs soportan múltiples protocolos.

#### --answer--

Cada servicio web es una API, pero no toda API es un servicio web.

### --question--

#### --text--

¿Qué encabezado HTTP usa un cliente para indicar al servidor qué formato de respuesta desea?

#### --distractors--

`Content-Type`

---

`Authorization`

---

`X-Requested-With`

#### --answer--

`Accept`

### --question--

#### --text--

¿Qué significa que una arquitectura REST sea sin estado?

#### --distractors--

El servidor almacena los datos de sesión del cliente entre solicitudes.

---

El cliente no mantiene ningún estado propio entre solicitudes.

---

REST restringe todas las solicitudes a operaciones de solo lectura.

#### --answer--

El servidor no guarda memoria de solicitudes previas.

### --question--

#### --text--

¿Qué método HTTP de REST se usa para eliminar un recurso?

#### --distractors--

`GET`

---

`POST`

---

`PUT`

#### --answer--

`DELETE`

### --question--

#### --text--

¿Cuál es una ventaja clave de los microservicios frente a un monolito?

#### --distractors--

Depuración más sencilla y menos llamadas de red entre servicios.

---

Una base de código compartida única a la que todos los equipos contribuyen.

---

Gestión de infraestructura más fácil en todos los servicios.

#### --answer--

Desarrollo más rápido, mejor aislamiento de fallos y la capacidad de usar diferentes lenguajes por servicio.

### --question--

#### --text--

¿Qué devuelve llamar a `express()`?

#### --distractors--

Un nuevo servidor HTTP.

---

Un objeto router.

---

Una función middleware.

#### --answer--

Una nueva instancia de aplicación Express.

### --question--

#### --text--

¿Qué es middleware en Express?

#### --distractors--

Un método integrado usado para iniciar el servidor Express.

---

Un tipo especial de método HTTP usado en APIs REST.

---

Una herramienta integrada para servir archivos estáticos desde un directorio.

#### --answer--

Código que se ejecuta entre recibir una solicitud y enviar una respuesta.

### --question--

#### --text--

¿Qué hace `app.listen(port, callback)` en Express?

#### --distractors--

Define una ruta para solicitudes entrantes.

---

Carga la librería Express.

---

Crea una nueva función middleware.

#### --answer--

Inicia el servidor y comienza a escuchar solicitudes HTTP entrantes.

### --question--

#### --text--

¿Qué es un parámetro de ruta en Express?

#### --distractors--

Una ruta URL fija como `/home`.

---

Una cadena de consulta agregada a la URL.

---

Un método para servir archivos estáticos.

#### --answer--

Un marcador de posición en la URL con prefijo `:` que captura un valor dinámico.

### --question--

#### --text--

¿Cómo accedes al valor dinámico capturado de una URL como `/post/42` en una ruta Express definida como `/post/:postId`?

#### --distractors--

`req.query.postId`

---

`req.body.postId`

---

`req.url.postId`

#### --answer--

`req.params.postId`

### --question--

#### --text--

¿Qué hace `res.json()` en Express?

#### --distractors--

Envía una respuesta en texto plano.

---

Redirecciona al cliente a una URL diferente.

---

Renderiza una vista con un motor de plantillas y envía el HTML resultante.

#### --answer--

Envía una respuesta JSON y convierte automáticamente un objeto JavaScript.

### --question--

#### --text--

¿Qué hace `res.redirect(url)` en Express?

#### --distractors--

Establece el código de estado HTTP.

---

Envía una respuesta JSON.

---

Renderiza una vista usando un motor de plantillas.

#### --answer--

Redirecciona al cliente a una URL diferente.

### --question--

#### --text--

¿Cuál es el propósito de `app.route()` en Express?

#### --distractors--

Crear una nueva instancia de la aplicación Express.

---

Servir archivos estáticos desde un directorio especificado.

---

Definir rutas separadas para diferentes rutas URL.

#### --answer--

Encadenar múltiples manejadores de métodos HTTP para la misma ruta en un solo bloque.

### --question--

#### --text--

¿Qué crea `express.Router()`?

#### --distractors--

Una nueva instancia de aplicación Express que reemplaza a `app` para manejar toda la lógica del servidor.

---

Una función middleware dedicada a capturar y manejar errores a nivel de ruta.

---

Un servidor integrado de archivos estáticos que mapea automáticamente archivos a rutas URL.

#### --answer--

Un objeto router, similar a una mini app Express, en el que defines rutas y montas en la aplicación principal.

### --question--

#### --text--

¿Cómo montas un router en la app principal de Express?

#### --distractors--

Llamando a `router.listen()`.

---

Llamando a `router.start()`.

---

Llamando a `app.get()` directamente dentro del router.

#### --answer--

Llamando a `app.use(path, router)`.

### --question--

#### --text--

¿Qué middleware integrado de Express se usa para servir archivos estáticos?

#### --distractors--

`express.json()`

---

`express.urlencoded()`

---

`express.raw()`

#### --answer--

`express.static()`

### --question--

#### --text--

¿Qué sucede cuando un usuario solicita un archivo estático que no existe en el directorio configurado con `express.static()`?

#### --distractors--

Express lanza un error y el servidor se cae.

---

Express redirecciona al usuario a la página principal.

---

Express crea un archivo vacío en la ruta solicitada.

#### --answer--

Express responde automáticamente con un 404.

### --question--

#### --text--

¿Cuál de las siguientes afirmaciones es cierta sobre SOAP comparado con REST?

#### --distractors--

SOAP usa JSON y es más flexible que REST, lo que lo hace ideal para aplicaciones modernas.

---

SOAP es el tipo de servicio web más usado hoy en día, habiendo reemplazado a REST en la mayoría de sistemas.

---

SOAP soporta múltiples formatos de respuesta, incluyendo texto plano, JSON y datos binarios.

#### --answer--

SOAP usa solo XML y es común en sistemas empresariales que necesitan estándares y seguridad más estrictos.

### --question--

#### --text--

¿Qué es un manejador de ruta en Express?

#### --distractors--

Un objeto de configuración que mapea todas las rutas disponibles a sus métodos HTTP.

---

Una función middleware que intercepta automáticamente y sirve archivos estáticos desde una carpeta.

---

Un conector de base de datos que procesa y maneja consultas relacionadas con rutas.

#### --answer--

Una función callback que se ejecuta cuando una solicitud coincide con una ruta, con acceso a `req` y `res`.

### --question--

#### --text--

¿Cuál es un beneficio de usar `express.Router()` en lugar de definir todas las rutas directamente en `app.js`?

#### --distractors--

Permite que el servidor maneje muchas más solicitudes HTTP concurrentes a la vez.

---

Genera y registra automáticamente endpoints REST API basados en los nombres de archivos de ruta.

---

Permite que el servidor inicie y se ejecute sin necesidad de especificar un número de puerto.

#### --answer--

Separa responsabilidades y mantiene limpio el archivo principal moviendo rutas relacionadas a sus propios módulos.
