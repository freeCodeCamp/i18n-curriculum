---
id: 66ed8fe7f45ce3ece4053eb2
title: CSS Flexbox 測驗
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是 flexbox？

#### --distractors--

一個用於佈局的二維模型。

---

一個用於佈局的三維模型。

---

一個用於版面配置的多維模型。

#### --answer--

一維佈局模型。

### --question--

#### --text--

設定哪個 CSS 屬性以啟用 `div` 元素的彈性盒子佈局？

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

哪個 CSS 屬性讓 flex 項目在目前的列已滿時換行到新列？

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

`justify-content` 這個 flexbox 屬性控制什麼？

#### --distractors--

它控制 `flexbox` 容器子元素的方向。

---

它控制 `flexbox` 容器子元素的文字樣式。

---

它控制在彈性容器中子元素的對齊方式。

#### --answer--

它控制子元素在主軸上的分佈。

### --question--

#### --text--

哪個屬性控制 `flexbox` 容器內元素的方向？

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

哪些 CSS 屬性可以用來讓 flex 項目從左到右排列，並且當目前列的寬度超過時，新的 flex 項目會出現在前一個項目的上方？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

哪個屬性控制元素在交叉軸上的定位？

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

哪一個不是 `justify-content` 的值？

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

你會使用哪些 CSS 屬性來在 `flex container` 中水平和垂直置中一個元素？

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

`flex-direction` 的什麼值會讓彈性項目從右向左排列？  

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

`flex-wrap` 屬性中不屬於的值是什麼？

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

`align-items` 中的 `stretch` 值有什麼作用？

#### --distractors--

項目直接緊貼彼此，朝向容器的起始邊緣排列。

---

項目對齊，使其彈性容器的基線對齊。

---

項目直接緊貼彼此包裝，朝向容器的末端邊緣。

#### --answer--

自動調整項目大小，使其同等大以填滿容器。

### --question--

#### --text--

哪個 `justify-content` 值會將第一個項目置於起點，最後一個項目置於終點，並在主軸上均勻分布其他項目？

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

哪個 `justify-content` 值會將項目間每一對相鄰項目與容器邊緣之間的間距全部設為相等？

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

哪一個 `justify-content` 值會沿主軸將項目平均分隔，同時在容器邊緣留下一半該間距？

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

以下哪一項會導致條款在交叉軸的起點對齊？

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

以下哪一項會導致條款在交叉軸的末端對齊？

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

哪個 `flex-direction` 的值會使項目沿主軸從左到右排列？

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

哪個 `flex-direction` 的值會使項目沿著交叉軸從上到下排列？

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

以下哪一個使用情境不適合使用 flexbox？

#### --distractors--

將項目置中於容器中。

---

列中項目的垂直對齊。

---

動態調整項目的分佈和對齊以適應不同的視窗。

#### --answer--

固定大小和項目之間的間距，如同網格。

## --quiz--

### --question--

#### --text--

像 Flexbox 這樣的一維佈局模型的目的為何？

#### --distractors--

要同時處理二維佈局，包括列和行。

---

要使用 `z-index` 控制定位元素的堆疊順序。

---

在頁面元素上創建 3D 變形和複雜動畫。

#### --answer--

將條款沿單一行或軸排列，無論是作為一列還是一行。

### --question--

#### --text--

哪一個 CSS 屬性會將所有的彈性項目對齊到 `.container` 元素的右側？

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Flex 模型中定義項目佈局方向的兩個軸是什麼？

#### --distractors--

水平軸和垂直軸。

---

x 軸和 y 軸。

---

主軸和次軸。

#### --answer--

主軸和交叉軸。

### --question--

#### --text--

以下哪一個 CSS 規則會將項目排列成垂直行，並且在其容器中水平置中？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

`flex-wrap` 屬性的主要用途是什麼？

#### --distractors--

它設定了彈性項目在彈性容器中排列的方向。

---

它沿著容器的主軸對齊彈性項目。

---

它沿著容器的交叉軸對齊彈性項目。

#### --answer--

它決定 `flex` 項目是否應換行以適應可用空間。

### --question--

#### --text--

`flex-wrap: nowrap;` 宣告的預設行為是什麼？

#### --distractors--

如果彈性項目對容器來說太寬，則強制它們換行到新的一行。

---

它會將項目換行，但順序相反。

---

它可防止彈性容器大於其父元素。

#### --answer--

它強制 flex 項目保持在單一行，即使它們超出容器的上限溢位。

### --question--

#### --text--

哪一個 CSS 屬性是同時設定 `flex-direction` 和 `flex-wrap` 屬性的縮寫？

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

將 `flex-direction: column-reverse;` 設定在彈性容器上會有什麼效果？

#### --distractors--

它將項目從右向左水平排列。

---

它會反轉一列中的項目，但保持它們在同一行。

---

它將項目由上而下垂直排列。

#### --answer--

它將項目由下而上以反向順序垂直排列。

### --question--

#### --text--

哪一段 CSS 程式碼能正確地將彈性條款垂直排列成行，並且當它們超出容器高度時允許換行到新的行？

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

哪一個 `justify-content` 值會將所有彈性項目對齊到容器主軸的末端？

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

以下 CSS 規則對 flex 項目的影響是什麼？

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

項目排列在一個行中，並且會換行到新的行。

---

項目排列在一列中，並會換行到新的一列。

---

項目排列在一個行中，如果不適合則會發生上限溢位。

#### --answer--

項目排列在一列中，如果不適合，將會超出容器的上限溢位。

### --question--

#### --text--

`justify-content: space-evenly;` 如何對齊彈性項目？

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

項目均勻分布，起始和結尾處沒有空格。

---

項目被集中包裝在容器的中央。

---

項目被推到容器的右側，且彼此之間保持相等間距。

#### --answer--

項目在它們之間以及容器兩端有相等的間距。

### --question--

#### --text--

當設定 `flex-direction: row;` 時，你會使用哪個屬性來垂直置中項目？

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

`justify-content` 屬性的 `flex-start` 值會做什麼？

#### --distractors--

它將項目對齊到交叉軸的起點。

---

它會拉伸項目以填滿容器的主軸。

---

它會沿著主軸將所有項目置中。

#### --answer--

它將項目對齊到容器主軸的起點。

### --question--

#### --text--

你要如何設定一個 flex 容器，使其項目在單行中從右到左排列，並且將它們定位在容器的頂部？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

以下哪一項是 `flex-flow` 屬性的有效宣告？

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

以下哪一條 CSS 規則會使 flex 項目在水平方向置中，並在垂直方向拉伸以填滿容器的高度？

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

當 `flex-direction` 設為 `column` 時，`align-items: stretch;` 會有什麼效果？

#### --distractors--

它會垂直拉伸項目以填滿容器的高度。

---

當方向為 `column` 時，沒有任何效果。

---

它會垂直拉伸項目以符合最高的項目。

#### --answer--

它會水平拉伸項目以填滿容器的寬度。

### --question--

#### --text--

如果你想反轉水平列中項目的順序，應該使用哪個宣告？

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

`flex-direction` 為 `row` 的容器中，哪個 `align-items` 的值會將項目置於頂部？

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
