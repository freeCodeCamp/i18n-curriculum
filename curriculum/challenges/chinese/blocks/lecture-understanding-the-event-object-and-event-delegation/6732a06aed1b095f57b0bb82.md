---
id: 6732a06aed1b095f57b0bb82
title: 什么是 `change` 事件，它是如何工作的？
challengeType: 19
dashedName: what-is-the-change-event-and-how-does-it-work
---

# --interactive--

`change` 事件是一种特殊事件，当用户修改某些输入元素的值时触发。更具体地说：

- 当复选框被选中或取消选中时。

- 当单选按钮被选中时。

- 当用户从类似日期选择器或下拉菜单的控件中进行选择时。

- 当输入失去焦点（用户切换到下一个字段的标签（页），或点击表单外部）且用户已更改值时。

- 当用户通过输入一些文本后按回车键等方式确认该值时。

请注意，用户在输入时不会触发 `change` 事件。只有当他们聚焦到另一个元素后，才会触发 `change` 事件。

下面是使用带有下拉菜单的 `change` 事件的示例：

:::interactive_editor

```html
<select id="select-menu">
  <option value="option1">Option 1</option>
  <option value="option2">Option 2</option>
  <option value="option3">Option 3</option>
</select>
<script src="index.js"></script>
```

```js
const selectMenu = document.getElementById("select-menu");
selectMenu.addEventListener("change", (event) => {
  console.log(`You selected: ${event.target.value}`);
});
```

:::

每次用户从下拉菜单中选择不同的选项时，都会触发 `change` 事件，并且所选的值将被记录到控制台。

`change` 事件仍然会生成一个 `Event` 对象，但与大多数其他事件不同，它不会生成自定义实现——你只能访问基于 `Event` 对象的属性和方法。

这与 `input` 事件不同，后者会生成一个专用的 `InputEvent` 对象。`change` 事件在某些方面也有所不同。例如，当用户在字段中输入内容时，会触发 `input` 事件。

这些差异很重要，需要记住，因为你可能会被这些事件触发的时机绊倒。

# --questions--

## --text--

`change` 事件何时触发于文本输入元素？

## --answers--

一旦用户开始 `typing`。

### --feedback--

课程提到文本输入的一个特定条件，这与即时类型指派不同。

---

每次输入一个字符。

### --feedback--

课程提到文本输入的一个特定条件，这与即时类型指派不同。

---

当 `input` 的值被修改后失去焦点时。

---

只有当用户按下 `Enter` 密钥时。

### --feedback--

课程提到文本输入的一个特定条件，这与即时类型指派不同。

## --video-solution--

3

## --text--

以下哪项不会触发 `change` 事件？

## --answers--

从下拉菜单中选择一个选项。

### --feedback--

本课讨论了 `change` 事件的几个触发器，但提到有一个常见操作不会立即触发它。

---

在不离开 `input` 字段的情况下输入 `text`。

---

勾选复选框。

### --feedback--

本课讨论了 `change` 事件的几个触发器，但提到有一个常见操作不会立即触发它。

---

从日期选择器中选择日期。

### --feedback--

本课讨论了 `change` 事件的几个触发器，但提到有一个常见操作不会立即触发它。

## --video-solution--

2

## --text--

`change` 事件的 `Event` 对象与 `input` 事件的 `Event` 对象有何不同？

## --answers--

`change` 事件比 `input` 事件具有更多属性。

### --feedback--

本课提到这两个 `Event` 对象实现中的具体差异。

---

`change` 事件使用了 `Event` 对象的自定义实现

### --feedback--

本课提到这两个 `Event` 对象实现中的具体差异。

---

`input` 事件使用专用的 `InputEvent` 对象，而 `change` 事件使用基础的 `Event` 对象

---

这两个 `Event` 对象之间没有区别。

### --feedback--

本课提到这两个 `Event` 对象实现中的具体差异。

## --video-solution--

3
