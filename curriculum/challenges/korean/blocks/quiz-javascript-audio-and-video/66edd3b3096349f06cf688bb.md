---
id: 66edd3b3096349f06cf688bb
title: JavaScript 오디오 및 비디오 퀴즈
challengeType: 8
dashedName: quiz-javascript-audio-and-video
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript에서 `Audio()` 생성자의 목적은 무엇인가요?

#### --distractors--

새 오디오 파일을 만듭니다.

---

기존 오디오 파일을 재생합니다.

---

오디오 파일을 수정합니다.

#### --answer--

새 `audio` 요소를 만듭니다.

### --question--

#### --text--

`audio` 또는 `video` 요소의 재생을 시작하는 데 사용되는 메서드는 무엇인가요?

#### --distractors--

`start()`

---

`begin()`

---

`resume()`

#### --answer--

`play()`

### --question--

#### --text--

미디어 요소에 대한 `pause()` 메서드와 `play()` 메서드의 차이점은 무엇인가요?

#### --distractors--

`pause()`은 프로미스를 반환하지만 `play()`은 반환하지 않습니다.

---

`pause()`은 재개할 수 없지만 `play()`은 어느 위치에서든 시작할 수 있습니다.

---

`pause()`은 자동으로 처음으로 리셋되지만 `play()`는 오디오가 일시정지된 위치에서 시작합니다.

#### --answer--

`pause()`은 재생을 정지하지만 현재 위치를 유지하고 `play()`은 재생을 시작합니다.

### --question--

#### --text--

가장 넓은 브라우저 지원을 받는 오디오 형식은 무엇인가요?

#### --distractors--

`FLAC`

---

`OGG`

---

`WAV`

#### --answer--

`MP3`

### --question--

#### --text--

`source` 또는 `audio` 요소 내에서 `video` 요소의 목적은 무엇인가요?

#### --distractors--

미디어의 볼륨을 지정합니다.

---

미디어에 자막을 추가합니다.

---

외부 오디오 프로세서에 링크합니다.

#### --answer--

다양한 형식에 대해 여러 미디어 소스를 지정합니다.

### --question--

#### --text--

MP4 형식이 가질 수 있는 두 가지 가능한 MIME 유형은 무엇인가요?

#### --distractors--

`mp4/audio` 및 `mp4/video`

---

`audio/mpeg4` 및 `video/mpeg4`

---

`mp4/mpeg` 및 `mp4/stream`

#### --answer--

`audio/mp4` 및 `video/mp4`

### --question--

#### --text--

오디오 및 비디오 맥락에서 코덱이 하는 역할은 무엇인가요?

#### --distractors--

미디어의 볼륨을 높입니다.

---

미디어의 파일 형식을 변경합니다.

---

보안 전송을 위해 미디어를 암호화합니다.

#### --answer--

미디어를 아날로그와 디지털 형식 간에 변환합니다.

### --question--

#### --text--

웹 애플리케이션에서 오디오를 생성, 처리 및 분석하는 방법을 제공하는 API는 무엇인가요?

#### --distractors--

오디오 처리 API.

---

사운드 조작 API.

---

미디어 스트림 API.

#### --answer--

웹 오디오 API.

### --question--

#### --text--

`getUserMedia()` 메서드의 목적은 무엇인가요?

#### --distractors--

미디어 서비스 사용자 인증.

---

미디어 재생에 대한 사용자 환경 설정 가져오기.

---

사용자 기기로 미디어 파일 다운로드.

#### --answer--

사용자의 카메라 및/또는 마이크에 접근합니다.

### --question--

#### --text--

`HTMLMediaElement` 인터페이스의 어떤 속성이 현재 재생 시간을 나타내나요?

#### --distractors--

`time`

---

`playbackPosition`

---

`currentPosition`

#### --answer--

`currentTime`

### --question--

#### --text--

다음 코드 스니펫은 무엇을 하나요?

```javascript
const audio = new Audio('song.mp3');
audio.loop = true;
```

#### --distractors--

오디오 파일을 한 번 재생합니다.

---

오디오 볼륨을 높입니다.

---

한 번 반복 후 오디오를 일시정지합니다.

#### --answer--

오디오를 무한 반복하도록 설정합니다.

### --question--

#### --text--

MIME의 전체 의미는 무엇인가요?

#### --distractors--

다중 인터넷 메시지 확장

---

메일 인터넷 멀티미디어 확장

---

멀티미디어 인터넷 메일 확장

#### --answer--

다목적 인터넷 메일 확장

### --question--

#### --text--

`canPlayType()` 메서드는 무엇을 도와주나요?

#### --distractors--

특정 형식의 오디오가 브라우저에서 현재 재생 중인지 확인합니다.

---

브라우저 캐시에 미디어 파일이 존재하고 재생 가능한지 확인합니다.

---

오디오 요소에 컨트롤이 활성화되어 있고 사용자가 상호작용 권한이 있는지 테스트합니다.

#### --answer--

브라우저가 특정 오디오 형식을 재생할 수 있을 가능성을 판단합니다.

### --question--

#### --text--

다음 중 `HTMLMediaElement` API의 유효하지 않은 이벤트는 무엇인가요?

#### --distractors--

`waiting`

---

`canplay`

---

`ended`

#### --answer--

`stopped`

### --question--

#### --text--

데이터 버퍼링으로 인해 재생이 일시정지될 때 발생하는 `HTMLMediaElement` 이벤트는 무엇인가요?

#### --distractors--

`stopped`

---

`pause`

---

`buffering`

#### --answer--

`waiting`

### --question--

#### --text--

오디오 처리 그래프를 나타내는 데 사용되는 웹 오디오 API 인터페이스는 무엇인가요?

#### --distractors--

`AudioNode`

---

`AudioBuffer`

---

`AudioListener`

#### --answer--

`AudioContext`

### --question--

#### --text--

Media Source Extensions API의 목적은 무엇인가요?

#### --distractors--

비디오에 시각 효과를 추가합니다.

---

다양한 미디어 형식 간 변환을 수행합니다.

---

JavaScript로 여러 오디오 트랙을 동기화합니다.

#### --answer--

사용자의 웹캠 피드를 비디오 요소에 직접 전달할 수 있게 합니다.

### --question--

#### --text--

`ended` 이벤트는 언제 발생하나요?

#### --distractors--

미디어 소스가 변경될 때.

---

`pause()` 메서드가 호출될 때.

---

미디어가 수동으로 정지될 때.

#### --answer--

미디어의 끝에 도달했을 때.

### --question--

#### --text--

스크린 캡처 API는 어떻게 노출되나요?

#### --distractors--

`window.screenCapture` 인터페이스를 통해

---

`navigator.capture` 속성을 사용하여

---

`captureScreen()` 메서드를 사용하여

#### --answer--

`getDisplayMedia()` 메서드를 호출하여.

### --question--

#### --text--

WMV 형식은 무엇과 관련이 있나요?

#### --distractors--

와이드 미디어 비디오

---

월드 미디어 비디오

---

웹 미디어 플레이어

#### --answer--

Windows Media Player
