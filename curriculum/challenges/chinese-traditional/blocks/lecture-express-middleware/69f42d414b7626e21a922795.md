---
id: 69f42d414b7626e21a922795
title: 內建與第三方中介層的範例有哪些？
challengeType: 19
dashedName: what-are-examples-of-built-in-and-3rd-party-middleware
---

# --description--

Express 提供一系列內建的中介層函式，幫助執行常見任務，例如提供靜態檔案服務、解析 JSON 請求主體等等。你也可以使用第三方中介層來擴充功能。

**以下是一些內建中介層的範例**：

1. `express.json()`：一個內建中介層，用來解析傳入的 JSON 載荷。

```js
app.use(express.json())
```

1. `express.static()`：用於提供靜態檔案服務的中介層，例如圖片、CSS 和 JavaScript 檔案。

```js
app.use(express.static('public'))
```

1. `express.urlencoded()`：解析傳入 URL 編碼載荷的中介層，這是 HTML 表單通常傳送的格式。它會將解析後的資料放在 `req.body` 上。

```js
app.use(express.urlencoded({ extended: true }))
```

**現在，以下是一些第三方中介層的範例：**

1. `cors`：一個流行的第三方中介層，用來啟用跨來源資源共享（CORS）。

```bash
npm install cors
```

```js
const cors = require('cors')
app.use(cors())
```

1. `morgan`：用於 HTTP 請求的記錄中介層。

```bash
npm install morgan
```

```js
const morgan = require('morgan')
app.use(morgan('tiny'))
```

現在，讓我們來看一個同時使用內建和第三方中介層的範例：

```js
const express = require('express')
const cors = require('cors')
const morgan = require('morgan')
const app = express()

app.use(express.json()) // Built-in middleware to parse JSON
app.use(cors())       // Third-party middleware for CORS
app.use(morgan('tiny')) // Third-party middleware for logging

app.get('/', (req, res) => {
  res.send('Hello, World!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* 內建中介層 `express.json()` 解析傳入的 JSON 載荷。
* 第三方中介層 `cors` 允許跨來源請求，`morgan` 則記錄傳入的 HTTP 請求。

重要的是，中介層函式是依照使用 `app.use()` 呼叫的順序加入的。

Express 提供處理常見任務（如 JSON 解析和靜態檔案服務）的基本內建中介層。你也可以透過第三方中介層擴充更多功能。

# --questions--

## --text--

以下哪一個是 Express 的內建中介層？

## --answers--

`express.json()`

---

`cors`

### --feedback--

想想看哪個中介層是 Express 內建的，不需要另外安裝。

---

`morgan`

### --feedback--

想想看哪個中介層是 Express 內建的，不需要另外安裝。

---

`helmet`

### --feedback--

想想看哪個中介層是 Express 內建的，不需要另外安裝。

## --video-solution--

1

## --text--

`express.static()` 中介層的功能是什麼？

## --answers--

解析傳入的 JSON 資料。

### --feedback--

注意哪個中介層讓你可以直接提供像是圖片、CSS 和 JavaScript 這類檔案。

---

記錄 HTTP 請求。

### --feedback--

注意哪個中介層讓你可以直接提供像是圖片、CSS 和 JavaScript 這類檔案。

---

提供靜態檔案服務。

---

啟用跨來源請求。

### --feedback--

注意哪個中介層讓你可以直接提供像是圖片、CSS 和 JavaScript 這類檔案。

## --video-solution--

3

## --text--

以下哪一個是第三方中介層？

## --answers--

`express.json()`

### --feedback--

想想看哪個中介層是從外部套件安裝的。

---

`express.static()`

### --feedback--

想想看哪個中介層是從外部套件安裝的。

---

`cors`

---

`express.Router()`

### --feedback--

想想看哪個中介層是從外部套件安裝的。

## --video-solution--

3
