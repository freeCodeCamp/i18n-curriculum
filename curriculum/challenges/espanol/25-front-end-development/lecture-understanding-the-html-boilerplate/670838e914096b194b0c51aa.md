---
id: 670838e914096b194b0c51aa
title: '¿Qué es un boilerplate HTML y por qué es importante?'
challengeType: 19
dashedName: what-is-an-html-boilerplate
---

# --description--

Aprendamos sobre el boilerplate HTML.

¿Qué es el boilerplate HTML, te preguntas? Es como una plantilla lista para tus páginas web. Piensa en ello como los cimientos de una casa. Un boilerplate incluye la estructura básica y los elementos esenciales que todo documento HTML necesita. Te ahorra tiempo y ayuda a asegurar que tus páginas estén configuradas correctamente. Aquí hay un ejemplo:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
       name="viewport"
       content="width=device-width, initial-scale=1.0" />
    <title>freeCodeCamp</title>
    <link rel="stylesheet" href="./styles.css" />
  </head>
  <body>
  </body>
</html>
```

Analicemos las partes clave de este boilerplate. Primero, está la declaración `DOCTYPE`:

```html
<!DOCTYPE html>
```

Le dice a los navegadores qué versión de HTML estás utilizando. A continuación, viene la etiqueta `html`:

```html
<!DOCTYPE html>
<html lang="en">
  <!--All other elements go inside here-->
</html>
```

Esta engloba todo tu contenido y puede especificar el idioma de tu página. Dentro de la etiqueta `html`, encontrarás dos secciones principales – un `head` y un `body`:

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <!--Important metadata goes here-->
  </head>
  <body>
    <!--Headings, paragraphs, images, etc. go inside here-->
  </body>
</html>
```

La sección `head` contiene información importante tras bambalinas:

```html
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document Title Goes Here</title>
  <link rel="stylesheet" href="./styles.css" />
</head>
```

Los metadatos de tu sitio, contenidos en elementos `meta`, tienen detalles sobre cosas como la codificación de caracteres y cómo sitios como Twitter deberían mostrar la vista previa del enlace de tu página. El título de tu sitio, encontrado en el elemento `title`, determina el texto que aparece en la pestaña o ventana del navegador. Finalmente, generalmente se vinculan las hojas de estilo externas de tu página en la sección `head` usando elementos `link`.

La sección `body` es donde va todo tu contenido:

```html
<body>
  <h1>I am a main title</h1>
  <p>Example paragraph text</p>
</body>
```

Ahora, ¿por qué es importante un boilerplate? Asegura que tus páginas estén estructuradas correctamente y funcionen bien en diferentes navegadores. Usar un boilerplate te ayuda a evitar errores comunes y seguir las mejores prácticas. Es un gran punto de partida para cualquier proyecto web. Recuerda, puedes personalizar tu propio boilerplate para adecuarse a tus necesidades. A medida que ganes experiencia, podrías añadir tus propios elementos preferidos o etiquetas `meta`. Al continuar mejorando tu boilerplate personal, descubrirás que te ahorra tiempo al comenzar nuevos proyectos.

La próxima vez que inicies un nuevo archivo HTML, considera usar un boilerplate. Definitivamente te dará una base sólida sobre la cual construir.

# --questions--

## --text--

¿Dónde establecerías la codificación de caracteres para tu página?

## --answers--

Un elemento `meta` en el `body`.

### --feedback--

La codificación de caracteres es información de metadatos.

---

Un elemento `head` en el `body`.

### --feedback--

Character encoding is metadata information.

---

Un elemento `meta` en el `head`.

---

En el `DOCTYPE`.

### --feedback--

La codificación de caracteres es información de metadatos.

## --video-solution--

3

## --text--

¿Dónde establecerías el idioma para tu página?

## --answers--

En la etiqueta `html` de apertura.

---

Un elemento `meta` en el `body`.

### --feedback--

Este es un atributo en el elemento más externo.

---

Un elemento `head` en el `body`.

### --feedback--

This is an attribute on the outer-most element.

---

Un elemento `meta` en el `head`.

### --feedback--

This is an attribute on the outer-most element.

## --video-solution--

1

## --text--

¿Cuál es el propósito de un boilerplate?

## --answers--

Proporciona una estructura de inicio para tus sitios web.

### --feedback--

Un boilerplate es útil de muchas maneras.

---

Asegura que no falten elementos esenciales.

### --feedback--

A boilerplate is helpful in many ways.

---

Te permite comenzar a escribir el contenido de tu página más rápido.

### --feedback--

A boilerplate is helpful in many ways.

---

Todo lo anterior.

## --video-solution--

4
