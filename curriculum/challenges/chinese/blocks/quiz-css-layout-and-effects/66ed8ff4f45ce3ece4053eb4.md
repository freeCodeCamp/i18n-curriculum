---
id: 66ed8ff4f45ce3ece4053eb4
title: CSS 布局和效果测验
challengeType: 8
dashedName: quiz-css-layout-and-effects
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

CSS 中使用什么术语来描述元素内容超出其容器大小的情况？

#### --distractors--

下溢

---

压缩

---

内边距

#### --answer--

溢出

### --question--

#### --text--

当内容溢出元素时，CSS 的默认行为是什么？

#### --distractors--

隐藏内容。

---

裁剪内容。

---

调整内容大小。

#### --answer--

保持内容可见。

### --question--

#### --text--

为什么 CSS 通常会在出现溢出时避免隐藏内容？

#### --distractors--

保持原有的布局和设计。

---

防止浏览器出现性能问题。

---

以便更好地优化搜索引擎。

#### --answer--

确保用户可以访问所有内容。

### --question--

#### --text--

如何在 CSS 中通过隐藏内容或显示滚动条来处理垂直溢出？

#### --distractors--

用 `overflow-y: visible` 隐藏内容，并用 `overflow-y: auto` 始终显示滚动条。

---

用 `overflow-y: collapse` 隐藏内容，并用 `overflow-y: expand` 显示滚动条。

---

用 `overflow-y: none` 隐藏内容，并用 `overflow-y: show` 显示滚动条。

#### --answer--

用 `overflow-y: hidden` 隐藏内容，并用 `overflow-y: scroll` 始终显示滚动条。

### --question--

#### --text--

如何在 CSS 中指定不同的水平和垂直滚动行为？

#### --distractors--

通过使用 `scroll-x` 和 `scroll-y` 属性。

---

通过为 `overflow-x` 和 `overflow-y` 指定相同的值。

---

通过为 `overflow` 属性指定两个值，第一个值应用于 `overflow-y`，第二个值应用于 `overflow-x`。

#### --answer--

通过为 `overflow` 属性指定两个值，第一个值应用于 `overflow-x`，第二个值应用于 `overflow-y`。

### --question--

#### --text--

`transform` CSS 属性允许你做什么？

#### --distractors--

更改元素的颜色和字体样式。

---

调整元素的外边距和内边距。

---

修改元素的可见性和显示效果。

#### --answer--

旋转、缩放、倾斜和平移元素。

### --question--

#### --text--

`transform: translate(15px, 200px)` 属性会如何改变一个元素？

#### --distractors--

它将元素从原来的位置向左移动 15 像素，向下移动 200 像素。

---

它将元素顺时针方向旋转 15 度。

---

它将元素从原来的位置向右移动 15 像素，向上移动 200 像素。

#### --answer--

它将元素从原来的位置向右移动 15 像素，向下移动 200 像素。

### --question--

#### --text--

CSS 属性 `overflow-x` and `overflow-y` 的作用是什么？

#### --distractors--

调整元素的内边距和外边距。

---

设置元素的背景颜色和字体样式。

---

定义元素的可见性和显示属性。

#### --answer--

控制元素内容的水平和垂直溢出。

### --question--

#### --text--

CSS 中 `margin` 属性的作用是什么？

#### --distractors--

定义元素内容与边框之间的空间。

---

定义元素边框和内边距之间的空间。

---

定义元素的宽度和高度。

#### --answer--

定义元素边框外的空间。

### --question--

#### --text--

CSS 中元素的内容区域代表什么？

#### --distractors--

定义元素周围的外边距和内边距。

---

定义元素边框的厚度。

---

控制元素在页面上的可见度。

#### --answer--

它含元素的实际内容，如文本、图像和视频。

### --question--

#### --text--

`content-box` 和 `border-box` 有什么区别？

#### --distractors--

`content-box` 包括内边距和边框，而 `border-box` 仅包括内容尺寸。

---

`content-box` 和 `border-box` 都只包括边框尺寸。

---

`content-box` 用于 2D 元素，而 `border-box` 则用于 3D 元素。

#### --answer--

`content-box` 仅包括内容尺寸，而 `border-box` 在尺寸中包括内容、内边距和边框。

### --question--

#### --text--

CSS 重置的作用是什么？

#### --distractors--

将默认样式应用于所有元素，以加快加载速度。

---

通过删除未使用的样式，尽量减小 CSS 文件的大小。

---

优化图像和字体，提高性能。

#### --answer--

移除默认浏览器样式，确保各浏览器样式一致。

### --question--

#### --text--

以下哪项是 CSS 重置的常见示例？

#### --distractors--

bootstrap.css

---

undo.css

---

restore.css

#### --answer--

normalize.css

### --question--

#### --text--

哪个 CSS 属性用于对元素进行旋转、缩放和平移等更改？

#### --distractors--

`box-shadow`

---

`opacity`

---

`z-index`

#### --answer--

`transform`

### --question--

#### --text--

在 CSS 中使用 `box-sizing` 属性的 `border-box` 值时，如何计算元素的总宽度？

#### --distractors--

总宽度 = 内容宽度 + 边框 + 外边距

---

总宽度 = 内容宽度 + 内边距 + 边框 + 外边距

---

总宽度 = 内容宽度

#### --answer--

总宽度 = 内容宽度 + 内边距 + 边框

### --question--

#### --text--

CSS 中 `filter` 属性的作用是什么？

#### --distractors--

改变元素的缩放和旋转。

---

改变元素的背景颜色和纹理。

---

改变元素的大小和位置。

#### --answer--

通过使用各种图形效果来修改元素的视觉外观。

### --question--

#### --text--

CSS 属性 `filter: grayscale(50%);` 对元素有什么作用？

#### --distractors--

将元素的亮度提高 50%。

---

将元素的对比度降低 50%。

---

为元素添加了棕褐色效果。

#### --answer--

对元素应用 50% 灰度效果。

### --question--

#### --text--

CSS 中 `contrast()` 函数的作用是什么？

#### --distractors--

调整图像亮度，`0%` 将为黑色，`100%` 将为白色。

---

调整图像对比度，`100%` 为灰色，`0%` 没有效果，值大于 `100%` 增加对比。

---

调整图像不透明度，`0%` 为透明，`100%` 为不透明。

#### --answer--

调整图像对比度，`0%` 为灰色，`100%` 没有效果，值大于 `100%` 增加对比。

### --question--

#### --text--

CSS 中 `hue-rotate()` 函数的作用是什么？

#### --distractors--

改变图像的亮度。

---

图像顺时针旋转一定的度数。

---

调整图像的饱和度和对比度。

#### --answer--

应用颜色偏移，其中 `angle` 值定义围绕颜色环旋转的度数。

### --question--

#### --text--

什么是 CSS 中的外边距折叠？

#### --distractors--

当元素的外边距设置为负数时，会导致布局问题。

---

当元素的外边距因内边距而减小，从而改变元素的尺寸时。

---

当浏览器忽略元素的外边距时，该元素不可见。

#### --answer--

当相邻元素的垂直外边距重叠时，将它们合并为一个边距。

## --quiz--

### --question--

#### --text--

关于 `translate()` 函数，以下哪项是正确的？

#### --distractors--

它用于缩放元素大小。

---

它用于翻译元素的文本。

---

它用于改变元素形状或大小。

#### --answer--

它用于从当前位置移动元素。

### --question--

#### --text--

以下哪项是 CSS 盒模型的组成部分？

#### --distractors--

宽度

---

高度

---

大小

#### --answer--

内边距

### --question--

#### --text--

当元素的 `box-sizing` 被设置为 `content-box` 时，`width` 和 `height` 应用于盒模型的哪部分？

#### --distractors--

内容、内边距、边框和外边距

---

内容和内边距

---

内容、内边距和边框

#### --answer--

内容

### --question--

#### --text--

当元素的 `box-sizing` 被设置为 `border-box` 时，`width` 和 `height` 不应用于盒模型的哪部分？

#### --distractors--

内容区域

---

内边距

---

边框

#### --answer--

外边距

### --question--

#### --text--

`brightness()` 函数的哪个值可以使元素完全变黑？

#### --distractors--

`100%`

---

`0px`

---

`100px`

#### --answer--

`0%`

### --question--

#### --text--

`hue-rotate()` 函数使用什么样的值？

#### --distractors--

像素

---

弧度

---

百分比

#### --answer--

度数

### --question--

#### --text--

CSS 属性 `filter: contrast(0%);` 对元素有什么作用？

#### --distractors--

将使元素完全变白。

---

将使元素完全变黑。

---

不会对元素产生任何影响。

#### --answer--

将使元素完全变灰。

### --question--

#### --text--

`brightness()` CSS 函数的基线值是多少，增加亮度是从该值开始的？

#### --distractors--

`100px`

---

`50px`

---

`50%`

#### --answer--

`100%`

### --question--

#### --text--

`grayscale()` 函数使用什么样的值？

#### --distractors--

像素

---

度数

---

弧度

#### --answer--

百分比

### --question--

#### --text--

`contrast()` 函数使用什么样的值？

#### --distractors--

像素

---

度数

---

弧度

#### --answer--

百分比

### --question--

#### --text--

CSS 中的 `overflow-x` 属性控制什么？

#### --distractors--

决定 y 轴上的水平溢出量。

---

决定 y 轴上的垂直溢出量。

---

决定 x 轴上的垂直溢出量。

#### --answer--

决定 x 轴上的水平溢出量。

### --question--

#### --text--

CSS 中的 `overflow-y` 属性控制什么？

#### --distractors--

决定 x 轴上的水平溢出量。

---

决定 x 轴上的垂直溢出量。

---

决定 y 轴上的水平溢出量。

#### --answer--

决定 y 轴上的垂直溢出量。

### --question--

#### --text--

CSS 重置的第三方选项示例是什么？

#### --distractors--

clean.css

---

restore.css

---

reset.css

#### --answer--

sanitize.css

### --question--

#### --text--

CSS 中的溢出属性有几个维度？

#### --distractors--

一个

---

三个

---

四个

#### --answer--

两个

### --question--

#### --text--

如果简写属性 `overflow` 有两个值，哪个属性将使用第一个值？

#### --distractors--

`overflow-y`

---

`overflow-w`

---

`overflow-z`

#### --answer--

`overflow-x`

### --question--

#### --text--

在 CSS 中，`blur()` 函数会对元素应用哪种模糊效果？

#### --distractors--

吉利亚尼

---

伽利莱

---

伽利略

#### --answer--

高斯

### --question--

#### --text--

`sepia()` 函数使用什么样的值？

#### --distractors--

像素

---

弧度

---

度数

#### --answer--

百分比

### --question--

#### --text--

如果简写属性 `overflow` 有两个值，哪个属性将使用第二个值？

#### --distractors--

`overflow-w`

---

`overflow-x`

---

`overflow-z`

#### --answer--

`overflow-y`

### --question--

#### --text--

CSS 重置有什么作用？

#### --distractors--

强制浏览器默认使用暗模式。

---

移除 HTML 元素的内联样式。

---

禁用包含 CSS 的浏览器扩展。

#### --answer--

移除默认浏览器样式。

### --question--

#### --text--

CSS 属性 `filter: grayscale(100%);` 对元素有什么作用？

#### --distractors--

不会对元素产生任何影响。

---

将使元素完全变黑。

---

将使元素完全变白。

#### --answer--

它在维护元素的色调结构的同时去除颜色饱和度。

