---
id: 69f42d414b7626e21a922795
title: 組み込みミドルウェアとサードパーティミドルウェアの例とは？
challengeType: 19
dashedName: what-are-examples-of-built-in-and-3rd-party-middleware
---

# --description--

Express は、静的ファイルの配信や JSON リクエストボディのパースなど、よくある処理を行うための組み込みミドルウェア関数をいくつか提供しています。また、機能を追加するためにサードパーティミドルウェアを使うこともできます。

**組み込みミドルウェアの例をいくつか紹介します**：

1. `express.json()`: 受信した JSON ペイロードをパースする組み込みミドルウェアです。

```js
app.use(express.json())
```

1. `express.static()`: 画像や CSS、JavaScript ファイルなどの静的ファイルを配信するためのミドルウェアです。

```js
app.use(express.static('public'))
```

1. `express.urlencoded()`: HTML フォームで送信される形式である URL エンコードされたペイロードをパースするミドルウェアです。パースされたデータは `req.body` で利用可能になります。

```js
app.use(express.urlencoded({ extended: true }))
```

**次に、サードパーティミドルウェアの例を紹介します：**

1. `cors`: クロスオリジンリソースシェアリングを有効にする人気のサードパーティミドルウェアです。

```bash
npm install cors
```

```js
const cors = require('cors')
app.use(cors())
```

1. `morgan`: HTTP リクエストのログを記録するミドルウェアです。

```bash
npm install morgan
```

```js
const morgan = require('morgan')
app.use(morgan('tiny'))
```

では、組み込みミドルウェアとサードパーティミドルウェアの両方を使った例を見てみましょう：

```js
const express = require('express')
const cors = require('cors')
const morgan = require('morgan')
const app = express()

app.use(express.json()) // Built-in middleware to parse JSON
app.use(cors())       // Third-party middleware for CORS
app.use(morgan('tiny')) // Third-party middleware for logging

app.get('/', (req, res) => {
  res.send('Hello, World!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* 組み込みミドルウェア `express.json()` は受信した JSON ペイロードをパースします。
* サードパーティミドルウェア `cors` はクロスオリジンリクエストを許可し、`morgan` は受信した HTTP リクエストのログを記録します。

ミドルウェア関数は `app.use()` で呼び出された順に追加されることが重要です。

Express は JSON パースや静的ファイル配信などの一般的な処理に必要な組み込みミドルウェアを提供しています。さらに、サードパーティミドルウェアで機能を拡張することもできます。

# --questions--

## --text--

次のうち、Express の組み込みミドルウェアはどれですか？

## --answers--

`express.json()`

---

`cors`

### --feedback--

別途インストールが不要で、Express に標準で含まれているミドルウェアを考えてみてください。

---

`morgan`

### --feedback--

別途インストールが不要で、Express に標準で含まれているミドルウェアを考えてみてください。

---

`helmet`

### --feedback--

別途インストールが不要で、Express に標準で含まれているミドルウェアを考えてみてください。

## --video-solution--

1

## --text--

`express.static()` ミドルウェアは何をしますか？

## --answers--

受信した JSON データをパースします。

### --feedback--

画像や CSS、JavaScript などのファイルを直接配信できるものに注目してください。

---

HTTP リクエストのログを記録します。

### --feedback--

画像や CSS、JavaScript などのファイルを直接配信できるものに注目してください。

---

静的ファイルを配信します。

---

クロスオリジンリクエストを有効にします。

### --feedback--

画像や CSS、JavaScript などのファイルを直接配信できるものに注目してください。

## --video-solution--

3

## --text--

次のうち、サードパーティミドルウェアはどれですか？

## --answers--

`express.json()`

### --feedback--

外部パッケージからインストールするミドルウェアを考えてみてください。

---

`express.static()`

### --feedback--

外部パッケージからインストールするミドルウェアを考えてみてください。

---

`cors`

---

`express.Router()`

### --feedback--

外部パッケージからインストールするミドルウェアを考えてみてください。

## --video-solution--

3
