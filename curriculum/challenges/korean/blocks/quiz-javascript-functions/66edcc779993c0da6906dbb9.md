---
id: 66edcc779993c0da6906dbb9
title: JavaScript 함수 퀴즈
challengeType: 8
dashedName: quiz-javascript-functions
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 맞히세요.

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript에서 함수를 선언하는 올바른 방법은 무엇인가요?

#### --distractors--

```js
func greet() {
  console.log("Hello there!");
}
```

---

```js
defineFunction greet() {
  console.log("Hello there!");
}
```

---

```js
def greet() {
  console.log("Hello there!");
}
```

#### --answer--

```js
function greet() {
  console.log("Hello there!");
}
```

### --question--

#### --text--

함수를 호출(또는 실행)하면 어떤 일이 발생하나요?

#### --distractors--

함수 내부의 코드가 실행되고 `null`가 항상 반환됩니다.

---

함수를 호출해도 아무 일도 일어나지 않습니다.

---

`TypeError`가 발생하고 프로그램이 충돌합니다.

#### --answer--

함수 내부의 코드가 실행되고 값이 반환됩니다.

### --question--

#### --text--

함수를 호출하는 올바른 방법은 무엇인가요?

#### --distractors--

```js
exampleFunction;
```

---

```js
exampleFunction>>;
```

---

```js
()exampleFunction();
```

#### --answer--

```js
exampleFunction();
```

### --question--

#### --text--

`return` 키워드의 역할은 무엇인가요?

#### --distractors--

`return` 키워드는 함수를 실행합니다.

---

`return` 키워드는 타입 오류를 발생시킵니다.

---

`return` 키워드는 함수에서 아무 역할도 하지 않습니다.

#### --answer--

`return` 키워드는 함수에서 값을 반환합니다.

### --question--

#### --text--

다음 중 올바른 함수 표현식은 무엇인가요?

#### --distractors--

```js
expression function getSum(x, y) {
    return x + y;
};
```

---

```js
getSum: function(x, y) {
    return x + y;
};
```

---

```js
function = getSum(x, y) {
    return x + y;
};
```

#### --answer--

```js
const getSum = function(x, y) {
    return x + y;
};
```

### --question--

#### --text--

함수 인수란 무엇인가요?

#### --distractors--

값의 부재를 나타내는 값입니다.

---

익명 함수와 비슷하게 작동하는 특별한 값입니다.

---

함수 내부에서 사용하는 자리 표시자 값입니다.

#### --answer--

함수가 호출될 때 전달되는 실제 값입니다.

### --question--

#### --text--

함수 매개변수란 무엇인가요?

#### --distractors--

함수 내부에서 사용하는 부동 소수점 값입니다.

---

함수가 호출될 때 전달되는 값입니다.

---

값의 의도적인 부재를 나타내는 값입니다.

#### --answer--

함수 정의 내부에 나열된 값입니다.

### --question--

#### --text--

콘솔에 무엇이 출력될까요?

```js
const calculateTotal = (amount, taxRate = 0.05) => {
  return amount + (amount * taxRate);
};

console.log(calculateTotal(100));
```

#### --distractors--

`10`

---

`undefined`

---

`1`

#### --answer--

`105`

### --question--

#### --text--

화살표 함수의 올바른 구문은 무엇인가요?

#### --distractors--

```js
const sum <<>> (num1, num2) => num1 + num2
```

---

```js
const sum === (num1, num2) === num1 + num2
```

---

```js
const sum >=> (num1, num2) => num1 + num2
```

#### --answer--

```js
const sum = (num1, num2) => num1 + num2
```

### --question--

#### --text--

다음 코드의 결과는 무엇인가요?

```js
function greet() {
  const developer = "Jessica";
  console.log("Hello there!");
}

console.log(developer);
```

#### --distractors--

아무 일도 일어나지 않고 코드가 정상 실행됩니다.

---

문자열 `"Hello there!"`가 콘솔에 출력됩니다.

---

문자열 `"Hello!"`가 콘솔에 출력됩니다.

#### --answer--

`developer`가 전역으로 정의되어 있지 않아 참조 오류가 발생합니다.

### --question--

#### --text--

화살표 함수에서 매개변수 목록 주위의 소괄호를 언제 생략할 수 있나요?

#### --distractors--

항상 매개변수 목록 주위의 소괄호를 생략해야 합니다.

---

화살표 함수에서 소괄호를 절대 생략하면 안 됩니다.

---

여러 매개변수를 사용할 때만 소괄호를 생략해야 합니다.

#### --answer--

매개변수가 하나일 때만 소괄호를 생략할 수 있습니다.

### --question--

#### --text--

화살표 함수에서 중괄호와 `return` 키워드를 언제 생략할 수 있나요?

#### --distractors--

항상 중괄호와 `return` 키워드를 생략해야 합니다.

---

중괄호와 `return` 키워드를 절대 생략하면 안 됩니다.

---

함수 본문에 여러 줄의 코드가 있을 때입니다.

#### --answer--

함수 본문이 단일 표현식일 때입니다.

### --question--

#### --text--

다음 함수는 무엇을 반환할까요?

```js
function exampleFunction() {
  return "Hello";
  return "World!";
};
```

#### --distractors--

`"World!"`

---

`"Hello World!"`

---

`undefined`

#### --answer--

`"Hello"`

### --question--

#### --text--

기본 매개변수를 사용하는 올바른 방법은 무엇인가요?

#### --distractors--

```js
function sum(num1, num2 <<>> 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 === 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 >> 12) {
  return num1 + num2
}
```

#### --answer--

```js
function sum(num1, num2 = 12) {
  return num1 + num2
}
```

### --question--

#### --text--

다음 코드의 결과는 무엇인가요?

```js
const developer = "Jessica";

function greet() {
  console.log("Hello, " + developer)
}

greet();
```

#### --distractors--

문자열 `"Hello, developer"`가 콘솔에 출력됩니다.

---

콘솔에 오류 메시지가 표시됩니다.

---

콘솔에 아무것도 출력되지 않습니다.

#### --answer--

문자열 `"Hello, Jessica"`가 콘솔에 출력됩니다.

### --question--

#### --text--

일반 함수에서 중괄호를 제거하면 어떤 일이 발생하나요?

#### --distractors--

오류가 있는 경고 상자가 페이지에 표시됩니다.

---

프로그램이 충돌하고 다시 시작하지 않습니다.

---

아무 일도 일어나지 않습니다.

#### --answer--

구문 오류가 발생합니다.

### --question--

#### --text--

다음 코드의 결과는 무엇인가요?

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + 10);
```

#### --distractors--

콘솔에 오류 메시지가 표시됩니다.

---

숫자 `0`가 콘솔에 출력됩니다.

---

콘솔에 아무것도 출력되지 않습니다.

#### --answer--

숫자 `10`가 콘솔에 출력됩니다.

### --question--

#### --text--

다음 코드의 출력 결과는 무엇인가요?

```js
const exampleFunction = (param1, param2) => param1 + param2;
console.log(exampleFunction(3, "Something"));
```

#### --distractors--

`null`

---

`undefined`

---

`3 + "Something"`

#### --answer--

`"3Something"`

### --question--

#### --text--

다음 코드의 결과는 무엇인가요?

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + num2);
```

#### --distractors--

`num1`가 정의되지 않았다는 오류 메시지입니다.

---

숫자 `0`가 콘솔에 표시됩니다.

---

`undefined`가 콘솔에 표시됩니다.

#### --answer--

`num2`가 정의되지 않았다는 오류 메시지입니다.

### --question--

#### --text--

다음 코드의 출력 결과는 무엇인가요?

```js
const divideTwoNumbers = (num1, num2) => num1 / num2;
console.log(divideTwoNumbers(3, 0));
```

#### --distractors--

`Error`

---

`0`

---

`null`

#### --answer--

`Infinity`
