---
id: 6923642b185cf68eed95dc46
title: 任务 10
challengeType: 19
dashedName: task-10
lang: es
---

<!-- NO AUDIO -->

# --instructions--

本任务无音频。

阅读下面的问题，选择正确的答案。

# --questions--

## --text--

选择写得正确的数字。

## --answers--

`Veintidós`

---

`Quinze`

### --feedback--

数字 15 使用字母 `c`，而不是 `z`。

---

`Veintiseis`

### --feedback--

数字 26 的最后一个 `e` 上必须有重音符号。

---

`Catorze`

### --feedback--

数字 14 使用字母 `c`，而不是 `z`。

## --video-solution--

1

# --explanation--

正确答案是 `veintidós`，对应数字 22。请记住它在字母 `o` 上有一个重音符号。

`Quinze` 是错误的。数字 15 是 `quince`。

`Veintiseis` 是错误的。数字 26 是 `veintiséis`（第二个 `e` 上有重音符号）。

`Catorze` 是错误的。数字 14 是 `catorce`。
