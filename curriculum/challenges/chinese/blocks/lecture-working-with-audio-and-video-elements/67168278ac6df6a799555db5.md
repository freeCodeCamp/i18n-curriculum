---
id: 67168278ac6df6a799555db5
title: HTML 的 audio 和 video 元素的作用是什么，它们是如何工作的？
challengeType: 19
dashedName: what-are-the-roles-of-the-html-audio-and-video-elements
---

# --description--

`audio` 和 `video` 元素可让你在 HTML 文档中添加声音和视频内容。 `audio` 元素支持流行的音频格式，如 mp3、wav 和 ogg。 `video` 元素支持 mp4、ogg 和 webm 格式。

要在网页中包含音频内容，可以使用带有指向音频文件位置的 `src` 属性的 `audio` 元素：

```html
<audio src="CrystalizeThatInnerChild.mp3"></audio>
```

如果你尝试运行这个示例，你会发现页面上没有任何显示。 但是，如果使用开发者工具检查页面，你会发现页面上确实存在 `audio` 元素。 如果你想在页面上看到音频播放器，则可以添加带有 `controls` 属性的 `audio` 元素：

```html
<audio src="CrystalizeThatInnerChild.mp3" controls></audio>
```

`controls` 属性使用户能够管理音频播放，包括调整音量、暂停或恢复播放。 `controls` 属性是一个布尔属性，可添加到元素中以启用内置播放控制。 如果省略，则不会显示任何控件。

除了 `src` 和 `controls` 属性外，还有其他几个属性可以增强 `audio` 元素的功能。 `loop` 属性是一个布尔属性，用于使音频连续重放。 这是一个使用 `loop` 属性播放 Quincy Larson 的歌曲 "Can't stay down" 的示例：

```html
<audio
  src="https://cdn.freecodecamp.org/curriculum/js-music-player/can't-stay-down.mp3"
  loop
  controls
></audio>
```

当歌曲结束时，它会从头开始循环播放。 你可以使用的另一个属性是 `muted` 属性。 当出现在 `audio` 元素中时，此布尔属性将以静音状态启动音频。 这是一个是使用 `muted` 属性的示例：

```html
<audio
  src="https://cdn.freecodecamp.org/curriculum/js-music-player/can't-stay-down.mp3"
  loop
  controls
  muted
></audio>
```

当你在浏览器中开始播放歌曲时，不会听到任何声音。 要听到音乐，你需要点击音量图标。

说到音频文件类型，不同的浏览器支持不同的类型。 为此，你可以在 `audio` 元素内使用 `source` 元素，浏览器将选择它能理解的第一个源。 下面是为 `audio` 元素使用多个 `source` 元素的示例：

```html
<audio controls>
  <source src="audio.ogg" type="audio/ogg" />
  <source src="audio.wav" type="audio/wav" />
  <source src="audio.mp3" type="audio/mpeg" />
</audio>
```

浏览器会首先从 ogg 类型开始，如果无法播放音频，就会下移到列表中的下一个类型。

`video` 元素也支持我们目前所学到的所有属性。 以下是一个使用带有 `loop`、`controls` 和 `muted` 属性的 `video` 元素的示例：

```html
<video
  src="https://archive.org/download/BigBuckBunny_124/Content/big_buck_bunny_720p_surround.mp4"
  loop
  controls
  muted
></video>
```

对于 `src`，或源属性，我们使用的是 archive.org 上名为 "Big Buck Bunny" 的视频。 如果你想在加载视频时显示图像，可以使用 `poster` 属性。 此属性不适用于 `audio` 元素，是 `video` 元素独有的属性。 下面是使用由 peach.blender.org 提供内容的 `poster` 属性的示例：

```html
<video
  src="https://archive.org/download/BigBuckBunny_124/Content/big_buck_bunny_720p_surround.mp4"
  loop
  controls
  muted
  poster="https://peach.blender.org/wp-content/uploads/title_anouncement.jpg?x11217"
  width="620"
></video>
```

此示例还使用 `width` 属性将宽度设置为 620 像素，以便视频在屏幕上更加适合。

# --questions--

## --text--

什么属性允许音频在静音状态下启动？

## --answers--

`mute`

### --feedback--

记住控制音频启动时声音状态的特定属性。

---

`quiet`

### --feedback--

记住控制音频启动时声音状态的特定属性。

---

`pause`

### --feedback--

记住控制音频启动时声音状态的特定属性。

---

`muted`

## --video-solution--

4

## --text--

通过哪个属性可以看到播放和暂停按钮？

## --answers--

`loop`

### --feedback--

想想为音频和视频用户界面生成播放的属性。

---

`controls`

---

`details`

### --feedback--

想想为音频和视频用户界面生成播放的属性。

---

`muted`

### --feedback--

想想为音频和视频用户界面生成播放的属性。

## --video-solution--

2

## --text--

`video` 元素中的 `poster` 属性的用途是什么？

## --answers--

设置视频源。

### --feedback--

想想在视频开始播放之前，你想向用户展示什么。

---

控制视频播放。

### --feedback--

想想在视频开始播放之前，你想向用户展示什么。

---

在加载视频时显示图像。

---

将视频静音。

### --feedback--

想想在视频开始播放之前，你想向用户展示什么。

## --video-solution--

3
