---
id: 66f1ae758b77cfc3e4da6151
title: TypeScript 測驗
challengeType: 8
dashedName: quiz-typescript
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是 Typescript？

#### --distractors--

用於系統程式設計的框架。

---

一個資料庫管理工具。

---

用於為網站設計樣式的程式語言。

#### --answer--

JavaScript 的超集，添加了靜態類型。

### --question--

#### --text--

為什麼應該在專案中使用 TypeScript？

#### --distractors--

它消除了在應用程式中進行測試的需求。

---

它比所有其他程式設計語言都更快。

---

它讓網站樣式設計更簡單。

#### --answer--

它透過為 JavaScript 添加型別定義來提升維護性。

### --question--

#### --text--

TypeScript 會被編譯成什麼語言？

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

以下程式碼片段的輸出會是什麼？

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

TypeScript 中介面的主要目的為何？

#### --distractors--

將 TypeScript 程式碼編譯成 JavaScript。

---

創建一個新的資料型別。

---

創建一個新的 JavaScript 物件。

#### --answer--

定義物件的結構。

### --question--

#### --text--

以下哪一項不是 TypeScript 的基本原始型別？

#### --distractors--

字串

---

布林值

---

數字

#### --answer--

陣列

### --question--

#### --text--

以下哪一項正確定義了 TypeScript 中的字面量型別？

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

`type` 關鍵字在 TypeScript 中的主要用途是什麼？

#### --distractors--

宣告變數。

---

創建類別。

---

實作介面。

#### --answer--

定義訂製型別。

### --question--

#### --text--

以下哪一項正確定義了聯集（數學）型別？

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

以下哪一項功能是 `interface` 獨有且無法使用 TypeScript 中的 `type` 達成？

#### --distractors--

定義物件形狀。

---

建立聯集（數學）型別。

---

定義只包含特定值的型別。

#### --answer--

宣告類別實作。

### --question--

#### --text--

在 TypeScript 中使用泛型的主要目的是什麼？

#### --distractors--

在類別之間實作繼承。

---

定義物件的結構。

---

創建一個新的資料型別。

#### --answer--

要撰寫可與多種型別共用的可重複使用程式碼。

### --question--

#### --text--

以下哪一項是 TypeScript 中 **泛型函式（程式）的適當範例**？

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

什麼是 TypeScript 中的型別縮小？

#### --distractors--

減少編譯後的 JavaScript 大小。

---

限制聯集（數學）中的型別數量。

---

從 TypeScript 專案中移除未使用的型別。

#### --answer--

撰寫將聯集（數學）型別縮小為更具體型別的程式碼。

### --question--

#### --text--

通常使用哪個檔案來組態 TypeScript 編譯器選項？

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

`tsconfig.json` 檔案中的 `rootDir` 選項指定什麼？

#### --distractors--

編譯期間要使用的 TypeScript 版本。

---

TypeScript 程式碼的標的執行期環境（例如，Node.js 或瀏覽器）。

---

用於 `import` 和 `export` 的特定模組系統。

#### --answer--

儲存你的原始碼檔案的目錄。

### --question--

#### --text--

哪個範例正確使用 `instanceof` 來縮小型別，以安全地存取 `value` 屬性？

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
在 TypeScript 中，於 `tsconfig.json` 設定 `"strict": true` 會做什麼？

#### --distractors--

啟用靜態型別檢查，讓你能在執行程式碼之前捕捉錯誤。

---

強制你始終使用 ES6 語法。

---

強制你在執行之前編譯程式碼。

#### --answer--

啟用一組型別檢查選項，例如要求處理 `null` 或 `undefined`，以及防止隱式的 `any`。

### --question--

#### --text--

在 TypeScript 中，`never` 型別的用途是什麼？

#### --distractors--

表述 null 或 undefined。

---

表述任何可能的值。

---

表述一個可以是任何東西但不包含 null 的型別。

#### --answer--

用來表述永遠不會出現的值的型別。

### --question--

#### --text--

以下哪一個 `getRandomValue` 函式（程式）的呼叫會在 TypeScript 中成功編譯？

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

你如何使用選用串鏈來修正以下程式碼中 TypeScript 編譯器錯誤 `'email' is possibly 'null'`？

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`

