---
id: 698f5a372871b5dfb9e01698
title: Was sind Union-Typen und wie funktionieren sie?
challengeType: 19
dashedName: what-are-union-types-and-how-do-they-work
---

# --description--

In den vorherigen Lektionen haben Sie gelernt, wie Sie mit primitiven Typen wie `string` und `boolean` arbeiten. Aber TypeScript ermöglicht es Ihnen, neue Typen aus bestehenden zu erstellen. Ein Beispiel dafür ist der Union-Typ.

Ein Union-Typ ist ein Typ, der zwei oder mehr andere Typen kombiniert.

Hier ist die grundlegende Syntax:

```ts
type1 | type2 | type3
```

Schauen wir uns einige Beispiele an, um das Konzept besser zu verstehen.

Hier ist eine Variable namens `id` mit einem Union-Typ für `string` oder `number`:

```ts
let id: string | number = "user_783";
```

Der Wert kann auf eine Zahl aktualisiert werden, was für den Union-Typ weiterhin gültig ist:

```ts
let id: string | number = 1024;
```

Wenn Sie versuchen, einen anderen Wert zuzuweisen, der weder `string` noch `number` ist, zeigt TypeScript eine Fehlermeldung an:

```ts
// Type 'string[]' is not assignable to type 'string | number'.
let id: string | number = ["Java", "Python"];
```

Beim Arbeiten mit Union-Typen ist es wichtig, Methoden zu verwenden, die für alle Typen funktionieren, sonst wirft TypeScript einen Fehler wie diesen:

```ts
function getId(id: string | number) {
  // Property 'toUpperCase' does not exist on type 'string | number'.
  return id.toUpperCase();
}
```

Es gibt eine Lösung dafür, die das Narrowing der Union beinhaltet. Aber Sie werden mehr darüber sowie darüber, wie Union-Typen mit Typ-Aliasen funktionieren, in zukünftigen Lektionen lernen.

Union-Typen sind auch hilfreich beim Arbeiten mit `null` und `undefined`-Werten.

Hier ist ein Beispiel, bei dem eine Variable entweder einen `string`-Wert enthalten kann oder `null` ist, falls noch kein Wert gesetzt wurde:

```ts
let username: string | null;

// Type 'number' is not assignable to type 'string | null'.
username = 42;
```

In diesem Fall kann `username` entweder ein `string` oder `null` sein, aber nichts anderes. Sie könnten `null` verwenden, wenn Sie absichtlich das Fehlen eines Werts darstellen möchten.

Hier ist ein weiteres Beispiel mit `undefined`:

```ts
let score: number | undefined;

score = undefined; 

// Type 'string' is not assignable to type 'number | undefined'.
score = "high";
```

In diesem Beispiel kann `score` entweder ein `number` oder `undefined` sein. Sie könnten `undefined` verwenden, wenn ein Wert noch nicht zugewiesen wurde.

Die Verwendung von Union-Typen mit `null` und `undefined` macht deutlich, wann ein Wert fehlen könnte, während TypeScript weiterhin ungültige Zuweisungen erkennt.

# --questions--

## --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, um einen Union-Typ zu erstellen?

## --answers--

```ts
let id: string >> number;
```

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

---

```ts
let id: string | number;
```

---

```ts
let id: string ? number;
```

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

---

```ts
let id: string << number;
```

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

## --video-solution--

2

## --text--

Welche Zuweisung wäre für Folgendes gültig?

```ts
let id: string | number
```

## --answers--

`false`

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

---

`"28djj38372"`

---

`["a", "b"]`

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

---

`null`

### --feedback--

Sehen Sie sich den Anfang der Lektion noch einmal an.

## --video-solution--

2

## --text--

Was passiert, wenn Sie eine Methode aufrufen, die nur für einen String existiert, bei einem Union-`type string | number`, ohne Typ-Narrowing zu verwenden?

## --answers--

Es passiert nichts.

### --feedback--

Sehen Sie sich das Ende der Lektion noch einmal an.

---

Die Zahl wird in einen String umgewandelt.

### --feedback--

Sehen Sie sich das Ende der Lektion noch einmal an.

---

Die Methode wird zur Laufzeit ignoriert.

### --feedback--

Sehen Sie sich das Ende der Lektion noch einmal an.

---

TypeScript wirft einen Fehler.

## --video-solution--

4
