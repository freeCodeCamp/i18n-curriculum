---
id: 5900f37b1000cf542c50fe8e
title: 'Завдання 15: шляхи сітки'
challengeType: 1
forumTopicId: 301780
dashedName: problem-15-lattice-paths
---

# --description--

Починаючи з верхнього лівого кута сітки 2×2 і рухаючись лише вправо та вниз, існує рівно 6 маршрутів до нижнього правого кута.

<img alt="діаграма з 6 сіток 2 на 2, що показує всі маршрути до нижнього правого кута" src="https://cdn-media-1.freecodecamp.org/project-euler/1Atixoj.gif" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

Скільки таких маршрутів існує через заданий `gridSize`?

# --hints--

`latticePaths(4)` має повертати число.

```js
assert.isNumber(latticePaths(4));
```

`latticePaths(4)` має повертати 70.

```js
assert.strictEqual(latticePaths(4), 70);
```

`latticePaths(9)` має повертати 48620.

```js
assert.strictEqual(latticePaths(9), 48620);
```

`latticePaths(20)` має повертати 137846528820.

```js
assert.strictEqual(latticePaths(20), 137846528820);
```

# --seed--

## --seed-contents--

```js
function latticePaths(gridSize) {

  return true;
}

latticePaths(4);
```

# --solutions--

```js
function latticePaths(gridSize) {
  let paths = 1;

  for (let i = 0; i < gridSize; i++) {
    paths *= (2 * gridSize) - i;
    paths /= i + 1;
  }
  return paths;
}
```
