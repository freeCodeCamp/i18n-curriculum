---
id: 66ed9009f45ce3ece4053eb7
title: CSS 相对单位和绝对单位测验
challengeType: 8
dashedName: quiz-css-relative-and-absolute-units
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是绝对单位？

#### --distractors--

根据屏幕大小动态调整的单位。

---

依赖于父元素字体大小的单位。

---

单位以视口的百分比为单位测量。

#### --answer--

固定长度单位，不相对于任何其他内容。

### --question--

#### --text--

`100vw` 覆盖了多少屏幕宽度？

#### --distractors--

屏幕宽度的 `100%`。

---

视口高度的 `100%`。

---

父元素宽度的 `100%`。

#### --answer--

视口宽度的 `100%`。

### --question--

#### --text--

以下哪项是相对单位？

#### --distractors--

`pt`

---

`cm`

---

`mm`

#### --answer--

`em`

### --question--

#### --text--

如果根字体大小是 `20px`，那么 `1rem` 相等于多少？

#### --distractors--

`18px`

---

`16px`

---

`10px`

#### --answer--

`20px`

### --question--

#### --text--

`calc()` 函数在 CSS 中能做什么？

#### --distractors--

动态修改颜色。

---

设置固定宽度。

---

仅处理百分比。

#### --answer--

添加、减去、相乘或相除不同的单位。

### --question--

#### --text--

哪个单位是 CSS 中最常用的绝对单位？

#### --distractors--

`inch`

---

`rem`

---

`%`

#### --answer--

`px`

### --question--

#### --text--

如果视口高度是 `800px`，那么 `50vh` 会是多少？

#### --distractors--

`50px`

---

`200px`

---

`100px`

#### --answer--

`400px`

### --question--

#### --text--

将元素的宽度设置为 `100%` 意味着什么？

#### --distractors--

它占据了文档的全宽度。

---

它占据屏幕的全宽。

---

它占用 `100px`。

#### --answer--

它占据其父元素的全部宽度。

### --question--

#### --text--

在 CSS 中，`calc(10px + 20%)` 会发生什么？

#### --distractors--

它为文档宽度的 `10%` 添加了 `10px`。

---

它始终相等于 `30px`。

---

它为屏幕宽度的 `20%` 添加了 `10px`。

#### --answer--

它为父元素宽度的 `20%` 添加了 `10px`。

### --question--

#### --text--

哪个单位相对于根元素的字体大小？

#### --distractors--

`px`

---

`pt`

---

`%`

#### --answer--

`rem`

### --question--

#### --text--

根字体大小为 `16px` 时，`10rem` 有多大？

#### --distractors--

`140px`

---

`10px`

---

`100px`

#### --answer--

`160px`

### --question--

#### --text--

为什么你可能在设计中使用 `point` 而不是 `pixels`？

#### --distractors--

点是可缩放的。

---

点数更适合响应式版型。

---

在屏幕上管理点更容易。

#### --answer--

点数用于打印设计。

### --question--

#### --text--

为什么在排版中更倾向于使用 `rem` 单位而不是 `pixels`？

#### --distractors--

因为它们提供固定大小，无论屏幕分辨率如何。

---

因为它们确保与旧版浏览器的兼容性。

---

因为它们在响应式设计中更容易实现。

#### --answer--

因为它们会随着用户浏览器的设置按比例缩放。

### --question--

#### --text--

`80vw` 占用多少屏幕空间？

#### --distractors--

父元素宽度的 `80%`。

---

视口高度的 `100%`。

---

`80px`

#### --answer--

视口宽度的 `80%`。

### --question--

#### --text--

如果你想让一个元素占据屏幕的某个宽度或高度的百分比，你应该使用哪些单位？

#### --distractors--

`pt` 和 `px`

---

`rem` 和 `em`

---

`pt` 和 `pc`

#### --answer--

`vw` 和 `vh`

### --question--

#### --text--

`vw` 代表什么？

#### --distractors--

垂直宽度。

---

虚拟宽度。

---

可变宽度。

#### --answer--

视口宽度。

### --question--

#### --text--

`rem` 和 `em` 在 CSS 中的主要区别是什么？

#### --distractors--

`em` 相对于根元素，而 `rem` 相对于父元素。

---

`rem` 相对于桌面宽度，而 `em` 相对于移动宽度。

---

`rem` 是相对于用户定义的宽度，而 `em` 是相对于浏览器宽度。

#### --answer--

`em` 是相对于父元素的字体大小，而 `rem` 是相对于根元素。

### --question--

#### --text--

什么是 `vh` 单位？

#### --distractors--

基于视口宽度的单位。

---

基于元素高度的单位。

---

一个根据字体大小调整的单位。

#### --answer--

一个等于视口高度 1% 的单位。

### --question--

#### --text--

将元素的宽度设置为 `auto` 在 CSS 中会有什么效果？

#### --distractors--

默认情况下，它将宽度设置为父元素的 `100%`。

---

它将宽度设置为视口的 `50%`，除非其他样式覆写它。

---

如果没有内容或内边距，则将宽度设置为 `0px`。 

#### --answer--

它允许浏览器根据内容和容器确定宽度。

### --question--

#### --text--

`calc()` 函数的一个主要优势是什么？

#### --distractors--

它会自动压缩你的 CSS 文件以提升性能。

---

它启用了在 CSS 中使用变量而无需任何预处理器。

---

它允许在 CSS 规则中直接嵌入 JavaScript 表达式。

#### --answer--

它让你根据应用或视图的不同方面动态确定 CSS 属性的值。

