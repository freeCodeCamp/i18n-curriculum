---
id: 66edd3011f18f4ee1bd9d28b
title: Quiz sulle espressioni regolari in JavaScript
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale delle seguenti NON è una espressione regolare valida?

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

Quale `RegExp` contiene lo stesso pattern dell'espressione regolare `/f[o0]{2} b[a4@]r/i`?

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

Qual è il tipo di ritorno del metodo `test` delle espressioni regolari?

#### --distractors--

Un array di stringhe che corrispondono all'espressione regolare.

---

Un oggetto contenente informazioni sulla (sotto)stringa che ha corrisposto all'espressione regolare.

---

`null`, il metodo `test` valida solo se l'espressione regolare data è valida.

#### --answer--

Un booleano che indica se la stringa corrisponde all'espressione regolare.

### --question--

#### --text--

Cosa fa il metodo `match` quando gli viene passata un'espressione regolare senza flag?

#### --distractors--

Cerca tutte le stringhe che corrispondono all'espressione regolare data e restituisce TUTTE le corrispondenze come array.

---

Cerca la prima corrispondenza completa e restituisce l'indice di inizio di quella corrispondenza.

---

Cerca la prima corrispondenza completa e restituisce un booleano che indica se è stata trovata una corrispondenza.

#### --answer--

Cerca la prima corrispondenza completa e restituisce un array contenente quella prima corrispondenza.

### --question--

#### --text--

Qual è il miglior caso d'uso per `test`?

#### --distractors--

Per verificare un'espressione regolare data.

---

Per ottenere informazioni dettagliate su una corrispondenza di stringa, come la lunghezza della stringa corrispondente.

---

Per estrarre tutte le corrispondenze da una stringa data.

#### --answer--

Per controllare se una stringa data corrisponde all'espressione regolare.

### --question--

#### --text--

Qual è lo scopo di usare `replace`?

#### --distractors--

Sostituire l'espressione regolare corrente con una nuova espressione regolare.

---

Quando viene fornito un indice e una stringa, sostituire il carattere all'indice specificato con la stringa fornita.

---

Rimuovere tutte le istanze di una sottostringa corrispondente da una stringa.

#### --answer--

Sostituire una stringa corrispondente con una stringa di sostituzione data.

### --question--

#### --text--

Qual è la differenza tra `match` e `matchAll`?

#### --distractors--

`match` restituisce sempre una stringa, mentre `matchAll` restituisce sempre un array di corrispondenze.

---

`match` restituisce un booleano che indica se è stata trovata una corrispondenza, mentre `matchAll` restituisce un numero che rappresenta la quantità di corrispondenze trovate.

---

`match` restituisce un numero che indica l'indice di inizio della prima corrispondenza trovata, mentre `matchAll` restituisce un array di numeri contenente gli indici dove è stata trovata una corrispondenza.

#### --answer--

`match` restituisce un array di stringhe che corrispondono, mentre `matchAll` restituisce un oggetto iteratore di tutte le corrispondenze trovate.

### --question--

#### --text--

Qual è l'uso corretto di `replaceAll`?

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

Quale delle seguenti classi di caratteri è equivalente all'espressione regolare `/[a-zA-Z0-9_]/`?

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

Quale delle seguenti classi di caratteri è più appropriata per estrarre le cifre da un numero di telefono?

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

Cosa succede quando si usa un lookahead `(?=)` in un'espressione regolare?

#### --distractors--

Il lookahead verifica a sinistra della stringa, controllando che il sotto-pattern SIA presente.

---

Un lookahead è un pattern alternativo di espressione regolare usato quando il pattern principale non corrisponde.

---

Il lookahead verifica a destra della stringa, controllando che il sotto-pattern NON sia presente.

#### --answer--

Un lookahead verifica a destra della stringa, controllando che il sotto-pattern SIA presente.

### --question--

#### --text--

Quando si crea un'espressione regolare, dove dovrebbe essere posizionato un lookbehind `(?<=)`?

#### --distractors--

Il lookbehind può essere posizionato ovunque nell'espressione regolare.

---

Il lookbehind dovrebbe essere posizionato a destra del pattern principale dell'espressione regolare.

---

Il lookbehind dovrebbe includere il pattern principale dell'espressione regolare.

#### --answer--

Il lookbehind dovrebbe essere posizionato a sinistra del pattern principale dell'espressione regolare.

### --question--

#### --text--

Quale quantificatore corrisponde all'elemento precedente zero o una volta?

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

Quale delle seguenti espressioni regolari permette solo numeri tra 0 e 999.999?

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

Quale delle seguenti affermazioni è vera riguardo alla classe di caratteri personalizzata (`[]`)?

#### --distractors--

È un insieme di caratteri da rimuovere dalla corrispondenza.

---

Può definire un insieme di caratteri da corrispondere senza bisogno di escape per i caratteri speciali.

---

Rappresenta un insieme di caratteri in forma Unicode.

#### --answer--

Può definire un insieme personalizzato di caratteri da corrispondere.

### --question--

#### --text--

Quale delle seguenti classi di caratteri corrisponde correttamente all'alfabeto maiuscolo?

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

Cosa succede quando si usa un gruppo di cattura `(...)` in un'espressione regolare?

#### --distractors--

Il gruppo di cattura viene valutato immediatamente, indipendentemente da dove si trova nell'espressione regolare.

---

Il gruppo di cattura tenta di corrispondere usando il sotto-pattern dato e continua senza memorizzare il risultato.

---

Il gruppo di cattura è un sotto-pattern prioritario, che restituisce immediatamente quando trova una corrispondenza per il sotto-pattern.

#### --answer--

Il gruppo di cattura tenta di corrispondere usando il sotto-pattern dato e continua con il risultato in memoria.

### --question--

#### --text--

Cosa succede quando si usa un gruppo non di cattura `(?:...)` in un'espressione regolare?

#### --distractors--

Il gruppo non di cattura tenta di corrispondere usando il sotto-pattern dato e continua con il risultato in memoria.

---

Il gruppo non di cattura termina immediatamente quando trova una corrispondenza per il sotto-pattern dato.

---

Il gruppo non di cattura è considerato una corrispondenza opzionale, quindi solo una corrispondenza riuscita viene aggiunta al risultato.

#### --answer--

Il gruppo non di cattura tenta di corrispondere usando il sotto-pattern dato e continua senza mantenere il risultato in memoria.

### --question--

#### --text--

Data la seguente espressione regolare, quale di questi test restituirà `true`?

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

Cosa fa la classe di caratteri jolly (`.`) in un'espressione regolare?

#### --distractors--

Corrisponde solo ai caratteri di spazio.

---

Corrisponde all'inizio di una stringa.

---

Corrisponde a una singola cifra.

#### --answer--

Corrisponde a qualsiasi singolo carattere tranne il carattere di nuova riga.
