---
id: 672d45651d83b450801efb3a
title: 如何在 VS Code 中創建專案並在區域的環境執行你的程式碼
challengeType: 19
dashedName: how-to-create-a-project-and-run-your-code-locally-in-vs-code
---

# --description--

要開始，你應該知道 VS Code 將專案視為「工作區」。工作區是你在 VS Code 中開啟的任何目錄。

首先，你需要使用命令列介面為你的新專案創建一個新的目錄。目錄是資料夾的另一種稱呼。

命令列介面，簡稱 CLI，允許你透過基於文字的指令與你的作業系統互動。你將在未來的課程中學習更多關於這些工具的內容。

如果你使用的是 Windows，你可以使用命令提示字元或 PowerShell。如果你使用的是 Mac，你可以開啟你的終端機應用程式。

當命令列開啟後，輸入 `cd ~` 指令並按下鍵盤上的 `enter` 鍵以切換到主目錄。接著輸入 `mkdir my-project` 指令並按下 `enter` 鍵。`mkdir`，或稱「make directory」，是用來創建新目錄的指令。然後你應該使用 VS Code 開啟你的新目錄。 

還有其他一些圖形化方式來創建新的目錄。

根據你的作業系統，你可能會有一個背景關係選單（或右鍵選項）可以直接從你的檔案總管開啟 VS Code。你也可以選擇雙擊 VS Code 應用程式。如果你對命令列感到熟悉，你可以執行命令 `code /path/to/folder`。

一旦開啟，你應該會看到一個新的空白工作區。

要創建一個新的檔案，請點擊左上角的 `File` 選單並選擇 `New File...`。然後將你的檔案命名為 `index.html` 並點擊 `Save`。

現在你應該可以在瀏覽器分頁中看到你的新檔案。試著為你的 `index.html` 檔案添加一些 HTML。

現在你可能會想直接在瀏覽器中開啟 HTML 檔案。雖然這樣可以運作，但也可能導致各種意想不到的錯誤，例如 CSS 無法正確載入。

相反地，你應該將你的 HTML 檔案放在適當的網頁伺服器後面。如果你是程式碼新手，這聽起來可能是一項令人生畏的任務，但其實有一種方法可以直接從 VS Code 完成這件事。

你需要先取得 *Live Server* 擴充套件，該擴充套件可在市集中免費取得。

點擊左下角的擴充套件分頁，並在搜尋欄中輸入 `Live Server`。

安裝完成後，你應該會在你的狀態列看到一個 `Go live` 按鈕。請確定你已開啟你的 HTML 檔案，然後點擊它。

你的新頁面應該會自動在瀏覽器的新分頁中開啟。如果沒有，你可以直接前往 `http://localhost:5500/`。

恭喜！你現在已經在 VS Code 中設定好你自己的專案，並且可以使用 live server 查看你的變更。

# --questions--

## --text--

Visual Studio Code 認為「工作區」是什麼？

## --answers--

整個硬碟。

### --feedback--

課程提到 VS Code 如何根據目錄定義工作區。

---

你從中開啟 VS Code 的目錄。

---

VS Code 中所有開啟的檔案。

### --feedback--

課程提到 VS Code 如何根據目錄定義工作區。

---

VS Code 應用程式資料夾。

### --feedback--

課程提到 VS Code 如何根據目錄定義工作區。

## --video-solution--

2

## --text--

為什麼課程建議使用網頁伺服器，而不是直接在瀏覽器中開啟 HTML 檔案？

## --answers--

載入速度更快，並且更容易在你的程式碼庫中發現問題。

### --feedback--

課程提到直接開啟 HTML 檔案時可能發生的特定問題。

---

它為大型軟體應用程式提供更佳的安全性。

### --feedback--

課程提到直接開啟 HTML 檔案時可能發生的特定問題。

---

它避免了意外的錯誤，例如 CSS 未正確載入。

---

它允許與團隊中的其他開發人員進行即時協作。

### --feedback--

課程提到直接開啟 HTML 檔案時可能發生的特定問題。

## --video-solution--

3

## --text--

哪一個 VS Code 擴充套件推薦用來輕鬆設定區域的網頁伺服器？

## --answers--

網頁伺服器。

### --feedback--

課程特別提到從市集安裝擴充套件來服務你的 HTML 檔案。

---

即時伺服器。

---

區域的主機。

### --feedback--

課程特別提到從市集安裝擴充套件來服務你的 HTML 檔案。

---

HTML 預覽。

### --feedback--

課程特別提到從市集安裝擴充套件來服務你的 HTML 檔案。

## --video-solution--

2
