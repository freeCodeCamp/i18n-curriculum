---
id: 672bb015cfc889794359c4e0
title: Quali sono le buone pratiche per progettare finestre modali?
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**NOTA**: Alcuni esempi interattivi potrebbero usare CSS e JavaScript che non hai ancora imparato. Non preoccuparti di capire tutto il codice. Lo scopo degli esempi è mostrarti anteprime di questi concetti di design per farti comprendere meglio come funzionano. Per vedere le anteprime, devi abilitare l'editor interattivo.

Cos’è una modale? È il tipo di pop-up che un sito web potrebbe mostrarti sopra il proprio contenuto. HTML ha un elemento `dialog` che puoi usare per creare modali.

Il contenuto dietro una modale è solitamente oscurato. Questo aiuta l’utente a concentrarsi visivamente sull’area con cui vuoi che interagisca – in questo caso, la modale.

È sempre una buona idea permettere all’utente di cliccare all’esterno della modale per chiuderla.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

Spesso vedrai pulsanti molto evidenti sulle modali. Questi si chiamano CTA, o call-to-action. Vuoi che siano facilmente riconoscibili perché lo scopo di interrompere il flusso dell’utente con una modale è invitarlo a compiere un’azione specifica.

Le modali dovrebbero avere anche un pulsante di chiusura. Anche se vuoi davvero che l’utente clicchi sulle tue CTA, è importante dargli la possibilità di uscire dalla modale e riprendere quello che stava facendo prima.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

Ci sono, ovviamente, questioni di accessibilità con le modali, come gestire correttamente il focus sugli elementi. Tuttavia, se usi queste pratiche generali come punto di partenza, avrai una base solida su cui costruire.

# --questions--

## --text--

Qual è lo scopo di oscurare il contenuto dietro una finestra modale?

## --answers--

Per risparmiare batteria sui dispositivi mobili.

### --feedback--

La lezione menziona una ragione specifica per questo effetto visivo.

---

Per aiutare gli utenti a identificare visivamente l’area con cui devono interagire.

---

Per far caricare il sito web più velocemente.

### --feedback--

La lezione menziona una ragione specifica per questo effetto visivo.

---

Per impedire agli utenti di vedere informazioni sensibili.

### --feedback--

La lezione menziona una ragione specifica per questo effetto visivo.

## --video-solution--

2

## --text--

Cosa significa CTA nel contesto del design delle modali?

## --answers--

Click To Activate.

### --feedback--

La lezione fornisce il termine completo per questo acronimo quando parla dei pulsanti evidenti nelle modali.

---

Complete The Action.

### --feedback--

La lezione fornisce il termine completo per questo acronimo quando parla dei pulsanti evidenti nelle modali.

---

Call-to-Action.

---

Close The Application.

### --feedback--

La lezione fornisce il termine completo per questo acronimo quando parla dei pulsanti evidenti nelle modali.

## --video-solution--

3

## --text--

Perché è importante includere un pulsante di chiusura in una finestra modale?

## --answers--

È richiesto dalla legge in alcuni paesi.

### --feedback--

La lezione sottolinea l’importanza di permettere agli utenti di uscire dalla modale.

---

Per evitare che la modale faccia crashare il browser.

### --feedback--

La lezione sottolinea l’importanza di permettere agli utenti di uscire dalla modale.

---

Per dare agli utenti la possibilità di uscire dalla modale e riprendere l’attività precedente.

---

Per far sembrare la modale più professionale.

### --feedback--

La lezione sottolinea l’importanza di permettere agli utenti di uscire dalla modale.

## --video-solution--

3
