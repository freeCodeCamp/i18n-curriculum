---
id: 6a226d36b5d21265afeadc82
title: WebSockets 퀴즈
challengeType: 8
dashedName: quiz-websockets
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답하세요.

# --quizzes--

## --quiz--

### --question--

#### --text--

Node.js가 WebSocket 서버에 외부 라이브러리를 요구하는 이유는 무엇인가요?

#### --distractors--

Node.js에는 내장 WebSocket 서버가 있지만 활성화하려면 추가 구성이 필요합니다.

---

Node.js는 Express 프레임워크를 사용할 때만 WebSocket을 지원합니다.

---

Node.js는 외부 패키지와 상관없이 WebSocket을 전혀 지원하지 않습니다.

#### --answer--

Node.js에는 내장 WebSocket 서버가 없습니다.

### --question--

#### --text--

Pub/Sub 아키텍처의 세 가지 주요 구성 요소는 무엇인가요?

#### --distractors--

발신자, 큐, 수신자.

---

생산자, 채널, 소비자.

---

클라이언트, 서버, 데이터베이스.

#### --answer--

퍼블리셔, 토픽, 구독자.

### --question--

#### --text--

HTTP는 어떤 통신 모델을 따르나요?

#### --distractors--

지속적인 양방향 모델.

---

브로드캐스트 모델.

---

퍼브/섭 모델.

#### --answer--

요청-응답 모델.

### --question--

#### --text--

새 클라이언트가 서버에 연결될 때 발생하는 WebSocket 이벤트는 무엇인가요?

#### --distractors--

`connect`

---

`open`

---

`join`

#### --answer--

`connection`

### --question--

#### --text--

Pub/Sub에 비해 직접 애플리케이션 간 통신의 주요 단점은 무엇인가요?

#### --distractors--

직접 통신은 항상 Pub/Sub보다 느립니다.

---

직접 통신은 애플리케이션이 한 번에 한 메시지만 보낼 수 있게 합니다.

---

직접 통신은 항상 제3자 인증 서버가 필요합니다.

#### --answer--

애플리케이션 간 불필요한 의존성이 생기며, 한 쪽의 변경이나 실패가 다른 쪽을 망가뜨릴 수 있습니다.

### --question--

#### --text--

WebSocket 연결은 업그레이드 전에 어떤 프로토콜로 시작하나요?

#### --distractors--

WebSocket 연결은 처음부터 전용 WebSocket 프로토콜로 시작합니다.

---

WebSocket 연결은 HTTP를 완전히 우회하고 TCP로 시작합니다.

---

모든 WebSocket 연결은 FTP로 시작한 후 WebSocket으로 업그레이드됩니다.

#### --answer--

모든 WebSocket 연결은 HTTP로 시작합니다.

### --question--

#### --text--

WebSocket 연결을 통해 클라이언트로 데이터를 보내려면 어떤 메서드를 호출하나요?

#### --distractors--

`socket.emit()`

---

`socket.write()`

---

`socket.push()`

#### --answer--

`socket.send()`

### --question--

#### --text--

Pub/Sub 시스템에서 토픽이 하는 역할은 무엇인가요?

#### --distractors--

메시지를 영구 저장하여 나중에 검색할 수 있게 합니다.

---

퍼블리셔가 메시지를 보내기 전에 인증합니다.

---

퍼블리셔를 특정 구독자와 직접 연결합니다.

#### --answer--

퍼블리셔와 구독자 사이의 중개자 역할을 합니다.

### --question--

#### --text--

HTTP 통신과 WebSocket 통신은 어떻게 다른가요?

#### --distractors--

HTTP는 양방향이고 WebSocket은 단방향입니다.

---

HTTP는 영구 연결을 유지하지만 WebSocket은 교환 후 연결을 닫습니다.

---

HTTP와 WebSocket은 모두 동일한 요청-응답 패턴을 따릅니다.

#### --answer--

HTTP는 `Client --> Request --> Server --> Response`를 따르고 WebSocket은 `Client <--> Server`입니다.

### --question--

#### --text--

연결된 클라이언트에 브로드캐스트하기 전에 `client.readyState === WebSocket.OPEN`을 확인해야 하는 이유는 무엇인가요?

#### --distractors--

각 클라이언트에 보내기 전에 메시지를 암호화하기 위해서입니다.

---

각 클라이언트에 초당 전송되는 메시지 수를 제한하기 위해서입니다.

---

클라이언트가 메시지를 받을 권한이 있는지 확인하기 위해서입니다.

#### --answer--

연결이 끊겼거나 아직 연결 중인 클라이언트에 보내는 오류를 방지하기 위해서입니다.

### --question--

#### --text--

WebSocket 업그레이드 요청을 서버가 수락할 때 보내는 HTTP 상태 코드는 무엇인가요?

#### --distractors--

`200 OK`

---

`301 Moved Permanently`

---

`426 Upgrade Required`

#### --answer--

`101 Switching Protocols`

### --question--

#### --text--

다음 중 Pub/Sub의 실제 사용 사례는 무엇인가요?

#### --distractors--

서버에서 HTML을 불러오는 정적 웹 페이지.

---

한 번에 하나씩 파일을 처리하는 명령줄 도구.

---

구조화된 데이터를 저장하고 쿼리하는 관계형 데이터베이스.

#### --answer--

모든 구독자에게 실시간 알림을 전달하는 알림 시스템.

### --question--

#### --text--

`3000` 라이브러리를 사용해 포트 `ws`에서 WebSocket 서버를 만드는 방법은 무엇인가요?

#### --distractors--

```js
const server = new WebSocket({ port: 3000 });
```

---

```js
const server = WebSocket.createServer(3000);
```

---

```js
const server = ws.listen(3000);
```

#### --answer--

```js
const server = new WebSocket.Server({ port: 3000 });
```

### --question--

#### --text--

연결이 종료될 때 발생하는 WebSocket 이벤트는 무엇인가요?

#### --distractors--

`disconnect`

---

`end`

---

`terminate`

#### --answer--

`close`

### --question--

#### --text--

Pub/Sub 메시지 흐름은 어떻게 되나요?

#### --distractors--

퍼블리셔가 구독자에게 보내고 --> 구독자가 토픽에 전달하고 --> 토픽이 메시지를 저장합니다.

---

토픽이 메시지를 보내고 --> 퍼블리셔가 받고 --> 퍼블리셔가 구독자에게 전달합니다.

---

구독자가 퍼블리셔에 연결하고 --> 퍼블리셔가 데이터를 보내고 --> 구독자가 수신을 확인합니다.

#### --answer--

구독자가 토픽을 구독하고 --> 퍼블리셔가 그 토픽에 메시지를 보내면 --> 메시징 시스템이 해당 토픽의 모든 구독자에게 전달합니다.

### --question--

#### --text--

HTTP와 WebSocket의 차이를 가장 잘 설명하는 비유는 무엇인가요?

#### --distractors--

HTTP는 전화 통화 같고 WebSocket은 편지를 주고받는 것 같습니다.

---

HTTP는 채팅방 같고 WebSocket은 신문 같습니다.

---

HTTP는 라이브 스트림 같고 WebSocket은 저장된 비디오 같습니다.

#### --answer--

HTTP는 편지를 주고받는 것 같고 WebSocket은 전화 통화 같습니다.

### --question--

#### --text--

연결된 클라이언트가 서버로 데이터를 보낼 때 발생하는 WebSocket 이벤트는 무엇인가요?

#### --distractors--

`data`

---

`receive`

---

`incoming`

#### --answer--

`message`

### --question--

#### --text--

다음 중 WebSocket 연결 흐름을 올바르게 설명한 것은 무엇인가요?

#### --distractors--

클라이언트가 TCP로 연결 --> 서버가 데이터 전송 --> 클라이언트가 수신 --> 연결 종료.

---

클라이언트가 데이터 전송 --> 서버가 업그레이드 요청 --> 연결 열림 --> 클라이언트가 확인.

---

서버가 연결 시작 --> 클라이언트가 수락 --> 양쪽이 데이터 교환 --> 연결 종료.

#### --answer--

클라이언트가 HTTP로 연결 --> 서버가 `101 Switching Protocols`로 응답 --> 연결 유지 --> 양쪽이 언제든지 데이터 교환.

### --question--

#### --text--

실시간 애플리케이션에서 WebSocket과 Pub/Sub는 어떻게 다른가요?

#### --distractors--

WebSocket은 인증을 처리하고 Pub/Sub는 데이터 저장을 처리합니다.

---

WebSocket은 서버 간 통신용이고 Pub/Sub는 클라이언트-서버 통신용입니다.

---

둘은 이름만 다른 같은 기술입니다.

#### --answer--

WebSocket은 통신 채널을 제공하고 Pub/Sub는 메시징 패턴을 제공합니다.

### --question--

#### --text--

다음 중 WebSocket의 실제 사용 사례는 무엇인가요?

#### --distractors--

기사들을 보여주는 정적 블로그 페이지.

---

서버 측 파일 렌더링 시스템.

---

사용자 기록을 저장하는 데이터베이스.

#### --answer--

채팅 애플리케이션.
