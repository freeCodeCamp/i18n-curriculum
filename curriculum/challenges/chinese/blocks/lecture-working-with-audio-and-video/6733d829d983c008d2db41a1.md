---
id: 6733d829d983c008d2db41a1
title: 什么是 Codec 以及它们如何工作？
challengeType: 19
dashedName: what-is-codecs-and-how-does-it-work
---

# --description--

让我们了解一下 codecs 及其工作原理。

如果你以前处理过视频或音频，你可能听说过 codec。codec 是 “encoder/decoder” 的缩写，是一种可以在模拟和数字的音频与视频之间转换的算法或软件。

编解码器可以作为 MIME 类型的一部分指定。定义编解码器的基本语法是在媒体类型后添加分号，然后是 `codecs=` 和编解码器。

例如，使用 Vorbis 编解码器的 OGG 音频文件的 MIME 类型可能是 `audio/ogg; codecs=vorbis`。或者，如果一个文件支持多种编解码器，你可以将它们指定为逗号分隔，但必须用引号括起来：`video/webm; codecs="vp8, vorbis"`。

但是某些文件类型的编解码器语法要复杂得多。例如，MP4 可能有一个 `codecs="avc1.4d002a"` 的编解码器，表示它是用 H.264 编码的。

但是你什么时候实际上会使用这些呢？你可以将它们包含在 `source` 元素的 `type` 属性中。这允许你为相同的格式指定不同的编解码器，从而在确定为该用户环境使用哪种格式时，给浏览器提供更细粒度的选项。

但你也可以将它们用作传递给全局 `MediaSource.isTypeSupported()` 方法的 MIME 类型的一部分。该方法接受一个 MIME 类型，如果环境可能支持它，则返回 `true`。或者更确切地说，如果无法为该文件类型实例化缓冲区，则返回 `false`。这种方法允许你以编程方式自行选择源，而不是依赖浏览器确定的“最佳”选项。

# --questions--

## --text--

在多媒体的上下文中，术语 "codec" 代表什么？

## --answers--

压缩/解压

### --feedback--

编解码器表示文件是如何被编码的。

---

编码/解码

### --feedback--

编解码器表示文件是如何被编码的。

---

编码器/解码器

---

加密/解密

### --feedback--

编解码器表示文件是如何被编码的。

## --video-solution--

3

## --text--

编解码器如何作为 MIME `type` 的一部分被指定？

## --answers--

通过在媒体类型前添加前缀。

### --feedback--

编解码器在 MIME 类型中指定，作为媒体类型后的一个属性。

---

通过在媒体类型后添加后缀。

### --feedback--

编解码器在 MIME 类型中指定，作为媒体类型后的一个属性。

---

通过在媒体类型后添加分号，随后添加 `codecs=` 和编解码器名称。

---

通过用编解码器名称完全替换媒体类型。

### --feedback--

编解码器在 MIME 类型中指定，作为媒体类型后的一个属性。

## --video-solution--

3

## --text--

`MediaSource.isTypeSupported()` 方法的目的是什么？

## --answers--

自动将文件转换为支持的格式。

### --feedback--

此方法尝试为指定的文件类型实例化一个缓冲区，以确定该类型是否被支持。

---

查看当前环境中是否可能支持特定的 MIME `type` 和 `codec`。

---

迫使浏览器使用特定的 codec。

### --feedback--

此方法尝试为指定的文件类型实例化一个缓冲区，以确定该类型是否被支持。

---

列出当前环境中所有支持的编解码器。

### --feedback--

此方法尝试为指定的文件类型实例化一个缓冲区，以确定该类型是否被支持。

## --video-solution--

2
