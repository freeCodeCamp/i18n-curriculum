---
id: 66edc47c11492ac5cf258ad9
title: JavaScript 比較與條件判斷測驗
challengeType: 8
dashedName: quiz-javascript-comparisons-and-conditionals
---

# --description--

要通過測驗，你必須正確回答以下 10 題中的至少 9 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

`undefined > 0` 這個表達式的結果是什麼？

#### --distractors--

`true`

---

`undefined`

---

`null`

#### --answer--

`false`

### --question--

#### --text--

`||` 表現哪一種邏輯操作？

#### --distractors--

異或

---

和

---

不重要

#### --answer--

或

### --question--

#### --text--

以下 `code` 的輸出是什麼？

```js
console.log(5 === 2 + 3 || 4 == 2);
```

#### --distractors--

`undefined`

---

`false`

---

引發錯誤。

#### --answer--

`true`

### --question--

#### --text--

什麼是 truthy 和 falsy 值？

#### --distractors--

一個同時為 `true` 和 `false` 的值。

---

根據背景關係而改變的值。

---

一個與 `true` 或 `false` 值功能不同的值。

#### --answer--

在布林值背景關係中被視為 `true` 或 `false` 的值。

### --question--

#### --text--

以下程式碼會在主控台記錄什麼？

```js
if (1) {
  console.log("True!");
}
```

#### --distractors--

`false`

---

主控台不會列印任何內容。

---

引發錯誤。

#### --answer--

`True!`

### --question--

#### --text--

`undefined` 和 `null` 有什麼差別？

#### --distractors--

`null` 和 `undefined` 指向一個超出範圍的記憶體位置且無法存取。

---

`null` 是隱式指定給變數的值且無法更改，而 `undefined` 是明確指定給變數的值。

---

`null` 是一個全域的屬性，`undefined` 不是。

#### --answer--

沒有值的變數是 `undefined`，而 `null` 表示有意缺少物件值。

### --question--

#### --text--

當你在實作 `switch` 述語時沒有包含 `break`，會發生什麼情況？

#### --distractors--

`switch` 述語會突然停止。

---

`switch` 述語會在前一個述語之後發出錯誤。

---

`switch` 述語會在第一次匹配後離開。

#### --answer--

即使找到匹配，程式碼仍會繼續評估以下的 `case` 述語。

### --question--

#### --text--

以下程式碼會在主控台列印什麼？

```js
const a = 2;
if (1 == "1") {
  let b = 3;
  console.log(a + b);
}
console.log(b);
```

#### --distractors--

引發錯誤。

---

```js
5
3
```

---

```js
5
undefined
```

#### --answer--

`5`，然後引發錯誤。

### --question--

#### --text--

以下 JavaScript 程式碼的輸出會是什麼？

```js
let vehicle = "car";

switch (vehicle) {
  case "bike":
    console.log("Bikes are two-wheelers.");
    break;
  case "car":
    console.log("Some cars are 4x4.");
  case "truck":
    console.log("Trucks can carry heavy loads.");
    break;
  default:
    console.log("Unknown vehicle.");
}
```

#### --distractors--

```md
Some cars are 4x4.
```

---

```md
Some cars are 4x4.
Trucks can carry heavy loads.
Unknown vehicle.
```

---

```md
Unknown vehicle.
```

#### --answer--

```md
Some cars are 4x4.
Trucks can carry heavy loads.
```

### --question--

#### --text--

以下程式碼會在主控台列印什麼？  

```js
let x = 5;
if (x > 1 && x < 10) {
  console.log("x is between 1 and 10");
} else {
  console.log("x is not between 1 and 10");
}
```

#### --distractors--

引發錯誤。

---

沒有任何東西被列印。

---

`x is not between 1 and 10`

#### --answer--

`x is between 1 and 10`

