---
id: 5900f3aa1000cf542c50febd
title: '문제 62: 세제곱수 순열'
challengeType: 1
forumTopicId: 302174
dashedName: problem-62-cubic-permutations
---

# --description--

세제곱수 41063625 ($345^3$)는 순열을 바꾸어 두 개의 다른 세제곱수인 56623104 ($384^3$)와 66430125 ($405^3$)를 만들 수 있습니다. 실제로 41063625는 정확히 세 개의 자릿수 순열이 세제곱수인 가장 작은 세제곱수입니다.

정확히 `n`개의 자릿수 순열이 세제곱수인 가장 작은 세제곱수를 찾으세요.

# --hints--

`cubicPermutations(2)`은 숫자를 반환해야 합니다.

```js
assert(typeof cubicPermutations(2) === 'number');
```

`cubicPermutations(2)`는 `125`을 반환해야 합니다.

```js
assert.strictEqual(cubicPermutations(2), 125);
```

`cubicPermutations(3)`는 `41063625`를 반환해야 합니다.

```js
assert.strictEqual(cubicPermutations(3), 41063625);
```

`cubicPermutations(4)`는 `1006012008`을 반환해야 합니다.

```js
assert.strictEqual(cubicPermutations(4), 1006012008);
```

`cubicPermutations(5)`는 `127035954683`를 반환해야 합니다.

```js
assert.strictEqual(cubicPermutations(5), 127035954683);
```

# --seed--

## --seed-contents--

```js
function cubicPermutations(n) {

  return true;
}

cubicPermutations(2);
```

# --solutions--

```js
function cubicPermutations(n) {
  function getDigits(num) {
    const digits = [];
    while (num > 0) {
      digits.push(num % 10);
      num = Math.floor(num / 10);
    }
    return digits;
  }

  function getCube(num) {
    return num ** 3;
  }

  const digitsToCubeCounts = {};
  let curNum = 1;
  let digits;

  while (!digitsToCubeCounts[digits] || digitsToCubeCounts[digits].count < n) {
    const cube = getCube(curNum);
    digits = getDigits(cube).sort().join();
    if (!digitsToCubeCounts[digits]) {
      digitsToCubeCounts[digits] = {
        count: 1,
        smallestCube: cube
      };
    } else {
      digitsToCubeCounts[digits].count += 1;
    }

    curNum++;
  }
  return digitsToCubeCounts[digits].smallestCube;
}
```
