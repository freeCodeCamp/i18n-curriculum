---
id: 671682b3983489a819507553
title: 如何使用内联框架元素在页面上嵌入视频？
challengeType: 19
dashedName: how-do-you-embed-videos-onto-your-page-using-the-iframe-element
---

# --description--

首先，究竟什么是 `iframe` 元素？ `iframe` 代表内联框架。 它是一种内联元素，用于在 HTML 页面中直接嵌入其他 HTML 内容。 这些 HTML 内容可以是视频、地图、其他 HTML 元素，甚至是其他网页。 下面是 `iframe` 元素的语法：

```html
<iframe
  src="video-url"
  width="width-value"
  height="height-value"
  allowfullscreen
></iframe>
```

`src` 属性指定了要嵌入的页面的 URL。 `width` 属性指定 `iframe` 的宽度。 `height` 属性指定 `iframe` 的高度。 `allowfullscreen` 属性允许用户以全屏模式显示 `iframe` 。 为 `iframe` 指定 `title` 属性也是一种好的做法，因为它对可访问性非常重要。

要在 `iframe` 中嵌入视频，你可以直接从 YouTube 和 Vimeo 等流行的视频服务中复制视频链接，或者使用指向视频 URL 的 `src` 属性自定义嵌入方式。 下面是一个嵌入来自 YouTube 的 freeCodeCamp 热门课程的示例：

```html
<h1>A freeCodeCamp YouTube Video Embedded with the iframe Element</h1>

<iframe
  width="560"
  height="315"
  src="https://www.youtube.com/embed/PkZNo7MFNFg?si=-UBVIUNM3csdeiWF"
  title="YouTube video player"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
  referrerpolicy="strict-origin-when-cross-origin"
  allowfullscreen
></iframe>
```

稍后，你可以根据自己的需要自定义 `iframe` ，甚至可以通过复制 YouTube 视频链接并将其粘贴为 URL 值来定义自己的视频。 需要注意的是，你必须将 https://youtu.be 替换为 https://youtube.com/embed/。

youtu.be 是复制分享链接时的域名，而 youtube.com/embed/ 是使用 `iframe` 元素嵌入视频时所需的域名。 这是一个示例：

```html
<iframe
  src="https://youtube.com/embed/gp5H0Vw39yw?si=Rb_2nDK6abv1iIAM"
  title="freeCodeCamp Typescript Course"
  width="500"
  height="285"
  allowfullscreen
></iframe>
```

注意，视频可以来自任何地方。 它不一定要来自 YouTube 和 Vimeo 等视频服务。 下面是一段用 `iframe` 元素嵌入的 Pixabay 视频：

```html
<h1>A Video from Pixabay Embedded with the iframe Element</h1>

<iframe
  src="https://cdn.pixabay.com/video/2022/07/24/125310-733046613_large.mp4"
  width="500"
  height="285"
></iframe>
```

别忘了，你还可以在 `iframe` 元素中嵌入地图、其他网页或直接嵌入 HTML。 下面是我使用 `iframe` 元素嵌入的地图：

```html
<h1>A Map from Openstreetmap.org Embedded with the iframe Element</h1>

<iframe
  width="425"
  height="350"
  src="https://www.openstreetmap.org/export/embed.html?bbox=3.006134033203125%2C6.150112578753815%2C3.6357879638671875%2C6.749850810550778&amp;layer=mapnik"
  style="border: 1px solid black"
>
</iframe>
<br />
<small>
  <a href="https://www.openstreetmap.org/#map=11/6.4501/3.3210">
    View Larger Map
  </a>
</small>
```

如果你想在 `iframe` 元素中直接嵌入 HTML，则必须使用 `srcdoc` 属性而不是 `src`。

# --questions--

## --text--

iframe 是什么意思？

## --answers--

国际框架。

### --feedback--

复习讲座的开头部分，获取答案。

---

内联表单。

### --feedback--

复习讲座的开头部分，获取答案。

---

内联框架。

---

内联表单元素。

### --feedback--

复习讲座的开头部分，获取答案。

## --video-solution--

3

## --text--

`iframe` 元素的哪个属性指定了要嵌入内容的位置？

## --answers--

`src`

---

`url`

### --feedback--

想想表示 "源 "的属性。

---

`frameborder`

### --feedback--

想想表示 "源 "的属性。

---

`cross-origin`

### --feedback--

想想表示 "源 "的属性。

## --video-solution--

1

## --text--

如果要直接嵌入 HTML，应使用 `iframe` 元素的哪个属性而不是 `src`？

## --answers--

`html`

### --feedback--

寻找与 src 相关的属性。

---

`document`

### --feedback--

找出与 src 相关的属性。

---

`srcweb`

### --feedback--

找出与 src 相关的属性。

---

`srcdoc`

## --video-solution--

4
