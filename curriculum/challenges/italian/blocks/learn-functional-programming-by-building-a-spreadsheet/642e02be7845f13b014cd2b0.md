---
id: 642e02be7845f13b014cd2b0
title: Passaggio 10
challengeType: 0
dashedName: step-10
---

# --description--

Ora che hai una funzione `range`, puoi usarla anche per creare un intervallo di lettere.

Dichiara una funzione `charRange` usando `const` e la sintassi arrow. Deve prendere un parametro `start` e un parametro `end`. La funzione deve restituire implicitamente il risultato della chiamata a `range()` con `start` e `end` come argomenti.

# --hints--

Dovresti dichiarare una variabile `charRange`.

```js
assert.match(code, /(?:let|const|var)\s+charRange/);
```

La tua variabile `charRange` dovrebbe essere dichiarata con `const`.

```js
assert.match(code, /const\s+charRange/);
```

La tua variabile `charRange` dovrebbe essere una funzione.

```js
assert.isFunction(charRange);
```

La tua funzione `charRange` dovrebbe usare la sintassi arrow.

```js
assert.match(code, /const\s+charRange\s*=\s*\(.*\)\s*=>/);
```

La tua funzione `charRange` dovrebbe prendere `start` come primo parametro.

```js
assert.match(code, /const\s+charRange\s*=\s*\(\s*start/);
```

La tua funzione `charRange` dovrebbe prendere `end` come secondo parametro.

```js
assert.match(code, /const\s+charRange\s*=\s*\(\s*start\s*,\s*end\s*\)/);
```

La tua funzione `charRange` dovrebbe usare un return implicito.

```js
assert.notMatch(code, /const\s+charRange\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*{/);
```

La tua funzione `charRange` dovrebbe chiamare la tua funzione `range`.

```js
assert.match(code, /const\s+charRange\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*range\(/);
```

Dovresti passare `start` e `end` come argomenti alla tua chiamata `range`.

```js
assert.match(code, /const\s+charRange\s*=\s*\(\s*start\s*,\s*end\s*\)\s*=>\s*range\(\s*start\s*,\s*end\s*\)/);
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

```css
#container {
  display: grid;
  grid-template-columns: 50px repeat(10, 200px);
  grid-template-rows: repeat(11, 30px);
}

.label {
  background-color: lightgray;
  text-align: center;
  vertical-align: middle;
  line-height: 30px;
}
```

```js
const range = (start, end) => Array(end - start + 1).fill(start).map((element, index) => element + index);
--fcc-editable-region--

--fcc-editable-region--

window.onload = () => {
  const container = document.getElementById("container");
  const createLabel = (name) => {
    const label = document.createElement("div");
    label.className = "label";
    label.textContent = name;
    container.appendChild(label);
  }
}
```
