---
id: 66f1a2009e65c9a40a26d51e
title: React 基礎測驗
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一項不是 JavaScript 函式庫或框架？

#### --distractors--

Angular

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

以下哪一項不是 React 中條件式呈現的方法？

#### --distractors--

三元運算子。

---

邏輯 `&&` 運算子。

---

`if` 述語。

#### --answer--

`setTimeout`

### --question--

#### --text--

在 React 中呈現列表時，為什麼要為元素添加 `key` 屬性的主要目的為何？

#### --distractors--

為你的每個列表項目套用獨特的 CSS 樣式。

---

作為該元素的文字內容。

---

為列表中每個特定項目添加事件監聽器。

#### --answer--

幫助 React 識別項目以進行高效呈現。

### --question--

#### --text--

專案的軟體包和函式庫安裝在哪裡？

#### --distractors--

你電腦的使用者目錄。

---

`package.json` 檔案。

---

`src` 資料夾。

#### --answer--

`node_modules` 資料夾。

### --question--

#### --text--

以下關於 JSX，哪一項是正確的？

#### --distractors--

JSX 必須始終與 JavaScript 分別寫在不同的檔案中。

---

JSX 無法使用 JavaScript 表達式。

---

不建議在 React 應用程式中使用 JSX。

#### --answer--

JSX 允許在 JavaScript 程式碼中撰寫 HTML。

### --question--

#### --text--

以下哪一項是 SPA 常見的問題？

#### --distractors--

由於持續的頁面呈現，伺服器資源使用更為頻繁。

---

由於頻繁的 DOM 更新，瀏覽器快取變得不可靠。

---

多次頁面載入會產生不一致的狀態管理。

#### --answer--

螢幕閱讀器難以處理動態更新的內容。

### --question--

#### --text--

單頁應用程式通常如何更新視圖？

#### --distractors--

透過完整的伺服器載入。

---

透過重新載入整個 HTML 文件。

---

僅使用伺服端呈現。

#### --answer--

只更新頁面中必要的零件。

### --question--

#### --text--

以下哪個指令會以開發模式啟動 React 專案？

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

以下哪一項不是有效的 JSX 元素？

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

哪一個 JavaScript 陣列方法常用於將一個資料陣列轉換成 JSX 元素陣列？

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

以下哪一項是傳遞 React 中 `key` 屬性的最佳實踐？

#### --distractors--

在所有情況下使用陣列索引。

---

在每次 `render` 時隨機產生金鑰。

---

完全省略 `key` prop。

#### --answer--

使用來自資料的唯一且穩定的 ID。

### --question--

#### --text--

什麼指令會安裝列在 `package.json` 中的相依性？

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

你會如何將 `name` prop 傳遞給子組件？

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

在 React 中，使用展開運算子傳遞多個 props 的正確方式是什麼？

#### --distractors--

`<Child person={name..., age..., city...} />`。

---

`<Child spreader={...person} />`。

---

`<Child spread={person} />`。

#### --answer--

`<Child {...person} />`。

### --question--

#### --text--

React 的建置區塊是什麼？

#### --distractors--

函式（程式）和模組。

---

DOM 元素和事件。

---

類別和物件。

#### --answer--

可重複使用的組件。

### --question--

#### --text--

在 React 中為行內樣式添加的正確方式是什麼？

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

條件式呈現 React 組件的正確方式是什麼？

#### --distractors--

在 JSX 中使用 `if` 述語。

---

在 `render()` 方法內宣告一個新的組件。

---

在 JSX 中使用 `while` 迴圈。

#### --answer--

在 JSX 中使用三元運算子或邏輯 `&&` 運算子。

### --question--

#### --text--

哪個關鍵字用來將組件引入另一個檔案？

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

SPAs 的完整意義是什麼？

#### --distractors--

伺服端頁面架構。

---

選擇性頁面應用程式。

---

靜態進階應用程式。

#### --answer--

單頁應用程式。

### --question--

#### --text--

React 中用來描述 UI 的 JavaScript 語法擴充套件是哪一個？

#### --distractors--

TSC

---

HTML+

---

ReactML

#### --answer--

JSX
