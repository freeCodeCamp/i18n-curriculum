---
id: 67f412eaf6e68343aab8ea81
title: 오류 처리 퀴즈
challengeType: 8
dashedName: quiz-error-handling
---

# --description--

퀴즈를 통과하려면 아래 10문제 중 최소 9문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

이 코드의 출력 결과는 무엇일까요?

  ```py
try:
    print(22 / 0)
except ZeroDivisionError:
    print("You can't divide by zero!")
  ```

#### --distractors--

`22`

---

`10`

---

`0`

#### --answer--

`You can't divide by zero!`

### --question--

#### --text--

Python 표준 라이브러리의 어떤 모듈이 코드를 대화형으로 디버깅할 수 있게 해줄까요?

#### --distractors--

ABC

---

numpy

---

debugpy

#### --answer--

pdb

### --question--

#### --text--

존재하지 않는 메서드나 속성을 사용하려 할 때 Python이 발생시키는 예외는 무엇인가요?

#### --distractors--

`SyntaxError`

---

`AttributeBug`

---

`SyntaxBug`

#### --answer--

`AttributeError`

### --question--

#### --text--

디버깅 중 변수 값을 확인하기 위해 코드 여러 지점에 삽입할 수 있는 Python 문장은 무엇인가요?

#### --distractors--

`len()`

---

`console()`

---

`log()`

#### --answer--

`print()`

### --question--

#### --text--

수동으로 예외를 발생시키는 문장은 무엇인가요?

#### --distractors--

`if`

---

`throw`

---

`from`

#### --answer--

`raise`

### --question--

#### --text--

Python 코드에서 `print("Hello world"` 코드는 어떤 오류를 발생시킬까요?

#### --distractors--

오류를 발생시키지 않습니다.

---

`ValueError`

---

`NameError`

#### --answer--

`SyntaxError`

### --question--

#### --text--

Python에서 `try...except`는 무엇을 할 수 있게 해주나요?

#### --distractors--

코드를 대화형으로 테스트할 수 있는 방법을 제공합니다.

---

수학 표현식을 작성할 수 있게 해줍니다.

---

테스트 속도를 높여줍니다.

#### --answer--

예외가 발생할 수 있는 코드 블록을 실행할 수 있게 해줍니다.

### --question--

#### --text--

더 나은 디버깅과 오류 메시지 직접 출력을 위해 예외 자체에 접근할 수 있게 해주는 객체는 무엇인가요?

#### --distractors--

Debugger Object

---

Traceback Object

---

BugFinder Object

#### --answer--

Exception Object

### --question--

#### --text--

`try...except` 문장에 추가할 수 있는 선택적 절은 무엇인가요?

#### --distractors--

`if` 및 `else`

---

`elif` 및 `if`

---

`else` 및 `elif`

#### --answer--

`else` 및 `finally`

### --question--

#### --text--

오류 발생 여부와 상관없이 실행되는 `try` 문장의 블록은 무엇인가요?

#### --distractors--

`else`

---

`except`

---

`try`

#### --answer--

`finally`
