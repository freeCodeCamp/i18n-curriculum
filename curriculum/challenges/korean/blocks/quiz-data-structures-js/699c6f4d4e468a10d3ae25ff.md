---
id: 699c6f4d4e468a10d3ae25ff
title: 자료 구조 퀴즈
challengeType: 8
dashedName: quiz-data-structures-js
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답하세요.

# --quizzes--

## --quiz--

### --question--

#### --text--

알고리즘 분석에서 Big O 표기법은 무엇을 설명하나요?

#### --distractors--

특정 컴퓨터에서의 정확한 실행 시간(초).

---

실행 중 실행된 코드 줄의 비율.

---

다른 개발자가 코드를 얼마나 읽기 쉬운지.

#### --answer--

입력 크기에 따라 시간 또는 공간이 어떻게 증가하는지(상한선).

### --question--

#### --text--

알고리즘 도전 과제를 시작할 때 가장 좋은 첫 단계는 무엇인가요?

#### --distractors--

모멘텀을 얻기 위해 바로 코딩을 시작한다.

---

문제를 이해하기 전에 성능 최적화를 한다.

---

해결책을 완성한 후에만 단위 테스트를 작성한다.

#### --answer--

예제와 극한 사례로 문제와 제약 조건을 명확히 한다.

### --question--

#### --text--

동적 배열과 정적 배열의 주요 차이점은 무엇인가요?

#### --distractors--

동적 배열은 서로 다른 타입의 값을 저장하고, 정적 배열은 그렇지 않다.

---

정적 배열은 중복 값을 허용하고, 동적 배열은 허용하지 않는다.

---

동적 배열은 모든 연산에서 정적 배열보다 빠르다.

#### --answer--

동적 배열은 크기를 조절해 늘어나거나 줄어들 수 있고, 정적 배열은 크기가 고정되어 있다.

### --question--

#### --text--

동적 배열 끝에 요소를 추가할 때의 상쇄 시간 복잡도는 무엇인가요?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` 상쇄

### --question--

#### --text--

단일 연결 리스트에서 인덱스 k번째 요소에 접근하는 데 `O(n)` 시간이 걸리는 이유는 무엇인가요?

#### --distractors--

접근 전에 리스트 크기를 조절해야 한다.

---

인덱스가 해시되어 표에서 조회된다.

---

노드가 연속적으로 저장되어 있어 이동이 필요하다.

#### --answer--

헤드 노드부터 k번째 노드까지 하나씩 순회해야 한다.

### --question--

#### --text--

단일 연결 리스트에는 없고 이중 연결 리스트에 있는 기능은 무엇인가요?

#### --distractors--

`O(1)` 시간에 임의 인덱스에 랜덤 접근.

---

더 빠른 반복을 위한 내장 배열 버퍼.

---

리스트 길이를 자동으로 일정하게 유지.

#### --answer--

앞뒤 노드를 가리키는 포인터로 역방향 순회가 가능하다.

### --question--

#### --text--

스택을 가장 잘 설명하는 것은 무엇인가요?

#### --distractors--

앞에서 제거하는 선입선출(FIFO, `FIFO`).

---

`O(1)` 시간에 임의 요소를 제거할 수 있는 구조.

---

상수 시간 랜덤 접근이 가능한 원형 버퍼.

#### --answer--

후입선출(LIFO, `LIFO`)으로 `push`와 `pop`이 위에 있다.

### --question--

#### --text--

큐에서 앞쪽 요소를 제거하는 연산은 무엇인가요?

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

알고리즘 설계에서 엣지 케이스란 무엇인가요?

#### --distractors--

프로그램 실행 중 항상 런타임 오류를 일으키는 입력입니다.

---

알고리즘 성능을 측정하기 위해 무작위로 생성된 입력입니다.

---

테스트 중 절대 고려해서는 안 되는 잘못된 입력입니다.

#### --answer--

알고리즘이 처리해야 하는 경계에 있는 특정하고 유효한 입력입니다.

### --question--

#### --text--

단일 연결 리스트에서 헤드 노드란 무엇인가요?

#### --distractors--

리스트 중간에 위치한 노드입니다.

---

리스트에서 가장 큰 값을 가진 노드입니다.

---

리스트에 가장 최근에 추가된 노드입니다.

#### --answer--

리스트의 첫 번째 노드로, 보통 유일하게 직접 접근 가능한 노드입니다.

### --question--

#### --text--

동적 배열에서 끝이 아닌 인덱스 i에 요소를 삽입할 때 최악의 시간 복잡도는 무엇인가요?

#### --distractors--

`O(1)`

---

`O(log n)`

---

`O(1)` 상쇄

#### --answer--

`O(n)`

### --question--

#### --text--

단일 연결 리스트의 헤드에 새 노드를 삽입하는 시간 복잡도는 무엇인가요?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

스택에서 요소를 제거하지 않고 맨 위 요소를 반환하는 연산은 무엇인가요?

#### --distractors--

`push`

---

`pop`

---

맨 아래에 삽입.

#### --answer--

`peek`

### --question--

#### --text--

큐를 가장 잘 설명하는 것은 무엇인가요?

#### --distractors--

후입선출(LIFO, `LIFO`)으로 위에서 제거.

---

`O(1)` 시간에 임의 인덱스에 랜덤 접근.

---

요소가 항상 자동으로 정렬된 상태로 유지된다.

#### --answer--

선입선출(FIFO, `FIFO`)으로 뒤에서 `enqueue`, 앞에서 `dequeue`.

### --question--

#### --text--

언어에 독립적인 알고리즘 논리의 고수준 설명을 무엇이라 하나요?

#### --distractors--

구문

---

기계어

---

디버거

#### --answer--

의사 코드

### --question--

#### --text--

문제를 이해하기 위해 해야 할 일 중 하나는 무엇인가요?

#### --distractors--

즉시 코딩을 시작한다.

---

극한 사례를 무시한다.

---

바로 최적화로 넘어간다.

#### --answer--

문제 설명을 여러 번 읽는다.

### --question--

#### --text--

문제를 해결할 때 식별해야 할 두 가지 구성 요소는 무엇인가요?

#### --distractors--

프로그래밍 언어와 IDE.

---

변수 이름과 기타 식별자.

---

색상 구성표와 코드 포맷팅.

#### --answer--

입력과 예상 출력.

### --question--

#### --text--

n이 커질 때 `O(n log n)`보다 더 빠르게 증가하는 시간 복잡도는 무엇인가요?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

브루트포스 해결책을 구현한 후 좋은 다음 단계는 무엇인가요?

#### --distractors--

측정 전에 상수 계수를 미세 최적화한다.

---

테스트를 버리고 해결책을 처음부터 다시 작성한다.

---

코드를 단순하게 유지하려고 극한 사례를 고려하지 않는다.

#### --answer--

시간/공간 복잡도를 분석하고 병목 구간을 최적화한다.

### --question--

#### --text--

공간 복잡도는 무엇을 측정하나요?

#### --distractors--

프로그램이 사용하는 CPU 코어 수.

---

프로그램 코드 줄 수.

---

프로그램 컴파일 시간.

#### --answer--

입력 크기에 따라 메모리 사용량이 어떻게 증가하는지.
