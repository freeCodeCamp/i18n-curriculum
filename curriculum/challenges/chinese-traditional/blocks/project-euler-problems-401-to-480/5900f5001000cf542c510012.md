---
id: 5900f5001000cf542c510012
title: '問題 404：交錯簡略號'
challengeType: 1
forumTopicId: 302072
dashedName: problem-404-crisscross-ellipses
---

# --description--

$E_a$ 是一個簡略號，其方程式為 $x^2 + 4y^2 = 4a^2$。

$E_a'$ 是 $E_a$ 以原點 $O(0, 0)$ 為中心逆時針旋轉 $θ$ 度後的影像，且 $0° < θ < 90°$。

<img alt="ellipse E_a and ellipse rotated by θ degrees E_a'" src="https://cdn.freecodecamp.org/curriculum/project-euler/crisscross-ellipses.gif" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

$b$ 是距離原點最近的兩個交點到原點的距離，$c$ 是另外兩個交點的距離。

我們將有序三元組（$a$、$b$、$c$）稱為典範橢球三元組，當且僅當 $a$、$b$ 和 $c$ 是正整數。

例如，(209, 247, 286) 是一個典範的橢球三元組。

令 $C(N)$ 為 $a ≤ N$ 時不同標準橢球三元組 ($a$、$b$、$c$) 的數量。

可以驗證 $C({10}^3) = 7$、$C({10}^4) = 106$ 和 $C({10}^6) = 11\\,845$。

尋找 $C({10}^{17})$。

# --hints--

`crisscrossEllipses()` 應該傳回 `1199215615081353`。

```js
assert.strictEqual(crisscrossEllipses(), 1199215615081353);
```

# --seed--

## --seed-contents--

```js
function crisscrossEllipses() {

  return true;
}

crisscrossEllipses();
```

# --solutions--

```js
// solution required
```
