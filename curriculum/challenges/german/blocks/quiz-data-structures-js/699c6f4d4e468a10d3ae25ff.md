---
id: 699c6f4d4e468a10d3ae25ff
title: Quiz zu Datenstrukturen
challengeType: 8
dashedName: quiz-data-structures-js
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was beschreibt die Big-O-Notation in der Algorithmusanalyse?

#### --distractors--

Die genaue Laufzeit in Sekunden für einen bestimmten Computer.

---

Der Prozentsatz der während eines Laufs ausgeführten Codezeilen.

---

Wie gut der Code für andere Entwickler lesbar ist.

#### --answer--

Wie Zeit- oder Speicherbedarf relativ zur Eingabegröße wächst (eine obere Schranke).

### --question--

#### --text--

Was ist der beste erste Schritt, wenn Sie eine algorithmische Aufgabe beginnen?

#### --distractors--

Beginnen Sie sofort mit dem Codieren, um Schwung zu gewinnen.

---

Optimieren Sie die Leistung, bevor Sie das Problem verstehen.

---

Schreiben Sie Unit-Tests erst nach Fertigstellung der Lösung.

#### --answer--

Klären Sie das Problem und die Einschränkungen mit Beispielen und Randfällen.

### --question--

#### --text--

Was ist der entscheidende Unterschied zwischen dynamischen und statischen Arrays?

#### --distractors--

Dynamische Arrays speichern Werte unterschiedlicher Typen; statische Arrays nicht.

---

Statische Arrays erlauben doppelte Werte; dynamische Arrays nicht.

---

Dynamische Arrays sind bei jeder Operation schneller als statische Arrays.

#### --answer--

Dynamische Arrays können durch Größenänderung wachsen oder schrumpfen; statische Arrays haben eine feste Größe.

### --question--

#### --text--

Wie lautet die amortisierte Zeitkomplexität, um ein Element am Ende eines dynamischen Arrays anzufügen?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` amortisiert

### --question--

#### --text--

Warum dauert der Zugriff auf das k-te Element per Index in einer einfach verketteten Liste `O(n)` Zeit?

#### --distractors--

Die Liste muss vor jedem Zugriff neu dimensioniert werden.

---

Der Index wird gehasht und in einer Tabelle nachgeschlagen.

---

Knoten sind zusammenhängend gespeichert, daher ist Verschieben erforderlich.

#### --answer--

Man muss vom Kopf-Knoten aus den Weg zum k-ten Knoten einzeln ablaufen.

### --question--

#### --text--

Welche Funktion hat eine doppelt verkettete Liste, die eine einfach verkettete Liste nicht hat?

#### --distractors--

Zufälliger Zugriff auf jeden Index in `O(1)` Zeit.

---

Eingebauter Array-Puffer für schnellere Iteration.

---

Automatische Wartung der Listenlänge als Konstante.

#### --answer--

Zeiger auf sowohl den nächsten als auch den vorherigen Knoten, die Rückwärtsdurchlauf ermöglichen.

### --question--

#### --text--

Welche der folgenden Beschreibungen trifft am besten auf einen Stack zu?

#### --distractors--

First In, First Out (`FIFO`) mit Entfernen am Anfang.

---

Eine Struktur, bei der jedes Element in `O(1)` Zeit entfernt werden kann.

---

Ein Ringpuffer mit konstantem Zeit-Zufallszugriff.

#### --answer--

Last In, First Out (`LIFO`) mit `push` und `pop` oben.

### --question--

#### --text--

Welche Operation entfernt das Element am Anfang einer Queue?

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

Was sind Randfälle im Algorithmendesign?

#### --distractors--

Eingaben, die während der Programmausführung immer Laufzeitfehler verursachen.

---

Zufällig generierte Eingaben, um die Leistung eines Algorithmus zu messen.

---

Ungültige Eingaben, die während des Testens niemals berücksichtigt werden sollten.

#### --answer--

Spezifische, gültige Eingaben an den Grenzen dessen, was ein Algorithmus verarbeiten sollte.

### --question--

#### --text--

Was ist der Kopf-Knoten in einer einfach verketteten Liste?

#### --distractors--

Der Knoten, der sich in der Mitte der Liste befindet.

---

Der Knoten mit dem größten Wert in der Liste.

---

Der zuletzt hinzugefügte Knoten in der Liste.

#### --answer--

Der erste Knoten in der Liste, normalerweise der einzige direkt zugängliche Knoten.

### --question--

#### --text--

Wie lautet die Worst-Case-Zeitkomplexität, um in einem dynamischen Array ein Element an Index i (nicht am Ende) einzufügen?

#### --distractors--

`O(1)`

---

`O(log n)`

---

`O(1)` amortisiert

#### --answer--

`O(n)`

### --question--

#### --text--

Wie lautet die Zeitkomplexität, um einen neuen Knoten am Kopf einer einfach verketteten Liste einzufügen?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

Welche Operation gibt das oberste Element eines Stacks zurück, ohne es zu entfernen?

#### --distractors--

`push`

---

`pop`

---

Am unteren Ende einfügen.

#### --answer--

`peek`

### --question--

#### --text--

Welche der folgenden Beschreibungen trifft am besten auf eine Queue zu?

#### --distractors--

Last In, First Out (`LIFO`) mit Entfernen oben.

---

Zufälliger Zugriff auf jeden Index in `O(1)` Zeit.

---

Elemente werden immer automatisch in sortierter Reihenfolge gehalten.

#### --answer--

First In, First Out (`FIFO`) mit `enqueue` hinten und `dequeue` vorne.

### --question--

#### --text--

Wie nennt man eine sprachunabhängige, abstrakte Beschreibung der Algorithmuslogik?

#### --distractors--

Syntax

---

Maschinencode

---

Debugger

#### --answer--

Pseudocode

### --question--

#### --text--

Was sollten Sie tun, um ein Problem zu verstehen?

#### --distractors--

Sofort mit dem Codieren beginnen.

---

Randfälle ignorieren.

---

Direkt zur Optimierung springen.

#### --answer--

Die Problemstellung mehrfach lesen.

### --question--

#### --text--

Welche zwei Komponenten sollten Sie bei der Problemlösung identifizieren?

#### --distractors--

Die Programmiersprache und die IDE.

---

Die Variablennamen und andere Bezeichner.

---

Das Farbschema und die Codeformatierung.

#### --answer--

Die Eingabe und die erwartete Ausgabe.

### --question--

#### --text--

Welche Zeitkomplexität wächst schneller als `O(n log n)`, wenn n groß wird?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

Was ist ein guter nächster Schritt, nachdem Sie eine Brute-Force-Lösung implementiert haben?

#### --distractors--

Mikro-Optimierung konstanter Faktoren vor dem Messen.

---

Tests verwerfen und die Lösung neu schreiben.

---

Randfälle ignorieren, um den Code einfach zu halten.

#### --answer--

Analysieren Sie Zeit-/Speicherkomplexität und optimieren Sie erkannte Engpässe.

### --question--

#### --text--

Was misst die Speicherkomplexität?

#### --distractors--

Wie viele CPU-Kerne ein Programm nutzt.

---

Die Länge eines Programms in Codezeilen.

---

Wie lange ein Programm zum Kompilieren braucht.

#### --answer--

Wie der Speicherbedarf relativ zur Eingabegröße wächst.
