---
id: 672bd814105a0ffcf36f9233
title: フォントファミリーとは何か、そしてどのように機能するのか？
challengeType: 19
dashedName: what-are-font-families
---

# --interactive--

フォントファミリーとは、共通のデザインを持つフォントのグループです。同じファミリーに属するすべてのフォントは同じ基本の書体に基づいていますが、スタイル、太さ、幅に違いがあります。兄弟姉妹のように似た特徴を共有しつつも違いがあると考えてください。例えば、ArialはArial BoldやArial Italicのようなバリエーションを含むフォントファミリーです。

CSSでは、`font-family` プロパティでフォントファミリーを設定できます。以下は段落のフォントを`Arial`に設定する例です。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="arial-font">Example paragraph using Arial font.</p>
<p>Paragraph not using Arial font.</p>
```

```css
#arial-font {
  font-family: Arial;
}
```

:::

では、フォントファミリーが見つからなかった場合はどうなるでしょうか？優先度の高い順から低い順に複数のフォントファミリーをカンマで区切って指定できます。これらの代替フォントはフォールバックとして機能します。以下の例では、Arialが主要フォントでLatoが代替フォントです。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="specified-font">Example paragraph using specified fonts.</p>
<p>Paragraph not using specified fonts.</p>
```

```css
#specified-font {
  font-family: Arial, Lato;
}
```

:::

ブラウザはデバイスに最初のフォント（Arial）があればそれをレンダリングします。なければ2番目のフォント（Lato）がレンダリングされます。

この仕組みが裏でどのように動いているかも知っておくと良いでしょう。最初のフォントが利用可能でも選択はそこで止まりません。フォントファミリーは文字ごとに選ばれるため、特定の文字がフォントにない場合は優先度の低いフォントから探されます。面白いですね。

ウェブ開発の文脈では、総称フォントファミリーもよく使われます。これは非常に重要です。総称フォントファミリーとは、優先度の高いフォントが利用できない場合にウェブブラウザがレンダリングするデフォルトのフォントです。コンテンツの可読性を保つために、ブラウザは指定された総称フォントファミリーに基づいて最も適切なフォントに置き換えます。よく使われるものには以下があります：

- serif
- sans-serif
- monospace
- cursive
- fantasy

他にも選択肢はあります。以下のコードでは3つの代替フォントファミリーを指定しています。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="specified-font">Example paragraph using specified fonts.</p>
<p>Paragraph not using specified fonts.</p>
```

```css
#specified-font {
  font-family: Arial, Lato, sans-serif;
}
```

:::

Arialが最も優先されます。Arialが見つからなければブラウザはLatoをレンダリングしようとします。どちらもなければ、ブラウザは総称フォントファミリーのsans-serifを使い、ユーザーのシステムにインストールされているこれらの特徴を持つフォントを選びます。

フォントファミリーリストの最後には必ず総称フォントファミリーを含めるべきです。総称フォントは元々デザインで想定したフォントとは見た目が異なる場合があるため、異なるブラウザでフォールバックフォントの見え方を確認するのが役立ちます。

ユーザー体験をできるだけ一貫させるために、ウェブセーフフォントを使うこともお勧めします。これらのフォントファミリーはほとんどのデバイスにインストールされているため、多くのユーザーに対して正しくレンダリングされる可能性が高いです。次のレッスンで詳しく学びます。

フォントファミリーはウェブデザインの世界で欠かせません。font-family プロパティを使うことで、テキストの見た目や雰囲気を瞬時に変えることができます。

# --questions--

## --text--

`font-family` CSSプロパティの主な目的は何ですか？

## --answers--

テキストの色を設定すること。

### --feedback--

テキストの見た目について考えてみてください。

---

テキストのフォントサイズを制御すること。

### --feedback--

テキストの見た目について考えてみてください。

---

テキストに使う書体を定義すること。

---

テキストの行間を調整すること。

### --feedback--

テキストの見た目について考えてみてください。

## --video-solution--

3

## --text--

次のうち、一般的な総称フォントファミリーはどれですか？

## --answers--

`Helvetica`

### --feedback--

フォントの大まかなカテゴリについて考えてみてください。

---

`Times New Roman`

### --feedback--

フォントの大まかなカテゴリについて考えてみてください。

---

`Sans-Serif`

---

`Arial`

### --feedback--

フォントの大まかなカテゴリについて考えてみてください。

## --video-solution--

3

## --text--

CSSで複数のフォントファミリーをフォールバックとして指定するにはどうすればよいですか？

## --answers--

カンマで区切って指定する。

---

ネストされた`font-family`宣言を使う。

### --feedback--

主要フォントが利用できない場合の代替フォントの指定方法を考えてみてください。

---

`&`文字を使う。

### --feedback--

主要フォントが利用できない場合の代替フォントの指定方法を考えてみてください。

---

`|`文字を使う。

### --feedback--

主要フォントが利用できない場合の代替フォントの指定方法を考えてみてください。

## --video-solution--

1
