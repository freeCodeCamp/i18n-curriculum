---
id: 67358be1c7903489c0a7db78
title: JavaScript Map와 Set 퀴즈
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript에서 Set이란 무엇인가요?

#### --distractors--

중복 값을 허용하는 자료 구조입니다.

---

문자열과 숫자만 저장하는 자료 구조입니다.

---

요소 삭제를 허용하지 않는 컬렉션입니다.

#### --answer--

값이 한 번만 나타날 수 있는 컬렉션입니다.

### --question--

#### --text--

다음 중 Map에는 있지만 Set에는 없는 메서드는 무엇인가요?

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

Set에 요소를 추가할 때 사용하는 메서드는 무엇인가요?

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

Set에 특정 값이 포함되어 있는지 어떻게 확인하나요?

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

다음 중 Set을 사용하기 좋은 사례는 무엇인가요?

#### --distractors--

데이터베이스에서 키-값 쌍을 관리하는 것.

---

항목의 순서 있는 목록을 만드는 것.

---

브라우저 로컬 저장소에 계층적 자료 구조를 저장하는 것.

#### --answer--

배열에서 중복을 제거하는 것.

### --question--

#### --text--

JavaScript에서 WeakSet이란 무엇인가요?

#### --distractors--

원시 데이터 타입만 허용하는 Set과 유사한 컬렉션.

---

중복 값을 허용하는 Set과 유사한 컬렉션.

---

값 삭제를 막는 Set과 유사한 컬렉션.

#### --answer--

객체만 저장하는 Set과 유사한 컬렉션.

### --question--

#### --text--

Set과 WeakSet의 차이점 하나는 무엇인가요?

#### --distractors--

WeakSet은 숫자를 포함한 모든 타입의 값을 저장할 수 있습니다.

---

Set은 중복 값을 허용하지 않지만 WeakSet은 허용합니다.

---

Set은 가비지 컬렉션이 가능하지만 WeakSet은 그렇지 않습니다.

#### --answer--

Set은 모든 타입의 값을 저장할 수 있지만 WeakSet은 객체만 저장합니다.

### --question--

#### --text--

JavaScript에서 Map이란 무엇인가요?

#### --distractors--

고유한 값만 허용하는 컬렉션.

---

키로 문자열만 사용할 수 있는 컬렉션.

---

항목 삭제를 허용하지 않는 컬렉션.

#### --answer--

모든 타입의 키-값 쌍으로 이루어진 컬렉션.

### --question--

#### --text--

Map에 키-값 쌍을 삽입할 때 사용하는 메서드는 무엇인가요?

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

다음 코드는 무엇을 출력하나요?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

JavaScript에서 Map과 Object의 차이점은 무엇인가요?

#### --distractors--

Map은 키로 숫자만 허용합니다.

---

Object는 모든 타입을 키로 허용하지만 Map은 문자열만 허용합니다.

---

Map은 반복을 지원하지 않지만 Object는 지원합니다.

#### --answer--

Map은 모든 타입의 키를 허용하지만 Object는 문자열과 심볼만 허용합니다.

### --question--

#### --text--

Map에서 키-값 쌍을 어떻게 삭제하나요?

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

Map의 `size` 속성은 무엇을 반환하나요?

#### --distractors--

Map의 인덱스 개수.

---

Map의 고유 값 개수.

---

Map이 사용하는 총 메모리.

#### --answer--

Map의 키-값 쌍 개수.

### --question--

#### --text--

JavaScript에서 WeakMap이란 무엇인가요?

#### --distractors--

원시 값을 키로 허용하는 Map과 유사한 컬렉션.

---

항목 삭제를 막는 Map과 유사한 컬렉션.

---

키를 자동으로 정렬하는 Map과 유사한 컬렉션.

#### --answer--

키에 약한 참조를 가지는 Map과 유사한 컬렉션.

### --question--

#### --text--

Map과 WeakMap의 차이점 하나는 무엇인가요?

#### --distractors--

Map은 키로 원시 타입만 허용하지만 WeakMap은 객체만 허용합니다.

---

Map은 가비지 컬렉션이 가능하지만 WeakMap은 그렇지 않습니다.

---

WeakMap은 중복 키를 허용하지만 Map은 허용하지 않습니다.

#### --answer--

Map은 반복 가능하지만 WeakMap은 그렇지 않습니다.

### --question--

#### --text--

다음 중 WeakMap에 없는 메서드는 무엇인가요?

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

다음 코드는 무엇을 출력하나요?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

Set의 값들을 어떻게 순회하나요?

#### --distractors--

`.map()` 메서드를 사용해서.

---

`.reduce()` 메서드를 사용해서.

---

`.filter()` 메서드를 사용해서.

#### --answer--

`.forEach()` 메서드를 사용해서.

### --question--

#### --text--

Set의 값들을 포함하는 `SetIterator`를 반환하는 두 메서드는 무엇인가요?

#### --distractors--

`entries()`와 `values()`

---

`forEach()`와 `values()`

---

`has()`와 `add()`

#### --answer--

`keys()`와 `values()`

### --question--

#### --text--

다음 코드는 무엇을 출력하나요?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
