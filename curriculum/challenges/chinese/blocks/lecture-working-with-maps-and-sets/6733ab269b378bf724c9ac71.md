---
id: 6733ab269b378bf724c9ac71
title: JavaScript 中的集合是什么，它与 WeakSets 有何不同？
challengeType: 19
dashedName: what-are-sets-in-javascript-and-how-does-it-differ-from-weaksets
---

# --description--

在 JavaScript 中，`Set` 是一个内置对象，用于管理数据集合。它允许你保存任何类型的唯一值，无论是原语还是对象引用。`Set` 确保其中的每个值只出现一次，这使它在消除数组中的重复项或处理不同值的集合时非常有用。

至于 `WeakSet`，它是一种特殊类型的 `Set`，功能较少，允许你保存弱引用的对象引用和符号。与 `Set` 不同，`WeakSet` 不支持数字或字串等原语。

与普通的 `Set` 不同，`WeakSet` 只存储对象，并且对这些对象的引用是“弱引用”，这意味着如果没有其他引用指向这些对象，WeakSets 不会阻止它们被垃圾回收。简单来说，如果对象在代码的其他地方没有被使用，它会自动被移除以释放内存。

要创建一个 `Set`，你可以使用 `Set` 构造函数并赋值给一个变量：

```js
const myFirstSet = new Set();
```

你也可以用值来初始化 `Set`：

```js
const treeSet = new Set(["Baobab", "Jackalberry", "Mopane Tree", "Breadfruit"]);
```

如果你将 `Set` 记录到控制台，输出看起来是这样的：

```js
/*
Set(4) {"Baobab", "Jackalberry", "Mopane Tree", "Breadfruit"}
  [[Entries]]
  0: "Baobab"
    value: "Baobab"
  1: "Jackalberry"
    value: "Jackalberry"
  2: "Mopane Tree"
    value: "Mopane Tree"
  3: "Breadfruit"
    value: "Breadfruit"
  size: 4
  [[Prototype]]: Set
*/
```

如果你没有用值初始化 `Set`，你可以使用 `add()` 方法向 `Set` 添加一项：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");
```

结果和控制台中的显示效果保持不变。

别忘了，`Set` 中的重复项会被忽略：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");
treeSet.add("Baobab"); //duplicate item will be ignored

console.log(treeSet);
// Set(4) {"Baobab", "Jackalberry", "Mopane Tree", "Breadfruit"}
```

你还可以使用以下方法来操作 `Set`：

- `delete()`  
- `clear()`
- `has()`
- `entries()`
- `forEach()`
- `keys()`
- `values()`

让我们逐一看看这些方法的作用。`delete()` 会从 `Set` 中移除指定的项：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");

treeSet.delete("Breadfruit");

console.log(treeSet); // Set(3) {"Baobab", "Jackalberry", "Mopane Tree"}
```

`has()` 用于检查 `Set` 中是否存在指定的值：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");

treeSet.delete("Breadfruit");

console.log(treeSet.has("Breadfruit")); // false
```

`entries()` 返回一个 `Set` 迭代器，包含 `[value, value]` 格式的值数组：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");

treeSet.delete("Breadfruit");

console.log(treeSet.entries());
// SetIterator {"Baobab" => "Baobab", "Jackalberry" => "Jackalberry", "Mopane Tree" => "Mopane Tree"}
```

`keys()` 和 `values()` 显示 `Set` 中的值。`keys()` 只是 `values()` 方法的别名：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");

treeSet.delete("Breadfruit");

console.log("Keys: ", treeSet.keys());
console.log("Values: ", treeSet.values());
// Keys: SetIterator {"Baobab", "Jackalberry", "Mopane Tree"}
// Values: SetIterator {"Baobab", "Jackalberry", "Mopane Tree"}
```

`forEach()` 允许你迭代 `Set`：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");

treeSet.delete("Breadfruit");

treeSet.forEach((tree) => console.log(tree));
/*
Baobab
Jackalberry
Mopane Tree
*/
```

`clear()` 会移除数组中的所有项：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");

treeSet.delete("Breadfruit");

treeSet.clear();

console.log(treeSet); // Set(0) {size: 0}
```

还值得一提的是，`size` 属性返回 `Set` 中项的数量：

```js
const treeSet = new Set();

// Add items to the treeSet
treeSet.add("Baobab");
treeSet.add("Jackalberry");
treeSet.add("Mopane Tree");
treeSet.add("Breadfruit");

treeSet.delete("Breadfruit");

console.log(treeSet.size); // 3
```

就像 `Set` 一样，你也可以使用 `WeakSet` 构造函数来创建 `WeakSet`：

```javascript
const treeWeakSet = new WeakSet();
```

`WeakSet` 也有 `add()`、`delete()` 和 `has()` 方法：

```javascript
const treeWeakSet = new WeakSet();

const baobab = { name: "Baobab" };
const jackalberry = { name: "Jackalberry" };
const mopaneTree = { name: "Mopane Tree" };
const breadfruit = { name: "Breadfruit" };

treeWeakSet.add(baobab);
treeWeakSet.add(jackalberry);
treeWeakSet.add(mopaneTree);
treeWeakSet.add(breadfruit);

treeWeakSet.delete(jackalberry);
console.log(treeWeakSet.has(jackalberry)); // false

console.log(treeWeakSet);
```

输出中，`WeakSet` 的内容显示如下：

```javascript
/*
WeakSet {{…}, {…}, {…}}
  [[Entries]]
    0: value: {name: "Mopane Tree"}
    1: value: {name: "Baobab"}
    2: value: {name: "Breadfruit"}
  [[Prototype]]: WeakSet
*/
```

这里能看到内容是因为对象变量仍然持有强引用，保持这些对象在内存中。请记住，WeakSets 不能被迭代，因此无法循环访问它们的条目或以编程方式访问它们。你在输出中看到的只是调试辅助。

别忘了，只有带有明确定义的键和值的符号和对象被支持。添加原语（如数字或字串）会导致错误：

```js
treeWeakSet.add("Alan Smith");

console.log(treeWeakSet); // Invalid value used in weak set
//    at WeakSet.add (<anonymous>)
```

`Set` 和 `WeakSet` 之间的关键区别是，`Set` 可以存储任何值，而 `WeakSet` 只能存储对象。

以下是 `Set` 和 `WeakSet` 之间的一些显著差异：

| 特色 | Set | WeakSet |
| --- | --- | --- |
| 存储值的类型 | 存储任何数据类型 | 仅存储对象 |
| 引用方式 | 强引用 | 弱引用 |
| 迭代 | 支持使用 `forEach` 和循环迭代 | 不支持迭代 |
| 方法和属性 | `add()`、`delete()`、`has()`、`keys()`、`values()`、`size` 等 | 仅 `add()`、`delete()` 和 `has()` |
| 使用场景 | 通用的唯一值集合和数组去重 | 对对象引用的高效内存跟踪 |

你可以看到这两种集合能存储的值类型、它们对存储对象的迭代支持以及理想的使用场景的差异。请花点时间阅读这张表的内容。

# --questions--

## --text--

`Set` 和 `WeakSet` 在它们支持的数据方面有何不同？

## --answers--

`Set` 允许弱引用的对象引用，而 `WeakSet` 支持对象和原语。

### --feedback--

记住 `Set` 可以保存多种数据类型。

---

`Set` 可以存储任何类型，包括原语，而 `WeakSet` 只允许弱引用的对象引用。

---

`Set` 的功能比 `WeakSet` 少。

### --feedback--

记住 `Set` 可以保存多种数据类型。

---

`Set` 专门用于对象，而 `WeakSet` 用于数字。

### --feedback--

记住 `Set` 可以保存多种数据类型。

## --video-solution--

2

## --text--

如何在 JavaScript 中创建 `Set` 或 `WeakSet`？

## --answers--

通过调用 `Set()` 或 `WeakSet()`，不使用 `new` 关键字

### --feedback--

想想创建 `Set` 或 `WeakSet` 需要包含什么关键字。

---

通过使用对象字面量 `{}`

### --feedback--

想想创建 `Set` 或 `WeakSet` 需要包含什么关键字。

---

通过使用数组字面量 `[]`

### --feedback--

想想创建 `Set` 或 `WeakSet` 需要包含什么关键字。

---

通过使用 `Set` 或 `WeakSet` 构造函数和 `new` 关键字

## --video-solution--

4

## --text--

JavaScript 中 `Set` 和 `WeakSet` 都有哪些可用的方法？

## --answers--

`keys()`、`values()`、`size()`

### --feedback--

复习课程结尾部分的相关内容。

---

`add()`、`delete()`、`has()`

---

`clear()`、`entries()`、`forEach()`

### --feedback--

复习课程结尾部分的相关内容。

---

`set()`、`get()`、`remove()`

### --feedback--

复习课程结尾部分的相关内容。

## --video-solution--

2
