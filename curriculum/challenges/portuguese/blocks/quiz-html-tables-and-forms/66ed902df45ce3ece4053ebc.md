---
id: 66ed902df45ce3ece4053ebc
title: Questionário sobre tabelas e formulários em HTML
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual dos seguintes atributos é usado para especificar a URL para onde os dados do formulário devem ser enviados?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

Quais dois elementos são usados para especificar uma linha e um cabeçalho de linha em uma tabela?

#### --distractors--

O elemento `r` é usado para linhas e o elemento `thead` é usado para o cabeçalho.

---

O elemento `row` é usado para linhas e o elemento `th` é usado para o cabeçalho.

---

O elemento `tr` é usado para linhas e o elemento `head` é usado para o cabeçalho.

#### --answer--

O elemento `tr` é usado para linhas e o elemento `th` é usado para o cabeçalho.

### --question--

#### --text--

Para que serve o elemento `td`?

#### --distractors--

É usado para definir um tipo de dado de tabela.

---

É usado para mesclar duas colunas.

---

É usado para mesclar duas linhas.

#### --answer--

É usado para definir uma célula de tabela.

### --question--

#### --text--

Qual é a função do atributo `colspan`?

#### --distractors--

Define o comprimento de uma coluna em uma tabela.

---

Ele remove a quebra de linha entre o texto em uma coluna.

---

É usado para definir o número de colunas.

#### --answer--

Define o número de colunas que uma célula da tabela deve abranger.

### --question--

#### --text--

Qual é o papel do atributo `placeholder`?

#### --distractors--

É usado para enviar dados ao servidor quando o formulário é enviado.

---

É usado para associar um elemento `label` com um elemento `input`.

---

É usado para especificar que um campo de entrada é somente leitura para o usuário.

#### --answer--

É usado para fornecer uma dica para um campo de entrada.

### --question--

#### --text--

Qual é o propósito do elemento `thead`?

#### --distractors--

É usado para agrupar o conteúdo do corpo em uma tabela HTML.

---

É usado para agrupar a descrição de uma tabela HTML.

---

É usado para agrupar o conteúdo do rodapé em uma tabela HTML.

#### --answer--

É usado para agrupar o conteúdo do cabeçalho em uma tabela HTML.

### --question--

#### --text--

Qual dos seguintes é um exemplo de associação explícita de formulário entre labels e inputs?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

Para que serve um validador de HTML?

#### --distractors--

Um validador é uma ferramenta que formata automaticamente seu código HTML.

---

Um validador é uma ferramenta que aplica estilos ao seu HTML.

---

Um validador é uma ferramenta que faz seu código HTML rodar mais rápido.

#### --answer--

Um validador é uma ferramenta que verifica a sintaxe do código HTML para garantir que ele seja válido.

### --question--

#### --text--

Qual elemento é usado para definir uma célula em uma tabela?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

Qual das seguintes opções é uma ferramenta que permite inspecionar e modificar a estrutura HTML de uma página web?

#### --distractors--

Remoção do DOM.

---

Testador DOM.

---

Validação DOM.

#### --answer--

Inspetor DOM.

### --question--

#### --text--

Qual das seguintes é a maneira correta de agrupar campos de entrada relacionados?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

Qual dos seguintes é usado para especificar que um campo de entrada é somente leitura?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

Qual atributo especifica que um input deve ser preenchido antes de enviar o formulário?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

Para que servem as Devtools?

#### --distractors--

Essas ferramentas são incorporadas diretamente no navegador e são usadas para identificar quaisquer problemas de linting no seu código.

---

Essas ferramentas são incorporadas diretamente no navegador e são usadas para formatar seu código automaticamente.

---

Essas ferramentas são incorporadas diretamente no navegador e são usadas para ajudar você a garantir 100% de cobertura de teste para o seu código.

#### --answer--

Essas ferramentas são incorporadas diretamente no navegador e são usadas para ajudar você a depurar, fazer perfil e analisar páginas da web.

### --question--

#### --text--

Qual das seguintes é a maneira correta de desabilitar um input?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

Qual dos seguintes é um valor válido para o atributo `type`?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

Qual das seguintes opções é o uso correto do atributo `size`?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

Quais dos seguintes atributos são usados para especificar o número mínimo e máximo de caracteres exigidos em um campo de entrada?

#### --distractors--

`minlen` e `maxlen`

---

`min` e `max`

---

`minimumLen` e `maximumLen`

#### --answer--

`minlength` e `maxlength`

### --question--

#### --text--

Qual dos seguintes exemplos de botão **NÃO** usa um valor correto para o atributo `type`?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

Qual dos seguintes atributos é usado para especificar o valor de um botão?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

Qual é o papel do atributo `action` dentro da tag de abertura `form`?

#### --distractors--

É usado para especificar que um campo de entrada é somente leitura.

---

É usado para definir o número de caracteres que devem estar visíveis enquanto o usuário digita no input.

---

É usado para mostrar uma dica ao usuário para indicar o que deve ser inserido no campo de entrada.

#### --answer--

É usado para especificar a URL para onde os dados do formulário devem ser enviados.

### --question--

#### --text--

Qual dos seguintes atributos é usado para especificar o método HTTP a ser usado ao enviar os dados do formulário?

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

Qual dos seguintes é um método comum usado ao trabalhar com formulários?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

Qual dos seguintes NÃO é um valor válido para o atributo `type`?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

Qual das seguintes é a maneira correta de criar um botão em um formulário?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

O que é associação de formulário implícita?

#### --distractors--

É aqui que os inputs podem ser associados a labels usando os atributos `action` e `for` no elemento `label`.

---

É aqui que os inputs podem ser associados a labels usando os atributos `required` e `for` no elemento `label`.

---

É aqui que os inputs podem ser associados a labels usando o atributo `for` no elemento `label`.

#### --answer--

É aqui que os inputs podem ser associados a labels envolvendo o campo de input dentro do elemento `label`.

### --question--

#### --text--

Qual é o papel do elemento `fieldset`?

#### --distractors--

É usado para agrupar o conteúdo do cabeçalho em uma tabela HTML.

---

É usado para criar um rótulo para um campo de entrada.

---

É usado para adicionar uma legenda para descrever o grupo de entradas.

#### --answer--

É usado para agrupar entradas relacionadas.

### --question--

#### --text--

Qual dos seguintes é usado para criar uma checkbox em um formulário?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

Qual dos seguintes elementos é usado para adicionar um título a uma tabela HTML?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

Qual é o papel do elemento `legend`?

#### --distractors--

É usado para agrupar o conteúdo do corpo em uma tabela HTML.

---

É usado para adicionar uma legenda para descrever as células de uma tabela.

---

É usado para agrupar entradas relacionadas.

#### --answer--

É usado para adicionar uma legenda para descrever o grupo de entradas.

### --question--

#### --text--

Qual dos seguintes elementos é usado para agrupar o conteúdo do cabeçalho em uma tabela HTML?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

Qual das seguintes é a maneira correta de adicionar linhas e células de dados a uma tabela?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

O que é o inspetor do DOM?

#### --distractors--

Uma ferramenta que é usada para remover todos os elementos do DOM.

---

Um conjunto de ferramentas integradas diretamente no navegador que ajudam você a depurar, analisar desempenho e analisar páginas web.

---

Uma ferramenta que verifica a sintaxe do código HTML para garantir que ele seja válido.

#### --answer--

Uma ferramenta que permite inspecionar e modificar a estrutura HTML de uma página web.

### --question--

#### --text--

Qual das seguintes é uma ferramenta que verifica a sintaxe do código HTML para garantir que ele seja válido?

#### --distractors--

Validar HTML

---

HTML isValid

---

HTML válido

#### --answer--

Validador HTML

### --question--

#### --text--

Qual das seguintes é a maneira correta de especificar o número de colunas para a célula de dados em uma tabela?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

O que é um estado focado para um campo de entrada?

#### --distractors--

Este é o estado de um campo de entrada quando o foco é removido pelo usuário.

---

Este é o estado de um campo de entrada quando ele é definido como readonly pelo usuário.

---

Este é o estado de um campo de entrada quando ele está desabilitado pelo usuário.

#### --answer--

Este é o estado de um campo de entrada quando ele é selecionado pelo usuário.

### --question--

#### --text--

Qual das seguintes é a maneira correta de criar um botão de rádio?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

Qual é o papel do atributo `for`? 

#### --distractors--

É usado para mostrar uma dica ao usuário.

---

É usado para especificar o valor da entrada

---

É usado para criar um rótulo para um campo de entrada.

#### --answer--

É usado para especificar para qual campo de entrada o rótulo é.

### --question--

#### --text--

Qual dos seguintes atributos é usado para definir o número de caracteres que devem ser visíveis enquanto o usuário digita no input?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

Qual dos seguintes atributos é usado para mostrar uma dica ao usuário para indicar o que deve ser inserido no campo de entrada?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
