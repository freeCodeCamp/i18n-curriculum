---
id: 5900f3731000cf542c50fe86
title: "المشكلة 7: العدد الأولي رقم 10001"
challengeType: 1
forumTopicId: 302182
dashedName: problem-7-10001st-prime
---

# --description--

بذكر أول ستة أعداد أولية: 2، 3، 5، 7، 11، و13، نرى أن العدد الأولي السادس هو 13.

ما هو العدد الأولي الـ `n`؟

# --hints--

يجب أن تُرجع الدالة `nthPrime(6)` رقمًا.

```js
assert.isNumber(nthPrime(6));
```

يجب أن تُرجع الدالة `nthPrime(6)` العدد 13.

```js
assert.strictEqual(nthPrime(6), 13);
```

يجب أن تُرجع الدالة `nthPrime(10)` العدد 29.

```js
assert.strictEqual(nthPrime(10), 29);
```

يجب أن تُرجع الدالة `nthPrime(100)` العدد 541.

```js
assert.strictEqual(nthPrime(100), 541);
```

يجب أن تُرجع الدالة `nthPrime(1000)` العدد 7919.

```js
assert.strictEqual(nthPrime(1000), 7919);
```

يجب أن تُرجع الدالة `nthPrime(10001)` العدد 104743.

```js
assert.strictEqual(nthPrime(10001), 104743);
```

# --seed--

## --seed-contents--

```js
function nthPrime(n) {

  return true;
}

nthPrime(10001);
```

# --solutions--

```js
const nthPrime = n => {
  let pN = 2;
  let step = 0;
  while (step < n) {
    let isPrime = true;
    let rootN = Math.sqrt(pN);
    for (let i = 2; i <= rootN; i++) {
      if (!(pN % i)) {
        isPrime = false;
        break;
      }
    }
    isPrime ? step++ : '';
    pN++;
  }
  return pN - 1;
}
```
