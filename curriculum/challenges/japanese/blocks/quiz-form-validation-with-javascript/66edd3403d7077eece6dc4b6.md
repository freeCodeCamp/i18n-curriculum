---
id: 66edd3403d7077eece6dc4b6
title: JavaScriptによるフォームバリデーションのクイズ
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

クイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

`input` 要素の値がバリデーションルールを満たしているかどうかを確認するために使われるメソッドはどれですか？

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

`checkValidity()` メソッドが無効な `input` を見つけた場合、何が起こりますか？

#### --distractors--

フォームが自動的に送信されます。

---

何も起こりません。

---

`input` の値がクリアされます。

#### --answer--

ブラウザがバリデーションメッセージを表示します。

### --question--

#### --text--

バリデーション失敗時に表示されるメッセージをカスタマイズするために使われるメソッドはどれですか？

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

フォーム送信時の必須フィールドに対するデフォルトのバリデーション動作は何ですか？

#### --distractors--

必須フィールドが空でもフォームは送信されます。

---

フォームは送信されますが警告が表示されます。

---

フォームは `input` の値をリセットします。

#### --answer--

必須フィールドが空の場合、ブラウザがフォーム送信を停止します。

### --question--

#### --text--

HTMLフォームのエラー検証が行われない操作はどれですか？

#### --distractors--

`input` の値を変更する。

---

フォームを送信する。

---

`reportValidity()` を呼び出す。

#### --answer--

リセットボタンをクリックする。

### --question--

#### --text--

すべてのフォーム入力が有効な場合、`checkValidity()` は何を返しますか？

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

`checkValidity()` メソッドはいつフォーム送信を停止しますか？

#### --distractors--

フォームに送信ボタンがない場合。

---

フォームの `action` が空の場合。

---

フォームのメソッドが POST の場合。

#### --answer--

フォームに少なくとも1つの無効な `input` がある場合（かつフォーム送信のデフォルト動作が防止されている場合）。

### --question--

#### --text--

`checkValidity()` が呼び出され、フィールドがバリデーションに失敗した場合、何が起こりますか？

#### --distractors--

フォームはエラーなしで送信されます。

---

`input` の値がクリアされます。

---

フォームがすぐに閉じます。

#### --answer--

無効なフィールドがハイライトされ、メッセージが表示されます。

### --question--

#### --text--

どの操作で `checkValidity()` が実行されますか？

#### --distractors--

フォームをリセットする。

---

`input` の値を変更する。

---

アンカータグをクリックする。

#### --answer--

フォームを手動で送信する。

### --question--

#### --text--

フォーム送信前に `checkValidity()` を呼び出す目的は何ですか？

#### --distractors--

フォームを自動的に送信するため。

---

ページをリロードするため。

---

すべての `input` フィールドをクリアするため。

#### --answer--

すべての `input` 要素が指定された制約を満たしているか確認するため。

### --question--

#### --text--

`e.preventDefault()` の主な目的は何ですか？

#### --distractors--

フォームをリロードするため。

---

フォームデータをクリアするため。

---

フォームの値をリセットするため。

#### --answer--

イベントのデフォルト動作を停止するため。

### --question--

#### --text--

フォームの送信イベントで使われた場合、`e.preventDefault()` は何をしますか？

#### --distractors--

フォームを送信する。

---

すべてのフォームフィールドをクリアする。

---

フォームを一時的に無効にする。

#### --answer--

フォームの送信を防止します。

### --question--

#### --text--

どのシナリオで `e.preventDefault()` が最も役立ちますか？

#### --distractors--

フォームを送信するボタンクリックを処理する場合。

---

モーダルダイアログを閉じる場合。

---

新しいページを開く場合。

#### --answer--

フォーム送信時にページのリロードを防ぐ場合。

### --question--

#### --text--

フォーム送信イベントで `e.preventDefault()` を呼び出し忘れた場合、何が起こりますか？

#### --distractors--

すべての入力フィールドがデフォルト値に設定される。

---

フォームは送信されない。

---

フォームデータが失われる。

#### --answer--

フォームが送信されてページが更新される。

### --question--

#### --text--

`e.preventDefault()` を使って防止できるデフォルト動作はどれですか？

#### --distractors--

フォームの編集。

---

フォームのレンダリング。

---

フォームのリロード。

#### --answer--

フォーム送信によるデフォルト動作。

### --question--

#### --text--

フォームの送信イベントは何によってトリガーされますか？

#### --distractors--

入力フィールドが変更されたとき。

---

フォームがリセットされたとき。

---

フォームの action がクリアされたとき。

#### --answer--

フォームの送信ボタンがクリックされるか Enter キーが押されたとき。

### --question--

#### --text--

JavaScriptで送信イベントを通常どこで処理しますか？

#### --distractors--

テキスト入力要素で。

---

フォーム内の任意のフィールドで。

---

フォーム外のボタン要素で。

#### --answer--

フォーム要素自体で。

### --question--

#### --text--

送信イベントのデフォルト動作は何ですか？

#### --distractors--

フォームをバリデーションするが送信しない。

---

フォームフィールドをクリアする。

---

フォーム送信をキャンセルする。

#### --answer--

フォームの action で指定されたサーバーにフォームデータを送信する。

### --question--

#### --text--

JavaScriptでフォーム送信時にフォームデータを処理するためにリッスンすべきイベントはどれですか？

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

HTMLフォームの `method` 属性のデフォルト値は何ですか？

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
