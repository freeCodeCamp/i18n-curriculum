---
id: 683766860f71d4a96e429f3a
title: JavaScript와 접근성 검토
challengeType: 31
dashedName: review-js-a11y
---

# --interactive--

## 일반적인 ARIA 접근성 속성

- **`aria-expanded` 속성**: 토글(또는 공개) 기능의 상태를 화면 읽기 프로그램 사용자에게 전달하는 데 사용합니다.

:::interactive_editor

```html
<button id="menuBtn" aria-expanded="false">Menu</button>

<script>
  const btn = document.getElementById("menuBtn");

  btn.addEventListener("click", () => {
    const expanded = btn.getAttribute("aria-expanded") === "true";
    btn.setAttribute("aria-expanded", String(!expanded));
  });
</script>
```

:::

- **`aria-haspopup` 속성**: 이 상태는 상호작용 요소가 활성화될 때 팝업 요소를 트리거한다는 것을 나타내는 데 사용합니다. 팝업이 다음 역할 중 하나일 때만 `aria-haspopup` 속성을 사용할 수 있습니다: `menu`, `listbox`, `tree`, `grid`, 또는 `dialog`. `aria-haspopup`의 값은 이 역할 중 하나이거나 `true`이어야 하며, 이는 `menu`과 동일합니다.

:::interactive_editor

```html
<button
  id="menubutton"
  aria-haspopup="menu"
  aria-controls="filemenu"
  aria-expanded="false"
>
  File
</button>

<ul
  id="filemenu"
  role="menu"
  aria-labelledby="menubutton"
  hidden
>
  <li role="menuitem" tabindex="-1">Open</li>
  <li role="menuitem" tabindex="-1">New</li>
  <li role="menuitem" tabindex="-1">Save</li>
  <li role="menuitem" tabindex="-1">Delete</li>
</ul>

<script>
  const button = document.getElementById("menubutton");
  const menu = document.getElementById("filemenu");

  button.addEventListener("click", () => {
    const expanded = button.getAttribute("aria-expanded") === "true";

    button.setAttribute("aria-expanded", String(!expanded));
    menu.hidden = expanded;
  });
</script>
```

:::

- **`aria-checked` 속성**: 이 속성은 요소가 선택된 상태인지 여부를 나타내는 데 사용합니다. 주로 사용자 정의 체크박스, 라디오 버튼, 스위치, 리스트박스를 만들 때 사용합니다.

:::interactive_editor

```html
<div
  id="checkbox"
  role="checkbox"
  aria-checked="true"
  tabindex="0"
  style="
    display: inline-flex;
    align-items: center;
    gap: 6px;
    cursor: pointer;
  "
>
  <span
    id="box"
    aria-hidden="true"
    style="
      width: 16px;
      height: 16px;
      border: 2px solid blue;
      background: blue;
      display: inline-block;
    "
  ></span>
  Checkbox
</div>

<script>
  const checkbox = document.getElementById("checkbox");
  const box = document.getElementById("box");

  const toggle = () => {
    const checked = checkbox.getAttribute("aria-checked") === "true";
    checkbox.setAttribute("aria-checked", String(!checked));
    box.style.background = checked ? "white" : "black";
  };

  checkbox.addEventListener("click", toggle);

  checkbox.addEventListener("keydown", (e) => {
    if (e.key === " " || e.key === "Enter") {
      e.preventDefault();
      toggle();
    }
  });
</script>
```

:::

- **`aria-disabled` 속성**: 이 상태는 화면 읽기 프로그램과 같은 보조 기술을 사용하는 사람들에게만 요소가 비활성화되었음을 나타내는 데 사용합니다.

:::interactive_editor

```html
<div
  id="editBtn"
  role="button"
  tabindex="-1"
  aria-disabled="true"
  style="opacity: 0.5; cursor: not-allowed;"
>
  Edit
</div>

<button id="toggle">Toggle Disabled</button>

<script>
  const editBtn = document.getElementById("editBtn");
  const toggleBtn = document.getElementById("toggle");

  toggleBtn.addEventListener("click", () => {
    const disabled = editBtn.getAttribute("aria-disabled") === "true";

    editBtn.setAttribute("aria-disabled", String(!disabled));
    editBtn.tabIndex = disabled ? 0 : -1;
    editBtn.style.opacity = disabled ? "1" : "0.5";
    editBtn.style.cursor = disabled ? "pointer" : "not-allowed";
  });
</script>
```

:::

- **`aria-selected` 속성**: 이 상태는 요소가 선택되었음을 나타내는 데 사용합니다. 탭 인터페이스, 리스트박스, 그리드 같은 사용자 정의 컨트롤에 이 상태를 사용할 수 있습니다.

:::interactive_editor

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>

<script>
  const tabs = document.querySelectorAll('[role="tab"]');

  tabs.forEach((tab) => {
    tab.addEventListener("click", () => {
      tabs.forEach(t => t.setAttribute("aria-selected", "false"));
      tab.setAttribute("aria-selected", "true");
    });
  });
</script>
```

:::

- **`aria-controls` 속성**: 요소를 제어하는 다른 요소와 연결하는 데 사용합니다. 이는 보조 기술 사용자가 요소 간의 관계를 이해하는 데 도움이 됩니다.

:::interactive_editor

```html
<div role="tablist">
  <button 
    role="tab"
    id="tab1"
    aria-controls="section1"
    aria-selected="true"
  >
    Tab 1
  </button>
  <button
    role="tab"
    id="tab2"
    aria-controls="section2"
    aria-selected="false"
  >
    Tab 2
  </button>
  <button
    role="tab"
    id="tab3"
    aria-controls="section3"
    aria-selected="false"
  >
    Tab 3
  </button>
</div>
```

:::

- **`hidden` 속성**: 비활성 패널을 시각적 사용자와 보조 기술 사용자 모두에게 숨깁니다.

## 라이브 영역과 동적 콘텐츠 작업

- **`aria-live` 속성**: 웹페이지의 일부를 라이브 영역으로 만들어 해당 영역 내의 업데이트가 화면 읽기 프로그램에 의해 알림되어 사용자가 중요한 변경 사항을 놓치지 않도록 합니다.
- **`polite` 값**: 대부분의 라이브 영역이 이 값을 사용합니다. 이 값은 업데이트가 긴급하지 않음을 의미하며, 화면 읽기 프로그램이 현재 알림을 마치거나 사용자가 현재 작업을 완료할 때까지 기다렸다가 업데이트를 알릴 수 있습니다.

다음은 JavaScript로 동적으로 업데이트되는 라이브 영역의 예입니다:

:::interactive_editor

```html
<div aria-live="polite" id="status"></div>

<button id="updateStatus">Update Status</button>

<script>
  const statusEl = document.getElementById("status");
  const btn = document.getElementById("updateStatus");

  btn.addEventListener("click", () => {
    statusEl.textContent = "Your file has been successfully uploaded.";
  });
</script>
```

:::

- **`contenteditable` 속성**: 요소를 라이브 편집기로 만들어 사용자가 텍스트 필드처럼 내용을 업데이트할 수 있게 합니다. contenteditable 영역에 눈에 띄는 레이블이나 제목이 없으면, 화면 읽기 프로그램 사용자가 편집 영역의 목적을 이해할 수 있도록 `aria-label` 속성으로 접근 가능한 이름을 추가하세요.

:::interactive_editor

```html
<div
  contenteditable="true"
  aria-label="Note editor"
  id="editor"
  style="border: 1px solid #ccc; padding: 8px;"
>
  Editable content goes here
</div>

<p id="status" aria-live="polite"></p>

<script>
  const editor = document.getElementById("editor");
  const status = document.getElementById("status");

  editor.addEventListener("input", () => {
    status.textContent = "Content updated";
  });
</script>
```

:::

## `focus` 및 `blur` 이벤트

- **`blur` 이벤트**: 요소가 포커스를 잃을 때 발생합니다.

:::interactive_editor

```html
<input
  id="nameInput"
  type="text"
  placeholder="Type here and click outside"
  aria-label="Name input"
/>

<p id="status" aria-live="polite"></p>

<script>
  const input = document.getElementById("nameInput");
  const status = document.getElementById("status");

  input.addEventListener("blur", () => {
    status.textContent = "Input lost focus";
  });
</script>
```

:::

- **`focus` 이벤트**: 요소가 포커스를 받을 때 발생합니다.

:::interactive_editor

```html
<input
  id="emailInput"
  type="email"
  placeholder="Click or tab into this field"
  aria-label="Email input"
/>

<p id="status" aria-live="polite"></p>

<script>
  const input = document.getElementById("emailInput");
  const status = document.getElementById("status");

  input.addEventListener("focus", () => {
    status.textContent = "Input received focus";
  });
</script>
```

:::

# --assignment--

JavaScript와 접근성 주제 및 개념을 검토하세요.
