---
id: 66ed8fd7f45ce3ece4053eb0
title: 列表、链接、背景和边框测验
challengeType: 8
dashedName: quiz-css-backgrounds-and-borders
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

哪个 CSS 属性用于指定用作列表项标记的图像？

#### --distractors--

`list-style-position`

---

`background-image`

---

`content-image`

#### --answer--

`list-style-image`

### --question--

#### --text--

哪个属性用于控制列表项标记的位置？

#### --distractors--

`list-style-type`

---

`list-position`

---

`position-marker`

#### --answer--

`list-style-position`

### --question--

#### --text--

`line-height` 属性的作用是什么？

#### --distractors--

它用于设置元素的背景大小。

---

它用于为用户未访问的链接设置样式。

---

它用于在列表项的右侧创建空白。

#### --answer--

它用于创建文本行间的间距。

### --question--

#### --text--

当用户悬停在链接上时，使用哪个伪类来设置样式？

#### --distractors--

`:hovered`

---

`:hovering`

---

`:hasHovered`

#### --answer--

`:hover`

### --question--

#### --text--

`background-repeat` 属性的默认值是什么？

#### --distractors--

`no-repeat`

---

`repeat-x`

---

`space`

#### --answer--

`repeat`

### --question--

#### --text--

哪个 CSS `property` 可用于控制背景 `Image` 的大小？

#### --distractors--

`image-size`

---

`background-fit`

---

`size-image`

#### --answer--

`background-size`

### --question--

#### --text--

以下哪种方法是为元素应用实心红色顶部边框的正确方式？

#### --distractors--

`set-top-border: 3px solid red;`

---

`border-top: apply 3px solid red;`

---

`top-border: 3px solid red;`

#### --answer--

`border-top: 3px solid red;`

### --question--

#### --text--

以下哪项不是有效的 `border` 属性？

#### --distractors--

`border-bottom`

---

`border-right`

---

`border-top`

#### --answer--

`border-side`

### --question--

#### --text--

以下哪项是有效的 `border-style` 值？

#### --distractors--

`wave`

---

`triple`

---

`zig zag`

#### --answer--

`dotted`

### --question--

#### --text--

以下哪种方式是使用简写属性应用边框的正确方法？

#### --distractors--

```css
.box {
  borders: 1px solid black;
}
```

---

```css
.box {
  borderShort: 1px solid black;
}
```

---

```css
.box {
  border-short: 1px solid black;
}
```

#### --answer--

```css
.box {
  border: 1px solid black;
}
```

### --question--

#### --text--

`linear-gradient` 函数的作用是什么？

#### --distractors--

它用于设置元素的边框样式。

---

它用于确定背景图像应如何沿水平和垂直轴重复。

---

它用于为被用户激活的元素设置样式。

#### --answer--

它用于在多种颜色之间沿直线创建过渡。

### --question--

#### --text--

`background-repeat` 属性的哪个值确保图像在水平方向重复但不在垂直方向重复？

#### --distractors--

`repeat-y`

---

`space`

---

`no-repeat`

#### --answer--

`repeat-x`

### --question--

#### --text--

`border-radius` 属性的作用是什么？

#### --distractors--

为边框创建阴影。

---

要更改边框的宽度。

---

使边框消失。

#### --answer--

将元素的角落圆角化。

### --question--

#### --text--

以下哪项不是 `background-position` 属性的有效值？

#### --distractors--

`top`

---

`center`

---

`bottom`

#### --answer--

`side`

### --question--

#### --text--

以下哪个 `background-repeat` 值可用于垂直重复背景图像？

#### --distractors--

`repeat-x`

---

`vertical-only`

---

`stretch`

#### --answer--

`repeat-y`

### --question--

#### --text--

`background-position: center;` 对背景图像有什么作用？

#### --distractors--

它将背景图像对齐到元素的顶部。

---

它在水平方向和垂直方向重复背景 `Image`。

---

它将背景图像缩放以填充整个元素。

#### --answer--

它将背景图像定位在元素的中心。

### --question--

#### --text--

以下哪个不是有效的 `border` 值？

#### --distractors--

`1px solid black`

---

`3px dotted blue`

---

`2px dashed red`

#### --answer--

`5px inverted green`

### --question--

#### --text--

`radial-gradient` 函数的作用是什么？

#### --distractors--

它创建一个沿直线在多种颜色之间过渡的背景。

---

它指定背景图像是应随内容滚动还是保持固定位置。

---

它用于为被用户激活的元素设置样式。

#### --answer--

它创建一个从特定点辐射并在多种颜色之间逐渐过渡的图像。

### --question--

#### --text--

以下哪项不是有效的伪类？

#### --distractors--

`:focus`

---

`:visited`

---

`:link`

#### --answer--

`:before`

### --question--

#### --text--

以下哪项正确使用了 `background` 简写属性？

#### --distractors--

```css
body {
  background: apply url("example-url-goes-here");
}
```

---

```css
body {
  background: set url("example-url-goes-here");
}
```

---

```css
body {
  background: 2px solid red url("example-url-goes-here");
}
```

#### --answer--

```css
body {
  background: no-repeat url("example-url-goes-here");
}
```

## --quiz--

### --question--

#### --text--

`line-height` 属性可以接受什么类型的值？

#### --distractors--

仅像素值

---

只有关键字值如 `inherit`

---

仅限 em 单位

#### --answer--

数字、百分比和长度单位

### --question--

#### --text--

以下哪项不是 `line-height` 的有效值？

#### --distractors--

`normal`

---

`150%`

---

`2em`

#### --answer--

`bold`

### --question--

#### --text--

`list-style-type` 控制什么？

#### --distractors--

列表标记的位置

---

列表项的间距

---

列表项的对齐

#### --answer--

列表中使用的标记符号

### --question--

#### --text--

哪个值对于 `list-style-type` 来说无效？

#### --distractors--

`circle`

---

`disc`

---

`square`

#### --answer--

`hexagon`

### --question--

#### --text--

`list-style-position` 的两个可接受值是什么？

#### --distractors--

`top` 和 `bottom`

---

`inline` 和 `block`

---

`left` 和 `right`

#### --answer--

`inside` 和 `outside`

### --question--

#### --text--

哪个 CSS 属性为每个列表项外部添加间距以提高可读性？

#### --distractors--

`padding`

---

`line-height`

---

`text-indent`

#### --answer--

`margin`

### --question--

#### --text--

`:link` 伪类的目标是什么？

#### --distractors--

所有超链接

---

悬停的链接

---

已访问的链接

#### --answer--

未访问的链接

### --question--

#### --text--

当用户已经点击过链接时，哪个伪类会为该链接设置样式？

#### --distractors--

`:hover`

---

`:link`

---

`:focus`

#### --answer--

`:visited`

### --question--

#### --text--

哪个伪类在元素获得输入焦点时应用样式？

#### --distractors--

`:hover`

---

`:visited`

---

`:target`

#### --answer--

`:focus`

### --question--

#### --text--

`:active` 伪类何时应用？

#### --distractors--

当 `link` 第一次装载时

---

当 `user` 未进行交互时

---

当浏览器完成渲染时

#### --answer--

当用户点击或触碰该元素

### --question--

#### --text--

哪个伪类用于悬停效果？

#### --distractors--

`:link`

---

`:click`

---

`:active`

#### --answer--

`:hover` 

### --question--

#### --text--

`background-size: cover` 做什么？

#### --distractors--

在所有方向重复图像

---

裁剪图像

---

将图像固定在一个位置

#### --answer--

将图像缩放以填充元素，同时保持纵横比

### --question--

#### --text--

`background-repeat` 的哪个值会使背景图像在水平方向和垂直方向上重复？

#### --distractors--

`no-repeat`

---

`repeat-x`

---

`space`

#### --answer--

`repeat`

### --question--

#### --text--

哪个 `property` 设置背景 `Image` 的位置？

#### --distractors--

`background-location`

---

`background-origin`

---

`background-place`

#### --answer--

`background-position`

### --question--

#### --text--

`background-attachment` 的什么值可以在滚动时保持图像固定？

#### --distractors--

`scroll`

---

`static`

---

`sticky`

#### --answer--

`fixed`

### --question--

#### --text--

以下哪项正确设置了不重复且居中的背景图像？

#### --distractors--

`background: no-repeat-center img("img.png");`

---

`background: image url("img.png") center;`

---

`background-position: center no-repeat url("img.png");`

#### --answer--

`background: center no-repeat url("img.png");`

### --question--

#### --text--

哪个属性用于在单个声明中设置上、右、下、左边框？

#### --distractors--

`border-style`

---

`border-radius`

---

`outline`

#### --answer--

`border`

### --question--

#### --text--

设置 `border-radius: 10px;` 对元素有什么效果？

#### --distractors--

它改变边框的不透明度

---

它创建了一个 10 像素的边框

---

它添加了 10px 阴影

#### --answer--

它将元素的角落进行圆角处理

### --question--

#### --text--

以下哪个 CSS 规则正确地创建了从红色到黄色的线性渐变？

#### --distractors--

`background: gradient-linear(red to yellow);`

---

`background: linear(red, yellow);`

---

`background-color: gradient(red, yellow);`

#### --answer--

`background: linear-gradient(red, yellow);`

### --question--

#### --text--

以下哪个 CSS 声明正确地应用了从中心白色到边缘蓝色的径向渐变？

#### --distractors--

`background: linear-gradient(white to blue);`

---

`background: gradient-radial(white to blue);`

---

`background: radial-gradient(blue, white);`

#### --answer--

`background: radial-gradient(white, blue);`

