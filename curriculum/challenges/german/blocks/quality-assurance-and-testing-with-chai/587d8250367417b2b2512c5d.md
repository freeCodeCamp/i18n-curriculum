---
id: 587d8250367417b2b2512c5d
title: Funktionale Tests mit einem Headless-Browser ausführen
challengeType: 2
forumTopicId: 301595
dashedName: run-functional-tests-using-a-headless-browser
---

# --description--

Zur Erinnerung: Dieses Projekt basiert auf dem folgenden Starter-Projekt, das von <a href="https://github.com/freeCodeCamp/boilerplate-mochachai/" target="_blank" rel="noopener noreferrer nofollow">GitHub</a> geklont wurde.

Auf der Seite gibt es ein Eingabeformular. Es sendet Daten als AJAX-Anfrage an den `PUT /travellers`-Endpunkt.

Wenn die Anfrage erfolgreich abgeschlossen wird, fügt der Client-Code ein `<div>` mit den Informationen aus der Antwort dem DOM hinzu.

Hier ein Beispiel, wie Sie Zombie.js verwenden, um mit dem Formular zu interagieren:

```js
test('Submit the surname "Polo" in the HTML form', function (done) {
  browser.fill('surname', 'Polo').then(() => {
    browser.pressButton('submit', () => {
      browser.assert.success();
      browser.assert.text('span#name', 'Marco');
      browser.assert.text('span#surname', 'Polo');
      browser.assert.elements('span#dates', 1);
      done();
    });
  });
});
```

Zuerst füllt die `fill`-Methode des `browser`-Objekts das `surname`-Feld des Formulars mit dem Wert `'Polo'` aus. `fill` gibt ein Promise zurück, daher wird `then` daran angehängt.

Innerhalb des `then`-Callbacks wird die `pressButton`-Methode des `browser`-Objekts verwendet, um den `submit`-Ereignis-Listener des Formulars auszulösen. Die `pressButton`-Methode ist asynchron.

Sobald eine Antwort von der AJAX-Anfrage empfangen wird, werden einige Zusicherungen getroffen, die bestätigen:

1.  Der Status der Antwort ist `200`
2.  Der Text innerhalb des `<span id='name'></span>`-Elements entspricht `'Marco'`
3.  Der Text innerhalb des `<span id='surname'></span>`-Elements entspricht `'Polo'`
4.  Es gibt `1` `<span id='dates'></span>`-Element(e).

Schließlich wird das `done`-Callback aufgerufen, das wegen des asynchronen Tests benötigt wird.

# --instructions--

Innerhalb von `tests/2_functional-tests.js`, im `'Submit the surname "Colombo" in the HTML form'`-Test (`// #5`), automatisieren Sie Folgendes:

1.  Füllen Sie das Formular mit dem Nachnamen `Colombo` aus
2.  Drücken Sie die Absenden-Schaltfläche

Und innerhalb des `pressButton`-Callbacks:

1.  Versichern Sie, dass der Status OK ist `200`
2.  Versichern Sie, dass der Text im Element `span#name` `'Cristoforo'` ist
3.  Versichern Sie, dass der Text im Element `span#surname` `'Colombo'` ist
4.  Versichern Sie, dass das/die Element(e) `span#dates` existieren und deren Anzahl `1` beträgt

Vergessen Sie nicht, den `assert.fail()`-Aufruf zu entfernen.

# --hints--

Alle Tests sollten bestanden werden.

```js
const response = await fetch(code + '/_api/get-tests?type=functional&n=5');
if (!response.ok) {
  throw Error(await response.text());
}
const data = await response.json();
assert.equal(data.state, 'passed');
```

Sie sollten versichern, dass die Anfrage des Headless-Browsers erfolgreich war.

```js
const response = await fetch(code + '/_api/get-tests?type=functional&n=5');
if (!response.ok) {
  throw Error(await response.text());
}
const data = await response.json();
assert.equal(data.assertions[0].method, 'browser.success');
```

Sie sollten versichern, dass der Text im Element `span#name` `'Cristoforo'` ist.

```js
const response = await fetch(code + '/_api/get-tests?type=functional&n=5');
if (!response.ok) {
  throw Error(await response.text());
}
const data = await response.json();
assert.equal(data.assertions[1].method, 'browser.text');
assert.match(data.assertions[1].args[0], /('|")span#name\1/);
assert.match(data.assertions[1].args[1], /('|")Cristoforo\1/);
```

Sie sollten versichern, dass der Text im Element `span#surname` `'Colombo'` ist.

```js
const response = await fetch(code + '/_api/get-tests?type=functional&n=5');
if (!response.ok) {
  throw Error(await response.text());
}
const data = await response.json();
assert.equal(data.assertions[2].method, 'browser.text');
assert.match(data.assertions[2].args[0], /('|")span#surname\1/);
assert.match(data.assertions[2].args[1], /('|")Colombo\1/);
```

Sie sollten versichern, dass das Element `span#dates` existiert und seine Anzahl 1 beträgt.

```js
const response = await fetch(code + '/_api/get-tests?type=functional&n=5');
if (!response.ok) {
  throw Error(await response.text());
}
const data = await response.json();
assert.equal(data.assertions[3].method, 'browser.elements');
assert.match(data.assertions[3].args[0], /('|")span#dates\1/);
assert.equal(data.assertions[3].args[1], 1);
```

