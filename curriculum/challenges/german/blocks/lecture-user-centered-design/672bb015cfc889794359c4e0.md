---
id: 672bb015cfc889794359c4e0
title: Was sind bewährte Vorgehensweisen für das Design von Modal-Dialogen?
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**HINWEIS**: Einige der interaktiven Beispiele verwenden möglicherweise CSS und JavaScript, die Sie noch nicht gelernt haben. Machen Sie sich keine Sorgen, wenn Sie nicht versuchen, den gesamten Code zu verstehen. Das Ziel der Beispiele ist es, Ihnen Vorschauen zu diesen Designkonzepten zu zeigen, damit Sie besser verstehen, wie die Dinge funktionieren. Um die Vorschauen zu sehen, müssen Sie den interaktiven Editor aktivieren.

Was ist ein Modal? Es ist die Art von Pop-up, die eine Website Ihnen über ihrem Inhalt anzeigen könnte. HTML hat ein `dialog`-Element, das Sie verwenden können, um Modals zu erstellen.

Der Inhalt hinter einem Modal wird normalerweise abgedunkelt. Das hilft dem Benutzer, sich visuell auf den Bereich zu konzentrieren, mit dem er interagieren soll – in diesem Fall das Modal.

Es ist immer eine gute Idee, dem Benutzer zu erlauben, außerhalb des Modals zu klicken, um es zu schließen.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

Sie werden oft sehr auffällige Schaltflächen auf Modals sehen. Diese werden CTAs genannt, oder call-to-action. Sie sollen leicht erkennbar sein, da der Zweck, den Benutzerfluss mit einem Modal zu unterbrechen, darin besteht, sie zu einer bestimmten Aktion aufzufordern.

Modals sollten auch eine Schließen-Schaltfläche haben. Auch wenn Sie wirklich möchten, dass der Benutzer auf Ihre CTAs klickt, ist es wichtig, ihm eine Möglichkeit zu geben, das Modal zu verlassen und mit dem fortzufahren, was er zuvor gemacht hat.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

Natürlich gibt es Barrierefreiheitsaspekte bei Modals, wie zum Beispiel das korrekte Verwalten des Fokus auf Elemente. Wenn Sie jedoch diese allgemeinen Praktiken als Ausgangspunkt verwenden, haben Sie eine solide Grundlage, auf der Sie aufbauen können.

# --questions--

## --text--

Was ist der Zweck, den Inhalt hinter einem Modal-Dialog abzudunkeln?

## --answers--

Um die Akkulaufzeit auf mobilen Geräten zu sparen.

### --feedback--

Die Lektion nennt einen bestimmten Grund für diesen visuellen Effekt.

---

Um den Benutzern zu helfen, visuell den Bereich zu erkennen, mit dem sie interagieren sollen.

---

Um die Website schneller laden zu lassen.

### --feedback--

Die Lektion nennt einen bestimmten Grund für diesen visuellen Effekt.

---

Um zu verhindern, dass Benutzer sensible Informationen sehen.

### --feedback--

Die Lektion nennt einen bestimmten Grund für diesen visuellen Effekt.

## --video-solution--

2

## --text--

Wofür steht CTA im Kontext des Modal-Designs?

## --answers--

Click To Activate.

### --feedback--

Die Lektion nennt den vollständigen Begriff für dieses Akronym beim Thema auffällige Schaltflächen in Modals.

---

Complete The Action.

### --feedback--

Die Lektion nennt den vollständigen Begriff für dieses Akronym beim Thema auffällige Schaltflächen in Modals.

---

Call-to-Action.

---

Close The Application.

### --feedback--

Die Lektion nennt den vollständigen Begriff für dieses Akronym beim Thema auffällige Schaltflächen in Modals.

## --video-solution--

3

## --text--

Warum ist es wichtig, eine Schließen-Schaltfläche in einem Modal-Dialog einzufügen?

## --answers--

Es ist in einigen Ländern gesetzlich vorgeschrieben.

### --feedback--

Die Lektion betont die Wichtigkeit, den Benutzern eine Möglichkeit zu geben, das Modal zu verlassen.

---

Um zu verhindern, dass das Modal den Browser zum Absturz bringt.

### --feedback--

Die Lektion betont die Wichtigkeit, den Benutzern eine Möglichkeit zu geben, das Modal zu verlassen.

---

Um den Benutzern eine Option zu geben, das Modal zu verlassen und ihre vorherige Tätigkeit fortzusetzen.

---

Um das Modal professioneller aussehen zu lassen.

### --feedback--

Die Lektion betont die Wichtigkeit, den Benutzern eine Möglichkeit zu geben, das Modal zu verlassen.

## --video-solution--

3
