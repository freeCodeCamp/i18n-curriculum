---
id: 6882902bc844b1723da7eb13
title: ¿Cómo instalas y configuras Git?
challengeType: 19
dashedName: how-do-you-install-and-set-up-git
---

# --description--

Antes de que realmente puedas empezar a usar el control de versiones y sitios como GitHub, necesitas tener Git instalado y listo para usar.

Para verificar si Git ya está instalado en tu máquina, puedes ejecutar el siguiente comando en la terminal:

```sh
git --version
```

Si ves un número de versión, eso significa que Git está instalado. Si no, entonces necesitarás instalarlo.

Para sistemas Linux, Git a menudo viene preinstalado con la mayoría de las distribuciones. Si no tienes Git preinstalado, deberías poder instalarlo con los comandos de tu gestor de paquetes, como `sudo apt-get install git` o `sudo pacman -S git`.

Para usuarios de Mac, puedes instalar Git vía Homebrew con `brew install git`, o puedes descargar el instalador ejecutable desde el sitio web de Git.

Para Windows, puedes descargar el instalador ejecutable desde el sitio web de Git. O, si has configurado Chocolatey, puedes ejecutar `choco install git.install` en PowerShell. Ten en cuenta que en Windows, también puede que quieras descargar GitBash para tener un entorno de shell similar a Unix disponible.

Para asegurarte de que la instalación funcionó, ejecuta el comando `git --version` nuevamente en la terminal.

Una vez que tengas Git instalado, necesitarás hacer algunos cambios en la configuración. Solo deberás hacer estos cambios una vez en tu computadora y no tendrás que hacer cambios continuamente cuando realices actualizaciones.

`git config` se usa para establecer variables de configuración que determinan cómo Git funciona en tu máquina. Para ver las variables de configuración actuales y dónde están almacenadas en tu sistema, puedes ejecutar el siguiente comando:

```sh
git config --list --show-origin
```

En este momento deberías estar viendo solo configuraciones a nivel de sistema si acabas de instalar Git por primera vez. Si ya habías instalado Git antes de esta lección, entonces podrías estar viendo configuraciones de usuario para un nombre de usuario y correo electrónico.

Para establecer tu nombre de usuario, puedes ejecutar el siguiente comando:

```sh
git config --global user.name "Jane Doe"
```

La bandera `--global` se usa aquí para establecer el nombre de usuario para todos los proyectos en tu sistema que usan Git. Si necesitas sobrescribir el nombre de usuario para un proyecto en particular, entonces puedes ejecutar el comando en ese directorio de proyecto sin la bandera `--global`.

Para establecer la dirección de correo electrónico del usuario, puedes ejecutar el siguiente comando:

```sh
git config --global user.email janedoe@example.com
```

Configurar tu nombre de usuario y correo electrónico es importante porque Git usará esta información para cada commit que hagas en un proyecto. Aprenderás más sobre commits en una lección futura.

Otra configuración que puedes establecer es el editor preferido que quieres que Git use. Aquí tienes un ejemplo de cómo establecer tu editor preferido a Emacs:

```sh
git config --global core.editor emacs
```

Otras opciones incluyen Vim, Nano, VS Code y más. Por ejemplo, si usas VS Code como tu editor, entonces puedes configurar los ajustes de Git así:

```sh
git config --global core.editor "code --wait"
```

Si eliges no configurar un editor preferido, entonces Git usará el editor predeterminado de tu sistema.

Si estás usando una máquina con Windows y quieres cambiar tu editor, necesitarás proporcionar la ruta completa al archivo ejecutable así:

```sh
git config --global core.editor "'C:/Program Files/Notepad++/notepad++.exe' -multiInst -notabbar -nosession -noPlugin"
```

Para ver tu lista de configuraciones, puedes ejecutar el siguiente comando:

```sh
git config --list
```

Ahora que tienes Git instalado y configurado, ya estás listo para comenzar a agregar control de versiones a tus proyectos.

# --questions--

## --text--

¿Qué hace el siguiente comando?

```sh
git config --list --show-origin
```

## --answers--

Muestra una lista de solo los editores disponibles para usar con Git y dónde están almacenados en tu sistema.

### --feedback--

Consulta la sección que habla sobre la configuración de Git para la respuesta.

---

Muestra una lista solo de opciones de color en tu configuración de Git.

### --feedback--

Consulta la sección que habla sobre la configuración de Git para la respuesta.

---

Muestra las variables de configuración actuales y dónde están almacenadas en tu sistema.

---

Muestra una lista de todos los proyectos en tu sistema que usan Git.

### --feedback--

Consulta la sección que habla sobre la configuración de Git para la respuesta.

## --video-solution--

3

## --text--

¿Cuál es el propósito de usar el flag `--global` al configurar tu nombre de usuario?

## --answers--

La bandera `--global` se usa para establecer el nombre de usuario en algunos proyectos de tu sistema que usan Git.

### --feedback--

Piensa en cómo esta bandera puede afectar a todos los proyectos que usan Git.

---

La bandera `--global` se usa para establecer el nombre de usuario en todos los proyectos de tu sistema que usan Git.

---

La bandera `--global` se usa para establecer el nombre de usuario para ninguno de los proyectos en tu sistema que usan Git.

### --feedback--

Piensa en cómo esta bandera puede afectar a todos los proyectos que usan Git.

---

La bandera `--global` se usa para establecer el nombre de usuario solo en proyectos de tu sistema que tienen más de 5 años y usan Git.

### --feedback--

Piensa en cómo esta bandera puede afectar a todos los proyectos que usan Git.

## --video-solution--

2

## --text--

¿Cuál de las siguientes NO es una opción válida mencionada para configurar tu editor preferido en Git?

## --answers--

Vim

### --feedback--

Consulta el final del script donde se configuraron Git y los editores.

---

Emacs

### --feedback--

Consulta el final del script donde se configuraron Git y los editores.

---

Nano

### --feedback--

Consulta el final del script donde se configuraron Git y los editores.

---

ESLint

## --video-solution--

4
