---
id: 66edcdd18a4ef8df16e6bb7e
title: اختبار الدوال ذات الرتبة العليا في JavaScript
challengeType: 8
dashedName: quiz-javascript-higher-order-functions
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي من العبارات التالية حول الدوال ذات الرتبة العليا في JavaScript غير صحيحة؟

#### --distractors--

يمكن للدوال ذات الرتبة العليا تعزيز قابلية قراءة الشفرة وصيانتها بشكل كبير من خلال تمكين تقنيات البرمجة الوظيفية.

---

الدوال ذات الرتبة العليا مثل map وfilter وreduce هي أدوات قوية لمعالجة المصفوفات، لكنها ليست حصرية للبرمجة الوظيفية.

---

قد تُدخل الدوال ذات الرتبة العليا تعقيدًا في فهم الشفرة، لكنها قد تؤدي أيضًا إلى حلول أكثر تعبيرًا واختصارًا.

#### --answer--

جميع الدوال في JavaScript، بما في ذلك تلك التي لا تأخذ أو تُرجع دوال أخرى، يمكن تصنيفها كدوال ذات رتبة عليا.

### --question--

#### --text--

ما هي دالة المصنع في سياق الدوال ذات الرتبة العليا؟

#### --distractors--

دالة تُنشئ متغيرات جديدة.

---

دالة تعمل فقط مع السلاسل النصية.

---

دالة تُولد تعليقات شفرة تلقائيًا.

#### --answer--

دالة تُرجع دالة جديدة بناءً على معلمات محددة.

### --question--

#### --text--

بعد تنفيذ الشفرة، ما ستكون قيمة `forEachRes` و`mapRes`؟

```js
const numbers = [1, 1, 1, 1, 1];
let sum = 0;
const forEachRes = numbers.forEach(num => {
  return (sum += num);
});
const mapRes = numbers.map(num => {
  return (sum += num);
});
```

#### --distractors--

`forEachRes` هي `undefined` و`mapRes` هي `[1,2,3,4,5]`

---

`forEachRes` هي `0` و`mapRes` هي `[1,2,3,4,5]`

---

`forEachRes` هي `5` و`mapRes` هي `[1,2,3,4,5]`

#### --answer--

`forEachRes` هي `undefined` و`mapRes` هي `[6,7,8,9,10]`

### --question--

#### --text--

ما نتيجة هذه الشفرة؟

```js
[, undefined, 'a', 'b', { 20: 5 }].sort();
```

#### --distractors--

عناصر غير مدعومة لمصفوفة للفرز، لذا يحدث خطأ.

---

لم يتم توفير استدعاء راجع، لذا يحدث خطأ.

---

```js
[empty, 'a', 'b', undefined, { '20': 5 }]
```

#### --answer--

```js
[{ '20': 5 }, 'a', 'b', undefined, empty]
```

### --question--

#### --text--

أي من التالي يصف دالة استدعاء راجع في JavaScript؟

#### --distractors--

دالة تُستدعى فور إعلانها.

---

دالة تُستدعى في سياق محدد.

---

دالة تُرجع دالة أخرى.

#### --answer--

دالة تُمرر كمعلمة إلى دالة أخرى، ليتم تنفيذها بواسطة منطق تلك الدالة.

### --question--

#### --text--

ما نتيجة استخدام `reduce()` على مصفوفة؟

#### --distractors--

قيمة منطقية تشير إلى ما إذا كانت أي عناصر تحقق شرطًا.

---

مصفوفة مع تقليل جميع العناصر بواسطة دالة الاستدعاء الراجعة المحددة.

---

مصفوفة من القيم المنطقية.

#### --answer--

تختلف حسب القيمة الابتدائية للمُجمّع ودالة الاستدعاء الراجعة.

### --question--

#### --text--

كيف تتصرف طريقة `sort()` إذا لم تُقدم دالة مقارنة في الفرز الرقمي؟

#### --distractors--

تملأ الخانات الفارغة بـ `null`.

---

تعيد مصفوفة من الأحرف الخاصة.

---

ترتب المصفوفة بترتيب عكسي.

#### --answer--

ترتب المصفوفة كسلاسل نصية بناءً على وحدات شفرة UTF-16.

### --question--

#### --text--

ما الغرض من طريقة `some()` في JavaScript؟

#### --distractors--

إنشاء مصفوفة جديدة بنتائج دالة تُطبق على كل عنصر.

---

التكرار خلال مصفوفة دون إنتاج نتيجة.

---

تقليل مصفوفة إلى قيمة واحدة بناءً على دالة استدعاء راجعة.

#### --answer--

تحديد ما إذا كانت أي عناصر في المصفوفة تجتاز اختبارًا محددًا.

### --question--

#### --text--

أي من الأمثلة التالية هو مثال صالح لتسلسل الطرق؟

#### --distractors--

```js
Math.random();
```

---

```js
array.push(1).pop();
```

---

```js
console.log('Hello');
```

#### --answer--

```js
str.toLowerCase().trim().replace(' ', '_');
```

### --question--

#### --text--

ما ناتج الشفرة التالية؟

```js
let numbers = [2, 4, 8, 10];

numbers.forEach(function(number) {
    console.log(number % 2);
});
```

#### --distractors--

`2 4 8 10`

---

`null null null null`

---

`1 2 4 5`

#### --answer--

`0 0 0 0`

### --question--

#### --text--

أي من التالي فائدة لتسلسل الطرق؟

#### --distractors--

يحسن الأداء بطبيعته عن طريق تقليل وقت تنفيذ الدوال.

---

يلغي الحاجة إلى متغيرات مؤقتة، لكنه قد يزيد من استخدام الذاكرة في بعض الحالات.

---

يسمح بأن يكون التعامل مع الأخطاء وتصحيحها أكثر سهولة.

#### --answer--

يعزز صيغة نحوية مبسطة وشفرة أكثر قابلية للقراءة بالسماح بتنفيذ عمليات متعددة في تعبير واحد.

### --question--

#### --text--

كيف يمكنك فرز مصفوفة من الكائنات حسب خاصية محددة باستخدام طريقة `sort`؟

#### --distractors--

طريقة `sort` لا يمكنها فرز الكائنات.

---

استخدم طريقة `reverse` بعد الفرز.

---

حوّل الكائنات إلى سلاسل نصية وفرزها.

#### --answer--

استخدم دالة مقارنة تقارن قيم الخاصية.

### --question--

#### --text--

في تسلسل الطرق، ما الممارسة الشائعة لتعزيز الوضوح وتصحيح الأخطاء؟

#### --distractors--

استخدم عددًا أقل من الطرق في التسلسل.

---

تجنب تسلسل الطرق التي تُرجع قيمًا بدائية فقط.

---

استخدم الطرق المدمجة فقط.

#### --answer--

قسّم التسلسلات الطويلة إلى خطوات متعددة.

### --question--

#### --text--

ما العيب المحتمل للاستخدام المفرط لتسلسل الطرق في الشفرة؟

#### --distractors--

يجعل الشفرة تعمل أبطأ.

---

يمنع استخدام التعليقات.

---

يزيد من حجم الملف.

#### --answer--

يمكن أن يجعل الشفرة أصعب في تصحيح الأخطاء.

### --question--

#### --text--

أي طريقة تستخدم لتحديد ما إذا كانت كل العناصر في مصفوفة هي سلاسل نصية؟

#### --distractors--

`some()`

---

`everyInstance()`

---

`filter()`

#### --answer--

`every()`

### --question--

#### --text--

ما ستكون قيمة `originalArray` بعد تنفيذ الشفرة التالية؟

```js
const originalArray = [{ id: 1 }, { id: 2 }, { id: 3 }];
const filteredArray = originalArray.filter(item => item.id > 1);
filteredArray[0].id = 4;
```

#### --distractors--

`[{ id: 1 }, { id: 2 }, { id: 3 }]`

---

`[{ id: 1 }]`

---

`[{ id: 4 }, { id: 2 }, { id: 3 }]`

#### --answer--

`[{ id: 1 }, { id: 4 }, { id: 3 }]`
### --question--

#### --text--

ما ستكون قيمة `shortWords` بعد تنفيذ الشفرة التالية؟

```js
const words = ['apple', 'banana', 'pear', 'kiwi'];
const shortWords = words.filter(word => word.length <= 5);
```

#### --distractors--

`[]`

---

`['pear', 'kiwi']`

---

`['apple', 'banana']`

#### --answer--

`['apple', 'pear', 'kiwi']`

### --question--

#### --text--

ما الغرض من توفير قيمة ابتدائية كمعلمة لطريقة `reduce()`؟

#### --distractors--

لتحديد طول المصفوفة.

---

لتحديد عدد التكرارات.

---

لتحديد نوع القيمة المرجعة للدالة.

#### --answer--

لتعريف القيمة الابتدائية للمُجمّع.

### --question--

#### --text--

هل يمكن استخدام طريقة `map` على كائنات ليست مصفوفات؟

#### --distractors--

نعم، يمكن استخدامها على أي كائن.

---

نعم، لكن فقط على الكائنات ذات الخصائص الرقمية.

---

يعتمد على إصدار JavaScript.

#### --answer--

لا، هي مصممة خصيصًا للمصفوفات.

### --question--

#### --text--

ما الغرض الأساسي من طريقة `map` في JavaScript؟

#### --distractors--

فرز مصفوفة وإرجاع مصفوفة جديدة مع الحفاظ على الترتيب الأصلي.

---

تصفية عناصر من مصفوفة وإزالة أو إضافة عناصر بناءً على شرط.

---

العثور على عنصر محدد في مصفوفة وإرجاع فهرسه مع العنصر.

#### --answer--

إنشاء مصفوفة جديدة تحتوي على نتائج استدعاء دالة معينة على كل عنصر في المصفوفة الأصلية.
