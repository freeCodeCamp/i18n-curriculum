---
id: 68c3bc4ddb7b469fb7d17c28
title: 什么是多态以及它如何促进代码重用？
challengeType: 19
dashedName: what-is-polymorphism-and-how-does-it-promote-code-reuse
---

# --description--

多态是我们将要讨论的下一个面向对象编程（OOP）的关键概念。

通过多态，你可以访问一个接口，在那里你可以与许多同类对象进行交互。

让我们深入了解多态以及它如何让你重用代码。

多态允许不同类中的方法共享相同的名称，但执行不同的任务。你在不同的对象上调用相同的方法名称，每个对象都会以自己的方式响应。

这是多态的基本示例：

```py
class A:
   def action(self): ...

class B:
   def action(self): ...

class C:
   def action(self): ...

Class().method()  # Works for A, B, or C
```

这是一个使用不同动物叫声来描述多态的示例：

```py
class Cat:
   def speak(self):
       return "A cat meow"

class Bird:
   def speak(self):
       return "A bird tweet"
  
class Monkey:
   def speak(self):
       return "A monkey ooh ooh aah aah ooh ooh aah aah"

def animal_sound(animal):
   print(animal.speak())

animal_sound(Cat())
animal_sound(Bird())
animal_sound(Monkey())
```

在这个例子中，`animal_sound()` 是一个函数，它接受任何带有 `speak()` 方法的对象。 

当你传入 `Cat`、`Bird` 或 `Monkey` 时，它会调用该对象的 `speak()` 方法并打印结果。因为每个类都以不同方式定义了 `speak()`，所以你会从同一个函数得到不同的输出。这就是多态的体现。

这是另一个示例，这次包含实例和一个属性：

```py
class Twitter:
   def __init__(self, content):
       self.content = content

   def post(self):
       return f"🐦 Tweet: '{self.content}' (280 chars max)"

class Instagram:
   def __init__(self, content):
       self.content = content

   def post(self):
       return f"📸 Instagram Post: '{self.content}' + ✨ filters"

class LinkedIn:
   def __init__(self, content):
       self.content = content

   def post(self):
       return f"💼 LinkedIn Article: '{self.content}' (Professional Mode)"

def start(social_media):
   print(social_media.post())  # Calls .post() on any object

# Instances
tweet = Twitter('Just learned Python polymorphism!')
photo = Instagram('Sunset vibes 🌅')
article = LinkedIn('Why OOP matters in 2024')

# The polymorphic calls - same function, different outputs
start(tweet) # 🐦 Tweet: 'Just learned Python polymorphism!' (280 chars max)
start(photo) # 📸 Instagram Post: 'Sunset vibes 🌅' + ✨ filters
start(article) # 💼 LinkedIn Article: 'Why OOP matters in 2024' (Professional Mode)
```

还有一种称为**基于继承的多态**的多态。

在基于继承的多态中，父类定义一个方法，多个子类以各自的方式覆写该方法。然后你可以对任何子类对象调用相同的方法，并且它会根据所属的子类表现出不同的行为。

这是一个示例：

```py
class Animal:
   def speak(self):
       return 'Some generic sound'

class Cat(Animal):
   def speak(self):
       return 'A cat meow'

class Dog(Animal):
   def speak(self):
       return 'A dog barks woof woof'

class Monkey(Animal):
   def speak(self):
       return 'A monkey ooh ooh aah aah ooh ooh aah aah'
  
print(Cat().speak()) # A cat meow
print(Dog().speak()) # A dog barks woof woof
print(Monkey().speak()) # A monkey ooh ooh aah aah ooh ooh aah aah
print(Animal().speak()) # Some generic sound
```

你可以看到，父类 `Animal` 的每个子类都覆写了 `speak()` 方法以提供其自己的实现。因此，当你在每个子类的实例上调用 `speak()` 方法时，它会返回与该动物关联的特定声音。

你也可以更进一步，在一个列表中进行调用，然后循环遍历该列表以显示每个 `speak()` 方法返回的内容：

```py
animals = [Cat(), Dog(), Monkey()]

for animal in animals:
   print(animal.speak())

# Output:
# A cat meow
# A dog barks woof woof
# A monkey ooh ooh aah aah ooh ooh aah aah
```

# --questions--

## --text--

面向对象编程中的多态是什么？

## --answers--

当一个类不能有同名的方法时。

### --feedback--

思考“polymorphism”这个词如何表示同一动作的“多种形式”。

---

当不同的类可以使用相同的方法名但实现方式不同。

---

当一个方法必须在所有类中唯一时。

### --feedback--

思考“polymorphism”这个词如何表示同一动作的“多种形式”。

---

当子类删除父类的`method`时。

### --feedback--

思考“polymorphism”这个词如何表示同一动作的“多种形式”。

## --video-solution--

2

## --text--

在基于继承的多态中，子类必须做什么才能为父类中定义的某个方法提供自己的实现？

## --answers--

通过使用相同名称重新定义该方法来覆写该方法。

---

将该方法声明为 `@static`。

### --feedback--

考虑子类如何在保持相同签名的同时重新定义该方法。

---

请先删除父方法。

### --feedback--

考虑子类如何在保持相同签名的同时重新定义该方法。

---

使用 `@polymorphic` 装饰器。

### --feedback--

考虑子类如何在保持相同签名的同时重新定义该方法。

## --video-solution--

1

## --text--

基于继承的多态的主要好处是什么？

## --answers--

它迫使所有子类实现一致的行为。

### --feedback--

注意多态如何让你在不同的子类上调用相同的方法。

---

它防止子类修改父类的方法。

### --feedback--

注意多态如何让你在不同的子类上调用相同的方法。

---

它允许在不同的子对象上调用相同的方法名，每个子对象执行其自己的版本。

---

它将所有子方法合并到父类中。

### --feedback--

注意多态如何让你在不同的子类上调用相同的方法。

## --video-solution--

3
