---
id: 5900f48d1000cf542c50ffa0
title: '문제 289: 오일러 경로'
challengeType: 1
forumTopicId: 301940
dashedName: problem-289-eulerian-cycles
---

# --description--

$C(x,y)$를 점 ($x$, $y$), ($x$, $y + 1$), ($x + 1$, $y$), ($x + 1$, $y + 1$)를 지나는 원이라고 하겠습니다.

양의 정수 $m$과 $n$에 대해, $E(m,n)$을 다음 $m·n$개의 원들로 이루어진 구성으로 정의합니다: { $C(x,y)$: $0 ≤ x < m$, $0 ≤ y < n$, $x$와 $y$는 정수 }

$E(m,n)$ 위의 오일러 경로는 각 호를 정확히 한 번씩 지나는 닫힌 경로입니다. $E(m,n)$ 위에는 여러 경로가 가능하지만, 여기서는 자기 교차하지 않는 경로만 관심 있습니다: 자기 교차하지 않는 경로는 격자점에서만 자신과 접할 뿐, 절대 교차하지 않습니다.

아래 그림은 $E(3,3)$과 오일러 자기 교차하지 않는 경로의 예를 보여줍니다.

<img alt="Eulerian cycle E(3, 3) and Eulerian non-crossing path" src="https://cdn.freecodecamp.org/curriculum/project-euler/eulerian-cycles.gif" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

$L(m,n)$을 $E(m,n)$ 위의 오일러 자기 교차하지 않는 경로의 개수라고 하겠습니다. 예를 들어, $L(1,2) = 2$, $L(2,2) = 37$, $L(3,3) = 104290$입니다.

$L(6,10) \bmod {10}^{10}$ 값을 구하세요.

# --hints--

`eulerianCycles()`는 `6567944538`를 반환해야 합니다.

```js
assert.strictEqual(eulerianCycles(), 6567944538);
```

# --seed--

## --seed-contents--

```js
function eulerianCycles() {

  return true;
}

eulerianCycles();
```

# --solutions--

```js
// solution required
```
