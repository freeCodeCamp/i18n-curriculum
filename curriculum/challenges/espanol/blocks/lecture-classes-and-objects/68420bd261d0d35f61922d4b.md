---
id: 68420bd261d0d35f61922d4b
title: ¿Cómo funcionan las clases y en qué se diferencian de los objetos?
challengeType: 19
dashedName: how-do-classes-work-and-how-do-they-differ-from-objects
---

# --description--

En Python, las clases y los objetos trabajan juntos para organizar y gestionar datos. Construyes una clase para definir comportamientos compartidos, luego creas objetos que usan esos comportamientos.

En otras palabras, una clase es como un plano o plantilla que usas para crear objetos.

Veamos qué son las clases y cómo usarlas para crear objetos.

Para crear una clase, usa la palabra clave `class` seguida del nombre de la clase y dos puntos. Luego, dentro de la clase, puedes agregar un inicializador, junto con cualquier atributo y método.

Los atributos son como variables dentro de una clase y se usan para almacenar datos. Los métodos son funciones definidas dentro de una clase y son las acciones que los objetos creados con una clase pueden realizar.

Aquí está la sintaxis básica de una clase:

```python
class ClassName:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def sample_method(self):               
        print(self.name.upper())
```

* `class ClassName` está compuesto por la palabra clave `class` para crear una clase, seguida del nombre de la clase, aquí llamado `ClassName`. Es común en Python usar la convención **PascalCase** al nombrar clases.
    
* `def __init__(self, name, age)` is the special method automatically called when a new object is created. It initializes the attributes of the objects that will be created with the class.
    
    In addition to that, the first parameter of `__init__` is always a reference to the specific object being created or used. By convention, this parameter is named `self`, but technically, you can use any name. `self` lets you access the object's own attributes and methods.
    
* `self.name = name` y `self.age = age` son los atributos que tendrán los objetos.
    
* `def sample_method(self):` es el método que cada objeto creado puede llamar.
    
* `print(self.name.upper())` es lo que hará el método `sample_method`, en este caso, imprime el nombre en mayúsculas.
    

Si todo eso suena a mucho, no te preocupes. Veamos un ejemplo similar de una clase `Dog`, y cómo puedes crear objetos a partir de ella:

```python
class Dog:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def bark(self):
        print(f"{self.name.upper()} says woof woof!")
```

Con esta clase `Dog`, puedes crear un objeto. Aquí está la sintaxis básica para crear objetos a partir de una clase:

```python
object_1 = ClassName(attribute_1, attribute_2)
object_2 = ClassName(attribute_1, attribute_2)
```

También puedes llamar a cualquiera de los métodos definidos en la clase desde cada objeto:

```python
object_1.method_name()
object_2.method_name()
```

Ahora vamos a crear dos perros usando la clase `Dog` como la estructura:

```python
class Dog:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def bark(self):
        print(f"{self.name.upper()} says woof woof! I'm {self.age} years old!")

dog_1 = Dog("Jack", 3)
dog_2 = Dog("Thatcher", 5)

# Call the bark method
dog_1.bark()  # JACK says woof woof! I'm 3 years old!
dog_2.bark()  # THATCHER says woof woof! I'm 5 years old!
```

Como puedes ver, creamos dos objetos perro usando la clase `Dog`. Al inicializar `dog_1`, se pasan la cadena `Jack` y el número `3`, lo que establece los atributos `name` y `age` para esa instancia. Y `dog_2` se inicializa con la cadena `Thatcher` y el número `5` como su `name` y `age`, respectivamente.

Luego, cuando llames al método `.bark()` en `dog_1` y `dog_2`, podrás ver cómo difieren ambas salidas y usar los atributos únicos `name` y `age` que pasaste al crear cada objeto.

En resumen, la diferencia entre una clase y un objeto es que una clase es la plantilla o la estructura, y un objeto es lo que se crea usando esa plantilla.

Además, una clase define qué datos y comportamiento debe tener el objeto, y un objeto contiene los datos reales y usa ese comportamiento. Escribes una clase una vez, y puedes crear muchos objetos a partir de ella, cada uno con datos diferentes.


# --questions--

## --text--

¿Cuál es la salida de este código?

```python
class Dog:  
    def __init__(self, name):  
        self.name = name

    def bark(self):  
        print(f"{self.name} says Woof!")  

my_dog = Dog("Rex")
print(my_dog.name)
```

## --answers--

`Rex says Woof!`

### --feedback--

Mira qué se está imprimiendo: ¿está llamando a un método o accediendo a un atributo?

---

`name`

### --feedback--

Mira qué se está imprimiendo: ¿está llamando a un método o accediendo a un atributo?

---

`Rex`

---

Error

### --feedback--

Mira qué se está imprimiendo: ¿está llamando a un método o accediendo a un atributo?

## --video-solution--

3

## --text--

¿Cuál es el método especial que se llama automáticamente cuando se crea un nuevo objeto?

## --answers--

`__create_object__`

### --feedback--

Piensa en qué inicializa los atributos que tendrá un objeto.

---

`__init__`

---

`__new__`

### --feedback--

Piensa en qué inicializa los atributos que tendrá un objeto.

---

`__setup__`

### --feedback--

Piensa en qué inicializa los atributos que tendrá un objeto.

## --video-solution--

2

## --text--

¿Cuál es la estructura o plantilla para crear objetos?

## --answers--

Una variable

### --feedback--

Piensa en lo que define la estructura y el comportamiento de los objetos creados a partir de ella.

---

Una función

### --feedback--

Piensa en lo que define la estructura y el comportamiento de los objetos creados a partir de ella.

---

Una clase

---

Un bucle

### --feedback--

Piensa en lo que define la estructura y el comportamiento de los objetos creados a partir de ella.

## --video-solution--

3

