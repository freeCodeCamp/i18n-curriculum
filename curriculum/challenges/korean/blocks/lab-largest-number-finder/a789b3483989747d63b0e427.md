---
id: a789b3483989747d63b0e427
title: 가장 큰 수 찾기 함수 만들기
challengeType: 26
dashedName: build-the-largest-number-finder
---

# --description--

이 실습에서는 각 하위 배열에서 가장 큰 수를 포함하는 배열을 반환하는 함수를 만드세요.

배열을 간단한 `for` 반복문으로 순차적으로 반복하고 배열 구문 `arr[i]`으로 각 요소에 접근할 수 있다는 점을 기억하세요.

**목표:** 아래 사용자 스토리를 충족하고 모든 테스트를 통과하여 실습을 완료하세요.

**사용자 스토리:**

1. 배열들의 배열을 인수로 받는 `largestOfAll` 함수를 만들어야 합니다.
2. 함수는 각 하위 배열에서 가장 큰 수를 포함하는 배열을 반환해야 합니다.

# --hints--

`largestOfAll`라는 이름의 함수를 만들어야 합니다.

```js
assert.isFunction(largestOfAll);
```

`largestOfAll`는 하나의 매개변수를 받아야 합니다.

```js
assert.lengthOf(largestOfAll, 1);
```

`largestOfAll([[4, 5, 1, 3], [13, 27, 18, 26], [32, 35, 37, 39], [1000, 1001, 857, 1]])`는 배열을 반환해야 합니다.

```js
assert.isArray(
  largestOfAll([
    [4, 5, 1, 3],
    [13, 27, 18, 26],
    [32, 35, 37, 39],
    [1000, 1001, 857, 1]
  ])
);
```

`largestOfAll([[13, 27, 18, 26], [4, 5, 1, 3], [32, 35, 37, 39], [1000, 1001, 857, 1]])`는 `[27, 5, 39, 1001]`를 반환해야 합니다.

```js
assert.deepEqual(
  largestOfAll([
    [13, 27, 18, 26],
    [4, 5, 1, 3],
    [32, 35, 37, 39],
    [1000, 1001, 857, 1]
  ]),
  [27, 5, 39, 1001]
);
```

`largestOfAll([[4, 9, 1, 3], [13, 35, 18, 26], [32, 35, 97, 39], [1000000, 1001, 857, 1]])`는 `[9, 35, 97, 1000000]`를 반환해야 합니다.

```js
assert.deepEqual(
  largestOfAll([
    [4, 9, 1, 3],
    [13, 35, 18, 26],
    [32, 35, 97, 39],
    [1000000, 1001, 857, 1]
  ]),
  [9, 35, 97, 1000000]
);
```

`largestOfAll([[17, 23, 25, 12], [25, 7, 34, 48], [4, -10, 18, 21], [-72, -3, -17, -10]])`는 `[25, 48, 21, -3]`를 반환해야 합니다.

```js
assert.deepEqual(
  largestOfAll([
    [17, 23, 25, 12],
    [25, 7, 34, 48],
    [4, -10, 18, 21],
    [-72, -3, -17, -10]
  ]),
  [25, 48, 21, -3]
);
```

# --seed--

## --seed-contents--

```js

```

# --solutions--

```js
function largestOfAll(arr) {
  return arr.map(subArr => Math.max.apply(null, subArr));
}

largestOfAll([
  [4, 5, 1, 3],
  [13, 27, 18, 26],
  [32, 35, 37, 39],
  [1000, 1001, 857, 1]
]);
```
