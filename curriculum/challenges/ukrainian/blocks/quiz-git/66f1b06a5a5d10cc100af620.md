---
id: 66f1b06a5a5d10cc100af620
title: Тест з Git
challengeType: 8
dashedName: quiz-git
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке `git`?

#### --distractors--

Популярний тип інструменту для доступності.

---

Загальновживаний лінтер у програмних проєктах.

---

Особливий тип бібліотеки JavaScript.

#### --answer--

Система контролю версій, яка дозволяє відстежувати та керувати змінами у вашому проєкті.

### --question--

#### --text--

Який із наведених є популярним хмарним провайдером системи контролю версій?

#### --distractors--

Spring Boot

---

Laravel

---

Remix

#### --answer--

GitHub

### --question--

#### --text--

Що робить команда `git config`?

#### --distractors--

Встановлює змінні конфігурації для ребейзу ваших змін.

---

Встановлює конфігурації для керування лише віддаленими репозиторіями на GitLab.

---

Встановлює конфігурації для вирішення конфліктів злиття.

#### --answer--

Встановлює змінні конфігурації, які відповідають за те, як Git працює на вашій машині.

### --question--

#### --text--

Яка команда показує поточну встановлену версію Git на вашій машині?

#### --distractors--

`git --versions`

---

`git --setVersion`

---

`git --set`

#### --answer--

`git --version`

### --question--

#### --text--

Який із наведених інструментів використовується для виконання специфічних завдань GitHub без виходу з командного рядка?

#### --distractors--

GitHub CI/CD

---

GitHub Actions

---

GitHub Pages

#### --answer--

GitHub CLI

### --question--

#### --text--

Яка з наведених функцій дозволяє автоматизувати робочі процеси безпосередньо у вашому репозиторії GitHub, включно зі збіркою, тестуванням і розгортанням коду?

#### --distractors--

GitHub Pipelines

---

GitHub Cloud

---

GitHub API

#### --answer--

GitHub Actions

### --question--

#### --text--

Яка з наведених команд ініціалізує порожній репозиторій `git`?

#### --distractors--

`git status`

---

`git set`

---

`git log`

#### --answer--

`git init`

### --question--

#### --text--

Що робить команда `git push`?

#### --distractors--

Ця команда використовується для завантаження всієї історії Git на GitHub.

---

Ця команда використовується для завантаження змін із віддаленого репозиторію на вашу локальну машину.

---

Ця команда використовується для завантаження вашого коду на GitHub і автоматичного злиття його у гілку `main`.

#### --answer--

Ця команда використовується для завантаження ваших локальних комітів у віддалений репозиторій.

### --question--

#### --text--

Яка з наведених команд використовується для клонування репозиторію?

#### --distractors--

`git cloning`

---

`git cloned`

---

`git clones`

#### --answer--

`git clone`

### --question--

#### --text--

Яка з наведених команд використовується для виведення списку всіх попередніх комітів із корисною інформацією, такою як автор, дата коміту, повідомлення коміту та хеш коміту?

#### --distractors--

`git rebase`

---

`git status`

---

`git commit`

#### --answer--

`git log`

### --question--

#### --text--

Яка з наведених команд виведе список усіх віддалених репозиторіїв, пов’язаних із вашим локальним репозиторієм Git?

#### --distractors--

`git rebase -v`

---

`git pull -v`

---

`git checkout -v`

#### --answer--

`git remote -v`

### --question--

#### --text--

Що робить команда `git reset`?

#### --distractors--

Ця команда дозволяє скинути всі спроби вирішення конфліктів злиття.

---

Ця команда дозволяє скинути всі спроби `git rebase`.

---

Ця команда дозволяє скинути всі коміти для репозиторію.

#### --answer--

Ця команда дозволяє скинути поточний стан гілки.

### --question--

#### --text--

Що таке запит на злиття?

#### --distractors--

Запит на об’єднання послідовності комітів з однієї гілки в іншу.

---

Запит на вирішення всіх конфліктів злиття.

---

Запит на видалення всіх комітів із репозиторію.

#### --answer--

Запит на внесення змін із вашої гілки у цільову гілку.

### --question--

#### --text--

Що представляє гілка `main` у проєкті?

#### --distractors--

Вона представляє основну гілку документації для застосунку.

---

Вона представляє гілку гарячих виправлень для застосунку.

---

Вона представляє основну гілку функцій для застосунку.

#### --answer--

Вона представляє основну або продуктивну гілку для застосунку.

### --question--

#### --text--

Для чого найкраще використовувати GitHub Pages?

#### --distractors--

Найкраще для розгортання ігор на C# та Unity.

---

Найкраще для розгортання застосунків лише на Python.

---

Найкраще для розгортання вебсайтів із великою кількістю бекенду.

#### --answer--

Найкраще для розгортання статичних сайтів.

### --question--

#### --text--

Яка з наведених команд використовується для показу поточного стану вашого робочого каталогу?

#### --distractors--

`git show`

---

`git check`

---

`git update`

#### --answer--

`git status`

### --question--

#### --text--

Яку команду ви використовуєте, щоб отримати зміни з віддаленого репозиторію?

#### --distractors--

`git get`

---

`git sync`

---

`git update`

#### --answer--

`git fetch`

### --question--

#### --text--

Що таке файл `.gitignore`?

#### --distractors--

Особливий тип файлу, який показує всю вашу історію `git`.

---

Особливий тип файлу, який показує різниці у ваших змінах.

---

Особливий тип файлу, який використовується для вирішення конфліктів у проєкті.

#### --answer--

Особливий тип файлу, який повідомляє `git` не відстежувати певні файли та папки у проєкті.

### --question--

#### --text--

Яка з наведених команд використовується для виведення списку всіх ваших локальних гілок?

#### --distractors--

`git show`

---

`git log`

---

`git list`

#### --answer--

`git branch`

### --question--

#### --text--

Яка з наведених команд використовується для переміщення або об’єднання послідовності комітів з однієї гілки в іншу?

#### --distractors--

`git remote`

---

`git merge`

---

`git switch`

#### --answer--

`git rebase`
