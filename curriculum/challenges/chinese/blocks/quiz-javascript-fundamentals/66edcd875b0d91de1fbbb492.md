---
id: 66edcd875b0d91de1fbbb492
title: JavaScript 基础测验
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

下面哪个会创建一个 String 对象？

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

你如何将一个 String 对象转换为字面？

#### --distractors--

使用 `String()` 构造函数。

---

使用 `.stringify()` 方法。

---

将 `String` 对象用双引号括起来。

#### --answer--

使用 `.toString()` 方法。

### --question--

#### --text--

为什么你可以在 JavaScript 的字符串原语上使用 `.length`、`.repeat()`、`.concat()` 和 `.slice()` 这样的方法？

#### --distractors--

这些方法本地存在于字串原语上。

---

字串原语在内部作为对象保存。

---

JavaScript 会临时将字串原语转换为数组。

#### --answer--

JavaScript 临时将字串原语包装在一个 String 对象中。

### --question--

#### --text--

以下 `code` 会在控制台输出什么 `Log`？

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

你如何将字面转换为 Number 对象？

#### --distractors--

使用 `Object()` 构造函数。

---

使用 `.toNumber()` 方法。

---

使用 `.parseInt()` 方法。

#### --answer--

使用 `Number()` 构造函数。

### --question--

#### --text--

`Number(true)` 和 `Number(false)` 分别会返回什么？

#### --distractors--

`undefined` 和 `undefined`

---

`NaN` 和 `NaN`

---

`true` 和 `false`

#### --answer--

`1` 和 `0`

### --question--

#### --text--

`Number(undefined)` 和 `Number(null)` 分别会返回什么？

#### --distractors--

`NaN` 和 `NaN`

---

`0` 和 `0`

---

`undefined` 和 `null`

#### --answer--

`NaN` 和 `0`

### --question--

#### --text--

以下哪个是检查用户是否被允许在你的网站上执行某操作的布尔变量的最佳名称？

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

以下哪个是当用户提交 `form` 时运行的函数的最佳名称？

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

什么是稀疏数组？

#### --distractors--

一个数组，其中一个或多个索引的值为 `null`。

---

一个数组，其中一个或多个索引的值为 `undefined`。

---

一个数组，其中一个或多个索引的值是空对象字面。

#### --answer--

一个数组，其中一个或多个索引未被赋值。

### --question--

#### --text--

以下哪个示例中 `arr` 不会是稀疏数组？

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

linter 的主要目的是什么？

#### --distractors--

自动格式化代码。

---

为了优化代码性能。

---

为代码创建文档。

#### --answer--

检测代码中的潜在误差和问题。

### --question--

#### --text--

在代码库中使用格式化工具的好处是什么？

#### --distractors--

它们允许开发者更高效地编译代码。

---

它们将代码转换为特定的编程语言。

---

它们检测逻辑错误。

#### --answer--

它们确保一致的代码风格。

### --question--

#### --text--

在 JavaScript 中，通常是谁或什么负责为变量分配内存？

#### --distractors--

开发者。

---

操作系统。

---

服务器。

#### --answer--

运行期引擎。

### --question--

#### --text--

以下代码将记录到控制台的内容是什么？

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

JavaScript 中的提升是什么？

#### --distractors--

将误差提升到代码顶部。

---

创建新建变量的进程。

---

自动将 `var` 声明移动到块的顶部。

#### --answer--

将变量声明移动到其作用域顶部的进程。

### --question--

#### --text--

为什么在现代 JavaScript 中使用 `var` 关键字被认为是不良实践？

#### --distractors--

它们比 `let` 和 `const` 慢。

---

它们会导致语法误差。

---

它们不允许类型检查。

#### --answer--

它们在块外可访问。

### --question--

#### --text--

JavaScript 中模块的目的是什么？

#### --distractors--

为了提升代码性能。

---

使代码更难理解。

---

创建新编程语言。

#### --answer--

将代码组织成可重用的单元。

### --question--

#### --text--

如何在超文本标记语言文档中将 `app.js` 脚本作为模块添加？

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

以下哪个示例从 `utils.js` 模块导出 `calculateSum` 函数，并在 `app.js` 文件中正确导入和使用它？

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
