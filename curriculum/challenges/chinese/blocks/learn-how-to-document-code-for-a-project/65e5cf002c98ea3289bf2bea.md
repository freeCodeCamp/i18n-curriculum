---
id: 65e5cf002c98ea3289bf2bea
title: 任务 14
challengeType: 19
dashedName: task-14
lang: en-US
---

# --description--

当以 `y` 结尾的名词变为复数时，`y` 通常会变成 `ies`。这个规则在技术领域中特别有用，因为某些术语经常以 `y` 结尾。

# --questions--

## --text--

以 `y` 结尾的名词复数形式如何构成？

## --answers--

通过将 `y` 改为 `ys`

### --feedback--

这不是通常的规则。一般来说，复数形式中 `y` 会变成 `ies`，比如 `query` 变成 `queries`。

---

通过添加 `s`

### --feedback--

对于大多数以 `y` 结尾的名词，简单地添加 `s` 是不正确的。例如，`directory` 变成 `directories`，而不是 `directorys`。

---

通过将 `y` 改为 `ies`

---

通过移除 `y`

### --feedback--

去掉 `y` 不是形成复数的正确方法。复数中 `y` 通常变成 `ies`，比如 `binary` 变成 `binaries`。

## --video-solution--

3
