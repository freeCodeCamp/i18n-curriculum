---
id: 69f42d414b7626e21a922794
title: エラーハンドリングミドルウェアとは何ですか？
challengeType: 19
dashedName: what-is-error-handling-middleware
---

# --description--

Expressのエラーハンドリングミドルウェアは、リクエストとレスポンスのサイクル中に発生したエラーを処理するための特別な種類のミドルウェアです。予期しないエラーをキャッチし、ログを記録し、クライアントに適切なレスポンスを返すために不可欠です。

前のレッスンのエラーハンドリングの項目で見たように、Expressのエラーハンドリングミドルウェアは4つのパラメータを持ちます：`err`、`req`、`res`、および`next`です。このミドルウェアは、エラーが`next()`関数に渡されたときに呼び出されます。

構文をもう一度おさらいしましょう：

```js
app.use((err, req, res, next) => {
  console.error(err.message);
  res.status(500).send("Internal Server Error");
});
```

そして、基本的なExpressアプリでの例は次の通りです：

```js
const express = require('express')
const app = express()

// A simple route that throws an error
app.get('/', (req, res) => {
  throw new Error('Something went wrong!')
})

// Error-handling middleware
app.use((err, req, res, next) => {
  console.error(err.stack); // Logs the error stack
  res.status(500).send('Something went wrong!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* ルートハンドラ：問題をシミュレートするために意図的にエラーをスローします。
* エラーハンドリングミドルウェア：`app.use()`で登録されています。`next(err)`を介して渡されたエラーをキャッチし、クライアントに500のHTTPレスポンスと一般的なエラーメッセージを送信します。
* `err`：ミドルウェアに渡されるエラーオブジェクトです。

エラーハンドリングミドルウェアは、アプリケーションで何か問題が起きたときでも、ユーザーに適切なレスポンスが返されるようにします。エラーをキャッチするために、必ずミドルウェアスタックの最後にこのミドルウェアを含めることが重要です。

# --questions--

## --text--

エラーハンドリングミドルウェアは何をしますか？

## --answers--

リクエストを処理してレスポンスを送信します。

### --feedback--

リクエスト中に何か問題が起きたときに何が起きるかに注目してください。

---

リクエストとレスポンスのサイクル中に発生したエラーを処理します。

---

静的ファイルを提供します。

### --feedback--

リクエスト中に何か問題が起きたときに何が起きるかに注目してください。

---

ルーティングのロジックを処理します。

### --feedback--

リクエスト中に何か問題が起きたときに何が起きるかに注目してください。

## --video-solution--

2

## --text--

エラーハンドリングミドルウェアに必要なパラメータはどれですか？

## --answers--

`err`、`req`、`res`、`next`。

---

`req` と `res`。

### --feedback--

Expressがエラーハンドラを認識するためのパラメータについて考えてみてください。

---

`err` と `next`。

### --feedback--

Expressがエラーハンドラを認識するためのパラメータについて考えてみてください。

---

`err` と `req`。

### --feedback--

Expressがエラーハンドラを認識するためのパラメータについて考えてみてください。

## --video-solution--

1

## --text--

エラーハンドリングミドルウェアはミドルウェアスタックのどこに配置すべきですか？

## --answers--

他のミドルウェアの前、スタックの上部に。

### --feedback--

失敗する可能性のあるコードでエラーハンドラがどこに置かれるか考えてみてください。

---

スタックの中間に。

### --feedback--

失敗する可能性のあるコードでエラーハンドラがどこに置かれるか考えてみてください。

---

他のミドルウェアの後、スタックの下部に。

---

特に決まった場所はありません。

### --feedback--

失敗する可能性のあるコードでエラーハンドラがどこに置かれるか考えてみてください。

## --video-solution--

3
