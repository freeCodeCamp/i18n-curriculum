---
id: 66ed8ff4f45ce3ece4053eb4
title: CSS 版面配置與效果測驗
challengeType: 8
dashedName: quiz-css-layout-and-effects
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

在 CSS 中，用什麼術語來描述元素的內容超出其 `container` 大小？

#### --distractors--

下限溢位

---

壓縮

---

內邊距

#### --answer--

上限溢位(相對於 underflow)

### --question--

#### --text--

當內容超出元素範圍時，CSS 的預設行為是什麼？

#### --distractors--

隱藏內容。

---

裁切內容。

---

調整內容大小。

#### --answer--

保持內容可見。

### --question--

#### --text--

為什麼 CSS 通常會避免在有 `overflow` 時隱藏內容？

#### --distractors--

為了維護原始的版面配置和設計。

---

為了防止瀏覽器中的效率問題。

---

為了允許更好的搜尋引擎優化。

#### --answer--

為確保所有內容皆可供使用者存取。

### --question--

#### --text--

你如何透過隱藏內容或顯示捲軸來控制 CSS 中的垂直上限溢位？

#### --distractors--

使用 `overflow-y: visible` 來隱藏內容，使用 `overflow-y: auto` 來始終顯示捲軸。

---

使用 `overflow-y: collapse` 來隱藏內容，使用 `overflow-y: expand` 來顯示捲軸。

---

使用 `overflow-y: none` 隱藏內容，使用 `overflow-y: show` 顯示捲軸。

#### --answer--

使用 `overflow-y: hidden` 來隱藏內容，使用 `overflow-y: scroll` 來始終顯示捲軸。

### --question--

#### --text--

你如何在 CSS 中指定不同的水平和垂直滾動行為？

#### --distractors--

透過使用 `scroll-x` 和 `scroll-y` 屬性。

---

透過為 `overflow-x` 和 `overflow-y` 指定相同的值。

---

透過為 `overflow` 屬性指定兩個值，其中第一個值套用到 `overflow-y`，第二個值套用到 `overflow-x`。

#### --answer--

透過為 `overflow` 屬性指定兩個值，其中第一個值套用到 `overflow-x`，第二個值套用到 `overflow-y`。

### --question--

#### --text--

`transform` CSS 屬性允許你做什麼？

#### --distractors--

變更元素的顏色和字型樣式。

---

調整元素的邊距和內距。

---

修改元素的可見性和顯示方式。

#### --answer--

旋轉、縮放、傾斜和平移一個元素。

### --question--

#### --text--

`transform: translate(15px, 200px)` 屬性如何修改元素？

#### --distractors--

它將元素從原始位置向左移動 15 像素，向下移動 200 像素。

---

它將元素順時針旋轉 15 度。

---

它將元素從原始位置向右移動 15 像素，向上移動 200 像素。

#### --answer--

它將元素從原始位置向右移動 15 像素，向下移動 200 像素。

### --question--

#### --text--

`overflow-x` 和 `overflow-y` 這兩個 CSS 屬性的目的為何？

#### --distractors--

它們調整元素的內距和外距。

---

它們設定元素的背景顏色和字型樣式。

---

它們定義元素的可見性和顯示屬性。

#### --answer--

它們控制元素內容的水平和垂直上限溢位。

### --question--

#### --text--

CSS 中 `margin` 屬性的用途是什麼？

#### --distractors--

它定義了內容與元素邊框之間的空間。

---

它定義元素的邊框與內距之間的空間。

---

它定義元素的寬度和高度。

#### --answer--

它定義元素周圍、其邊框外的空間。

### --question--

#### --text--

在 CSS 中，元素的內容區域表述什麼？

#### --distractors--

它定義了元素周圍的邊距和內距。

---

它定義元素邊框的厚度。

---

它控制頁面上元素的可見性。

#### --answer--

它包含元素的實際內容，例如文字、影像、圖片和影片、視訊。

### --question--

#### --text--

`content-box` 和 `border-box` 在 CSS 中有什麼差別？

#### --distractors--

`content-box` 包含內距和邊框，而 `border-box` 僅包含內容尺寸。

---

`content-box` 和 `border-box` 都只包含邊框尺寸。

---

`content-box` 用於 2D 元素，而 `border-box` 用於 3D 元素。

#### --answer--

`content-box` 僅包含內容尺寸，而 `border-box` 則包含內容、內距和邊框的尺寸。

### --question--

#### --text--

CSS 重置的目的為何？

#### --distractors--

為所有元素套用預設樣式以加快載入速度。

---

透過移除未使用的樣式來最小化 CSS 檔案的大小。

---

為了最佳化影像和字型以提升效率。

#### --answer--

為了移除預設瀏覽器樣式並確保跨瀏覽器的一致樣式。

### --question--

#### --text--

以下哪一項是常見的 CSS 重置範例？

#### --distractors--

bootstrap.css

---

undo.css

---

restore.css

#### --answer--

normalize.css

### --question--

#### --text--

哪一個 CSS 屬性用於對元素套用旋轉、縮放和平移等變化？

#### --distractors--

`box-shadow`

---

`opacity`

---

`z-index`

#### --answer--

`transform`

### --question--

#### --text--

當 `box-sizing` 屬性使用 `border-box` 值時，CSS 中元素的總寬度如何計算？

#### --distractors--

總寬度＝內容寬度＋邊框＋外距

---

總寬度＝內容寬度＋內距＋邊框＋外距

---

總寬度 = 內容寬度

#### --answer--

總寬度＝內容寬度＋內距＋邊框

### --question--

#### --text--

CSS 中 `filter` 屬性的用途是什麼？

#### --distractors--

變更元素的縮放和旋轉。

---

變更元素的背景顏色和紋理。

---

更改元素的大小和位置。

#### --answer--

透過使用各種圖形效果來修改元素的視覺外觀。

### --question--

#### --text--

CSS 屬性 `filter: grayscale(50%);` 對元素有什麼作用？

#### --distractors--

它將元素的亮度提高 50%。

---

它將元素的對比度降低 50%。

---

它會對該元素套用棕褐色效果。

#### --answer--

它對元素套用 50% 的灰階效果。

### --question--

#### --text--

`contrast()` 函式（程式）在 CSS 中的用途是什麼？

#### --distractors--

它會調整影像的亮度，其中 `0%` 會使其變黑，`100%` 會使其變白。

---

它會調整影像的對比度，其中 `100%` 會使其變成灰階，`0%` 則沒有影響，超過 `100%` 的數值會增加對比度。

---

它會調整影像的不透明度，其中 `0%` 使其透通，`100%` 使其不透明。

#### --answer--

它會調整影像的對比度，其中 `0%` 會使其變成灰色，`100%` 則不會有任何效果，超過 `100%` 的數值會增加對比度。

### --question--

#### --text--

`hue-rotate()` 函式（程式）在 CSS 中的用途是什麼？

#### --distractors--

它會改變影像的亮度。

---

它會將影像順時針旋轉指定的角度。

---

它會調整影像的飽和度和對比度。

#### --answer--

它會套用色彩偏移，其中 `angle` 值定義繞色彩圓旋轉的度數。

### --question--

#### --text--

什麼是 CSS 中的 margin collapsing？

#### --distractors--

當元素的邊距設定為負數且這導致版面配置出現問題時。

---

當元素的外距被內距減少，且這改變了元素的尺寸時。

---

當瀏覽器忽略元素的外邊距時，因此它不可見。

#### --answer--

當相鄰元素的垂直邊距重疊時，會合併成單一邊距。

## --quiz--

### --question--

#### --text--

以下關於 `translate()` 函式（程式）哪一項是正確的？

#### --distractors--

它用於縮放元素的大小。

---

它用於翻譯元素的文字。

---

它用於改變元素的形狀或大小。

#### --answer--

它用於將元素從其當前位置移動。

### --question--

#### --text--

以下哪一項是 CSS 盒模型的組件？

#### --distractors--

寬度

---

高度

---

大小

#### --answer--

內邊距

### --question--

#### --text--

當元素的 `box-sizing` 設定為 `content-box` 時，`width` 和 `height` 適用於盒模型的哪個零件？

#### --distractors--

內容、內距、邊框和外距

---

內容和內距

---

內容、內距和邊框

#### --answer--

內容

### --question--

#### --text--

當元素的 `box-sizing` 設定為 `border-box` 時，`width` 和 `height` 不適用於 CSS 盒模型的哪些零件？

#### --distractors--

內容區域。

---

內邊距

---

邊框

#### --answer--

邊距

### --question--

#### --text--

`brightness()` 函式（程式）中哪個值會使元素完全變黑？

#### --distractors--

`100%`

---

`0px`

---

`100px`

#### --answer--

`0%`

### --question--

#### --text--

`hue-rotate()` 函式（程式）使用什麼類型的值？

#### --distractors--

像素

---

徑度

---

百分比

#### --answer--

度

### --question--

#### --text--

CSS 屬性 `filter: contrast(0%);` 對元素有什麼作用？

#### --distractors--

它會使該元素顯示為完全白色。

---

它會使該元素顯示為完全黑色。

---

它不會對該元素產生任何影響。

#### --answer--

它會使該元素完全呈現灰色。

### --question--

#### --text--

`brightness()` CSS 函式（程式）的基準值是什麼，從該值開始增加亮度？

#### --distractors--

`100px`

---

`50px`

---

`50%`

#### --answer--

`100%`

### --question--

#### --text--

`grayscale()` 函式使用什麼樣的值？

#### --distractors--

像素

---

度

---

徑度

#### --answer--

百分比

### --question--

#### --text--

`contrast()` 函式使用什麼樣的值？

#### --distractors--

像素

---

度

---

徑度

#### --answer--

百分比

### --question--

#### --text--

CSS 中的 `overflow-x` 屬性控制什麼？

#### --distractors--

它決定 y 軸上的水平上限溢位(相對於 underflow)。

---

它決定 y 軸上的垂直上限溢位(相對於 underflow)。

---

它決定 x 軸上的垂直上限溢位(相對於 underflow)。

#### --answer--

它決定 x 軸上的水平上限溢位(相對於 underflow)。

### --question--

#### --text--

CSS 中的 `overflow-y` 屬性控制什麼？

#### --distractors--

它決定 x 軸上的水平上限溢位(相對於 underflow)。

---

它決定 x 軸上的垂直上限溢位(相對於 underflow)。

---

它決定 y 軸上的水平上限溢位(相對於 underflow)。

#### --answer--

它決定 y 軸上的垂直上限溢位(相對於 underflow)。

### --question--

#### --text--

CSS 重置的第三方選項範例是什麼？

#### --distractors--

clean.css

---

restore.css

---

reset.css

#### --answer--

sanitize.css

### --question--

#### --text--

CSS 中的 `overflow` 屬性有多少個維度？

#### --distractors--

一

---

三

---

四

#### --answer--

二

### --question--

#### --text--

如果縮寫屬性 `overflow` 給定兩個值，哪個屬性會使用第一個值？

#### --distractors--

`overflow-y`

---

`overflow-w`

---

`overflow-z`

#### --answer--

`overflow-x`

### --question--

#### --text--

`blur()` 函式在 CSS 中對元素應用什麼樣的模糊效果？

#### --distractors--

Giliani

---

Galilei

---

伽利略

#### --answer--

高斯

### --question--

#### --text--

`sepia()` 函式使用什麼樣的值？

#### --distractors--

像素

---

徑度

---

度

#### --answer--

百分比

### --question--

#### --text--

如果簡寫屬性 `overflow` 給定兩個值，哪個屬性會使用第二個值？

#### --distractors--

`overflow-w`

---

`overflow-x`

---

`overflow-z`

#### --answer--

`overflow-y`

### --question--

#### --text--

CSS 重置的作用是什麼？

#### --distractors--

它強制瀏覽器預設使用深色模式。

---

它會從 HTML 元素中移除行內樣式。

---

它會停用包含 CSS 的瀏覽器擴充套件。

#### --answer--

它會移除瀏覽器的預設樣式。

### --question--

#### --text--

CSS 屬性 `filter: grayscale(100%);` 對元素有什麼作用？

#### --distractors--

它不會對該元素產生任何影響。

---

它會使該元素顯示為完全黑色。

---

它會使該元素顯示為完全白色。

#### --answer--

它在維護元素的色調結構的同時移除色彩飽和度。

