---
id: 672d45651d83b450801efb3a
title: VS Code에서 프로젝트를 만들고 코드를 로컬에서 실행하는 방법
challengeType: 19
dashedName: how-to-create-a-project-and-run-your-code-locally-in-vs-code
---

# --description--

시작하려면 VS Code가 프로젝트를 "워크스페이스"로 간주한다는 점을 알아야 합니다. 워크스페이스는 VS Code에서 연 디렉터리입니다.

먼저, 명령줄 인터페이스를 사용해 새 프로젝트용 새 디렉터리를 만들어야 합니다. 디렉터리는 폴더의 다른 이름입니다.

명령줄 인터페이스, 줄여서 CLI는 텍스트 기반 명령어로 운영체제와 상호작용할 수 있게 해줍니다. 앞으로 수업에서 이 도구들에 대해 더 배우게 됩니다.

Windows를 사용한다면 명령 프롬프트나 PowerShell을 사용할 수 있습니다. Mac이라면 터미널 앱을 열면 됩니다.

명령줄이 열리면 `cd ~` 명령어를 입력하고 키보드에서 `enter`를 눌러 홈 디렉터리로 이동하세요. 그런 다음 `mkdir my-project` 명령어를 입력하고 `enter`를 누르세요. `mkdir`, 즉 "make directory"는 새 디렉터리를 만드는 명령어입니다. 그런 다음 VS Code로 새 디렉터리를 열어야 합니다.

새 디렉터리를 만드는 다른 그래픽 방식도 있습니다.

운영체제에 따라 파일 탐색기에서 바로 VS Code를 열 수 있는 컨텍스트 메뉴(또는 오른쪽 클릭 옵션)가 있을 수 있습니다. VS Code 애플리케이션을 더블클릭해 열 수도 있습니다. 명령줄에 익숙하다면 `code /path/to/folder` 명령어를 실행할 수도 있습니다.

열리면 새 빈 워크스페이스가 보일 것입니다.

새 파일을 만들려면 좌측 상단의 `File` 메뉴를 클릭하고 `New File...`를 선택하세요. 그런 다음 파일 이름을 `index.html`로 지정하고 `Save`를 클릭하세요.

이제 탐색기 탭에서 새 파일이 보일 것입니다. `index.html` 파일에 HTML을 추가해 보세요.

이제 HTML 파일을 브라우저에서 직접 열고 싶을 수 있습니다. 그렇게 할 수도 있지만, CSS가 제대로 로드되지 않는 등 예상치 못한 여러 버그가 발생할 수 있습니다.

대신 HTML 파일을 적절한 웹 서버 뒤에 두어야 합니다. 코딩 초보자에게는 겁나는 작업처럼 들릴 수 있지만, VS Code에서 바로 할 수 있는 방법이 있습니다.

먼저 마켓플레이스에서 무료로 제공되는 _Live Server_ 확장 기능을 설치해야 합니다.

왼쪽 모서리의 확장 탭을 클릭하고 검색창에 `Live Server`를 입력하세요.

설치가 완료되면 상태 표시줄에 `Go live` 버튼이 보일 것입니다. HTML 파일을 연 상태에서 클릭하세요.

새 페이지가 브라우저의 새 탭에서 자동으로 열릴 것입니다. 열리지 않는다면 `http://localhost:5500/`로 직접 이동할 수 있습니다.

축하합니다! 이제 VS Code에서 자신만의 프로젝트를 설정했고 라이브 서버로 변경 사항을 실시간으로 확인할 수 있습니다.

# --questions--

## --text--

Visual Studio Code가 "워크스페이스"로 간주하는 것은 무엇인가요?

## --answers--

하드 드라이브 전체입니다.

### --feedback--

수업에서는 VS Code가 디렉터리와 관련해 워크스페이스를 정의하는 방식을 언급합니다.

---

VS Code를 연 디렉터리입니다.

---

VS Code에서 열린 모든 파일입니다.

### --feedback--

수업에서는 VS Code가 디렉터리와 관련해 워크스페이스를 정의하는 방식을 언급합니다.

---

VS Code 애플리케이션 폴더입니다.

### --feedback--

수업에서는 VS Code가 디렉터리와 관련해 워크스페이스를 정의하는 방식을 언급합니다.

## --video-solution--

2

## --text--

왜 수업에서는 HTML 파일을 브라우저에서 직접 여는 대신 웹 서버를 사용할 것을 권장하나요?

## --answers--

더 빠르게 로드되고 코드베이스의 문제를 더 쉽게 찾을 수 있기 때문입니다.

### --feedback--

수업에서는 HTML 파일을 직접 열 때 발생할 수 있는 특정 문제를 언급합니다.

---

대규모 소프트웨어 애플리케이션에 더 나은 보안을 제공하기 때문입니다.

### --feedback--

수업에서는 HTML 파일을 직접 열 때 발생할 수 있는 특정 문제를 언급합니다.

---

CSS가 제대로 로드되지 않는 등 예상치 못한 버그를 방지할 수 있기 때문입니다.

---

팀 내 다른 개발자와 실시간 협업이 가능하기 때문입니다.

### --feedback--

수업에서는 HTML 파일을 직접 열 때 발생할 수 있는 특정 문제를 언급합니다.

## --video-solution--

3

## --text--

로컬 웹 서버를 쉽게 설정하기 위해 권장하는 VS Code 확장 기능은 무엇인가요?

## --answers--

Web Server.

### --feedback--

수업에서는 HTML 파일을 서빙하기 위해 마켓플레이스에서 확장 기능을 설치하는 것을 구체적으로 언급합니다.

---

Live Server.

---

Local Host.

### --feedback--

수업에서는 HTML 파일을 서빙하기 위해 마켓플레이스에서 확장 기능을 설치하는 것을 구체적으로 언급합니다.

---

HTML Preview.

### --feedback--

수업에서는 HTML 파일을 서빙하기 위해 마켓플레이스에서 확장 기능을 설치하는 것을 구체적으로 언급합니다.

## --video-solution--

2
