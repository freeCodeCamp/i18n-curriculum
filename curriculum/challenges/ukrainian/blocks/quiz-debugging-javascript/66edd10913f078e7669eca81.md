---
id: 66edd10913f078e7669eca81
title: Тест на налагодження JavaScript
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який тип помилки виникає, коли змінна використовується до того, як її оголосили?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

Яку JavaScript інструкцію можна використати, щоб навмисно призупинити виконання коду для налагодження?

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

Який метод консолі можна використати для відображення даних у вигляді таблиці?

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

Як можна переглянути мережеві запити у інструментах розробника Chrome?

#### --distractors--

Використовуючи вкладку "Network Links".

---

Використовуючи вкладку "Console".

---

Використовуючи вкладку "Sources".

#### --answer--

Використовуючи вкладку "Network".

### --question--

#### --text--

Який правильний синтаксис для використання `try...catch` у JavaScript?

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

Який із наведених прикладів є прикладом `SyntaxError`?

#### --distractors--

Використання змінної до її оголошення.

---

Використання змінної двічі в одній області видимості.

---

Використання змінної, яка знаходиться поза областю видимості.

#### --answer--

Відсутня кома між елементами масиву.

### --question--

#### --text--

Яка мета точок зупинки у налагодженні JavaScript?

#### --distractors--

Автоматично виправляти помилки коду.

---

Запобігати виконанню коду.

---

Виводити повідомлення у консоль.

#### --answer--

Призупинити виконання у певній точці.

### --question--

#### --text--

Що відбувається, коли виконується інструкція `throw`?

#### --distractors--

Програма негайно завершується, ігноруючи весь залишковий код.

---

Інструкція throw виводить помилку у консоль, але не зупиняє виконання.

---

Вона призупиняє виконання коду, доки не буде відновлено шляхом відключення точок зупинки.

#### --answer--

Викидається виняток і виконується блок `catch` (якщо він є).

### --question--

#### --text--

Який метод ви б використали для перегляду змінних або виразів під час налагодження в інструментах розробника?

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

Який тип помилки виникне при виконанні наступного коду?

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

Помилка переповнення стека

#### --answer--

`ReferenceError`

### --question--

#### --text--

Який інструмент зазвичай використовується для покрокового проходження коду під час налагодження?

#### --distractors--

`console.log()`

---

`alert()`

---

Інструкція `throw`.

#### --answer--

Точки зупинки в інструментах розробника.

### --question--

#### --text--

Який тип помилки виникає при спробі отримати доступ до властивості `undefined`?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

Що робить блок `finally` в інструкції `try...catch`?

#### --distractors--

Виконується лише якщо сталася помилка.

---

Пропускає виконання, якщо спрацьовує `catch`.

---

Негайно виходить із блоку коду.

#### --answer--

Завжди виконується, незалежно від помилок.

### --question--

#### --text--

Яким буде результат виконання наступного коду?

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

Код викине `SyntaxError`.

---

Код виведе у консоль порожній масив.

---

Код викине помилку нестачі пам’яті.

#### --answer--

Код викине `RangeError`.

### --question--

#### --text--

Яке твердження є правдивим про інструкцію `debugger` у JavaScript?

#### --distractors--

Інструкція `debugger` завжди ігнорується браузером.

---

Інструкція `debugger` надсилає звіт про помилку на сервер.

---

Інструкція `debugger` завжди перезавантажує сторінку і очищує консоль.

#### --answer--

Інструкція `debugger` ігнорується, коли інструменти розробника не відкриті.

### --question--

#### --text--

Яка основна мета "профілювання" у JavaScript?

#### --distractors--

Писати юніт-тести для JavaScript функцій і створювати профілі виконання коду.

---

Призупиняти виконання коду і звільняти захоплені ресурси, такі як пам’ять і центральний процесор.

---

Перетворювати JavaScript код у машинний код для швидшого виконання.

#### --answer--

Виявляти вузькі місця продуктивності, записуючи використання центрального процесора, виклики функцій і час виконання.

### --question--

#### --text--

Що таке "watcher" у контексті налагодження?

#### --distractors--

Інструмент для призупинення виконання коду.

---

Вбудована функція для логування помилок.

---

JavaScript метод для обробки винятків.

#### --answer--

Інструмент для моніторингу значення змінних.

### --question--

#### --text--

Коли слід використовувати `console.error()` замість `console.log()`?

#### --distractors--

Щоб відобразити дані у вигляді таблиці.

---

Щоб логувати повідомлення лише під час налагодження.

---

Щоб логувати інформацію про браузер.

#### --answer--

Щоб логувати повідомлення про помилки.

### --question--

#### --text--

Що робить конструктор `Error()` у JavaScript?

#### --distractors--

Логує повідомлення про помилки у консоль.

---

Запускає код у пісочниці.

---

Завершує програму.

#### --answer--

Створює новий об’єкт помилки, який можна викинути.

### --question--

#### --text--

Яка мета методу `console.dir()` у JavaScript?

#### --distractors--

Створити окрему директорію для лог-файлів для кожного типу помилки.

---

Згенерувати візуальне представлення вихідних даних консолі і зберегти його у pdf-файл.

---

Зупинити виконання програми і показати повідомлення про помилку, пов’язану з пам’яттю та центральним процесором.

#### --answer--

Вивести ієрархічний, інтерактивний список властивостей для заданого JavaScript об’єкта.
