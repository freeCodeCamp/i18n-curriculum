---
id: 6887550fe36f64e27074475a
title: Passo 18
challengeType: 20
dashedName: step-18
---

# --description--

Peça para Alice enviar um email para Bob para verificar se a entrega do email funciona corretamente.

Use o método `send_email` de Alice para enviar um email para Bob com o assunto `"Hello"` e o corpo `"Hi Bob, how are you?"`.

# --hints--

Você deve chamar `alice.send_email()` com os parâmetros corretos.

```js
({
    test: () => {
        assert(runPython(`_Node(_code).has_call('alice.send_email(bob, "Hello", "Hi Bob, how are you?")')`));
    }
})
```

# --seed--

## --seed-contents--

```py
class Email:
    def __init__(self, sender, receiver, subject, body):
        self.sender = sender
        self.receiver = receiver
        self.subject = subject
        self.body = body
        self.read = False

    def mark_as_read(self):
        self.read = True

class User:
    def __init__(self, name):
        self.name = name
        self.inbox = Inbox()

    def send_email(self, receiver, subject, body):
        email = Email(sender=self, receiver=receiver, subject=subject, body=body)
        receiver.inbox.receive_email(email)

class Inbox:
    def __init__(self):
        self.emails = []

    def receive_email(self, email):
        self.emails.append(email)

--fcc-editable-region--
alice = User("Alice")
bob = User("Bob")

--fcc-editable-region--
```
