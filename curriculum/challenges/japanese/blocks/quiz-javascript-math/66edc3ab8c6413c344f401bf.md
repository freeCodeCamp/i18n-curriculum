---
id: 66edc3ab8c6413c344f401bf
title: JavaScript Math クイズ
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

`NaN` は何を表していますか？

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

次のうち、JavaScriptの算術演算子ではないものはどれですか？

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

JavaScriptでゼロで割ろうとするとどうなりますか？

#### --distractors--

JavaScriptは0を出力します。

---

何も起こりません。

---

JavaScriptはエラーをスローします。

#### --answer--

JavaScriptは`Infinity`を出力します。

### --question--

#### --text--

次のうち、べき乗演算子の正しい使い方はどれですか？

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

JavaScriptで変数をデクリメントするときに使うべき演算子はどれですか？

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

次のうち、ビット演算子ではないものはどれですか？

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

余りを取得するための正しい複合演算子はどれですか？

#### --distractors--

`//=`

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

`==` 演算子と `===` 演算子の違いは何ですか？

#### --distractors--

違いはありません。比較結果は常に同じになります。

---

`==` 演算子はデータ型が同じかどうかをチェックし、`===` は値が等しいかどうかをチェックします。

---

`==` 演算子はプリミティブ型にのみ使え、`===` 演算子はオブジェクトにのみ使えます。

#### --answer--

`==` 演算子は値だけを比較し、`===` 演算子は値と型の両方を比較します。

### --question--

#### --text--

単項プラス演算子（`+`）は何に使われますか？

#### --distractors--

値を1減らすために使われます。

---

演算の余りを取得するために使われます。

---

数値の2進表現を反転させるために使われます。

#### --answer--

オペランドを数値に変換するために使われます。

### --question--

#### --text--

次の`Math` メソッドのうち、値を最も近い整数に丸めるものはどれですか？

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

`Math.sqrt()` メソッドは何をしますか？

#### --distractors--

このメソッドはランダムな浮動小数点数を返します。

---

このメソッドは数値の絶対値を返します。

---

このメソッドは数値の3乗を返します。

#### --answer--

このメソッドは数値の平方根を返します。

### --question--

#### --text--

次のうち、`Math` オブジェクトの有効なメソッドではないものはどれですか？

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

次のうち、`false` を返すものはどれですか？

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

`parseFloat()` メソッドは何をしますか？

#### --distractors--

文字列の引数をパースしてビット演算を行います。

---

文字列の引数をパースして `false` を返します。

---

文字列の引数をパースして整数を返します。

#### --answer--

文字列の引数をパースして浮動小数点数を返します。

### --question--

#### --text--

コンソールに何が表示されますか？

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

次のコードの出力は何になりますか？

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

演算子の優先順位とは何ですか？

#### --distractors--

演算子の優先順位は左の値が右の値より小さいかどうかをチェックするために使われます。

---

演算子の優先順位は変数を指定された数のべき乗にして、その結果を変数に再代入します。

---

演算子の優先順位は値を1増やすために使われます。

#### --answer--

演算子の優先順位は式の中で演算が評価される順序を決定します。

### --question--

#### --text--

次のうち、比較演算子ではないものはどれですか？

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

単項否定演算子（`-`）は何をしますか？

#### --distractors--

オペランドのブール値を反転させます。

---

値を1減らします。

---

値を1増やします。

#### --answer--

オペランドを否定します。

### --question--

#### --text--

次のうち、null合体演算子を使った例はどれですか？

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```
