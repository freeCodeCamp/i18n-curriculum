---
id: 673407e02bcf0d682b9a49a9
title: JavaScript 引擎如何工作，以及什么是 JavaScript 运行期？
challengeType: 19
dashedName: how-does-the-javascript-engine-work-and-what-is-a-javascript-runtime
---

# --description--

JavaScript 引擎具有读取、理解和执行你的代码的能力。它就像一个转换器，将你的代码转换成计算机能够理解并据此工作的指令。 

最著名的 JavaScript 引擎之一是由谷歌开发的 `V8`，用于 `Chrome` 和 `Node.js`。JavaScript 引擎的工作分为几个步骤。首先，它解析你的代码，逐行读取以确保 JavaScript 代码没有错误。然后，它将这段代码转换成字节码，这是一种更简单的、中间版本的代码，更容易被计算机理解和执行。最后，它运行这段字节码以执行你的程序指令。下面是一个 JavaScript 代码示例：

```js
const greeting = "Hello, World!";
console.log(greeting);
```

当你运行这段代码时，JavaScript 引擎首先会解析它以查看是否有任何语法误差。解析意味着引擎读取代码并将其分解成它能理解的结构，同时检查过程中是否有错误。 

然后，它将代码编译成过渡形式（通常是字节码或机器码，取决于引擎）。编译是将人类可读的代码转换为计算机可以更快执行的更高效形式的过程。 

最后，引擎执行代码，将 `Hello, World!` 打印到控制台。

现在，让我们谈谈 JavaScript 运行期。JavaScript 运行期是执行你的 JavaScript 代码的环境。它包括 JavaScript 引擎（例如 Chrome 中的 V8 或 Firefox 中的 SpiderMonkey），负责处理和执行代码，以及环境提供的额外特色（例如网页浏览器或 Node.js，你将在后续课程中详细了解）。

虽然核心 JavaScript 语言处理诸如变量、循环和函数等内容，但运行期提供了额外的工具，使 JavaScript 能够与语言本身之外的事物交互，例如 DOM（用于网页）或 Fetch API（用于发起网络请求）。

简而言之，运行期使 JavaScript 不仅能完成基本的编程任务，还能通过提供语言本身之外的这些额外特色来与网页交互或处理基于时间的操作。

虽然你不需要了解引擎和运行期的每个细节来编写 JavaScript，但具备基本的理解可以帮助你编写更高效的代码并更有效地调试错误。

# --questions--

## --text--

JavaScript 引擎的主要函数是什么？

## --answers--

提供一个用于编写代码的用户界面。

### --feedback--

考虑你的代码需要发生什么才能真正运行。

---

连接到互联网。

### --feedback--

考虑你的代码需要发生什么才能真正运行。

---

读取、解释和执行 JavaScript 代码。

---

将数据保存到数据库中。

### --feedback--

考虑你的代码需要发生什么才能真正运行。

## --video-solution--

3

## --text--

以下哪项通常不被视为 JavaScript 运行期的一部分？

## --answers--

JavaScript 引擎。

### --feedback--

考虑你提供的内容与环境提供的内容。

---

Web API（在浏览器环境中）。

### --feedback--

考虑你提供的内容与环境提供的内容。

---

事件循环。

### --feedback--

考虑你提供的内容与环境提供的内容。

---

你编写的 JavaScript 代码。

## --video-solution--

4

## --text--

以下哪项最能描述 JavaScript 引擎与其运行期之间的区别？

## --answers--

引擎提供 Fetch API 和 DOM 等特色；运行期仅解析并执行代码。

### --feedback--

考虑 JavaScript 引擎和运行期环境的不同角色。

---

引擎解析并执行代码；运行期提供引擎以及额外的特色，如 Fetch API 和 DOM。

---

运行期将代码编译成字节码和机器码；引擎只是解释代码。

### --feedback--

考虑 JavaScript 引擎和运行期环境的不同角色。

---

它们是同一回事——engine 和 runtime 是可互换的术语。

### --feedback--

考虑 JavaScript 引擎和运行期环境的不同角色。

## --video-solution--

2
