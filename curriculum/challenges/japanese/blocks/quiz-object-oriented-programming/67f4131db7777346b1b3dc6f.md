---
id: 67f4131db7777346b1b3dc6f
title: オブジェクト指向プログラミング クイズ
challengeType: 8
dashedName: quiz-object-oriented-programming
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

オブジェクトを作成するための設計図として機能するものは何ですか？

#### --distractors--

Functions

---

Variables

---

Loops

#### --answer--

Classes

### --question--

#### --text--

Pythonが抽象クラスを実装するために使用するモジュールはどれですか？

#### --distractors--

`Time`

---

`xyz`

---

`Random`

#### --answer--

`abc`

### --question--

#### --text--

オブジェクトのデータと振る舞いを定義するものは何ですか？

#### --distractors--

Functions and classes.

---

Variables and loops.

---

Seeders and databases.

#### --answer--

Attributes and methods.

### --question--

#### --text--

単一継承とは何ですか？

#### --distractors--

子クラスが単一の関数から属性とメソッドを継承すること。

---

子クラスが複数の関数から属性とメソッドを継承すること。

---

子クラスが複数のクラスから属性とメソッドを継承すること。

#### --answer--

子クラスが単一のクラスから属性とメソッドを継承すること。

### --question--

#### --text--

次のうち、オブジェクト指向プログラミングの主要な原則ではないものはどれですか？

#### --distractors--

Encapsulation

---

Inheritance

---

Abstraction

#### --answer--

Don't repeat yourself (DRY)

### --question--

#### --text--

属性やメソッドにダブルアンダースコアとシングルアンダースコアを接頭辞として付ける違いは何ですか？

#### --distractors--

シングルアンダースコアは属性を完全に隠し、ダブルアンダースコアはメソッド内でのみ読み取り可能にする。

---

シングルアンダースコアはプライベート、ダブルアンダースコアはプロテクトを意味する。

---

シングルアンダースコアはパブリックにし、ダブルアンダースコアはスタティックにする。

#### --answer--

シングルアンダースコアは内部使用の慣例にすぎず、ダブルアンダースコアは名前のマングリングを引き起こして誤って属性やメソッドを上書きするのを防ぐ。

### --question--

#### --text--

複雑な実装の詳細を隠し、オブジェクトやシステムの本質的な機能だけを示すオブジェクト指向の概念はどれですか？

#### --distractors--

Inheritance

---

Polymorphism

---

Encapsulation

#### --answer--

Abstraction

### --question--

#### --text--

セッターを作成するために使うデコレーターはどれですか？

#### --distractors--

`@<property_name>.creator`

---

`@<method_name>.creater`

---

`@<method_name>.creator`

#### --answer--

`@<property_name>.setter`

### --question--

#### --text--

オブジェクトの内部状態を、ドアのように振る舞うパブリックなメソッドや属性のセットの背後に隠すオブジェクト指向の概念はどれですか？

#### --distractors--

Polymorphism

---

Abstraction

---

Single inheritance

#### --answer--

Encapsulation

### --question--

#### --text--

ゲッターとは何ですか？

#### --distractors--

属性の値を更新または変更するメソッド。

---

オブジェクトから属性を削除するメソッド。

---

オブジェクトに新しい属性を作成するメソッド。

#### --answer--

属性の値を取得または返すメソッド。

### --question--

#### --text--

セッターとは何ですか？

#### --distractors--

属性の値を取得するメソッド。

---

属性の値を削除するメソッド。

---

オブジェクトから属性を削除するメソッド。

#### --answer--

属性の値を設定または更新するメソッド。

### --question--

#### --text--

セッターとゲッターで設定および取得した値を削除できるものは何ですか？

#### --distractors--

Remover

---

Eraser

---

Delayer

#### --answer--

Deleter

### --question--

#### --text--

コードの再利用を促進し、明確な階層を提供し、すべてを書き直さずに振る舞いのカスタマイズを可能にするものは何ですか？

#### --distractors--

DRY

---

WET

---

Abstraction

#### --answer--

Inheritance

### --question--

#### --text--

次のうち、継承の正しい構文はどれですか？

#### --distractors--

```py
class Parent:
    pass
class Child(InheritParent):
    pass
```

---

```py
class Parent:
    pass
class Child(Inheriter):
    pass
```

---

```py
class Child:
   pass
class Parent(Parent):
   pass
```

#### --answer--

```py
class Parent:
   pass
class Child(Parent):
   pass
```

### --question--

#### --text--

セッターとゲッターをつなぐものは何ですか？

#### --distractors--

Methods

---

Functions

---

Classes

#### --answer--

Properties

### --question--

#### --text--

Pythonがダブルアンダースコアを接頭辞に持つ属性の名前を、クラス名を接頭辞として追加して内部的に変更するプロセスは何ですか？

#### --distractors--

Name Mingling

---

Polymorphism

---

Abstraction

#### --answer--

Name Mangling

### --question--

#### --text--

子クラス内で親クラスのメソッドを呼び出すことができる関数はどれですか？

#### --distractors--

`constructor()`

---

`__init__()`

---

`property()`

#### --answer--

`super()`

### --question--

#### --text--

オブジェクト指向プログラミングにおけるポリモーフィズムを最もよく説明する文はどれですか？

#### --distractors--

異なる属性とメソッドを持つ複数のコンストラクタをクラスに持たせることができる。

---

クラスが別のクラスから属性とメソッドを継承できる。

---

`class` キーワードでオブジェクトを作成できる。

#### --answer--

異なるクラスが同じメソッド名を使い、呼び出されたときに異なる動作を実行できる。

### --question--

#### --text--

なぜPythonはダブルアンダースコアを接頭辞に持つ属性の名前をマングリングするのですか？

#### --distractors--

属性を完全にプライベートにしてクラス外からアクセスできなくするため。

---

属性をより効率的に保存してパフォーマンスを向上させるため。

---

属性を自動的に読み取り専用プロパティに変換するため。

#### --answer--

継承を使う際にサブクラスで誤って属性やメソッドを上書きするのを防ぐため。

### --question--

#### --text--

なぜゲッターとセッターにメソッドではなくプロパティが使われるのですか？

#### --distractors--

コードを長くしてより明示的にするため。

---

値へのアクセスを完全に防ぐため。

---

すべての値の変更を自動的にログに記録するため。

#### --answer--

ドット記法で直接属性のようにアクセスでき、可読性が向上するため。
