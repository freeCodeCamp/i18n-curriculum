---
id: 66edd4f31ff19bf5573bf64b
title: JavaScript 함수형 프로그래밍 퀴즈
challengeType: 8
dashedName: quiz-javascript-functional-programming
---

# --description--

퀴즈를 통과하려면 아래 10문제 중 최소 9문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

순수한 함수란 무엇인가요?

#### --distractors--

전역 변수를 수정하고 `undefined` 또는 `null`을 반환하는 함수.

---

출력을 콘솔에 기록하는 함수.

---

외부 요인에 따라 동작이 달라지고 `null`를 반환하는 함수.

#### --answer--

같은 입력에 대해 항상 같은 출력을 반환하고 부작용이 없는 함수.

### --question--

#### --text--

함수형 프로그래밍에서 부작용이란 무엇인가요?

#### --distractors--

코드에서 예상치 못한 `TypeError`.

---

실행 시간이 너무 길어 프로그램이 중단되는 함수.

---

무한 루프를 발생시켜 프로그램이 중단되는 재귀 함수 호출.

#### --answer--

함수 외부에서 관찰 가능한 프로그램 상태의 변화.

### --question--

#### --text--

함수형 프로그래밍에서 커링이란 무엇인가요?

#### --distractors--

코드가 100% 오류 없이 동작하는지 확인하기 위한 테스트 설계 기법.

---

재귀 함수를 두 배 빠르게 실행하도록 최적화하는 방법.

---

ECMAScript 기준에 맞게 코드를 작성, 설계, 테스트하는 과정.

#### --answer--

여러 인수를 받는 함수를 하나의 인수만 받는 함수들의 수열로 변환하는 과정.

### --question--

#### --text--

다음 순수 함수의 출력은 무엇인가요?

```js
const add = (a, b) => a + b;
console.log(add(2, 5));
```

#### --distractors--

`3`

---

`4`

---

`undefined`

#### --answer--

`7`

### --question--

#### --text--

다음 중 커링의 예시는 무엇인가요?

#### --distractors--

```js
const curriedAverage = (a, b, c) => a + b + c / 3
```

---

```js
const curriedAverage = (a, b, c) => a + b + c
```

---

```js
function curriedAverage(a) {
  return a 
}

function curried(c) {
  return c
}
```

#### --answer--

```js
function curriedAverage(a) {
  return function(b) {
    return function(c) {
      return (a + b + c) / 3;
    };
  };
}
```

### --question--

#### --text--

다음 중 함수형 프로그래밍의 핵심 원칙은 무엇인가요?

#### --distractors--

전역 변수를 자주 수정하는 것.

---

가변 자료 구조를 광범위하게 사용하는 것.

---

객체지향 상속을 강조하는 것.

#### --answer--

부작용을 피하고 불변 데이터를 사용하는 것.

### --question--

#### --text--

불순 함수란 무엇인가요?

#### --distractors--

객체를 반환하는 함수.

---

부작용이 없는 함수.

---

`null`를 반환하는 함수.

#### --answer--

부작용이 있는 함수.

### --question--

#### --text--

다음 중 커링된 함수를 올바르게 호출하는 방법은 무엇인가요?

#### --distractors--

```js
curriedAverage(2 3 4);
```

---

```js
curriedAverage(2)==(3)==(4);
```

---

```js
curriedAverage(2)=>(3)=>(4);
```

#### --answer--

```js
curriedAverage(2)(3)(4);
```

### --question--

#### --text--

다음 중 불순 함수의 예시는 무엇인가요?

#### --distractors--

```js
function example(num) {
  return num;
}
```

---

```js
function sum(num1, num2) {
 return num1 + num2
}
```

---

```js
function addToTotal(value) {
  let total = 0;
  total += value;
  return total;
}
```

#### --answer--

```js
let total = 0;
function addToTotal(value) {
  total += value;
  return total;
}
```

### --question--

#### --text--

다음 중 부작용이 아닌 예시는 무엇인가요?

#### --distractors--

파일에 쓰기.

---

전역 변수 수정.

---

API 호출.

#### --answer--

두 값의 합을 반환하기.
