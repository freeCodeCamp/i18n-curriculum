---
id: 65796fac81f983127558f3f4
title: Крок 5
challengeType: 20
dashedName: step-5
---

# --description--

Тепер змініть свій існуючий виклик `print()`, щоб вивести значення ключа `age`.


# --hints--

Ви повинні використовувати `copper['age']` для доступу до значення ключа `age`.

```js
({ test: () => assert.match(code, /copper\s*\[\s*("|')age\1\s*\]/) })
```

Ви повинні викликати `print()`, передаючи `copper['age']` як аргумент.

```js
({ test: () => assert.match(code, /^print\(\s*copper\s*\[\s*("|')age\1\s*\]\s*\)/m) })
```

# --seed--

## --seed-contents--

```py
--fcc-editable-region--
copper = {
    'species': 'guinea pig',
    'age': 2
}
print(copper['species'])
--fcc-editable-region--
```
