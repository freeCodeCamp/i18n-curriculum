---
id: 67358ac128957c865dcf3ddf
title: Тест на класи JavaScript
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Яка мета класу в JavaScript?

#### --distractors--

Визначати лише властивості, а не методи.

---

Визначати лише статичні змінні.

---

Організовувати лише примітивні дані.

#### --answer--

Створювати багаторазові шаблони для об’єктів.

### --question--

#### --text--

Як визначити клас у JavaScript?

#### --distractors--

Використовуючи ключове слово `className`.

---

Використовуючи ключове слово `object`.

---

Використовуючи лише ключове слово `constructor`.

#### --answer--

Використовуючи ключове слово `class`.

### --question--

#### --text--

Який метод використовується в класі для ініціалізації властивостей?

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

Що представляє `this` всередині конструктора класу?

#### --distractors--

Глобальний об’єкт класу.

---

Саму функцію.

---

Невизначений референс.

#### --answer--

Екземпляр класу.

### --question--

#### --text--

Як поводиться `this` у стрілковій функції всередині методу класу?

#### --distractors--

Вона успадковує значення `this` від найближчої функції в іншій області видимості.

---

Вона посилається на новостворений екземпляр класу.

---

Вона успадковує значення `this` від глобального об’єкта в строгому режимі.

#### --answer--

Вона успадковує значення `this` із зовнішньої області видимості, де визначена.

### --question--

#### --text--

Яка основна мета ключового слова `extends`?

#### --distractors--

Визначати статичні методи в конструкторі батьківського класу.

---

Створювати метод у класі.

---

Ініціалізувати властивості за замовчуванням у батьківському класі.

#### --answer--

Створювати підклас, який успадковує від батьківського класу.

### --question--

#### --text--

Який із наведених прикладів створює `Car` клас, що успадковує від `Vehicle` класу?

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

Який буде вихідний результат цього коду?

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

Виникає помилка.

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

Яка функція ключового слова `super` у підкласі?

#### --distractors--

Визначити новий екземпляр батьківського класу.

---

Видалити методи та властивості батьківського класу.

---

Отримати доступ лише до статичних методів батьківського класу.

#### --answer--

Викликати конструктор або методи батьківського класу.

### --question--

#### --text--

Що описує статичний метод?

#### --distractors--

Метод, недоступний всередині самого класу.

---

Метод, який використовується лише у стрілкових функціях.

---

Метод, унікальний для кожного екземпляра класу.

#### --answer--

Метод, що належить самому класу, а не жодному екземпляру.

### --question--

#### --text--

Що виведе наступний код?

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

Виникає помилка.

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

Як отримати доступ до статичної властивості в класі JavaScript?

#### --distractors--

Використовуючи `this.propertyName` всередині методу.

---

Використовуючи `instance.propertyName` після створення екземпляра.

---

Використовуючи `super.propertyName` у підкласі.

#### --answer--

Використовуючи `ClassName.propertyName`.

### --question--

#### --text--

У якому випадку слід використовувати статичний метод замість методу екземпляра?

#### --distractors--

Коли потрібно отримати доступ і змінити властивості екземпляра.

---

Коли метод потрібно викликати на об’єкті-екземплярі.

---

Коли метод не залежить від даних конкретного екземпляра і працює з даними на рівні класу.

#### --answer--

Коли метод працює з даними, спільними для всіх екземплярів класу.

### --question--

#### --text--

Що виведе наступний код?

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

Виникне помилка.

### --question--

#### --text--

Яке ключове слово використовується для успадкування властивостей і методів від іншого класу?

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

Що виведе наступний код?

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

Виникає помилка.

#### --answer--

`Human`

### --question--

#### --text--

Як найкраще описати успадкування?

#### --distractors--

Воно дозволяє методу повертати новий об’єкт.

---

Воно дозволяє екземпляру визначати власні властивості.

---

Воно дозволяє перетворити статичну властивість на нестатичну.

#### --answer--

Воно дозволяє класу отримувати властивості та методи від іншого класу.

### --question--

#### --text--

Що станеться, якщо конструктор не визначений у підкласі?

#### --distractors--

Виникне помилка.

---

Підклас успадкує конструктор від будь-якого класу в коді.

---

Підклас не зможе створювати нові екземпляри.

#### --answer--

За замовчуванням буде використаний конструктор надкласу.

### --question--

#### --text--

Що представляє `this` у наступному коді?

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

Глобальний об’єкт.

---

Сам клас `Animal`.

---

Невизначена змінна.

#### --answer--

Екземпляр `Animal`.

### --question--

#### --text--

Яке основне призначення статичних властивостей?

#### --distractors--

Визначати властивості за замовчуванням для кожного екземпляра класу.

---

Заміщати методи у підкласі.

---

Визначати дані, специфічні для екземпляра.

#### --answer--

Зберігати дані, спільні для всього класу і його екземплярів.
