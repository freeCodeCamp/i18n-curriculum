---
id: 66edcd875b0d91de1fbbb492
title: Тест з основ JavaScript
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який із цих варіантів створить об’єкт String?

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

Як можна перетворити об’єкт String у рядковий літерал?

#### --distractors--

За допомогою конструктора `String()`.

---

За допомогою методу `.stringify()`.

---

Обернути об’єкт `String` у подвійні лапки.

#### --answer--

За допомогою методу `.toString()`.

### --question--

#### --text--

Чому в JavaScript можна використовувати методи, як-от `.length`, `.repeat()`, `.concat()` і `.slice()`, на рядковому примітиві?

#### --distractors--

Ці методи існують безпосередньо на рядкових примітивах.

---

Рядкові примітиви внутрішньо зберігаються як об’єкти.

---

JavaScript тимчасово перетворює рядковий примітив у масив.

#### --answer--

JavaScript тимчасово обгортає рядковий примітив в об’єкт String.

### --question--

#### --text--

Що виведе в консоль наступний код?

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

Який із наведених варіантів повертає об’єкт `Number` із рядка `"123"`?

#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--

Що повернуть `Number(true)` і `Number(false)` відповідно?

#### --distractors--

`undefined` і `undefined`

---

`NaN` і `NaN`

---

`true` і `false`

#### --answer--

`1` і `0`

### --question--

#### --text--

Що повернуть `Number(undefined)` і `Number(null)` відповідно?

#### --distractors--

`NaN` і `NaN`

---

`0` і `0`

---

`undefined` і `null`

#### --answer--

`NaN` і `0`

### --question--

#### --text--

Яка з наведених назв найкраще підходить для булевої змінної, що перевіряє, чи дозволено користувачу щось робити на вашому сайті?

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

Яка з наведених назв найкраще підходить для функції, що виконується, коли користувач відправляє форму?

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

Що таке розріджений масив?

#### --distractors--

Масив, у якому один або кілька індексів мають значення `null`.

---

Масив, у якому один або кілька індексів мають значення `undefined`.

---

Масив, у якому один або кілька індексів мають значення порожнього об’єкта.

#### --answer--

Масив, у якому один або кілька індексів не мають призначеного значення.

### --question--

#### --text--

У якому з наведених прикладів `arr` НЕ буде розрідженим масивом?

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

Яка основна мета лінтера?

#### --distractors--

Автоматично форматувати код.

---

Оптимізувати код для продуктивності.

---

Створювати документацію для коду.

#### --answer--

Виявляти потенційні помилки та проблеми в коді.

### --question--

#### --text--

Яка перевага використання форматера в кодовій базі?

#### --distractors--

Вони дозволяють розробникам ефективніше компілювати код.

---

Вони конвертують код у конкретну мову програмування.

---

Вони виявляють логічні помилки.

#### --answer--

Вони забезпечують послідовний стиль коду.

### --question--

#### --text--

У JavaScript хто або що зазвичай відповідає за виділення пам’яті для змінних?

#### --distractors--

Розробник.

---

Операційна система.

---

Сервер.

#### --answer--

Рuntime-двигун.

### --question--

#### --text--

Що буде виведено в консоль у наступному коді?

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

Що таке hoisting у JavaScript?

#### --distractors--

Підняття помилок на початок коду.

---

Процес створення нових змінних.

---

Автоматичне переміщення оголошень `var` на початок блоку.

#### --answer--

Процес переміщення оголошень змінних на початок їхньої області видимості.

### --question--

#### --text--

Чому використання ключового слова `var` вважається поганою практикою в сучасному JavaScript?

#### --distractors--

Вони повільніші за `let` і `const`.

---

Вони викликають синтаксичні помилки.

---

Вони не дозволяють перевірку типів.

#### --answer--

Вони доступні поза межами свого блоку.

### --question--

#### --text--

Яка мета модулів у JavaScript?

#### --distractors--

Покращити продуктивність коду.

---

Зробити код складнішим для розуміння.

---

Створювати нові мови програмування.

#### --answer--

Організовувати код у повторно використовувані одиниці.

### --question--

#### --text--

Як додати скрипт `app.js` як модуль у HTML-документ?

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

Який із наведених прикладів експортує функцію `calculateSum` з модуля `utils.js` і правильно імпортує та використовує її у файлі `app.js`?

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
