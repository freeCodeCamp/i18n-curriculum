---
id: 637f4e3672c65bc8e73dfe23
videoId: V8UAEoOvqFg
title: 超文本标记语言 基础课程 F
challengeType: 15
dashedName: html-foundations-lesson-f
---

# --description--

完成超文本标记语言公式化所需的最后一个元素是 `<body>` 元素。这里将放置所有将显示给用户的内容——文本、图像、列表、链接等等。

要完成公式化，为 `index.html` 文件添加一个 `body` 元素。`body` 元素也位于 `html` 元素内，并且始终在 `head` 元素下方，如下所示：

# --assignment--

观看并跟随 Kevin Powell 出色的 Building your first web page `video`。

---

通过删除 `index.html` 文件的内容并尝试从记忆中重新写出所有公式化，来建立一些肌肉记忆。如果你卡住了，前几次查看课程内容也不用担心。只要坚持，直到你能从记忆中完成几次为止。

---

将你的公式化通过此 [HTML 验证器](https://www.freeformatter.com/html-validator.html) 运行。验证器确保你的标记是正确的，并且是极好的学习工具，因为它们会对你可能经常犯且未察觉的语法错误提供反馈，例如缺少闭合标签和 HTML 中的多余空格。

# --questions--

## --text--

`body` 元素的作用是什么？

## --answers--

这里显示网页的所有重要信息，例如 `title` 和 `meta` 标签。

---

这里是你告诉浏览器如何正确 `Render` 网页的地方。

---

这里是页面上所有内容显示的位置，例如图像、文本和链接。


## --video-solution--

3
