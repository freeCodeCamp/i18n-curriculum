---
id: 66df3b712c41c499e9d31e5b
title: Grundlegendes HTML-Quiz
challengeType: 8
dashedName: quiz-basic-html
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Wie funktioniert das `loop`-Attribut innerhalb des `audio`-Elements?

#### --distractors--

Es ermöglicht dem `audio`-Element, die Wiedergabe mit anderen Multimediaelementen auf der Webseite zu synchronisieren.

---

Es passt Tonhöhe und Klangfarbe des Audios an.

---

Es veranlasst das `audio`-Element, die Wiedergabe in einer zufälligen Folge zu stoppen und neu zu starten.

#### --answer--

Es startet das Audio erneut, sobald es beendet ist.

### --question--

#### --text--

Was ist ein Void-Element in HTML?

#### --distractors--

Ein Element, das verwendet wird, um Videos auf dem Bildschirm einzubetten.

---

Ein Element zur Anzeige von Listen.

---

Ein Element, das zum Einbetten von Ton in das Dokument verwendet wird.

#### --answer--

Ein Element ohne Inhalt oder End-Tag.

### --question--

#### --text--

Welches der folgenden ist ein korrekter Wert für das `id`-Attribut?

#### --distractors--

`id="main@content"`

---

`id="main content"`

---

`id="main~content"`

#### --answer--

`id="main-content"`

### --question--

#### --text--

Wie funktioniert `target="_parent"`?

#### --distractors--

Es öffnet einen Link in einem neuen Browserkontext.

---

Es gibt die Position im verknüpften Dokument an, zu der der Browser nach dem Klicken scrollen soll.

---

Es leitet den Benutzer auf eine andere Webseite um, die im `href`-Attribut angegeben ist.

#### --answer--

Es öffnet einen Link im übergeordneten Kontext des aktuellen.

### --question--

#### --text--

Was ist der Unterschied zwischen einem Boolean-Attribut und einem regulären Attribut?

#### --distractors--

Boolean-Attribute können nur mit Kontrollkästchen verwendet werden, während reguläre Attribute mit Optionsschaltflächen verwendet werden.

---

Reguläre Attribute sind immer für Bilder erforderlich, während Boolean-Attribute optional sind.

---

Boolean-Attribute müssen immer einen Wert zugewiesen bekommen, reguläre Attribute benötigen keine Werte.

#### --answer--

Ein Boolean-Attribut kann vorhanden oder nicht vorhanden sein, während ein reguläres Attribut immer einen angegebenen Wert hat.

### --question--

#### --text--

Welches Element wird verwendet, um Skripte mit Ihrer HTML-Datei zu verknüpfen?

#### --distractors--

`img`

---

`ul`

---

`h2`

#### --answer--

`script`

### --question--

#### --text--

Was ist eine HTML-Boilerplate?

#### --distractors--

Ein spezielles Werkzeug, das Webentwickler verwenden, um Wasserzeichen zu Dokumenten hinzuzufügen.

---

Eine Art Auszeichnungssprache.

---

Ein Plugin, das automatisch „Lorem Ipsum“-Text auf der Seite generiert.

#### --answer--

Eine Vorlage, die alle wesentlichen Informationen für ein HTML-Dokument enthält.

### --question--

#### --text--

Welches Attribut weist einem HTML-Element eine eindeutige Kennung zu?

#### --distractors--

`class`

---

`href`

---

`action`

#### --answer--

`id`

### --question--

#### --text--

Was ist eine HTML-Entität (Zeichenreferenz)?

#### --distractors--

Eine grafische Darstellung eines Zeichens in HTML.

---

Ein spezielles Formatierungstag in HTML.

---

Ein Bild, das ein Zeichen im Webdesign darstellt.

#### --answer--

Eine Zeichenfolge, die ein reserviertes Zeichen in HTML darstellt.

### --question--

#### --text--

Was ist der Zweck des `audio`-Elements?

#### --distractors--

Es wird verwendet, um Nutzern zu erlauben, ihre Stimme direkt auf einer Webseite aufzunehmen.

---

Es wird verwendet, um `audio`- und `video`-Elemente auf der Seite zu stylen.

---

Es wird verwendet, um Text auf einer Webseite hörbar zu machen, wenn darauf geklickt wird.

#### --answer--

Es wird verwendet, um Audio zum HTML-Dokument hinzuzufügen.

### --question--

#### --text--

Welches der folgenden ist KEIN Beispiel für ein HTML-Element?

#### --distractors--

`img`

---

`h1`

---

`link`

#### --answer--

`byte`

### --question--

#### --text--

Welche Rolle hat das `target`-Attribut innerhalb von Ankerelementen?

#### --distractors--

Es definiert die Farbe des Links, wenn der Benutzer mit der Maus darüber fährt.

---

Es gibt die Position im verknüpften Dokument an, zu der der Browser nach dem Klicken scrollen soll.

---

Es bestimmt die Schriftgröße des Ankertexts im `link`-Element.

#### --answer--

Es gibt an, wo das verknüpfte Dokument geöffnet wird, wenn der Benutzer auf den Link klickt.

### --question--

#### --text--

Was ist ein absoluter Pfad?

#### --distractors--

Ein Pfad, der relativ zur aktuellen Datei ist.

---

Ein Pfad, der Variablen enthält.

---

Ein Pfad, der Platzhalterzeichen enthält.

#### --answer--

Ein vollständiger Pfad zu einer Ressource, beginnend im Stammverzeichnis.

### --question--

#### --text--

Welche Rolle hat das `link`-Element in HTML?

#### --distractors--

Es wird verwendet, um Hyperlinks innerhalb derselben Webseite zu erstellen.

---

Es wird verwendet, um das Layout und die Struktur einer Webseite zu definieren.

---

Es wird verwendet, um Audio- oder Videodateien in eine Webseite einzubetten.

#### --answer--

Es wird verwendet, um auf externe Ressourcen wie Stylesheets und Seitenicons zu verknüpfen.

### --question--

#### --text--

Welcher der folgenden Werte ist gültig für die Arbeit mit Open-Graph-Eigenschaften?

#### --distractors--

`"og:socialMedia"`

---

`"og:mediaOG"`

---

`"og:openGraph"`

#### --answer--

`"og:title"`

### --question--

#### --text--

Welches der folgenden ist ein Beispiel für ein Boolean-Attribut?

#### --distractors--

`href`

---

`src`

---

`width`

#### --answer--

`checked`

### --question--

#### --text--

Welche der folgenden ist die korrekte Syntax für das `img`-Element?

#### --distractors--

`<img src="image.jpg" alt="Description of the image"></img>`

---

`<img source="image.jpg" desc="Description of the image"></img>`

---

`<img src="image.jpg" desc="Description of the image">`

#### --answer--

`<img src="image.jpg" alt="Description of the image">`

### --question--

#### --text--

Wofür wird das `iframe`-Element verwendet?

#### --distractors--

Es wird verwendet, um Bildunterschriften hinzuzufügen.

---

Es wird verwendet, um Urheberrechtsinformationen für Medien hinzuzufügen.

---

Es wird verwendet, um Hyperlinks innerhalb derselben Webseite zu erstellen.

#### --answer--

Es wird verwendet, um ein anderes Dokument innerhalb des aktuellen HTML-Dokuments einzubetten.

### --question--

#### --text--

Welcher der folgenden ist KEIN gültiger Linkzustand für Ankerelemente?

#### --distractors--

`hover`

---

`visited`

---

`active`

#### --answer--

`href`

### --question--

#### --text--

Welche Rolle hat das `title`-Element?

#### --distractors--

Ein Element, das Nutzern erlaubt, den Quellcode einer Webseite zu bearbeiten.

---

Es definiert das Layout und die Positionierung von Elementen innerhalb einer Webseite.

---

Es wird verwendet, um Videos und Multimedia-Inhalte direkt in ein HTML-Dokument einzubetten.

#### --answer--

Es gibt den Titel eines Dokuments an und erscheint im Browser-Tab oder -Fenster.

## --quiz--

### --question--

#### --text--

Welches der folgenden `target`-Attribute öffnet einen Link im übergeordneten Kontext des aktuellen?

#### --distractors--

`_blank`

---

`_self`

---

`_unfencedTop`

#### --answer--

`_parent`

### --question--

#### --text--

Welches Attribut wird verwendet, um die `UTF-8`-Zeichenkodierung für ein HTML-Dokument festzulegen?

#### --distractors--

`accept`

---

`capture`

---

`enctype`

#### --answer--

`charset`

### --question--

#### --text--

Welche Rolle hat HTML im Web?

#### --distractors--

HTML wird für Stil und Layout verwendet.

---

HTML wird verwendet, um Interaktivität auf der Seite hinzuzufügen.

---

HTML ist ein Register für Softwarepakete.

#### --answer--

HTML stellt den Inhalt und die Struktur einer Webseite dar.

### --question--

#### --text--

Welche Bildlizenz gilt für die Gemeinfreiheit?

#### --distractors--

JPG

---

Creative Commons.

---

BSD

#### --answer--

Creative Commons 0.

### --question--

#### --text--

Wie setzen Sie ein Boolean-Attribut auf `false`?

#### --distractors--

Indem Sie den Attributwert auf `0` setzen.

---

Indem Sie den `not`-Operator vor dem Attributnamen verwenden.

---

Indem Sie das Attribut auf einen leeren String (`""`) setzen.

#### --answer--

Indem Sie das Attribut im Element weglassen.

### --question--

#### --text--

Wofür wird das `script`-Element verwendet?

#### --distractors--

Es wird verwendet, um CSS in das HTML-Dokument einzubetten oder eine externe CSS-Datei zu verknüpfen.

---

Es wird verwendet, um Pascal-Code in das HTML-Dokument einzubetten.

---

Es wird verwendet, um C#-Code in das HTML-Dokument einzubetten.

#### --answer--

Es wird verwendet, um JavaScript in das HTML-Dokument einzubetten oder eine externe JavaScript-Datei zu verknüpfen.

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um den Titel für ein HTML-Dokument festzulegen?

#### --distractors--

`footer`

---

`section`

---

`figcaption`

#### --answer--

`title`

### --question--

#### --text--

Wofür wird das `class`-Attribut typischerweise verwendet?

#### --distractors--

Es wird verwendet, um Metadaten über das Element einzubetten.

---

Es wird verwendet, um inline Styles direkt im HTML-Tag zu definieren.

---

Es wird verwendet, um eindeutige Kennungen für JavaScript-Funktionen anzugeben.

#### --answer--

Es wird verwendet, um eine Reihe von Styles auf mehrere Elemente anzuwenden.

### --question--

#### --text--

Welches der folgenden ist KEIN Beispiel für eine häufig verwendete HTML-Entität?

#### --distractors--

`&quot;` (Anführungszeichen).

---

`&copy;` (Copyright-Symbol).

---

`&gt;` (Größer-als-Symbol).

#### --answer--

`&div;` (div-Element).

### --question--

#### --text--

Welches Element wird verwendet, um Audio zu Ihrem HTML-Dokument hinzuzufügen?

#### --distractors--

`media`

---

`video`

---

`hr`

#### --answer--

`audio`

### --question--

#### --text--

Wo wird der auf der Seite dargestellte Inhalt in einem HTML-Dokument platziert?

#### --distractors--

Zwischen den `head`- und `body`-Elementen.

---

Innerhalb des `repr`-Elements.

---

Innerhalb des `script`-Elements.

#### --answer--

Innerhalb des `body`-Elements.

### --question--

#### --text--

Welches Attribut wird verwendet, um das Audio erneut zu starten, sobald es beendet ist?

#### --distractors--

`multiple`

---

`inputmode`

---

`enctype`

#### --answer--

`loop`

### --question--

#### --text--

Welche der folgenden ist die korrekte Syntax für ein `div`-Element?

#### --distractors--

`<<div>>block container<</div>>`

---

`>>div>>block container>>div>>`

---

`[div]block container[/div]`

#### --answer--

`<div>block container</div>`

### --question--

#### --text--

Wie funktioniert `target="_self"`?

#### --distractors--

Es gibt die Position im verknüpften Dokument an, zu der der Browser nach dem Klicken scrollen soll.

---

Es lädt das verknüpfte Dokument automatisch auf den Computer des Nutzers herunter.

---

Es leitet den Benutzer auf eine andere Webseite um, die im `href`-Attribut angegeben ist.

#### --answer--

Es öffnet einen Link im aktuellen Browserkontext.

### --question--

#### --text--

Was ist ein relativer Pfad?

#### --distractors--

Er gibt den genauen Speicherort einer Datei oder eines Verzeichnisses vom Stammverzeichnis des Dateisystems an.

---

Er enthält die vollständige URL, beginnend mit dem Domainnamen bis zur spezifischen Datei oder dem Verzeichnis.

---

Er beginnt immer mit einem Schrägstrich (`/`), der das Stammverzeichnis der Webseite oder des Dateisystems angibt.

#### --answer--

Er gibt den Speicherort einer Datei oder eines Verzeichnisses relativ zum aktuellen Arbeitsverzeichnis an.

### --question--

#### --text--

Welches der folgenden Elemente ist die korrekte Syntax für ein `link`-Element?

#### --distractors--

`<link rel="stylesheet" href="./styles.css"></link>`

---

`</link rel="stylesheet" href="./styles.css"></link>`

---

`<<link rel="stylesheet" href="./styles.css"></link>>`

#### --answer--

`<link rel="stylesheet" href="./styles.css" />`

### --question--

#### --text--

Welcher der folgenden Werte ist KEIN gültiger Wert für die Arbeit mit Open-Graph-Eigenschaften?

#### --distractors--

`"og:title"`

---

`"og:type"`

---

`"og:image"`

#### --answer--

`"og:socialMedia"`

### --question--

#### --text--

Welche der folgenden ist die korrekte Syntax für ein Absatz-Element?

#### --distractors--

`>p>paragraph element>/p>`

---

`<<p>>paragraph element<</p>>`

---

`<p>`

#### --answer--

`<p>paragraph element</p>`

### --question--

#### --text--

Wofür steht der Name des `iframe`-Elements?

#### --distractors--

Inline Framing

---

Inner Frame

---

Inline video

#### --answer--

Inline Frame

### --question--

#### --text--

Was bedeutet der aktive Zustand für Ankerelemente?

#### --distractors--

Der Link ist deaktiviert und kann nicht angeklickt werden.

---

Der Mauszeiger befindet sich über dem Link.

---

Der Link wurde vom Benutzer bereits besucht.

#### --answer--

Ein Benutzer klickt gerade aktiv darauf.

## --quiz--

### --question--

#### --text--

Welches der folgenden öffnet den Link in einem neuen Browser-Tab?

#### --distractors--

```html
<a target="_parent" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_top" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_self" href="https://freecatphotoapp.com">cat photos</a>
```

#### --answer--

```html
<a target="_blank" href="https://freecatphotoapp.com">cat photos</a>
```

### --question--

#### --text--

Welches der folgenden ist ein Void-Element?

#### --distractors--

`div`

---

`section`

---

`p`

#### --answer--

`img`

### --question--

#### --text--

Wie verwendet man HTML-Entitäten korrekt?

#### --distractors--

`<p>This is an &lt&img /&gt& element</p>`

---

`<p>This is an ;lt&img /;gt& element</p>`

---

`<p>This is an ;lt;img /;gt; element</p>`

#### --answer--

`<p>This is an &lt;img /&gt; element</p>`

### --question--

#### --text--

Wofür wird das `ul`-Element in HTML verwendet?

#### --distractors--

Es wird verwendet, um nur eine Liste von Bildern zu gruppieren.

---

Es wird verwendet, um nur eine Liste von Links zu gruppieren.

---

Es wird verwendet, um eine geordnete Liste von Elementen zu gruppieren.

#### --answer--

Es wird verwendet, um eine ungeordnete Liste von Elementen zu gruppieren.

### --question--

#### --text--

Wie wendet man mehrere Klassen korrekt auf ein HTML-Element an?

#### --distractors--

```html
<div class="box>>red>>box"></div>
``` 

---

```html
<div class="box<<red<<box"></div>
``` 

---

```html
<div class="box-red-box"></div>
``` 

#### --answer--

```html
<div class="box red-box"></div>
``` 

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um eine geordnete Liste von Elementen darzustellen?

#### --distractors--

`span`

---

`div`

---

`ul`

#### --answer--

`ol` 

### --question--

#### --text--

Wofür werden Open-Graph-Eigenschaften verwendet?

#### --distractors--

Diese Eigenschaften werden verwendet, um interaktive Multimedia-Inhalte direkt in Webseiten einzubetten.

---

Diese Eigenschaften werden verwendet, um dynamische Pop-up-Werbung auf Webseiten zu erzeugen.

---

Diese Eigenschaften werden verwendet, um sensible Daten zu verschlüsseln, die zwischen Webservern und Browsern übertragen werden.

#### --answer--

Diese Eigenschaften legen fest, wie der Inhalt einer Webseite auf verschiedenen sozialen Medien dargestellt wird.

### --question--

#### --text--

Welches der folgenden wird verwendet, um eine kurze Beschreibung für eine Webseite festzulegen?

#### --distractors--

```html
<meta
  add="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  set="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  description="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

#### --answer--

```html
<meta
  name="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

### --question--

#### --text--

Wofür steht SVG?

#### --distractors--

Scalable Vector Graph

---

Scalable Vistor Graphics

---

Site Vector Graphics

#### --answer--

Scalable Vector Graphics 

### --question--

#### --text--

Welche Rolle hat das `footer`-Element?

#### --distractors--

Dieses Element wird verwendet, um verwandte Bildunterschriften und Bilder auf einer Webseite zu gruppieren.

---

Dieses Element wird verwendet, um horizontale Linien zwischen Absätzen auf der Seite zu erzeugen.

---

Dieses Element wird verwendet, um eine Liste geordneter Elemente am Seitenende zu erstellen.

#### --answer--

Dieses Element wird verwendet, um Urheberrechtsinhalte oder verwandte Dokumente am Seitenende zu gruppieren.

### --question--

#### --text--

Was ist `UTF-8`-Zeichenkodierung?

#### --distractors--

Es ist eine standardisierte Zeichenkodierung, die verwendet wird, um den Titel einer Webseite festzulegen.

---

Es ist eine Zeichengruppe, die verwendet wird, um die Seitenbeschreibung für die Webseite festzulegen.

---

Es ist eine spezielle Zeichengruppe, die in Elementen verwendet wird, um die Medienleistung zu verbessern.

#### --answer--

Es ist eine standardisierte Zeichenkodierung, die im Web weit verbreitet ist.

### --question--

#### --text--

Welches der folgenden ist ein Beispiel für ein ersetztes Element?

#### --distractors--

`slot`

---

`div`

---

`p`

#### --answer--

`iframe` 

### --question--

#### --text--

Welches der folgenden gilt als wichtigste Überschrift auf einer Webseite?

#### --distractors--

`h6`

---

`h3`

---

`h5`

#### --answer--

`h1` 

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um auf externe Ressourcen wie Stylesheets und Seitenicons zu verknüpfen?

#### --distractors--

`a`

---

`p`

---

`div`

#### --answer--

`link` 

### --question--

#### --text--

Welches der folgenden Elemente vermittelt ein Gefühl von Dringlichkeit, Ernsthaftigkeit oder starker Wichtigkeit?

#### --distractors--

`important`

---

`heavy`

---

`strength`

#### --answer--

`strong`

### --question--

#### --text--

Welche Rolle hat das `figcaption`-Element?

#### --distractors--

Es liefert Bildunterschriften für den Inhalt innerhalb eines `span`-Elements.

---

Es liefert Bildunterschriften für den Inhalt innerhalb eines `slot`-Elements.

---

Es liefert Bildunterschriften für den Inhalt innerhalb eines `div`-Elements.

#### --answer--

Es liefert Bildunterschriften für den Inhalt innerhalb eines `figure`-Elements.

### --question--

#### --text--

Wie wird die `DOCTYPE`-Deklaration für ein HTML-Dokument korrekt gesetzt?

#### --distractors--

`<DOCTYPE>`

---

`<*DOCTYPE html>`

---

`<<DOCTYPE html>>`

#### --answer--

`<!DOCTYPE html>`

### --question--

#### --text--

Was ist der Zweck des `head`-Elements in einem HTML-Dokument?

#### --distractors--

Es ist ein Container für Ankerelemente im HTML-Dokument.

---

Es ist ein Container für Bilder im HTML-Dokument.

---

Es ist ein Container für Überschriften im HTML-Dokument.

#### --answer--

Es ist ein Container für Metadaten im HTML-Dokument.

### --question--

#### --text--

Wie wird die Sprache des HTML-Dokuments korrekt auf Englisch gesetzt?

#### --distractors--

`<html lang-set="en">` 

---

`<html set-lang="en">` 

---

`<html language="en">` 

#### --answer--

`<html lang="en">` 

### --question--

#### --text--

Welches der folgenden Elemente sollte NICHT innerhalb von `head` platziert werden?

#### --distractors--

`link`

---

`meta`

---

`title`

#### --answer--

`img` 

## --quiz--

### --question--

#### --text--

Welche der folgenden Aussagen über absolute und relative Pfade ist NICHT korrekt?

#### --distractors--

Absolute Pfade beginnen im Stammverzeichnis, während relative Pfade vom Speicherort der aktuellen Datei abhängen.

---

Relative Pfade sind kürzer und flexibler für Verknüpfungen innerhalb einer Webseite, während absolute Pfade die vollständige URL enthalten.

---

Sowohl absolute als auch relative Pfade können verwendet werden, um auf Dateien innerhalb derselben Webseite zu verlinken.

#### --answer--

Relative Pfade können `..` nicht verwenden, um Verzeichnisse nach oben zu wechseln, absolute Pfade jedoch schon.

### --question--

#### --text--

Welcher der folgenden ist KEIN gültiger Wert für das target-Attribut in einem `a`-Tag?

#### --distractors--

`_self`

---

`_parent`

---

`_top`

#### --answer--

`_main`

### --question--

#### --text--

Was ist der Zweck des `controls`-Attributs im `audio`-Element?

#### --distractors--

Es definiert das Audio-Dateiformat, das der Browser verwenden soll.

---

Es startet das Audio automatisch, wenn die Seite geladen wird.

---

Es gibt die URL der Audioquelle an.

#### --answer--

Es fügt eingebaute Wiedergabesteuerungen wie Abspielen, Pause und Lautstärkeanpassung hinzu.

### --question--

#### --text--

Welches der folgenden Elemente verbessert SEO, indem es eine kurze Beschreibung der Webseite liefert?

#### --distractors--

```html
<meta
 description="describe"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 name="desc"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 description="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

#### --answer--

```html
<meta
 name="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

### --question--

#### --text--

Was macht `<meta charset="utf-8" />`?

#### --distractors--

Setzt die Sprache der Webseite auf Englisch.

---

Verknüpft ein externes CSS-Stylesheet.

---

Gibt den Titel der Webseite an.

#### --answer--

Definiert die Zeichenkodierung, um die korrekte Textanzeige sicherzustellen.

### --question--

#### --text--

Was ist der Unterschied zwischen `link`- und `script`-Elementen in HTML?

#### --distractors--

`link` ist für JavaScript-Dateien, `script` für CSS-Dateien.

---

Sowohl `link` als auch `script` werden verwendet, um Webseiten zu stylen.

---

`script` dient zum Hinzufügen von Styles, `link` zum Ausführen von JavaScript.

#### --answer--

`link` wird verwendet, um Styles aus einer externen CSS-Datei hinzuzufügen, während `script` verwendet wird, um JavaScript-Code auszuführen.

### --question--

#### --text--

Welche der folgenden Aussagen über HTML-Klassen und IDs ist falsch?

#### --distractors--

Ein Klassenname kann mehrfach im gesamten HTML-Dokument verwendet werden.

---

Das `rel`-Attribut definiert die Beziehung zwischen der verknüpften Ressource und dem HTML-Dokument.

---

Die `og:image`-Eigenschaft wird verwendet, um das Bild anzugeben, das in Social-Media-Beiträgen angezeigt wird.

#### --answer--

ID-Namen dürfen keine Unterstriche enthalten und müssen eindeutig sein.

### --question--

#### --text--

Welche der folgenden Aussagen über HTML-Überschriftenelemente ist richtig?

#### --distractors--

Die Textgröße nimmt von `h1` zu `h6` zu.

---

Die Textgröße bleibt für alle Überschriften gleich.

---

`h3` ist größer als `h2`.

#### --answer--

Die Textgröße nimmt von `h1` zu `h6` ab.

### --question--

#### --text--

Welche Aussage über HTML-Elemente ist falsch?

#### --distractors--

HTML-Elemente sind die grundlegenden Bausteine einer Webseite.

---

Einige Codebasen enthalten einen Schrägstrich (/) innerhalb von Void-Elementen.

---

Ein Boolean-Attribut wirkt einfach dadurch, dass es im Tag vorhanden oder nicht vorhanden ist.

#### --answer--

Void-Elemente enthalten Inhalt und haben nur einen Start-Tag.

### --question--

#### --text--

Welche der folgenden Aussagen über Void-Elemente in HTML ist richtig?

#### --distractors--

Ein Void-Element muss immer einen Schrägstrich / vor dem Schließen enthalten.

---

Ein Void-Element sollte niemals einen Schrägstrich / enthalten.

---

Der Schrägstrich / ist für selbstschließende Void-Elemente in allen HTML-Versionen verpflichtend.

#### --answer--

Sowohl `<img>` als auch `<img/>` sind in modernem HTML akzeptabel.

### --question--

#### --text--

Was ist der Hauptzweck von Kommentaren in der Programmierung?

#### --distractors--

Den Code schneller ausführen zu lassen.

---

Zu ändern, wie der Browser Inhalte anzeigt.

---

Temporäre Daten im Code zu speichern.

#### --answer--

Notizen für sich selbst und andere Entwickler im Code zu hinterlassen.

### --question--

#### --text--

Welches target-Attribut öffnet einen Link im gleichen Browserkontext wie den aktuellen?

#### --distractors--

`_blank`

---

`_top`

---

`_parent`

#### --answer--

`_self`

### --question--

#### --text--

Wozu dient das `em`-Element in HTML?

#### --distractors--

Um Text größer zu machen.

---

Um die Textfarbe zu ändern.

---

Um Wörter zur Hervorhebung zu unterstreichen.

#### --answer--

Um wichtigen Text durch Kursivschrift hervorzuheben.

### --question--

#### --text--

Was bewirkt das `strong`-Element in HTML?

#### --distractors--

Es vergrößert die Schriftgröße.

---

Es ändert die Textfarbe.

---

Es unterstreicht den Text.

#### --answer--

Es macht Text fett, um Wichtigkeit zu zeigen.

### --question--

#### --text--

Was ist der Hauptunterschied zwischen den `figure`- und `figcaption`-Elementen in HTML?

#### --distractors--

`figure` wird für Bildunterschriften verwendet, `figcaption` zum Gruppieren verwandter Inhalte wie Bilder oder Diagramme.

---

`figcaption` muss immer außerhalb des `figure`-Elements platziert werden.

---

Es gibt keinen Unterschied; beide werden für denselben Zweck verwendet.

#### --answer--

`figure` gruppiert verwandte Inhalte wie Bilder oder Diagramme, während `figcaption` eine Bildunterschrift für diese Inhalte liefert.

### --question--

#### --text--

Warum werden HTML-Entitäten verwendet?

#### --distractors--

Um Text zu stylen.

---

Um Hyperlinks in einer Webseite zu erstellen.

---

Um die Schriftgröße von Sonderzeichen zu erhöhen.

#### --answer--

Um reservierte Zeichen wie `<` und `&` in HTML darzustellen.

### --question--

#### --text--

Welche der folgenden Aussagen über Open-Graph-Tags ist korrekt?

#### --distractors--

Open-Graph-Tags werden verwendet, um eine Webseite mit CSS zu stylen.

---

Open-Graph-Tags werden nur für SEO verwendet und haben keine Auswirkung auf soziale Medien.

---

Open-Graph-Tags müssen im `body`-Abschnitt von HTML platziert werden.

#### --answer--

Die `og:title`-Eigenschaft legt den Titel fest, der angezeigt wird, wenn Inhalte in sozialen Medien geteilt werden.

### --question--

#### --text--

Welche der folgenden Aussagen über Bildformate ist falsch?

#### --distractors--

WEBP und AVIF sind besser für moderne Bildoptimierung.

---

SVG-Bilder können skaliert werden, ohne an Qualität zu verlieren.

---

Kompression hilft, die Dateigröße von Bildern zu reduzieren.

#### --answer--

PNG und JPG sind heute die besten Formate für optimierte Bilder.

### --question--

#### --text--

Was bewirkt das `loop`-Attribut im `audio`-Element?

#### --distractors--

Spielt das Audio einmal ab.

---

Stoppt das Audio nach einmaligem Abspielen.

---

Verlangsamt die Audiowiedergabe.

#### --answer--

Wiederholt das Audio kontinuierlich.

### --question--

#### --text--

Wie lässt sich eine HTML-Boilerplate am besten beschreiben?

#### --distractors--

Ein spezielles Werkzeug, das Webentwickler verwenden, um Wasserzeichen zu Dokumenten hinzuzufügen.

---

Ein Browser-Plugin, das das Laden von Webseiten beschleunigt.

---

Ein Coding-Framework, das ausschließlich für Animationen in HTML verwendet wird.

#### --answer--

Eine vordefinierte Vorlage, die die wesentliche HTML-Struktur für die Webentwicklung enthält.
