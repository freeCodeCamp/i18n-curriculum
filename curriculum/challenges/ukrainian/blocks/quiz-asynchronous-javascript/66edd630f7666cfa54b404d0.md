---
id: 66edd630f7666cfa54b404d0
title: "Асинхронний JavaScript: тест"
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке асинхронне програмування?

#### --distractors--

Метод для послідовного виконання коду.

---

Техніка для обробки помилок у вашому коді.

---

Спосіб робити мережеві запити, зазвичай для отримання або відправлення даних на сервер.

#### --answer--

Метод виконання коду одночасно без блокування головного потоку.

### --question--

#### --text--

Що таке потік?

#### --distractors--

Функція, яка використовується для отримання даних із сервера.

---

Програма, що виконує JavaScript-код у веббраузері.

---

Особливий тип функції, яка передається як аргумент іншій функції.

#### --answer--

Послідовність інструкцій, яку можна виконувати незалежно від основного потоку програми.

### --question--

#### --text--

Яка мета Fetch API у JavaScript?

#### --distractors--

Керувати асинхронними функціями зворотного виклику.

---

Створювати та керувати куками.

---

Відтворювати динамічний HTML-вміст.

#### --answer--

Робити мережеві запити до серверів.

### --question--

#### --text--

Який `HTTP` метод використовується для відправлення даних на сервер для обробки?

#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--

Який із наведених способів є правильним для отримання даних з API?

#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--

Що таке проміс у JavaScript?

#### --distractors--

Спосіб синхронізувати кілька операцій при роботі з Fetch API.

---

Особливий тип функції зворотного виклику, яку потрібно використовувати у всіх функціях вищого порядку.

---

Техніка для обробки помилок при роботі з Fetch API.

#### --answer--

Об’єкт, який представляє майбутнє завершення (або невдачу) асинхронної операції.

### --question--

#### --text--

Що дозволяє робити ланцюжок промісів?

#### --distractors--

Дозволяє виконувати кілька синхронних операцій паралельно.

---

Дозволяє ефективніше обробляти синхронні операції.

---

Дозволяє автоматично повторювати невдалі операції.

#### --answer--

Дозволяє виконувати послідовність асинхронних операцій одна за одною.

### --question--

#### --text--

Який метод використовується для обробки успішного завершення промісу?

#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--

Який метод використовується для обробки помилок у ланцюжку промісів?

#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--

Який із наведених HTTP-методів НЕ є дійсним?

#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--

Який із наведених синтаксисів є правильним для створення async-функції?

#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--

Що робить ключове слово `await` в async-функції?

#### --distractors--

Створює новий проміс.

---

Виконує код негайно без очікування і повертає об’єкт.

---

Виводить результат промісу і повертає `null`.

#### --answer--

Призупиняє виконання функції, доки проміс не буде виконаний.

### --question--

#### --text--

Який із наведених API використовується для надання вебсайтам можливості запитувати місцезнаходження користувача?

#### --distractors--

Locator API

---

Geo API

---

Locate API

#### --answer--

Geolocation API

### --question--

#### --text--

Який із наведених атрибутів завантажує скрипт асинхронно, але чекає, поки HTML-документ буде повністю проаналізований, перш ніж запускати скрипт?

#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--

Що поверне наступний код?

```js
fetch('https://api.example.com/data')
```

#### --distractors--

Помилку, що дані пошкоджені.

---

Помилку, що дані не можуть бути отримані.

---

Нічого не поверне, і JavaScript перейде до наступної функції в скрипті.

#### --answer--

`Promise`, який виконується з об’єктом `Response`.

### --question--

#### --text--

Який HTTP-метод використовується за замовчуванням у Fetch API?

#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--

Який із наведених методів використовується для видалення ресурсів на сервері?

#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--

Який із наведених способів є правильним для створення промісу?

#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--

Який із наведених атрибутів повідомляє браузеру завантажити файл скрипта асинхронно, продовжуючи аналіз HTML-документа?

#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--

Який метод Geolocation API використовується для отримання поточного положення пристрою?

#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`
