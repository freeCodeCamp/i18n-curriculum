---
id: 66ed9018f45ce3ece4053eb9
title: CSS変数クイズ
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

次のうち、`@property`ルールで有効な記述子ではないものはどれですか？

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

次のコードで、`color`が定義されていない場合、`--main-color`はどの値になりますか？

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

色は適用されません。

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

CSSでカスタムプロパティを宣言する正しい方法はどれですか？

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

CSSでカスタムプロパティを使う主な目的は何ですか？

#### --distractors--

DOM構造を変更すること。

---

動的なセレクターを作成すること。

---

アクセシビリティを向上させること。

#### --answer--

再利用可能なスタイルを定義すること。

### --question--

#### --text--

`:root`セレクターは何を表していますか？

#### --distractors--

`body`の最初の子要素。

---

すべての`header`要素の親要素。

---

`html`タグの最初の子。

#### --answer--

DOMツリーの最上位の要素。

### --question--

#### --text--

CSSで`--foreground`カスタムプロパティを`color`として適用するにはどうしますか？

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

CSSの`@property`ルールの目的は何ですか？

#### --distractors--

メディアクエリを定義すること。

---

CSSアニメーションとそのタイミングを制御すること。

---

CSSプロパティを1つのルールにまとめること。

#### --answer--

カスタムプロパティの動作を定義すること。

### --question--

#### --text--

カスタム`@property`を宣言するとき、その定義内の`syntax`の目的は何ですか？

#### --distractors--

カスタムプロパティのデフォルト値を指定すること。

---

子要素に継承されるかどうかを決めること。

---

すべての要素に適用されるかどうかを定義すること。

#### --answer--

カスタムプロパティに特定のデータ型や値のパターンを強制すること。

### --question--

#### --text--

カスタムプロパティを使う際に注意すべきことは何ですか？

#### --distractors--

カスタムプロパティは自動的に`px`単位に変換される。

---

カスタムプロパティは常にインラインスタイルをオーバーライドする。

---

カスタムプロパティはフォント値の設定に使えない。

#### --answer--

古いブラウザではカスタムプロパティがサポートされていない場合がある。

### --question--

#### --text--

CSSカスタムプロパティの`var()`関数でフォールバック値を提供する目的は何ですか？

#### --distractors--

CSSコードの量を減らすこと。

---

最新ブラウザでのパフォーマンスを向上させること。

---

遅いネットワークでのレンダリング時間を最適化すること。

#### --answer--

カスタムプロパティが未定義の場合に有効な値を適用するため。

### --question--

#### --text--

メディアクエリ内で変数`--foreground`を定義した場合、メディアクエリが現在のビューポートに合致しなくなるとどうなりますか？

#### --distractors--

カスタムプロパティは保持されて適用され続ける。

---

カスタムプロパティは初期値に戻る。

---

カスタムプロパティはビューポートに基づいて再計算される。

#### --answer--

カスタムプロパティはメディアクエリのスコープ内のため利用できなくなる。

### --question--

#### --text--

次の宣言はどのように動作しますか？

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

定義されていなくても`--main-color`を適用し、その値をデフォルトとする。

---

`--main-color`と`--fallback-color`を同時に適用し、2つの色が混ざる。

---

`#000`や`--main-color`が定義されていても常に`--fallback-color`をデフォルトとする。

#### --answer--

`--main-color`が定義されていれば適用し、なければ`--fallback-color;`をチェックし、両方未定義なら`#000`を使う。

### --question--

#### --text--

CSSで`@property`を使う利点は何ですか？

#### --distractors--

カスタムプロパティの値を事前計算してパフォーマンスを向上させる。

---

ブラウザ対応のためにカスタムプロパティに自動で接頭辞を付ける。

---

カスタムプロパティの使用を特定の要素に制限する。

#### --answer--

カスタムプロパティのアニメーションを可能にする。

### --question--

#### --text--

カスタム`inherits`宣言の`@property`プロパティは何を制御しますか？

#### --distractors--

カスタムプロパティに初期値があるかどうか。

---

プロパティにショートハンド版があるかどうか。

---

プロパティがフォールバック値を受け入れるかどうか。

#### --answer--

カスタムプロパティの値が子要素に渡されるかどうか。

### --question--

#### --text--

カスタム`@property`の宣言で`initial-value`は何を指定しますか？

#### --distractors--

プロパティが受け入れられる値の範囲。

---

カスケードにおけるプロパティの優先度。

---

プロパティが持つべき値の型。

#### --answer--

プロパティのフォールバック値。

### --question--

#### --text--

次のHTMLとCSSコードにおいて、`color`要素の`.box`プロパティの値は何になりますか？

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

CSSグラデーションはどのプロパティに適用すべきですか？

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

CSSグラデーションのカラーストップの目的は何ですか？

#### --distractors--

グラデーションの透明度レベルを定義すること。

---

グラデーションの方向を指定すること。

---

グラデーションを固定パターンで繰り返すこと。

#### --answer--

グラデーション内で色が変わる特定のポイントを定義すること。

### --question--

#### --text--

CSS線形グラデーションで角度や方向が指定されていない場合はどうなりますか？

#### --distractors--

グラデーションは斜め方向がデフォルトになる。

---

グラデーションは下から上に移動するのがデフォルトになる。

---

グラデーションは左から右に移動するのがデフォルトになる。

#### --answer--

グラデーションは上から下に移動するのがデフォルトになる。

### --question--

#### --text--

中心点から放射状に広がるグラデーションを作成できるCSSグラデーション関数はどれですか？

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

CSSカスタムプロパティを宣言する正しい方法はどれですか？

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

グローバルなCSSカスタムプロパティを定義するために通常使われるセレクターはどれですか？

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

`var()`を使う際にフォールバック値を含めることが推奨される理由は何ですか？

#### --distractors--

変数が正しくアニメーションすることを保証するため。

---

外部スタイルシートの読み込みを避けるため。

---

ブラウザのリフローを防ぐため。

#### --answer--

カスタムプロパティが未定義の場合に有効な値を保証するため。

### --question--

#### --text--

カスタムプロパティを背景色として適用する正しい構文はどれですか？

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

開発者がカスタムプロパティの動作をより細かく制御できるCSSルールはどれですか？

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

`inherits`定義の`@property`フィールドは何を制御しますか？

#### --distractors--

プロパティがJavaScriptで使われるかどうか。

---

プロパティが再描画を引き起こすかどうか。

---

プロパティが関数を含めるかどうか。

#### --answer--

プロパティの値が子要素に渡されるかどうか。

### --question--

#### --text--

`@property`を使ってアニメーションするグラデーション角度を定義するとき、どの構文を使うべきですか？

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

CSSカスタムプロパティが標準のCSSプロパティと定義方法で異なる点は何ですか？

#### --distractors--

標準のCSSプロパティは1つのダッシュで定義され、カスタムプロパティは2つのダッシュを使う。

---

標準のCSSプロパティは`var()`を使って定義され、カスタムプロパティは使わない。

---

標準のCSSプロパティは`@property`ルールで定義され、カスタムプロパティはそうではない。

#### --answer--

カスタムプロパティは2つのダッシュ（`--`）で始まる必要があり、標準のCSSプロパティはそうではない。

### --question--

#### --text--

CSSカスタムプロパティを使う利点はどれですか？

#### --distractors--

CSSコメントの必要性を減らす。

---

画像を自動的に最適化する。

---

JavaScriptの変数を不要にする。

#### --answer--

スタイルを再利用可能かつ一元管理できる。

### --question--

#### --text--

メディアクエリを使って動的に変わるCSSプロパティはどれですか？

#### --distractors--

`background-color`のみ。

---

`font-family`のみ。

---

`z-index`のみ。

#### --answer--

値を受け入れるすべてのプロパティ。

### --question--

#### --text--

CSS変数の`initial-value`ルールにおける`@property`の役割は何ですか？

#### --distractors--

プロパティの最小値を設定する。

---

アニメーションの最大値を定義する。

---

セレクターの優先度を変更する。

#### --answer--

プロパティに値が設定されていない場合のデフォルト値を割り当てる。

### --question--

#### --text--

次のHTMLとCSSで、`.card`要素に適用される背景色は何ですか？

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

次のCSSで、ユーザーが`--padding`に無効な値（例えば色ではなく長さ）を代入しようとした場合どうなりますか？

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

ブラウザは値を受け入れるがレイアウト時に無視する。

---

ブラウザは実行時エラーをスローする。

---

ブラウザは値を自動的に有効な長さに変換する。

#### --answer--

ブラウザはプロパティの初期値にフォールバックする。

### --question--

#### --text--

この例の`--gradient-angle`カスタムプロパティの目的は何ですか？

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

グラデーションの遷移速度を定義する。

---

グラデーションのサイズを指定する。

---

色のブレンドモードを設定する。

#### --answer--

グラデーションの方向を制御する。

### --question--

#### --text--

テーマにカスタムプロパティを使う利点を最もよく示すシナリオはどれですか？

#### --distractors--

フォントサイズだけに使う。

---

`:hover`にのみ適用する。

---

1つのCSSクラスに制限する。

#### --answer--

クラスを切り替えて値を変更する。

### --question--

#### --text--

開発者がCSS変数を定義する際に`:root`を好む理由は何ですか？

#### --distractors--

`:root`がページの読み込み速度を向上させるため。

---

`:root`が特異性を無効にするため。

---

`:root`がJavaScriptに必要なため。

#### --answer--

`:root`がプロパティをグローバルにスコープできるため。

### --question--

#### --text--

`--secondary-color`が未定義の場合、次のCSSはどうなりますか？

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

`black`を適用する。

---

`color`プロパティを無視する。

---

`--secondary-color`を適用する。

#### --answer--

`orange`を適用する。

### --question--

#### --text--

`<color>`の構文フィールドで`@property`はどのような値を表しますか？

#### --distractors--

`90deg`のような角度。

---

`10px`のような長さ。

---

`50%`のようなパーセンテージ。

#### --answer--

`#ff0000`や`red`のような色の値。

### --question--

#### --text--

`@property`と組み合わせてカスタムプロパティを使う主な利点は何ですか？

#### --distractors--

フォールバック値が不要になる。

---

静的レイアウトを強制する。

---

クラスの必要性を減らす。

#### --answer--

プロパティのアニメーションを可能にする。

### --question--

#### --text--

カスタムプロパティが未定義の場合、`var(--undefined-property, fallback)`は何をしますか？

#### --distractors--

エラーをスローしてスタイル適用を停止する。

---

CSSルール全体を無視する。

---

変数名をそのまま適用する。

#### --answer--

代わりにフォールバック値を適用する。
