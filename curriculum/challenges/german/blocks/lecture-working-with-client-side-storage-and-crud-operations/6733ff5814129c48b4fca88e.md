---
id: 6733ff5814129c48b4fca88e
title: Was ist sessionStorage und welche gängigen Methoden gibt es?
challengeType: 19
dashedName: what-is-sessionstorage-and-what-are-some-common-methods
---

# --description--

In der vorherigen Lektion haben wir gelernt, wie man mit `localStorage` arbeitet, und wurden kurz in `sessionStorage` eingeführt.

Erinnern Sie sich, dass `sessionStorage` bedeutet, dass die Daten gelöscht werden, sobald der Benutzer den Tab oder das Fenster schließt, in dem die Webanwendung läuft. Es ist ideal für Situationen, in denen Daten nur für die Dauer einer einzelnen Sitzung erhalten bleiben müssen, wie zum Beispiel das Speichern von Formulardaten während der Navigation oder das Zwischenspeichern von temporären Zustandsinformationen während eines Bestellvorgangs.

Ähnlich wie `localStorage` verwendet `sessionStorage` Schlüssel-Wert-Paare, um Daten zu speichern und abzurufen. Die Methoden, die mit `sessionStorage` verwendet werden, sind auch dieselben wie bei `localStorage`, wobei der einzige wirkliche Unterschied darin besteht, wie lange die Daten gespeichert werden.

Hier sind einige Beispiele für die Arbeit mit den verschiedenen Methoden:

- `sessionStorage.setItem()`: Speichert ein Schlüssel-Wert-Paar in `sessionStorage`.

```js
sessionStorage.setItem('cart', '3 items');
```

- `sessionStorage.getItem()`: Ruft den Wert eines bestimmten Schlüssels aus `sessionStorage` ab.

```js
let cart = sessionStorage.getItem('cart');
console.log(cart); // Outputs: '3 items'
```

- `sessionStorage.removeItem()`: Entfernt einen bestimmten Eintrag aus `sessionStorage` anhand seines Schlüssels.

```js
sessionStorage.removeItem('cart');
```

- `sessionStorage.clear()`: Löscht alle in `sessionStorage` gespeicherten Daten.

```js
sessionStorage.clear();
```

Schauen wir uns ein Beispiel an, bei dem wir Daten in `sessionStorage` speichern, die nur so lange bestehen bleiben, wie der Browser-Tab oder das Fenster geöffnet ist:

```js
// Store data in sessionStorage
sessionStorage.setItem('currentUser', 'JohnDoe');

// Retrieve the stored data
const user = sessionStorage.getItem('currentUser');
console.log(user); // 'JohnDoe'

// Remove a specific key from sessionStorage
sessionStorage.removeItem('currentUser');

// Clear all sessionStorage data
sessionStorage.clear();
```

In diesem Beispiel:

1. Speichern wir den Namen des aktuellen Benutzers (`JohnDoe`) in `sessionStorage`.

2. Rufen ihn ab und zeigen ihn an.

3. Entfernen den Eintrag mit dem Schlüssel `currentUser`.

4. Löschen alle `sessionStorage`-Daten.

Der entscheidende Unterschied zu `localStorage` ist, dass alle gespeicherten Sitzungsdaten verloren gehen, sobald der Benutzer den Tab schließt.

`sessionStorage` ist besonders nützlich in Szenarien wie:

- Temporäres Speichern von Daten wie Formulareinträgen während eines mehrseitigen Formularprozesses.

- Temporäres Speichern von Auswahlen oder Einstellungen, die nicht über Sitzungen hinweg erhalten bleiben müssen.

- Aufrechterhalten des Zustands in einer Single-Page-Anwendung, der nicht gespeichert werden muss, wenn der Tab geschlossen wird.

`sessionStorage` stellt sicher, dass die Sitzungsdaten gelöscht werden, sobald der Benutzer die Seite verlässt, was ideal für Situationen ist, in denen Informationen nicht über die aktuelle Sitzung hinaus behalten werden sollen.

# --questions--

## --text--

Was passiert mit den in `sessionStorage` gespeicherten Daten, wenn der Browser-Tab geschlossen wird?

## --answers--

Die Daten werden dauerhaft gespeichert, bis sie manuell gelöscht werden.

### --feedback--

`sessionStorage` besteht nur für die Dauer einer Browsersitzung.

---

Die Daten bleiben erhalten, bis der Computer ausgeschaltet wird.

### --feedback--

`sessionStorage` besteht nur für die Dauer einer Browsersitzung.

---

Die Daten werden gelöscht, wenn der Tab oder das Fenster geschlossen wird.

---

Die Daten werden mit `localStorage` synchronisiert.

### --feedback--

`sessionStorage` besteht nur für die Dauer einer Browsersitzung.

## --video-solution--

3

## --text--

Welche der folgenden Methoden wird verwendet, um ein einzelnes Schlüssel-Wert-Paar aus `sessionStorage` zu entfernen?

## --answers--

`sessionStorage.removeItem()`

---

`sessionStorage.deleteItem()`

### --feedback--

Der Methodenname beginnt mit `remove` und betrifft einen bestimmten Eintrag.

---

`sessionStorage.clear()`

### --feedback--

Der Methodenname beginnt mit `remove` und betrifft einen bestimmten Eintrag.

---

`sessionStorage.unset()`

### --feedback--

Der Methodenname beginnt mit `remove` und betrifft einen bestimmten Eintrag.

## --video-solution--

1

## --text--

In welchem der folgenden Szenarien wäre `sessionStorage` geeigneter als `localStorage`?

## --answers--

Speichern von Benutzereinstellungen wie Themes, die über Sitzungen hinweg erhalten bleiben.

### --feedback--

Überlegen Sie, welche Daten nur für eine einzelne Sitzung erhalten bleiben müssen.

---

Speichern von Formulardaten, die nur während der aktuellen Browsersitzung benötigt werden.

---

Speichern sensibler Benutzerdaten wie Passwörter.

### --feedback--

Überlegen Sie, welche Daten nur für eine einzelne Sitzung erhalten bleiben müssen.

---

Speichern von Daten, die zwischen verschiedenen Browserfenstern geteilt werden müssen.

### --feedback--

Überlegen Sie, welche Daten nur für eine einzelne Sitzung erhalten bleiben müssen.

## --video-solution--

2
