---
id: 69a98755ee5508d58e91dccc
title: Quiz su npm
challengeType: 8
dashedName: quiz-npm
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cosa offre npm agli sviluppatori?

#### --distractors--

Una piattaforma selezionata che ospita solo risorse di design come caratteri tipografici e icone.

---

Un servizio di hosting che distribuisce automaticamente il tuo progetto sul cloud.

---

Una soluzione database che gestisce i record degli utenti per le tue app.

#### --answer--

Un archivio di pacchetti software precompilati che risolve problemi comuni.

### --question--

#### --text--

Quale delle seguenti non è una delle tre parti a cui si riferisce npm?

#### --distractors--

Il sito web npmjs.com.

---

L’archivio dei pacchetti dove sono conservate le librerie.

---

Lo strumento da riga di comando per installare e gestire i pacchetti.

#### --answer--

Il runtime Node.js.

### --question--

#### --text--

Cosa fa il comando `npm install`?

#### --distractors--

Crea un nuovo progetto Node.js da un modello.

---

Esegue la tua applicazione Node.js su un server di sviluppo locale.

---

Rimuove i pacchetti che non servono più nel progetto.

#### --answer--

Aggiunge pacchetti precompilati al progetto per attività comuni.

### --question--

#### --text--

Quale file conserva i metadati come nome del progetto, versione, autore e dipendenze per un progetto Node.js?

#### --distractors--

`README.md`.

---

`node_modules/package.json`.

---

`package-lock.json`.

#### --answer--

`package.json`.

### --question--

#### --text--

Come puoi creare in modo interattivo un file `package.json`?

#### --distractors--

Eseguendo `npm install` senza specificare il nome del pacchetto.

---

Scaricando un modello dalla homepage dell’archivio npm.

---

Copiando il file da un altro progetto e rinominandolo.

#### --answer--

Eseguendo il comando `npm init`.

### --question--

#### --text--

Come crei un file `package.json` con valori predefiniti senza rispondere alle domande?

#### --distractors--

Eseguendo `npm init --interactive`.

---

Creando un file vuoto chiamato `package.json` e lasciandolo vuoto.

---

Usando `npm init` ed eliminando ogni domanda dopo aver aperto l’editor.

#### --answer--

Eseguendo `npm init -y` (o `npm init --yes`) per accettare automaticamente tutti i valori predefiniti.

### --question--

#### --text--

Dove elenca npm i pacchetti necessari solo durante lo sviluppo, come linters o test runner?

#### --distractors--

Nella sezione `"dependencies"`.

---

Nella sezione `"scripts"` sotto comandi personalizzati.

---

All’interno della cartella `node_modules` stessa.

#### --answer--

Nella sezione `"devDependencies"` di `package.json`.

### --question--

#### --text--

Quale comando installa `nodemon` come dipendenza solo per lo sviluppo?

#### --distractors--

`npm uninstall nodemon`.

---

`npm save nodemon --dev`.

---

`npm install nodemon`.

#### --answer--

`npm install nodemon -D`.

### --question--

#### --text--

Quale cartella e file vengono aggiunti alla radice del progetto dopo aver installato una dipendenza?

#### --distractors--

Cartella `public` e `index.html`.

---

Cartella `dist` e `webpack.config.js`.

---

Cartella `src` e `README.md`.

#### --answer--

Cartella `node_modules` e file `package-lock.json`.

### --question--

#### --text--

Come aiuta `package-lock.json` a mantenere coerenti le build?

#### --distractors--

Eliminando le dipendenze inutilizzate prima di ogni installazione.

---

Conservando solo i numeri di versione principali dei pacchetti.

---

Aggiornando automaticamente i pacchetti alle ultime versioni.

#### --answer--

Bloccando le versioni esatte di ogni dipendenza installata.

### --question--

#### --text--

Perché dovresti includere `package-lock.json` nel tuo repository Git?

#### --distractors--

Per far funzionare npm più velocemente sulla macchina dello sviluppatore.

---

Per aggiornare automaticamente le dipendenze obsolete.

---

Per ridurre la dimensione del repository.

#### --answer--

Così tutto il team installa le stesse versioni e configurazioni delle dipendenze.

### --question--

#### --text--

Come installi una versione specifica di un pacchetto, come `express@4.21.2`?

#### --distractors--

`npm update express@4.21.2`.

---

`npm get express@4.21.2`.

---

`npm use express@4.21.2`.

#### --answer--

`npm install express@4.21.2`.

### --question--

#### --text--

Cosa permette il simbolo caret (`^`) quando si specificano le versioni delle dipendenze?

#### --distractors--

Blocca la dipendenza a una versione patch esatta.

---

Permette aggiornamenti solo alla versione principale.

---

Esclude tutte le versioni inferiori a quella specificata.

#### --answer--

Permette versioni minor e patch più recenti ma blocca nuove versioni principali.

### --question--

#### --text--

Cosa significa il simbolo tilde (`~`) in un intervallo di versioni come `~1.2.3`?

#### --distractors--

Permette aggiornamenti sia alle versioni principali che minor.

---

Installa la versione principale più recente indipendentemente dall’intervallo.

---

Impedisce a npm di installare qualsiasi aggiornamento.

#### --answer--

Permette aggiornamenti solo alla versione patch mantenendo fissa la minor.

### --question--

#### --text--

Che tipo di cambiamenti causano un aumento della versione principale nel versionamento semantico?

#### --distractors--

Piccole modifiche stilistiche a commenti e formattazione.

---

Piccole correzioni di bug che non cambiano il comportamento.

---

Funzionalità opzionali che non rompono il codice esistente.

#### --answer--

Cambiamenti che rompono la compatibilità e possono far smettere di funzionare il codice esistente.

### --question--

#### --text--

Quale comando elenca le dipendenze che hanno versioni pubblicate più recenti?

#### --distractors--

`npm check`.

---

`npm list`.

---

`npm refresh`.

#### --answer--

`npm outdated`.

### --question--

#### --text--

Yarn, PNPM e Bun sono citati come alternative a npm. Che tipo di strumenti sono?

#### --distractors--

Siti di archivio dove pubblichi o scarichi pacchetti npm.

---

Servizi database che conservano dati utenti per pacchetti npm.

---

Piattaforme di hosting cloud che distribuiscono automaticamente progetti npm.

#### --answer--

Gestori di pacchetti alternativi.

### --question--

#### --text--

Dove si dichiarano gli script npm personalizzati in un progetto?

#### --distractors--

Nel file `.npmrc`.

---

Nella sezione `"dependencies"` di `package.json`.

---

In un file `scripts.json` separato nella radice.

#### --answer--

Nell’oggetto `"scripts"` di `package.json`.

### --question--

#### --text--

Come esegui uno script npm personalizzato chiamato `build`?

#### --distractors--

`build npm`.

---

`node build`.

---

`run build`.

#### --answer--

`npm run build`.

### --question--

#### --text--

Cosa fa il doppio trattino (`--`) quando esegui `npm run start -- 8000`?

#### --distractors--

Silenzia l’output dello script.

---

Forza npm a eseguire lo script in parallelo.

---

Richiede privilegi amministrativi prima di eseguire lo script.

#### --answer--

Dice a npm di passare tutto ciò che segue `--` come argomenti al comando sottostante.
