---
id: 672a54f29d783890d1f94740
title: Які загальні ARIA стани використовуються на кастомних елементах керування?
challengeType: 19
dashedName: what-are-some-common-aria-states-used-on-custom-control-elements
---

# --interactive--

Семантичні елементи керування формою, такі як `input`, `select`, `textarea`, `button` і `fieldset`, мають вбудовані стани, які передаються допоміжним технологіям.

Наприклад, ви можете використати атрибут `disabled`, щоб відключити кнопку, або атрибут `checked`, щоб вказати, що прапорець встановлено.

Але якщо ви створюєте кастомний елемент керування, потрібно використовувати ARIA атрибути, щоб передати стан керування допоміжним технологіям.

У цьому уроці ми розглянемо кілька загальних ARIA станів, які можна використовувати на кастомних елементах керування.

Перший ARIA стан, який ми розглянемо, — це `aria-selected`. Цей стан використовується, щоб вказати, що елемент вибрано. Ви можете застосовувати цей стан на кастомних керуваннях, таких як вкладковий інтерфейс, список або сітка.

Ось приклад, як можна використати `aria-selected` на кастомному вкладковому керуванні:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
<script src="index.js"></script>
```

```css
[role="tablist"] {
  display: flex;
  border-bottom: 2px solid #ddd;
  gap: 0.25rem;
  font-family: system-ui, sans-serif;
}

[role="tab"] {
  appearance: none;
  border: none;
  background: none;
  padding: 0.5rem 1rem;
  cursor: pointer;
  font-size: 1rem;
  color: #444;
  border-radius: 4px 4px 0 0;
  transition: background-color 0.2s, color 0.2s;
}

[role="tab"]:hover {
  background-color: #f3f3f3;
}

[role="tab"][aria-selected="true"] {
  background-color: #fff;
  color: #0078d4;
  border: 2px solid #0078d4;
  border-bottom: 2px solid #fff; 
  font-weight: 600;
  position: relative;
  z-index: 1;
}

[role="tab"]:focus {
  outline: 2px solid #0078d4;
  outline-offset: 2px;
}

```

```js
document.addEventListener("click", (event) => {
  const clickedTab = event.target.closest('[role="tab"]');
  if (!clickedTab) return;

  const tablist = clickedTab.closest('[role="tablist"]');
  const tabs = tablist.querySelectorAll('[role="tab"]');

  tabs.forEach((tab) => {
    const isSelected = tab === clickedTab;
    tab.setAttribute("aria-selected", isSelected);
    tab.tabIndex = isSelected ? 0 : -1;
  });
});

document.addEventListener("keydown", (event) => {
  const activeTab = document.activeElement;
  if (activeTab.getAttribute("role") !== "tab") return;

  const tablist = activeTab.closest('[role="tablist"]');
  const tabs = Array.from(tablist.querySelectorAll('[role="tab"]'));
  const index = tabs.indexOf(activeTab);

  let newIndex = index;
  if (event.key === "ArrowRight") newIndex = (index + 1) % tabs.length;
  if (event.key === "ArrowLeft") newIndex = (index - 1 + tabs.length) % tabs.length;

  if (newIndex !== index) {
    tabs[newIndex].focus();
    tabs[newIndex].click();
  }
});

```

:::

Вкладки використовують для відображення кількох панелей вмісту в обмеженому просторі. Стан `aria-selected` використовується, щоб вказати, яка вкладка наразі вибрана.

Коли користувач вибирає вкладку, стан `aria-selected` вибраної вкладки встановлюється в `true`, а стан `aria-selected` інших вкладок встановлюється в `false`.

Ще один загальний ARIA стан — це `aria-disabled`. Цей стан використовується, щоб вказати, що елемент вимкнено лише для користувачів допоміжних технологій, таких як читачі екрана. Важливо зазначити, що `aria-disabled` фактично не відключає елемент. Це залежить від вас, розробника, зробити так, щоб елемент виглядав і поводився як відключений. Цей атрибут також часто використовується на нативних HTML елементах замість атрибута `disabled`. Який саме обрати — залежить від контексту використання кнопки.

Ось приклад, як можна використати `aria-disabled` на кастомній кнопці редагування:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

```css
[role="button"] {
  display: inline-block;
  background-color: #0078d4;
  color: #fff;
  padding: 0.5rem 1rem;
  border-radius: 6px;
  font-family: system-ui, sans-serif;
  font-size: 1rem;
  text-align: center;
  cursor: pointer;
  user-select: none;
  transition: background-color 0.2s, transform 0.1s, opacity 0.2s;
}

[role="button"]:not([aria-disabled="true"]):hover {
  background-color: #005fa3;
}

[role="button"]:not([aria-disabled="true"]):focus {
  outline: 2px solid #005fa3;
  outline-offset: 2px;
}

[role="button"]:not([aria-disabled="true"]):active {
  transform: scale(0.97);
}

[role="button"][aria-disabled="true"] {
  opacity: 0.5;
  pointer-events: none;
  cursor: not-allowed;
  background-color: #b0b0b0;
  color: #f2f2f2;
}
```

:::

Атрибут `aria-disabled` повідомляє користувачам читачів екрана, що кнопка редагування відключена і з нею не можна взаємодіяти. Знову ж таки, він фактично не відключає кнопку. При використанні `aria-disabled` потрібно застосувати стилі та JavaScript, щоб керування виглядало і поводилося як відключена кнопка.

У більшості випадків ви, ймовірно, використовуватимете нативний елемент button, але бувають випадки, коли потрібен кастомний контрол. Тому важливо знати, як передати стан керування допоміжним технологіям.

Наступний ARIA стан, який ми розглянемо, — це `aria-haspopup`. Цей стан використовується, щоб вказати, що інтерактивний елемент при активації відкриє спливаючий елемент. Атрибут `aria-haspopup` можна використовувати лише тоді, коли спливаючий елемент має одну з таких ролей: `menu`, `listbox`, `tree`, `grid` або `dialog`. Значення `aria-haspopup` має бути однією з цих ролей або `true`, що за замовчуванням відповідає ролі `menu`.

Ось приклад меню редактора файлів, що використовує `aria-haspopup`:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="menubutton" aria-haspopup="menu" aria-controls="filemenu" aria-expanded="false">File</button>
<ul id="filemenu" role="menu" aria-labelledby="menubutton" hidden>
  <li role="menuitem" tabindex="-1">Open</li>
  <li role="menuitem" tabindex="-1">New</li>
  <li role="menuitem" tabindex="-1">Save</li>
  <li role="menuitem" tabindex="-1">Delete</li>
</ul>
```

```css
#menubutton {
  background-color: #0078d4;
  color: #fff;
  border: none;
  padding: 8px 14px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
  position: relative;
}

#menubutton:hover,
#menubutton:focus {
  background-color: #005ea2;
  outline: none;
}

#filemenu {
  list-style: none;
  padding: 4px 0;
  margin: 4px 0 0;
  border: 1px solid #ccc;
  border-radius: 4px;
  background-color: #fff;
  width: 160px;
  position: absolute;
  z-index: 1000;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}

#filemenu[hidden] {
  display: none;
}

#filemenu [role="menuitem"] {
  display: block;
  padding: 8px 12px;
  font-size: 14px;
  color: #333;
  cursor: pointer;
}

#filemenu [role="menuitem"]:hover,
#filemenu [role="menuitem"]:focus {
  background-color: #e5f1fb;
  outline: none;
}

#filemenu [role="menuitem"]:focus-visible {
  box-shadow: inset 0 0 0 2px #0078d4;
}

```

:::

Стан `aria-haspopup` вказує, що кнопка меню `File` відкриє спливаюче меню при активації. Користувачі читачів екрана можуть почути цю додаткову інформацію, коли переходять до кнопки.

Вам потрібно буде використати JavaScript, щоб показувати і приховувати спливаюче меню, а також реалізувати правильну підтримку клавіатури для взаємодії з меню. Також зверніть увагу, що ARIA роль `menu` стосується дуже специфічного типу меню. Зазвичай це список дій, які користувач може викликати, подібно до меню в настільному застосунку. Вона не включає більш загальні випадки, які ми зазвичай називаємо «меню», наприклад навігаційні меню. Реалістично, більшість «меню», які ви створюєте в інтернеті, не будуть ARIA меню, і ви не використовуватимете `aria-haspopup` з ними.

Наступний ARIA стан, який ми розглянемо, — це `aria-required`. Атрибут `aria-required` використовується, щоб вказати, що поле потрібно заповнити перед відправленням форми.

Ось приклад роботи з атрибутом `aria-required` для кастомного елемента керування формою.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div id="name-label">Full Name*</div>
<div role="textbox" contenteditable aria-labelledby="name-label" aria-required="true" id="name"></div>
```

```css
#name-label {
  font-family: system-ui, sans-serif;
  font-size: 0.95rem;
  font-weight: 600;
  margin-bottom: 0.25rem;
  color: #333;
}

[role="textbox"] {
  display: block;
  width: 100%;
  min-height: 2rem;
  padding: 0.5rem 0.75rem;
  border: 1.5px solid #ccc;
  border-radius: 4px;
  font-family: system-ui, sans-serif;
  font-size: 1rem;
  color: #222;
  background-color: #fff;
  line-height: 1.4;
  transition: border-color 0.2s, box-shadow 0.2s;
}

[role="textbox"]:hover {
  border-color: #999;
}

[role="textbox"]:focus {
  outline: none;
  border-color: #0078d4;
  box-shadow: 0 0 0 3px rgba(0, 120, 212, 0.25);
}

[role="textbox"]:empty::before {
  content: attr(data-placeholder);
  color: #aaa;
  pointer-events: none;
}

[role="textbox"][aria-required="true"] {
  border-left: 3px solid #e81123;
  padding-left: calc(0.75rem - 3px);
}
```

:::

Нам потрібно використати атрибут `contenteditable`, щоб користувачі могли вводити свої дані. Ми також використовуємо атрибут `aria-required`, встановлений у `true`, щоб вказати, що цей кастомний елемент керування формою є обов’язковим.

Щоб зробити елемент керування формою схожим на звичайний, потрібно додати CSS. Також потрібно додати JavaScript, щоб запобігти відправленню форми без вмісту.

Якщо мітка вже містить слово `required`, тоді слід опустити атрибут `aria-required`. Це гарантує, що читачі екрана оголошують слово «обов’язковий» лише один раз.

У більшості випадків ви, ймовірно, використовуватимете нативні елементи `label` і `form` з атрибутом `required`. Але якщо потрібно створити кастомний елемент керування формою, важливо додати атрибут `aria-required`, коли це необхідно.

Крім того, атрибут `aria-required` також можна використовувати на нативних вхідних елементах форми, таких як `input`, `textarea` і `select`. Це часто переважніше за нативний атрибут required, оскільки required може мати потенційні проблеми з юзабіліті та доступністю, особливо з типовою обробкою помилок браузером. Врешті-решт, вам потрібно тестувати, щоб визначити, який атрибут найкращий для вашої ситуації.

Останній ARIA стан, який ми розглянемо, — це `aria-checked`. Цей атрибут використовується, щоб вказати, чи елемент перебуває у стані вибрано. Найчастіше його застосовують при створенні кастомних прапорців, радіокнопок, перемикачів і списків.

Ось приклад, як можна використати `aria-checked` на кастомному прапорці:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div role="checkbox" aria-checked="true" tabindex="0">Checkbox</div>
```

```css
[role="checkbox"] {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-family: system-ui, sans-serif;
  font-size: 1rem;
  cursor: pointer;
  user-select: none;
  color: #222;
}

[role="checkbox"]::before {
  content: "";
  display: inline-block;
  width: 1rem;
  height: 1rem;
  border: 2px solid #666;
  border-radius: 4px;
  background-color: #fff;
  transition: all 0.2s ease;
  box-sizing: border-box;
}

[role="checkbox"]:hover::before {
  border-color: #0078d4;
}

[role="checkbox"]:focus::before {
  outline: 2px solid #0078d4;
  outline-offset: 2px;
}

[role="checkbox"][aria-checked="true"]::before {
  background-color: #0078d4;
  border-color: #0078d4;
  background-image: url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='14' height='14' viewBox='0 0 14 14'><path fill='white' d='M5.2 10.4L2 7.2l1.1-1.1 2.1 2.1L10.9 2.5 12 3.6z'/></svg>");
  background-repeat: no-repeat;
  background-position: center;
}

[role="checkbox"][aria-disabled="true"] {
  opacity: 0.5;
  pointer-events: none;
  cursor: not-allowed;
}

```

:::

Нативні елементи прапорців мають вбудований стан `checked`, який передається допоміжним технологіям. Але якщо ви створюєте кастомний прапорець, потрібно використовувати атрибут `aria-checked`, щоб вказати його стан.

Коли користувач взаємодіє з кастомним прапорцем, потрібно використовувати стан `aria-checked`, щоб відобразити новий стан прапорця. Коли прапорець встановлено, атрибут `aria-checked` встановлюється в `true`. Коли прапорець знято, він встановлюється в `false`.

Нативні елементи зазвичай мають кращу підтримку та вбудовані функції доступності.

Однак, якщо потрібно створювати кастомні керування, використання ARIA атрибутів є необхідним для ефективної передачі стану цих керувань допоміжним технологіям.

Як завжди, тестуйте свою роботу, щоб переконатися, що ARIA атрибути застосовані правильно і кастомний контрол працює доступно та зручно для користувача.

# --questions--

## --text--

Який ARIA стан ви б використали, щоб вказати, що вкладка наразі вибрана?

## --answers--

`aria-disabled`

### --feedback--

Цей стан показує, яка вкладка активна.

---

`aria-selected`

---

`aria-haspopup`

### --feedback--

Цей стан показує, яка вкладка активна.

---

`aria-checked`

### --feedback--

Цей стан показує, яка вкладка активна.

## --video-solution--

2

## --text--

У контексті кастомних керувань, який ARIA стан використовується, щоб вказати, що елемент наразі відключений?

## --answers--

`aria-checked`

### --feedback--

Цей стан показує, що з елементом не можна взаємодіяти.

---

`aria-disabled`

---

`aria-selected`

### --feedback--

Цей стан показує, що з елементом не можна взаємодіяти.

---

`aria-haspopup`

### --feedback--

Цей стан показує, що з елементом не можна взаємодіяти.

## --video-solution--

2

## --text--

Коли ви хочете вказати, що пункт меню має підменю, який ARIA стан слід використовувати?

## --answers--

`aria-checked`

### --feedback--

Один зі станів сильно вказує на наявність спливаючого вікна.

---

`aria-disabled`

### --feedback--

Один зі станів сильно вказує на наявність спливаючого вікна.

---

`aria-haspopup`

---

`aria-selected`

### --feedback--

Один зі станів сильно вказує на наявність спливаючого вікна.

## --video-solution--

3
