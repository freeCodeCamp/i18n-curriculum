---
id: 5900f37f1000cf542c50fe92
title: 'Aufgabe 19: Sonntage zählen'
challengeType: 1
forumTopicId: 301827
dashedName: problem-19-counting-sundays
---

# --description--

Sie erhalten die folgenden Informationen, aber vielleicht möchten Sie selbst noch etwas recherchieren.

<ul>
  <li>Der 1. Januar 1900 war ein Montag.</li>
  <li>September, April, Juni und November haben dreißig Tage.<br>Alle anderen haben einunddreißig,<br>Nur der Februar allein nicht,<br>Der hat achtundzwanzig, bei Regen oder Sonnenschein.<br>Und in Schaltjahren neunundzwanzig.</li>
  <li>Ein Schaltjahr tritt in jedem Jahr auf, das durch 4 teilbar ist, aber nicht in einem Jahrhundertjahr, es sei denn, es ist durch 400 teilbar.</li>
</ul>

Wie viele Sonntage fielen im zwanzigsten Jahrhundert (1. Januar 1901 bis 31. Dezember 2000) auf den ersten Tag des Monats?

# --hints--

`countingSundays(1943, 1946)` sollte eine Zahl zurückgeben.

```js
assert.isNumber(countingSundays(1943, 1946));
```

`countingSundays(1943, 1946)` sollte 6 zurückgeben.

```js
assert.strictEqual(countingSundays(1943, 1946), 6);
```

`countingSundays(1995, 2000)` sollte 10 zurückgeben.

```js
assert.strictEqual(countingSundays(1995, 2000), 10);
```

`countingSundays(1901, 2000)` sollte 171 zurückgeben.

```js
assert.strictEqual(countingSundays(1901, 2000), 171);
```

# --seed--

## --seed-contents--

```js
function countingSundays(firstYear, lastYear) {

  return true;
}

countingSundays(1943, 1946);
```

# --solutions--

```js
function countingSundays(firstYear, lastYear) {
  let sundays = 0;

  for (let year = firstYear; year <= lastYear; year++) {
    for (let month = 0; month <= 11; month++) {
      const thisDate = new Date(year, month, 1);
      if (thisDate.getDay() === 0) {
        sundays++;
      }
    }
  }
  return sundays;
}
```
