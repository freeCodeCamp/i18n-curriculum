---
id: 5e46f8dcac417301a38fb92e
title: مصنف صور القطط والكلاب
challengeType: 10
forumTopicId: 462377
dashedName: cat-and-dog-image-classifier
---

# --description--

ستعمل <a href="https://colab.research.google.com/github/freeCodeCamp/boilerplate-cat-and-dog-image-classifier/blob/master/fcc_cat_dog.ipynb" target="_blank" rel="noopener noreferrer nofollow">على هذا المشروع باستخدام Google Colaboratory</a>.

بعد الدخول إلى ذلك الرابط، أنشئ نسخة من الدفتر إما في حسابك الخاص أو محليًا. بمجرد إكمال المشروع واجتيازه للاختبار (المضمن في ذلك الرابط)، أرسل رابط مشروعك أدناه. إذا كنت ترسل رابط Google Colaboratory، فتأكد من تفعيل مشاركة الرابط لـ"أي شخص لديه الرابط".

ما زلنا نطور المحتوى التعليمي التفاعلي لمنهج تعلم الآلة. في الوقت الحالي، يمكنك متابعة تحديات الفيديو في هذه الشهادة. قد تحتاج أيضًا إلى البحث عن موارد تعلم إضافية، كما تفعل عند العمل على مشروع حقيقي.

# --instructions--

في هذا التحدٍّ، ستكمل الشفرة لتصنيف صور الكلاب والقطط. ستستخدم TensorFlow 2.0 وKeras لإنشاء شبكة عصبية تلافيفية تصنف صور القطط والكلاب بدقة لا تقل عن 63%. (تحصل على نقاط إضافية إذا وصلت إلى دقة 70%!)

بعض الشفرة معطاة لك، لكن عليك ملء بعض الأجزاء لإكمال هذا التحدٍّ. اقرأ التعليمات في كل خلية نصية لتعرف ما يجب عليك فعله في كل خلية شفرة.

تستورد أول خلية شفرة المكتبات المطلوبة. الخلية الثانية تحمل البيانات وتحدد المتغيرات الرئيسية. الخلية الثالثة هي أول مكان تكتب فيه شفرتك الخاصة.

هيكل ملفات مجموعة البيانات التي تم تحميلها يبدو هكذا (ستلاحظ أن مجلد الاختبار لا يحتوي على مجلدات فرعية وأن الصور غير معنونة):

```py
cats_and_dogs
|__ train:
    |______ cats: [cat.0.jpg, cat.1.jpg ...]
    |______ dogs: [dog.0.jpg, dog.1.jpg ...]
|__ validation:
    |______ cats: [cat.2000.jpg, cat.2001.jpg ...]
    |______ dogs: [dog.2000.jpg, dog.2001.jpg ...]
|__ test: [1.jpg, 2.jpg ...]
```

يمكنك تعديل عدد العصور وحجم الدُفعة إذا أردت، لكن هذا ليس ضروريًا.

التعليمات التالية تتوافق مع أرقام خلايا محددة، موضحة بتعليق في أعلى الخلية (مثل `# 3`).

## الخلية 3

حان دورك الآن! عرّف كل المتغيرات في هذه الخلية بشكل صحيح. (يجب ألا تساوي `None` بعد الآن.)

أنشئ مولدات صور لكل من مجموعات بيانات الصور الثلاث (التدريب، التحقق، الاختبار). استخدم `ImageDataGenerator` لقراءة/فك ترميز الصور وتحويلها إلى موترات ذات قيم عشرية. استخدم المعامل `rescale` (دون أي معاملات أخرى الآن) لإعادة تحجيم الموترات من قيم بين 0 و255 إلى قيم بين 0 و1.

بالنسبة لمتغيرات `*_data_gen`، استخدم طريقة `flow_from_directory`. مرر حجم الدُفعة، المجلد، الحجم المستهدف (`(IMG_HEIGHT, IMG_WIDTH)`)، وضع الصنف، وأي شيء آخر مطلوب. ستكون `test_data_gen` الأصعب. بالنسبة لـ `test_data_gen`، تأكد من تمرير `shuffle=False` لطريقة `flow_from_directory`. هذا يضمن بقاء التوقعات النهائية بترتيب يتوقعه اختبارنا. سيكون من المفيد أيضًا ملاحظة هيكل المجلد لـ `test_data_gen`.


بعد تشغيل الشفرة، يجب أن يبدو الناتج هكذا:

```py
Found 2000 images belonging to 2 classes.
Found 1000 images belonging to 2 classes.
Found 50 images belonging to 1 class.
```

## الخلية 4

ستُستخدم الدالة `plotImages` عدة مرات لرسم الصور. تأخذ مصفوفة من الصور وقائمة احتمالات، رغم أن قائمة الاحتمالات اختيارية. هذه الشفرة معطاة لك. إذا أنشأت المتغير `train_data_gen` بشكل صحيح، فسيعرض تشغيل هذه الخلية خمس صور تدريب عشوائية.

## الخلية 5

أعد إنشاء `train_image_generator` باستخدام `ImageDataGenerator`.

بما أن عدد أمثلة التدريب قليل، هناك خطر الإفراط في التخصيص. إحدى الطرق لحل هذه المشكلة هي إنشاء بيانات تدريب إضافية من الأمثلة الحالية باستخدام تحولات عشوائية.

أضف 4-6 تحولات عشوائية كمعاملات إلى `ImageDataGenerator`. تأكد من إعادة التحجيم كما كان من قبل.

## الخلية 6

لا تحتاج إلى فعل أي شيء في هذه الخلية. يُنشأ `train_data_gen` كما كان من قبل لكن باستخدام `train_image_generator` الجديد. ثم تُرسم صورة واحدة خمس مرات مختلفة باستخدام تنويعات مختلفة.

## الخلية 7

في هذه الخلية، أنشئ نموذجًا للشبكة العصبية يُخرج احتمالات الأصناف. يجب أن يستخدم نموذج Keras Sequential. ربما يتضمن تكديسًا من طبقات Conv2D وMaxPooling2D ثم طبقة متصلة بالكامل في الأعلى تُفعّل بواسطة دالة تفعيل ReLU.

جمّع النموذج بتمرير المعاملات لتحديد المحسّن والخسارة. مرر أيضًا `metrics=['accuracy']` لعرض دقة التدريب والتحقق في كل عصر تدريبي.

## الخلية 8

استخدم طريقة `fit` على `model` لتدريب الشبكة. تأكد من تمرير المعاملات لـ `x`، `steps_per_epoch`، `epochs`، `validation_data`، و`validation_steps`.

## الخلية 9

شغّل هذه الخلية لتصور دقة وخسارة النموذج.

## الخلية 10

حان الوقت الآن لاستخدام نموذجك للتنبؤ بما إذا كانت صورة جديدة قطة أم كلب.

في هذه الخلية، احصل على احتمال أن تكون كل صورة اختبار (من `test_data_gen`) كلبًا أو قطة. يجب أن تكون `probabilities` قائمة من الأعداد الصحيحة.

استدعِ دالة `plotImages` ومرر صور الاختبار والاحتمالات المقابلة لكل صورة اختبار.

بعد تشغيل الخلية، سترى جميع صور الاختبار الخمسين مع تسمية تُظهر نسبة "الثقة" بأن الصورة لقطة أو كلب. ستتوافق الدقة مع الدقة المعروضة في الرسم البياني أعلاه (بعد تشغيل الخلية السابقة). قد تؤدي المزيد من صور التدريب إلى دقة أعلى.

## الخلية 11

شغّل هذه الخلية النهائية لترى ما إذا كنت قد اجتزت التحدي أم تحتاج إلى المحاولة مجددًا.

# --hints--

يجب أن يجتاز جميع اختبارات Python.

```js

```

# --solutions--

```py
  # Python challenges don't need solutions,
  # because they would need to be tested against a full working project.
  # Please check our contributing guidelines to learn more.
```
