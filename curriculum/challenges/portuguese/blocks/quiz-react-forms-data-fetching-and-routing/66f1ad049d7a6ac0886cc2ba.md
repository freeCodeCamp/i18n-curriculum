---
id: 66f1ad049d7a6ac0886cc2ba
title: Quiz sobre Formulários React, Busca de Dados e Roteamento
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual dos seguintes é um exemplo de um input controlado?

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

Qual dos seguintes é um exemplo de uma entrada não controlada?

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

Qual das seguintes opções NÃO é uma forma comumente usada para buscar dados em React?

#### --distractors--

SWR

---

Axios

---

API Fetch

#### --answer--

Redux

### --question--

#### --text--

Qual dos seguintes hooks está comumente associado à busca de dados em React?

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

O que é memoization?

#### --distractors--

Um algoritmo comum usado para encontrar o caminho mais curto entre dois vértices em um grafo.

---

Uma técnica especial usada para otimizar o desempenho para todas as entradas controladas e não controladas.

---

Um algoritmo de dividir e conquistar usado para ordenar recursivamente subarrays e mesclá-los juntos.

#### --answer--

Uma técnica de otimização na qual o resultado de chamadas de função caras é armazenado em cache (lembrado) com base em argumentos específicos.

### --question--

#### --text--

Qual dos seguintes hooks é usado para memorizar valores computados?

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

Qual dos seguintes usa o hook correto para memorizar referências de função?

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

O que é uma dependência?

#### --distractors--

Uma dependency é uma biblioteca de testes comumente usada em aplicações React.

---

Uma dependência é um tipo de componente React que só é executado no servidor.

---

Uma dependency é um compilador especial usado em aplicações React para compilar o código React em JavaScript. 

#### --answer--

Uma dependência é quando um componente ou módulo em uma aplicação depende de outro para funcionar corretamente.

### --question--

#### --text--

O que é um gerenciador de pacotes?

#### --distractors--

Uma ferramenta usada para atualizar pacotes de terceiros instalados em aplicações.

---

Uma ferramenta usada para gerenciar pacotes criados exclusivamente dentro de um projeto.

---

Uma ferramenta usada para mover dependências da pasta `node_modules` para uma pasta de configuração especial.

#### --answer--

Uma ferramenta usada para instalação, atualizações e remoção de dependências.

### --question--

#### --text--

Qual dos seguintes NÃO é um gerenciador de pacotes comumente usado no ecossistema JavaScript?

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

Qual dos seguintes é um arquivo de configuração chave que contém metadados sobre o seu projeto?

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

Qual dos seguintes é um framework React comumente usado?

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

Qual das seguintes é a maneira correta de adicionar rotas à sua aplicação React usando React Router?

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

Qual das seguintes é a maneira correta de criar uma rota dinâmica no React Router?

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

Qual dos seguintes hooks é usado para acessar os parâmetros dinâmicos de um caminho de URL?

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

Qual das seguintes é a maneira correta de criar um manipulador de requisição personalizado em Next.js?

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

O que é prop drilling?

#### --distractors--

O processo de criar um reducer que especifica como o estado deve mudar em resposta a props particulares usadas.

---

O processo de passar props do componente filho para o componente pai. 

---

O processo de criar um context provider para uma aplicação React e usar isso para enviar props de componentes pai para filho.

#### --answer--

O processo de passar props de um componente pai para componentes filhos profundamente aninhados, mesmo quando alguns dos componentes filhos não precisam dos props.

### --question--

#### --text--

Qual das seguintes opções NÃO é uma solução comumente usada para gerenciamento de estado em aplicações React?

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Primavera

### --question--

#### --text--

Para que servem as React Developer Tools?

#### --distractors--

Estas ferramentas são usadas apenas para verificar problemas de acessibilidade em aplicações React.

---

Estas ferramentas são usadas para adicionar testes adicionais para componentes React.

---

Estas ferramentas são usadas para memorizar todos os componentes React dentro da aplicação.

#### --answer--

Estas ferramentas são usadas para inspecionar componentes React e identificar problemas de desempenho.

### --question--

#### --text--

O que são React Server Components?

#### --distractors--

Estes são componentes que gerenciam o contexto exclusivamente no servidor.

---

Estes são componentes memoizados que renderizam exclusivamente no servidor.

---

Estes são componentes que gerenciam estado exclusivamente no servidor.

#### --answer--

Estes são componentes que são renderizados exclusivamente no servidor.

