---
id: 6732b76c03f7d825c7fc74ee
title: ¿Qué es el Operador de Encadenamiento Opcional, y Cómo Funciona?
challengeType: 19
dashedName: what-is-the-optional-chaining-operator-and-how-does-it-work
---

# --interactive--

El operador de encadenamiento opcional (`?.`) es una herramienta útil en JavaScript que te permite acceder de forma segura a propiedades de objetos o llamar métodos sin preocuparte de si existen. Es como una red de seguridad para trabajar con objetos que podrían tener partes faltantes.

:::interactive_editor

```js
const person = {
  name: "Alice",
  age: 30
};

console.log(person.name); // "Alice"
console.log(person.job); // undefined
```

:::

En este ejemplo, `person.name` existe, así que muestra `Alice`. Pero `person.job` no existe, por lo que nos da `undefined`.

Ahora, digamos que queremos acceder a una propiedad de un objeto que podría no existir:

```js
const person = {
  name: "Alice",
  age: 30
};

console.log(person.address.street); // This will throw an error!
```

Este ejemplo generará un `Uncaught TypeError`. Dado que `person.address` es `undefined`, no podemos acceder a la propiedad `street`. Aquí es donde el operador de encadenamiento opcional resulta útil. Aquí hay un ejemplo de uso del operador de encadenamiento opcional:

:::interactive_editor

```ts
const user = {
  name: "John",
  profile: {
    email: "john@example.com",
    address: {
      street: "123 Main St",
      city: "Somewhere"
    }
  }
};

console.log(user?.profile?.address?.street); // "123 Main St"
console.log(user?.profile?.phone?.number);   // undefined
```

:::

Al usar el operador de encadenamiento opcional, le indicamos a JavaScript que solo continúe con la operación si el objeto (o el valor antes de `?.`) existe y no es `null` o `undefined`. 

Si el valor antes del `?.` es `null` o `undefined`, JavaScript devuelve `undefined` en lugar de intentar continuar with la operación y generar un error.

Recuerda, el operador de encadenamiento opcional es más útil cuando no estás seguro de si una propiedad o un método existe. Ayuda a prevenir errores y hace que tu código sea más robusto.

# --questions--

## --text--

¿Qué hace el operador de encadenamiento opcional (`?.`) en JavaScript?

## --answers--

Asigna un valor predeterminado cuando falta una propiedad.

### --feedback--

El operador de encadenamiento opcional solo previene errores cuando un valor es `null` o `undefined`.

---

Evita que aparezcan valores `undefined` en los objetos.

### --feedback--

El operador de encadenamiento opcional solo previene errores cuando un valor es `null` o `undefined`.

---

Accede de forma segura a propiedades anidadas y devuelve `undefined` en lugar de generar un error si falta un valor.

---

Obliga a JavaScript a evaluar propiedades faltantes incluso si no existen.

### --feedback--

El operador de encadenamiento opcional solo previene errores cuando un valor es `null` o `undefined`.

## --video-solution--

3

## --text--

¿Cuál será la salida del siguiente código?

```js
const person = {
  name: "Alice",
  age: 30
};

console.log(person.address.street);
```

## --answers--

`undefined`

### --feedback--

Acceder a una propiedad anidada faltante sin encadenamiento opcional detiene la ejecución del código.

---

`street`

### --feedback--

Acceder a una propiedad anidada faltante sin encadenamiento opcional detiene la ejecución del código.

---

`{}`

### --feedback--

Acceder a una propiedad anidada faltante sin encadenamiento opcional detiene la ejecución del código.

---

Se lanza un `TypeError` porque `person.address` está indefinido.

## --video-solution--

4

## --text--

¿Cuál será la salida del siguiente código?

```js
const user = {
  name: "John",
  profile: {
    email: "john@example.com",
    "home address": {
      street: "123 Main St",
      city: "Somewhere"
    }
  }
};

console.log(user?.profile?.address?.street);
```

## --answers--

`undefined`

---

`"123 Main St"`

### --feedback--

Piensa en lo que representa un valor que no ha sido asignado.

---

`null`

### --feedback--

Piensa en lo que representa un valor que no ha sido asignado.

---

Se lanza un `TypeError`.

### --feedback--

Piensa en lo que representa un valor que no ha sido asignado.

## --video-solution--

1
