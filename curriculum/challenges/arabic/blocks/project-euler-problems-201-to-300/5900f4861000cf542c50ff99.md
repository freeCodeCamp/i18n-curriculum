---
id: 5900f4861000cf542c50ff99
title: "المشكلة 282: دالة Ackermann"
challengeType: 1
forumTopicId: 301933
dashedName: problem-282-the-ackermann-function
---

# --description--

لدوال $m$ و $n$ من الأعداد الصحيحة غير السالبة، تُعرّف دالة Ackermann $A(m, n)$ كما يلي:

$$A(m, n) =
\begin{cases}
n + 1                 & \text{if $m = 0$}             \\\\
A(m - 1, 1)           & \text{if $m > 0$ and $n = 0$} \\\\
A(m - 1, A(m, n - 1)) & \text{if $m > 0$ and $n > 0$}
\end{cases}$$

على سبيل المثال، $A(1, 0) = 2$، $A(2, 2) = 7$ و $A(3, 4) = 125$.

احسب $\displaystyle\sum_{n = 0}^6 A(n, n)$ وقدم إجابتك مقسومة على ${14}^8$.

# --hints--

يجب أن تُرجع الدالة `ackermanFunction()` القيمة `1098988351`.

```js
assert.strictEqual(ackermanFunction(), 1098988351);
```

# --seed--

## --seed-contents--

```js
function ackermanFunction() {

  return true;
}

ackermanFunction();
```

# --solutions--

```js
// solution required
```
