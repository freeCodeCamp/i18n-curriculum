---
id: 5900f38c1000cf542c50fe9f
title: 'Zoezi la 32: Bidhaa yenye tarakimu zote'
challengeType: 1
forumTopicId: 301976
dashedName: problem-32-pandigital-products
---

# --description--

Tutasema kwamba nambari yenye tarakimu `n` ni yenye tarakimu zote ikiwa inatumia tarakimu zote kutoka 1 hadi `n` mara moja tu; kwa mfano, nambari yenye tarakimu 5, 15234, ni yenye tarakimu zote kutoka 1 hadi 5.

Bidhaa 7254 ni ya kipekee, kwani utambulisho, 39 × 186 = 7254, unaojumuisha mzalishaji, mzidishaji, na bidhaa ni yenye tarakimu zote kutoka 1 hadi 9.

Tafuta jumla ya bidhaa zote ambazo utambulisho wa mzalishaji/mzidishaji/bidhaa unaweza kuandikwa kuwa yenye tarakimu zote kutoka 1 hadi `n`.

**Kidokezo:** Baadhi ya bidhaa zinaweza kupatikana kwa njia zaidi ya moja, hivyo hakikisha unajumuisha mara moja tu katika jumla yako.

# --hints--

`pandigitalProducts(4)` inapaswa kurudisha nambari.

```js
assert(typeof pandigitalProducts(4) === 'number');
```

`pandigitalProducts(4)` inapaswa kurudisha `12`.

```js
assert.strictEqual(pandigitalProducts(4), 12);
```

`pandigitalProducts(6)` inapaswa kurudisha `162`.

```js
assert.strictEqual(pandigitalProducts(6), 162);
```

`pandigitalProducts(7)` inapaswa kurudisha `0`.

```js
assert.strictEqual(pandigitalProducts(7), 0);
```

`pandigitalProducts(8)` inapaswa kurudisha `13458`.

```js
assert.strictEqual(pandigitalProducts(8), 13458);
```

`pandigitalProducts(9)` inapaswa kurudisha `45228`.

```js
assert.strictEqual(pandigitalProducts(9), 45228);
```

# --seed--

## --seed-contents--

```js
function pandigitalProducts(n) {

  return true;
}

pandigitalProducts(4);
```

# --solutions--

```js
// Check for pandigital number
function isPandigital(digitStr) {
  return digitStr
    .split('')
    .sort()
    .every((c, i) => c == i + 1);
}

// Concatenate 3 numbers
function concatenate3Nums(a, b, c) {
  return a.toString() + b.toString() + c.toString();
}

// Find sum of all pandigital products
function pandigitalProducts(n) {
  const products = [];
  let sum = 0;
  const max = Number(Array(n)
    .fill(0)
    .map((_, i) => (n - i).toString())
    .join(''));
  const outerLimit = Math.sqrt(max);

  for (let factor1 = 2; factor1 < outerLimit; factor1++) {
    const innerLimit = max / factor1;

    for (let factor2 = factor1; factor2 < innerLimit; factor2++) {
      const product = factor1 * factor2;
      const concatenated = concatenate3Nums(factor1, factor2, product);

      if (concatenated.length > n) {
        break;
      } else if (concatenated.length == n &&
        isPandigital(concatenated) &&
        !products.includes(product)
      ) {
        products.push(product);
        sum += product;
      }
    }
  }
  return sum;
}
```
