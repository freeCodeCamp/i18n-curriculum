---
id: 5900f4311000cf542c50ff44
title: 'Завдання 197: дослідження поведінки рекурсивно визначеної послідовності'
challengeType: 1
forumTopicId: 301835
dashedName: problem-197-investigating-the-behaviour-of-a-recursively-defined-sequence
---

# --description--

Дано функцію $f(x) = ⌊{2}^{30.403243784 - x^2}⌋ × {10}^{-9}$ (⌊ ⌋ — це функція підлоги), послідовність $u_n$ визначена як $u_0 = -1$ та $u_{n + 1} = f(u_n)$.

Знайдіть $u_n + u_{n + 1}$ для $n = {10}^{12}$. Наведіть відповідь з 9 знаками після десяткової крапки.

# --hints--

`recursivelyDefinedSequence()` має повертати `1.710637717`.

```js
assert.strictEqual(recursivelyDefinedSequence(), 1.710637717);
```

# --seed--

## --seed-contents--

```js
function recursivelyDefinedSequence() {

  return true;
}

recursivelyDefinedSequence();
```

# --solutions--

```js
// solution required
```
