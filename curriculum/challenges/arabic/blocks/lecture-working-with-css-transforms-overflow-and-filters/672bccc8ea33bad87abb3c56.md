---
id: 672bccc8ea33bad87abb3c56
title: ما الفرق بين content-box و border-box؟
challengeType: 19
dashedName: what-is-the-difference-between-content-box-and-border-box
---

# --interactive--

يمكن تعيين الخاصية `box-sizing` إلى إما `content-box` أو `border-box` للتحكم في كيفية حساب عرض وارتفاع العناصر.

يمكن تعيين هذه الخاصية على المحدد العام (`*`) لتطبيقها على جميع العناصر في المستند:

```css
* {
  box-sizing: border-box;
}
```

قيمة الخاصية `box-sizing` هي `content-box` افتراضيًا، لكن يمكنك اختيار `border-box` إذا احتجت لذلك. سنستعرض أولًا `content-box` ثم ننتقل إلى `border-box`.

لفهم كيفية عمل النماذج، يجب أن تكون على دراية بالمفاهيم الأربعة الأساسية من نموذج الصندوق في CSS. دعنا نراجعها بسرعة.

- مساحة المحتوى هي المساحة التي يشغلها محتوى العنصر.
- الحشو هو المساحة بين مساحة المحتوى والحدود.
- الحدود هي الإطار الذي يحيط بمساحة المحتوى والحشو.
- الهامش هو المساحة خارج الحدود التي تفصل العنصر عن العناصر الأخرى.

في نموذج `content-box`، العرض والارتفاع الذي تحدده لعنصر يحدد أبعاد مساحة المحتوى، لكنه لا يشمل الحشو أو الحدود أو الهامش. استخدم `content-box` عندما تحتاج إلى تحكم دقيق في مساحة المحتوى. عند تعيين `width` و `height`، فأنت تحدد فقط حجم المحتوى نفسه.

لإيجاد العرض الكلي للعنصر، تحتاج إلى إضافة الحشو الأيسر والأيمن، والحدود اليسرى واليمنى. وبالمثل، يمكن إيجاد الارتفاع الكلي للعنصر بإضافة ارتفاع المحتوى، الحشو العلوي والسفلي، والحدود العلوية والسفلية.

على سبيل المثال، هنا لدينا محدد نوع CSS لجميع عناصر `div`.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div></div>
```

```css
div {
  width: 300px;
  height: 200px;
  padding: 20px;
  border: 4px solid black;
}
```

:::

في هذه الحالة، إذا استخدمنا `content-box` ستكون مساحة المحتوى 300px في 200px. الحجم الكلي المعروض يشمل الحشو والحدود — على سبيل المثال، العرض الكلي = 300px (المحتوى) + 40px (الحشو) + 8px (الحدود) = 348px؛ ويتم حساب الارتفاع الكلي بنفس الطريقة.

رائع! الآن دعنا نستعرض `border-box`. هو مختلف لأنه العرض والارتفاع الذي تحدده يشمل محتوى العنصر، الحشو، والحدود (لكن لا يشمل الهامش). استخدم `border-box` عندما تريد أن يبقى الحجم الكلي للعنصر ثابتًا حتى لو تغير الحشو أو الحدود — وهذا مفيد غالبًا في التخطيطات المتجاوبة.

مع `border-box`، يُدرج الحشو والحدود داخل الحجم المحدد للعنصر. يصبح `width` و `height` اللذان تحددهما هما الأبعاد الكلية للعنصر: المحتوى + الحشو + الحدود؛ بينما يبقى الهامش مستبعدًا.

في المثال التالي، هناك عنصران `div` لهما نفس الأبعاد لكن بقيم مختلفة لـ `box-sizing`. لاحظ كيف يؤدي هذا إلى أحجام كلية مختلفة عند العرض في المتصفح:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="box" id="red-div"></div>
<div class="box" id="blue-div"></div>
```


```css
.box {
  width: 300px;
  height: 200px;
  padding: 20px;
  border: 4px solid black;
  margin: 10px;
}

#red-div {
  box-sizing: content-box;
  background-color: red;
}

#blue-div {
  box-sizing: border-box;
  background-color: blue;
}
```

:::

يمكنك أن ترى أنهما لهما نفس `width` و `height` و `padding` و `border` و `margin`. الاختلافات الوحيدة هي في الألوان وقيمة الخاصية `box-sizing`. هذا الاختلاف الصغير له تأثير مهم جدًا على الأبعاد النهائية.

الاختيار بين `content-box` و `border-box` يعتمد حقًا على الاحتياجات الخاصة لمشروعك. بينما أصبح `border-box` شائعًا أكثر لبساطته ومرونته، فإن فهم كلا النموذجين مهم لتنفيذ تخطيطات CSS فعالة.

# --questions--

## --text--

ما هي القيمة الافتراضية للخاصية `box-sizing` في معظم المتصفحات؟

## --answers--

`content-box`

---

`border-box`

### --feedback--

فكر في السلوك الافتراضي لحجم العنصر.

---

`padding-box`

### --feedback--

فكر في السلوك الافتراضي لحجم العنصر.

---

`margin-box`

### --feedback--

فكر في السلوك الافتراضي لحجم العنصر.

## --video-solution--

1

## --text--

ما هي الميزة الأساسية لاستخدام `border-box` في إنشاء تخطيطات متجاوبة؟

## --answers--

يجعل الحسابات أكثر تعقيدًا.

### --feedback--

فكر في كيفية تعامل نموذج `border-box` مع `padding` و `border` ضمن `width` و `height` المحددين.

---

يسمح بتحكم أكثر دقة في أبعاد العنصر.

### --feedback--

فكر في كيفية تعامل نموذج `border-box` مع `padding` و `border` ضمن `width` و `height` المحددين.

---

يضمن أن تحافظ العناصر على أبعادها المحددة بغض النظر عن التغيرات في `padding` أو `border`.

---

يحسن التوافق مع المتصفحات.

### --feedback--

فكر في كيفية تعامل نموذج `border-box` مع `padding` و `border` ضمن `width` و `height` المحددين.

## --video-solution--

3

## --text--

في نموذج `content-box`، ماذا يمثل `width` المحدد لعنصر؟

## --answers--

إجمالي `width` للعنصر، بما في ذلك `padding` و`border` و`margin`.

### --feedback--

فكر في العلاقة بين مساحة المحتوى والأبعاد الكلية للعنصر في نموذج `content-box`.

---

`width` لمساحة المحتوى فقط.

---

`width` الخاص بـ `border`.

### --feedback--

فكر في العلاقة بين مساحة المحتوى والأبعاد الكلية للعنصر في نموذج `content-box`.

---

قيمة `width` الخاصة بـ `padding`.

### --feedback--

فكر في العلاقة بين مساحة المحتوى والأبعاد الكلية للعنصر في نموذج `content-box`.

## --video-solution--

2
