---
id: 69691f96a3a2f3f6c220ab52
title: "Cos'è la ricerca binaria e in cosa si differenzia dalla ricerca lineare?"
challengeType: 19
dashedName: what-is-binary-search-and-how-does-it-differ-from-linear-search
---

# --description--

La ricerca in una lista di elementi è un'operazione comune nell'informatica. Ci sono due algoritmi chiave che dovresti conoscere quando si tratta di ricerca: la ricerca lineare e la ricerca binaria.

La ricerca lineare inizia dall'inizio di una lista e itera attraverso ogni elemento finché non trova il valore obiettivo che sta cercando.

Se il valore obiettivo viene trovato, viene restituito l'indice in cui si trova nella lista. Se il valore obiettivo non viene trovato, viene restituito `-1`. Restituiamo `-1` perché non è un indice valido nella maggior parte dei linguaggi di programmazione.

Ecco come appare il codice per la ricerca lineare:

```javascript
function linearSearch(arr, target) {
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === target) {
            return i;
        }
    }
    return -1;
}
```

Se la lista che cercheremo è `[13, 4, 7, 9, 10]` e il valore obiettivo è `9`, la funzione restituirebbe `3` perché `9` si trova all'indice `3`.

Se cambiassimo il valore obiettivo in `5`, la funzione restituirebbe `-1` perché `5` non è nella lista.

Anche se questo è un algoritmo relativamente semplice, non è il più efficiente. Se hai una lista grande di elementi, la ricerca lineare può richiedere molto tempo per trovare il valore obiettivo.

La complessità temporale della ricerca lineare è `O(n)` perché il tempo necessario per cercare nella lista cresce linearmente con la dimensione della lista.

La complessità spaziale della ricerca lineare è `O(1)` perché non richiede spazio aggiuntivo per cercare nella lista.

La ricerca binaria è un algoritmo più efficiente per cercare in una lista grande di elementi. La condizione è che la lista deve essere ordinata in ordine crescente.

La ricerca binaria funziona dividendo la lista a metà e controllando se il valore obiettivo si trova nel mezzo della lista. Se il valore obiettivo è nel mezzo della lista, viene restituito l'indice del valore obiettivo. Altrimenti, l'algoritmo verifica se il valore obiettivo si trova nella metà sinistra o destra della lista.

Continua a dividere le parti rimanenti della lista in metà finché il valore obiettivo non viene trovato. Se il valore obiettivo non è nella lista, restituisce `-1`

Ecco come appare il codice per la ricerca binaria:

```javascript
function binarySearch(arr, target) {
    let low = 0;
    let high = arr.length - 1;

    while (low <= high) {
        const mid = Math.floor((low + high) / 2);

        if (arr[mid] === target) {
            return mid;
        } else if (arr[mid] < target) {
            low = mid + 1;
        } else {
            high = mid - 1;
        }
    }

    return -1;
}
```

Iniziamo identificando un indice `low` e un indice `high`. Questo rappresenta l'intervallo della lista che stiamo cercando.

Poi controlliamo la condizione che `low` sia minore o uguale a `high`. Se `low` è maggiore di `high`, abbiamo cercato in tutta la lista e il valore obiettivo non è stato trovato. In tal caso fermiamo la ricerca e restituiamo `-1`.

Se l'indice `low` è minore o uguale all'indice `high`, calcoliamo l'indice centrale della lista, `mid`. Poi controlliamo se il valore obiettivo si trova all'indice centrale. Se sì, restituiamo l'indice centrale.

Altrimenti, verifichiamo se il valore al punto medio è minore del valore obiettivo. Se lo è, aggiorniamo l'indice basso al valore dell'indice centrale più uno. Questo significa che cercheremo nella metà destra della lista.

Infine, se nessuna delle altre condizioni è `True`, aggiorniamo l'indice `high` al valore dell'indice centrale meno uno. Questo significa che cercheremo nella metà sinistra della lista.

Continuiamo a ripetere questo processo finché non troviamo il valore obiettivo o determiniamo che non è nella lista.

La complessità temporale della ricerca binaria è `O(log n)` perché il tempo necessario per cercare nella lista cresce in modo logaritmico con la dimensione della lista.

La complessità spaziale della ricerca binaria è `O(1)` perché non richiede spazio aggiuntivo per cercare nella lista.

La ricerca binaria e la ricerca lineare possono essere usate per una varietà di problemi che incontrerai nell'informatica. È importante capire le differenze tra i due algoritmi e quando usare ciascuno.

# --questions--

## --text--

Qual è la differenza principale tra la ricerca lineare e la ricerca binaria?

## --answers--

La ricerca lineare è più veloce della ricerca binaria.

### --feedback--

Considera i requisiti e l'efficienza di ciascun algoritmo.

---

La ricerca binaria richiede una lista ordinata, mentre la ricerca lineare no.

---

La ricerca lineare può essere usata solo con numeri.

### --feedback--

Considera i requisiti e l'efficienza di ciascun algoritmo.

---

La ricerca binaria restituisce sempre la prima occorrenza di un obiettivo.

### --feedback--

Considera i requisiti e l'efficienza di ciascun algoritmo.

## --video-solution--

2

## --text--

Qual è la complessità temporale della ricerca lineare?

## --answers--

`O(1)`

### --feedback--

Pensa a come cambia il tempo per completare la ricerca con la dimensione della lista.

---

`O(log n)`

### --feedback--

Pensa a come cambia il tempo per completare la ricerca con la dimensione della lista.

---

`O(n)`

---

`O(n²)`

### --feedback--

Pensa a come cambia il tempo per completare la ricerca con la dimensione della lista.

## --video-solution--

3

## --text--

Nella ricerca binaria, cosa succede se il valore obiettivo non viene trovato nella lista?

## --answers--

Restituisce l'indice centrale.

### --feedback--

Rifletti su cosa la funzione è progettata per fare quando l'obiettivo è assente.

---

Restituisce `-1`.

---

Restituisce l'ultimo indice controllato.

### --feedback--

Rifletti su cosa la funzione è progettata per fare quando l'obiettivo è assente.

---

Entra in un loop infinito.

### --feedback--

Rifletti su cosa la funzione è progettata per fare quando l'obiettivo è assente.

## --video-solution--

2
