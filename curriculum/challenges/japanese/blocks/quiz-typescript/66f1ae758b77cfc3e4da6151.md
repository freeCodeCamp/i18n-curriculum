---
id: 66f1ae758b77cfc3e4da6151
title: TypeScript クイズ
challengeType: 8
dashedName: quiz-typescript
---

# --description--

クイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

TypeScriptとは何ですか？

#### --distractors--

システムプログラミングに使われるフレームワーク。

---

データベース管理ツール。

---

ウェブサイトのスタイリングに使われるプログラミング言語。

#### --answer--

JavaScriptのスーパーセットで、静的型付けを追加したもの。

### --question--

#### --text--

なぜTypeScriptをプロジェクトで使うべきですか？

#### --distractors--

アプリケーションのテストが不要になるから。

---

すべての他のプログラミング言語より速いから。

---

ウェブサイトのスタイリングが簡単になるから。

#### --answer--

JavaScriptに型定義を追加して保守性を向上させるから。

### --question--

#### --text--

TypeScriptはどの言語にコンパイルされますか？

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

次のコードスニペットの出力は何ですか？

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

TypeScriptにおけるインターフェースの主な目的は何ですか？

#### --distractors--

TypeScriptコードをJavaScriptにコンパイルすること。

---

新しいデータ型を作成すること。

---

新しいJavaScriptオブジェクトを作成すること。

#### --answer--

オブジェクトの構造を定義すること。

### --question--

#### --text--

次のうちTypeScriptの基本的なプリミティブ型ではないものはどれですか？

#### --distractors--

string

---

boolean

---

number

#### --answer--

array

### --question--

#### --text--

TypeScriptでリテラル型を正しく定義しているのはどれですか？

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

TypeScriptで`type`キーワードの主な目的は何ですか？

#### --distractors--

変数を宣言すること。

---

クラスを作成すること。

---

インターフェースを実装すること。

#### --answer--

カスタム型を定義すること。

### --question--

#### --text--

次のうちユニオン型を正しく定義しているのはどれですか？

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

TypeScriptで`interface`に固有で、`type`では実現できない機能はどれですか？

#### --distractors--

オブジェクトの形状を定義すること。

---

ユニオン型を作成すること。

---

特定の値のみを含む型を定義すること。

#### --answer--

クラスの実装を宣言すること。

### --question--

#### --text--

TypeScriptでジェネリクスを使う主な目的は何ですか？

#### --distractors--

クラス間の継承を実装すること。

---

オブジェクトの構造を定義すること。

---

新しいデータ型を作成すること。

#### --answer--

複数の型で動作する再利用可能なコードを書くこと。

### --question--

#### --text--

次のうちTypeScriptのジェネリック関数の**正しい例**はどれですか？

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

TypeScriptの型絞り込みとは何ですか？

#### --distractors--

コンパイルされたJavaScriptのサイズを減らすこと。

---

ユニオン型の型数を制限すること。

---

TypeScriptプロジェクトから未使用の型を削除すること。

#### --answer--

ユニオン型をより具体的な型に絞り込むコードを書くこと。

### --question--

#### --text--

TypeScriptコンパイラのオプションを設定するために通常使われるファイルはどれですか？

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

`rootDir`ファイルの`tsconfig.json`オプションは何を指定しますか？

#### --distractors--

コンパイル時に使うTypeScriptのバージョン。

---

TypeScriptコードのターゲット実行環境（例：Node.jsやブラウザ）。

---

インポートとエクスポートに使う特定のモジュールシステム。

#### --answer--

ソースファイルを格納するディレクトリ。

### --question--

#### --text--

次の例のうち、`instanceof`を正しく使って`value`プロパティに安全にアクセスしているのはどれですか？

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
TypeScriptで`"strict": true`に`tsconfig.json`を設定すると何が起こりますか？

#### --distractors--

コードを実行する前にミスを検出できる静的型チェックが有効になる。

---

常にES6構文を使うことが強制される。

---

コードを実行する前にコンパイルすることが強制される。

#### --answer--

`null`や`undefined`の処理を必須にし、暗黙の`any`を防ぐなどの型チェックオプションが有効になる。

### --question--

#### --text--

TypeScriptの`never`型の目的は何ですか？

#### --distractors--

nullまたはundefinedを表すこと。

---

あらゆる可能な値を表すこと。

---

null以外の何でもあり得る型を表すこと。

#### --answer--

決して発生しない値の型を表すこと。

### --question--

#### --text--

次の`getRandomValue`関数呼び出しのうち、TypeScriptで正常にコンパイルされるのはどれですか？

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

次のコードでTypeScriptコンパイラエラー`'email' is possibly 'null'`をオプショナルチェーンを使ってどう直しますか？

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`
