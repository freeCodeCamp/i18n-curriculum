---
id: 66f1a417757b6ca4eecd89d6
title: React 状态和 Hooks 测验
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪种方式是在 React 中为按钮添加点击事件的正确方法？

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

什么是 React Synthetic Event？

#### --distractors--

仅用于 `inputs` 和 `submit` 事件的 JSX 元素类型。

---

仅用于 `submit` 事件的 JSX 元素类型。

---

一种改变事件传播冒泡阶段以使其更符合用户习惯的方法。

#### --answer--

一个围绕本地的 `click` 和 `submit` 事件的包装器。

### --question--

#### --text--

以下哪项是用于命名事件处理函数的常见前缀？

#### --distractors--

isHandling

---

hasHandled

---

处理

#### --answer--

处理器

### --question--

#### --text--

React 的渲染有哪三个主要阶段？

#### --distractors--

握手、装载和水合。

---

获取、解码和执行。

---

挂载、更新和卸载。

#### --answer--

触发、渲染和提交。

### --question--

#### --text--

以下哪种方式是使用 `useState` 钩子的正确方法？

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

以下哪项不是用于处理 React `event` 的有效 `attribute`？

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

以下哪种方法是更新数组状态的正确方式？

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Frontend");
```

---

```js
setCertificates.push("Frontend");
```

---

```js
setCertificates().append("Frontend");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Frontend"]);
```

### --question--

#### --text--

这个函数有什么问题？

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

不允许在嵌套函数内部更新 `useState` 变量。

---

`previousState` 需要用括号括起来。

---

不应在状态设置函数内部使用箭头函数。

#### --answer--

必须在 `name` 之前展开 `previousState`，以确保不会覆盖任何其他属性。

### --question--

#### --text--

以下哪种方法是更新状态以从数组中移除项的正确方式？

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

`ref` 属性在 React 中通常用于什么？

#### --distractors--

它用于审计 react 组件中的性能问题。

---

它用于审核 react 组件中的无障碍问题。

---

它用于移除 DOM 节点。

#### --answer--

它用于访问 DOM 节点。

### --question--

#### --text--

以下哪种方式是访问 `ref` 当前值的正确方法？

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

这条消息将被记录到控制台多少次？

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

三次。

---

两次。

---

没有任何内容，因为依赖数组为空。

#### --answer--

仅在初始 `render` 时执行一次。

### --question--

#### --text--

React 自定义钩子通常用于什么？

#### --distractors--

它们用于执行应用程序最复杂的功能性。

---

它们通常不被使用，优先使用内置钩子。

---

它们替代了 React 早期版本中的生命周期方法。

#### --answer--

它们有助于在各个组件之间重用有状态逻辑。

### --question--

#### --text--

这条消息将被记录到控制台多少次？

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

每当提交 `form` 或发生点击 `event` 时，都会记录日志。

---

它将被记录两次。

---

它只会被记录一次。

#### --answer--

每次组件渲染时都会记录日志。

### --question--

#### --text--

以下哪种方法是防止浏览器刷新 `onSubmit` 事件的正确方式？

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

以下哪个钩子通常用于获取数据？

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

以下哪项不是本地 `Event` 对象的有效属性？

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

以下哪个渲染阶段发生在 React 检测到某些内容已更改且用户界面（UI）可能需要更新时？

#### --distractors--

设置器

---

提交

---

循环

#### --answer--

触发器

### --question--

#### --text--

以下哪个阶段是指 React 从虚拟 DOM 获取准备好的更改并将其应用到真实 DOM？

#### --distractors--

触发器

---

渲染

---

设置器

#### --answer--

提交

### --question--

#### --text--

`useEffect` 中的 `dependencies` 的目的是什么？

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

可选的 `dependencies` 参数不会影响与 `useEffect` 相关的任何内容。

---

可选的 `dependencies` 参数使效果在无限循环中运行。

---

可选的 `dependencies` 参数用于防止该效果运行。

#### --answer--

可选的 `dependencies` 参数控制效果何时运行。

