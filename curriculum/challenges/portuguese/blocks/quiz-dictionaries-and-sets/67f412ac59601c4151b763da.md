---
id: 67f412ac59601c4151b763da
title: Questionário sobre Dicionários e Sets
challengeType: 8
dashedName: quiz-dictionaries-and-sets
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

O que é um dicionário?

#### --distractors--

Uma estrutura de dados que contém apenas strings e listas.

---

Uma estrutura de dados que é uma coleção de pares chave-valor que aceita apenas números grandes.

---

Uma estrutura de dados que contém apenas uma coleção de tuplas.

#### --answer--

Uma estrutura de dados que é uma coleção de pares chave-valor.

### --question--

#### --text--

Qual das seguintes é a sintaxe correta para um dicionário?

#### --distractors--

```py
dictionary = {
    <key1: value1>,
    <key2: value2>
}
```

---

```py
dictionary = {
    (key1: value1),
    (key2: value2)
}
```

---

```py
dictionary = {
    [key1: value1],
    [key2: value2]
}
```

#### --answer--

```py
dictionary = {
    key1: value1,
    key2: value2
}
```

### --question--

#### --text--

Qual das seguintes afirmações é verdadeira sobre dicionários?

#### --distractors--

As chaves devem ter pelo menos dois caracteres de comprimento.

---

As chaves devem incluir um símbolo especial.

---

As chaves devem incluir pelo menos um número.

#### --answer--

As chaves devem ser únicas.

### --question--

#### --text--

Quais dos seguintes construtores podem ser usados para criar um dicionário?

#### --distractors--

`diction()`

---

`dic()`

---

`dictionary()`

#### --answer--

`dict()`

### --question--

#### --text--

Qual das seguintes é a maneira correta de acessar um valor de um dicionário?

#### --distractors--

`dictionary<key>`

---

`dictionary/key/`

---

`dictionary{key}`

#### --answer--

`dictionary[key]`

### --question--

#### --text--

Qual das seguintes é a maneira correta de atualizar um valor em um dicionário?

#### --distractors--

`pizza['name'] >> 'Margherita'`

---

`pizza['name'] << 'Margherita'`

---

`pizza['name'] == 'Margherita'`

#### --answer--

`pizza['name'] = 'Margherita'`

### --question--

#### --text--

Quais dos seguintes métodos podem ser usados para recuperar um valor associado a uma chave?

#### --distractors--

`access()`

---

`set()`

---

`retrieve()`

#### --answer--

`get()`

### --question--

#### --text--

O que é um objeto view?

#### --distractors--

Um objeto especial usado para transformar strings em dicionários.

---

Uma maneira de visualizar o conteúdo de um dicionário somente se o dicionário tiver dois ou mais pares chave-valor.

---

Um objeto especial usado para transformar listas em dicionários.

#### --answer--

Uma maneira de ver o conteúdo de um dicionário sem criar uma cópia separada dos dados.

### --question--

#### --text--

Qual dos seguintes métodos retorna um objeto view com todos os pares chave-valor no dicionário?

#### --distractors--

`lists()`

---

`dictionaries()`

---

`collections()`

#### --answer--

`items()`

### --question--

#### --text--

Qual dos seguintes métodos remove todos os pares chave-valor do dicionário?

#### --distractors--

`empty()`

---

`replace()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

Qual será a saída para o seguinte código?

```py
products = {
    'Laptop': 990,
    'Smartphone': 600,
    'Tablet': 250,
    'Headphones': 70,
}

for product in products.items():
    print(product)
```

#### --distractors--

```py
<'Laptop', 990>
<'Smartphone', 600>
<'Tablet', 250>
<'Headphones', 70>
```

---

```py
'Laptop', 990
'Smartphone', 600
'Tablet', 250
'Headphones', 70
```

---

```py
['Laptop', 990]
['Smartphone', 600]
['Tablet', 250]
['Headphones', 70]
```

#### --answer--

```py
('Laptop', 990)
('Smartphone', 600)
('Tablet', 250)
('Headphones', 70)
```

### --question--

#### --text--

Qual das seguintes afirmações é verdadeira sobre conjuntos?

#### --distractors--

Conjuntos são mutáveis e ordenados em ordem reversa.

---

Conjuntos são mutáveis e ordenados.

---

Conjuntos são imutáveis e não ordenados.

#### --answer--

Conjuntos são mutáveis e não ordenados.

### --question--

#### --text--

Qual das seguintes é a maneira correta de criar um set?

#### --distractors--

`my_set = (1, 2, 3, 4, 5)`

---

`my_set = <1, 2, 3, 4, 5>`

---

`my_set = [1, 2, 3, 4, 5]`

#### --answer--

`my_set = {1, 2, 3, 4, 5}`

### --question--

#### --text--

Qual das seguintes funções é usada para criar um conjunto vazio?

#### --distractors--

`create_set()`

---

`empty_set()`

---

`sets()`

#### --answer--

`set()`

### --question--

#### --text--

O que será exibido no terminal?

```py
my_set = {1, 2, 3, 4, 5, 6}
my_set.add(5)

print(my_set)
```

#### --distractors--

`SyntaxError`

---

`RangeError`

---

`{1, 2, 3, 4, 5, 5, 6}`

#### --answer--

`{1, 2, 3, 4, 5, 6}`

### --question--

#### --text--

Qual dos seguintes métodos verifica se um conjunto é um subconjunto? 

#### --distractors--

`issub()`

---

`isset()`

---

`subset()`

#### --answer--

`issubset()`

### --question--

#### --text--

Qual dos seguintes métodos verifica se dois conjuntos são disjuntos?

#### --distractors--

`joint()`

---

`isjoint()`

---

`disjoint()`

#### --answer--

`isdisjoint()`

### --question--

#### --text--

O que o operador de diferença simétrica (`^`) faz?

#### --distractors--

Ele retorna um novo conjunto com apenas os elementos que os conjuntos têm em comum.

---

Ele encontra a diferença entre os conjuntos e atualiza o primeiro conjunto com esse resultado.

---

Ele retorna um novo conjunto com os elementos do primeiro conjunto que não estão no outro conjunto. 

#### --answer--

Ele retorna um novo conjunto com os elementos que estão no primeiro ou no segundo conjunto, mas não em ambos.

### --question--

#### --text--

Qual dos seguintes módulos internos é usado para gerar números aleatórios?

#### --distractors--

`set_random`

---

`get_random`

---

`rand`

#### --answer--

`random`

### --question--

#### --text--

Qual dos seguintes módulos é usado para trabalhar com expressões regulares?

#### --distractors--

`regex`

---

`reg`

---

`r`

#### --answer--

`re`

