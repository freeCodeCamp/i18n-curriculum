---
id: 66edcd49e73385dd4df54ac7
title: JavaScript 迴圈小測驗
challengeType: 8
dashedName: quiz-javascript-loops
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一項最能描述迭代？

#### --distractors--

這是反覆撰寫程式碼直到它正確運作的處理程序。

---

這是一種將資料儲存在連續記憶體位置的技術。

---

這是組件之間傳送信號以建立連接的處理程序。

#### --answer--

這是重複執行一組指令多次的處理程序。

### --question--

#### --text--

程式設計中使用什麼技術來進行迭代？

#### --distractors--

遞迴

---

條件語句

---

編譯

#### --answer--

迴圈中

### --question--

#### --text--

以下哪一項是可迭代物件？

#### --distractors--

未定義

---

數字

---

布林值

#### --answer--

字串

### --question--

#### --text--

以下哪一項不是 JavaScript 支援的迴圈型別？

#### --distractors--

`for` 迴圈。

---

`while` 迴圈。

---

`for...in` 迴圈。

#### --answer--

`if...else` 迴圈。

### --question--

#### --text--

`for` 迴圈宣告的正確順序是什麼？

#### --distractors--

```javascript
for (condition; increment/decrement; initialization) {
  statement;
}
```

---

```javascript
for (increment/decrement; condition; initialization) {
  statement;
}
```

---

```javascript
for (initialization; increment/decrement; condition) {
  statement;
}
```

#### --answer--

```javascript
for (initialization; condition; increment/decrement) {
  statement;
}
```

### --question--

#### --text--

哪一個迴圈會先執行該區塊一次，再檢查條件是否為真，並且只要條件持續為真就會繼續執行？

#### --distractors--

`while` 迴圈。

---

`for...of` 迴圈。

---

`for...in` 迴圈。

#### --answer--

`do...while` 迴圈。

### --question--

#### --text--

以下哪一項會對可迭代物件的值進行迴圈？

#### --distractors--

`for...in` 迴圈。

---

`for` 迴圈。

---

`do...while` 迴圈。

#### --answer--

`for...of` 迴圈。

### --question--

#### --text--

以下哪一項最能定義無窮迴圈？

#### --distractors--

一個會持續執行直到條件變為 false 的迴圈。

---

一個執行一次後即終止的迴圈。

---

一個在固定次數迭代後自動停止的迴圈。

#### --answer--

一個其終止條件永遠不會被滿足或缺失的迴圈。

### --question--

#### --text--

以下迴圈將執行多少次？  

```js
for (let i = 2; i < 10; i+=2) {  
  console.log(i);  
}
```

#### --distractors--

5

---

9

---

10

#### --answer--

4

### --question--

#### --text--

哪一種迴圈最適合用來遍歷物件？

#### --distractors--

`for...of` 迴圈。

---

`do...while` 迴圈。

---

`for` 迴圈。

#### --answer--

`for...in` 迴圈。

### --question--

#### --text--

`for...in` 迴圈和 `for...of` 迴圈有什麼差別？

#### --distractors--

`for...in` 迴圈迭代屬性值，而 `for...of` 迴圈迭代屬性名稱（鍵）。

---

`for...in` 迴圈創建新屬性，而 `for...of` 迴圈修改現有屬性。

---

`for...in` 迴圈只能用於字串，而 `for...of` 迴圈則可用於字串和數字。

#### --answer--

`for...in` 迴圈迭代屬性名稱（鍵），而 `for...of` 迴圈迭代屬性值。

### --question--

#### --text--

這些中哪一個會導致無窮迴圈？

#### --distractors--

變數的初始化。

---

最終會變成 false 的迴圈條件。

---

遞增/遞減邏輯。

#### --answer--

一個永遠傳回 true 的迴圈條件。

### --question--

#### --text--

什麼述語可以用來完全跳出迴圈？

#### --distractors--

`continue` 述語。

---

`label` 述語。

---

`skip` 述語。

#### --answer--

`break` 述語。

### --question--

#### --text--

哪個述語可以用來跳過當前的迭代並移動到下一次迭代？

#### --distractors--

`break` 述語。

---

`label` 述語。

---

`return` 述語。

#### --answer--

`continue` 述語。

### --question--

#### --text--

以下程式碼會在主控台列印什麼？

```js
for (let i = 0; i < 25; i += 2) {
  if (i % 5 === 0) {
    continue;
  }
  if (i % 13 === 0) {
    break;
  }
  console.log(i);
}
```

#### --distractors--

`0`、`2`、`4`、`6`、`8`、`12`、`14`、`16`、`18`、`22`、`24`

---

`0`、`2`、`4`、`6`、`8`、`12`

---

`2`、`4`、`6`、`8`、`12`

#### --answer--

`2`、`4`、`6`、`8`、`12`、`14`、`16`、`18`、`22`、`24`

### --question--

#### --text--

以下程式碼會在主控台列印什麼？

```javascript
for (let i = 1; i < 6; i++) {
  if (i === 4) break;
  console.log(i);
}
```

#### --distractors--

`1`、`2`、`3`、`4`、`5`、`6`

---

`1`、`2`、`3`、`4`、`5`

---

`1`、`2`、`3`、`4`

#### --answer--

`1`、`2` 和 `3`

### --question--

#### --text--

以下程式碼會在主控台列印什麼？

```javascript
const shoppingList = { tomatoes: 4, apples: 10 };
for (const item in shoppingList) {
  console.log(item);
}
```

#### --distractors--

`tomatoes - 4`、`apples - 10`

---

`4`、`10`

---

`tomatoes:4`、`apples:10`

#### --answer--

`tomatoes`、`apples`

### --question--

#### --text--

以下程式碼的主控台輸出會是什麼？

```javascript
for (let i = 2; i <= 13; i++) {
  if (i % 2 === 0) continue;
  console.log(i);
}
```

#### --distractors--

`2`、`4`、`6`、`8`、`10`、`12`

---

`1`、`3`、`5`、`7`、`9`、`11`、`13`

---

`1`、`3`、`5`、`7`、`9`、`11`

#### --answer--

`3`、`5`、`7`、`9`、`11`、`13`

### --question--

#### --text--

以下程式碼會在主控台列印出什麼？

```javascript
const fruits = ["Mango", "Pineapple", "Oranges"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

#### --distractors--

`0`、`1`、`2`

---

`Pineapple`、`Oranges` 和 `Mango`

---

`Oranges`、`Pineapple` 和 `Mango`

#### --answer--

`Mango`、`Pineapple` 和 `Oranges`

### --question--

#### --text--

以下程式碼會輸出字串 `in the loop` 幾次？

```javascript
let x = 0
while (x < 5) {
  console.log("in the loop");
}
```

#### --distractors--

3 次

---

4 次

---

5 次

#### --answer--

無限次

