---
id: 66ed8fe7f45ce3ece4053eb2
title: CSS Flexbox クイズ
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

flexboxとは何ですか？

#### --distractors--

レイアウトのための2次元モデル。

---

レイアウトのための3次元モデル。

---

多次元のレイアウトモデル。

#### --answer--

レイアウトのための1次元モデル。

### --question--

#### --text--

`div` 要素に対してflexboxレイアウトを有効にするために設定するCSSプロパティは何ですか？

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

現在の行がいっぱいになったときにフレックスアイテムを新しい行に折り返すことができるCSSプロパティはどれですか？

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

flexboxプロパティ`justify-content`は何を制御しますか？

#### --distractors--

フレックスコンテナの子要素の方向を制御します。

---

フレックスコンテナの子要素のテキストスタイルを制御します。

---

フレックスコンテナ内の子要素の整列を制御します。

#### --answer--

主軸上の子要素の分布を制御します。

### --question--

#### --text--

フレックスコンテナ内の要素の方向を制御するプロパティは何ですか？

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

フレックスアイテムを左から右に流し、現在の行の幅を超えた場合に新しいフレックスアイテムが前のアイテムの上に表示されるようにするCSSプロパティはどれですか？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

交差軸上の要素の位置を制御するプロパティは何ですか？

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

`justify-content` の値として存在しないものはどれですか？

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

フレックスコンテナ内の要素を水平方向と垂直方向の両方で中央に配置するために使用するCSSプロパティは何ですか？

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

`flex-direction` のどの値がフレックスアイテムを右から左に並べますか？

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

`flex-wrap` プロパティの値として存在しないものはどれですか？

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

`stretch` の `align-items` 値は何をしますか？

#### --distractors--

アイテムがコンテナの開始端に向かって直接詰められます。

---

アイテムがフレックスコンテナのベースラインに揃えられます。

---

アイテムがコンテナの終了端に向かって直接詰められます。

#### --answer--

アイテムのサイズを自動調整してコンテナを均等に埋めます。

### --question--

#### --text--

主軸上で最初のアイテムを開始位置に、最後のアイテムを終了位置に配置し、他のアイテムを均等に間隔を空けて配置する`justify-content`の値はどれですか？

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

隣接するすべてのアイテム間の間隔とコンテナの端の間隔がすべて等しくなるようにアイテムを配置する`justify-content`の値はどれですか？

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

主軸上でアイテムを均等に配置し、コンテナの端にはその半分の間隔を空ける`justify-content`の値はどれですか？

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

交差軸の開始位置にアイテムを揃える結果になるのはどれですか？

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

交差軸の終了位置にアイテムを揃える結果になるのはどれですか？

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

`flex-direction` のどの値が主軸上でアイテムを左から右に揃えますか？

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

`flex-direction` のどの値が交差軸上でアイテムを上から下に揃えますか？

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

次のうちflexboxに適さないユースケースはどれですか？

#### --distractors--

コンテナ内のアイテムを中央に配置すること。

---

行内のアイテムを垂直方向に揃えること。

---

異なるビューポートに応じてアイテムの分布と整列を動的に調整すること。

#### --answer--

グリッドのようにアイテムのサイズと間隔を固定すること。

## --quiz--

### --question--

#### --text--

Flexboxのような1次元レイアウトモデルの目的は何ですか？

#### --distractors--

行と列の両方を同時に扱う2次元レイアウト。

---

`z-index` を使って位置指定要素の重なり順を制御すること。

---

ページ要素に3D変形や複雑なアニメーションを作成すること。

#### --answer--

アイテムを1本の線または軸に沿って、行または列として配置すること。

### --question--

#### --text--

`.container` 要素内のすべてのフレックスアイテムを右側に揃えるCSSプロパティはどれですか？

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Flexモデルでアイテムの配置方向を定義する2つの軸は何ですか？

#### --distractors--

水平軸と垂直軸。

---

x軸とy軸。

---

主軸と副軸。

#### --answer--

主軸と交差軸。

### --question--

#### --text--

次のうち、アイテムを縦方向の列に並べ、コンテナ内で水平方向に中央揃えにするCSSルールはどれですか？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

`flex-wrap` プロパティの主な目的は何ですか？

#### --distractors--

フレックスアイテムがフレックスコンテナ内に配置される方向を設定します。

---

フレックスアイテムを主軸に沿って揃えます。

---

フレックスアイテムを交差軸に沿って揃えます。

#### --answer--

フレックスアイテムが利用可能なスペースに合わせて新しい行に折り返すかどうかを決定します。

### --question--

#### --text--

`flex-wrap: nowrap;` 宣言のデフォルトの動作は何ですか？

#### --distractors--

アイテムがコンテナに収まらない場合、新しい行に折り返します。

---

アイテムを新しい行に折り返しますが、逆順になります。

---

フレックスコンテナが親要素より大きくなるのを防ぎます。

#### --answer--

アイテムがオーバーフローしても1行にとどまります。

### --question--

#### --text--

`flex-direction` と `flex-wrap` の両方を一度に設定するショートハンドのCSSプロパティはどれですか？

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

flexコンテナに`flex-direction: column-reverse;`を設定するとどうなりますか？

#### --distractors--

アイテムが右から左に水平に並びます。

---

アイテムが1行で逆順に並びます。

---

アイテムが上から下に垂直に並びます。

#### --answer--

アイテムが下から上に逆順で垂直に並びます。

### --question--

#### --text--

次のCSSスニペットのうち、flexアイテムを縦方向の列に正しく並べ、コンテナの高さを超えた場合に新しい列に折り返すものはどれですか？

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

`justify-content` のどの値がコンテナの主軸の終わりにすべてのフレックスアイテムを揃えますか？

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

次のCSSルールがフレックスアイテムに与える効果は何ですか？

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

アイテムは列に並び、新しい列に折り返します。

---

アイテムは行に並び、新しい行に折り返します。

---

アイテムは列に並び、収まらなければオーバーフローします。

#### --answer--

アイテムは行に並び、収まらなければコンテナをオーバーフローします。

### --question--

#### --text--

`justify-content: space-evenly;` はフレックスアイテムをどのように整列させますか？

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

アイテムは均等に間隔を空けられ、開始端と終了端にはスペースがありません。

---

アイテムはコンテナの中央に詰められます。

---

アイテムはコンテナの右側に押しやられ、間隔は均等です。

#### --answer--

アイテム間と両端に均等なスペースがあります。

### --question--

#### --text--

`flex-direction: row;` が設定されている場合、アイテムを垂直方向に中央揃えにするにはどのプロパティを使いますか？

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

`flex-start` プロパティの `justify-content` 値は何をしますか？

#### --distractors--

アイテムを交差軸の開始位置に揃えます。

---

アイテムをコンテナの主軸いっぱいに伸ばします。

---

すべてのアイテムを主軸の中央に揃えます。

#### --answer--

アイテムをコンテナの主軸の開始位置に揃えます。

### --question--

#### --text--

フレックスコンテナ内のアイテムを右から左に1行で並べ、かつコンテナの上部に配置するにはどう設定しますか？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

`flex-flow` プロパティの有効な宣言はどれですか？

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

次のCSSルールのうち、フレックスアイテムを水平方向に中央揃えし、垂直方向にコンテナの高さいっぱいに伸ばすものはどれですか？

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

`align-items: stretch;` が `flex-direction` に設定されている場合、`column` はどのような効果がありますか？

#### --distractors--

アイテムを垂直方向に伸ばしてコンテナの高さを満たします。

---

方向が `column` の場合は効果がありません。

---

アイテムを垂直方向に伸ばして最も高いアイテムに合わせます。

#### --answer--

アイテムを水平方向に伸ばしてコンテナの幅を満たします。

### --question--

#### --text--

水平方向の行でアイテムの順序を逆にしたい場合、どの宣言を使いますか？

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

`align-items` のどの値が `flex-direction` が `row` のコンテナ内でアイテムを上部に配置しますか？

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
