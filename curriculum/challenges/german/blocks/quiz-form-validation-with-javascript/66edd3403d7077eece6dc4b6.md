---
id: 66edd3403d7077eece6dc4b6
title: Quiz zur Formularvalidierung mit JavaScript
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche Methode wird verwendet, um zu prüfen, ob der Wert eines `input`-Elements die Validierungsregeln erfüllt?

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

Was passiert, wenn die `checkValidity()`-Methode ein ungültiges `input` findet?

#### --distractors--

Das Formular wird automatisch abgesendet.

---

Die Seite aktualisiert sich automatisch.

---

Der `input`-Wert wird gelöscht.

#### --answer--

Es gibt `false` zurück, ohne eine Fehlermeldung für den Benutzer anzuzeigen.

### --question--

#### --text--

Welche Methode wird verwendet, um die Nachricht anzupassen, die bei einem Validierungsfehler angezeigt wird?

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

Wie verhält sich die Validierung standardmäßig bei Pflichtfeldern, wenn das Formular abgesendet wird?

#### --distractors--

Das Formular wird abgesendet, auch wenn Pflichtfelder leer sind.

---

Das Formular wird abgesendet, gibt aber eine Warnung aus.

---

Das Formular setzt die `input`-Werte zurück.

#### --answer--

Der Browser verhindert die Formularabsendung, wenn Pflichtfelder leer sind.

### --question--

#### --text--

Welche Aktion führt nicht dazu, dass ein HTML-Formular auf Fehler überprüft wird?

#### --distractors--

Ändern des `input`-Werts.

---

Absenden des Formulars.

---

Aufrufen von `reportValidity()`.

#### --answer--

Klicken auf eine Zurücksetzen-Schaltfläche.

### --question--

#### --text--

Was gibt `checkValidity()` zurück, wenn alle Formulareingaben gültig sind?

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

Was bedeutet es, wenn `patternMismatch` `true` ist?

#### --distractors--

Das Feld ist leer.

---

Der Wert ist zu lang.

---

Das Feld ist deaktiviert.

#### --answer--

Der Wert entspricht nicht dem `pattern`-Attribut.

### --question--

#### --text--

Was passiert, wenn `reportValidity()` aufgerufen wird, aber ein Feld die Validierung nicht besteht?

#### --distractors--

Das Formular wird ohne Fehler abgesendet.

---

Die `input`-Werte werden gelöscht.

---

Das Formular schließt sich sofort.

#### --answer--

Das ungültige Feld wird hervorgehoben und eine Nachricht erscheint.

### --question--

#### --text--

Welche Aktion löst die eingebaute Formularvalidierung des Browsers aus?

#### --distractors--

Zurücksetzen des Formulars.

---

Ändern eines `input`-Werts.

---

Klicken auf einen Anker-Tag.

#### --answer--

Manuelles Absenden des Formulars.

### --question--

#### --text--

Was ist der Zweck des Aufrufs von `checkValidity()` vor dem Absenden eines Formulars?

#### --distractors--

Das Formular automatisch absenden.

---

Die Seite neu laden.

---

Alle `input`-Felder löschen.

#### --answer--

Prüfen, ob alle `input`-Elemente ihre angegebenen Einschränkungen erfüllen.

### --question--

#### --text--

Was ist der Hauptzweck von `e.preventDefault()`?

#### --distractors--

Das Formular neu laden.

---

Formulardaten löschen.

---

Formularwerte zurücksetzen.

#### --answer--

Die Standardaktion eines Ereignisses verhindern.

### --question--

#### --text--

Was bewirkt `e.preventDefault()`, wenn es beim Absende-Ereignis eines Formulars verwendet wird?

#### --distractors--

Es sendet das Formular ab.

---

Es löscht alle Formularfelder.

---

Es deaktiviert das Formular vorübergehend.

#### --answer--

Es verhindert, dass das Formular abgesendet wird.

### --question--

#### --text--

In welchem Szenario ist `e.preventDefault()` am nützlichsten?

#### --distractors--

Beim Umgang mit einem Button-Klick, der ein Formular absendet.

---

Beim Schließen eines modalen Dialogs.

---

Beim Öffnen einer neuen Seite.

#### --answer--

Beim Absenden eines Formulars und Verhindern eines Seiten-Reloads.

### --question--

#### --text--

Was passiert, wenn Sie vergessen, `e.preventDefault()` beim Absende-Ereignis eines Formulars aufzurufen?

#### --distractors--

Alle Eingabefelder werden auf ihre Standardwerte gesetzt.

---

Das Formular wird nicht abgesendet.

---

Die Formulardaten gehen verloren.

#### --answer--

Das Formular wird abgesendet und die Seite wird aktualisiert.

### --question--

#### --text--

Welches Standardverhalten kann mit `e.preventDefault()` verhindert werden?

#### --distractors--

Ein Formular bearbeiten.

---

Das Formular rendern.

---

Das Formular neu laden.

#### --answer--

Das Standardverhalten, das durch das Absenden eines Formulars ausgelöst wird.

### --question--

#### --text--

Was löst das Absende-Ereignis eines Formulars aus?

#### --distractors--

Wenn sich ein Eingabefeld ändert.

---

Wenn das Formular zurückgesetzt wird.

---

Wenn die Aktion des Formulars gelöscht wird.

#### --answer--

Wenn die Absenden-Schaltfläche eines Formulars geklickt wird oder Enter gedrückt wird.

### --question--

#### --text--

Wo behandeln Sie normalerweise das Absende-Ereignis in JavaScript?

#### --distractors--

An einem Texteingabe-Element.

---

An einem beliebigen Feld im Formular.

---

An einem Button-Element außerhalb des Formulars.

#### --answer--

Am Formularelement selbst.

### --question--

#### --text--

Was bewirkt das Absende-Ereignis standardmäßig?

#### --distractors--

Es validiert das Formular, sendet es aber nicht ab.

---

Es löscht die Formularfelder.

---

Es bricht die Formularabsendung ab.

#### --answer--

Es sendet die Formulardaten an den im Formular angegebenen Server.

### --question--

#### --text--

Welchem Ereignis sollten Sie lauschen, um Formulardaten zu verarbeiten, wenn das Formular in JavaScript abgesendet wird?

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

Welchen Standardwert hat das `method`-Attribut in einem HTML-Formular?

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
