---
id: 66f1affc0ef4fcca423d4688
title: Bash 和 SQL 测验
challengeType: 8
dashedName: quiz-bash-and-sql
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

SQL 代表什么？

#### --distractors--

标准查询语言。

---

简单量子语言。

---

复杂的查询语言。

#### --answer--

结构化查询语言。

### --question--

#### --text--

哪个 SQL `Command` 用于 `create` 一个 `New` `database`？

#### --distractors--

`BUILD DATABASE`

---

`GENERATE DATABASE`

---

`NEW DATABASE`

#### --answer--

`CREATE DATABASE`

### --question--

#### --text--

什么是第一范式（1NF）？

#### --distractors--

没有冗余数据。

---

无部分依赖。

---

没有传递依赖。

#### --answer--

列中的原子型值。

### --question--

#### --text--

如何从一个表格中选择所有列？

#### --distractors--

`SELECT []`

---

`SELECT ALL`

---

`SELECT COLUMNS`

#### --answer--

`SELECT *`

### --question--

#### --text--

`FOREIGN KEY` 建立了什么？

#### --distractors--

唯一的标识符密钥。

---

一个主密钥。

---

一个空约束。

#### --answer--

表格之间的关系。

### --question--

#### --text--

SQL 中归一化的目的是什么？

#### --distractors--

加快查询速度。

---

增加存储空间。

---

添加更多索引。

#### --answer--

减少数据冗余。

### --question--

#### --text--

防止 SQL 注入的最佳防御措施是什么？

#### --distractors--

删除所有用户输入。

---

禁用 SQL 访问。

---

移除数据库访问。

#### --answer--

使用预处理语句。

### --question--

#### --text--

哪个命令修改现有的表格数据？

#### --distractors--

`MODIFY`

---

`CHANGE`

---

`ALTER`

#### --answer--

`UPDATE`

### --question--

#### --text--

在连接表中，通常包含什么？

#### --distractors--

仅外部密钥。

---

只有时间戳。

---

仅限主密钥。

#### --answer--

外键和附加数据。

### --question--

#### --text--

哪个命令将新数据添加到 `table`？

#### --distractors--

`ADD`

---

`PUT`

---

`NEW DATA`

#### --answer--

`INSERT`

### --question--

#### --text--

哪个密钥可以有副本值？

#### --distractors--

主密钥。

---

唯一密钥。

---

复合密钥。

#### --answer--

外键。

### --question--

#### --text--

第三范式（3NF）消除了什么？

#### --distractors--

多值依赖。

---

部分依赖。

---

所有依赖项。

#### --answer--

传递依赖。

### --question--

#### --text--

你如何过滤 SQL 结果？

#### --distractors--

`FILTER`

---

`SORT`

---

`TAGS`

#### --answer--

`WHERE`

### --question--

#### --text--

SQL 中 N+1 问题的主要特征是什么？

#### --distractors--

执行一个大型、复杂的 `query` 并将其发送回 `server`。

---

一次返回过多数据但按字母顺序排序的查询。

---

在单个 `query` 中使用过多的 `JOIN` 并返回过多的 `data`。

#### --answer--

执行一个初始查询，随后执行 N 个用于相关数据的附加查询。

### --question--

#### --text--

以下哪项是 `Super Key` 和 `Candidate Key` 之间的关键区别？

#### --distractors--

超级密钥不能包含多个属性或列，而候选密钥可以。

---

候选密钥必须是单个列，不能是复合密钥，而超级密钥可以是复合的。

---

超级密钥由数据库管理员选择，并且不必唯一。

#### --answer--

超级密钥可以包含对唯一标识不必要的额外属性，而候选密钥则不能。

### --question--

#### --text--

在归一化时，我们如何称呼仅依赖于复合密钥部分的 `data`？

#### --distractors--

传递依赖。

---

多值依赖

---

函数依赖。

#### --answer--

部分依赖。

### --question--

#### --text--

在 Bash 中，`>` 操作符的主要用途是什么？

#### --distractors--

将输出追加到文件并在 `tmp` 中创建一个拷贝。

---

从文件读取输入并进行编码。

---

比较两个文件的差异并打印差异。

#### --answer--

将标准输出重定向到一个 `file`，如果该 `file` 存在则覆盖它。

### --question--

#### --text--

`wc -l` 命令的作用是什么？

#### --distractors--

计算文件中的单词数。

---

计算文件中的字符数。

---

列出当前目录中的所有文件。

#### --answer--

计算 `file` 中的行数。

### --question--

#### --text--

在 Bash 脚本中，如何访问传递给它的第一个参数？

#### --distractors--

`$ARG1`

---

`$0`

---

`$#`

#### --answer--

`$1`

### --question--

#### --text--

在学生和课程之间的多对多关系中，连接表包含什么？

#### --distractors--

仅限学生 ID。

---

仅课程 ID。

---

完整的学生和课程详情。

#### --answer--

ID 和注册日期。

