---
id: 66edd3403d7077eece6dc4b6
title: JavaScript를 이용한 폼 유효성 검사 퀴즈
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제에 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

`input` 요소의 값이 유효성 검사 규칙을 충족하는지 확인하는 데 사용되는 메서드는 무엇인가요?

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

`checkValidity()` 메서드가 유효하지 않은 `input`를 발견하면 어떤 일이 발생하나요?

#### --distractors--

폼이 자동으로 제출됩니다.

---

아무 일도 일어나지 않습니다.

---

`input` 값이 지워집니다.

#### --answer--

브라우저가 유효성 검사 메시지를 표시합니다.

### --question--

#### --text--

유효성 검사 실패 시 표시되는 메시지를 사용자 지정하는 데 사용되는 메서드는 무엇인가요?

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

폼이 제출될 때 필수 필드에 대한 기본 유효성 검사 동작은 무엇인가요?

#### --distractors--

필수 필드가 비어 있어도 폼이 제출됩니다.

---

폼이 제출되지만 경고가 표시됩니다.

---

폼이 `input` 값을 초기화합니다.

#### --answer--

필수 필드가 비어 있으면 브라우저가 폼 제출을 정지합니다.

### --question--

#### --text--

어떤 동작이 HTML 폼의 오류 검사를 발생시키지 않나요?

#### --distractors--

`input` 값을 변경하는 경우.

---

폼을 제출하는 경우.

---

`reportValidity()`를 호출하는 경우.

#### --answer--

리셋 버튼을 클릭하는 경우.

### --question--

#### --text--

모든 폼 입력이 유효할 때 `checkValidity()`는 무엇을 반환하나요?

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

`checkValidity()` 메서드는 언제 폼 제출을 정지하나요?

#### --distractors--

폼에 제출 버튼이 없을 때.

---

폼 `action`가 비어 있을 때.

---

폼 메서드가 POST일 때.

#### --answer--

폼에 하나 이상의 유효하지 않은 `input`가 있을 때(그리고 기본 폼 제출 동작이 방지될 때).

### --question--

#### --text--

`checkValidity()`가 호출되었지만 필드가 유효성 검사에 실패하면 어떤 일이 발생하나요?

#### --distractors--

폼이 오류 없이 제출됩니다.

---

`input` 값이 지워집니다.

---

폼이 즉시 닫힙니다.

#### --answer--

유효하지 않은 필드가 강조 표시되고 메시지가 나타납니다.

### --question--

#### --text--

어떤 동작이 `checkValidity()`를 실행하게 하나요?

#### --distractors--

폼을 리셋하는 경우.

---

`input` 값을 변경하는 경우.

---

앵커 태그를 클릭하는 경우.

#### --answer--

폼을 수동으로 제출하는 경우.

### --question--

#### --text--

폼 제출 전에 `checkValidity()`를 호출하는 목적은 무엇인가요?

#### --distractors--

폼을 자동으로 제출하기 위해서.

---

페이지를 새로 고침하기 위해서.

---

모든 `input` 필드를 지우기 위해서.

#### --answer--

모든 `input` 요소가 지정된 제약 조건을 충족하는지 확인하기 위해서.

### --question--

#### --text--

`e.preventDefault()`의 주요 목적은 무엇인가요?

#### --distractors--

폼을 새로 고침하기 위해서.

---

폼 데이터를 지우기 위해서.

---

폼 값을 초기화하기 위해서.

#### --answer--

이벤트의 기본 동작을 정지하기 위해서.

### --question--

#### --text--

폼의 제출 이벤트에서 `e.preventDefault()`는 무엇을 하나요?

#### --distractors--

폼을 제출합니다.

---

모든 폼 필드를 지웁니다.

---

폼을 일시적으로 비활성화합니다.

#### --answer--

폼 제출을 방지합니다.

### --question--

#### --text--

어떤 상황에서 `e.preventDefault()`가 가장 유용한가요?

#### --distractors--

폼을 제출하는 버튼 클릭을 처리할 때.

---

모달 대화상자를 닫을 때.

---

새 페이지를 열 때.

#### --answer--

폼 제출 시 페이지 새로 고침을 방지할 때.

### --question--

#### --text--

폼 제출 이벤트에서 `e.preventDefault()`를 호출하는 것을 잊으면 어떤 일이 발생하나요?

#### --distractors--

모든 입력 필드가 기본값으로 설정됩니다.

---

폼이 제출되지 않습니다.

---

폼 데이터가 손실됩니다.

#### --answer--

폼이 제출되고 페이지가 새로 고침됩니다.

### --question--

#### --text--

어떤 기본 동작을 `e.preventDefault()`를 사용해 방지할 수 있나요?

#### --distractors--

폼 편집.

---

폼 렌더링.

---

폼 새로 고침.

#### --answer--

폼 제출로 인한 기본 동작.

### --question--

#### --text--

폼의 제출 이벤트는 무엇에 의해 발생하나요?

#### --distractors--

입력 필드가 변경될 때.

---

폼이 리셋될 때.

---

폼의 action이 지워질 때.

#### --answer--

폼의 제출 버튼을 클릭하거나 Enter 키를 누를 때.

### --question--

#### --text--

JavaScript에서 보통 어디서 제출 이벤트를 처리하나요?

#### --distractors--

텍스트 입력 요소에서.

---

폼 내의 아무 필드에서나.

---

폼 외부의 버튼 요소에서.

#### --answer--

폼 요소 자체에서.

### --question--

#### --text--

기본적으로 제출 이벤트는 무엇을 하나요?

#### --distractors--

폼을 유효성 검사하지만 제출하지는 않습니다.

---

폼 필드를 지웁니다.

---

폼 제출을 취소합니다.

#### --answer--

폼의 action에 지정된 서버로 폼 데이터를 전송합니다.

### --question--

#### --text--

JavaScript에서 폼이 제출될 때 폼 데이터를 처리하려면 어떤 이벤트를 들어야 하나요?

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

HTML 폼에서 `method` 속성의 기본값은 무엇인가요?

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
