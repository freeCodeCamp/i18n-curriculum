---
id: 65e97251484dd50f720e6fe8
title: 学习数据类型和条件式 第 C 课
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-c
---

# --description--

看到下面的例子，你想确保字串中存在单词 `loves`。

```javascript
let sentence = "freeCodeCamp loves The Odin Project!";
```

<a href="https://www.freecodecamp.org/news/javascript-string-handbook" target="_blank"> 指南链接 </a>

# --questions--

## --text--

应该使用哪种方法来查看字串中是否存在单词 `loves`？

## --answers--

应使用 `includes()` 方法来查看字串中是否存在单词 `loves`。

---

应使用 `indexOf()` 方法查看字串中是否存在单词 `loves`。

---

应使用 `search()` 方法来查看字串中是否存在单词 `loves`。

---

应使用 `toUpperCase()` 方法来查看字串中是否存在单词 `loves`。

## --video-solution--

1
