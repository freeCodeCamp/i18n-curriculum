---
id: 66ed9034f45ce3ece4053ebd
title: 반응형 웹 디자인 퀴즈
challengeType: 8
dashedName: quiz-responsive-web-design
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

반응형 웹 디자인에서 브레이크포인트란 무엇인가요?

#### --distractors--

디자인에서 플로트가 페이지의 다른 요소와 겹치는 특정 지점입니다.

---

그리드 또는 플렉스 레이아웃의 열과 행을 설정하는 데 사용되는 특정 지점입니다.

---

테이블 레이아웃이 페이지에서 어떻게 동작할지 결정하는 데 사용되는 특정 지점입니다.

#### --answer--

다양한 화면 크기에 맞게 레이아웃과 콘텐츠가 조정되는 디자인의 특정 지점입니다.

### --question--

#### --text--

다음 중 작은 기기용 브레이크포인트가 아닌 것은 무엇인가요?

#### --distractors--

`600px`

---

`480px`

---

`320px`

#### --answer--

`960px`

### --question--

#### --text--

반응형 웹 디자인에서 미디어 쿼리를 사용하는 주요 목적은 무엇인가요?

#### --distractors--

Safari 브라우저용 페이지 색상표를 변경하기 위해서입니다.

---

테이블 레이아웃에 애니메이션을 만들기 위해서입니다.

---

CSS 플렉스박스를 사용하는 레이아웃의 여백과 패딩을 조정하기 위해서입니다.

#### --answer--

화면 크기나 기기 유형에 따라 다른 스타일을 적용하기 위해서입니다.

### --question--

#### --text--

미디어 쿼리에서 브라우저 창의 너비를 확인하는 미디어 기능은 무엇인가요?

#### --distractors--

`set-width`

---

`accept-width`

---

`allow-width`

#### --answer--

`min-width`

### --question--

#### --text--

다음 코드는 무엇을 할까요?

```css
@media screen and (min-width: 768px) {
  /* Styles go here */
}
```

#### --distractors--

이 코드는 `768px` 너비인 화면에 대한 스타일을 무시합니다.

---

이 코드는 `768px` 너비인 화면에만 스타일을 적용합니다.

---

이 코드는 `768px`보다 작은 화면에 스타일을 적용합니다.

#### --answer--

이 코드는 `768px` 이상 너비인 화면에 스타일을 적용합니다.

### --question--

#### --text--

모바일 퍼스트 디자인이 강조하는 점은 무엇인가요?

#### --distractors--

작은 기기만 디자인하고 다른 기기는 무시하는 것입니다.

---

Apple에서 만든 모바일 기기만 디자인하는 것입니다.

---

작은 Android 화면을 먼저 디자인하고 Apple 기기는 무시하는 것입니다.

#### --answer--

작은 화면을 먼저 디자인하고 큰 기기로 확장하는 것입니다.

### --question--

#### --text--

기기의 너비가 600px에서 1200px 사이일 때 스타일을 적용하는 미디어 쿼리는 무엇인가요?

#### --distractors--

`@media screen and (max-width: 1200px)`

---

`@media screen and (min-width: 600px)`

---

`@media screen and (width: 800px)`

#### --answer--

`@media screen and (min-width: 600px) and (max-width: 1200px)`

### --question--

#### --text--

다음 중 유효하지 않은 미디어 타입은 무엇인가요?

#### --distractors--

`all`

---

`print`

---

`screen`

#### --answer--

`poster`

### --question--

#### --text--

다음 중 인쇄 미리보기 모드에서 화면에 문서와 페이지 자료를 표시하는 데 사용되는 미디어 타입은 무엇인가요?

#### --distractors--

`aspect-ratio`

---

`flex`

---

`screen`

#### --answer--

`print`

### --question--

#### --text--

미디어 쿼리에서 `aspect-ratio`는 무엇을 하나요?

#### --distractors--

플렉스 레이아웃에서 플렉스 속성 간 비율을 설명합니다.

---

그리드 컨테이너의 x축과 y축 비율을 설명합니다.

---

테이블 레이아웃의 행과 열 비율을 설명합니다.

#### --answer--

뷰포트의 너비와 높이 비율을 설명합니다.

### --question--

#### --text--

기기가 가로 방향인지 세로 방향인지 나타내는 데 사용되는 것은 무엇인가요?

#### --distractors--

`apply-orientation`

---

`set-orientation`

---

`oriente`

#### --answer--

`orientation`

### --question--

#### --text--

데스크톱 화면 이상을 타겟팅하는 데 일반적으로 사용되는 것은 무엇인가요?

#### --distractors--

```css
@media screen and (min-width: 140000px) {
  /* Styles go here */
}
```

---

```css
@media screen and (min-width: 140px) {
  /* Styles go here */
}
```

---

```css
@media screen and (min-width: 14000px) {
  /* Styles go here */
}
```

#### --answer--

```css
@media screen and (min-width: 1400px) {
  /* Styles go here */
}
```

### --question--

#### --text--

사용자가 밝거나 어두운 색상 테마를 요청했는지 감지하는 데 사용되는 것은 무엇인가요?

#### --distractors--

`allow-colors-scheme`

---

`apply-color-scheme`

---

`set-colors-scheme`

#### --answer--

`prefers-color-scheme`

### --question--

#### --text--

기본 입력 장치가 요소 위에 호버할 수 있는지 테스트하는 데 사용되는 것은 무엇인가요?

#### --distractors--

```css
@media (set: hover) {
  /* Styles for devices that support hover */
}
```

---

```css
@media (apply: hover) {
  /* Styles for devices that support hover */
}
```

---

```css
@media (hover: apply) {
  /* Styles for devices that support hover */
}
```

#### --answer--

```css
@media (hover: hover) {
  /* Styles for devices that support hover */
}
```

### --question--

#### --text--

미디어 쿼리에서 사용할 수 없는 논리 연산자 유형은 무엇인가요?

#### --distractors--

`only`

---

`not`

---

`and`

#### --answer--

`accept`

### --question--

#### --text--

반응형 웹 디자인에서 태블릿에 일반적으로 사용되는 브레이크포인트는 무엇인가요?

#### --distractors--

`480px`

---

`1920px`

---

`2560px`

#### --answer--

`768px`

### --question--

#### --text--

미디어 쿼리에서 `aspect-ratio`를 적용하는 올바른 방법은 무엇인가요?

#### --distractors--

```css
@media screen and (aspect-ratio: 16-9) {
  /* Styles for screens with a 16:9 aspect ratio */
}
```

---

```css
@media screen and (aspect-ratio: 16=9) {
  /* Styles for screens with a 16:9 aspect ratio */
}
```

---

```css
@media screen and (aspect-ratio: 16:9) {
  /* Styles for screens with a 16:9 aspect ratio */
}
```

#### --answer--

```css
@media screen and (aspect-ratio: 16/9) {
  /* Styles for screens with a 16:9 aspect ratio */
}
```

### --question--

#### --text--

미디어 쿼리 없이 화면 크기에 따라 조정되는 레이아웃을 만들 때 사용하는 CSS 속성은 무엇인가요?

#### --distractors--

`float`

---

`display: block;`

---

`width: 100%;`

#### --answer--

`flex`

### --question--

#### --text--

`768px` 이상 가로 방향 화면을 타겟팅하는 데 사용할 수 있는 것은 무엇인가요?

#### --distractors--

```css
@media screen and (min-width: 768px) and (landscape: orientation) {
  /* Styles go here */
}
```

---

```css
@media screen and (max-width: 768px) and (orientation: landscape) {
  /* Styles go here */
}
```

---

```css
@media screen and (min-width: 768px) and (landscape: set) {
  /* Styles go here */
}
```

#### --answer--

```css
@media screen and (min-width: 768px) and (orientation: landscape) {
  /* Styles go here */
}
```

### --question--

#### --text--

미디어 타입이 지정되지 않았을 때 기본 미디어 타입은 무엇인가요?

#### --distractors--

`screen`

---

`mobile`

---

`print`

#### --answer--

`all`

## --quiz--

### --question--

#### --text--

반응형 디자인에서 유동적인 레이아웃을 만들 때 일반적으로 사용하는 CSS 단위는 무엇인가요?

#### --distractors--

`px`

---

`em`

---

`rem`

#### --answer--

`%`

### --question--

#### --text--

반응형 웹 디자인에서 미디어 쿼리를 사용하는 주요 목적은 무엇인가요?

#### --distractors--

사용자 입력에 따라 다른 스타일을 적용하기 위해서입니다.

---

웹사이트 로딩 속도를 빠르게 하기 위해서입니다.

---

웹사이트 색상표를 자동으로 변경하기 위해서입니다.

#### --answer--

화면 크기에 따라 레이아웃과 디자인을 조정하기 위해서입니다.

### --question--

#### --text--

어떤 CSS `width` 값이 부모 컨테이너 너비에 따라 요소가 비례적으로 크기 조절되어 반응형 디자인에 적합한가요?

#### --distractors--

`auto`

---

`100vh`

---

`fit-content`

#### --answer--

`100%`

### --question--

#### --text--

미디어 쿼리에서 사용할 수 있는 미디어 기능의 예는 무엇인가요?

#### --distractors--

`background-color`

---

`font-size`

---

`text-align`

#### --answer--

`min-width`

### --question--

#### --text--

`@media screen and (max-width: 768px)` 미디어 쿼리는 무엇에 스타일을 적용하나요?

#### --distractors--

너비가 768px 이상인 화면에 스타일을 적용합니다.

---

크기에 상관없이 모든 화면에 스타일을 적용합니다.

---

해상도가 768dpi 이상인 화면에 스타일을 적용합니다.

#### --answer--

너비가 768px 미만인 화면에 스타일을 적용합니다.

### --question--

#### --text--

해상도가 최소 300dpi인 기기를 타겟팅하는 미디어 쿼리 기능은 무엇인가요?

#### --distractors--

`@media screen and (min-width: 300dpi)`

---

`@media screen and (resolution: 300dpi)`

---

`@media screen and (min-res: 300dpi)`

#### --answer--

`@media screen and (min-resolution: 300dpi)`

### --question--

#### --text--

호버 상호작용을 지원하지 않는 기기에만 스타일을 적용하려면 어떤 미디어 기능을 사용하나요?

#### --distractors--

`@media (hover: hover)`

---

`@media (hover: auto)`

---

`@media (no-hover: false)`

#### --answer--

`@media (hover: none)`

### --question--

#### --text--

미디어 쿼리로 세로 방향 기기를 타겟팅하는 올바른 방법은 무엇인가요?

#### --distractors--

`@media screen and (orientation: landscape) { ... }`

---

`@media (portrait: orientation) { ... }`

---

`@media screen and (min-orientation: portrait) { ... }`

#### --answer--

`@media screen and (orientation: portrait) { ... }`

### --question--

#### --text--

기기 해상도에 따라 스타일을 적용하는 미디어 쿼리 기능은 무엇인가요?

#### --distractors--

`@media (resolution: high) { ... }`

---

`@media screen and (resolution: 300dpi) { ... }`

---

`@media (min-resolution: 150) { ... }`

#### --answer--

`@media (min-resolution: 300dpi) { ... }`

### --question--

#### --text--

CSS에서 `@media (hover: hover)` 쿼리의 목적은 무엇인가요?

#### --distractors--

터치 기기에서만 스타일을 적용합니다.

---

기기 방향에 따라 스타일을 변경합니다.

---

기기가 터치스크린을 사용하는지 감지합니다.

#### --answer--

기기가 마우스 호버를 지원하는지 확인합니다.

### --question--

#### --text--

레티나 디스플레이 같은 고해상도 화면에만 스타일을 적용하려면 어떤 미디어 기능을 사용하나요?

#### --distractors--

`device-type`

---

`aspect-ratio`

---

`min-width`

#### --answer--

`min-resolution`

### --question--

#### --text--

미디어 쿼리에서 여러 조건을 결합할 때 사용할 수 있는 키워드는 무엇인가요?

#### --distractors--

if

---

then

---

combine

#### --answer--

and

### --question--

#### --text--

미디어 쿼리와 함께 특정 조건에서만 스타일을 적용하는 데 일반적으로 사용하는 CSS 기법은 무엇인가요?

#### --distractors--

모든 요소에 인라인 스타일을 사용하는 것

---

JavaScript로 화면 크기를 감지해 클래스를 적용하는 것

---

모바일과 데스크톱용으로 다른 HTML 파일을 연결하는 것

#### --answer--

미디어 쿼리에서 `and` 같은 논리 연산자를 사용해 미디어 기능을 결합하는 것

### --question--

#### --text--

다양한 화면 크기에서 이미지가 비례적으로 크기 조절되도록 돕는 CSS 기법은 무엇인가요?

#### --distractors--

고정 픽셀 너비를 사용하는 것

---

이미지를 배경 요소로 설정하는 것

---

정확한 크기로 인라인 스타일을 적용하는 것

#### --answer--

이미지 너비를 `width: 100%` 같은 백분율로 설정하는 것

### --question--

#### --text--

`@media screen and (min-resolution: 300dpi)` 쿼리의 목적은 무엇인가요?

#### --distractors--

저해상도 화면에 스타일을 적용합니다.

---

화면 너비에 따라 스타일을 적용합니다.

---

기기 해상도에 따라 스타일을 적용합니다.

#### --answer--

고해상도 화면에 스타일을 적용합니다.

### --question--

#### --text--

반응형 디자인에서 모바일 퍼스트 접근법에 대해 올바른 설명은 무엇인가요?

#### --distractors--

큰 데스크톱 화면부터 디자인하고 점차 축소합니다.

---

태블릿 화면을 먼저 디자인하고 점차 축소합니다.

---

스마트폰만 타겟팅하고 태블릿이나 데스크톱은 무시합니다.

#### --answer--

모바일 기기를 먼저 디자인하고 큰 화면으로 확장합니다.

### --question--

#### --text--

화면 방향(가로 또는 세로)을 감지하는 미디어 쿼리 기능은 무엇인가요?

#### --distractors--

`aspect-ratio`

---

`device-width`

---

`max-width`

#### --answer--

`orientation`

### --question--

#### --text--

`prefers-color-scheme` 미디어 기능은 무엇을 감지하나요?

#### --distractors--

`The type of device used for display`

---

`The screen resolution`

---

`The user's preferred font size`

#### --answer--

`Whether the user prefers a light or dark theme`

### --question--

#### --text--

`orientation` 미디어 기능은 무엇을 감지하나요?

#### --distractors--

`The width of the screen`

---

`The resolution of the display`

---

`The type of device (tablet, phone, etc.)`

#### --answer--

`Whether the device is in landscape or portrait mode`

### --question--

#### --text--

픽셀 밀도가 최소 2배 이상인 기기에 스타일을 적용하는 올바른 구문은 무엇인가요?

#### --distractors--

`@media (resolution: 2x)`

---

`@media screen and (pixel-ratio: 2)`

---

`@media (dpi: 300)`

#### --answer--

`@media screen and (min-resolution: 192dpi)`
