---
id: 6a298d88d1606c0f1ef322a9
title: Quiz de autenticación y autorización
challengeType: 8
dashedName: quiz-authentication-and-authorization
---

# --description--

Para aprobar el quiz, debes responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Cuáles son las tres partes de un JSON Web Token?

#### --distractors--

Encabezado, cuerpo y pie de página

---

Token, carga útil y hash

---

ID, reclamaciones y certificado

#### --answer--

Encabezado, carga útil y firma

### --question--

#### --text--

¿Cuál de los siguientes es un ejemplo de "algo que tienes" como factor de autenticación?

#### --distractors--

Una huella dactilar.

---

Una contraseña que memorizaste.

---

La respuesta a una pregunta de seguridad.

#### --answer--

Un código de un solo uso enviado a tu smartphone.

### --question--

#### --text--

¿Cuál de las siguientes opciones describe mejor la autorización?

#### --distractors--

Verificar que las credenciales de un usuario coincidan con los registros almacenados.

---

Generar un token de sesión después de que un usuario inicia sesión correctamente.

---

Encriptar la contraseña de un usuario antes de guardarla en la base de datos.

#### --answer--

Decidir qué acciones puede realizar un usuario autenticado.

### --question--

#### --text--

¿Qué es una estrategia de Passport.js?

#### --distractors--

Un archivo de configuración que asigna rutas de la aplicación a reglas de autenticación.

---

Una función integrada que hashea y almacena contraseñas de usuarios de forma segura.

---

Un token generado después de que un usuario completa exitosamente el flujo de inicio de sesión.

#### --answer--

Un plugin que define cómo autenticar usuarios con un método o proveedor específico.

### --question--

#### --text--

¿Qué hace Helmet.js con el encabezado X-Powered-By por defecto?

#### --distractors--

Reemplaza el encabezado con un valor genérico de marcador de posición.

---

Encripta el valor del encabezado antes de enviarlo al cliente.

---

Mueve la información del encabezado a una cookie HTTP-only.

#### --answer--

Elimina completamente el encabezado X-Powered-By.

### --question--

#### --text--

¿Qué hace establecer el atributo `SameSite` de una cookie en `Strict`?

#### --distractors--

Encripta la cookie antes de almacenarla en el navegador.

---

Garantiza que la cookie solo se envíe en conexiones HTTPS.

---

Impide que el servidor establezca más de una cookie por respuesta.

#### --answer--

Evita que el navegador envíe la cookie en cualquier solicitud entre sitios.

### --question--

#### --text--

En un JWT, ¿dónde se almacenan los datos de reclamación del usuario como el id o el rol?

#### --distractors--

En la sección de firma.

---

En la sección de encabezado.

---

En un contenedor encriptado fuera del token.

#### --answer--

En la sección de carga útil.

### --question--

#### --text--

¿Qué encabezado HTTP establecido por Helmet evita que tu sitio se incruste dentro de un iframe en otro dominio?

#### --distractors--

Content-Security-Policy

---

X-Powered-By

---

Strict-Transport-Security

#### --answer--

X-Frame-Options

### --question--

#### --text--

¿Cómo explota un ataque CSRF el navegador?

#### --distractors--

Inyecta JavaScript en el navegador de la víctima para robar cookies directamente.

---

Interceta el tráfico entre el navegador de la víctima y el servidor confiable.

---

Envía un enlace de phishing que redirige a la víctima a una página falsa de inicio de sesión.

#### --answer--

Engaña al navegador para que envíe una solicitud que incluye automáticamente la cookie de sesión de la víctima.

### --question--

#### --text--

¿Qué hace `app.use(passport.initialize())` en una app Express?

#### --distractors--

Registra una nueva estrategia de autenticación con la aplicación.

---

Crea una cookie de sesión para cada usuario que visita la app.

---

Instala automáticamente todas las estrategias de Passport disponibles.

#### --answer--

Configura Passport como middleware para manejar la autenticación en cada solicitud entrante.

### --question--

#### --text--

¿Qué rol en un sistema de autorización basado en roles típicamente tiene acceso completo a la configuración del sistema y gestión de usuarios?

#### --distractors--

Editor

---

Moderador

---

Usuario estándar

#### --answer--

Administrador

### --question--

#### --text--

¿Qué comando instala el paquete principal de Passport.js y la estrategia local?

#### --distractors--

`npm install passport.js passport-local`

---

`npm install passport passport-auth`

---

`npm install passportjs local-strategy`

#### --answer--

`npm install passport passport-local`

### --question--

#### --text--

¿Por qué nunca deberías almacenar una contraseña en la carga útil de un JWT?

#### --distractors--

La carga útil tiene un límite de tamaño que hace imposible almacenar cadenas del tamaño de una contraseña.

---

Almacenar una contraseña en la carga útil invalida automáticamente la firma del token.

---

Los tokens JWT expiran demasiado rápido para que las contraseñas almacenadas dentro sean útiles.

#### --answer--

La carga útil está codificada en Base64, no encriptada, por lo que cualquiera con el token puede decodificarla.

### --question--

#### --text--

¿Cuál es el propósito de un token CSRF?

#### --distractors--

Encriptar la cookie de sesión del usuario antes de almacenarla en el navegador.

---

Verificar la identidad del usuario en cada solicitud API enviada al servidor.

---

Reemplazar la cookie de sesión con una credencial de autenticación de corta duración.

#### --answer--

Confirmar que una solicitud provino del frontend legítimo, que un sitio malicioso no puede leer.

### --question--

#### --text--

¿Qué encabezado de respuesta HTTP establece Helmet para defender contra Cross-Site Scripting (XSS)?

#### --distractors--

X-Frame-Options

---

X-Powered-By

---

Strict-Transport-Security

#### --answer--

Política de seguridad de contenido

### --question--

#### --text--

Cuando un usuario intenta acceder a un recurso protegido, ¿qué proceso debe ocurrir primero?

#### --distractors--

Autorización

---

Asignación de rol

---

Creación de sesión

#### --answer--

Autenticación

### --question--

#### --text--

¿Qué adjunta Passport.js al objeto request después de que un usuario se autentica correctamente?

#### --distractors--

El token de sesión activo como `req.sessionToken`.

---

El nombre de la estrategia usada para el inicio de sesión como `req.strategy`.

---

La contraseña hasheada del usuario como `req.credentials`.

#### --answer--

El objeto del usuario autenticado como `req.user`.

### --question--

#### --text--

¿Qué sucede cuando un servidor recibe un JWT cuya carga útil ha sido manipulada?

#### --distractors--

El servidor decodifica la carga útil y actualiza el registro del usuario con los nuevos datos.

---

El servidor ignora silenciosamente los datos manipulados y usa las reclamaciones originales del token.

---

El servidor elimina el token y envía al usuario de vuelta a la página de registro.

#### --answer--

La firma no coincidirá y el servidor rechaza la solicitud.

### --question--

#### --text--

Comparado con `SameSite=Strict`, ¿qué permite `SameSite=Lax`?

#### --distractors--

Permite la cookie en todas las solicitudes entre sitios, incluyendo envíos de formularios.

---

Bloquea la cookie en todas las solicitudes que se originan fuera del mismo dominio.

---

Requiere que el usuario inicie sesión de nuevo en cualquier navegación entre sitios.

#### --answer--

Permite la cookie en la navegación de nivel superior pero aún la bloquea en envíos de formularios y solicitudes API entre sitios.

### --question--

#### --text--

¿Qué hace cumplir el encabezado Strict-Transport-Security cuando lo establece Helmet?

#### --distractors--

Obliga al servidor a rechazar todas las solicitudes no HTTPS con una respuesta de error.

---

Encripta todo el cuerpo de la respuesta HTTP antes de enviarlo al navegador.

---

Requiere que todas las cookies tengan las banderas Secure y HttpOnly establecidas.

#### --answer--

Obliga al navegador a comunicarse con el servidor solo a través de HTTPS.
