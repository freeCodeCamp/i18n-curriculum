---
id: 5900f3e91000cf542c50fefc
title: '문제 125: 회문 합계'
challengeType: 1
forumTopicId: 301752
dashedName: problem-125-palindromic-sums
---

# --description--

회문 수 595는 연속된 제곱수의 합으로 나타낼 수 있어서 흥미롭습니다: $6^2 + 7^2 + 8^2 + 9^2 + 10^2 + 11^2 + 12^2$.

연속된 제곱수의 합으로 나타낼 수 있는 1000 미만의 회문 수는 정확히 열한 개이며, 이 회문 수들의 합은 4164입니다. 이 문제는 양의 정수의 제곱에 관한 것이므로 $1 = 0^2 + 1^2$는 포함하지 않았음을 유의하세요.

연속된 제곱수의 합으로 나타낼 수 있으면서 회문인  `limit` 미만의 모든 수의 합을 구하세요.

# --hints--
`palindromicSums(100000000)` 은 `2906969179` 를 반환해야 합니다.

```js

assert.strictEqual(palindromicSums(100000000), 2906969179);

```

`palindromicSums(100)` 은 `137` 를 반환해야 합니다.

```js
assert.strictEqual(palindromicSums(100), 137);
```

`palindromicSums(1000)` 은 `4164` 를 반환해야 합니다.

```js
assert.strictEqual(palindromicSums(1000),4164);
```

# --seed--

## --seed-contents--

```js
function palindromicSums(limit) {

  return true;
}

palindromicSums(100);
```

# --solutions--

```js
function isPalindrome(num) {
  return num
    .toString()
    .split('')
    .every((digit, i, arr) => digit === arr[arr.length - 1 - i]);
}

function palindromicSums(limit) {
  let sumOfPalindromes = 0;
  const sqrtLimit = Math.sqrt(limit);
  const list = new Set();

  for (let i = 1; i <= sqrtLimit; i++) {
    let sumOfSquares = i * i;
    for (let j = i + 1; j <= sqrtLimit; j++) {
      sumOfSquares += j * j;
      if (sumOfSquares > limit) break;
      if (isPalindrome(sumOfSquares) && !list.has(sumOfSquares)) {
        sumOfPalindromes += sumOfSquares;
        list.add(sumOfSquares);
      }
    }
  }
  return sumOfPalindromes;
}
```
