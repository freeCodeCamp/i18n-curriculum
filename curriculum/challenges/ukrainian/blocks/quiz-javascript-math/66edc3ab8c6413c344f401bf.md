---
id: 66edc3ab8c6413c344f401bf
title: Тест з математики JavaScript
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що означає `NaN`?

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

Який із наведених операторів НЕ є арифметичним оператором у JavaScript?

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

Що станеться, якщо спробувати поділити на нуль у JavaScript?

#### --distractors--

JavaScript виведе 0.

---

Нічого не станеться.

---

JavaScript видасть помилку.

#### --answer--

JavaScript виведе `Infinity`

### --question--

#### --text--

Яке з наведених правильне використання оператора піднесення до степеня?

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

Який оператор слід використовувати для зменшення значення змінної на одиницю у JavaScript?

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

Який із наведених НЕ є побітовим оператором?

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

Який із наведених є правильним складеним оператором для отримання остачі?

#### --distractors--

`//=`

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

У чому різниця між операторами `==` та `===`?

#### --distractors--

Різниці немає. Результат порівняння завжди буде однаковим.

---

Оператор `==` використовується для перевірки однаковості типів, тоді як `===` — для перевірки рівності значень.

---

Оператор `==` можна використовувати лише з примітивними типами, а оператор `===` — лише для об’єктів.

#### --answer--

Оператор `==` порівнює лише значення, тоді як `===` порівнює значення і типи.

### --question--

#### --text--

Для чого використовується унарний плюс (оператор `+`)?

#### --distractors--

Він використовується для зменшення значення на одиницю.

---

Він використовується для отримання остачі від операції.

---

Він використовується для інвертування двійкового представлення числа.

#### --answer--

Він використовується для перетворення операнда у число.

### --question--

#### --text--

Який із наведених методів `Math` округлює значення до найближчого цілого числа?

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

Що робить метод `Math.sqrt()`?

#### --distractors--

Цей метод повертає випадкове число з плаваючою комою.

---

Цей метод повертає абсолютне значення числа.

---

Цей метод повертає куб числа.

#### --answer--

Цей метод повертає квадратний корінь числа.

### --question--

#### --text--

Який із наведених НЕ є дійсним методом об’єкта `Math`?

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

Що з наведеного поверне `false`?

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

Що робить метод `parseFloat()`?

#### --distractors--

Він аналізує рядковий аргумент і виконує над ним побітову операцію.

---

Він аналізує рядковий аргумент і повертає `false`.

---

Він аналізує рядковий аргумент і повертає ціле число.

#### --answer--

Він аналізує рядковий аргумент і повертає число з плаваючою комою.

### --question--

#### --text--

Що буде виведено в консоль?

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

Який буде вихідний результат для наступного коду?

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

Що таке пріоритет операторів?

#### --distractors--

Пріоритет операторів використовується для перевірки, чи значення зліва менше за значення справа.

---

Пріоритет операторів використовується для піднесення змінної до степеня і повторного призначення результату змінній.

---

Пріоритет операторів використовується для збільшення значення на одиницю.

#### --answer--

Пріоритет операторів визначає порядок, у якому операції виконуються у виразі.

### --question--

#### --text--

Який із наведених НЕ є оператором порівняння?

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

Що робить унарний оператор заперечення (`-`)?

#### --distractors--

Він змінює булеве значення операнда на протилежне.

---

Він зменшує значення на одиницю.

---

Він збільшує значення на одиницю.

#### --answer--

Він заперечує операнд.

### --question--

#### --text--

Який із наведених є прикладом використання оператора nullish coalescing?

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```
