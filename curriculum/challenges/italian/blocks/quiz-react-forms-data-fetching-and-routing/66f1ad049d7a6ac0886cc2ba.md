---
id: 66f1ad049d7a6ac0886cc2ba
title: "Quiz su form React, recupero dati e routing"
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale dei seguenti è un esempio di input controllato?

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

Quale dei seguenti è un esempio di input non controllato?

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

Quale dei seguenti NON è un modo comunemente usato per recuperare dati in React?

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

Quale dei seguenti hook è comunemente associato al recupero dati in React?

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

Cos’è la memoizzazione?

#### --distractors--

Un algoritmo comune usato per trovare il percorso più breve tra due vertici in un grafo.

---

Una tecnica speciale usata per ottimizzare le prestazioni di tutti gli input controllati e non controllati.

---

Un algoritmo divide et impera usato per ordinare ricorsivamente sotto-array e unirli insieme.

#### --answer--

Una tecnica di ottimizzazione in cui il risultato di chiamate di funzione costose viene memorizzato (cachato) in base a specifici argomenti.

### --question--

#### --text--

Quale dei seguenti hook si usa per memoizzare valori calcolati?

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

Quale dei seguenti usa l’hook corretto per memoizzare riferimenti a funzioni?

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

Cos’è una dipendenza?

#### --distractors--

Una dipendenza è una libreria di test comunemente usata nelle applicazioni React.

---

Una dipendenza è un tipo di componente React che gira solo sul server.

---

Una dipendenza è un compilatore speciale usato nelle applicazioni React per compilare il codice React in JavaScript.

#### --answer--

Una dipendenza è quando un componente o modulo in un’applicazione si basa su un altro per funzionare correttamente.

### --question--

#### --text--

Cos’è un package manager?

#### --distractors--

Uno strumento usato per aggiornare pacchetti di terze parti installati nelle applicazioni.

---

Uno strumento usato per gestire pacchetti creati solo all’interno di un progetto.

---

Uno strumento usato per spostare dipendenze fuori dalla cartella `node_modules` e dentro una cartella di configurazione speciale.

#### --answer--

Uno strumento usato per installare, aggiornare e rimuovere dipendenze.

### --question--

#### --text--

Quale dei seguenti NON è un package manager comunemente usato nell’ecosistema JavaScript?

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

Quale dei seguenti è un file di configurazione chiave che contiene metadati sul tuo progetto?

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

Quale dei seguenti è un framework React comunemente usato?

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

Qual è il modo corretto per aggiungere rotte alla tua applicazione React usando React Router?

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

Qual è il modo corretto per creare una rotta dinamica in React Router?

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

Quale dei seguenti hook si usa per consultare i parametri dinamici da un percorso URL?

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

Qual è il modo corretto per creare un gestore di richieste personalizzato in Next.js?

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

Cos’è il prop drilling?

#### --distractors--

Il processo di creare un reducer che specifica come lo stato dovrebbe cambiare in risposta a particolari props usate.

---

Il processo di passare props da un componente figlio verso il componente genitore.

---

Il processo di creare un context provider per un’app React e usarlo per inviare props da genitore a figli.

#### --answer--

Il processo di passare props da un componente genitore a componenti figli profondamente annidati, anche quando alcuni di questi figli non hanno bisogno delle props.

### --question--

#### --text--

Quale dei seguenti NON è una soluzione comunemente usata per la gestione dello stato nelle applicazioni React?

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

A cosa servono i React Developer Tools?

#### --distractors--

Questi strumenti servono solo per controllare problemi di accessibilità nelle applicazioni React.

---

Questi strumenti servono per aggiungere test aggiuntivi ai componenti React.

---

Questi strumenti servono per memoizzare tutti i componenti React dentro l’applicazione.

#### --answer--

Questi strumenti servono per ispezionare i componenti React e identificare problemi di prestazioni.

### --question--

#### --text--

Cosa sono i React Server Components?

#### --distractors--

Sono componenti che gestiscono il context esclusivamente sul server.

---

Sono componenti memoizzati che renderizzano esclusivamente sul server.

---

Sono componenti che gestiscono lo stato esclusivamente sul server.

#### --answer--

Sono componenti che renderizzano esclusivamente sul server.
