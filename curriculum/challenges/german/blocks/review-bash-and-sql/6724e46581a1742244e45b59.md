---
id: 6724e46581a1742244e45b59
title: Bash- und SQL-Überblick
challengeType: 31
dashedName: review-bash-and-sql
---

# --description--

## Datenbanknormalisierung

Dies ist der Prozess der Organisation einer relationalen Datenbank, um Datenredundanz zu verringern und die Integrität zu verbessern.

Zu den Vorteilen gehören:

- Minimierung von doppelten Daten, was Speicher spart und Inkonsistenzen reduziert.
- Durchsetzung der Datenintegrität durch Verwendung von Primär- und Fremdschlüsseln.
- Erleichterung der Wartung und des Verständnisses von Datenbanken.

### Normalformen

- **1NF (erste Normalform)**
  - Jede Zelle enthält einen einzelnen (atomaren) Wert.
  - Jeder Datensatz ist eindeutig (durch einen Primärschlüssel sichergestellt).
  - Die Reihenfolge der Zeilen/Spalten ist unerheblich.
  - Beispiel: Verschieben mehrerer Telefonnummern aus einer `students`-Tabelle in eine separate `student_phones`-Tabelle.

- **2NF (zweite Normalform)**
  - Erfüllt die Anforderungen der 1NF.
  - Keine **partiellen Abhängigkeiten**: Jedes Nicht-Schlüssel-Attribut muss vom gesamten zusammengesetzten Primärschlüssel abhängen.
  - Beispiel: Aufteilen der `orders`-Tabelle in `order_header` und `order_items`, um zu vermeiden, dass Attribute nur von einem Teil des Schlüssels abhängen.

- **3NF (dritte Normalform)**
  - Erfüllt die Anforderungen der 2NF.
  - Keine **transitiven Abhängigkeiten**: Nicht-Schlüssel-Attribute dürfen nicht von anderen Nicht-Schlüssel-Attributen abhängen.
  - Beispiel: Verschieben von `city_postal_code` in eine `cities`-Tabelle anstatt es bei jeder Bestellung zu speichern.

- **BCNF (Boyce-Codd-Normalform)**
  - Erfüllt die Anforderungen der 3NF.
  - Jeder Determinant (linke Seite einer funktionalen Abhängigkeit) muss ein Superschlüssel sein.

**Tipp**: Streben Sie in den meisten Entwürfen die 3NF an, um ein gutes Gleichgewicht zwischen Integrität und Leistung zu erreichen.

## Wichtige SQL-Konzepte

- SQL steht für Structured Query Language, eine Sprache zur Kommunikation mit relationalen Datenbanken.
- **Grundlegende Befehle** → `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `CREATE TABLE`, `ALTER TABLE` usw.
- **Joins** → Kombiniert Daten aus mehreren Tabellen (`INNER JOIN`, `LEFT JOIN`, `RIGHT JOIN`, `FULL JOIN`).

## Ausführen von SQL-Befehlen in Bash

Sie können SQL-Befehle direkt über die Befehlszeilenschnittstelle `psql` für PostgreSQL oder ähnliche Tools für andere Datenbanken ausführen.

Zum Beispiel, um eine SQL-Datei in PostgreSQL auszuführen:

```bash
psql -U username -d database_name -c "SELECT * FROM students;"
```

Sie können auch MySQL-Befehle direkt ausführen:

```bash
mysql -u username -p database_name -e "SELECT * FROM students;"
```

### SQL aus einer Datei ausführen

```bash
# PostgreSQL
psql -U username -d database_name -f script.sql

# MySQL
mysql -u username -p database_name < script.sql
```

### SQL in ein Bash-Skript einbetten

```bash
#!/bin/bash
DB_USER="school_admin"
DB_NAME="school"

# Insert student data
psql -U "$DB_USER" -d "$DB_NAME" -c \
"INSERT INTO students (name, age, major) VALUES ('Alice', 20, 'CS');"
```

### Verwendung von Variablen in SQL

```bash
#!/bin/bash
DB_USER="school_admin"
DB_NAME="school"
STUDENT_NAME="Bob"
AGE=21

psql -U "$DB_USER" -d "$DB_NAME" -c \
"INSERT INTO students (name, age) VALUES ('$STUDENT_NAME', $AGE);"
```

**Tipp**: Bereinigen Sie Variablen, um SQL-Injektionen zu vermeiden.

## Abrufen und Verwenden von SQL-Abfrageergebnissen in Bash

Wenn Sie SQL-Abfragen über `psql` ausführen, können Sie die zurückgegebenen Werte in Ihren Bash-Skripten **erfassen** und **verarbeiten**.

### Erfassen eines einzelnen Werts

```bash
#!/bin/bash
DB_USER="school_admin"
DB_NAME="school"

# Get total student count
STUDENT_COUNT=$(psql -U "$DB_USER" -d "$DB_NAME" -t -A -c \
"SELECT COUNT(*) FROM students;")

echo "Total students: $STUDENT_COUNT"
```

Ausgabe → 42

### Abrufen mehrerer Spalten

```bash
#!/bin/bash
DB_USER="school_admin"
DB_NAME="school"

# Get top 3 students' names and ages
RESULTS=$(psql -U "$DB_USER" -d "$DB_NAME" -t -A -F"," -c \
"SELECT name, age FROM students LIMIT 3;")

echo "Top 3 students:"
echo "$RESULTS"
```

Ausgabe

```bash
Alice,20
Bob,21
Charlie,22
```

### Durchlaufen von Abfrageergebnissen in einer Schleife

```bash
#!/bin/bash
DB_USER="school_admin"
DB_NAME="school"

# Get student names and majors
psql -U "$DB_USER" -d "$DB_NAME" -t -A -F"," -c \
"SELECT name, major FROM students;" | while IFS="," read -r name major
do
    echo "Student: $name | Major: $major"
done
```

Form der Ausgabe

```bash
Student: Alice | Major: CS
Student: Bob   | Major: Math
Student: Carol | Major: Physics
```

## SQL-Injektion

Dies ist eine Sicherheitslücke im Web, bei der Angreifer bösartigen SQL-Code in Eingabefelder einfügen, um die Datenbank zu manipulieren.

Dies kann zu riskanten Aktionen führen wie:

- Umgehen der Authentifizierung.
- Diebstahl sensibler Daten.
- Ändern oder Löschen von Datensätzen.

Ein Beispiel für einen SQL-Injektionsangriff:

```sql
SELECT * FROM users WHERE username = ' " " OR "1"="1" -- ' AND password = 'anything';
```

Diese Abfrage würde alle Benutzer zurückgeben, weil die Bedingung `OR "1"="1"` immer wahr ist, was Angreifern erlaubt, die Login-Prüfungen zu umgehen.

### Vermeidung von SQL-Injektionen

1. **Verwenden Sie vorbereitete Anweisungen**: Diese trennen SQL-Code von Daten und verhindern Injektionen. Hier ein Beispiel (Node.js mit pg):

    ```js
    client.query('SELECT * FROM users WHERE username = $1 AND password = $2', [username, password]);
    ```

2. **Validierung der Eingaben**: Bereinigen und validieren Sie alle Benutzereingaben, um sicherzustellen, dass sie den erwarteten Formaten entsprechen.

3. **Minimalrechteprinzip**: Verwenden Sie Datenbankkonten mit den minimal notwendigen Berechtigungen für die Anwendung.

**Hinweis**: Gewähren Sie Anwendungs-Konten niemals Administratorrechte.

## N+1-Problem

Das N+1-Problem tritt auf, wenn eine Anwendung eine Abfrage ausführt, um eine Liste von Elementen (N) abzurufen, und dann für jedes Element eine zusätzliche Abfrage ausführt, um verwandte Daten abzurufen, was zu N+1 Abfragen führt.

**Warum das schlecht ist**

- Jede Abfrage verursacht Netzwerk- und Verarbeitungsaufwand.
- Viele kleine Abfragen sind langsamer als eine optimierte Abfrage.

### Beispiel für das N+1-Muster

```sql
-- 1: Get list of orders
SELECT * FROM orders LIMIT 50;

-- N: For each order, get customer
SELECT * FROM customers WHERE customer_id = ...;
```

**Lösung**: Verwenden Sie `JOINs` oder andere set-basierte Operationen.

```sql
SELECT 
  orders.order_id,
  orders.product,
  orders.quantity,
  customers.customer_id,
  customers.name,
  customers.email,
  customers.address
FROM orders
JOIN customers 
  ON orders.customer_id = customers.customer_id
WHERE orders.order_id IN (SELECT order_id FROM orders LIMIT 50);
```

Suchen Sie immer nach Möglichkeiten, verwandte Daten in einer einzigen Abfrage zusammenzufassen.

# --assignment--

Überprüfen Sie die Bash- und SQL-Themen und -Konzepte.
