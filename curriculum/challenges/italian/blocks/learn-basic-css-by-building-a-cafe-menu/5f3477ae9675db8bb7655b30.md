---
id: 5f3477ae9675db8bb7655b30
title: Passaggio 12
challengeType: 0
dashedName: step-12
---

# --description--

Nel passaggio precedente, hai usato un <dfn>selettore di tipo</dfn> per definire lo stile dell'elemento `h1`. Centra il contenuto degli elementi `h2` e `p` aggiungendo un nuovo selettore di tipo per ciascuno all'interno dell'elemento `style` esistente.

# --hints--

Non dovresti modificare il selettore `h1` esistente.

```js
const hasH1 = new __helpers.CSSHelp(document).getStyle('h1');
assert(hasH1);
```

Non dovresti aggiungere un nuovo tag `style`. Aggiungi le nuove regole CSS al tag `style` esistente.

```js
assert.isAtMost(document.querySelectorAll('style').length, 2);
```

Dovresti aggiungere un nuovo selettore `h2`.

```js
const hasH2 = new __helpers.CSSHelp(document).getStyle('h2');
assert(hasH2);
```

Dovresti aggiungere un nuovo selettore `p`.

```js
const hasP = new __helpers.CSSHelp(document).getStyle('p');
assert(hasP);
```

Il tuo elemento `h1` dovrebbe avere un `text-align` di `center`.

```js
const h1TextAlign = new __helpers.CSSHelp(document).getStyle('h1')?.getPropertyValue('text-align');
assert(h1TextAlign === 'center');
```

Il tuo elemento `h2` dovrebbe avere un `text-align` di `center`.

```js
const h2TextAlign = new __helpers.CSSHelp(document).getStyle('h2')?.getPropertyValue('text-align');
assert(h2TextAlign === 'center');
```

Il tuo elemento `p` dovrebbe avere un `text-align` di `center`.

```js
const pTextAlign = new __helpers.CSSHelp(document).getStyle('p')?.getPropertyValue('text-align');
assert(pTextAlign === 'center');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Cafe Menu</title>
--fcc-editable-region--
    <style>
      h1 {
        text-align: center;
      }
    </style>
--fcc-editable-region--
  </head>
  <body>
    <main>
      <h1>CAMPER CAFE</h1>
      <p>Est. 2020</p>
      <section>
        <h2>Coffee</h2>
      </section>
    </main>
  </body>
</html>
```
