---
id: 684165a72e33e26a40904f8b
title: ステップ 13
challengeType: 20
dashedName: step-13
---

# --description--

最後に、`value` が中央に見つからず、かつ `value_at_middle` より大きくもない場合は、`value` が `value_at_middle` より小さく、左側にある必要があります。`else` ブロックがこれを処理します。

したがって、ループを終わらせるために `break` キーワードを削除し、`else` ブロックを追加してください。その中で、`high` 変数を `1` から `mid` を引いた値に更新します。

# --hints--

`break` キーワードを else ブロックに置き換え、`high` 変数を `mid - 1` に更新してください。

```js
({ test: () => runPython(`
_Node(_code).find_function("binary_search").find_whiles()[0].find_ifs()[0].find_bodies()[2].is_equivalent("high = mid - 1")
`) })
```

# --seed--

## --seed-contents--

```py
def binary_search(search_list, value):
    path_to_target = []
    low = 0
    high = len(search_list) - 1

    while low <= high:
        mid = (low + high) // 2
        value_at_middle = search_list[mid]
        path_to_target.append(value_at_middle)

        if value == value_at_middle:
            return path_to_target
        elif value > value_at_middle:
            low = mid + 1
--fcc-editable-region--
        break
--fcc-editable-region--
    return []
    
print(binary_search([1, 2, 3, 4, 5], 3))
print(binary_search([1, 2, 3, 4, 5, 9], 4))
```
