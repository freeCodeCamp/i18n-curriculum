---
id: 692487767b6b728454f53f29
title: NodeJS 核心模块测验
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

哪个 `fs` 方法将内容添加到现有文件而不覆盖它？

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

当你使用 `fs` 模块中的同步的方法时会发生什么？

#### --distractors--

它们会自动删除现有的文件。

---

它们在所有分支中都比异步的运行得更快。

---

它们默认返回 Promise。

#### --answer--

它们会阻塞程序直到操作完成。

### --question--

#### --text--

`fs.readFile()` 中的 `utf8` 参数有什么作用？

#### --distractors--

它防止误差被抛出。

---

它更改文件的扩展名。

---

它压缩大文件。

#### --answer--

它告诉 Node 返回一个可读取的字串而不是缓冲区。

### --question--

#### --text--

哪个 `Buffer` 方法创建一个填充为零的新建缓冲区？

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

`Buffer.toString()` 做什么？

#### --distractors--

将缓冲区转换为二进制代码。

---

清除 `buffer` 的内容。

---

显示缓冲区字节长度。

#### --answer--

将缓冲区内容转换为可读取的字串。

### --question--

#### --text--

哪个 `crypto` 特色需要私钥来生成签名？

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

为什么你应该避免直接编写你自己的加密算法？

#### --distractors--

在 Node.js 应用中这是非法的。

---

Node.js 不支持加密。

---

JavaScript 无法计算哈希。

#### --answer--

如果没有经过充分测试的库，可能不安全。

### --question--

#### --text--

推荐使用什么 `crypto` 方法来生成安全的随机令牌，而不是使用 `Math.random()`？

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

哪个 OS 模块的方法返回 CPU 架构？

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

`os.userInfo()` 返回什么？

#### --distractors--

只有用户名。

---

仅设备 IP 地址。

---

只有已登录用户的 ID。

#### --answer--

关于当前系统用户的信息。

### --question--

#### --text--

哪个 `path` 方法连接段并规范化多余的斜线？

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

哪个 `path` 方法基于当前工作目录生成绝对路径？

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

全局的变量 `__dirname` 表现什么？

#### --distractors--

运行 Node 的用户名称。

---

当前文件的扩展名。

---

以 `./` 开头的相对文件夹路径。

#### --answer--

包含当前文件的目录的绝对路径。

### --question--

#### --text--

哪个进程属性包含像 `NODE_ENV` 这样的环境变量？

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

`process.on("exit")` 事件的作用是什么？

#### --distractors--

它会重启 Node 应用。

---

它向子进程发送 `exit` 信号。

---

它清空事件循环队列。

#### --answer--

在 Node 进程结束前执行代码。

### --question--

#### --text--

哪种 `stream` 类型既可以读取又可以写入 `data`，但不对其进行转换？

#### --distractors--

可读取流

---

转换流

---

可写流

#### --answer--

双工流

### --question--

#### --text--

实现高效读取和写入大文件需要什么？

#### --distractors--

仅使用 `Buffer.alloc()`

---

导入 `crypto` 模块

---

首先将整个 `file` 保存在 `memory` 中

#### --answer--

使用流以块方式处理数据

### --question--

#### --text--

哪个方法将可读取的 `stream` 链接到可写入的 `stream`？

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

哪个流事件表示没有更多数据可写？

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

如果你打印一个来自可读取流的块而不使用 `toString()`，会记录什么？

#### --distractors--

纯文本内容

---

进程 PID

---

操作系统名称

#### --answer--

包含二进制数据的缓冲区对象
