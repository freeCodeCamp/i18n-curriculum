---
id: 671a915858de057ad84c5dbd
title: 表單樣式檢視
challengeType: 31
dashedName: review-styling-forms
---

# --description--

## 輸入樣式的最佳實踐

- **輸入欄位樣式**：如同所有文字元素，你需要確保套用到文字輸入欄位的樣式是可存取的。這表示字型需要有足夠大小，且顏色需要與背景有足夠對比。輸入元素也可以被聚焦。當你編輯樣式時，應該注意保留明顯的指示器，當元素被聚焦時，例如粗體邊框。

## 為輸入使用 `appearance: none`

- **`appearance: none`**：瀏覽器會對許多元素套用預設樣式。`appearance: none` CSS 屬性讓你能完全控制樣式，但也有一些注意事項。當為輸入元素建置訂製樣式時，你需要確保焦點和錯誤指示仍然存在。

## 常見問題：為 `datetime-local` 和 `color` 屬性設定樣式

- **常見問題**：這些特殊類型的輸入依賴複雜的偽元素來創建像日期和顏色選擇器這樣的東西。這對於這些輸入的樣式設計帶來了重大挑戰。一個挑戰是預設樣式完全依賴瀏覽器，因此你撰寫的 CSS 使選擇器呈現你想要的樣子，在另一個瀏覽器上可能會完全不同。

# --assignment--

檢視 Styling Forms 主題和概念。
