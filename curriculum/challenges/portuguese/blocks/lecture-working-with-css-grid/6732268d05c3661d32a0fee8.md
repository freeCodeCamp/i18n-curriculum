---
id: 6732268d05c3661d32a0fee8
title: Como você pode repetir listas de faixas em um layout de grade?
challengeType: 19
dashedName: how-can-you-repeat-track-listings-in-a-grid-layout
---

# --interactive--

Nas lições anteriores, temos trabalhado com a propriedade `grid-template-columns` e definido o valor para algumas unidades fracionárias.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="grid-container">
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
</div>
```

```css
.grid-container {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr 1fr;
  column-gap: 10px;
}

.box {
  width: 100px;
  height: 100px;
  background-color: darkblue;
}
```

:::

Embora o código a seguir seja completamente válido, existe uma maneira mais fácil de repetir uma seção ou toda a sua lista de faixas.

A função `repeat()` é usada para repetir uma seção ou todas as faixas para colunas ou linhas. Esta função recebe uma contagem de repetições e as faixas que você deseja repetir.

Aqui está uma versão revisada do exemplo anterior usando a função `repeat`:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="grid-container">
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
</div>
```

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  column-gap: 10px;
}

.box {
  width: 100px;
  height: 100px;
  background-color: darkblue;
}
```

:::

Não haverá mudança nos estilos no navegador, mas esta é uma forma mais concisa de escrever valores repetidos para as colunas.

A função `repeat()` aceitará qualquer padrão válido que você possa usar para linhas ou colunas.

Aqui está um exemplo usando a função `repeat()` para definir a primeira e a terceira colunas com 20 pixels e a segunda e a quarta colunas com uma unidade fracionária.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="grid-container">
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
  <div class="box"></div>
</div>
```

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(2, 20px 1fr);
  column-gap: 10px;
}

.grid-container div {
  height: 100px;
  background-color: darkblue;
}
```

:::

Às vezes, você pode optar por escrever cada valor individualmente em vez de usar a função `repeat()`. Mas há momentos em que essa função é útil, especialmente quando você quer repetir um padrão específico para uma lista de faixas.

# --questions--

## --text--

Qual das seguintes é a sintaxe correta para repetir listas de faixas?

## --answers--

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(1fr times 4);
  column-gap: 10px;
}
```

### --feedback--

Revise o início da lição onde isso foi discutido.

---

```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  column-gap: 10px;
}
```

---

```css
.grid-container {
  display: grid;
  grid-template-columns: multiply(4, 1fr);
  column-gap: 10px;
}
```

### --feedback--

Revise o início da lição onde isso foi discutido.

---

```css
.grid-container {
  display: grid;
  grid-template-columns: repeatSections(4, 1fr);
  column-gap: 10px;
}
```

### --feedback--

Revise o início da lição onde isso foi discutido.

## --video-solution--

2

## --text--

Qual é o principal benefício de usar a função `repeat()` na propriedade `grid-template-columns`?

## --answers--

Ele permite repetir definições de colunas de forma mais concisa e reduz a redundância.

---

Isso faz o código rodar mais rápido.


### --feedback--

Pense em como essa propriedade reduz a repetição.

---

Isso leva a um código sem erros.

### --feedback--

Pense em como essa propriedade reduz a repetição.

---

Isso torna a página web mais responsiva.

### --feedback--

Pense em como essa propriedade reduz a repetição.

## --video-solution--

1

## --text--

Qual é o resultado de definir `grid-template-columns` como `repeat(2, 20px 1fr);`?

## --answers--

Ele cria quatro colunas todas definidas com largura `1fr`.

### --feedback--

Revise o final da lição onde isso foi discutido.

---

Ele cria duas colunas onde cada uma tem `20px` de largura.

### --feedback--

Revise o final da lição onde isso foi discutido.

---

Ele cria quatro colunas onde a primeira e a terceira têm `20px` de largura e a segunda e a quarta têm `1fr` de largura.

---

Ele cria três colunas onde a primeira e a segunda têm `20px` de largura e a terceira tem `fr` de largura.

### --feedback--

Revise o final da lição onde isso foi discutido.

## --video-solution--

3
