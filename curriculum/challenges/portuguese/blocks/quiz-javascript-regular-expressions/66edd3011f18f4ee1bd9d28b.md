---
id: 66edd3011f18f4ee1bd9d28b
title: Questionário sobre expressões regulares em JavaScript
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qual dos seguintes NÃO é uma expressão regular válida?

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

Qual `RegExp` contém o mesmo padrão que a expressão regular `/f[o0]{2} b[a4@]r/i`?

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

Qual é o tipo de retorno do método de expressão regular `test`?

#### --distractors--

Um array de strings que corresponderam à expressão regular.

---

Um objeto contendo informações sobre a (sub)string que a expressão regular correspondeu.

---

`null`, o método `test` apenas valida se a expressão regular fornecida é válida.

#### --answer--

Um booleano que indica se a string corresponde à expressão regular.

### --question--

#### --text--

O que o método `match` faz quando recebe uma expressão regular sem nenhuma flag?

#### --distractors--

Ele procura por quaisquer strings que correspondam à expressão regular fornecida e retorna TODOS os correspondentes como um array.

---

Ele procura a primeira correspondência completa e retorna o índice inicial dessa correspondência.

---

Ele procura a primeira correspondência completa e retorna um booleano indicando se uma correspondência foi encontrada ou não.

#### --answer--

Ele procura a primeira correspondência completa e retorna um array contendo essa primeira correspondência.

### --question--

#### --text--

Qual é o melhor caso de uso para `test`?

#### --distractors--

Para verificar uma expressão regular dada.

---

Para obter informações detalhadas sobre uma correspondência de string, como o comprimento da string correspondida.

---

Para extrair todas as correspondências de uma determinada string.

#### --answer--

Para verificar se uma determinada string corresponde à expressão regular.

### --question--

#### --text--

Qual é o propósito de usar `replace`?

#### --distractors--

Para substituir a expressão regular atual por uma nova expressão regular.

---

Quando fornecido um índice e uma string, substituir o caractere no índice especificado pela string fornecida.

---

Para remover todas as instâncias de uma substring correspondente de uma string.

#### --answer--

Para substituir uma string correspondente por uma string de substituição fornecida.

### --question--

#### --text--

Qual é a diferença entre `match` e `matchAll`?

#### --distractors--

`match` sempre retorna uma string, enquanto `matchAll` sempre retorna um array de correspondências.

---

`match` retorna um booleano indicando se uma correspondência foi encontrada, enquanto `matchAll` retorna um número representando a quantidade de correspondências encontradas.

---

`match` retorna um número indicando o índice inicial da primeira correspondência encontrada, enquanto `matchAll` retorna um array de números contendo os índices onde uma correspondência foi encontrada.

#### --answer--

`match` retorna um array de strings que correspondem, enquanto `matchAll` retorna um objeto iterador de todas as correspondências encontradas.

### --question--

#### --text--

Qual das seguintes opções é o uso correto de `replaceAll`?

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

Qual das seguintes classes de caracteres é equivalente à expressão regular `/[a-zA-Z0-9_]/`?

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

Qual das seguintes classes de caracteres é mais apropriada para extrair dígitos de um número de telefone?

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

O que acontece quando um lookahead `(?=)` é usado em uma expressão regular?

#### --distractors--

O lookahead irá afirmar à esquerda da string, verificando que o sub-padrão ESTÁ presente.

---

Um lookahead é um padrão alternativo de expressão regular usado quando o padrão principal não consegue corresponder.

---

O lookahead irá afirmar à direita da string, verificando que o sub-padrão NÃO está presente.

#### --answer--

Um lookahead afirma à direita da string, verificando que o sub-padrão está presente.

### --question--

#### --text--

Ao criar uma expressão regular, onde um lookbehind `(?<=)` deve ser colocado?

#### --distractors--

O lookbehind pode ser colocado em qualquer lugar na expressão regular.

---

O lookbehind deve ser colocado à direita do padrão principal da expressão regular.

---

O lookbehind deve incluir o padrão principal da expressão regular.

#### --answer--

O lookbehind deve ser colocado à esquerda do padrão principal da expressão regular.

### --question--

#### --text--

Qual quantificador corresponde ao elemento precedente zero ou uma vez?

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

Qual das seguintes expressões regulares permite apenas números entre 0 e 999.999?

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

Qual das seguintes afirmações é verdadeira sobre a classe de caracteres personalizada (`[]`)?

#### --distractors--

É um conjunto de caracteres a serem removidos da correspondência.

---

Ele pode definir um conjunto de caracteres para corresponder sem a necessidade de escapar quaisquer caracteres especiais.

---

Ele representa um conjunto de caracteres na forma Unicode.

#### --answer--

Ele pode definir um conjunto personalizado de caracteres para corresponder.

### --question--

#### --text--

Qual das seguintes classes de caracteres corresponde corretamente ao alfabeto maiúsculo?

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

O que acontece quando um grupo de captura `(...)` é usado em uma expressão regular?

#### --distractors--

O grupo de captura é avaliado imediatamente, independentemente de onde ele esteja localizado na expressão regular.

---

O grupo de captura tenta corresponder usando o subpadrão fornecido e continua sem memorizar o resultado.

---

O grupo de captura é um subpadrão priorizado, que retornará imediatamente quando uma correspondência for encontrada para o subpadrão.

#### --answer--

O grupo de captura tenta corresponder usando o subpadrão fornecido e continua com o resultado na memória.

### --question--

#### --text--

O que acontece quando um grupo não capturante `(?:...)` é usado em uma expressão regular?

#### --distractors--

O grupo não capturador tenta corresponder usando o subpadrão fornecido e continua com o resultado na memória.

---

O grupo sem captura termina imediatamente quando encontra uma correspondência para o subpadrão dado.

---

O grupo de não captura é considerado uma correspondência opcional, então apenas uma correspondência bem-sucedida é adicionada ao resultado.

#### --answer--

O grupo não capturador tenta corresponder usando o subpadrão fornecido e continua sem manter o resultado na memória.

### --question--

#### --text--

Dada a seguinte expressão regular, quais desses testes retornarão `true`?

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

O que a classe de caractere curinga (`.`) faz em uma expressão regular?

#### --distractors--

Ele corresponde apenas a caracteres de espaço em branco.

---

Ele corresponde ao início de uma string.

---

Ele corresponde a um único dígito.

#### --answer--

Ele corresponde a qualquer caractere único exceto uma nova linha.
