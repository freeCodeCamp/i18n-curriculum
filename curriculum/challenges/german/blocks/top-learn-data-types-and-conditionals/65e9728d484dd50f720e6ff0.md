---
id: 65e9728d484dd50f720e6ff0
title: Datentypen und bedingte Anweisungen lernen Lektion K
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-k
---
# --description--

Die `switch`-Anweisung wird verwendet, um verschiedene Aktionen basierend auf unterschiedlichen Bedingungen auszuführen. Sie ist ähnlich wie die `if-else`-Anweisung, aber lesbarer und leichter verständlich, wenn mehrere Bedingungen überprüft werden müssen. Die `switch`-Anweisung wird verwendet, um einen von vielen Code-Blöcken auszuwählen, der ausgeführt werden soll.

Die `switch`-Anweisung wertet einen Ausdruck aus und vergleicht ihn mit den Werten jedes Falls. Wenn eine Übereinstimmung vorliegt, wird der zugehörige Code-Block ausgeführt. Gibt es keine Übereinstimmung, wird der Standard-Code-Block ausgeführt.

```javascript
let day = "Monday";

switch (day) {
  case "Monday":
    console.log("Today is Monday");
    break;
  case "Tuesday":
    console.log("Today is Tuesday");
    break;
  case "Wednesday":
    console.log("Today is Wednesday");
    break;
  case "Thursday":
    console.log("Today is Thursday");
    break;
  case "Friday":
    console.log("Today is Friday");
    break;
  case "Saturday":
    console.log("Today is Saturday");
    break;
  case "Sunday":
    console.log("Today is Sunday");
    break;
  default:
    console.log("Invalid day");
}
```

Im obigen Beispiel wird der Wert der `day`-Variablen mit den Werten jedes Falls verglichen. Wenn eine Übereinstimmung vorliegt, wird der zugehörige Code-Block ausgeführt. Gibt es keine Übereinstimmung, wird der Standard-Code-Block ausgeführt.

# --questions--

## --text--

Angesichts des unten stehenden JavaScript-Code-Ausschnitts, der die `activity`-Variable basierend auf dem Wochentag ändert, welchen Wert wird die `activity`-Variable haben, wenn der Wert der `day`-Variablen `"Tuesday"` ist?

```javascript
let day = "Tuesday";
let activity;

switch (day) {
  case "Monday":
    activity = "Go to the gym";
    break;
  case "Tuesday":
    activity = "Attend coding meetup";
    break;
  case "Wednesday":
    activity = "Watch a movie";
    break;
  case "Thursday":
    activity = "Visit a museum";
    break;
  case "Friday":
    activity = "Dinner with friends";
    break;
  case "Saturday":
    activity = "Hiking in the mountains";
    break;
  case "Sunday":
    activity = "Rest at home";
    break;
  default:
    activity = "Undefined day";
}

```

## --answers--

Der Wert der `activity`-Variablen wird `"Attend coding meetup"` sein.

---

Der Wert der `activity`-Variablen wird `"Watch a movie"` sein.

---

Der Wert der `activity`-Variablen wird `"Undefined day"` sein.

---

Der Wert der `activity`-Variablen wird `"Go to the gym"` sein.

## --video-solution--

1
