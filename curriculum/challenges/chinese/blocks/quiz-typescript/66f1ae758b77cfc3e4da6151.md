---
id: 66f1ae758b77cfc3e4da6151
title: TypeScript 测验
challengeType: 8
dashedName: quiz-typescript
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是 Typescript？

#### --distractors--

用于系统编程的框架。

---

一个数据库管理工具。

---

用于为网站设置样式的编程语言。

#### --answer--

JavaScript 的超集，添加了静态类型指派。

### --question--

#### --text--

为什么应该在项目中使用 TypeScript？

#### --distractors--

它消除了在应用中的测试需求。

---

它比所有其他编程语言都快。

---

它使网站样式设计更简单。

#### --answer--

它通过为 JavaScript 添加类型定义来提高可维护性。

### --question--

#### --text--

TypeScript 被编译成什么语言？

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

以下代码截图的输出是什么？

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

TypeScript 中接口的主要目的是什么？

#### --distractors--

将 TypeScript 代码编译成 JavaScript。

---

创建一个新建数据类型。

---

创建一个新的 JavaScript 对象。

#### --answer--

定义一个对象的结构。

### --question--

#### --text--

以下哪项不是 TypeScript 中的基本原语？

#### --distractors--

字串

---

布尔

---

数字

#### --answer--

数组

### --question--

#### --text--

以下哪项正确地定义了 TypeScript 中的字面类型？

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

`type` 关键字在 TypeScript 中的主要用途是什么？

#### --distractors--

声明变量。

---

创建类。

---

实现接口。

#### --answer--

定义自定义类型。

### --question--

#### --text--

以下哪项正确定义了一个并集类型？

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

以下哪项功能是 `interface` 独有的，且无法通过 TypeScript 中的 `type` 实现？

#### --distractors--

定义对象形状。

---

创建并集类型。

---

定义仅包含特定值的类型。

#### --answer--

声明类实现。

### --question--

#### --text--

在 TypeScript 中使用泛型的主要目的是什麽？

#### --distractors--

实现类之间的继承。

---

定义对象的结构。

---

创建一个新建数据类型。

#### --answer--

编写可与多种类型一起使用的可重用代码。

### --question--

#### --text--

以下哪项是 TypeScript 中 **通用函数** 的**正确示例**？

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

什么是 TypeScript 中的类型缩小？

#### --distractors--

减少编译后 JavaScript 的大小。

---

限制并集中类型的数量。

---

从 TypeScript 项目中移除未使用的类型。

#### --answer--

编写代码，将并集类型缩小为更具体的类型。

### --question--

#### --text--

通常使用哪个文件来配置 TypeScript 编译器选项？

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

`tsconfig.json` 文件中的 `rootDir` 选项指定了什么？

#### --distractors--

编译期间使用的 TypeScript 版本。

---

TypeScript 代码的目标运行期环境（例如，Node.js 或浏览器）。

---

用于导入和导出的特定模块系统。

#### --answer--

保存你源代码文件的目录。

### --question--

#### --text--

哪个示例正确使用了 `instanceof` 来缩小类型以安全地访问 `value` 属性？

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
在 TypeScript 中，在 `tsconfig.json` 中设置 `"strict": true` 会有什么作用？

#### --distractors--

启用静态类型检查，以便你在运行你的代码之前发现错误。

---

迫使你始终使用 ES6 语法。

---

迫使你在运行之前编译代码。

#### --answer--

启用一组类型检查选项，例如要求处理 `null` 或 `undefined` 并防止隐式 `any`。

### --question--

#### --text--

TypeScript 中 `never` 类型的目的是什么？

#### --distractors--

表现 null 或 undefined。

---

表现任何可能的值。

---

表现一个可以是任何类型但不包括 null 的类型。

#### --answer--

表现永远不会出现的值的类型。

### --question--

#### --text--

以下哪个 `getRandomValue` 函数调用将在 TypeScript 中成功编译？

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

如何使用可选链修复以下代码中 TypeScript 编译器误差 `'email' is possibly 'null'`？

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`

