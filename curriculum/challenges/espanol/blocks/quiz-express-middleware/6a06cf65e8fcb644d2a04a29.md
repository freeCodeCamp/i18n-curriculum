---
id: 6a06cf65e8fcb644d2a04a29
title: Quiz sobre middleware en Express
challengeType: 8
dashedName: quiz-express-middleware
---

# --description--

Para aprobar el quiz, debes responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué es middleware en Express?

#### --distractors--

Una función que inicia el servidor Express y lo vincula a un puerto.

---

Un objeto de configuración que define rutas y sus manejadores.

---

Un conector de base de datos integrado para manejar datos persistentes.

#### --answer--

Una función que se ejecuta durante el ciclo de vida de una solicitud al servidor.

### --question--

#### --text--

¿A qué tres cosas tiene acceso una función middleware?

#### --distractors--

La conexión a la base de datos, el almacenamiento de sesión y la definición de la ruta.

---

La configuración del servidor, el número de puerto y la instancia de la app.

---

Las variables de entorno, los encabezados de la solicitud y el estado de la respuesta.

#### --answer--

El objeto solicitud (`req`), el objeto respuesta (`res`) y la función `next`.

### --question--

#### --text--

¿Qué pasa si una función middleware no llama a `next()`?

#### --distractors--

El control se pasa automáticamente al siguiente middleware en la pila.

---

Express lanza un error no manejado y el servidor se cae.

---

La solicitud se reintenta desde el inicio de la pila de middleware.

#### --answer--

El ciclo solicitud-respuesta se detiene.

### --question--

#### --text--

¿Qué hace `app.use()`?

#### --distractors--

Define un manejador de ruta `GET` para una ruta específica.

---

Inicia el servidor y comienza a escuchar conexiones entrantes.

---

Crea una nueva instancia de la aplicación Express.

#### --answer--

Agrega middleware globalmente a la aplicación.

### --question--

#### --text--

¿En qué orden se ejecuta el middleware en una app Express?

#### --distractors--

En orden inverso, comenzando por el último middleware agregado.

---

Alfabéticamente, basado en los nombres de las funciones middleware.

---

De forma aleatoria, dependiendo de qué solicitud llegue primero.

#### --answer--

En el orden en que se agrega a la app.

### --question--

#### --text--

¿A qué está vinculado el middleware a nivel de aplicación?

#### --distractors--

A una instancia de ruta específica creada con `express.Router()`.

---

Al servidor HTTP de Node.js devuelto por `app.listen()`.

---

A un manejador de ruta individual definido con `app.get()` o `app.post()`.

#### --answer--

A una instancia de la app Express.

### --question--

#### --text--

¿Cuál de los siguientes se puede usar para registrar middleware a nivel de aplicación?

#### --distractors--

`app.listen()` o métodos específicos de ruta como `app.get()` y `app.delete()`

---

`app.route()` o métodos específicos de ruta como `app.patch()` y `app.options()`

---

`router.use()` o métodos específicos de ruta como `router.get()` y `router.post()`

#### --answer--

`app.use()` o métodos específicos de ruta como `app.get()` y `app.post()`

### --question--

#### --text--

¿A qué está vinculado el middleware a nivel de enrutador?

#### --distractors--

La instancia principal de la app Express creada con `express()`.

---

El objeto servidor del módulo `http` de Node.js.

---

Una función callback manejadora de ruta específica.

#### --answer--

A una instancia de `express.Router()`.

### --question--

#### --text--

¿Qué método se usa para aplicar middleware a nivel de enrutador?

#### --distractors--

`app.use()`

---

`express.use()`

---

`app.router()`

#### --answer--

`router.use()`

### --question--

#### --text--

¿Cómo limitas el middleware a nivel de enrutador a una ruta específica en tu app?

#### --distractors--

Llamando a `router.scope('/path')` antes de definir las rutas.

---

Pasando la ruta directamente a `router.use()` en el archivo principal.

---

Configurando `router.basePath = '/path'` antes de montarlo.

#### --answer--

Montando el enrutador con `app.use('/path', router)`.

### --question--

#### --text--

¿Qué diferencia al middleware para manejo de errores del middleware regular en Express?

#### --distractors--

Usa `res.error()` en lugar de `res.send()` para responder al cliente, saltándose el flujo normal de respuesta.

---

Debe definirse antes que todas las demás rutas para interceptar errores antes de que lleguen a cualquier manejador.

---

Se registra con `app.error()` en lugar de `app.use()`, lo que indica a Express que debe manejar errores.

#### --answer--

Tiene cuatro parámetros (`err`, `req`, `res` y `next`), y el parámetro `err` indica a Express que es un manejador de errores.

### --question--

#### --text--

¿Cuándo se invoca el middleware para manejo de errores?

#### --distractors--

Cuando un manejador de ruta devuelve una respuesta con código de estado 4xx o 5xx.

---

Cuando el servidor recibe más de una solicitud al mismo tiempo.

---

Cuando la solicitud tarda más que el tiempo de espera predeterminado en completarse.

#### --answer--

Cuando se pasa un error a `next(err)`.

### --question--

#### --text--

¿Dónde debe colocarse el middleware para manejo de errores en la pila de middleware?

#### --distractors--

Al principio, antes de definir cualquier ruta.

---

Inmediatamente después de la ruta que probablemente lance un error.

---

Al mismo nivel que otros middleware, en cualquier posición.

#### --answer--

Al final, después de todos los demás middleware y rutas.

### --question--

#### --text--

¿Qué hace el middleware integrado `express.json()`?

#### --distractors--

Sirve archivos JSON almacenados en el directorio público al cliente.

---

Convierte el objeto respuesta a JSON antes de enviarlo al cliente.

---

Valida que todas las solicitudes entrantes contengan un encabezado content-type JSON válido.

#### --answer--

Interpreta solicitudes entrantes con cargas útiles JSON.

### --question--

#### --text--

¿Qué hace `express.static()`?

#### --distractors--

Interpreta solicitudes entrantes con datos de formulario codificados en URL.

---

Cachea respuestas de manejadores de ruta para mejorar el rendimiento.

---

Registra rutas estáticas que no pueden cambiarse en tiempo de ejecución.

#### --answer--

Sirve archivos estáticos como imágenes, CSS y JavaScript.

### --question--

#### --text--

¿Qué interpreta `express.urlencoded()` y dónde está disponible el resultado?

#### --distractors--

Interpreta cargas útiles JSON y hace el resultado disponible en `req.json`.

---

Interpreta cadenas de consulta de la URL y las hace disponibles en `req.query`.

---

Interpreta datos de formulario multipart y hace los archivos disponibles en `req.files`.

#### --answer--

Interpreta cargas útiles codificadas en URL de formularios HTML y hace los datos disponibles en `req.body`.

### --question--

#### --text--

¿Qué hace el middleware de terceros `cors`?

#### --distractors--

Registra todas las solicitudes HTTP entrantes con método, URL y tiempo de respuesta.

---

Interpreta cookies de solicitudes entrantes y las adjunta a `req.cookies`.

---

Comprime cuerpos de respuesta para reducir el tamaño de datos enviados al cliente.

#### --answer--

Habilita el intercambio de recursos de origen cruzado.

### --question--

#### --text--

¿Cuál de las siguientes clasifica correctamente el middleware de Express?

#### --distractors--

`morgan` es integrado mientras `express.static()` es de terceros

---

`cors` es integrado mientras `morgan` es de terceros

---

`express.urlencoded()` es integrado mientras `express.json()` es de terceros

#### --answer--

`express.json()` es integrado mientras `cors` es de terceros

### --question--

#### --text--

¿Qué hace el middleware de terceros `morgan`?

#### --distractors--

Habilita el intercambio de recursos de origen cruzado.

---

Interpreta cookies de solicitudes entrantes y las adjunta a `req.cookies`.

---

Comprime cuerpos de respuesta para reducir el tamaño de datos enviados al cliente.

#### --answer--

Registra solicitudes HTTP.

### --question--

#### --text--

¿Qué pasa cuando una función middleware llama a `next()`?

#### --distractors--

La solicitud se envía inmediatamente de vuelta al cliente con estado 200.

---

La función middleware actual reinicia desde el principio.

---

Express omite todo el middleware restante y ejecuta el manejador de ruta final.

#### --answer--

El control pasa al siguiente middleware o manejador de ruta en la pila.
