---
id: 5900f48d1000cf542c50ffa0
title: '問題 289: オイラー閉路'
challengeType: 1
forumTopicId: 301940
dashedName: problem-289-eulerian-cycles
---

# --description--

$C(x,y)$ を点 ($x$, $y$)、($x$, $y + 1$)、($x + 1$, $y$)、($x + 1$, $y + 1$) を通る円とします。

正の整数 $m$ と $n$ に対して、$E(m,n)$ を次の $m·n$ 個の円の集合とします：{ $C(x,y)$: $0 ≤ x < m$, $0 ≤ y < n$, $x$ と $y$ は整数 }

$E(m,n)$ 上のオイラー閉路とは、各弧をちょうど一度だけ通る閉じた経路のことです。$E(m,n)$ 上には多くのそのような経路がありますが、ここで注目するのは自己交差しないものだけです：自己交差しない経路は格子点で接することはありますが、決して交差しません。

下の画像は $E(3,3)$ とオイラー自己交差しない経路の例を示しています。

<img alt="Eulerian cycle E(3, 3) and Eulerian non-crossing path" src="https://cdn.freecodecamp.org/curriculum/project-euler/eulerian-cycles.gif" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

$L(m,n)$ を $E(m,n)$ 上のオイラー自己交差しない経路の数とします。例えば、$L(1,2) = 2$、$L(2,2) = 37$、$L(3,3) = 104290$ です。

$L(6,10) \bmod {10}^{10}$ を求めてください。

# --hints--

`eulerianCycles()` should return `6567944538`.

```js
assert.strictEqual(eulerianCycles(), 6567944538);
```

# --seed--

## --seed-contents--

```js
function eulerianCycles() {

  return true;
}

eulerianCycles();
```

# --solutions--

```js
// solution required
```
