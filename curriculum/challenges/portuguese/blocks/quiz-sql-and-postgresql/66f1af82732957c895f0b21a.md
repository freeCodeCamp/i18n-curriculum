---
id: 66f1af82732957c895f0b21a
title: Quiz de SQL e PostgreSQL
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

O que é um banco de dados relacional?

#### --distractors--

Um banco de dados estruturado para reconhecer a não relação entre itens armazenados.

---

Um banco de dados que armazena dados não estruturados.

---

Um sistema baseado em arquivo plano.

#### --answer--

Um banco de dados estruturado para reconhecer e gerenciar relacionamentos entre dados em tabelas.

### --question--

#### --text--

Como um banco de dados não relacional difere de um banco de dados relacional?

#### --distractors--

Bancos de dados não relacionais usam tabelas, enquanto bancos de dados relacionais não usam.

---

Bancos de dados relacionais podem armazenar dados não estruturados.

---

Bancos de dados não relacionais são mais lentos do que bancos de dados relacionais.

#### --answer--

Bancos de dados não relacionais podem armazenar dados não estruturados.

### --question--

#### --text--

O que o comando `\l` faz quando executado no terminal interativo do PostgreSQL (`psql`)?

#### --distractors--

Lista todos os esquemas.

---

Lista todas as tabelas.

---

Conecta-se a um banco de dados.

#### --answer--

Lista todos os bancos de dados.

### --question--

#### --text--

Qual dos seguintes comandos cria uma nova tabela em SQL?

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

Qual é o propósito de uma chave estrangeira em um banco de dados relacional?

#### --distractors--

Para permitir a duplicação de dados.

---

Para identificar exclusivamente uma linha na tabela.

---

Para armazenar grandes quantidades de dados de texto.

#### --answer--

Para criar um relacionamento entre duas tabelas.

### --question--

#### --text--

Qual comando adicionará uma nova coluna a uma tabela existente?

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

Que tipo de relacionamento existe quando um registro na Tabela A corresponde a um ou mais registros na Tabela B?

#### --distractors--

Um-para-um (1-1)

---

Muitos-para-Muitos (m-m)

---

Muitos-para-um(m-1)

#### --answer--

Um-para-Muitos (1-m)

### --question--

#### --text--

O que é uma query?

#### --distractors--

Uma solicitação para remover todos os dados do banco de dados.

---

Uma linha especial para valores únicos em um banco de dados.

---

Uma coluna especial para valores únicos em um banco de dados.

#### --answer--

Uma solicitação para dados específicos do banco de dados.

### --question--

#### --text--

Como você exclui uma coluna de uma tabela em SQL?

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

Qual é a função da restrição `PRIMARY KEY` no SQL?

#### --distractors--

Para vincular duas tabelas.

---

Para definir o valor padrão para uma coluna.

---

Para permitir valores nulos na coluna.

#### --answer--

Para garantir que a coluna contenha valores únicos.

### --question--

#### --text--

O que o comando `\c` faz no terminal interativo do PostgreSQL (`psql`)?

#### --distractors--

Lista todas as tabelas no banco de dados atual.

---

Exibe os detalhes atuais da conexão com o banco de dados.

---

Sai do shell `psql`.

#### --answer--

Conecta ao banco de dados especificado.

### --question--

#### --text--

O que é uma chave composta em um banco de dados relacional?

#### --distractors--

Uma chave que é derivada de outra chave.

---

Uma chave que muda frequentemente com base nas atualizações de dados.

---

Uma chave usada exclusivamente para relacionamentos de chave estrangeira.

#### --answer--

Uma chave que é composta por múltiplas colunas para identificar unicamente um registro.

### --question--

#### --text--

Qual comando insere dados em uma tabela?

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

Como você renomeia uma coluna em SQL?

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

Qual consulta irá deletar todos os registros da tabela employees?

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

Qual restrição SQL garante especificamente que uma coluna possa conter apenas valores únicos, permitindo nulos?

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

Qual dos seguintes NÃO é um exemplo de um banco de dados relacional comumente usado na indústria?

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQLQuery

### --question--

#### --text--

O que uma relação one-to-one (1-1) representa em um banco de dados relacional?

#### --distractors--

Cada registro na Tabela A corresponde a um ou mais registros na Tabela B.

---

Cada registro na Tabela A não corresponde a nenhum registro na Tabela B.

---

Cada registro na Tabela A corresponde a múltiplos registros na Tabela B e vice-versa.

#### --answer--

Cada registro na Tabela A corresponde exatamente a um registro na Tabela B.

### --question--

#### --text--

Qual comando listará todas as tabelas no banco de dados PostgreSQL atual?

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

Qual das seguintes opções permite que você una uma tabela com ela mesma?

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`


