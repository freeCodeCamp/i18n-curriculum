---
id: 6621a7b1a89964984aa2fa6b
title: 任务 3
challengeType: 22
dashedName: task-3
lang: zh-CN
inputType: pinyin-tone
---

<!-- (Audio) Lin Yating: bǎi -->

# --description--

你已经学会了声调符号总是标在韵母上，而不是声母上。如果一个音节中有多个元音，以下是决定哪个元音获得声调符号的三个规则：

- 如果有 `a`，声调符号就标在 `a` 上。

- 如果没有 `a`，但有 `o` 或 `e`，声调符号就标在出现的那个字母上。这两个字母永远不会同时出现在同一个音节中。

- 如果 `i` 和 `u` 一起出现，声调符号应标在第二个字母上。

因为 `ai` 包含 `a`，声调符号标在 `a` 上。例如，`lái`。

# --instructions--

听音频并用正确的声调符号完成音节。

# --fillInTheBlank--

## --sentence--

`BLANK`

## --blanks--

`bǎi`

### --feedback--

这个音节将声母 `b` 与韵母 `ai` 结合，并使用第三声。

# --explanation--

音频发音为音节 `bǎi`，它由声母 `b` 和复合韵母 `ai` 组成。声调符号标在 `a` 上，遵循如果存在 `a`，则其接受声调符号的规则。

该音节使用第三声，起始于中间，下降到低点然后再次上升。

# --scene--

```json
{
  "setup": {
    "background": "company2-center.png",
    "characters": [
      {
        "character": "Lin Yating",
        "position": {
          "x": 50,
          "y": 15,
          "z": 1.4
        },
        "opacity": 0
      }
    ],
    "audio": {
      "filename": "ZH_A1_pinyin_compound_finals.mp3",
      "startTime": 1,
      "startTimestamp": 53.94,
      "finishTimestamp": 54.8
    }
  },
  "commands": [
    {
      "character": "Lin Yating",
      "opacity": 1,
      "startTime": 0
    },
    {
      "character": "Lin Yating",
      "startTime": 1,
      "finishTime": 1.86,
      "dialogue": {
        "text": "bǎi",
        "align": "center"
      }
    },
    {
      "character": "Lin Yating",
      "opacity": 0,
      "startTime": 2.36
    }
  ]
}
```
