---
id: 69a98755ee5508d58e91dccc
title: NPM 测验
challengeType: 8
dashedName: quiz-npm
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

npm 为开发者提供了什么？

#### --distractors--

一个专门托管诸如 `fonts` 和 `icons` 等设计资源的精选平台。

---

一个自动将你的 `project` 部署到云端的托管服务。

---

一个管理你应用程序用户记录的数据库方案。

#### --answer--

一个预构建软件包的注册表，用于解决常见问题。

### --question--

#### --text--

以下哪一项不是 npm 所 `refer` 的三部分之一？

#### --distractors--

网站 npmjs.com。

---

库被 `store` 的 `package` 注册表。

---

用于安装和管理 `package` 的命令行工具。

#### --answer--

Node.js 运行期。

### --question--

#### --text--

`npm install` 命令做什么？

#### --distractors--

它从一个模板脚手架一个全新的 Node.js 项目。

---

它在局部的开发服务器上运行你的 Node.js 应用。

---

它会移除你项目中不再需要的包。

#### --answer--

它为你的项目添加预构建的包以完成常见任务。

### --question--

#### --text--

哪个 `file` 保存 Node.js `project` 的元数据，如 `project` 名称、版本、作者和依赖项？

#### --distractors--

`README.md`。

---

`node_modules/package.json`。

---

`package-lock.json`。

#### --answer--

`package.json`。

### --question--

#### --text--

你如何交互式地创建一个 `package.json` 文件？

#### --distractors--

运行 `npm install`，不指定包名。

---

从 npm 注册表主页下载一个 `template`。

---

从另一个 `project` 拷贝 `file` 并重命名。

#### --answer--

运行 `npm init` 命令。

### --question--

#### --text--

如何在不回答提示符的情况下创建带有默认值的 `package.json` 文件？

#### --distractors--

运行 `npm init --interactive`。

---

创建一个名为 `package.json` 的空白文件并保持为空。

---

使用 `npm init` 并在打开编辑器后删除每个提示符。

#### --answer--

运行 `npm init -y`（或 `npm init --yes`）以自动接受所有默认值。

### --question--

#### --text--

npm 将仅在开发时需要的包（例如 linter 或测试运行器）列在哪里？

#### --distractors--

在 `"dependencies"` 部分内。

---

在自定义命令下的 `"scripts"` 部分内。

---

就在 `node_modules` 文件夹内。

#### --answer--

在 `package.json` 的 `"devDependencies"` 部分内。

### --question--

#### --text--

哪个命令将 `nodemon` 安装为仅开发依赖？

#### --distractors--

`npm uninstall nodemon`。

---

`npm save nodemon --dev`。

---

`npm install nodemon`。

#### --answer--

`npm install nodemon -D`。

### --question--

#### --text--

安装依赖后，哪个文件夹和文件被添加到你的项目根目录？

#### --distractors--

`public` 文件夹和 `index.html`。

---

`dist` 文件夹和 `webpack.config.js`。

---

`src` 文件夹和 `README.md`。

#### --answer--

`node_modules` 文件夹和 `package-lock.json` 文件。

### --question--

#### --text--

`package-lock.json` 如何帮助保持构建的一致性？

#### --distractors--

通过在每次安装之前删除未使用的依赖项。

---

仅存储包的主版本号。

---

通过自动将包升级到其最新版本。

#### --answer--

通过冻结安装的每个依赖的确切版本。

### --question--

#### --text--

为什么你应该将 `package-lock.json` 提交到你的 Git 仓库？

#### --distractors--

为了使 npm 在开发者的机器上运行更快。

---

自动更新过时的依赖项。

---

为了减少仓库大小。

#### --answer--

因此整个团队安装相同的依赖版本和设置。

### --question--

#### --text--

你如何安装特定版本的包，比如 `express@4.21.2`？

#### --distractors--

`npm update express@4.21.2`。

---

`npm get express@4.21.2`。

---

`npm use express@4.21.2`。

#### --answer--

`npm install express@4.21.2`。

### --question--

#### --text--

当指定依赖版本时，插入符号（`^`）允许什么？

#### --distractors--

它将依赖锁定到一个精确的补丁版本。

---

它只允许更新主版本。

---

它排除了指定版本以下的所有版本。

#### --answer--

它允许较新的次要版本和补丁版本，但阻止新的主要版本。

### --question--

#### --text--

在像 `~1.2.3` 这样的版本范围中，波浪号（`~`）符号是什么意思？

#### --distractors--

它允许对主版本和次版本进行更新。

---

它会安装不管 `range` 如何的最新主版本。

---

它阻止 npm 安装任何更新。

#### --answer--

它只允许更新补丁版本，同时保持次版本不变。

### --question--

#### --text--

语义版本控制中，什么样的更改会触发主版本号的提升？

#### --distractors--

对评论和格式进行细微的样式调整。

---

不改变行为的小错误修复。

---

不会破坏现有代码的可选特色。

#### --answer--

可能导致现有代码停止工作的重大更改。

### --question--

#### --text--

哪个命令列出有更新发布版本的依赖项？

#### --distractors--

`npm check`。

---

`npm list`。

---

`npm refresh`。

#### --answer--

`npm outdated`。

### --question--

#### --text--

Yarn、PNPM 和 Bun 被提及为 npm 的备选。它们是什么类型的工具？

#### --distractors--

你发布或下载 npm 包的注册表网站。

---

为 npm `package` 存储 `user` `data` 的数据库服务。

---

自动部署 npm 项目的云托管平台。

#### --answer--

备选包管理器。

### --question--

#### --text--

自定义 npm 脚本在项目中声明在哪里？

#### --distractors--

在 `.npmrc` 文件内。

---

在 `package.json` 的 `"dependencies"` 部分内。

---

在根目录的单独 `scripts.json` 文件中。

#### --answer--

在 `package.json` 的 `"scripts"` 对象内。

### --question--

#### --text--

你如何执行名为 `build` 的自定义 npm 脚本？

#### --distractors--

`build npm`。

---

`node build`。

---

`run build`。

#### --answer--

`npm run build`。

### --question--

#### --text--

在运行 `npm run start -- 8000` 时，双连字符 (`--`) 有什么作用？

#### --distractors--

它会使脚本输出静音。

---

它迫使 npm 并行运行该 `script`。

---

它在执行 `script` 之前请求管理员权限。

#### --answer--

它告诉 npm 将 `--` 之后的所有内容作为参数转发给底层命令。
