---
id: 66f1a417757b6ca4eecd89d6
title: Jaribio fupi la hali na hooks za React
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

Ili kupita jaribio fupi, lazima ujibu kwa usahihi angalau maswali 18 kati ya 20 yaliyo hapa chini.

# --quizzes--

## --quiz--

### --question--

#### --text--

Ni ipi kati ya zifuatazo njia sahihi ya kuongeza tukio la bofya kwenye kitufe katika React?

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

React Synthetic Event ni nini?

#### --distractors--

Aina ya kipengele cha JSX kwa ajili ya tukio la `inputs` na `submit` tu.

---

Aina ya kipengele cha JSX kwa ajili ya tukio la `submit` tu.

---

Njia ya kubadilisha awamu ya kuenea kwa tukio ili iwe rafiki zaidi kwa mtumizi.

#### --answer--

Kifungashio cha matukio ya asili kama matukio ya `click` na `submit`.

### --question--

#### --text--

Ni ipi kati ya zifuatazo ni kiambishi awali kinachotumika kwa kawaida kwa majina ya njia za mshughulikiaji wa matukio?

#### --distractors--

isHandling

---

hasHandled

---

handling

#### --answer--

handle

### --question--

#### --text--

Ni hatua gani kuu tatu katika kuonyesha (rendering) kwa React?

#### --distractors--

Handshake, kupakia na hydration.

---

Kuvuta data, kufasiri na utekelezaji.

---

Kuweka, kusasisha na kuondoa.

#### --answer--

Kuchochea, kuonyesha na kuidhinisha.

### --question--

#### --text--

Ni ipi kati ya zifuatazo njia sahihi ya kufanya kazi na hook ya `useState`?

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

Ni ipi kati ya sifa zifuatazo si sahihi kwa ajili ya kushughulikia matukio ya React?

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

Ni ipi kati ya zifuatazo njia sahihi ya kusasisha hali ya safu ya data?

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--

Ni nini kilicho kosa katika kitendakazi hiki?

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

Kusasisisha variable ya `useState` haikubaliki ndani ya kitendakazi kilichopangwa ndani.

---

`previousState` inahitaji kuzungukwa na mabano.

---

Kitendakazi cha mshale hakipaswi kutumika ndani ya kitendakazi cha kuweka hali.

#### --answer--

`previousState` lazima isambazwe kabla ya `name` ili kuhakikisha sifa nyingine hazibadilishwi.

### --question--

#### --text--

Ni ipi kati ya zifuatazo njia sahihi ya kusasisha hali ili kuondoa vitu kutoka kwenye safu ya data?

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

Sifa ya `ref` kwa kawaida hutumika kwa nini katika React?

#### --distractors--

Inatumika kuchunguza matatizo ya utendaji katika sehemu za react.

---

Inatumika kuchunguza matatizo ya ufikikaji katika sehemu za react.

---

Inatumika kuondoa nodi za DOM.

#### --answer--

Inatumika kupata upatikanaji wa nodi ya DOM.

### --question--

#### --text--

Ni ipi kati ya zifuatazo njia sahihi ya kupata thamani ya sasa ya `ref`?

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

Je, ujumbe huu utaandikwa mara ngapi kwenye konsoli?

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

Mara tatu.

---

Mara mbili.

---

Hakuna kwa sababu safu ya utegemezi ni tupu.

#### --answer--

Mara moja tu wakati wa kuonyesha kwa mara ya kwanza.

### --question--

#### --text--

Hooks maalum za React kwa kawaida hutumika kwa nini?

#### --distractors--

Zinatumika kutekeleza kazi ngumu zaidi za programu.

---

Hazitumiki sana, hooks zilizojengwa ndani zinapendekezwa.

---

Zinachukua nafasi ya njia za mzunguko wa maisha kutoka matoleo ya awali ya React.

#### --answer--

Zinasaidia kutumia tena mantiki yenye hali katika sehemu mbalimbali.

### --question--

#### --text--

Je, ujumbe huu utaandikwa mara ngapi kwenye konsoli?

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

Utaandikwa kila wakati fomu inapotumwa au tukio la bofya linapotokea.

---

Utaandikwa mara mbili.

---

Utaandikwa mara moja tu.

#### --answer--

Utaandikwa kila wakati sehemu inaonyesha tena.

### --question--

#### --text--

Ni ipi kati ya zifuatazo njia sahihi ya kuzuia onyesha upya ya kivinjari kwa tukio la `onSubmit`?

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

Ni ipi kati ya hooks zifuatazo hutumika kwa kawaida kuvuta data?

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

Ni ipi kati ya sifa zifuatazo si halali kwenye kitu cha Event asili?

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

Ni ipi kati ya hatua za kuonyesha hutokea wakati React inagundua kuwa kitu kimebadilika na kiolesura cha mtumizi kinaweza kuhitaji kusasishwa?

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

Ni ipi kati ya hatua hizi inahusu wakati React inachukua mabadiliko yaliyotayarishwa kutoka kwa virtual DOM na kuyaweka kwenye DOM halisi?

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

Nini kusudi la `dependencies` katika `useEffect`?

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

Hoja ya hiari ya `dependencies` haina athari yoyote inayohusiana na `useEffect`.

---

Hoja ya hiari ya `dependencies` husababisha athari kuendelea kuendeshwa bila kikomo.

---

Hoja ya hiari ya `dependencies` hutumika kuzuia athari kuendeshwa.

#### --answer--

Hoja ya hiari ya `dependencies` hudhibiti wakati athari inaendeshwa.
