---
id: 675961ce80a70e23e933fa8c
title: 작업 38
challengeType: 19
dashedName: task-38
lang: en-US
---

# --instructions--

이 도전 과제에는 오디오가 없습니다. 아래 질문을 읽고 올바른 답을 선택하세요.

# --questions--

## --text--

어떤 선택지가 태그 질문에서 `haven't` 또는 `hasn't`을 올바르게 사용했나요?

## --answers--

`She's completed the report, haven't she?`

### --feedback--

주문 문장에 `Has`이 사용되었으므로 태그 질문에는 `hasn't she?`가 와야 합니다.

---

`We've been waiting for hours, hasn't we`

### --feedback--

주문 문장에 `Have`이 사용되었으므로 태그 질문에는 `haven't we?`가 와야 합니다.

---

`They've solved the issue, hasn't they?`

### --feedback--

주문 문장에 `Have`이 사용되었으므로 태그 질문에는 `haven't they?`가 와야 합니다.

---

`You've checked the logs, haven't you?`

## --video-solution--

4

# --explanation--

태그 질문은 주 문장의 조동사와 일치해야 합니다. 주 문장에 `haven't`이 사용되면 `have`를 사용하세요. 주 문장에 `hasn't`이 사용되면 `has`를 사용하세요.
