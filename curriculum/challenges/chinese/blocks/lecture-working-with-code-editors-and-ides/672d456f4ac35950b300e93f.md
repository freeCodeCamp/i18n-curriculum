---
id: 672d456f4ac35950b300e93f
title: 在 VS Code 中，有哪些有用的键盘快捷键能最大化工作效率？
challengeType: 19
dashedName: what-are-several-useful-keyboard-shortcuts-for-maximizing-productivity-in-vs-code
---

# --description--

你可能已经熟悉了一些基本快捷方式，它们都是从操作系统中继承下来的。 例如，在 VS Code 中，`Ctrl + S` 保存文件、`Ctrl + C` 复制内容、`Ctrl + V` 粘贴内容，这些快捷键都可以正常使用。

但还有一些特有的快捷键能进一步提升你的效率。 值得注意的是，部分快捷键可能因操作系统不同而有所差异。

例如，`Shift + Alt + F` 会对当前打开的文件运行你配置好的格式化工具（比如 JavaScript 项目中的 Prettier）。

使用 `Ctrl + Shift + F`（Windows）或者`Cmd + Shift + F`（Mac），可以搜索工作区中所有文件的文本内容。 如果想进行查找替换，使用 `Ctrl + Shift + H`（Windows）或 `Cmd + Shift + H`（Mac）。

如果要删除一行代码，使用 `Ctrl + Shift + K`（Windows）或 `Cmd + Shift + K` （Mac）即可。

需要更多空间来容纳你的代码？按 `Ctrl + B`（Windows）或 `Cmd + B`（Mac）将隐藏侧边栏——其中包含文件列表和扩展名菜单。

或者你只是看不到你的代码？`Ctrl + plus`（Windows）或 `Cmd + plus`（Mac）将增加编辑器的缩放比例，`Ctrl + minus`（Windows）或 `Cmd + minus`（Mac）将减少它。

VS Code 中另一个有用的特色是多行编辑，它允许你一次对多行进行更改。例如，你可以使用 `Ctrl + Alt + down` 和 `Ctrl + Alt + up`（Windows/Linux），或 `Option + Command + down` 和 `Option + Command + up`（Mac），将光标扩展到连续的多行。你也可以按住 `Alt`（Windows/Linux）或 `Option`（Mac）并点击，在需要的不同行添加光标。光标放置好后，你输入或删除的任何内容都会同时应用到所有这些位置——这在处理重复代码时可以节省大量时间。

最后，如果你忘记了这些快捷键，随时可以使用 `Ctrl + Shift + P`（Windows）或 `Cmd + Shift + P`（Mac），这会打开命令面板，你可以从中选择所需的任何内容。

掌握这些知识，再加上一些练习，你离成为 VS Code 高级用户就不远了。

# --questions--

## --text--

在 VS Code 中，哪个 Windows 键盘快捷键用于对当前打开的文件运行已配置的格式化程序？

## --answers--

`Ctrl + F`

### --feedback--

课程提到了用于格式化当前 `file` 的特定快捷。

---

`Shift + Alt + F`

---

`Ctrl + Shift + F`

### --feedback--

课程提到了用于格式化当前 `file` 的特定快捷。

---

`Alt + F4`

### --feedback--

课程提到了用于格式化当前 `file` 的特定快捷。

## --video-solution--

2

## --text--

在 VS Code 中，键盘快捷键 `Ctrl + Shift + K`（Windows/Linux）或 `Cmd + Shift + K`（macOS）执行什么函数？

## --answers--

打开命令面板。

### --feedback--

本课介绍了一个用于删除一行代码的快捷。

---

增大编辑器的文字大小。

### --feedback--

本课介绍了一个用于删除一行代码的快捷。

---

删除当前行。

---

隐藏侧边栏。

### --feedback--

本课介绍了一个用于删除一行代码的快捷。

## --video-solution--

3

## --text--

哪个 Windows 键盘快捷打开 VS Code 中的命令面板？

## --answers--

`Ctrl + P`

### --feedback--

课程最后提到这个快捷是作为访问你可能忘记的任何命令的方法。

---

`Ctrl + Shift + P`

---

`Alt + F4`

### --feedback--

课程最后提到这个快捷是作为访问你可能忘记的任何命令的方法。

---

`Ctrl + Space`

### --feedback--

课程最后提到这个快捷是作为访问你可能忘记的任何命令的方法。

## --video-solution--

2
