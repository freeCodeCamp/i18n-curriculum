---
id: 672c3aa9bc3a481425cb52b3
title: 什么是 z-index 属性，它是如何控制已定位元素的堆叠顺序的？
challengeType: 19
dashedName: what-is-the-z-index-property
---

# --interactive--

CSS 中的 `z-index` 属性用于控制页面上堆叠的已定位元素的垂直顺序。 当多个元素堆叠在一起时，`z-index` 值将决定哪个元素出现在顶部。 `z-index` 值越高，元素离查看者越近，而值越低，元素在堆叠中的位置越靠后。

然而，`z-index` 仅对已定位的元素有效，这意味着该元素的位置属性必须设置为除 `static` 以外的其他值，例如 `relative`、`absolute` 或 `fixed`。 默认的 `z-index` 值是 `auto`，它将元素放置在默认的堆叠顺序中。 让我们来看一个有三个盒子的示例：

```html
<div class="container">
  <div class="box1">Box 1</div>
  <div class="box2">Box 2</div>
  <div class="box3">Box 3</div>
</div>
```

现在，我们可以应用一些样式来定位盒子，使其相互重叠，就像这样：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="container">
  <div class="box1">Box 1</div>
  <div class="box2">Box 2</div>
  <div class="box3">Box 3</div>
</div>
```

```css
.container {
  position: relative;
  width: 300px;
  height: 300px;
  border: 1px solid black;
}

.box1 {
  position: absolute;
  z-index: 1;
  background: lightcoral;
  top: 20px;
  left: 20px;
  width: 100px;
  height: 100px;
}

.box2 {
  position: absolute;
  z-index: 3;
  background: gold;
  top: 40px;
  left: 40px;
  width: 100px;
  height: 100px;
}

.box3 {
  position: absolute;
  z-index: 2;
  background: lightgreen;
  top: 60px;
  left: 60px;
  width: 100px;
  height: 100px;
}
```

:::

对于容器，位置将设置为 `relative`，所有嵌套在其中的盒子都将设置为 `absolute` 定位。 每个盒子的 `z-index` 值都不同，这导致盒子相互分层。

你可以把 `z-index` 看作是网页上的一种分层方式，具有较高 `z-index` 值的元素将置于较低值的元素之上。 这对于控制重叠元素在复杂布局（如模态、弹出式窗口或提示）中的行为特别有用。

# --questions--

## --text--

CSS 中的 `z-index` 属性控制什么？

## --answers--

元素的颜色。

### --feedback--

`z-index` 负责元素在视觉上的堆叠顺序。

---

定位元素的垂直堆叠顺序。

---

元素的大小。

### --feedback--

`z-index` 负责元素在视觉上的堆叠顺序。

---

文本的水平对齐方式。

### --feedback--

`z-index` 负责元素在视觉上的堆叠顺序。

## --video-solution--

2

## --text--

要使 `z-index` 属性起作用，元素必须具备什么条件？

## --answers--

它必须有 `fixed` 宽度。

### --feedback--

考虑是什么让一个元素能够被放置在堆叠上下文中。

---

它必须具有除 `static` 以外的 `position` 值。

---

他必须具有 `block` 的 `display` 值。

### --feedback--

考虑是什么让一个元素能够被放置在堆叠上下文中。

---

它必须设置背景颜色。

### --feedback--

考虑是什么让一个元素能够被放置在堆叠上下文中。

## --video-solution--

2

## --text--

哪个 `z-index` 值可将元素置于所有其他元素之上？

## --answers--

`z-index: 0;`

### --feedback--

想想哪个数字最高，以及 `z-index` 是如何工作的。

---

`z-index: 5;`

---

`z-index: auto;`

### --feedback--

想想哪个数字最高，以及 `z-index` 是如何工作的。

---

`z-index: -1;`

### --feedback--

想想哪个数字最高，以及 `z-index` 是如何工作的。

## --video-solution--

2
