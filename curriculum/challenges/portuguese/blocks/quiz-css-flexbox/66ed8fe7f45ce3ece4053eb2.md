---
id: 66ed8fe7f45ce3ece4053eb2
title: Questionário sobre o Flexbox do CSS
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

Para passar no quiz, você deve responder corretamente pelo menos 18 das 20 perguntas abaixo.

# --quizzes--

## --quiz--

### --question--

#### --text--

O que é flexbox?

#### --distractors--

Um modelo bidimensional para layout.

---

Um modelo tridimensional para layout.

---

Um modelo multidimensional para layout.

#### --answer--

Um modelo unidimensional para layout.

### --question--

#### --text--

Qual propriedade CSS é definida para habilitar o layout flexbox para o elemento `div`?

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

Qual propriedade CSS permite que os itens flex se quebrem para uma nova linha quando a linha atual estiver cheia?

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

O que a propriedade flexbox `justify-content` controla?

#### --distractors--

Ele controla a direção dos elementos filhos do contêiner flexbox.

---

Ele controla o estilo do texto dos elementos filhos do contêiner flexbox.

---

Ele controla o alinhamento dos elementos filhos dentro do contêiner flexbox.

#### --answer--

Ele controla a distribuição dos elementos filhos no eixo principal.

### --question--

#### --text--

Qual propriedade controla a direção dos elementos dentro do contêiner flexbox?

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

Quais propriedades CSS podem ser usadas para fazer os itens flex fluírem da esquerda para a direita e fazer com que o novo item flex apareça acima do anterior se a largura da linha atual for excedida?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

Qual propriedade controla o posicionamento dos elementos no eixo cruzado?

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

Qual não é um valor para `justify-content`?

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

Quais propriedades CSS você usaria para centralizar um elemento dentro de um contêiner flex horizontalmente e verticalmente?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

Qual valor de `flex-direction` faz com que os itens flex se movam da direita para a esquerda?  

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

O que não é um valor da propriedade `flex-wrap`?

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

O que o valor `stretch` em `align-items` faz?

#### --distractors--

Os itens são embalados diretamente uns contra os outros em direção à borda inicial do contêiner.

---

Os itens são alinhados de forma que as linhas de base do contêiner flex estejam alinhadas.

---

Os itens são embalados diretamente uns contra os outros em direção à borda final do contêiner.

#### --answer--

Redimensione automaticamente os itens para que tenham o mesmo tamanho e preencham o contêiner.

### --question--

#### --text--

Qual valor de `justify-content` posiciona o primeiro item no início, o último item no final e distribui uniformemente os outros itens entre eles ao longo do eixo principal?

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

Qual valor de `justify-content` espaça os itens de modo que os espaços entre cada par de itens adjacentes e as bordas do contêiner sejam todos iguais?

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

Qual valor de `justify-content` distribui os itens igualmente ao longo do eixo principal deixando metade desse espaço nas bordas do container?

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

Qual das opções a seguir faz com que os itens fiquem alinhados no início do eixo cruzado?

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

Qual das opções a seguir faz com que os itens fiquem alinhados no final do eixo cruzado?

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

Qual valor para `flex-direction` fará com que os itens sejam alinhados ao longo do eixo principal da esquerda para a direita?

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

Qual valor para `flex-direction` fará com que os itens sejam alinhados ao longo do eixo cruzado de cima para baixo?

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

Qual dos seguintes casos de uso não é adequado para flexbox?

#### --distractors--

Centralizando um item em um container.

---

Alinhamento vertical dos itens em uma linha.

---

Ajustando dinamicamente a distribuição e o alinhamento dos itens para diferentes viewports.

#### --answer--

Tamanho fixo e espaço entre os itens como uma grade.

## --quiz--

### --question--

#### --text--

Qual é o propósito de um modelo de layout unidimensional como o Flexbox?

#### --distractors--

Para lidar com o layout em duas dimensões, tanto linhas e colunas ao mesmo tempo.

---

Para controlar a ordem de empilhamento dos elementos posicionados usando `z-index`.

---

Para criar transformações 3D e animações complexas em elementos da página.

#### --answer--

Para dispor itens ao longo de uma única linha ou eixo, seja como uma linha ou como uma coluna.

### --question--

#### --text--

Qual propriedade CSS alinhará todos os itens flexíveis ao lado direito do elemento `.container`?

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Quais são os dois eixos que definem a direção do layout dos itens no Modelo Flex?

#### --distractors--

O eixo horizontal e o eixo vertical.

---

O eixo x e o eixo y.

---

O eixo primário e o eixo secundário.

#### --answer--

O eixo principal e o eixo transversal.

### --question--

#### --text--

Qual das seguintes regras CSS organizará os itens em uma coluna vertical e também os centralizará horizontalmente dentro do seu contêiner?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

Qual é o principal propósito da propriedade `flex-wrap`?

#### --distractors--

Define a direção em que os itens flex são posicionados no contêiner flexível.

---

Ele alinha os itens flexíveis ao longo do eixo principal do container.

---

Ele alinha os itens flex ao longo do eixo cruzado do container.

#### --answer--

Determina se os itens flex devem quebrar em novas linhas para caber no espaço disponível.

### --question--

#### --text--

Qual é o comportamento padrão da declaração `flex-wrap: nowrap;`?

#### --distractors--

Ele força os itens flex a quebrarem para uma nova linha se eles forem muito largos para o container.

---

Ele quebra os itens em uma nova linha, mas na ordem inversa.

---

Ele impede que o contêiner flex seja maior que seu elemento pai.

#### --answer--

Ele força os itens flex a permanecerem em uma única linha, mesmo se eles ultrapassarem o contêiner.

### --question--

#### --text--

Qual propriedade CSS é um atalho para definir as propriedades `flex-direction` e `flex-wrap` ao mesmo tempo?

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

Qual é o efeito de definir `flex-direction: column-reverse;` em um contêiner flexível?

#### --distractors--

Ele organiza os itens horizontalmente da direita para a esquerda.

---

Ele inverte os itens em uma linha, mas os mantém em uma única linha.

---

Ele organiza os itens verticalmente de cima para baixo.

#### --answer--

Ele organiza os itens verticalmente em ordem reversa de baixo para cima.

### --question--

#### --text--

Qual trecho de CSS organiza corretamente os itens flex em uma coluna vertical e permite que eles quebrem para uma nova coluna se ultrapassarem a altura do contêiner?

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

Qual valor de `justify-content` alinha todos os itens flex ao final do eixo principal do container?

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Qual é o efeito da seguinte regra CSS nos itens flexíveis?

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

Os itens são organizados em uma coluna e irão quebrar para uma nova coluna.

---

Os itens são organizados em uma linha e irão quebrar para uma nova linha.

---

Os itens são organizados em uma coluna e irão transbordar se não couberem.

#### --answer--

Os itens são organizados em uma linha e irão transbordar o contêiner se não couberem.

### --question--

#### --text--

Como `justify-content: space-evenly;` alinha os itens flexíveis?

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

Os itens são espaçados uniformemente, sem espaço no início ou no fim.

---

Os itens são agrupados no centro do container.

---

Os itens são empurrados para o lado direito do container, com espaçamento igual entre eles.

#### --answer--

Os itens têm espaço igual entre eles e em ambas as extremidades do contêiner.

### --question--

#### --text--

Qual propriedade você usaria para centralizar verticalmente os itens quando `flex-direction: row;` está definido?

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

O que o valor `flex-start` para a propriedade `justify-content` faz?

#### --distractors--

Ele alinha os itens ao início do eixo cruzado.

---

Ele estica os itens para preencher o eixo principal do contêiner.

---

Ele centraliza todos os itens ao longo do eixo principal.

#### --answer--

Alinha os itens ao início do eixo principal do container.

### --question--

#### --text--

Como você configuraria um contêiner flex para organizar seus itens da direita para a esquerda em uma única linha e também posicioná-los no topo do contêiner?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

Qual das seguintes é uma declaração válida para a propriedade `flex-flow`?

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

Qual das seguintes regras CSS resultará em itens flex sendo centralizados horizontalmente e esticados verticalmente para preencher a altura do contêiner?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

Qual efeito `align-items: stretch;` tem quando `flex-direction` está definido como `column`?

#### --distractors--

Ele estica os itens verticalmente para preencher a altura do contêiner.

---

Não tem efeito quando a direção é `column`.

---

Ele estica os itens verticalmente para corresponder ao item mais alto.

#### --answer--

Ele estica os itens horizontalmente para preencher a largura do container.

### --question--

#### --text--

Se você quiser inverter a ordem dos itens em uma linha horizontal, qual declaração deve usar?

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

Qual valor de `align-items` posicionará os itens no topo de um container cujo `flex-direction` é `row`?

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
