---
id: 6a04bbc9815e737ed711622e
title: اختبار معالجة الأخطاء في Express
challengeType: 8
dashedName: quiz-error-handling-in-express
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ماذا يعني رمز حالة HTTP 201؟

#### --distractors--

نجح الطلب بدون وجود جسم استجابة.

---

العميل غير مخول للوصول إلى المورد.

---

المورد المطلوب غير موجود.

#### --answer--

تم إنشاء مورد جديد بنجاح.

### --question--

#### --text--

أي رمز حالة يشير إلى أن الطلب نجح ولكن لا يوجد جسم استجابة؟

#### --distractors--

`200`

---

`201`

---

`301`

#### --answer--

`204`

### --question--

#### --text--

ماذا يخبر رمز الحالة `304 Not Modified` العميل؟

#### --distractors--

تم نقل المورد إلى عنوان URL دائم جديد.

---

الخادم غير متاح مؤقتًا للصيانة.

---

يتطلب الوصول إلى المورد توثيقًا.

#### --answer--

لم يتغير شيء، لذا يمكن للعميل استخدام النسخة المخزنة مؤقتًا.

### --question--

#### --text--

ما الفرق بين رموز الحالة `401` و `403`؟

#### --distractors--

`401` تعني أن المورد غير موجود بينما `403` تعني أن الخادم متوقف.

---

`401` تعني أن الطلب غير صحيح بينما `403` تعني أن المورد تم نقله.

---

`401` تعني أن الخادم تعطل بينما `403` تعني أن الطلب انتهت صلاحيته.

#### --answer--

`401` تعني أن التوثيق مطلوب بينما `403` تعني أن المستخدم موثّق لكنه لا يملك الإذن.

### --question--

#### --text--

أي رمز حالة يعني أن الخادم مثقل مؤقتًا أو متوقف للصيانة؟

#### --distractors--

`500`

---

`502`

---

`301`

#### --answer--

`503`

### --question--

#### --text--

ماذا يفعل `res.status(200).json({ message: "Success" })` في Express؟

#### --distractors--

يرسل استجابة `200` مع رسالة نصية عادية.

---

يرسل استجابة `200` بدون جسم.

---

يعيد توجيه العميل إلى صفحة النجاح.

#### --answer--

يرسل استجابة `200` مع جسم JSON.

### --question--

#### --text--

ما الذي يجعل Express يتعرف على دالة برمجيات وسيطة كمعالج للأخطاء؟

#### --distractors--

يُوضع في أعلى مكدس البرمجيات الوسيطة.

---

يستخدم `res.error()` بدلًا من `res.send()`.

---

يُعرف داخل معالج مسار.

#### --answer--

يحتوي على المعلمات `err`، `req`، `res`، و `next`.

### --question--

#### --text--

أين يجب وضع برمجيات وسيطة لمعالجة الأخطاء في تطبيق Express؟

#### --distractors--

قبل جميع تعريفات المسارات.

---

في أعلى الملف تمامًا، قبل أي استيراد.

---

داخل أول معالج مسار قد يرمي خطأً.

#### --answer--

في نهاية مكدس البرمجيات الوسيطة، بعد جميع المسارات.

### --question--

#### --text--

في Express 4، ماذا يجب أن تفعل لمعالجة أخطاء غير المتزامنة في معالج مسار؟

#### --distractors--

ارمِ الخطأ مباشرة من معالج المسار ودع Node.js يتعامل معه على مستوى العملية.

---

أرجع كائن الخطأ كجسم استجابة JSON مع رمز حالة 500.

---

استخدم `process.on('uncaughtException')` لالتقاط جميع أخطاء غير المتزامنة غير المعالجة عالميًا.

#### --answer--

لفّ المنطق غير المتزامن في `try/catch` وادعُ `next(err)` لإرسال الخطأ إلى معالج الأخطاء.

### --question--

#### --text--

ما الغرض من دالة التغليف `asyncHandler` في Express 4؟

#### --distractors--

تحول معالجات المسارات المتزامنة إلى غير متزامنة تلقائيًا.

---

تعيد تشغيل الخادم تلقائيًا وتمسح الحالة عند حدوث خطأ.

---

تحل محل برمجيات وسيطة معالجة الأخطاء الموضوعة في نهاية المكدس.

#### --answer--

تجنب تكرار `try/catch` في كل مسار عن طريق إرسال الوعود المرفوضة إلى `next`.

### --question--

#### --text--

كيف يتعامل Express 5 مع أخطاء غير المتزامنة بشكل مختلف عن Express 4؟

#### --distractors--

يتطلب Express 5 إضافة مستمع عالمي `process.on('unhandledRejection')` لالتقاط أخطاء غير المتزامنة.

---

يتجاهل Express 5 أخطاء غير المتزامنة بصمت ويسجلها فقط في وحدة التحكم دون إيقاف التطبيق.

---

يتطلب Express 5 من كل مسار غير متزامن تعريف دالة برمجيات وسيطة مخصصة لمعالجة الأخطاء.

#### --answer--

يرسل Express 5 أخطاء غير المتزامنة والوعود المرفوضة تلقائيًا إلى معالج الأخطاء دون الحاجة إلى `next(err)`.

### --question--

#### --text--

أين يجب وضع برمجيات وسيطة شاملة `404` في تطبيق Express؟

#### --distractors--

قبل جميع المسارات الأخرى، في أعلى الملف.

---

داخل برمجيات وسيطة معالجة الأخطاء، كفحص بديل.

---

في النهاية تمامًا، بعد برمجيات وسيطة معالجة الأخطاء.

#### --answer--

بعد جميع المسارات ولكن قبل برمجيات وسيطة معالجة الأخطاء.

### --question--

#### --text--

أي مما يلي يعد أفضل الممارسات لمعالجة الأخطاء في تطبيقات Express الإنتاجية؟

#### --distractors--

أرسل دائمًا تتبع المكدس الكامل إلى العميل لتسهيل التصحيح.

---

ضع برمجيات وسيطة معالجة الأخطاء قبل مساراتك لتسريع المعالجة.

---

عالج جميع الأخطاء، بما في ذلك 404، داخل دالة برمجيات وسيطة واحدة.

#### --answer--

سجل تفاصيل الخطأ داخليًا وأرجع رسالة سهلة الفهم بدلاً من إرسال تتبع المكدس للمستخدمين.

### --question--

#### --text--

كيف تفعل جميع رسائل تصحيح الأخطاء المدمجة في Express عند بدء التطبيق؟

#### --distractors--

مرر `{ debug: true }` إلى دالة البناء `express()`.

---

ادعُ `app.enable('debug')` قبل تعريف المسارات.

---

أضف `require('express-debug')` في أعلى ملف الدخول.

#### --answer--

اضبط متغير البيئة `DEBUG` إلى `express:*` عند تشغيل التطبيق.

### --question--

#### --text--

ماذا يفعل الأمر `DEBUG=express:router node index.js`؟

#### --distractors--

يفعل كل مخرجات تصحيح الأخطاء في Express، بما في ذلك البرمجيات الوسيطة، التوجيه، وأحداث التطبيق.

---

يعطل كل مخرجات التصحيح تمامًا ويشغل التطبيق في وضع الإنتاج الصامت.

---

يسجل فقط تفاصيل طلبات HTTP الواردة مثل الطريقة وعنوان URL في الطرفية.

#### --answer--

يعرض فقط مخرجات تصحيح الأخطاء المتعلقة بالموجّه، مع تصفية أسماء النطاقات الأخرى في Express.

### --question--

#### --text--

ما استخدام `morgan` في تطبيق Express؟

#### --distractors--

التقاط وإرسال أخطاء غير المتزامنة تلقائيًا في معالجات مسارات Express إلى `next`.

---

خدمة الملفات الثابتة من دليل محدد بدون تعريف مسارات فردية.

---

ربط Express بقاعدة بيانات علائقية وتسجيل كل الاستعلامات المنفذة.

#### --answer--

تسجيل طلبات HTTP مع تفاصيل مثل الطريقة، عنوان URL، رمز الحالة، ووقت الاستجابة.

### --question--

#### --text--

ما الغرض من مسار فحص الصحة في تطبيق Express؟

#### --distractors--

إعادة تشغيل الخادم تلقائيًا وإعادة توجيه المرور عند ارتفاع حجم الطلبات الواردة.

---

تسجيل وإرسال كل طلبات HTTP الواردة إلى خدمة مراقبة أو تحليلات خارجية.

---

فحص وحظر الطلبات من عناوين IP غير المدرجة في قائمة السماح المعتمدة.

#### --answer--

الإبلاغ عما إذا كان التطبيق يعمل بشكل صحيح حتى يقرر موزعو الحمل والمنسقون ما إذا كانت الخدمة صحية.

### --question--

#### --text--

ماذا يخبر إشارة `SIGTERM` عملية Node.js أن تفعل؟

#### --distractors--

إعادة التشغيل فورًا، مسح كل الحالة في الذاكرة، وإعادة تهيئة كل الاتصالات النشطة.

---

تفريغ لقطة كاملة للذاكرة على القرص للتحليل والتصحيح لاحقًا.

---

قتل فوري لكل اتصالات قاعدة البيانات النشطة وإغلاق كل ملفات الوصف المفتوحة على الفور.

#### --answer--

بدء الإغلاق، مع إعطاء التطبيق فرصة لإنهاء العمل الحالي قبل الخروج.

### --question--

#### --text--

ماذا يفعل استدعاء `server.close()` أثناء إيقاف التشغيل السلس؟

#### --distractors--

ينهي فورًا كل الاتصالات النشطة ويخرج من العملية.

---

يرسل استجابة 500 لأي عملاء متصلين حاليًا.

---

يحذف كل بيانات الجلسة في الذاكرة ويعيد تشغيل الخادم.

#### --answer--

يتوقف عن قبول الاتصالات الجديدة مع السماح للاتصالات الحالية بالانتهاء.

### --question--

#### --text--

لماذا تعيد رمز حالة 503 من مسار فحص الصحة أثناء إيقاف التشغيل السلس؟

#### --distractors--

لتسجيل حدث الإيقاف والوقت في لوحة مراقبة خارجية لأغراض التدقيق.

---

لإجبار المنسق على تشغيل نسخة بديلة جديدة فورًا قبل اكتمال الإيقاف.

---

لإعلام جميع العملاء المتصلين حاليًا بأن جلساتهم النشطة تم حفظها بنجاح.

#### --answer--

لإشارة إلى موزع الحمل أن التطبيق غير متاح فيتوقف عن توجيه حركة المرور الجديدة إليه.
