---
id: 6895d06b5968736797c408e4
title: 動態陣列與靜態陣列有何不同？
challengeType: 19
dashedName: how-do-dynamic-arrays-differ-from-static-arrays
---

# --description--

陣列是計算機科學中一種基本的資料結構。所有陣列都儲存有序的資料群集，但根據它們的型別，背後的運作方式可能不同。

它們的底層行為可能對程式的效率產生重要影響，因此讓我們來了解動態陣列和靜態陣列及其差異，這樣你就能為你的程式選擇最高效的陣列。

我們將從靜態陣列開始。

**靜態陣列** 有固定大小。它們將元素儲存在相鄰的記憶體位置。

靜態陣列的大小是在陣列初始化時決定的。一旦該特定的記憶體區段被配置，它就是固定的，且在程式執行期間無法更改。這是靜態陣列的一個關鍵特性。

將元素儲存在相鄰的記憶體位置，使資料擷取處理程序更高效，因為程式可以儲存第一個元素的位置，然後使用索引進行簡單計算以找到記憶體中的其他元素。

多虧了這個，存取靜態陣列的值需要恆定時間 `O(1)`，這非常高效。

當你事先知道將要儲存的元素數量時，可以使用靜態陣列。當值將被非常頻繁地存取時，這也很有幫助，因為存取操作非常高效。

然而，這個資料結構無法擴展或縮減，所以如果將要儲存的元素數量會變動，你應該改用動態陣列。

嘗試增加靜態陣列的大小會涉及建立一個新的陣列，並將所有元素從舊陣列複製到新的陣列，這樣做效率不高。在這種情況下，動態陣列會更好，因為它會自動控制代碼這個處理程序。

Python 並未將傳統靜態陣列作為內建資料結構。

但其他程式設計語言，如 Java，確實支援它們。這是一個在 Java 中可以儲存三個整數的靜態陣列範例：

```java
int[] numbers = new int[3]; 
```

Python 中的陣列是動態的，所以讓我們來看看那些。

**動態陣列** 更具彈性，因為它們可以在程式執行時自動增長或縮小。

它們透過一個自動調整大小的機制運作，當原始陣列已滿時，會將元素複製到新的陣列。這個處理程序是高效完成的，因為新陣列的大小是以一種高效的方式選擇，使這些計算上昂貴的操作行為較不頻繁。

存取動態陣列的元素需要恆定時間 `O(1)`，所以這個操作非常高效。

在陣列中間插入元素需要線性時間 `O(n)`，因為其後的元素需要重新定位。

如果動態陣列中仍有空間，將元素插入陣列末端需要恆定時間 `O(1)`，但如果陣列已滿且需要調整大小，這個操作行為的時間複雜度為 `O(n)`。

當你事先不知道需要在陣列中儲存多少個值時，你應該使用動態陣列。當你會頻繁插入和刪除元素時，它們也很有幫助。

Python 內建的 `list` 資料結構作為動態陣列運作。你可以透過在方括號內寫入元素並以逗號分隔來創建列表。

```python
numbers = [3, 4, 5, 6]
```

你可以透過寫出儲存該列表的變數名稱，接著加上中括號，並在中括號內填入對應的索引來存取元素。

索引從第一個元素的 0 開始，並且每個後續元素的索引遞增 1：

```python
numbers[0]  # 3
numbers[1]  # 4
numbers[2]  # 5
numbers[3]  # 6
```

要更新一個值，你只需要重新指定它：

```python
numbers[2] = 16
```

你可以使用 `.append()` 方法將元素附加到列表中：

```python
numbers.append(7)
```

你可以使用 `.insert()` 方法在特定索引插入元素，將索引作為第一個引數，元素本身作為第二個引數傳入。

```python
numbers.insert(3, 15)
```

你可以使用 `.pop()` 方法在特定索引移除元素：

```python
numbers.pop(2)
```

如果你沒有指定索引，`.pop()` 將會移除最後一個元素。

還有其他內建的列表方法，你可以在文件中查看，輕鬆地新增和移除元素。

這就是動態陣列，或在此情況下的列表的威力。

一般來說，當你事先知道元素數量且需要頻繁存取時，應該使用靜態陣列；當元素數量未知或隨時間變動時，則應使用動態陣列。

你應該始終考慮靜態陣列的簡單性與動態陣列的彈性之間的權衡。它們兩者對特定的使用案例和情境都很有幫助。能夠為特定問題選擇最適合的那一種，是你隨著練習逐漸培養的解決問題技能的一部分。

# --questions--

## --text--

靜態陣列和動態陣列在大小上的主要差異是什麼？

## --answers--

靜態陣列在創建後可以改變其大小，而動態陣列則不行。

### --feedback--

思考每種陣列型別將需要多少空間。

---

靜態陣列具有固定大小，而動態陣列可以在執行期改變大小。

---

它們的大小如何被控制代碼在實務上沒有差別。

### --feedback--

思考每種陣列型別將需要多少空間。

---

動態陣列總是比靜態陣列大。

### --feedback--

思考每種陣列型別將需要多少空間。

## --video-solution--

2

## --text--

如果你需要為已滿的靜態陣列添加更多元素，通常涉及的處理程序是什麼？

## --answers--

靜態陣列會自動擴展其記憶體以容納新的元素。

### --feedback--

思考當一個具有固定容量的容器空間用盡時所需的事項。

---

你必須創建一個新的、更大的陣列，並將所有現有元素複製到其中。

---

該陣列會自動轉換成動態陣列。

### --feedback--

思考當一個具有固定容量的容器空間用盡時所需的事項。

---

如果陣列已滿，新的元素將會被直接丟棄。

### --feedback--

思考當一個具有固定容量的容器空間用盡時所需的事項。

## --video-solution--

2

## --text--

在什麼情況下，靜態陣列通常比動態陣列更適合？

## --answers--

當元素的確切數量未知且經常變動時。

### --feedback--

思考靜態陣列在大小和資源使用方面的主要優勢。

---

當你需要儲存可能無限增長的非常大型資料集時。

### --feedback--

思考靜態陣列在大小和資源使用方面的主要優勢。

---

當你需要在群集內任意位置頻繁插入和刪除時。

### --feedback--

思考靜態陣列在大小和資源使用方面的主要優勢。

---

當資料大小在撰寫程式時是固定且已知的。

## --video-solution--

4

