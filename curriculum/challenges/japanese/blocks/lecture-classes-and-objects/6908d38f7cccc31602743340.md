---
id: 6908d38f7cccc31602743340
title: オブジェクトの属性を動的に扱う方法
challengeType: 19
dashedName: how-to-handle-object-attributes-dynamically
---

# --description--

前のレッスンでは、属性がオブジェクトに属する変数であることを学びました。つまり、属性はオブジェクトの状態や振る舞いを表すデータを保持しています。

例えば、車には通常ブランドとモデルがあります。ブランドとモデルは `Car` クラスの属性になるでしょう。

```python
class Car: 
    def __init__(self, brand, model): 
        self.brand = brand 
        self.model = model 

my_car = Car('Lamborghini', 'Gallardo') 
print(my_car.brand) # Lamborghini 
print(my_car.model) # Gallardo 
```

しかし、プログラムの実行中になるまでどの属性が必要かわからない場合もあります。ユーザーや設定ファイルから属性名を受け取るスクリプトを書いていると想像してください。これらは事前にハードコードできる属性ではありません。

そこで属性を動的に扱う方法が役立ちます。こうすることで、属性名を変数として使い、コード内で固定された名前ではなく、属性にアクセス、変更、確認、さらには削除までできます。これにより、プログラムは異なるデータやユーザー入力に柔軟に対応できるようになります。

Python にはオブジェクトの属性を動的に扱うための便利な組み込み関数が4つあります。`getattr()`、`setattr()`、`hasattr()`、そして `delattr()` です。

これらは変数名を使って属性にアクセス、作成、確認、削除を可能にします。それぞれの使い方を見ていきましょう。

`getattr()` は、実行時まで属性名がわからない場合にオブジェクトから属性を読み取ることを可能にします。属性が存在しない場合は `AttributeError` を発生させますが、デフォルト値を指定すればそれを返します。

使い方は、オブジェクト、属性名、そして省略可能なデフォルト値を渡します。

```python
getattr(object, attribute_name, default_value) 
```

例を見てみましょう。

```python
class Person: 
    def __init__(self, name, age): 
        self.name = name 
        self.age = age 

person = Person('John Doe', 30) 
 
print(getattr(person, 'name')) # John Doe 
print(getattr(person, 'age')) # 30 
print(getattr(person, 'city', 'Milano')) # Milano
```

上の例では、`Milano` はデフォルト値です。なぜなら `city` は `Person` クラスに存在しないからです。

先ほど述べたように、`getattr()` の真の力は属性名がユーザー入力やファイルなどの変数から来る場合に発揮されます。

その場合、属性名が固定されていないため、通常の `object.attribute_name` 構文は使えません。

```python
class Person: 
    def __init__(self, name, age): 
        self.name = name 
        self.age = age 

person = Person('John Doe', 30)

attr_name = input('Enter the attribute you want to see: ')
print(getattr(person, attr_name, 'Attribute not found'))
```

この例では、ユーザーが `name` と入力すると `John Doe` が表示され、`age` と入力すると `30` が表示されます。クラスに存在しない `email` と入力すると `Attribute not found` が表示されます。

まさにここで動的な属性操作が活躍します。未知の入力やデータにコードが対応できるようにするのです。

さらに、既知の属性だけでなく、オブジェクトが持つすべての属性を調べたい場合もあります。組み込みの `dir()` 関数を使うとそれが可能です。オブジェクト上のすべての属性名のリストを返します。使い方は以下の通りです。

```python
class Person: 
    def __init__(self, name, age): 
        self.name = name 
        self.age = age 

person = Person('John Doe', 30)

# Loop through all attributes of the person object with dir() function
for attr in dir(person):
    # Ignore dunder methods like __init__ or __str__ and regular methods
    if not attr.startswith('__') and not callable(getattr(person, attr)): 
        value = getattr(person, attr)
        print(f'{attr}: {value}')

# Output
# age: 30
# name: John Doe
```

`setattr()` 関数は、新しい属性を作成したり既存の属性を動的に更新したりできます。構文は次のようになります。

```python
setattr(object, attribute_name, value) 
```

以下は、設定ファイルや環境変数ファイルのデータに基づいて設定属性をセットする例です。

```python
class Configuration:
    pass

# Data loaded at runtime (like from a config or env file)
settings_data = {
    'server_url': 'https://api.example.com',
    'timeout_sec': 30,
    'max_retries': 5
}

config_obj = Configuration()

# Dynamically set attributes using dictionary keys and values
for attr_name, attr_value in settings_data.items():
    setattr(config_obj, attr_name, attr_value)

print(config_obj.server_url) # https://api.example.com
print(config_obj.timeout_sec) # 30
```

また `hasattr()` もあります。属性を操作したり削除したりする前に、その属性が存在するか確認するのは良い習慣です。`hasattr()` は属性の存在をチェックし、結果に応じて `True` または `False` を返します。

基本的な構文は以下の通りです。

```python
hasattr(object, attribute_name)  
```

以下は `Product` クラスのインスタンスで属性の存在を動的にチェックする例です。

```python
class Product:
    def __init__(self, name, price):
        self.name = name
        self.price = price

product_a = Product('T-Shirt', 25)

required_attributes = ['name', 'price', 'inventory_id']

for attr in required_attributes:
    if not hasattr(product_a, attr):
        print(f"ERROR: Product is missing the required attribute: '{attr}'")
    else:
        # Access the attributes dynamically once their existence is confirmed
        print(f'{attr}: {getattr(product_a, attr)}')

# Output:
# name: T-Shirt
# price: 25
# ERROR: Product is missing the required attribute: 'inventory_id'
```

ERROR 出力は `inventory_id` が `Product` クラスおよびそのインスタンスに存在しないために発生しました。

最後に、`delattr()` は属性を動的に削除できます。

```python
delattr(object, attribute_name) 
```

例えば、オブジェクトの処理が完了した後、保存前に存在するかもしれない機密または一時的な属性を削除してクリーンアップしたい場合があります。その後、残った属性を `dir()` でループ処理できます。

```python
class UserSession:
    def __init__(self, user_id, token):
        self.user_id = user_id
        self.auth_token = token # sensitive
        self.temp_counter = 0 # temporary

session = UserSession(101, 'a1b2c3d4e5')

# List of attributes to remove dynamically before "saving" the session
attributes_to_clean = ['auth_token', 'temp_counter']

# Dynamically remove specified attributes
for attr in attributes_to_clean:
    if hasattr(session, attr):
        delattr(session, attr)
        print(f'Removed attribute: {attr}')

print('\nFinal attributes remaining:')

# Loop through the remaining attributes with dir()
for attr in dir(session):
    # Ignore dunder methods like __init__ or __str__ and regular methods
    if not attr.startswith('__') and not callable(getattr(session, attr)):
        print(f' - {attr}: {getattr(session, attr)}')

# Output:
# Removed attribute: auth_token
# Removed attribute: temp_counter

# Final attributes remaining:
#  - user_id: 101
```

これが属性を動的に扱う方法です！

# --questions--

## --text--

Python の `getattr()` 関数は何を可能にしますか？

## --answers--

実行時まで名前がわからない属性をオブジェクトから読み取る。

---

オブジェクトから属性を削除する。

### --feedback--

動的に属性を*取得*するのに役立つ関数はどれか考えてみてください。

---

オブジェクトに新しい属性を設定する。

### --feedback--

動的に属性を*取得*するのに役立つ関数はどれか考えてみてください。

---

オブジェクトに属性が存在するか確認する。

### --feedback--

動的に属性を*取得*するのに役立つ関数はどれか考えてみてください。

## --video-solution--

1

## --text--

なぜ Python でオブジェクトの属性を動的に扱いたいのでしょうか？

## --answers--

属性を自動的にリストに変換するため。

### --feedback--

ユーザー入力や外部データに動的に対応することを考えてみてください。

---

実行時に属性を読み取り専用にするため。

### --feedback--

ユーザー入力や外部データに動的に対応することを考えてみてください。

---

作成されるクラス属性の数を減らすため。

### --feedback--

ユーザー入力や外部データに動的に対応することを考えてみてください。

---

実行時まで名前がわからない属性を扱うため。

## --video-solution--

4

## --text--

Python でオブジェクトに特定の属性があるかどうかを確認する正しい構文はどれですか？

## --answers--

`checkattr(object, attribute_name)`

### --feedback--

「属性があるか」を文字通り示す関数名を考えてみてください。

---

`hasattr(object, attribute_name)`

---

`setattr(object, attribute_name)`

### --feedback--

「属性があるか」を文字通り示す関数名を考えてみてください。

---

`getattr(object, attribute_name)`

### --feedback--

「属性があるか」を文字通り示す関数名を考えてみてください。

## --video-solution--

2
