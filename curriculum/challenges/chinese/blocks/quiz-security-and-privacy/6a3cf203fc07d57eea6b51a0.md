---
id: 6a3cf203fc07d57eea6b51a0
title: 安全性和隐私测验
challengeType: 8
dashedName: quiz-security-and-privacy
---

# --description--

要通过测验，你必须正确回答下面 20 个问题中的至少 18 个。

# --quizzes--

## --quiz--

### --question--

#### --text--

HTTPS 使用什么加密协议来加密浏览器和服务器之间的通信通道？

#### --distractors--

SSL（安全套接字层）

---

HTTP/1

---

HTTP/2

#### --answer--

TLS（传输层安全）

### --question--

#### --text--

数据最小化和目的限制如何协同工作以保护用户隐私？

#### --distractors--

数据最小化限制数据的使用方式，而目的限制限制收集的数据量。

---

只要数据被加密，这两个原则都允许无限制的数据收集。

---

这两个原则都要求公司与广告网络共享数据。

#### --answer--

数据最小化限制收集的数据量，而目的限制限制这些数据的使用方式。

### --question--

#### --text--

当在 cookie 上设置 `HttpOnly` 标记时，它有什么作用？

#### --distractors--

确保 cookie 仅通过 HTTPS 连接发送。

---

它阻止浏览器在跨站请求时发送 cookie。

---

它在将 cookie 值存储到浏览器之前对其进行加密。

#### --answer--

它阻止 JavaScript 读取 cookie。

### --question--

#### --text--

COPPA 对收集 13 岁以下儿童数据的网站施加了什么具体要求？

#### --distractors--

在主页上显示“禁止出售我的个人信息”链接。

---

允许用户一键选择退出数据共享。

---

在账户关闭后 30 天内删除所有用户数据。

#### --answer--

在收集个人信息之前获得可验证的父母同意。

### --question--

#### --text--

以下哪项正确描述了安全性和隐私之间的关系？

#### --distractors--

隐私可以存在于没有安全性的情况下，但安全性不能没有隐私而存在。

---

安全性和隐私是相同的概念，目标一致。

---

安全性和隐私都不能没有对方而存在。

#### --answer--

安全性可以没有隐私而存在，但隐私不能没有安全性而存在。

### --question--

#### --text--

内容安全策略主要防御哪种类型的攻击？

#### --distractors--

分布式拒绝服务（DDoS）攻击。

---

凭证填充攻击。

---

中间人（MITM）攻击。

#### --answer--

跨站脚本（XSS）攻击。

### --question--

#### --text--

当应用程序严重依赖外部库、第三方 API 和微服务时，会引入什么供应链风险？

#### --distractors--

由于过多的网络调用，应用变慢。

---

外部工具自动更新并引入破坏性更改。

---

代码库随着时间变得更难阅读和维护。

#### --answer--

来自外部来源的漏洞可能传播到主应用中。

### --question--

#### --text--

两个 URL 要共享相同源，必须完全匹配哪三部分？

#### --distractors--

用户名、密码和域名。

---

IP 地址、端口号和文件路径。

---

浏览器版本、操作系统和域名。

#### --answer--

协议、域名和端口号。

### --question--

#### --text--

为什么开发者应该将 `.env` 添加到 `.gitignore`？

#### --distractors--

通过跳过配置文件来加快构建过程。

---

无需使用 dotenv 包即可自动加载环境变量。

---

与团队中的其他开发者共享配置设置。

#### --answer--

防止敏感凭据被提交到版本控制。

### --question--

#### --text--

针对凭证填充攻击，最有效的缓解策略是什么？

#### --distractors--

要求用户每 30 天更改密码。

---

阻止所有来自国外 IP 地址的登录尝试。

---

将密码存储在单独的加密数据库中。

#### --answer--

强制执行多因素认证。

### --question--

#### --text--

HTTPS 所需数字证书的主要目的是什么？

#### --distractors--

加快网页加载时间。

---

加密服务器上保存的所有文件。

---

安全地存储用户的登录会话。

#### --answer--

证明网站所有者的真实身份。

### --question--

#### --text--

GDPR 和 CCPA 在处理用户数据跟踪同意方面的关键区别是什么？

#### --distractors--

GDPR 侧重于选择退出权利，而 CCPA 要求严格的选择加入同意。

---

两部法律都要求在任何数据收集之前获得严格的选择加入同意。

---

两部法律都允许公司使用预先勾选的同意框。

#### --answer--

GDPR 要求严格的选择加入同意，而 CCPA 侧重于选择退出权利。

### --question--

#### --text--

以下哪项是 PII 下的间接标识符示例？

#### --distractors--

一个人的完整法定姓名。

---

国家身份证号码。

---

家庭街道地址。

#### --answer--

设备序列号或 IP 地址。

### --question--

#### --text--

第一方 cookie 和第三方 cookie 的关键区别是什么？

#### --distractors--

第一方 cookie 是加密的；第三方 cookie 不是。

---

第一方 cookie 存储在服务器上；第三方 cookie 存储在浏览器中。

---

第一方 cookie 需要同意；第三方 cookie 不需要任何同意。

#### --answer--

第一方 cookie 跟踪你访问的网站；第三方 cookie 跟踪你跨不同网站的行为。

### --question--

#### --text--

与数据安全相比，数据隐私的主要关注点是什么？

#### --distractors--

构建防火墙和加密等技术防御。

---

保护系统免受黑客的未经授权访问。

---

监控网络流量中的恶意活动。

#### --answer--

控制组织如何收集、保存和使用个人数据。

### --question--

#### --text--

哪种 Permissions-Policy 语法会完全禁用页面上所有用户的浏览器功能？

#### --distractors--

`feature=(none)`

---

`feature=(self)`

---

`feature=(*)`

#### --answer--

`feature=()`，空列表。

### --question--

#### --text--

`dotenv` 库如何使 `.env` 变量在 Node.js 应用中可访问？

#### --distractors--

它将变量存储在浏览器本地存储中以供客户端访问。

---

它创建一个包含所有值的新全局 `config` 对象。

---

它加密敏感数据并存储在远程数据库中。

#### --answer--

它读取 `.env` 文件并将密钥注入 `process.env`。

### --question--

#### --text--

服务器发送什么以允许浏览器读取跨源响应？

#### --distractors--

带有请求域的 `Set-Cookie` 头部。

---

`Content-Security-Policy` 头部。

---

`Strict-Transport-Security` 头部。

#### --answer--

`Access-Control-Allow-Origin` 头部。

### --question--

#### --text--

使用标准出厂设置部署服务器会导致什么类型的安全漏洞？

#### --distractors--

系统会自动打上最新的安全补丁。

---

出厂设置默认强制执行强加密。

---

默认配置开箱即用要求多因素认证。

#### --answer--

默认配置通常包含空白管理员密码和开放端口。

### --question--

#### --text--

公司应用什么安全原则来减少内部威胁造成的损害？

#### --distractors--

授予所有员工完全系统访问权限以实现透明。

---

监控员工社交媒体以发现可疑行为。

---

要求所有员工每年签署保密协议。

#### --answer--

执行最小权限原则，仅限制访问必要的文件。
