---
id: 672990ecf71a852804ababe7
title: Was ist der Unterschied zwischen presentational und semantic HTML?
challengeType: 19
dashedName: what-is-the-difference-between-presentational-and-semantic-html
---

# --interactive--

Presentational HTML konzentriert sich auf das Aussehen und den Stil des Inhalts. In den frühen Tagen von HTML verwendeten Entwickler Elemente wie die `center`, `big` und `font` Elemente. In der modernen Webentwicklung sollten Sie diese Arten von Elementen jedoch nicht verwenden, wegen ihrer Einschränkungen und negativen Auswirkungen auf Barrierefreiheit und Wartbarkeit.

Viele presentational HTML-Elemente sind veraltet, was bedeutet, dass sie nicht mehr empfohlen werden. Es gibt bessere Möglichkeiten, dieselben Ergebnisse zu erzielen. Es ist jedoch hilfreich zu wissen, dass sie existieren, deshalb schauen wir uns einige Beispiele an.

Das `font` Element ist ein veraltetes Element, das verwendet wurde, um die Schriftgröße und Farbe des Texts festzulegen. Hier ist ein Beispiel für ein `font` Element.

Aktivieren Sie den interaktiven Editor und ändern Sie die Größe von 5 auf 7, um die Schriftgröße zu erhöhen.

:::interactive_editor

```html
<font size="5" color="blue">This text is blue and large.</font>
```

:::

Dieses Beispiel setzt die Farbe des Texts auf `blue` und die Größe auf den Wert `5`. Der Bereich für das `size`-Attribut reicht von `1` bis `7`, wobei `1` die kleinste und `7` die größte Größe ist. Der Standardwert ist `3`, wenn Sie keinen Wert explizit festlegen.

Obwohl dieses Element noch funktioniert, sollten Sie es nicht verwenden, da die Schriftgröße und Farbe immer mit CSS und nicht mit HTML festgelegt werden sollten.

Das `center` Element ist ein weiteres veraltetes Element, das verwendet wurde, um den Inhalt horizontal innerhalb seines Containers zu zentrieren. Hier ist ein Beispiel für das `center` Element, das Text und ein Absatz-Element enthält.

Aktivieren Sie den interaktiven Editor und fügen Sie `center`-Tags um das `<p>Another example text.</p>` hinzu, um zu sehen, wie es auf der Seite zentriert wird.

:::interactive_editor

```html
<center>
  This text is centered.
  <p>HTML is awesome.</p>
</center>

<p>Another example text.</p>
```

:::

Als Nächstes haben wir das `big` Element. Dies ist ein weiteres veraltetes presentational HTML-Element, das den eingeschlossenen Text eine Stufe größer macht als den umgebenden Text. Hier haben wir ein Beispiel, das einen Absatz mit zwei Teilen definiert.

Aktivieren Sie den interaktiven Editor und fügen Sie `big`-Tags um das `Some other text` hinzu und sehen Sie die Änderungen im Vorschaufenster.

:::interactive_editor

```html
<p>
  This text has a normal font size.
  <big>This text is larger.</big>
  Some other text.
</p>
```

:::

Denken Sie daran, dass die Schriftgröße immer mit CSS festgelegt werden sollte, daher sollten Sie dieses Element in modernem HTML nicht verwenden.

Dies waren Beispiele für presentational HTML-Elemente. Aber alle sind veraltet und werden nicht mehr empfohlen. Was sollten Sie stattdessen verwenden? Schauen wir es uns an.

Semantic HTML ist jetzt die empfohlene Praxis. Es beschreibt den Inhalt der Elemente, sodass es viel einfacher ist, sie zu lesen, zu verstehen und zu warten.

Suchmaschinen können Ihre Webseite leicht verstehen, wenn Sie semantic HTML verwenden. Es ist auch hilfreich für Barrierefreiheit, da Screenreader semantische Informationen benötigen, um zu beschreiben, was sich auf der Webseite befindet.

Beispiele für semantic HTML-Elemente sind:

- Das `header` Element zur Definition der Kopfzeile des Dokuments oder Abschnitts.
- Das Navigations-Abschnitts-Element `nav` für Abschnitte mit Navigationslinks.
- Das `section` Element zum Gruppieren verwandter Informationen.
- Das `figure` Element für Illustrationen und Diagramme.

Dies ist ein Beispiel für ein `header` Element, das ein Navigations-Abschnitts-Element enthält.

Aktivieren Sie den interaktiven Editor und fügen Sie ein `<a href="#">Products</a>` innerhalb des `nav` hinzu und sehen Sie die Änderungen im Vorschaufenster.

:::interactive_editor

```html
<header>
  <nav>
    <a href="#">Home</a>
    <a href="#">About</a>
    <!--Add the products link here-->

    <a href="#">Contact</a>
  </nav>
</header>
```

:::

Die semantischen Elemente zeigen klar ihren Zweck innerhalb der HTML-Struktur. Es gibt viele verschiedene semantic HTML-Elemente. Sie werden definitiv eines finden, das den Anforderungen Ihres Projekts entspricht.

Großartige Arbeit. Jetzt wissen Sie den Unterschied zwischen presentational und semantic HTML: semantic HTML beschreibt den Inhalt, während presentational HTML sich auf das Aussehen konzentriert.

# --questions--

## --text--

Welche der folgenden Aussagen beschreibt am besten den Unterschied zwischen presentational und semantic HTML?

## --answers--

Presentational HTML konzentriert sich auf die Inhaltsstruktur, während semantic HTML sich auf das Aussehen konzentriert.

### --feedback--

Denken Sie darüber nach, was „Präsentation“ und „Semantik“ bedeuten und wie sie sich auf HTML-Elemente beziehen.

---

Semantic HTML konzentriert sich auf die Inhaltsstruktur, während presentational HTML sich auf das Aussehen konzentriert.

---

Es gibt keinen Unterschied zwischen presentational und semantic HTML.

### --feedback--

Denken Sie darüber nach, was „Präsentation“ und „Semantik“ bedeuten und wie sie sich auf HTML-Elemente beziehen.

---

Beide konzentrieren sich auf den Stil, aber presentational HTML ist moderner.

### --feedback--

Denken Sie darüber nach, was „Präsentation“ und „Semantik“ bedeuten und wie sie sich auf HTML-Elemente beziehen.

## --video-solution--

2

## --text--

Welches der folgenden ist ein Beispiel für presentational HTML?

## --answers--

Die Verwendung des `header` Elements zur Definition einer Kopfzeile.

### --feedback--

Denken Sie darüber nach, welches Element direkt steuert, wie der Inhalt aussieht.

---

Die Verwendung des `nav` Elements zur Definition eines Navigationsabschnitts.

### --feedback--

Denken Sie darüber nach, welches Element direkt steuert, wie der Inhalt aussieht.

---

Die Verwendung des `article` Elements zur Definition von unabhängigem Inhalt.

### --feedback--

Denken Sie darüber nach, welches Element direkt steuert, wie der Inhalt aussieht.

---

Die Verwendung des `center` Elements, um den Inhalt zu zentrieren.

## --video-solution--

4

## --text--

Welches der folgenden ist ein Beispiel für semantic HTML?

## --answers--

Die Verwendung von `<font color="red">`, um Text rot zu machen.

### --feedback--

Denken Sie darüber nach, welches Element die Bedeutung des Inhalts beschreibt.

---

Die Verwendung des `center` Elements, um den Inhalt zu zentrieren.

### --feedback--

Denken Sie darüber nach, welches Element die Bedeutung des Inhalts beschreibt.

---

Verwenden des `nav`-Elements, um einen Navigationsabschnitt darzustellen.

---

Die Verwendung des `big` Elements, um Text größer zu machen.

### --feedback--

Denken Sie darüber nach, welches Element die Bedeutung des Inhalts beschreibt.

## --video-solution--

3
