---
id: 697dc755c709772fad37de9d
title: 深さ優先探索と幅優先探索はどのように動作するのか？
challengeType: 19
dashedName: how-do-depth-first-and-breadth-first-search-work
---

# --description--

データ構造やアルゴリズムを扱い始めると、よく行う必要がある操作の一つが各ノードを訪問することだと気づくでしょう。

このプロセスはデータ構造の「走査」と呼ばれます。

走査は、データ構造内のすべてのノードに対して何かを行うために使われます。例えば、値を出力したり、特定の値を見つけたり、ノードに対して特定の操作を実行したりします。

各ノードを体系的に訪問することで、どのノードも見逃さないようにします。

しかし、どの順番でデータ構造を走査すればよいのでしょうか？どこから始めて、次のノードはどう選べばよいのでしょうか？

走査の明確な方法がなければ、特定の道筋を持たずに迷路を歩くようなものです。

そこで、幅優先探索（BFS）や深さ優先探索（DFS）のようなアルゴリズムが非常に重要になります。これらはグラフを走査したり、2つのノード間の経路を見つけたりするのに一般的に使われます。

データ構造を走査する際には、これらのアルゴリズムがノードを訪問する順序を定め、どのノードも見逃さないようにします。

まずは幅優先探索（BFS）から始めましょう。

## 幅優先探索（BFS）

**幅優先探索（BFS）** は、グラフの隣接するすべてのノードを訪問してから次のレベルに進むアルゴリズムです。

重みのないグラフで2つのノード間の最短経路を見つけるのに使えます。なぜなら、各レベルのすべてのノードを分析するため、最も少ないエッジ数の経路を最初に見つけるからです。

このアルゴリズムは、訪問したノードを追跡するためにキューというデータ構造を使って実装されることが多いです。キューはFIFO（先入れ先出し）方式で、最初に追加されたノードが最初に取り出されます。

アルゴリズムの動作は以下の通りです：

* 特定のノードから開始します。
    
* そのノードを訪問済みとしてマークし、キューに追加します。
    
* キューが空でない間、現在のノードをキューから取り出します（デキュー）。次に、そのノードの隣接ノードのうち未訪問のものを訪問済みとしてマークし、キューに追加します。
    

重要な点は、幅優先探索（BFS）はキューをメモリに保持する必要があり、このキューに多くのノードが入る場合があるため、空間的な要求が大きくなることがあることです。特に同じレベルに多くのノードがあるグラフでは顕著です。

ここで、木と呼ばれる特定の種類のグラフに幅優先探索（BFS）を適用した例を見てみましょう。

木については今後のレッスンで詳しく学びますが、基本的にはサイクルのないグラフで、ノードが階層的に整理されています。サイクルとは、同じノードで始まり終わる経路のことです。

この木に幅優先探索（BFS）を適用してみましょう：

<img src="https://cdn.freecodecamp.org/curriculum/lecture-transcripts/how-do-depth-first-and-breadth-first-search-work-1.png" alt="A tree diagram showing nodes A through G arranged in a hierarchy. Node A is at the root, with children B and C. Node B has children D and E, and node C has children F and G.">

**ステップ 1:**

木のルートであるノードAから始めます。Aをキューに追加し、すぐに訪問済みとしてマークします。

* **キュー:** `[A]`
    
* **訪問済み:** `{A}`
    

**ステップ 2:**

ノードAをキューから取り出します。未訪問の子ノード（ノードB、次にノードC）をキューに追加し、訪問済みとしてマークします。

* **キュー:** `[B, C]`
    
* **訪問済み:** `{A, B, C}`
    

同じレベルのノードがキューに追加される順序は、データ構造の実装やグラフ表現でエッジ（接続）が保存されている順序によって決まります。

実装が一貫していれば、同じレベルのノードの走査順序がアルゴリズムの正確性に影響を与えることはありません。すべてのノードをレベルごとに訪問します。

**ステップ 3:**

ノードBをキューから取り出します。未訪問の子ノード（ノードD、次にノードE）をキューに追加し、訪問済みとしてマークします。

* **キュー:** `[C, D, E]`
    
* **訪問済み:** `{A, B, C, D, E}`
    

**ステップ 4:**

ノードCをキューから取り出します。未訪問の子ノード（ノードF、次にノードG）をキューに追加し、訪問済みとしてマークします。

* **キュー:** `[D, E, F, G]`
    
* **訪問済み:** `{A, B, C, D, E, F, G}`
    

**ステップ 5:**

ノードDをキューから取り出します。このノードには未訪問の子ノードがないため、訪問済みセットは変わりません。

* **キュー:** `[E, F, G]`
    
* **訪問済み:** `{A, B, C, D, E, F, G}`
    

**ステップ 6:**

ノードEをキューから取り出します。このノードには未訪問の子ノードがないため、訪問済みセットは変わりません。

* **キュー:** `[F, G]`
    
* **訪問済み:** `{A, B, C, D, E, F, G}`
    

**ステップ 7:**

ノードFをキューから取り出します。このノードには未訪問の子ノードがないため、訪問済みセットは変わりません。

* **キュー:** `[G]`
    
* **訪問済み:** `{A, B, C, D, E, F, G}`
    

**ステップ 8:**

ノードGをキューから取り出します。このノードには未訪問の子ノードがないため、訪問済みセットは変わりません。

* **キュー:** `[]`
    
* **訪問済み:** `{A, B, C, D, E, F, G}`
    

キューが空になると、走査は完了です。

ノードはこの順序で走査されました：

**A → B → C → D → E → F → G**

<img src="https://cdn.freecodecamp.org/curriculum/lecture-transcripts/how-do-depth-first-and-breadth-first-search-work-2.png" alt="The same tree diagram with arrows showing the BFS traversal order: A to B to C to D to E to F to G, demonstrating level-by-level visitation.">

アルゴリズムがレベルごとにノードを訪問していることに注目してください。

ノードAから始めて、次のレベルのノードBとCを訪問し、その次のレベルのノードD、E、F、Gを訪問します。これが幅優先探索（BFS）の基本原理です。

## 深さ優先探索（DFS）

幅優先探索（BFS）が同じレベルの隣接ノードをすべて訪問するのに対し、**深さ優先探索（DFS）** は各枝をできるだけ深くたどってから戻ります。

このアルゴリズムは、迷路を特定の道筋に沿って進み、行き止まりや出口に達するまで進みます。行き止まりに達したら戻って別の道を選びます。

深さ優先探索（DFS）は、解が一つだけのパズルを解く、グラフのサイクルを検出する、連結成分を見つけるのに使われます。

このアルゴリズムは、再帰やスタックというデータ構造を使って実装されることが多いです。

スタックはLIFO（後入れ先出し）方式で、最後に追加されたノードが最初に取り出されます。

アルゴリズムの動作は以下の通りです：

* 特定のノードから開始します。
    
* そのノードを訪問済みとしてマークし、スタックに追加します。
    
* スタックが空でない間、現在のノードをポップ（取り出し）します。これが「訪問」や処理（例えば値の出力）を行うタイミングです。次に、そのノードの未訪問の隣接ノードをすべて訪問済みとしてマークし、スタックに追加します。
    

このアルゴリズムの制限の一つは、重みのないグラフで2つのノード間の最短経路を必ずしも見つけられるとは限らないことです。

深さ優先探索（DFS）を先ほどの木の例に適用した例を見てみましょう。

<img src="https://cdn.freecodecamp.org/curriculum/lecture-transcripts/how-do-depth-first-and-breadth-first-search-work-3.png" alt="The same tree diagram as before, showing nodes A through G in their hierarchical structure, ready for DFS demonstration.">

**ステップ 1:**

ルートノードAから始めます。訪問済みとしてマークし、スタックに追加します。

* **スタック:** `[A]`
    
* **訪問済み:** `{A}`
    

**ステップ 2:**

ノードAをスタックからポップします。

次に、未訪問の子ノードであるノードBとノードCを逆順（`C`、次に`B`）でスタックに追加します。これにより`B`がスタックのトップ（LIFO）になり、次に処理されます。訪問済みとしてマークもします。

* **スタック:** `[C, B]`
    
* **訪問済み:** `{A, B, C}`
    

**ステップ 3:**

ノードBをスタックからポップします。

次に、未訪問の子ノードであるノードDとノードEを逆順（`E`、次に`D`）でスタックに追加します。訪問済みとしてマークもします。

* **スタック:** `[C, E, D]`
    
* **訪問済み:** `{A, B, C, D, E}`
    

**ステップ 4:**

ノードDをスタックからポップします。このノードにはスタックに追加する子ノードがありません。

* **スタック:** `[C, E]`
    
* **訪問済み:** `{A, B, C, D, E}`
    

**ステップ 5:**

ノードEをスタックからポップします。このノードにはスタックに追加する子ノードがありません。

* **スタック:** `[C]`
    
* **訪問済み:** `{A, B, C, D, E}`
    

**ステップ 6:**

ノードCをポップします。

次に、子ノードであるノードFとノードGを逆順（ノードG、次にノードF）でスタックに追加し、訪問済みとしてマークします。

* **スタック:** `[G, F]`
    
* **訪問済み:** `{A, B, C, D, E, F, G}`
    

**ステップ 7:**

ノードFをスタックからポップします。このノードにはスタックに追加する子ノードがありません。

* **スタック:** `[G]`
    
* **訪問済み:** `{A, B, C, D, E, F, G}`
    

**ステップ 8:**

ノードGをポップします。このノードにはスタックに追加する子ノードがありません。

* **スタック:** `[]`
    
* **訪問済み:** `{A, B, C, D, E, F, G}`
    

スタックが空になると、走査は完了し、すべてのノードが訪問されました。

アルゴリズムはこの順序でノードを訪問しました：

**A → B → D → E → C → F → G**

<img src="https://cdn.freecodecamp.org/curriculum/lecture-transcripts/how-do-depth-first-and-breadth-first-search-work-4.png" alt="The tree diagram with numbers showing the DFS traversal order: A(1), B(2), D(3), E(4), C(5), F(6), G(7), demonstrating depth-first exploration of branches.">

ノードAから始めて、木の枝を下ってノードB、ノードD、ノードEを訪問し、その後また上に戻ってノードC、ノードF、ノードGを訪問する様子がわかります。これが深さ優先探索（DFS）の基本原理で、枝を深くたどってから戻り、別の枝を探す方法です。

この例ではスタックを使って解きましたが、深さ優先探索（DFS）は再帰を使って実装することもできます。関数が現在のノードを処理し、未訪問の隣接ノードごとに自分自身を呼び出します。関数呼び出しのスタックがLIFO（後入れ先出し）順序を暗黙的に管理します。

幅優先探索（BFS）と深さ優先探索（DFS）はどちらもグラフや木を走査するための重要なアルゴリズムです。幅優先探索（BFS）はレベルごとにノードを探索し、重みのないグラフで最短経路を見つけるのに最適です。一方、深さ優先探索（DFS）は枝をできるだけ深くたどってから戻るため、迷路を解いたりサイクルを検出したりするのに適しています。それぞれの長所と短所を理解することで、問題に応じて適切なアルゴリズムを選べるようになります。

# --questions--

## --text--

標準的な幅優先探索（BFS）アルゴリズムを実装するのに一般的に使われるデータ構造はどれですか？

## --answers--

スタック

### --feedback--

キューがどのようにデータを処理し、レベルごとにノードを訪問することとどう関係しているか考えてみてください。

---

キュー

---

連結リスト

### --feedback--

キューがどのようにデータを処理し、レベルごとにノードを訪問することとどう関係しているか考えてみてください。

---

木

### --feedback--

キューがどのようにデータを処理し、レベルごとにノードを訪問することとどう関係しているか考えてみてください。

## --video-solution--

2

## --text--

深さ優先探索（DFS）について正しい記述はどれですか？

## --answers--

深さ優先探索は、重みのないグラフで2つのノード間の最短経路を必ず見つける。

### --feedback--

深さ優先探索（DFS）がデータ構造を走査する戦略について考えてみてください。

---

深さ優先探索は、現在のレベルのすべての隣接ノードを訪問してから次のレベルに進む。

### --feedback--

深さ優先探索（DFS）がデータ構造を走査する戦略について考えてみてください。

---

深さ優先探索は常に幅優先探索（BFS）よりも空間効率が良い。

### --feedback--

深さ優先探索（DFS）がデータ構造を走査する戦略について考えてみてください。

---

深さ優先探索は通常、再帰またはスタックを使って実装される。

## --video-solution--

4

## --text--

重みのないグラフで、開始ノードから目標ノードまでの最短経路を見つけたい場合、最も適したアルゴリズムはどれですか？

## --answers--

幅優先探索（BFS）

---

深さ優先探索（DFS）

### --feedback--

幅優先探索と深さ優先探索の基本的な走査戦略と、どちらが2つのノード間の最短経路を保証するか考えてみてください。

---

二分探索

### --feedback--

幅優先探索と深さ優先探索の基本的な走査戦略と、どちらが2つのノード間の最短経路を保証するか考えてみてください。

---

マージソート

### --feedback--

幅優先探索と深さ優先探索の基本的な走査戦略と、どちらが2つのノード間の最短経路を保証するか考えてみてください。

## --video-solution--

1
