---
id: 699c6f4d4e468a10d3ae25ff
title: データ構造クイズ
challengeType: 8
dashedName: quiz-data-structures-js
---

# --description--

クイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

アルゴリズム解析におけるビッグO記法は何を表していますか？

#### --distractors--

特定のコンピューターでの正確な実行時間（秒単位）。

---

実行中に実行されたコード行の割合。

---

他の開発者にとってのコードの読みやすさ。

#### --answer--

入力サイズに対して時間または空間がどのように増加するか（上限の目安）。

### --question--

#### --text--

アルゴリズムのチャレンジを始めるとき、最初に行うべき最良のステップは何ですか？

#### --distractors--

勢いをつけるためにすぐにコーディングを始める。

---

問題を理解する前にパフォーマンスを最適化する。

---

解決策を完成させてから単体テストを書く。

#### --answer--

例やエッジケースを使って問題と制約を明確にする。

### --question--

#### --text--

動的配列と静的配列の主な違いは何ですか？

#### --distractors--

動的配列は異なる型の値を格納できるが、静的配列はできない。

---

静的配列は重複値を許可するが、動的配列は許可しない。

---

動的配列はすべての操作で静的配列より高速である。

#### --answer--

動的配列はサイズ変更で増減できるが、静的配列は固定サイズである。

### --question--

#### --text--

動的配列の末尾に要素を追加する際の償却時間計算量は何ですか？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` の償却時間

### --question--

#### --text--

単方向連結リストでインデックスkの要素にアクセスするのに`O(n)`時間かかるのはなぜですか？

#### --distractors--

アクセス前にリストのサイズを変更する必要がある。

---

インデックスはハッシュ化されテーブルで検索される。

---

ノードは連続して格納されているためシフトが必要。

#### --answer--

先頭ノードからk番目のノードまで一つずつ辿る必要があるから。

### --question--

#### --text--

単方向連結リストにはないが、双方向連結リストにある機能は何ですか？

#### --distractors--

`O(1)`時間で任意のインデックスにランダムアクセスできる。

---

高速な反復のための組み込み配列バッファ。

---

リストの長さを自動的に一定に保つ機能。

#### --answer--

次と前の両方のノードへのポインターがあり、逆方向の走査が可能。

### --question--

#### --text--

スタックを最もよく表す説明はどれですか？

#### --distractors--

先入れ先出し（`FIFO`）で先頭から削除。

---

任意の要素を`O(1)`時間で削除できる構造。

---

定数時間でランダムアクセスできる循環バッファ。

#### --answer--

後入れ先出し（`LIFO`）で、トップで`push`と`pop`を行う。

### --question--

#### --text--

キューの先頭の要素を削除する操作はどれですか？

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

ハッシュマップでキーによる値の検索の典型的な平均時間計算量は何ですか？

#### --distractors--

`O(n)`、すべてのキーを順番にスキャンするため。

---

`O(log n)`、バケット内で二分探索を行うため。

---

`O(n log n)`、挿入時にキーがソートされるため。

#### --answer--

良いハッシュ関数と低い負荷率で平均`O(1)`。

### --question--

#### --text--

セットデータ構造が保証することは何ですか？

#### --distractors--

要素はデフォルトでソートされた順序で格納される。

---

重複値が許可され、一緒に保持される。

---

要素は挿入位置でインデックスされる。

#### --answer--

重複しない一意の要素のみを格納する。

### --question--

#### --text--

動的配列で末尾以外のインデックスiに要素を挿入する最悪時間計算量は何ですか？

#### --distractors--

`O(1)`

---

`O(log n)`

---

`O(1)` の償却時間

#### --answer--

`O(n)`

### --question--

#### --text--

単方向連結リストの先頭に新しいノードを挿入する時間計算量は何ですか？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

スタックのトップ要素を削除せずに返す操作はどれですか？

#### --distractors--

`push`

---

`pop`

---

底に挿入する。

#### --answer--

`peek`

### --question--

#### --text--

キューを最もよく表す説明はどれですか？

#### --distractors--

後入れ先出し（`LIFO`）でトップから削除。

---

`O(1)`時間で任意のインデックスにランダムアクセス。

---

要素は常に自動的にソートされた順序で保持される。

#### --answer--

先入れ先出し（`FIFO`）で、バックで`enqueue`、フロントで`dequeue`を行う。

### --question--

#### --text--

言語に依存しないアルゴリズムの論理の高レベルな説明は何と呼ばれますか？

#### --distractors--

構文

---

機械語

---

デバッガー

#### --answer--

疑似コード

### --question--

#### --text--

問題を理解するためにすべきことの一つは何ですか？

#### --distractors--

すぐにコーディングを始める。

---

エッジケースを無視する。

---

最適化に直接進む。

#### --answer--

問題文を何度も読み返す。

### --question--

#### --text--

問題を解くときに特定すべき2つの要素は何ですか？

#### --distractors--

プログラミング言語とIDE。

---

変数名やその他の識別子。

---

カラースキームとコードのフォーマット。

#### --answer--

入力と期待される出力。

### --question--

#### --text--

nが大きくなるにつれて`O(n log n)`よりも速く増加する時間計算量はどれですか？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

ブルートフォースの解決策を実装した後、次にすべき良いステップは何ですか？

#### --distractors--

定数係数を測定前にマイクロ最適化する。

---

テストを破棄して解決策を最初から書き直す。

---

コードをシンプルに保つためにエッジケースを考慮しない。

#### --answer--

時間・空間計算量を分析し、特定したボトルネックを最適化する。

### --question--

#### --text--

空間計算量は何を測定しますか？

#### --distractors--

プログラムが使用するCPUコア数。

---

プログラムのコード行数。

---

プログラムのコンパイル時間。

#### --answer--

入力サイズに対してメモリ使用量がどのように増加するか。
