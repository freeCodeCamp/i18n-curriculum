---
id: 6900a59dedf6c5c52824304f
title: Вступний тест з NodeJS
challengeType: 8
dashedName: quiz-node-js-intro
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 9 із 10 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Чому JavaScript на стороні клієнта має обмежений доступ до локальної файлової системи?

#### --distractors--

Тому що JavaScript — повільна мова програмування.

---

Тому що браузери не мають достатньо пам’яті для роботи з файлами.

---

Тому що DOM API забороняє доступ до файлів.

#### --answer--

Тому що він розроблений для запуску у веббраузерах, де необмежений доступ до файлів створював би загрози безпеці.

### --question--

#### --text--

Що означає, що Node.js є «кросплатформним»?

#### --distractors--

Він може одночасно запускати кілька мов програмування.

---

Він може з’єднувати різні бази даних між собою.

---

Для кожної операційної системи потрібні різні методи встановлення.

#### --answer--

Він працює на будь-якій операційній системі, включно з Windows, macOS і Linux.

### --question--

#### --text--

Який API доступний у браузерному середовищі, але НЕ доступний у середовищі Node.js?

#### --distractors--

API файлової системи

---

API мережі

---

API операційної системи

#### --answer--

DOM API

### --question--

#### --text--

Що таке `npm` у контексті Node.js?

#### --distractors--

Фреймворк JavaScript для створення інтерфейсів користувача.

---

Веббраузер, оптимізований для застосунків Node.js.

---

Інструмент для налагодження застосунків Node.js.

#### --answer--

Менеджер пакетів для Node.js, який дозволяє встановлювати та керувати пакетами і модулями.

### --question--

#### --text--

Що означає прапорець `--lts` під час запуску `nvm install --lts`?

#### --distractors--

Встановлює останню тестову версію.

---

Встановлює найлегшу і найменшу версію.

---

Встановлює версію з найбільшою кількістю функцій.

#### --answer--

Встановлює версію з довготривалою підтримкою, яка пріоритетно забезпечує стабільність, надійність і безпеку.

### --question--

#### --text--

Що таке «функція зворотного виклику» у контексті асинхронного програмування в Node.js?

#### --distractors--

Метод, який перезапускає невдалу операцію.

---

Змінна, що зберігає результат синхронних операцій.

---

Інструмент для моніторингу продуктивності застосунку.

#### --answer--

Функція, яка визначає, що відбувається після завершення асинхронних операцій.

### --question--

#### --text--

Яке призначення файлу `package.json`, створеного `npm init`?

#### --distractors--

Зберігати змінні середовища для застосунку.

---

Налаштовувати параметри вебсервера.

---

Визначати HTML-структуру застосунку.

#### --answer--

Відстежувати деталі проєкту та залежності.

### --question--

#### --text--

Чому задачі, що інтенсивно використовують центральний процесор, можуть бути проблемними в Node.js?

#### --distractors--

Node.js не може виконувати математичні операції.

---

Node.js не підтримує криптографію чи обробку зображень.

---

Цикл подій заважає виконанню операцій центрального процесора.

#### --answer--

Вони можуть блокувати головний потік, оскільки Node.js працює в одному потоці, що призводить до проблем з продуктивністю.

### --question--

#### --text--

Яку команду ви використаєте, щоб запустити JavaScript-файл з назвою `server.js` за допомогою Node.js?

#### --distractors--

`npm run server.js`

---

`nvm execute server.js`

---

`javascript server.js`

#### --answer--

`node server.js`

### --question--

#### --text--

Що розробникам слід перевірити перед вибором пакетів з npm?

#### --distractors--

Чи має пакет щонайменше 1000 завантажень.

---

Чи був пакет створений нещодавно.

---

Чи потрібно платити за пакет.

#### --answer--

Чи підтримується пакет і чи відповідає він найкращим практикам якості та безпеки.
