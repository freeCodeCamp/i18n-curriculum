---
id: 66ed9002f45ce3ece4053eb6
title: Cuestionario sobre Pseudoclases CSS
challengeType: 8
dashedName: quiz-css-pseudo-classes
---

# --description--

Para aprobar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué son las pseudo-clases?

#### --distractors--

Son reglas CSS que crean contenido adicional dentro de un elemento cuando es necesario.

---

Son propiedades que ajustan el tamaño o diseño de un elemento en la página.

---

Son selectores usados para agregar efectos de movimiento a un elemento durante las interacciones.

#### --answer--

Son palabras clave añadidas a un selector que estilizan un elemento según su estado.

### --question--

#### --text--

¿Qué pseudoclase se aplica cuando un dispositivo apuntador se posiciona sobre un elemento?

#### --distractors--

`:focus`

---

`:active`

---

`:checked`

#### --answer--

`:hover`

### --question--

#### --text--

¿Cuál pseudoelemento te permite estilizar la primera letra de un párrafo?

#### --distractors--

`:first-letter`

---

`:first-child`

---

`:last-of-type`

#### --answer--

`::first-letter`

### --question--

#### --text--

¿Qué pseudoclase cambia el estilo de un elemento mientras está siendo clicado?

#### --distractors--

`:focus`

---

`:hover`

---

`:checked`

#### --answer--

`:active`

### --question--

#### --text--

¿Qué pseudoclase se usa para estilizar un elemento cuando está listo para recibir entrada del usuario, como un campo de texto que es clicado o al que se accede con la tecla de tabulación?

#### --distractors--

`::focus`

---

`:active`

---

`:visited`

#### --answer--

`:focus`

### --question--

#### --text--

¿Cuál de las siguientes reglas CSS añade correctamente el texto `Nota:` delante de cada elemento de párrafo con una clase de `nota`?

#### --distractors--

```css
.note::before {
  content: "Note:";
}
```

---

```css
p.note::after {
  content: "Note:";
}
```

---

```css
p::before {
  content: "Note:";
}
```

#### --answer--

```css
p.note::before {
  content: "Note:";
}
```

### --question--

#### --text--

¿Qué pseudoclase se aplica a un campo de entrada cuando está seleccionado o activado?

#### --distractors--

`:required`

---

`:disabled`

---

`:optional`

#### --answer--

`:checked`

### --question--

#### --text--

¿Cuál de las siguientes es la sintaxis correcta para estilizar el último hijo de una lista?

#### --distractors--

```css
li:nth-child(last) {
  color: blue;
}
```

---

```css
#li:last-child {
  color: blue;
}
```

---

```css
.li:last-child() {
  color: blue;
}
```

#### --answer--

```css
li:last-child {
  color: blue;
}
```

### --question--

#### --text--

¿Qué pseudoclase se dirige a los campos de entrada que no son obligatorios llenar?

#### --distractors--

`:required`

---

`:enabled`

---

`::optional`

#### --answer--

`:optional`

### --question--

#### --text--

¿Qué hace la pseudoclase `:disabled`?

#### --distractors--

Estiliza entradas verificadas.

---

Estiliza elementos sobre los que se está haciendo clic.

---

Selecciona elementos que no coinciden con un selector dado.

#### --answer--

Estiliza elementos que no están disponibles para la interacción del usuario.

### --question--

#### --text--

¿Qué pseudoclase se aplica cuando una entrada de formulario cumple sus criterios de validación?

#### --distractors--

`:checked`

---

`:required`

---

`:disabled`

#### --answer--

`:valid`

### --question--

#### --text--

¿Cuál de estas no es una pseudoclase de ubicación?

#### --distractors--

`:visited`

---

`:any-link`

---

`:link`

#### --answer--

`:current`

### --question--

#### --text--

¿Cuál de los siguientes selecciona el tercer elemento de lista?

#### --distractors--

```css
li:child(3) {
  color: red;
}
```

---

```css
li:last-child(3) {
  color: red;
}
```

---

```css
li:nth-child(three) {
  color: red;
}
```

#### --answer--

```css
li:nth-child(3) {
  color: red;
}
```

### --question--

#### --text--

¿Qué elementos tendrán un `color` de `azul` con el siguiente CSS?

```css
p:is(.blue, .highlight) {
  color: blue;
}
```

#### --distractors--

```html
<p class="class">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

---

```html
<div class="blue">Paragraph 1</div>
<div class="highlight">Paragraph 2</div>
```

---

```html
<p>Paragraph 1</p>
<span class="highlight">Paragraph 2</span>
```

#### --answer--

```html
<p class="blue">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

### --question--

#### --text--

¿Qué hace la pseudoclase `:not()`?

#### --distractors--

Añade estilos a todos los elementos.

---

Selecciona todos los elementos hijos de un padre.

---

Selecciona elementos que coinciden con un selector dado.

#### --answer--

Selecciona elementos que no coinciden con un selector dado.

### --question--

#### --text--

¿Qué hace la siguiente regla CSS?

```css
p:first-of-type {
  font-style: italic;
}
```

#### --distractors--

Selecciona el primer elemento `p` en el documento.

---

Selecciona todos los elementos `p` en el documento.

---

Selecciona el primer hijo de cada elemento `p`.

#### --answer--

Selecciona el primer elemento `p` dentro de un contenedor padre.

### --question--

#### --text--

¿Qué hace la pseudoclase `:last-of-type`?

#### --distractors--

Selecciona el primer elemento hijo de un tipo específico dentro de su padre.

---

Selecciona el elemento hijo medio de un tipo específico dentro de su padre.

---

Selecciona cada elemento hijo de un tipo específico dentro de su padre.

#### --answer--

Selecciona el último elemento hijo de un tipo específico dentro de su padre.

### --question--

#### --text--

¿Qué pseudoclase se usa para seleccionar el segundo elemento en una lista?

#### --distractors--

`:first-child`

---

`:required`

---

`:is()`

#### --answer--

`:nth-child(2)`

### --question--

#### --text--

¿Cuál de estos es una pseudoclase funcional?

#### --distractors--

`:first-child`

---

`:match()`

---

`:checked`

#### --answer--

`:is()`

### --question--

#### --text--

¿Cuál de estos no es una pseudoclase funcional?

#### --distractors--

`:has()`

---

`:not()`

---

`:where()`

#### --answer--

`:contains()`

## --quiz--

### --question--

#### --text--

¿Qué pseudoclase se usa para dirigirse a los elementos de formulario que están habilitados?

#### --distractors--

`:disabled`

---

`:active`

---

`:focus`

#### --answer--

`:enabled`

### --question--

#### --text--

¿Qué pseudoclase te permite seleccionar elementos contando desde el final?

#### --distractors--

`:nth-child(n)`

---

`:last-child`

---

`:last-of-type`

#### --answer--

`:nth-last-child(n)`

### --question--

#### --text--

¿Cuál de lo siguiente te permite seleccionar elementos que contienen elementos hijos específicos?

#### --distractors--

`:is()`

---

`:where()`

---

`:in-range`

#### --answer--

`:has()`

### --question--

#### --text--

¿Cuál de lo siguiente selecciona elementos que no contienen contenido o elementos hijos?

#### --distractors--

`:only-child`

---

`:last-child`

---

`:not()`

#### --answer--

`:empty`

### --question--

#### --text--

¿A qué se dirige este selector CSS?

```css
input:invalid {
  background-color: red;
}
```

#### --distractors--

Todos los elementos de entrada.

---

Todos los elementos de entrada con valores dentro del rango permitido.

---

Todos los elementos de entrada que pasan la validación.

#### --answer--

Todos los elementos de entrada que no pasan la validación.

### --question--

#### --text--

¿Qué pseudoclase selecciona campos de entrada cuyo valor es llenado automáticamente por el navegador?

#### --distractors--

`:visited`

---

`:valid`

---

`:where()`

#### --answer--

`:autofill`

### --question--

#### --text--

¿Qué pseudoclase selecciona un elemento si éste o cualquiera de sus descendientes está enfocado?

#### --distractors--

`:focus`

---

`:in-range`

---

`:only-child`

#### --answer--

`:focus-within`

### --question--

#### --text--

¿Qué pseudoclase representa links que apuntan al mismo documento?

#### --distractors--

`:target`

---

`:visited`

---

`:link`

#### --answer--

`:local-link`

### --question--

#### --text--

¿Cuál de lo siguiente estiliza el elemento `p` cuando es el objetivo de un fragmento de URL?

#### --distractors--

```css
p:empty {
  background-color: gold;
}
```

---

```css
p:not(.targeted) {
  background-color: gold;
}
```

---

```css
p:is(.target) {
  background-color: gold;
}
```

#### --answer--

```css
p:target {
  background-color: gold;
}
```

### --question--

#### --text--

¿Qué pseudo-clase se usa cuando un elemento es el objetivo de un fragmento de URL?

#### --distractors--

`:focus-within`

---

`:hover`

---

`:visited`

#### --answer--

`:target`

### --question--

#### --text--

¿Qué selecciona la pseudoclase `:only-child`?

#### --distractors--

Selecciona el elemento padre que tiene solo un hijo.

---

Selecciona todos los elementos hijos dentro del elemento padre.

---

Selecciona el elemento padre que contiene solo un tipo de elemento hijo.

#### --answer--

Selecciona un elemento que no tiene hermanos dentro de su elemento padre.

### --question--

#### --text--

¿Qué pseudoclase selecciona un elemento si es el único de su tipo dentro de su padre?

#### --distractors--

`:only-child`

---

`:nth-of-type(n)`

---

`:first-of-type`

#### --answer--

`:only-of-type`

### --question--

#### --text--

¿Qué regla CSS aplicará un `color` de `amarillo` al segundo elemento `p` en el siguiente HTML?

```html
<div>
  <h1>Courses</h1>
  <p>HTML</p>
  <p>CSS</p>
  <p>JavaScript</p>
</div>
```

#### --distractors--

```css
p:nth-child(2) {
  color: yellow;
}
```

---

```css
p:first-of-type {
  color: yellow;
}
```

---

```css
p:last-of-type {
  color: yellow;
}
```

#### --answer--

```css
p:nth-of-type(2) {
  color: yellow;
}
```

### --question--

#### --text--

¿Qué pseudoelemento te permite seleccionar el marcador de los elementos de lista para estilizar?

#### --distractors--

`::before`

---

`::after`

---

`:root`

#### --answer--

`::marker`

### --question--

#### --text--

¿Qué pseudoclase te permite detallar el elemento de nivel más alto en el documento, típicamente el elemento `html`?

#### --distractors--

`:first-child`

---

`:in-range`

---

`:target`

#### --answer--

`:root`

### --question--

#### --text--

¿Qué pseudoclase CSS tiene una especificidad de cero, asegurando que no interfiera con otras reglas de estilo específicas?

#### --distractors--

`:is()`

---

`:not()`

---

`:focus`

#### --answer--

`:where()`

### --question--

#### --text--

¿Cuál de lo siguiente es una pseudoclase de estructura de árbol?

#### --distractors--

`:where()`

---

`:valid`

---

`:link`

#### --answer--

`:root`

### --question--

#### --text--

¿Qué regla CSS establecerá el `background-color` del siguiente elemento a `rojo` si su valor está fuera del rango especificado?

```html
<input type="number" min="10" max="25"/>
```

#### --distractors--

```css
input:in-range {
  background-color: red;
}
```

---

```css
input {
  background-color: red;
}
```

---

```css
input:valid {
  background-color: red;
}
```

#### --answer--

```css
input:out-of-range {
  background-color: red;
}
```

### --question--

#### --text--

¿Qué pseudoelemento usa la propiedad `content` para insertar contenido después del elemento?

#### --distractors--

`::before`

---

`::marker`

---

`::first-letter`

#### --answer--

`::after`

### --question--

#### --text--

¿Qué pseudoclase aplica estilos a un elemento si su valor está dentro del rango especificado?

#### --distractors--

`:out-of-range`

---

`:enabled`

---

`:checked`

#### --answer--

`:in-range`
