---
id: 66ed8ff4f45ce3ece4053eb4
title: CSSレイアウトとエフェクトのクイズ
challengeType: 8
dashedName: quiz-css-layout-and-effects
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

要素のコンテンツがコンテナのサイズを超える場合、CSSではどの用語が使われますか？

#### --distractors--

アンダーフロー

---

圧縮

---

パディング

#### --answer--

オーバーフロー

### --question--

#### --text--

コンテンツが要素からはみ出した場合、CSSのデフォルトの動作は何ですか？

#### --distractors--

コンテンツを隠す。

---

コンテンツをクリップする。

---

コンテンツのサイズを変更する。

#### --answer--

コンテンツを可視のままにする。

### --question--

#### --text--

なぜCSSは通常、オーバーフローがある場合にコンテンツを隠すことを避けるのでしょうか？

#### --distractors--

元のレイアウトとデザインを維持するため。

---

ブラウザのパフォーマンス問題を防ぐため。

---

より良い検索エンジン最適化を可能にするため。

#### --answer--

すべてのコンテンツがユーザーにアクセス可能であることを保証するため。

### --question--

#### --text--

CSSで縦方向のオーバーフローを、コンテンツを隠すかスクロールバーを表示することで処理するにはどうしますか？

#### --distractors--

`overflow-y: visible`を使ってコンテンツを隠し、`overflow-y: auto`を使って常にスクロールバーを表示する。

---

`overflow-y: collapse`を使ってコンテンツを隠し、`overflow-y: expand`を使ってスクロールバーを表示する。

---

`overflow-y: none`を使ってコンテンツを隠し、`overflow-y: show`を使ってスクロールバーを表示する。

#### --answer--

`overflow-y: hidden`を使ってコンテンツを隠し、`overflow-y: scroll`を使って常にスクロールバーを表示する。

### --question--

#### --text--

CSSで水平方向と垂直方向のスクロール動作を別々に指定するにはどうしますか？

#### --distractors--

`scroll-x`と`scroll-y`のプロパティを使う。

---

`overflow-x`と`overflow-y`に同じ値を指定する。

---

`overflow`プロパティに2つの値を指定し、最初の値が`overflow-y`に、2つ目の値が`overflow-x`に適用される。

#### --answer--

`overflow`プロパティに2つの値を指定し、最初の値が`overflow-x`に、2つ目の値が`overflow-y`に適用される。

### --question--

#### --text--

`transform` CSSプロパティで何ができますか？

#### --distractors--

要素の色やフォントスタイルを変更する。

---

要素のマージンとパディングを調整する。

---

要素の可視性と表示を変更する。

#### --answer--

要素を回転、拡大縮小、傾斜、移動させる。

### --question--

#### --text--

`transform: translate(15px, 200px)`プロパティは要素をどのように変更しますか？

#### --distractors--

要素を元の位置から左に15ピクセル、下に200ピクセル移動させる。

---

要素を時計回りに15度回転させる。

---

要素を元の位置から右に15ピクセル、上に200ピクセル移動させる。

#### --answer--

要素を元の位置から右に15ピクセル、下に200ピクセル移動させる。

### --question--

#### --text--

CSSプロパティ`overflow-x`と`overflow-y`の目的は何ですか？

#### --distractors--

要素のパディングとマージンを調整する。

---

要素の背景色とフォントスタイルを設定する。

---

要素の可視性と表示プロパティを定義する。

#### --answer--

要素のコンテンツの水平方向と垂直方向のオーバーフローを制御する。

### --question--

#### --text--

CSSの`margin`プロパティの目的は何ですか？

#### --distractors--

要素のコンテンツとボーダーの間のスペースを定義する。

---

要素のボーダーとパディングの間のスペースを定義する。

---

要素の幅と高さを定義する。

#### --answer--

要素の周囲、ボーダーの外側のスペースを定義する。

### --question--

#### --text--

CSSで要素のコンテンツエリアは何を表しますか？

#### --distractors--

要素の周囲のマージンとパディングを定義する。

---

要素のボーダーの厚さを定義する。

---

ページ上の要素の可視性を制御する。

#### --answer--

テキスト、画像、動画など、要素の実際のコンテンツを含む。

### --question--

#### --text--

CSSで`content-box`と`border-box`の違いは何ですか？

#### --distractors--

`content-box`はパディングとボーダーを含み、`border-box`はコンテンツの寸法のみを含む。

---

`content-box`と`border-box`はどちらもボーダーの寸法のみを含む。

---

`content-box`は2D要素用、`border-box`は3D要素用。

#### --answer--

`content-box`はコンテンツの寸法のみを含み、`border-box`はコンテンツ、パディング、ボーダーを含む寸法。

### --question--

#### --text--

CSSリセットの目的は何ですか？

#### --distractors--

すべての要素にデフォルトスタイルを適用して読み込みを速くする。

---

未使用のスタイルを削除してCSSファイルのサイズを最小化する。

---

画像とフォントを最適化してパフォーマンスを向上させる。

#### --answer--

ブラウザのデフォルトスタイルを削除し、ブラウザ間で一貫したスタイルを保証する。

### --question--

#### --text--

一般的なCSSリセットの例はどれですか？

#### --distractors--

bootstrap.css

---

undo.css

---

restore.css

#### --answer--

normalize.css

### --question--

#### --text--

要素に回転、拡大縮小、移動などの変化を適用するCSSプロパティはどれですか？

#### --distractors--

`box-shadow`

---

`opacity`

---

`z-index`

#### --answer--

`transform`

### --question--

#### --text--

`border-box`の値を`box-sizing`プロパティに使った場合、要素の合計幅はどのように計算されますか？

#### --distractors--

合計幅 = コンテンツ幅 + ボーダー + マージン

---

合計幅 = コンテンツ幅 + パディング + ボーダー + マージン

---

合計幅 = コンテンツ幅

#### --answer--

合計幅 = コンテンツ幅 + パディング + ボーダー

### --question--

#### --text--

CSSの`filter`プロパティの目的は何ですか？

#### --distractors--

要素の拡大縮小と回転を変更する。

---

要素の背景色とテクスチャを変更する。

---

要素のサイズと位置を変更する。

#### --answer--

さまざまなグラフィカル効果を使って要素の見た目を変更する。

### --question--

#### --text--

CSSプロパティ`filter: grayscale(50%);`は要素に何をしますか？

#### --distractors--

要素の明るさを50%増加させる。

---

要素のコントラストを50%減少させる。

---

要素にセピア効果を適用する。

#### --answer--

要素に50%のグレースケール効果を適用する。

### --question--

#### --text--

CSSの`contrast()`関数の目的は何ですか？

#### --distractors--

画像の明るさを調整し、`0%`は黒く、`100%`は白くする。

---

画像のコントラストを調整し、`100%`はグレーにし、`0%`は効果なし、`100%`を超える値でコントラストが増加する。

---

画像の不透明度を調整し、`0%`は透明に、`100%`は不透明にする。

#### --answer--

画像のコントラストを調整し、`0%`はグレーにし、`100%`は効果なし、`100%`を超える値でコントラストが増加する。

### --question--

#### --text--

CSSの`hue-rotate()`関数の目的は何ですか？

#### --distractors--

画像の明るさを変える。

---

画像を一定の度数だけ時計回りに回転させる。

---

画像の彩度とコントラストを調整する。

#### --answer--

色相をシフトさせ、`angle`の値が色相環を回転させる度数を定義する。

### --question--

#### --text--

CSSのマージンコラプシングとは何ですか？

#### --distractors--

要素のマージンが負の値に設定され、レイアウトに問題が生じること。

---

要素のマージンがパディングによって減少し、要素の寸法が変わること。

---

ブラウザが要素のマージンを無視し、見えなくなること。

#### --answer--

隣接する要素の垂直マージンが重なり合い、1つのマージンにまとめられること。

## --quiz--

### --question--

#### --text--

`translate()`関数について正しいのはどれですか？

#### --distractors--

要素のサイズを拡大縮小するために使われる。

---

要素のテキストを移動するために使われる。

---

要素の形やサイズを変えるために使われる。

#### --answer--

要素を現在の位置から移動させるために使われる。

### --question--

#### --text--

CSSボックスモデルの構成要素はどれですか？

#### --distractors--

幅

---

高さ

---

サイズ

#### --answer--

パディング

### --question--

#### --text--

要素の`box-sizing`が`content-box`に設定されている場合、ボックスモデルのどの部分に`width`と`height`が適用されますか？

#### --distractors--

コンテンツ、パディング、ボーダー、マージン

---

コンテンツとパディング

---

コンテンツ、パディング、ボーダー

#### --answer--

コンテンツ

### --question--

#### --text--

要素の`box-sizing`が`border-box`に設定されている場合、CSSボックスモデルのどの部分に`width`と`height`が適用されませんか？

#### --distractors--

コンテンツエリア。

---

パディング

---

ボーダー

#### --answer--

マージン

### --question--

#### --text--

`brightness()`関数で要素を完全に黒くする値はどれですか？

#### --distractors--

`100%`

---

`0px`

---

`100px`

#### --answer--

`0%`

### --question--

#### --text--

`hue-rotate()`関数はどのような値を使いますか？

#### --distractors--

ピクセル

---

ラジアン

---

パーセンテージ

#### --answer--

度

### --question--

#### --text--

CSSプロパティ`filter: contrast(0%);`は要素に何をしますか？

#### --distractors--

要素を完全に白くする。

---

要素を完全に黒くする。

---

要素に影響を与えない。

#### --answer--

要素を完全にグレーに見せる。

### --question--

#### --text--

`brightness()` CSS関数の明るさを増加させ始める基準値は何ですか？

#### --distractors--

`100px`

---

`50px`

---

`50%`

#### --answer--

`100%`

### --question--

#### --text--

`grayscale()`関数はどのような値を使いますか？

#### --distractors--

ピクセル

---

度

---

ラジアン

#### --answer--

パーセンテージ

### --question--

#### --text--

`contrast()`関数はどのような値を使いますか？

#### --distractors--

ピクセル

---

度

---

ラジアン

#### --answer--

パーセンテージ

### --question--

#### --text--

CSSの`overflow-x`プロパティは何を制御しますか？

#### --distractors--

y軸の水平方向のオーバーフローを決定する。

---

y軸の垂直方向のオーバーフローを決定する。

---

x軸の垂直方向のオーバーフローを決定する。

#### --answer--

x軸の水平方向のオーバーフローを決定する。

### --question--

#### --text--

CSSの`overflow-y`プロパティは何を制御しますか？

#### --distractors--

x軸の水平方向のオーバーフローを決定する。

---

x軸の垂直方向のオーバーフローを決定する。

---

y軸の水平方向のオーバーフローを決定する。

#### --answer--

y軸の垂直方向のオーバーフローを決定する。

### --question--

#### --text--

CSSリセットのサードパーティ製の例はどれですか？

#### --distractors--

clean.css

---

restore.css

---

reset.css

#### --answer--

sanitize.css

### --question--

#### --text--

CSSのオーバーフロープロパティには何次元ありますか？

#### --distractors--

1つ

---

3つ

---

4つ

#### --answer--

2つ

### --question--

#### --text--

省略形プロパティ`overflow`に2つの値が与えられた場合、最初の値を使うプロパティはどれですか？

#### --distractors--

`overflow-y`

---

`overflow-w`

---

`overflow-z`

#### --answer--

`overflow-x`

### --question--

#### --text--

CSSの`blur()`関数は要素にどのようなぼかし効果を適用しますか？

#### --distractors--

ギリアニ

---

ガリレイ

---

ガリレオ

#### --answer--

ガウシアン

### --question--

#### --text--

`sepia()`関数はどのような値を使いますか？

#### --distractors--

ピクセル

---

ラジアン

---

度

#### --answer--

パーセンテージ

### --question--

#### --text--

省略形プロパティ`overflow`に2つの値が与えられた場合、2つ目の値を使うプロパティはどれですか？

#### --distractors--

`overflow-w`

---

`overflow-x`

---

`overflow-z`

#### --answer--

`overflow-y`

### --question--

#### --text--

CSSリセットは何をしますか？

#### --distractors--

ブラウザをデフォルトでダークモードに強制する。

---

HTML要素からインラインスタイルを削除する。

---

CSSを含むブラウザ拡張機能を無効にする。

#### --answer--

ブラウザのデフォルトスタイルを削除する。

### --question--

#### --text--

CSSプロパティ`filter: grayscale(100%);`は要素に何をしますか？

#### --distractors--

要素に影響を与えない。

---

要素を完全に黒くする。

---

要素を完全に白くする。

#### --answer--

要素の色の彩度を除去しつつ、トーン構造を維持する。
