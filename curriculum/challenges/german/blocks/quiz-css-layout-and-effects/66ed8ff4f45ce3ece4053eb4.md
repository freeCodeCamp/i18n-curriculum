---
id: 66ed8ff4f45ce3ece4053eb4
title: CSS-Layout- und Effekt-Quiz
challengeType: 8
dashedName: quiz-css-layout-and-effects
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welcher Begriff wird in CSS verwendet, um zu beschreiben, wenn der Inhalt eines Elements die Größe seines Containers überschreitet?

#### --distractors--

Underflow

---

Compression

---

Padding

#### --answer--

Overflow

### --question--

#### --text--

Wie verhält sich CSS standardmäßig, wenn der Inhalt eines Elements überläuft?

#### --distractors--

Den Inhalt ausblenden.

---

Den Inhalt beschneiden.

---

Den Inhalt skalieren.

#### --answer--

Den Inhalt sichtbar halten.

### --question--

#### --text--

Warum vermeidet CSS normalerweise das Ausblenden von Inhalten bei Überlauf?

#### --distractors--

Um das ursprüngliche Layout und Design beizubehalten.

---

Um Leistungsprobleme im Browser zu verhindern.

---

Um eine bessere Suchmaschinenoptimierung zu ermöglichen.

#### --answer--

Um sicherzustellen, dass alle Inhalte für Benutzer zugänglich sind.

### --question--

#### --text--

Wie können Sie vertikalen Überlauf in CSS behandeln, indem Sie Inhalte ausblenden oder Bildlaufleisten anzeigen?

#### --distractors--

Verwenden Sie `overflow-y: visible`, um Inhalte auszublenden, und `overflow-y: auto`, um Bildlaufleisten immer anzuzeigen.

---

Verwenden Sie `overflow-y: collapse`, um Inhalte auszublenden, und `overflow-y: expand`, um Bildlaufleisten anzuzeigen.

---

Verwenden Sie `overflow-y: none`, um Inhalte auszublenden, und `overflow-y: show`, um Bildlaufleisten anzuzeigen.

#### --answer--

Verwenden Sie `overflow-y: hidden`, um Inhalte auszublenden, und `overflow-y: scroll`, um Bildlaufleisten immer anzuzeigen.

### --question--

#### --text--

Wie können Sie in CSS unterschiedliche horizontale und vertikale Bildlaufverhalten angeben?

#### --distractors--

Indem Sie die Eigenschaften `scroll-x` und `scroll-y` verwenden.

---

Indem Sie für `overflow-x` und `overflow-y` denselben Wert angeben.

---

Indem Sie für die Eigenschaft `overflow` zwei Werte angeben, wobei der erste Wert für `overflow-y` und der zweite für `overflow-x` gilt.

#### --answer--

Indem Sie für die Eigenschaft `overflow` zwei Werte angeben, wobei der erste Wert für `overflow-x` und der zweite für `overflow-y` gilt.

### --question--

#### --text--

Was ermöglicht Ihnen die CSS-Eigenschaft `transform`?

#### --distractors--

Die Farbe und Schriftart eines Elements ändern.

---

Den Außenabstand und Innenabstand eines Elements anpassen.

---

Die Sichtbarkeit und Anzeige eines Elements ändern.

#### --answer--

Ein Element drehen, skalieren, schräg stellen und verschieben.

### --question--

#### --text--

Wie verändert die Eigenschaft `transform: translate(15px, 200px)` ein Element?

#### --distractors--

Sie verschiebt das Element 15 Pixel nach links und 200 Pixel nach unten von seiner ursprünglichen Position.

---

Sie dreht das Element 15 Grad im Uhrzeigersinn.

---

Sie verschiebt das Element 15 Pixel nach rechts und 200 Pixel nach oben von seiner ursprünglichen Position.

#### --answer--

Sie verschiebt das Element 15 Pixel nach rechts und 200 Pixel nach unten von seiner ursprünglichen Position.

### --question--

#### --text--

Was ist der Zweck der CSS-Eigenschaften `overflow-x` und `overflow-y`?

#### --distractors--

Sie passen den Innenabstand und Außenabstand eines Elements an.

---

Sie legen die Hintergrundfarbe und Schriftart eines Elements fest.

---

Sie definieren die Sichtbarkeit und Anzeige eines Elements.

#### --answer--

Sie steuern den horizontalen und vertikalen Überlauf des Inhalts eines Elements.

### --question--

#### --text--

Was bewirkt die Eigenschaft `margin` in CSS?

#### --distractors--

Sie definiert den Abstand zwischen dem Inhalt und dem Rahmen eines Elements.

---

Sie definiert den Abstand zwischen dem Rahmen und dem Innenabstand eines Elements.

---

Sie definiert die Breite und Höhe eines Elements.

#### --answer--

Sie definiert den Abstand um ein Element herum, außerhalb seines Rahmens.

### --question--

#### --text--

Was stellt der Inhaltsbereich eines Elements in CSS dar?

#### --distractors--

Er definiert die Außenabstände und Innenabstände um das Element.

---

Er definiert die Dicke des Rahmens des Elements.

---

Er steuert die Sichtbarkeit des Elements auf der Seite.

#### --answer--

Er enthält den tatsächlichen Inhalt des Elements, wie Text, Bilder und Videos.

### --question--

#### --text--

Was ist der Unterschied zwischen `content-box` und `border-box` in CSS?

#### --distractors--

`content-box` beinhaltet Innenabstand und Rahmen, während `border-box` nur die Inhaltsmaße enthält.

---

Sowohl `content-box` als auch `border-box` beinhalten nur die Rahmenmaße.

---

`content-box` wird für 2D-Elemente verwendet, während `border-box` für 3D-Elemente verwendet wird.

#### --answer--

`content-box` beinhaltet nur die Inhaltsmaße, während `border-box` Inhalt, Innenabstand und Rahmen in den Maßen einschließt.

### --question--

#### --text--

Was ist der Zweck von CSS-Resets?

#### --distractors--

Standardstile auf alle Elemente anwenden, um schnelleres Laden zu ermöglichen.

---

Die Größe von CSS-Dateien minimieren, indem ungenutzte Stile entfernt werden.

---

Bilder und Schriftarten für bessere Leistung optimieren.

#### --answer--

Standardmäßige Browser-Styling entfernen und konsistentes Styling über verschiedene Browser sicherstellen.

### --question--

#### --text--

Welches der folgenden ist ein gängiges Beispiel für ein CSS-Reset?

#### --distractors--

bootstrap.css

---

undo.css

---

restore.css

#### --answer--

normalize.css

### --question--

#### --text--

Welche CSS-Eigenschaft wird verwendet, um Änderungen wie Drehung, Skalierung und Verschiebung auf Elemente anzuwenden?

#### --distractors--

`box-shadow`

---

`opacity`

---

`z-index`

#### --answer--

`transform`

### --question--

#### --text--

Wie wird die Gesamtbreite eines Elements in CSS berechnet, wenn für die Eigenschaft `border-box` der Wert `box-sizing` verwendet wird?

#### --distractors--

Gesamtbreite = Inhaltsbreite + Rahmen + Außenabstand

---

Gesamtbreite = Inhaltsbreite + Innenabstand + Rahmen + Außenabstand

---

Gesamtbreite = Inhaltsbreite

#### --answer--

Gesamtbreite = Inhaltsbreite + Innenabstand + Rahmen

### --question--

#### --text--

Was ist der Zweck der Eigenschaft `filter` in CSS?

#### --distractors--

Die Skalierung und Drehung eines Elements ändern.

---

Die Hintergrundfarbe und Textur eines Elements ändern.

---

Die Größe und Position eines Elements verändern.

#### --answer--

Das visuelle Erscheinungsbild eines Elements durch verschiedene grafische Effekte verändern.

### --question--

#### --text--

Was bewirkt die CSS-Eigenschaft `filter: grayscale(50%);` an einem Element?

#### --distractors--

Sie erhöht die Helligkeit des Elements um 50 %.

---

Sie verringert den Kontrast des Elements um 50 %.

---

Sie wendet einen Sepia-Effekt auf das Element an.

#### --answer--

Sie wendet einen 50 % Graustufen-Effekt auf das Element an.

### --question--

#### --text--

Was ist der Zweck der Funktion `contrast()` in CSS?

#### --distractors--

Sie passt die Helligkeit des Bildes an, wobei `0%` es schwarz und `100%` es weiß macht.

---

Sie passt den Kontrast des Bildes an, wobei `100%` es grau macht, `0%` keine Wirkung hat und Werte über `100%` den Kontrast erhöhen.

---

Sie passt die Deckkraft des Bildes an, wobei `0%` es transparent und `100%` es undurchsichtig macht.

#### --answer--

Sie passt den Kontrast des Bildes an, wobei `0%` es grau macht, `100%` keine Wirkung hat und Werte über `100%` den Kontrast erhöhen.

### --question--

#### --text--

Was ist der Zweck der Funktion `hue-rotate()` in CSS?

#### --distractors--

Sie ändert die Helligkeit eines Bildes.

---

Sie dreht ein Bild im Uhrzeigersinn um eine bestimmte Anzahl Grad.

---

Sie passt die Sättigung und den Kontrast eines Bildes an.

#### --answer--

Sie wendet eine Farbverschiebung an, wobei der Wert `angle` die Anzahl der Grad definiert, um die im Farbkreis rotiert wird.

### --question--

#### --text--

Was versteht man unter Margin Collapsing in CSS?

#### --distractors--

Wenn der Außenabstand eines Elements auf einen negativen Wert gesetzt wird und dies Probleme im Layout verursacht.

---

Wenn der Außenabstand eines Elements durch den Innenabstand verringert wird und sich dadurch die Abmessungen des Elements ändern.

---

Wenn der Außenabstand eines Elements vom Browser ignoriert wird und somit nicht sichtbar ist.

#### --answer--

Wenn sich die vertikalen Außenabstände benachbarter Elemente überlappen und zu einem einzigen Außenabstand zusammengefasst werden.

## --quiz--

### --question--

#### --text--

Welche der folgenden Aussagen ist WAHR über die Funktion `translate()`?

#### --distractors--

Sie wird verwendet, um die Größe eines Elements zu skalieren.

---

Sie wird verwendet, um den Text eines Elements zu verschieben.

---

Sie wird verwendet, um die Form oder Größe eines Elements zu ändern.

#### --answer--

Sie wird verwendet, um ein Element von seiner aktuellen Position zu verschieben.

### --question--

#### --text--

Welche der folgenden ist eine Komponente des CSS-Box-Modells?

#### --distractors--

Breite

---

Höhe

---

Größe

#### --answer--

Innenabstand

### --question--

#### --text--

Wenn die `box-sizing` eines Elements auf `content-box` gesetzt ist, auf welchen Teil des Box-Modells beziehen sich `width` und `height`?

#### --distractors--

Inhalt, Innenabstand, Rahmen und Außenabstand

---

Inhalt und Innenabstand

---

Inhalt, Innenabstand und Rahmen

#### --answer--

Inhalt

### --question--

#### --text--

Wenn die `box-sizing` eines Elements auf `border-box` gesetzt ist, auf welche Teile des CSS-Box-Modells beziehen sich `width` und `height` NICHT?

#### --distractors--

Der Inhaltsbereich.

---

Innenabstand

---

Rahmen

#### --answer--

Außenabstand

### --question--

#### --text--

Welcher Wert der Funktion `brightness()` macht ein Element vollständig schwarz?

#### --distractors--

`100%`

---

`0px`

---

`100px`

#### --answer--

`0%`

### --question--

#### --text--

Welche Art von Werten verwendet die Funktion `hue-rotate()`?

#### --distractors--

Pixel

---

Radiant

---

Prozent

#### --answer--

Grad

### --question--

#### --text--

Was bewirkt die CSS-Eigenschaft `filter: contrast(0%);` an einem Element?

#### --distractors--

Sie lässt das Element vollständig weiß erscheinen.

---

Sie lässt das Element vollständig schwarz erscheinen.

---

Sie hat keine Wirkung auf das Element.

#### --answer--

Sie lässt das Element vollständig grau erscheinen.

### --question--

#### --text--

Was ist der Basiswert der CSS-Funktion `brightness()`, ab dem die Helligkeit erhöht wird?

#### --distractors--

`100px`

---

`50px`

---

`50%`

#### --answer--

`100%`

### --question--

#### --text--

Welche Art von Werten verwendet die Funktion `grayscale()`?

#### --distractors--

Pixel

---

Grad

---

Radiant

#### --answer--

Prozent

### --question--

#### --text--

Welche Art von Werten verwendet die Funktion `contrast()`?

#### --distractors--

Pixel

---

Grad

---

Radiant

#### --answer--

Prozent

### --question--

#### --text--

Was steuert die Eigenschaft `overflow-x` in CSS?

#### --distractors--

Sie bestimmt den horizontalen Überlauf auf der y-Achse.

---

Sie bestimmt den vertikalen Überlauf auf der y-Achse.

---

Sie bestimmt den vertikalen Überlauf auf der x-Achse.

#### --answer--

Sie bestimmt den horizontalen Überlauf auf der x-Achse.

### --question--

#### --text--

Was steuert die Eigenschaft `overflow-y` in CSS?

#### --distractors--

Sie bestimmt den horizontalen Überlauf auf der x-Achse.

---

Sie bestimmt den vertikalen Überlauf auf der x-Achse.

---

Sie bestimmt den horizontalen Überlauf auf der y-Achse.

#### --answer--

Sie bestimmt den vertikalen Überlauf auf der y-Achse.

### --question--

#### --text--

Was ist ein Beispiel für eine Drittanbieter-Option für ein CSS-Reset?

#### --distractors--

clean.css

---

restore.css

---

reset.css

#### --answer--

sanitize.css

### --question--

#### --text--

Wie viele Dimensionen hat die Eigenschaft overflow in CSS?

#### --distractors--

Eine

---

Drei

---

Vier

#### --answer--

Zwei

### --question--

#### --text--

Wenn die Kurzschreibweise-Eigenschaft `overflow` zwei Werte erhält, welche Eigenschaft verwendet dann den ersten Wert?

#### --distractors--

`overflow-y`

---

`overflow-w`

---

`overflow-z`

#### --answer--

`overflow-x`

### --question--

#### --text--

Welche Art von Weichzeichner-Effekt wendet die Funktion `blur()` in CSS auf ein Element an?

#### --distractors--

Giliani

---

Galilei

---

Galileo

#### --answer--

Gaussian

### --question--

#### --text--

Welche Art von Werten verwendet die Funktion `sepia()`?

#### --distractors--

Pixel

---

Radiant

---

Grad

#### --answer--

Prozent

### --question--

#### --text--

Wenn die Kurzschreibweise-Eigenschaft `overflow` zwei Werte erhält, welche Eigenschaft verwendet dann den zweiten Wert?

#### --distractors--

`overflow-w`

---

`overflow-x`

---

`overflow-z`

#### --answer--

`overflow-y`

### --question--

#### --text--

Was bewirkt ein CSS-Reset?

#### --distractors--

Es zwingt Browser standardmäßig zum Dunkelmodus.

---

Es entfernt inline-Stile von HTML-Elementen.

---

Es deaktiviert Browser-Erweiterungen, die CSS enthalten.

#### --answer--

Es entfernt das standardmäßige Browser-Styling.

### --question--

#### --text--

Was bewirkt die CSS-Eigenschaft `filter: grayscale(100%);` an einem Element?

#### --distractors--

Sie hat keine Wirkung auf das Element.

---

Sie lässt das Element vollständig schwarz erscheinen.

---

Sie lässt das Element vollständig weiß erscheinen.

#### --answer--

Sie entfernt die Farbsättigung, während die Tonstruktur des Elements erhalten bleibt.

