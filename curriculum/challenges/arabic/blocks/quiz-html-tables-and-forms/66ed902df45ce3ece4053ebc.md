---
id: 66ed902df45ce3ece4053ebc
title: اختبار جداول ونماذج HTML
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي من الخصائص التالية تُستخدم لتحديد عنوان URL الذي يجب إرسال بيانات النموذج إليه؟

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

ما العنصران المستخدمان لتحديد صف ورأس صف في جدول؟

#### --distractors--

العنصر `r` يُستخدم للصفوف والعنصر `thead` يُستخدم للرأس.

---

العنصر `row` يُستخدم للصفوف والعنصر `th` يُستخدم للرأس.

---

العنصر `tr` يُستخدم للصفوف والعنصر `head` يُستخدم للرأس.

#### --answer--

العنصر `tr` يُستخدم للصفوف والعنصر `th` يُستخدم للرأس.

### --question--

#### --text--

ما هو استخدام العنصر `td`؟

#### --distractors--

يُستخدم لتعريف نوع بيانات الجدول.

---

يُستخدم لدمج عمودين.

---

يُستخدم لدمج صفين.

#### --answer--

يُستخدم لتعريف خلية في الجدول.

### --question--

#### --text--

ما وظيفة الخاصية `colspan`؟

#### --distractors--

تعرف طول العمود في الجدول.

---

تزيل فاصل السطر بين النص في العمود.

---

تُستخدم لتحديد عدد الأعمدة.

#### --answer--

تعرف عدد الأعمدة التي يجب أن تمتد عليها خلية الجدول.

### --question--

#### --text--

ما دور الخاصية `placeholder`؟

#### --distractors--

تُستخدم لإرسال البيانات إلى الخادم عند إرسال النموذج.

---

تُستخدم لربط عنصر `label` بعنصر `input`.

---

تُستخدم لتحديد أن حقل الإدخال للقراءة فقط للمستخدم.

#### --answer--

تُستخدم لتوفير تلميح لحقل الإدخال.

### --question--

#### --text--

ما الغرض من العنصر `thead`؟

#### --distractors--

يُستخدم لتجميع محتوى الجسم في جدول HTML.

---

يُستخدم لتجميع وصف جدول HTML.

---

يُستخدم لتجميع محتوى التذييل في جدول HTML.

#### --answer--

يُستخدم لتجميع محتوى القسم العلوي في جدول HTML.

### --question--

#### --text--

أي مما يلي مثال على الربط الصريح بين التسميات والمدخلات في النموذج؟

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

ما هو استخدام مدقق HTML؟

#### --distractors--

المدقق هو أداة تقوم تلقائيًا بتنسيق شفرة HTML الخاصة بك.

---

المدقق هو أداة تطبق أنماطًا على شفرة HTML الخاصة بك.

---

المدقق هو أداة تجعل شفرة HTML الخاصة بك تعمل بشكل أسرع.

#### --answer--

المدقق هو أداة تتحقق من صيغة شفرة HTML لضمان صحتها.

### --question--

#### --text--

أي عنصر يُستخدم لتعريف خلية في جدول؟

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

أي مما يلي أداة تسمح لك بفحص وتعديل هيكل HTML لصفحة ويب؟

#### --distractors--

إزالة DOM.

---

مختبر DOM.

---

التحقق من DOM.

#### --answer--

مفتش DOM.

### --question--

#### --text--

أي مما يلي الطريقة الصحيحة لتجميع حقول الإدخال ذات الصلة؟

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

أي مما يلي يُستخدم لتحديد أن حقل الإدخال للقراءة فقط؟

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

أي خاصية تحدد أن الحقل يجب ملؤه قبل إرسال النموذج؟

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

ما استخدام أدوات المطوِّر (Devtools)؟

#### --distractors--

هذه الأدوات مدمجة مباشرة في المتصفح وتُستخدم لاكتشاف أي مشاكل في تنسيق الشفرة.

---

هذه الأدوات مدمجة مباشرة في المتصفح وتُستخدم لتنسيق الشفرة تلقائيًا.

---

هذه الأدوات مدمجة مباشرة في المتصفح وتُستخدم لمساعدتك في ضمان تغطية اختبار 100% لشفرتك.

#### --answer--

هذه الأدوات مدمجة مباشرة في المتصفح وتُستخدم لمساعدتك في معالجة الأخطاء، وتحليل الأداء، وتحليل صفحات الويب.

### --question--

#### --text--

أي مما يلي الطريقة الصحيحة لتعطيل حقل إدخال؟

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

أي مما يلي قيمة صحيحة للخاصية `type`؟

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

أي مما يلي الاستخدام الصحيح للخاصية `size`؟

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

أي من الخصائص التالية تُستخدم لتحديد الحد الأدنى والحد الأقصى لعدد الأحرف المطلوبة في حقل الإدخال؟

#### --distractors--

`minlen` و `maxlen`

---

`min` و `max`

---

`minimumLen` و `maximumLen`

#### --answer--

`minlength` و `maxlength`

### --question--

#### --text--

أي من أمثلة الأزرار التالية **لا** تستخدم قيمة صحيحة للخاصية `type`؟

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

أي من الخصائص التالية تُستخدم لتحديد القيمة لزر؟

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

ما دور الخاصية `action` داخل وسم `form` الافتتاحي؟

#### --distractors--

تُستخدم لتحديد أن حقل الإدخال للقراءة فقط.

---

تُستخدم لتعريف عدد الأحرف التي يجب أن تظهر أثناء كتابة المستخدم في الحقل.

---

تُستخدم لإظهار تلميح للمستخدم ليعرف ماذا يدخل في حقل الإدخال.

#### --answer--

تُستخدم لتحديد عنوان URL الذي يجب إرسال بيانات النموذج إليه.

### --question--

#### --text--

أي من الخصائص التالية تُستخدم لتحديد طريقة HTTP المستخدمة عند إرسال بيانات النموذج؟

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

أي من الطرق التالية شائعة الاستخدام عند العمل مع النماذج؟

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

أي مما يلي ليس قيمة صحيحة للخاصية `type`؟

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

أي مما يلي الطريقة الصحيحة لإنشاء زر في نموذج؟

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

ما هو الربط الضمني للنموذج؟

#### --distractors--

حيث يمكن ربط المدخلات بالتسميات باستخدام الخاصيتين `action` و `for` على عنصر `label`.

---

حيث يمكن ربط المدخلات بالتسميات باستخدام الخاصيتين `required` و `for` على عنصر `label`.

---

حيث يمكن ربط المدخلات بالتسميات باستخدام الخاصية `for` على عنصر `label`.

#### --answer--

حيث يمكن ربط المدخلات بالتسميات عن طريق تغليف حقل الإدخال داخل عنصر `label`.

### --question--

#### --text--

ما دور العنصر `fieldset`؟

#### --distractors--

يُستخدم لتجميع محتوى القسم العلوي في جدول HTML.

---

يُستخدم لإنشاء تسمية لحقل إدخال.

---

يُستخدم لإضافة تسمية توضيحية لوصف مجموعة المدخلات.

#### --answer--

يُستخدم لتجميع المدخلات ذات الصلة معًا.

### --question--

#### --text--

أي مما يلي يُستخدم لإنشاء مربع اختيار في نموذج؟

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

أي من العناصر التالية يُستخدم لإضافة عنوان لجدول HTML؟

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

ما دور العنصر `legend`؟

#### --distractors--

يُستخدم لتجميع محتوى الجسم في جدول HTML.

---

يُستخدم لإضافة تسمية توضيحية لوصف خلايا الجدول.

---

يُستخدم لتجميع المدخلات ذات الصلة معًا.

#### --answer--

يُستخدم لإضافة تسمية توضيحية لوصف مجموعة المدخلات.

### --question--

#### --text--

أي من العناصر التالية يُستخدم لتجميع محتوى القسم العلوي في جدول HTML؟

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

أي مما يلي الطريقة الصحيحة لإضافة صفوف وخلايا بيانات إلى جدول؟

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

ما هو مفتش DOM؟

#### --distractors--

أداة تُستخدم لإزالة كل العناصر من DOM.

---

مجموعة أدوات مدمجة مباشرة في المتصفح تساعدك على معالجة الأخطاء، وتحليل الأداء، وتحليل صفحات الويب.

---

أداة تتحقق من صيغة شفرة HTML لضمان صحتها.

#### --answer--

أداة تسمح لك بفحص وتعديل هيكل HTML لصفحة ويب.

### --question--

#### --text--

أي مما يلي أداة تتحقق من صيغة شفرة HTML لضمان صحتها؟

#### --distractors--

HTML validate

---

HTML isValid

---

HTML valid

#### --answer--

HTML validator

### --question--

#### --text--

أي مما يلي الطريقة الصحيحة لتحديد عدد الأعمدة لخلية بيانات في جدول؟

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

ما هي حالة التركيز لحقل الإدخال؟

#### --distractors--

هي حالة حقل الإدخال عندما يُزال التركيز عنه بواسطة المستخدم.

---

هي حالة حقل الإدخال عندما يُحدد كقراءة فقط بواسطة المستخدم.

---

هذه هي حالة حقل الإدخال عندما يعطّل المستخدم هذا الحقل.

#### --answer--

هذه هي حالة حقل الإدخال عندما يختاره المستخدم.

### --question--

#### --text--

ما هي الطريقة الصحيحة لإنشاء زر اختيار؟

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

ما هو دور الخاصية `for`؟

#### --distractors--

تُستخدم لإظهار تلميح للمستخدم.

---

تُستخدم لتحديد قيمة المُدخَل.

---

تُستخدم لإنشاء مسمى لحقل الإدخال.

#### --answer--

تُستخدم لتحديد حقل الإدخال الذي ينتمي إليه المسمى.

### --question--

#### --text--

أي من الخصائص التالية تُستخدم لتحديد عدد الأحرف التي يجب أن تظهر أثناء كتابة المستخدم في حقل الإدخال؟

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

أي من الخصائص التالية تُستخدم لإظهار تلميح للمستخدم ليعرف ما الذي يجب إدخاله في حقل الإدخال؟

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
