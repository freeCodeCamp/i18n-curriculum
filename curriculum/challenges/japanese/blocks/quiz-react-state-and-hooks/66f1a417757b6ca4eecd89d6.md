---
id: 66f1a417757b6ca4eecd89d6
title: Reactのステートとフックに関するクイズ
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

Reactでボタンにクリックイベントを追加する正しい方法はどれですか？

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

ReactのSynthetic Eventとは何ですか？

#### --distractors--

`inputs`と`submit`イベント専用のJSX要素の一種。

---

`submit`イベント専用のJSX要素の一種。

---

イベント伝播のバブリングフェーズをよりユーザーフレンドリーに変更する方法。

#### --answer--

`click`や`submit`イベントのようなネイティブイベントをラップしたもの。

### --question--

#### --text--

イベントハンドラ関数の名前付けによく使われる接頭辞はどれですか？

#### --distractors--

isHandling

---

hasHandled

---

handling

#### --answer--

handle

### --question--

#### --text--

Reactのレンダリングにおける3つの主要な段階は何ですか？

#### --distractors--

Handshake、loading、hydration。

---

Fetching、decoding、execution。

---

Mounting、updating、unmounting。

#### --answer--

Triggering、rendering、committing。

### --question--

#### --text--

`useState`フックを使う正しい方法はどれですか？

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

Reactのイベント処理に使われる有効な属性でないものはどれですか？

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

配列のステートを更新する正しい方法はどれですか？

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--

この関数の問題点は何ですか？

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

ネストされた関数内で`useState`変数を更新することは許されていません。

---

`previousState`は括弧で囲む必要があります。

---

ステートのセッター関数内でアロー関数を使うべきではありません。

#### --answer--

他のプロパティが上書きされないように、`previousState`の前に`name`をスプレッドする必要があります。

### --question--

#### --text--

配列からアイテムを削除するためにステートを更新する正しい方法はどれですか？

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

Reactで`ref`属性は通常何に使われますか？

#### --distractors--

Reactコンポーネントのパフォーマンス問題を監査するために使われます。

---

Reactコンポーネントのアクセシビリティ問題を監査するために使われます。

---

DOMノードを削除するために使われます。

#### --answer--

DOMノードにアクセスするために使われます。

### --question--

#### --text--

`ref`の現在の値にアクセスする正しい方法はどれですか？

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

このメッセージはコンソールに何回ログされますか？

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

3回。

---

2回。

---

依存配列が空なのでログされません。

#### --answer--

初回レンダリング時に1回だけ。

### --question--

#### --text--

Reactのカスタムフックは通常何に使われますか？

#### --distractors--

アプリの最も複雑な機能を実行するために使われます。

---

通常は使われず、組み込みフックが好まれます。

---

以前のReactのライフサイクルメソッドの代わりです。

#### --answer--

コンポーネント間でステートフルなロジックを再利用するのに役立ちます。

### --question--

#### --text--

このメッセージはコンソールに何回ログされますか？

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

フォームが送信されたりクリックイベントが発生したときにログされます。

---

2回ログされます。

---

1回だけログされます。

#### --answer--

コンポーネントがレンダリングされるたびにログされます。

### --question--

#### --text--

`onSubmit`イベントでブラウザの更新を防ぐ正しい方法はどれですか？

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--

```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

データ取得に通常使われるフックはどれですか？

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

ネイティブEventオブジェクトに存在しない有効なプロパティはどれですか？

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

Reactが何かが変わったことを検知し、ユーザーインターフェース（UI）を更新する必要があるかもしれない段階はどれですか？

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

Reactが仮想DOMで準備した変更を実際のDOMに適用する段階はどれですか？

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

`dependencies`の`useEffect`の目的は何ですか？

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

オプションの`dependencies`引数は`useEffect`に関係する何も影響しません。

---

オプションの`dependencies`引数はエフェクトを無限ループで実行させます。

---

オプションの`dependencies`引数はエフェクトの実行を防ぐために使われます。

#### --answer--

オプションの`dependencies`引数はエフェクトがいつ実行されるかを制御します。
