---
id: 692487767b6b728454f53f29
title: Cuestionario de Módulos Core de NodeJS
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

Para aprobar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué método de `fs` agrega contenido a un archivo existente sin sobrescribirlo?

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

¿Qué sucede cuando usas métodos síncronos del módulo `fs`?

#### --distractors--

Eliminan archivos existentes automáticamente.

---

Se ejecutan más rápido que los asíncronos en todos los casos.

---

Devuelven Promesas por defecto.

#### --answer--

Bloquean el programa hasta que la operación termine.

### --question--

#### --text--

¿Qué hace el parámetro `utf8` en `fs.readFile()`?

#### --distractors--

Evita que se lancen errores.

---

Cambia la extensión del archivo.

---

Comprime archivos grandes.

#### --answer--

Le indica a Node que devuelva una cadena legible en lugar de un buffer.

### --question--

#### --text--

¿Qué método de `Buffer` crea un nuevo buffer lleno de ceros?

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

¿Qué hace `Buffer.toString()`?

#### --distractors--

Convierte un buffer en código binario.

---

Borra el contenido de un buffer.

---

Muestra la longitud en bytes del buffer.

#### --answer--

Convierte el contenido del buffer en una cadena legible.

### --question--

#### --text--

¿Qué característica de `crypto` requiere una clave privada para generar una firma?

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

¿Por qué deberías evitar escribir tus propios algoritmos criptográficos directamente?

#### --distractors--

Es ilegal en aplicaciones Node.js.

---

Node.js no soporta cifrado.

---

JavaScript no puede calcular hashes.

#### --answer--

Puede ser inseguro sin bibliotecas bien probadas.

### --question--

#### --text--

¿Qué método `crypto` se recomienda para generar tokens aleatorios seguros en lugar de `Math.random()`?

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

¿Qué método del módulo OS devuelve la arquitectura de la CPU?

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

¿Qué devuelve `os.userInfo()`?

#### --distractors--

Solo el nombre de usuario.

---

Solo la dirección IP del dispositivo.

---

Solo el ID del usuario que ha iniciado sesión.

#### --answer--

Información sobre el usuario actual del sistema.

### --question--

#### --text--

¿Qué método `path` une segmentos y normaliza las barras adicionales?

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

¿Qué método `path` produce una ruta absoluta basada en el directorio de trabajo actual?

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

¿Qué representa la variable global `__dirname`?

#### --distractors--

El nombre del usuario que ejecuta Node.

---

La extensión del archivo actual.

---

Una ruta de carpeta relativa que comienza con `./`.

#### --answer--

La ruta absoluta del directorio que contiene el archivo actual.

### --question--

#### --text--

¿Qué propiedad del proceso contiene variables de entorno como `NODE_ENV`?

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

¿Qué hace el evento `process.on("exit")`?

#### --distractors--

Reinicia la aplicación Node.

---

Envía una señal `exit` a los procesos hijos.

---

Limpia la cola del event loop.

#### --answer--

Ejecuta código justo antes de que el proceso Node termine.

### --question--

#### --text--

¿Qué tipo de stream puede tanto leer como escribir datos pero no los transforma?

#### --distractors--

Flujo legible

---

Flujo de transformación

---

Flujo escribible

#### --answer--

Flujo dúplex

### --question--

#### --text--

¿Qué se requiere para implementar la lectura y escritura eficiente de archivos grandes?

#### --distractors--

Usando solo `Buffer.alloc()`

---

Importando el módulo `crypto`

---

Guardar todo el archivo en memoria primero

#### --answer--

Usando streams para procesar datos en fragmentos

### --question--

#### --text--

¿Qué método enlaza un readable stream con un writable stream?

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

¿Qué evento de stream indica que no hay más datos para escribir?

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

¿Qué se registra si imprimes un chunk de un readable stream sin `toString()`?

#### --distractors--

Contenido de texto plano

---

El PID del proceso

---

Nombre del sistema operativo

#### --answer--

Un objeto buffer con datos binarios
