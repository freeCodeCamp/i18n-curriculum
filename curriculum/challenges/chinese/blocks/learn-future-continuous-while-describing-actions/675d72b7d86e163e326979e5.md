---
id: 675d72b7d86e163e326979e5
title: 任务 56
challengeType: 22
dashedName: task-56
lang: en-US
---

<!-- (audio) Mark: Sure. We can do some tests on the staging server, can't we? -->

# --instructions--

请听音频，并完成下面的填空。

# --fillInTheBlank--

## --sentence--

`Sure. We can do some tests on the BLANK, BLANK we?`

## --blanks--

`staging server`

### --feedback--

这两个词的短语指的是一个独立的环境，用于在更改上线前进行测试。它镜射生产服务器，以确保更新按预期工作。

---

`can't`

### --feedback--

它是标签问题的一部分，这里用于在使用 `can` 的肯定主语句后确认或查看一致性。

# --explanation--

`Staging server` 指的是一个测试环境，在该环境中对系统或应用的更改进行测试，然后再在实时的生产服务器上实现。例如：

我们将新建的网站特色上传到用于测试的 `staging server`。

`Can't` 在此上下文中是标签疑问的一部分。它跟随一个肯定语句，用于以礼貌或对话的方式确认或查看一致性。例如：

`We can finish the task today, can't we?` - 在这个句子中，肯定语句（`We can finish the task today`）后面跟着否定的反义标签问句（`can't we?`），用于查看是否一致或确认。

# --scene--

```json
{
  "setup": {
    "background": "company2-center.png",
    "characters": [
      {
        "character": "Mark",
        "position": {
          "x": 50,
          "y": 0,
          "z": 1.4
        },
        "opacity": 0
      }
    ],
    "audio": {
      "filename": "B1_6-2.mp3",
      "startTime": 1,
      "startTimestamp": 36.44,
      "finishTimestamp": 39.76
    }
  },
  "commands": [
    {
      "character": "Mark",
      "opacity": 1,
      "startTime": 0
    },
    {
      "character": "Mark",
      "startTime": 1,
      "finishTime": 4.32,
      "dialogue": {
        "text": "Sure. We can do some tests on the staging server, can't we?",
        "align": "center"
      }
    },
    {
      "character": "Mark",
      "opacity": 0,
      "startTime": 4.82
    }
  ]
}
```
