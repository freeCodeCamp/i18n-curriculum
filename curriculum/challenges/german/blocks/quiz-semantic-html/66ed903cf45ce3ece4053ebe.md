---
id: 66ed903cf45ce3ece4053ebe
title: Quiz zu semantischem HTML
challengeType: 8
dashedName: quiz-semantic-html
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist präsentationales HTML?

#### --distractors--

Präsentationales HTML steuert, wie Daten auf dem Server gespeichert werden.

---

Präsentationales HTML bestimmt das gesamte Layout und die Navigationsstruktur der Webseite.

---

Präsentationales HTML wird verwendet, um Multimedia-Inhalte einzubetten.

#### --answer--

Präsentationales HTML verwendet Elemente, um das Aussehen von Inhalten zu steuern, nicht deren Bedeutung oder Struktur.

### --question--

#### --text--

Welche Rolle hat das `pre`-Element?

#### --distractors--

Es wird verwendet, um einen Platzhalter innerhalb einer Web-Komponente darzustellen.

---

Es wird verwendet, um einen Indikator anzuzeigen, der den Fortschritt einer Aufgabe zeigt.

---

Es wird verwendet, um Ernsthaftigkeit oder starke Wichtigkeit auszudrücken.

#### --answer--

Es wird verwendet, um vorformatierten Text darzustellen.

### --question--

#### --text--

Welche Rolle hat das `h1`-Element?

#### --distractors--

Es wird verwendet, um mehrere Überschriftenelemente auf der Seite zu gruppieren.

---

Es dient als Container für die Metadaten im HTML-Dokument.

---

Es stellt das Wurzel- oder Hauptelement des HTML-Dokuments dar.

#### --answer--

Es definiert die Hauptüberschrift einer Webseite und zeigt ihr primäres Thema an.

### --question--

#### --text--

Welche Rolle hat das `address`-Element?

#### --distractors--

Es gibt die Schriftgröße für den Hauptinhalt an.

---

Es erstellt ein Navigationsmenü für die Webseite.

---

Es formatiert Text als Hochgestellt.

#### --answer--

Es wird verwendet, um Kontaktinformationen für einen Abschnitt auf einer Webseite darzustellen.

### --question--

#### --text--

Welches der folgenden Elemente hat keine semantische Bedeutung?

#### --distractors--

`article`

---

`main`

---

`footer`

#### --answer--

`div`

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um einen bestimmten Zeitpunkt darzustellen?

#### --distractors--

`output`

---

`ins`

---

`div`

#### --answer--

`time`

### --question--

#### --text--

Was ist ein typischer Anwendungsfall des `ruby`-Elements?

#### --distractors--

Es gibt die Zeichencodierung des Dokuments an.

---

Es fügt eine Tabelle zum Anzeigen tabellarischer Daten ein.

---

Es bettet einen Video- oder Audioplayer in das Dokument ein.

#### --answer--

Es wird verwendet, um die Aussprache ostasiatischer Schriftzeichen anzuzeigen.

### --question--

#### --text--

Welches Element wird verwendet, um Schlüsselwörter in Zusammenfassungen oder Produktnamen in Bewertungen hervorzuheben?

#### --distractors--

`ruby`

---

`table`

---

`template`

#### --answer--

`b`

### --question--

#### --text--

Welche Rolle hat das `u`-Element?

#### --distractors--

Es stylt Text mit benutzerdefinierten Schriftarten.

---

Es gibt die Zeichencodierung des Dokuments an.

---

Es erstellt ein Dropdown-Menü zur Auswahl von Optionen.

#### --answer--

Es wird verwendet, um inline-Text darzustellen, der eine nicht-textuelle Anmerkung enthält.

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um ein erweitertes Zitat darzustellen?

#### --distractors--

`textarea`

---

`sup`

---

`article`

#### --answer--

`blockquote`

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um eine Beschreibung oder Definition für einen Begriff in einer Definitionsliste bereitzustellen?

#### --distractors--

`dt`

---

`dl`

---

`div`

#### --answer--

`dd`

### --question--

#### --text--

Welches der folgenden Elemente wird typischerweise mit dem `code`-Element verwendet?

#### --distractors--

`ins`

---

`summary`

---

`div`

#### --answer--

`pre`

### --question--

#### --text--

Welches der folgenden Attribute wird verwendet, um Daten und Zeiten in ein maschinenlesbares Format zu übersetzen?

#### --distractors--

`alt`

---

`rel`

---

`accept`

#### --answer--

`datetime`

### --question--

#### --text--

Warum ist es wichtig, keine Überschriftenebenen zu überspringen?

#### --distractors--

Das Überspringen von Überschriftenebenen verbessert die Ästhetik des Webseitendesigns.

---

Das Überspringen von Überschriftenebenen lässt Text in verschiedenen Farben erscheinen.

---

Das Überspringen von Überschriftenebenen erhöht die Ladegeschwindigkeit der Seite.

#### --answer--

Das Überspringen von Überschriftenebenen kann Screenreader verwirren und die Barrierefreiheit der Inhalte durch eine gestörte logische Inhaltsstruktur beeinträchtigen.

### --question--

#### --text--

Welches der folgenden Elemente ist ein veraltetes präsentationales HTML-Element?

#### --distractors--

`div`

---

`footer`

---

`article`

#### --answer--

`font`

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um Navigationslinks darzustellen?

#### --distractors--

`figure`

---

`div`

---

`section`

#### --answer--

`nav`

### --question--

#### --text--

Welches der folgenden Elemente stellt einen Beschreibungsterm in einer Definitionsliste dar?

#### --distractors--

`dd`

---

`dfn`

---

`del`

#### --answer--

`dt`

### --question--

#### --text--

Welche Rolle hat das `i`-Element?

#### --distractors--

Es wird verwendet, um Text darzustellen, der zu Referenzzwecken markiert wurde.

---

Es wird verwendet, um eine Bildunterschrift oder Legende für Ausklappboxen bereitzustellen.

---

Es wird verwendet, um numerische Daten zu formatieren und von klein nach groß zu sortieren.

#### --answer--

Es wird verwendet, um idiomatischen Text, Fachbegriffe und taxonomische Bezeichnungen darzustellen.

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um Text hervorzuheben?

#### --distractors--

`anchor`

---

`div`

---

`optgroup`

#### --answer--

`em`

### --question--

#### --text--

Welches der folgenden Attribute wird verwendet, um die Quelle oder Referenz des zitierten Inhalts für ein Blockzitat anzugeben?

#### --distractors--

`alt`

---

`class`

---

`lang`

#### --answer--

`cite`

## --quiz--

### --question--

#### --text--

Welches der folgenden ist KEIN Beispiel für ein präsentationales HTML-Element?

#### --distractors--

`font`

---

`center`

---

`big`

#### --answer--

`article`

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um eine einzelne Codezeile in HTML darzustellen?

#### --distractors--

`codes`

---

`codecs`

---

`coding`

#### --answer--

`code`

### --question--

#### --text--

Was ist der Unterschied zwischen den `strong`- und `b`-Elementen?

#### --distractors--

Das `b`-Element wird verwendet, um die Wichtigkeit des Textes zu betonen, während das `strong`-Element alternative Stimme oder Stimmung hervorhebt.

---

Das `strong`-Element wird verwendet, um alternative Stimme oder Stimmung hervorzuheben, während das `b`-Element Text betont, der entscheidend oder dringend ist.

---

Das `strong`-Element wird häufig verwendet, um Schlüsselwörter in Zusammenfassungen hervorzuheben, während das `b`-Element Text betont, der entscheidend oder dringend ist.

#### --answer--

Das `b`-Element wird häufig verwendet, um Schlüsselwörter in Zusammenfassungen hervorzuheben, während das `strong`-Element Text betont, der entscheidend oder dringend ist.

### --question--

#### --text--

Wie verwendet man korrekt ein Element, um einen Textabschnitt zu betonen?

#### --distractors--

```html
<p>
  Never give up on <emphasis>your</emphasis> dreams.
</p>
```

---

```html
<p>
  Never give up on <emphasize>your</emphasize> dreams.
</p>
```

---

```html
<p>
  Never give up on <e>your</e> dreams.
</p>
```

#### --answer--

```html
<p>
  Never give up on <em>your</em> dreams.
</p>
```

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um Aufmerksamkeit auf Text zu lenken, der für die Bedeutung des Inhalts nicht wichtig ist?

#### --distractors--

`em`

---

`small`

---

`strong`

#### --answer--

`b`

### --question--

#### --text--

Wann sollten Sie CSS anstelle der `i`- oder `em`-Elemente verwenden?

#### --distractors--

Wenn der Text einen besonderen Zweck oder eine besondere Bedeutung im Absatz hat.

---

Wenn der Text eine idiomatische Wendung ist.

---

Wenn der Text zur Betonung der Wichtigkeit hervorgehoben werden muss.

#### --answer--

Wenn Sie Text nur aus präsentationalen Gründen kursiv darstellen möchten.

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um einen Bereich von inline-Text darzustellen, der eine nicht-textuelle Anmerkung enthält?

#### --distractors--

`sub`

---

`span`

---

`s`

#### --answer--

`u`

### --question--

#### --text--

Wie stellt man korrekt einen Abschnitt dar, der aus einer anderen Quelle zitiert wurde?

#### --distractors--

```html
<cite cite="https://www.freecodecamp.org/news/learn-to-code-book/">
  "Can you imagine what it would be like to be a successful developer? To have built software systems that people rely upon?"
</cite>
```

---

```html
<block cite="https://www.freecodecamp.org/news/learn-to-code-book/">
  "Can you imagine what it would be like to be a successful developer? To have built software systems that people rely upon?"
</block>
```

---

```html
<quotes cite="https://www.freecodecamp.org/news/learn-to-code-book/">
  "Can you imagine what it would be like to be a successful developer? To have built software systems that people rely upon?"
</quotes>
```

#### --answer--

```html
<blockquote cite="https://www.freecodecamp.org/news/learn-to-code-book/">
  "Can you imagine what it would be like to be a successful developer? To have built software systems that people rely upon?"
</blockquote>
```

### --question--

#### --text--

Welches Element wird verwendet, um den Titel eines referenzierten kreativen Werks wie eines Buchs zu markieren?

#### --distractors--

`figure`

---

`article`

---

`div`

#### --answer--

`cite`

### --question--

#### --text--

Welche Rolle hat das `q`-Element?

#### --distractors--

Es stellt einen Ausschnitt von Computercode dar.

---

Es stellt hochgestellten Text dar.

---

Es stellt ein Datum und/oder eine Uhrzeit dar.

#### --answer--

Es stellt ein kurzes inline-Zitat dar.

### --question--

#### --text--

Was wird verwendet, um Abkürzungen und Akronyme in HTML anzuzeigen?

#### --distractors--

```html
<p><acronym>HTML</acronym> is the foundation of the web.</p>
```

---

```html
<p><acr>HTML</acr> is the foundation of the web.</p>
```

---

```html
<p><abbreviation>HTML</abbreviation> is the foundation of the web.</p>
```

#### --answer--

```html
<p><abbr>HTML</abbr> is the foundation of the web.</p>
```

### --question--

#### --text--

Welche Rolle hat das `title`-Attribut innerhalb des Abkürzungselements?

#### --distractors--

Es wird verwendet, um tiefgestellten Text darzustellen.

---

Es wird verwendet, um einen Bereich von inline-Text darzustellen, der eine nicht-textuelle Anmerkung enthält.

---

Es wird verwendet, um Text mit Aussprache- oder Bedeutungs-Erklärungen zu versehen.

#### --answer--

Es hilft Benutzern zu verstehen, was dieses Akronym bedeutet.

### --question--

#### --text--

Welches der folgenden Elemente wird verwendet, um Kontaktinformationen auf der Webseite darzustellen?

#### --distractors--

`location`

---

`residence`

---

`contact`

#### --answer--

`address`

### --question--

#### --text--

Welche Rolle hat das `datetime`-Attribut im `time`-Element?

#### --distractors--

Es wird verwendet, um eine Warnung anzuzeigen, die das aktuelle Datum und die Uhrzeit zeigt.

---

Es wird verwendet, um die Zeit im 12-Stunden-Format einzustellen.

---

Es wird verwendet, um die Zeit im Militärformat einzustellen.

#### --answer--

Es wird verwendet, um Daten und Zeiten in einem maschinenlesbaren Format darzustellen.

### --question--

#### --text--

Welches der folgenden wird verwendet, um einen Textabschnitt als hochgestellt darzustellen?

#### --distractors--

```html
<p>2<superscript>2</superscript> (2 squared) is 4.</p>
```

---

```html
<p>2<s>2</s> (2 squared) is 4.</p>
```

---

```html
<p>2<super>2</super> (2 squared) is 4.</p>
```

#### --answer--

```html
<p>2<sup>2</sup> (2 squared) is 4.</p>
```

### --question--

#### --text--

Welches der folgenden ist KEIN häufiger Anwendungsfall für das Verwenden des tiefgestellten (`sub`)-Elements?

#### --distractors--

Variablen-Indizes

---

Fußnoten

---

Chemische Formeln

#### --answer--

Datumsangaben anzeigen

### --question--

#### --text--

Welche Rolle hat das `s`-Element?

#### --distractors--

Es wird verwendet, um einen Textabschnitt mit einer angehobenen Grundlinie zu stylen.

---

Es wird verwendet, um Text für die Ruby-Anmerkung zu kennzeichnen.

---

Es wird verwendet, um inline-Text darzustellen, der eine nicht-textuelle Anmerkung enthält.

#### --answer--

Es wird verwendet, um darzustellen, wenn Text nicht mehr genau oder relevant ist.

### --question--

#### --text--

Welches der folgenden ist KEIN häufiger Anwendungsfall für das Verwenden des hochgestellten Elements?

#### --distractors--

Exponenten

---

Hochgestellte Buchstaben

---

Ordnungszahlen

#### --answer--

Chemische Formeln

### --question--

#### --text--

Welche Rolle hat das `ruby`-Element?

#### --distractors--

Es stellt eine Liste von Begriff-Beschreibungs-Gruppierungen dar.

---

Es stellt den Begriff dar, der in einer Definitionsliste definiert wird.

---

Es stellt vorformatierten Text für Codebeispiele dar.

#### --answer--

Es stellt kleinen Text dar, der über oder unter dem Haupttext angezeigt wird.

### --question--

#### --text--

Welches der folgenden Elemente wird als Fallback für Browser verwendet, die keine Ruby-Anmerkungen unterstützen?

#### --distractors--

`rub`

---

`pr`

---

`r`

#### --answer--

`rp`

## --quiz--

### --question--

#### --text--

Was ist der Zweck des `header`-Elements in HTML?

#### --distractors--

Es definiert den Hauptinhalt des Dokuments.

---

Es stellt einen Abschnitt mit Navigationslinks dar.

---

Es definiert eine Seitenleiste für das Dokument.

#### --answer--

Es markiert die Kopfzeile eines Dokuments oder Abschnitts.

### --question--

#### --text--

Welches Element wird verwendet, um den Hauptinhalt einer Webseite zu enthalten?

#### --distractors--

`nav`

---

`article`

---

`section`

#### --answer--

`main`

### --question--

#### --text--

Wie wird das `nav`-Element korrekt verwendet?

#### --distractors--

Um die Kopfzeile eines Dokuments zu definieren.

---

Um Metadaten über eine Webseite anzuzeigen.

---

Um den Hauptinhalt der Seite zu enthalten.

#### --answer--

Um einen Abschnitt mit Navigationslinks darzustellen.

### --question--

#### --text--

Welches Element wird verwendet, um Text mit Betonung durch Stress darzustellen?

#### --distractors--

`mark`

---

`i`

---

`b`

#### --answer--

`em`

### --question--

#### --text--

Was ist der Zweck des `figure`-Elements?

#### --distractors--

Um den Hauptinhalt der Seite zu definieren.

---

Um mehrere Artikel zusammenzufassen.

---

Um einen Abschnitt mit Navigationslinks darzustellen.

#### --answer--

Um Illustrationen und Diagramme zu enthalten.

### --question--

#### --text--

Welches Element wird verwendet, um ein kurzes inline-Zitat darzustellen?

#### --distractors--

`blockquote`

---

`span`

---

`cite`

#### --answer--

`q`

### --question--

#### --text--

Was ist der Zweck des `time`-Elements?

#### --distractors--

Um eine Skriptausführungszeit zu definieren.

---

Um den Hauptinhalt der Seite zu definieren.

---

Um wichtigen Text hervorzuheben.

#### --answer--

Um einen Datums- oder Zeitwert darzustellen.

### --question--

#### --text--

Welches Element wird verwendet, um eine Abkürzung oder ein Akronym darzustellen?

#### --distractors--

`var`

---

`cite`

---

`code`

#### --answer--

`abbr`

### --question--

#### --text--

Was ist der Zweck des `blockquote`-Elements?

#### --distractors--

Um ein kurzes inline-Zitat darzustellen.

---

Um einen Absatz mit größerem Text zu definieren.

---

Um den Hauptinhalt der Seite zu definieren.

#### --answer--

Um einen Abschnitt darzustellen, der aus einer anderen Quelle zitiert wurde.

### --question--

#### --text--

Welches Element wird verwendet, um eine Liste von Begriff-Beschreibungs-Gruppierungen darzustellen?

#### --distractors--

`ul`

---

`table`

---

`ol`

#### --answer--

`dl`

### --question--

#### --text--

Was ist der Zweck des `cite`-Elements?

#### --distractors--

Um ein kurzes inline-Zitat darzustellen.

---

Um ein hervorgehobenes Wort darzustellen.

---

Um den Hauptinhalt der Seite zu definieren.

#### --answer--

Um die Quelle eines referenzierten Werks anzugeben.

### --question--

#### --text--

Welches Element wird verwendet, um hochgestellten Text darzustellen?

#### --distractors--

`sub`

---

`abbr`

---

`strong`

#### --answer--

`sup`

### --question--

#### --text--

Was ist der Zweck des `address`-Elements?

#### --distractors--

Um den Hauptinhalt der Seite zu definieren.

---

Um die Adresse eines Hyperlinks zu definieren.

---

Um einen Abschnitt mit Navigationslinks darzustellen.

#### --answer--

Um Kontaktinformationen für den Autor oder Eigentümer einer Webseite darzustellen.

### --question--

#### --text--

Welches Element wird verwendet, um vorformatierten Text darzustellen?

#### --distractors--

`code`

---

`samp`

---

`blockquote`

#### --answer--

`pre`

### --question--

#### --text--

Was ist der Zweck des `strong`-Elements?

#### --distractors--

Um Text mit Betonung durch Stress zu markieren.

---

Um Text kursiv zu machen.

---

Um Text hervorzuheben, der nicht wichtig ist.

#### --answer--

Um Text mit starker Wichtigkeit zu markieren.

### --question--

#### --text--

Welches Element wird verwendet, um einen Ausschnitt von Computercode darzustellen?

#### --distractors--

`pre`

---

`var`

---

`blockquote`

#### --answer--

`code`

### --question--

#### --text--

Was ist der Zweck des `abbr`-Elements?

#### --distractors--

Um einen Datums- oder Zeitwert darzustellen.

---

Um Tooltips zu Text hinzuzufügen.

---

Um ein kurzes inline-Zitat darzustellen.

#### --answer--

Um eine Abkürzung oder ein Akronym darzustellen.

### --question--

#### --text--

Welches Element wird verwendet, um ein langes Zitat aus einer anderen Quelle darzustellen?

#### --distractors--

`q`

---

`mark`

---

`cite`

#### --answer--

`blockquote`

### --question--

#### --text--

Was ist der Zweck des `dl`-Elements?

#### --distractors--

Um eine Liste von Elementen darzustellen.

---

Um eine nummerierte Liste zu definieren.

---

Um einen Abschnitt mit Navigationslinks darzustellen.

#### --answer--

Um eine Liste von Begriff-Beschreibungs-Gruppierungen darzustellen.

### --question--

#### --text--

Welches Element wird verwendet, um tiefgestellten Text darzustellen?

#### --distractors--

`sup`

---

`small`

---

`strong`

#### --answer--

`sub`
