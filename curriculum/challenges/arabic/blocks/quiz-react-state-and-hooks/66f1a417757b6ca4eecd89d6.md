---
id: 66f1a417757b6ca4eecd89d6
title: اختبار حالة React والدوال المساعدة
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

لتجاوز الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي من الطرق التالية هي الصحيحة لإضافة حدث نقر إلى زر في React؟

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

ما هو حدث React الاصطناعي (Synthetic Event)؟

#### --distractors--

نوع من عناصر JSX مخصص فقط لأحداث `inputs` و `submit`.

---

نوع من عناصر JSX مخصص فقط لأحداث `submit`.

---

طريقة لتعديل مرحلة الفقاعات في انتشار الحدث لتكون أكثر سهولة للمستخدم.

#### --answer--

غلاف حول الأحداث الأصلية مثل أحداث `click` و `submit`.

### --question--

#### --text--

أي من البادئات التالية شائعة الاستخدام لتسمية دوال معالِج الأحداث؟

#### --distractors--

isHandling

---

hasHandled

---

handling

#### --answer--

handle

### --question--

#### --text--

ما هي المراحل الثلاث الرئيسية في عرض React؟

#### --distractors--

المصافحة، التحميل، والترطيب.

---

الجلب، فك التشفير، والتنفيذ.

---

التركيب، التحديث، والإزالة.

#### --answer--

التشغيل، العرض، والتثبيت.

### --question--

#### --text--

أي من الطرق التالية هي الصحيحة للعمل مع دالة `useState`؟

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

أي من الخصائص التالية ليست صالحة للتعامل مع أحداث React؟

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

أي من الطرق التالية هي الصحيحة لتحديث حالة مصفوفة؟

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--

ما الخطأ في هذه الدالة؟

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

لا يُسمح بتحديث متغير `useState` داخل دالة مسكنة.

---

يجب إحاطة `previousState` بأقواس.

---

لا يجب استخدام الدوال السهمية داخل دالة تعيين الحالة.

#### --answer--

يجب نشر `previousState` قبل `name` لضمان عدم استبدال أي خصائص أخرى.

### --question--

#### --text--

أي من الطرق التالية هي الصحيحة لتحديث الحالة لإزالة عناصر من مصفوفة؟

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

ما هو الاستخدام النموذجي لخاصية `ref` في React؟

#### --distractors--

تُستخدم لتدقيق مشاكل الأداء في مكونات React.

---

تُستخدم لتدقيق مشاكل إمكانية الوصول في مكونات React.

---

تُستخدم لإزالة عقد DOM.

#### --answer--

تُستخدم للوصول إلى عقدة DOM.

### --question--

#### --text--

أي من الطرق التالية هي الصحيحة للوصول إلى القيمة الحالية لـ `ref`؟

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

كم مرة سيتم تسجيل هذه الرسالة في وحدة التحكم؟

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

ثلاث مرات.

---

مرتين.

---

لا شيء لأن مصفوفة التبعيات فارغة.

#### --answer--

مرة واحدة فقط عند العرض الأولي.

### --question--

#### --text--

ما هو الاستخدام النموذجي للدوال المساعدة المخصصة في React؟

#### --distractors--

تُستخدم لتنفيذ أكثر وظائف التطبيق تعقيدًا.

---

لا تُستخدم عادة، ويفضل استخدام الدوال المدمجة.

---

تحل محل طرق دورة الحياة من إصدارات React السابقة.

#### --answer--

تساعد في إعادة استخدام المنطق ذو الحالة عبر المكونات.

### --question--

#### --text--

كم مرة سيتم تسجيل هذه الرسالة في وحدة التحكم؟

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

سيتم تسجيلها في كل مرة يتم فيها إرسال نموذج أو حدوث حدث نقر.

---

سيتم تسجيلها مرتين.

---

سيتم تسجيلها مرة واحدة فقط.

#### --answer--

سيتم تسجيلها في كل مرة يتم فيها عرض المكوِّن.

### --question--

#### --text--

أي من الطرق التالية هي الصحيحة لمنع تحديث المتصفح عند حدث `onSubmit`؟

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

أي من الدوال المساعدة التالية تُستخدم عادة لجلب البيانات؟

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

أي من الخصائص التالية ليست صالحة على كائن الحدث الأصلي؟

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

أي من مراحل العرض التالية تحدث عندما يكتشف React أن شيئًا ما قد تغير وقد يحتاج واجهة المستخدم إلى تحديث؟

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

أي من المراحل التالية تشير إلى عندما يأخذ React التغييرات المحضرة من DOM الافتراضي ويطبقها على DOM الحقيقي؟

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

ما هو الغرض من `dependencies` في دالة `useEffect`؟

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

المعلمة الاختيارية `dependencies` لا تؤثر على أي شيء يتعلق بـ `useEffect`.

---

المعلمة الاختيارية `dependencies` تجعل التأثير يعمل في حلقة لا نهائية.

---

المعلمة الاختيارية `dependencies` تُستخدم لمنع تشغيل التأثير.

#### --answer--

المعلمة الاختيارية `dependencies` تتحكم في توقيت تشغيل التأثير.

