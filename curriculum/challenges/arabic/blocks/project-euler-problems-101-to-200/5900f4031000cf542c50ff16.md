---
id: 5900f4031000cf542c50ff16
title: "المشكلة 151: أوراق الورق ذات الأحجام القياسية: مسألة القيمة المتوقعة"
challengeType: 1
forumTopicId: 301782
dashedName: problem-151-paper-sheets-of-standard-sizes-an-expected-value-problem
---

# --description--

تدير مطبعة 16 دفعة (وظيفة) كل أسبوع، وكل دفعة تحتاج إلى ورقة خاصة مقاومة للألوان بحجم A5.

كل صباح اثنين، يفتح المشرف ظرفًا جديدًا يحتوي على ورقة كبيرة من الورق الخاص بحجم A1.

يقوم بقصها إلى نصفين، فيحصل على ورقتين بحجم A2. ثم يقطع واحدة منهما إلى نصفين ليحصل على ورقتين بحجم A3، وهكذا حتى يحصل على ورقة بحجم A5 اللازمة لأول دفعة في الأسبوع.

توضع كل الأوراق غير المستخدمة مرة أخرى في الظرف.

<img alt="A1-size sheet split into: A2, A3, A4 and two A5 sheets" src="https://cdn.freecodecamp.org/curriculum/project-euler/paper-sheets-of-standard-sizes-an-expected-value-problem.png" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

في بداية كل دفعة لاحقة، يأخذ ورقة واحدة من الظرف عشوائيًا. إذا كانت بحجم A5، يستخدمها. وإذا كانت أكبر، يعيد إجراء عملية "القص إلى نصفين" حتى يحصل على ما يحتاجه، وتوضع الأوراق المتبقية دائمًا مرة أخرى في الظرف.

باستثناء الدفعة الأولى والأخيرة من الأسبوع، احسب العدد المتوقع من المرات (خلال كل أسبوع) التي يجد فيها المشرف ورقة واحدة فقط في الظرف.

أعطِ إجابتك مقربة إلى ستة أرقام عشرية باستخدام الصيغة `x.xxxxxx`.

# --hints--

يجب أن تُرجع الدالة `expectedValueProblem()` القيمة `0.464399`.

```js
assert.strictEqual(expectedValueProblem(), 0.464399);
```

# --seed--

## --seed-contents--

```js
function expectedValueProblem() {

  return true;
}

expectedValueProblem();
```

# --solutions--

```js
// solution required
```
