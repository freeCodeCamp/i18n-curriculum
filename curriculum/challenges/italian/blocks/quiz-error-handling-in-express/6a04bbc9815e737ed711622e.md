---
id: 6a04bbc9815e737ed711622e
title: Quiz sulla gestione degli errori in Express
challengeType: 8
dashedName: quiz-error-handling-in-express
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cosa significa il codice di stato HTTP 201?

#### --distractors--

La richiesta è andata a buon fine senza corpo della risposta.

---

Il client non è autorizzato a consultare la risorsa.

---

La risorsa richiesta non esiste.

#### --answer--

Una nuova risorsa è stata creata con successo.

### --question--

#### --text--

Quale codice di stato indica che la richiesta è andata a buon fine ma non c'è un corpo della risposta?

#### --distractors--

`200`

---

`201`

---

`301`

#### --answer--

`204`

### --question--

#### --text--

Cosa comunica al client il codice di stato `304 Not Modified`?

#### --distractors--

La risorsa è stata spostata a un nuovo URL permanente.

---

Il server è temporaneamente non disponibile per manutenzione.

---

È richiesta l'autenticazione per consultare la risorsa.

#### --answer--

Non è cambiato nulla, quindi il client può usare la sua versione in cache.

### --question--

#### --text--

Qual è la differenza tra i codici di stato `401` e `403`?

#### --distractors--

`401` significa che la risorsa non è stata trovata mentre `403` significa che il server è inattivo.

---

`401` significa che la richiesta era malformata mentre `403` significa che la risorsa si è spostata.

---

`401` significa che il server è andato in crash mentre `403` significa che la richiesta è scaduta.

#### --answer--

`401` significa che è richiesta l'autenticazione mentre `403` significa che l'utente è autenticato ma non ha il permesso.

### --question--

#### --text--

Quale codice di stato indica che il server è temporaneamente sovraccarico o inattivo per manutenzione?

#### --distractors--

`500`

---

`502`

---

`301`

#### --answer--

`503`

### --question--

#### --text--

Cosa fa `res.status(200).json({ message: "Success" })` in Express?

#### --distractors--

Invia una risposta `200` con un messaggio in testo semplice.

---

Invia una risposta `200` senza corpo.

---

Reindirizza il client alla pagina di successo.

#### --answer--

Invia una risposta `200` con un corpo JSON.

### --question--

#### --text--

Cosa fa riconoscere a Express una funzione middleware come gestore di errori?

#### --distractors--

È posizionata in cima allo stack dei middleware.

---

Usa `res.error()` invece di `res.send()`.

---

È definita all'interno di un gestore di rotta.

#### --answer--

Ha i parametri `err`, `req`, `res` e `next`.

### --question--

#### --text--

Dove dovrebbe essere posizionato il middleware per la gestione degli errori in un'app Express?

#### --distractors--

Prima di tutte le definizioni di rotta.

---

All'inizio del file, prima di qualsiasi importazione.

---

All'interno del primo gestore di rotta che potrebbe lanciare un errore.

#### --answer--

Alla fine dello stack dei middleware, dopo tutte le rotte.

### --question--

#### --text--

In Express 4, cosa devi fare per gestire gli errori asincroni in un gestore di rotta?

#### --distractors--

Lanciare l'errore direttamente dal gestore di rotta e lasciare che Node.js lo gestisca a livello di processo.

---

Restituire l'oggetto errore come corpo della risposta JSON con codice di stato 500.

---

Usare `process.on('uncaughtException')` per catturare globalmente tutti gli errori asincroni non gestiti.

#### --answer--

Includere la logica asincrona in `try/catch` e chiamare `next(err)` per inoltrare l'errore al gestore di errori.

### --question--

#### --text--

Qual è lo scopo di una funzione wrapper `asyncHandler` in Express 4?

#### --distractors--

Converte automaticamente i gestori di rotta sincroni in asincroni.

---

Riavvia automaticamente il server e cancella lo stato quando si verifica un errore.

---

Sostituisce il middleware per la gestione degli errori posizionato alla fine dello stack dei middleware.

#### --answer--

Evita di ripetere `try/catch` in ogni rotta inoltrando le promise rifiutate a `next`.

### --question--

#### --text--

Come gestisce Express 5 gli errori asincroni in modo diverso rispetto a Express 4?

#### --distractors--

Express 5 richiede di aggiungere un listener globale `process.on('unhandledRejection')` per catturare gli errori asincroni.

---

Express 5 ignora silenziosamente gli errori asincroni e li registra solo in console senza fermare l'app.

---

Express 5 richiede che ogni rotta asincrona definisca il proprio middleware dedicato per la gestione degli errori.

#### --answer--

Express 5 inoltra automaticamente gli errori asincroni e le promise rifiutate al gestore di errori senza bisogno di `next(err)`.

### --question--

#### --text--

Dove dovrebbe essere posizionato un middleware `404` catch-all in un'app Express?

#### --distractors--

Prima di tutte le altre rotte, in cima al file.

---

All'interno del middleware per la gestione degli errori, come controllo fallback.

---

Alla fine, dopo il middleware per la gestione degli errori.

#### --answer--

Dopo tutte le rotte ma prima del middleware per la gestione degli errori.

### --question--

#### --text--

Quale delle seguenti è una buona pratica per la gestione degli errori nelle app Express in produzione?

#### --distractors--

Inviare sempre la traccia completa dello stack al client per facilitare il debugging.

---

Posizionare il middleware per la gestione degli errori prima delle rotte per una elaborazione più veloce.

---

Gestire tutti gli errori, inclusi i 404, all'interno di un unico middleware.

#### --answer--

Registrare internamente i dettagli degli errori e restituire un messaggio amichevole all'utente invece di inviare tracce dello stack.

### --question--

#### --text--

Come abiliti tutti i messaggi di debug integrati di Express all'avvio della tua app?

#### --distractors--

Passare `{ debug: true }` al costruttore `express()`.

---

Chiamare `app.enable('debug')` prima di definire le rotte.

---

Aggiungere `require('express-debug')` in cima al file di ingresso.

#### --answer--

Impostare la variabile d'ambiente `DEBUG` su `express:*` quando si esegue l'app.

### --question--

#### --text--

Cosa fa `DEBUG=express:router node index.js`?

#### --distractors--

Abilita tutto l'output di debug di Express, inclusi middleware, routing ed eventi a livello di applicazione.

---

Disabilita completamente l'output di debug e fa girare l'app in modalità silenziosa di produzione.

---

Registra solo i dettagli delle richieste HTTP in entrata come metodo e URL nel terminale.

#### --answer--

Mostra solo l'output di debug relativo al router, filtrando gli altri namespace di Express.

### --question--

#### --text--

A cosa serve `morgan` in un'applicazione Express?

#### --distractors--

Catturare automaticamente e inoltrare gli errori asincroni nei gestori di rotta Express a `next`.

---

Servire file statici da una directory specificata senza definire rotte individuali.

---

Collegare Express a un database relazionale e registrare tutte le query eseguite.

#### --answer--

Registrare le richieste HTTP con dettagli come metodo, URL, codice di stato e tempo di risposta.

### --question--

#### --text--

Qual è lo scopo di una rotta di controllo dello stato di salute in un'app Express?

#### --distractors--

Riavviare automaticamente il server e reindirizzare il traffico quando il volume delle richieste in entrata è troppo alto.

---

Registrare e inoltrare tutte le richieste HTTP in entrata a un servizio esterno di monitoraggio o analisi.

---

Ispezionare e bloccare le richieste da indirizzi IP non presenti in una lista di permessi approvata.

#### --answer--

Segnalare se l'app sta funzionando correttamente così che bilanciatori di carico e orchestratori possano decidere se il servizio è sano.

### --question--

#### --text--

Cosa indica il segnale `SIGTERM` a un processo Node.js?

#### --distractors--

Riavvia immediatamente, cancella tutto lo stato in memoria e re-inizializza tutte le connessioni attive.

---

Scarica un'istantanea completa della memoria su disco per analisi e debugging successivi.

---

Termina forzatamente tutte le connessioni attive al database e chiude subito tutti i descrittori di file aperti.

#### --answer--

Inizia la chiusura, dando all'app la possibilità di completare il lavoro esistente prima di uscire.

### --question--

#### --text--

Cosa succede chiamando `server.close()` durante una chiusura graduale?

#### --distractors--

Termina immediatamente tutte le connessioni attive ed esce dal processo.

---

Invia una risposta 500 a tutti i client attualmente connessi.

---

Elimina tutti i dati di sessione in memoria e riavvia il server.

#### --answer--

Smette di accettare nuove connessioni permettendo a quelle esistenti di terminare.

### --question--

#### --text--

Perché restituiresti un codice 503 dalla tua rotta di controllo dello stato di salute durante una chiusura graduale?

#### --distractors--

Per registrare l'evento di chiusura e il timestamp su una dashboard di monitoraggio esterna a scopo di audit.

---

Per forzare l'orchestratore a far partire immediatamente una nuova istanza sostitutiva prima che la chiusura sia completata.

---

Per notificare a tutti i client attualmente connessi che le loro sessioni attive sono state salvate con successo.

#### --answer--

Per segnalare al bilanciatore di carico che l'app non è disponibile così da fermare il reindirizzamento di nuovo traffico verso di essa.
