---
id: 66edc47c11492ac5cf258ad9
title: JavaScript の比較と条件分岐クイズ
challengeType: 8
dashedName: quiz-javascript-comparisons-and-conditionals
---

# --description--

このクイズに合格するには、以下の10問のうち少なくとも9問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

式 `undefined > 0` の結果は何ですか？

#### --distractors--

`true`

---

`undefined`

---

`null`

#### --answer--

`false`

### --question--

#### --text--

`||` はどの論理演算を表していますか？

#### --distractors--

XOR

---

AND

---

NOT

#### --answer--

OR

### --question--

#### --text--

次のコードの出力は何ですか？

```js
console.log(5 === 2 + 3 || 4 == 2);
```

#### --distractors--

`undefined`

---

`false`

---

エラーが発生します。

#### --answer--

`true`

### --question--

#### --text--

truthy値とfalsy値とは何ですか？

#### --distractors--

`true` と `false` の両方である値。

---

文脈によって変わる値。

---

`true` または `false` の値とは異なる動作をする値。

#### --answer--

ブール値の文脈で `true` または `false` と見なされる値。

### --question--

#### --text--

以下のコードからコンソールに何が出力されますか？

```js
if (1) {
  console.log("True!");
}
```

#### --distractors--

`false`

---

コンソールには何も出力されません。

---

エラーが発生します。

#### --answer--

`True!`

### --question--

#### --text--

`undefined` と `null` の違いは何ですか？

#### --distractors--

`null` と `undefined` は範囲外のメモリ位置を指しておりアクセスできません。

---

`null` は変数に暗黙的に代入され変更できませんが、`undefined` は変数に明示的に代入されます。

---

`null` はグローバルプロパティですが、`undefined` はそうではありません。

#### --answer--

値が代入されていない変数は `undefined` であり、`null` はオブジェクト値の意図的な不在を表します。

### --question--

#### --text--

`break` を含めずに `switch` 文を実装するとどうなりますか？

#### --distractors--

`switch` 文は突然停止します。

---

`switch` 文は前の文の後にエラーをスローします。

---

`switch` 文は最初の一致後に終了します。

#### --answer--

一致が見つかっても、後続の `case` 文の評価が続行されます。

### --question--

#### --text--

以下のコードからコンソールに何が出力されますか？

```js
const a = 2;
if (1 == "1") {
  let b = 3;
  console.log(a + b);
}
console.log(b);
```

#### --distractors--

エラーが発生します。

---

```js
5
3
```

---

```js
5
undefined
```

#### --answer--

`5`、その後エラーが発生します。

### --question--

#### --text--

次のJavaScriptコードの出力は何ですか？

```js
let vehicle = "car";

switch (vehicle) {
  case "bike":
    console.log("Bikes are two-wheelers.");
    break;
  case "car":
    console.log("Some cars are 4x4.");
  case "truck":
    console.log("Trucks can carry heavy loads.");
    break;
  default:
    console.log("Unknown vehicle.");
}
```

#### --distractors--

```md
Some cars are 4x4.
```

---

```md
Some cars are 4x4.
Trucks can carry heavy loads.
Unknown vehicle.
```

---

```md
Unknown vehicle.
```

#### --answer--

```md
Some cars are 4x4.
Trucks can carry heavy loads.
```

### --question--

#### --text--

以下のコードでコンソールに何が出力されますか？

```js
let x = 5;
if (x > 1 && x < 10) {
  console.log("x is between 1 and 10");
} else {
  console.log("x is not between 1 and 10");
}
```

#### --distractors--

エラーが発生します。

---

何も出力されません。

---

`x is not between 1 and 10`

#### --answer--

`x is between 1 and 10`
