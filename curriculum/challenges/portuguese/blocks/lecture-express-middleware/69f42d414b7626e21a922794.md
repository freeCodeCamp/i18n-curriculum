---
id: 69f42d414b7626e21a922794
title: O que é middleware para tratamento de erros?
challengeType: 19
dashedName: what-is-error-handling-middleware
---

# --description--

Middleware para tratamento de erros no Express é um tipo especial de middleware usado para lidar com erros que ocorrem durante o ciclo de requisição-resposta. Ele é essencial para capturar erros inesperados, registrá-los e fornecer respostas apropriadas ao cliente.

Como você viu em uma lição anterior sobre tratamento de erros, o middleware de tratamento de erros do Express tem quatro parâmetros: `err`, `req`, `res` e `next`. Esse middleware é invocado quando um erro é passado para a função `next()`.

Aqui está um lembrete da sintaxe mais uma vez:

```js
app.use((err, req, res, next) => {
  console.error(err.message);
  res.status(500).send("Internal Server Error");
});
```

E aqui está como isso fica em um app básico do Express:

```js
const express = require('express')
const app = express()

// A simple route that throws an error
app.get('/', (req, res) => {
  throw new Error('Something went wrong!')
})

// Error-handling middleware
app.use((err, req, res, next) => {
  console.error(err.stack); // Logs the error stack
  res.status(500).send('Something went wrong!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* Manipulador de rota: o manipulador de rota lança intencionalmente um erro para simular um problema.
* Middleware para tratamento de erros: esse middleware é registrado com `app.use()`. Ele captura erros passados via `next(err)` e envia uma resposta HTTP 500 para o cliente com uma mensagem genérica de erro.
* `err`: este é o objeto de erro que é passado para o middleware.

O middleware para tratamento de erros garante que, quando algo dá errado na sua aplicação, os usuários ainda recebam uma resposta adequada. É importante sempre incluir esse middleware no final da sua pilha de middleware para capturar quaisquer erros.

# --questions--

## --text--

O que o middleware para tratamento de erros faz?

## --answers--

Ele processa requisições e envia respostas.

### --feedback--

Concentre-se no que acontece quando algo dá errado durante uma requisição.

---

Ele lida com erros que ocorrem durante o ciclo de requisição-resposta.

---

Ele serve arquivos estáticos.

### --feedback--

Concentre-se no que acontece quando algo dá errado durante uma requisição.

---

Ele lida com a lógica de rotas.

### --feedback--

Concentre-se no que acontece quando algo dá errado durante uma requisição.

## --video-solution--

2

## --text--

Quais dos seguintes parâmetros são obrigatórios no middleware para tratamento de erros?

## --answers--

`err`, `req`, `res`, `next`.

---

`req` e `res`.

### --feedback--

Pense nos parâmetros que o Express usa para reconhecer manipuladores de erro.

---

`err` e `next`.

### --feedback--

Pense nos parâmetros que o Express usa para reconhecer manipuladores de erro.

---

`err` e `req`.

### --feedback--

Pense nos parâmetros que o Express usa para reconhecer manipuladores de erro.

## --video-solution--

1

## --text--

Onde o middleware para tratamento de erros deve ser colocado na pilha de middleware?

## --answers--

No topo, antes dos outros middleware.

### --feedback--

Pense em onde os manipuladores de erro são colocados em código que pode falhar.

---

No meio da pilha.

### --feedback--

Pense em onde os manipuladores de erro são colocados em código que pode falhar.

---

Na parte inferior, depois dos outros middleware.

---

Não há um lugar específico para ele.

### --feedback--

Pense em onde os manipuladores de erro são colocados em código que pode falhar.

## --video-solution--

3
