---
id: 66edcd0ecb4b25dc64a34804
title: JavaScriptオブジェクトクイズ
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

次のうち、JavaScriptのオブジェクトを説明していないものはどれですか？

#### --distractors--

キーと値のペアの集合。

---

プロパティとメソッドのコンテナ。

---

非プリミティブなデータ型。

#### --answer--

プリミティブなデータ型。

### --question--

#### --text--

次のうち無効なオブジェクト定義はどれですか？

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

この`job`オブジェクトの`person`プロパティにどうやってアクセスしますか？

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

以下の`street`オブジェクトの`person`プロパティにどうやってアクセスしますか？

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

次のうち、ブラケット記法でのみアクセスできるプロパティキーはどれですか？

#### --distractors--

既知で静的なプロパティキー。

---

複数レベルにネストされたプロパティキー。

---

オブジェクトに存在しない可能性のあるプロパティキー。

#### --answer--

動的なプロパティキー。

### --question--

#### --text--

以下の`name`オブジェクトの`person`プロパティに分割代入で正しくアクセスしているのはどれですか？

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

以下の`flour`オブジェクトの`recipe`プロパティに分割代入で正しくアクセスしているのはどれですか？

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

JavaScriptで分割代入を使うときに、プロパティキーにデフォルト値を代入するにはどうしますか？

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

`car`オブジェクトが`year`プロパティを持っているかどうかをどうやって確認しますか？

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

オブジェクトメソッドとは何ですか？

#### --distractors--

オブジェクトに関連付けられた文字列。

---

オブジェクトに関連付けられた配列。

---

オブジェクトに関連付けられた数値。

#### --answer--

オブジェクトに関連付けられた関数。

### --question--

#### --text--

この`add`オブジェクトの`calculator`メソッドを呼び出して、`10`と`7`の数値を足すにはどうしますか？

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

オブジェクトからプロパティを削除する演算子はどれですか？

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

`score`プロパティが`player`オブジェクトに存在するかどうかをどうやって確認しますか？

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

JavaScriptで新しい空のオブジェクトを作成するにはどうしますか？

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

JavaScriptでオプショナルチェーンを使う正しい構文はどれですか？

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

JavaScriptでオプショナルチェーンは何のために使いますか？

#### --distractors--

デフォルト値付きの変数を定義するために使う。

---

呼び出されるかもしれないオプショナルなメソッドをオブジェクト内に定義するために使う。

---

複数のメソッド呼び出しを1つの文でチェーンして、より簡潔なコードを書くために使う。

#### --answer--

`null` または `undefined` である可能性のあるオブジェクトのプロパティに安全にアクセスするために使う。

### --question--

#### --text--

JavaScriptオブジェクトをJSON文字列に変換するメソッドは何ですか？

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

JSON文字列をJavaScriptオブジェクトに戻すメソッドは何ですか？

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

次のうち、ブラケット記法で`color`オブジェクトの`toy`プロパティにアクセスしているのはどれですか？

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

この`home`オブジェクトの`learner`電話番号にドット記法でアクセスするにはどうしますか？

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`
