---
id: 67f41242431cbf3db8ca79c7
title: Python 기초 퀴즈
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

다음 중 변수의 데이터 타입이 특정 타입과 일치하는지 확인하는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

다음 중 문자열 연결 방식이 아닌 것은 무엇인가요?

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

다음 중 문자열의 문자 수를 반환하는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

이 예제에서 `result`의 값은 무엇일까요?

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

부동소수점 숫자와 정수를 더하면 어떻게 되나요?

#### --distractors--

결과는 오류 메시지가 나타납니다.

---

결과는 `None`입니다.

---

결과는 정수입니다.

#### --answer--

결과는 부동소수점 숫자입니다.

### --question--

#### --text--

다음 중 함수를 정의하는 올바른 방법은 무엇인가요?

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

콘솔에 무엇이 출력될까요?

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

변수 이름을 지을 때 다음 중 틀린 설명은 무엇인가요?

#### --distractors--

변수 이름은 숫자가 아닌 문자나 밑줄(_)로만 시작할 수 있습니다.

---

변수 이름은 영숫자(a-z, A-Z, 0-9)와 밑줄(_)만 포함할 수 있습니다.

---

변수 이름은 `if`, `class`, `def` 같은 Python 예약어일 수 없습니다.

#### --answer--

변수 이름은 최대 10자여야 하며, 그렇지 않으면 Python이 오류를 발생시킵니다.

### --question--

#### --text--

다음 중 Python에서 지원하지 않는 데이터 타입은 무엇인가요?

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

다음 중 모든 문자를 대문자로 변환한 새 문자열을 반환하는 함수는 무엇인가요?

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

사용자로부터 입력을 받는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

다음 코드의 결과는 무엇일까요?

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

`split()` 메서드는 무엇을 하나요?

#### --distractors--

이 메서드는 튜플을 하위 문자열 리스트로 분할하는 데 사용됩니다.

---

이 메서드는 부동소수점을 하위 문자열 리스트로 분할하는 데 사용됩니다.

---

이 메서드는 딕셔너리를 하위 문자열 리스트로 분할하는 데 사용됩니다.

#### --answer--

이 메서드는 문자열을 하위 문자열 리스트로 분할하는 데 사용됩니다.

### --question--

#### --text--

다음이 콘솔에 출력하는 결과는 무엇인가요?

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

다음 중 번역을 위한 1대1 문자 매핑 표를 만드는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

다음 코드의 결과는 무엇일까요?

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

문자열에서 하위 문자열이 발견되지 않을 때 `find()` 메서드는 무엇을 반환하나요?

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

다음 중 문자열에서 하위 문자열이 나타난 횟수를 세는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

Python에서 floor division은 무엇을 하나요?

#### --distractors--

이 연산자는 두 수를 곱하고 결과를 가장 가까운 정수로 올림합니다.

---

이 연산자는 부동소수점을 정수로 변환하는 데 사용됩니다.

---

이 연산자는 한 수를 다른 수의 거듭제곱으로 올리는 데 사용됩니다.

#### --answer--

이 연산자는 두 수를 나누고 결과를 가장 가까운 정수로 내림합니다.

### --question--

#### --text--

다음 중 숫자를 가장 가까운 정수로 반올림하는 데 사용되는 함수는 무엇인가요?

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`
