---
id: 691b559495c5cb5a37b9b481
title: "Zoezi la 121: Kinachojirudia Mara Nyingi Zaidi"
challengeType: 28
dashedName: challenge-121
---

# --description--

Ukitoa safu ya data ya vitu, rudisha kitu kinachojirudia mara nyingi zaidi.

- Daima kutakuwa na kitu kimoja tu kinachojirudia mara nyingi zaidi.

# --hints--

`mostFrequent(["a", "b", "a", "c"])` inapaswa kurudisha `"a"`.

```js
assert.equal(mostFrequent(["a", "b", "a", "c"]), "a");
```

`mostFrequent([2, 3, 5, 2, 6, 3, 2, 7, 2, 9])` inapaswa kurudisha `2`.

```js
assert.equal(mostFrequent([2, 3, 5, 2, 6, 3, 2, 7, 2, 9]), 2);
```

`mostFrequent([true, false, "false", "true", false])` inapaswa kurudisha `false`.

```js
assert.isFalse(mostFrequent([true, false, "false", "true", false]));
```

`mostFrequent([40, 20, 70, 30, 10, 40, 10, 50, 40, 60])` inapaswa kurudisha `40`.

```js
assert.equal(mostFrequent([40, 20, 70, 30, 10, 40, 10, 50, 40, 60]), 40);
```

# --seed--

## --seed-contents--

```js
function mostFrequent(arr) {
  return arr;
}
```

# --solutions--

```js
function mostFrequent(arr) {
  const freq = {};
  let maxCount = 0;
  let mostElem = null;

  for (const el of arr) {
    freq[el] = (freq[el] || 0) + 1;
    if (freq[el] > maxCount) {
      maxCount = freq[el];
      mostElem = el;
    }
  }

  return mostElem;
}
```
