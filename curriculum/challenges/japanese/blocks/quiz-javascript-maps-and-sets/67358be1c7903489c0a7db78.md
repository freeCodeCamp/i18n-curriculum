---
id: 67358be1c7903489c0a7db78
title: JavaScript の Map と Set に関するクイズ
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript における Set とは何ですか？

#### --distractors--

重複した値を許可するデータ構造。

---

文字列と数字のみを格納するデータ構造。

---

要素の削除を許可しないコレクション。

#### --answer--

値が一度だけしか現れないコレクション。

### --question--

#### --text--

次のうち、Maps には存在するが Sets には存在しないメソッドはどれですか？

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

Set に要素を追加するために使うメソッドは何ですか？

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

Set に特定の値が含まれているかどうかを確認するにはどうしますか？

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

Set の優れた使用例はどれですか？

#### --distractors--

データベースでキーと値のペアを管理すること。

---

アイテムの番号付きリストを作成すること。

---

ブラウザのローカルストレージに階層的なデータ構造を保存すること。

#### --answer--

配列から重複を取り除くこと。

### --question--

#### --text--

JavaScript における WeakSet とは何ですか？

#### --distractors--

プリミティブ型のみを許可する Set に似たコレクション。

---

重複した値を許可する Set に似たコレクション。

---

値の削除を防ぐ Set に似たコレクション。

#### --answer--

オブジェクトのみを格納する Set に似たコレクション。

### --question--

#### --text--

Set と WeakSet の違いの一つは何ですか？

#### --distractors--

WeakSet は数値を含むあらゆる型の値を格納できる。

---

Set は重複を許さないが、WeakSet は許す。

---

Set はガベージコレクションされるが、WeakSet はされない。

#### --answer--

Set はあらゆる型の値を保持できるが、WeakSet はオブジェクトのみを保持する。

### --question--

#### --text--

JavaScript における Map とは何ですか？

#### --distractors--

ユニークな値のみを許可するコレクション。

---

キーに文字列のみを使えるコレクション。

---

アイテムの削除を許可しないコレクション。

#### --answer--

あらゆる型のキーと値のペアのコレクション。

### --question--

#### --text--

Map にキーと値のペアを挿入するために使うメソッドは何ですか？

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

次のコードは何を出力しますか？

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

JavaScript において Map は Object とどのように異なりますか？

#### --distractors--

Map はキーに数値のみを許可する。

---

Object はあらゆる型のキーを許可し、Map は文字列のみを許可する。

---

Map は反復をサポートしないが、Object はサポートする。

#### --answer--

Map はあらゆる型のキーを許可するが、Object は文字列とシンボルのみを許可する。

### --question--

#### --text--

Map からキーと値のペアを削除するにはどうしますか？

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

Map の `size` プロパティは何を返しますか？

#### --distractors--

Map のインデックス数。

---

Map のユニークな値の数。

---

Map が使用する合計メモリ。

#### --answer--

Map のキーと値のペアの数。

### --question--

#### --text--

JavaScript における WeakMap とは何ですか？

#### --distractors--

プリミティブ値をキーに使える Map に似たコレクション。

---

アイテムの削除をブロックする Map に似たコレクション。

---

キーを自動でソートする Map に似たコレクション。

#### --answer--

キーへの弱い参照を持つ Map に似たコレクション。

### --question--

#### --text--

Map と WeakMap の違いの一つは何ですか？

#### --distractors--

Map はキーにプリミティブ型のみを許可し、WeakMap はオブジェクトのみを許可する。

---

Map はガベージコレクションされるが、WeakMap はされない。

---

WeakMap は重複したキーを許可し、Map は許可しない。

#### --answer--

Map はイテラブルだが、WeakMap はイテラブルではない。

### --question--

#### --text--

次のうち、WeakMap に存在しないメソッドはどれですか？

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

次のコードの出力は何ですか？

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

Set の値を繰り返すにはどうしますか？

#### --distractors--

`.map()` メソッドを使う。

---

`.reduce()` メソッドを使う。

---

`.filter()` メソッドを使う。

#### --answer--

`.forEach()` メソッドを使う。

### --question--

#### --text--

Set のどの2つのメソッドが、特定の Set の値を含む `SetIterator` を返しますか？

#### --distractors--

`entries()` と `values()`

---

`forEach()` と `values()`

---

`has()` と `add()`

#### --answer--

`keys()` と `values()`

### --question--

#### --text--

次のコードの出力は何ですか？

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
