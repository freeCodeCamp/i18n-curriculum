---
id: 65e18d61500d930ce8ed90a5
title: 學習變數和運算子 課程 B
challengeType: 15
dashedName: learn-variables-and-operators-lesson-b
---

# --description--

你可以將變數視為程式碼中用來儲存資料的「儲存容器」。

<img src="https://cdn.freecodecamp.org/curriculum/odin-project/variables-and-operators/top-learn-variables-and-operators.png" alt='the concept of variables in coding as boxes labeled "birthday" that store and provide access to data, specifically a date "16-09-2003"'>

變數是資料的「命名儲存」。你可以使用變數來儲存 goodies、visitors 和其他資料。

要在 JavaScript 中創建變數，請使用 `let` 關鍵字。

下面的述語創建（換句話說：宣告）一個名為 "message" 的變數：

```js
let message;
```

現在，你可以使用指定運算子 `=` 將一些資料放入其中：

```js
let message;

// store the string 'Hello' in the variable named message
message = 'Hello'; 
```

為了簡潔起見，你可以將變數宣告與指派合併成一行：

```js
let message = 'Hello';
```


# --questions--

## --text--

在 JavaScript 中使用 `let` 關鍵字的目的為何？

## --answers--

執行數學操作。

---

宣告一個不可更改的常數值。

---

宣告一個可以儲存資料、能被指定及更改的變數。

---

要創建一個函式（程式）。


## --video-solution--

3
