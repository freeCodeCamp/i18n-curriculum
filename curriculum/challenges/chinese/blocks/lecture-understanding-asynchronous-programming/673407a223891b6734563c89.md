---
id: 673407a223891b6734563c89
title: 什么是 Fetch API，常见的从网络获取的资源类型有哪些？
challengeType: 19
dashedName: what-is-the-fetch-api-and-what-are-common-types-of-resources-that-are-fetched-from-the-network
---

# --description--

Fetch API 允许网页应用发起网络请求，通常用于从服务器检索或发送数据。该 API 提供了一个 `fetch()` 方法，你可以用它来发起这些请求。让我们来看一个如何使用 `fetch` 的基本示例：

```js
fetch('https://api.example.com/data')
```

在这个例子中，我们正在对 `https://api.example.com/data` 发起一个 `GET` 请求。然后这将返回一些我们需要转换为 JSON 形式并可以在任何地方使用的数据。 

默认情况下，Fetch API 使用 `GET` 方法来检索数据。下一课将介绍这一点，以及其他常见的超文本传输协议请求方法。

现在，让我们讨论一些常见的从 `network` 获取的资源 `type`。

在我们的网页应用中，我们需要一些常见的数据，比如天气数据、职业列表数据、国家名称列表、国家代码或国家标记图标/图像。使用这些数据，我们可以使应用更具信息性和互动性。多亏了 Fetch API，我们可以从网络获取这些资源。

图像是一些经常被获取的资源。你可能会根据用户操作使用 fetch 静态或动态地装载图像，并将它们显示在你的网页应用中。

文本文件是另一种经常获取的资源类型。这可能包括配置文件、日志文件，甚至是你想在网页上显示的整个文档。

在某些分支中，你可能会获取音频或视频文件。`Fetch API` 也可以处理这些类型的资源，使你能够处理各种数据类型。

# --questions--

## --text--

JavaScript 中 Fetch API 的主要用途是什么？

## --answers--

创建新的 `HTML` 元素。

### --feedback--

考虑 fetch 允许你在与服务器相关的操作中做什么。

---

进行网络请求并检索资源。

---

操作 DOM。

### --feedback--

考虑 fetch 允许你在与服务器相关的操作中做什么。

---

在浏览器中保存数据。

### --feedback--

考虑 fetch 允许你在与服务器相关的操作中做什么。

## --video-solution--

2

## --text--

从对 API 的 fetch `request` 通常接收到什么 `type` 的 `data`？

## --answers--

PDF

### --feedback--

考虑哪种形式便于 JavaScript 处理。

---

JSON

---

MP3

### --feedback--

考虑哪种形式便于 JavaScript 处理。

---

EXE

### --feedback--

考虑哪种形式便于 JavaScript 处理。

## --video-solution--

2

## --text--

以下哪项不是从 `network` 获取的常见资源 `type`？

## --answers--

JSON 数据。

### --feedback--

考虑通常存储在服务器上并由网页应用检索的数据类型。

---

超文本标记语言 内容。

### --feedback--

考虑通常存储在服务器上并由网页应用检索的数据类型。

---

JavaScript 函数。

---

图像文件。

### --feedback--

考虑通常存储在服务器上并由网页应用检索的数据类型。

## --video-solution--

3
