---
id: 5900f3ad1000cf542c50fec0
title: 'المسألة 65: التقاربات لـ e'
challengeType: 1
forumTopicId: 302177
dashedName: problem-65-convergents-of-e
---

# --description--

يمكن كتابة الجذر التربيعي للعدد 2 على شكل كسر مستمر لا نهائي.

$\\sqrt{2} = 1 + \\dfrac{1}{2 + \\dfrac{1}{2 + \\dfrac{1}{2 + \\dfrac{1}{2 + ...}}}}$

يمكن كتابة الكسر المستمر اللا نهائي كالتالي، $\\sqrt{2} = \[1; (2)]$ حيث يشير إلى أن الرقم 2 يتكرر *إلى ما لا نهاية*. وبطريقة مماثلة، $\\sqrt{23} = \[4; (1, 3, 1, 8)]$. يتضح أن متتالية القيم الجزئية للأكسر المستمرة للجذور التربيعية توفر أفضل التقريبات النسبية. دعنا ندرس التقاربات لـ $\\sqrt{2}$.

$1 + \\dfrac{1}{2} = \\dfrac{3}{2}\\\\ 1 + \\dfrac{1}{2 + \\dfrac{1}{2}} = \\dfrac{7}{5}\\\\ 1 + \\dfrac{1}{2 + \\dfrac{1}{2 + \\dfrac{1}{2}}} = \\dfrac{17}{12}\\\\ 1 + \\dfrac{1}{2 + \\dfrac{1}{2 + \\dfrac{1}{2 + \\dfrac{1}{2}}}} = \\dfrac{41}{29}$

لذا فإن متتالية أول عشرة تقاربات لـ $\\sqrt{2}$ هي:

$1, \\dfrac{3}{2}, \\dfrac{7}{5}, \\dfrac{17}{12}, \\dfrac{41}{29}, \\dfrac{99}{70}, \\dfrac{239}{169}, \\dfrac{577}{408}, \\dfrac{1393}{985}, \\dfrac{3363}{2378}, ...$

الأمر الأكثر إثارة للدهشة هو أن الثابت الرياضي المهم، $e = \[2; 1, 2, 1, 1, 4, 1, 1, 6, 1, ... , 1, 2k, 1, ...]$. أول عشرة حدود في متتالية التقاربات لـ `e` هي:

$2, 3, \\dfrac{8}{3}, \\dfrac{11}{4}, \\dfrac{19}{7}, \\dfrac{87}{32}, \\dfrac{106}{39}, \\dfrac{193}{71}, \\dfrac{1264}{465}, \\dfrac{1457}{536}, ...$

مجموع الأرقام في البسط للتقارب العاشر هو $1 + 4 + 5 + 7 = 17$.

احسب مجموع الأرقام في البسط للتقارب الـ `n`<sup>th</sup> للكسر المستمر لـ `e`.

# --hints--

يجب أن تُرجع الدالة `convergentsOfE(10)` رقمًا.

```js
assert(typeof convergentsOfE(10) === 'number');
```

يجب أن تُرجع الدالة `convergentsOfE(10)` القيمة `17`.

```js
assert.strictEqual(convergentsOfE(10), 17);
```

يجب أن تُرجع الدالة `convergentsOfE(30)` القيمة `53`.

```js
assert.strictEqual(convergentsOfE(30), 53);
```

يجب أن تُرجع الدالة `convergentsOfE(50)` القيمة `91`.

```js
assert.strictEqual(convergentsOfE(50), 91);
```

يجب أن تُرجع الدالة `convergentsOfE(70)` القيمة `169`.

```js
assert.strictEqual(convergentsOfE(70), 169);
```

يجب أن تُرجع الدالة `convergentsOfE(100)` القيمة `272`.

```js
assert.strictEqual(convergentsOfE(100), 272);
```

# --seed--

## --seed-contents--

```js
function convergentsOfE(n) {

  return true;
}

convergentsOfE(10);
```

# --solutions--

```js
function convergentsOfE(n) {
  function sumDigits(num) {
    let sum = 0n;
    while (num > 0) {
      sum += num % 10n;
      num = num / 10n;
    }
    return parseInt(sum);
  }

  // BigInt is needed for high convergents
  let convergents = [
    [2n, 1n],
    [3n, 1n]
  ];
  const multipliers = [1n, 1n, 2n];
  for (let i = 2; i < n; i++) {
    const [secondLastConvergent, lastConvergent] = convergents;
    const [secondLastNumerator, secondLastDenominator] = secondLastConvergent;
    const [lastNumerator, lastDenominator] = lastConvergent;
    const curMultiplier = multipliers[i % 3];

    const numerator = secondLastNumerator + curMultiplier * lastNumerator;
    const denominator = secondLastDenominator + curMultiplier * lastDenominator;

    convergents = [lastConvergent, [numerator, denominator]]
    if (i % 3 === 2) {
      multipliers[2] += 2n;
    }
  }
  return sumDigits(convergents[1][0]);
}
```
