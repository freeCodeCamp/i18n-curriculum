---
id: 68ff6d2f232c60f0fa68ef86
title: ما هو Node وما هي بعض الفروقات بين بيئة تشغيل المتصفح وبيئة تشغيل Node؟
challengeType: 19
dashedName: what-is-node-and-what-are-some-differences-between-the-browser-and-node-runtime-environment
---

# --description--

حتى الآن، كنت تكتب شفرة على جانب العميل باستخدام JavaScript، وهي الشفرة التي تُنفذ على جانب العميل في تطبيق ويب، عادةً في متصفح ويب.

يمكن أن تكون JavaScript على جانب العميل قوية جدًا، لكنها تملك بعض القيود في المهام الأكثر تعقيدًا.

على سبيل المثال:

- لديها وصول محدود جدًا إلى الملفات المحلية.
- ليست مصممة حقًا للتعامل مع منطق تطبيق معقد.
- ولديها مخاوف أمنية محتملة. على سبيل المثال، لا تريد إتاحة معلومات حساسة، مثل بيانات اعتماد قاعدة البيانات، لشفرة جانب العميل.
    
تملك JavaScript على جانب العميل هذه القيود لأنها صُممت في الأصل لتعمل حصريًا على متصفحات الويب.

توفر متصفحات الويب بيئة العمل اللازمة لتشغيل شفرة JavaScript، بما في ذلك محرك JavaScript. كما توفر وصولًا إلى نموذج كائن المستند (DOM)، حتى تتمكن من الوصول إلى عناصر HTML في شفرتك.

في السابق، لم نكن نستطيع تشغيل JavaScript خارج المتصفح، لكن الآن يمكننا ذلك باستخدام Node.js.

Node.js هي بيئة تشغيل JavaScript. تُعرف رسميًا بأنها "بيئة تشغيل JavaScript مفتوحة المصدر وعبر المنصات".

لنرَ ماذا يعني هذا:

- Node.js مفتوح المصدر لأن شفرته متاحة للجمهور ويُصان من قبل مجتمع كبير من المطورين.
- هو عبر المنصات لأنه يعمل على أي نظام تشغيل، بما في ذلك Windows وmacOS وLinux.
- وهو بيئة تشغيل JavaScript لأنه يسمح لك بتشغيل شفرة JavaScript خارج المتصفح.
    
يُستخدم Node.js في مجموعة واسعة من الصناعات. يستخدمه المطورون لبناء خوادم ويب وواجهات برمجة التطبيقات التي تتعامل مع طلبات HTTP، بالإضافة إلى تطبيقات الويب والهاتف المحمول.

إذا كنت تستخدم أو تطور موقع ويب أو تطبيق ويب يتفاعل مع قاعدة بيانات أو يتعامل مع بيانات معقدة، فهناك احتمال كبير أنك ستستخدم Node.js في الخلفية في مرحلة ما.

هو فعال وقابل للتوسع، ولديه مجتمع كبير من المطورين والمُصانين حول العالم.

الآن بعد أن عرفت ما هو Node.js، لنرَ بعض الفروقات بين بيئة المتصفح وبيئة تشغيل Node.

بيئة المتصفح مصممة أساسًا لتطوير الويب على الواجهة الأمامية، لذا تُشغّل JavaScript على جانب العميل. بالمقابل، بيئة تشغيل Node مصممة أساسًا لتطوير الويب على الواجهة الخلفية، لذا تُشغّل JavaScript على جانب الخادم. وهذا يحدد أيضًا واجهات برمجة التطبيقات التي تقدمها كل منهما.

على سبيل المثال، من المتصفح يمكنك الوصول إلى واجهة برمجة تطبيقات DOM، لكن هناك قيود على الوصول إلى نظام الملفات المحلي. بالمقابل، من بيئة تشغيل Node يمكنك الوصول إلى معظم موارد النظام، بما في ذلك نظام الملفات، لكن ليس إلى DOM.

كلتا بيئتي JavaScript توفران كائنًا عالميًا. في بيئة تشغيل JavaScript في المتصفح، يُسمى هذا الكائن `window`. يوفر كائن `window` وصولًا إلى وظائف متعلقة بالمتصفح، مثل الوصول إلى طرق التلاعب بـ DOM، وإدارة الكوكيز، والتعامل مع أحداث المتصفح. في بيئة Node.js، يُسمى هذا الكائن العالمي `global`. يوفر كائن `global` وصولًا إلى وظائف خاصة بـ Node.js، مثل الوحدات المدمجة للعمل مع الملفات المحلية، والشبكات، والتفاعل مع نظام التشغيل.

أخيرًا، فرق مهم آخر هو أنه يمكنك اختيار إصدار Node.js الذي تريد استخدامه في خادمك. لكن لا يمكنك التحكم في إصدار بيئة المتصفح التي سيستخدمها زوار موقعك.

أصبح Node.js مهمًا جدًا للمطورين حول العالم. هو أداة قوية وقابلة للتوسع ومتعددة الاستخدامات، غيّرت تطوير الويب بتمكين المطورين من استخدام JavaScript لبناء كل من الواجهة الأمامية والخلفية لتطبيقات الويب الكاملة. هذا جعل عملية التطوير أكثر كفاءة لأن المطورين لا يحتاجون لتعلم لغة برمجة جديدة فقط لتطوير الواجهة الخلفية.

# --questions--

## --text--

أي مما يلي يصف Node.js بشكل أفضل؟

## --answers--

مكتبة JavaScript تُستخدم لتطوير الواجهة الأمامية.

### --feedback--

فكر في مكان وكيفية تنفيذ شفرة JavaScript عند استخدام Node.js.

---

بيئة تشغيل JavaScript تُنفذ JavaScript على جانب الخادم.

---

متصفح ويب مصمم لتشغيل تطبيقات JavaScript.

### --feedback--

فكر في مكان وكيفية تنفيذ شفرة JavaScript عند استخدام Node.js.

---

إطار عمل JavaScript لبناء واجهات المستخدم.

### --feedback--

فكر في مكان وكيفية تنفيذ شفرة JavaScript عند استخدام Node.js.

## --video-solution--

2

---

## --text--

ما هو الفرق الرئيسي بين بيئة المتصفح وبيئة تشغيل Node؟

## --answers--

المتصفحات لديها وصول مباشر إلى نظام الملفات، بينما بيئة تشغيل Node لا تملك ذلك.

### --feedback--

فكر في أدوار وغرض كل بيئة.

---

بيئة تشغيل Node تستخدم كائن window ككائن عالمي، بينما المتصفحات تستخدم الكائن global.

### --feedback--

فكر في أدوار وغرض كل بيئة.

---

المتصفحات تُستخدم أساسًا لتطوير الواجهة الأمامية، بينما بيئة تشغيل Node تُستخدم أساسًا لتطوير الواجهة الخلفية.

---

المتصفحات تتفاعل عادةً مع نظام التشغيل، بينما بيئة تشغيل Node تتفاعل مع DOM.

### --feedback--

فكر في أدوار وغرض كل بيئة.

## --video-solution--

3

---

## --text--

أي من المهام التالية يُستخدم Node.js عادةً لأدائها؟

## --answers--

التلاعب ببنية HTML لصفحة ويب.

### --feedback--

فكر في قدرات Node.js على جانب الخادم.

---

بناء خادم ويب للتعامل مع طلبات HTTP.

---

تنسيق عناصر صفحة الويب باستخدام CSS.

### --feedback--

فكر في قدرات Node.js على جانب الخادم.

---

إنشاء رسوم متحركة تفاعلية داخل متصفح ويب.

### --feedback--

فكر في قدرات Node.js على جانب الخادم.

## --video-solution--

2
