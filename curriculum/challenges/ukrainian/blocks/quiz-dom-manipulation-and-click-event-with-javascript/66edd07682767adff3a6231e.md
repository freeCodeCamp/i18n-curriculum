---
id: 66edd07682767adff3a6231e
title: "Маніпуляції з DOM і події кліку в JavaScript — тест"
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке Document Object Model?

#### --distractors--

Мова програмування, яка використовується для взаємодії з HTML-сторінкою.

---

Множина об’єктів, що містять лише числа з плаваючою комою.

---

Множина класів JavaScript, які представляють HTML-елементи.

#### --answer--

Програмний інтерфейс, який дозволяє взаємодіяти з HTML-документами.

### --question--

#### --text--

Що означає абревіатура API?

#### --distractors--

Додатковий програмний інтерфейс

---

Інтерфейс додавання програмування

---

Інший програмний інтерфейс

#### --answer--

Інтерфейс програмування застосунків

### --question--

#### --text--

Як за допомогою JavaScript знайти всі HTML-якорі без гіперпосилань?

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

Що робить властивість `textContent`?

#### --distractors--

Видаляє текстовий вміст з усіх HTML-елементів.

---

Видаляє текстовий вміст з вашого JavaScript-файлу.

---

Додає текст до всіх масивів і об’єктів у вашому JavaScript-файлі.

#### --answer--

Повертає простий текстовий вміст елемента.

### --question--

#### --text--

Що робить властивість `innerHTML`?

#### --distractors--

Це властивість `Element`, яка використовується для отримання всього текстового вмісту HTML-елемента.

---

Це властивість `Element`, яка використовується для додавання анотацій до вашого HTML.

---

Це властивість `Element`, яка використовується для запуску подій у вашому HTML.

#### --answer--

Це властивість `Element`, яка використовується для встановлення або оновлення частин HTML-розмітки.

### --question--

#### --text--

Який із наведених варіантів додасть дійсний обробник подій?

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

Який із наведених методів НЕ є дійсним для об’єкта `document`?

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()`

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

Який із наведених варіантів встановить атрибут `class` зі значенням `my-class`?

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

Що таке об’єкт `Event`?

#### --distractors--

Це спеціальний об’єкт, який використовується для видалення подій із вашого JavaScript-проєкту.

---

Він використовується для автоматичного додавання подій до ваших JavaScript-проєктів.

---

Це спеціальний об’єкт, який використовується при роботі з масивами.

#### --answer--

Це корисне навантаження, яке спрацьовує, коли користувач взаємодіє з вашою вебсторінкою певним чином.

### --question--

#### --text--

Який із наведених методів використовується для видалення обробника подій, який раніше було додано до елемента за допомогою методу `addEventListener()`?

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

Що таке рядкові обробники подій?

#### --distractors--

Спеціальні атрибути HTML-елемента, які використовуються для скасування всіх подій.

---

Спеціальні атрибути HTML-елемента, які використовуються лише для відправлення форм у вашому JavaScript-коді.

---

Спеціальні атрибути HTML-елемента, які використовуються для видалення подій.

#### --answer--

Спеціальні атрибути HTML-елемента, які використовуються для виконання JavaScript-коду, коли відбувається подія.

### --question--

#### --text--

Які дві основні категорії веб-API?

#### --distractors--

Java API та сторонні API.

---

API браузера та Rust API.

---

Системні API та сторонні API.

#### --answer--

API браузера та сторонні API.

### --question--

#### --text--

Яка подія спрацьовує, коли всі елементи HTML-сторінки були проаналізовані браузером?

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

Яка з наведених властивостей представляє рядковий стиль елемента?

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

Який із наведених способів є правильним для використання рядкового обробника подій?

#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--

Який із наведених методів використовується для затримки дії на певний час?

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

Що можна використати для створення та керування анімаціями безпосередньо в JavaScript?

#### --distractors--

Allow Animations API

---

Control Animations API

---

Create Animations API

#### --answer--

Web Animations API

### --question--

#### --text--

Що робить Canvas API?

#### --distractors--

Він використовується для анімації графіки у вашому CSS-файлі.

---

Він використовується лише для видалення графіки з вашого JavaScript-файлу.

---

Він використовується для видалення графіки з вашого HTML-файлу.

#### --answer--

Він використовується для маніпуляції графікою через ваш JavaScript-файл.

### --question--

#### --text--

Що таке делегування подій?

#### --distractors--

Процес прослуховування подій, коли подія скасовується.

---

Процес прослуховування подій, коли відбувається подія кліку.

---

Процес прослуховування подій, коли відбувається подія зміни.

#### --answer--

Процес прослуховування подій, які піднялися до батьківського елемента.

### --question--

#### --text--

Який із наведених методів НЕ можна використовувати при роботі з діалогами та модальними вікнами?

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`
