---
id: 68c3ba8940e4df8afa83a723
title: 상속이란 무엇이며 코드 재사용을 어떻게 촉진할까요?
challengeType: 19
dashedName: what-is-inheritance-and-how-does-it-promote-code-reuse
---

# --description--

상속은 객체지향 프로그래밍(OOP)에서 다음으로 다룰 핵심 개념입니다.

이 개념과 코드 재사용을 가능하게 하는 방식을 좀 더 깊이 살펴보겠습니다.

상속을 사용하면 서브클래스(또는 자식 클래스)가 베이스 클래스(또는 부모 클래스)의 속성과 메서드를 사용할 수 있습니다. 이를 통해 코드를 재사용하고, 명확한 클래스 계층 구조를 만들며, 모든 것을 다시 작성하지 않고도 동작을 맞춤 설정할 수 있습니다. 기존 메서드를 확장하거나 자식 클래스에서 재정의하여 맞춤 설정할 수 있습니다.

상속의 기본 구문은 다음과 같습니다:

```py
class Parent:
    # Parent attributes and methods

class Child(Parent):
    # Child inherits, extends, and/or overrides where necessary
```

`Child` 클래스가 `Parent` 클래스로부터 상속받으려면, 부모 클래스를 자식 클래스에 전달해야 합니다.

이 방식은 자식 클래스가 정확히 하나의 부모 클래스로부터 상속받기 때문에 단일 상속이라고 부릅니다.

예를 들어 보겠습니다:

```py
class Animal:
    def __init__(self, name):
        self.name = name

    def sound(self):
        return f'{self.name} makes a sound'

class Dog(Animal):
    bark = 'woof! woof!! woof!!!'

jack = Dog('Jack')
print(jack.sound())  # Jack makes a sound
print(jack.bark)  # woof! woof!! woof!!!
```

부모 `self.name` 클래스의 `sound()` 속성과 `Animal` 메서드를 자식 `Dog` 클래스에서 재사용할 수 있음을 알 수 있습니다.

부모 `sound()` 클래스의 `Animal` 메서드를 자식 `Dog` 클래스에서 재정의하여 `sound()`가 `bark` 클래스 변수를 사용하도록 해보겠습니다:

```py
class Animal:
    def __init__(self, name):
        self.name = name

    def sound(self):
        return f'{self.name} makes a sound.'

class Dog(Animal):
    bark = 'woof! woof!! woof!!!'

    # Override sound() to use bark class variable
    def sound(self):
        return f'{self.name} barks {self.bark}'

jack = Dog('Jack')
print(jack.sound())  # Jack barks woof! woof!! woof!!!
```

`sound()`의 반환값을 유지하면서 bark 클래스 변수를 나중에 추가하려면 `sound()` 함수를 사용해 `super()`를 확장할 수 있습니다:

```py
class Animal:
    def __init__(self, name):
        self.name = name

    def sound(self):
        return f'{self.name} makes a sound'

class Dog(Animal):
    bark = 'woof! woof!! woof!!!'

    # Call Animal.sound(), then append bark
    def sound(self):
        base = super().sound()
        return f'{base}, then {self.name} barks {self.bark}'

jack = Dog('Jack')
print(jack.sound())  # Jack makes a sound, then Jack barks woof! woof!! woof!!!
```

이 예제에서 `base`는 `sound()` 클래스의 `Animal` 메서드를 호출한 결과이며, 여기에 `Dog` 클래스의 특정 소리를 덧붙였습니다. 이렇게 하면 부모 `Animal` 클래스의 원래 동작을 유지하면서 기능을 확장할 수 있습니다.

또한 자식 클래스가 여러 부모 클래스로부터 상속받을 수 있는 다중 상속도 있습니다.

다중 상속의 기본 구문은 다음과 같습니다:

```py
class Parent:
    # Attributes and methods for Parent

class Child:
    # Attributes and methods for Child

class GrandChild(Parent, Child):
    # GrandChild inherits from both Parent and Child
    # GrandChild can combine or override behavior from each
```

다중 상속을 간단히 보여주는 예는 땅 위를 걷고 물 속에서 헤엄칠 수 있는 개구리입니다:

```py
class Walker:
    def walk(self):
        return 'I can walk on land'

class Swimmer:
    def swim(self):
        return 'I can swim in water'

# Amphibian inherits from both Walker and Swimmer
class Amphibian(Walker, Swimmer):
    def __init__(self, name):
        self.name = name

    def introduce(self):
        return f"I'm {self.name} the frog. {self.walk()} and {self.swim()}."

frog = Amphibian('Freddy')
print(frog.introduce())
# Output: I'm Freddy the frog. I can walk on land and I can swim in water.
```

# --questions--

## --text--

객체지향 프로그래밍에서 상속의 주요 목적은 무엇인가요?

## --answers--

부모 클래스의 동일한 복사본을 만드는 것입니다.

### --feedback--

자식 클래스가 부모 클래스의 속성과 메서드를 재사용하고 확장할 수 있는 방식을 생각해 보세요.

---

자식 클래스가 부모 클래스의 속성과 메서드를 재사용하고 확장할 수 있도록 하는 것입니다.

---

클래스 내 모든 메서드를 비공개로 만드는 것입니다.

### --feedback--

자식 클래스가 부모 클래스의 속성과 메서드를 재사용하고 확장할 수 있는 방식을 생각해 보세요.

---

관련 없는 클래스를 병합하여 코드 중복을 방지하는 것입니다.

### --feedback--

자식 클래스가 부모 클래스의 속성과 메서드를 재사용하고 확장할 수 있는 방식을 생각해 보세요.

## --video-solution--

2

## --text--

Python에서 다중 상속이란 무엇인가요?

## --answers--

자식 클래스가 하나의 부모 클래스로부터 상속받는 것입니다.

### --feedback--

자식이 여러 클래스로부터 상속받을 수 있는 상속 방식을 생각해 보세요.

---

하나의 부모 클래스가 여러 자식 클래스를 가진 것입니다.

### --feedback--

자식이 여러 클래스로부터 상속받을 수 있는 상속 방식을 생각해 보세요.

---

자식 클래스가 여러 부모 클래스로부터 상속받는 것입니다.

---

상속할 수 없는 클래스입니다.

### --feedback--

자식이 여러 클래스로부터 상속받을 수 있는 상속 방식을 생각해 보세요.

## --video-solution--

3

## --text--

Python에서 자식 클래스가 부모 클래스로부터 상속받게 하려면 어떻게 해야 하나요?

## --answers--

`Parent.inherit(Child)`를 호출하여 합니다.

### --feedback--

클래스 정의에 소괄호가 포함되는 구문을 생각해 보세요.

---

모든 메서드를 수동으로 복사하여 합니다.

### --feedback--

클래스 정의에 소괄호가 포함되는 구문을 생각해 보세요.

---

`extends` 키워드를 사용하여 합니다.

### --feedback--

클래스 정의에 소괄호가 포함되는 구문을 생각해 보세요.

---

부모 클래스를 인수로 자식 클래스에 전달하여 합니다.

## --video-solution--

4
