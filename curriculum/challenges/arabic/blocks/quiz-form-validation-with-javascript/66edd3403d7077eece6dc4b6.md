---
id: 66edd3403d7077eece6dc4b6
title: اختبار التحقق من صحة النموذج باستخدام JavaScript
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

لتجاوز الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هي الطريقة المستخدمة للتحقق مما إذا كانت قيمة عنصر `input` تفي بقواعد التحقق؟

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

ماذا يحدث إذا وجدت الطريقة `checkValidity()` أن `input` غير صالح؟

#### --distractors--

يتم إرسال النموذج تلقائيًا.

---

تُحدَّث الصفحة تلقائيًا.

---

يتم مسح قيمة الـ `input`.

#### --answer--

تعيد `false` دون عرض أي رسالة خطأ للمستخدم.

### --question--

#### --text--

ما هي الطريقة المستخدمة لتخصيص الرسالة المعروضة عند فشل التحقق؟

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

ما هو سلوك التحقق الافتراضي للحقول المطلوبة عند إرسال النموذج؟

#### --distractors--

يتم إرسال النموذج حتى لو كانت الحقول المطلوبة فارغة.

---

يتم إرسال النموذج مع ظهور تحذير.

---

يعيد النموذج تعيين قيم الـ `input`.

#### --answer--

يوقف المتصفح إرسال النموذج إذا كانت الحقول المطلوبة فارغة.

### --question--

#### --text--

أي إجراء لا يسبب التحقق من الأخطاء في نموذج HTML؟

#### --distractors--

تغيير قيمة الـ `input`.

---

إرسال النموذج.

---

استدعاء `reportValidity()`.

#### --answer--

النقر على زر إعادة التعيين.

### --question--

#### --text--

ماذا تُرجع `checkValidity()` عندما تكون جميع مدخلات النموذج صالحة؟

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

ماذا يعني عندما تكون قيمة `patternMismatch` هي `true`؟

#### --distractors--

الحقل فارغ.

---

القيمة طويلة جدًا.

---

الحقل معطل.

#### --answer--

القيمة لا تطابق الخاصية `pattern`.

### --question--

#### --text--

ماذا يحدث إذا تم استدعاء `reportValidity()`، ولكن فشل أحد الحقول في التحقق؟

#### --distractors--

يتم إرسال النموذج بدون خطأ.

---

يتم مسح قيم الـ `input`.

---

يغلق النموذج فورًا.

#### --answer--

يتم تمييز الحقل غير الصالح وتظهر رسالة.

### --question--

#### --text--

إرسال النموذج يدويًا.

#### --distractors--

إعادة تعيين النموذج.

---

تغيير قيمة `input`.

---

النقر على وسم الرابط.

#### --answer--

إرسال النموذج يدويًا.

### --question--

#### --text--

ما هو الغرض من استدعاء `checkValidity()` قبل إرسال النموذج؟

#### --distractors--

لإرسال النموذج تلقائيًا.

---

لإعادة تحميل الصفحة.

---

لمسح جميع حقول الـ `input`.

#### --answer--

للتحقق مما إذا كانت جميع عناصر الـ `input` تفي بالقيود المحددة لها.

### --question--

#### --text--

ما هو الغرض الرئيسي من `e.preventDefault()`؟

#### --distractors--

لإعادة تحميل النموذج.

---

لمسح بيانات النموذج.

---

لإعادة تعيين قيم النموذج.

#### --answer--

لإيقاف السلوك الافتراضي لحدث ما.

### --question--

#### --text--

عند استخدامه على حدث إرسال النموذج، ماذا يفعل `e.preventDefault()`؟

#### --distractors--

يرسل النموذج.

---

يمسح جميع حقول النموذج.

---

يعطل النموذج مؤقتًا.

#### --answer--

يمنع إرسال النموذج.

### --question--

#### --text--

في أي سيناريو يكون `e.preventDefault()` مفيدًا أكثر؟

#### --distractors--

عند التعامل مع نقر زر يرسل نموذجًا.

---

عند إغلاق مربع حوار منبثق.

---

عند فتح صفحة جديدة.

#### --answer--

عند إرسال نموذج ومنع إعادة تحميل الصفحة.

### --question--

#### --text--

ماذا يحدث إذا نسيت استدعاء `e.preventDefault()` على حدث إرسال النموذج؟

#### --distractors--

تُعاد جميع حقول الإدخال إلى قيمها الافتراضية.

---

لا يتم إرسال النموذج.

---

تُفقد بيانات النموذج.

#### --answer--

يتم إرسال النموذج وتحديث الصفحة.

### --question--

#### --text--

أي سلوك افتراضي يمكن منعه باستخدام `e.preventDefault()`؟

#### --distractors--

تحرير نموذج.

---

عرض النموذج.

---

إعادة تحميل النموذج.

#### --answer--

السلوك الافتراضي الناتج عن إرسال نموذج.

### --question--

#### --text--

ما الذي يُشغل حدث إرسال النموذج؟

#### --distractors--

عندما يتغير حقل إدخال.

---

عند إعادة تعيين النموذج.

---

عندما يتم مسح خاصية action للنموذج.

#### --answer--

عند النقر على زر إرسال النموذج أو الضغط على Enter.

### --question--

#### --text--

أين عادةً ما تتعامل مع حدث الإرسال في JavaScript؟

#### --distractors--

على عنصر إدخال نصي.

---

على أي حقل في النموذج.

---

على عنصر زر خارج النموذج.

#### --answer--

على عنصر النموذج نفسه.

### --question--

#### --text--

ماذا يفعل حدث الإرسال افتراضيًا؟

#### --distractors--

يتحقق من صحة النموذج لكنه لا يرسله.

---

يمسح حقول النموذج.

---

يلغي إرسال النموذج.

#### --answer--

يرسل بيانات النموذج إلى الخادم المحدد في خاصية action للنموذج.

### --question--

#### --text--

أي حدث يجب الاستماع إليه لمعالجة بيانات النموذج عند إرساله في JavaScript؟

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

ما هي القيمة الافتراضية لخاصية `method` في نموذج HTML؟

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
