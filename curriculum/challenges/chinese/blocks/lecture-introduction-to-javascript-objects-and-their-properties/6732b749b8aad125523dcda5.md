---
id: 6732b749b8aad125523dcda5
title: 函数和对象方法之间有什么区别？
challengeType: 19
dashedName: what-is-the-difference-between-functions-and-object-methods
---

# --interactive--

在 JavaScript 中，函数和对象方法都是封装可重用代码的方式，但它们在定义、使用以及运行的上下文方面有一些关键区别。理解这些区别对于编写高效且有条理的 JavaScript 代码至关重要。

正如你在之前的模块中学到的，函数是可重用的代码块，用于执行特定的任务：

:::interactive_editor

```js
function greet(name) {
    return "Hello, " + name + "!";
}
console.log(greet("Alice")); // "Hello, Alice!"
```

:::

另一方面，对象方法是关联到对象的函数。它们被定义为对象的属性，并且可以访问和操作对象的数据。下面是一个带有方法的对象示例：

:::interactive_editor

```js
const person = {
    name: "Bob",
    age: 30,
    sayHello: function() {
        return "Hello, my name is " + this.name;
    }
};

console.log(person.sayHello()); // "Hello, my name is Bob"
```

:::

在这个例子中，`sayHello` 是 `person` 对象的一个方法。`this` 关键字允许 `sayHello` 方法访问名为 `person` 的对象的属性。你将在后续课程中学习更多关于 `this` 关键字的内容。

函数和方法之间的区别在于它们的调用方式。函数通过其名称调用，而方法通过所属对象上的点符号调用。例如，我们调用 `greet` 函数为 `greet("Alice")`，但调用 `sayHello` 方法为 `person.sayHello()`。

另一个重要的区别是它们运行的上下文。普通函数有自己的作用域，但它们没有对任何特定对象的内置引用。方法则绑定到它们的对象，并且可以使用 `this` 关键字访问该对象的属性和其他方法。

一个关键点是，方法有助于将代码组织成逻辑对象，而函数用于更通用的、可重用的代码。

理解函数和对象方法之间的区别对于你在 JavaScript 旅程中的进步非常重要。虽然它们乍看之下可能相似，但识别何时使用每一个将帮助你编写更有条理、高效且更好的代码。

# --questions--

## --text--

函数和对象方法之间的主要区别是什么？

## --answers--

函数可以接受参数，但方法不能。

### --feedback--

考虑函数和方法是如何定义的以及它们与对象的关系。

---

方法是与对象关联的，而函数是独立的。

---

函数可以返回值，但方法不能。

### --feedback--

考虑函数和方法是如何定义的以及它们与对象的关系。

---

方法可以使用 `this` 关键字，但函数不能。

### --feedback--

考虑函数和方法是如何定义的以及它们与对象的关系。

## --video-solution--

2

## --text--

给定以下代码，如何正确调用 `greet` 方法？

```js
let person = {
    name: "Alice",
    greet: function() {
        console.log("Hello, I'm " + this.name);
    }
};
```

## --answers--

`greet();`

### --feedback--

记住方法是如何通过点符号在对象上被调用的。

---

`person.greet;`

### --feedback--

记住方法是如何通过点符号在对象上被调用的。

---

`person.greet();`

---

`person(greet);`

### --feedback--

记住方法是如何通过点符号在对象上被调用的。

## --video-solution--

3

## --text--

下面代码的输出是什么？

```js
function sayHello() {
    return "Hello!";
}

let obj = {
    sayHello: function() {
        return "Hi there!";
    }
};

console.log(sayHello());
console.log(obj.sayHello());
```

## --answers--

`"Hello!", "Hello!"`

### --feedback--

考虑到函数和方法具有相同的名称，但定义和调用方式不同。

---

`"Hi there!", "Hi there!"`

### --feedback--

考虑到函数和方法具有相同的名称，但定义和调用方式不同。

---

`"Hello!", "Hi there!"`

---

会报错。

### --feedback--

考虑到函数和方法具有相同的名称，但定义和调用方式不同。

## --video-solution--

3
