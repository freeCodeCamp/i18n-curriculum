---
id: 5900f38c1000cf542c50fe9f
title: 'Problema 32: Productos pandigitales'
challengeType: 1
forumTopicId: 301976
dashedName: problem-32-pandigital-products
---

# --description--

Diremos que un número de `n` dígitos es pandigital si utiliza todos los dígitos del 1 al `n` exactamente una vez; por ejemplo, el número de 5 dígitos, 15234, es pandigital del 1 al 5.

El producto 7254 es inusual, ya que la identidad, 39 × 186 = 7254, que contiene multiplicando, multiplicador y producto, es pandigital del 1 al 9.

Encuentra la suma de todos los productos cuya identidad multiplicando/multiplicador/producto pueda escribirse como un pandigital del 1 al `n`.

**Sugerencia:** Algunos productos se pueden obtener de más de una manera, así que asegúrate de incluirlo solo una vez en tu suma.

# --hints--

`pandigitalProducts(4)` debería devolver un número.

```js
assert(typeof pandigitalProducts(4) === 'number');
```

`pandigitalProducts(4)` debería devolver `12`.

```js
assert.strictEqual(pandigitalProducts(4), 12);
```

`pandigitalProducts(6)` debería devolver `162`.

```js
assert.strictEqual(pandigitalProducts(6), 162);
```

`pandigitalProducts(7)` debería devolver `0`.

```js
assert.strictEqual(pandigitalProducts(7), 0);
```

`pandigitalProducts(8)` debería devolver `13458`.

```js
assert.strictEqual(pandigitalProducts(8), 13458);
```

`pandigitalProducts(9)` debería devolver `45228`.

```js
assert.strictEqual(pandigitalProducts(9), 45228);
```

# --seed--

## --seed-contents--

```js
function pandigitalProducts(n) {

  return true;
}

pandigitalProducts(4);
```

# --solutions--

```js
// Check for pandigital number
function isPandigital(digitStr) {
  return digitStr
    .split('')
    .sort()
    .every((c, i) => c == i + 1);
}

// Concatenate 3 numbers
function concatenate3Nums(a, b, c) {
  return a.toString() + b.toString() + c.toString();
}

// Find sum of all pandigital products
function pandigitalProducts(n) {
  const products = [];
  let sum = 0;
  const max = Number(Array(n)
    .fill(0)
    .map((_, i) => (n - i).toString())
    .join(''));
  const outerLimit = Math.sqrt(max);

  for (let factor1 = 2; factor1 < outerLimit; factor1++) {
    const innerLimit = max / factor1;

    for (let factor2 = factor1; factor2 < innerLimit; factor2++) {
      const product = factor1 * factor2;
      const concatenated = concatenate3Nums(factor1, factor2, product);

      if (concatenated.length > n) {
        break;
      } else if (concatenated.length == n &&
        isPandigital(concatenated) &&
        !products.includes(product)
      ) {
        products.push(product);
        sum += product;
      }
    }
  }
  return sum;
}
```
