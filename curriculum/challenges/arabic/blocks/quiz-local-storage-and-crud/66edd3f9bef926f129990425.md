---
id: 66edd3f9bef926f129990425
title: اختبار التخزين المحلي وعمليات CRUD
challengeType: 8
dashedName: quiz-local-storage-and-crud
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ماذا تعني CRUD في سياق تطوير الويب؟

#### --distractors--

إنشاء، استبدال، تراجع، حذف.

---

اتصال، إزالة، تحديث، عرض.

---

نسخ، استرجاع، تحديث، حذف.

#### --answer--

إنشاء، قراءة، تحديث، حذف.

### --question--

#### --text--

أي طريقة HTTP ترتبط عادةً بعملية الإنشاء في واجهة برمجة التطبيقات RESTful؟

#### --distractors--

`GET`

---

`PUT`

---

`DELETE`

#### --answer--

`POST`

### --question--

#### --text--

أي من عمليات CRUD التالية تُجرى عادةً عند تعديل سجل موجود في قاعدة بيانات؟

#### --distractors--

قراءة

---

حذف

---

إنشاء

#### --answer--

تحديث

### --question--

#### --text--

ما الفرق الرئيسي بين localStorage و sessionStorage في واجهة برمجة تطبيقات Web Storage؟

#### --distractors--

localStorage يخزن البيانات في الذاكرة، بينما sessionStorage يخزن البيانات على القرص.

---

sessionStorage أسرع من localStorage في استرجاع البيانات.

---

بيانات localStorage متاحة فقط خلال الجلسة، بينما sessionStorage تستمر عبر الجلسات.

#### --answer--

بيانات localStorage تبقى حتى تُحذف صراحة، بينما بيانات sessionStorage تُمسح عند انتهاء جلسة الصفحة.

### --question--

#### --text--

أي طريقة تُستخدم لتخزين البيانات في localStorage؟

#### --distractors--

`localStorage.setData()`

---

`localStorage.saveItem()`

---

`localStorage.storeData()`

#### --answer--

`localStorage.setItem()`

### --question--

#### --text--

كيف يمكنك حذف عنصر واحد من sessionStorage؟

#### --distractors--

`sessionStorage.clearItem('key')`

---

`sessionStorage.remove('key')`

---

`sessionStorage.deleteItem('key')`

#### --answer--

`sessionStorage.removeItem('key')`

### --question--

#### --text--

ما الفرق بين `PUT` و `PATCH`؟

#### --distractors--

`PUT` يعدل حقولًا محددة، بينما `PATCH` يستبدل المورد بأكمله.

---

`PUT` يضيف موارد جديدة فقط، بينما `PATCH` يحدث الموارد الموجودة.

---

`PUT` يُستخدم لاسترجاع البيانات، بينما `PATCH` يُستخدم لإرسال البيانات.

#### --answer--

`PUT` يستبدل المورد بأكمله، بينما `PATCH` يحدث حقولًا محددة.

### --question--

#### --text--

ما الغرض من تخزين الكاش؟

#### --distractors--

يُستخدم تخزين الكاش لحفظ بيانات اعتماد المستخدم بشكل دائم وآمن في المتصفح لأغراض التوثيق.

---

يُستخدم تخزين الكاش لتخزين كميات كبيرة من البيانات المنظمة في المتصفح باستخدام نظام قاعدة بيانات.

---

يُستخدم تخزين الكاش لتتبع جلسات المستخدم وتفضيلاته عبر تخزين أزواج المفتاح-القيمة المتاحة عبر مواقع متعددة.

#### --answer--

يُستخدم تخزين الكاش لحفظ موارد الشبكة المطلوبة بشكل متكرر، مما يقلل أوقات التحميل ويحسن الأداء.

### --question--

#### --text--

ما هي الكوكي في سياق تطوير الويب؟

#### --distractors--

نوع من طلبات HTTP.

---

قطعة شفرة تعمل في المتصفح.

---

طريقة لتخزين ملفات كبيرة على الخادم.

#### --answer--

قطعة صغيرة من البيانات يخزنها متصفح الويب الخاص بالمستخدم.

### --question--

#### --text--

أي من الطرق التالية تُستخدم لضبط كوكي يدويًا في JavaScript؟

#### --distractors--

`document.setCookie`

---

`document.cookie.set`

---

`document.createCookie`

#### --answer--

`document.cookie`

### --question--

#### --text--

أي من مقتطفات الشفرة التالية تحذف بشكل صحيح كوكي باسم `username`؟

#### --distractors--

`document.cookie.delete("username");`

---

`document.removeCookie("username");`

---

`cookie.delete("username");`

#### --answer--

`document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";`

### --question--

#### --text--

أي من العبارات التالية عن الكوكيز خاطئة؟

#### --distractors--

تنتهي صلاحية كوكيز الجلسة عند إغلاق المتصفح.

---

كوكيز HttpOnly لا يمكن الوصول إليها بواسطة JavaScript.

---

كوكيز الآمنة تُنقل فقط عبر HTTPS.

#### --answer--

يمكن تعيين الكوكيز فقط بواسطة الخادم.

### --question--

#### --text--

ما هو بصمة المتصفح؟

#### --distractors--

تقنية تُستخدم لتحديد المستخدمين بناءً على عنوان IP الخاص بهم.

---

شكل من أشكال تشفير البيانات.

---

طريقة لتخزين البيانات في كاش المتصفح.

#### --answer--

طريقة لتتبع المستخدمين عبر خصائص المتصفح والجهاز.

### --question--

#### --text--

ما هو الخطر الأساسي المرتبط بتخزين كلمات المرور في التخزين المحلي؟

#### --distractors--

بيانات التخزين المحلي تُحذف بمجرد إغلاق تبويب المتصفح.

---

تفقد بيانات التخزين المحلي عند حذف الكوكيز.

---

التخزين المحلي لا يعمل في وضع عدم الاتصال.

#### --answer--

التخزين المحلي غير مشفر ويمكن الوصول إليه بسهولة.

### --question--

#### --text--

أي من العبارات التالية صحيحة عن التخزين على جانب العميل؟

#### --distractors--

التخزين على جانب العميل يستخدم كائنات بدلاً من سلاسل نصية لتخزين البيانات.

---

التخزين على جانب العميل يستخدم خدمات السحابة لمزامنة بيانات المستخدم عبر أجهزة متعددة.

---

التخزين على جانب العميل أكثر أمانًا من التخزين على جانب الخادم.

#### --answer--

التخزين على جانب العميل يسمح للمواقع بتخزين البيانات على جهاز المستخدم.

### --question--

#### --text--

ما هي الفائدة الأساسية لاستخدام IndexedDB بدلاً من localStorage؟

#### --distractors--

IndexedDB لا يتطلب موافقة المستخدم لتخزين البيانات.

---

IndexedDB يمتلك واجهة برمجة تطبيقات أبسط.

---

IndexedDB مدعوم في جميع متصفحات الويب.

#### --answer--

IndexedDB يمكنه التعامل مع كميات أكبر من البيانات واستعلامات أكثر تعقيدًا.

### --question--

#### --text--

أي من الطرق التالية صحيحة لاسترجاع البيانات من مخزن كائنات في IndexedDB؟

#### --distractors--

استدعاء `retrieve()` على مخزن الكائنات.

---

استخدام `query()` للوصول إلى سجلات محددة.

---

استخدام طريقة `fetch()`.

#### --answer--

استخدام `get()` على مرجع المخزن.

### --question--

#### --text--

ما نوع المعاملات التي يدعمها IndexedDB؟

#### --distractors--

معاملات قراءة فقط.

---

معاملات غير متزامنة فقط.

---

معاملات متزامنة فقط.

#### --answer--

معاملات قراءة وكتابة.

### --question--

#### --text--

أي من العبارات التالية صحيحة عن دورة حياة Service Worker؟

#### --distractors--

تعمل Service Workers في الخيط الرئيسي لتطبيق الويب.

---

يمكن تفعيل Service Workers قبل تثبيتها.

---

يمكن لـ Service Workers العمل فقط عندما يكون تطبيق الويب مفتوحًا في المتصفح.

#### --answer--

يمكن لـ Service Worker اعتراض طلبات الشبكة بعد تفعيله.

### --question--

#### --text--

أي عبارة تصف أفضل ميزة رئيسية لتطبيقات الويب التقدمية (PWAs)؟

#### --distractors--

يمكن تثبيت PWAs فقط على الأجهزة المحمولة.

---

تتطلب PWAs وجود خادم خلفي.

---

يمكن الوصول إلى PWAs فقط من خلال متصفحات محددة.

#### --answer--

توفر PWAs وظيفة العمل في وضع عدم الاتصال عبر Service Workers.

