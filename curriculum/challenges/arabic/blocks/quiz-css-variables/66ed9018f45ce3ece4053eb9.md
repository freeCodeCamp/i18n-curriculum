---
id: 66ed9018f45ce3ece4053eb9
title: اختبار متغيرات CSS
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي مما يلي ليس واصفًا صالحًا في قاعدة `@property`؟

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

في الشفرة التالية، ما القيمة التي سيأخذها `color` إذا لم يُعرّف `--main-color`؟

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

لن يتم تطبيق أي لون.

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

أي مما يلي طريقة صحيحة لإعلان خاصية مخصصة في CSS؟

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

ما الغرض الرئيسي من استخدام الخصائص المخصصة في CSS؟

#### --distractors--

تغيير هيكل DOM.

---

إنشاء محددات ديناميكية.

---

تحسين إمكانية الوصول.

#### --answer--

تعريف أنماط قابلة لإعادة الاستخدام.

### --question--

#### --text--

ماذا يمثل محدد `:root`؟

#### --distractors--

أول عنصر فرعي في `body`.

---

العنصر الأب لكل عناصر `header`.

---

أول عنصر فرعي لوسم `html`.

#### --answer--

العنصر الأعلى مستوى في شجرة DOM.

### --question--

#### --text--

كيف تطبق خاصية مخصصة `--foreground` كـ `color` في CSS؟

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

ما الغرض من قاعدة `@property` في CSS؟

#### --distractors--

تعريف استعلام وسائط.

---

التحكم في تحريكات CSS وتوقيتها.

---

تجميع خصائص CSS في قاعدة واحدة.

#### --answer--

تعريف كيفية تصرف الخصائص المخصصة.

### --question--

#### --text--

عند إعلان `@property` مخصص، ما الغرض من `syntax` في تعريفه؟

#### --distractors--

تحديد القيمة الافتراضية للخاصية المخصصة.

---

تحديد ما إذا كانت الخاصية يمكن وراثتها من العناصر الفرعية.

---

تحديد ما إذا كانت الخاصية تُطبق على كل العناصر.

#### --answer--

فرض نوع بيانات أو نمط قيمة محدد للخاصية المخصصة.

### --question--

#### --text--

ما الذي يجب أن تحذر منه عند استخدام الخصائص المخصصة؟

#### --distractors--

الخصائص المخصصة تتحول تلقائيًا إلى وحدات `px`.

---

الخصائص المخصصة تتجاوز دائمًا الأنماط المضمّنة.

---

لا يمكن استخدام الخصائص المخصصة لتعيين قيم الخطوط.

#### --answer--

قد لا تدعم المتصفحات القديمة الخصائص المخصصة.

### --question--

#### --text--

ما الغرض من توفير قيمة نظام بديل في دالة `var()` للخصائص المخصصة في CSS؟

#### --distractors--

تقليل كمية شفرة CSS.

---

تحسين الأداء في المتصفحات الحديثة.

---

تحسين وقت العرض على الشبكات البطيئة.

#### --answer--

ضمان تطبيق قيمة صالحة إذا كانت الخاصية المخصصة غير معرفة.

### --question--

#### --text--

عند تعريف متغير `--foreground` داخل استعلام وسائط، ماذا يحدث عندما لا يعود الاستعلام متطابقًا مع إطار العرض الحالي؟

#### --distractors--

تُحفظ الخاصية المخصصة وتستمر في التطبيق.

---

تعود الخاصية المخصصة إلى قيمتها الأولية.

---

تُعاد حساب الخاصية المخصصة بناءً على إطار العرض.

#### --answer--

تصبح الخاصية المخصصة غير متاحة لأنها محصورة في نطاق استعلام الوسائط.

### --question--

#### --text--

كيف سيتصرف الإعلان التالي؟

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

سيطبق `--main-color` حتى لو لم يُعرّف، معتمداً على تلك القيمة.

---

سيطبق `--main-color` و`--fallback-color` معًا، مما يؤدي إلى مزج اللونين.

---

سيعتمد دائمًا على `#000` بغض النظر عن تعريف `--main-color` أو `--fallback-color`.

#### --answer--

يُطبّق `--main-color` إذا كان معرفًا؛ وإلا، يتحقق من `--fallback-color;` وإذا لم يكن أي منهما معرفًا، يستخدم `#000`.

### --question--

#### --text--

ما فائدة واحدة لاستخدام `@property` في CSS؟

#### --distractors--

يحسن الأداء بحساب قيم الخصائص المخصصة مسبقًا.

---

يضيف بادئات تلقائيًا للخصائص المخصصة لدعم المتصفحات.

---

يقيد استخدام الخصائص المخصصة لعناصر معينة.

#### --answer--

يسمح بتحريك الخصائص المخصصة.

### --question--

#### --text--

ماذا يتحكم الحقل `inherits` في إعلان `@property` مخصص؟

#### --distractors--

ما إذا كانت الخاصية المخصصة ستأخذ قيمة أولية.

---

ما إذا كانت الخاصية يمكن أن يكون لها نسخة مختصرة.

---

ما إذا كانت الخاصية تقبل قيم النظام البديل.

#### --answer--

ما إذا كانت قيمة الخاصية المخصصة تُمرر إلى العناصر الفرعية.

### --question--

#### --text--

في إعلان `@property` مخصص، ماذا يحدد `initial-value`؟

#### --distractors--

القيم المقبولة التي يمكن للخاصية قبولها.

---

أولوية الخاصية في التراكم.

---

نوع القيمة التي يجب أن تمتلكها الخاصية.

#### --answer--

قيمة النظام البديل للخاصية.

### --question--

#### --text--

بالنظر إلى شفرة HTML وCSS التالية، ما قيمة خاصية `color` للعنصر `.box`؟

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

على أي خاصية يجب تطبيق تدرج CSS؟

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

ما الغرض من توقفات اللون في تدرجات CSS؟

#### --distractors--

تحديد مستوى شفافية التدرج.

---

تحديد اتجاه التدرج.

---

تكرار التدرج بنمط ثابت.

#### --answer--

تحديد النقاط المحددة التي تتغير عندها الألوان في التدرج.

### --question--

#### --text--

ماذا يحدث إذا لم يُحدد زاوية أو اتجاه في تدرج خطي في CSS؟

#### --distractors--

يُفترض التدرج باتجاه قطري.

---

يُفترض التدرج بالتحرك من الأسفل إلى الأعلى.

---

يُفترض التدرج بالتحرك من اليسار إلى اليمين.

#### --answer--

يُفترض التدرج بالتحرك من الأعلى إلى الأسفل.

### --question--

#### --text--

أي دالة تدرج CSS تسمح بإنشاء تدرج يشع من نقطة مركزية؟

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

أي مما يلي طريقة صحيحة لإعلان خاصية مخصصة في CSS؟

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

أي محدد يُستخدم عادة لتعريف الخصائص المخصصة العالمية في CSS؟

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

عند استخدام `var()`، لماذا يُنصح بتضمين قيمة نظام بديل؟

#### --distractors--

يضمن تحريك المتغير بشكل صحيح.

---

يتجنب تحميل أوراق أنماط خارجية.

---

يمنع إعادة تدفق المتصفح.

#### --answer--

يضمن قيمة صالحة إذا كانت الخاصية المخصصة غير معرفة.

### --question--

#### --text--

ما الصيغة الصحيحة لتطبيق خاصية مخصصة كلون خلفية؟

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

أي قاعدة CSS تسمح للمطورين بتعريف خصائص مخصصة مع تحكم أكبر في سلوكها؟

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

ماذا يتحكم الحقل `inherits` في تعريف `@property`؟

#### --distractors--

ما إذا كانت الخاصية تُستخدم في JavaScript.

---

ما إذا كانت الخاصية ستؤدي إلى إعادة الطلاء.

---

ما إذا كانت الخاصية يمكن أن تحتوي على دوال.

#### --answer--

ما إذا كانت قيمة الخاصية تُمرر إلى العناصر الفرعية.

### --question--

#### --text--

عند تعريف زاوية تدرج متحركة باستخدام `@property`، أي صيغة يجب استخدامها؟

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

ما الذي يميز الخاصية المخصصة في CSS عن الخاصية القياسية من حيث طريقة تعريفها؟

#### --distractors--

الخصائص القياسية تُعرف بشرطة واحدة، بينما الخصائص المخصصة تبدأ بشرطتين.

---

الخصائص القياسية تستخدم `var()` للتعريف، بينما الخصائص المخصصة لا تستخدم.

---

الخصائص القياسية تُعرف باستخدام قاعدة `@property`، بينما الخصائص المخصصة لا تُعرف بها.

#### --answer--

يجب أن تبدأ الخصائص المخصصة بشرطتين (`--`)، بينما الخصائص القياسية لا تبدأ بهما.

### --question--

#### --text--

أي من هذه فوائد استخدام الخصائص المخصصة في CSS؟

#### --distractors--

تقلل الحاجة لتعليقات CSS.

---

تحسن الصور تلقائيًا.

---

تلغي الحاجة لمتغيرات JavaScript.

#### --answer--

تسمح بإعادة استخدام الأنماط وصيانتها مركزيًا.

### --question--

#### --text--

أي خاصية CSS يمكن أن تتغير ديناميكيًا بناءً على استعلامات الوسائط باستخدام الخصائص المخصصة؟

#### --distractors--

فقط `background-color`

---

فقط `font-family`

---

فقط `z-index`

#### --answer--

أي خاصية تقبل قيمة.

### --question--

#### --text--

في سياق متغيرات CSS، ما دور `initial-value` في قاعدة `@property`؟

#### --distractors--

يحدد الحد الأدنى لقيمة الخاصية.

---

يحدد الحد الأقصى للقيمة للرسوم المتحركة.

---

يغير أولوية المحدد.

#### --answer--

يُعيّن قيمة افتراضية للخاصية إذا لم تُحدد قيمة.

### --question--

#### --text--

بالنظر إلى HTML وCSS التاليين، ما لون الخلفية الذي سيُطبق على العنصر `.card`؟

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

في CSS التالي، ماذا يحدث إذا حاول المستخدم تعيين قيمة غير صالحة لـ `--padding` (مثلًا، لون بدلًا من طول)؟

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

المتصفح يقبل القيمة لكنه يتجاهلها أثناء التخطيط.

---

المتصفح يرمي خطأ اثناء التشغيل.

---

المتصفح يحول القيمة تلقائيًا إلى طول صالح.

#### --answer--

المتصفح يرجع إلى القيمة الابتدائية للخاصية.

### --question--

#### --text--

ما الغرض من الخاصية المخصصة `--gradient-angle` في هذا المثال؟

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

تحدد سرعة انتقال التدرج.

---

تحدد حجم التدرج.

---

تحدد وضع مزج الألوان.

#### --answer--

تتحكم في اتجاه التدرج.

### --question--

#### --text--

أي سيناريو يوضح أفضل فائدة لاستخدام الخصائص المخصصة للسمات؟

#### --distractors--

استخدامها فقط لأحجام الخطوط.

---

تطبيقها فقط على `:hover`.

---

تقييدها على صنف CSS واحد.

#### --answer--

تغيير القيم عبر الصنف.

### --question--

#### --text--

لماذا قد يفضل المطوّرون استخدام `:root` لتعريف متغيرات CSS؟

#### --distractors--

لأن `:root` يحسن سرعة تحميل الصفحة.

---

لأن `:root` يعطل التحديدية.

---

لأن `:root` مطلوب لجافاسكريبت.

#### --answer--

لأن `:root` يسمح بأن تكون الخصائص ذات نطاق عالمي.

### --question--

#### --text--

ماذا سيفعل CSS التالي إذا لم يُعرف `--secondary-color`؟

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

سيطبق `black`.

---

سيتجاهل خاصية `color`.

---

سيطبق `--secondary-color`.

#### --answer--

سيطبق `orange`.

### --question--

#### --text--

ما نوع القيم التي يمثلها `<color>` في حقل صيغة `@property`؟

#### --distractors--

زوايا مثل `90deg`.

---

أطوال مثل `10px`.

---

نسب مئوية مثل `50%`.

#### --answer--

قيم ألوان مثل `#ff0000` أو `red`.

### --question--

#### --text--

ما الفائدة الرئيسية لاستخدام الخصائص المخصصة مع `@property`؟

#### --distractors--

تمنع الحاجة إلى قيم النظام البديل.

---

تجبر التخطيط الثابت.

---

تقلل الحاجة إلى الأصناف.

#### --answer--

تسمح بتحريك الخاصية.

### --question--

#### --text--

ماذا يفعل `var(--undefined-property, fallback)` عندما لا تُعرف الخاصية المخصصة؟

#### --distractors--

يرمي خطأ ويتوقف عن تطبيق الأنماط.

---

يتجاهل قاعدة CSS بأكملها.

---

يطبق اسم المتغير كما هو.

#### --answer--

يطبق قيمة النظام البديل بدلًا من ذلك.
