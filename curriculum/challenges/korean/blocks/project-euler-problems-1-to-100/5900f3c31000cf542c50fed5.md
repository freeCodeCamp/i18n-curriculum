---
id: 5900f3c31000cf542c50fed5
title: '문제 86: 직육면체 라우트'
challengeType: 1
forumTopicId: 302200
dashedName: problem-86-cuboid-route
---

# --description--

거미 S가 6×5×3 크기의 직육면체 방 한 모서리에 앉아 있고, 파리 F는 반대편 모서리에 앉아 있습니다. 방의 표면을 따라 이동할 때 S에서 F까지의 가장 짧은 "직선" 거리는 10이며, 경로는 도면에 표시되어 있습니다.

<img alt="직육면체 방 한 모서리에서 반대편 모서리까지 거미와 파리의 경로 도면" src="https://cdn-media-1.freecodecamp.org/project-euler/cuboid-route.png" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

하지만 주어진 직육면체마다 최대 세 개의 "최단" 경로 후보가 있을 수 있으며, 최단 경로가 항상 정수 길이를 갖는 것은 아닙니다.

회전을 무시하고 정수 치수를 가진 직육면체 중에서, M = 100일 때 최단 경로가 정수 길이를 갖는 경우가 정확히 `2060`개임을 보일 수 있습니다. 이 값은 해답 수가 처음으로 2000을 넘는 최소 M 값입니다. M = 99일 때 해답 수는 `1975`입니다.

해답 수가 처음으로 `n`를 넘는 최소 M 값을 찾으세요.

# --hints--

`cuboidRoute(2000)`는 숫자를 반환해야 합니다.

```js
assert(typeof cuboidRoute(2000) === 'number');
```

`cuboidRoute(2000)`는 `100`를 반환해야 합니다.

```js
assert.strictEqual(cuboidRoute(2000), 100);
```

`cuboidRoute(25000)`는 `320`를 반환해야 합니다.

```js
assert.strictEqual(cuboidRoute(25000), 320);
```

`cuboidRoute(500000)`는 `1309`를 반환해야 합니다.

```js
assert.strictEqual(cuboidRoute(500000), 1309);
```

`cuboidRoute(1000000)`는 `1818`를 반환해야 합니다.

```js
assert.strictEqual(cuboidRoute(1000000), 1818);
```

# --seed--

## --seed-contents--

```js
function cuboidRoute(n) {

  return true;
}

cuboidRoute(2000);
```

# --solutions--

```js
function cuboidRoute(n) {
  // Based on https://www.mathblog.dk/project-euler-86-shortest-path-cuboid/
  function getLength(a, b) {
    return Math.sqrt(a ** 2 + b ** 2);
  }

  let M = 2;
  let counter = 0;

  while (counter < n) {
    M++;
    for (let baseHeightWidth = 3; baseHeightWidth <= 2 * M; baseHeightWidth++) {
      const pathLength = getLength(M, baseHeightWidth);
      if (Number.isInteger(pathLength)) {
        if (baseHeightWidth <= M) {
          counter += Math.floor(baseHeightWidth / 2);
        } else {
          counter += 1 + M - Math.floor((baseHeightWidth + 1) / 2);
        }
      }
    }
  }

  return M;
}
```
