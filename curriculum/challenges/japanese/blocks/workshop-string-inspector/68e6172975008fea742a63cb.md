---
id: 68e6172975008fea742a63cb
title: ステップ 5
challengeType: 1
dashedName: step-5
---

# --description--

部分文字列が見つからなかった場合に何が起こるかを見てみましょう。

`hasJavaScript` という名前の変数を作成してください。次に、`includes()` メソッドを使って `fccSentence` が `"JavaScript"` を含むかどうかを確認した結果を代入してください。

その後、テンプレートリテラルか文字列の連結を使って、メッセージ `fccSentence.includes("JavaScript") returns <hasJavaScript> because the word "JavaScript" is not in the sentence.` をコンソールに出力してください。`<hasJavaScript>` は変数の実際の値に置き換えてください。

# --before-each--

```js
const spy = __helpers.spyOn(console, 'log');
```

# --hints--

`hasJavaScript` という名前の変数を宣言してください。

```js
assert.exists(hasJavaScript);
```

`fccSentence.includes("JavaScript")` を使ってください。

```js
assert.match(__helpers.removeJSComments(code), /fccSentence\.includes\(\s*(['"`])JavaScript\1\s*\)/);
```

`fccSentence.includes("JavaScript")` の結果を `hasJavaScript` 変数に代入してください。

```js
assert.match(__helpers.removeJSComments(code), /hasJavaScript\s*=\s*fccSentence\.includes\(\s*(['"`])JavaScript\1\s*\)/);
```

`fccSentence.includes("JavaScript")` の結果は `false` になるはずです。

```js
assert.isFalse(hasJavaScript);
```

メッセージ `fccSentence.includes("JavaScript") returns <hasJavaScript> because the word "JavaScript" is not in the sentence.` をログに出力してください。`<hasJavaScript>` は変数の実際の値に置き換えてください。

```js
assert.deepInclude(spy.calls, ['fccSentence.includes("JavaScript") returns false because the word "JavaScript" is not in the sentence.']);
```

# --seed--

## --seed-contents--

```js
const fccSentence = "freeCodeCamp is a great place to learn web development.";

console.log("Here are some examples of the includes() method:");

const hasFreeCodeCamp = fccSentence.includes("freeCodeCamp");
console.log(`fccSentence.includes("freeCodeCamp") returns ${hasFreeCodeCamp} because the word "freeCodeCamp" is in the sentence.`);

--fcc-editable-region--

--fcc-editable-region--
```
