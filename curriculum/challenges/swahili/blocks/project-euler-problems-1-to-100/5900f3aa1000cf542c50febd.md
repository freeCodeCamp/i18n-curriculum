---
id: 5900f3aa1000cf542c50febd
title: 'Zoezi la 62: Permutations za nambari za ujazo'
challengeType: 1
forumTopicId: 302174
dashedName: problem-62-cubic-permutations
---

# --description--

Nambari ya ujazo, 41063625 ($345^3$), inaweza kubadilishwa mpangilio wake ili kutoa nambari nyingine mbili za ujazo: 56623104 ($384^3$) na 66430125 ($405^3$). Kwa kweli, 41063625 ni nambari ndogo kabisa ya ujazo ambayo ina mpangilio wa tarakimu zake tatu tu ambao pia ni nambari za ujazo.

Tafuta nambari ndogo kabisa ya ujazo ambayo ina mpangilio wa tarakimu zake `n` ambao pia ni nambari za ujazo.

# --hints--

`cubicPermutations(2)` inapaswa kurudisha nambari.

```js
assert(typeof cubicPermutations(2) === 'number');
```

`cubicPermutations(2)` inapaswa kurudisha `125`.

```js
assert.strictEqual(cubicPermutations(2), 125);
```

`cubicPermutations(3)` inapaswa kurudisha `41063625`.

```js
assert.strictEqual(cubicPermutations(3), 41063625);
```

`cubicPermutations(4)` inapaswa kurudisha `1006012008`.

```js
assert.strictEqual(cubicPermutations(4), 1006012008);
```

`cubicPermutations(5)` inapaswa kurudisha `127035954683`.

```js
assert.strictEqual(cubicPermutations(5), 127035954683);
```

# --seed--

## --seed-contents--

```js
function cubicPermutations(n) {

  return true;
}

cubicPermutations(2);
```

# --solutions--

```js
function cubicPermutations(n) {
  function getDigits(num) {
    const digits = [];
    while (num > 0) {
      digits.push(num % 10);
      num = Math.floor(num / 10);
    }
    return digits;
  }

  function getCube(num) {
    return num ** 3;
  }

  const digitsToCubeCounts = {};
  let curNum = 1;
  let digits;

  while (!digitsToCubeCounts[digits] || digitsToCubeCounts[digits].count < n) {
    const cube = getCube(curNum);
    digits = getDigits(cube).sort().join();
    if (!digitsToCubeCounts[digits]) {
      digitsToCubeCounts[digits] = {
        count: 1,
        smallestCube: cube
      };
    } else {
      digitsToCubeCounts[digits].count += 1;
    }

    curNum++;
  }
  return digitsToCubeCounts[digits].smallestCube;
}
```
