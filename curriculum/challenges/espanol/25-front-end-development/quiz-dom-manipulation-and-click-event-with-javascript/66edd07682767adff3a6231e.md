---
id: 66edd07682767adff3a6231e
title: Manipulación del DOM y eventos de clic con Cuestionario de JavaScript
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

Para pasar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué es el Modelo de Objetos del Documento?

#### --distractors--

Un lenguaje de programación que se utiliza para interactuar con una página HTML.

---

Un conjunto de objetos que contienen sólo números de punto flotante.

---

Un conjunto de clases de JavaScript que representan elementos HTML.

#### --answer--

Una interfaz de programación que te permite interactuar con documentos HTML.

### --question--

#### --text--

¿Qué significa API?

#### --distractors--

Interfaz de Programación Adicional.

---

Interfaz de Programación de Adición.

---

Interfaz de Programación Adicional.

#### --answer--

Interfaz de Programación de Aplicaciones.

### --question--

#### --text--

¿Cómo puedes usar JavaScript para encontrar todos los anclajes HTML con enlaces faltantes?

#### --distractors--

```js
document.findAll("link![href]");
```

---

```js
document.find("link:not([href])");
```

---

```js
document.querySelector("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

¿Qué hace la propiedad `textContent`?

#### --distractors--

Elimina el contenido de texto de todos los elementos HTML.

---

Elimina el contenido de texto de tu archivo JavaScript.

---

Agrega texto a todos los arrays y objetos en tu archivo JavaScript.

#### --answer--

Devuelve el contenido de texto plano de un elemento.

### --question--

#### --text--

¿Qué hace la propiedad `innerHTML`?

#### --distractors--

Es una propiedad del `Element` utilizada para obtener el contenido de texto de un elemento HTML.

---

Es una propiedad del `Element` utilizada para agregar anotaciones a tu HTML.

---

Es una propiedad del `Element` utilizada para disparar eventos en tu HTML.

#### --answer--

Es una propiedad del `Element` utilizada para establecer o actualizar partes del marcado HTML.

### --question--

#### --text--

¿Cuál de los siguientes agregará un listener de eventos válido?

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

¿Cuál de los siguientes NO es un método válido del objeto `document`?

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()`

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

¿Cuál de los siguientes establecerá el atributo `class` a `my-class`?

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

¿Qué es el objeto `Event`?

#### --distractors--

Es un objeto especial utilizado para eliminar eventos de tu proyecto de JavaScript.

---

Se utiliza para agregar automáticamente eventos a tus proyectos de JavaScript.

---

Es un objeto especial utilizado al trabajar con arrays.

#### --answer--

Es una carga útil que se activa cuando un usuario interactúa con tu página web de alguna manera.

### --question--

#### --text--

¿Cuál de los siguientes es un método utilizado para eliminar un listener de eventos que se agregó previamente a un elemento usando el método `addEventListener()`?

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

¿Qué son los manejadores de eventos inline?

#### --distractors--

Atributos especiales en un elemento HTML que se utilizan para cancelar todos los eventos.

---

Atributos especiales en un elemento HTML utilizados únicamente para disparar envíos de formularios en tu código JavaScript.

---

Atributos especiales en un elemento HTML utilizados para eliminar eventos.

#### --answer--

Atributos especiales en un elemento HTML que se utilizan para ejecutar código JavaScript cuando ocurre un evento.

### --question--

#### --text--

¿Cuáles son las dos principales categorías para APIs web?

#### --distractors--

APIs de Java y APIs de terceros.

---

APIs de navegador y APIs de Rust.

---

APIs del sistema y APIs de terceros.

#### --answer--

APIs del navegador y APIs de terceros.

### --question--

#### --text--

¿Qué evento se activa cuando todos los elementos de la página HTML han sido analizados por el navegador?

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

¿Cuál de los siguientes es una propiedad que representa el estilo inline de un elemento?

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

¿Cuál de los siguientes es la forma correcta de usar un manejador de eventos inline?

#### --distractors--

```html
<button eventclick="validate()">Validate</button>
```

---

```html
<button click="validate()">Validate</button>
```

---

```html
<button clickevent="validate()">Validate</button>
```

#### --answer--

```html
<button onclick="validate()">Validate</button>
```

### --question--

#### --text--

¿Cuál de los siguientes métodos se utiliza para retrasar una acción por un tiempo especificado?

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

¿Cuál de los siguientes se puede utilizar para crear y controlar animaciones directamente en JavaScript?

#### --distractors--

Permitir API de Animaciones

---

Controlar API de Animaciones

---

Crear API de Animaciones

#### --answer--

API de Animaciones Web

### --question--

#### --text--

¿Qué hace la API de Canvas?

#### --distractors--

Se utiliza para animar gráficos en tu archivo CSS.

---

Se utiliza sólo para eliminar gráficos de tu archivo JavaScript.

---

Se utiliza para eliminar gráficos de tu archivo HTML.

#### --answer--

Se utiliza para manipular gráficos a través de tu archivo JavaScript.

### --question--

#### --text--

¿Qué es la delegación de eventos?

#### --distractors--

El proceso de escuchar eventos cuando un evento es cancelado.

---

El proceso de escuchar eventos cuando ocurre un evento de clic.

---

El proceso de escuchar eventos cuando ocurre un evento de cambio.

#### --answer--

El proceso de escuchar eventos que se han propagado hasta un padre.

### --question--

#### --text--

¿Cuál de los siguientes NO es un método que puedes usar al trabajar con cuadros de diálogo y modales?

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`

