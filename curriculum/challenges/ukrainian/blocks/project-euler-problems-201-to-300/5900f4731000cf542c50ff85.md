---
id: 5900f4731000cf542c50ff85
title: 'Завдання 262: гірський хребет'
challengeType: 1
forumTopicId: 301911
dashedName: problem-262-mountain-range
---

# --description--

Наступне рівняння представляє безперервну топографію гірської області, даючи висоту $h$ у будь-якій точці ($x$,$y$):

$$h = \left(5000 - \frac{x^2 + y^2 + xy}{200} + \frac{25(x + y)}{2}\right) \times e^{-\left|\frac{x^2 + y^2}{1\\,000\\,000} - \frac{3(x + y)}{2000} + \frac{7}{10}\right|}$$

Комар має намір полетіти з A(200,200) до B(1400,1400), не виходячи за область, задану $0 ≤ x$, $y ≤ 1600$.

Через перешкоди у вигляді гір він спочатку піднімається вертикально вгору до точки A', що має висоту $f$. Потім, залишаючись на тій самій висоті $f$, він летить навколо будь-яких перешкод, доки не досягне точки B', що знаходиться безпосередньо над B.

Спочатку визначте $f_{min}$ — мінімальну сталу висоту, що дозволяє такий переліт від A до B, залишаючись у вказаній області. Потім знайдіть довжину найкоротшого шляху між A' і B', летячи на цій сталій висоті $f_{min}$.

Надайте цю довжину як відповідь, округлену до трьох десяткових знаків.

**Примітка:** Для зручності функція висоти, наведена вище, повторена нижче у формі, придатній для більшості мов програмування: `h=( 5000-0.005*(x*x+y*y+x*y)+12.5*(x+y) )* exp( -abs(0.000001*(x*x+y*y)-0.0015*(x+y)+0.7) )`.

# --hints--

`mountainRange()` має повертати `2531.205`.

```js
assert.strictEqual(mountainRange(), 2531.205);
```

# --seed--

## --seed-contents--

```js
function mountainRange() {

  return true;
}

mountainRange();
```

# --solutions--

```js
// solution required
```
