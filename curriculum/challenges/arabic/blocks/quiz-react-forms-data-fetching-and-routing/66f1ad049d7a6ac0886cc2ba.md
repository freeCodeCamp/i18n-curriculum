---
id: 66f1ad049d7a6ac0886cc2ba
title: اختبار نماذج React، جلب البيانات والتوجيه
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

لتجاوز الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي مما يلي يُعد مثالًا على مُدخل مُتحكم به؟

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

أي مما يلي يُعد مثالًا على مُدخل غير مُتحكم به؟

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

أي مما يلي ليس طريقة شائعة لجلب البيانات في React؟

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

أي من الـ hooks التالية يرتبط عادة بجلب البيانات في React؟

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

ما هي تقنية memoization؟

#### --distractors--

خوارزمية شائعة تُستخدم لإيجاد أقصر مسار بين رأسين في رسم بياني.

---

تقنية خاصة تُستخدم لتحسين الأداء لجميع المُدخلات المُتحكم بها وغير المُتحكم بها.

---

خوارزمية تقسيم وغزو تُستخدم لفرز المصفوفات الفرعية بشكل متكرر ودمجها معًا.

#### --answer--

تقنية تحسين تُخزن (تتذكر) نتائج استدعاءات الدوال المكلفة بناءً على معلمات محددة.

### --question--

#### --text--

أي من الـ hooks التالية تُستخدم لتخزين القيم المحسوبة؟

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

أي مما يلي يستخدم الـ hook الصحيح لتخزين مراجع الدوال؟

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

ما هي التبعية؟

#### --distractors--

التبعية هي مكتبة اختبار شائعة الاستخدام في تطبيقات React.

---

التبعية هي نوع من مكونات React تعمل فقط على الخادم.

---

التبعية هي مترجم خاص يُستخدم في تطبيقات React لتحويل شفرة React إلى JavaScript.

#### --answer--

التبعية هي عندما يعتمد مكوِّن أو وحدة في التطبيق على آخر ليعمل بشكل صحيح.

### --question--

#### --text--

ما هو مدير الحزم؟

#### --distractors--

أداة تُستخدم لتحديث الحزم الخارجية المثبتة في التطبيقات.

---

أداة تُستخدم لإدارة الحزم التي تم إنشاؤها فقط داخل المشروع.

---

أداة تُستخدم لنقل التبعيات من مجلد `node_modules` إلى مجلد إعدادات خاص.

#### --answer--

أداة تُستخدم للتثبيت والتحديث وإزالة التبعيات.

### --question--

#### --text--

أي مما يلي ليس مدير حزم شائع الاستخدام في نظام JavaScript البيئي؟

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

أي من الملفات التالية هو ملف إعدادات رئيسي يحتوي على بيانات وصفية عن مشروعك؟

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

أي من الأُطُر التالية هو إطار عمل React شائع الاستخدام؟

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

ما هي الطريقة الصحيحة لإضافة المسارات إلى تطبيق React باستخدام React Router؟

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

ما هي الطريقة الصحيحة لإنشاء مسار ديناميكي في React Router؟

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

أي من الـ hooks التالية تُستخدم للوصول إلى المعلمات الديناميكية من مسار URL؟

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

ما هي الطريقة الصحيحة لإنشاء معالِج طلب مخصص في Next.js؟

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

ما هو prop drilling؟

#### --distractors--

عملية إنشاء مخفض يحدد كيف يجب أن يتغير الحالة استجابةً لاستخدام خصائص معينة.

---

عملية تمرير الخصائص من مكوِّن فرعي إلى المكوِّن الأب.

---

عملية إنشاء موفر سياق لتطبيق React واستخدامه لإرسال الخصائص من المكوِّن الأب إلى الأبناء.

#### --answer--

عملية تمرير الخصائص من مكوِّن أب إلى مكونات فرعية مسكنة بعمق، حتى عندما لا تحتاج بعض المكونات الفرعية إلى هذه الخصائص.

### --question--

#### --text--

أي مما يلي ليس حلًا شائعًا لإدارة الحالة في تطبيقات React؟

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

ما هو استخدام أدوات مطوِّري React؟

#### --distractors--

تُستخدم هذه الأدوات فقط لفحص مشاكل إمكانية الوصول في تطبيقات React.

---

تُستخدم هذه الأدوات لإضافة اختبارات إضافية لمكونات React.

---

تُستخدم هذه الأدوات لتخزين جميع مكونات React داخل التطبيق.

#### --answer--

تُستخدم هذه الأدوات لفحص مكونات React وتحديد مشاكل الأداء.

### --question--

#### --text--

ما هي مكونات خادم React؟

#### --distractors--

هي مكونات تدير السياق حصريًا على الخادم.

---

هي مكونات مخزنة تُعرض حصريًا على الخادم.

---

هي مكونات تدير الحالة حصريًا على الخادم.

#### --answer--

هي مكونات تُعرض حصريًا على الخادم.

