---
id: 66f1a2009e65c9a40a26d51e
title: React 기초 퀴즈
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

다음 중 JavaScript 라이브러리나 프레임워크가 아닌 것은 무엇인가요?

#### --distractors--

Angular

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

다음 중 React에서 조건부 렌더링을 위한 방법이 아닌 것은 무엇인가요?

#### --distractors--

삼항 연산자.

---

논리 `&&` 연산자.

---

`if` 문장.

#### --answer--

`setTimeout`

### --question--

#### --text--

React에서 리스트를 렌더링할 때 `key` 프로프를 추가하는 주된 목적은 무엇인가요?

#### --distractors--

각 리스트 항목에 고유한 CSS 스타일을 적용하기 위해서.

---

요소의 텍스트 내용을 제공하기 위해서.

---

리스트 내 각 특정 항목에 이벤트 리스너를 추가하기 위해서.

#### --answer--

React가 효율적으로 렌더링할 항목을 식별하도록 돕기 위해서.

### --question--

#### --text--

프로젝트의 패키지와 라이브러리는 어디에 설치되나요?

#### --distractors--

컴퓨터의 사용자 디렉터리.

---

`package.json` 파일.

---

`src` 폴더.

#### --answer--

`node_modules` 폴더.

### --question--

#### --text--

JSX에 대해 올바른 설명은 무엇인가요?

#### --distractors--

JSX는 항상 JavaScript와 별도의 파일에 작성해야 한다.

---

JSX는 JavaScript 표현식을 사용할 수 없다.

---

JSX는 React 애플리케이션에 권장되지 않는다.

#### --answer--

JSX는 JavaScript 코드 내에서 HTML을 작성할 수 있게 한다.

### --question--

#### --text--

다음 중 SPA에서 흔히 발생하는 문제는 무엇인가요?

#### --distractors--

서버 자원이 페이지를 계속 렌더링하느라 더 많이 사용된다.

---

자주 DOM이 업데이트되어 브라우저 캐싱이 신뢰할 수 없게 된다.

---

여러 페이지 로드로 인해 상태 관리가 일관되지 않게 된다.

#### --answer--

화면 읽기 프로그램이 동적으로 업데이트된 콘텐츠를 처리하기 어렵다.

### --question--

#### --text--

싱글 페이지 애플리케이션은 보통 어떻게 뷰를 업데이트하나요?

#### --distractors--

서버를 완전히 다시 로드한다.

---

전체 HTML 문서를 다시 로드한다.

---

서버 사이드 렌더링만 사용한다.

#### --answer--

페이지의 필요한 부분만 업데이트한다.

### --question--

#### --text--

다음 중 개발 모드에서 React 프로젝트를 시작하는 명령어는 무엇인가요?

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

다음 중 유효하지 않은 JSX 요소는 무엇인가요?

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

JavaScript 배열 메서드 중 데이터를 JSX 요소 배열로 변환할 때 흔히 사용하는 것은 무엇인가요?

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

React에서 `key` 프로프를 전달할 때 모범 사례는 무엇인가요?

#### --distractors--

모든 경우에 배열 인덱스를 사용한다.

---

렌더링할 때마다 키를 무작위로 생성한다.

---

`key` 프로프를 아예 생략한다.

#### --answer--

데이터에서 고유하고 안정적인 ID를 사용한다.

### --question--

#### --text--

`package.json`에 나열된 의존성을 설치하는 명령어는 무엇인가요?

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

자식 컴포넌트에 `name` 프로프를 전달하려면 어떻게 해야 하나요?

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

React에서 전개 연산자를 사용해 여러 프로프를 전달하는 올바른 방법은 무엇인가요?

#### --distractors--

`<Child person={name..., age..., city...} />`.

---

`<Child spreader={...person} />`.

---

`<Child spread={person} />`.

#### --answer--

`<Child {...person} />`.

### --question--

#### --text--

React의 기본 구성 요소는 무엇인가요?

#### --distractors--

함수와 모듈.

---

DOM 요소와 이벤트.

---

클래스와 객체.

#### --answer--

재사용 가능한 컴포넌트.

### --question--

#### --text--

React에서 인라인 스타일을 추가하는 올바른 방법은 무엇인가요?

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

React 컴포넌트를 조건부로 렌더링하는 올바른 방법은 무엇인가요?

#### --distractors--

JSX 내부에서 `if` 문장을 사용한다.

---

`render()` 메서드 내에서 새 컴포넌트를 선언한다.

---

JSX 내부에서 `while` 루프를 사용한다.

#### --answer--

JSX 내에서 삼항 연산자 또는 논리 `&&` 연산자를 사용한다.

### --question--

#### --text--

다른 파일에서 컴포넌트를 가져올 때 사용하는 키워드는 무엇인가요?

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

SPA의 전체 의미는 무엇인가요?

#### --distractors--

서버 페이지 아키텍처.

---

선택적 페이지 애플리케이션.

---

정적 진행형 앱.

#### --answer--

싱글 페이지 애플리케이션.

### --question--

#### --text--

React에서 UI를 설명하는 데 사용하는 JavaScript 구문 확장은 무엇인가요?

#### --distractors--

TSC

---

HTML+

---

ReactML

#### --answer--

JSX
