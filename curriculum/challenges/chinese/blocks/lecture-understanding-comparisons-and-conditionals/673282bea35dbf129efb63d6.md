---
id: 673282bea35dbf129efb63d6
title: 什么是 switch 语句，它和 if/else if/else 有什么不同？
challengeType: 19
dashedName: what-are-switch-statements-and-how-do-they-differ-from-if-else-chains
---

# --interactive--

`switch` 语句和 `if/else if/else` 都是编程中的控制流结构，允许我们根据特定条件执行不同的代码块。 不过，它们有各自不同的特点和用途。

`switch` 语句对表达式进行计算，并将其值与一系列 `case` 子句匹配。 找到匹配后，将执行与该 `case` 相关的代码块。 以下是 `switch` 语句的基本结构：

```js
switch (expression) {
  case value1:
    // code to be executed if expression === value1
    break;
  case value2:
    // code to be executed if expression === value2
    break;
  default:
    // code to be executed if expression doesn't match any case
}
```

每个 `case` 结尾的 `break` 语句至关重要。 一旦执行了匹配的 `case`，它就会告诉程序退出 switch 代码块。 如果没有它，程序将继续执行后续 case，这种行为被称为“贯穿”。

`switch` 语句通常用于将单个变量与多个可能值进行比较。 当需要针对一个变量检查许多潜在条件时，它们尤其有用。 下面是一个示例，使用 `switch` 语句来显示一周的天数：

:::interactive_editor

```js
let dayOfWeek = 3; 

switch (dayOfWeek) {
    case 1:
        console.log("It's Monday! Time to start the week strong.");
        break;
    case 2:
        console.log("It's Tuesday! Keep the momentum going.");
        break;
    case 3:
        console.log("It's Wednesday! We're halfway there.");
        break;
    case 4:
        console.log("It's Thursday! Almost the weekend.");
        break;
    case 5:
        console.log("It's Friday! The weekend is near.");
        break;
    case 6:
        console.log("It's Saturday! Enjoy your weekend.");
        break;
    case 7:
        console.log("It's Sunday! Rest and recharge.");
        break;
    default:
        console.log("Invalid day! Please enter a number between 1 and 7.");
}
```

:::

`switch` 语句在处理单个变量的多种可能值时，可以更加简洁易读。 

而 `if/else if` 语句则更加灵活。 它们可以评估每个子句中的复杂条件和不同变量。 这使得它们适用于更广泛的场景。 下面是一个示例，说明何时可以使用 `if/else` 语句而不是 `switch` 语句：

:::interactive_editor

```js
let creditScore = 720; 
let annualIncome = 60000; 
let loanAmount = 200000; 

let eligibilityStatus;

if (creditScore >= 750 && annualIncome >= 80000) {
    eligibilityStatus = "Eligible for premium loan rates.";
} else if (creditScore >= 700 && annualIncome >= 50000) {
    eligibilityStatus = "Eligible for standard loan rates.";
} else if (creditScore >= 650 && annualIncome >= 40000) {
    eligibilityStatus = "Eligible for subprime loan rates.";
} else if (creditScore < 650) {
    eligibilityStatus = "Not eligible due to low credit score.";
} else {
    eligibilityStatus = "Not eligible due to insufficient income.";
}

console.log(eligibilityStatus);
```

:::

在这个例子中，我们有一个人的年收入和信用评分，并检查他有资格申请哪些类型的贷款。 由于我们要处理更复杂的逻辑评估和多个变量，因此这里最好使用 `if/else` 语句，而不是 `switch` 语句。

值得注意的是，JavaScript 中的 `switch` 语句使用严格相等（`===`），这意味着它们不执行类型强制。 在可预测性和避免细微错误方面，这可能是一个优势。

总之，虽然 `switch` 语句和 `if/else if` 链都允许你的代码中实现多分支逻辑，但它们各有优势。`switch` 语句擅长处理单个变量的多种可能值，而 `if/else if` 链则为复杂条件提供了更多灵活性。它们之间的选择通常取决于你的代码的具体需求以及个人或团队的编码风格偏好。

# --questions--

## --text--

如果在 `switch` 例中省略 `break` 语句，会发生什么？

## --answers--

`switch` 语句将报错。

### --feedback--

思考一下 `switch` 语句中的“贯穿”概念。

---

程序将立即退出 `switch` 代码块。

### --feedback--

思考一下 `switch` 语句中的“贯穿”概念。

---

无论是否匹配，代码都将继续下一个案例。

---

什么都不会发生，在 `switch` 语句中，`break` 是可选的。

### --feedback--

思考一下 `switch` 语句中的“贯穿”概念。

## --video-solution--

3

## --text--

以下哪项是使用 `switch` 语句而不使用 `if`/`else` 的关键优势？

## --answers--

`switch` 语句可以处理更复杂的条件。

### --feedback--

考虑一下 `switch` 语句的典型用例以及它们设计用于高效处理的内容。

---

`switch` 语句可以比较多个变量。

### --feedback--

考虑一下 `switch` 语句的典型用例以及它们设计用于高效处理的内容。

---

`switch` 语句在比较单个变量和多个值时通常更为简洁。

---

`switch` 语句总是比 `if`/`else` 执行得更快。

### --feedback--

考虑一下 `switch` 语句的典型用例以及它们设计用于高效处理的内容。

## --video-solution--

3

## --text--

在 JavaScript 中，`switch` 语句使用哪种比较类型？

## --answers--

宽松相等（`==`）。

### --feedback--

想一想 JavaScript 中的 `switch` 语句是否会在比较过程中执行类型强制。

---

严格相等（`===`）。

---

大于或相等于 (`>=`)。

### --feedback--

想一想 JavaScript 中的 `switch` 语句是否会在比较过程中执行类型强制。

---

小于或相等于（`<=`）。

### --feedback--

想一想 JavaScript 中的 `switch` 语句是否会在比较过程中执行类型强制。

## --video-solution--

2
