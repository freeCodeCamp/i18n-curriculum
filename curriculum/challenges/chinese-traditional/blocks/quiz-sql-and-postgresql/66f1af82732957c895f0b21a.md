---
id: 66f1af82732957c895f0b21a
title: SQL 和 PostgreSQL 測驗
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是關聯式資料庫？

#### --distractors--

一個結構化的資料庫，用於識別儲存項目之間的非關聯性。

---

一個儲存非結構化資料的資料庫。

---

基於平面檔案的系統。

#### --answer--

一個用來識別並管理資料表中資料之間關係的資料庫結構。

### --question--

#### --text--

非關聯式資料庫與關聯式資料庫有何不同？

#### --distractors--

非關聯式資料庫使用表格，而關聯式資料庫則不使用。

---

關聯式資料庫可以儲存非結構化資料。

---

非關聯式資料庫比關聯式資料庫較慢。

#### --answer--

非關聯式資料庫可以儲存非結構化資料。

### --question--

#### --text--

在 PostgreSQL 互動式終端機（`psql`）中執行 `\l` 指令會做什麼？

#### --distractors--

列出所有架構。

---

列出所有資料表。

---

連接到資料庫。

#### --answer--

列出所有資料庫。

### --question--

#### --text--

以下哪個指令會在 SQL 中創建新資料表？

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

在關聯式資料庫中，外鍵的目的是什麼？

#### --distractors--

允許資料複製。

---

用以唯一識別表格中的一列。

---

用於儲存大量文字資料。

#### --answer--

在兩個資料表之間建立關聯。

### --question--

#### --text--

哪個指令會為現有的資料表添加一個新的行？

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

當 Table A 中的一筆記錄對應到 Table B 中一筆或多筆記錄時，存在什麼型別的關係？

#### --distractors--

一對一 (1-1)

---

多對多（m-m）

---

多對一(m-1)

#### --answer--

一對多 (1-m)

### --question--

#### --text--

什麼是查詢？

#### --distractors--

一個請求以移除資料庫中的所有資料。

---

資料庫中用於唯一值的特殊列。

---

資料庫中用於唯一值的特殊行。

#### --answer--

對資料庫中特定資料的請求。

### --question--

#### --text--

如何在 SQL 中刪除資料表中的一個行？

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

`PRIMARY KEY` 條件在 SQL 中的作用是什麼？

#### --distractors--

將兩個資料表聯結在一起。

---

定義行的預設值。

---

允許該行為空值。

#### --answer--

為了確保該行包含唯一值。

### --question--

#### --text--

在 PostgreSQL 互動式終端機（`psql`）中，指令 `\c` 有什麼作用？

#### --distractors--

列出目前資料庫中的所有資料表。

---

顯示目前資料庫連接詳細資訊。

---

離開 `psql` shell。

#### --answer--

連接到指定的資料庫。

### --question--

#### --text--

關聯式資料庫中的複合鍵是什麼？

#### --distractors--

由另一個金鑰衍生出的金鑰。

---

一個根據資料更新頻繁變更的金鑰。

---

專門用於外鍵關係的鍵。

#### --answer--

由多個行組成以唯一識別一筆記錄的鍵。

### --question--

#### --text--

哪個指令會將資料插入到資料表中？

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

如何在 SQL 中重新命名一個行？

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

哪個查詢會刪除 `employees` 表中的所有記錄？

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

哪一個 SQL 條件專門確保一個行只能包含唯一值，且允許 null？

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

以下哪一項不是業界常用的關聯式資料庫範例？

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQL查詢

### --question--

#### --text--

在關聯式資料庫中，一對一（1-1）關係表述什麼？

#### --distractors--

Table A 中的每個記錄對應到 Table B 中的一個或多個記錄。

---

Table A 中的每個記錄都不對應 Table B 中的任何記錄。

---

Table A 中的每個記錄對應到 Table B 中的多個記錄，反之亦然。

#### --answer--

Table A 中的每個記錄都對應 Table B 中的唯一一個記錄。

### --question--

#### --text--

什麼指令會列出目前 PostgreSQL 資料庫中的所有資料表？

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

以下哪一項允許你將一個資料表與其自身連接？

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`


