---
id: 66edd07682767adff3a6231e
title: Quiz sulla manipolazione del DOM e sugli eventi click con JavaScript
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos'è il Document Object Model?

#### --distractors--

Un linguaggio di programmazione usato per interagire con una pagina HTML.

---

Un insieme di oggetti che contengono solo numeri in virgola mobile.

---

Un insieme di classi JavaScript che rappresentano elementi HTML.

#### --answer--

Un'interfaccia di programmazione che ti permette di interagire con documenti HTML.

### --question--

#### --text--

Cosa significa API?

#### --distractors--

Additional Programming Interface

---

Adding Programming Interface

---

Another Programming Interface

#### --answer--

Application Programming Interface

### --question--

#### --text--

Come puoi usare JavaScript per trovare tutte le àncore HTML senza collegamenti ipertestuali?

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

Cosa fa la proprietà `textContent`?

#### --distractors--

Rimuove il contenuto testuale da tutti gli elementi HTML.

---

Rimuove il contenuto testuale dal tuo file JavaScript.

---

Aggiunge testo a tutti gli array e oggetti nel tuo file JavaScript.

#### --answer--

Restituisce il contenuto testuale semplice di un elemento.

### --question--

#### --text--

Cosa fa la proprietà `innerHTML`?

#### --distractors--

È una proprietà di `Element` usata per ottenere tutto il contenuto testuale di un elemento HTML.

---

È una proprietà di `Element` usata per aggiungere annotazioni al tuo HTML.

---

È una proprietà di `Element` usata per attivare eventi nel tuo HTML.

#### --answer--

È una proprietà di `Element` usata per impostare o aggiornare parti del markup HTML.

### --question--

#### --text--

Quale delle seguenti aggiungerà un listener di eventi valido?

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

Quale dei seguenti NON è un metodo valido dell'oggetto `document`?

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()`

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

Quale delle seguenti imposterà l'attributo `class` su `my-class`?

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

Cos'è l'oggetto `Event`?

#### --distractors--

È un oggetto speciale usato per rimuovere eventi dal tuo progetto JavaScript.

---

Viene usato per aggiungere automaticamente eventi ai tuoi progetti JavaScript.

---

È un oggetto speciale usato quando si lavora con gli array.

#### --answer--

È un payload che si attiva quando un utente interagisce in qualche modo con la tua pagina web.

### --question--

#### --text--

Quale dei seguenti è un metodo usato per rimuovere un listener di eventi precedentemente aggiunto a un elemento usando il metodo `addEventListener()`?

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

Cosa sono i gestori di eventi in linea?

#### --distractors--

Attributi speciali su un elemento HTML usati per annullare tutti gli eventi.

---

Attributi speciali su un elemento HTML usati solo per attivare l'invio di moduli nel tuo codice JavaScript.

---

Attributi speciali su un elemento HTML usati per rimuovere eventi.

#### --answer--

Attributi speciali su un elemento HTML usati per eseguire codice JavaScript quando si verifica un evento.

### --question--

#### --text--

Quali sono le due categorie principali di API web?

#### --distractors--

API Java e API di terze parti.

---

API del browser e API Rust.

---

API di sistema e API di terze parti.

#### --answer--

API del browser e API di terze parti.

### --question--

#### --text--

Quale evento si attiva quando tutti gli elementi della pagina HTML sono stati analizzati dal browser?

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

Quale delle seguenti è una proprietà che rappresenta lo stile in linea di un elemento?

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

Qual è il modo corretto di usare un gestore di eventi in linea?

#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--

Quale dei seguenti metodi viene usato per ritardare un'azione per un tempo specificato?

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

Quale delle seguenti può essere usata per creare e controllare animazioni direttamente dentro JavaScript?

#### --distractors--

Allow Animations API

---

Control Animations API

---

Create Animations API

#### --answer--

Web Animations API

### --question--

#### --text--

Cosa fa la Canvas API?

#### --distractors--

Viene usata per animare grafica nel tuo file CSS.

---

Viene usata solo per rimuovere grafica dal tuo file JavaScript.

---

Viene usata per rimuovere grafica dal tuo file HTML.

#### --answer--

Viene usata per manipolare la grafica tramite il tuo file JavaScript.

### --question--

#### --text--

Cos'è la delega degli eventi?

#### --distractors--

Il processo di ascoltare eventi quando un evento viene annullato.

---

Il processo di ascoltare eventi quando si verifica un evento click.

---

Il processo di ascoltare eventi quando si verifica un evento di modifica.

#### --answer--

Il processo di ascoltare eventi che sono risaliti fino a un genitore.

### --question--

#### --text--

Quale dei seguenti NON è un metodo che puoi usare quando lavori con dialoghi e modali?

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`
