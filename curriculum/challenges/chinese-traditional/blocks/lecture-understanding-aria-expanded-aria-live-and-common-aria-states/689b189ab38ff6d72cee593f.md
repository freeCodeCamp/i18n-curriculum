---
id: 689b189ab38ff6d72cee593f
title: 如何讓動態且互動式內容可被存取？
challengeType: 19
dashedName: how-do-you-make-dynamic-and-interactive-content-accessible
---

# --interactive--

你之前已經學習過語意標記和 ARIA 屬性如何使內容可存取。在實際應用程式中，內容很少是靜態的，頁面經常透過 JavaScript 動態更新。當發生這種情況時，確保這些變更也反映在 HTML 中非常重要。這讓螢幕閱讀器能準確地向使用者傳達更新後的狀態。若沒有這樣做，依賴輔助工具的人可能永遠不會知道有變更，或可能會收到過時或誤導的資訊。

讓我們來看看一個常見的情境，JavaScript 變更元素的狀態，以及你如何透過更新 ARIA 屬性來保持你的 HTML 可存取。

假設你有一個按鈕可以開啟和關閉下拉選單：

:::interactive_editor

```html
<button id="menuButton" aria-expanded="false" aria-controls="menuList">
  Menu
</button>

<ul id="menuList" hidden>
  <li>List item 1</li>
  <li>List item 2</li>
</ul>
<script src="index.js"></script>
```

```js
const button = document.getElementById("menuButton");
const menu = document.getElementById("menuList");

button.addEventListener("click", () => {
  const expanded = button.getAttribute("aria-expanded") === "true";
  button.setAttribute("aria-expanded", String(!expanded));
  menu.hidden = expanded;
});
```

:::

HTML 設定了一個控制下拉選單的按鈕。該按鈕使用 `aria-expanded` 來顯示選單是否開啟，並使用 `aria-controls` 透過取用選單的 `id` 將其聯結到選單。

JavaScript 監聽選單按鈕的點擊事件。當被點擊時，它會切換 `aria-expanded` 的值並更新選單上的 `hidden` 屬性。這可保持選單的可見性與存取資訊同步，讓有視力的使用者和螢幕閱讀器使用者都知道選單是開啟還是關閉。

你將在接下來的課程中學習更多關於那些 ARIA 屬性。現在，只要記住當你的 JavaScript 改變元素的狀態時，你也應該更新相關的 ARIA 屬性，讓輔助技術能準確反映螢幕上的實際內容。

# --questions--

## --text--

當 JavaScript 變更頁面元素的狀態時，為什麼更新 HTML 很重要？

## --answers--

以便輔助技術能準確地向使用者傳達當前狀態。

---

因此使用輔助技術的使用者可以與該元素互動。

### --feedback--

請考慮如果 ARIA 屬性與頁面上顯示的內容不符，使用輔助技術的使用者可能會錯過什麼。

---

因此頁面在狀態變更時不會崩潰。

### --feedback--

請考慮如果 ARIA 屬性與頁面上顯示的內容不符，使用輔助技術的使用者可能會錯過什麼。

---

因此，元素的狀態可以在頁面載入之間自動存檔。

### --feedback--

請考慮如果 ARIA 屬性與頁面上顯示的內容不符，使用輔助技術的使用者可能會錯過什麼。

## --video-solution--

1

## --text--

`aria-controls` 屬性在選單按鈕上指定什麼？

## --answers--

頁面載入時選單的預設狀態。

### --feedback--

請回到課程中說明 `aria-controls` 的那一部分。

---

用於作用選單的鍵盤快速鍵。

### --feedback--

請回到課程中說明 `aria-controls` 的那一部分。

---

按鈕所控制的元素的類別名稱。

### --feedback--

請回到課程中說明 `aria-controls` 的那一部分。

---

按鈕所控制的元素的 ID。

## --video-solution--

4

## --text--

`aria-expanded` 屬性在選單按鈕上顯示什麼？

## --answers--

是否已點擊選單按鈕。

### --feedback--

請回到課程中說明 `aria-expanded` 的零件。

---

選單按鈕是啟用還是停用。

### --feedback--

請回到課程中說明 `aria-expanded` 的零件。

---

無論選單是開啟還是關閉。

---

選單是否包含子選單。

### --feedback--

請回到課程中說明 `aria-expanded` 的零件。

## --video-solution--

3
