---
id: 6a042b8be919e91c35460341
title: مقدمة إلى اختبار Express
challengeType: 8
dashedName: quiz-introduction-to-express
---

# --description--

لتجاوز الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما العلاقة بين واجهة برمجة التطبيقات (API) وخدمة الويب؟

#### --distractors--

كل واجهة برمجة تطبيقات هي خدمة ويب، لكن ليست كل خدمة ويب هي واجهة برمجة تطبيقات.

---

واجهات برمجة التطبيقات وخدمات الويب مفاهيم متطابقة.

---

خدمات الويب تعمل فقط مع REST، بينما تدعم واجهات برمجة التطبيقات عدة بروتوكولات.

#### --answer--

كل خدمة ويب هي واجهة برمجة تطبيقات، لكن ليست كل واجهة برمجة تطبيقات هي خدمة ويب.

### --question--

#### --text--

أي قسم علوي HTTP يستخدمه العميل ليخبر الخادم بصيغة الاستجابة التي يريدها؟

#### --distractors--

`Content-Type`

---

`Authorization`

---

`X-Requested-With`

#### --answer--

`Accept`

### --question--

#### --text--

ماذا يعني أن يكون النظام عديم الحالة في بنية REST؟

#### --distractors--

يخزن الخادم بيانات جلسة العميل بين الطلبات.

---

العميل لا يحتفظ بأي حالة خاصة به عبر الطلبات.

---

REST يقيّد كل الطلبات لتكون عمليات قراءة فقط.

#### --answer--

الخادم لا يخزن ذاكرة للطلبات السابقة.

### --question--

#### --text--

أي طريقة HTTP في REST تُستخدم لإزالة مورد؟

#### --distractors--

`GET`

---

`POST`

---

`PUT`

#### --answer--

`DELETE`

### --question--

#### --text--

ما هي الميزة الرئيسية للخدمات المصغرة مقارنة بالنظام الأحادي؟

#### --distractors--

تصحيح أخطاء أبسط وعدد أقل من المكالمات الشبكية بين الخدمات.

---

قاعدة شفرة مشتركة واحدة تساهم فيها كل الفرق.

---

إدارة بنية تحتية أسهل عبر كل الخدمات.

#### --answer--

تطوير أسرع، عزل أفضل للأخطاء، والقدرة على استخدام لغات مختلفة لكل خدمة.

### --question--

#### --text--

ماذا يُرجع استدعاء `express()`؟

#### --distractors--

خادم HTTP جديد.

---

كائن موجّه.

---

دالة برمجيات وسيطة.

#### --answer--

مثيل جديد من تطبيق Express.

### --question--

#### --text--

ما هي البرمجيات الوسيطة في Express؟

#### --distractors--

طريقة مدمجة تُستخدم لبدء خادم Express.

---

نوع خاص من طرق HTTP المستخدمة في REST APIs.

---

أداة مدمجة لخدمة الملفات الثابتة من مجلد.

#### --answer--

شفرة تُنفذ بين استقبال الطلب وإرسال الاستجابة.

### --question--

#### --text--

ماذا يفعل `app.listen(port, callback)` في Express؟

#### --distractors--

يُعرّف مسارًا للطلبات الواردة.

---

يحمّل مكتبة Express.

---

ينشئ دالة برمجيات وسيطة جديدة.

#### --answer--

يبدأ الخادم ويبدأ الاستماع لطلبات HTTP الواردة.

### --question--

#### --text--

ما هو معلمة المسار في Express؟

#### --distractors--

مسار URL ثابت مثل `/home`.

---

سلسلة استعلام تُضاف إلى URL.

---

طريقة لخدمة الملفات الثابتة.

#### --answer--

عنصر نائب في URL مسبوق بـ `:` يلتقط قيمة ديناميكية.

### --question--

#### --text--

كيف تصل إلى القيمة الديناميكية الملتقطة من URL مثل `/post/42` في مسار Express معرف كـ `/post/:postId`؟

#### --distractors--

`req.query.postId`

---

`req.body.postId`

---

`req.url.postId`

#### --answer--

`req.params.postId`

### --question--

#### --text--

ماذا يفعل `res.json()` في Express؟

#### --distractors--

يرسل استجابة نصية عادية.

---

يعيد توجيه العميل إلى URL مختلف.

---

يعرض قالب محرك ويُرسل HTML الناتج.

#### --answer--

يرسل استجابة JSON ويحوّل تلقائيًا كائن JavaScript.

### --question--

#### --text--

ماذا يفعل `res.redirect(url)` في Express؟

#### --distractors--

يحدد رمز حالة HTTP.

---

يرسل استجابة JSON.

---

يعرض عرضًا باستخدام محرك القوالب.

#### --answer--

يعيد توجيه العميل إلى URL مختلف.

### --question--

#### --text--

ما الغرض من `app.route()` في Express؟

#### --distractors--

لإنشاء مثيل جديد من تطبيق Express.

---

لخدمة الملفات الثابتة من مجلد محدد.

---

لتعريف مسارات منفصلة لمسارات URL مختلفة.

#### --answer--

لربط عدة معالجات طرق HTTP لمسار واحد في كتلة واحدة.

### --question--

#### --text--

ماذا ينشئ `express.Router()`؟

#### --distractors--

مثيل جديد من تطبيق Express يحل محل `app` لمعالجة كل منطق الخادم.

---

دالة برمجيات وسيطة مخصصة لالتقاط ومعالجة أخطاء مستوى المسار.

---

خادم ملفات ثابتة مدمج يربط الملفات تلقائيًا بمسارات URL.

#### --answer--

كائن موجّه، يشبه تطبيق Express صغير، تعرف عليه المسارات وتثبته في التطبيق الرئيسي.

### --question--

#### --text--

كيف تثبت موجّهًا في تطبيق Express الرئيسي؟

#### --distractors--

باستدعاء `router.listen()`.

---

باستدعاء `router.start()`.

---

باستدعاء `app.get()` مباشرة داخل الموجّه.

#### --answer--

باستدعاء `app.use(path, router)`.

### --question--

#### --text--

أي برمجيات وسيطة مدمجة في Express تُستخدم لخدمة الملفات الثابتة؟

#### --distractors--

`express.json()`

---

`express.urlencoded()`

---

`express.raw()`

#### --answer--

`express.static()`

### --question--

#### --text--

ماذا يحدث عندما يطلب المستخدم ملفًا ثابتًا غير موجود في المجلد المهيأ بـ `express.static()`؟

#### --distractors--

يرمي Express خطأ ويتوقف الخادم.

---

يعيد Express توجيه المستخدم إلى الصفحة الرئيسية.

---

ينشئ Express ملفًا فارغًا في المسار المطلوب.

#### --answer--

يرد Express تلقائيًا برمز 404.

### --question--

#### --text--

أي مما يلي صحيح عن SOAP مقارنة بـ REST؟

#### --distractors--

SOAP يستخدم JSON وهو أكثر مرونة من REST، مما يجعله مثاليًا للتطبيقات الحديثة.

---

SOAP هو نوع خدمة الويب الأكثر استخدامًا اليوم، وقد حل محل REST في معظم الأنظمة.

---

SOAP يدعم عدة صيغ استجابة، بما في ذلك النص العادي وJSON والبيانات الثنائية.

#### --answer--

SOAP يستخدم XML فقط وشائع في أنظمة المؤسسات التي تحتاج إلى معايير وأمان أكثر صرامة.

### --question--

#### --text--

ما هو معالج المسار في Express؟

#### --distractors--

كائن تكوين يربط كل مسارات المسار المتاحة بطرق HTTP الخاصة بها.

---

دالة برمجيات وسيطة تعترض تلقائيًا وتخدم الملفات الثابتة من مجلد.

---

موصل قاعدة بيانات يعالج ويعالج استعلامات قاعدة البيانات المتعلقة بالمسار.

#### --answer--

دالة استدعاء راجع تُنفذ عندما يطابق الطلب مسارًا، مع وصول إلى `req` و `res`.

### --question--

#### --text--

ما فائدة استخدام `express.Router()` بدلاً من تعريف كل المسارات مباشرة في `app.js`؟

#### --distractors--

يسمح للخادم بمعالجة عدد أكبر بكثير من طلبات HTTP المتزامنة مرة واحدة.

---

ينشئ ويسجل تلقائيًا نقاط نهاية REST API بناءً على أسماء ملفات المسار.

---

يتيح للخادم البدء والتشغيل دون الحاجة إلى تحديد رقم منفذ.

#### --answer--

يفصل الاهتمامات ويحافظ على نظافة الملف الرئيسي بنقل المسارات المرتبطة إلى وحداتها الخاصة.
