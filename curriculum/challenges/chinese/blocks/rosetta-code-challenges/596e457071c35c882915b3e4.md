---
id: 596e457071c35c882915b3e4
title: 比较字符串列表
challengeType: 1
forumTopicId: 302235
dashedName: compare-a-list-of-strings
---

# --description--

列表是一组有序的值，可能包含副本。以下是一个示例：

```js
const list = [['AA',  'BB', 'CC'], ['AA', 'ACB', 'AA'], [], ['AA']];
````

给定一个任意数量字串的列表，实现一个函数以满足以下每个条件：

<ul>
  <li>test if they are all lexically equal</li>
  <li>test if every string is lexically less than the one after it  (i.e. whether the list is in strict ascending order)</li>
</ul>

# --hints--

`allEqual` 应该是一个函数。

```js
assert(typeof allEqual === 'function');
```

`azSorted` 应该是一个函数。

```js
assert(typeof azSorted === 'function');
```

`allEqual(["AA", "AA", "AA", "AA"])` 应该返回 true。

```js
assert(allEqual(testCases[0]));
```

`azSorted(["AA", "AA", "AA", "AA"])` 应该返回 false。

```js
assert(!azSorted(testCases[0]));
```

`allEqual(["AA", "ACB", "BB", "CC"])` 应该返回 false。

```js
assert(!allEqual(testCases[1]));
```

`azSorted(["AA", "ACB", "BB", "CC"])` 应该返回 true。

```js
assert(azSorted(testCases[1]));
```

`allEqual([])` 应该返回 true。

```js
assert(allEqual(testCases[2]));
```

`azSorted([])` 应该返回 true。

```js
assert(azSorted(testCases[2]));
```

`allEqual(["AA"])` 应该返回 true。

```js
assert(allEqual(testCases[3]));
```

`azSorted(["AA"])` 应该返回 true。

```js
assert(azSorted(testCases[3]));
```

`allEqual(["BB", "AA"])` 应该返回 false。

```js
assert(!allEqual(testCases[4]));
```

`azSorted(["BB", "AA"])` 应该返回 false。

```js
assert(!azSorted(testCases[4]));
```

# --seed--

## --after-user-code--

```js
const testCases = [['AA', 'AA', 'AA', 'AA'], ['AA', 'ACB', 'BB', 'CC'], [], ['AA'], ['BB', 'AA']];
```

## --seed-contents--

```js
function allEqual(arr) {

  return true;
}

function azSorted(arr) {

  return true;
}
```

# --solutions--

```js
function allEqual(a) {
  let out = true;
  let i = 0;
  while (++i < a.length) {
    out = out && (a[i - 1] === a[i]);
  } return out;
}

function azSorted(a) {
  let out = true;
  let i = 0;
  while (++i < a.length) {
    out = out && (a[i - 1] < a[i]);
  } return out;
}
```
