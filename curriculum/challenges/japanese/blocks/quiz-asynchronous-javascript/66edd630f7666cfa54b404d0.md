---
id: 66edd630f7666cfa54b404d0
title: 非同期JavaScriptクイズ
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--

クイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

非同期プログラミングとは何ですか？

#### --distractors--

コードを順番に実行する手法。

---

コードのエラーを処理する手法。

---

通常、サーバーからデータを取得または送信するためのネットワークリクエストの方法。

#### --answer--

メインスレッドをブロックせずにコードを同時に実行する手法。

### --question--

#### --text--

スレッドとは何ですか？

#### --distractors--

サーバーからデータを取得するための関数。

---

ウェブブラウザでJavaScriptコードを実行するプログラム。

---

別の関数に引数として渡される特別なタイプの関数。

#### --answer--

メインプログラムの流れとは独立して実行できる命令のシーケンス。

### --question--

#### --text--

JavaScriptのFetch APIの目的は何ですか？

#### --distractors--

非同期のコールバックを管理するため。

---

クッキーを作成および管理するため。

---

動的なHTMLコンテンツをレンダリングするため。

#### --answer--

サーバーへのネットワークリクエストを行うため。

### --question--

#### --text--

どの`HTTP`メソッドがサーバーにデータを送信して処理させるために使われますか？

#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--

APIからデータを取得する正しい方法はどれですか？

#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--

JavaScriptのPromiseとは何ですか？

#### --distractors--

Fetch APIを使う際に複数の操作を同期させる方法。

---

すべての高階関数で使わなければならない特別なタイプのコールバック関数。

---

Fetch APIを使う際のエラー処理に使う手法。

#### --answer--

非同期操作の最終的な完了（または失敗）を表すオブジェクト。

### --question--

#### --text--

Promiseのチェーンは何を可能にしますか？

#### --distractors--

複数の同期操作を並行して実行できるようにする。

---

同期操作をより効率的に処理できるようにする。

---

失敗した操作を自動的に再試行できるようにする。

#### --answer--

非同期操作を順番に連続して実行できるようにする。

### --question--

#### --text--

Promiseの成功時の処理に使うメソッドはどれですか？

#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--

Promiseチェーンのエラー処理に使うメソッドはどれですか？

#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--

次のうち有効なHTTPメソッドでないものはどれですか？

#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--

async関数を作成する正しい構文はどれですか？

#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--

async関数内で`await`キーワードは何をしますか？

#### --distractors--

新しいPromiseを作成する。

---

待たずにコードを即座に実行し、オブジェクトを返す。

---

Promiseの結果をログに出力し、`null`を返す。

#### --answer--

Promiseが解決されるまで関数の実行を一時停止する。

### --question--

#### --text--

ウェブサイトがユーザーの位置情報を取得するために使うAPIはどれですか？

#### --distractors--

Locator API

---

Geo API

---

Locate API

#### --answer--

Geolocation API

### --question--

#### --text--

スクリプトを非同期でダウンロードするが、HTML文書の解析が完了するまでスクリプトの実行を待つ属性はどれですか？

#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--

次のコードは何を返しますか？

```js
fetch('https://api.example.com/data')
```

#### --distractors--

データが破損しているというエラー。

---

データを取得できないというエラー。

---

何も返さず、JavaScriptはスクリプトの次の関数に進む。

#### --answer--

`Promise`で、`Response`オブジェクトに解決されるもの。

### --question--

#### --text--

Fetch APIのデフォルトのHTTPメソッドは何ですか？

#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--

サーバー上のリソースを削除するために使うメソッドはどれですか？

#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--

Promiseを作成する正しい方法はどれですか？

#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--

スクリプトファイルを非同期でダウンロードしつつHTML文書の解析を続けるようブラウザに指示する属性はどれですか？

#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--

Geolocation APIのどのメソッドがデバイスの現在位置を取得するために使われますか？

#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`
