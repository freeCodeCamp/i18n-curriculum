---
id: 5900f52c1000cf542c51003d
title: '問題 446：回縮 B'
challengeType: 1
forumTopicId: 302118
dashedName: problem-446-retractions-b
---

# --description--

對於每個整數 $n > 1$，函式族 $f_{n, a, b}$ 定義為：

$f_{n, a, b}(x) ≡ ax + b\bmod n$，其中 $a, b, x$ 為整數且 $0 \lt a \lt n$、$0 \le b \lt n$、$0 \le x \lt n$。

如果對每個 $0 \le x \lt n$ 都有 $f_{n, a, b}(f_{n, a, b}(x)) \equiv f_{n, a, b}(x)\bmod n$，我們將稱 $f_{n, a, b}$ 為一個縮回。

令 $R(n)$ 為 $n$ 的縮回數量。

$F(N) = \displaystyle\sum_{n = 1}^N R(n^4 + 4)$。

$F(1024) = 77\,532\,377\,300\,600$。

求 $F({10}^7)$。將你的答案對 $1\,000\,000\,007$ 取模。

# --hints--

`retractionsB()` 應該傳回 `907803852`。

```js
assert.strictEqual(retractionsB(), 907803852);
```

# --seed--

## --seed-contents--

```js
function retractionsB() {

  return true;
}

retractionsB();
```

# --solutions--

```js
// solution required
```
