---
id: 6a06cf65e8fcb644d2a04a29
title: اختبار برمجيات وسيطة في Express
challengeType: 8
dashedName: quiz-express-middleware
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هي البرمجيات الوسيطة في Express؟

#### --distractors--

دالة تبدأ خادم Express وتربطه بمنفذ.

---

كائن تكوين يحدد المسارات ومعالجاتها.

---

موصل قاعدة بيانات مدمج للتعامل مع البيانات الدائمة.

#### --answer--

دالة تُنفذ خلال دورة حياة طلب إلى الخادم.

### --question--

#### --text--

ما هي الأشياء الثلاثة التي يمكن للبرمجيات الوسيطة الوصول إليها؟

#### --distractors--

اتصال قاعدة البيانات، مخزن الجلسة، وتعريف المسار.

---

تكوين الخادم، رقم المنفذ، ومثيل التطبيق.

---

متغيرات البيئة، رؤوس الطلب، وحالة الاستجابة.

#### --answer--

كائن الطلب (`req`)، كائن الاستجابة (`res`)، ودالة `next`.

### --question--

#### --text--

ماذا يحدث إذا لم تستدعِ دالة البرمجيات الوسيطة `next()`؟

#### --distractors--

يتم تمرير التحكم تلقائيًا إلى البرمجيات الوسيطة التالية في المكدس.

---

يرمي Express خطأ غير معالج ويتوقف الخادم.

---

يُعاد محاولة الطلب من بداية مكدس البرمجيات الوسيطة.

#### --answer--

تتوقف دورة الطلب والاستجابة.

### --question--

#### --text--

ماذا يفعل `app.use()`؟

#### --distractors--

يعرّف معالج مسار `GET` لمسار محدد.

---

يبدأ الخادم ويبدأ الاستماع للاتصالات الواردة.

---

ينشئ مثيلًا جديدًا لتطبيق Express.

#### --answer--

يضيف البرمجيات الوسيطة على مستوى التطبيق بشكل عام.

### --question--

#### --text--

بأي ترتيب تُنفذ البرمجيات الوسيطة في تطبيق Express؟

#### --distractors--

بترتيب عكسي، بدءًا من آخر برمجيات وسيطة أُضيفت.

---

ترتيب أبجدي بناءً على أسماء دوال البرمجيات الوسيطة.

---

عشوائيًا، حسب الطلب الذي يصل أولًا.

#### --answer--

بالترتيب الذي تُضاف به إلى التطبيق.

### --question--

#### --text--

على ماذا ترتبط البرمجيات الوسيطة على مستوى التطبيق؟

#### --distractors--

مثيل مسار محدد تم إنشاؤه بـ `express.Router()`.

---

خادم HTTP الخاص بـ Node.js الذي يُرجع بواسطة `app.listen()`.

---

معالج مسار فردي معرف بـ `app.get()` أو `app.post()`.

#### --answer--

مثيل لتطبيق Express.

### --question--

#### --text--

أي مما يلي يمكن استخدامه لتسجيل البرمجيات الوسيطة على مستوى التطبيق؟

#### --distractors--

`app.listen()` أو طرق خاصة بالمسار مثل `app.get()` و `app.delete()`

---

`app.route()` أو طرق خاصة بالمسار مثل `app.patch()` و `app.options()`

---

`router.use()` أو طرق خاصة بالمسار مثل `router.get()` و `router.post()`

#### --answer--

`app.use()` أو طرق خاصة بالمسار مثل `app.get()` و `app.post()`

### --question--

#### --text--

على ماذا ترتبط البرمجيات الوسيطة على مستوى الموجّه؟

#### --distractors--

مثيل تطبيق Express الرئيسي الذي يُنشأ بـ `express()`.

---

كائن خادم وحدة `http` في Node.js.

---

دالة استدعاء راجع لمعالج مسار محدد.

#### --answer--

مثيل لـ `express.Router()`.

### --question--

#### --text--

أي طريقة تُستخدم لتطبيق البرمجيات الوسيطة على مستوى الموجّه؟

#### --distractors--

`app.use()`

---

`express.use()`

---

`app.router()`

#### --answer--

`router.use()`

### --question--

#### --text--

كيف تحدد نطاق البرمجيات الوسيطة على مستوى الموجّه لمسار محدد في تطبيقك؟

#### --distractors--

عن طريق استدعاء `router.scope('/path')` قبل تعريف المسارات.

---

عن طريق تمرير المسار مباشرة إلى `router.use()` في الملف الرئيسي.

---

عن طريق تعيين `router.basePath = '/path'` قبل تركيبه.

#### --answer--

عن طريق تركيب الموجّه باستخدام `app.use('/path', router)`.

### --question--

#### --text--

ما الذي يميز برمجيات الوسيطة لمعالجة الأخطاء عن البرمجيات الوسيطة العادية في Express؟

#### --distractors--

تستخدم `res.error()` بدلًا من `res.send()` للرد على العميل، متجاوزة تدفق الاستجابة العادي.

---

يجب تعريفها قبل جميع المسارات الأخرى لتتمكن من اعتراض الأخطاء قبل وصولها لأي معالج مسار.

---

تُسجل باستخدام `app.error()` بدلًا من `app.use()`، مما يشير إلى Express بأنها لمعالجة الأخطاء.

#### --answer--

لديها أربع معلمات (`err`، `req`، `res`، و `next`)، ومعلمة `err` تخبر Express بأنها معالج أخطاء.

### --question--

#### --text--

متى تُستدعى برمجيات الوسيطة لمعالجة الأخطاء؟

#### --distractors--

عندما يعيد معالج مسار استجابة بحالة 4xx أو 5xx.

---

عندما يستقبل الخادم أكثر من طلب في نفس الوقت.

---

عندما يستغرق الطلب وقتًا أطول من المهلة الافتراضية لإكماله.

#### --answer--

عندما يُمرر خطأ إلى `next(err)`.

### --question--

#### --text--

أين يجب وضع برمجيات الوسيطة لمعالجة الأخطاء في مكدس البرمجيات الوسيطة؟

#### --distractors--

في الأعلى، قبل تعريف أي مسارات.

---

مباشرة بعد المسار الذي من المرجح أن يرمي خطأ.

---

على نفس مستوى البرمجيات الوسيطة الأخرى، في أي موضع.

#### --answer--

في النهاية، بعد جميع البرمجيات الوسيطة والمسارات الأخرى.

### --question--

#### --text--

ماذا تفعل برمجيات الوسيطة المدمجة `express.json()`؟

#### --distractors--

تخدم ملفات JSON المخزنة في الدليل العام للعميل.

---

تحول كائن الاستجابة إلى JSON قبل إرساله إلى العميل.

---

تتحقق من أن جميع الطلبات الواردة تحتوي على رأس نوع محتوى JSON صالح.

#### --answer--

تحلل الطلبات الواردة التي تحتوي على حمولات JSON.

### --question--

#### --text--

ماذا تفعل `express.static()`؟

#### --distractors--

تحلل الطلبات الواردة التي تحتوي على بيانات نموذج مشفرة في URL.

---

تخزن مؤقتًا استجابات معالجات المسارات لتحسين الأداء.

---

تسجل مسارات ثابتة لا يمكن تغييرها أثناء وقت التشغيل.

#### --answer--

تخدم ملفات ثابتة مثل الصور وCSS وجافاسكريبت.

### --question--

#### --text--

ماذا تحلل `express.urlencoded()` وأين يتوفر الناتج؟

#### --distractors--

تحلل حمولات JSON وتجعل النتيجة متاحة على `req.json`.

---

تحلل سلاسل الاستعلام من URL وتجعلها متاحة على `req.query`.

---

تحلل بيانات النموذج متعددة الأجزاء وتجعل الملفات متاحة على `req.files`.

#### --answer--

تحلل الحمولات المشفرة في URL من نماذج HTML وتجعل البيانات متاحة على `req.body`.

### --question--

#### --text--

ماذا تفعل برمجيات الوسيطة الخارجية `cors`؟

#### --distractors--

تسجل جميع طلبات HTTP الواردة مع الطريقة، URL، ووقت الاستجابة.

---

تحلل ملفات تعريف الارتباط من الطلبات الواردة وتربطها بـ `req.cookies`.

---

تضغط أجسام الاستجابة لتقليل حجم البيانات المرسلة إلى العميل.

#### --answer--

تمكّن مشاركة الموارد عبر الأصول المختلفة (Cross-Origin Resource Sharing).

### --question--

#### --text--

أي مما يلي يصنف برمجيات وسيطة Express بشكل صحيح؟

#### --distractors--

`morgan` مدمجة بينما `express.static()` طرف ثالث

---

`cors` مدمجة بينما `morgan` طرف ثالث

---

`express.urlencoded()` مدمجة بينما `express.json()` طرف ثالث

#### --answer--

`express.json()` مدمجة بينما `cors` طرف ثالث

### --question--

#### --text--

ماذا تفعل برمجيات الوسيطة الخارجية `morgan`؟

#### --distractors--

تمكّن مشاركة الموارد عبر الأصول المختلفة.

---

تحلل ملفات تعريف الارتباط من الطلبات الواردة وتربطها بـ `req.cookies`.

---

تضغط أجسام الاستجابة لتقليل حجم البيانات المرسلة إلى العميل.

#### --answer--

تسجل طلبات HTTP.

### --question--

#### --text--

ماذا يحدث عندما تستدعي دالة برمجيات وسيطة `next()`؟

#### --distractors--

يُرسل الطلب فورًا إلى العميل بحالة 200.

---

تُعاد دالة البرمجيات الوسيطة الحالية من البداية.

---

يتخطى Express جميع البرمجيات الوسيطة المتبقية وينفذ معالج المسار النهائي.

#### --answer--

ينتقل التحكم إلى البرمجيات الوسيطة أو معالج المسار التالي في المكدس.
