---
id: 5900f5001000cf542c510012
title: '问题 404：交叉椭圆'
challengeType: 1
forumTopicId: 302072
dashedName: problem-404-crisscross-ellipses
---

# --description--

$E_a$ 是一个椭圆，其方程形式为 $x^2 + 4y^2 = 4a^2$。

$E_a'$ 是 $E_a$ 绕原点 $O(0, 0)$ 逆时针旋转 $θ$ 度后的图像，且 $0° &lt; θ &lt; 90°$。

<img alt="ellipse E_a and ellipse rotated by θ degrees E_a'" src="https://cdn.freecodecamp.org/curriculum/project-euler/crisscross-ellipses.gif" style="background-color: white; padding: 10px; display: block; margin-right: auto; margin-left: auto; margin-bottom: 1.2rem;">

$b$ 是距离原点最近的两个交集点到原点的距离，$c$ 是另外两个交集点的距离。

我们称有序三元组（$a$、$b$、$c$）为规范椭球三元组，如果 $a$、$b$ 和 $c$ 是正整数。

例如，（209,247,286）是规范的椭圆体三重态。

设 $C(N)$ 为满足 $a ≤ N$ 的不同规范椭球三元组 ($a$, $b$, $c$) 的数量。

可以验证 $C({10}^3) = 7$，$C({10}^4) = 106$ 和 $C({10}^6) = 11\\,845$。

查找 $C({10}^{17})$。

# --hints--

`crisscrossEllipses()` 应该返回 `1199215615081353`。

```js
assert.strictEqual(crisscrossEllipses(), 1199215615081353);
```

# --seed--

## --seed-contents--

```js
function crisscrossEllipses() {

  return true;
}

crisscrossEllipses();
```

# --solutions--

```js
// solution required
```
