---
id: 66edcd49e73385dd4df54ac7
title: JavaScriptのループに関するクイズ
challengeType: 8
dashedName: quiz-javascript-loops
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

反復処理を最もよく表しているのはどれですか？

#### --distractors--

正しく動作するまでコードを繰り返し書くプロセスです。

---

連続したメモリ領域にデータを格納する手法です。

---

接続を確立するためにコンポーネント間で信号を送るプロセスです。

#### --answer--

一連の命令を複数回繰り返すプロセスです。

### --question--

#### --text--

プログラミングで反復処理に使われる手法は何ですか？

#### --distractors--

再帰

---

条件分岐

---

コンパイル

#### --answer--

ループ処理

### --question--

#### --text--

次のうちイテラブルなオブジェクトはどれですか？

#### --distractors--

Undefined

---

Number

---

Boolean

#### --answer--

String

### --question--

#### --text--

JavaScriptでサポートされていないループの種類はどれですか？

#### --distractors--

`for` ループ。

---

`while` ループ。

---

`for...in` ループ。

#### --answer--

`if...else` ループ。

### --question--

#### --text--

`for` ループの宣言の正しい順序はどれですか？

#### --distractors--

```javascript
for (condition; increment/decrement; initialization) {
  statement;
}
```

---

```javascript
for (increment/decrement; condition; initialization) {
  statement;
}
```

---

```javascript
for (initialization; increment/decrement; condition) {
  statement;
}
```

#### --answer--

```javascript
for (initialization; condition; increment/decrement) {
  statement;
}
```

### --question--

#### --text--

条件が真かどうかをチェックする前にコードブロックを1回実行し、条件が真の間は繰り返し動作するループはどれですか？

#### --distractors--

`while` ループ。

---

`for...of` ループ。

---

`for...in` ループ。

#### --answer--

`do...while` ループ。

### --question--

#### --text--

次のうちイテラブルなオブジェクトの値を反復するループはどれですか？

#### --distractors--

`for...in` ループ。

---

`for` ループ。

---

`do...while` ループ。

#### --answer--

`for...of` ループ。

### --question--

#### --text--

無限ループを最もよく定義しているのはどれですか？

#### --distractors--

条件が偽になるまで実行されるループ。

---

1回だけ実行されて終了するループ。

---

固定回数の反復後に自動的に停止するループ。

#### --answer--

終了条件が満たされないか、存在しないループ。

### --question--

#### --text--

次のループは何回実行されますか？  

```js
for (let i = 2; i < 10; i+=2) {  
  console.log(i);  
}
```

#### --distractors--

5

---

9

---

10

#### --answer--

4

### --question--

#### --text--

オブジェクトの反復に最適なループはどれですか？

#### --distractors--

`for...of` ループ。

---

`do...while` ループ。

---

`for` ループ。

#### --answer--

`for...in` ループ。

### --question--

#### --text--

`for...in` ループと`for...of` ループの違いは何ですか？

#### --distractors--

`for...in` ループはプロパティの値を反復し、`for...of` ループはプロパティ名（キー）を反復します。

---

`for...in` ループは新しいプロパティを作成し、`for...of` ループは既存のプロパティを変更します。

---

`for...in` ループは文字列でのみ使え、`for...of` ループは文字列と数値の両方で使えます。

#### --answer--

`for...in` ループはプロパティ名（キー）を反復し、`for...of` ループはプロパティの値を反復します。

### --question--

#### --text--

次のうち無限ループを引き起こすのはどれですか？

#### --distractors--

変数の初期化。

---

最終的に偽になるループ条件。

---

インクリメント／デクリメントのロジック。

#### --answer--

常に真を返すループ条件。

### --question--

#### --text--

ループを完全に終了させるために使える文はどれですか？

#### --distractors--

`continue` 文。

---

`label` 文。

---

`skip` 文。

#### --answer--

`break` 文。

### --question--

#### --text--

現在の反復をスキップして次の反復に進むために使える文はどれですか？

#### --distractors--

`break` 文。

---

`label` 文。

---

`return` 文。

#### --answer--

`continue` 文。

### --question--

#### --text--

次のコードでコンソールに何が出力されますか？

```js
for (let i = 0; i < 25; i += 2) {
  if (i % 5 === 0) {
    continue;
  }
  if (i % 13 === 0) {
    break;
  }
  console.log(i);
}
```

#### --distractors--

`0`, `2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

---

`0`, `2`, `4`, `6`, `8`, `12`

---

`2`, `4`, `6`, `8`, `12`

#### --answer--

`2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

### --question--

#### --text--

次のコードはコンソールに何を出力しますか？

```javascript
for (let i = 1; i < 6; i++) {
  if (i === 4) break;
  console.log(i);
}
```

#### --distractors--

`1`, `2`, `3`, `4`, `5`, `6`

---

`1`, `2`, `3`, `4`, `5`

---

`1`, `2`, `3`, `4`

#### --answer--

`1`, `2`, `3`

### --question--

#### --text--

以下のコードでコンソールに何が出力されますか？

```javascript
const shoppingList = { tomatoes: 4, apples: 10 };
for (const item in shoppingList) {
  console.log(item);
}
```

#### --distractors--

`tomatoes - 4`, `apples - 10`

---

`4`, `10`

---

`tomatoes:4`, `apples:10`

#### --answer--

`tomatoes`, `apples`

### --question--

#### --text--

以下のコードのコンソール出力は何ですか？

```javascript
for (let i = 2; i <= 13; i++) {
  if (i % 2 === 0) continue;
  console.log(i);
}
```

#### --distractors--

`2`, `4`, `6`, `8`, `10`, `12`

---

`1`, `3`, `5`, `7`, `9`, `11`, `13`

---

`1`, `3`, `5`, `7`, `9`, `11`

#### --answer--

`3`, `5`, `7`, `9`, `11`, `13`

### --question--

#### --text--

以下のコードでコンソールに何が出力されますか？

```javascript
const fruits = ["Mango", "Pineapple", "Oranges"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

#### --distractors--

`0`, `1`, `2`

---

`Pineapple`, `Oranges`, `Mango`

---

`Oranges`, `Pineapple`, `Mango`

#### --answer--

`Mango`, `Pineapple`, `Oranges`

### --question--

#### --text--

以下のコードは何回文字列`in the loop`を出力しますか？

```javascript
let x = 0
while (x < 5) {
  console.log("in the loop");
}
```

#### --distractors--

3回

---

4回

---

5回

#### --answer--

無限回数
