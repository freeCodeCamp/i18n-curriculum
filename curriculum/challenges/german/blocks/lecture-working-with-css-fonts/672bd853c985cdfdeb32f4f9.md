---
id: 672bd853c985cdfdeb32f4f9
title: Was ist die text-shadow-Eigenschaft und wie funktioniert sie?
challengeType: 19
dashedName: what-is-the-text-shadow-property
---

# --interactive--

CSS wendet standardmäßig keine Schatten auf den Text an. Dies ist ein Beispiel für einen Absatz ohne Schatten. Wenn Sie jedoch Schatten hinzufügen möchten, können Sie die `text-shadow`-Eigenschaft verwenden.

```css
text-shadow: /* Values */
```

In CSS können Sie einen Schatten durch eine Kombination aus seinem `X`-Versatz, `Y`-Versatz, dem Unschärferadius und der Farbe beschreiben. Zuerst müssen Sie den `X`- und `Y`-Versatz angeben, die jeweils den horizontalen und vertikalen Abstand des Schattens vom Text darstellen. Diese Werte sind erforderlich.

Hier ist ein Beispiel, wie Sie einen `X`- und `Y`-Versatz auf einen Schatten anwenden. Wir verwenden die `text-shadow`-Eigenschaft mit einem x-Versatz von `3px` und einem y-Versatz von `2px`:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px;
}
```

:::

Im Browser sehen der Text und der Schatten ähnlich aus, aber wir können auch die Farbe des Schattens anpassen, indem wir den Wert vor oder nach dem Versatz angeben. Legen wir die Schattenfarbe fest. Wir verwenden hier eine hexadezimale Farbe, aber Sie können jedes gültige Farbformat verwenden.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px #00ffc3;
}
```

:::

Wir schreiben die Farbe nach dem Versatz, aber Sie können die Farbe auch vor dem Versatz schreiben, wenn Sie möchten. Es ist gleichwertig:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: #00ffc3 3px 2px;
}
```

:::

Im Browser hat der Schatten eine bestimmte Farbe, sodass wir ihn sehr leicht vom Text unterscheiden können. Jetzt, wo wir den Schatten vom Text unterscheiden können, ist es auch wichtig zu sehen, wie sich positive und negative Werte auf den Schattenversatz auswirken. Positive Werte des `X`- und `Y`-Versatzes verschieben den Schatten nach rechts bzw. nach unten, während negative Werte den Schatten nach links und oben verschieben. Hier ist ein Beispiel, das stattdessen negative Werte verwendet:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: -3px -2px #00ffc3;
}
```

:::

Jetzt verschiebt sich der Schatten relativ zum Text nach links und oben. Prima. Aber der Schatten sieht nicht sehr schön aus, weil er genau wie der Originaltext aussieht, nur dahinter platziert ist. Um ihn schöner aussehen zu lassen, müssen wir einen dritten Wert hinzufügen, den Unschärferadius. Dieser Wert ist optional, macht den Schatten aber viel weicher und subtiler. Der Standardwert des Unschärferadius ist null. Je höher der Wert, desto größer die Unschärfe, was bedeutet, dass der Schatten heller wird.

Hier setzen wir den Unschärferadius auf `3px` und verwenden wieder positive Werte für den Versatz:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px 3px #00ffc3;
}
```

:::

Jetzt sieht es langsam wie ein Schatten aus. Er ist unschärfer und subtiler, sodass wir den Fokus auf den Haupttext legen können, während der Schatten im Hintergrund etwas Stil hinzufügt. Von hier aus können wir die Werte ein wenig anpassen, bis wir eine Kombination aus Versatz, Farbe und Unschärferadius finden, die unseren Bedürfnissen entspricht.

Es ist auch hilfreich zu wissen, dass der Text mehr als einen Schatten haben kann. Sie müssen sie nur in derselben `text-shadow`-Eigenschaft durch Kommas getrennt schreiben. Sie werden in Schichten angewendet, von vorne nach hinten, wobei der erste Schatten oben liegt.

Hier ist ein Beispiel für einen Absatz mit drei Schatten, die in Schichten angewendet werden. Beachten Sie, dass alle diese Schatten in derselben `text-shadow`-Eigenschaft definiert und durch Kommas getrennt sind:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 
    3px 2px 3px #00ffc3, 
    -3px -2px 3px #0077ff, 
    5px 4px 3px #dee7e5;
}
```

:::

`text-shadow` ist eine leistungsstarke CSS-Eigenschaft, um Ihren Text hervorzuheben. Sie kann auch das Gesamtdesign Ihrer Web-Anwendung verbessern. Durch die Wahl von Versatz, Unschärfe und Farbe können Sie eine breite Palette von Schatteneffekten erzeugen.

# --questions--

## --text--

Was ist der Hauptzweck der `text-shadow`-CSS-Eigenschaft?

## --answers--

Die Schriftgröße von Text anpassen.

### --feedback--

Denken Sie darüber nach, wie die `text-shadow`-Eigenschaft das visuelle Erscheinungsbild von Text beeinflusst.

---

Die Farbe von Text ändern.

### --feedback--

Denken Sie darüber nach, wie die `text-shadow`-Eigenschaft das visuelle Erscheinungsbild von Text beeinflusst.

---

Einen Schatteneffekt für Text erzeugen.

---

Den Abstand zwischen Textzeilen steuern.

### --feedback--

Denken Sie darüber nach, wie die `text-shadow`-Eigenschaft das visuelle Erscheinungsbild von Text beeinflusst.

## --video-solution--

3

## --text--

Wie können Sie einen schwarzen Textschatten mit einem horizontalen Versatz nach rechts und einem Unschärferadius von 5 Pixeln erzeugen?

## --answers--

`text-shadow: 5px 0px 0px black;`

### --feedback--

Denken Sie an die Reihenfolge der Werte in der text-shadow-Eigenschaft.

---

`text-shadow: 5px 0px 5px black;`

---

`text-shadow: 5px 5px 0px black;`

### --feedback--

Denken Sie an die Reihenfolge der Werte in der text-shadow-Eigenschaft.

---

`text-shadow: 0px 5px 0px black;`

### --feedback--

Denken Sie an die Reihenfolge der Werte in der text-shadow-Eigenschaft.

## --video-solution--

2

## --text--

Wie können Sie einen mehrschichtigen Textschatteneffekt erzeugen?

## --answers--

Indem Sie mehrere Schatten in der text-shadow-Eigenschaft definieren.

---

Indem Sie Elemente ineinander verschachteln.

### --feedback--

Denken Sie darüber nach, wie man mehrere Schatteneffekte kombiniert.

---

Indem Sie die `filter`-Eigenschaft verwenden.

### --feedback--

Denken Sie darüber nach, wie man mehrere Schatteneffekte kombiniert.

---

Indem Sie ein Hintergrundbild anwenden.

### --feedback--

Denken Sie darüber nach, wie man mehrere Schatteneffekte kombiniert.

## --video-solution--

1
