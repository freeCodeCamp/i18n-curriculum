---
id: 672d45651d83b450801efb3a
title: Wie Sie ein Projekt erstellen und Ihren Code lokal in VS Code ausführen
challengeType: 19
dashedName: how-to-create-a-project-and-run-your-code-locally-in-vs-code
---

# --description--

Um zu beginnen, sollten Sie wissen, dass VS Code Projekte als „Workspaces“ betrachtet. Der Workspace ist das Verzeichnis, das Sie in VS Code geöffnet haben.

Zunächst müssen Sie eine Befehlszeilenschnittstelle verwenden, um ein neues Verzeichnis für Ihr neues Projekt zu erstellen. Ein Verzeichnis ist ein anderer Begriff für Ordner.

Befehlszeilenschnittstellen, kurz CLI, ermöglichen es Ihnen, über textbasierte Befehle mit Ihrem Betriebssystem zu interagieren. Mehr über diese Werkzeuge lernen Sie in zukünftigen Lektionen.

Wenn Sie Windows verwenden, können Sie die Eingabeaufforderung oder PowerShell nutzen. Wenn Sie einen Mac haben, öffnen Sie die Terminal-App.

Sobald die Befehlszeile geöffnet ist, navigieren Sie zum Home-Verzeichnis, indem Sie den `cd ~`-Befehl eingeben und `enter` auf Ihrer Tastatur drücken. Dann geben Sie den `mkdir my-project`-Befehl ein und drücken `enter`. `mkdir`, oder „make directory“, ist der Befehl, um ein neues Verzeichnis zu erstellen. Anschließend sollten Sie Ihr neues Verzeichnis mit VS Code öffnen.

Es gibt auch andere grafische Möglichkeiten, ein neues Verzeichnis zu erstellen.

Je nach Betriebssystem haben Sie möglicherweise ein Kontextmenü (oder Rechtsklick-Option), um VS Code direkt aus Ihrem Datei-Explorer zu öffnen. Sie können auch doppelt auf die VS Code-Anwendung klicken. Wenn Sie sich mit der Befehlszeile wohlfühlen, können Sie den Befehl `code /path/to/folder` ausführen.

Sobald es geöffnet ist, sollten Sie einen neuen leeren Workspace sehen.

Um eine neue Datei zu erstellen, klicken Sie auf das `File`-Menü oben links und wählen `New File...` aus. Benennen Sie dann Ihre Datei `index.html` und klicken Sie auf `Save`.

Jetzt sollten Sie Ihre neue Datei im Explorer-Tab sehen. Versuchen Sie, etwas HTML in Ihre `index.html`-Datei einzufügen.

Vielleicht sind Sie versucht, die HTML-Datei direkt im Browser zu öffnen. Das kann zwar funktionieren, führt aber oft zu unerwarteten Bugs, wie zum Beispiel, dass CSS nicht richtig geladen wird.

Stattdessen sollten Sie Ihre HTML-Datei hinter einem richtigen Webserver ablegen. Das klingt vielleicht einschüchternd, wenn Sie neu im Programmieren sind, aber es gibt tatsächlich eine Möglichkeit, dies direkt in VS Code zu machen.

Dafür müssen Sie zuerst die _Live Server_-Erweiterung installieren, die kostenlos im Marketplace verfügbar ist.

Klicken Sie auf den Erweiterungs-Tab in der linken Ecke und geben Sie `Live Server` in die Suchleiste ein.

Nachdem Sie diese installiert haben, sollten Sie einen `Go live`-Button in Ihrer Statusleiste sehen. Stellen Sie sicher, dass Ihre HTML-Datei geöffnet ist, und klicken Sie dann darauf.

Ihre neue Seite sollte automatisch in einem neuen Tab im Browser geöffnet werden. Falls nicht, können Sie direkt zu `http://localhost:5500/` gehen.

Herzlichen Glückwunsch! Sie haben nun Ihr eigenes Projekt in VS Code eingerichtet und können Ihre Änderungen mit Live Server live ansehen.

# --questions--

## --text--

Was betrachtet Visual Studio Code als „Workspace“?

## --answers--

Die gesamte Festplatte.

### --feedback--

In der Lektion wird erklärt, wie VS Code einen Workspace in Bezug auf Verzeichnisse definiert.

---

Das Verzeichnis, von dem aus Sie VS Code geöffnet haben.

---

Alle geöffneten Dateien in VS Code.

### --feedback--

In der Lektion wird erklärt, wie VS Code einen Workspace in Bezug auf Verzeichnisse definiert.

---

Der VS Code-Anwendungsordner.

### --feedback--

In der Lektion wird erklärt, wie VS Code einen Workspace in Bezug auf Verzeichnisse definiert.

## --video-solution--

2

## --text--

Warum empfiehlt die Lektion, einen Webserver zu verwenden, anstatt eine HTML-Datei direkt im Browser zu öffnen?

## --answers--

Es lädt schneller und erleichtert das Erkennen von Problemen im Code.

### --feedback--

In der Lektion wird ein spezielles Problem erwähnt, das beim direkten Öffnen von HTML-Dateien auftreten kann.

---

Es bietet bessere Sicherheit für große Softwareanwendungen.

### --feedback--

In der Lektion wird ein spezielles Problem erwähnt, das beim direkten Öffnen von HTML-Dateien auftreten kann.

---

Es vermeidet unerwartete Bugs, wie zum Beispiel, dass CSS nicht richtig geladen wird.

---

Es ermöglicht die Echtzeit-Zusammenarbeit mit anderen Entwicklern im Team.

### --feedback--

In der Lektion wird ein spezielles Problem erwähnt, das beim direkten Öffnen von HTML-Dateien auftreten kann.

## --video-solution--

3

## --text--

Welche VS Code-Erweiterung wird empfohlen, um einfach einen lokalen Webserver einzurichten?

## --answers--

Web Server.

### --feedback--

In der Lektion wird ausdrücklich erwähnt, eine Erweiterung aus dem Marketplace zu installieren, um Ihre HTML-Datei zu bedienen.

---

Live Server.

---

Local Host.

### --feedback--

In der Lektion wird ausdrücklich erwähnt, eine Erweiterung aus dem Marketplace zu installieren, um Ihre HTML-Datei zu bedienen.

---

HTML Preview.

### --feedback--

In der Lektion wird ausdrücklich erwähnt, eine Erweiterung aus dem Marketplace zu installieren, um Ihre HTML-Datei zu bedienen.

## --video-solution--

2
