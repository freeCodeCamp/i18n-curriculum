---
id: 67f41341453c2247fb2828f7
title: Quiz sulle strutture dati
challengeType: 8
dashedName: quiz-data-structures
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cosa descrive la notazione Big O nell'analisi degli algoritmi?

#### --distractors--

Il tempo esatto di esecuzione in secondi per un computer specifico.

---

La percentuale di righe di codice eseguite durante una run.

---

Quanto il codice è leggibile per altri sviluppatori.

#### --answer--

Come il tempo o lo spazio crescono in relazione alla dimensione dell'input (un limite superiore).

### --question--

#### --text--

Quando inizi una sfida algoritmica, qual è il miglior primo passaggio?

#### --distractors--

Iniziare subito a scrivere codice per prendere slancio.

---

Ottimizzare le prestazioni prima di capire il problema.

---

Scrivere test unitari solo dopo aver finito la soluzione.

#### --answer--

Chiarire il problema e i vincoli con esempi e casi limite.

### --question--

#### --text--

Qual è la differenza chiave tra array dinamici e array statici?

#### --distractors--

Gli array dinamici memorizzano valori di tipi diversi; gli array statici no.

---

Gli array statici permettono valori duplicati; gli array dinamici no.

---

Gli array dinamici sono più veloci degli array statici in ogni operazione.

#### --answer--

Gli array dinamici possono crescere o ridursi ridimensionandosi; gli array statici hanno una dimensione fissa.

### --question--

#### --text--

Qual è la complessità temporale ammortizzata per aggiungere un elemento alla fine di un array dinamico?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` ammortizzata.

### --question--

#### --text--

Perché consultare l’elemento k-esimo per indice in una lista concatenata singola richiede `O(n)` tempo?

#### --distractors--

La lista deve essere ridimensionata prima di ogni consultazione.

---

L’indice viene hashato e cercato in una tabella.

---

I nodi sono memorizzati contigui, quindi serve uno spostamento.

#### --answer--

Devi attraversare dal nodo testa fino al nodo k-esimo uno per uno.

### --question--

#### --text--

Quale funzionalità ha una lista doppiamente concatenata che una lista singolarmente concatenata non ha?

#### --distractors--

Accesso casuale a qualsiasi indice in `O(1)` tempo.

---

Un buffer array integrato per iterazioni più veloci.

---

Manutenzione automatica della lunghezza della lista come costante.

#### --answer--

Puntatori sia al nodo successivo che a quello precedente che permettono la traversata all’indietro.

### --question--

#### --text--

Quale delle seguenti descrive meglio uno stack?

#### --distractors--

First In, First Out (`FIFO`) con rimozioni dalla parte frontale.

---

Una struttura dove qualsiasi elemento può essere rimosso in `O(1)` tempo.

---

Un buffer circolare con accesso casuale in tempo costante.

#### --answer--

Last In, First Out (`LIFO`) con `push` e `pop` in cima.

### --question--

#### --text--

Quale operazione rimuove l’elemento dalla parte frontale di una coda?

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

Qual è la complessità temporale tipica nel caso medio per cercare un valore tramite chiave in una mappa hash?

#### --distractors--

`O(n)` perché tutte le chiavi devono essere scansionate sequenzialmente.

---

`O(log n)` a causa della ricerca binaria all’interno dei bucket.

---

`O(n log n)` perché le chiavi sono ordinate durante l’inserimento.

#### --answer--

`O(1)` in media con una buona funzione hash e basso fattore di carico.

### --question--

#### --text--

Quale garanzia fornisce una struttura dati set?

#### --distractors--

Gli elementi sono memorizzati in ordine ordinato per impostazione predefinita.

---

Sono permessi valori duplicati e mantenuti insieme.

---

Gli elementi sono indicizzati dalla loro posizione di inserimento.

#### --answer--

Memorizza solo elementi unici (senza duplicati).

### --question--

#### --text--

In un array dinamico, qual è la complessità temporale nel caso peggiore per inserire un elemento all’indice i (non alla fine)?

#### --distractors--

`O(1)`

---

`O(log n)`

---

`O(1)` ammortizzata

#### --answer--

`O(n)`

### --question--

#### --text--

Qual è la complessità temporale per inserire un nuovo nodo all’inizio di una lista concatenata singola?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

Quale operazione si usa per rimuovere un elemento da uno stack?

#### --distractors--

`push`

---

`dequeue`

---

Inserire in fondo.

#### --answer--

`pop`

### --question--

#### --text--

Quale delle seguenti descrive meglio una coda?

#### --distractors--

Last In, First Out (`LIFO`) con rimozioni in cima.

---

Accesso casuale a qualsiasi indice in `O(1)` tempo.

---

Gli elementi sono sempre mantenuti in ordine ordinato automaticamente.

#### --answer--

First In, First Out (`FIFO`) con `enqueue` in coda e `dequeue` in testa.

### --question--

#### --text--

Cos’è una collisione hash in una mappa hash?

#### --distractors--

Quando una chiave mappa a più valori distinti per progettazione.

---

Quando due chiavi identiche sono memorizzate in bucket diversi.

---

Quando la mappa esaurisce la memoria e deve essere ridimensionata.

#### --answer--

Quando due chiavi diverse producono lo stesso indice hash.

### --question--

#### --text--

Perché le mappe hash si ridimensionano (rehash) man mano che crescono?

#### --distractors--

Per ordinare le chiavi in ordine crescente per iterazioni più veloci.

---

Per comprimere i valori e ridurre la frammentazione della memoria.

---

Per evitare di attivare il garbage collector del linguaggio.

#### --answer--

Per mantenere basso il fattore di carico così che le operazioni medie restino `O(1)`.

### --question--

#### --text--

Quale affermazione sui set è vera?

#### --distractors--

I set preservano l’ordine di inserimento per definizione.

---

I set permettono elementi duplicati e tengono il conto.

---

I test di appartenenza al set sono `O(n log n)` in media.

#### --answer--

I test di appartenenza sono tipicamente `O(1)` in media.

### --question--

#### --text--

Quale complessità temporale cresce più velocemente di `O(n log n)` al crescere di n?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

Dopo aver implementato una soluzione brute-force, qual è un buon passaggio successivo?

#### --distractors--

Micro-ottimizzare i fattori costanti prima di misurare.

---

Scartare i test e riscrivere la soluzione da zero.

---

Evitare di considerare casi limite per mantenere il codice semplice.

#### --answer--

Analizzare la complessità temporale/spaziale e ottimizzare i colli di bottiglia identificati.

### --question--

#### --text--

Cosa misura la complessità spaziale?

#### --distractors--

Quanti core CPU usa un programma.

---

La lunghezza di un programma in righe di codice.

---

Quanto tempo impiega un programma a compilare.

#### --answer--

Come cresce l’uso di memoria in relazione alla dimensione dell’input.
