---
id: 637f4e3672c65bc8e73dfe23
videoId: V8UAEoOvqFg
title: HTML 基礎課程 F
challengeType: 15
dashedName: html-foundations-lesson-f
---

# --description--

完成 HTML 樣板所需的最後一個元素是 `<body>` 元素。這裡將放置所有會顯示給使用者的內容－文字、影像、列表、聯結等等。

為了完成樣板，請為 `index.html` 檔案添加一個 `body` 元素。`body` 元素也位於 `html` 元素內，且永遠在 `head` 元素之下，如下所示：

# --assignment--

觀看並跟隨 Kevin Powell 出色的 Building your first web page 影片。

---

透過刪除 `index.html` 檔案的內容並嘗試從記憶中重新寫出所有樣板程式碼，來建立一些肌肉記憶。如果你卡住了，前幾次必須偷看課程內容也不用擔心。只要持續練習，直到你能從記憶中完成幾次為止。

---

將你的樣板程式碼透過這個 [HTML validator](https://www.freeformatter.com/html-validator.html) 進行驗證。驗證工具可確保你的標記正確，並且是極佳的學習工具，因為它們會提供你可能經常犯且未察覺的語法錯誤回授，例如遺漏的結束標記和 HTML 中多餘的空格。

# --questions--

## --text--

`body` 元素的用途是什麼？

## --answers--

這裡會顯示所有關於網頁的重要資訊，例如 `title` 和 `meta` 頁籤。

---

這是你告訴瀏覽器如何正確呈現網頁的地方。

---

這裡將顯示頁面上的所有內容，例如影像、文字和聯結。


## --video-solution--

3
