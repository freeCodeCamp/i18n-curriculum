---
id: 5900f38f1000cf542c50fea2
title: '문제 35: 순환 소수'
challengeType: 1
forumTopicId: 302009
dashedName: problem-35-circular-primes
---

# --description--

숫자 197은 순환 소수라고 하는데, 그 이유는 숫자의 모든 회전인 197, 971, 719가 모두 소수이기 때문입니다.

100 이하에는 이런 소수가 13개 있습니다: 2, 3, 5, 7, 11, 13, 17, 31, 37, 71, 73, 79, 그리고 97.

`n` 이하에서, 100 ≤ `n` ≤ 1000000인 경우 순환 소수가 몇 개인지 구하세요.

**참고:**  

순환 소수의 개별 회전은 `n`를 초과할 수 있습니다.

# --hints--

`circularPrimes(100)`는 숫자를 반환해야 합니다.

```js
assert(typeof circularPrimes(100) === 'number');
```

`circularPrimes(100)`는 13을 반환해야 합니다.

```js
assert(circularPrimes(100) == 13);
```

`circularPrimes(100000)`는 43을 반환해야 합니다.

```js
assert(circularPrimes(100000) == 43);
```

`circularPrimes(250000)`는 45를 반환해야 합니다.

```js
assert(circularPrimes(250000) == 45);
```

`circularPrimes(500000)`는 49를 반환해야 합니다.

```js
assert(circularPrimes(500000) == 49);
```

`circularPrimes(750000)`는 49를 반환해야 합니다.

```js
assert(circularPrimes(750000) == 49);
```

`circularPrimes(1000000)`는 55를 반환해야 합니다.

```js
assert(circularPrimes(1000000) == 55);
```

# --seed--

## --seed-contents--

```js
function circularPrimes(n) {

  return n;
}

circularPrimes(1000000);
```

# --solutions--

```js
function rotate(n) {
  if (n.length == 1) return n;
  return n.slice(1) + n[0];
}

function circularPrimes(n) {
  // Nearest n < 10^k
  const bound = 10 ** Math.ceil(Math.log10(n));
  const primes = [0, 0, 2];
  let count = 0;

  // Making primes array
  for (let i = 4; i <= bound; i += 2) {
    primes.push(i - 1);
    primes.push(0);
  }

  // Getting upperbound
  const upperBound = Math.ceil(Math.sqrt(bound));

  // Setting other non-prime numbers to 0
  for (let i = 3; i < upperBound; i += 2) {
    if (primes[i]) {
      for (let j = i * i; j < bound; j += i) {
        primes[j] = 0;
      }
    }
  }

  // Iterating through the array
  for (let i = 2; i < n; i++) {
    if (primes[i]) {
      let curr = String(primes[i]);
      let tmp = 1; // tmp variable to hold the no of rotations
      for (let x = rotate(curr); x != curr; x = rotate(x)) {
        if (x > n && primes[x]) {
          continue;
        }
        else if (!primes[x]) {
          // If the rotated value is 0 then it isn't a circular prime, break the loop
          tmp = 0;
          break;
        }
        tmp++;
        primes[x] = 0;
      }
      count += tmp;
    }
  }
  return count;
}
```
