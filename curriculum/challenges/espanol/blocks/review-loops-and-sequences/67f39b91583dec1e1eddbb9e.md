---
id: 67f39b91583dec1e1eddbb9e
title: Revisión de Bucles y Secuencias
challengeType: 31
dashedName: review-loops-and-sequences
---

# --description--

## Listas en Python

- **Introducción**: En Python, el tipo de dato lista es una secuencia ordenada de elementos que pueden ser cadenas, números o incluso otras listas. Las listas son mutables y tienen índice basado en cero.

```python
cities = ['Los Angeles', 'London', 'Tokyo']
```

- **Acceder a elementos en una lista**: Para acceder a un elemento de la lista `cities`, puedes referenciar su número de índice en la secuencia:

```python
cities = ['Los Angeles', 'London', 'Tokyo']
cities[0] # Los Angeles
```

- **Acceder a elementos usando índices negativos**: Para acceder al último elemento de cualquier lista, puedes usar `-1` como número de índice:

```python
cities = ['Los Angeles', 'London', 'Tokyo']
cities[-1] # Tokyo
```

- El índice negativo se usa para acceder a elementos comenzando desde el final de la lista en lugar del principio en el índice `0`.

- **Crear listas usando el constructor `list()`**: Las listas también pueden crearse usando el constructor `list()`. El constructor `list()` se usa para convertir un iterable en una lista:

```python
developer = 'Jessica'

print(list(developer)) 
# Result: ['J', 'e', 's', 's', 'i', 'c', 'a']
```

- **Encontrar la longitud de una lista**: Puedes usar la función `len()` para obtener la longitud de una lista:

```python
numbers = [1, 2, 3, 4, 5]
len(numbers) # 5
```

- **Mutabilidad de listas**: Las listas son mutables, lo que significa que puedes actualizar cualquier elemento en la lista siempre que pases un número de índice válido. Para actualizar listas en un índice particular, puedes asignar un nuevo valor a ese índice:

```python
programming_languages = ['Python', 'Java', 'C++', 'Rust']
programming_languages[0] = 'JavaScript'
print(programming_languages) # ['JavaScript', 'Java', 'C++', 'Rust']
```

- **Error de índice fuera de rango**: Si pasas un índice (positivo o negativo) que está fuera de los límites de la lista, recibirás un `IndexError`:

```python
programming_languages = ['Python', 'Java', 'C++', 'Rust']
programming_languages[10] = 'JavaScript'

"""
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
IndexError: list assignment index out of range
"""
```

- **Eliminar elementos de una lista**: Los elementos pueden eliminarse de una lista usando la palabra clave `del`:

```python
developer = ['Jane Doe', 23, 'Python Developer']
del developer[1]
print(developer) # ['Jane Doe', 'Python Developer']
```

- **Verificar si un elemento existe en una lista**: La palabra clave `in` puede usarse para verificar si un elemento existe en una lista:

```py
programming_languages = ['Python', 'Java', 'C++', 'Rust']

'Rust' in programming_languages # True
'JavaScript' in programming_languages # False
```

- **Anidar listas**: Las listas pueden anidarse dentro de otras listas:

```python
developer = ['Alice', 25, ['Python', 'Rust', 'C++']]
```

- Para acceder a la lista anidada, necesitarás acceder usando el índice `2` ya que las listas tienen índice basado en cero.

```python
developer = ['Alice', 25, ['Python', 'Rust', 'C++']]
developer[2] # ['Python', 'Rust', 'C++']
```

- Para acceder más a fondo al segundo idioma de esa lista anidada, necesitarás usar el índice `1`:

```python
developer = ['Alice', 25, ['Python', 'Rust', 'C++']]
developer[2][1] # Rust
```

- **Desempaquetar valores de una lista**: Desempaquetar valores de una lista es una técnica usada para asignar valores de una lista a nuevas variables. Aquí un ejemplo para desempaquetar la lista `developer` en nuevas variables llamadas `name`, `age` y `job` así:

```py
developer = ['Alice', 34, 'Rust Developer']
name, age, job = developer
```

- Si el número de variables a la izquierda del operador de asignación no coincide con el total de elementos en la lista, recibirás un `ValueError`.

- **Recolectar elementos restantes de una lista**: Para recolectar cualquier elemento restante de una lista, puedes usar el operador asterisco (`*`) así:

```py
developer = ['Alice', 34, 'Rust Developer']
name, *rest = developer
```

- **Rebanar listas**: Rebanar es el concepto de acceder a una porción de una lista usando el operador de rebanado `:`. Para rebanar una lista que comienza en el índice `1` y termina antes del índice `3`, puedes usar la siguiente sintaxis:

```python
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
desserts[1:3] # ['Cookies', 'Ice Cream']
```

- **Intervalos de paso**: También es posible especificar un intervalo de paso que determina cuánto incrementar entre los índices. Aquí un ejemplo si quieres extraer una lista solo con números pares usando rebanado:

```python
numbers = [1, 2, 3, 4, 5, 6]
numbers[1::2] # [2, 4, 6]
```

## Métodos de listas

- **append()**: Se usa para agregar un elemento al final de la lista. Aquí un ejemplo usando el método `append()` para agregar el número `6` a esta lista `numbers`:

```py
numbers = [1, 2, 3, 4, 5]
numbers.append(6)
print(numbers) # [1, 2, 3, 4, 5, 6]
```

- **Agregar listas**: El método `append()` también puede usarse para agregar una lista al final de otra:

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.append(even_numbers)
print(numbers) # [1, 2, 3, 4, 5, [6, 8, 10]]
```

- **extend()**: Se usa para agregar múltiples elementos al final de una lista. Aquí un ejemplo de agregar los números `6`, `8` y `10` al final de la lista `numbers`:

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers) # [1, 2, 3, 4, 5, 6, 8, 10]
```

- **insert()**: Se usa para insertar un elemento en un índice específico de la lista. Aquí un ejemplo usando el método `insert()`:

```py
numbers = [1, 2, 3, 4, 5]
numbers.insert(2, 2.5)

print(numbers) # [1, 2, 2.5, 3, 4, 5]
```

- **remove()**: Se usa para eliminar un elemento de la lista. El método `remove()` solo eliminará la primera ocurrencia de un elemento en la lista:

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers) # [1, 2, 3, 4, 5, 5]

```

- **pop()**: Se usa para eliminar un elemento específico de la lista y devolverlo:

```py
numbers = [1, 2, 3, 4, 5]
numbers.pop(1) # The number 2 is returned
```

- Si no especificas un elemento para el método `pop`, se elimina el último elemento.

```py
numbers = [1, 2, 3, 4, 5]
numbers.pop() # The number 5 is returned
```

- **clear()**: Se usa para eliminar todos los elementos de la lista:

```py
numbers = [1, 2, 3, 4, 5]
numbers.clear()

print(numbers) # []
```

- **sort()**: El método `sort()` se usa para ordenar los elementos en su lugar. Aquí un ejemplo de ordenar una lista aleatoria de `numbers` en su lugar:

```py
numbers = [19, 2, 35, 1, 67, 41]
numbers.sort()

print(numbers) # [1, 2, 19, 35, 41, 67]
```

- **sorted()**: Una función integrada que devuelve una nueva lista ordenada en lugar de modificar la lista original:

```py
numbers = [19, 2, 35, 1, 67, 41]
sorted_numbers = sorted(numbers)

print(sorted_numbers) # [1, 2, 19, 35, 41, 67]
print(numbers) # [19, 2, 35, 1, 67, 41]
```

- **reverse()**: Se usa para invertir el orden de los elementos en una lista:

```py
numbers = [6, 5, 4, 3, 2, 1]
numbers.reverse()

print(numbers) # [1, 2, 3, 4, 5, 6]
```

- **index()**: Se usa para encontrar el primer índice donde se puede encontrar un elemento en una lista:

```python
programming_languages = ['Rust', 'Java', 'Python', 'C++']
programming_languages.index('Java') # 1
```

- Si el elemento no se puede encontrar usando el método `index()`, el resultado será un `ValueError`.

## Tuplas en Python

- **Definición**: Una tupla es un tipo de dato en Python usado para crear una secuencia ordenada de valores. Las tuplas pueden contener un conjunto mixto de tipos de datos:

```py
developer = ('Alice', 34, 'Rust Developer')
```

- Las tuplas son inmutables, lo que significa que los elementos en la tupla no pueden cambiarse una vez creados. Si intentas actualizar uno de los elementos en la tupla, obtendrás un `TypeError`:

```py
programming_languages = ('Python', 'Java', 'C++', 'Rust')
programming_languages[0] = 'JavaScript'

"""
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
TypeError: "tuple" object does not support item assignment
"""
```

- **Acceder a elementos de una tupla**: Para acceder a un elemento de una tupla, usa notación de corchetes y el número de índice:

```py
developer = ('Alice', 34, 'Rust Developer')
developer[1] # 34
```

- Se puede usar índice negativo para acceder a elementos comenzando desde el final de la tupla:

```py
numbers = (1, 2, 3, 4, 5)
numbers[-2] # 4
```

- Si intentas pasar un número de índice que excede o es igual a la longitud de la tupla, recibirás un `IndexError`:

```py
numbers = (1, 2, 3, 4, 5)
numbers[7]

"""
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
IndexError: tuple index out of range
"""
```

- Una tupla también puede crearse usando el constructor `tuple()`. Dentro del constructor, puedes pasar diferentes iterables como cadenas, listas e incluso otras tuplas.

```python
developer = 'Jessica'

print(tuple(developer)) 
# Result: ('J', 'e', 's', 's', 'i', 'c', 'a')
```

- **Verificar elementos en una tupla**: Para verificar si un elemento está en una tupla, puedes usar la palabra clave `in` así:

```py
programming_languages = ('Python', 'Java', 'C++', 'Rust')

'Rust' in programming_languages # True
'JavaScript' in programming_languages # False
```

- **Desempaquetar tuplas**: Los elementos pueden desempaquetarse de una tupla así:

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

- Si necesitas recolectar elementos restantes de una tupla, puedes usar el operador asterisco (`*`) así:

```py
developer = ('Alice', 34, 'Rust Developer')
name, *rest = developer
```

- **Rebanar tuplas**: El rebanado puede usarse para extraer una porción de una tupla. Por ejemplo, los elementos `pie` y `cookies` pueden rebanarse en una tupla separada:

```py
desserts = ('cake', 'pie', 'cookies', 'ice cream')
desserts[1:3] # ('pie', 'cookies')
```

- **Eliminar elementos de tuplas**: Eliminar un elemento de una tupla generará un `TypeError` ya que las tuplas son inmutables:

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]

"""
Traceback (most recent call last):
  File "<stdin>", line 1, in <module>
TypeError: "tuple" object doesn't support item deletion
"""
```

- **¿Cuándo usar una tupla vs una lista?**: Si necesitas una colección dinámica de elementos donde puedas agregar, eliminar y actualizar elementos, debes usar una lista. Si sabes que trabajas con una colección fija e inmutable de datos, debes usar una tupla.

## Métodos comunes de tuplas

- **`count()`**: Se usa para determinar cuántas veces aparece un elemento en una tupla. Por ejemplo, puedes verificar cuántas veces aparece el lenguaje `'Rust'` en la tupla:

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')
programming_languages.count('Rust') # 2
```

- Si el elemento especificado en el método `count()` no está presente en la tupla, entonces el valor de retorno será `0`:

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')
programming_languages.count('JavaScript') # 0
```

- Si no se pasan argumentos al método `count()`, entonces Python devolverá un `TypeError`.

- **index()**: Se usa para encontrar el índice donde está presente un elemento particular en la tupla. Aquí tienes un ejemplo de uso del método `index()` para encontrar el índice del lenguaje `'Java'`:

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust')
programming_languages.index('Java') # 1
```

- Si no se encuentra el elemento especificado, Python devolverá un `ValueError`.

- Puedes pasar un índice inicial opcional al método `index()` para especificar dónde comenzar a buscar el elemento en la tupla:

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3) # 5
```

- También puedes pasar un índice final opcional al método `index()` para especificar dónde detener la búsqueda del elemento en la tupla:

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python', 'JavaScript', 'Python')
programming_languages.index('Python', 2, 5) # 2
```

- **`sorted()`**: Se usa para ordenar los elementos en cualquier iterable y devolver una nueva lista ordenada. Aquí tienes un ejemplo de creación de una nueva lista de números usando la función `sorted()`:

```py
numbers = (13, 2, 78, 3, 45, 67, 18, 7)
sorted(numbers) # [2, 3, 7, 13, 18, 45, 67, 78]
```

- **Modificar el comportamiento de ordenamiento**: Puedes personalizar el comportamiento de ordenamiento para un iterable usando los argumentos opcionales `reverse` y `key`. Aquí tienes un ejemplo de uso del argumento `key` para ordenar elementos en una tupla por longitud:

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
sorted(programming_languages, key=len)

# Result
# ['C++', 'Rust', 'Java', 'Rust', 'Python', 'Python']
```

- Puedes crear una nueva lista de valores en orden inverso, usando el argumento `reverse` así:

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')

print(sorted(programming_languages, reverse=True))

# Result
# ['Rust', 'Rust', 'Python', 'Python', 'Java', 'C++']
```

## Ciclos en Python

- **Definición**: Los ciclos se usan para repetir un bloque de código un número determinado de veces.

- **Ciclo `for`**: Se usa para iterar sobre una secuencia (como una lista, tupla o cadena) y ejecutar un bloque de código para cada elemento en esa secuencia. Aquí tienes un ejemplo de uso de un ciclo `for` para iterar a través de una lista e imprimir cada lenguaje en la consola:

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)

"""
Result 

Rust
Java
Python
C++
"""
```

- Aquí tienes un ejemplo de uso de un ciclo `for` para recorrer la cadena `code` e imprimir cada carácter:

```py
for char in 'code':
    print(char)

"""
Result 

c
o
d
e
"""
```

- Los ciclos `for` pueden anidarse. Aquí tienes un ejemplo de uso de un ciclo `for` anidado:

```py
categories = ['Fruit', 'Vegetable']
foods = ['Apple', 'Carrot', 'Banana']

for category in categories:
    for food in foods:
        print(category, food)

"""
Result

Fruit Apple
Fruit Carrot
Fruit Banana
Vegetable Apple
Vegetable Carrot
Vegetable Banana
"""

```

- **Ciclo `while`**: Repite un bloque de código hasta que la condición sea `False`. Aquí tienes un ejemplo de uso de un ciclo `while` para un juego de adivinanzas:

```py
secret_number = 3
guess = 0

while guess != secret_number:
    guess = int(input('Guess the number (1-5): '))
    if guess != secret_number:
        print('Wrong! Try again.')

print('You got it!')

"""
Result

Guess the number (1-5): 2
Wrong! Try again.
Guess the number (1-5): 1
Wrong! Try again.
Guess the number (1-5): 3
You got it!
"""

```

- **Sentencias `break` y `continue`**: Se usan en ciclos para modificar la ejecución del ciclo.

- La sentencia `break` se usa para salir del ciclo inmediatamente cuando se cumple cierta condición. Aquí tienes un ejemplo de uso de la sentencia `break` para una lista de `developer_names`:

```py
developer_names = ['Jess', 'Naomi', 'Tom']

for developer in developer_names:
    if developer == 'Naomi':
        break
    print(developer)
```

- La sentencia `continue` se usa para saltar la iteración actual y pasar a la siguiente iteración del ciclo. Aquí tienes un ejemplo de uso de la sentencia `continue` en lugar de una sentencia `break`:

```py
developer_names = ['Jess', 'Naomi', 'Tom']

for developer in developer_names:
    if developer == 'Naomi':
        continue
    print(developer)
```

- Ambos ciclos `for` y `while` pueden combinarse con una cláusula `else`, que se ejecuta solo cuando el ciclo no fue terminado por un `break`:

```python
words = ['sky', 'apple', 'rhythm', 'fly', 'orange']

for word in words:
    for letter in word:
        if letter.lower() in 'aeiou':
            print(f"'{word}' contains the vowel '{letter}'")
            break
    else:
        print(f"'{word}' has no vowels")
```

## Rangos y su uso en ciclos

- **La función `range()`**: Se usa para generar una secuencia de enteros.

```py
range(start, stop, step)
```

- El argumento obligatorio `stop` es un entero (no inclusivo) que representa el punto final para la secuencia de números que se está generando. Aquí hay un ejemplo de cómo usar la función `range()`:

```py
for num in range(3):
    print(num)
```

- Si no se especifica un argumento `start`, entonces el valor predeterminado será `0`. Por defecto, la secuencia de enteros incrementa en `1`. Puedes usar el argumento opcional `step` para cambiar el valor de incremento predeterminado. Aquí tienes un ejemplo de generación de una secuencia de enteros pares desde 2 hasta pero sin incluir 11 (es decir, incluye 10):

```py
for num in range(2, 11, 2):
    print(num)
```

- Si no proporcionas ningún argumento a la función `range()`, obtendrás un `TypeError`.

- La función `range()` solo acepta enteros como argumentos y no flotantes. Usar flotantes también resultará en un `TypeError`:

```py
ERROR!
Traceback (most recent call last):
  File "<main.py>", line 1, in <module>
TypeError: 'float' object cannot be interpreted as an integer
```

- Puedes usar un entero negativo para el argumento `step` para generar una secuencia de enteros en orden decreciente:

```py
for num in range(40, 0, -10):
    print(num)
```

- La función `range()` también puede usarse para crear una lista de enteros usándola con el constructor `list`. El constructor `list` se usa para convertir un iterable en una lista. Aquí tienes un ejemplo de generación de una lista de enteros pares entre 2 y 10 inclusive:

```py
numbers = list(range(2, 11, 2))
print(numbers) # [2, 4, 6, 8, 10]
```

## Funciones `enumerate()` y `zip()` en Python

- **`enumerate()`**: se usa para iterar sobre una secuencia y llevar un registro del índice para cada elemento en esa secuencia. La función `enumerate()` toma un iterable como argumento y devuelve un objeto `enumerate` que consiste en el índice y valor de cada elemento en el iterable.

```python
languages = ['Spanish', 'English', 'Russian', 'Chinese']

for index, language in enumerate(languages):
    print(f'Index {index} and language {language}')

# Result
# Index 0 and language Spanish
# Index 1 and language English
# Index 2 and language Russian
# Index 3 and language Chinese

```

- La función `enumerate()` también puede usarse fuera de un ciclo `for`:

```python
languages = ['Spanish', 'English', 'Russian', 'Chinese']

print(list(enumerate(languages)))
# [(0, 'Spanish'), (1, 'English'), (2, 'Russian'), (3, 'Chinese')]
```

- La función `enumerate()` también acepta un argumento opcional `start` que especifica el valor inicial para el conteo. Si se omite este argumento, el conteo comenzará en `0`.

- **`zip()`**: se usa para iterar sobre múltiples iterables en paralelo. Aquí tienes un ejemplo usando la función `zip()` para iterar sobre `developers` y `ids`:

```python
developers = ['Naomi', 'Dario', 'Jessica', 'Tom']
ids = [1, 2, 3, 4]

for name, dev_id in zip(developers, ids):
    print(f'Name: {name}')
    print(f'ID: {dev_id}')


"""
Result

Name: Naomi
ID: 1
Name: Dario
ID: 2
Name: Jessica
ID: 3
Name: Tom
ID: 4
"""

```

## Comprehensiones de Lista en Python

- **Definición**: La comprensión de listas te permite crear una nueva lista en una sola línea combinando el ciclo y la condición directamente dentro de corchetes. Esto hace que el código sea más corto y a menudo más fácil de leer.

```py
even_numbers = [num for num in range(21) if num % 2 == 0]
print(even_numbers)
```

## Métodos Iterables

- **`filter()`**: se usa para filtrar elementos de un iterable basándose en una condición. Devuelve un iterador que contiene solo los elementos que satisfacen la condición. Aquí tienes un ejemplo de creación de una nueva lista con solo palabras de más de cuatro caracteres:

```py
words = ['tree', 'sky', 'mountain', 'river', 'cloud', 'sun']

def is_long_word(word):
    return len(word) > 4

long_words = list(filter(is_long_word, words))
print(long_words) # ['mountain', 'river', 'cloud']
```

- **`map()`**: se usa para aplicar una función a cada elemento en un iterable y devolver un nuevo iterable con los resultados. Aquí tienes un ejemplo de uso de la función `map()` para convertir una lista de temperaturas en celsius a fahrenheit:

```py
celsius = [0, 10, 20, 30, 40]

def to_fahrenheit(temp):
    return (temp * 9/5) + 32

fahrenheit = list(map(to_fahrenheit, celsius))
print(fahrenheit) # [32.0, 50.0, 68.0, 86.0, 104.0]
```

- **`sum()`**: se usa para obtener la suma de un iterable como una lista o tupla. Aquí tienes un ejemplo de uso de la función `sum()`:

```py
numbers = [5, 10, 15, 20]
total = sum(numbers)
print(total) # Result: 50
```

- También puedes pasar un argumento opcional `start` que establece el valor inicial para la suma. Aquí tienes un ejemplo actualizado usando el argumento `start` como argumento posicional:

```py
numbers = [5, 10, 15, 20]
total = sum(numbers, 10) # positional argument
print(total) # 60
```

- También puedes usar el argumento `start` como argumento con palabra clave así:

```py
numbers = [5, 10, 15, 20]
total = sum(numbers, start=10) # keyword argument
print(total) # 60
```

## Funciones Lambda

- **Definición**: Una función lambda en Python es una forma concisa de crear una función sin nombre (una función anónima).
- Las funciones lambda se usan a menudo como argumento para otra función. Aquí tienes un ejemplo de una función lambda:

```py
numbers = [1, 2, 3, 4, 5]

even_numbers = list(filter(lambda x: x % 2 == 0, numbers))
print(even_numbers)  # [2, 4]
```

- Las buenas prácticas para usar funciones lambda incluyen no asignarlas a una variable, mantenerlas simples y legibles, y usarlas para funciones cortas y puntuales.

# --assignment--

Revise los temas y conceptos de Bucles y Secuencias.
