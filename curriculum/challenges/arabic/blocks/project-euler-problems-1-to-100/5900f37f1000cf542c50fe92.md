---
id: 5900f37f1000cf542c50fe92
title: "المشكلة 19: عد أيام الأحد"
challengeType: 1
forumTopicId: 301827
dashedName: problem-19-counting-sundays
---

# --description--

تُعطى المعلومات التالية، لكن قد تفضل إجراء بعض البحث بنفسك.

<ul>
<li>كان 1 يناير 1900 يوم اثنين.</li>
  <li>سبعة وثلاثون يومًا في سبتمبر،<br>أبريل، يونيو ونوفمبر.<br>جميع الشهور الأخرى بها واحد وثلاثون يومًا،<br>باستثناء فبراير فقط،<br>الذي يحتوي على ثمانية وعشرين يومًا، سواء كان الجو ممطرًا أو مشمسًا.<br>وفي سنوات الكبيسة، يكون بها تسعة وعشرون يومًا.</li>
  <li>تحدث السنة الكبيسة في أي سنة تقبل القسمة على 4 بدون باقي، ولكن ليس في القرن إلا إذا كانت تقبل القسمة على 400.</li>
</ul>

كم عدد أيام الأحد التي وقعت في أول يوم من الشهر خلال القرن العشرين (1 يناير 1901 إلى 31 ديسمبر 2000)؟

# --hints--

يجب أن تُرجع الدالة `countingSundays(1943, 1946)` رقمًا.

```js
assert.isNumber(countingSundays(1943, 1946));
```

يجب أن تُرجع الدالة `countingSundays(1943, 1946)` القيمة 6.

```js
assert.strictEqual(countingSundays(1943, 1946), 6);
```

يجب أن تُرجع الدالة `countingSundays(1995, 2000)` القيمة 10.

```js
assert.strictEqual(countingSundays(1995, 2000), 10);
```

يجب أن تُرجع الدالة `countingSundays(1901, 2000)` القيمة 171.

```js
assert.strictEqual(countingSundays(1901, 2000), 171);
```

# --seed--

## --seed-contents--

```js
function countingSundays(firstYear, lastYear) {

  return true;
}

countingSundays(1943, 1946);
```

# --solutions--

```js
function countingSundays(firstYear, lastYear) {
  let sundays = 0;

  for (let year = firstYear; year <= lastYear; year++) {
    for (let month = 0; month <= 11; month++) {
      const thisDate = new Date(year, month, 1);
      if (thisDate.getDay() === 0) {
        sundays++;
      }
    }
  }
  return sundays;
}
```
