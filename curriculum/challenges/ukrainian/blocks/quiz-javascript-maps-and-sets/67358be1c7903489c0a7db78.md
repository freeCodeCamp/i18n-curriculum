---
id: 67358be1c7903489c0a7db78
title: "Тест з JavaScript: множини та мапи"
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що таке множина (Set) у JavaScript?

#### --distractors--

Структура даних, яка дозволяє дублікати значень.

---

Структура даних, що зберігає лише рядки та числа.

---

Колекція, яка не дозволяє видаляти свої елементи.

#### --answer--

Колекція, у якій значення може зустрічатися лише один раз.

### --question--

#### --text--

Який із наведених методів доступний у мапах (Maps), але відсутній у множинах (Sets)?

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

Який метод використовується для додавання елементів у множину (Set)?

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

Як перевірити, чи містить множина (Set) певне значення?

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

Для чого найкраще підходять множини (Sets)?

#### --distractors--

Керування парами ключ-значення у базі даних.

---

Створення впорядкованих списків елементів.

---

Зберігання ієрархічних структур даних у локальному сховищі браузера.

#### --answer--

Видалення дублікатів із масиву.

### --question--

#### --text--

Що таке WeakSet у JavaScript?

#### --distractors--

Колекція, схожа на множину, що дозволяє лише примітивні типи даних.

---

Колекція, схожа на множину, що дозволяє дублікати значень.

---

Колекція, схожа на множину, яка не дозволяє видаляти значення.

#### --answer--

Колекція, схожа на множину, що зберігає лише об’єкти.

### --question--

#### --text--

Яка різниця між Set і WeakSet?

#### --distractors--

WeakSet може зберігати будь-який тип значень, включно з числами.

---

Set не дозволяє дублікати, а WeakSet дозволяє.

---

Set дозволяє збирати сміття, а WeakSet — ні.

#### --answer--

Set може містити будь-який тип значень, а WeakSet — лише об’єкти.

### --question--

#### --text--

Що таке Map у JavaScript?

#### --distractors--

Колекція, що дозволяє лише унікальні значення.

---

Колекція, яка може використовувати лише рядки як ключі.

---

Колекція, яка не дозволяє видаляти свої елементи.

#### --answer--

Колекція пар ключ-значення будь-якого типу.

### --question--

#### --text--

Який метод використовується для додавання пар ключ-значення у Map?

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

Що виведе наступний код?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

Чим Map відрізняється від об’єкта (Object) у JavaScript?

#### --distractors--

Map дозволяє лише числа як ключі.

---

Об’єкти дозволяють будь-який тип ключів, а Map — лише рядки.

---

Map не підтримує ітерацію, а об’єкти — так.

#### --answer--

Map дозволяє будь-який тип ключів, а об’єкти — лише рядки та символи.

### --question--

#### --text--

Як видалити пару ключ-значення з Map?

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

Що повертає властивість `size` у Map?

#### --distractors--

Кількість індексів у Map.

---

Кількість унікальних значень у Map.

---

Загальний обсяг пам’яті, що використовується Map.

#### --answer--

Кількість пар ключ-значення у Map.

### --question--

#### --text--

Що таке WeakMap у JavaScript?

#### --distractors--

Колекція, схожа на Map, що дозволяє примітивні значення як ключі.

---

Колекція, схожа на Map, що блокує видалення елементів.

---

Колекція, схожа на Map, що автоматично сортує свої ключі.

#### --answer--

Колекція, схожа на Map, з слабкими референсами на ключі.

### --question--

#### --text--

Яка різниця між Map і WeakMap?

#### --distractors--

Map дозволяє лише примітивні типи як ключі, а WeakMap — лише об’єкти.

---

Map дозволяє збирати сміття, а WeakMap — ні.

---

WeakMap дозволяє дублікати ключів, а Map — ні.

#### --answer--

Map є ітерованим, а WeakMap — ні.

### --question--

#### --text--

Якого з наведених методів НЕ існує у WeakMap?

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

Що виведе наступний код?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

Як ітерувати над значеннями у множині (Set)?

#### --distractors--

Використовуючи метод `.map()`.

---

Використовуючи метод `.reduce()`.

---

Використовуючи метод `.filter()`.

#### --answer--

Використовуючи метод `.forEach()`.

### --question--

#### --text--

Які два методи множин (Sets) повертають `SetIterator`, що містить значення певної множини?

#### --distractors--

`entries()` і `values()`

---

`forEach()` і `values()`

---

`has()` і `add()`

#### --answer--

`keys()` і `values()`

### --question--

#### --text--

Що виведе наступний код?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
