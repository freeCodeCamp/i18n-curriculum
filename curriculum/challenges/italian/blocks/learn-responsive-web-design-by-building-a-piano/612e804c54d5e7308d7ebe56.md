---
id: 612e804c54d5e7308d7ebe56
title: Passaggio 5
challengeType: 0
dashedName: step-5
---

# --description--

All'interno del tuo elemento `.keys`, aggiungi sette elementi `div`. Assegna a tutti la classe `key`.

# --hints--

Dovresti creare sette nuovi elementi `div`.

```js
const divDivDiv = document.querySelectorAll('div');
assert(divDivDiv?.length === 9);
```

I tuoi sette nuovi elementi `div` devono essere all'interno del tuo elemento `.keys`.

```js
const keys = document.querySelector('.keys');
assert([...keys?.children].length === 7);
assert([...keys?.children].every(child => child?.tagName === 'DIV'));
```

I tuoi sette nuovi elementi `div` devono avere tutti l'insieme `class` impostato su `key`.

```js
const keys = document.querySelector('.keys');
assert([...keys?.children].every(child => child?.classList?.contains('key')));
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Piano</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
    --fcc-editable-region--
    <div id="piano">
      <div class="keys"></div>
    </div>
    --fcc-editable-region--
  </body>
</html>
```

```css

```
