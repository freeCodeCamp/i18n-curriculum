---
id: 69a92f0b9cc04bb0d5327bb5
title: implementar uma pilha
challengeType: 26
dashedName: implement-a-stack
---

# --description--

Neste laboratório, você vai implementar uma estrutura de dados pilha usando funções. Uma pilha é uma estrutura de dados Last-In-First-Out (LIFO), onde os elementos são adicionados e removidos do topo.

**Objetivo:** Atenda às user stories abaixo e faça todos os testes passarem para concluir o laboratório.

**User stories:**

1. Você deve ter uma função `initStack` que retorna um objeto com uma propriedade `collection` definida como um array vazio.
1. Você deve ter uma função `push` que adiciona um elemento ao topo da pilha.
1. Você deve ter uma função `pop` que remove e retorna o elemento do topo da pilha, ou `undefined` se não houver nenhum.
1. Você deve ter uma função `peek` que retorna o elemento do topo da pilha sem removê-lo, ou `undefined` se não houver nenhum.
1. Você deve ter uma função `isEmpty` que retorna `true` se a pilha não contiver elementos, e `false` caso contrário.
1. Você deve ter uma função `clear` que remove todos os elementos da pilha.

**Nota:** A maioria dos testes depende das funções `initStack` e `push`. Implemente-as primeiro, pois os testes para `pop`, `peek`, `isEmpty` e `clear` exigem adicionar elementos à pilha.

# --hints--

Você deve ter uma função `push`.

```js
assert.isFunction(push);
```

A função `push` deve adicionar um elemento ao topo da pilha.

```js
const stack = initStack();
push(stack, 'first');
push(stack, 'second');
assert.deepEqual(stack.collection, ['first', 'second']);
```

Você deve ter uma função `pop`.

```js
assert.isFunction(pop);
```

A função `pop` deve remover e retornar o elemento do topo da pilha.

```js
const stack = initStack();
push(stack, 'first');
push(stack, 'second');
push(stack, 'third');
assert.strictEqual(pop(stack), 'third');
assert.strictEqual(pop(stack), 'second');
```

A função `pop` deve retornar `undefined` se a pilha estiver vazia.

```js
const stack = initStack();
assert.isUndefined(pop(stack));
```

A função `pop` deve retornar valores falsy corretamente.

```js
const stack = initStack();
push(stack, '');
assert.strictEqual(pop(stack), '');
assert.lengthOf(stack.collection, 0);
```

Você deve ter uma função `peek`.

```js
assert.isFunction(peek);
```

A função `peek` deve retornar o elemento do topo da pilha sem removê-lo.

```js
const stack = initStack();
push(stack, 'first');
push(stack, 'second');
assert.strictEqual(peek(stack), 'second');
assert.strictEqual(peek(stack), 'second');
```

A função `peek` deve retornar `undefined` se a pilha estiver vazia.

```js
const stack = initStack();
assert.isUndefined(peek(stack));
```

A função `peek` deve retornar valores falsy corretamente.

```js
const stack = initStack();
push(stack, 0);
assert.strictEqual(peek(stack), 0);
```

Você deve ter uma função `isEmpty`.

```js
assert.isFunction(isEmpty);
```

A função `isEmpty` deve retornar `true` para uma pilha vazia.

```js
const stack = initStack();
assert.isTrue(isEmpty(stack));
```

A função `isEmpty` deve retornar `false` para uma pilha não vazia.

```js
const stack = initStack();
push(stack, 'element');
assert.isFalse(isEmpty(stack));
```

A função `isEmpty` deve retornar `false` quando o elemento do topo for falsy.

```js
const stack = initStack();
push(stack, false);
assert.isFalse(isEmpty(stack));
```

Você deve ter uma função `clear`.

```js
assert.isFunction(clear);
```

A função `clear` deve remover todos os elementos da pilha.

```js
const stack = initStack();
push(stack, 'first');
push(stack, 'second');
push(stack, 'third');
clear(stack);
assert.lengthOf(stack.collection, 0);
```

# --seed--

## --seed-contents--

```js
```

# --solutions--

```js
function initStack() {
  return {
    collection: []
  };
}

function print(stack) {
  console.log(stack.collection);
}

function push(stack, element) {
  stack.collection.push(element);
}

function pop(stack) {
  return stack.collection.pop();
}

function peek(stack) {
  return stack.collection[stack.collection.length - 1];
}

function isEmpty(stack) {
  return stack.collection.length === 0;
}

function clear(stack) {
  stack.collection = [];
}
```
