---
id: 66f1ae758b77cfc3e4da6151
title: TypeScript 퀴즈
challengeType: 8
dashedName: quiz-typescript
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

TypeScript란 무엇인가요?

#### --distractors--

시스템 프로그래밍에 사용되는 프레임워크입니다.

---

데이터베이스 관리 도구입니다.

---

웹사이트 스타일링에 사용되는 프로그래밍 언어입니다.

#### --answer--

정적 타이핑을 추가한 JavaScript의 상위 집합입니다.

### --question--

#### --text--

프로젝트에서 TypeScript를 사용해야 하는 이유는 무엇인가요?

#### --distractors--

애플리케이션에서 테스트가 필요 없게 만듭니다.

---

모든 다른 프로그래밍 언어보다 빠릅니다.

---

웹사이트 스타일링을 쉽게 만듭니다.

#### --answer--

JavaScript에 타입 정의를 추가하여 유지보수성을 향상시킵니다.

### --question--

#### --text--

TypeScript는 어떤 언어로 컴파일되나요?

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

다음 코드 스니펫의 출력 결과는 무엇인가요?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

TypeScript에서 인터페이스의 주요 목적은 무엇인가요?

#### --distractors--

TypeScript 코드를 JavaScript로 컴파일하는 것입니다.

---

새로운 데이터 타입을 만드는 것입니다.

---

새로운 JavaScript 객체를 만드는 것입니다.

#### --answer--

객체의 구조를 정의하는 것입니다.

### --question--

#### --text--

다음 중 TypeScript의 기본 원시 타입이 아닌 것은 무엇인가요?

#### --distractors--

string

---

boolean

---

number

#### --answer--

array

### --question--

#### --text--

다음 중 TypeScript에서 리터럴 타입을 올바르게 정의한 것은 무엇인가요?

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

TypeScript에서 `type` 키워드의 주요 목적은 무엇인가요?

#### --distractors--

변수를 선언하는 것입니다.

---

클래스를 만드는 것입니다.

---

인터페이스를 구현하는 것입니다.

#### --answer--

사용자 정의 타입을 정의하는 것입니다.

### --question--

#### --text--

다음 중 유니언 타입을 올바르게 정의한 것은 무엇인가요?

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

다음 중 `interface`에서만 가능하고 TypeScript의 `type`로는 할 수 없는 기능은 무엇인가요?

#### --distractors--

객체 형태 정의하기.

---

유니언 타입 만들기.

---

특정 값만 포함하는 타입 정의하기.

#### --answer--

클래스 구현 선언하기.

### --question--

#### --text--

TypeScript에서 제네릭을 사용하는 주요 목적은 무엇인가요?

#### --distractors--

클래스 간 상속을 구현하는 것입니다.

---

객체 구조를 정의하는 것입니다.

---

새로운 데이터 타입을 만드는 것입니다.

#### --answer--

여러 타입과 함께 작동할 수 있는 재사용 가능한 코드를 작성하는 것입니다.

### --question--

#### --text--

다음 중 TypeScript에서 제네릭 함수의 **적절한 예**는 무엇인가요?

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

TypeScript에서 타입 좁히기(type narrowing)란 무엇인가요?

#### --distractors--

컴파일된 JavaScript 크기를 줄이는 것입니다.

---

유니언 내 타입 수를 제한하는 것입니다.

---

TypeScript 프로젝트에서 사용하지 않는 타입을 제거하는 것입니다.

#### --answer--

유니언 타입을 더 구체적인 타입으로 좁히는 코드를 작성하는 것입니다.

### --question--

#### --text--

TypeScript 컴파일러 옵션을 설정하는 데 일반적으로 사용되는 파일은 무엇인가요?

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

`rootDir` 옵션은 `tsconfig.json` 파일에서 무엇을 지정하나요?

#### --distractors--

컴파일 시 사용할 TypeScript 버전입니다.

---

TypeScript 코드의 대상 런타임 환경(예: Node.js 또는 브라우저)입니다.

---

가져오기와 내보내기에 사용할 특정 모듈 시스템입니다.

#### --answer--

소스 파일이 위치한 디렉터리입니다.

### --question--

#### --text--

다음 예제 중 `instanceof`를 올바르게 사용하여 `value` 속성에 안전하게 접근하도록 타입을 좁힌 것은 무엇인가요?

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
TypeScript에서 `"strict": true`에 `tsconfig.json`을 설정하면 어떤 효과가 있나요?

#### --distractors--

코드를 실행하기 전에 실수를 잡을 수 있도록 정적 타입 검사를 활성화합니다.

---

항상 ES6 구문을 사용하도록 강제합니다.

---

코드를 실행하기 전에 컴파일하도록 강제합니다.

#### --answer--

`null` 또는 `undefined` 처리 요구와 암시적 `any` 방지 같은 타입 검사 옵션 세트를 활성화합니다.

### --question--

#### --text--

TypeScript에서 `never` 타입의 목적은 무엇인가요?

#### --distractors--

null 또는 undefined를 나타냅니다.

---

모든 가능한 값을 나타냅니다.

---

null이 아닌 모든 것을 나타냅니다.

#### --answer--

절대 발생하지 않는 값의 타입을 나타냅니다.

### --question--

#### --text--

다음 중 TypeScript에서 컴파일이 성공하는 `getRandomValue` 함수 호출은 무엇인가요?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

다음 코드에서 선택적 체이닝을 사용해 TypeScript 컴파일러 오류 `'email' is possibly 'null'`을 어떻게 고칠 수 있나요?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`
