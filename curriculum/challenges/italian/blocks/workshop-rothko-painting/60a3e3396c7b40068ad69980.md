---
id: 60a3e3396c7b40068ad69980
title: Passaggio 22
challengeType: 0
dashedName: step-22
---

# --description--

Aggiungi un altro `div` con un valore `class` di `two` subito sotto il tuo elemento `one`. Questo sarà il tuo secondo rettangolo.

# --hints--

Non dovresti modificare l'elemento `.one` esistente.

```js
assert(document.querySelectorAll('.one').length === 1);
```

Dovresti avere un secondo elemento `div` nel tuo elemento `.canvas`.

```js
assert(document.querySelector('.canvas').children[1].tagName === 'DIV');
```

Il tuo secondo elemento `div` dovrebbe avere un valore `class` di `two`.

```js
assert(document.querySelector('.canvas').children[1].className.split(' ').includes('two'));
```

Il tuo elemento `.two` dovrebbe venire dopo il tuo elemento `.one`.

```js
assert(document.querySelector('.two').previousElementSibling.className.split(' ').includes('one'));
```

# --seed--

## --seed-contents--

```css
.canvas {
  width: 500px;
  height: 600px;
  background-color: #4d0f00;
  overflow: hidden;
}

.frame {
  border: 50px solid black;
  width: 500px;
  padding: 50px;
  margin: 20px auto;
}

.one {
  width: 425px;
  height: 150px;
  background-color: #efb762;
  margin: 20px auto;
}
```

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Rothko Painting</title>
    <link href="./styles.css" rel="stylesheet">
  </head>
  <body>
    <div class="frame">
      <div class="canvas">
        <div class="one"></div>
--fcc-editable-region--
        
--fcc-editable-region--
      </div>
    </div>
  </body>
</html>
```
