---
id: 66f1b06a5a5d10cc100af620
title: Quiz su Git
challengeType: 8
dashedName: quiz-git
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos'è `git`?

#### --distractors--

Un tipo popolare di strumento per l'accessibilità.

---

Un linter comunemente usato nei progetti software.

---

Un tipo speciale di libreria JavaScript.

#### --answer--

Un sistema di controllo versione ti permette di tracciare e gestire le modifiche nel tuo progetto.

### --question--

#### --text--

Quale tra i seguenti è un popolare provider di controllo versione basato sul cloud?

#### --distractors--

Spring Boot

---

Laravel

---

Remix

#### --answer--

GitHub

### --question--

#### --text--

Cosa fa il comando `git config`?

#### --distractors--

Imposta variabili di configurazione per il rebase delle tue modifiche.

---

Imposta configurazioni per gestire solo repository remoti su GitLab.

---

Imposta configurazioni per risolvere conflitti di merge.

#### --answer--

Imposta variabili di configurazione responsabili di come Git opera sulla tua macchina.

### --question--

#### --text--

Quale comando mostra la versione di Git attualmente installata sulla tua macchina?

#### --distractors--

`git --versions`

---

`git --setVersion`

---

`git --set`

#### --answer--

`git --version`

### --question--

#### --text--

Quale tra i seguenti è uno strumento usato per svolgere attività specifiche di GitHub senza uscire dalla riga di comando?

#### --distractors--

GitHub CI/CD

---

GitHub Actions

---

GitHub Pages

#### --answer--

GitHub CLI

### --question--

#### --text--

Quale tra le seguenti è una funzionalità che ti permette di automatizzare flussi di lavoro direttamente nel tuo repository GitHub, inclusi build, test e deploy del codice?

#### --distractors--

GitHub Pipelines

---

GitHub Cloud

---

GitHub API

#### --answer--

GitHub Actions

### --question--

#### --text--

Quale tra i seguenti comandi inizializza un repository `git` vuoto?

#### --distractors--

`git status`

---

`git set`

---

`git log`

#### --answer--

`git init`

### --question--

#### --text--

Cosa fa il comando `git push`?

#### --distractors--

Questo comando serve a caricare tutta la cronologia Git su GitHub.

---

Questo comando serve a scaricare modifiche dal repository remoto sulla tua macchina locale.

---

Questo comando serve a caricare il tuo codice su GitHub e unirlo automaticamente nel ramo `main`.

#### --answer--

Questo comando serve a caricare i tuoi commit locali su un repository remoto.

### --question--

#### --text--

Quale tra i seguenti comandi è usato per clonare un repository?

#### --distractors--

`git cloning`

---

`git cloned`

---

`git clones`

#### --answer--

`git clone`

### --question--

#### --text--

Quale tra i seguenti comandi è usato per elencare tutti i commit precedenti con informazioni utili come autore, data del commit, messaggio del commit e hash del commit?

#### --distractors--

`git rebase`

---

`git status`

---

`git commit`

#### --answer--

`git log`

### --question--

#### --text--

Quale tra i seguenti comandi elenca tutti i repository remoti associati al tuo repository Git locale?

#### --distractors--

`git rebase -v`

---

`git pull -v`

---

`git checkout -v`

#### --answer--

`git remote -v`

### --question--

#### --text--

Cosa fa il comando `git reset`?

#### --distractors--

Questo comando ti permette di resettare tutti i tentativi di risoluzione dei conflitti di merge.

---

Questo comando ti permette di resettare tutti i tentativi di un `git rebase`.

---

Questo comando ti permette di resettare tutti i commit per il repository.

#### --answer--

Questo comando ti permette di resettare lo stato corrente di un ramo.

### --question--

#### --text--

Cos'è una pull request?

#### --distractors--

Una richiesta di unire una sequenza di commit da un ramo a un altro.

---

Una richiesta di risolvere tutti i conflitti di merge.

---

Una richiesta di rimuovere tutti i commit da un repository.

#### --answer--

Una richiesta di integrare le modifiche dal tuo ramo nel ramo obiettivo.

### --question--

#### --text--

Cosa rappresenta il ramo `main` in un progetto?

#### --distractors--

Rappresenta il ramo principale della documentazione per un'applicazione.

---

Rappresenta il ramo hotfix per un'applicazione.

---

Rappresenta il ramo principale delle funzionalità per un'applicazione.

#### --answer--

Rappresenta il ramo principale o di produzione per un'applicazione.

### --question--

#### --text--

Per cosa è meglio usare GitHub Pages?

#### --distractors--

È meglio per distribuire giochi in C# e Unity.

---

È meglio per distribuire applicazioni solo Python.

---

È meglio per distribuire siti web con pesante back-end.

#### --answer--

È meglio per distribuire siti statici.

### --question--

#### --text--

Quale tra i seguenti comandi è usato per mostrare lo stato attuale della tua directory di lavoro?

#### --distractors--

`git show`

---

`git check`

---

`git update`

#### --answer--

`git status`

### --question--

#### --text--

Quale comando usi per recuperare modifiche dal repository remoto?

#### --distractors--

`git get`

---

`git sync`

---

`git update`

#### --answer--

`git fetch`

### --question--

#### --text--

Cos'è un file `.gitignore`?

#### --distractors--

Un tipo speciale di file usato per mostrare tutta la cronologia `git`.

---

Un tipo speciale di file usato per mostrare le differenze nelle tue modifiche.

---

Un tipo speciale di file usato per risolvere conflitti in un progetto.

#### --answer--

Un tipo speciale di file usato per dire a `git` di non tracciare certi file e cartelle in un progetto.

### --question--

#### --text--

Quale tra i seguenti comandi è usato per elencare tutti i tuoi rami locali?

#### --distractors--

`git show`

---

`git log`

---

`git list`

#### --answer--

`git branch`

### --question--

#### --text--

Quale tra i seguenti comandi è usato per spostare o unire una sequenza di commit da un ramo a un altro?

#### --distractors--

`git remote`

---

`git merge`

---

`git switch`

#### --answer--

`git rebase`
