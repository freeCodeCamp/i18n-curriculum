---
id: 66edcd0ecb4b25dc64a34804
title: JavaScript 객체 퀴즈
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

다음 중 JavaScript의 객체를 설명하지 않는 것은 무엇인가요?

#### --distractors--

키-값 쌍의 모음입니다.

---

속성과 메서드의 컨테이너입니다.

---

비원시 데이터 타입입니다.

#### --answer--

원시 데이터 타입입니다.

### --question--

#### --text--

다음 객체 정의 중 올바르지 않은 것은 무엇인가요?

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

이 `job` 객체의 `person` 속성에 어떻게 접근할 수 있나요?

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

아래 `street` 객체의 `person` 속성에 어떻게 접근할 수 있나요?

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

다음 중 대괄호 표기법으로만 접근할 수 있는 속성 키는 무엇인가요?

#### --distractors--

알려져 있고 고정된 속성 키입니다.

---

객체의 여러 단계에 중첩된 속성 키입니다.

---

객체에 존재하지 않을 수도 있는 속성 키입니다.

#### --answer--

동적인 속성 키입니다.

### --question--

#### --text--

아래 `name` 객체의 `person` 속성에 구조 분해를 사용해 올바르게 접근하는 방법은 무엇인가요?

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

아래 `flour` 객체의 `recipe` 속성에 구조 분해를 사용해 올바르게 접근하는 방법은 무엇인가요?

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

JavaScript에서 구조 분해를 사용할 때 속성 키에 기본값을 할당하는 방법은 무엇인가요?

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

`car` 객체가 `year` 속성을 가지고 있는지 어떻게 확인할 수 있나요?

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

객체 메서드란 무엇인가요?

#### --distractors--

객체와 연관된 문자열입니다.

---

객체와 연관된 배열입니다.

---

객체와 연관된 숫자입니다.

#### --answer--

객체와 연관된 함수입니다.

### --question--

#### --text--

이 `add` 객체의 `calculator` 메서드를 호출해 `10`과 `7` 숫자를 더하려면 어떻게 해야 하나요?

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

객체에서 속성을 제거하는 연산자는 무엇인가요?

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

`score` 객체에 `player` 속성이 존재하는지 어떻게 확인할 수 있나요?

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

JavaScript에서 새 빈 객체를 만드는 방법은 무엇인가요?

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

JavaScript에서 선택적 체이닝을 사용하는 올바른 구문은 무엇인가요?

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

JavaScript에서 선택적 체이닝은 무엇을 위해 사용되나요?

#### --distractors--

기본값이 있는 변수를 정의하는 데 사용됩니다.

---

호출될 수도 있고 호출되지 않을 수도 있는 선택적 메서드를 객체 내에 정의하는 데 사용됩니다.

---

여러 메서드 호출을 한 문장에 체이닝해 더 간결한 코드를 작성하는 데 사용됩니다.

#### --answer--

객체의 속성이 `null`이거나 `undefined`일 수 있을 때 안전하게 접근하는 데 사용됩니다.

### --question--

#### --text--

JavaScript 객체를 JSON 문자열로 변환하는 메서드는 무엇인가요?

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

JSON 문자열을 다시 JavaScript 객체로 변환하는 메서드는 무엇인가요?

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

대괄호 표기법을 사용해 `color` 객체의 `toy` 속성에 접근하는 옵션은 무엇인가요?

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

JavaScript에서 점 표기법을 사용해 이 `home` 객체의 `learner` 전화번호에 어떻게 접근할 수 있나요?

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`
