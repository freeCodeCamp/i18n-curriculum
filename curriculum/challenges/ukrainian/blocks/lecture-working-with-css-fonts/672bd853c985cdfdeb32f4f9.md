---
id: 672bd853c985cdfdeb32f4f9
title: Що таке властивість text-shadow і як вона працює?
challengeType: 19
dashedName: what-is-the-text-shadow-property
---

# --interactive--

CSS за замовчуванням не застосовує тіні до тексту. Ось приклад абзацу без тіней. Але якщо вам потрібно додати тіні, ви можете використати властивість `text-shadow`.

```css
text-shadow: /* Values */
```

У CSS тінь можна описати через комбінацію її `X` зміщення, `Y` зміщення, радіуса розмиття та кольору. Спочатку потрібно вказати `X` та `Y` зміщення, які відповідно представляють горизонтальну та вертикальну відстань тіні від тексту. Ці значення є обов’язковими.

Ось приклад, як застосувати `X` та `Y` зміщення для тіні. Ми застосовуємо властивість `text-shadow` з x-зміщенням `3px` та y-зміщенням `2px`:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px;
}
```

:::

У браузері текст і тінь виглядатимуть схоже, але ми також можемо налаштувати колір тіні, вказавши значення перед або після зміщення. Давайте встановимо колір тіні. Тут ми використаємо шістнадцятковий колір, але ви можете використати будь-який дійсний формат кольору.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px #00ffc3;
}
```

:::

Ми пишемо колір після зміщення, але ви також можете написати колір перед зміщенням, якщо хочете. Це еквівалентно:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: #00ffc3 3px 2px;
}
```

:::

У браузері тінь матиме конкретний колір, тому ми легко відрізнимо її від тексту. Тепер, коли ми можемо розрізняти тінь і текст, також важливо побачити, як позитивні та від’ємні значення впливають на зміщення тіні. Позитивні значення `X` зміщення та `Y` зміщення відповідно перемістять тінь вправо та вниз, а від’ємні значення — вліво та вгору. Ось приклад із від’ємними значеннями:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: -3px -2px #00ffc3;
}
```

:::

Тепер тінь переміститься вліво та вгору відносно тексту. Чудово. Але тінь виглядає не дуже гарно, бо вона виглядає точно як оригінальний текст, але розташована позаду нього. Щоб зробити її приємнішою, потрібно додати третє значення — радіус розмиття. Це значення необов’язкове, але робить тінь більш плавною та делікатною. Значення радіуса розмиття за замовчуванням — нуль. Чим більше значення, тим більший розмиття, а отже тінь стає світлішою.

Тут ми встановлюємо радіус розмиття `3px` і повертаємося до позитивних значень зміщення:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 3px 2px 3px #00ffc3;
}
```

:::

Тепер це починає виглядати як тінь. Вона більш розмита і делікатна, тому ми можемо зосередитися на основному тексті, а тінь додає стилю на фоні. Звідси ми можемо трохи підкоригувати значення, доки не знайдемо комбінацію зміщення, кольору та радіуса розмиття, яка підходить нам.

Також корисно знати, що текст може мати більше ніж одну тінь. Просто потрібно записати їх у тій самій властивості `text-shadow`, розділені комами. Вони застосовуватимуться шарами, спереду назад, причому перша тінь буде зверху.

Ось приклад абзацу з трьома тінями, застосованими шарами. Зверніть увагу, що всі ці тіні визначені в одній властивості `text-shadow` і розділені комами:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p>Hello, World!</p>
```

```css
p {
  text-shadow: 
    3px 2px 3px #00ffc3, 
    -3px -2px 3px #0077ff, 
    5px 4px 3px #dee7e5;
}
```

:::

`text-shadow` — це потужна CSS-властивість, яка допомагає вашому тексту виділятися. Вона також може покращити загальний дизайн вашого вебзастосунку. Вибираючи зміщення, розмиття та колір, ви можете створити широкий спектр ефектів тіні.

# --questions--

## --text--

Яка основна мета CSS-властивості `text-shadow`?

## --answers--

Змінити розмір шрифту тексту.

### --feedback--

Подумайте, як властивість `text-shadow` впливає на візуальний вигляд тексту.

---

Змінити колір тексту.

### --feedback--

Подумайте, як властивість `text-shadow` впливає на візуальний вигляд тексту.

---

Створити ефект тіні для тексту.

---

Керувати відстанню між рядками тексту.

### --feedback--

Подумайте, як властивість `text-shadow` впливає на візуальний вигляд тексту.

## --video-solution--

3

## --text--

Як створити чорну тінь тексту з горизонтальним зміщенням вправо та радіусом розмиття 5 пікселів?

## --answers--

`text-shadow: 5px 0px 0px black;`

### --feedback--

Подумайте про порядок значень у властивості text-shadow.

---

`text-shadow: 5px 0px 5px black;`

---

`text-shadow: 5px 5px 0px black;`

### --feedback--

Подумайте про порядок значень у властивості text-shadow.

---

`text-shadow: 0px 5px 0px black;`

### --feedback--

Подумайте про порядок значень у властивості text-shadow.

## --video-solution--

2

## --text--

Як створити багатошаровий ефект тіні тексту?

## --answers--

Визначивши кілька тіней у властивості text-shadow.

---

Вклавши елементи один в одного.

### --feedback--

Подумайте, як поєднати кілька ефектів тіні.

---

Використовуючи властивість `filter`.

### --feedback--

Подумайте, як поєднати кілька ефектів тіні.

---

Застосувавши фонове зображення.

### --feedback--

Подумайте, як поєднати кілька ефектів тіні.

## --video-solution--

1
