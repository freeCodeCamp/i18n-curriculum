---
id: 5900f39c1000cf542c50feaf
title: '문제 48: 자기 거듭제곱'
challengeType: 1
forumTopicId: 302157
dashedName: problem-48-self-powers
---

# --description--

수열 1<sup>1</sup> + 2<sup>2</sup> + 3<sup>3</sup> + ... + 10<sup>10</sup> = 10405071317입니다.

수열 1<sup>1</sup> + 2<sup>2</sup> + 3<sup>3</sup> + ... + 1000<sup>1000</sup>의 마지막 열 자리 숫자를 구하세요.

# --hints--

`selfPowers(10, 3)`는 숫자를 반환해야 합니다.

```js
assert(typeof selfPowers(10, 3) === 'number');
```

`selfPowers(10, 3)`는 317을 반환해야 합니다.

```js
assert.strictEqual(selfPowers(10, 3), 317);
```

`selfPowers(150, 6)`는 29045를 반환해야 합니다.

```js
assert.strictEqual(selfPowers(150, 6), 29045);
```

`selfPowers(673, 7)`는 2473989를 반환해야 합니다.

```js
assert.strictEqual(selfPowers(673, 7), 2473989);
```

`selfPowers(1000, 10)`는 9110846700을 반환해야 합니다.

```js
assert.strictEqual(selfPowers(1000, 10), 9110846700);
```

# --seed--

## --seed-contents--

```js
function selfPowers(power, lastDigits) {

  return true;
}

selfPowers(1000, 10);
```

# --solutions--

```js
function selfPowers(power, lastDigits) {
  let sum = 0;
  const modulo = Math.pow(10, lastDigits);

  for (let i = 1; i <= power; i++) {
    let temp = i;
    for (let j = 1; j < i; j++) {
      temp *= i;
      temp %= modulo;
    }

    sum += temp;
    sum %= modulo;
  }

  return sum;
}
```
