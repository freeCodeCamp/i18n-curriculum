---
id: 6a02e90ad969a6287d1b4f87
title: HTTP 和网络标准模型测验
challengeType: 8
dashedName: quiz-http-and-the-web-standards-model
---

# --description--

要通过测验，你必须正确回答下面 20 道题中的至少 18 道。

# --quizzes--

## --quiz--

### --question--

#### --text--

DNS 代表什么，它的主要功能是什么？

#### --distractors--

数据命名系统，它在通过网络发送之前加密域名。

---

域节点服务器，它存储网站文件以便浏览器下载。

---

动态网络服务，它在设备连接时分配 IP 地址。

#### --answer--

域名系统，它将人类可读的域名转换为 IP 地址。

### --question--

#### --text--

在 DNS 解析过程中，哪个组件通过根服务器、顶级域服务器和权威名称服务器执行迭代查询？

#### --distractors--

权威名称服务器

---

顶级域服务器

---

根服务器

#### --answer--

递归解析器

### --question--

#### --text--

TCP 做了什么是单独的互联网协议（IP）做不到的？

#### --distractors--

它使用 IP 地址将数据包从源路由到目标。

---

它在通过网络发送之前加密数据。

---

它将域名分配给 IP 地址。

#### --answer--

它通过对分段编号并请求重传丢失的数据包，确保可靠、有序的传输。

### --question--

#### --text--

应该使用哪个 HTTP 方法来部分更新服务器上的现有资源？

#### --distractors--

`PUT`

---

`POST`

---

`DELETE`

#### --answer--

`PATCH`

### --question--

#### --text--

用于提供 JSON 响应的正确 `Content-Type` 头部是什么？

#### --distractors--

`text/json`

---

`text/plain`

---

`application/xml`

#### --answer--

`application/json`

### --question--

#### --text--

哪个 HTTP 状态码表示请求的资源已永久移动到新的 URL？

#### --distractors--

`302`

---

`304`

---

`404`

#### --answer--

`301`

### --question--

#### --text--

`401 Unauthorized` 和 `403 Forbidden` 响应之间有什么区别？

#### --distractors--

`401` 表示服务器崩溃，而 `403` 表示资源不存在。

---

`401` 是客户端错误，而 `403` 是服务器错误。

---

`401` 表示资源已永久移动，而 `403` 表示资源是临时移动。

#### --answer--

`401` 表示需要凭据，而 `403` 表示客户端有有效凭据但无权访问资源。

### --question--

#### --text--

HTTP 请求的哪一部分包含方法、路径和 HTTP 版本？

#### --distractors--

响应正文

---

请求正文

---

请求头部

#### --answer--

请求行

### --question--

#### --text--

`304 Not Modified` 状态码告诉客户端什么？

#### --distractors--

资源已从服务器删除。

---

请求格式错误，应重新发送。

---

服务器暂时不可用。

#### --answer--

资源自上次请求后未更改，客户端应使用其缓存版本。

### --question--

#### --text--

在 HTTP 请求-响应周期中，服务器处理请求后立即发生什么？

#### --distractors--

客户端发送第二个请求以确认收到。

---

DNS 解析器缓存响应。

---

客户端发起新的 TCP 连接。

#### --answer--

服务器准备带有状态码、头部和可选正文的响应。

### --question--

#### --text--

哪个 HTML 属性指定表单提交时数据发送到哪里？

#### --distractors--

`method`

---

`name`

---

`type`

#### --answer--

`action`

### --question--

#### --text--

即使已经有客户端验证，为什么仍必须始终执行服务器端验证？

#### --distractors--

现代浏览器不支持客户端验证。

---

服务器端验证只针对文件上传。

---

客户端验证比服务器端验证快。

#### --answer--

客户端验证可以被绕过，因此为了安全必须进行服务器端验证。

### --question--

#### --text--

哪个标准机构维护 HTML 和 DOM 作为持续更新的活标准？

#### --distractors--

W3C

---

ECMA International

---

Khronos Group

#### --answer--

WHATWG

### --question--

#### --text--

TC39 是什么，它在网络标准生态系统中的作用是什么？

#### --distractors--

负责 CSS 规格的 W3C 工作组。

---

决定 Chrome 中功能的浏览器厂商联盟。

---

管理 WebGL 标准的 Khronos Group 委员会。

#### --answer--

ECMA International 内的技术委员会，通过五阶段提案流程推进 JavaScript 语言特性。

### --question--

#### --text--

在网络标准生命周期的哪个阶段，功能通过公开讨论、用例收集和竞争设计方案进行压力测试？

#### --distractors--

规格测试

---

最终测试

---

互操作性测试

#### --answer--

孵化阶段

### --question--

#### --text--

W3C 网络平台测试套件在标准流程中的目的是什么？

#### --distractors--

为营销目的排名浏览器性能。

---

替代书面规格说明。

---

允许开发者提交新功能提案。

#### --answer--

验证每个浏览器是否一致实现某个功能。

### --question--

#### --text--

哪个网络标准原则确保按照规格构建的页面在 Chrome、Firefox 和 Safari 中表现一致？

#### --distractors--

向后兼容性

---

渐进增强

---

开放性

#### --answer--

互操作性

### --question--

#### --text--

渐进增强原则在设计网络功能时要求什么？

#### --distractors--

功能必须只针对最新浏览器版本。

---

功能必须先用 JavaScript 实现，再用 CSS 或 HTML。

---

功能必须最终定稿后才能被任何浏览器实现。

#### --answer--

功能必须首先对所有人有效，然后为拥有更强浏览器或设备的用户提供改进。

### --question--

#### --text--

为什么网络在引入新标准时几乎保持绝对的向后兼容性？

#### --distractors--

新标准总是替代旧标准，因此旧页面会自动更新。

---

浏览器厂商法律上必须支持所有已发布页面。

---

旧页面被归档并从独立网络提供。

#### --answer--

这样多年前构建的页面才能在现代浏览器中继续正常工作。

### --question--

#### --text--

哪个 HTTP 状态码类别用于服务器端错误？

#### --distractors--

`2xx`

---

`3xx`

---

`4xx`

#### --answer--

`5xx`
