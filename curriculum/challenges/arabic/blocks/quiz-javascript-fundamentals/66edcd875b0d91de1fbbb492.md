---
id: 66edcd875b0d91de1fbbb492
title: اختبار أساسيات JavaScript
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي من هذه الخيارات سينشئ كائنًا من نوع String؟

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

كيف يمكنك تحويل كائن String إلى سلسلة نصية حرفية؟

#### --distractors--

باستخدام دالة البناء `String()`.

---

باستخدام الطريقة `.stringify()`.

---

احتواء كائن `String` داخل علامات اقتباس مزدوجة.

#### --answer--

باستخدام الطريقة `.toString()`.

### --question--

#### --text--

لماذا يمكنك استخدام طرق مثل `.length`، `.repeat()`، `.concat()`، و `.slice()` على سلسلة نصية بدائية في JavaScript؟

#### --distractors--

توجد تلك الطرق أصلاً على السلاسل النصية البدائية.

---

تُخزن السلاسل النصية البدائية ككائنات داخليًا.

---

تحول JavaScript السلسلة النصية البدائية مؤقتًا إلى مصفوفة.

#### --answer--

تغلف JavaScript السلسلة النصية البدائية مؤقتًا داخل كائن String.

### --question--

#### --text--

ماذا ستسجل الشفرة التالية في وحدة التحكم؟

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

أي من التالي يعيد كائن `Number` من السلسلة النصية `"123"`؟

#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--

ماذا سترجع `Number(true)` و `Number(false)` على التوالي؟

#### --distractors--

`undefined` و `undefined`

---

`NaN` و `NaN`

---

`true` و `false`

#### --answer--

`1` و `0`

### --question--

#### --text--

ماذا سترجع `Number(undefined)` و `Number(null)` على التوالي؟

#### --distractors--

`NaN` و `NaN`

---

`0` و `0`

---

`undefined` و `null`

#### --answer--

`NaN` و `0`

### --question--

#### --text--

أي من الأسماء التالية هو الأفضل لمتغير قيمة منطقية يتحقق مما إذا كان المستخدم مسموحًا له بفعل شيء ما على موقعك؟

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

أي من الأسماء التالية هو الأفضل لدالة تُنفذ عند إرسال المستخدم لنموذج؟

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

ما هي المصفوفة المتفرقة (sparse array)؟

#### --distractors--

مصفوفة تحتوي على قيمة `null` في موقع واحد أو أكثر من المواقع.

---

مصفوفة تحتوي على قيمة `undefined` في موقع واحد أو أكثر من المواقع.

---

مصفوفة تحتوي على قيمة كائن فارغ في موقع واحد أو أكثر من المواقع.

#### --answer--

مصفوفة لا تُعيَّن فيها قيمة لموقع واحد أو أكثر من المواقع.

### --question--

#### --text--

في أي من الأمثلة التالية لن تكون `arr` مصفوفة متفرقة؟

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

ما هو الغرض الأساسي من أداة فحص الشفرة (linter)؟

#### --distractors--

تنسيق الشفرة تلقائيًا.

---

تحسين أداء الشفرة.

---

إنشاء توثيق للشفرة.

#### --answer--

كشف الأخطاء والمشكلات المحتملة في الشفرة.

### --question--

#### --text--

ما فائدة استخدام أداة تنسيق في قاعدة الشفرة؟

#### --distractors--

تمكين المطورين من تجميع الشفرة بكفاءة أكبر.

---

تحويل الشفرة إلى لغة برمجة محددة.

---

كشف الأخطاء المنطقية.

#### --answer--

ضمان نمط شفرة متسق.

### --question--

#### --text--

في JavaScript، من المسؤول عادة عن تخصيص الذاكرة للمتغيرات؟

#### --distractors--

المطور.

---

نظام التشغيل.

---

الخادم.

#### --answer--

محرك وقت التشغيل.

### --question--

#### --text--

ماذا سيتم تسجيله في وحدة التحكم في الشفرة التالية؟

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

ما هو الرفع (hoisting) في JavaScript؟

#### --distractors--

رفع الأخطاء إلى أعلى الشفرة.

---

عملية إنشاء متغيرات جديدة.

---

نقل إعلانات `var` تلقائيًا إلى أعلى الكتلة.

#### --answer--

عملية نقل إعلانات المتغيرات إلى أعلى نطاقها.

### --question--

#### --text--

لماذا يُعتبر استخدام الكلمة المفتاحية `var` ممارسة سيئة في JavaScript الحديثة؟

#### --distractors--

إنها أبطأ من `let` و `const`.

---

تسبب أخطاء نحوية.

---

لا تسمح بفحص النوع.

#### --answer--

يمكن الوصول إليها خارج الكتلة الخاصة بها.

### --question--

#### --text--

ما هو الغرض من الوحدات (modules) في JavaScript؟

#### --distractors--

تحسين أداء الشفرة.

---

جعل الشفرة أكثر صعوبة في الفهم.

---

إنشاء لغات برمجة جديدة.

#### --answer--

تنظيم الشفرة إلى وحدات قابلة لإعادة الاستخدام.

### --question--

#### --text--

كيف تضيف سكربت `app.js` كوحدة في مستند HTML؟

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

أي من الأمثلة التالية يصدر الدالة `calculateSum` من وحدة `utils.js` ويستوردها ويستخدمها بشكل صحيح في ملف `app.js`؟

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
