---
id: 66edd10913f078e7669eca81
title: JavaScript 디버깅 퀴즈
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

변수가 정의되기 전에 사용될 때 발생하는 오류 유형은 무엇인가요?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

디버깅 목적으로 코드 실행을 의도적으로 일시 중지하는 데 사용할 수 있는 JavaScript 문장은 무엇인가요?

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

데이터를 표 형식으로 표시하는 데 사용할 수 있는 콘솔 메서드는 무엇인가요?

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

Chrome 개발자 도구에서 네트워크 요청을 어떻게 검사할 수 있나요?

#### --distractors--

"Network Links" 탭을 사용하여.

---

"Console" 탭을 사용하여.

---

"Sources" 탭을 사용하여.

#### --answer--

"Network" 탭을 사용하여.

### --question--

#### --text--

JavaScript에서 `try...catch`를 사용하는 올바른 구문은 무엇인가요?

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

다음 중 `SyntaxError`의 예는 무엇인가요?

#### --distractors--

변수를 선언하기 전에 사용하는 것.

---

같은 범위 내에서 변수를 두 번 사용하는 것.

---

범위를 벗어난 변수를 사용하는 것.

#### --answer--

배열 요소 사이에 쉼표가 빠진 경우.

### --question--

#### --text--

JavaScript 디버깅에서 중단점(breakpoints)의 목적은 무엇인가요?

#### --distractors--

코드 오류를 자동으로 수정하는 것.

---

코드 실행을 방지하는 것.

---

콘솔에 메시지를 기록하는 것.

#### --answer--

특정 지점에서 실행을 일시 중지하는 것.

### --question--

#### --text--

`throw` 문장이 실행되면 어떤 일이 발생하나요?

#### --distractors--

프로그램이 즉시 종료되고 남은 코드를 무시합니다.

---

throw 문은 콘솔에 오류를 기록하지만 실행을 멈추지 않습니다.

---

중단점을 비활성화할 때까지 코드 실행이 일시 중지됩니다.

#### --answer--

예외가 발생하고 `catch` 블록(있다면)이 실행됩니다.

### --question--

#### --text--

개발자 도구에서 디버깅 중 변수나 표현식을 검사하려면 어떤 메서드를 사용하나요?

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

다음 코드를 실행할 때 발생하는 오류 유형은 무엇인가요?

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

스택 오버플로우 오류

#### --answer--

`ReferenceError`

### --question--

#### --text--

디버깅 중 코드 한 줄씩 실행하는 데 일반적으로 사용하는 도구는 무엇인가요?

#### --distractors--

`console.log()`

---

`alert()`

---

`throw` 문장.

#### --answer--

개발자 도구의 중단점.

### --question--

#### --text--

`undefined`의 속성에 접근하려 할 때 발생하는 오류 유형은 무엇인가요?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

`finally` 문장에서 `try...catch` 블록은 어떤 역할을 하나요?

#### --distractors--

오류가 있을 때만 실행됩니다.

---

`catch`가 발생하면 실행을 건너뜁니다.

---

코드 블록을 즉시 종료합니다.

#### --answer--

오류 여부와 관계없이 항상 실행됩니다.

### --question--

#### --text--

다음 코드를 실행하면 어떤 결과가 나오나요?

```js
const arr = [];
arr.length = -1; 
```

#### --distractors--

코드가 `SyntaxError`를 발생시킵니다.

---

코드가 빈 배열을 콘솔에 기록합니다.

---

코드가 메모리 부족 오류를 발생시킵니다.

#### --answer--

코드가 `RangeError`를 발생시킵니다.

### --question--

#### --text--

JavaScript에서 `debugger` 문장에 대해 올바른 설명은 무엇인가요?

#### --distractors--

`debugger` 문장은 항상 브라우저에서 무시됩니다.

---

`debugger` 문장은 서버에 오류 보고서를 보냅니다.

---

`debugger` 문장은 항상 페이지를 새로고침하고 콘솔을 지웁니다.

#### --answer--

개발자 도구가 열려 있지 않으면 `debugger` 문장은 무시됩니다.

### --question--

#### --text--

JavaScript에서 "프로파일링"의 주요 목적은 무엇인가요?

#### --distractors--

JavaScript 함수에 대한 단위 테스트를 작성하고 코드 실행 프로필을 만드는 것.

---

코드 실행을 일시 중지하고 메모리 및 CPU 같은 캡처된 자원을 해제하는 것.

---

JavaScript 코드를 더 빠른 실행을 위해 기계어로 변환하는 것.

#### --answer--

CPU 사용량, 함수 호출, 실행 시간을 기록하여 성능 병목 현상을 식별하는 것.

### --question--

#### --text--

디버깅 맥락에서 "워처(watcher)"란 무엇인가요?

#### --distractors--

코드 실행을 일시 중지하는 도구.

---

오류를 기록하는 내장 함수.

---

예외를 처리하는 JavaScript 메서드.

#### --answer--

변수 값을 모니터링하는 도구.

### --question--

#### --text--

`console.error()`를 `console.log()` 대신 언제 사용해야 하나요?

#### --distractors--

테이블 데이터를 표시할 때.

---

디버깅 중에만 메시지를 기록할 때.

---

브라우저 정보를 기록할 때.

#### --answer--

오류 메시지를 기록할 때.

### --question--

#### --text--

JavaScript에서 `Error()` 생성자는 무엇을 하나요?

#### --distractors--

오류 메시지를 콘솔에 기록합니다.

---

샌드박스에서 코드를 실행합니다.

---

프로그램을 종료합니다.

#### --answer--

발생시킬 수 있는 새 오류 객체를 생성합니다.

### --question--

#### --text--

JavaScript에서 `console.dir()` 메서드의 목적은 무엇인가요?

#### --distractors--

개별 오류 유형별로 로그 파일을 위한 별도 디렉터리를 생성하는 것.

---

콘솔 출력을 시각적으로 나타내고 pdf 파일로 저장하는 것.

---

프로그램 실행을 중단하고 메모리 및 CPU 사용과 관련된 오류 메시지를 표시하는 것.

#### --answer--

지정한 JavaScript 객체의 속성을 계층적이고 상호작용 가능한 목록으로 출력하는 것.
