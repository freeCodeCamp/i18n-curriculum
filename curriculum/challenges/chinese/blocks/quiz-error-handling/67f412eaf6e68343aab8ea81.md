---
id: 67f412eaf6e68343aab8ea81
title: 误差处理测验
challengeType: 8
dashedName: quiz-error-handling
---

# --description--

要通过测验，你必须正确回答以下 10 道题中的至少 9 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

这段代码的输出是什么？

  ```py
try:
    print(22 / 0)
except ZeroDivisionError:
    print("You can't divide by zero!")
  ```

#### --distractors--

`22`

---

`10`

---

`0`

#### --answer--

`You can't divide by zero!`

### --question--

#### --text--

Python 标准函数库中的哪个模块允许你以交互方式调试你的代码？

#### --distractors--

ABC

---

numpy

---

debugpy

#### --answer--

pdb

### --question--

#### --text--

当你尝试使用该类型不存在的方法或属性时，Python 会引起哪个异常？

#### --distractors--

`SyntaxError`

---

`AttributeBug`

---

`SyntaxBug`

#### --answer--

`AttributeError`

### --question--

#### --text--

你可以在代码的各个点周围插入哪个 Python 语句，以便在调试时查看变量的值？

#### --distractors--

`len()`

---

`console()`

---

`log()`

#### --answer--

`print()`

### --question--

#### --text--

哪个语句允许你手动引起一个异常？ 

#### --distractors--

`if`

---

`throw`

---

`from`

#### --answer--

`raise`

### --question--

#### --text--

你的 Python 代码中，代码 `print("Hello world"` 会引起哪种误差？

#### --distractors--

它不会引起任何误差。

---

`ValueError`

---

`NameError`

#### --answer--

`SyntaxError`

### --question--

#### --text--

`try...except` 让你在 Python 中做什么？

#### --distractors--

它提供了一种交互式测试你的代码的方法。

---

它让你编写数学表达式。

---

它加快了测试。

#### --answer--

它允许你执行可能引起异常的代码块。

### --question--

#### --text--

哪个对象让你访问异常本身，以便更好地调试和直接打印错误信息？

#### --distractors--

调试器对象

---

追踪对象

---

错误查找器对象

#### --answer--

异常对象

### --question--

#### --text--

以下哪些可选语句可以添加到 `try...except` 语句中？

#### --distractors--

`if` 和 `else`

---

`elif` 和 `if`

---

`else` 和 `elif`

#### --answer--

`else` 和 `finally`

### --question--

#### --text--

`try` 语句的哪个块无论是否发生误差都会运行？

#### --distractors--

`else`

---

`except`

---

`try`

#### --answer--

`finally`
