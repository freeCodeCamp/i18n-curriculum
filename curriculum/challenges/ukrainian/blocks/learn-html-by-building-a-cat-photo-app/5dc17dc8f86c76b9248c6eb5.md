---
id: 5dc17dc8f86c76b9248c6eb5
title: Крок 4
challengeType: 0
dashedName: step-4
---

# --description--

Коментування дозволяє залишати повідомлення, не впливаючи на відображення в браузері. Воно також дає змогу зробити код неактивним. Коментар у HTML починається з `<!--`, містить будь-яку кількість рядків тексту і закінчується `-->`.

Ось приклад коментаря з `TODO: Remove h1`:

```html
<!-- TODO: Remove h1 -->
```

Додайте коментар над елементом `p` з таким текстом:

`TODO: Add link to cat photos`

# --hints--

Ваш коментар повинен починатися з `<!--`. У вас відсутній один або кілька символів, які визначають початок коментаря.

```js
assert(code.match(/<!--/));
```

Ваш коментар повинен закінчуватися `-->`. У вас відсутній один або кілька символів, які визначають кінець коментаря.

```js
assert(code.match(/-->/));
```

У вашому коді не повинно бути зайвих символів відкриття/закриття коментаря. У браузері відображається зайвий `<!--` або `-->`.

```js
const noSpaces = code.replace(/\s/g, '');
assert(noSpaces.match(/<!--/g).length < 2 && noSpaces.match(/-->/g).length < 2);
```

Ваш коментар повинен бути над елементом `p`. Ви розмістили їх у неправильному порядку.

```js
assert.match(
  code.replace(/\s/g, ''),
  /<!--(.*?)--><p>everyonelovescutecatsonline!<\/p>/i
);
```

Ваш коментар повинен містити текст `TODO: Add link to cat photos`.

```js
assert.match(code, /<!--\s*todo:\s+add\s+link\s+to\s+cat\s+photos\s*-->/i);
```

# --seed--

## --seed-contents--

```html
<html>
  <body>
    <h1>CatPhotoApp</h1>
    <h2>Cat Photos</h2>
--fcc-editable-region--
    
    <p>Everyone loves cute cats online!</p>
    
--fcc-editable-region--
  </body>
</html>
```
