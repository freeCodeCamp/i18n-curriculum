---
id: 67358ac128957c865dcf3ddf
title: JavaScript 클래스 퀴즈
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제에 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScript에서 클래스의 목적은 무엇인가요?

#### --distractors--

메서드가 아닌 속성만 정의하기 위해서입니다.

---

정적 변수만 정의하기 위해서입니다.

---

원시 데이터만 조직하기 위해서입니다.

#### --answer--

객체를 위한 재사용 가능한 청사진을 만들기 위해서입니다.

### --question--

#### --text--

JavaScript에서 클래스를 어떻게 정의하나요?

#### --distractors--

`className` 키워드를 사용합니다.

---

`object` 키워드를 사용합니다.

---

`constructor` 키워드만 사용합니다.

#### --answer--

`class` 키워드를 사용합니다.

### --question--

#### --text--

클래스에서 속성을 초기화하는 데 사용되는 메서드는 무엇인가요?

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

클래스 생성자 내부에서 `this`는 무엇을 나타내나요?

#### --distractors--

전역 클래스 객체입니다.

---

함수 자체입니다.

---

정의되지 않은 참조입니다.

#### --answer--

클래스의 인스턴스입니다.

### --question--

#### --text--

클래스 메서드 내 화살표 함수에서 `this`가 어떻게 동작하는지 설명하는 것은 무엇인가요?

#### --distractors--

다른 범위에 있는 가장 가까운 함수에서 `this` 값을 상속받습니다.

---

새로 생성된 클래스 인스턴스를 가리킵니다.

---

엄격 모드에서 전역 객체로부터 `this` 값을 상속받습니다.

#### --answer--

정의된 외부 범위에서 `this` 값을 상속받습니다.

### --question--

#### --text--

`extends` 키워드의 주요 목적은 무엇인가요?

#### --distractors--

상위 클래스 생성자에서 정적 메서드를 정의하기 위해서입니다.

---

클래스에서 메서드를 만들기 위해서입니다.

---

상위 클래스에서 기본 속성을 초기화하기 위해서입니다.

#### --answer--

상위 클래스로부터 상속받는 하위 클래스를 만들기 위해서입니다.

### --question--

#### --text--

`Car` 클래스로부터 상속받는 `Vehicle` 클래스를 만드는 예시는 무엇인가요?

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

다음 코드의 출력 결과는 무엇인가요?

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

오류가 발생합니다.

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

하위 클래스에서 `super` 키워드의 기능은 무엇인가요?

#### --distractors--

상위 클래스의 새 인스턴스를 정의하기 위해서입니다.

---

상위 클래스의 메서드와 속성을 삭제하기 위해서입니다.

---

상위 클래스의 정적 메서드에만 접근하기 위해서입니다.

#### --answer--

상위 클래스의 생성자나 메서드를 호출하기 위해서입니다.

### --question--

#### --text--

정적 메서드를 가장 잘 설명하는 것은 무엇인가요?

#### --distractors--

클래스 내부에서 접근할 수 없는 메서드입니다.

---

화살표 함수에서만 사용되는 메서드입니다.

---

클래스의 각 인스턴스에 고유한 메서드입니다.

#### --answer--

어떤 인스턴스에도 속하지 않고 클래스 자체에 속하는 메서드입니다.

### --question--

#### --text--

다음 코드는 무엇을 출력하나요?

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

오류가 발생합니다.

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

JavaScript 클래스에서 정적 속성에 접근하려면 어떻게 해야 하나요?

#### --distractors--

메서드 내에서 `this.propertyName`를 사용합니다.

---

인스턴스를 만든 후 `instance.propertyName`를 사용합니다.

---

하위 클래스 내에서 `super.propertyName`를 사용합니다.

#### --answer--

`ClassName.propertyName`를 사용합니다.

### --question--

#### --text--

인스턴스 메서드 대신 정적 메서드를 사용해야 하는 경우는 언제인가요?

#### --distractors--

인스턴스의 속성에 접근하고 수정해야 할 때입니다.

---

메서드를 객체 인스턴스에서 호출해야 할 때입니다.

---

메서드가 인스턴스별 데이터에 의존하지 않고 클래스 수준 데이터로 작동할 때입니다.

#### --answer--

메서드가 클래스의 모든 인스턴스가 공유하는 데이터로 작동할 때입니다.

### --question--

#### --text--

다음 코드는 무엇을 출력하나요?

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

오류가 발생합니다.

### --question--

#### --text--

다른 클래스의 속성과 메서드를 상속받기 위해 사용하는 키워드는 무엇인가요?

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

다음 코드가 출력하는 것은 무엇인가요?

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

오류가 발생합니다.

#### --answer--

`Human`

### --question--

#### --text--

상속을 가장 잘 설명하는 것은 무엇인가요?

#### --distractors--

메서드가 새 객체를 반환하도록 허용합니다.

---

인스턴스가 자신의 속성을 정의하도록 허용합니다.

---

정적 속성을 비정적 속성으로 변환하도록 허용합니다.

#### --answer--

클래스가 다른 클래스의 속성과 메서드를 획득하도록 허용합니다.

### --question--

#### --text--

하위 클래스에 생성자가 정의되어 있지 않으면 어떻게 되나요?

#### --distractors--

오류가 발생합니다.

---

하위 클래스가 코드 내 어떤 클래스에서든 생성자를 상속받습니다.

---

하위 클래스가 새 인스턴스를 만들 수 없습니다.

#### --answer--

기본적으로 상위 클래스의 생성자가 사용됩니다.

### --question--

#### --text--

다음 코드에서 `this`는 무엇을 나타내나요?

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

전역 객체입니다.

---

`Animal` 클래스 자체입니다.

---

정의되지 않은 변수입니다.

#### --answer--

`Animal`의 인스턴스입니다.

### --question--

#### --text--

정적 속성의 주요 용도는 무엇인가요?

#### --distractors--

각 클래스 인스턴스의 기본 속성을 정의하기 위해서입니다.

---

하위 클래스에서 메서드를 재정의하기 위해서입니다.

---

인스턴스별 데이터를 정의하기 위해서입니다.

#### --answer--

인스턴스 간에 공유되는 클래스 전체 데이터를 저장하기 위해서입니다.
