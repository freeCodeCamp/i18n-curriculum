---
id: 6732269a7aa2ca1d6b6574fe
title: 隱式網格和明確網格之間的差異是什麼？
challengeType: 19
dashedName: what-is-the-difference-between-an-implicit-and-explicit-grid
---

# --interactive--

隱式網格是指瀏覽器在將項目放置於網格佈局時自動生成的列和行－那些未使用 `grid-template-rows` 或 `grid-template-columns` 明確定義的列和行。

控制瀏覽器隱式產生的行和列的屬性是 `grid-auto-columns` 和 `grid-auto-rows`。

隱式網格也指當你將一個項目放置在明確定義的列和行之外時，瀏覽器自動產生的額外列和行。

例如，假設你以這種方式在網格佈局中只定義兩個明確的行：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="grid-container">
  <div class="grid-item">Item 1</div>
  <div class="grid-item">Item 2</div>
  <div class="grid-item">Item 3</div>
  <div class="grid-item">Item 4</div>
  <div class="grid-item">Item 5</div>
  <div class="grid-item">Item 6</div>
</div>
```

```css
.grid-container {
  display: grid;
  grid-template-columns: 100px 100px; /* Only 2 explicit columns */
}

.grid-item {
  background-color: burlywood;
  border: 1px solid orangered;
  padding: 0.5rem;
  margin: 0.5rem;
}
```

:::

兩個項目使用兩個明確的行填滿了第一列－第一行的 `Item 1` 和第二行的 `Item 2`。

接下來的條款會開始新的一列，`Item 3` 位於第二列的第一行，`Item 4` 位於第二列的第二行，依此類推。

如你所見，明確的網格是你有意設置的網格區域。也就是說，使用 `grid-template-rows` 和 `grid-template-columns` 屬性為網格佈局明確定義的列和行。

以下是總結隱式與明確網格差異的表格：

| **Feature**          | **Explicit Grid**                                                                  | **Implicit Grid**                                                                          |
|----------------------|------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------|
| **Size control**     | Fully customizable using  ` grid-template-rows `  and  ` grid-template-columns ` . | Controlled by  ` grid-auto-rows `  and  ` grid-auto-columns ` , or defaults to  ` auto ` . |
| **Default Behavior** | Does not change unless explicitly defined.                                         | Automatically adapts to items placed outside the explicit grid.                            |
| **Complexity**       | Requires more planning for layout structure.                                       | Easier to implement for unstructured or variable content.                                  |
| **Flexibility**      | Rigid structure with defined rows and columns.                                     | Flexible and adapts to dynamically placed content.                                         |
| **Performance**      | Potentially more performant as the layout is predefined.                           | May require additional browser computations for implicit grid generation.                  |
| **Use case**         | Useful when the grid structure is predictable and defined upfront.                 | Useful for dynamic layouts where content is unknown or changes frequently.                 |

# --questions--

## --text--

哪些屬性控制瀏覽器在 CSS 網格配置中隱式產生的行和列？

## --answers--

`cols` 和 `rows`

### --feedback--

思考定義隱式網格軌道的屬性。

---

`col` 和 `row`

### --feedback--

思考定義隱式網格軌道的屬性。

---

`隱式-行` 和 `隱式-列`

### --feedback--

思考定義隱式網格軌道的屬性。

---

`grid-auto-columns` 和 `grid-auto-rows`

## --video-solution--

4

## --text--

哪些屬性用於定義 CSS grid 佈局中的明確的列和行？

## --answers--

`row` 和 `fr`

### --feedback--

回顧課程開始時討論的內容。

---

`grid-明確的-列` 和 `grid-明確的-行`

### --feedback--

回顧課程開始時討論的內容。

---

`grid-template-rows` 和 `grid-template-columns`

---

`grid-gap` 和 `grid-flow`

### --feedback--

回顧課程開始時討論的內容。

## --video-solution--

3

## --text--

以下哪個程式碼範例是設定兩個明確的行的正確方式？

## --answers--

```css
.grid-container {
   display: grid;
   grid-template-columns: 100px 100px;
}
```

---

```css
.grid-container {
   display: grid;
   grid-template-columns: 100px 100px 100px 100px;
}
```

### --feedback--

請密切注意範例中的屬性名稱和正確的值數量。

---

```css
.grid-container {
   display: grid;
   grid-template-rows: 100px 100px;
}
```

### --feedback--

請密切注意範例中的屬性名稱和正確的值數量。

---

```css
.grid-container {
   display: grid;
   grid-column-start: 100px 100px;
}
```

### --feedback--

請密切注意範例中的屬性名稱和正確的值數量。

## --video-solution--

1
