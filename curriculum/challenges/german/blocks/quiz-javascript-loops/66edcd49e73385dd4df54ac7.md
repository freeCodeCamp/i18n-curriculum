---
id: 66edcd49e73385dd4df54ac7
title: JavaScript-Schleifen-Quiz
challengeType: 8
dashedName: quiz-javascript-loops
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 folgenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche der folgenden Beschreibungen trifft am besten auf Iteration zu?

#### --distractors--

Es ist der Prozess, Code wiederholt zu schreiben, bis er korrekt funktioniert.

---

Es ist eine Technik, um Daten in aufeinanderfolgenden Speicherplätzen zu speichern.

---

Es ist der Prozess, Signale zwischen Komponenten zu senden, um eine Verbindung herzustellen.

#### --answer--

Es ist der Prozess, eine Reihe von Anweisungen mehrfach zu wiederholen.

### --question--

#### --text--

Welche Technik wird für Iterationen in der Programmierung verwendet?

#### --distractors--

Rekursion

---

Bedingungen

---

Kompilierung

#### --answer--

Schleifen

### --question--

#### --text--

Welches der folgenden Objekte ist iterierbar?

#### --distractors--

Undefined

---

Number

---

Boolean

#### --answer--

String

### --question--

#### --text--

Welche der folgenden Schleifenarten wird in JavaScript nicht unterstützt?

#### --distractors--

`for` Schleife.

---

`while` Schleife.

---

`for...in` Schleife.

#### --answer--

`if...else` Schleife.

### --question--

#### --text--

Wie ist die korrekte Reihenfolge der Deklaration einer `for` Schleife?

#### --distractors--

```javascript
for (condition; increment/decrement; initialization) {
  statement;
}
```

---

```javascript
for (increment/decrement; condition; initialization) {
  statement;
}
```

---

```javascript
for (initialization; increment/decrement; condition) {
  statement;
}
```

#### --answer--

```javascript
for (initialization; condition; increment/decrement) {
  statement;
}
```

### --question--

#### --text--

Welche Schleife führt den Codeblock einmal aus, bevor geprüft wird, ob die Bedingung wahr ist, und läuft weiter, solange die Bedingung wahr bleibt?

#### --distractors--

`while` Schleife.

---

`for...of` Schleife.

---

`for...in` Schleife.

#### --answer--

`do...while` Schleife.

### --question--

#### --text--

Welche der folgenden Schleifen iterieren über die Werte eines iterierbaren Objekts?

#### --distractors--

`for...in` Schleife.

---

`for` Schleife.

---

`do...while` Schleife.

#### --answer--

`for...of` Schleife.

### --question--

#### --text--

Wie wird eine Endlosschleife am besten definiert?

#### --distractors--

Eine Schleife, die läuft, bis die Bedingung falsch wird.

---

Eine Schleife, die einmal läuft und dann beendet wird.

---

Eine Schleife, die sich nach einer festen Anzahl von Iterationen automatisch beendet.

#### --answer--

Eine Schleife, deren Abbruchbedingung nie erfüllt wird oder fehlt.

### --question--

#### --text--

Wie oft wird die folgende Schleife ausgeführt?  

```js
for (let i = 2; i < 10; i+=2) {  
  console.log(i);  
}
```

#### --distractors--

5

---

9

---

10

#### --answer--

4

### --question--

#### --text--

Welche Schleife eignet sich am besten zum Iterieren über Objekte?

#### --distractors--

`for...of` Schleife.

---

`do...while` Schleife.

---

`for` Schleife.

#### --answer--

`for...in` Schleife.

### --question--

#### --text--

Was ist der Unterschied zwischen der `for...in` Schleife und der `for...of` Schleife?

#### --distractors--

`for...in` Schleife iteriert über Eigenschaftswerte, während die `for...of` Schleife über Eigenschaftsnamen (Schlüssel) iteriert.

---

`for...in` Schleifen erstellen neue Eigenschaften, während `for...of` Schleifen bestehende Eigenschaften ändern.

---

`for...in` Schleifen können nur mit Strings verwendet werden, während `for...of` Schleifen mit Strings und Zahlen funktionieren.

#### --answer--

`for...in` Schleife iteriert über Eigenschaftsnamen (Schlüssel), während die `for...of` Schleife über Eigenschaftswerte iteriert.

### --question--

#### --text--

Welche der folgenden Situationen würde eine Endlosschleife verursachen?

#### --distractors--

Initialisierung einer Variablen.

---

Eine Schleifenbedingung, die schließlich false ergibt.

---

Inkrement-/Dekrement-Logik.

#### --answer--

Eine Schleifenbedingung, die immer true zurückgibt.

### --question--

#### --text--

Welche Anweisung kann verwendet werden, um eine Schleife vollständig zu beenden?

#### --distractors--

Die `continue` Anweisung.

---

Die `label` Anweisung.

---

Die `skip` Anweisung.

#### --answer--

Die `break` Anweisung.

### --question--

#### --text--

Welche Anweisung kann verwendet werden, um die aktuelle Iteration zu überspringen und mit der nächsten fortzufahren?

#### --distractors--

Die `break` Anweisung.

---

Die `label` Anweisung.

---

Die `return` Anweisung.

#### --answer--

Die `continue` Anweisung.

### --question--

#### --text--

Was wird mit folgendem Code in der Konsole ausgegeben?

```js
for (let i = 0; i < 25; i += 2) {
  if (i % 5 === 0) {
    continue;
  }
  if (i % 13 === 0) {
    break;
  }
  console.log(i);
}
```

#### --distractors--

`0`, `2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

---

`0`, `2`, `4`, `6`, `8`, `12`

---

`2`, `4`, `6`, `8`, `12`

#### --answer--

`2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

### --question--

#### --text--

Was würde der folgende Code in der Konsole ausgeben?

```javascript
for (let i = 1; i < 6; i++) {
  if (i === 4) break;
  console.log(i);
}
```

#### --distractors--

`1`, `2`, `3`, `4`, `5`, `6`

---

`1`, `2`, `3`, `4`, `5`

---

`1`, `2`, `3`, `4`

#### --answer--

`1`, `2`, `3`

### --question--

#### --text--

Was wird mit dem folgenden Code in der Konsole ausgegeben?

```javascript
const shoppingList = { tomatoes: 4, apples: 10 };
for (const item in shoppingList) {
  console.log(item);
}
```

#### --distractors--

`tomatoes - 4`, `apples - 10`

---

`4`, `10`

---

`tomatoes:4`, `apples:10`

#### --answer--

`tomatoes`, `apples`

### --question--

#### --text--

Was wird die Konsolenausgabe des folgenden Codes sein?

```javascript
for (let i = 2; i <= 13; i++) {
  if (i % 2 === 0) continue;
  console.log(i);
}
```

#### --distractors--

`2`, `4`, `6`, `8`, `10`, `12`

---

`1`, `3`, `5`, `7`, `9`, `11`, `13`

---

`1`, `3`, `5`, `7`, `9`, `11`

#### --answer--

`3`, `5`, `7`, `9`, `11`, `13`

### --question--

#### --text--

Was würde mit dem folgenden Code in der Konsole ausgegeben?

```javascript
const fruits = ["Mango", "Pineapple", "Oranges"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

#### --distractors--

`0`, `1`, `2`

---

`Pineapple`, `Oranges`, `Mango`

---

`Oranges`, `Pineapple`, `Mango`

#### --answer--

`Mango`, `Pineapple`, `Oranges`

### --question--

#### --text--

Wie oft wird der untenstehende Code den String `in the loop` ausgeben?

```javascript
let x = 0
while (x < 5) {
  console.log("in the loop");
}
```

#### --distractors--

3 Mal

---

4 Mal

---

5 Mal

#### --answer--

Unendlich oft

