---
id: 67358be1c7903489c0a7db78
title: اختبار قصير لخرائط ومجموعات JavaScript
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

لنجاح الاختبار القصير، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما هي المجموعة (Set) في JavaScript؟

#### --distractors--

هيكل بيانات يسمح بالقيم المكررة.

---

هيكل بيانات يخزن فقط السلاسل النصية والأرقام.

---

مجموعة لا تسمح بحذف عناصرها.

#### --answer--

مجموعة لا يمكن أن تتكرر فيها القيمة أكثر من مرة واحدة.

### --question--

#### --text--

أي من الطرق التالية متاحة في الخرائط (Maps) ولكن ليست في المجموعات (Sets)؟

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

ما الطريقة المستخدمة لإضافة عناصر إلى مجموعة (Set)؟

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

كيف تتحقق مما إذا كانت المجموعة تحتوي على قيمة معينة؟

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

أي من هذه الحالات استخدام ممتاز للمجموعات (Sets)؟

#### --distractors--

إدارة أزواج المفتاح-القيمة في قاعدة بيانات.

---

إنشاء قوائم مرتبة من العناصر.

---

تخزين هياكل بيانات هرمية في التخزين المحلي للمتصفح.

#### --answer--

إزالة القيم المكررة من مصفوفة.

### --question--

#### --text--

ما هي WeakSet في JavaScript؟

#### --distractors--

مجموعة تشبه Set تسمح فقط بأنواع البيانات الأولية.

---

مجموعة تشبه Set تسمح بالقيم المكررة.

---

مجموعة تشبه Set تمنع إزالة القيم.

#### --answer--

مجموعة تشبه Set تخزن فقط الكائنات.

### --question--

#### --text--

ما الفرق بين Set و WeakSet؟

#### --distractors--

يمكن لـ WeakSet تخزين أي نوع من القيم، بما في ذلك الأرقام.

---

Set لا يسمح بالقيم المكررة، لكن WeakSet يسمح بذلك.

---

Set يسمح بجمع القمامة، بينما WeakSet لا يسمح.

#### --answer--

يمكن لـ Set تخزين أي نوع من القيم، بينما WeakSet يخزن فقط الكائنات.

### --question--

#### --text--

ما هي Map في JavaScript؟

#### --distractors--

مجموعة تسمح فقط بالقيم الفريدة.

---

مجموعة تستخدم السلاسل النصية فقط كمفاتيح.

---

مجموعة لا تسمح بحذف عناصرها.

#### --answer--

مجموعة من أزواج المفتاح-القيمة من أي نوع.

### --question--

#### --text--

ما الطريقة المستخدمة لإدخال أزواج المفتاح-القيمة في Map؟

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

ما الناتج الذي ستطبعه الشفرة التالية؟

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

كيف تختلف Map عن Object في JavaScript؟

#### --distractors--

Map تسمح فقط بالأرقام كمفاتيح.

---

الكائنات تسمح بأي نوع كمفاتيح، بينما الخرائط تسمح فقط بالسلاسل النصية.

---

الخرائط لا تدعم التكرار، بينما الكائنات تدعم.

#### --answer--

الخرائط تسمح بأي نوع من المفاتيح، لكن الكائنات تسمح فقط بالسلاسل النصية والرموز.

### --question--

#### --text--

كيف يمكنك إزالة زوج مفتاح-قيمة من Map؟

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

ماذا تُرجع خاصية `size` في Map؟

#### --distractors--

عدد الفهارس في Map.

---

عدد القيم الفريدة في Map.

---

إجمالي الذاكرة المستخدمة بواسطة Map.

#### --answer--

عدد أزواج المفتاح-القيمة في Map.

### --question--

#### --text--

ما هي WeakMap في JavaScript؟

#### --distractors--

مجموعة تشبه Map تسمح بالقيم الأولية كمفاتيح.

---

مجموعة تشبه Map تمنع حذف العناصر.

---

مجموعة تشبه Map ترتب مفاتيحها تلقائيًا.

#### --answer--

مجموعة تشبه Map تحتوي على مراجع ضعيفة للمفاتيح.

### --question--

#### --text--

ما الفرق بين Map و WeakMap؟

#### --distractors--

Map تسمح فقط بالأنواع الأولية كمفاتيح، بينما WeakMap تسمح فقط بالكائنات.

---

Map تسمح بجمع القمامة، بينما WeakMap لا تسمح.

---

WeakMap تسمح بالمفاتيح المكررة، بينما Map لا تسمح.

#### --answer--

Map قابلة للتكرار، بينما WeakMap ليست كذلك.

### --question--

#### --text--

أي من الطرق التالية غير موجودة في WeakMap؟

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

ما الناتج الذي ستطبعه الشفرة التالية؟

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

كيف يمكنك التكرار على القيم في مجموعة (Set)؟

#### --distractors--

باستخدام الطريقة `.map()`.

---

باستخدام الطريقة `.reduce()`.

---

باستخدام الطريقة `.filter()`.

#### --answer--

باستخدام الطريقة `.forEach()`.

### --question--

#### --text--

أي طريقتين من طرق المجموعات (Sets) تُرجع `SetIterator` يحتوي على قيم مجموعة معينة؟

#### --distractors--

`entries()` و `values()`

---

`forEach()` و `values()`

---

`has()` و `add()`

#### --answer--

`keys()` و `values()`

### --question--

#### --text--

ما الناتج الذي ستطبعه الشفرة التالية؟

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
