---
id: 67f4136110dddb491be2e90a
title: 查找和排序算法测验
challengeType: 8
dashedName: quiz-searching-and-sorting-algorithms
---

# --description--

要通过测验，你必须正确回答以下 10 道题中的至少 9 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

哪种查找算法迭代一个项列表，从头开始查看每个项，直到找到目标项？

#### --distractors--

合并查找

---

气泡查找

---

二分查找

#### --answer--

线性查找

### --question--

#### --text--

哪种技术用于将问题分解为更小的子问题？

#### --distractors--

分解并解决

---

分而治之

---

摧毁和征服

#### --answer--

分治法

### --question--

#### --text--

线性查找算法的空间复杂度是多少？

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n²)`

#### --answer--

`O(1)`

### --question--

#### --text--

在 `binary search` 和线性查找之间，哪个更适合更大的项列表？

#### --distractors--

两者

---

线性查找

---

无

#### --answer--

二分查找

### --question--

#### --text--

二分查找的空间复杂度是多少？

#### --distractors--

`O(log n)`

---

`O(n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

当二分查找算法找到目标项时，它会返回什么？

#### --distractors--

目标项的值。

---

被查找的列表大小。

---

目标的布尔值。

#### --answer--

目标项的索引。

### --question--

#### --text--

如果线性查找算法没有找到目标项，它会返回什么？

#### --distractors--

`0`

---

`Null`

---

`Undefined`

#### --answer--

`-1`

### --question--

#### --text--

归并排序算法是如何工作的？

#### --distractors--

它通过逐一查看每项来查找元素。

---

它通过反复查找最小的元素来构建一个排序的列表。

---

它交换相邻的元素，直到列表排序完成。

#### --answer--

它递归地将列表拆分成更小的项，然后按排序顺序合并它们。

### --question--

#### --text--

二分查找生效的条件是什么？

#### --distractors--

列表必须只包含唯一值。

---

列表必须有偶数个元素。

---

列表中不得包含负数。

#### --answer--

列表必须排序。

### --question--

#### --text--

哪种计算机科学方法用于实现 `merge sort` 算法？

#### --distractors--

动态编程

---

贪心算法

---

追踪

#### --answer--

分治法 
