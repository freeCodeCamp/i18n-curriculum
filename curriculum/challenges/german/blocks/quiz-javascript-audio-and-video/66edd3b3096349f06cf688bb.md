---
id: 66edd3b3096349f06cf688bb
title: JavaScript-Audio- und Video-Quiz
challengeType: 8
dashedName: quiz-javascript-audio-and-video
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist der Zweck des `Audio()`-Konstruktors in JavaScript?

#### --distractors--

Eine neue Audiodatei erstellen.

---

Eine vorhandene Audiodatei abspielen.

---

Audiodateien bearbeiten.

#### --answer--

Ein neues `audio`-Element erstellen.

### --question--

#### --text--

Welche Methode wird verwendet, um die Wiedergabe eines `audio`- oder `video`-Elements zu starten?

#### --distractors--

`start()`

---

`begin()`

---

`resume()`

#### --answer--

`play()`

### --question--

#### --text--

Was ist der Unterschied zwischen den `pause()`- und `play()`-Methoden für Medienelemente?

#### --distractors--

`pause()` gibt ein Promise zurück, während `play()` dies nicht tut.

---

`pause()` kann nicht fortgesetzt werden, während `play()` von jeder Position starten kann.

---

`pause()` setzt automatisch auf den Anfang zurück, während `play()` an der Stelle startet, an der die Wiedergabe pausiert wurde.

#### --answer--

`pause()` stoppt die Wiedergabe, behält aber die aktuelle Position bei, während `play()` die Wiedergabe startet.

### --question--

#### --text--

Welches Audioformat hat die breiteste Browserunterstützung?

#### --distractors--

`FLAC`

---

`OGG`

---

`WAV`

#### --answer--

`MP3`

### --question--

#### --text--

Was ist der Zweck des `source`-Elements innerhalb eines `audio`- oder `video`-Elements?

#### --distractors--

Die Lautstärke des Mediums festlegen.

---

Untertitel zum Medium hinzufügen.

---

Mit einem externen Audioprozessor verknüpfen.

#### --answer--

Mehrere Medienquellen für verschiedene Formate angeben.

### --question--

#### --text--

Welche zwei MIME-Typen kann ein MP4-Format haben?

#### --distractors--

`mp4/audio` und `mp4/video`

---

`audio/mpeg4` und `video/mpeg4`

---

`mp4/mpeg` und `mp4/stream`

#### --answer--

`audio/mp4` und `video/mp4`

### --question--

#### --text--

Was macht ein Codec im Kontext von Audio und Video?

#### --distractors--

Erhöht die Lautstärke des Mediums.

---

Ändert das Dateiformat des Mediums.

---

Verschlüsselt das Medium für eine sichere Übertragung.

#### --answer--

Wandelt Medien zwischen analogen und digitalen Formaten um.

### --question--

#### --text--

Welche API bietet eine Möglichkeit, Audio in Webanwendungen zu erstellen, zu verarbeiten und zu analysieren?

#### --distractors--

Audio Processing API.

---

Sound-Manipulations-API.

---

Media Stream API.

#### --answer--

Web Audio API.

### --question--

#### --text--

Was ist der Zweck der `getUserMedia()`-Methode?

#### --distractors--

Benutzer bei einem Mediensystem authentifizieren.

---

Benutzereinstellungen für die Medienwiedergabe abrufen.

---

Mediendateien auf das Gerät des Benutzers herunterladen.

#### --answer--

Auf die Kamera und/oder das Mikrofon des Benutzers zugreifen.

### --question--

#### --text--

Welche Eigenschaft der `HTMLMediaElement`-Schnittstelle stellt die aktuelle Wiedergabezeit dar?

#### --distractors--

`time`

---

`playbackPosition`

---

`currentPosition`

#### --answer--

`currentTime`

### --question--

#### --text--

Was bewirkt der folgende Code-Ausschnitt?

```javascript
const audio = new Audio('song.mp3');
audio.loop = true;
```

#### --distractors--

Spielt die Audiodatei einmal ab.

---

Erhöht die Lautstärke des Audios.

---

Pausiert das Audio nach einer Schleife.

#### --answer--

Setzt das Audio auf unendliche Wiederholung.

### --question--

#### --text--

Was bedeutet die Abkürzung MIME vollständig?

#### --distractors--

Mehrfache Internet-Nachrichtenerweiterungen

---

Mail-Internet-Multimedia-Erweiterungen

---

Multimedia-Internet-Mail-Erweiterungen

#### --answer--

Mehrzweck-Internet-Mail-Erweiterungen

### --question--

#### --text--

Wobei hilft Ihnen die `canPlayType()`-Methode?

#### --distractors--

Prüfen, ob Audio in einem bestimmten Format gerade im Browser abgespielt wird.

---

Überprüfen, ob eine Mediendatei im Browser-Cache vorhanden und für die Wiedergabe zugänglich ist.

---

Testen, ob ein Audioelement Steuerungen aktiviert hat und ob der Benutzer die entsprechenden Berechtigungen zum Interagieren besitzt.

#### --answer--

Feststellen, ob ein Browser wahrscheinlich in der Lage ist, Ihr spezifisches Audioformat abzuspielen.

### --question--

#### --text--

Welches der folgenden ist KEIN gültiges Ereignis der `HTMLMediaElement`-API?

#### --distractors--

`waiting`

---

`canplay`

---

`ended`

#### --answer--

`stopped`

### --question--

#### --text--

Welches `HTMLMediaElement`-Ereignis wird ausgelöst, wenn die Wiedergabe aufgrund von Datenpuffern pausiert wird?

#### --distractors--

`stopped`

---

`pause`

---

`buffering`

#### --answer--

`waiting`

### --question--

#### --text--

Welche Web Audio API-Schnittstelle wird verwendet, um einen Audioverarbeitungsgraphen darzustellen?

#### --distractors--

`AudioNode`

---

`AudioBuffer`

---

`AudioListener`

#### --answer--

`AudioContext`

### --question--

#### --text--

Was ist der Zweck der Media Source Extensions API?

#### --distractors--

Visuelle Effekte zu Videos hinzufügen.

---

Zwischen verschiedenen Medienformaten konvertieren.

---

JavaScript ermöglichen, mehrere Audiotracks zu synchronisieren.

#### --answer--

Sie ermöglicht es, den Webcam-Feed eines Benutzers direkt an ein Videoelement weiterzugeben.

### --question--

#### --text--

Wann wird das `ended`-Ereignis ausgelöst?

#### --distractors--

Wenn die Medienquelle geändert wird.

---

Wenn die `pause()`-Methode aufgerufen wird.

---

Wenn das Medium manuell gestoppt wird.

#### --answer--

Wenn das Ende des Mediums erreicht ist.

### --question--

#### --text--

Wie wird die Screen Capture API verfügbar gemacht?

#### --distractors--

Über die `window.screenCapture`-Schnittstelle

---

Durch die Verwendung der `navigator.capture`-Eigenschaft

---

Durch die Verwendung der `captureScreen()`-Methode

#### --answer--

Durch den Aufruf der `getDisplayMedia()`-Methode.

### --question--

#### --text--

Womit ist das WMV-Format verbunden?

#### --distractors--

Wide Media Video

---

World Media Video

---

Web Media Player

#### --answer--

Windows Media Player
