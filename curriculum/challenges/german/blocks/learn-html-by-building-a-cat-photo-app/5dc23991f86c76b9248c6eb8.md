---
id: 5dc23991f86c76b9248c6eb8
title: Schritt 6
challengeType: 0
dashedName: step-6
---

# --description--

Im vorherigen Schritt haben Sie die `h1`, `h2`, den Kommentar und die `p`-Elemente innerhalb des `main`-Elements platziert. Das nennt man *Verschachtelung*. Verschachtelte Elemente sollten zwei Leerzeichen weiter rechts vom Element, in dem sie verschachtelt sind, stehen. Dieser Abstand heißt Einrückung und dient dazu, HTML leichter lesbar zu machen.

Hier ist ein Beispiel für Verschachtelung und Einrückung:

```html
<main>
  <h1>Most important content of the document</h1>
  <p>Some more important content...</p>
</main>
```

Das `h1`-Element, das `h2`-Element und der Kommentar sind im folgenden Code zwei Leerzeichen weiter eingerückt als das `main`-Element. Verwenden Sie die Leertaste auf Ihrer Tastatur, um vor dem `p`-Element zwei weitere Leerzeichen hinzuzufügen, damit es ebenfalls richtig eingerückt ist.

# --hints--

Ihr Code sollte ein `h2`-Element mit dem Text `Cat Photos` enthalten. Möglicherweise haben Sie es versehentlich gelöscht, es fehlen sowohl öffnendes als auch schließendes Tag, oder der Text wurde geändert.

```js
assert(
  document.querySelector('h2') &&
    code.match(/<\/h2\>/) &&
    document.querySelector('h2').innerText.toLowerCase() === 'cat photos'
);
```

Sie sollten die `ul`- oder `li`-Elemente aus dem Beispiel nicht hinzufügen.

```js
assert(
  !document.querySelector('ul') && !document.querySelector('li')
);
```

Sie sollten die Einrückung in der Zeile mit Ihrem `h2`-Element nicht ändern. Das öffnende Tag sollte 6 Leerzeichen vom Zeilenanfang entfernt beginnen. Sie können den Schritt neu starten, um die ursprüngliche Einrückung wiederherzustellen.

```js
assert(code.toLowerCase().match(/<\/h1\>\s*\n\s{6}<h2>/));
```

Ihr Code sollte einen Kommentar enthalten. Sie haben den Kommentar aus einem früheren Schritt entfernt.

```js
assert(code.match(/<!--.*-->/));
```

Der Text des Kommentars sollte `TODO: Add link to cat photos` sein. Ändern Sie weder den Text noch die Einrückung des Kommentars.

```js
assert(code.match(/<!--\s*todo:\s+add\s+link\s+to\s+cat\s+photos\.?\s*-->/i));
```

Sie sollten die Einrückung in der Zeile mit Ihrem Kommentar-Element nicht ändern. Das öffnende Tag sollte 6 Leerzeichen vom Zeilenanfang entfernt beginnen. Sie können den Schritt neu starten, um die ursprüngliche Einrückung wiederherzustellen.

```js
assert(
  code
    .toLowerCase()
    .match(/<\/h2>\s*\n\s{6}<!--\s*todo:\s+add\s+link\s+to\s+cat\s+photos\s*-->/)
);
```

Ihr Code sollte ein `p`-Element enthalten. Sie haben das `p`-Element aus einem früheren Schritt entfernt.

```js
assert(document.querySelector('p'));
```

Der Text des `p`-Elements sollte `Everyone loves cute cats online!` sein. Ändern Sie weder den Text, die Einrückung noch die Zeichensetzung des `p`-Elements.

```js
assert.match(
  document
    .querySelector('p')
    .innerText.toLowerCase(),
    /everyone\s+loves\s+cute\s+cats\s+online!$/
);
```

Das öffnende `p`-Tag sollte die gleiche Einrückung haben wie Ihre `h2`- und Kommentar-Elemente. Das öffnende Tag sollte 6 Leerzeichen vom Zeilenanfang entfernt beginnen.

```js
assert(code.toLowerCase().match(/-->\s*\n\s{6}<p>/));
```

# --seed--

## --seed-contents--

```html
<html>
  <body>
--fcc-editable-region--
    <main>
      <h1>CatPhotoApp</h1>
      <h2>Cat Photos</h2>
      <!-- TODO: Add link to cat photos -->
    <p>Everyone loves cute cats online!</p>
    </main>
--fcc-editable-region--
  </body>
</html>
```

