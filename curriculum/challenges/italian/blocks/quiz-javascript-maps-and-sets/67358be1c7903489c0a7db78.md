---
id: 67358be1c7903489c0a7db78
title: Quiz su mappe e set in JavaScript
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos'è un Set in JavaScript?

#### --distractors--

Una struttura dati che permette valori duplicati.

---

Una struttura dati che memorizza solo stringhe e numeri.

---

Una collezione che non permette la cancellazione dei suoi elementi.

#### --answer--

Una collezione in cui un valore può comparire una sola volta.

### --question--

#### --text--

Quale dei seguenti metodi è disponibile nelle Mappe ma non nei Set?

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

Quale metodo si usa per aggiungere elementi a un Set?

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

Come si verifica se un Set contiene un certo valore?

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

Quale di questi è un ottimo caso d'uso per i Set?

#### --distractors--

Gestire coppie chiave-valore in un database.

---

Creare liste ordinate di elementi.

---

Memorizzare strutture dati gerarchiche nel local storage del browser.

#### --answer--

Eliminare i duplicati da un array.

### --question--

#### --text--

Cos'è un WeakSet in JavaScript?

#### --distractors--

Una collezione simile a un Set che permette solo tipi di dati primitivi.

---

Una collezione simile a un Set che permette valori duplicati.

---

Una collezione simile a un Set che impedisce la rimozione dei valori.

#### --answer--

Una collezione simile a un Set che memorizza solo oggetti.

### --question--

#### --text--

Qual è una differenza tra un Set e un WeakSet?

#### --distractors--

Un WeakSet può memorizzare qualsiasi tipo di valore, inclusi i numeri.

---

Un Set non permette valori duplicati, mentre un WeakSet sì.

---

Un Set permette la garbage collection, mentre un WeakSet no.

#### --answer--

Un Set può contenere qualsiasi tipo di valore, mentre un WeakSet contiene solo oggetti.

### --question--

#### --text--

Cos'è una Map in JavaScript?

#### --distractors--

Una collezione che permette solo valori unici.

---

Una collezione che può usare solo stringhe come chiavi.

---

Una collezione che non permette la cancellazione dei suoi elementi.

#### --answer--

Una collezione di coppie chiave-valore di qualsiasi tipo.

### --question--

#### --text--

Quale metodo si usa per inserire coppie chiave-valore in una Map?

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

Quale sarà l'output del seguente codice?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

In cosa differisce una Map da un Oggetto in JavaScript?

#### --distractors--

Una Map permette solo numeri come chiavi.

---

Gli oggetti permettono qualsiasi tipo come chiavi, mentre le Map solo stringhe.

---

Le Map non supportano l'iterazione, mentre gli oggetti sì.

#### --answer--

Le Map permettono qualsiasi tipo di chiave, mentre gli oggetti solo stringhe e simboli.

### --question--

#### --text--

Come si può rimuovere una coppia chiave-valore da una Map?

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

Cosa restituisce la proprietà `size` di una Map?

#### --distractors--

Il numero di indici nella Map.

---

Il numero di valori unici nella Map.

---

La memoria totale usata dalla Map.

#### --answer--

Il numero di coppie chiave-valore nella Map.

### --question--

#### --text--

Cos'è una WeakMap in JavaScript?

#### --distractors--

Una collezione simile a una Map che permette valori primitivi come chiavi.

---

Una collezione simile a una Map che blocca la cancellazione degli elementi.

---

Una collezione simile a una Map che ordina automaticamente le sue chiavi.

#### --answer--

Una collezione simile a una Map con riferimenti deboli alle chiavi.

### --question--

#### --text--

Qual è una differenza tra una Map e una WeakMap?

#### --distractors--

Una Map permette solo tipi primitivi come chiavi, mentre una WeakMap solo oggetti.

---

Una Map permette la garbage collection, mentre una WeakMap no.

---

Una WeakMap permette chiavi duplicate, mentre una Map no.

#### --answer--

Una Map è iterabile, mentre una WeakMap no.

### --question--

#### --text--

Quale dei seguenti metodi NON esiste in una WeakMap?

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

Quale sarà l'output del seguente codice?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

Come puoi iterare sui valori in un Set?

#### --distractors--

Usando il metodo `.map()`.

---

Usando il metodo `.reduce()`.

---

Usando il metodo `.filter()`.

#### --answer--

Usando il metodo `.forEach()`.

### --question--

#### --text--

Quali due metodi dei Set restituiscono un `SetIterator` che contiene i valori di un certo Set?

#### --distractors--

`entries()` e `values()`

---

`forEach()` e `values()`

---

`has()` e `add()`

#### --answer--

`keys()` e `values()`

### --question--

#### --text--

Quale sarà l'output del seguente codice?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
