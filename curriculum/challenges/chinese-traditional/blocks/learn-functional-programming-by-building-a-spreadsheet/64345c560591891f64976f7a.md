---
id: 64345c560591891f64976f7a
title: 步驟 13
challengeType: 0
dashedName: step-13
---

# --description--

現在你的輔助函式已完成，在你的 `onload` 事件處理常式中，你應該宣告一個 `letters` 變數。將呼叫 `charRange()` 並以字母 `"A"` 和 `"J"` 作為引數的結果指定給它。

# --hints--

你應該宣告一個 `letters` 變數。

```js
assert.match(code, /(?:let|const|var)\s+letters/);
```

你應該使用 `const` 來宣告你的 `letters` 變數。

```js
assert.match(code, /const\s+letters/);
```

你應該將 `charRange()` 呼叫指定給你的 `letters` 變數。

```js
assert.match(code, /const\s+letters\s*=\s*charRange\(/);
```

你應該將 `"A"` 作為第一個引數傳遞給你的 `charRange()` 呼叫。

```js
assert.match(code, /const\s+letters\s*=\s*charRange\(\s*('|"|`)A\1/);
```

你應該將 `"J"` 作為第二個引數傳遞給你的 `charRange()` 呼叫。

```js
assert.match(code, /const\s+letters\s*=\s*charRange\(\s*('|"|`)A\1\s*,\s*('|"|`)J\2\s*\)/)
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Functional Programming Spreadsheet</title>
  </head>
  <body>
    <div id="container">
      <div></div>
    </div>
    <script src="./script.js"></script>
  </body>
</html>
```

```css
#container {
  display: grid;
  grid-template-columns: 50px repeat(10, 200px);
  grid-template-rows: repeat(11, 30px);
}

.label {
  background-color: lightgray;
  text-align: center;
  vertical-align: middle;
  line-height: 30px;
}
```

```js
const range = (start, end) => Array(end - start + 1).fill(start).map((element, index) => element + index);
const charRange = (start, end) => range(start.charCodeAt(0), end.charCodeAt(0)).map(code => String.fromCharCode(code));

window.onload = () => {
  const container = document.getElementById("container");
  const createLabel = (name) => {
    const label = document.createElement("div");
    label.className = "label";
    label.textContent = name;
    container.appendChild(label);
  }
--fcc-editable-region--

--fcc-editable-region--
}
```
