---
id: 6a02e90ad969a6287d1b4f87
title: Quiz su HTTP e il modello degli standard web
challengeType: 8
dashedName: quiz-http-and-the-web-standards-model
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cosa significa DNS e qual è la sua funzione principale?

#### --distractors--

Data Naming System, cripta i nomi di dominio prima di inviarli sulla rete.

---

Domain Node Server, memorizza file per siti web in modo che i browser possano scaricarli.

---

Dynamic Network Service, assegna indirizzi IP ai dispositivi quando si connettono.

#### --answer--

Domain Name System, traduce i nomi di dominio leggibili dall’uomo in indirizzi IP.

### --question--

#### --text--

Nel processo di risoluzione DNS, quale componente esegue le query iterative attraverso Root Server, TLD Server e Authoritative Name Server?

#### --distractors--

L’Authoritative Name Server

---

Il TLD Server

---

Il Root Server

#### --answer--

Il Recursive Resolver

### --question--

#### --text--

Cosa fa TCP che il solo Internet Protocol (IP) non fa?

#### --distractors--

Instrada i pacchetti dalla sorgente alla destinazione usando indirizzi IP.

---

Cripta i dati prima di inviarli sulla rete.

---

Assegna nomi di dominio agli indirizzi IP.

#### --answer--

Garantisce una consegna affidabile e ordinata numerando i segmenti e richiedendo la ritrasmissione dei pacchetti persi.

### --question--

#### --text--

Quale metodo HTTP dovrebbe essere usato per aggiornare parzialmente una risorsa esistente su un server?

#### --distractors--

`PUT`

---

`POST`

---

`DELETE`

#### --answer--

`PATCH`

### --question--

#### --text--

Qual è l’intestazione `Content-Type` corretta per servire una risposta JSON?

#### --distractors--

`text/json`

---

`text/plain`

---

`application/xml`

#### --answer--

`application/json`

### --question--

#### --text--

Quale codice di stato HTTP indica che una risorsa richiesta è stata spostata permanentemente a un nuovo URL?

#### --distractors--

`302`

---

`304`

---

`404`

#### --answer--

`301`

### --question--

#### --text--

Qual è la differenza tra una risposta `401 Unauthorized` e una `403 Forbidden`?

#### --distractors--

`401` significa che il server è andato in crash mentre `403` significa che la risorsa non esiste.

---

`401` è un errore del client mentre `403` è un errore del server.

---

`401` significa che la risorsa è stata spostata permanentemente mentre `403` significa che è stata spostata temporaneamente.

#### --answer--

`401` significa che sono richieste credenziali mentre `403` significa che il client ha credenziali valide ma non ha il permesso per la risorsa.

### --question--

#### --text--

Quale parte di una richiesta HTTP contiene il metodo, il percorso e la versione HTTP?

#### --distractors--

Il corpo della risposta

---

Il corpo della richiesta

---

Le intestazioni della richiesta

#### --answer--

La riga della richiesta

### --question--

#### --text--

Cosa comunica al client il codice di stato `304 Not Modified`?

#### --distractors--

La risorsa è stata eliminata dal server.

---

La richiesta era malformata e deve essere inviata di nuovo.

---

Il server è temporaneamente non disponibile.

#### --answer--

La risorsa non è cambiata dall’ultima richiesta, quindi il client dovrebbe usare la sua versione in cache.

### --question--

#### --text--

Nel ciclo di richiesta-risposta HTTP, cosa succede immediatamente dopo che il server ha elaborato la richiesta?

#### --distractors--

Il client invia una seconda richiesta per confermare la ricezione.

---

Il resolver DNS memorizza la risposta nella cache.

---

Il client avvia una nuova connessione TCP.

#### --answer--

Il server prepara una risposta con un codice di stato, intestazioni e un corpo opzionale.

### --question--

#### --text--

Quale attributo HTML specifica dove un modulo invia i suoi dati quando viene inviato?

#### --distractors--

`method`

---

`name`

---

`type`

#### --answer--

`action`

### --question--

#### --text--

Perché la validazione lato server deve sempre essere eseguita anche quando è già presente la validazione lato client?

#### --distractors--

La validazione lato client non è supportata nei browser moderni.

---

La validazione lato server è necessaria solo per il caricamento di file.

---

La validazione lato client è più veloce di quella lato server.

#### --answer--

La validazione lato client può essere bypassata, quindi la validazione lato server è necessaria per la sicurezza.

### --question--

#### --text--

Quale organismo di standard mantiene HTML e il DOM come standard vivente continuamente aggiornato?

#### --distractors--

W3C

---

ECMA International

---

Khronos Group

#### --answer--

WHATWG

### --question--

#### --text--

Cos’è TC39 e qual è il suo ruolo nell’ecosistema degli standard web?

#### --distractors--

Un gruppo di lavoro W3C responsabile delle specifiche CSS.

---

Un consorzio di vendor di browser che decide quali funzionalità includere in Chrome.

---

Un comitato del Khronos Group che gestisce gli standard WebGL.

#### --answer--

Un comitato tecnico all’interno di ECMA International che sviluppa le funzionalità del linguaggio JavaScript attraverso un processo di proposta in cinque fasi.

### --question--

#### --text--

Durante quale fase del ciclo di vita degli standard web una funzionalità viene sottoposta a stress test tramite discussioni aperte, raccolta di casi d’uso e approcci di design concorrenti?

#### --distractors--

Test della specifica

---

Test di finalizzazione

---

Test di interoperabilità

#### --answer--

Incubazione

### --question--

#### --text--

Qual è lo scopo della suite W3C Web Platform Tests durante il processo di standardizzazione?

#### --distractors--

Classificare le prestazioni dei browser a scopo di marketing.

---

Sostituire la necessità di una specifica scritta.

---

Consentire agli sviluppatori di proporre nuove funzionalità.

#### --answer--

Verificare che ogni browser implementi una funzionalità in modo identico.

### --question--

#### --text--

Quale principio degli standard web garantisce che una pagina costruita secondo la specifica si comporti allo stesso modo in Chrome, Firefox e Safari?

#### --distractors--

Compatibilità retroattiva

---

Miglioramento progressivo

---

Apertura

#### --answer--

Interoperabilità

### --question--

#### --text--

Cosa richiede il principio del miglioramento progressivo nella progettazione di una funzionalità web?

#### --distractors--

La funzionalità deve essere destinata solo alle versioni più recenti dei browser.

---

La funzionalità deve essere implementata in JavaScript prima di CSS o HTML.

---

La funzionalità deve essere finalizzata prima che qualsiasi browser possa implementarla.

#### --answer--

La funzionalità deve funzionare prima per tutti, poi migliorare per gli utenti con browser o dispositivi più potenti.

### --question--

#### --text--

Perché il web mantiene una compatibilità quasi assoluta con le versioni precedenti quando introduce nuovi standard?

#### --distractors--

I nuovi standard sostituiscono sempre quelli vecchi, quindi le pagine più vecchie vengono aggiornate automaticamente.

---

I vendor dei browser sono legalmente obbligati a supportare ogni pagina mai pubblicata.

---

Le pagine vecchie sono archiviate e servite da una rete separata.

#### --answer--

Affinché le pagine costruite anni fa continuino a funzionare correttamente nei browser moderni.

### --question--

#### --text--

Quale categoria di codice di stato HTTP viene usata per gli errori lato server?

#### --distractors--

`2xx`

---

`3xx`

---

`4xx`

#### --answer--

`5xx`
