---
id: 672bd814105a0ffcf36f9233
title: 什麼是字型家族以及它們如何運作？
challengeType: 19
dashedName: what-are-font-families
---

# --interactive--

字型家族是一組具有共同設計的字型。所有屬於同一家族的字型都是基於相同的核心字體，但它們在樣式、粗細和寬度上也有變化。你可以將它們想像成具有相似特徵但也有些差異的兄弟姊妹。<dfn>例如</dfn>，Arial 是一個字型家族，包含像是 Arial Bold 和 Arial Italic 這樣的變體。

在 CSS 中，你可以使用 `font-family` 屬性設定字型。以下是將段落字型設定為 `Arial` 的範例：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="arial-font">Example paragraph using Arial font.</p>
<p>Paragraph not using Arial font.</p>
```

```css
#arial-font {
  font-family: Arial;
}
```

:::

但是如果找不到字型家族怎麼辦？你可以依照優先權從高到低指定多個字型家族，並以逗號分隔。這些替代字型將作為備援選項。在此範例中，Arial 是主要字型，而 Lato 是替代字型：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="specified-font">Example paragraph using specified fonts.</p>
<p>Paragraph not using specified fonts.</p>
```

```css
#specified-font {
  font-family: Arial, Lato;
}
```

:::

瀏覽器會在裝置上找到第一個字型（Arial）時呈現它。如果找不到，則會呈現第二個字型（Lato）。

你也應該知道這個處理程序在幕後如何運作。選擇處理程序不會因為第一個字型可用而停止。字型家族是逐一字元選擇的，所以如果某個字型缺少特定字元，瀏覽器會在較低優先權的字型中尋找。很有趣，對吧？

在網頁開發的背景關係中，你也會找到泛型字型家族。它們非常重要。泛型字型家族是當較高優先權的字型無法使用時，網頁瀏覽器會呈現的預設字型。為了確保內容仍然可讀，瀏覽器會根據指定的泛型字型家族，將原始字型取代為最適合的字型。其中一些最常用的是： 

- 襯線
- 無襯線
- 等寬字體
- 斜體
- 奇幻

但是還有更多選項可供選擇。在下面的程式碼中，我們有三個替代的字型家族：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<p id="specified-font">Example paragraph using specified fonts.</p>
<p>Paragraph not using specified fonts.</p>
```

```css
#specified-font {
  font-family: Arial, Lato, sans-serif;
}
```

:::

Arial 擁有最高優先權。如果找不到 Arial，瀏覽器將嘗試呈現 Lato。如果兩者皆找不到，瀏覽器將使用泛型 sans-serif 字型家族，從使用者系統中已安裝且具有這些特性的字型中選擇。

你應該總是在字型系列列表的最後包含一個泛型字型系列。請記住，泛型字型可能看起來與你在設計中原本想像的字型不同，因此檢查這些備用字型在不同瀏覽器上的顯示效果總是很有幫助。

為了讓使用者體驗盡可能一致，也建議使用網頁安全字型。這些字型家族通常已安裝在大多數裝置上，因此大多數使用者很可能會找到並正確呈現它們。你將在接下來的課程中學習更多相關內容。

字型家族在網頁設計的世界中至關重要。透過使用 `font-family` 屬性，你可以立即改變你的文字的外觀與感覺。

# --questions--

## --text--

`font-family` CSS 屬性的主要用途是什麼？

## --answers--

設定文字的顏色。

### --feedback--

思考文字的視覺外觀。

---

控制元件中文字的字型大小。

### --feedback--

思考文字的視覺外觀。

---

定義用於文字的字型。

---

調整文字的行距。

### --feedback--

思考文字的視覺外觀。

## --video-solution--

3

## --text--

以下哪一項是常見的泛型字型家族？

## --answers--

`Helvetica`

### --feedback--

思考字型的廣泛類別。

---

`Times New Roman`

### --feedback--

思考字型的廣泛類別。

---

`Sans-Serif`

---

`Arial`

### --feedback--

思考字型的廣泛類別。

## --video-solution--

3

## --text--

你如何在 CSS 中指定多個字型家族作為備用？

## --answers--

透過逗號分隔它們。

---

透過使用巢狀的 `font-family` 宣告。

### --feedback--

考慮在主要字型無法使用時，如何提供替代字型。

---

透過使用 `&` 字元。

### --feedback--

考慮在主要字型無法使用時，如何提供替代字型。

---

透過使用 `|` 字元。

### --feedback--

考慮在主要字型無法使用時，如何提供替代字型。

## --video-solution--

1
