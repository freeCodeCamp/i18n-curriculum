---
id: 67f41242431cbf3db8ca79c7
title: اختبار أساسيات Python
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

لتجاوز الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي من الدوال التالية تُستخدم للتحقق مما إذا كان المتغير يطابق نوع بيانات محدد؟

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

أي مما يلي ليس شكلًا من أشكال ربط السلاسل النصية؟

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

أي من الدوال التالية تُستخدم لإرجاع عدد الأحرف في السلسلة النصية؟

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

ما قيمة `result` في هذا المثال؟

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

ماذا يحدث عند جمع عدد عشري وعدد صحيح؟

#### --distractors--

النتيجة ستكون رسالة خطأ.

---

النتيجة ستكون `None`.

---

النتيجة ستكون عددًا صحيحًا.

#### --answer--

النتيجة ستكون عددًا عشريًا.

### --question--

#### --text--

أي من الطرق التالية هي الطريقة الصحيحة لتعريف دالة؟

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

ماذا سيُطبع في وحدة التحكم؟

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

أي من العبارات التالية خاطئة عند تسمية المتغيرات؟

#### --distractors--

أسماء المتغيرات يمكن أن تبدأ فقط بحرف أو بشرطة سفلية (_) وليس برقم.

---

أسماء المتغيرات يمكن أن تحتوي فقط على أحرف أبجدية رقمية (a-z, A-Z, 0-9) والشرطة السفلية (_).

---

أسماء المتغيرات لا يمكن أن تكون من الكلمات المفتاحية المحجوزة في Python مثل `if`، `class`، أو `def`.

#### --answer--

يجب أن يكون الحد الأقصى لطول أسماء المتغيرات 10 أحرف، وإلا ستُصدر Python خطأ.

### --question--

#### --text--

أي مما يلي ليس نوع بيانات مدعومًا في Python؟

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

أي مما يلي سيُرجع سلسلة نصية جديدة مع تحويل جميع الأحرف إلى أحرف كبيرة؟

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

أي من الدوال التالية تُستخدم للحصول على مُدخَل من المستخدم؟

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

ما النتيجة للكود التالي؟

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

ماذا تفعل الطريقة `split()`؟

#### --distractors--

تُستخدم هذه الطريقة لتقسيم صف مرتب إلى قائمة من السلاسل النصية الفرعية.

---

تُستخدم هذه الطريقة لتقسيم عدد عشري إلى قائمة من السلاسل النصية الفرعية.

---

تُستخدم هذه الطريقة لتقسيم قاموس إلى قائمة من السلاسل النصية الفرعية.

#### --answer--

تُستخدم هذه الطريقة لتقسيم سلسلة نصية إلى قائمة من السلاسل النصية الفرعية.

### --question--

#### --text--

ماذا سيُطبع التالي في وحدة التحكم؟

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

أي من الدوال التالية تُستخدم لإنشاء جدول من تعيينات حرف إلى حرف للترجمة؟

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

ما النتيجة للكود التالي؟

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

ماذا سترجع الطريقة `find()` إذا لم يتم العثور على أي تكرارات للسلسلة الفرعية في السلسلة النصية؟

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

أي من الدوال التالية تُستخدم لحساب عدد مرات ظهور سلسلة فرعية في سلسلة نصية؟

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

ماذا يفعل القسمة الأرضية في Python؟

#### --distractors--

يُستخدم هذا العامل لضرب رقمين وتقريب النتيجة لأعلى إلى أقرب عدد صحيح.

---

يُستخدم هذا العامل لتحويل عدد عشري إلى عدد صحيح.

---

يُستخدم هذا العامل لرفع رقم إلى قوة رقم آخر.

#### --answer--

يُستخدم هذا العامل لقسمة رقمين وتقريب النتيجة لأسفل إلى أقرب عدد صحيح.

### --question--

#### --text--

أي من الدوال التالية تُستخدم لتقريب رقم إلى أقرب عدد صحيح؟

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`

