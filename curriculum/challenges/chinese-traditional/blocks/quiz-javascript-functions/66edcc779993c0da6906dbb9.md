---
id: 66edcc779993c0da6906dbb9
title: JavaScript 函式（程式）小測驗
challengeType: 8
dashedName: quiz-javascript-functions
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一種是用 JavaScript 宣告函式（程式）的正確方式？

#### --distractors--

```js
func greet() {
  console.log("Hello there!");
}
```

---

```js
defineFunction greet() {
  console.log("Hello there!");
}
```

---

```js
def greet() {
  console.log("Hello there!");
}
```

#### --answer--

```js
function greet() {
  console.log("Hello there!");
}
```

### --question--

#### --text--

當你呼叫（或執行）一個函式（程式）時會發生什麼？

#### --distractors--

函式內的程式碼將會執行，且會永遠傳回 `null`。

---

當你呼叫函式時不會發生任何事情。

---

會丟出 `TypeError` 且程式將當機。

#### --answer--

函式內的程式碼將會執行並傳回一個值。

### --question--

#### --text--

以下哪一種是呼叫函式的正確方式？

#### --distractors--

```js
exampleFunction;
```

---

```js
exampleFunction>>;
```

---

```js
()exampleFunction();
```

#### --answer--

```js
exampleFunction();
```

### --question--

#### --text--

`return` 關鍵字的角色是什麼？

#### --distractors--

`return` 關鍵字將執行該函式（程式）。

---

`return` 關鍵字會丟出型別錯誤。

---

`return` 關鍵字在函式中沒有任何用途。

#### --answer--

`return` 關鍵字會從函式傳回一個值。

### --question--

#### --text--

以下哪一項是有效的函式（程式）表達式？

#### --distractors--

```js
expression function getSum(x, y) {
    return x + y;
};
```

---

```js
getSum: function(x, y) {
    return x + y;
};
```

---

```js
function = getSum(x, y) {
    return x + y;
};
```

#### --answer--

```js
const getSum = function(x, y) {
    return x + y;
};
```

### --question--

#### --text--

什麼是函式（程式）的引數？

#### --distractors--

表述值缺失的值。

---

類似匿名函式（程式）的特殊值。

---

函式內使用的佔位符值。

#### --answer--

呼叫函式時傳遞的實際值。

### --question--

#### --text--

什麼是函式（程式）的參數？

#### --distractors--

在函式內使用的浮點數值。

---

在呼叫函式時傳遞的值。

---

表示刻意缺少該值的值。

#### --answer--

列在函式（程式）定義內的值。

### --question--

#### --text--

什麼會被記錄到主控台？

```js
const calculateTotal = (amount, taxRate = 0.05) => {
  return amount + (amount * taxRate);
};

console.log(calculateTotal(100));
```

#### --distractors--

`10`

---

`undefined`

---

`1`

#### --answer--

`105`

### --question--

#### --text--

以下哪一項是箭頭函式的正確語法？

#### --distractors--

```js
const sum <<>> (num1, num2) => num1 + num2
```

---

```js
const sum === (num1, num2) === num1 + num2
```

---

```js
const sum >=> (num1, num2) => num1 + num2
```

#### --answer--

```js
const sum = (num1, num2) => num1 + num2
```

### --question--

#### --text--

以下 `code` 的結果會是什麼？

```js
function greet() {
  const developer = "Jessica";
  console.log("Hello there!");
}

console.log(developer);
```

#### --distractors--

不會發生任何事情，且程式碼會正常執行。

---

字串 `"Hello there!"` 將會被記錄到主控台。

---

字串 `"Hello!"` 將會被記錄到主控台。

#### --answer--

你會得到一個引用錯誤，因為 `developer` 沒有在全域的定義。

### --question--

#### --text--

你何時可以省略箭頭函式的參數列周圍的一組小括弧？

#### --distractors--

你應該總是省略參數列周圍的小括弧。

---

你絕對不應該省略箭頭函式的小括弧。

---

只有在你處理多個參數時，才應省略小括弧。

#### --answer--

只有當你只有一個參數時，才應該省略小括弧。

### --question--

#### --text--

你何時可以省略箭頭函式的花括號和 `return` 關鍵字？

#### --distractors--

你應該始終省略大括號和 `return` 關鍵字。

---

你絕對不應該省略大括號和 `return` 關鍵字。

---

當函式（程式）主體有多行程式碼時。

#### --answer--

當函式（程式）主體由單一表達式組成時。

### --question--

#### --text--

以下函式會傳回什麼？

```js
function exampleFunction() {
  return "Hello";
  return "World!";
};
```

#### --distractors--

`"World!"`

---

`"Hello World!"`

---

`undefined`

#### --answer--

`"Hello"`

### --question--

#### --text--

以下哪一種是使用預設參數的正確方式？

#### --distractors--

```js
function sum(num1, num2 <<>> 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 === 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 >> 12) {
  return num1 + num2
}
```

#### --answer--

```js
function sum(num1, num2 = 12) {
  return num1 + num2
}
```

### --question--

#### --text--

以下 `code` 的結果會是什麼？

```js
const developer = "Jessica";

function greet() {
  console.log("Hello, " + developer)
}

greet();
```

#### --distractors--

字串 `"Hello, developer"` 將會被記錄到主控台。

---

錯誤訊息將顯示在主控台。

---

主控台中不會顯示任何內容。

#### --answer--

字串 `"Hello, Jessica"` 將會被記錄到主控台。

### --question--

#### --text--

如果你嘗試移除一般函式的花括號會發生什麼？

#### --distractors--

頁面上會顯示一個帶有錯誤的警示框。

---

程式會當機且無法重新啟動。

---

不會發生任何事情。

#### --answer--

你會得到語法錯誤。

### --question--

#### --text--

以下 `code` 的結果會是什麼？

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + 10);
```

#### --distractors--

錯誤訊息將顯示在主控台。

---

數字 `0` 將會被記錄到主控台。

---

不會有任何內容記錄到主控台。

#### --answer--

數字 `10` 將會被記錄到主控台。

### --question--

#### --text--

以下 `code` 的輸出結果會是什麼？

```js
const exampleFunction = (param1, param2) => param1 + param2;
console.log(exampleFunction(3, "Something"));
```

#### --distractors--

`null`

---

`undefined`

---

`3 + "Something"`

#### --answer--

`"3Something"`

### --question--

#### --text--

以下 `code` 的結果會是什麼？

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + num2);
```

#### --distractors--

一則錯誤訊息顯示 `num1` 未定義。

---

數字 `0` 將會顯示在主控台。

---

`undefined` 將會顯示在主控台。

#### --answer--

一則錯誤訊息顯示 `num2` 未定義。

### --question--

#### --text--

以下 `code` 的輸出結果會是什麼？

```js
const divideTwoNumbers = (num1, num2) => num1 / num2;
console.log(divideTwoNumbers(3, 0));
```

#### --distractors--

`Error`

---

`0`

---

`null`

#### --answer--

`Infinity`

