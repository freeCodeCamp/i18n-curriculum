---
id: 672bd658c0e190f674a5e057
title: 常见的 Flex 属性有哪些，它们是如何工作的？
challengeType: 19
dashedName: what-are-some-common-flex-properties
---

# --interactive--

Flex 属性是你可以应用到弹性容器上的属性，用于确定子元素的分布。我们将介绍一些最常用的属性：`flex-wrap`、`justify-content` 和 `align-items`。

让我们从 `flex-wrap` 开始。该属性决定了弹性项如何在弹性容器内换行以适应可用空间。`flex-wrap` 可以取三个可能的值：`nowrap`、`wrap` 和 `wrap-reverse`。`nowrap` 是默认值——即使弹性项的宽度超过容器宽度，也不会换行到新的一行。

在下面的代码中，我们有三个 `div` 元素。让我们关注 `width`。用红色边框标记的 `main` 容器的 `width` 是 `200px`，而它的三个子 `div` 元素的总 `width` 是 `240px`（每个 `80px`）：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::


`div` 元素的宽度超过了它们容器的宽度，但默认情况下它们会被缩小以适应可用空间。如果你确实想在它们超过容器宽度时换行，可以在弹性容器上设置 `flex-wrap: wrap`：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  flex-wrap: wrap;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

当 `div` 元素超出其容器宽度时，它们将按行重新排列。你可以使用 `flex-wrap: wrap-reverse` 以相反顺序换行弹性项。`flex-flow` 属性是 `flex-direction` 和 `flex-wrap` 的简写属性。在此示例中，我们将 `flex-direction` 设置为 `column`，将 `flex-wrap` 设置为 `wrap-reverse`：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  width: 200px;
  display: flex;
  flex-flow: column wrap-reverse;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

很好。现在让我们来谈谈 `justify-content`。`justify-content` 沿着弹性容器的主轴对子元素进行对齐。如果你为 `justify-content` 赋值为 `flex-start`，弹性项将对齐到主轴的起点。这可以是水平的也可以是垂直的：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: flex-start;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

使用 `justify-content: flex-end`，弹性项沿主轴的末端对齐，水平或垂直方向。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: flex-end;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

要沿主轴居中 `flex` 项，可以使用 `justify-content: center`。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: center;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

要沿主轴均匀分布元素，你可以使用 `justify-content: space-between`。如果需要，这将为弹性项之间添加一些间距。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-between;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

`justify-content: space-around` 在主轴内均匀分布弹性项，为第一个项之前和最后一个项之后添加空间。这个额外的空间是每对相邻项之间空间的一半。如果只有一项需要分布，它将被居中。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-around;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::


最后但同样重要的是，我们有 `justify-content: space-evenly`，它沿主轴均匀分布这些项。项之间的间距以及第一个和最后一个元素之前和之后的间距完全相同：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  display: flex;
  justify-content: space-evenly;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

太好了。现在你知道如何沿主轴分布 flex 项。但你可能还想沿交叉轴分布它们。记住交叉轴垂直于主轴。你可以使用 `align-items` 属性来实现这一点。要沿交叉轴居中项，只需为 flex 容器添加 `align-items: center`： 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: center;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

在此示例中，弹性项沿交叉轴居中，交叉轴默认是垂直的。如果交叉轴是水平的，它们将改为水平居中。相比之下，`align-items: flex-start` 将项对齐到交叉轴的起点：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: flex-start;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

相反的是 `align-items: flex-end`。这将使弹性项沿交叉轴的末端对齐，垂直或水平。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<main>
  <div id="first-div"></div>
  <div id="second-div"></div>
  <div id="third-div"></div>
</main>
```

```css
main {
  height: 300px;
  display: flex;
  align-items: flex-end;
  border: 2px solid red;
}

div {
  width: 80px;
  height: 50px;
}

#first-div {
  background-color: #4d70b2;
}

#second-div {
  background-color: #5c4db2;
}

#third-div {
  background-color: #4da3b2;
}
```

:::

要沿交叉轴拉伸弹性项，你可以使用 `align-items: stretch`。这也适用于自动调整大小的元素，例如那些没有设置 `width` 或 `height`，或者只有最小 `width` 或 `height` 的元素。弹性项将在交叉轴方向上拉伸以填充容器。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: stretch; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;           
  border: 1px solid #888;
  background-color: lightblue;
}
```

:::

最后，你可以使用 `align-self` 属性为单个弹性项赋值不同的交叉轴对齐方式。例如，你可以使用 `align-self: stretch` 将其拉伸。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: stretch; 
  background-color: lightcoral;
}
```

:::

你可以用 `align-self: center` 将其居中。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: center; 
  background-color: lightcoral;
}
```

:::

你可以使用 `align-self: flex-start` 将其对齐到交叉轴的起点。 

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: flex-start; 
  background-color: lightcoral;
}
```

:::

或者你可以使用 `align-self: flex-end` 将其对齐到交叉轴的末端。

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">

<div class="container">
  <div class="box">One</div>
  <div class="box">Two</div>
  <div class="box special">Three</div>
</div>

```

```css
.container {
  display: flex;
  align-items: flex-start; 
  border: 2px solid #444;
  height: 200px; 
}

.box {
  width: 100px;
  border: 1px solid #888;
  background-color: lightblue;
  margin: 4px;
}

.special {
  align-self: flex-end; 
  background-color: lightcoral;
}
```

:::

还有其他的 flex 属性和值，你可以选择来创建你设想的响应式版型，但这些是最常用的。通过这些 CSS flex 属性和你对 CSS flex 模型的新知识，你可以开始创建响应式版型，以在各种设备上打造流畅且包容的用户体验。

# --questions--

## --text--

哪个 CSS `property` 用于在 `flex container` 内水平（默认）对齐子 `Element`？

## --answers--

`flex-direction`

### --feedback--

考虑元素在弹性容器内的水平对齐方式。

---

`justify-content`

---

`align-items`

### --feedback--

考虑元素在弹性容器内的水平对齐方式。

---

`flex-wrap`

### --feedback--

考虑元素在弹性容器内的水平对齐方式。

## --video-solution--

2

## --text--

哪个 CSS `property` 用于在弹性容器内垂直（默认）对齐子元素？

## --answers--

`flex-direction`

### --feedback--

考虑元素在弹性容器内如何垂直对齐。

---

`justify-content`

### --feedback--

考虑元素在弹性容器内如何垂直对齐。

---

`align-items`

---

`flex-wrap`

### --feedback--

考虑元素在弹性容器内如何垂直对齐。

## --video-solution--

3

## --text--

以下哪些属性可以合并到 `flex-flow` 属性中？

## --answers--

`flex-direction` 和 `justify-content`。

### --feedback--

考虑 flex 项布局的两个主要方面。

---

`flex-direction` 和 `align-items`。

### --feedback--

考虑 flex 项布局的两个主要方面。

---

`flex-direction` 和 `align-content`。

### --feedback--

考虑 flex 项布局的两个主要方面。

---

`flex-direction` 和 `flex-wrap`。

## --video-solution--

4
