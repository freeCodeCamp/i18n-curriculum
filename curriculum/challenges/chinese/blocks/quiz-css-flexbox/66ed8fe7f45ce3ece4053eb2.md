---
id: 66ed8fe7f45ce3ece4053eb2
title: CSS 弹性盒子测验
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

什么是 flexbox？

#### --distractors--

一个用于版型的二维模型。

---

用于版型的三维模型。

---

一个用于版型的多维模型。

#### --answer--

一个用于 `layout` 的一维 `model`。

### --question--

#### --text--

哪个 CSS 属性被设置为启用 `div` 元素的 flexbox 版型？

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

哪个 CSS 属性允许弹性项在当前行满时换行到新行？

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

`justify-content` 控制什么？

#### --distractors--

它控制 `flexbox` 容器子元素的方向。

---

它控制 `flexbox` 容器子元素的文本样式。

---

它控制 flexbox 容器内子元素的对齐方式。

#### --answer--

它控制子元素在主轴上的分布。

### --question--

#### --text--

哪个属性控制 flexbox 容器内元素的方向？

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

哪些 CSS 属性可以用于使 flex 项从左到右排列，并且如果当前行的宽度超出，新建的 flex 项会出现在前一个项的上方？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

哪个属性控制元素在交叉轴上的定位？

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

哪个不是 `justify-content` 的值？

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

你会使用哪些 CSS 属性将一个元素在弹性容器中水平和垂直居中？

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

`flex-direction` 的什么值使弹性项从右向左排列？  

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

`flex-wrap` 属性的值中不包括什么？

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

`align-items` 中的 `stretch` 值有什么作用？

#### --distractors--

项直接紧挨着彼此朝容器的起始边缘打包。

---

项对齐，使它们的 flex 容器基线对齐。

---

项直接紧贴容器的末端边缘打包。

#### --answer--

自动调整项大小，使其等大以填充容器。

### --question--

#### --text--

哪个 `justify-content` 值将第一项放在起点，最后一项放在终点，并沿主轴均匀分布其他项？

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

哪个 `justify-content` 值使项之间以及项与容器边缘之间的间隙全部相等？

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

哪个 `justify-content` 值沿主轴均匀分布项，同时在容器边缘留出一半的间距？

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

以下哪项会导致项在交叉轴的起点对齐？

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

以下哪项会导致项在交叉轴的末端对齐？

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

`flex-direction` 的哪个值会使项沿主轴从左到右排列？

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

`flex-direction` 的哪个值会使项沿交叉轴从上到下排列？

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

以下哪个使用分支不适合使用 flexbox？

#### --distractors--

将一项居中放置在容器中。

---

行内项的垂直对齐。

---

动态调整项的分布（版本）和对齐以适应不同的视图端口。

#### --answer--

固定大小和项之间的间距，类似于网格。

## --quiz--

### --question--

#### --text--

像 `Flexbox` 这样的单维版型模型的目的是什么？

#### --distractors--

同时处理行和列的二维版型。

---

使用 `z-index` 控制定位元素的堆叠顺序。

---

为页面元素创建 3D 变换和复杂动画。

#### --answer--

将项沿单行或轴线布局，可以作为一行或一列。

### --question--

#### --text--

哪个 CSS `property` 会将所有的 flex `items` 对齐到 `.container` `element` 的右侧？

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Flex 模型中定义项版型方向的两个轴是什么？

#### --distractors--

水平轴和垂直轴。

---

x 轴和 y 轴。

---

主轴和次轴。

#### --answer--

主轴和交叉轴。

### --question--

#### --text--

以下哪个 CSS 规则会将项垂直排列成一列，并且在其容器内水平居中？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

`flex-wrap` 属性的主要用途是什么？

#### --distractors--

它设置了弹性项在弹性容器中排列的方向。

---

它沿着容器的主轴对齐弹性项。

---

它沿着容器的交叉轴对齐弹性项。

#### --answer--

它确定弹性项是否应换行以适应可用空间。

### --question--

#### --text--

`flex-wrap: nowrap;` 声明的默认行为是什么？

#### --distractors--

它迫使 flex 项如果对容器来说太宽，则换行到新的一行。

---

它将项换行，但顺序是反向的。

---

它防止 `flex container` 大于其父元素。

#### --answer--

它迫使 flex 项保持在单行，即使它们溢出容器。

### --question--

#### --text--

哪个 CSS 属性是同时设置 `flex-direction` 和 `flex-wrap` 属性的简写？

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

将 `flex-direction: column-reverse;` 设置在弹性容器上会产生什么效果？

#### --distractors--

它将项从右向左水平排列。

---

它会反转一行中的项，但保持它们在同一行。

---

它将项从上到下垂直排列。

#### --answer--

它将项按从下到上的反向顺序垂直排列。

### --question--

#### --text--

哪个 CSS 截图能正确地将 flex 项垂直排列成列，并允许它们在溢出容器高度时换行到新列？

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

哪个 `justify-content` 值将所有弹性项对齐到容器主轴的末端？

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

以下 CSS 规则对 flex 项的效果是什么？

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

项按列排列，并且会换行到新建列。

---

项按一行排列，并将在达到一行末尾时换行到新的一行。

---

项按列排列，如果不适合则会溢出。

#### --answer--

项按一行排列，如果不适合则会溢出容器。

### --question--

#### --text--

`justify-content: space-evenly;` 如何对齐 flex 项？

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

项均匀分布，起始和结束处无间距。

---

项被打包在容器的中心。

---

项被推到容器的右侧，且它们之间间距相等。

#### --answer--

项之间以及容器两端有相等的间距。

### --question--

#### --text--

当设置了 `flex-direction: row;` 时，你会使用哪个属性来垂直居中项？

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

`justify-content` 属性的 `flex-start` 值有什么作用？

#### --distractors--

它将项对齐到交叉轴的起点。

---

它将项拉伸以填充容器的主轴。

---

它沿主轴将所有项居中。

#### --answer--

它将项对齐到容器主轴的起点。

### --question--

#### --text--

你如何设置一个 flex 容器，使其项在单行内从右到左排列，并且将它们定位在容器的顶部？

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

以下哪项是 `flex-flow` 属性的有效声明？

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

以下哪个 CSS 规则会导致 flex 项水平居中且垂直拉伸以填充容器高度？

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

当 `flex-direction` 设置为 `column` 时，`align-items: stretch;` 有什么效果？

#### --distractors--

它垂直拉伸项以填充容器的高度。

---

当方向为 `column` 时没有效果。

---

它垂直拉伸项以匹配最高的项。

#### --answer--

它水平拉伸项以填充容器的宽度。

### --question--

#### --text--

如果你想反转水平行中项的顺序，应使用哪个声明？

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

`flex-direction` 为 `row` 的容器中，哪个 `align-items` 的值会将项定位在顶部？

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
