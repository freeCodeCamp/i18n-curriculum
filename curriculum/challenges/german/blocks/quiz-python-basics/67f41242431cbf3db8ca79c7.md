---
id: 67f41242431cbf3db8ca79c7
title: Python-Grundlagen-Quiz
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um zu überprüfen, ob eine Variable einem bestimmten Datentyp entspricht?

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

Welche der folgenden ist KEINE Form der String-Verkettung?

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um die Anzahl der Zeichen in einem String zurückzugeben?

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

Was wird `result` in diesem Beispiel sein?

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

Was passiert, wenn Sie eine Fließkommazahl und eine Ganzzahl addieren?

#### --distractors--

Das Ergebnis wird eine Fehlermeldung sein.

---

Das Ergebnis wird `None` sein.

---

Das Ergebnis wird eine Ganzzahl sein.

#### --answer--

Das Ergebnis wird eine Fließkommazahl sein.

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist der korrekte Weg, eine Funktion zu definieren?

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

Was wird in der Konsole ausgegeben?

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

Welche der folgenden Aussagen ist falsch, wenn es um die Benennung von Variablen geht?

#### --distractors--

Variablennamen dürfen nur mit einem Buchstaben oder einem Unterstrich (_) beginnen, nicht mit einer Zahl.

---

Variablennamen dürfen nur alphanumerische Zeichen (a-z, A-Z, 0-9) und Unterstriche (_) enthalten.

---

Variablennamen dürfen keine der reservierten Schlüsselwörter von Python wie `if`, `class` oder `def` sein.

#### --answer--

Variablennamen müssen eine maximale Länge von 10 Zeichen haben, sonst wirft Python einen Fehler.

### --question--

#### --text--

Welcher der folgenden Datentypen wird in Python NICHT unterstützt?

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

Welche der folgenden Methoden gibt einen neuen String zurück, bei dem alle Zeichen in Großbuchstaben umgewandelt wurden?

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um Eingaben von einem Benutzer zu erhalten?

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

Was wird das Ergebnis des folgenden Codes sein?

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

Was macht die `split()`-Methode?

#### --distractors--

Diese Methode wird verwendet, um ein Tuple in eine Liste von Teilstrings zu zerlegen.

---

Diese Methode wird verwendet, um eine Fließkommazahl in eine Liste von Teilstrings zu zerlegen.

---

Diese Methode wird verwendet, um ein Wörterbuch in eine Liste von Teilstrings zu zerlegen.

#### --answer--

Diese Methode wird verwendet, um einen String in eine Liste von Teilstrings zu zerlegen.

### --question--

#### --text--

Was wird Folgendes in der Konsole ausgeben?

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um eine Tabelle von 1-zu-1-Zeichenzuordnungen für die Übersetzung zu erstellen?

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

Was wird das Ergebnis des folgenden Codes sein?

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

Was wird von der `find()`-Methode zurückgegeben, wenn keine Vorkommen eines Teilstrings in einem String gefunden werden?

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um zu zählen, wie oft ein Teilstring in einem String vorkommt?

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

Was macht die Ganzzahldivision in Python?

#### --distractors--

Dieser Operator wird verwendet, um zwei Zahlen zu multiplizieren und das Ergebnis auf die nächste ganze Zahl aufzurunden.

---

Dieser Operator wird verwendet, um eine Fließkommazahl in eine Ganzzahl umzuwandeln.

---

Dieser Operator wird verwendet, um eine Zahl mit einer anderen potenzieren.

#### --answer--

Dieser Operator wird verwendet, um zwei Zahlen zu dividieren und das Ergebnis auf die nächste ganze Zahl abzurunden.

### --question--

#### --text--

Welche der folgenden Funktionen wird verwendet, um eine Zahl auf die nächste ganze Zahl zu runden?

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`

