---
id: 672990ecf71a852804ababe7
title: Qual è la differenza tra HTML presentazionale e semantico?
challengeType: 19
dashedName: what-is-the-difference-between-presentational-and-semantic-html
---

# --interactive--

L'HTML presentazionale si concentra sull'aspetto e sullo stile del contenuto. Nei primi tempi dell'HTML, gli sviluppatori usavano elementi come `center`, `big` e `font`. Ma nello sviluppo web moderno non dovresti usare questo tipo di elementi, a causa delle loro limitazioni e del loro impatto negativo sull'accessibilità e sulla manutenibilità.

Molti elementi HTML presentazionali sono deprecati, cioè obsoleti e non più consigliati. Esistono modi migliori per ottenere gli stessi risultati. Tuttavia, è utile sapere che esistono, quindi vedremo alcuni esempi.

L'elemento `font` è un elemento deprecato usato per definire la dimensione e il colore del carattere tipografico del testo. Ecco un esempio di un elemento `font`.

Attiva l'editor interattivo e cambia la dimensione da 5 a 7 per vedere il carattere aumentare.

:::interactive_editor

```html
<font size="5" color="blue">This text is blue and large.</font>
```

:::

Questo esempio imposta il colore del testo su `blue` e la dimensione su un valore di `5`. L'intervallo per l'attributo `size` va da `1` a `7`, con `1` come valore più piccolo e `7` come valore più grande. Il valore predefinito è `3`, se non imposti esplicitamente il valore.

Anche se questo elemento funziona ancora, non dovresti usarlo perché la dimensione e il colore del carattere tipografico dovrebbero sempre essere definiti con CSS, non in HTML.

L'elemento `center` è un altro elemento deprecato usato per centrare orizzontalmente il contenuto all'interno del suo contenitore. Ecco un esempio dell'elemento `center` che contiene testo e un elemento paragrafo.

Attiva l'editor interattivo e aggiungi i tag `center` intorno a `<p>Another example text.</p>` per vederlo centrato nella pagina.

:::interactive_editor

```html
<center>
  This text is centered.
  <p>HTML is awesome.</p>
</center>

<p>Another example text.</p>
```

:::

E poi abbiamo l'elemento `big`. Questo è un altro elemento HTML presentazionale deprecato che rende il testo racchiuso un livello più grande rispetto al testo circostante. Qui abbiamo un esempio che definisce un paragrafo con due parti.

Attiva l'editor interattivo e aggiungi i tag `big` intorno a `Some other text` e osserva i cambiamenti nella finestra di anteprima.

:::interactive_editor

```html
<p>
  This text has a normal font size.
  <big>This text is larger.</big>
  Some other text.
</p>
```

:::

Ricorda che la dimensione del carattere tipografico dovrebbe sempre essere definita con CSS, quindi non dovresti usare questo elemento nell'HTML moderno.

Questi erano esempi di elementi HTML presentazionali. Ma tutti sono deprecati e non più consigliati. Quindi cosa dovresti usare invece? Vediamolo.

L'HTML semantico è ora la pratica consigliata. Descrive il contenuto degli elementi, quindi è molto più facile da leggere, comprendere e mantenere.

I motori di ricerca possono facilmente comprendere il tuo sito web quando usi HTML semantico. È anche utile per l'accessibilità, perché i lettori di schermo hanno bisogno di informazioni semantiche per descrivere cosa c'è nella pagina web.

Esempi di elementi HTML semantici includono:

- L'elemento `header` per definire l'intestazione del documento o della sezione.
- L'elemento sezione di navigazione, `nav`, per sezioni con collegamenti di navigazione.
- L'elemento `section` per raggruppare informazioni correlate.
- L'elemento `figure` per illustrazioni e diagrammi.

Questo è un esempio di un elemento `header` che contiene un elemento sezione di navigazione.

Attiva l'editor interattivo e aggiungi un `<a href="#">Products</a>` all'interno di `nav` e osserva i cambiamenti nella finestra di anteprima.

:::interactive_editor

```html
<header>
  <nav>
    <a href="#">Home</a>
    <a href="#">About</a>
    <!--Add the products link here-->

    <a href="#">Contact</a>
  </nav>
</header>
```

:::

Gli elementi semantici mostrano chiaramente il loro scopo all'interno della struttura HTML. Esistono molti diversi elementi HTML semantici. Troverai sicuramente uno che si adatta alle esigenze del tuo progetto.

Ottimo lavoro. Ora sai qual è la differenza tra HTML presentazionale e semantico: l'HTML semantico descrive il contenuto, mentre l'HTML presentazionale si concentra sull'aspetto.

# --questions--

## --text--

Quale delle seguenti descrive meglio la differenza tra HTML presentazionale e semantico?

## --answers--

L'HTML presentazionale si concentra sulla struttura del contenuto, mentre l'HTML semantico si concentra sull'aspetto.

### --feedback--

Pensa a cosa significano "presentazione" e "semantica" e a come si collegano agli elementi HTML.

---

L'HTML semantico si concentra sulla struttura del contenuto, mentre l'HTML presentazionale si concentra sull'aspetto.

---

Non c'è differenza tra HTML presentazionale e semantico.

### --feedback--

Pensa a cosa significano "presentazione" e "semantica" e a come si collegano agli elementi HTML.

---

Entrambi si concentrano sullo stile, ma l'HTML presentazionale è più moderno.

### --feedback--

Pensa a cosa significano "presentazione" e "semantica" e a come si collegano agli elementi HTML.

## --video-solution--

2

## --text--

Quale dei seguenti è un esempio di HTML presentazionale?

## --answers--

Usare l'elemento `header` per definire un'intestazione.

### --feedback--

Pensa a quale elemento controlla direttamente come apparirà il contenuto.

---

Usare l'elemento `nav` per definire una sezione di navigazione.

### --feedback--

Pensa a quale elemento controlla direttamente come apparirà il contenuto.

---

Usare l'elemento `article` per definire contenuto indipendente.

### --feedback--

Pensa a quale elemento controlla direttamente come apparirà il contenuto.

---

Usare l'elemento `center` per centrare il contenuto.

## --video-solution--

4

## --text--

Quale dei seguenti è un esempio di HTML semantico?

## --answers--

Usare `<font color="red">` per rendere il testo rosso.

### --feedback--

Pensa a quale elemento descrive il significato del contenuto.

---

Usare l'elemento `center` per centrare il contenuto.

### --feedback--

Pensa a quale elemento descrive il significato del contenuto.

---

Usare l'elemento `nav` per rappresentare una sezione di navigazione

---

Usare l'elemento `big` per rendere il testo più grande.

### --feedback--

Pensa a quale elemento descrive il significato del contenuto.

## --video-solution--

3
