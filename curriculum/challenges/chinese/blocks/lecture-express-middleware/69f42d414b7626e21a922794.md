---
id: 69f42d414b7626e21a922794
title: 什么是错误处理中间件？
challengeType: 19
dashedName: what-is-error-handling-middleware
---

# --description--

Express 中的错误处理中间件是一种特殊类型的中间件，用于处理请求-响应周期中发生的错误。它对于捕获意外错误、记录错误以及向客户提供适当的响应至关重要。

正如你在之前关于错误处理的课程中看到的，Express 的错误处理中间件有四个参数：`err`、`req`、`res` 和 `next`。当错误被传递给 `next()` 函数时，会调用这个中间件。

这里再次提醒一下它的语法：

```js
app.use((err, req, res, next) => {
  console.error(err.message);
  res.status(500).send("Internal Server Error");
});
```

下面是在一个基本 Express 应用中它的样子：

```js
const express = require('express')
const app = express()

// A simple route that throws an error
app.get('/', (req, res) => {
  throw new Error('Something went wrong!')
})

// Error-handling middleware
app.use((err, req, res, next) => {
  console.error(err.stack); // Logs the error stack
  res.status(500).send('Something went wrong!')
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

* 路由处理器：路由处理器故意抛出一个错误来模拟问题。
* 错误处理中间件：这个中间件通过 `app.use()` 注册。它捕获通过 `next(err)` 传递的错误，并向客户发送带有通用错误信息的 500 HTTP 响应。
* `err`：这是传递给中间件的错误对象。

错误处理中间件确保当你的应用出现问题时，用户仍然能收到合适的响应。务必将此中间件放在中间件栈的末尾，以捕获所有错误。

# --questions--

## --text--

错误处理中间件的作用是什么？

## --answers--

它处理请求并发送响应。

### --feedback--

关注请求过程中出现问题时会发生什么。

---

它处理请求-响应周期中发生的错误。

---

它提供静态文件服务。

### --feedback--

关注请求过程中出现问题时会发生什么。

---

它处理路由逻辑。

### --feedback--

关注请求过程中出现问题时会发生什么。

## --video-solution--

2

## --text--

错误处理中间件需要哪些参数？

## --answers--

`err`、`req`、`res`、`next`。

---

`req` 和 `res`。

### --feedback--

想想 Express 用哪些参数来识别错误处理器。

---

`err` 和 `next`。

### --feedback--

想想 Express 用哪些参数来识别错误处理器。

---

`err` 和 `req`。

### --feedback--

想想 Express 用哪些参数来识别错误处理器。

## --video-solution--

1

## --text--

错误处理中间件应该放在中间件栈的什么位置？

## --answers--

放在顶部，其他中间件之前。

### --feedback--

想想错误处理器通常放在可能出错的代码中什么位置。

---

放在中间件栈的中间。

### --feedback--

想想错误处理器通常放在可能出错的代码中什么位置。

---

放在底部，其他中间件之后。

---

没有特定的位置要求。

### --feedback--

想想错误处理器通常放在可能出错的代码中什么位置。

## --video-solution--

3
