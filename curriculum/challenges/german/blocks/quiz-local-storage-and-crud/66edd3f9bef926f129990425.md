---
id: 66edd3f9bef926f129990425
title: Local Storage und CRUD-Quiz
challengeType: 8
dashedName: quiz-local-storage-and-crud
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Wofür steht CRUD im Kontext der Webentwicklung?

#### --distractors--

Create, Replace, Undo, Delete.

---

Connect, Remove, Update, Display.

---

Copy, Retrieve, Update, Delete.

#### --answer--

Create, Read, Update, Delete.

### --question--

#### --text--

Welche HTTP-Methode wird üblicherweise mit der Create-Operation in einer RESTful-API assoziiert?

#### --distractors--

`GET`

---

`PUT`

---

`DELETE`

#### --answer--

`POST`

### --question--

#### --text--

Welche der folgenden CRUD-Operationen wird typischerweise ausgeführt, wenn ein bestehender Datensatz in einer Datenbank geändert wird?

#### --distractors--

Read

---

Delete

---

Create

#### --answer--

Update

### --question--

#### --text--

Was ist der Hauptunterschied zwischen localStorage und sessionStorage in der Web Storage API?

#### --distractors--

localStorage speichert Daten im Speicher, während sessionStorage Daten auf der Festplatte speichert.

---

sessionStorage ist schneller als localStorage beim Abrufen von Daten.

---

localStorage-Daten sind nur während einer Sitzung verfügbar, während sessionStorage über Sitzungen hinweg bestehen bleibt.

#### --answer--

localStorage-Daten bleiben erhalten, bis sie explizit gelöscht werden, während sessionStorage-Daten gelöscht werden, wenn die Sitzung der Seite endet.

### --question--

#### --text--

Welche Methode wird verwendet, um Daten in localStorage zu speichern?

#### --distractors--

`localStorage.setData()`

---

`localStorage.saveItem()`

---

`localStorage.storeData()`

#### --answer--

`localStorage.setItem()`

### --question--

#### --text--

Wie können Sie ein einzelnes Element aus sessionStorage entfernen?

#### --distractors--

`sessionStorage.clearItem('key')`

---

`sessionStorage.remove('key')`

---

`sessionStorage.deleteItem('key')`

#### --answer--

`sessionStorage.removeItem('key')`

### --question--

#### --text--

Was ist der Unterschied zwischen `PUT` und `PATCH`?

#### --distractors--

`PUT` ändert spezifische Felder, während `PATCH` die gesamte Ressource ersetzt.

---

`PUT` fügt nur neue Ressourcen hinzu, während `PATCH` bestehende aktualisiert.

---

`PUT` wird zum Abrufen von Daten verwendet, während `PATCH` zum Senden von Daten genutzt wird.

#### --answer--

`PUT` ersetzt die gesamte Ressource, während `PATCH` spezifische Felder aktualisiert.

### --question--

#### --text--

Wofür wird der Cache-Speicher verwendet?

#### --distractors--

Der Cache-Speicher wird verwendet, um Benutzeranmeldeinformationen dauerhaft sicher im Browser für Authentifizierungszwecke zu speichern.

---

Der Cache-Speicher wird verwendet, um große Mengen strukturierter Daten im Browser mit einem Datenbanksystem zu speichern.

---

Der Cache-Speicher wird verwendet, um Benutzersitzungen und Präferenzen zu verfolgen, indem Schlüssel-Wert-Paare gespeichert werden, die über mehrere Websites zugänglich sind.

#### --answer--

Der Cache-Speicher wird verwendet, um häufig angeforderte Netzwerkressourcen zu speichern, wodurch Ladezeiten verkürzt und die Leistung verbessert wird.

### --question--

#### --text--

Was ist ein Cookie im Kontext der Webentwicklung?

#### --distractors--

Eine Art HTTP-Anfrage.

---

Ein Stück Code, das im Browser ausgeführt wird.

---

Eine Methode, um große Dateien auf einem Server zu speichern.

#### --answer--

Ein kleines Datenstück, das vom Webbrowser des Benutzers gespeichert wird.

### --question--

#### --text--

Welche der folgenden Methoden wird verwendet, um manuell ein Cookie in JavaScript zu setzen?

#### --distractors--

`document.setCookie`

---

`document.cookie.set`

---

`document.createCookie`

#### --answer--

`document.cookie`

### --question--

#### --text--

Welcher der folgenden Code-Ausschnitte löscht korrekt ein Cookie mit dem Namen `username`?

#### --distractors--

`document.cookie.delete("username");`

---

`document.removeCookie("username");`

---

`cookie.delete("username");`

#### --answer--

`document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";`

### --question--

#### --text--

Welche der folgenden Aussagen über Cookies ist falsch?

#### --distractors--

Session-Cookies verfallen, wenn der Browser geschlossen wird.

---

HttpOnly-Cookies können von JavaScript nicht aufgerufen werden.

---

Sichere Cookies werden nur über HTTPS übertragen.

#### --answer--

Cookies können nur vom Server gesetzt werden.

### --question--

#### --text--

Was ist Browser-Fingerprinting?

#### --distractors--

Eine Technik zur Identifizierung von Benutzern basierend auf ihrer IP-Adresse.

---

Eine Form der Datenverschlüsselung.

---

Eine Methode, um Daten im Browser-Cache zu speichern.

#### --answer--

Eine Methode, Benutzer durch Browser- und Geräteattribute zu verfolgen.

### --question--

#### --text--

Was ist ein Hauptproblem beim Speichern von Passwörtern im localStorage?

#### --distractors--

localStorage-Daten werden gelöscht, sobald Sie den Browser-Tab schließen.

---

localStorage-Daten gehen verloren, sobald Sie Cookies löschen.

---

localStorage funktioniert nicht offline.

#### --answer--

localStorage ist nicht verschlüsselt und kann leicht aufgerufen werden.

### --question--

#### --text--

Welche der folgenden Aussagen trifft auf die Speicherung auf der Client-Seite zu?

#### --distractors--

Die Speicherung auf der Client-Seite verwendet Objekte statt Strings zur Datenspeicherung.

---

Die Speicherung auf der Client-Seite verwendet Cloud-Dienste, um Benutzerdaten über mehrere Geräte zu synchronisieren.

---

Die Speicherung auf der Client-Seite ist sicherer als die Speicherung auf der Server-Seite.

#### --answer--

Die Speicherung auf der Client-Seite ermöglicht es Websites, Daten auf dem Gerät eines Benutzers zu speichern.

### --question--

#### --text--

Was ist der Hauptvorteil von IndexedDB gegenüber localStorage?

#### --distractors--

IndexedDB benötigt keine Zustimmung des Benutzers zur Datenspeicherung.

---

IndexedDB hat eine einfachere API.

---

IndexedDB wird in allen Webbrowsern unterstützt.

#### --answer--

IndexedDB kann größere Datenmengen und komplexere Abfragen verarbeiten.

### --question--

#### --text--

Welche der folgenden Möglichkeiten ist gültig, um Daten aus einem Objekt-Store in IndexedDB abzurufen?

#### --distractors--

Aufrufen von `retrieve()` auf dem Objekt-Store.

---

Verwenden von `query()`, um spezifische Datensätze zuzugreifen.

---

Verwenden der `fetch()`-Methode.

#### --answer--

Verwenden von `get()` auf der Store-Referenz.

### --question--

#### --text--

Welche Art von Transaktionen unterstützt IndexedDB?

#### --distractors--

Nur Lese-Transaktionen.

---

Nur asynchrone Transaktionen.

---

Nur synchrone Transaktionen.

#### --answer--

Lese- und Schreibtransaktionen.

### --question--

#### --text--

Welche der folgenden Aussagen trifft auf den Lebenszyklus eines Service Workers zu?

#### --distractors--

Service Worker laufen im Haupt-Thread der Webanwendung.

---

Service Worker können aktiviert werden, bevor sie installiert sind.

---

Service Worker können nur laufen, wenn die Webanwendung im Browser geöffnet ist.

#### --answer--

Ein Service Worker kann Netzwerk-Anfragen abfangen, nachdem er aktiviert wurde.

### --question--

#### --text--

Welche Aussage beschreibt am besten eine Hauptfunktion von Progressive Web Apps (PWAs)?

#### --distractors--

PWAs können nur auf mobilen Geräten installiert werden.

---

PWAs benötigen zwingend einen Back-End-Server.

---

PWAs sind nur über bestimmte Browser zugänglich.

#### --answer--

PWAs bieten Offline-Funktionalität durch Service Worker.

