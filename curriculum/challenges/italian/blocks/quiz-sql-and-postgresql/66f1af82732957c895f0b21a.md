---
id: 66f1af82732957c895f0b21a
title: Quiz su SQL e PostgreSQL
challengeType: 8
dashedName: quiz-sql-and-postgresql
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos'è un database relazionale?

#### --distractors--

Un database strutturato per riconoscere la non-relazione tra gli elementi archiviati.

---

Un database che archivia dati non strutturati.

---

Un sistema basato su file piatti.

#### --answer--

Un database strutturato per riconoscere e gestire le relazioni tra i dati nelle tabelle.

### --question--

#### --text--

In cosa differisce un database non relazionale da un database relazionale?

#### --distractors--

I database non relazionali usano tabelle, mentre i database relazionali no.

---

I database relazionali possono archiviare dati non strutturati.

---

I database non relazionali sono più lenti dei database relazionali.

#### --answer--

I database non relazionali possono archiviare dati non strutturati.

### --question--

#### --text--

Cosa fa il comando `\l` quando viene eseguito nel terminale interattivo di PostgreSQL (`psql`)?

#### --distractors--

Elenca tutti gli schemi.

---

Elenca tutte le tabelle.

---

Si connette a un database.

#### --answer--

Elenca tutti i database.

### --question--

#### --text--

Quale dei seguenti comandi crea una nuova tabella in SQL?

#### --distractors--

`ADD TABLE`

---

`NEW TABLE`

---

`ALTER TABLE`

#### --answer--

`CREATE TABLE`

### --question--

#### --text--

Qual è lo scopo di una chiave esterna in un database relazionale?

#### --distractors--

Permettere la duplicazione dei dati.

---

Identificare univocamente una riga nella tabella.

---

Archiviare grandi quantità di dati testuali.

#### --answer--

Creare una relazione tra due tabelle.

### --question--

#### --text--

Quale comando aggiunge una nuova colonna a una tabella esistente?

#### --distractors--

`ADD COLUMN column_name TO table_name data_type;`

---

`INSERT COLUMN column_name INTO table_name data_type;`

---

`MODIFY TABLE table_name ADD COLUMN column_name data_type;`

#### --answer--

`ALTER TABLE table_name ADD COLUMN column_name data_type;`

### --question--

#### --text--

Che tipo di relazione esiste quando un record nella Tabella A corrisponde a uno o più record nella Tabella B?

#### --distractors--

Uno a uno (1-1)

---

Molti a molti (m-m)

---

Molti a uno (m-1)

#### --answer--

Uno a molti (1-m)

### --question--

#### --text--

Cos'è una query?

#### --distractors--

Una richiesta per rimuovere tutti i dati dal database.

---

Una riga speciale per valori unici in un database.

---

Una colonna speciale per valori unici in un database.

#### --answer--

Una richiesta di dati specifici dal database.

### --question--

#### --text--

Come si elimina una colonna da una tabella in SQL?

#### --distractors--

`REMOVE COLUMN column_name FROM table_name;`

---

`DELETE COLUMN column_name FROM table_name;`

---

`MODIFY TABLE table_name DELETE column_name;`

#### --answer--

`ALTER TABLE table_name DROP COLUMN column_name;`

### --question--

#### --text--

Qual è la funzione del vincolo `PRIMARY KEY` in SQL?

#### --distractors--

Collegare due tabelle insieme.

---

Definire il valore predefinito per una colonna.

---

Permettere valori nulli nella colonna.

#### --answer--

Garantire che la colonna contenga valori unici.

### --question--

#### --text--

Cosa fa il comando `\c` nel terminale interattivo di PostgreSQL (`psql`)?

#### --distractors--

Elenca tutte le tabelle nel database corrente.

---

Mostra i dettagli della connessione al database corrente.

---

Esce dalla shell `psql`.

#### --answer--

Si connette al database specificato.

### --question--

#### --text--

Cos'è una chiave composta in un database relazionale?

#### --distractors--

Una chiave derivata da un'altra chiave.

---

Una chiave che cambia frequentemente in base agli aggiornamenti dei dati.

---

Una chiave usata esclusivamente per le relazioni di chiave esterna.

#### --answer--

Una chiave composta da più colonne per identificare un record in modo univoco.

### --question--

#### --text--

Quale comando inserisce dati in una tabella?

#### --distractors--

`PUT VALUES`

---

`UPDATE INTO`

---

`ADD DATA`

#### --answer--

`INSERT INTO`

### --question--

#### --text--

Come si rinomina una colonna in SQL?

#### --distractors--

`MODIFY COLUMN`

---

`CHANGE COLUMN`

---

`ALTER COLUMN`

#### --answer--

`RENAME COLUMN`

### --question--

#### --text--

Quale query elimina tutti i record dalla tabella employees?

#### --distractors--

`REMOVE ALL`

---

`TRUNCATE employees`

---

`DROP RECORDS`

#### --answer--

`DELETE FROM employees`

### --question--

#### --text--

Quale vincolo SQL garantisce specificamente che una colonna possa contenere solo valori unici, permettendo valori nulli?

#### --distractors--

`NOT NULL`

---

`FOREIGN KEY`

---

`PRIMARY KEY`

#### --answer--

`UNIQUE`

### --question--

#### --text--

Quale dei seguenti NON è un esempio di database relazionale comunemente usato nell'industria?

#### --distractors--

MySQL

---

SQLite

---

PostgreSQL

#### --answer--

SQLQuery

### --question--

#### --text--

Cosa rappresenta una relazione uno a uno (1-1) in un database relazionale?

#### --distractors--

Ogni record nella Tabella A corrisponde a uno o più record nella Tabella B.

---

Ogni record nella Tabella A non corrisponde a nessun record nella Tabella B.

---

Ogni record nella Tabella A corrisponde a più record nella Tabella B e viceversa.

#### --answer--

Ogni record nella Tabella A corrisponde esattamente a un record nella Tabella B.

### --question--

#### --text--

Quale comando elenca tutte le tabelle nel database PostgreSQL corrente?

#### --distractors--

`\l`

---

`SHOW TABLES`

---

`\t`

#### --answer--

`\d`

### --question--

#### --text--

Quale delle seguenti opzioni permette di unire una tabella con se stessa?

#### --distractors--

`LEFT OUTER JOIN`

---

`CROSS JOIN`

---

`RIGHT OUTER JOIN`

#### --answer--

`SELF JOIN`
