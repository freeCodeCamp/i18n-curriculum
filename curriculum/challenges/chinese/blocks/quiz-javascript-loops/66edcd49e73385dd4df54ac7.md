---
id: 66edcd49e73385dd4df54ac7
title: JavaScript 循环测验
challengeType: 8
dashedName: quiz-javascript-loops
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项最能描述迭代？

#### --distractors--

这是反复编写代码直到其正确工作的进程。

---

这是一种将数据存储在连续内存位置的技术。

---

这是在组件之间发送信号以建立连接的进程。

#### --answer--

这是重复执行一组指令多次的进程。

### --question--

#### --text--

编程中用于迭代的技术是什么？

#### --distractors--

递归

---

条件式

---

编译

#### --answer--

循环

### --question--

#### --text--

以下哪一个是可迭代的对象？

#### --distractors--

未定义

---

数字

---

布尔

#### --answer--

字串

### --question--

#### --text--

以下哪项不是 JavaScript 中支持的循环类型？

#### --distractors--

`for` 循环。

---

`while` 循环。

---

`for...in` 循环。

#### --answer--

`if...else` 循环。

### --question--

#### --text--

`for` 循环声明的正确顺序是什么？

#### --distractors--

```javascript
for (condition; increment/decrement; initialization) {
  statement;
}
```

---

```javascript
for (increment/decrement; condition; initialization) {
  statement;
}
```

---

```javascript
for (initialization; increment/decrement; condition) {
  statement;
}
```

#### --answer--

```javascript
for (initialization; condition; increment/decrement) {
  statement;
}
```

### --question--

#### --text--

哪个循环会先执行一次代码块，然后检查条件是否为真，并且只要条件保持为真就会继续执行？

#### --distractors--

`while` 循环。

---

`for...of` 循环。

---

`for...in` 循环。

#### --answer--

`do...while` 循环。

### --question--

#### --text--

以下哪项将对可迭代对象的值进行循环？

#### --distractors--

`for...in` 循环。

---

`for` 循环。

---

`do...while` 循环。

#### --answer--

`for...of` 循环。

### --question--

#### --text--

以下哪项最能定义无限循环？

#### --distractors--

一个循环，直到条件变为假时停止运行。

---

一个运行一次然后终止的循环。

---

一个在固定迭代次数后自动停止的循环。

#### --answer--

一个其终止条件永远不满足或缺失的循环。

### --question--

#### --text--

以下 `loop` 会运行多少次？  

```js
for (let i = 2; i < 10; i+=2) {  
  console.log(i);  
}
```

#### --distractors--

5

---

9

---

10

#### --answer--

4

### --question--

#### --text--

哪个循环最适合遍历对象？

#### --distractors--

`for...of` 循环。

---

`do...while` 循环。

---

`for` 循环。

#### --answer--

`for...in` 循环。

### --question--

#### --text--

`for...in` 循环和 `for...of` 循环有什么区别？

#### --distractors--

`for...in` 循环迭代属性值，而 `for...of` 循环迭代属性名称（密钥）。

---

`for...in` 循环创建新属性，而 `for...of` 循环修改现有属性。

---

`for...in` 循环只能用于字串，而 `for...of` 循环既可以用于字串也可以用于数字。

#### --answer--

`for...in` 循环迭代属性名（密钥），而 `for...of` 循环迭代属性值。

### --question--

#### --text--

以下哪项会导致无限循环？

#### --distractors--

变量的初始化。

---

最终返回 false 的循环条件。

---

递增/递减逻辑。

#### --answer--

一个始终 `return` true 的循环条件。

### --question--

#### --text--

什么语句可以用来完全跳出一个循环？

#### --distractors--

`continue` 语句。

---

`label` 语句。

---

`skip` 语句。

#### --answer--

`break` 语句。

### --question--

#### --text--

哪个语句可以用来跳过当前迭代并进入下一次迭代？

#### --distractors--

`break` 语句。

---

`label` 语句。

---

`return` 语句。

#### --answer--

`continue` 语句。

### --question--

#### --text--

以下代码在控制台打印什么？

```js
for (let i = 0; i < 25; i += 2) {
  if (i % 5 === 0) {
    continue;
  }
  if (i % 13 === 0) {
    break;
  }
  console.log(i);
}
```

#### --distractors--

`0`、`2`、`4`、`6`、`8`、`12`、`14`、`16`、`18`、`22`、`24`

---

`0`、`2`、`4`、`6`、`8`、`12`

---

`2`、`4`、`6`、`8`、`12`

#### --answer--

`2`、`4`、`6`、`8`、`12`、`14`、`16`、`18`、`22`、`24`

### --question--

#### --text--

以下 `code` 会打印到控制台什么内容？

```javascript
for (let i = 1; i < 6; i++) {
  if (i === 4) break;
  console.log(i);
}
```

#### --distractors--

`1`、`2`、`3`、`4`、`5`、`6`

---

`1`、`2`、`3`、`4`、`5`

---

`1`、`2`、`3`、`4`

#### --answer--

`1`、`2`、`3`

### --question--

#### --text--

下面的代码将打印到控制台什么？

```javascript
const shoppingList = { tomatoes: 4, apples: 10 };
for (const item in shoppingList) {
  console.log(item);
}
```

#### --distractors--

`tomatoes - 4`、`apples - 10`

---

`4`、`10`

---

`tomatoes:4`、`apples:10`

#### --answer--

`tomatoes`、`apples`

### --question--

#### --text--

下面代码的控制台输出是什么？

```javascript
for (let i = 2; i <= 13; i++) {
  if (i % 2 === 0) continue;
  console.log(i);
}
```

#### --distractors--

`2`、`4`、`6`、`8`、`10`、`12`

---

`1`、`3`、`5`、`7`、`9`、`11`、`13`

---

`1`、`3`、`5`、`7`、`9`、`11`

#### --answer--

`3`、`5`、`7`、`9`、`11`、`13`

### --question--

#### --text--

下面的代码会在控制台打印什么？

```javascript
const fruits = ["Mango", "Pineapple", "Oranges"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

#### --distractors--

`0`、`1`、`2`

---

`Pineapple`、`Oranges` 和 `Mango`

---

`Oranges`、`Pineapple` 和 `Mango`

#### --answer--

`Mango`、`Pineapple` 和 `Oranges`

### --question--

#### --text--

下面的代码将输出字串 `in the loop` 多少次？

```javascript
let x = 0
while (x < 5) {
  console.log("in the loop");
}
```

#### --distractors--

3 次

---

4 次

---

5 次

#### --answer--

无限次

