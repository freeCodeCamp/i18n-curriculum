---
id: 66edcd875b0d91de1fbbb492
title: JavaScript 기초 퀴즈
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

다음 중 String 객체를 생성하는 것은 무엇인가요?

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

String 객체를 문자열 리터럴로 변환하는 방법은 무엇인가요?

#### --distractors--

`String()` 생성자를 사용합니다.

---

`.stringify()` 메서드를 사용합니다.

---

`String` 객체를 큰따옴표로 감쌉니다.

#### --answer--

`.toString()` 메서드를 사용합니다.

### --question--

#### --text--

JavaScript에서 문자열 원시값에 `.length`, `.repeat()`, `.concat()`, `.slice()` 같은 메서드를 사용할 수 있는 이유는 무엇인가요?

#### --distractors--

그 메서드들은 문자열 원시값에 기본적으로 존재합니다.

---

문자열 원시값이 내부적으로 객체로 저장됩니다.

---

JavaScript가 문자열 원시값을 배열로 일시 변환합니다.

#### --answer--

JavaScript가 문자열 원시값을 일시적으로 String 객체로 감싸기 때문입니다.

### --question--

#### --text--

다음 코드는 콘솔에 무엇을 출력할까요?

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

다음 중 문자열 `Number`에서 `"123"` 객체를 반환하는 것은 무엇인가요?

#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--

`Number(true)`와 `Number(false)`는 각각 무엇을 반환할까요?

#### --distractors--

`undefined`와 `undefined`

---

`NaN`와 `NaN`

---

`true`와 `false`

#### --answer--

`1`와 `0`

### --question--

#### --text--

`Number(undefined)`와 `Number(null)`는 각각 무엇을 반환할까요?

#### --distractors--

`NaN`와 `NaN`

---

`0`와 `0`

---

`undefined`와 `null`

#### --answer--

`NaN`와 `0`

### --question--

#### --text--

웹사이트에서 사용자가 어떤 작업을 할 수 있는지 확인하는 불리언 변수에 가장 적합한 이름은 무엇인가요?

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

사용자가 폼을 제출할 때 실행되는 함수에 가장 적합한 이름은 무엇인가요?

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

희소 배열(sparse array)이란 무엇인가요?

#### --distractors--

하나 이상의 인덱스가 `null` 값을 가진 배열입니다.

---

하나 이상의 인덱스가 `undefined` 값을 가진 배열입니다.

---

하나 이상의 인덱스가 빈 객체 리터럴 값을 가진 배열입니다.

#### --answer--

하나 이상의 인덱스에 값이 할당되지 않은 배열입니다.

### --question--

#### --text--

다음 예제 중 `arr`이 희소 배열이 아닌 경우는 어느 것인가요?

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

린터(linter)의 주요 목적은 무엇인가요?

#### --distractors--

코드를 자동으로 포맷팅하는 것입니다.

---

코드 성능을 최적화하는 것입니다.

---

코드 문서를 생성하는 것입니다.

#### --answer--

코드 내 잠재적 오류와 문제를 감지하는 것입니다.

### --question--

#### --text--

코드베이스에서 포매터(formatter)를 사용하는 이점은 무엇인가요?

#### --distractors--

개발자가 코드를 더 효율적으로 컴파일할 수 있게 합니다.

---

코드를 특정 프로그래밍 언어로 변환합니다.

---

논리 오류를 감지합니다.

#### --answer--

일관된 코드 스타일을 보장합니다.

### --question--

#### --text--

JavaScript에서 변수에 메모리를 할당하는 주체는 보통 누구 또는 무엇인가요?

#### --distractors--

개발자입니다.

---

운영체제입니다.

---

서버입니다.

#### --answer--

런타임 엔진입니다.

### --question--

#### --text--

다음 코드가 콘솔에 출력하는 것은 무엇인가요?

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

JavaScript에서 호이스팅(hoisting)이란 무엇인가요?

#### --distractors--

오류를 코드 상단으로 올리는 것입니다.

---

새 변수를 생성하는 과정입니다.

---

`var` 선언을 블록 상단으로 자동 이동하는 것입니다.

#### --answer--

변수 선언을 자신의 범위 상단으로 이동하는 과정입니다.

### --question--

#### --text--

현대 JavaScript에서 `var` 키워드 사용이 나쁜 습관으로 여겨지는 이유는 무엇인가요?

#### --distractors--

`let`와 `const`보다 느리기 때문입니다.

---

구문 오류를 발생시키기 때문입니다.

---

타입 검사를 허용하지 않기 때문입니다.

#### --answer--

블록 외부에서도 접근 가능하기 때문입니다.

### --question--

#### --text--

JavaScript에서 모듈의 목적은 무엇인가요?

#### --distractors--

코드 성능을 향상시키기 위해서입니다.

---

코드를 더 이해하기 어렵게 만들기 위해서입니다.

---

새 프로그래밍 언어를 만들기 위해서입니다.

#### --answer--

코드를 재사용 가능한 단위로 조직하기 위해서입니다.

### --question--

#### --text--

HTML 문서에서 `app.js` 스크립트를 모듈로 추가하는 방법은 무엇인가요?

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

다음 예제 중 `calculateSum` 모듈에서 `utils.js` 함수를 내보내고 `app.js` 파일에서 올바르게 가져와 사용하는 것은 무엇인가요?

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
