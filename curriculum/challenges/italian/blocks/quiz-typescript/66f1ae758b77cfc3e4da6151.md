---
id: 66f1ae758b77cfc3e4da6151
title: Quiz su TypeScript
challengeType: 8
dashedName: quiz-typescript
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Cos’è TypeScript?

#### --distractors--

Un framework usato per la programmazione di sistema.

---

Uno strumento per la gestione di database.

---

Un linguaggio di programmazione usato per lo stile dei siti web.

#### --answer--

Un superset di JavaScript che aggiunge il typing statico.

### --question--

#### --text--

Perché si dovrebbe usare TypeScript in un progetto?

#### --distractors--

Elimina la necessità di testare le applicazioni.

---

È più veloce di tutti gli altri linguaggi di programmazione.

---

Rende più facile lo stile dei siti web.

#### --answer--

Migliora la manutenibilità aggiungendo definizioni di tipo a JavaScript.

### --question--

#### --text--

In quale linguaggio viene compilato TypeScript?

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

Quale sarebbe l’output del seguente frammento di codice?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

Qual è lo scopo principale di un’interfaccia in TypeScript?

#### --distractors--

Compilare il codice TypeScript in JavaScript.

---

Creare un nuovo tipo di dato.

---

Creare un nuovo oggetto JavaScript.

#### --answer--

Definire la struttura di un oggetto.

### --question--

#### --text--

Quale dei seguenti NON è un tipo primitivo base in TypeScript?

#### --distractors--

stringa

---

booleano

---

numero

#### --answer--

array

### --question--

#### --text--

Quale delle seguenti definisce correttamente un tipo letterale in TypeScript?

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

Qual è lo scopo principale della parola chiave `type` in TypeScript?

#### --distractors--

Dichiarare variabili.

---

Creare classi.

---

Implementare interfacce.

#### --answer--

Definire tipi personalizzati.

### --question--

#### --text--

Quale delle seguenti definisce correttamente un tipo unione?

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

Quale capacità è unica di `interface` e NON può essere ottenuta usando `type` in TypeScript?

#### --distractors--

Definire forme di oggetti.

---

Creare tipi unione.

---

Definire tipi che includono solo valori specifici.

#### --answer--

Dichiarare implementazioni di classi.

### --question--

#### --text--

Qual è lo scopo principale di usare i generici in TypeScript?

#### --distractors--

Implementare l’ereditarietà tra classi.

---

Definire la struttura di un oggetto.

---

Creare un nuovo tipo di dato.

#### --answer--

Scrivere codice riutilizzabile che può lavorare con tipi multipli.

### --question--

#### --text--

Quale dei seguenti è un **esempio corretto** di una funzione generica in TypeScript?

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

Cos’è il type narrowing in TypeScript?

#### --distractors--

Ridurre la dimensione del JavaScript compilato.

---

Limitare il numero di tipi in un’unione.

---

Rimuovere tipi inutilizzati da un progetto TypeScript.

#### --answer--

Scrivere codice che restringe un tipo unione a un tipo più specifico.

### --question--

#### --text--

Quale file viene tipicamente usato per configurare le opzioni del compilatore TypeScript?

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

Cosa specifica l’opzione `rootDir` nel file `tsconfig.json`?

#### --distractors--

La versione di TypeScript da usare durante la compilazione.

---

L’ambiente di runtime obiettivo (es. Node.js o browser) per il codice TypeScript.

---

Il sistema di moduli specifico da usare per importazioni ed esportazioni.

#### --answer--

La directory che contiene i tuoi file sorgente.

### --question--

#### --text--

Quale esempio usa correttamente `instanceof` per restringere il tipo e accedere in sicurezza alla proprietà `value`?

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
Cosa succede impostando `"strict": true` in `tsconfig.json` in TypeScript?

#### --distractors--

Abilita il controllo statico dei tipi così puoi individuare errori prima di eseguire il codice.

---

Ti obbliga a usare sempre la sintassi ES6.

---

Ti obbliga a compilare il codice prima di eseguirlo.

#### --answer--

Abilita un insieme di opzioni di controllo dei tipi come richiedere la gestione di `null` o `undefined` e prevenire `any` impliciti.

### --question--

#### --text--

Qual è lo scopo del tipo `never` in TypeScript?

#### --distractors--

Rappresentare null o undefined.

---

Rappresentare qualsiasi valore possibile.

---

Rappresentare un tipo che può essere qualsiasi cosa tranne null.

#### --answer--

Rappresentare il tipo di valori che non si verificano mai.

### --question--

#### --text--

Quale delle seguenti chiamate di funzione `getRandomValue` compilerà con successo in TypeScript?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

Come puoi correggere l’errore del compilatore TypeScript `'email' is possibly 'null'` nel seguente codice usando il concatenamento opzionale?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`
