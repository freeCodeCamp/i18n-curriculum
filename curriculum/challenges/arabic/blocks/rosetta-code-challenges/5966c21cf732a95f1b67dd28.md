---
id: 5966c21cf732a95f1b67dd28
title: معالجة التاريخ
challengeType: 1
forumTopicId: 302244
dashedName: date-manipulation
---

# --description--

بالنظر إلى سلسلة نصية تمثل تاريخًا بتوقيت EST، أخرج التاريخ كسلسلة نصية مع إضافة 12 ساعة إلى الوقت. يجب الحفاظ على المنطقة الزمنية.

مثال على المُدخَل: `"March 6 2009 7:30pm EST"`

مثال على المخرَج: `"March 7 2009 7:30am EST"`

# --hints--

يجب أن تكون `add12Hours` دالة.

```js
assert(typeof add12Hours === 'function');
```

يجب أن تُرجع `add12Hours(dateString)` سلسلة نصية.

```js
assert(typeof add12Hours('January 17 2017 11:43am EST') === 'string');
```

يجب أن تُرجع `add12Hours("January 17 2017 11:43am EST")` القيمة `"January 17 2017 11:43pm EST"`

```js
assert(
  add12Hours('January 17 2017 11:43am EST') === 'January 17 2017 11:43pm EST'
);
```

يجب التعامل مع تغيير اليوم. يجب أن تُرجع `add12Hours("March 6 2009 7:30pm EST")` القيمة `"March 7 2009 7:30am EST"`

```js
assert(add12Hours('March 6 2009 7:30pm EST') === 'March 7 2009 7:30am EST');
```

يجب التعامل مع تغيير الشهر في سنوات الكبيسة. يجب أن تُرجع `add12Hours("February 29 2004 9:15pm EST")` القيمة `"March 1 2004 9:15am EST"`

```js
assert(add12Hours('February 29 2004 9:15pm EST') === 'March 1 2004 9:15am EST');
```

يجب التعامل مع تغيير الشهر في السنوات العادية. يجب أن تُرجع `add12Hours("February 28 1999 3:15pm EST")` القيمة `"March 1 1999 3:15am EST"`

```js
assert(add12Hours('February 28 1999 3:15pm EST') === 'March 1 1999 3:15am EST');
```

يجب التعامل مع تغيير السنة. يجب أن تُرجع `add12Hours("December 31 2020 1:45pm EST")` القيمة `"January 1 2021 1:45am EST"`

```js
assert(
  add12Hours('December 31 2020 1:45pm EST') === 'January 1 2021 1:45am EST'
);
```

# --seed--

## --seed-contents--

```js
function add12Hours(dateString) {

  return true;
}
```

# --solutions--

```js
function add12Hours(dateString) {
  const months = ['January', 'February', 'March', 'April', 'May', 'June',
    'July', 'August', 'September', 'October', 'November', 'December'];
  // Get the parts of the string
  const parts = dateString.split(' ');
  const month = months.indexOf(parts[0]);
  const day = parseInt(parts[1], 10);
  const year = parseInt(parts[2], 10);
  const time = parts[3].split(':');
  let hours = parseInt(time[0], 10);
  if (time[1].slice(-2) === 'pm') {
    hours += 12;
  }
  const minutes = parseInt(time[1].slice(0, -2), 10);

  // Create a date with given parts, and updated hours
  const date = new Date();
  date.setFullYear(year, month, day);
  date.setHours(hours + 12);  // Add 12 hours
  date.setMinutes(minutes);

  let hoursOutput = date.getHours();
  let abbreviation = 'am';
  if (hoursOutput > 12) {
    hoursOutput -= 12;
    abbreviation = 'pm';
  }

  return `${months[date.getMonth()]} ${date.getDate()} ${date.getFullYear()} ${hoursOutput}:${date.getMinutes()}${abbreviation} EST`;
}
```
