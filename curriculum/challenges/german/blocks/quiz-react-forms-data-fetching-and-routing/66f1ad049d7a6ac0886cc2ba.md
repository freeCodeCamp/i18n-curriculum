---
id: 66f1ad049d7a6ac0886cc2ba
title: "React-Formulare, Datenabruf und Routing-Quiz"
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welches der folgenden Beispiele ist eine kontrollierte Eingabe?

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

Welches der folgenden Beispiele ist eine unkontrollierte Eingabe?

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

Welches der folgenden ist KEIN häufig verwendeter Weg, um in React Daten abzurufen?

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

Welcher der folgenden Hooks wird häufig mit dem Datenabruf in React in Verbindung gebracht?

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

Was ist Memoisierung?

#### --distractors--

Ein gängiger Algorithmus, um den kürzesten Weg zwischen zwei Knoten in einem Graphen zu finden.

---

Eine spezielle Technik zur Leistungsoptimierung für alle kontrollierten und unkontrollierten Eingaben.

---

Ein Divide-and-Conquer-Algorithmus, der verwendet wird, um Teilarrays rekursiv zu sortieren und zusammenzuführen.

#### --answer--

Eine Optimierungstechnik, bei der das Ergebnis teurer Funktionsaufrufe basierend auf bestimmten Argumenten zwischengespeichert (erinnert) wird.

### --question--

#### --text--

Welcher der folgenden Hooks wird verwendet, um berechnete Werte zu memoizieren?

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

Welches der folgenden Beispiele verwendet den korrekten Hook, um Funktionsreferenzen zu memoizieren?

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

Was ist eine Abhängigkeit?

#### --distractors--

Eine Abhängigkeit ist eine häufig verwendete Testbibliothek in React-Anwendungen.

---

Eine Abhängigkeit ist eine Art React-Komponente, die nur auf dem Server ausgeführt wird.

---

Eine Abhängigkeit ist ein spezieller Compiler, der in React-Anwendungen verwendet wird, um den React-Code in JavaScript zu übersetzen.

#### --answer--

Eine Abhängigkeit ist, wenn eine Komponente oder ein Modul in einer Anwendung auf ein anderes angewiesen ist, um richtig zu funktionieren.

### --question--

#### --text--

Was ist ein Paketmanager?

#### --distractors--

Ein Werkzeug, das verwendet wird, um Drittanbieter-Pakete in Anwendungen zu aktualisieren.

---

Ein Werkzeug, das ausschließlich zur Verwaltung von Paketen verwendet wird, die innerhalb eines Projekts erstellt wurden.

---

Ein Werkzeug, das Abhängigkeiten aus dem `node_modules`-Ordner in einen speziellen Konfigurationsordner verschiebt.

#### --answer--

Ein Werkzeug, das für Installation, Updates und Entfernung von Abhängigkeiten verwendet wird.

### --question--

#### --text--

Welcher der folgenden ist KEIN häufig verwendeter Paketmanager im JavaScript-Ökosystem?

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

Welche der folgenden Dateien ist eine wichtige Konfigurationsdatei, die Metadaten über Ihr Projekt enthält?

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

Welches der folgenden ist ein häufig verwendetes React-Framework?

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

Wie fügen Sie Routen zu Ihrer React-Anwendung mit React Router korrekt hinzu?

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

Wie erstellen Sie eine dynamische Route in React Router korrekt?

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

Welcher der folgenden Hooks wird verwendet, um auf die dynamischen Parameter eines URL-Pfads zuzugreifen?

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

Wie erstellen Sie in Next.js einen benutzerdefinierten Request-Handler korrekt?

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

Was ist Prop Drilling?

#### --distractors--

Der Prozess, einen Reducer zu erstellen, der angibt, wie sich der Zustand als Reaktion auf bestimmte Props ändern soll.

---

Der Prozess, Props von einer Kindkomponente zur Elternkomponente weiterzugeben.

---

Der Prozess, einen Context Provider für eine React-Anwendung zu erstellen und diesen zu verwenden, um Props von Eltern- zu Kindkomponenten zu senden.

#### --answer--

Der Prozess, Props von einer Elternkomponente an tief verschachtelte Kindkomponenten weiterzugeben, selbst wenn einige der Kindkomponenten die Props nicht benötigen.

### --question--

#### --text--

Welches der folgenden ist KEINE häufig verwendete State-Management-Lösung für React-Anwendungen?

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

Wofür werden die React Developer Tools verwendet?

#### --distractors--

Diese Tools werden nur verwendet, um Barrierefreiheitsprobleme in React-Anwendungen zu überprüfen.

---

Diese Tools werden verwendet, um zusätzliche Tests für React-Komponenten hinzuzufügen.

---

Diese Tools werden verwendet, um alle React-Komponenten innerhalb der Anwendung zu memoizieren.

#### --answer--

Diese Tools werden verwendet, um React-Komponenten zu inspizieren und Leistungsprobleme zu identifizieren.

### --question--

#### --text--

Was sind React Server Components?

#### --distractors--

Das sind Komponenten, die Kontext ausschließlich auf dem Server verwalten.

---

Das sind memoizierte Komponenten, die ausschließlich auf dem Server rendern.

---

Das sind Komponenten, die Zustand ausschließlich auf dem Server verwalten.

#### --answer--

Das sind Komponenten, die ausschließlich auf dem Server rendern.

