---
id: 67358ac128957c865dcf3ddf
title: اختبار أصناف JavaScript
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ما الهدف من الصنف في JavaScript؟

#### --distractors--

لتعريف الخصائص فقط، وليس الطرق.

---

لتعريف المتغيرات الثابتة فقط.

---

لتنظيم البيانات الأولية فقط.

#### --answer--

لإنشاء مخططات قابلة لإعادة الاستخدام للكائنات.

### --question--

#### --text--

كيف تعرف صنفًا في JavaScript؟

#### --distractors--

باستخدام الكلمة المفتاحية `className`.

---

باستخدام الكلمة المفتاحية `object`.

---

باستخدام الكلمة المفتاحية `constructor` فقط.

#### --answer--

باستخدام الكلمة المفتاحية `class`.

### --question--

#### --text--

أي طريقة تُستخدم في الصنف لتهيئة الخصائص؟

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

ماذا تمثل `this` داخل دالة البناء في الصنف؟

#### --distractors--

كائن الصنف العام.

---

الدالة نفسها.

---

مرجع غير معرف.

#### --answer--

مثيل من الصنف.

### --question--

#### --text--

أي مما يلي يصف كيف يتصرف `this` في الدالة السهمية داخل طريقة صنف؟

#### --distractors--

يرث قيمة `this` من أقرب دالة في نطاق مختلف.

---

يشير إلى مثيل جديد منشأ من الصنف.

---

يرث قيمة `this` من الكائن العام في الوضع الصارم.

#### --answer--

يرث قيمة `this` من النطاق المحيط حيث تم تعريفه.

### --question--

#### --text--

ما الهدف الأساسي من الكلمة المفتاحية `extends`؟

#### --distractors--

لتعريف طرق ثابتة في دالة البناء للصنف الأب.

---

لإنشاء طريقة في الصنف.

---

لتهيئة الخصائص الافتراضية في الصنف الأب.

#### --answer--

لإنشاء صنف فرعي يرث من صنف أب.

### --question--

#### --text--

أي مما يلي مثال على كيفية إنشاء صنف `Car` يرث من صنف `Vehicle`؟

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

ما ناتج الشفرة التالية؟

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

يتم رفع خطأ.

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

ما وظيفة الكلمة المفتاحية `super` في صنف فرعي؟

#### --distractors--

لتعريف مثيل جديد من الصنف الأب.

---

لحذف الطرق والخصائص من الصنف الأب.

---

لوصول فقط إلى الطرق الثابتة للصنف الأب.

#### --answer--

لاستدعاء دالة البناء أو الطرق في الصنف الأب.

### --question--

#### --text--

أي مما يلي يصف طريقة ثابتة؟

#### --distractors--

طريقة لا يمكن الوصول إليها داخل الصنف نفسه.

---

طريقة تُستخدم فقط في الدوال السهمية.

---

طريقة فريدة لكل مثيل من الصنف.

#### --answer--

طريقة تنتمي إلى الصنف نفسه، وليس لأي مثيل.

### --question--

#### --text--

ما ناتج الشفرة التالية؟

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

يتم رفع خطأ.

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

كيف تصل إلى خاصية ثابتة في صنف JavaScript؟

#### --distractors--

باستخدام `this.propertyName` داخل طريقة.

---

باستخدام `instance.propertyName` بعد إنشاء مثيل.

---

باستخدام `super.propertyName` داخل صنف فرعي.

#### --answer--

باستخدام `ClassName.propertyName`.

### --question--

#### --text--

في أي حالة تستخدم طريقة ثابتة بدلًا من طريقة مثيل؟

#### --distractors--

عندما تحتاج إلى الوصول إلى خصائص المثيل وتعديلها.

---

عندما يجب استدعاء الطريقة على مثيل كائن.

---

عندما لا تعتمد الطريقة على بيانات خاصة بالمثيل وتعمل على بيانات على مستوى الصنف.

#### --answer--

عندما تعمل الطريقة على بيانات مشتركة بين جميع مثيلات الصنف.

### --question--

#### --text--

ما ناتج الشفرة التالية؟

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

ترمي خطأ.

### --question--

#### --text--

ما الكلمة المفتاحية المستخدمة لوراثة الخصائص والطرق من صنف آخر؟

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

ماذا يُطبع بواسطة الشفرة التالية؟

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

يتم رفع خطأ.

#### --answer--

`Human`

### --question--

#### --text--

أي مما يلي يصف الوراثة بشكل أفضل؟

#### --distractors--

تسمح لطريقة بإرجاع كائن جديد.

---

تسمح لمثيل بتعريف خصائصه الخاصة.

---

تسمح بتحويل خاصية ثابتة إلى خاصية غير ثابتة.

#### --answer--

تسمح لصنف باكتساب الخصائص والطرق من صنف آخر.

### --question--

#### --text--

ماذا يحدث إذا لم تُعرف دالة البناء في صنف فرعي؟

#### --distractors--

سيتم رفع خطأ.

---

يرث الصنف الفرعي دالة البناء من أي صنف في الشفرة.

---

لا يمكن للصنف الفرعي إنشاء مثيلات جديدة.

#### --answer--

تُستخدم دالة البناء للصنف الأم افتراضيًا.

### --question--

#### --text--

ماذا تمثل `this` في الشفرة التالية؟

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

الكائن العام.

---

الصنف `Animal` نفسه.

---

متغير غير معرف.

#### --answer--

مثيل `Animal`.

### --question--

#### --text--

ما الاستخدام الأساسي للخصائص الثابتة؟

#### --distractors--

لتعريف خصائص افتراضية لكل مثيل من الصنف.

---

لتجاوز الطرق في صنف فرعي.

---

لتعريف بيانات خاصة بالمثيل.

#### --answer--

لتخزين بيانات مشتركة على مستوى الصنف بين جميع المثيلات.
