---
id: 66ed9009f45ce3ece4053eb7
title: CSS 相對與絕對單位測驗
challengeType: 8
dashedName: quiz-css-relative-and-absolute-units
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

什麼是絕對單位？

#### --distractors--

根據螢幕大小動態調整的單位。

---

依賴父元素字型大小的單位。

---

以視窗百分比為單位測量的單位。

#### --answer--

固定長度單位，不相對於其他任何東西。

### --question--

#### --text--

`100vw` 涵蓋多少螢幕寬度？

#### --distractors--

`100%` 的螢幕寬度。

---

視窗高度的 `100%`。

---

父元素寬度的 `100%`。

#### --answer--

視窗寬度的 `100%`。

### --question--

#### --text--

以下哪一項是相對單位？

#### --distractors--

`pt`

---

`cm`

---

`mm`

#### --answer--

`em`

### --question--

#### --text--

如果根字型大小是 `20px`，那麼 `1rem` 相等於多少？

#### --distractors--

`18px`

---

`16px`

---

`10px`

#### --answer--

`20px`

### --question--

#### --text--

`calc()` 函式（程式）在 CSS 中可以做什麼？

#### --distractors--

動態修改顏色。

---

設定固定寬度。

---

僅處理百分比。

#### --answer--

加、減、乘或除不同的單位。

### --question--

#### --text--

哪一個單位是 CSS 中最常用的絕對單位？

#### --distractors--

`inch`

---

`rem`

---

`%`

#### --answer--

`px`

### --question--

#### --text--

如果視窗高度是 `800px`，那麼 `50vh` 會是多少？

#### --distractors--

`50px`

---

`200px`

---

`100px`

#### --answer--

`400px`

### --question--

#### --text--

將元素的寬度設定為 `100%` 意味著什麼？

#### --distractors--

它佔據了整個文件的寬度。

---

它佔據了整個螢幕的寬度。

---

它佔用 `100px`。

#### --answer--

它佔滿其父元素的整個寬度。

### --question--

#### --text--

在 CSS 中，`calc(10px + 20%)` 會發生什麼？

#### --distractors--

它將 `10px` 添加到文件寬度的 `10%`。

---

它總是相等於 `30px`。

---

它將 `10px` 添加到螢幕寬度的 `20%`。

#### --answer--

它會為父元素寬度的 `20%` 添加 `10px`。

### --question--

#### --text--

哪一個單位是相對於根元素的字型大小？

#### --distractors--

`px`

---

`pt`

---

`%`

#### --answer--

`rem`

### --question--

#### --text--

當根字型大小為 `16px` 時，`10rem` 有多大？

#### --distractors--

`140px`

---

`10px`

---

`100px`

#### --answer--

`160px`

### --question--

#### --text--

為什麼你可能會在設計中使用 `point` 而不是 `pixels`？

#### --distractors--

點數是可縮放的。

---

使用點數對響應式佈局較為合適。

---

在螢幕上管理點數較為容易。

#### --answer--

點數是用於列印設計。

### --question--

#### --text--

為什麼在排版中偏好使用 `rem` 單位而非 `pixels`？

#### --distractors--

因為它們提供固定大小，不受螢幕解析度影響。

---

因為它們確保與舊版瀏覽器的相容性。

---

因為它們在響應式設計中較容易實作。

#### --answer--

因為它們會隨著使用者瀏覽器設定成比例縮放。

### --question--

#### --text--

`80vw` 佔用多少螢幕空間？

#### --distractors--

父元素寬度的 `80%`。

---

視窗高度的 `100%`。

---

`80px`

#### --answer--

視窗寬度的 `80%`。

### --question--

#### --text--

如果你想讓一個元素佔據螢幕某個寬度或高度的百分比，你應該使用哪些單位？

#### --distractors--

`pt` 和 `px`

---

`rem` 和 `em`

---

`pt` 和 `pc`

#### --answer--

`vw` 和 `vh`

### --question--

#### --text--

`vw` 代表什麼？

#### --distractors--

垂直寬度

---

虛擬寬度

---

可變寬度。

#### --answer--

視窗寬度

### --question--

#### --text--

`rem` 和 `em` 在 CSS 中的主要差異是什麼？

#### --distractors--

`em` 是相對於根元素，而 `rem` 是相對於父元素。

---

`rem` 是相對於桌面寬度，而 `em` 是相對於行動裝置寬度。

---

`rem` 是相對於使用者定義的寬度，而 `em` 是相對於瀏覽器寬度。

#### --answer--

`em` 是相對於父元素的字型大小，而 `rem` 是相對於根元素的字型大小。

### --question--

#### --text--

什麼是 `vh` 單位？

#### --distractors--

基於視窗寬度的單位。

---

基於元素高度的單位。

---

一個會根據字型大小調整的單位。

#### --answer--

一個等於視窗高度 1% 的單位。

### --question--

#### --text--

在 CSS 中，將元素的寬度設定為 `auto` 會怎麼做？

#### --distractors--

它會將寬度預設為父元素的 `100%`。

---

它將寬度設定為視窗的 `50%`，除非其他樣式改寫它。

---

如果沒有內容或內距，則將寬度設為 `0px`。 

#### --answer--

它允許瀏覽器根據內容和容器來決定寬度。

### --question--

#### --text--

`calc()` 函式（程式）的主要優勢是什麼？

#### --distractors--

它會自動將你的 CSS 檔案縮小，以提升效率。

---

它啟用在 CSS 中使用變數，而不需要任何前處理器。

---

它允許直接在 CSS 規則中嵌入 JavaScript 表達式。

#### --answer--

它讓你根據應用程式或視窗不同的面向動態決定 CSS 屬性的值。

