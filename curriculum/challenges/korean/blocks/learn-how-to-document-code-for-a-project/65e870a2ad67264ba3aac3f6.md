---
id: 65e870a2ad67264ba3aac3f6
title: 작업 25
challengeType: 22
dashedName: task-25
lang: en-US
---

<!-- (Audio) Brian: Great idea. To write comments, you simply need to use double slashes to start a comment. -->

# --description--

이 도전 과제에서는 `simple`와 `simply`의 차이를 배우고 `double`와 `slashes`가 의미하는 바를 이해합니다.

`Simple`는 쉽거나 복잡하지 않다는 뜻의 형용사로, `a simple task`와 비슷합니다. `Simply`는 부사로, 간단한 방식으로라는 뜻이며 `doing something simply`와 비슷합니다.

`Double`는 어떤 것의 두 개를 의미합니다. 그리고 `slashes`는 글을 쓸 때 사용하는 `/` 기호입니다. `es`를 추가해 `slash`를 복수형으로 만드는데, `sh`로 끝나는 단어들은 `es`를 붙여 복수형을 만듭니다. 예를 들어 `dishes`나 `brushes`처럼요.

# --fillInTheBlank--

## --sentence--

`Great idea. To write comments, you BLANK need to use BLANK BLANK to start a comment.`

## --blanks--

`simply`

### --feedback--

쉽거나 직관적인 방식으로라는 뜻입니다. Brian은 주석을 쓰는 것이 쉽다는 것을 설명할 때 이 표현을 사용합니다.

---

`double`

### --feedback--

어떤 것을 두 개 사용하는 것을 가리킵니다. 여기서는 슬래시 두 개를 사용하는 것을 의미합니다.

---

`slashes`

### --feedback--

이것들은 `/` 기호입니다. `sh`로 끝나는 단어는 복수형을 만들 때 `es`를 붙여야 합니다.

# --scene--

```json
{
  "setup": {
    "background": "company2-center.png",
    "characters": [
      {
        "character": "Brian",
        "position": {
          "x": 50,
          "y": 15,
          "z": 1.2
        },
        "opacity": 0
      }
    ],
    "audio": {
      "filename": "7.1-2.mp3",
      "startTime": 1,
      "startTimestamp": 6.24,
      "finishTimestamp": 11.36
    }
  },
  "commands": [
    {
      "character": "Brian",
      "opacity": 1,
      "startTime": 0
    },
    {
      "character": "Brian",
      "startTime": 1,
      "finishTime": 6.12,
      "dialogue": {
        "text": "Great idea. To write comments, you simply need to use double slashes to start a comment.",
        "align": "center"
      }
    },
    {
      "character": "Brian",
      "opacity": 0,
      "startTime": 6.62
    }
  ]
}
```
