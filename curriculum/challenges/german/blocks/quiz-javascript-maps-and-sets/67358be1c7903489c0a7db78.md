---
id: 67358be1c7903489c0a7db78
title: JavaScript-Maps- und Sets-Quiz
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 Fragen unten richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Was ist ein Set in JavaScript?

#### --distractors--

Eine Datenstruktur, die doppelte Werte zulässt.

---

Eine Datenstruktur, die nur Strings und Zahlen speichert.

---

Eine Sammlung, die das Löschen ihrer Elemente nicht erlaubt.

#### --answer--

Eine Sammlung, in der ein Wert nur einmal vorkommen kann.

### --question--

#### --text--

Welche der folgenden Methoden ist in Maps verfügbar, aber nicht in Sets?

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

Welche Methode wird verwendet, um Elemente zu einem Set hinzuzufügen?

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

Wie überprüfen Sie, ob ein Set einen bestimmten Wert enthält?

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

Welches dieser Beispiele ist ein guter Anwendungsfall für Sets?

#### --distractors--

Verwalten von Schlüssel-Wert-Paaren in einer Datenbank.

---

Erstellen geordneter Listen von Elementen.

---

Speichern hierarchischer Datenstrukturen im lokalen Browser-Speicher.

#### --answer--

Duplikate aus einem Array entfernen.

### --question--

#### --text--

Was ist ein WeakSet in JavaScript?

#### --distractors--

Eine Set-ähnliche Sammlung, die nur primitive Datentypen zulässt.

---

Eine Set-ähnliche Sammlung, die doppelte Werte erlaubt.

---

Eine Set-ähnliche Sammlung, die verhindert, dass Werte entfernt werden.

#### --answer--

Eine Set-ähnliche Sammlung, die nur Objekte speichert.

### --question--

#### --text--

Was ist ein Unterschied zwischen einem Set und einem WeakSet?

#### --distractors--

Ein WeakSet kann jeden Werttyp speichern, einschließlich Zahlen.

---

Ein Set erlaubt keine doppelten Werte, aber ein WeakSet schon.

---

Ein Set erlaubt Garbage Collection, während ein WeakSet das nicht tut.

#### --answer--

Ein Set kann jeden Werttyp halten, während ein WeakSet nur Objekte hält.

### --question--

#### --text--

Was ist eine Map in JavaScript?

#### --distractors--

Eine Sammlung, die nur eindeutige Werte zulässt.

---

Eine Sammlung, die nur Strings als Schlüssel verwenden kann.

---

Eine Sammlung, die das Löschen ihrer Elemente nicht erlaubt.

#### --answer--

Eine Sammlung von Schlüssel-Wert-Paaren beliebigen Typs.

### --question--

#### --text--

Welche Methode wird verwendet, um Schlüssel-Wert-Paare in eine Map einzufügen?

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

Was gibt der folgende Code aus?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

Worin unterscheidet sich eine Map von einem Objekt in JavaScript?

#### --distractors--

Eine Map erlaubt nur Zahlen als Schlüssel.

---

Objekte erlauben jeden Typ als Schlüssel, während Maps nur Strings erlauben.

---

Maps unterstützen keine Iteration, Objekte hingegen schon.

#### --answer--

Maps erlauben jeden Schlüsseltyp, Objekte nur Strings und Symbole.

### --question--

#### --text--

Wie können Sie ein Schlüssel-Wert-Paar aus einer Map entfernen?

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

Was gibt die `size`-Eigenschaft einer Map zurück?

#### --distractors--

Die Anzahl der Indizes in der Map.

---

Die Anzahl der eindeutigen Werte in der Map.

---

Der insgesamt genutzte Speicher der Map.

#### --answer--

Die Anzahl der Schlüssel-Wert-Paare in der Map.

### --question--

#### --text--

Was ist eine WeakMap in JavaScript?

#### --distractors--

Eine Map-ähnliche Sammlung, die primitive Werte als Schlüssel zulässt.

---

Eine Map-ähnliche Sammlung, die das Löschen von Elementen blockiert.

---

Eine Map-ähnliche Sammlung, die ihre Schlüssel automatisch sortiert.

#### --answer--

Eine Map-ähnliche Sammlung mit schwachen Referenzen auf die Schlüssel.

### --question--

#### --text--

Was ist ein Unterschied zwischen einer Map und einer WeakMap?

#### --distractors--

Eine Map erlaubt nur primitive Typen als Schlüssel, eine WeakMap nur Objekte.

---

Eine Map erlaubt Garbage Collection, eine WeakMap nicht.

---

Eine WeakMap erlaubt doppelte Schlüssel, eine Map nicht.

#### --answer--

Eine Map ist iterierbar, eine WeakMap nicht.

### --question--

#### --text--

Welche der folgenden Methoden existiert NICHT auf einer WeakMap?

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

Was wird der folgende Code ausgeben?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

Wie können Sie über die Werte in einem Set iterieren?

#### --distractors--

Indem Sie die `.map()`-Methode verwenden.

---

Indem Sie die `.reduce()`-Methode verwenden.

---

Indem Sie die `.filter()`-Methode verwenden.

#### --answer--

Indem Sie die `.forEach()`-Methode verwenden.

### --question--

#### --text--

Welche zwei Methoden von Sets geben eine `SetIterator` zurück, die die Werte eines bestimmten Sets enthält?

#### --distractors--

`entries()` und `values()`

---

`forEach()` und `values()`

---

`has()` und `add()`

#### --answer--

`keys()` und `values()`

### --question--

#### --text--

Was wird der folgende Code ausgeben?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
