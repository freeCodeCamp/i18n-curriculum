---
id: 692487767b6b728454f53f29
title: NodeJS 코어 모듈 퀴즈
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

기존 파일을 덮어쓰지 않고 내용을 추가하는 `fs` 메서드는 무엇인가요?

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

`fs` 모듈의 동기 메서드를 사용하면 어떤 일이 발생하나요?

#### --distractors--

기존 파일을 자동으로 삭제합니다.

---

모든 경우에 비동기 메서드보다 더 빠르게 실행됩니다.

---

기본적으로 프로미스를 반환합니다.

#### --answer--

작업이 끝날 때까지 프로그램이 차단됩니다.

### --question--

#### --text--

`utf8`에서 `fs.readFile()` 매개변수는 어떤 역할을 하나요?

#### --distractors--

오류가 발생하는 것을 방지합니다.

---

파일의 확장자를 변경합니다.

---

큰 파일을 압축합니다.

#### --answer--

노드가 버퍼 대신 읽을 수 있는 문자열을 반환하도록 지시합니다.

### --question--

#### --text--

제로로 채워진 새 버퍼를 생성하는 `Buffer` 메서드는 무엇인가요?

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

`Buffer.toString()`는 어떤 기능을 하나요?

#### --distractors--

버퍼를 이진 코드로 변환합니다.

---

버퍼 내용을 지웁니다.

---

버퍼 바이트 길이를 보여줍니다.

#### --answer--

버퍼 내용을 읽을 수 있는 문자열로 변환합니다.

### --question--

#### --text--

서명을 생성할 때 개인 키가 필요한 `crypto` 기능은 무엇인가요?

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

직접 암호화 알고리즘을 작성하는 것을 피해야 하는 이유는 무엇인가요?

#### --distractors--

Node.js 애플리케이션에서는 불법입니다.

---

Node.js는 암호화를 지원하지 않습니다.

---

JavaScript는 해시를 계산할 수 없습니다.

#### --answer--

잘 테스트된 라이브러리 없이는 안전하지 않을 수 있습니다.

### --question--

#### --text--

`crypto` 대신 안전한 무작위 토큰 생성을 위해 권장되는 `Math.random()` 메서드는 무엇인가요?

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

CPU 아키텍처를 반환하는 OS 모듈 메서드는 무엇인가요?

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

`os.userInfo()`는 무엇을 반환하나요?

#### --distractors--

사용자 이름만 반환합니다.

---

장치 IP 주소만 반환합니다.

---

로그인한 사용자 ID만 반환합니다.

#### --answer--

현재 시스템 사용자에 관한 정보를 반환합니다.

### --question--

#### --text--

세그먼트를 연결하고 여분의 슬래시를 표준화하는 `path` 메서드는 무엇인가요?

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

현재 작업 디렉터리를 기준으로 절대 경로를 생성하는 `path` 메서드는 무엇인가요?

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

전역 변수 `__dirname`는 무엇을 나타내나요?

#### --distractors--

Node를 실행하는 사용자의 이름입니다.

---

현재 파일의 확장자입니다.

---

`./`로 시작하는 상대 폴더 경로입니다.

#### --answer--

현재 파일이 포함된 디렉터리의 절대 경로입니다.

### --question--

#### --text--

`NODE_ENV` 같은 환경 변수를 포함하는 프로세스 속성은 무엇인가요?

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

`process.on("exit")` 이벤트는 어떤 역할을 하나요?

#### --distractors--

Node 애플리케이션을 재시작합니다.

---

자식 프로세스에 `exit` 신호를 보냅니다.

---

이벤트 루프 큐를 지웁니다.

#### --answer--

Node 프로세스가 종료되기 직전에 코드를 실행합니다.

### --question--

#### --text--

읽기와 쓰기가 모두 가능하지만 변환하지 않는 스트림 유형은 무엇인가요?

#### --distractors--

읽기 스트림

---

변환 스트림

---

쓰기 스트림

#### --answer--

듀플렉스 스트림

### --question--

#### --text--

대용량 파일을 효율적으로 읽고 쓰려면 무엇이 필요하나요?

#### --distractors--

`Buffer.alloc()`만 사용하기

---

`crypto` 모듈 가져오기

---

파일 전체를 메모리에 먼저 저장하기

#### --answer--

데이터를 청크 단위로 처리하는 스트림 사용하기

### --question--

#### --text--

읽기 스트림을 쓰기 스트림에 연결하는 메서드는 무엇인가요?

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

더 이상 쓸 데이터가 없음을 알리는 스트림 이벤트는 무엇인가요?

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

`toString()` 없이 읽기 스트림에서 청크를 출력하면 무엇이 기록되나요?

#### --distractors--

일반 텍스트 내용

---

프로세스 PID

---

운영 체제 이름

#### --answer--

이진 데이터가 담긴 버퍼 객체
