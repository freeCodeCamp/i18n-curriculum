---
id: 66edd3b3096349f06cf688bb
title: JavaScript オーディオとビデオのクイズ
challengeType: 8
dashedName: quiz-javascript-audio-and-video
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

JavaScriptにおける`Audio()`コンストラクタの目的は何ですか？

#### --distractors--

新しいオーディオファイルを作成する。

---

既存のオーディオファイルを再生する。

---

オーディオファイルを編集する。

#### --answer--

新しい`audio`要素を作成する。

### --question--

#### --text--

`audio`または`video`要素の再生を開始するために使われるメソッドはどれですか？

#### --distractors--

`start()`

---

`begin()`

---

`resume()`

#### --answer--

`play()`

### --question--

#### --text--

メディア要素の`pause()`メソッドと`play()`メソッドの違いは何ですか？

#### --distractors--

`pause()`はPromiseを返しますが、`play()`は返しません。

---

`pause()`は再開できませんが、`play()`は任意の位置から開始できます。

---

`pause()`は自動的に最初にリセットされますが、`play()`は一時停止した位置から開始します。

#### --answer--

`pause()`は再生を停止しますが現在の位置を維持し、`play()`は再生を開始します。

### --question--

#### --text--

最も広くブラウザでサポートされているオーディオフォーマットはどれですか？

#### --distractors--

`FLAC`

---

`OGG`

---

`WAV`

#### --answer--

`MP3`

### --question--

#### --text--

`source`または`audio`要素内の`video`要素の目的は何ですか？

#### --distractors--

メディアの音量を指定する。

---

メディアに字幕を追加する。

---

外部のオーディオプロセッサーにリンクする。

#### --answer--

異なるフォーマットの複数のメディアソースを指定する。

### --question--

#### --text--

MP4フォーマットが持つ可能性のある2つのMIMEタイプは何ですか？

#### --distractors--

`mp4/audio`と`mp4/video`

---

`audio/mpeg4`と`video/mpeg4`

---

`mp4/mpeg`と`mp4/stream`

#### --answer--

`audio/mp4`と`video/mp4`

### --question--

#### --text--

オーディオとビデオの文脈で、コーデックは何をしますか？

#### --distractors--

メディアの音量を上げる。

---

メディアのファイル形式を変更する。

---

安全な送信のためにメディアを暗号化する。

#### --answer--

メディアをアナログとデジタルの間で変換する。

### --question--

#### --text--

ウェブアプリケーションでオーディオを作成、処理、分析する方法を提供するAPIはどれですか？

#### --distractors--

Audio Processing API。

---

Sound Manipulation API。

---

Media Stream API。

#### --answer--

Web Audio API。

### --question--

#### --text--

`getUserMedia()`メソッドの目的は何ですか？

#### --distractors--

メディアサービスのユーザー認証を行う。

---

メディア再生のユーザー設定を取得する。

---

ユーザーのデバイスにメディアファイルをダウンロードする。

#### --answer--

ユーザーのカメラおよび/またはマイクにアクセスする。

### --question--

#### --text--

`HTMLMediaElement`インターフェースのどのプロパティが現在の再生時間を表しますか？

#### --distractors--

`time`

---

`playbackPosition`

---

`currentPosition`

#### --answer--

`currentTime`

### --question--

#### --text--

次のコードスニペットは何をしますか？

```javascript
const audio = new Audio('song.mp3');
audio.loop = true;
```

#### --distractors--

オーディオファイルを一度だけ再生する。

---

オーディオの音量を上げる。

---

1回ループ後にオーディオを一時停止する。

#### --answer--

オーディオを無限に繰り返すように設定する。

### --question--

#### --text--

MIMEの正式名称は何ですか？

#### --distractors--

Multiple Internet Message Extensions

---

Mail Internet Multimedia Extensions

---

Multimedia Internet Mail Extensions

#### --answer--

Multipurpose Internet Mail Extensions

### --question--

#### --text--

`canPlayType()`メソッドは何を助けますか？

#### --distractors--

ブラウザで特定のフォーマットのオーディオが現在再生中かどうかを確認する。

---

ブラウザのキャッシュにメディアファイルが存在し再生可能かどうかを検証する。

---

オーディオ要素にコントロールが有効かつユーザーが操作権限を持つかをテストする。

#### --answer--

ブラウザが特定のオーディオフォーマットを再生できる可能性があるかどうかを判定する。

### --question--

#### --text--

次のうち`HTMLMediaElement` APIの有効なイベントでないものはどれですか？

#### --distractors--

`waiting`

---

`canplay`

---

`ended`

#### --answer--

`stopped`

### --question--

#### --text--

データのバッファリングにより再生が一時停止されたときに発火する`HTMLMediaElement`イベントは何ですか？

#### --distractors--

`stopped`

---

`pause`

---

`buffering`

#### --answer--

`waiting`

### --question--

#### --text--

オーディオ処理グラフを表すために使われるWeb Audio APIのインターフェースはどれですか？

#### --distractors--

`AudioNode`

---

`AudioBuffer`

---

`AudioListener`

#### --answer--

`AudioContext`

### --question--

#### --text--

Media Source Extensions APIの目的は何ですか？

#### --distractors--

ビデオに視覚効果を追加する。

---

異なるメディアフォーマット間で変換する。

---

JavaScriptで複数のオーディオトラックを同期させる。

#### --answer--

ユーザーのウェブカメラ映像を直接ビデオ要素に渡せるようにする。

### --question--

#### --text--

`ended`イベントはいつ発火しますか？

#### --distractors--

メディアソースが変更されたとき。

---

`pause()`メソッドが呼ばれたとき。

---

メディアが手動で停止されたとき。

#### --answer--

メディアの終端に達したとき。

### --question--

#### --text--

Screen Capture APIはどのように公開されていますか？

#### --distractors--

`window.screenCapture`インターフェースを通じて

---

`navigator.capture`プロパティを使って

---

`captureScreen()`メソッドを使って

#### --answer--

`getDisplayMedia()`メソッドを呼び出して。

### --question--

#### --text--

WMVフォーマットは何に関連していますか？

#### --distractors--

Wide Media Video

---

World Media Video

---

Web Media Player

#### --answer--

Windows Media Player
