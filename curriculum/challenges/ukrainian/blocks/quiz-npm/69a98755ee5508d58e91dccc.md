---
id: 69a98755ee5508d58e91dccc
title: Тест з npm
challengeType: 8
dashedName: quiz-npm
---

# --description--

Щоб пройти тест, потрібно правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що надає npm розробникам?

#### --distractors--

Кураторську платформу, яка хостить лише дизайнерські ресурси, як-от шрифти та іконки.

---

Сервіс хостингу, який автоматично розгортає ваш проєкт у хмарі.

---

Рішення бази даних для керування записами користувачів у ваших застосунках.

#### --answer--

Реєстр готових пакетів програмного забезпечення, що вирішує загальні проблеми.

### --question--

#### --text--

Що з наведеного не є однією з трьох частин, на які посилається npm?

#### --distractors--

Вебсайт npmjs.com.

---

Реєстр пакетів, де зберігаються бібліотеки.

---

Інструмент командного рядка для встановлення та керування пакетами.

#### --answer--

Середовище виконання Node.js.

### --question--

#### --text--

Що робить команда `npm install`?

#### --distractors--

Створює новий проєкт Node.js за шаблоном.

---

Запускає ваш застосунок Node.js на локальному сервері розробки.

---

Видаляє пакети, які більше не потрібні у вашому проєкті.

#### --answer--

Додає готові пакети до вашого проєкту для загальних завдань.

### --question--

#### --text--

Який файл зберігає метадані, як-от назву проєкту, версію, автора та залежності для проєкту Node.js?

#### --distractors--

`README.md`.

---

`node_modules/package.json`.

---

`package-lock.json`.

#### --answer--

`package.json`.

### --question--

#### --text--

Як можна інтерактивно створити файл `package.json`?

#### --distractors--

Запустити `npm install` без вказання назви пакета.

---

Скачати шаблон із головної сторінки реєстру npm.

---

Скопіювати файл з іншого проєкту та перейменувати його.

#### --answer--

Запустити команду `npm init`.

### --question--

#### --text--

Як створити файл `package.json` із значеннями за замовчуванням без відповіді на запити?

#### --distractors--

Запустити `npm init --interactive`.

---

Створити порожній файл з назвою `package.json` і залишити його пустим.

---

Використати `npm init` і видалити всі запити після відкриття редактора.

#### --answer--

Запустити `npm init -y` (або `npm init --yes`) для автоматичного прийняття всіх значень за замовчуванням.

### --question--

#### --text--

Де npm перераховує пакети, які потрібні лише під час розробки, наприклад лінтери чи тестові ранери?

#### --distractors--

У розділі `"dependencies"`.

---

У розділі `"scripts"` під кастомними командами.

---

У самій папці `node_modules`.

#### --answer--

У розділі `"devDependencies"` файлу `package.json`.

### --question--

#### --text--

Яка команда встановлює `nodemon` як залежність лише для розробки?

#### --distractors--

`npm uninstall nodemon`.

---

`npm save nodemon --dev`.

---

`npm install nodemon`.

#### --answer--

`npm install nodemon -D`.

### --question--

#### --text--

Яка папка і файл додаються до кореня вашого проєкту після встановлення залежності?

#### --distractors--

Папка `public` і файл `index.html`.

---

Папка `dist` і файл `webpack.config.js`.

---

Папка `src` і файл `README.md`.

#### --answer--

Папка `node_modules` і файл `package-lock.json`.

### --question--

#### --text--

Як `package-lock.json` допомагає зберігати збірки послідовними?

#### --distractors--

Видаляє невикористані залежності перед кожною установкою.

---

Зберігає лише основні версії пакетів.

---

Автоматично оновлює пакети до останніх версій.

#### --answer--

Фіксує точні версії кожної встановленої залежності.

### --question--

#### --text--

Чому варто комітити `package-lock.json` у ваш репозиторій Git?

#### --distractors--

Щоб npm працював швидше на машині розробника.

---

Щоб автоматично оновлювати застарілі залежності.

---

Щоб зменшити розмір репозиторію.

#### --answer--

Щоб уся команда встановлювала однакові версії залежностей і налаштувань.

### --question--

#### --text--

Як встановити конкретну версію пакета, наприклад `express@4.21.2`?

#### --distractors--

`npm update express@4.21.2`.

---

`npm get express@4.21.2`.

---

`npm use express@4.21.2`.

#### --answer--

`npm install express@4.21.2`.

### --question--

#### --text--

Що дозволяє символ карет (`^`) при вказуванні версій залежностей?

#### --distractors--

Фіксує залежність на точній версії патчу.

---

Дозволяє оновлення лише основної версії.

---

Виключає всі версії нижче вказаної.

#### --answer--

Дозволяє новіші мінорні та патч-версії, але блокує нові основні релізи.

### --question--

#### --text--

Що означає символ тильда (`~`) у діапазоні версій, як `~1.2.3`?

#### --distractors--

Дозволяє оновлення як основної, так і мінорної версій.

---

Встановлює найновішу основну версію незалежно від діапазону.

---

Запобігає будь-яким оновленням npm.

#### --answer--

Дозволяє оновлення лише патч-версії, залишаючи мінорну фіксованою.

### --question--

#### --text--

Які зміни викликають збільшення основної версії за семантичним версіонуванням?

#### --distractors--

Незначні стилістичні правки коментарів і форматування.

---

Невеликі виправлення помилок, що не змінюють поведінку.

---

Опціональні функції, які не порушують існуючий код.

#### --answer--

Критичні зміни, що можуть призвести до непрацездатності існуючого коду.

### --question--

#### --text--

Яка команда показує залежності з новішими опублікованими версіями?

#### --distractors--

`npm check`.

---

`npm list`.

---

`npm refresh`.

#### --answer--

`npm outdated`.

### --question--

#### --text--

Yarn, PNPM і Bun згадуються як альтернативи npm. Якими інструментами вони є?

#### --distractors--

Вебсайти реєстрів, де ви публікуєте або скачувати пакети npm.

---

Сервіси баз даних для зберігання даних користувачів пакетів npm.

---

Хмарні платформи хостингу, які автоматично розгортають проєкти npm.

#### --answer--

Альтернативні менеджери пакетів.

### --question--

#### --text--

Де в проєкті оголошують кастомні npm-скрипти?

#### --distractors--

У файлі `.npmrc`.

---

У розділі `"dependencies"` файлу `package.json`.

---

У окремому файлі `scripts.json` у корені.

#### --answer--

У об’єкті `"scripts"` файлу `package.json`.

### --question--

#### --text--

Як виконати кастомний npm-скрипт з назвою `build`?

#### --distractors--

`build npm`.

---

`node build`.

---

`run build`.

#### --answer--

`npm run build`.

### --question--

#### --text--

Що робить подвійний дефіс (`--`) під час запуску `npm run start -- 8000`?

#### --distractors--

Приглушує вивід скрипта.

---

Змушує npm запускати скрипт паралельно.

---

Запитує адміністративні права перед виконанням скрипта.

#### --answer--

Казує npm передати все після `--` як аргументи до базової команди.
