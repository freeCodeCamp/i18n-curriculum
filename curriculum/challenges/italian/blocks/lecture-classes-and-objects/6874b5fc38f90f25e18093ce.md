---
id: 6874b5fc38f90f25e18093ce
title: Cosa sono i metodi e gli attributi e come funzionano?
challengeType: 19
dashedName: what-are-methods-and-attributes-and-how-do-they-work
---

# --description--

Nell'ultima lezione, hai imparato cosa sono le classi e come fungono da modelli per creare oggetti.

Qui approfondiremo gli attributi e i metodi.

Diamo prima uno sguardo più da vicino agli attributi, poi ai metodi.

Gli attributi sono variabili che appartengono a un oggetto, quindi contengono dati. Esistono due tipi di attributi: attributi di istanza e attributi di classe.

Gli attributi di istanza sono unici per ogni oggetto creato da una classe, e di solito li imposti con il metodo `__init__`. Gli attributi di classe, invece, appartengono alla classe stessa e sono condivisi da tutte le istanze di quella classe.

Per consultare un attributo, usi la notazione a punto.

Ecco esempi di entrambi gli attributi di istanza e di classe, e come consultarli dagli oggetti:

```python
class Dog:
    species = "French Bulldog" # Class attribute

    def __init__(self, name):
        self.name = name # Instance attribute

print(Dog.species) # French Bulldog

dog1 = Dog("Jack")
print(dog1.name)    # Jack
print(dog1.species) # French Bulldog

dog2 = Dog("Tom")
print(dog2.name)    # Tom
print(dog2.species) # French Bulldog
```

Nota che puoi consultare gli attributi di classe direttamente dalla classe stessa, ma devi prima creare un oggetto e passargli dati prima di poter consultare gli attributi di istanza.

Le auto sono un altro buon esempio, dato che tutte le auto hanno un modello e un colore:

```python
class Car:
    def __init__(self, color, model):
        self.color = color
        self.model = model

car_1 = Car("red", "Toyota Corolla")
car_2 = Car("green", "Lamborghini Revuelto")

print(car_1.model) # Toyota Corolla
print(car_2.model) # Lamborghini Revuelto

print(car_1.color) # red
print(car_2.color) # green
```

I metodi sono funzioni definite all'interno di una classe. Con essi, ogni oggetto definito da una classe può eseguire azioni che operano o modificano i propri dati. Anche i metodi si consultano con la notazione a punto.

Per esempio, i cani possono abbaiare. Quindi possiamo avere un metodo `bark` nella classe `Dog` come hai visto in una lezione precedente:

```python
class Dog:
   species = "French Bulldog"

   def __init__(self, name):
     self.name = name

   def bark(self):
       return f"{self.name} says woof woof!"

jack = Dog("Jack")
jill = Dog("Jill")

print(jack.bark()) # Jack says woof woof!
print(jill.bark()) # Jill says woof woof!
```

Una classe `Car` può anche avere un metodo `describe`:

```python
class Car:
    def __init__(self, color, model):
        self.color = color  # Instance attribute
        self.model = model  # Instance attribute

    def describe(self):
        return f"This car is a {self.color} {self.model}"

car_1 = Car("red", "Toyota Corolla")
car_2 = Car("green", "Lamborghini Revuelto")

print(car_1.describe()) # This car is a red Toyota Corolla
print(car_2.describe()) # This car is a green Lamborghini Revuelto
```

# --questions--

## --text--

Quali sono i due tipi di attributi in Python?

## --answers--

Attributi pubblici e privati

### --feedback--

Pensa ai due modi in cui puoi definire gli attributi.

---

Attributi locali e globali

### --feedback--

Pensa ai due modi in cui puoi definire gli attributi.

---

Attributi di istanza e attributi di classe

---

Attributi mutabili e immutabili

### --feedback--

Pensa ai due modi in cui puoi definire gli attributi.

## --video-solution--

3

## --text--

Cosa serve per consultare gli attributi di istanza?

## --answers--

Solo il nome della classe

### --feedback--

Pensa a come consulti gli attributi di classe rispetto a come consulti quelli di istanza.

---

Decoratori

### --feedback--

Pensa a come consulti gli attributi di classe rispetto a come consulti quelli di istanza.

---

Un'istanza o un oggetto della classe

---

Un metodo statico

### --feedback--

Pensa a come consulti gli attributi di classe rispetto a come consulti quelli di istanza.

## --video-solution--

3

## --text--

Come si definiscono e si consultano i metodi?

## --answers--

Come funzioni autonome esterne a una classe, consultate con parentesi

### --feedback--

Appartengono alla classe ma operano sui dati dell'oggetto.

---

Come variabili in una classe, consultate come attributi

### --feedback--

Appartengono alla classe ma operano sui dati dell'oggetto.

---

Con parole chiave speciali, chiamate automaticamente

### --feedback--

Appartengono alla classe ma operano sui dati dell'oggetto.

---

Sono definiti all'interno di una classe e consultati con la notazione a punto

## --video-solution--

4
