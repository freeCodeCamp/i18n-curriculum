---
id: 6554e0adc7bb193cbfdb36d5
title: Passo 73
challengeType: 20
dashedName: step-73
---

# --description--

Verifique se a função pode descriptografar a string de volta para o texto simples.

Declare outra variável chamada `decryption` e atribua a ela `vigenere(encryption, custom_key, -1)`.

# --hints--

Você deve chamar `vigenere` passando `encryption`, `custom_key` e `-1` como argumentos.

```js
({ test: () => assert.match(code, /vigenere\s*\(\s*encryption\s*,\s*custom_key\s*,\s*-\s*1\s*\)/) })
```

Você deve declarar uma variável `decryption`.

```js
({ test: () => assert(__userGlobals.has("decryption")) })
```

Sua variável `decryption` deve ter o valor de `vigenere(encryption, custom_key, -1)`.

```js
({ test: () => assert.match(code, /^decryption\s*=\s*vigenere\s*\(\s*encryption\s*,\s*custom_key\s*,\s*-\s*1\s*\)/m) })
```

# --seed--

## --seed-contents--

```py
text = 'Hello Zaira'
custom_key = 'python'

def vigenere(message, key, direction):
    key_index = 0
    alphabet = 'abcdefghijklmnopqrstuvwxyz'
    encrypted_text = ''

    for char in message.lower():
    
        # Append space to the message
        if char == ' ':
            encrypted_text += char
        else:        
            # Find the right key character to encode
            key_char = key[key_index % len(key)]
            key_index += 1

            # Define the offset and the encrypted letter
            offset = alphabet.index(key_char)
            index = alphabet.find(char)
            new_index = (index + offset*direction) % len(alphabet)
            encrypted_text += alphabet[new_index]
    
    return encrypted_text
--fcc-editable-region--    
encryption = vigenere(text, custom_key, 1)
print(encryption)

--fcc-editable-region--
```
