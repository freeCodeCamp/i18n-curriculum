---
id: 5900f3ab1000cf542c50febe
title: "المشكلة 63: عدد الأرقام القوية"
challengeType: 1
forumTopicId: 302175
dashedName: problem-63-powerful-digit-counts
---

# --description--

العدد المكون من 5 أرقام، 16807 = 7<sup>5</sup>، هو أيضًا قوة خامسة. وبالمثل، العدد المكون من 9 أرقام، 134217728 = 8<sup>9</sup>، هو قوة تاسعة.

أكمل الدالة بحيث تُرجع عدد الأعداد الصحيحة الموجبة التي طولها `n` وهي أيضًا قوة `n`.

# --hints--

يجب أن تُرجع `powerfulDigitCounts(1)` عددًا.

```js
assert(typeof powerfulDigitCounts(1) === 'number');
```

يجب أن تُرجع `powerfulDigitCounts(1)` القيمة `9`.

```js
assert.strictEqual(powerfulDigitCounts(1), 9);
```

يجب أن تُرجع `powerfulDigitCounts(2)` القيمة `6`.

```js
assert.strictEqual(powerfulDigitCounts(2), 6);
```

يجب أن تُرجع `powerfulDigitCounts(3)` القيمة `5`.

```js
assert.strictEqual(powerfulDigitCounts(3), 5);
```

يجب أن تُرجع `powerfulDigitCounts(4)` القيمة `4`.

```js
assert.strictEqual(powerfulDigitCounts(4), 4);
```

يجب أن تُرجع `powerfulDigitCounts(5)` القيمة `3`.

```js
assert.strictEqual(powerfulDigitCounts(5), 3);
```

يجب أن تُرجع `powerfulDigitCounts(6)` القيمة `3`.

```js
assert.strictEqual(powerfulDigitCounts(6), 3);
```

يجب أن تُرجع `powerfulDigitCounts(7)` القيمة `2`.

```js
assert.strictEqual(powerfulDigitCounts(7), 2);
```

يجب أن تُرجع `powerfulDigitCounts(8)` القيمة `2`.

```js
assert.strictEqual(powerfulDigitCounts(8), 2);
```

يجب أن تُرجع `powerfulDigitCounts(10)` القيمة `2`.

```js
assert.strictEqual(powerfulDigitCounts(10), 2);
```

يجب أن تُرجع `powerfulDigitCounts(21)` القيمة `1`.

```js
assert.strictEqual(powerfulDigitCounts(21), 1);
```

# --seed--

## --seed-contents--

```js
function powerfulDigitCounts(n) {

  return true;
}

powerfulDigitCounts(1);
```

# --solutions--

```js
function powerfulDigitCounts(n) {
  function countDigits(num) {
    let counter = 0;
    while (num > 0) {
      num = Math.floor(num / 10);
      counter++;
    }
    return counter;
  }

  let numbersCount = 0;

  let curNum = 1;
  while (curNum < 10) {
    let power = n;
    if (power === countDigits(curNum ** power)) {
      numbersCount++;
    }
    curNum++;
  }

  return numbersCount;
}
```
