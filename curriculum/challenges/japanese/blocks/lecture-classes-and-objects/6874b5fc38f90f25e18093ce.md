---
id: 6874b5fc38f90f25e18093ce
title: メソッドと属性とは何か、そしてそれらはどのように機能するのか？
challengeType: 19
dashedName: what-are-methods-and-attributes-and-how-do-they-work
---

# --description--

前のレッスンでは、クラスについて学び、それがオブジェクトを作成するための設計図として機能することを理解しました。

ここでは、属性とメソッドについてさらに深く掘り下げます。

まず属性を詳しく見てから、メソッドについて説明します。

属性はオブジェクトに属する変数で、データを保持します。属性にはインスタンス属性とクラス属性の2種類があります。

インスタンス属性はクラスから作成された各オブジェクトに固有で、通常は`__init__`メソッドで設定します。一方、クラス属性はクラス自体に属し、そのクラスのすべてのインスタンスで共有されます。

属性にアクセスするには、ドット記法を使います。

以下はインスタンス属性とクラス属性の例と、それらにオブジェクトからアクセスする方法です：

```python
class Dog:
    species = "French Bulldog" # Class attribute

    def __init__(self, name):
        self.name = name # Instance attribute

print(Dog.species) # French Bulldog

dog1 = Dog("Jack")
print(dog1.name)    # Jack
print(dog1.species) # French Bulldog

dog2 = Dog("Tom")
print(dog2.name)    # Tom
print(dog2.species) # French Bulldog
```

クラス属性はクラス自体から直接アクセスできますが、インスタンス属性にアクセスするにはオブジェクトを作成し、データを渡す必要があることに注意してください。

車は良い例で、すべての車にモデルと色があります：

```python
class Car:
    def __init__(self, color, model):
        self.color = color
        self.model = model

car_1 = Car("red", "Toyota Corolla")
car_2 = Car("green", "Lamborghini Revuelto")

print(car_1.model) # Toyota Corolla
print(car_2.model) # Lamborghini Revuelto

print(car_1.color) # red
print(car_2.color) # green
```

メソッドはクラス内で定義された関数です。これにより、クラスから定義された任意のオブジェクトは、自身のデータに対して操作や変更を行うアクションを実行できます。メソッドにもドット記法でアクセスします。

例えば、犬は吠えることができます。前のレッスンで見たように、`bark`クラスに`Dog`メソッドを持たせることができます：

```python
class Dog:
   species = "French Bulldog"

   def __init__(self, name):
     self.name = name

   def bark(self):
       return f"{self.name} says woof woof!"

jack = Dog("Jack")
jill = Dog("Jill")

print(jack.bark()) # Jack says woof woof!
print(jill.bark()) # Jill says woof woof!
```

`Car`クラスにも`describe`メソッドを持たせることができます：

```python
class Car:
    def __init__(self, color, model):
        self.color = color  # Instance attribute
        self.model = model  # Instance attribute

    def describe(self):
        return f"This car is a {self.color} {self.model}"

car_1 = Car("red", "Toyota Corolla")
car_2 = Car("green", "Lamborghini Revuelto")

print(car_1.describe()) # This car is a red Toyota Corolla
print(car_2.describe()) # This car is a green Lamborghini Revuelto
```

# --questions--

## --text--

Pythonにおける属性の2つの種類は何ですか？

## --answers--

パブリック属性とプライベート属性

### --feedback--

属性を定義する2つの方法について考えてみてください。

---

ローカル属性とグローバル属性

### --feedback--

属性を定義する2つの方法について考えてみてください。

---

インスタンス属性とクラス属性

---

ミュータブル属性とイミュータブル属性

### --feedback--

属性を定義する2つの方法について考えてみてください。

## --video-solution--

3

## --text--

インスタンス属性にアクセスするには何が必要ですか？

## --answers--

クラス名だけ

### --feedback--

クラス属性にアクセスする方法とインスタンス属性にアクセスする方法を考えてみてください。

---

デコレーター

### --feedback--

クラス属性にアクセスする方法とインスタンス属性にアクセスする方法を考えてみてください。

---

クラスのインスタンスまたはオブジェクト

---

スタティックメソッド

### --feedback--

クラス属性にアクセスする方法とインスタンス属性にアクセスする方法を考えてみてください。

## --video-solution--

3

## --text--

メソッドはどのように定義し、アクセスしますか？

## --answers--

クラスの外で独立した関数として定義し、括弧でアクセスする

### --feedback--

メソッドはクラスに属し、オブジェクトのデータに対して操作を行います。

---

クラス内の変数として定義し、属性のようにアクセスする

### --feedback--

メソッドはクラスに属し、オブジェクトのデータに対して操作を行います。

---

特別なキーワードで定義され、自動的に呼び出される

### --feedback--

メソッドはクラスに属し、オブジェクトのデータに対して操作を行います。

---

クラス内で定義され、ドット記法でアクセスする

## --video-solution--

4
