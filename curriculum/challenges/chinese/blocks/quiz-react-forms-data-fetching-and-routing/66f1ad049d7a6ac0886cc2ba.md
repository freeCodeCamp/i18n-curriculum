---
id: 66f1ad049d7a6ac0886cc2ba
title: React 表单、数据获取和路由测验
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项是受控 `input` 的示例？

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

以下哪项是不受控 `input` 的示例？

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

以下哪项不是在 React 中获取 `data` 的常用方法？

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

以下哪个 hook 通常与在 React 中获取数据关联？

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

什么是记忆化？

#### --distractors--

一种常用的算法，用于查找图中两个顶点之间的最短路径。

---

一种用于优化所有受控和非受控输入性能的特殊技术。

---

一种分治算法，用于递归地排序子数组并将它们合并在一起。

#### --answer--

一种优化技术，根据特定的参数缓存（记住）昂贵函数调用的结果。

### --question--

#### --text--

以下哪个钩子用于缓存计算值？

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

以下哪项使用了正确的 hook 来缓存函数引用？

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--

什么是依赖？

#### --distractors--

依赖是 React 应用中常用的测试库。

---

依赖是一种仅在服务器上运行的 React 组件类型。

---

依赖是 React 应用中用于将 React 代码编译成 JavaScript 的特殊编译器。 

#### --answer--

依赖是指应用中的一个组件或模块依赖另一个组件或模块才能正常运行。

### --question--

#### --text--

什么是包管理器？

#### --distractors--

用于更新安装在应用中的第三方包的工具。

---

一个用于管理仅在项目内创建的包的工具。

---

一个用于将依赖项从 `node_modules` 文件夹移出并放入特殊配置文件夹的工具。

#### --answer--

用于安装、更新和移除依赖项的工具。

### --question--

#### --text--

以下哪项不是 JavaScript 生态系统中常用的包管理器？

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

以下哪一个是包含有关你的项目元数据的关键配置文件？

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

以下哪一个是常用的 React 框架？

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

混音

### --question--

#### --text--

以下哪种方式是使用 React Router 为你的 React 应用添加路由的正确方法？

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

以下哪种方式是在 React Router 中创建动态路由的正确方法？

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

以下哪个钩子用于访问 URL 路径中的动态参数？

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

以下哪种方式是在 Next.js 中创建自定义请求处理器的正确方法？

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

什么是 prop drilling？

#### --distractors--

创建一个 reducer 的进程，该 reducer 指定状态应如何根据特定的 props 变化。

---

将 props 从子组件传递到父组件的进程。 

---

为 React 应用创建上下文提供者的过程，以及使用该提供者将属性从父组件传递到子组件。

#### --answer--

将 props 从父组件传递到深层嵌套的子组件的进程，即使某些子组件不需要这些 props。

### --question--

#### --text--

以下哪项不是 React 应用中常用的状态管理方案？

#### --distractors--

上下文 API

---

Zustand

---

Redux

#### --answer--

春季

### --question--

#### --text--

React Developer Tools 用于什么？

#### --distractors--

这些工具仅用于查看 React 应用中的无障碍问题。

---

这些工具用于为 React 组件添加额外的测试。

---

这些工具用于缓存应用内所有 React 组件。

#### --answer--

这些工具用于检查 React 组件并识别性能问题。

### --question--

#### --text--

什么是 React Server 组件？

#### --distractors--

这些是仅在服务器上管理上下文的组件。

---

这些是仅在服务器上渲染的备忘组件。

---

这些是仅在服务器上管理状态的组件。

#### --answer--

这些是仅在服务器上渲染的组件。

