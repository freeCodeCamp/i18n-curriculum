---
id: 67f41242431cbf3db8ca79c7
title: Тест з основ Python
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Яка з наведених функцій використовується для перевірки, чи відповідає змінна певному типу даних?

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

Який із наведених варіантів НЕ є формою конкатенації рядків?

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

Яка з наведених функцій використовується для повернення кількості символів у рядку?

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

Яким буде `result` у цьому прикладі?

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

Що відбувається, коли додати число з плаваючою крапкою і ціле число?

#### --distractors--

Результатом буде повідомлення про помилку.

---

Результатом буде `None`.

---

Результатом буде ціле число.

#### --answer--

Результатом буде число з плаваючою крапкою.

### --question--

#### --text--

Який із наведених способів є правильним для визначення функції?

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

Що буде виведено в консоль?

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

Яке з наведених тверджень є хибним щодо іменування змінних?

#### --distractors--

Імена змінних можуть починатися лише з літери або підкреслення (_), а не з числа.

---

Імена змінних можуть містити лише алфавітно-цифрові символи (a-z, A-Z, 0-9) та підкреслення (_).

---

Імена змінних не можуть бути одним із зарезервованих ключових слів Python, таких як `if`, `class` або `def`.

#### --answer--

Імена змінних повинні мати максимальну довжину 10 символів, інакше Python видасть помилку.

### --question--

#### --text--

Який із наведених типів даних НЕ підтримується в Python?

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

Яка з наведених функцій поверне новий рядок, у якому всі символи перетворені на верхній регістр?

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

Яка з наведених функцій використовується для отримання вхідних даних від користувача?

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

Яким буде результат для наступного коду?

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

Що робить метод `split()`?

#### --distractors--

Цей метод використовується для розбиття кортежу на список підрядків.

---

Цей метод використовується для розбиття числа з плаваючою крапкою на список підрядків.

---

Цей метод використовується для розбиття словника на список підрядків.

#### --answer--

Цей метод використовується для розбиття рядка на список підрядків.

### --question--

#### --text--

Що буде виведено в консоль?

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

Яка з наведених функцій використовується для створення таблиці відповідностей символів для перекладу?

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

Яким буде результат для наступного коду?

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

Що поверне метод `find()`, якщо в рядку не знайдено входжень підрядка?

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

Яка з наведених функцій використовується для підрахунку кількості входжень підрядка в рядок?

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

Що робить оператор цілочисельного ділення у Python?

#### --distractors--

Цей оператор використовується для множення двох чисел і округлення результату вгору до найближчого цілого числа.

---

Цей оператор використовується для перетворення числа з плаваючою крапкою на ціле число.

---

Цей оператор використовується для піднесення числа до степеня іншого числа.

#### --answer--

Цей оператор використовується для ділення двох чисел і округлення результату вниз до найближчого цілого числа.

### --question--

#### --text--

Яка з наведених функцій використовується для округлення числа до найближчого цілого?

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`
