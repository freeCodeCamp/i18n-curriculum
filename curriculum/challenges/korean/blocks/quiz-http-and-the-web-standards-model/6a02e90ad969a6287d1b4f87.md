---
id: 6a02e90ad969a6287d1b4f87
title: HTTP와 웹 표준 모델 퀴즈
challengeType: 8
dashedName: quiz-http-and-the-web-standards-model
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

DNS는 무엇의 약자이며, 주요 기능은 무엇인가요?

#### --distractors--

Data Naming System, 네트워크로 전송하기 전에 도메인 이름을 암호화합니다.

---

Domain Node Server, 브라우저가 다운로드할 수 있도록 웹사이트 파일을 저장합니다.

---

Dynamic Network Service, 장치가 연결될 때 IP 주소를 할당합니다.

#### --answer--

Domain Name System, 사람이 읽을 수 있는 도메인 이름을 IP 주소로 변환합니다.

### --question--

#### --text--

DNS 해석 과정에서 루트 서버, TLD 서버, 권한 있는 이름 서버를 반복적으로 조회하는 구성 요소는 무엇인가요?

#### --distractors--

권한 있는 이름 서버

---

TLD 서버

---

루트 서버

#### --answer--

재귀 해석기

### --question--

#### --text--

TCP가 인터넷 프로토콜(IP)만으로는 하지 않는 기능은 무엇인가요?

#### --distractors--

IP 주소를 사용해 출발지에서 목적지로 패킷을 라우팅합니다.

---

네트워크로 전송하기 전에 데이터를 암호화합니다.

---

도메인 이름을 IP 주소에 할당합니다.

#### --answer--

세그먼트에 번호를 매기고 손실된 패킷 재전송을 요청하여 신뢰할 수 있고 순서가 보장된 전달을 보장합니다.

### --question--

#### --text--

서버의 기존 리소스를 부분적으로 업데이트하려면 어떤 HTTP 메서드를 사용해야 하나요?

#### --distractors--

`PUT`

---

`POST`

---

`DELETE`

#### --answer--

`PATCH`

### --question--

#### --text--

JSON 응답을 제공할 때 올바른 `Content-Type` 헤더는 무엇인가요?

#### --distractors--

`text/json`

---

`text/plain`

---

`application/xml`

#### --answer--

`application/json`

### --question--

#### --text--

요청한 리소스가 새 URL로 영구 이동했음을 나타내는 HTTP 상태 코드는 무엇인가요?

#### --distractors--

`302`

---

`304`

---

`404`

#### --answer--

`301`

### --question--

#### --text--

`401 Unauthorized` 응답과 `403 Forbidden` 응답의 차이는 무엇인가요?

#### --distractors--

`401`는 서버가 다운된 것이고 `403`는 리소스가 존재하지 않는 것입니다.

---

`401`는 클라이언트 오류이고 `403`는 서버 오류입니다.

---

`401`는 리소스가 영구 이동한 것이고 `403`는 임시 이동한 것입니다.

#### --answer--

`401`는 자격 증명이 필요함을 의미하고 `403`는 클라이언트가 유효한 자격 증명은 있지만 리소스에 대한 권한이 없음을 의미합니다.

### --question--

#### --text--

HTTP 요청의 어느 부분에 메서드, 경로, HTTP 버전이 포함되나요?

#### --distractors--

응답 본문

---

요청 본문

---

요청 헤더

#### --answer--

요청 라인

### --question--

#### --text--

`304 Not Modified` 상태 코드는 클라이언트에게 무엇을 알려주나요?

#### --distractors--

리소스가 서버에서 삭제되었습니다.

---

요청이 잘못되어 다시 보내야 합니다.

---

서버가 일시적으로 사용할 수 없습니다.

#### --answer--

리소스가 마지막 요청 이후 변경되지 않아 클라이언트가 캐시된 버전을 사용해야 합니다.

### --question--

#### --text--

HTTP 요청-응답 주기에서 서버가 요청을 처리한 직후에 무슨 일이 발생하나요?

#### --distractors--

클라이언트가 수신 확인을 위해 두 번째 요청을 보냅니다.

---

DNS 해석기가 응답을 캐시합니다.

---

클라이언트가 새 TCP 연결을 시작합니다.

#### --answer--

서버가 상태 코드, 헤더, 선택적 본문을 포함한 응답을 준비합니다.

### --question--

#### --text--

HTML 속성 중 폼이 제출될 때 데이터를 어디로 보내는지 지정하는 것은 무엇인가요?

#### --distractors--

`method`

---

`name`

---

`type`

#### --answer--

`action`

### --question--

#### --text--

클라이언트 측 유효성 검사가 이미 있어도 서버 측 유효성 검사를 항상 수행해야 하는 이유는 무엇인가요?

#### --distractors--

클라이언트 측 유효성 검사는 최신 브라우저에서 지원되지 않습니다.

---

서버 측 유효성 검사는 파일 업로드에만 필요합니다.

---

클라이언트 측 유효성 검사가 서버 측보다 더 빠르게 실행됩니다.

#### --answer--

클라이언트 측 유효성 검사는 우회될 수 있으므로 보안을 위해 서버 측 유효성 검사가 필요합니다.

### --question--

#### --text--

HTML과 DOM을 지속적으로 업데이트되는 라이브 표준으로 유지하는 표준 기구는 어디인가요?

#### --distractors--

W3C

---

ECMA International

---

Khronos Group

#### --answer--

WHATWG

### --question--

#### --text--

TC39는 무엇이며, 웹 표준 생태계에서 어떤 역할을 하나요?

#### --distractors--

CSS 사양을 담당하는 W3C 작업 그룹입니다.

---

Chrome에 기능을 탑재할지 결정하는 브라우저 벤더 컨소시엄입니다.

---

WebGL 표준을 관리하는 Khronos Group 위원회입니다.

#### --answer--

ECMA International 내 기술 위원회로, 다섯 단계 제안 과정을 통해 JavaScript 언어 기능을 발전시킵니다.

### --question--

#### --text--

웹 표준 수명 주기 중 기능이 공개 토론, 사용 사례 수집, 경쟁 설계 방식을 통해 스트레스 테스트되는 단계는 언제인가요?

#### --distractors--

사양 테스트

---

최종화 테스트

---

상호운용성 테스트

#### --answer--

인큐베이션

### --question--

#### --text--

표준 프로세스 중 W3C 웹 플랫폼 테스트 스위트의 목적은 무엇인가요?

#### --distractors--

마케팅 목적으로 브라우저 성능 순위를 매기기 위해서입니다.

---

문서화된 사양을 대체하기 위해서입니다.

---

개발자가 새 기능 제안을 제출할 수 있도록 하기 위해서입니다.

#### --answer--

모든 브라우저가 기능을 동일하게 구현하는지 검증하기 위해서입니다.

### --question--

#### --text--

웹 표준의 어떤 원칙이 사양에 맞게 구축된 페이지가 Chrome, Firefox, Safari에서 동일하게 동작하도록 보장하나요?

#### --distractors--

하위 호환성

---

점진적 향상

---

개방성

#### --answer--

상호운용성

### --question--

#### --text--

점진적 향상 원칙은 웹 기능을 설계할 때 무엇을 요구하나요?

#### --distractors--

기능은 최신 브라우저 버전만 대상으로 해야 합니다.

---

기능은 CSS나 HTML보다 먼저 JavaScript로 구현해야 합니다.

---

기능은 어떤 브라우저도 구현하기 전에 최종화되어야 합니다.

#### --answer--

기능은 모든 사용자에게 먼저 작동해야 하며, 더 성능 좋은 브라우저나 장치 사용자에게 점차 향상되어야 합니다.

### --question--

#### --text--

새 표준을 도입할 때 웹이 거의 절대적인 하위 호환성을 유지하는 이유는 무엇인가요?

#### --distractors--

새 표준은 항상 이전 것을 대체하므로 오래된 페이지가 자동으로 업데이트됩니다.

---

브라우저 벤더는 법적으로 게시된 모든 페이지를 지원해야 합니다.

---

오래된 페이지는 별도의 네트워크에서 보관 및 제공됩니다.

#### --answer--

수년 전에 만들어진 페이지가 최신 브라우저에서도 제대로 작동하도록 하기 위해서입니다.

### --question--

#### --text--

서버 측 오류에 사용되는 HTTP 상태 코드 범주는 무엇인가요?

#### --distractors--

`2xx`

---

`3xx`

---

`4xx`

#### --answer--

`5xx`
