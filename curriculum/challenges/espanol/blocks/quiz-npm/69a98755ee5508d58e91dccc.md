---
id: 69a98755ee5508d58e91dccc
title: Cuestionario de NPM
challengeType: 8
dashedName: quiz-npm
---

# --description--

Para aprobar el cuestionario, debe responder correctamente al menos 18 de las 20 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Qué proporciona npm para los desarrolladores?

#### --distractors--

Una plataforma seleccionada que solo aloja recursos de diseño como fuentes e íconos.

---

Un servicio de hosting que despliega automáticamente tu proyecto en la nube.

---

Una solución de base de datos que gestiona los registros de usuario para tus apps.

#### --answer--

Un registro de paquetes de software preconstruidos que resuelve problemas comunes.

### --question--

#### --text--

¿Cuál de los siguientes no es una de las tres partes a las que se refiere npm?

#### --distractors--

El sitio web npmjs.com.

---

El registro de paquetes donde se almacenan las bibliotecas.

---

La herramienta de línea de comandos para instalar y gestionar paquetes.

#### --answer--

El entorno de ejecución de Node.js.

### --question--

#### --text--

¿Qué hace el comando `npm install`?

#### --distractors--

Estructura un proyecto nuevo de Node.js a partir de una plantilla.

---

Ejecuta tu aplicación Node.js en un servidor de desarrollo local.

---

Elimina paquetes que ya no son necesarios en tu proyecto.

#### --answer--

Agrega paquetes preconstruidos a tu proyecto para tareas comunes.

### --question--

#### --text--

¿Qué archivo almacena metadatos como el nombre del proyecto, versión, autor y dependencias para un proyecto de Node.js?

#### --distractors--

`README.md`.

---

`node_modules/package.json`.

---

`package-lock.json`.

#### --answer--

`package.json`.

### --question--

#### --text--

¿Cómo puedes crear interactivamente un archivo `package.json`?

#### --distractors--

Ejecuta `npm install` sin especificar un nombre de paquete.

---

Descarga una plantilla desde la página principal del registro npm.

---

Copia el archivo de otro proyecto y cámbiale el nombre.

#### --answer--

Ejecuta el comando `npm init`.

### --question--

#### --text--

¿Cómo creas un archivo `package.json` con valores predeterminados sin responder a las indicaciones?

#### --distractors--

Ejecuta `npm init --interactive`.

---

Crea un archivo BLANK llamado `package.json` y déjalo vacío.

---

Usa `npm init` y elimina cada aviso después de abrir el editor.

#### --answer--

Ejecuta `npm init -y` (o `npm init --yes`) para aceptar todos los valores predeterminados automáticamente.

### --question--

#### --text--

¿Dónde lista npm los paquetes que solo se necesitan durante el desarrollo, como linters o ejecutores de pruebas?

#### --distractors--

Dentro de la sección `"dependencies"`.

---

Dentro de la sección `"scripts"` bajo comandos personalizados.

---

Dentro de la carpeta `node_modules` misma.

#### --answer--

Dentro de la sección `"devDependencies"` de `package.json`.

### --question--

#### --text--

¿Qué comando instala `nodemon` como una dependencia solo para desarrollo?

#### --distractors--

`npm uninstall nodemon`.

---

`npm save nodemon --dev`.

---

`npm install nodemon`.

#### --answer--

`npm install nodemon -D`.

### --question--

#### --text--

¿Qué carpeta y archivo se agregan a la raíz de tu proyecto después de instalar una dependencia?

#### --distractors--

carpeta `public` e `index.html`.

---

carpeta `dist` y `webpack.config.js`.

---

carpeta `src` y `README.md`.

#### --answer--

Carpeta `node_modules` y archivo `package-lock.json`.

### --question--

#### --text--

¿Cómo ayuda `package-lock.json` a mantener las compilaciones consistentes?

#### --distractors--

Eliminando las dependencias no usadas antes de cada instalación.

---

Al almacenar solo los números de versión mayor de los paquetes.

---

Actualizando automáticamente los paquetes a sus últimas versiones.

#### --answer--

Al congelar las versiones exactas de cada dependencia que se instalaron.

### --question--

#### --text--

¿Por qué deberías hacer commit de `package-lock.json` en tu repositorio Git?

#### --distractors--

Para hacer que npm se ejecute más rápido en la máquina de un desarrollador.

---

Para actualizar automáticamente las dependencias obsoletas.

---

Para reducir el tamaño del repositorio.

#### --answer--

Así que todo el equipo instala las mismas versiones de dependencias y configuración.

### --question--

#### --text--

¿Cómo instalas una versión específica de un paquete, como `express@4.21.2`?

#### --distractors--

`npm update express@4.21.2`.

---

`npm get express@4.21.2`.

---

`npm use express@4.21.2`.

#### --answer--

`npm install express@4.21.2`.

### --question--

#### --text--

¿Qué permite el símbolo caret (`^`) al especificar versiones de dependencias?

#### --distractors--

Bloquea la dependencia a una versión exacta de parche.

---

Permite actualizaciones solo a la versión mayor.

---

Excluye todas las versiones anteriores a la especificada.

#### --answer--

Permite versiones menores y parches más recientes, pero bloquea nuevas versiones mayores.

### --question--

#### --text--

¿Qué significa el símbolo tilde (`~`) en un rango de versión como `~1.2.3`?

#### --distractors--

Permite actualizaciones tanto a versiones mayores como menores.

---

Instala la versión principal más reciente sin importar el rango.

---

Evita que npm instale cualquier actualización.

#### --answer--

Permite actualizaciones solo a la versión patch mientras mantiene fija la versión minor.

### --question--

#### --text--

¿Qué tipo de cambios provocan un aumento de versión mayor en el versionado semántico?

#### --distractors--

Pequeños ajustes estilísticos en comentarios y formato.

---

Pequeñas correcciones de errores que no cambian el comportamiento.

---

Características opcionales que no rompen el código existente.

#### --answer--

Cambios críticos que pueden hacer que el código existente deje de funcionar.

### --question--

#### --text--

¿Qué comando lista las dependencias que tienen versiones publicadas más recientes?

#### --distractors--

`npm check`.

---

`npm list`.

---

`npm refresh`.

#### --answer--

`npm outdated`.

### --question--

#### --text--

Yarn, PNPM y Bun se mencionan como alternativas a npm. ¿Qué tipo de herramientas son?

#### --distractors--

Sitios web de registro donde publicas o descargas paquetes npm.

---

Servicios de base de datos que almacenan datos de usuario para paquetes npm.

---

Plataformas de alojamiento en la nube que despliegan automáticamente proyectos npm.

#### --answer--

Gestores de paquetes alternativos.

### --question--

#### --text--

¿Dónde se declaran los scripts personalizados de npm en un proyecto?

#### --distractors--

Dentro del archivo `.npmrc`.

---

Dentro de la sección `"dependencies"` de `package.json`.

---

En un archivo `scripts.json` separado en la raíz.

#### --answer--

Dentro del objeto `"scripts"` de `package.json`.

### --question--

#### --text--

¿Cómo ejecutas un script personalizado de npm llamado `build`?

#### --distractors--

`build npm`

---

`node build`.

---

`run build`.

#### --answer--

`npm run build`.

### --question--

#### --text--

¿Qué hace el doble guion (`--`) al ejecutar `npm run start -- 8000`?

#### --distractors--

Silencia la salida del script.

---

Esto fuerza a npm a ejecutar el script en paralelo.

---

Solicita privilegios administrativos antes de ejecutar el script.

#### --answer--

Indica a npm que reenvíe todo lo que sigue después de `--` como argumentos al comando subyacente.
