---
id: 5900f52c1000cf542c51003e
title: '问题 447：回缩 C'
challengeType: 1
forumTopicId: 302119
dashedName: problem-447-retractions-c
---

# --description--

对于每个整数 $n > 1$，函数族 $f_{n, a, b}$ 被定义为：

$f_{n, a, b}(x) ≡ ax + b\bmod n$，其中 $a, b, x$ 为整数且 $0 \lt a \lt n$，$0 \le b \lt n$，$0 \le x \lt n$。

如果对于每个 $0 \le x \lt n$，都有 $f_{n, a, b}(f_{n, a, b}(x)) \equiv f_{n, a, b}(x)\bmod n$，我们将称 $f_{n, a, b}$ 为一个收缩映射。

令 $R(n)$ 为 $n$ 的收缩数。

$F(N) = \displaystyle\sum_{n = 2}^N R(n)$。

$F({10}^7) ≡ 638\\,042\\,271\bmod 1\\,000\\,000\\,007$。

查找 $F({10}^{14})$。将你的答案对 $1\,000\,000\,007$ 取模。

# --hints--

`retractionsC()` 应该返回 `530553372`。

```js
assert.strictEqual(retractionsC(), 530553372);
```

# --seed--

## --seed-contents--

```js
function retractionsC() {

  return true;
}

retractionsC();
```

# --solutions--

```js
// solution required
```
