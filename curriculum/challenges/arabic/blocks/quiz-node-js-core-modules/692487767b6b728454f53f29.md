---
id: 692487767b6b728454f53f29
title: اختبار وحدات NodeJS الأساسية
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي طريقة من `fs` تضيف محتوى إلى ملف موجود دون استبداله؟

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

ماذا يحدث عند استخدام الطرق المتزامنة من وحدة `fs`؟

#### --distractors--

تحذف الملفات الموجودة تلقائيًا.

---

تعمل أسرع من الطرق غير المتزامنة في جميع الحالات.

---

تعيد وعودًا افتراضيًا.

#### --answer--

توقف البرنامج حتى تنتهي العملية.

### --question--

#### --text--

ما وظيفة المعلمة `utf8` في `fs.readFile()`؟

#### --distractors--

تمنع رمي الأخطاء.

---

تغير امتداد الملف.

---

تضغط الملفات الكبيرة.

#### --answer--

تخبر Node بإرجاع سلسلة نصية قابلة للقراءة بدلًا من مخزن مؤقت.

### --question--

#### --text--

أي طريقة من `Buffer` تنشئ مخزنًا مؤقتًا جديدًا مملوءًا بالأصفار؟

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

ماذا تفعل `Buffer.toString()`؟

#### --distractors--

تحول المخزن المؤقت إلى شفرة ثنائية.

---

تمسح محتويات المخزن المؤقت.

---

تعرض طول المخزن المؤقت بالبايت.

#### --answer--

تحول محتويات المخزن المؤقت إلى سلسلة نصية قابلة للقراءة.

### --question--

#### --text--

أي ميزة من `crypto` تتطلب مفتاحًا خاصًا لتوليد توقيع؟

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

لماذا يجب تجنب كتابة خوارزميات التشفير الخاصة بك مباشرة؟

#### --distractors--

هذا غير قانوني في تطبيقات Node.js.

---

Node.js لا يدعم التشفير.

---

JavaScript لا يمكنها حساب التجزئات.

#### --answer--

يمكن أن تكون غير آمنة بدون مكتبات مختبرة جيدًا.

### --question--

#### --text--

أي طريقة من `crypto` يُنصح بها لتوليد رموز عشوائية آمنة بدلًا من `Math.random()`؟

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

أي طريقة من وحدة OS تعيد بنية وحدة المعالجة المركزية؟

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

ماذا تعيد `os.userInfo()`؟

#### --distractors--

اسم المستخدم فقط.

---

عنوان IP للجهاز فقط.

---

معرف المستخدم المسجل الدخول فقط.

#### --answer--

معلومات عن المستخدم الحالي للنظام.

### --question--

#### --text--

أي طريقة من `path` تربط المقاطع وتطبع الشرطات الزائدة؟

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

أي طريقة من `path` تنتج مسارًا مطلقًا بناءً على دليل العمل الحالي؟

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

ماذا يمثل المتغير العام `__dirname`؟

#### --distractors--

اسم المستخدم الذي يشغل Node.

---

امتداد الملف الحالي.

---

مسار مجلد نسبي يبدأ بـ `./`.

#### --answer--

المسار المطلق للدليل الذي يحتوي الملف الحالي.

### --question--

#### --text--

أي خاصية من process تحتوي على متغيرات البيئة مثل `NODE_ENV`؟

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

ماذا يفعل حدث `process.on("exit")`؟

#### --distractors--

يعيد تشغيل تطبيق Node.

---

يرسل إشارة `exit` إلى العمليات الفرعية.

---

يمسح قائمة انتظار حلقة الأحداث.

#### --answer--

ينفذ شفرة قبل انتهاء عملية Node مباشرة.

### --question--

#### --text--

أي نوع من التدفقات يمكنه القراءة والكتابة لكنه لا يحول البيانات؟

#### --distractors--

تدفق قابل للقراءة

---

تدفق تحويل

---

تدفق قابل للكتابة

#### --answer--

تدفق مزدوج

### --question--

#### --text--

ما المطلوب لتنفيذ قراءة وكتابة الملفات الكبيرة بكفاءة؟

#### --distractors--

استخدام `Buffer.alloc()` فقط

---

استيراد وحدة `crypto`

---

حفظ الملف كاملًا في الذاكرة أولًا

#### --answer--

استخدام التدفقات لمعالجة البيانات على أجزاء

### --question--

#### --text--

أي طريقة تربط تدفقًا قابلًا للقراءة بتدفق قابل للكتابة؟

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

أي حدث في التدفق يشير إلى عدم وجود المزيد من البيانات للكتابة؟

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

ماذا يُسجل إذا طبعت جزءًا من تدفق قابل للقراءة بدون `toString()`؟

#### --distractors--

محتوى نص عادي

---

معرف العملية PID

---

اسم نظام التشغيل

#### --answer--

كائن مخزن مؤقت يحتوي بيانات ثنائية
