---
id: 66edd3403d7077eece6dc4b6
title: 使用 JavaScript 的表单验证测验
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

使用哪个 `method` 来查看 `input` 元素的值是否满足验证规则？

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

如果 `checkValidity()` 方法查找到无效的 `input` 会发生什么？

#### --distractors--

该 form 会自动提交。

---

没有任何反应。

---

`input` 值已被清除。

#### --answer--

浏览器显示验证消息。

### --question--

#### --text--

哪个方法用于自定义验证失败时显示的消息？

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

当表单提交时，必填字段的默认验证行为是什么？

#### --distractors--

即使必填字段为空，表单也会被提交。

---

该形式提交但给出警告。

---

该形式会重置 `input` 值。

#### --answer--

如果必填字段为空，浏览器会停止表单提交。

### --question--

#### --text--

哪些操作不会导致对 HTML `form` 进行误差 `Check`？

#### --distractors--

更改 `input` 值。

---

提交该 `form`。

---

调用 `reportValidity()`。

#### --answer--

点击重置按钮。

### --question--

#### --text--

当所有表单输入有效时，`checkValidity()` 返回什么？

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

`checkValidity()` 方法何时停止表单提交？

#### --distractors--

当 `form` 没有提交按钮时。

---

当 `action` 形式为空时。

---

当 `form` 方法是 POST 时。

#### --answer--

当表单至少有一个无效的 `input`（且阻止了表单提交的默认行为）时。

### --question--

#### --text--

如果调用了 `checkValidity()`，但某个字段未通过验证，会发生什么？

#### --distractors--

该表单提交时无误差。

---

`input` 值已被清除。

---

该形式会立即关闭。

#### --answer--

无效的字段会被高亮显示，并且会出现一条消息。

### --question--

#### --text--

哪个操作会使 `checkValidity()` 运行？

#### --distractors--

重置该形式。

---

更改 `input` 值。

---

点击锚点标签。

#### --answer--

手动提交该 `form`。

### --question--

#### --text--

在提交表单之前调用 `checkValidity()` 的目的是什么？

#### --distractors--

自动提交表单。

---

重新装载页面。

---

清除所有 `input` 字段。

#### --answer--

查看所有 `input` 元素是否满足其指定的约束。

### --question--

#### --text--

`e.preventDefault()` 的主要目的是什么？

#### --distractors--

重新装载该形式。

---

清除表单数据。

---

重置表单值。

#### --answer--

停止事件的默认操作。

### --question--

#### --text--

当在表单的提交事件上使用时，`e.preventDefault()` 会做什么？

#### --distractors--

它提交该 `form`。

---

它清除所有表单字段。

---

它会暂时禁用该 `form`。

#### --answer--

它防止表单被提交。

### --question--

#### --text--

在什么场景下 `e.preventDefault()` 最有用？

#### --distractors--

处理提交 `form` 的按钮点击时。

---

关闭模态对话框时。

---

打开新建页面时。

#### --answer--

提交 `form` 并防止页面重新加载时。

### --question--

#### --text--

如果你忘记在表单提交 `event` 上调用 `e.preventDefault()` 会发生什么？

#### --distractors--

所有的输入字段都设置为它们的默认值。

---

该形式未提交。

---

表单 `data` 丢失。

#### --answer--

该形式提交并刷新页面。

### --question--

#### --text--

哪些默认行为可以通过 `e.preventDefault()` 来阻止？

#### --distractors--

编辑一个 `form`。

---

渲染该形式。

---

重新装载该形式。

#### --answer--

提交 `form` 导致的默认行为。

### --question--

#### --text--

是什么触发了该 `form` 的提交 `event`？

#### --distractors--

当一个输入字段发生变化时。

---

当 `form` 被重置时。

---

当 `form` 的 `action` 被清除时。

#### --answer--

当点击表单的提交按钮或按下 Enter 键时。

### --question--

#### --text--

你通常在哪里处理 JavaScript 中的 submit 事件？

#### --distractors--

在一个文本输入元素上。

---

在表单中的任何字段上。

---

在形式外的按钮元素上。

#### --answer--

在表单元素本身。

### --question--

#### --text--

`submit` 事件默认会做什么？

#### --distractors--

它验证了 `form` 但没有提交它。

---

它清除表单字段。

---

它取消了表单提交。

#### --answer--

它将表单数据发送到表单的 action 中指定的服务器。

### --question--

#### --text--

在 JavaScript 中，当表单提交时，你应该监听哪个事件来处理表单数据？

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

HTML 表单中 `method` 属性的默认值是什么？

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
