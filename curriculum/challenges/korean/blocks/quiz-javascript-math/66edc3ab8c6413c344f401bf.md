---
id: 66edc3ab8c6413c344f401bf
title: JavaScript 수학 퀴즈
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제에 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

`NaN`는 무엇을 나타내나요?

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

다음 중 JavaScript에서 산술 연산자가 아닌 것은 무엇인가요?

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

JavaScript에서 0으로 나누기를 시도하면 어떻게 되나요?

#### --distractors--

JavaScript가 0을 출력합니다.

---

아무 일도 일어나지 않습니다.

---

JavaScript가 오류를 발생시킵니다.

#### --answer--

JavaScript가 `Infinity`을 출력합니다.

### --question--

#### --text--

다음 중 거듭제곱 연산자를 올바르게 사용한 것은 무엇인가요?

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

JavaScript에서 변수를 감소시킬 때 어떤 연산자를 사용해야 하나요?

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

다음 중 비트 연산자가 아닌 것은 무엇인가요?

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

나머지를 구하는 올바른 복합 연산자는 무엇인가요?

#### --distractors--

`//=`

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

`==` 연산자와 `===` 연산자의 차이는 무엇인가요?

#### --distractors--

차이가 없습니다. 비교 결과는 항상 같습니다.

---

`==` 연산자는 데이터 타입이 같은지 확인하는 데 사용되고 `===` 연산자는 값이 같은지 확인하는 데 사용됩니다.

---

`==` 연산자는 원시 타입에만 사용 가능하고 `===` 연산자는 객체에만 사용됩니다.

#### --answer--

`==` 연산자는 값만 비교하고 `===` 연산자는 값과 타입을 모두 비교합니다.

### --question--

#### --text--

단항 더하기 연산자(`+`)는 무엇에 사용되나요?

#### --distractors--

값을 1만큼 감소시키는 데 사용됩니다.

---

연산의 나머지를 구하는 데 사용됩니다.

---

숫자의 이진 표현을 반전시키는 데 사용됩니다.

#### --answer--

피연산자를 숫자로 변환하는 데 사용됩니다.

### --question--

#### --text--

다음 `Math` 메서드 중 값을 가장 가까운 정수로 반올림하는 것은 무엇인가요?

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

`Math.sqrt()` 메서드는 무엇을 하나요?

#### --distractors--

이 메서드는 임의의 부동 소수점 숫자를 반환합니다.

---

이 메서드는 숫자의 절댓값을 반환합니다.

---

이 메서드는 숫자의 세제곱을 반환합니다.

#### --answer--

이 메서드는 숫자의 제곱근을 반환합니다.

### --question--

#### --text--

다음 중 `Math` 객체에 유효하지 않은 메서드는 무엇인가요?

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

다음 중 `false`을 반환하는 것은 무엇인가요?

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

`parseFloat()` 메서드는 무엇을 하나요?

#### --distractors--

문자열 인수를 파싱하고 비트 연산을 수행합니다.

---

문자열 인수를 파싱하고 `false`를 반환합니다.

---

문자열 인수를 파싱하고 정수를 반환합니다.

#### --answer--

문자열 인수를 파싱하고 부동 소수점 숫자를 반환합니다.

### --question--

#### --text--

콘솔에 무엇이 출력될까요?

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

다음 코드의 출력 결과는 무엇인가요?

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

연산자 우선순위란 무엇인가요?

#### --distractors--

연산자 우선순위는 왼쪽 값이 오른쪽 값보다 작은지 확인하는 데 사용됩니다.

---

연산자 우선순위는 변수를 지정한 수만큼 거듭제곱하고 결과를 변수에 다시 할당하는 데 사용됩니다.

---

연산자 우선순위는 값을 1만큼 증가시키는 데 사용됩니다.

#### --answer--

연산자 우선순위는 표현식에서 연산이 평가되는 순서를 결정합니다.

### --question--

#### --text--

다음 중 비교 연산자가 아닌 것은 무엇인가요?

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

단항 부정 연산자(`-`)는 무엇을 하나요?

#### --distractors--

피연산자의 불리언 값을 반전시킵니다.

---

값을 1만큼 감소시킵니다.

---

값을 1만큼 증가시킵니다.

#### --answer--

피연산자를 부정합니다.

### --question--

#### --text--

다음 중 null 병합 연산자를 사용하는 예는 무엇인가요?

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```
