---
id: 67f41242431cbf3db8ca79c7
title: Python 基礎測驗
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

下列哪一個函式用來檢查變數是否符合特定的資料型別？

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

以下哪一項不是字串串接的形式？

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

下列哪一個函式用來傳回字串中的字元數？

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

在這個範例中，`result` 會是什麼？

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

當你將浮點數和整數相加時會發生什麼？

#### --distractors--

結果將會是一則錯誤訊息。

---

結果將會是 `None`。

---

結果將會是一個整數。

#### --answer--

結果將會是浮點數。

### --question--

#### --text--

以下哪一種是定義函式（程式）的正確方式？

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

會列印到主控台的是什麼？

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

在命名變數時，下列哪一個述語是錯誤的？

#### --distractors--

變數名稱只能以字母或底線（_）開頭，不能以數字開頭。

---

變數名稱只能包含英數字元（a-z、A-Z、0-9）和底線（_）。

---

變數名稱不能是 Python 的保留關鍵字之一，例如 `if`、`class` 或 `def`。

#### --answer--

變數名稱的最大長度必須為 10 個字元，否則 Python 會丟出錯誤。

### --question--

#### --text--

以下哪一項不是 Python 中支援的資料型別？

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

以下哪一項會傳回一個所有字元都轉成大寫的新字串？

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

以下哪一個函式（程式）用於從使用者取得輸入？

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

以下 `code` 的結果會是什麼？

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

`split()` 方法做什麼？

#### --distractors--

此方法用於將元組拆分成子字串的列表。

---

此方法用於將浮點數拆分成子字串的列表。

---

此方法用於將字典拆分成字串的列表。

#### --answer--

此方法用於將字串拆分成子字串的列表。

### --question--

#### --text--

以下內容將列印到主控台什麼？

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

以下哪一個函式（程式）用於創建 1 對 1 字元對應的翻譯表？

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

以下 `code` 的結果會是什麼？

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

如果在字串中找不到子字串出現，`find()` 方法會傳回什麼？

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

以下哪一個函式（程式）用來計算子字串在字串中出現的次數？

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

在 Python 中，floor division 會執行除法並將結果向下取整。

#### --distractors--

此運算子用於將兩個數字相乘並將結果向上取整到最接近的整數。

---

此運算子用於將浮點數轉換為整數。

---

此運算子用於將一個數字引發到另一個數字的次方。

#### --answer--

此運算子用於除以兩個數字並將結果向下取整到最接近的整數。

### --question--

#### --text--

以下哪一個函式（程式）用於將數字四捨五入到最接近的整數？

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`

