---
id: 69a98755ee5508d58e91dccc
title: اختبار npm
challengeType: 8
dashedName: quiz-npm
---

# --description--

لتجاوز الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

ماذا يوفر npm للمطورين؟

#### --distractors--

منصة منظمة تستضيف فقط أصول التصميم مثل الخطوط والرموز.

---

خدمة استضافة تنشر مشروعك تلقائيًا على السحابة.

---

حل قاعدة بيانات يدير سجلات المستخدمين لتطبيقاتك.

#### --answer--

فهرس لحزم برمجيات مُعدة مسبقًا تحل مشاكل شائعة.

### --question--

#### --text--

أي مما يلي ليس من الأجزاء الثلاثة التي يشير إليها npm؟

#### --distractors--

موقع الويب npmjs.com.

---

فهرس الحزم حيث تُخزن المكتبات.

---

أداة سطر الأوامر لتثبيت وإدارة الحزم.

#### --answer--

بيئة تشغيل Node.js.

### --question--

#### --text--

ماذا يفعل أمر `npm install`؟

#### --distractors--

ينشئ مشروع Node.js جديد تمامًا من قالب.

---

يشغل تطبيق Node.js الخاص بك على خادم تطوير محلي.

---

يزيل الحزم التي لم تعد مطلوبة في مشروعك.

#### --answer--

يضيف حزمًا مُعدة مسبقًا إلى مشروعك لمهام شائعة.

### --question--

#### --text--

أي ملف يخزن البيانات الوصفية مثل اسم المشروع، الإصدار، المؤلف، والتبعيات لمشروع Node.js؟

#### --distractors--

`README.md`.

---

`node_modules/package.json`.

---

`package-lock.json`.

#### --answer--

`package.json`.

### --question--

#### --text--

كيف يمكنك إنشاء ملف `package.json` تفاعليًا؟

#### --distractors--

شغّل `npm install` بدون تحديد اسم الحزمة.

---

حمّل قالبًا من الصفحة الرئيسية لفهرس npm.

---

انسخ الملف من مشروع آخر وأعد تسميته.

#### --answer--

شغّل أمر `npm init`.

### --question--

#### --text--

كيف تنشئ ملف `package.json` بقيم افتراضية بدون الإجابة على المطالبات؟

#### --distractors--

شغّل `npm init --interactive`.

---

أنشئ ملفًا فارغًا باسم `package.json` واتركه فارغًا.

---

استخدم `npm init` واحذف كل مطالبة بعد فتح المحرر.

#### --answer--

شغّل `npm init -y` (أو `npm init --yes`) لقبول كل القيم الافتراضية تلقائيًا.

### --question--

#### --text--

أين يسرد npm الحزم التي تُستخدم فقط أثناء التطوير، مثل أدوات التدقيق أو مشغلات الاختبار؟

#### --distractors--

داخل قسم `"dependencies"`.

---

داخل قسم `"scripts"` تحت الأوامر المخصصة.

---

داخل مجلد `node_modules` نفسه.

#### --answer--

داخل قسم `"devDependencies"` في `package.json`.

### --question--

#### --text--

أي أمر يثبت `nodemon` كتبعيات خاصة بالتطوير فقط؟

#### --distractors--

`npm uninstall nodemon`.

---

`npm save nodemon --dev`.

---

`npm install nodemon`.

#### --answer--

`npm install nodemon -D`.

### --question--

#### --text--

أي مجلد وملف يُضافان إلى جذر مشروعك بعد تثبيت تبعية؟

#### --distractors--

مجلد `public` وملف `index.html`.

---

مجلد `dist` وملف `webpack.config.js`.

---

مجلد `src` وملف `README.md`.

#### --answer--

مجلد `node_modules` وملف `package-lock.json`.

### --question--

#### --text--

كيف يساعد `package-lock.json` في الحفاظ على اتساق عمليات البناء؟

#### --distractors--

بحذف التبعيات غير المستخدمة قبل كل تثبيت.

---

بتخزين أرقام الإصدارات الرئيسية فقط للحزم.

---

بترقية الحزم تلقائيًا إلى أحدث الإصدارات.

#### --answer--

بتجميد الإصدارات الدقيقة لكل تبعية تم تثبيتها.

### --question--

#### --text--

لماذا يجب عليك الالتزام بملف `package-lock.json` في مستودع Git الخاص بك؟

#### --distractors--

لجعل npm يعمل بشكل أسرع على جهاز المطور.

---

لتحديث التبعيات القديمة تلقائيًا.

---

لتقليل حجم المستودع.

#### --answer--

لكي يثبت الفريق بأكمله نفس إصدارات التبعيات والإعدادات.

### --question--

#### --text--

كيف تثبت إصدارًا محددًا من حزمة، مثل `express@4.21.2`؟

#### --distractors--

`npm update express@4.21.2`.

---

`npm get express@4.21.2`.

---

`npm use express@4.21.2`.

#### --answer--

`npm install express@4.21.2`.

### --question--

#### --text--

ماذا يسمح رمز caret (`^`) عند تحديد إصدارات التبعيات؟

#### --distractors--

يقفل التبعية على إصدار تصحيح دقيق.

---

يسمح بالتحديثات للإصدار الرئيسي فقط.

---

يستبعد كل الإصدارات الأقل من المحدد.

#### --answer--

يسمح بالإصدارات الفرعية وتصحيحات أحدث لكنه يمنع الإصدارات الرئيسية الجديدة.

### --question--

#### --text--

ماذا يعني رمز tilde (`~`) في نطاق الإصدار مثل `~1.2.3`؟

#### --distractors--

يسمح بالتحديثات للإصدارين الرئيسي والفرعي.

---

يثبت أحدث إصدار رئيسي بغض النظر عن النطاق.

---

يمنع npm من تثبيت أي تحديثات على الإطلاق.

#### --answer--

يسمح بتحديثات إصدار التصحيح فقط مع تثبيت الإصدار الفرعي.

### --question--

#### --text--

أي نوع من التغييرات يسبب زيادة في الإصدار الرئيسي في الإصدار الدلالي؟

#### --distractors--

تعديلات طفيفة على التعليقات والتنسيق.

---

تصحيحات أخطاء صغيرة لا تغير السلوك.

---

ميزات اختيارية لا تكسر الشفرة الحالية.

#### --answer--

تغييرات جذرية قد تتسبب في توقف الشفرة الحالية عن العمل.

### --question--

#### --text--

أي أمر يسرد التبعيات التي لها إصدارات منشورة أحدث؟

#### --distractors--

`npm check`.

---

`npm list`.

---

`npm refresh`.

#### --answer--

`npm outdated`.

### --question--

#### --text--

يُذكر Yarn وPNPM وBun كبدائل لـ npm. ما نوع هذه الأدوات؟

#### --distractors--

مواقع فهرس تنشر أو تحمل حزم npm.

---

خدمات قاعدة بيانات تخزن بيانات المستخدم لحزم npm.

---

منصات استضافة سحابية تنشر مشاريع npm تلقائيًا.

#### --answer--

مديرو حزم بديلون.

### --question--

#### --text--

أين تُعلن السكربتات المخصصة لـ npm في المشروع؟

#### --distractors--

داخل ملف `.npmrc`.

---

داخل قسم `"dependencies"` في `package.json`.

---

في ملف `scripts.json` منفصل في الجذر.

#### --answer--

داخل كائن `"scripts"` في `package.json`.

### --question--

#### --text--

كيف تنفذ سكربت npm مخصص باسم `build`؟

#### --distractors--

`build npm`.

---

`node build`.

---

`run build`.

#### --answer--

`npm run build`.

### --question--

#### --text--

ماذا يفعل الشرطتان المزدوجتان (`--`) عند تشغيل `npm run start -- 8000`؟

#### --distractors--

يصمت مخرجات السكربت.

---

يجبر npm على تشغيل السكربت بالتوازي.

---

يطلب صلاحيات إدارية قبل تنفيذ السكربت.

#### --answer--

يخبر npm بتمرير كل ما يلي `--` كمعلمات للأمر الأساسي.
