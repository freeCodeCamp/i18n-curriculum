---
id: 68376949899fcbad3c9cac23
title: JavaScript und Barrierefreiheit Quiz
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 9 der 10 Fragen unten richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welches Attribut wird für Barrierefreiheitszwecke verwendet, um anzuzeigen, ob ein Steuerelement erweitert oder eingeklappt ist?

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

Welche zwei Attribute können in Kombination mit `aria-expanded` verwendet werden, um eine programmatische Verbindung zwischen dem steuernden Element und dem von ihm gesteuerten Element herzustellen?

#### --distractors--

`aria-labelledBy` und `aria-owns`

---

`aria-controls` und `aria-label`

---

`aria-live` und `aria-owns`

#### --answer--

`aria-controls` und `aria-owns`

### --question--

#### --text--

Welches der folgenden Attribute erzeugt eine Live-Region auf Ihrer Seite, die verwendet werden kann, um Screenreader-Nutzende über dynamische Inhaltsänderungen in der Live-Region zu informieren, sobald sie auftreten?

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

Welches der folgenden `div`-Elemente wird nur für Personen deaktiviert, die unterstützende Technologien wie Screenreader verwenden?

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

Welches der folgenden ist eine gültige Verwendung des `aria-expanded`-Attributs?

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

Welches Attribut wird verwendet, um eine programmatische Beziehung zwischen einem steuernden Element und dem Element herzustellen, das es auf der Seite beeinflusst?

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

Welches der folgenden ist KEIN häufiger Anwendungsfall für Live-Regionen?

#### --distractors--

Nachrichten, die nach einer Aktion angezeigt werden (wie eine Fehlermeldung oder Bestätigung).

---

Inhalte, die sich periodisch aktualisieren (wie ein Ticker, Laufschrift oder Countdown-Timer).

---

Allgemeine Statusmeldungen (wie Updates zu einem Prozess).

#### --answer--

Nachrichten, die vor einer Aktion angezeigt werden (wie ein Bestätigungsdialog).

### --question--

#### --text--

Welches der folgenden Beispiele ist die korrekte Art anzugeben, welcher Tab aktuell ausgewählt ist?

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

Welcher der folgenden Werte ist KEIN gültiger Wert für das `aria-live`-Attribut?

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

Welcher der folgenden `aria-live`-Attributwerte bedeutet, dass das Update sehr wichtig ist und die höchste Priorität hat?

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`

