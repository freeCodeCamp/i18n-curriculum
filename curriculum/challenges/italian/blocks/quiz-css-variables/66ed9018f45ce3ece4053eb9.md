---
id: 66ed9018f45ce3ece4053eb9
title: Quiz sulle variabili CSS
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale dei seguenti NON è un descrittore valido nella regola `@property`?

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

Nel codice seguente, quale valore avrà `color` se `--main-color` non è definito?

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

Nessun colore sarà applicato.

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

Qual è un modo valido per dichiarare una proprietà personalizzata in CSS?

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

Qual è lo scopo principale di usare proprietà personalizzate in CSS?

#### --distractors--

Cambiare la struttura del DOM.

---

Creare selettori dinamici.

---

Migliorare l'accessibilità.

#### --answer--

Definire stili riutilizzabili.

### --question--

#### --text--

Cosa rappresenta il selettore `:root`?

#### --distractors--

Il primo elemento figlio di `body`.

---

L'elemento genitore di tutti gli elementi `header`.

---

Il primo figlio del tag `html`.

#### --answer--

L'elemento di livello più alto nell'albero DOM.

### --question--

#### --text--

Come si applica una proprietà personalizzata `--foreground` come `color` in CSS?

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

Qual è lo scopo della regola `@property` in CSS?

#### --distractors--

Definire una media query.

---

Controllare le animazioni CSS e i loro tempi.

---

Raggruppare proprietà CSS in un'unica regola.

#### --answer--

Definire come si comportano le proprietà personalizzate.

### --question--

#### --text--

Quando dichiari una proprietà personalizzata `@property`, qual è lo scopo di `syntax` nella sua definizione?

#### --distractors--

Specifica il valore predefinito della proprietà personalizzata.

---

Determina se la proprietà può essere ereditata dagli elementi figli.

---

Definisce se la proprietà si applica a tutti gli elementi.

#### --answer--

Imposta un tipo di dato o un pattern di valore specifico per la proprietà personalizzata.

### --question--

#### --text--

A cosa devi fare attenzione quando usi proprietà personalizzate?

#### --distractors--

Le proprietà personalizzate sono automaticamente convertite in unità `px`.

---

Le proprietà personalizzate sovrascrivono sempre gli stili in linea.

---

Le proprietà personalizzate non possono essere usate per impostare valori di carattere tipografico.

#### --answer--

Le proprietà personalizzate potrebbero non essere supportate nei browser più vecchi.

### --question--

#### --text--

Qual è lo scopo di fornire un valore fallback nella funzione `var()` per le proprietà personalizzate CSS?

#### --distractors--

Ridurre la quantità di codice CSS.

---

Migliorare le prestazioni nei browser moderni.

---

Ottimizzare il tempo di rendering su reti lente.

#### --answer--

Garantire che venga applicato un valore valido se la proprietà personalizzata non è definita.

### --question--

#### --text--

Quando definisci una variabile `--foreground` all'interno di una media query, cosa succede quando la media query non corrisponde più alla viewport corrente?

#### --distractors--

La proprietà personalizzata viene preservata e continua ad applicarsi.

---

La proprietà personalizzata torna al suo valore iniziale.

---

La proprietà personalizzata viene ricalcolata in base alla viewport.

#### --answer--

La proprietà personalizzata non è più disponibile perché è limitata all'ambito della media query.

### --question--

#### --text--

Come si comporterebbe la seguente dichiarazione?

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

Applicherà `--main-color` anche se non è definito, usando quel valore predefinito.

---

Applicherà `--main-color` e `--fallback-color` simultaneamente, risultando in una fusione dei due colori.

---

Userà sempre `#000` come valore predefinito, indipendentemente dal fatto che `--main-color` o `--fallback-color` siano definiti.

#### --answer--

Applica `--main-color` se definito; altrimenti controlla `--fallback-color;` e se entrambi non sono definiti, usa `#000`.

### --question--

#### --text--

Qual è un vantaggio dell'usare `@property` in CSS?

#### --distractors--

Migliora le prestazioni precomputando i valori delle proprietà personalizzate.

---

Aggiunge automaticamente prefissi alle proprietà personalizzate per miglior supporto browser.

---

Limita l'uso delle proprietà personalizzate a elementi specifici.

#### --answer--

Permette di animare le proprietà personalizzate.

### --question--

#### --text--

Cosa controlla la proprietà `inherits` in una dichiarazione di proprietà personalizzata `@property`?

#### --distractors--

Se la proprietà personalizzata avrà un valore iniziale.

---

Se la proprietà può avere una versione abbreviata.

---

Se la proprietà accetta valori fallback.

#### --answer--

Se il valore della proprietà personalizzata viene passato agli elementi figli.

### --question--

#### --text--

Nella dichiarazione di una proprietà personalizzata `@property`, cosa specifica `initial-value`?

#### --distractors--

I valori accettabili che la proprietà può assumere.

---

La priorità della proprietà nella cascade.

---

Il tipo di valore che la proprietà deve avere.

#### --answer--

Il valore fallback per la proprietà.

### --question--

#### --text--

Dato il seguente codice HTML e CSS, quale sarà il valore della proprietà `color` per l'elemento `.box`?

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

A quale proprietà dovrebbe essere applicato un gradiente CSS?

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

Qual è lo scopo delle fermate di colore nei gradienti CSS?

#### --distractors--

Definire il livello di trasparenza del gradiente.

---

Specificare la direzione del gradiente.

---

Ripetere il gradiente in un pattern fisso.

#### --answer--

Definire i punti specifici in cui i colori cambiano nel gradiente.

### --question--

#### --text--

Cosa succede se non viene specificato un angolo o una direzione in un gradiente lineare CSS?

#### --distractors--

Il gradiente assume come direzione quella diagonale.

---

Il gradiente assume come direzione dal basso verso l'alto.

---

Il gradiente assume come direzione da sinistra a destra.

#### --answer--

Il gradiente assume come direzione dall'alto verso il basso.

### --question--

#### --text--

Quale funzione di gradiente CSS permette di creare un gradiente che si irradia verso l'esterno da un punto centrale?

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

Quale dei seguenti è un modo corretto per dichiarare una proprietà personalizzata CSS?

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

Quale selettore viene tipicamente usato per definire proprietà personalizzate CSS globali?

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

Quando usi `var()`, perché è consigliato includere un valore fallback?

#### --distractors--

Garantisce che la variabile venga animata correttamente.

---

Evita il caricamento di fogli di stile esterni.

---

Previene il reflow del browser.

#### --answer--

Garantisce un valore valido se la proprietà personalizzata non è definita.

### --question--

#### --text--

Qual è la sintassi corretta per applicare una proprietà personalizzata come colore di sfondo?

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

Quale regola CSS permette agli sviluppatori di definire proprietà personalizzate con maggiore controllo sul loro comportamento?

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

Cosa controlla il campo `inherits` in una definizione `@property`?

#### --distractors--

Se la proprietà è usata in JavaScript.

---

Se la proprietà attiva repaint.

---

Se la proprietà può contenere funzioni.

#### --answer--

Se il valore della proprietà viene passato agli elementi figli.

### --question--

#### --text--

Quando definisci un angolo di gradiente animato usando `@property`, quale sintassi dovrebbe essere usata?

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

Cosa distingue una proprietà personalizzata CSS da una proprietà CSS standard in termini di definizione?

#### --distractors--

Le proprietà CSS standard sono definite con un singolo trattino, mentre le proprietà personalizzate usano due trattini.

---

Le proprietà CSS standard usano `var()` per la definizione, mentre le proprietà personalizzate no.

---

Le proprietà CSS standard sono definite usando la regola `@property`, mentre le proprietà personalizzate no.

#### --answer--

Le proprietà personalizzate devono iniziare con due trattini (`--`), mentre le proprietà CSS standard no.

### --question--

#### --text--

Qual è un vantaggio dell'usare proprietà personalizzate CSS?

#### --distractors--

Ridurre la necessità di commenti CSS.

---

Ottimizzare automaticamente le immagini.

---

Rendere obsolete le variabili JavaScript.

#### --answer--

Permettere di riutilizzare e mantenere centralmente gli stili.

### --question--

#### --text--

Quale proprietà CSS può cambiare dinamicamente in base alle media query usando proprietà personalizzate?

#### --distractors--

Solo `background-color`

---

Solo `font-family`

---

Solo `z-index`

#### --answer--

Qualsiasi proprietà che accetta un valore.

### --question--

#### --text--

Nel contesto delle variabili CSS, qual è il ruolo di `initial-value` in una regola `@property`?

#### --distractors--

Imposta il valore minimo della proprietà.

---

Definisce il valore massimo per le animazioni.

---

Cambia la priorità del selettore.

#### --answer--

Assegna un valore predefinito alla proprietà se nessuno è impostato.

### --question--

#### --text--

Considera il seguente HTML e CSS. Quale colore di sfondo sarà applicato all'elemento `.card`?

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

Nel seguente CSS, cosa succede se un utente prova ad assegnare un valore non valido a `--padding` (per esempio, un colore invece di una lunghezza)?

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

Il browser accetta il valore ma lo ignora durante il layout.

---

Il browser lancia un errore di runtime.

---

Il browser converte automaticamente il valore in una lunghezza valida.

#### --answer--

Il browser usa il valore iniziale della proprietà come fallback.

### --question--

#### --text--

Qual è lo scopo della proprietà personalizzata `--gradient-angle` in questo esempio?

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

Definisce la velocità della transizione del gradiente.

---

Specifica la dimensione del gradiente.

---

Imposta la modalità di fusione dei colori.

#### --answer--

Controlla la direzione del gradiente.

### --question--

#### --text--

Quale scenario mostra meglio il vantaggio di usare proprietà personalizzate per i temi?

#### --distractors--

Usarle solo per le dimensioni dei caratteri tipografici.

---

Applicarle solo su `:hover`.

---

Limitarle a una sola classe CSS.

#### --answer--

Cambiare i valori tramite classi.

### --question--

#### --text--

Perché gli sviluppatori potrebbero preferire usare `:root` per definire variabili CSS?

#### --distractors--

Perché `:root` migliora la velocità di caricamento della pagina.

---

Perché `:root` disabilita la specificità.

---

Perché `:root` è richiesto per JavaScript.

#### --answer--

Perché `:root` permette alle proprietà di avere ambito globale.

### --question--

#### --text--

Cosa farebbe il seguente CSS se `--secondary-color` non fosse definito?

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

Applicherebbe `black`.

---

Ignorerebbe la proprietà `color`.

---

Applicherebbe `--secondary-color`.

#### --answer--

Applicherebbe `orange`.

### --question--

#### --text--

Che tipo di valori rappresenta `<color>` in un campo di sintassi `@property`?

#### --distractors--

Angoli come `90deg`.

---

Lunghezze come `10px`.

---

Percentuali come `50%`.

#### --answer--

Valori di colore come `#ff0000` o `red`.

### --question--

#### --text--

Qual è il vantaggio principale di usare proprietà personalizzate in combinazione con `@property`?

#### --distractors--

Evitano la necessità di valori fallback.

---

Forzano un layout statico.

---

Riducendo la necessità di classi.

#### --answer--

Permettono l'animazione delle proprietà.

### --question--

#### --text--

Cosa fa `var(--undefined-property, fallback)` quando la proprietà personalizzata non è definita?

#### --distractors--

Lancia un errore e smette di applicare gli stili.

---

Ignora l'intera regola CSS.

---

Applica il nome della variabile così com'è.

#### --answer--

Applica invece il valore fallback.
