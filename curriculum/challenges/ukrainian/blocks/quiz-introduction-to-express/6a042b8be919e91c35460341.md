---
id: 6a042b8be919e91c35460341
title: Вступ до тесту з Express
challengeType: 8
dashedName: quiz-introduction-to-express
---

# --description--

Щоб пройти тест, ви повинні правильно відповісти щонайменше на 18 із 20 питань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Який зв’язок між API та вебсервісом?

#### --distractors--

Кожен API є вебсервісом, але не кожен вебсервіс є API.

---

API та вебсервіси — це ідентичні поняття.

---

Вебсервіси працюють лише з REST, тоді як API підтримують кілька протоколів.

#### --answer--

Кожен вебсервіс є API, але не кожен API є вебсервісом.

### --question--

#### --text--

Який HTTP-заголовок клієнт використовує, щоб повідомити сервер, у якому форматі він хоче отримати відповідь?

#### --distractors--

`Content-Type`

---

`Authorization`

---

`X-Requested-With`

#### --answer--

`Accept`

### --question--

#### --text--

Що означає безстанність у REST-архітектурі?

#### --distractors--

Сервер зберігає дані сесії клієнта між запитами.

---

Клієнт не зберігає власного стану між запитами.

---

REST обмежує всі запити лише операціями читання.

#### --answer--

Сервер не зберігає пам’ять про попередні запити.

### --question--

#### --text--

Який HTTP-метод REST використовується для видалення ресурсу?

#### --distractors--

`GET`

---

`POST`

---

`PUT`

#### --answer--

`DELETE`

### --question--

#### --text--

Яка ключова перевага мікросервісів над монолітом?

#### --distractors--

Простіше налагодження і менше мережевих викликів між сервісами.

---

Єдина спільна кодова база, до якої роблять внесок усі команди.

---

Легше керувати інфраструктурою для всіх сервісів.

#### --answer--

Швидша розробка, краща ізоляція помилок і можливість використовувати різні мови для кожного сервісу.

### --question--

#### --text--

Що повертає виклик `express()`?

#### --distractors--

Новий HTTP-сервер.

---

Об’єкт маршрутизатора.

---

Функція підпрограмного забезпечення.

#### --answer--

Новий екземпляр застосунку Express.

### --question--

#### --text--

Що таке підпрограмне забезпечення в Express?

#### --distractors--

Вбудований метод для запуску сервера Express.

---

Особливий тип HTTP-методу, що використовується в REST API.

---

Вбудований інструмент для обслуговування статичних файлів із папки.

#### --answer--

Код, який виконується між отриманням запиту і відправленням відповіді.

### --question--

#### --text--

Що робить `app.listen(port, callback)` в Express?

#### --distractors--

Визначає маршрут для вхідних запитів.

---

Завантажує бібліотеку Express.

---

Створює нову функцію підпрограмного забезпечення.

#### --answer--

Запускає сервер і починає слухати вхідні HTTP-запити.

### --question--

#### --text--

Що таке параметр маршруту в Express?

#### --distractors--

Фіксований URL-шлях, як `/home`.

---

Рядок запиту, доданий до URL.

---

Метод для обслуговування статичних файлів.

#### --answer--

Заповнювач у URL із префіксом `:`, який захоплює динамічне значення.

### --question--

#### --text--

Як отримати доступ до динамічного значення, захопленого з URL, як `/post/42`, у маршруті Express, визначеному як `/post/:postId`?

#### --distractors--

`req.query.postId`

---

`req.body.postId`

---

`req.url.postId`

#### --answer--

`req.params.postId`

### --question--

#### --text--

Що робить `res.json()` в Express?

#### --distractors--

Відправляє відповідь у вигляді простого тексту.

---

Перенаправляє клієнта на інший URL.

---

Відтворює подання шаблону і відправляє отриманий HTML.

#### --answer--

Відправляє JSON-відповідь і автоматично конвертує JavaScript-об’єкт.

### --question--

#### --text--

Що робить `res.redirect(url)` в Express?

#### --distractors--

Встановлює HTTP-код стану.

---

Відправляє JSON-відповідь.

---

Відтворює подання за допомогою шаблонного рушія.

#### --answer--

Перенаправляє клієнта на інший URL.

### --question--

#### --text--

Яка мета `app.route()` в Express?

#### --distractors--

Створити новий екземпляр застосунку Express.

---

Обслуговувати статичні файли з вказаної папки.

---

Визначати окремі маршрути для різних URL-шляхів.

#### --answer--

Об’єднувати кілька обробників HTTP-методів для одного шляху в одному блоці.

### --question--

#### --text--

Що створює `express.Router()`?

#### --distractors--

Новий екземпляр застосунку Express, який замінює `app` для обробки всієї логіки сервера.

---

Функцію підпрограмного забезпечення, призначену для перехоплення та обробки помилок на рівні маршруту.

---

Вбудований сервер статичних файлів, який автоматично зіставляє файли з URL-шляхами.

#### --answer--

Об’єкт маршрутизатора, схожий на міні-застосунок Express, на якому ви визначаєте маршрути і монтуєте його в основний застосунок.

### --question--

#### --text--

Як змонтувати маршрутизатор у головному застосунку Express?

#### --distractors--

Викликом `router.listen()`.

---

Викликом `router.start()`.

---

Прямим викликом `app.get()` всередині маршрутизатора.

#### --answer--

Викликом `app.use(path, router)`.

### --question--

#### --text--

Яке вбудоване підпрограмне забезпечення Express використовується для обслуговування статичних файлів?

#### --distractors--

`express.json()`

---

`express.urlencoded()`

---

`express.raw()`

#### --answer--

`express.static()`

### --question--

#### --text--

Що відбувається, коли користувач запитує статичний файл, якого немає в папці, налаштованій за допомогою `express.static()`?

#### --distractors--

Express викидає помилку і падає сервер.

---

Express перенаправляє користувача на домашню сторінку.

---

Express створює порожній файл за запитаним шляхом.

#### --answer--

Express автоматично відповідає з кодом 404.

### --question--

#### --text--

Що з наведеного є правдою про SOAP у порівнянні з REST?

#### --distractors--

SOAP використовує JSON і є гнучкішим за REST, що робить його ідеальним для сучасних застосунків.

---

SOAP сьогодні є найпоширенішим типом вебсервісів, замінивши REST у більшості систем.

---

SOAP підтримує кілька форматів відповіді, включно з простим текстом, JSON і бінарними даними.

#### --answer--

SOAP використовує лише XML і поширений у корпоративних системах, які потребують суворіших стандартів і безпеки.

### --question--

#### --text--

Що таке обробник маршруту в Express?

#### --distractors--

Об’єкт конфігурації, який зіставляє всі доступні шляхи маршруту з їх HTTP-методами.

---

Функція підпрограмного забезпечення, яка автоматично перехоплює і обслуговує статичні файли з папки.

---

Конектор бази даних, який обробляє запити до бази, пов’язані з маршрутом.

#### --answer--

Функція зворотного виклику, яка виконується, коли запит співпадає з маршрутом, з доступом до `req` і `res`.

### --question--

#### --text--

Яка перевага використання `express.Router()` замість визначення всіх маршрутів безпосередньо в `app.js`?

#### --distractors--

Дозволяє серверу обробляти значно більше одночасних HTTP-запитів.

---

Автоматично генерує і реєструє кінцеві точки REST API на основі назв файлів маршрутів.

---

Дозволяє серверу запускатися і працювати без необхідності вказувати номер порту.

#### --answer--

Розділяє обов’язки і тримає головний файл чистим, переміщуючи пов’язані маршрути у власні модулі.
