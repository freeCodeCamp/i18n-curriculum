---
id: 672c3a9d32c56113fcaedf24
title: 什麼是固定定位和黏性定位，以及它們與絕對定位有何不同？
challengeType: 19
dashedName: what-is-fixed-and-sticky-positioning
---

# --interactive--

固定定位和黏性定位是兩種重要的 CSS 定位策略，與絕對定位相比，各自提供不同的行為。當一個元素使用 `position: fixed` 定位時，它會從正常的文件流程中移除，並相對於視窗定位，這表示即使使用者捲動頁面，它仍會保持在相同位置。這通常用於像標題或導覽列這類需要始終保持可見的元素。<dfn>例如</dfn>，如果你想讓標題固定在頁面頂端，可以使用以下程式碼：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<h1>Fixed Header</h1>

<p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer nec odio. Praesent libero. Sed cursus ante dapibus diam.</p>
<p>Sed nisi. Nulla quis sem at nibh elementum imperdiet. Duis sagittis ipsum. Praesent mauris.</p>
<p>Fusce nec tellus sed augue semper porta. Mauris massa. Vestibulum lacinia arcu eget nulla.</p>
<p>Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos.</p>
<p>Curabitur sodales ligula in libero. Sed dignissim lacinia nunc. Curabitur tortor.</p>
<p>Pellentesque nibh. Aenean quam. In scelerisque sem at dolor. Maecenas mattis.</p>
<p>Sed convallis tristique sem. Proin ut ligula vel nunc egestas porttitor. Morbi lectus risus.</p>
<p>Donec congue lacinia dui, a porttitor lectus condimentum laoreet. Nunc eu ullamcorper orci.</p>
<p>Quisque eget odio ac lectus vestibulum faucibus eget in metus. In pellentesque faucibus vestibulum.</p>
<p>Nulla at nulla justo, eget luctus tortor. Nulla facilisi. Duis aliquet egestas purus in blandit.</p>
```

```css
body {
  margin: 0;
  padding-top: 60px;
  font-family: Arial, sans-serif;
  line-height: 1.6;
}

h1 {
  position: fixed;
  top: 0;
  width: 500px;
  background: white;
  padding: 10px;
  border-bottom: 2px solid #ccc;
}

p {
  max-width: 600px;
  margin: 20px auto;
}
```

:::

在此範例中，`h1` 元素將會固定在視窗頂端，即使使用者捲動時，它仍會保持在原位。這對於建立持久的使用者介面元素非常有用，例如固定標題列或永遠可見的導覽列。

`position: sticky` 的行為介於相對定位和固定定位之間。最初，元素的行為就像是相對定位，保持在文件的流程中。然而，一旦使用者將元素捲動過某個點，它就會「黏」在視窗（通常是頂部），並表現得像是固定定位。這非常適合用來建立像是黏性導覽列這類的元素，只有當使用者捲動到某個位置時才會變成固定。以下是你如何套用黏性定位的方法：

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<h1>Sticky Header</h1>

<p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Integer nec odio. Praesent libero. Sed cursus ante dapibus diam.</p>
<p>Sed nisi. Nulla quis sem at nibh elementum imperdiet. Duis sagittis ipsum. Praesent mauris.</p>
<p>Fusce nec tellus sed augue semper porta. Mauris massa. Vestibulum lacinia arcu eget nulla.</p>
<p>Class aptent taciti sociosqu ad litora torquent per conubia nostra, per inceptos himenaeos.</p>
<p>Curabitur sodales ligula in libero. Sed dignissim lacinia nunc. Curabitur tortor.</p>
<p>Pellentesque nibh. Aenean quam. In scelerisque sem at dolor. Maecenas mattis.</p>
<p>Sed convallis tristique sem. Proin ut ligula vel nunc egestas porttitor. Morbi lectus risus.</p>
<p>Donec congue lacinia dui, a porttitor lectus condimentum laoreet. Nunc eu ullamcorper orci.</p>
<p>Quisque eget odio ac lectus vestibulum faucibus eget in metus. In pellentesque faucibus vestibulum.</p>
<p>Nulla at nulla justo, eget luctus tortor. Nulla facilisi. Duis aliquet egestas purus in blandit.</p>
```

```css
h1 {
  position: sticky;
  top: 30px;
  left: 30px;
}
```

:::

在此範例中，`h1` 元素會正常運作，直到使用者將它向下捲動 `30px` 離開頂端，此時它會「黏附」在視窗頂端。

以下是絕對定位、固定定位和黏性定位之間的一些主要差異：

- 絕對定位會將元素從文件流程中移除，並相對於最近的已定位祖先或如果沒有則相對於初始包含區塊進行定位。該元素無論滾動與否都會保持在該位置。
- 固定定位也會將元素從文件流程中移除，但會相對於視窗固定，這表示即使頁面滾動，元素仍會保持在相同位置可見。
- Sticky 定位最初保持元素在正常流程中，但在滾動超過定義的門檻值後允許其固定在原位。

這些策略允許你創建包含浮動元素、黏性標題和更多動態 UI 組件的版面配置，透過讓重要資訊始終可存取來提升使用者體驗。

# --questions--

## --text--

`fixed` 和 `sticky` 定位的主要差異是什麼？

## --answers--

`fixed` 相對於視窗保持固定，`sticky` 根據捲動位置移動。

---

`sticky` 相對於視窗保持固定位置，`fixed` 則會根據捲動位置移動。

### --feedback--

當滾動超過門檻值後，某一項會變成 `fixed`。

---

`fixed` 和 `sticky` 的行為相同。

### --feedback--

當滾動超過門檻值後，某一項會變成 `fixed`。

---

標題或導覽列皆無法使用。

### --feedback--

當滾動超過門檻值後，某一項會變成 `fixed`。

## --video-solution--

1

## --text--

必須定義什麼屬性才能使 `sticky` 定位生效？

## --answers--

`position: absolute`

### --feedback--

它是 `fixed` 和 `relative` 定位的混合體。

---

`position: fixed`

### --feedback--

它是 `fixed` 和 `relative` 定位的混合體。

---

`position: sticky`

---

`z-index`

### --feedback--

它是 `fixed` 和 `relative` 定位的混合體。

## --video-solution--

3

## --text--

`fixed` 定位與 `absolute` 定位有何不同？

## --answers--

`fixed` 是相對於視窗，而 `absolute` 是相對於最近的已定位祖先。

---

`fixed` 定位不允許滾動。

### --feedback--

思考在捲動時什麼會保持可見。

---

`absolute` 定位會使元素保持在正常文件流程內。

### --feedback--

思考在捲動時什麼會保持可見。

---

`fixed` 定位在滾動時會隱藏該元素。

### --feedback--

思考在捲動時什麼會保持可見。

## --video-solution--

1
