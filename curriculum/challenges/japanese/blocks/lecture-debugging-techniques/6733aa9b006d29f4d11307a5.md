---
id: 6733aa9b006d29f4d11307a5
title: JavaScriptのよくあるエラーの例とは？
challengeType: 19
dashedName: what-are-some-examples-of-common-javascript-errors
---

# --description--

JavaScriptでプログラミングをしていると、必ずエラーメッセージに遭遇します。よくあるエラーメッセージを理解することで、より効果的にデバッグでき、より強い開発者に成長できます。よくあるエラーメッセージの種類は`SyntaxError`、`ReferenceError`、`TypeError`、`RangeError`の4つです。

`SyntaxError`は、コード内で誤って書いてしまったときに発生します。例えば、括弧やブラケットの閉じ忘れなどです。これは文法ミスのようなものだと考えてください。配列を作成するときに開発者がよく犯すミスの例を示します。

```js
const arr = ["Beau", "Quincy" "Tom"]
```

配列の各要素はカンマで区切る必要があり、そうしないとエラーメッセージが表示されます。

2つ目によくあるJavaScriptのエラーは`ReferenceError`です。`ReferenceError`にはいくつかの種類があり、発生する原因もさまざまです。最初のタイプの`ReferenceError`は未定義の変数です。

```js
console.log(price);
```

この例では、`price`変数をコンソールにログしようとしていますが、まだ宣言されていません。これにより`ReferenceError`が発生します。

`ReferenceError`のもう一つの例は、`let`や`const`で宣言された変数に、定義される前にアクセスしようとすることです。

```js
console.log(b);
const b = 50;
```

上の例は`Cannot access 'b' before initialization`エラーになります。

3つ目によくあるエラーは`TypeError`です。これは間違った型に対して操作をしようとしたときに発生します。オブジェクトに対して`map`メソッドを使おうとした例を示します。

```js
const developerObj = {
  name: "Jessica",
  country: "USA",
  isEmployed: true
};

developerObj.map()
```

この例は`developerObj.map is not a function`エラーになります。なぜなら`map`メソッドは配列に使うものであって、オブジェクトには使えないからです。

最後に見ていくよくあるエラーは`RangeError`です。

`RangeError`は、JavaScriptが扱える範囲外の値を使おうとしたときに発生します。配列の長さに無効な値を代入した例を示します。

```js
const arr = [];
arr.length = -1; 
```

配列の`length`は0以上の整数でなければならないため、`-1`を設定すると`RangeError`が発生します。

JavaScriptでプログラミングを続けるうえで、これらの異なるタイプのエラーがなぜ起きるのかを意識しておくとよいでしょう。

# --questions--

## --text--

JavaScriptで`SyntaxError`が発生する原因は何ですか？

## --answers--

変数を関数のように呼び出そうとすること。

### --feedback--

コードの文法ミスのようなものです。

---

範囲外の値を使おうとすること。

### --feedback--

コードの文法ミスのようなものです。

---

コード内で誤って書くこと。例えば括弧やセミコロンの閉じ忘れなど。

---

変数が宣言される前にアクセスすること。

### --feedback--

コードの文法ミスのようなものです。

## --video-solution--

3

## --text--

数値を保持している変数を関数のように呼び出そうとするとどうなりますか？

## --answers--

`SyntaxError`

### --feedback--

変数が関数のように扱われているためにエラーが発生します。

---

`ReferenceError`

### --feedback--

変数が関数のように扱われているためにエラーが発生します。

---

`TypeError`

---

`RangeError`

### --feedback--

変数が関数のように扱われているためにエラーが発生します。

## --video-solution--

3

## --text--

配列の`-1`に`length`のような無効な値を代入するとどのエラーが発生しますか？

## --answers--

`TypeError`

### --feedback--

このエラーは有効範囲外の値を使ったことに関連しています。

---

`ReferenceError`

### --feedback--

このエラーは有効範囲外の値を使ったことに関連しています。

---

`RangeError`

---

`SyntaxError`

### --feedback--

このエラーは有効範囲外の値を使ったことに関連しています。

## --video-solution--

3
