---
id: 672990ecf71a852804ababe7
title: プレゼンテーショナルHTMLとセマンティックHTMLの違いとは？
challengeType: 19
dashedName: what-is-the-difference-between-presentational-and-semantic-html
---

# --interactive--

プレゼンテーショナルHTMLはコンテンツの見た目やスタイルに焦点を当てています。HTMLの初期の頃、開発者は`center`、`big`、`font`のような要素を使っていました。しかし、現代のウェブ開発では、これらの要素は制限が多く、アクセシビリティや保守性に悪影響を与えるため、使うべきではありません。

多くのプレゼンテーショナルHTML要素は非推奨となっており、これは時代遅れで推奨されなくなったことを意味します。同じ結果を得るためのより良い方法があります。ただし、それらが存在することを知っておくのは役立つので、いくつかの例を見てみましょう。

`font`要素は、テキストのフォントサイズと色を設定するために使われた非推奨の要素です。以下は`font`要素の例です。

インタラクティブエディターを有効にして、サイズを5から7に変更するとフォントが大きくなるのがわかります。

:::interactive_editor

```html
<font size="5" color="blue">This text is blue and large.</font>
```

:::

この例ではテキストの色を`blue`に、サイズを`5`の値に設定しています。`size`属性の範囲は`1`から`7`までで、`1`が最小、`7`が最大です。値を明示的に設定しない場合のデフォルト値は`3`です。

この要素はまだ動作しますが、フォントサイズと色は常にCSSで設定すべきであり、HTMLで設定すべきではありません。

`center`要素は、コンテナ内のコンテンツを水平方向に中央揃えにするために使われた別の非推奨要素です。以下はテキストと段落要素を含む`center`要素の例です。

インタラクティブエディターを有効にして、`center`タグを`<p>Another example text.</p>`の周りに追加すると、ページ上で中央に配置されるのがわかります。

:::interactive_editor

```html
<center>
  This text is centered.
  <p>HTML is awesome.</p>
</center>

<p>Another example text.</p>
```

:::

次に`big`要素があります。これは囲まれたテキストを周囲のテキストより1段階大きくする非推奨のプレゼンテーショナルHTML要素です。ここでは2つの部分からなる段落の例があります。

インタラクティブエディターを有効にして、`big`タグを`Some other text`の周りに追加し、プレビューウィンドウで変化を確認してください。

:::interactive_editor

```html
<p>
  This text has a normal font size.
  <big>This text is larger.</big>
  Some other text.
</p>
```

:::

フォントサイズは常にCSSで設定すべきなので、この要素は現代のHTMLでは使うべきではありません。

これらはプレゼンテーショナルHTML要素の例でした。しかし、すべて非推奨で推奨されていません。では代わりに何を使うべきでしょうか？見てみましょう。

セマンティックHTMLが現在推奨される方法です。これは要素の内容を表すので、読みやすく理解しやすく、保守もしやすいです。

セマンティックHTMLを使うと検索エンジンがウェブサイトを簡単に理解できます。また、スクリーンリーダーがウェブページの内容を説明するためにセマンティックな情報が必要なため、アクセシビリティにも役立ちます。

セマンティックHTML要素の例には以下があります：

- ドキュメントやセクションのヘッダーを定義する`header`要素
- ナビゲーションリンクを含むセクションのためのナビゲーションセクション要素`nav`
- 関連情報をグループ化する`section`要素
- イラストや図を表す`figure`要素

以下はナビゲーションセクション要素を含む`header`要素の例です。

インタラクティブエディターを有効にして、`<a href="#">Products</a>`を`nav`の中に追加し、プレビューウィンドウで変化を確認してください。

:::interactive_editor

```html
<header>
  <nav>
    <a href="#">Home</a>
    <a href="#">About</a>
    <!--Add the products link here-->

    <a href="#">Contact</a>
  </nav>
</header>
```

:::

セマンティック要素はHTML構造内でその目的が明確に示されています。多くの異なるセマンティックHTML要素があり、プロジェクトのニーズに合うものが必ず見つかります。

よくできました。これでプレゼンテーショナルHTMLとセマンティックHTMLの違いがわかりましたね。セマンティックHTMLは内容を表し、プレゼンテーショナルHTMLは見た目に焦点を当てています。

# --questions--

## --text--

プレゼンテーショナルHTMLとセマンティックHTMLの違いを最もよく表しているのはどれですか？

## --answers--

プレゼンテーショナルHTMLはコンテンツの構造に焦点を当て、セマンティックHTMLは見た目に焦点を当てている。

### --feedback--

「プレゼンテーション」と「セマンティクス」が何を意味し、HTML要素とどう関係するか考えてみてください。

---

セマンティックHTMLはコンテンツの構造に焦点を当て、プレゼンテーショナルHTMLは見た目に焦点を当てている。

---

プレゼンテーショナルHTMLとセマンティックHTMLに違いはない。

### --feedback--

「プレゼンテーション」と「セマンティクス」が何を意味し、HTML要素とどう関係するか考えてみてください。

---

両方ともスタイルに焦点を当てているが、プレゼンテーショナルHTMLの方がよりモダンである。

### --feedback--

「プレゼンテーション」と「セマンティクス」が何を意味し、HTML要素とどう関係するか考えてみてください。

## --video-solution--

2

## --text--

次のうちプレゼンテーショナルHTMLの例はどれですか？

## --answers--

ヘッダーを定義するために`header`要素を使う。

### --feedback--

どの要素がコンテンツの見た目を直接制御するか考えてみてください。

---

ナビゲーションセクションを定義するために`nav`要素を使う。

### --feedback--

どの要素がコンテンツの見た目を直接制御するか考えてみてください。

---

独立したコンテンツを定義するために`article`要素を使う。

### --feedback--

どの要素がコンテンツの見た目を直接制御するか考えてみてください。

---

コンテンツを中央に配置するために`center`要素を使う。

## --video-solution--

4

## --text--

次のうちセマンティックHTMLの例はどれですか？

## --answers--

テキストを赤くするために`<font color="red">`を使う。

### --feedback--

どの要素がコンテンツの意味を表すか考えてみてください。

---

コンテンツを中央に配置するために`center`要素を使う。

### --feedback--

どの要素がコンテンツの意味を表すか考えてみてください。

---

ナビゲーションセクションを表すために`nav`要素を使う。

---

テキストを大きくするために`big`要素を使う。

### --feedback--

どの要素がコンテンツの意味を表すか考えてみてください。

## --video-solution--

3
