---
id: 672d45651d83b450801efb3a
title: VS Codeでプロジェクトを作成し、ローカルでコードを実行する方法
challengeType: 19
dashedName: how-to-create-a-project-and-run-your-code-locally-in-vs-code
---

# --description--

始めるにあたって、VS Codeではプロジェクトを「ワークスペース」と見なしていることを知っておく必要があります。ワークスペースとは、VS Codeで開いたディレクトリのことです。

まず、新しいプロジェクト用の新しいディレクトリを作成するために、コマンドラインインターフェースを使う必要があります。ディレクトリとはフォルダーの別名です。

コマンドラインインターフェース、略してCLIは、テキストベースのコマンドを使ってオペレーティングシステムとやり取りするためのものです。これらのツールについては今後のレッスンで詳しく学びます。

Windowsの場合は、コマンドプロンプトやPowerShellを使えます。Macの場合は、ターミナルアプリを開いてください。

コマンドラインが開いたら、`cd ~` コマンドを入力してキーボードの `enter` を押し、ホームディレクトリに移動します。次に `mkdir my-project` コマンドを入力して `enter` を押します。`mkdir`、つまり「make directory」は新しいディレクトリを作成するためのコマンドです。その後、新しいディレクトリをVS Codeで開いてください。

他にも新しいディレクトリを作成するグラフィカルな方法があります。

オペレーティングシステムによっては、ファイルエクスプローラーのコンテキストメニュー（右クリックメニュー）から直接VS Codeを開くオプションがあるかもしれません。または、VS Codeのアプリケーションをダブルクリックして開くこともできます。コマンドラインに慣れている場合は、`code /path/to/folder` コマンドを実行することもできます。

開くと、新しい空のワークスペースが表示されるはずです。

新しいファイルを作成するには、左上の`File` メニューをクリックし、`New File...` を選択してください。次にファイル名を`index.html` として、`Save` をクリックします。

すると、エクスプローラータブに新しいファイルが表示されます。`index.html` ファイルにHTMLを追加してみましょう。

HTMLファイルを直接ブラウザで開きたくなるかもしれませんが、それは動作する場合もありますが、CSSが正しく読み込まれないなど予期しないバグが発生することがあります。

代わりに、HTMLファイルは適切なウェブサーバーの背後に置くべきです。これはコーディング初心者には難しく感じるかもしれませんが、実はVS Codeから簡単にできます。

まず、マーケットプレイスで無料で入手できる_Live Server_拡張機能をインストールする必要があります。

左下の拡張機能タブをクリックし、検索バーに`Live Server` と入力してください。

インストールが完了すると、ステータスバーに`Go live` ボタンが表示されます。HTMLファイルを開いた状態で、そのボタンをクリックしてください。

新しいページがブラウザの新しいタブで自動的に開くはずです。もし開かない場合は、直接`http://localhost:5500/` にアクセスしてください。

おめでとうございます！これでVS Codeで自分のプロジェクトを設定し、ライブサーバーで変更を確認できるようになりました。

# --questions--

## --text--

Visual Studio Codeは「ワークスペース」を何と考えていますか？

## --answers--

ハードドライブ全体。

### --feedback--

レッスンでは、VS Codeがワークスペースをディレクトリに関連付けて定義していることが説明されています。

---

VS Codeを開いたディレクトリ。

---

VS Codeで開いているすべてのファイル。

### --feedback--

レッスンでは、VS Codeがワークスペースをディレクトリに関連付けて定義していることが説明されています。

---

VS Codeアプリケーションのフォルダー。

### --feedback--

レッスンでは、VS Codeがワークスペースをディレクトリに関連付けて定義していることが説明されています。

## --video-solution--

2

## --text--

なぜレッスンでは、HTMLファイルをブラウザで直接開くのではなくウェブサーバーを使うことを勧めていますか？

## --answers--

読み込みが速くなり、コードベースの問題を見つけやすくなるから。

### --feedback--

レッスンでは、HTMLファイルを直接開くと特定の問題が起こることが説明されています。

---

大規模なソフトウェアアプリケーションのセキュリティが向上するから。

### --feedback--

レッスンでは、HTMLファイルを直接開くと特定の問題が起こることが説明されています。

---

CSSが正しく読み込まれないなど、予期しないバグを避けられるから。

---

チームの他の開発者とリアルタイムで共同作業できるから。

### --feedback--

レッスンでは、HTMLファイルを直接開くと特定の問題が起こることが説明されています。

## --video-solution--

3

## --text--

ローカルウェブサーバーを簡単に設定するために推奨されているVS Codeの拡張機能は何ですか？

## --answers--

Web Server。

### --feedback--

レッスンでは、HTMLファイルを提供するためにマーケットプレイスから拡張機能をインストールすることが説明されています。

---

Live Server。

---

Local Host。

### --feedback--

レッスンでは、HTMLファイルを提供するためにマーケットプレイスから拡張機能をインストールすることが説明されています。

---

HTML Preview。

### --feedback--

レッスンでは、HTMLファイルを提供するためにマーケットプレイスから拡張機能をインストールすることが説明されています。

## --video-solution--

2
