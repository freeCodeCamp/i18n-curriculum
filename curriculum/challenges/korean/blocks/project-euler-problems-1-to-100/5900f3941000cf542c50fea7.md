---
id: 5900f3941000cf542c50fea7
title: '문제 40: 챔퍼노운 상수'
challengeType: 1
forumTopicId: 302066
dashedName: problem-40-champernownes-constant
---

# --description--

양의 정수를 이어 붙여서 무리 소수 분수를 만듭니다:

0.12345678910**1**112131415161718192021...

소수 부분의 12번째 숫자가 1임을 알 수 있습니다.

*d<sub>n</sub>*이 소수 부분의 *n*번째 숫자를 나타낸다면, 다음 표현식의 값을 구하세요.

d<sub>1</sub> × d<sub>10</sub> × d<sub>100</sub> × d<sub>1000</sub> × d<sub>10000</sub> × d<sub>100000</sub> × d<sub>1000000</sub>

# --hints--

`champernownesConstant(100)`은 숫자를 반환해야 합니다.

```js
assert(typeof champernownesConstant(100) === 'number');
```

`champernownesConstant(100)`은 5를 반환해야 합니다.

```js
assert.strictEqual(champernownesConstant(100), 5);
```

`champernownesConstant(1000)`은 15를 반환해야 합니다.

```js
assert.strictEqual(champernownesConstant(1000), 15);
```

`champernownesConstant(1000000)`은 210을 반환해야 합니다.

```js
assert.strictEqual(champernownesConstant(1000000), 210);
```

# --seed--

## --seed-contents--

```js
function champernownesConstant(n) {

  return true;
}

champernownesConstant(100);
```

# --solutions--

```js
function champernownesConstant(n) {
  let fractionalPart = '';
  for (let i = 0; fractionalPart.length <= n; i++) {
    fractionalPart += i.toString();
  }

  let product = 1;
  for (let i = 0; i < n.toString().length; i++) {
    const index = 10 ** i;
    product *= parseInt(fractionalPart[index], 10);
  }

  return product;
}
```
