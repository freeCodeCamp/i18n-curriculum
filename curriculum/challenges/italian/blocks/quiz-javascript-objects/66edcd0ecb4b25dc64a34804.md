---
id: 66edcd0ecb4b25dc64a34804
title: Quiz sugli oggetti in JavaScript
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale delle seguenti opzioni NON descrive un oggetto in JavaScript?

#### --distractors--

Una collezione di coppie chiave-valore.

---

Un contenitore per proprietà e metodi.

---

Un tipo di dato non primitivo.

#### --answer--

Un tipo di dato primitivo.

### --question--

#### --text--

Quale delle seguenti definizioni di oggetto è invalida?

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

Come puoi consultare la proprietà `job` di questo oggetto `person`?

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

Come puoi consultare la proprietà `street` dell'oggetto `person` qui sotto?

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

Quale di queste chiavi di proprietà può essere consultata solo usando la notazione a parentesi?

#### --distractors--

Una chiave di proprietà nota e statica.

---

Una chiave di proprietà annidata in più livelli di un oggetto.

---

Una chiave di proprietà che potrebbe non esistere nell'oggetto.

#### --answer--

Una chiave di proprietà dinamica.

### --question--

#### --text--

Quale delle seguenti opzioni usa correttamente la destrutturazione per consultare la proprietà `name` dell'oggetto `person` qui sotto?

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

Quale delle seguenti opzioni usa correttamente la destrutturazione per consultare la proprietà `flour` dell'oggetto `recipe` qui sotto?

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

Come puoi assegnare un valore predefinito a una chiave di proprietà quando usi la destrutturazione in JavaScript?

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

Come puoi verificare se un oggetto `car` ha una proprietà `year`?

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

Cos'è un metodo di un oggetto?

#### --distractors--

Una stringa associata a un oggetto.

---

Un array associato a un oggetto.

---

Un numero associato a un oggetto.

#### --answer--

Una funzione associata a un oggetto.

### --question--

#### --text--

Come puoi chiamare il metodo `add` di questo oggetto `calculator` per aggiungere i numeri `10` e `7`?

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

Quale operatore rimuove una proprietà da un oggetto?

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

Come puoi verificare se una proprietà `score` esiste in un oggetto `player`?

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

Come puoi creare un nuovo oggetto vuoto in JavaScript?

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

Qual è la sintassi corretta per usare il concatenamento opzionale in JavaScript?

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

A cosa serve il concatenamento opzionale in JavaScript?

#### --distractors--

Serve per definire variabili con valori predefiniti.

---

Serve per definire metodi opzionali all'interno di un oggetto che potrebbero o meno essere chiamati.

---

Serve per scrivere codice più conciso concatenando più chiamate di metodi in un'unica istruzione.

#### --answer--

Serve per consultare in sicurezza proprietà di un oggetto che potrebbero essere `null` o `undefined`.

### --question--

#### --text--

Quale metodo si usa per convertire un oggetto JavaScript in una stringa JSON?

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

Quale metodo converte una stringa JSON di nuovo in un oggetto JavaScript?

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

Quale di queste opzioni consulta la proprietà `color` di un oggetto `toy` usando la notazione a parentesi?

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

Come puoi consultare il telefono `home` di questo oggetto `learner` in JavaScript usando la notazione a punto?

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`
