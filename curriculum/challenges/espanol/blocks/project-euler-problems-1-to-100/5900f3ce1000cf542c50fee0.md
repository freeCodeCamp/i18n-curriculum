---
id: 5900f3ce1000cf542c50fee0
title: 'Problema 97: Primo grande no-Mersenne'
challengeType: 1
forumTopicId: 302214
dashedName: problem-97-large-non-mersenne-prime
---

# --description--

El primer primo conocido que supera un millón de dígitos fue descubierto en 1999, y es un primo de Mersenne de la forma $2^{6972593} − 1$; contiene exactamente 2,098,960 dígitos. Posteriormente, se han encontrado otros primos de Mersenne, de la forma $2^p − 1$, que contienen más dígitos.

Sin embargo, en 2004 se encontró un primo no-Mersenne masivo que contiene 2,357,207 dígitos: $28433 × 2^{7830457} + 1$.

Encuentra los últimos diez dígitos de ese primo no-Mersenne en la forma $multiplier × 2^{power} + 1$.

# --hints--

`largeNonMersennePrime(19, 6833086)` debería devolver una cadena.

```js
assert(typeof largeNonMersennePrime(19, 6833086) === 'string');
```

`largeNonMersennePrime(19, 6833086)` debería devolver la cadena `3637590017`.

```js
assert.strictEqual(largeNonMersennePrime(19, 6833086), '3637590017');
```

`largeNonMersennePrime(27, 7046834)` debería devolver la cadena `0130771969`.

```js
assert.strictEqual(largeNonMersennePrime(27, 7046834), '0130771969');
```

`largeNonMersennePrime(6679881, 6679881)` debería devolver la cadena `4455386113`.

```js
assert.strictEqual(largeNonMersennePrime(6679881, 6679881), '4455386113');
```

`largeNonMersennePrime(28433, 7830457)` debería devolver la cadena `8739992577`.

```js
assert.strictEqual(largeNonMersennePrime(28433, 7830457), '8739992577');
```

# --seed--

## --seed-contents--

```js
function largeNonMersennePrime(multiplier, power) {

  return true;
}

largeNonMersennePrime(19, 6833086);
```

# --solutions--

```js
function largeNonMersennePrime(multiplier, power) {
  function modStepsResults(number, other, mod, startValue, step) {
    let result = startValue;
    for (let i = 0; i < other; i++) {
      result = step(number, result) % mod;
    }
    return result;
  }

  const numOfDigits = 10;
  const mod = 10 ** numOfDigits;
  const digitsAfterPower = modStepsResults(2, power, mod, 1, (a, b) => a * b);
  const digitsAfterMultiply = modStepsResults(
    digitsAfterPower,
    multiplier,
    mod,
    0,
    (a, b) => a + b
  );
  const lastDigits = (digitsAfterMultiply + 1) % mod;

  return lastDigits.toString().padStart(10, '0');
}
```
