---
id: 67f41242431cbf3db8ca79c7
title: Python 基础测验
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪个函数用于查看变量是否匹配特定的数据类型？

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

以下哪项不是字串串接的形式？

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

以下哪个函数用于返回字串中的字符数？

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

在此示例中，`result` 会是什么？

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

当你将一个浮点数和一个整数相加时会发生什么？

#### --distractors--

结果将是一个错误信息。

---

结果将是 `None`。

---

结果将是一个整数。

#### --answer--

结果将是一个浮点数。

### --question--

#### --text--

以下哪种方式是正确的 `define` 一个 `function`？

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

控制台将打印什么？

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

以下哪条语句在命名变量时是错误的？

#### --distractors--

变量名只能以字母或下划线（_）开头，不能以数字开头。

---

变量名只能包含字母数字字符（a-z、A-Z、0-9）和下划线（_）。

---

变量名不能是 Python 的保留关键字之一，例如 `if`、`class` 或 `def`。

#### --answer--

变量名最长必须为 10 个字符，否则 Python 会抛出误差。

### --question--

#### --text--

以下哪项不是 Python 中支持的数据类型？

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

以下哪项将返回一个所有字符都转换为大写的新建字串？

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

以下哪个函数用于从用户获取输入？

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

下面代码的结果是什么？

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

`split()` 方法的作用是什么？

#### --distractors--

此方法用于将元组拆分为子字串的列表。

---

此方法用于将浮点数拆分为子字串的列表。

---

此方法用于将字典拆分为子字串的列表。

#### --answer--

此方法用于将字串拆分为子串的列表。

### --question--

#### --text--

以下内容将打印到控制台什么？

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

以下哪个函数用于创建 1 对 1 字符映射的 `table` 以进行翻译？

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

下面代码的结果是什么？

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

如果在字串中未找到任何子串出现，`find()` 方法将返回什么？

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

以下哪个函数用于计算子字串在字串中出现的次数？

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

Python 中的地板除法是做什么的？

#### --distractors--

此操作符用于将两个数字相乘并将结果向上取整到最接近的整数。

---

此操作符用于将浮点数转换为整数。

---

此操作符用于将一个数字提升到另一个数字的幂。

#### --answer--

此操作符用于将两个数字相除并将结果向下取整到最接近的整数。

### --question--

#### --text--

以下哪个函数用于将数字四舍五入到最接近的整数？

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`

