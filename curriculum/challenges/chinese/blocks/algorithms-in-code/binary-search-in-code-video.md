---
id: 698dcc2c7932b3cc4b19c927
title: 代码中的二分查找
challengeType: 11
videoId: mg7F5D8Wk5o
dashedName: binary-search-in-code
---

# --description--

在这个 `video` 中，你将使用 Python 编写 `binary search` `algorithm`。

# --questions--

## --text--

为什么在方案中使用了 `while first <= last:` 条件？

## --answers--

确保只查看数组的前半部分。

---

使 `loop` 永远运行，直到手动停止。

---

跳过查找范围内的最后一个元素的查看。

---

只要 `range` 中仍有元素，就继续 `search`，当 `range` 为空时停止。

## --video-solution--

4
