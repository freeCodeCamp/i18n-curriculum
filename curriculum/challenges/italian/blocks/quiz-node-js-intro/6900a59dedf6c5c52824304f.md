---
id: 6900a59dedf6c5c52824304f
title: Quiz introduttivo su NodeJS
challengeType: 8
dashedName: quiz-node-js-intro
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 9 delle 10 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Perché JavaScript lato client ha un accesso limitato al file system locale?

#### --distractors--

Perché JavaScript è un linguaggio di programmazione lento.

---

Perché i browser non hanno abbastanza memoria per gestire i file.

---

Perché l'API DOM impedisce l'accesso ai file.

#### --answer--

Perché è stato progettato per funzionare nei browser web dove un accesso illimitato ai file rappresenterebbe un rischio per la sicurezza.

### --question--

#### --text--

Cosa significa che Node.js è "cross-platform"?

#### --distractors--

Può eseguire più linguaggi di programmazione contemporaneamente.

---

Può collegare insieme diversi database.

---

Richiede metodi di installazione diversi per ogni sistema operativo.

#### --answer--

Funziona su qualsiasi sistema operativo, inclusi Windows, macOS e Linux.

### --question--

#### --text--

Quale API è accessibile nell'ambiente browser ma NON in quello Node.js?

#### --distractors--

L'API del file system

---

L'API di rete

---

L'API del sistema operativo

#### --answer--

L'API DOM

### --question--

#### --text--

Cos'è `npm` nel contesto di Node.js?

#### --distractors--

Un framework JavaScript per costruire interfacce utente.

---

Un browser web ottimizzato per applicazioni Node.js.

---

Uno strumento di debugging per applicazioni Node.js.

#### --answer--

Un gestore di pacchetti per Node.js che permette di installare e gestire pacchetti e moduli.

### --question--

#### --text--

Cosa indica la flag `--lts` quando si esegue `nvm install --lts`?

#### --distractors--

Installa l'ultima versione snapshot per i test.

---

Installa la versione più leggera e piccola disponibile.

---

Installa la versione con più funzionalità.

#### --answer--

Installa la versione Long-Term Support che dà priorità a stabilità, affidabilità e sicurezza.

### --question--

#### --text--

Cos'è una "callback" nel contesto della programmazione asincrona in Node.js?

#### --distractors--

Un metodo che riavvia un'operazione fallita.

---

Una variabile che memorizza il risultato di operazioni sincrone.

---

Uno strumento che monitora le prestazioni dell'applicazione.

#### --answer--

Una funzione che definisce cosa succede quando le operazioni asincrone sono completate.

### --question--

#### --text--

Qual è lo scopo del file `package.json` creato da `npm init`?

#### --distractors--

Memorizzare le variabili d'ambiente per l'applicazione.

---

Configurare le impostazioni del server web.

---

Definire la struttura HTML dell'applicazione.

#### --answer--

Tenere traccia dei dettagli e delle dipendenze del progetto.

### --question--

#### --text--

Perché le attività intensive per la CPU possono essere problematiche in Node.js?

#### --distractors--

Node.js non può eseguire operazioni matematiche.

---

Node.js non supporta crittografia o elaborazione di immagini.

---

Il loop degli eventi impedisce l'esecuzione di operazioni sulla CPU.

#### --answer--

Possono bloccare il thread principale poiché Node.js gira su un singolo thread, causando problemi di prestazioni.

### --question--

#### --text--

Quale comando useresti per eseguire un file JavaScript chiamato `server.js` usando Node.js?

#### --distractors--

`npm run server.js`

---

`nvm execute server.js`

---

`javascript server.js`

#### --answer--

`node server.js`

### --question--

#### --text--

Cosa dovrebbero controllare gli sviluppatori prima di scegliere pacchetti da npm?

#### --distractors--

Se il pacchetto ha almeno 1000 download.

---

Se il pacchetto è stato creato di recente.

---

Se il pacchetto richiede un pagamento.

#### --answer--

Se il pacchetto è mantenuto e segue le migliori pratiche di qualità e sicurezza.
