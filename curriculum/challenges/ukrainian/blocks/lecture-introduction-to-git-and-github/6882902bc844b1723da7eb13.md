---
id: 6882902bc844b1723da7eb13
title: Як встановити та налаштувати Git?
challengeType: 19
dashedName: how-do-you-install-and-set-up-git
---

# --description--

Перш ніж почати справді користуватися системою контролю версій і такими сайтами, як GitHub, потрібно встановити Git і підготувати його до роботи.

Щоб перевірити, чи встановлений Git на вашому комп’ютері, можна виконати таку команду в терміналі:

```sh
git --version
```

Якщо ви побачите номер версії, це означає, що Git встановлений. Якщо ні — потрібно його встановити.

Для систем Linux Git часто встановлений за замовчуванням у більшості дистрибутивів. Якщо Git не встановлений, ви зможете встановити його за допомогою команд вашого пакетного менеджера, наприклад `sudo apt-get install git` або `sudo pacman -S git`.

Для користувачів Mac можна встановити Git через Homebrew за допомогою `brew install git`, або скачати інсталяційний файл з офіційного сайту Git.

Для Windows можна скачати інсталяційний файл з сайту Git. Або, якщо ви налаштували Chocolatey, можна виконати `choco install git.install` у PowerShell. Зверніть увагу, що на Windows також варто скачати GitBash, щоб мати середовище оболонки, схоже на Unix.

Щоб переконатися, що встановлення пройшло успішно, знову виконайте команду `git --version` у терміналі.

Після встановлення Git потрібно внести кілька змін у конфігурацію. Ці зміни потрібно зробити лише один раз на комп’ютері, і не доведеться повторювати їх при оновленнях.

`git config` використовується для встановлення змінних конфігурації, які визначають, як Git працює на вашому комп’ютері. Щоб переглянути поточні налаштування і де вони зберігаються у вашій системі, виконайте таку команду:

```sh
git config --list --show-origin
```

Зараз ви повинні бачити лише системні налаштування, якщо Git встановлений вперше. Якщо Git був встановлений раніше, ви можете бачити налаштування користувача, такі як ім’я користувача та е-пошта.

Щоб встановити ім’я користувача, виконайте таку команду:

```sh
git config --global user.name "Jane Doe"
```

Прапорець `--global` тут використовується, щоб встановити ім’я користувача для всіх проєктів на вашій системі, які використовують Git. Якщо потрібно замістити ім’я користувача для конкретного проєкту, виконайте команду у каталозі цього проєкту без прапорця `--global`.

Щоб встановити адресу е-пошти користувача, виконайте таку команду:

```sh
git config --global user.email janedoe@example.com
```

Налаштування імені користувача та е-пошти важливе, бо Git використовуватиме цю інформацію для кожного коміту у проєкті. Ви дізнаєтеся більше про коміти у наступних уроках.

Ще одну конфігурацію, яку можна встановити — це улюблений редактор, який Git буде використовувати. Ось приклад, як встановити Emacs як улюблений редактор:

```sh
git config --global core.editor emacs
```

Інші варіанти включають Vim, Nano, VS Code та інші. Наприклад, якщо ви використовуєте VS Code як редактор, можна налаштувати Git так:

```sh
git config --global core.editor "code --wait"
```

Якщо ви не встановите улюблений редактор, Git за замовчуванням використовуватиме редактор вашої системи.

Якщо ви користуєтеся Windows і хочете змінити редактор, потрібно вказати повний шлях до виконуваного файлу, наприклад:

```sh
git config --global core.editor "'C:/Program Files/Notepad++/notepad++.exe' -multiInst -notabbar -nosession -noPlugin"
```

Щоб побачити список ваших налаштувань, виконайте таку команду:

```sh
git config --list
```

Тепер, коли Git встановлений і налаштований, ви готові почати додавати контроль версій до своїх проєктів.

# --questions--

## --text--

Що робить наступна команда?

```sh
git config --list --show-origin
```

## --answers--

Показує список лише редакторів, доступних для використання з Git, і де вони зберігаються у вашій системі.

### --feedback--

Поверніться до розділу про налаштування Git для відповіді.

---

Показує список лише кольорових опцій у вашій конфігурації Git.

### --feedback--

Поверніться до розділу про налаштування Git для відповіді.

---

Показує ваші поточні змінні налаштувань і де вони зберігаються у вашій системі.

---

Показує список усіх проєктів на вашій системі, які використовують Git.

### --feedback--

Поверніться до розділу про налаштування Git для відповіді.

## --video-solution--

3

## --text--

Яка мета використання прапорця `--global` при налаштуванні імені користувача?

## --answers--

Прапорець `--global` використовується, щоб встановити ім’я користувача для деяких проєктів на вашій системі, які використовують Git.

### --feedback--

Подумайте, як цей прапорець впливає на всі проєкти, що використовують Git.

---

Прапорець `--global` використовується, щоб встановити ім’я користувача для всіх проєктів на вашій системі, які використовують Git.

---

Прапорець `--global` використовується, щоб не встановлювати ім’я користувача для жодного проєкту на вашій системі, які використовують Git.

### --feedback--

Подумайте, як цей прапорець впливає на всі проєкти, що використовують Git.

---

Прапорець `--global` використовується, щоб встановити ім’я користувача лише для проєктів на вашій системі, яким більше 5 років і які використовують Git.

### --feedback--

Подумайте, як цей прапорець впливає на всі проєкти, що використовують Git.

## --video-solution--

2

## --text--

Який із наведених варіантів НЕ є дійсним для встановлення улюбленого редактора в Git?

## --answers--

Vim

### --feedback--

Перегляньте кінець тексту, де налаштовували редактори Git.

---

Emacs

### --feedback--

Перегляньте кінець тексту, де налаштовували редактори Git.

---

Nano

### --feedback--

Перегляньте кінець тексту, де налаштовували редактори Git.

---

ESLint

## --video-solution--

4
