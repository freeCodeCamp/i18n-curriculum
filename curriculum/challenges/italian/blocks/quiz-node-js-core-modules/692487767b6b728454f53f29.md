---
id: 692487767b6b728454f53f29
title: Quiz sui moduli core di NodeJS
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

Per superare il quiz, devi rispondere correttamente ad almeno 18 delle 20 domande seguenti.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quale metodo `fs` aggiunge contenuto a un file esistente senza sovrascriverlo?

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

Cosa succede quando usi metodi sincroni dal modulo `fs`?

#### --distractors--

Eliminano automaticamente i file esistenti.

---

In tutti i casi sono più veloci di quelli asincroni.

---

Ritornano Promise per impostazione predefinita.

#### --answer--

Bloccano il programma finché l’operazione non termina.

### --question--

#### --text--

Cosa fa il parametro `utf8` in `fs.readFile()`?

#### --distractors--

Impedisce che vengano lanciati errori.

---

Cambia l’estensione del file.

---

Comprimi file di grandi dimensioni.

#### --answer--

Dice a Node di restituire una stringa leggibile invece di un buffer.

### --question--

#### --text--

Quale metodo `Buffer` crea un nuovo buffer riempito di zeri?

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

Cosa fa `Buffer.toString()`?

#### --distractors--

Converte un buffer in codice binario.

---

Cancella il contenuto di un buffer.

---

Mostra la lunghezza in byte del buffer.

#### --answer--

Converte il contenuto del buffer in una stringa leggibile.

### --question--

#### --text--

Quale funzionalità `crypto` richiede una chiave privata per generare una firma?

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

Perché dovresti evitare di scrivere direttamente i tuoi algoritmi crittografici?

#### --distractors--

È illegale nelle applicazioni Node.js.

---

Node.js non supporta la crittografia.

---

JavaScript non può calcolare hash.

#### --answer--

Può essere pericoloso senza librerie ben testate.

### --question--

#### --text--

Quale metodo `crypto` è consigliato per generare token casuali sicuri invece di `Math.random()`?

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

Quale metodo del modulo OS restituisce l’architettura della CPU?

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

Cosa restituisce `os.userInfo()`?

#### --distractors--

Solo il nome utente.

---

Solo l’indirizzo IP del dispositivo.

---

Solo l’id dell’utente connesso.

#### --answer--

Informazioni sull’utente di sistema corrente.

### --question--

#### --text--

Quale metodo `path` unisce segmenti e normalizza le barre in eccesso?

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

Quale metodo `path` produce un percorso assoluto basato sulla directory di lavoro corrente?

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

Cosa rappresenta la variabile globale `__dirname`?

#### --distractors--

Il nome dell’utente che esegue Node.

---

L’estensione del file corrente.

---

Un percorso di cartella relativo che inizia con `./`.

#### --answer--

Il percorso assoluto della directory che contiene il file corrente.

### --question--

#### --text--

Quale proprietà di process contiene variabili d’ambiente come `NODE_ENV`?

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

Cosa fa l’evento `process.on("exit")`?

#### --distractors--

Riavvia l’applicazione Node.

---

Invia un segnale `exit` ai processi figli.

---

Cancella la coda dell’event loop.

#### --answer--

Esegue codice subito prima che il processo Node termini.

### --question--

#### --text--

Quale tipo di stream può sia leggere che scrivere dati ma non li trasforma?

#### --distractors--

Readable stream

---

Transform stream

---

Writable stream

#### --answer--

Duplex stream

### --question--

#### --text--

Cosa serve per implementare la lettura e scrittura efficiente di file di grandi dimensioni?

#### --distractors--

Usare solo `Buffer.alloc()`

---

Importare il modulo `crypto`

---

Salvare prima l’intero file in memoria

#### --answer--

Usare stream per processare i dati a blocchi

### --question--

#### --text--

Quale metodo collega uno stream leggibile a uno scrivibile?

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

Quale evento di stream indica che non ci sono più dati da scrivere?

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

Cosa viene stampato se stampi un blocco da uno stream leggibile senza `toString()`?

#### --distractors--

Contenuto in testo semplice

---

Il PID del processo

---

Il nome del sistema operativo

#### --answer--

Un oggetto buffer con dati binari
