---
id: 65704482e7b02272663824e5
title: 学习块和内联课程 G
challengeType: 15
dashedName: learn-block-and-inline-lesson-g
---

# --description--

既然你对 `block` 和 `inline` 元素有了一些基本了解，请调出几道题之前你列出的 `block` 和 `inline` 元素列表，看看你是否能识别以下哪些元素是 `block` 元素，哪些是 `inline` 元素。

# --questions--    

## --text--

以下哪项不是 `inline` 元素？

## --answers--

形式元素

---

锚点元素

---

图像元素

---

span 元素

## --video-solution--

1
