---
id: 66a334a1a7cca6354999f9bf
title: Step 10
challengeType: 0
dashedName: step-10
---

# --description--

對於第一篇博文，將使用 <dfn>article</dfn> 元素。

`article` 元素表示網頁中的自包含內容。

```html
<article>
  <h1>Example heading</h1>
  <p>Example article text</p>
</article>
```

在 `h2` 元素下方，添加一個 `article` 元素。

在 `article` 元素中，添加一個 `h3` 元素，其文本爲 `Mr. Whiskers' First Day Home`。

這裏使用 `h3` 的原因是，爲標題元素保持適當的結構層級非常重要。 由於博文副標題是一個 `h2` 元素，層級結構中的下一級是 `h3` 。

# --hints--

你的代碼應該有一個 `article` 開始標籤。

```js
assert.match(code, /<article>/i);
```

你的代碼應該有一個 `article` 結束標籤。

```js
assert.match(code, /<\/article>/i);
```

你的 `article` 元素應在 `section` 元素中，`id` 設置爲 `posts`。

```js
const articleElement = document.querySelector('#posts article');
assert.strictEqual(articleElement?.parentElement.tagName, 'SECTION');
```

你的代碼應該有一個 `h3` 開始標籤。

```js
assert.match(code, /<h3>/i);
```

你的代碼應該有一個 `h3` 結束標籤。

```js
assert.match(code, /<\/h3>/i);
```

你的 `h3` 元素的文本應爲 `Mr. Whiskers' First Day Home`。

```js
const h3Element = document.querySelector('h3');
assert.strictEqual(h3Element?.innerText.trim(), "Mr. Whiskers' First Day Home");
```

你的 `h3` 元素應在 `article` 元素中。

```js
const h3Element = document.querySelector('#posts article h3');
assert.strictEqual(h3Element?.parentElement.tagName, 'ARTICLE');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Mr. Whiskers' Blog</title>
    <meta charset="UTF-8" />
  </head>
  <body>
    <header>
      <h1>Welcome to Mr. Whiskers' Blog Page!</h1>
      <figure>
        <img
          src="https://cdn.freecodecamp.org/curriculum/css-photo-gallery/1.jpg"
          alt="a cat in the garden"
        />
        <figcaption>Mr. Whiskers in the Garden</figcaption>
      </figure>
      <nav>
        <ul>
          <li><a href="#about">About</a></li>
          <li><a href="#posts">Posts</a></li>
          <li><a href="#contact">Contact</a></li>
        </ul>
      </nav>
    </header>
    <main>
      <section id="about">
        <h2>About</h2>
        <p>
          Hi there! I'm Jane Doe, a passionate writer who finds endless inspiration in the antics of my beloved cat, Mr. Whiskers.
        </p>
        <p>
          His playful nature and boundless energy keeps me on my toes. I love him so much.
        </p>
      </section>
      <section id="posts">
        <h2>Posts</h2>
--fcc-editable-region--

--fcc-editable-region--
      </section>
    </main>
  </body>
</html>
```
