---
id: 62ff8b9dab5ac88e4d3d43a3
title: Schritt 18
challengeType: 0
dashedName: step-18
---

# --description--

Folgen Sie bewährten Vorgehensweisen zur Barrierefreiheit und verknüpfen Sie die `input`-Elemente mit den `label`-Elementen mithilfe des `for`-Attributs.

Verwenden Sie `first-name`, `last-name`, `email` und `new-password` als Werte für die jeweiligen `id`-Attribute.

# --hints--

Das erste `input`-Element sollte ein `id` mit dem Wert `first-name` haben.

```js
assert(document.querySelectorAll('input')?.[0]?.matches('#first-name'))
```

Das zweite `input`-Element sollte ein `id` mit dem Wert `last-name` haben.

```js
assert(document.querySelectorAll('input')?.[1]?.matches('#last-name'))
```

Das dritte `input`-Element sollte ein `id` mit dem Wert `email` haben.

```js
assert(document.querySelectorAll('input')?.[2]?.matches('#email'))
```

Das vierte `input`-Element sollte ein `id` mit dem Wert `new-password` haben.

```js
assert(document.querySelectorAll('input')?.[3]?.matches('#new-password'))
```

Das erste `label`-Element sollte ein `for`-Attribut mit dem Wert `first-name` haben.

```js
assert(document.querySelectorAll('label')?.[0]?.matches('label[for="first-name"]'))
```

Das zweite `label`-Element sollte ein `for`-Attribut mit dem Wert `last-name` haben.

```js
assert(document.querySelectorAll('label')?.[1]?.matches('label[for="last-name"]'))
```

Das dritte `label`-Element sollte ein `for`-Attribut mit dem Wert `email` haben.

```js
assert(document.querySelectorAll('label')?.[2]?.matches('label[for="email"]'))
```

Das vierte `label`-Element sollte ein `for`-Attribut mit dem Wert `new-password` haben.

```js
assert(document.querySelectorAll('label')?.[3]?.matches('label[for="new-password"]'))
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Registration Form</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <h1>Registration Form</h1>
    <p>Please fill out this form with the required information</p>
    <form method="post" action='https://register-demo.freecodecamp.org'>
--fcc-editable-region--
      <fieldset>
        <label>Enter Your First Name: <input /></label>
        <label>Enter Your Last Name: <input /></label>
        <label>Enter Your Email: <input /></label>
        <label>Create a New Password: <input /></label>
      </fieldset>
--fcc-editable-region--
      <fieldset></fieldset>
      <fieldset></fieldset>
    </form>
  </body>
</html>
```

```css
body {
  width: 100%;
  height: 100vh;
  margin: 0;
  background-color: #1b1b32;
  color: #f5f6f7;
}

label {
  display: block;
  margin: 0.5rem 0;
}

```
