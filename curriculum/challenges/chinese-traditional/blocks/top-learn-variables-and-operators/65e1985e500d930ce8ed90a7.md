---
id: 65e1985e500d930ce8ed90a7
title: 學習變數和運算子 第 D 課
challengeType: 15
dashedName: learn-variables-and-operators-lesson-d
---

# --description--

JavaScript 中對變數名稱有兩個限制：

1. 名稱必須只包含字母、數字或符號 `$` 和 `_`。
1. 第一個字元不得為數字。

有效名稱的範例：

```js
let userName;
let test123;
```

有趣的是－美元符號 `'$'` 和底線 `'_'` 也可以用在名稱中。它們是一般符號，就像字母一樣，沒有任何特殊意義。

這些名稱是有效的：

```js
let $ = 1; // declared a variable with the name "$"
let _ = 2; // and now a variable with the name "_"

console.log($ + _); // 3
```

錯誤變數名稱的範例：

```js
let 1a; // cannot start with a digit

let my-name; // hyphens '-' aren't allowed in the name
```

# --questions--

## --text--

以下哪一項是有效的 JavaScript 變數名稱？

## --answers--

`let 2things;`

---

`let my-name;`

---

`let var!;`

---

`let $myVar;`


## --video-solution--

4
