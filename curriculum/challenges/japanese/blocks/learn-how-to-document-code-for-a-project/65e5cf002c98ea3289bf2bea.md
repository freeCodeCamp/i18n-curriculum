---
id: 65e5cf002c98ea3289bf2bea
title: タスク 14
challengeType: 19
dashedName: task-14
lang: en-US
---

# --description--

`y`で終わる名詞が複数形になるとき、`y`はしばしば`ies.`に変わります。このルールは、特定の用語が`y.`で終わることが多い技術分野で特に役立ちます。

# --questions--

## --text--

`y`で終わる名詞の複数形はどのように作りますか？

## --answers--

`y`を`ys`に変えることで

### --feedback--

これは一般的なルールではありません。通常、複数形では`y`が`ies`に変わります。例えば、`query`が`queries.`になるように。

---

`s`を追加することで

### --feedback--

`y`で終わるほとんどの名詞は、単に`s`を追加するだけでは正しくありません。例えば、`directory`は`directories`になり、`directorys.`にはなりません。

---

`y`を`ies`に変えることで

---

`y`を取り除くことで

### --feedback--

`y`を取り除くのは複数形の正しい作り方ではありません。複数形では通常、`y`が`ies`に変わります。例えば、`binary`が`binaries.`になるように。

## --video-solution--

3
