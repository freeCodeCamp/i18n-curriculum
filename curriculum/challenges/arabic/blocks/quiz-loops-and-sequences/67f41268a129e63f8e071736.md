---
id: 67f41268a129e63f8e071736
title: اختبار الحلقات والمتتاليات
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--

لنجاح الاختبار، يجب أن تجيب بشكل صحيح على 18 سؤالًا على الأقل من أصل 20 سؤالًا أدناه.

# --quizzes--

## --quiz--

### --question--

#### --text--

أي مما يلي هو الصيغة النحوية الصحيحة لقائمة؟

#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--

ماذا سيُطبع في وحدة التحكم؟

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--

ما هو الكائن القابل للتكرار؟

#### --distractors--

هو نوع خاص من السلاسل النصية يمكنه احتواء كمية كبيرة من الأحرف.

---

هو نوع خاص من الأعداد يُستخدم لتمثيل قيم كبيرة جدًا.

---

هو نوع بيانات خاص يُستخدم بشكل رئيسي في الأصناف والدوال الخاصة.

#### --answer--

هو نوع خاص من الكائنات يمكن التكرار عليه عنصرًا واحدًا في كل مرة.

### --question--

#### --text--

ماذا سيُطبع التالي في وحدة التحكم؟

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--

أي مما يلي يُستخدم للحصول على عدد العناصر في القائمة؟

#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--

ماذا يحدث إذا حاولت تمرير فهرس (موجب أو سالب) خارج حدود القائمة؟

#### --distractors--

ستتلقى `SyntaxError`.

---

ستتلقى `RangeError`.

---

ستتلقى `TypeError`.

#### --answer--

ستتلقى `IndexError`.

### --question--

#### --text--

ماذا سيُطبع في وحدة التحكم؟

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--

أي من الطرق التالية تُستخدم لإضافة عنصر إلى نهاية القائمة؟

#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--

ماذا سيُطبع في وحدة التحكم؟

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--

أي من الطرق التالية تُستخدم لإدخال عنصر في فهرس محدد داخل القائمة؟

#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--

ماذا سيُطبع في وحدة التحكم؟

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--

أي من الطرق التالية تُستخدم لتفريغ القائمة؟

#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

ما هو الصف المرتب (tuple)؟

#### --distractors--

هو نوع بيانات قابل للتغيير يحتوي على متتالية عشوائية من القيم.

---

هو نوع بيانات خاص يُستخدم لتمثيل الأعداد الكبيرة.

---

هو طريقة تُستخدم لإضافة عناصر إلى القائمة.

#### --answer--

هو متتالية مرتبة غير قابلة للتغيير من القيم.

### --question--

#### --text--

أي مما يلي هو الطريقة الصحيحة لفك تجميع العناصر من صف مرتب؟

#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--

ما هي نتيجة الشفرة التالية؟

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--

سينتج عنها `RangeError`.

---

لن يحدث شيء.

---

سينشئ حلقة لا نهائية.

#### --answer--

سينتج عنها `TypeError`.

### --question--

#### --text--

ما هي النتيجة للشفرة التالية؟

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--

أي من الدوال التالية تُستخدم لإنشاء قائمة جديدة بالقيم المرتبة؟

#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--

أي مما يلي هو الطريقة الصحيحة لاستخدام حلقة `for`؟

#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--

أي من الدوال التالية تُستخدم لتوليد متتالية من الأعداد الصحيحة؟

#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--

أي من الدوال التالية تُستخدم للتكرار على عدة كائنات قابلة للتكرار بالتوازي؟

#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`

