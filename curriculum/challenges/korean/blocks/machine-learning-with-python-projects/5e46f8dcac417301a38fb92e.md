---
id: 5e46f8dcac417301a38fb92e
title: 고양이와 강아지 이미지 분류기
challengeType: 10
forumTopicId: 462377
dashedName: cat-and-dog-image-classifier
---

# --description--

<a href="https://colab.research.google.com/github/freeCodeCamp/boilerplate-cat-and-dog-image-classifier/blob/master/fcc_cat_dog.ipynb" target="_blank" rel="noopener noreferrer nofollow">Google Colaboratory에서 이 프로젝트를 진행할 것입니다</a>.

해당 링크로 이동한 후, 노트북을 자신의 계정이나 로컬에 복사하세요. 프로젝트를 완료하고 테스트(해당 링크에 포함됨)를 통과하면 아래에 프로젝트 링크를 제출하세요. Google Colaboratory 링크를 제출하는 경우, "링크가 있는 모든 사용자"에 대해 링크 공유를 켜야 합니다.

머신러닝 커리큘럼의 대화형 교육 콘텐츠는 아직 개발 중입니다. 지금은 이 인증 과정의 비디오 도전 과제를 진행할 수 있습니다. 실제 프로젝트를 진행할 때처럼 추가 학습 자료를 찾아봐야 할 수도 있습니다.

# --instructions--

이번 도전 과제에서는 강아지와 고양이 이미지를 분류하는 코드를 완성할 것입니다. TensorFlow 2.0과 Keras를 사용해 고양이와 강아지 이미지를 최소 63% 정확도로 올바르게 분류하는 합성곱 신경망을 만드세요. (70% 정확도 달성 시 추가 점수!)

일부 코드는 제공되지만, 도전 과제를 완료하려면 일부 코드를 직접 작성해야 합니다. 각 텍스트 셀의 지시사항을 읽어 각 코드 셀에서 해야 할 일을 파악하세요.

첫 번째 코드 셀은 필요한 라이브러리를 가져옵니다. 두 번째 셀은 데이터를 다운로드하고 주요 변수를 설정합니다. 세 번째 셀부터 직접 코드를 작성할 차례입니다.

다운로드된 데이터셋 파일 구조는 다음과 같습니다 (테스트 디렉터리는 하위 디렉터리가 없고 이미지에 레이블이 없다는 점을 확인할 수 있습니다):

```py
cats_and_dogs
|__ train:
    |______ cats: [cat.0.jpg, cat.1.jpg ...]
    |______ dogs: [dog.0.jpg, dog.1.jpg ...]
|__ validation:
    |______ cats: [cat.2000.jpg, cat.2001.jpg ...]
    |______ dogs: [dog.2000.jpg, dog.2001.jpg ...]
|__ test: [1.jpg, 2.jpg ...]
```

에포크와 배치 크기는 원한다면 조정할 수 있지만 필수는 아닙니다.

다음 지시사항은 셀 번호에 해당하는 주석(`# 3` 등)과 함께 제공됩니다.

## Cell 3

이제 직접 해볼 차례입니다! 이 셀의 각 변수를 올바르게 설정하세요. (더 이상 `None`이 아니어야 합니다.)

세 가지 이미지 데이터셋(학습, 검증, 테스트) 각각에 대해 이미지 생성기를 만드세요. 이미지를 읽고 디코딩하여 부동 소수점 텐서로 변환하려면 `ImageDataGenerator`를 사용하세요. `rescale` 인수(현재는 다른 인수 없이)를 사용해 텐서 값을 0~255 범위에서 0~1 범위로 재조정하세요.

`*_data_gen` 변수에는 `flow_from_directory` 메서드를 사용하세요. 배치 크기, 디렉터리, 대상 크기(`(IMG_HEIGHT, IMG_WIDTH)`), 클래스 모드, 그리고 필요한 다른 인수를 전달하세요. `test_data_gen`이 가장 까다로울 것입니다. `test_data_gen`에는 `shuffle=False`를 `flow_from_directory` 메서드에 전달하세요. 이렇게 하면 최종 예측이 테스트에서 기대하는 순서를 유지합니다. `test_data_gen`에서는 디렉터리 구조를 참고하는 것도 도움이 됩니다.

코드를 실행하면 출력은 다음과 같아야 합니다:

```py
Found 2000 images belonging to 2 classes.
Found 1000 images belonging to 2 classes.
Found 50 images belonging to 1 class.
```

## Cell 4

`plotImages` 함수는 이미지를 몇 번이고 그릴 때 사용됩니다. 이미지 배열과 확률 목록을 인수로 받으며, 확률 목록은 선택 사항입니다. 이 코드는 제공됩니다. `train_data_gen` 변수를 올바르게 만들었다면 이 셀을 실행해 임의의 학습 이미지 5개를 그릴 수 있습니다.

## Cell 5

`train_image_generator`를 `ImageDataGenerator`를 사용해 다시 만드세요.

학습 예제가 적기 때문에 과적합 위험이 있습니다. 이를 해결하는 한 방법은 기존 학습 예제에서 무작위 변환을 사용해 더 많은 학습 데이터를 만드는 것입니다.

`ImageDataGenerator`에 4~6개의 무작위 변환을 인수로 추가하세요. 이전과 동일하게 재조정하는 것도 잊지 마세요.

## Cell 6

이 셀에서는 아무 작업도 할 필요 없습니다. `train_data_gen`는 이전과 같이 새 `train_image_generator`로 생성됩니다. 그런 다음 단일 이미지를 다섯 번 다른 변형으로 그립니다.

## Cell 7

이 셀에서는 클래스 확률을 출력하는 신경망 모델을 만드세요. Keras Sequential 모델을 사용해야 합니다. Conv2D와 MaxPooling2D 층을 쌓고, 그 위에 ReLU 활성화 함수가 적용된 완전 연결층이 있을 것입니다.

모델을 컴파일할 때 최적화 도구와 손실 함수를 설정하는 인수를 전달하세요. 또한 각 학습 에포크의 학습 및 검증 정확도를 보기 위해 `metrics=['accuracy']`도 전달하세요.

## Cell 8

`fit` 메서드를 사용해 `model`에서 네트워크를 학습시키세요. `x`, `steps_per_epoch`, `epochs`, `validation_data`, `validation_steps` 인수도 전달해야 합니다.

## Cell 9

이 셀을 실행해 모델의 정확도와 손실을 시각화하세요.

## Cell 10

이제 모델을 사용해 완전히 새로운 이미지가 고양이인지 강아지인지 예측할 차례입니다.

이 셀에서 각 테스트 이미지(`test_data_gen`)가 강아지인지 고양이인지 확률을 구하세요. `probabilities`은 정수 목록이어야 합니다.

`plotImages` 함수를 호출해 테스트 이미지와 각 이미지에 해당하는 확률을 전달하세요.

셀을 실행하면 50개의 테스트 이미지가 모두 표시되고, 각 이미지가 고양이나 강아지일 확률이 레이블로 나타납니다. 정확도는 위 그래프(이전 셀 실행 후)에 표시된 정확도와 일치합니다. 학습 이미지가 더 많으면 정확도가 더 높아질 수 있습니다.

## Cell 11

마지막 셀을 실행해 도전 과제를 통과했는지 아니면 계속 시도해야 하는지 확인하세요.

# --hints--

모든 Python 테스트를 통과해야 합니다.

```js

```

# --solutions--

```py
  # Python challenges don't need solutions,
  # because they would need to be tested against a full working project.
  # Please check our contributing guidelines to learn more.
```
