---
id: 66edd3011f18f4ee1bd9d28b
title: JavaScript 정규 표현식 퀴즈
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 맞혀야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

다음 중 올바르지 않은 정규 표현식은 무엇인가요?

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

다음 중 `RegExp`가 정규 표현식 `/f[o0]{2} b[a4@]r/i`과 동일한 패턴을 포함하는 것은 무엇인가요?

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

정규 표현식 메서드 `test`의 반환 타입은 무엇인가요?

#### --distractors--

정규 표현식과 일치한 문자열들의 배열입니다.

---

정규 표현식이 일치한 (부분)문자열에 대한 정보를 담은 객체입니다.

---

`null`이며, 메서드 `test`는 주어진 정규 표현식이 유효한지만 검증합니다.

#### --answer--

문자열이 정규 표현식과 일치하는지 여부를 나타내는 불리언입니다.

### --question--

#### --text--

플래그가 없는 정규 표현식을 `match` 메서드에 전달하면 어떤 동작을 하나요?

#### --distractors--

주어진 정규 표현식과 일치하는 모든 문자열을 찾아 배열로 반환합니다.

---

첫 번째 전체 일치를 찾아 그 일치의 시작 인덱스를 반환합니다.

---

첫 번째 전체 일치를 찾아 일치 여부를 나타내는 불리언을 반환합니다.

#### --answer--

첫 번째 전체 일치를 찾아 그 첫 번째 일치를 포함하는 배열을 반환합니다.

### --question--

#### --text--

`test` 메서드의 가장 적절한 사용 사례는 무엇인가요?

#### --distractors--

주어진 정규 표현식의 유효성을 검증할 때 사용합니다.

---

일치한 문자열의 길이 같은 상세 정보를 얻을 때 사용합니다.

---

주어진 문자열에서 모든 일치를 추출할 때 사용합니다.

#### --answer--

주어진 문자열이 정규 표현식과 일치하는지 확인할 때 사용합니다.

### --question--

#### --text--

`replace` 메서드를 사용하는 목적은 무엇인가요?

#### --distractors--

현재 정규 표현식을 새 정규 표현식으로 교체할 때 사용합니다.

---

인덱스와 문자열을 받아 지정한 인덱스의 문자를 해당 문자열로 교체할 때 사용합니다.

---

일치한 모든 부분 문자열을 문자열에서 제거할 때 사용합니다.

#### --answer--

일치한 문자열을 주어진 교체 문자열로 바꾸기 위해 사용합니다.

### --question--

#### --text--

`match`와 `matchAll`의 차이점은 무엇인가요?

#### --distractors--

`match`은 항상 문자열을 반환하는 반면, `matchAll`은 항상 일치 배열을 반환합니다.

---

`match`은 일치 여부를 나타내는 불리언을 반환하고, `matchAll`은 발견된 일치 개수를 나타내는 숫자를 반환합니다.

---

`match`는 첫 번째 일치의 시작 인덱스를 숫자로 반환하고, `matchAll`는 일치가 발견된 인덱스들의 배열을 반환합니다.

#### --answer--

`match`은 일치하는 문자열 배열을 반환하고, `matchAll`은 발견된 모든 일치에 대한 이터레이터 객체를 반환합니다.

### --question--

#### --text--

다음 중 `replaceAll`의 올바른 사용법은 무엇인가요?

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

다음 문자 클래스 중 정규 표현식 `/[a-zA-Z0-9_]/`와 동등한 것은 무엇인가요?

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

전화번호에서 숫자를 추출할 때 가장 적절한 문자 클래스는 무엇인가요?

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

정규 표현식에서 전방 탐색 `(?=)`를 사용하면 어떤 일이 발생하나요?

#### --distractors--

전방 탐색은 문자열의 왼쪽을 확인하여 하위 패턴이 존재하는지 검증합니다.

---

전방 탐색은 주 패턴이 일치하지 않을 때 사용하는 대체 정규 표현식 패턴입니다.

---

전방 탐색은 문자열의 오른쪽을 확인하여 하위 패턴이 존재하지 않는지 검증합니다.

#### --answer--

전방 탐색은 문자열의 오른쪽을 확인하여 하위 패턴이 존재하는지 검증합니다.

### --question--

#### --text--

정규 표현식을 만들 때 전방 탐색 `(?<=)`는 어디에 위치해야 하나요?

#### --distractors--

전방 탐색은 정규 표현식 어디에나 위치할 수 있습니다.

---

전방 탐색은 정규 표현식의 주 패턴 오른쪽에 위치해야 합니다.

---

전방 탐색은 정규 표현식의 주 패턴을 포함해야 합니다.

#### --answer--

전방 탐색은 정규 표현식의 주 패턴 왼쪽에 위치해야 합니다.

### --question--

#### --text--

앞선 요소를 0번 또는 1번 일치시키는 수량자는 무엇인가요?

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

다음 정규 표현식 중 0부터 999,999 사이의 숫자만 허용하는 것은 무엇인가요?

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

커스텀 문자 클래스 (`[]`)에 대해 올바른 설명은 무엇인가요?

#### --distractors--

일치에서 제거할 문자 집합입니다.

---

특수 문자를 이스케이프하지 않고도 일치할 문자 집합을 정의할 수 있습니다.

---

유니코드 형식의 문자 집합을 나타냅니다.

#### --answer--

일치할 커스텀 문자 집합을 정의할 수 있습니다.

### --question--

#### --text--

다음 문자 클래스 중 대문자 알파벳과 올바르게 일치하는 것은 무엇인가요?

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

정규 표현식에서 캡처 그룹 `(...)`를 사용하면 어떤 일이 발생하나요?

#### --distractors--

캡처 그룹은 정규 표현식 내 위치와 상관없이 즉시 평가됩니다.

---

캡처 그룹은 주어진 하위 패턴으로 일치 시도하며 결과를 기억하지 않고 계속 진행합니다.

---

캡처 그룹은 우선순위가 높은 하위 패턴으로, 일치가 발견되면 즉시 반환합니다.

#### --answer--

캡처 그룹은 주어진 하위 패턴으로 일치 시도하며 결과를 기억하면서 계속 진행합니다.

### --question--

#### --text--

정규 표현식에서 비캡처 그룹 `(?:...)`를 사용하면 어떤 일이 발생하나요?

#### --distractors--

비캡처 그룹은 주어진 하위 패턴으로 일치 시도하며 결과를 기억하면서 계속 진행합니다.

---

비캡처 그룹은 주어진 하위 패턴과 일치하면 즉시 종료합니다.

---

비캡처 그룹은 선택적 일치로 간주되어 성공한 일치만 결과에 추가됩니다.

#### --answer--

비캡처 그룹은 주어진 하위 패턴으로 일치 시도하며 결과를 기억하지 않고 계속 진행합니다.

### --question--

#### --text--

다음 정규 표현식이 주어졌을 때, 어떤 테스트가 `true`를 반환하나요?

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

정규 표현식에서 와일드카드 문자 클래스 (`.`)는 어떤 역할을 하나요?

#### --distractors--

공백 문자만 일치시킵니다.

---

문자열의 시작과 일치합니다.

---

한 자리 숫자와 일치합니다.

#### --answer--

줄 바꿈 문자를 제외한 모든 단일 문자와 일치합니다.
