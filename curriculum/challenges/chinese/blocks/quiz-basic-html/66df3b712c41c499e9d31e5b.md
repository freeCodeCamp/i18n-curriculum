---
id: 66df3b712c41c499e9d31e5b
title: 基础 HTML 测验
challengeType: 8
dashedName: quiz-basic-html
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

`audio` 元素中的 `loop` 属性是如何工作的？

#### --distractors--

它允许 `audio` 元素与网页上的其他多媒体元素同步播放。

---

它可以调整音频的音高和音调。

---

它会触发 `audio` 元素以随机顺序停止和重播。

#### --answer--

播放结束后，它会重播音频。

### --question--

#### --text--

HTML 中的空元素是什么？

#### --distractors--

用于在屏幕上嵌入视频的元素。

---

用于显示列表的元素。

---

用于在文档中嵌入声音的元素。

#### --answer--

没有任何内容或结束标签的元素。

### --question--

#### --text--

以下哪项是 `id` 属性的正确值？

#### --distractors--

`id="main@content"`

---

`id="main content"`

---

`id="main~content"`

#### --answer--

`id="main-content"`

### --question--

#### --text--

`target="_parent"` 是如何工作的？

#### --distractors--

它会在新的浏览环境中打开链接。

---

它指定了点击链接后浏览器应滚动到的链接文档中的位置。

---

它会将用户重定向到 `href` 属性中指定的其他网站。

#### --answer--

它会在当前环境的父级中打开一个链接。

### --question--

#### --text--

布尔属性和常规属性的区别是什么？

#### --distractors--

布尔属性只能用于复选框，而常规属性则用于单选按钮。

---

对于图像来说，常规属性总是必需的，而布尔属性则是可选的。

---

布尔属性必须始终赋值，而常规属性则不需要赋值。

#### --answer--

布尔属性可以存在或不存在，而常规属性总是有一个指定值。

### --question--

#### --text--

哪个元素用来将脚本链接到 HTML 文件？

#### --distractors--

`img`

---

`ul`

---

`h2`

#### --answer--

`script`

### --question--

#### --text--

什么是 HTML 模板？

#### --distractors--

网络开发人员用于在文档中添加水印的一种特殊工具。

---

一种标记语言。

---

在页面上自动生成“lorem ipsum”文本的一种插件。

#### --answer--

包含 HTML 文档所需全部基本信息的一种模板。

### --question--

#### --text--

哪个属性可为 HTML 元素应用唯一标识符？

#### --distractors--

`class`

---

`href`

---

`action`

#### --answer--

`id`

### --question--

#### --text--

什么是 HTML 实体（字符引用）？

#### --distractors--

HTML 中字符的一种图形表示。

---

HTML 中使用的一种特殊格式标记。

---

网页设计中用于代表角色的一种图像。

#### --answer--

用于表示 HTML 中的保留字符的一组字符。

### --question--

#### --text--

`audio` 元素的作用是什么？

#### --distractors--

它允许用户直接在网页上录制自己的声音。

---

它用于为页面上的 `audio` 和 `video` 元素设计样式。

---

它用于使网页上的文字在点击时发出声音。

#### --answer--

它用于在 HTML 文档中添加音频。

### --question--

#### --text--

以下哪项不是 HTML 元素的示例？

#### --distractors--

`img`

---

`h1`

---

`link`

#### --answer--

`byte`

### --question--

#### --text--

锚元素中的 `target` 属性的作用是什么？

#### --distractors--

它定义了用户悬停链接时链接的颜色。

---

它指定了点击链接后浏览器应滚动到的链接文档中的位置。

---

它决定 `link` 元素内锚文本的字体大小。

#### --answer--

用于指定当用户点击链接时链接文档将在哪里打开。

### --question--

#### --text--

什么是绝对路径？

#### --distractors--

相对于当前文件的路径。

---

包含变量的路径。

---

包含通配符的路径。

#### --answer--

资源的完整路径，从根目录开始。

### --question--

#### --text--

HTML 中的 `link` 元素的作用是什么？

#### --distractors--

它用于在同一网页内创建超链接。

---

它用于定义网页的布局和结构。

---

它用于在网页中嵌入音频或视频文件。

#### --answer--

它用于链接外部资源，如样式表和网站图标。

### --question--

#### --text--

以下哪项是开放图谱属性的有效值？

#### --distractors--

`"og:socialMedia"`

---

`"og:mediaOG"`

---

`"og:openGraph"`

#### --answer--

`"og:title"`

### --question--

#### --text--

以下哪项是布尔属性的示例？

#### --distractors--

`href`

---

`src`

---

`width`

#### --answer--

`checked`

### --question--

#### --text--

以下哪项是 `img` 元素的正确语法？

#### --distractors--

`<img src="image.jpg" alt="Description of the image"></img>`

---

`<img source="image.jpg" desc="Description of the image"></img>`

---

`<img src="image.jpg" desc="Description of the image">`

#### --answer--

`<img src="image.jpg" alt="Description of the image">`

### --question--

#### --text--

`iframe` 元素的用途是什么？

#### --distractors--

它用于为图像添加标题。

---

它用于为媒体添加版权信息。

---

它用于在同一网页内创建超链接。

#### --answer--

它用于在当前 HTML 文档中嵌入另一个文档。

### --question--

#### --text--

以下哪项不是锚元素的有效链接状态？

#### --distractors--

`hover`

---

`visited`

---

`active`

#### --answer--

`href`

### --question--

#### --text--

`title` 元素的作用是什么？

#### --distractors--

允许用户编辑网页源代码的元素。

---

它定义了网页中元素的布局和定位。

---

它用于将视频和多媒体内容直接嵌入 HTML 文档。

#### --answer--

它指定了文档的标题，并显示在浏览器标签页或窗口中。

## --quiz--

### --question--

#### --text--

以下哪个 `target` 属性可以在当前环境的父级中打开链接？

#### --distractors--

`_blank`

---

`_self`

---

`_unfencedTop`

#### --answer--

`_parent`

### --question--

#### --text--

哪个属性用于设置 HTML 文档的 `UTF-8` 字符编码？

#### --distractors--

`accept`

---

`capture`

---

`enctype`

#### --answer--

`charset`

### --question--

#### --text--

HTML 在网络中的作用是什么？

#### --distractors--

HTML 用于样式和布局。

---

HTML 用于在页面上增加互动性。

---

HTML 是软件包的注册表。

#### --answer--

HTML 表示网页的内容和结构。

### --question--

#### --text--

哪种图像许可协议适用于公有领域？

#### --distractors--

JPG

---

知识共享。

---

BSD

#### --answer--

知识共享零。

### --question--

#### --text--

如何将布尔属性设置为 `false`？

#### --distractors--

通过将属性值设置为 `0`。

---

通过在属性名称前使用 `not` 操作符。

---

通过将属性设置为空字符串（`""`）。

#### --answer--

通过从元素中省略该属性。

### --question--

#### --text--

`script` 元素的用途是什么？

#### --distractors--

它用于将 CSS 嵌入 HTML 文档或链接到外部 CSS 文件。

---

它用于在 HTML 文档中嵌入 Pascal 代码。

---

它用于在 HTML 文档中嵌入 C# 代码。

#### --answer--

它用于将 JavaScript 嵌入 HTML 文档或链接到外部 JavaScript 文件。

### --question--

#### --text--

以下哪个元素用于设置 HTML 文档的标题？

#### --distractors--

`footer`

---

`section`

---

`figcaption`

#### --answer--

`title`

### --question--

#### --text--

`class` 属性通常用于什么用途？

#### --distractors--

用于嵌入元素的元数据。

---

用于在 HTML 标签中直接定义内联样式。

---

用于为 JavaScript 函数指定唯一标识符。

#### --answer--

用于将一组样式应用于多个元素。

### --question--

#### --text--

以下哪项不是常用 HTML 实体的示例？

#### --distractors--

`&quot;`（双引号）。

---

`&copy;`（版权符号）。

---

`&gt;`（大于号）。

#### --answer--

`&div;`（div 元素）。

### --question--

#### --text--

哪个元素用于在 HTML 文档中添加音频？

#### --distractors--

`media`

---

`video`

---

`hr`

#### --answer--

`audio`

### --question--

#### --text--

网页上的内容放在 HTML 文档的什么位置？

#### --distractors--

在 `head` 和 `body` 元素之间。

---

在 `repr` 元素中。

---

在 `script` 元素中。

#### --answer--

在 `body` 元素中。

### --question--

#### --text--

哪个属性用于在音频播放结束后重播音频？

#### --distractors--

`multiple`

---

`inputmode`

---

`enctype`

#### --answer--

`loop`

### --question--

#### --text--

以下哪项是 `div` 元素的正确语法？

#### --distractors--

`<<div>>block container<</div>>`

---

`>>div>>block container>>div>>`

---

`[div]block container[/div]`

#### --answer--

`<div>block container</div>`

### --question--

#### --text--

`target="_self"` 是如何工作的？

#### --distractors--

它指定了点击链接后浏览器应滚动到的链接文档中的位置。

---

它会自动将链接文档下载到用户的电脑。

---

它会将用户重定向到 `href` 属性中指定的其他网站。

#### --answer--

它会在当前浏览环境中打开链接。

### --question--

#### --text--

什么是相对路径？

#### --distractors--

它指定文件或目录从文件系统根目录开始的确切位置。

---

它包括从域名到特定文件或目录的完整 URL。

---

它总是以正向斜杠（`/`）开头，表示网站或文件系统的根目录。

#### --answer--

它指定文件或目录相对于当前工作目录的位置。

### --question--

#### --text--

以下哪项是 `link` 元素的正确语法？

#### --distractors--

`<link rel="stylesheet" href="./styles.css"></link>`

---

`</link rel="stylesheet" href="./styles.css"></link>`

---

`<<link rel="stylesheet" href="./styles.css"></link>>`

#### --answer--

`<link rel="stylesheet" href="./styles.css" />`

### --question--

#### --text--

以下哪项不是开放图谱属性的有效值？

#### --distractors--

`"og:title"`

---

`"og:type"`

---

`"og:image"`

#### --answer--

`"og:socialMedia"`

### --question--

#### --text--

以下哪项是段落元素的正确语法？

#### --distractors--

`>p>paragraph element>/p>`

---

`<<p>>paragraph element<</p>>`

---

`<p>`

#### --answer--

`<p>paragraph element</p>`

### --question--

#### --text--

`iframe` 元素的名称代表什么？

#### --distractors--

Inline Framing

---

Inner Frame

---

Inline video

#### --answer--

Inline Frame

### --question--

#### --text--

活动状态对于锚元素意味着什么？

#### --distractors--

链接已禁用，无法点击。

---

鼠标光标位于链接上方。

---

用户已访问过该链接。

#### --answer--

用户正在点击它。

## --quiz--

### --question--

#### --text--

以下哪项会在新的浏览器标签页中打开链接？

#### --distractors--

```html
<a target="_parent" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_top" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_self" href="https://freecatphotoapp.com">cat photos</a>
```

#### --answer--

```html
<a target="_blank" href="https://freecatphotoapp.com">cat photos</a>
```

### --question--

#### --text--

以下哪项是空元素？

#### --distractors--

`div`

---

`section`

---

`p`

#### --answer--

`img`

### --question--

#### --text--

以下哪项是使用 HTML 实体的正确方法？

#### --distractors--

`<p>This is an &lt&img /&gt& element</p>`

---

`<p>This is an ;lt&img /;gt& element</p>`

---

`<p>This is an ;lt;img /;gt; element</p>`

#### --answer--

`<p>This is an &lt;img /&gt; element</p>`

### --question--

#### --text--

HTML 中的 `ul` 元素的用途是什么？

#### --distractors--

它仅用于对图像列表进行分组。

---

它仅用于对链接列表进行分组。

---

它用于对有序项目列表进行分组。

#### --answer--

它用于对无序项目列表进行分组。

### --question--

#### --text--

以下哪项是对 HTML 元素应用多个类的正确方法？

#### --distractors--

```html
<div class="box>>red>>box"></div>
```

---

```html
<div class="box<<red<<box"></div>
```

---

```html
<div class="box-red-box"></div>
```

#### --answer--

```html
<div class="box red-box"></div>
```

### --question--

#### --text--

以下哪个元素用于表示有序项目列表？

#### --distractors--

`span`

---

`div`

---

`ul`

#### --answer--

`ol`

### --question--

#### --text--

开放图谱属性的作用是什么？

#### --distractors--

这些属性用于将交互式多媒体内容直接嵌入网页。

---

这些属性用于在网站上生成动态弹出式广告。

---

这些属性用于加密网络服务器和用户浏览器之间传输的敏感数据。

#### --answer--

这些属性用于设置网站内容在不同社交媒体平台上的显示方式。

### --question--

#### --text--

以下哪项用于为网页设置简短描述？

#### --distractors--

```html
<meta
  add="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
  set="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
  description="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

#### --answer--

```html
<meta
  name="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

### --question--

#### --text--

SVG 代表什么？

#### --distractors--

Scalable Vector Graph

---

Scalable Vistor Graphics

---

Site Vector Graphics

#### --answer--

Scalable Vector Graphics

### --question--

#### --text--

`footer` 元素的作用是什么？

#### --distractors--

该元素用于将网页上的相关标题和图像分组。

---

该元素用于在页面段落之间创建水平线条。

---

该元素用于在页面底部创建有序项目列表。

#### --answer--

该元素用于在页面底部对版权内容或相关文件进行分组。

### --question--

#### --text--

什么是 `UTF-8` 字符编码？

#### --distractors--

它是一种标准化字符编码，用于设置网页标题。

---

它是一组用于设置网页描述的字符。

---

它是元素中使用的一组特殊字符，用于提高媒体性能。

#### --answer--

它是一种在网络上广泛使用的标准化字符编码。

### --question--

#### --text--

以下哪项是可替换元素的例子？

#### --distractors--

`slot`

---

`div`

---

`p`

#### --answer--

`iframe`

### --question--

#### --text--

以下哪项被认为是网页中最重要的标题？

#### --distractors--

`h6`

---

`h3`

---

`h5`

#### --answer--

`h1`

### --question--

#### --text--

以下哪个元素用于链接样式表和网站图标等外部资源？

#### --distractors--

`a`

---

`p`

---

`div`

#### --answer--

`link`

### --question--

#### --text--

以下哪个元素用于传达紧迫感、严肃性或强烈的重要性？

#### --distractors--

`important`

---

`heavy`

---

`strength`

#### --answer--

`strong`

### --question--

#### --text--

`figcaption` 元素的作用是什么？

#### --distractors--

它为 `span` 元素的内容提供标题。

---

它为 `slot` 元素的内容提供标题。

---

它为 `div` 元素的内容提供标题。

#### --answer--

它为 `figure` 元素的内容提供标题。

### --question--

#### --text--

以下哪项是为 HTML 文档设置 `DOCTYPE` 声明的正确方式？

#### --distractors--

`<DOCTYPE>`

---

`<*DOCTYPE html>`

---

`<<DOCTYPE html>>`

#### --answer--

`<!DOCTYPE html>`

### --question--

#### --text--

HTML 文档中的 `head` 元素作用是什么？

#### --distractors--

它是 HTML 文档中锚元素的容器。

---

它是 HTML 文档中图像的容器。

---

它是 HTML 文档中标题元素的容器。

#### --answer--

它是 HTML 文档中元数据的容器。

### --question--

#### --text--

以下哪项是将 HTML 文档的语言设置为英语的正确方式？

#### --distractors--

`<html lang-set="en">`

---

`<html set-lang="en">`

---

`<html language="en">`

#### --answer--

`<html lang="en">`

### --question--

#### --text--

以下哪个元素不应放在 `head` 内？

#### --distractors--

`link`

---

`meta`

---

`title`

#### --answer--

`img`

## --quiz--

### --question--

#### --text--

以下哪项不是关于绝对路径和相对路径的正确说法？

#### --distractors--

绝对路径从根目录开始，而相对路径则取决于当前文件的位置。

---

相对路径在网站内部链接时更短更灵活，而绝对路径包括完整的 URL。

---

绝对路径和相对路径都可用于链接同一网站内的文件。

#### --answer--

相对路径不能使用 `..` 向上移动目录，但绝对路径可以。

### --question--

#### --text--

以下哪项不是 `a` 标签中有效的目标属性值？

#### --distractors--

`_self`

---

`_parent`

---

`_top`

#### --answer--

`_main`

### --question--

#### --text--

`audio` 元素中的 `controls` 属性的用途是什么？

#### --distractors--

它定义了供浏览器使用的音频文件格式。

---

页面加载时，它会自动播放音频。

---

它指定音频文件的源 URL。

#### --answer--

它增加了播放、暂停和音量调节等内置播放控制。

### --question--

#### --text--

以下哪个元素可以通过提供网页的简要说明来提高 SEO 效果？

#### --distractors--

```html
<meta
 description="describe"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 name="desc"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 description="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

#### --answer--

```html
<meta
 name="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

### --question--

#### --text--

`<meta charset="utf-8" />` 的作用是什么？

#### --distractors--

将网页语言设置为英语。

---

链接外部 CSS 样式表。

---

指定网页标题。

#### --answer--

定义字符编码，以确保正确显示文本。

### --question--

#### --text--

HTML 中的 `link` 元素和 `script` 元素的区别是什么？

#### --distractors--

`link` 用于 JavaScript 文件，`script` 用于 CSS 文件。

---

`link` 和 `script` 都用于设计网页样式。

---

`script` 用于添加样式，`link` 用于运行 JavaScript。

#### --answer--

`link` 用于从外部 CSS 文件中添加样式，`script` 用于运行 JavaScript 代码。

### --question--

#### --text--

关于 HTML 类和 ID，以下哪个说法是错误的？

#### --distractors--

一个类名可以在整个 HTML 文档中多次使用。

---

`rel` 属性定义了链接资源与 HTML 文档之间的关系。

---

`og:image` 属性用于指定社交媒体帖子中显示的图片。

#### --answer--

ID 名称不能包含下划线，且必须唯一。

### --question--

#### --text--

关于 HTML 标题元素，以下哪个说法是正确的？

#### --distractors--

从 `h1` 到 `h6`，文本大小逐渐变大。

---

所有标题元素的文字大小保持不变。

---

`h3` 比 `h2` 更大。

#### --answer--

从 `h1` 到 `h6`，文本大小逐渐变小。

### --question--

#### --text--

关于 HTML 元素，哪个说法是错误的？

#### --distractors--

HTML 元素是网页的基本组成部分。

---

有些代码库在空元素内包含一个正斜杠 (/)。

---

布尔属性是一种只需在标签中存在或不存在即可起作用的属性。

#### --answer--

空元素包含内容，只有开始标签。

### --question--

#### --text--

关于 HTML 中的空元素，以下哪个说法是正确的？

#### --distractors--

空元素在结束前必须包含一个正斜杠 /。

---

空元素绝不能包含正斜杠 /。

---

在所有 HTML 版本中，自闭合空元素都必须使用正斜杠 /。

#### --answer--

`<img>` 和 `<img/>` 在现代HTML中都是可以接受的。

### --question--

#### --text--

程序设计中注释的主要用途是什么？

#### --distractors--

使代码运行得更快。

---

更改浏览器显示内容的方式。

---

在代码中存储临时数据。

#### --answer--

在代码中为自己和其他开发人员留下备注。

### --question--

#### --text--

哪个目标属性能在当前浏览环境中打开链接？

#### --distractors--

`_blank`

---

`_top`

---

`_parent`

#### --answer--

`_self`

### --question--

#### --text--

HTML 中的 `em` 元素的作用是什么？

#### --distractors--

放大文本。

---

更改文本颜色。

---

用下划线强调词语。

#### --answer--

用斜体突出重要文本。

### --question--

#### --text--

`strong` 元素在 HTML 中的作用是什么？

#### --distractors--

增加字体大小。

---

更改文本颜色。

---

给文本添加下划线。

#### --answer--

加粗文本，以显示其重要性。

### --question--

#### --text--

HTML 中的 `figure` 元素和 `figcaption` 元素的主要区别是什么？

#### --distractors--

`figure` 用于标题，而 `figcaption` 用于对图像或图表等相关内容进行分组。

---

`figcaption` 必须始终置于 `figure` 元素之外。

---

没有区别，都是为了同样的目的。

#### --answer--

`figure` 将图像或图表等相关内容分组，而 `figcaption` 为该内容提供标题。

### --question--

#### --text--

为什么要使用 HTML 实体？

#### --distractors--

为文本添加样式。

---

在网页中创建超链接。

---

增加特殊字符的字体大小。

#### --answer--

在 HTML中代表保留的字符，例如 `<` 和 `&`。

### --question--

#### --text--

关于开放图谱标签，以下哪项说法是正确的？

#### --distractors--

开放图谱标签用于使用 CSS 创建网页样式。

---

开放图谱标签仅用于 SEO，对社交媒体没有影响。

---

开放图谱标签必须置于 HTML 的 `body` 内。

#### --answer--

`og:title` 属性可设置在社交媒体上共享内容时显示的标题。

### --question--

#### --text--

关于图像格式，以下哪种说法是错误的？

#### --distractors--

WEBP 和 AVIF 更适合现代图像优化。

---

SVG 图像可以缩放而不会降低质量。

---

压缩有助于减小图像文件的大小。

#### --answer--

PNG 和 JPG 是目前优化图像的最佳格式。

### --question--

#### --text--

`loop` 属性在 `audio` 元素中的作用是什么？

#### --distractors--

播放一次音频。

---

播放一次后停止音频。

---

减慢音频播放速度。

#### --answer--

连续重播音频。

### --question--

#### --text--

以下哪项能最恰当的描述 HTML 模板？

#### --distractors--

网络开发人员用于在文档中添加水印的一种特殊工具。

---

一个加快网页加载速度的浏览器插件。

---

一个专门用于 HTML 动画的编码框架。

#### --answer--

一个预定义模板，包含用于网页开发的基本 HTML 结构。
