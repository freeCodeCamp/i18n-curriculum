---
id: 67f41268a129e63f8e071736
title: 迴圈與序列測驗
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪一項是正確的 `list` 語法？

#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--

會列印到主控台的是什麼？

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--

什麼是可迭代物件？

#### --distractors--

它是一種特殊的字串型別，可以容納大量字元。

---

它是一種用來表述非常大數值的特殊數字型別。

---

它是一種主要用於類別和特殊函式的特殊資料型別。

#### --answer--

它是一種特殊型別的物件，可以一次迭代一個項目。

### --question--

#### --text--

以下內容將列印到主控台什麼？

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--

以下哪一項用於取得 `list` 中元素的數量？

#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--

如果你嘗試傳入一個超出 `list` 範圍的索引（無論是正數還是負數），會發生什麼情況？

#### --distractors--

你將收到一個 `SyntaxError`。

---

你將收到 `RangeError`。

---

你將收到 `TypeError`。

#### --answer--

你會收到一個 `IndexError`。

### --question--

#### --text--

會列印到主控台的是什麼？

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--

下列哪一種方法用於將項目添加到列表的末端？

#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--

會列印到主控台的是什麼？

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--

下列哪一個方法用於在列表的特定索引處插入元素？

#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--

會列印到主控台的是什麼？

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--

以下哪一種方法用於清空一個列表？

#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

什麼是 tuple？

#### --distractors--

它是一種可變的隨機值序列資料型別。

---

它是一種用於表述大型數字的特殊資料型別。

---

這是一種用於將元素添加到 `list` 的方法。

#### --answer--

它是一個不可變的有序值序列。

### --question--

#### --text--

以下哪一種方式是從 tuple 中解包項目的正確方法？

#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--

以下 `code` 的結果會是什麼？

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--

它將產生 `RangeError`。

---

不會發生任何事情。

---

它將產生一個無窮迴圈。

#### --answer--

它將產生 `TypeError`。

### --question--

#### --text--

以下 `code` 的結果會是什麼？

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--

以下哪一個函式（程式）用於創建一個排序後值的新列表？

#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--

以下哪一種是使用 `for` 迴圈的正確方式？

#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--

以下哪一個函式（程式）用於產生整數序列？

#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--

以下哪一個函式用於平行迭代多個可迭代物件？

#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`

