---
id: 66ed9018f45ce3ece4053eb9
title: CSS 변수 퀴즈
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

다음 중 `@property` 규칙에서 유효하지 않은 설명자는 무엇인가요?

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

다음 코드에서 `color`가 정의되어 있지 않으면 `--main-color`는 어떤 값을 가지나요?

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

색상이 적용되지 않습니다.

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

CSS에서 사용자 정의 속성을 선언하는 올바른 방법은 무엇인가요?

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

CSS에서 사용자 정의 속성을 사용하는 주요 목적은 무엇인가요?

#### --distractors--

DOM 구조를 변경하기 위해서입니다.

---

동적 선택자를 만들기 위해서입니다.

---

접근성을 향상시키기 위해서입니다.

#### --answer--

재사용 가능한 스타일을 정의하기 위해서입니다.

### --question--

#### --text--

`:root` 선택자는 무엇을 나타내나요?

#### --distractors--

`body`의 첫 번째 자식 요소입니다.

---

모든 `header` 요소의 부모 요소입니다.

---

`html` 태그의 첫 번째 자식입니다.

#### --answer--

DOM 트리에서 가장 상위 요소입니다.

### --question--

#### --text--

CSS에서 `--foreground` 사용자 정의 속성을 `color`로 적용하려면 어떻게 해야 하나요?

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

CSS에서 `@property` 규칙의 목적은 무엇인가요?

#### --distractors--

미디어 쿼리를 정의하기 위해서입니다.

---

CSS 애니메이션과 타이밍을 제어하기 위해서입니다.

---

CSS 속성을 하나의 규칙으로 그룹화하기 위해서입니다.

#### --answer--

사용자 정의 속성의 동작 방식을 정의하기 위해서입니다.

### --question--

#### --text--

사용자 정의 `@property`를 선언할 때 `syntax`의 목적은 무엇인가요?

#### --distractors--

사용자 정의 속성의 기본값을 지정합니다.

---

속성이 자식 요소에 상속될 수 있는지 결정합니다.

---

속성이 모든 요소에 적용되는지 정의합니다.

#### --answer--

사용자 정의 속성에 특정 데이터 유형이나 값 패턴을 강제합니다.

### --question--

#### --text--

사용자 정의 속성을 사용할 때 주의해야 할 점은 무엇인가요?

#### --distractors--

사용자 정의 속성은 자동으로 `px` 단위로 변환됩니다.

---

사용자 정의 속성은 항상 인라인 스타일을 재정의합니다.

---

사용자 정의 속성은 폰트 값을 설정할 수 없습니다.

#### --answer--

사용자 정의 속성은 오래된 브라우저에서 지원되지 않을 수 있습니다.

### --question--

#### --text--

CSS 사용자 정의 속성의 `var()` 함수에서 대체 수단 값을 제공하는 목적은 무엇인가요?

#### --distractors--

CSS 코드 양을 줄이기 위해서입니다.

---

최신 브라우저에서 성능을 향상시키기 위해서입니다.

---

느린 네트워크에서 렌더링 시간을 최적화하기 위해서입니다.

#### --answer--

사용자 정의 속성이 정의되지 않았을 때 유효한 값이 적용되도록 보장하기 위해서입니다.

### --question--

#### --text--

미디어 쿼리 내에서 변수 `--foreground`를 정의할 때, 미디어 쿼리가 현재 뷰포트와 더 이상 일치하지 않으면 어떻게 되나요?

#### --distractors--

사용자 정의 속성이 유지되어 계속 적용됩니다.

---

사용자 정의 속성이 초기값으로 되돌아갑니다.

---

사용자 정의 속성이 뷰포트를 기준으로 다시 계산됩니다.

#### --answer--

사용자 정의 속성은 미디어 쿼리에 범위가 한정되어 더 이상 사용할 수 없습니다.

### --question--

#### --text--

다음 선언은 어떻게 동작하나요?

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

정의되어 있지 않아도 `--main-color`을 적용하며, 기본값으로 사용합니다.

---

`--main-color`과 `--fallback-color`를 동시에 적용하여 두 색상이 혼합됩니다.

---

`#000` 또는 `--main-color`가 정의되어 있든 상관없이 항상 `--fallback-color`을 기본값으로 사용합니다.

#### --answer--

정의되어 있으면 `--main-color`을 적용하고, 그렇지 않으면 `--fallback-color;`을 확인하며 둘 다 없으면 `#000`을 사용합니다.

### --question--

#### --text--

CSS에서 `@property`를 사용하는 한 가지 이점은 무엇인가요?

#### --distractors--

사용자 정의 속성 값을 미리 계산하여 성능을 향상시킵니다.

---

브라우저 지원을 위해 사용자 정의 속성에 자동으로 접두사를 붙입니다.

---

사용자 정의 속성 사용을 특정 요소로 제한합니다.

#### --answer--

사용자 정의 속성의 애니메이션을 허용합니다.

### --question--

#### --text--

사용자 정의 `inherits` 선언에서 `@property` 속성은 무엇을 제어하나요?

#### --distractors--

사용자 정의 속성에 초기값이 있는지 여부입니다.

---

속성에 축약형이 있는지 여부입니다.

---

속성이 대체 수단 값을 허용하는지 여부입니다.

#### --answer--

사용자 정의 속성 값이 자식 요소에 전달되는지 여부입니다.

### --question--

#### --text--

사용자 정의 `@property` 선언에서 `initial-value`는 무엇을 지정하나요?

#### --distractors--

속성이 허용할 수 있는 값입니다.

---

캐스케이드에서 속성의 우선순위입니다.

---

속성이 가져야 하는 값의 유형입니다.

#### --answer--

속성의 대체 수단 값입니다.

### --question--

#### --text--

다음 HTML과 CSS 코드에서 `color` 요소의 `.box` 속성 값은 무엇인가요?

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

CSS 그라디언트는 어떤 속성에 적용해야 하나요?

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

CSS 그라디언트에서 색상 정지점(color stops)의 목적은 무엇인가요?

#### --distractors--

그라디언트의 투명도 수준을 정의합니다.

---

그라디언트의 방향을 지정합니다.

---

그라디언트를 고정된 패턴으로 반복합니다.

#### --answer--

그라디언트에서 색상이 변하는 특정 지점을 정의합니다.

### --question--

#### --text--

CSS 선형 그라디언트에서 각도나 방향이 지정되지 않으면 어떻게 되나요?

#### --distractors--

그라디언트가 대각선 방향으로 기본 설정됩니다.

---

그라디언트가 아래에서 위로 이동하는 방향으로 기본 설정됩니다.

---

그라디언트가 왼쪽에서 오른쪽으로 이동하는 방향으로 기본 설정됩니다.

#### --answer--

그라디언트가 위에서 아래로 이동하는 방향으로 기본 설정됩니다.

### --question--

#### --text--

중심점에서 바깥쪽으로 방사형 그라디언트를 만드는 CSS 그라디언트 함수는 무엇인가요?

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

다음 중 CSS 사용자 정의 속성을 올바르게 선언한 방법은 무엇인가요?

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

전역 CSS 사용자 정의 속성을 정의할 때 일반적으로 사용하는 선택자는 무엇인가요?

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

`var()`를 사용할 때 대체 수단 값을 포함하는 것이 권장되는 이유는 무엇인가요?

#### --distractors--

변수가 올바르게 애니메이션되도록 보장합니다.

---

외부 스타일시트 로드를 방지합니다.

---

브라우저 리플로우를 방지합니다.

#### --answer--

사용자 정의 속성이 정의되지 않았을 때 유효한 값을 보장합니다.

### --question--

#### --text--

사용자 정의 속성을 배경색으로 적용하는 올바른 구문은 무엇인가요?

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

개발자가 사용자 정의 속성의 동작을 더 세밀하게 제어할 수 있게 하는 CSS 규칙은 무엇인가요?

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

`inherits` 정의에서 `@property` 필드는 무엇을 제어하나요?

#### --distractors--

속성이 JavaScript에서 사용되는지 여부입니다.

---

속성이 다시 그리기를 유발하는지 여부입니다.

---

속성에 함수가 포함될 수 있는지 여부입니다.

#### --answer--

속성 값이 자식 요소에 전달되는지 여부입니다.

### --question--

#### --text--

`@property`를 사용해 애니메이션 그라디언트 각도를 정의할 때 어떤 구문을 사용해야 하나요?

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

CSS 사용자 정의 속성과 표준 CSS 속성의 정의 방식에서 차이점은 무엇인가요?

#### --distractors--

표준 CSS 속성은 한 개의 대시로 정의되고, 사용자 정의 속성은 두 개의 대시를 사용합니다.

---

표준 CSS 속성은 `var()`를 사용해 정의되고, 사용자 정의 속성은 그렇지 않습니다.

---

표준 CSS 속성은 `@property` 규칙을 사용해 정의되고, 사용자 정의 속성은 그렇지 않습니다.

#### --answer--

사용자 정의 속성은 반드시 두 개의 대시(`--`)로 시작해야 하며, 표준 CSS 속성은 그렇지 않습니다.

### --question--

#### --text--

CSS 사용자 정의 속성을 사용하는 이점 중 하나는 무엇인가요?

#### --distractors--

CSS 주석 필요성을 줄입니다.

---

이미지를 자동으로 최적화합니다.

---

JavaScript 변수를 대체합니다.

#### --answer--

스타일을 재사용하고 중앙에서 관리할 수 있게 합니다.

### --question--

#### --text--

미디어 쿼리를 사용해 사용자 정의 속성으로 동적으로 변경할 수 있는 CSS 속성은 무엇인가요?

#### --distractors--

`background-color`만 가능합니다.

---

`font-family`만 가능합니다.

---

`z-index`만 가능합니다.

#### --answer--

값을 허용하는 모든 속성입니다.

### --question--

#### --text--

CSS 변수 문맥에서 `initial-value` 규칙의 `@property` 역할은 무엇인가요?

#### --distractors--

속성의 최솟값을 설정합니다.

---

애니메이션의 최댓값을 정의합니다.

---

선택자 우선순위를 변경합니다.

#### --answer--

값이 설정되지 않았을 때 속성의 기본값을 할당합니다.

### --question--

#### --text--

다음 HTML과 CSS에서 `.card` 요소에 적용될 배경색은 무엇인가요?

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

다음 CSS에서 사용자가 `--padding`에 잘못된 값을 할당하려고 하면(예: 길이 대신 색상) 어떻게 되나요?

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

브라우저가 값을 수용하지만 레이아웃에서 무시합니다.

---

브라우저가 런타임 오류를 발생시킵니다.

---

브라우저가 값을 자동으로 유효한 길이로 변환합니다.

#### --answer--

브라우저가 속성의 초기값으로 대체합니다.

### --question--

#### --text--

이 예제에서 `--gradient-angle` 사용자 정의 속성의 목적은 무엇인가요?

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

그라디언트 전환 속도를 정의합니다.

---

그라디언트 크기를 지정합니다.

---

색상 혼합 모드를 설정합니다.

#### --answer--

그라디언트 방향을 제어합니다.

### --question--

#### --text--

테마에 사용자 정의 속성을 사용하는 장점을 가장 잘 보여주는 시나리오는 무엇인가요?

#### --distractors--

폰트 크기에만 사용하는 경우입니다.

---

`:hover`에만 적용하는 경우입니다.

---

하나의 CSS 클래스에만 제한하는 경우입니다.

#### --answer--

클래스를 통해 값을 변경하는 경우입니다.

### --question--

#### --text--

개발자가 CSS 변수를 정의할 때 `:root`를 선호하는 이유는 무엇인가요?

#### --distractors--

`:root`가 페이지 로드 속도를 향상시키기 때문입니다.

---

`:root`가 특이성(specificity)을 비활성화하기 때문입니다.

---

`:root`가 JavaScript에 필요하기 때문입니다.

#### --answer--

`:root`가 속성을 전역 범위로 지정할 수 있기 때문입니다.

### --question--

#### --text--

`--secondary-color`가 정의되어 있지 않으면 다음 CSS는 어떻게 동작하나요?

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

`black`을 적용합니다.

---

`color` 속성을 무시합니다.

---

`--secondary-color`을 적용합니다.

#### --answer--

`orange`을 적용합니다.

### --question--

#### --text--

`<color>` 구문 필드에서 `@property`가 나타내는 값 유형은 무엇인가요?

#### --distractors--

`90deg` 같은 각도입니다.

---

`10px` 같은 길이입니다.

---

`50%` 같은 백분율입니다.

#### --answer--

`#ff0000` 또는 `red` 같은 색상 값입니다.

### --question--

#### --text--

`@property`와 함께 사용자 정의 속성을 사용하는 주요 이점은 무엇인가요?

#### --distractors--

대체 수단 값이 필요 없게 합니다.

---

정적 레이아웃을 강제합니다.

---

클래스 필요성을 줄입니다.

#### --answer--

속성 애니메이션을 허용합니다.

### --question--

#### --text--

사용자 정의 속성이 정의되지 않았을 때 `var(--undefined-property, fallback)`는 무엇을 하나요?

#### --distractors--

오류를 발생시키고 스타일 적용을 중단합니다.

---

전체 CSS 규칙을 무시합니다.

---

변수 이름을 그대로 적용합니다.

#### --answer--

대체 수단 값을 대신 적용합니다.
