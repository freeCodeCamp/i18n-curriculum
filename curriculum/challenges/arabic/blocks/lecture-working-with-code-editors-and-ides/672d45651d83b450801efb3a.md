---
id: 672d45651d83b450801efb3a
title: كيفية إنشاء مشروع وتشغيل الشفرة محليًا في VS Code
challengeType: 19
dashedName: how-to-create-a-project-and-run-your-code-locally-in-vs-code
---

# --description--

لتبدأ، يجب أن تعرف أن VS Code يعتبر المشاريع "مساحات عمل". مساحة العمل هي أي مجلد تفتحه في VS Code.

لبدء العمل، ستحتاج إلى استخدام واجهة سطر الأوامر لإنشاء مجلد جديد لمشروعك الجديد. المجلد هو اسم آخر للدليل.

تسمح لك واجهات سطر الأوامر، أو CLI اختصارًا، بالتفاعل مع نظام التشغيل الخاص بك من خلال أوامر نصية. ستتعلم المزيد عن هذه الأدوات في دروس لاحقة.

إذا كنت تستخدم ويندوز، يمكنك استخدام موجه الأوامر أو PowerShell. وإذا كنت على ماك، يمكنك فتح تطبيق Terminal.

بمجرد فتح سطر الأوامر، انتقل إلى مجلد المنزل بكتابة الأمر `cd ~` واضغط `enter` على لوحة المفاتيح. ثم اكتب الأمر `mkdir my-project` واضغط `enter`. الأمر `mkdir`، أو "إنشاء مجلد"، هو الأمر المستخدم لإنشاء مجلد جديد. بعد ذلك، يجب أن تفتح مجلدك الجديد في VS Code.

هناك أيضًا بعض الطرق الرسومية الأخرى لإنشاء مجلد جديد.

اعتمادًا على نظام التشغيل، قد يكون لديك قائمة سياقية (أو خيار النقر بزر الماوس الأيمن) لفتح VS Code مباشرة من مستكشف الملفات. يمكنك أيضًا النقر نقرًا مزدوجًا على تطبيق VS Code. إذا كنت مرتاحًا مع سطر الأوامر، يمكنك تشغيل الأمر `code /path/to/folder`.

بمجرد فتحه، يجب أن ترى مساحة عمل جديدة فارغة.

لإنشاء ملف جديد، انقر على قائمة `File` في الزاوية العلوية اليسرى واختر `New File...`. ثم سمّ ملفك `index.html` وانقر على `Save`.

الآن يجب أن ترى ملفك الجديد في تبويب المستكشف. جرّب إضافة بعض HTML إلى ملف `index.html` الخاص بك.

قد تميل الآن إلى فتح ملف HTML في المتصفح مباشرة. رغم أن ذلك قد يعمل، إلا أنه قد يؤدي إلى جميع أنواع الأخطاء غير المتوقعة، مثل عدم تحميل CSS بشكل صحيح.

بدلاً من ذلك، يجب أن تضع ملف HTML خلف خادم ويب مناسب. قد يبدو هذا مهمة مخيفة إذا كنت جديدًا على البرمجة، لكن هناك طريقة لفعل ذلك مباشرة من VS Code.

ستحتاج أولًا إلى تثبيت إضافة _Live Server_، المتوفرة مجانًا في السوق.

انقر على تبويب الإضافات في الزاوية اليسرى واكتب `Live Server` في شريط البحث.

بمجرد تثبيتها، يجب أن ترى زر `Go live` في شريط الحالة. تأكد من فتح ملف HTML الخاص بك، ثم انقر عليه.

يجب أن تفتح صفحتك الجديدة تلقائيًا في تبويب جديد في المتصفح. إذا لم يحدث ذلك، يمكنك الذهاب مباشرة إلى `http://localhost:5500/`

تهانينا! لقد أعددت الآن مشروعك الخاص في VS Code، ويمكنك عرض تغييراتك باستخدام الخادم الحي.

# --questions--

## --text--

ما الذي يعتبره Visual Studio Code "مساحة عمل"؟

## --answers--

القرص الصلب بأكمله.

### --feedback--

يذكر الدرس كيف يعرّف VS Code مساحة العمل بالنسبة للمجلدات.

---

المجلد الذي فتحت منه VS Code.

---

جميع الملفات المفتوحة في VS Code.

### --feedback--

يذكر الدرس كيف يعرّف VS Code مساحة العمل بالنسبة للمجلدات.

---

مجلد تطبيق VS Code.

### --feedback--

يذكر الدرس كيف يعرّف VS Code مساحة العمل بالنسبة للمجلدات.

## --video-solution--

2

## --text--

لماذا يوصي الدرس باستخدام خادم ويب بدلاً من فتح ملف HTML مباشرة في المتصفح؟

## --answers--

التحميل أسرع ويسهل اكتشاف المشكلات في شفرتك.

### --feedback--

يذكر الدرس مشكلة محددة قد تحدث عند فتح ملفات HTML مباشرة.

---

يوفر أمانًا أفضل لتطبيقات البرمجيات الكبيرة.

### --feedback--

يذكر الدرس مشكلة محددة قد تحدث عند فتح ملفات HTML مباشرة.

---

يتجنب الأخطاء غير المتوقعة، مثل عدم تحميل CSS بشكل صحيح.

---

يسمح بالتعاون في الوقت الحقيقي مع مطوّرين آخرين في الفريق.

### --feedback--

يذكر الدرس مشكلة محددة قد تحدث عند فتح ملفات HTML مباشرة.

## --video-solution--

3

## --text--

ما هي إضافة VS Code الموصى بها لإعداد خادم ويب محلي بسهولة؟

## --answers--

Web Server.

### --feedback--

يذكر الدرس تحديدًا تثبيت إضافة من السوق لخدمة ملف HTML الخاص بك.

---

Live Server.

---

Local Host.

### --feedback--

يذكر الدرس تحديدًا تثبيت إضافة من السوق لخدمة ملف HTML الخاص بك.

---

HTML Preview.

### --feedback--

يذكر الدرس تحديدًا تثبيت إضافة من السوق لخدمة ملف HTML الخاص بك.

## --video-solution--

2
