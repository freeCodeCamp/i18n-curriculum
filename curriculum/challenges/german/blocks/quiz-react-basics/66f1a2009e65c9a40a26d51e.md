---
id: 66f1a2009e65c9a40a26d51e
title: React-Grundlagen-Quiz
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 untenstehenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche der folgenden Optionen ist keine JavaScript-Bibliothek oder kein Framework?

#### --distractors--

Angular

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

Welche der folgenden Methoden ist KEINE Möglichkeit für bedingtes Rendern in React?

#### --distractors--

Ternäre Operatoren.

---

Logischer `&&` Operator.

---

`if` Anweisungen.

#### --answer--

`setTimeout`

### --question--

#### --text--

Was ist der Hauptzweck, eine `key` Prop beim Rendern einer Liste in React hinzuzufügen?

#### --distractors--

Um jedem Listenelement einen einzigartigen CSS-Stil zuzuweisen.

---

Um als Textinhalt für das Element zu dienen.

---

Um jedem spezifischen Element in der Liste einen Ereignis-Listener hinzuzufügen.

#### --answer--

Um React dabei zu helfen, Elemente für effizientes Rendern zu identifizieren.

### --question--

#### --text--

Wo werden die Pakete und Bibliotheken des Projekts installiert?

#### --distractors--

Im Benutzerverzeichnis Ihres Computers.

---

In der `package.json` Datei.

---

Im `src` Ordner.

#### --answer--

Im `node_modules` Ordner.

### --question--

#### --text--

Welche der folgenden Aussagen über JSX ist WAHR?

#### --distractors--

JSX muss immer in separaten Dateien von JavaScript geschrieben werden.

---

JSX kann keine JavaScript-Ausdrücke verwenden.

---

JSX wird für React-Anwendungen nicht empfohlen.

#### --answer--

JSX erlaubt das Schreiben von HTML innerhalb von JavaScript-Code.

### --question--

#### --text--

Welches der folgenden Probleme tritt häufig bei SPAs auf?

#### --distractors--

Serverressourcen werden durch ständiges Seiten-Rendering stärker beansprucht.

---

Browser-Caching wird durch häufige DOM-Aktualisierungen unzuverlässig.

---

Mehrfache Seitenladevorgänge erzeugen inkonsistentes Zustandsmanagement.

#### --answer--

Screenreader haben Schwierigkeiten mit dynamisch aktualisierten Inhalten.

### --question--

#### --text--

Wie aktualisiert eine Single-Page-Anwendung typischerweise die Ansicht?

#### --distractors--

Durch einen vollständigen Server-Neuladevorgang.

---

Durch das Neuladen des gesamten HTML-Dokuments.

---

Nur durch serverseitiges Rendern.

#### --answer--

Indem nur die notwendigen Teile der Seite aktualisiert werden.

### --question--

#### --text--

Welcher der folgenden Befehle startet ein React-Projekt im Entwicklungsmodus?

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

Welches der folgenden ist kein gültiges JSX-Element?

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

Welche JavaScript-Array-Methode wird häufig verwendet, um ein Array von Daten in ein Array von JSX-Elementen umzuwandeln?

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

Welche bewährte Vorgehensweise gilt für das Übergeben der `key` Prop in React?

#### --distractors--

Verwenden Sie in allen Fällen Array-Indizes.

---

Generieren Sie bei jedem Rendern zufällige Schlüssel.

---

Lassen Sie die `key` Prop ganz weg.

#### --answer--

Verwenden Sie eindeutige und stabile IDs aus den Daten.

### --question--

#### --text--

Welcher Befehl installiert die in `package.json` aufgeführten Abhängigkeiten?

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

Wie würden Sie eine `name` Prop an eine Kind-Komponente übergeben?

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

Wie ist die korrekte Methode, um mehrere Props mit dem Spread-Operator in React zu übergeben?

#### --distractors--

`<Child person={name..., age..., city...} />`.

---

`<Child spreader={...person} />`.

---

`<Child spread={person} />`.

#### --answer--

`<Child {...person} />`.

### --question--

#### --text--

Was sind die Bausteine von React?

#### --distractors--

Funktionen und Module.

---

DOM-Elemente und Ereignisse.

---

Klassen und Objekte.

#### --answer--

Wiederverwendbare Komponenten.

### --question--

#### --text--

Wie fügt man in React korrekt einen inline-Stil hinzu?

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

Wie ist die korrekte Methode, um eine React-Komponente bedingt zu rendern?

#### --distractors--

Verwendung von `if` Anweisungen innerhalb von JSX.

---

Deklaration einer neuen Komponente innerhalb einer `render()` Methode.

---

Verwendung von `while` Schleifen innerhalb von JSX.

#### --answer--

Verwendung von ternären Operatoren oder logischen `&&` Operatoren innerhalb von JSX.

### --question--

#### --text--

Welches Schlüsselwort wird verwendet, um eine Komponente in eine andere Datei zu importieren?

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

Was bedeutet die Abkürzung SPAs vollständig?

#### --distractors--

Server-Page-Architekturen.

---

Selektive Seitenanwendungen.

---

Statische Progressive Apps.

#### --answer--

Single-Page-Anwendungen.

### --question--

#### --text--

Welche Erweiterung der JavaScript-Syntax wird verwendet, um die Benutzeroberfläche in React zu beschreiben?

#### --distractors--

TSC

---

HTML+

---

ReactML

#### --answer--

JSX
