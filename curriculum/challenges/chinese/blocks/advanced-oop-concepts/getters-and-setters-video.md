---
id: 69ae9f3ca9e6da4eb0d5f7f9
title: 访问器和设置器
challengeType: 11
videoId: o8zKHbpd87s
dashedName: getters-and-setters
---

# --description--

在这个 `video` 中，你将学习关于 getter 和 setter。

# --questions--

## --text--

什么是 setter？

## --answers--

一个在对象超出作用域时自动删除该对象的函数。

---

一种用于获取或读取 `attribute` 值的 `method`。

---

用于设置或更新私有或受保护 `attribute` 值的 `class` 中的 `method`。

---

一个创建新建类实例且不修改任何属性的方法。

## --video-solution--

3


