---
id: 673362a34edda41dedf87623
title: '¿Qué es el método Map y cómo funciona?'
challengeType: 11
videoId: CdSr7rv8pwo
dashedName: what-is-the-map-method-and-how-does-it-work
---

# --description--

Mira el video o lee la transcripción y responde las preguntas a continuación.

# --transcript--

¿Qué es el método `map` y cómo funciona?

El método `map` es una función potente y ampliamente utilizada en JavaScript que opera sobre arrays. Está diseñado para crear un nuevo array aplicando una función dada a cada elemento del array original.

Este método no modifica el array original, sino que devuelve un nuevo array que contiene los resultados de la función aplicada a cada elemento.

Aquí hay un ejemplo de uso del método `map` en un array de números:

```js
const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map((num) => num * 2);
console.log(numbers); // [1, 2, 3, 4, 5]
console.log(doubled); // [2, 4, 6, 8, 10]
```

Para crear un nuevo array donde cada número esté duplicado, estamos utilizando el método `map`. El método `map` acepta una función de callback donde la función se llama en cada elemento individual del array.

En este caso, cada número del array será multiplicado por `2`. El resultado será un nuevo array de los números `2,4,6,8,10`.

La función de callback puede aceptar hasta tres argumentos.

El primer argumento es el elemento actual que se está procesando.

```js
const numbers = [3, 4, 5, 6, 7].map((element) => {
  console.log("Element:", element);
  return element * 2;
});
```

El segundo argumento es el índice del elemento actual que se está procesando.

```js
const numbers = [3, 4, 5, 6, 7].map((element, index) => {
  console.log("Element:", element);
  console.log("Index:", index);
  return element * 2;
});
```

El tercer argumento es el array en el cual se está llamando `map`.

```js
const numbers = [3, 4, 5, 6, 7].map((element, index, array) => {
  console.log("Element:", element);
  console.log("Index:", index);
  console.log("Array:", array);
  return element * 2;
});
```

Entender y usar efectivamente el método `map` puede mejorar significativamente tu habilidad para trabajar con arrays en JavaScript. En futuros videos de lección, profundizaremos en usos más avanzados de `map` y exploraremos cómo puede ser una herramienta poderosa para construir programas dinámicos y eficientes.

# --questions--

## --text--

¿Qué devuelve el método `map`?

## --answers--

El array original, modificado.

### --feedback--

Piensa en lo que dijimos acerca del resultado de la operación `map`.

---

Un nuevo array con el mismo número de elementos que el original.

---

Un solo valor.

### --feedback--

Piensa en lo que dijimos acerca del resultado de la operación `map`.

---

No devuelve nada, solo modifica el array original.

### --feedback--

Piensa en lo que dijimos acerca del resultado de la operación `map`.

## --video-solution--

2

## --text--

¿Cuántos argumentos puede recibir la función de callback en `map`?

## --answers--

Uno. El elemento actual.

### --feedback--

Recuerda el conjunto completo de parámetros que mencionamos que la función de callback puede recibir.

---

Dos. El elemento actual y su índice.

### --feedback--

Recuerda el conjunto completo de parámetros que mencionamos que la función de callback puede recibir.

---

Tres. El elemento actual, su índice y el array original.

---

Depende de cómo se llame a `map`.

### --feedback--

Recuerda el conjunto completo de parámetros que mencionamos que la función de callback puede recibir.

## --video-solution--

3

## --text--

¿Cuál de los siguientes NO es un uso correcto del método `map`?

## --answers--

Convertir un array de cadenas a mayúsculas.

### --feedback--

Considera qué operación no encaja con la idea de transformar cada elemento en un nuevo elemento.

---

Duplicar cada número en un array.

### --feedback--

Considera qué operación no encaja con la idea de transformar cada elemento en un nuevo elemento.

---

Eliminar elementos específicos de un array.

---

Extraer una propiedad de cada objeto en un array.

### --feedback--

Considera qué operación no encaja con la idea de transformar cada elemento en un nuevo elemento.

## --video-solution--

3
