---
id: 5e46f8dcac417301a38fb92e
title: Classificatore di immagini di gatti e cani
challengeType: 10
forumTopicId: 462377
dashedName: cat-and-dog-image-classifier
---

# --description--

Lavorerai su questo progetto con <a href="https://colab.research.google.com/github/freeCodeCamp/boilerplate-cat-and-dog-image-classifier/blob/master/fcc_cat_dog.ipynb" target="_blank" rel="noopener noreferrer nofollow">Google Colaboratory</a>.

Dopo aver aperto quel collegamento, crea una copia del notebook nel tuo account o localmente. Una volta completato il progetto e superato il test (incluso in quel collegamento), invia il link del progetto qui sotto. Se invii un link di Google Colaboratory, assicurati di attivare la condivisione del link per "chiunque abbia il link".

Stiamo ancora sviluppando i contenuti didattici interattivi per il curriculum di machine learning. Per ora, puoi seguire le sfide video di questa certificazione. Potresti anche dover cercare risorse di apprendimento aggiuntive, come faresti lavorando su un progetto reale.

# --instructions--

Per questa sfida, completerai il codice per classificare immagini di cani e gatti. Userai TensorFlow 2.0 e Keras per creare una rete neurale convoluzionale che classifichi correttamente le immagini di gatti e cani almeno nel 63% dei casi. (Un extra se raggiungi il 70% di accuratezza!)

Parte del codice ti è fornita, ma dovrai completare alcune parti per portare a termine questa sfida. Leggi le istruzioni in ogni cella di testo per sapere cosa devi fare in ogni cella di codice.

La prima cella di codice importa le librerie necessarie. La seconda cella scarica i dati e imposta le variabili chiave. La terza cella è il primo punto in cui scriverai il tuo codice.

La struttura dei file del dataset scaricato è la seguente (noterai che la directory test non ha sottodirectory e le immagini non sono etichettate):

```py
cats_and_dogs
|__ train:
    |______ cats: [cat.0.jpg, cat.1.jpg ...]
    |______ dogs: [dog.0.jpg, dog.1.jpg ...]
|__ validation:
    |______ cats: [cat.2000.jpg, cat.2001.jpg ...]
    |______ dogs: [dog.2000.jpg, dog.2001.jpg ...]
|__ test: [1.jpg, 2.jpg ...]
```

Puoi modificare il numero di epoche e la dimensione del batch se vuoi, ma non è obbligatorio.

Le istruzioni seguenti corrispondono a numeri di cella specifici, indicati con un commento in cima alla cella (come `# 3`).

## Cell 3

Ora tocca a te! Imposta correttamente ciascuna delle variabili in questa cella. (Non dovrebbero più essere uguali a `None`.)

Crea generatori di immagini per ciascuno dei tre set di dati di immagini (train, validation, test). Usa `ImageDataGenerator` per leggere / decodificare le immagini e convertirle in tensori a virgola mobile. Usa l'argomento `rescale` (e nessun altro argomento per ora) per riscalare i tensori da valori tra 0 e 255 a valori tra 0 e 1.

Per le variabili `*_data_gen`, usa il metodo `flow_from_directory`. Passa la dimensione del batch, la directory, la dimensione target (`(IMG_HEIGHT, IMG_WIDTH)`), la modalità classe e tutto ciò che serve. `test_data_gen` sarà la più complicata. Per `test_data_gen`, assicurati di passare `shuffle=False` al metodo `flow_from_directory`. Questo garantirà che le previsioni finali rimangano nell'ordine previsto dal nostro test. Per `test_data_gen` sarà utile osservare anche la struttura delle directory.

Dopo aver eseguito il codice, l'output dovrebbe apparire così:

```py
Found 2000 images belonging to 2 classes.
Found 1000 images belonging to 2 classes.
Found 50 images belonging to 1 class.
```

## Cell 4

La funzione `plotImages` verrà usata più volte per tracciare immagini. Prende un array di immagini e una lista di probabilità, anche se la lista di probabilità è opzionale. Questo codice ti è fornito. Se hai creato correttamente la variabile `train_data_gen`, eseguendo questa cella verranno tracciate cinque immagini casuali del training.

## Cell 5

Ricrea `train_image_generator` usando `ImageDataGenerator`.

Poiché ci sono pochi esempi di training, c'è il rischio di overfitting. Un modo per risolvere questo problema è creare più dati di training a partire dagli esempi esistenti usando trasformazioni casuali.

Aggiungi 4-6 trasformazioni casuali come argomenti a `ImageDataGenerator`. Assicurati di riscalare come prima.

## Cell 6

Non devi fare nulla in questa cella. `train_data_gen` viene creato come prima ma con il nuovo `train_image_generator`. Poi, una singola immagine viene tracciata cinque volte con variazioni diverse.

## Cell 7

In questa cella, crea un modello per la rete neurale che produca probabilità di classe. Dovrebbe usare il modello Keras Sequential. Probabilmente coinvolgerà una pila di layer Conv2D e MaxPooling2D e poi un layer completamente connesso in cima attivato da una funzione di attivazione ReLU.

Compila il modello passando gli argomenti per impostare l'ottimizzatore e la perdita. Passa anche `metrics=['accuracy']` per visualizzare l'accuratezza di training e validazione per ogni epoca di training.

## Cell 8

Usa il metodo `fit` sul tuo `model` per addestrare la rete. Assicurati di passare gli argomenti per `x`, `steps_per_epoch`, `epochs`, `validation_data` e `validation_steps`.

## Cell 9

Esegui questa cella per visualizzare l'accuratezza e la perdita del modello.

## Cell 10

Ora è il momento di usare il tuo modello per prevedere se una nuova immagine è un gatto o un cane.

In questa cella, ottieni la probabilità che ogni immagine di test (da `test_data_gen`) sia un cane o un gatto. `probabilities` dovrebbe essere una lista di numeri interi.

Chiama la funzione `plotImages` e passa le immagini di test e le probabilità corrispondenti a ciascuna immagine di test.

Dopo aver eseguito la cella, dovresti vedere tutte e 50 le immagini di test con un'etichetta che mostra la percentuale di "certezza" che l'immagine sia un gatto o un cane. L'accuratezza corrisponderà a quella mostrata nel grafico sopra (dopo aver eseguito la cella precedente). Più immagini di training potrebbero portare a un'accuratezza più alta.

## Cell 11

Esegui questa cella finale per vedere se hai superato la sfida o se devi continuare a provare.

# --hints--

Dovrebbe superare tutti i test Python.

```js

```

# --solutions--

```py
  # Python challenges don't need solutions,
  # because they would need to be tested against a full working project.
  # Please check our contributing guidelines to learn more.
```
