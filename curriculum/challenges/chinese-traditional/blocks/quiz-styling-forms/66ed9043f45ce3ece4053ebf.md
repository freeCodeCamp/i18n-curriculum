---
id: 66ed9043f45ce3ece4053ebf
title: 表單樣式測驗
challengeType: 8
dashedName: quiz-styling-forms
---

# --description--

要通過測驗，你必須正確回答以下 10 題中的至少 9 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

`datetime-local` 輸入型別的常見樣式問題是什麼？

#### --distractors--

它不適用於 `float` 屬性。

---

無法使其具備響應式設計。

---

它不適用於 `rem` 單位。

#### --answer--

輸入格式在不同瀏覽器間有所不同。

### --question--

#### --text--

以下哪一項被視為為文字輸入設定樣式的最佳實務？

#### --distractors--

文字輸入字型大小需要使用 `em` 單位設定，顏色應該是淺灰色。

---

文字輸入框大小在聚焦時應只使用深綠色邊框，且文字顏色應為深灰色。

---

文字輸入字型顏色需要是淺色，且大小應僅使用 `px` 單位設定。

#### --answer--

文字輸入字型需要有足夠的大小，且顏色需要與背景有足夠的對比。

### --question--

#### --text--

錯誤狀態的樣式應該如何與焦點狀態的樣式相關聯？

#### --distractors--

它們應該是相同的以保持一致性。

---

錯誤狀態應該較不明顯，以避免驚動使用者。

---

錯誤狀態應該是動畫效果，而焦點狀態應該是靜態的。

#### --answer--

錯誤狀態應該與焦點狀態明顯區分。

### --question--

#### --text--

在聚焦狀態下，為輸入欄位設計樣式的最佳做法是什麼？

#### --distractors--

對於聚焦的輸入，重要的是只使用淺灰色邊框。

---

重要的是只對聚焦的輸入使用深紅色邊框。

---

移除任何明顯的指示器非常重要。

#### --answer--

保留明顯的指示器是很重要的。

### --question--

#### --text--

在錯誤狀態下為輸入欄位設計樣式的最佳做法是什麼？

#### --distractors--

將輸入文字的字型大小增加到 `3rem`。

---

將輸入設定為 `display: none;`，以便將其從頁面中移除。

---

將輸入設定為停用，讓使用者無法再與其互動。

#### --answer--

提供帶有訊息的視覺指示器，讓使用者知道某些內容不正確且需要修正。

### --question--

#### --text--

為什麼在表單輸入中使用具描述性的佔位文字很重要？

#### --distractors--

它有助於為使用者預先填寫表單。

---

它會在使用者輸入時自動驗證輸入內容。

---

它會隱藏輸入標籤，減少視覺雜亂。

#### --answer--

它為使用者提供預期輸入格式的指引。

### --question--

#### --text--

什麼是 WebKit？

#### --distractors--

它是一個瀏覽器引擎，確保你的 CSS 中沒有驗證錯誤。

---

它是一個用於 CSS Grid 的特殊 CSS 屬性。

---

它是在 CSS flexbox 中使用的特殊 CSS 屬性。

#### --answer--

它是一個用來顯示網頁的瀏覽器引擎。

### --question--

#### --text--

何時適合對表單元素使用 `appearance: none;`？

#### --distractors--

從 DOM 和頁面中完全移除表單元素。

---

將表單元素隱藏起來，避免使用像是螢幕閱讀器等輔助技術的使用者看到。

---

只移除套用到表單元素的預設顏色，並使用你自己的顏色。

#### --answer--

要移除套用到表單元素的一些預設樣式。

### --question--

#### --text--

在處理表單元素的 `appearance: none;` 時，有哪些注意事項？

#### --distractors--

保留互動元素上的暫留和提交指示器非常重要。

---

只有當你想為 Safari 瀏覽器套用不同樣式時，才重要使用 `appearance: none;`。

---

當你需要完全從頁面中移除一個表單元素時，使用 `appearance: none;` 是很重要的。

#### --answer--

保持互動元素上的焦點和錯誤指示器非常重要。

### --question--

#### --text--

哪一種型別的表單元素通常使用 `appearance: none;` 來移除其預設外觀？

#### --distractors--

`label` 元素。

---

`div` 元素。

---

`span` 元素。

#### --answer--

`input` 元素。
