---
id: 6a04bbc9815e737ed711622e
title: "Обробка помилок у Express — тест"
challengeType: 8
dashedName: quiz-error-handling-in-express
---

# --description--

Щоб пройти тест, потрібно правильно відповісти щонайменше на 18 із 20 запитань нижче.

# --quizzes--

## --quiz--

### --question--

#### --text--

Що означає HTTP статус-код 201?

#### --distractors--

Запит успішний, але без тіла відповіді.

---

Клієнт не має права доступу до ресурсу.

---

Запитуваний ресурс не існує.

#### --answer--

Новий ресурс успішно створено.

### --question--

#### --text--

Який статус-код вказує, що запит успішний, але немає тіла відповіді?

#### --distractors--

`200`

---

`201`

---

`301`

#### --answer--

`204`

### --question--

#### --text--

Що повідомляє клієнту статус-код `304 Not Modified`?

#### --distractors--

Ресурс переміщено на нову постійну URL-адресу.

---

Сервер тимчасово недоступний через обслуговування.

---

Для доступу до ресурсу потрібна автентифікація.

#### --answer--

Нічого не змінилося, тому клієнт може використовувати свою кешовану версію.

### --question--

#### --text--

У чому різниця між статус-кодами `401` і `403`?

#### --distractors--

`401` означає, що ресурс не знайдено, а `403` — що сервер не працює.

---

`401` означає, що запит некоректний, а `403` — що ресурс переміщено.

---

`401` означає, що сервер аварійно завершив роботу, а `403` — що запит вийшов за часом.

#### --answer--

`401` означає, що потрібна автентифікація, а `403` — що автентифіковано, але немає дозволу.

### --question--

#### --text--

Який статус-код означає, що сервер тимчасово перевантажений або недоступний через обслуговування?

#### --distractors--

`500`

---

`502`

---

`301`

#### --answer--

`503`

### --question--

#### --text--

Що робить `res.status(200).json({ message: "Success" })` в Express?

#### --distractors--

Надсилає `200` відповідь із простим текстовим повідомленням.

---

Надсилає `200` відповідь без тіла.

---

Перенаправляє клієнта на сторінку успіху.

#### --answer--

Надсилає `200` відповідь із JSON тілом.

### --question--

#### --text--

Що змушує Express розпізнавати підпрограму як обробник помилок?

#### --distractors--

Вона розміщена на початку стеку підпрограм.

---

Вона використовує `res.error()` замість `res.send()`.

---

Визначена всередині обробника маршруту.

#### --answer--

Вона має параметри `err`, `req`, `res` і `next`.

### --question--

#### --text--

Де слід розміщувати підпрограму для обробки помилок у застосунку Express?

#### --distractors--

Перед усіма визначеннями маршрутів.

---

На самому початку файлу, перед будь-якими імпортами.

---

Всередині першого обробника маршруту, який може викликати помилку.

#### --answer--

Наприкінці стеку підпрограм, після всіх маршрутів.

### --question--

#### --text--

Що потрібно зробити в Express 4, щоб обробляти асинхронні помилки в обробнику маршруту?

#### --distractors--

Викинути помилку безпосередньо з обробника маршруту і дозволити Node.js обробити її на рівні процесу.

---

Повернути об’єкт помилки як JSON у тілі відповіді зі статусом 500.

---

Використовувати `process.on('uncaughtException')` для глобального перехоплення всіх необроблених асинхронних помилок.

#### --answer--

Обгорнути асинхронну логіку в `try/catch` і викликати `next(err)` для передачі помилки обробнику помилок.

### --question--

#### --text--

Яка мета обгорткової функції `asyncHandler` в Express 4?

#### --distractors--

Вона автоматично перетворює синхронні обробники маршрутів на асинхронні.

---

Вона автоматично перезапускає сервер і очищує стан при виникненні помилки.

---

Вона замінює підпрограму для обробки помилок, розміщену в кінці стеку підпрограм.

#### --answer--

Вона уникає повторення `try/catch` у кожному маршруті, передаючи відхилені проміси обробнику помилок `next`.

### --question--

#### --text--

Як Express 5 інакше обробляє асинхронні помилки порівняно з Express 4?

#### --distractors--

Express 5 вимагає додавання глобального слухача `process.on('unhandledRejection')` для перехоплення асинхронних помилок.

---

Express 5 мовчки ігнорує асинхронні помилки і лише записує їх у консоль, не зупиняючи застосунок.

---

Express 5 вимагає, щоб кожен асинхронний маршрут визначав власну підпрограму для обробки помилок.

#### --answer--

Express 5 автоматично передає асинхронні помилки та відхилені проміси обробнику помилок без потреби в `next(err)`.

### --question--

#### --text--

Де слід розміщувати підпрограму `404`, яка ловить усі помилки, у застосунку Express?

#### --distractors--

Перед усіма іншими маршрутами, на початку файлу.

---

Всередині підпрограми для обробки помилок, як запасний варіант.

---

Наприкінці, після підпрограми для обробки помилок.

#### --answer--

Після всіх маршрутів, але перед підпрограмою для обробки помилок.

### --question--

#### --text--

Яка найкраща практика для обробки помилок у продуктивних застосунках Express?

#### --distractors--

Завжди надсилати клієнту повний стек викликів для легшого налагодження.

---

Розміщувати підпрограму для обробки помилок перед маршрутами для швидшої обробки.

---

Обробляти всі помилки, включно з 404, в одній підпрограмі.

#### --answer--

Логувати деталі помилок внутрішньо і повертати користувачам дружнє повідомлення замість стеків викликів.

### --question--

#### --text--

Як увімкнути всі вбудовані повідомлення налагодження Express при запуску застосунку?

#### --distractors--

Передати `{ debug: true }` конструктору `express()`.

---

Викликати `app.enable('debug')` перед визначенням маршрутів.

---

Додати `require('express-debug')` на початку вхідного файлу.

#### --answer--

Встановити змінну середовища `DEBUG` у значення `express:*` під час запуску застосунку.

### --question--

#### --text--

Що робить `DEBUG=express:router node index.js`?

#### --distractors--

Увімкнює весь вивід налагодження Express, включно з підпрограмним забезпеченням, маршрутизацією та подіями застосунку.

---

Повністю вимикає вивід налагодження і запускає застосунок у тихому продуктивному режимі.

---

Записує лише деталі вхідних HTTP-запитів, такі як метод і URL, у термінал.

#### --answer--

Показує лише налагодження, пов’язане з маршрутизатором, фільтруючи інші простори імен Express.

### --question--

#### --text--

Для чого використовується `morgan` у застосунку Express?

#### --distractors--

Автоматично перехоплює і передає асинхронні помилки в обробниках маршрутів Express до `next`.

---

Обслуговує статичні файли з вказаної директорії без визначення окремих маршрутів.

---

Підключає Express до реляційної бази даних і логуює всі виконані запити.

#### --answer--

Логування HTTP-запитів з деталями, такими як метод, URL, статус-код і час відповіді.

### --question--

#### --text--

Яка мета маршруту перевірки стану здоров’я у застосунку Express?

#### --distractors--

Автоматично перезапускати сервер і перенаправляти трафік, коли обсяг вхідних запитів надто великий.

---

Записувати і передавати всі вхідні HTTP-запити на зовнішній сервіс моніторингу або аналітики.

---

Перевіряти і блокувати запити з IP-адрес, яких немає у дозволеному списку.

#### --answer--

Повідомляти, чи працює застосунок коректно, щоб балансувальники навантаження і оркестратори могли визначити, чи сервіс здоровий.

### --question--

#### --text--

Що сигнал `SIGTERM` повідомляє процесу Node.js?

#### --distractors--

Негайно перезапустити, очистити весь стан у пам’яті і повторно ініціалізувати всі активні з’єднання.

---

Зробити повний знімок пам’яті на диск для подальшого аналізу і налагодження.

---

Примусово завершити всі активні з’єднання з базою даних і закрити всі відкриті файлові дескриптори.

#### --answer--

Почати завершення роботи, даючи застосунку можливість завершити поточні завдання перед виходом.

### --question--

#### --text--

Що робить виклик `server.close()` під час коректного завершення роботи?

#### --distractors--

Негайно завершує всі активні з’єднання і виходить з процесу.

---

Надсилає відповідь 500 усім клієнтам, які зараз підключені.

---

Видаляє всі дані сесії в пам’яті і перезапускає сервер.

#### --answer--

Припиняє приймати нові з’єднання, дозволяючи існуючим завершитись.

### --question--

#### --text--

Чому під час коректного завершення роботи ви повертаєте статус 503 із маршруту перевірки стану здоров’я?

#### --distractors--

Щоб зафіксувати подію завершення і час у зовнішній панелі моніторингу для аудиту.

---

Щоб змусити оркестратор негайно запустити новий замінний екземпляр до завершення вимкнення.

---

Щоб повідомити всім підключеним клієнтам, що їхні активні сесії успішно збережено.

#### --answer--

Щоб сигналізувати балансувальнику навантаження, що застосунок недоступний, і він припинив направляти до нього новий трафік.
