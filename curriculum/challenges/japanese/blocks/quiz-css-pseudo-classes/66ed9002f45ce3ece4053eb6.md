---
id: 66ed9002f45ce3ece4053eb6
title: CSS 疑似クラス クイズ
challengeType: 8
dashedName: quiz-css-pseudo-classes
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

疑似クラスとは何ですか？

#### --distractors--

必要に応じて要素内に追加のコンテンツを作成するCSSルールです。

---

要素のサイズやレイアウトを調整するプロパティです。

---

インタラクション中に要素に動きの効果を加えるためのセレクターです。

#### --answer--

セレクターに追加され、要素の状態に基づいてスタイルを適用するキーワードです。

### --question--

#### --text--

ポインティングデバイスが要素の上に位置しているときに適用される疑似クラスはどれですか？

#### --distractors--

`:focus`

---

`:active`

---

`:checked`

#### --answer--

`:hover`

### --question--

#### --text--

段落の最初の文字をスタイル設定できる疑似要素はどれですか？

#### --distractors--

`:first-letter`

---

`:first-child`

---

`:last-of-type`

#### --answer--

`::first-letter`

### --question--

#### --text--

クリックされている間に要素のスタイルを変更する疑似クラスはどれですか？

#### --distractors--

`:focus`

---

`:hover`

---

`:checked`

#### --answer--

`:active`

### --question--

#### --text--

テキストフィールドがクリックされたりタブで移動されたりして、ユーザーの入力を受け付ける準備ができているときにスタイルを適用する疑似クラスはどれですか？

#### --distractors--

`::focus`

---

`:active`

---

`:visited`

#### --answer--

`:focus`

### --question--

#### --text--

クラスが`Note:`の各段落要素の前にテキスト`note`を正しく追加するCSSルールはどれですか？

#### --distractors--

```css
.note::before {
  content: "Note:";
}
```

---

```css
p.note::after {
  content: "Note:";
}
```

---

```css
p::before {
  content: "Note:";
}
```

#### --answer--

```css
p.note::before {
  content: "Note:";
}
```

### --question--

#### --text--

選択またはオンになっている入力フィールドに適用される疑似クラスはどれですか？

#### --distractors--

`:required`

---

`:disabled`

---

`:optional`

#### --answer--

`:checked`

### --question--

#### --text--

リストの最後の子要素をスタイル設定する正しい構文はどれですか？

#### --distractors--

```css
li:nth-child(last) {
  color: blue;
}
```

---

```css
#li:last-child {
  color: blue;
}
```

---

```css
.li:last-child() {
  color: blue;
}
```

#### --answer--

```css
li:last-child {
  color: blue;
}
```

### --question--

#### --text--

入力が必須でない入力フィールドを対象とする疑似クラスはどれですか？

#### --distractors--

`:required`

---

`:enabled`

---

`::optional`

#### --answer--

`:optional`

### --question--

#### --text--

`:disabled` 疑似クラスは何をしますか？

#### --distractors--

チェックされた入力をスタイル設定します。

---

ホバーされている要素をスタイル設定します。

---

指定されたセレクターに一致しない要素を選択します。

#### --answer--

ユーザーが操作できない要素をスタイル設定します。

### --question--

#### --text--

フォーム入力がバリデーション基準を満たしたときに適用される疑似クラスはどれですか？

#### --distractors--

`:checked`

---

`:required`

---

`:disabled`

#### --answer--

`:valid`

### --question--

#### --text--

次のうち、位置に関する疑似クラスでないものはどれですか？

#### --distractors--

`:visited`

---

`:any-link`

---

`:link`

#### --answer--

`:current`

### --question--

#### --text--

次のうち、3番目のリスト項目を選択するものはどれですか？

#### --distractors--

```css
li:child(3) {
  color: red;
}
```

---

```css
li:last-child(3) {
  color: red;
}
```

---

```css
li:nth-child(three) {
  color: red;
}
```

#### --answer--

```css
li:nth-child(3) {
  color: red;
}
```

### --question--

#### --text--

次のCSSで`color`が`blue`になる要素はどれですか？

```css
p:is(.blue, .highlight) {
  color: blue;
}
```

#### --distractors--

```html
<p class="class">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

---

```html
<div class="blue">Paragraph 1</div>
<div class="highlight">Paragraph 2</div>
```

---

```html
<p>Paragraph 1</p>
<span class="highlight">Paragraph 2</span>
```

#### --answer--

```html
<p class="blue">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

### --question--

#### --text--

`:not()` 疑似クラスは何をしますか？

#### --distractors--

すべての要素にスタイルを追加します。

---

親のすべての子要素を選択します。

---

指定されたセレクターに一致する要素を選択します。

#### --answer--

指定されたセレクターに一致しない要素を選択します。

### --question--

#### --text--

次のCSSルールは何をしますか？

```css
p:first-of-type {
  font-style: italic;
}
```

#### --distractors--

ドキュメント内の最初の`p`要素を選択します。

---

ドキュメント内のすべての`p`要素を選択します。

---

すべての`p`要素の最初の子要素を選択します。

#### --answer--

親コンテナ内の最初の`p`要素を選択します。

### --question--

#### --text--

`:last-of-type` 疑似クラスは何をしますか？

#### --distractors--

親内の特定のタイプの最初の子要素を選択します。

---

親内の特定のタイプの中央の子要素を選択します。

---

親内の特定のタイプのすべての子要素を選択します。

#### --answer--

親内の特定のタイプの最後の子要素を選択します。

### --question--

#### --text--

リストの2番目の項目を選択する疑似クラスはどれですか？

#### --distractors--

`:first-child`

---

`:required`

---

`:is()`

#### --answer--

`:nth-child(2)`

### --question--

#### --text--

次のうち、関数型疑似クラスはどれですか？

#### --distractors--

`:first-child`

---

`:match()`

---

`:checked`

#### --answer--

`:is()`

### --question--

#### --text--

次のうち、関数型疑似クラスでないものはどれですか？

#### --distractors--

`:has()`

---

`:not()`

---

`:where()`

#### --answer--

`:contains()`

## --quiz--

### --question--

#### --text--

有効なフォーム要素を対象とする疑似クラスはどれですか？

#### --distractors--

`:disabled`

---

`:active`

---

`:focus`

#### --answer--

`:enabled`

### --question--

#### --text--

末尾から数えて要素を選択できる疑似クラスはどれですか？

#### --distractors--

`:nth-child(n)`

---

`:last-child`

---

`:last-of-type`

#### --answer--

`:nth-last-child(n)`

### --question--

#### --text--

特定の子要素を含む要素を選択できるのはどれですか？

#### --distractors--

`:is()`

---

`:where()`

---

`:in-range`

#### --answer--

`:has()`

### --question--

#### --text--

コンテンツや子要素を一切含まない要素を選択するのはどれですか？

#### --distractors--

`:only-child`

---

`:last-child`

---

`:not()`

#### --answer--

`:empty`

### --question--

#### --text--

このCSSセレクターは何を対象にしていますか？

```css
input:invalid {
  background-color: red;
}
```

#### --distractors--

すべての入力要素。

---

許容範囲内の値を持つすべての入力要素。

---

バリデーションを通過したすべての入力要素。

#### --answer--

バリデーションに失敗したすべての入力要素。

### --question--

#### --text--

ブラウザによって自動的に値が入力された入力フィールドを選択する疑似クラスはどれですか？

#### --distractors--

`:visited`

---

`:valid`

---

`:where()`

#### --answer--

`:autofill`

### --question--

#### --text--

要素自身またはその子孫要素のいずれかにフォーカスがある場合に選択する疑似クラスはどれですか？

#### --distractors--

`:focus`

---

`:in-range`

---

`:only-child`

#### --answer--

`:focus-within`

### --question--

#### --text--

同じドキュメントを指すリンクを表す疑似クラスはどれですか？

#### --distractors--

`:target`

---

`:visited`

---

`:link`

#### --answer--

`:local-link`

### --question--

#### --text--

URLフラグメントのターゲットになっているときに`p`要素をスタイル設定するのはどれですか？

#### --distractors--

```css
p:empty {
  background-color: gold;
}
```

---

```css
p:not(.targeted) {
  background-color: gold;
}
```

---

```css
p:is(.target) {
  background-color: gold;
}
```

#### --answer--

```css
p:target {
  background-color: gold;
}
```

### --question--

#### --text--

要素がURLフラグメントのターゲットであるときに使われる疑似クラスはどれですか？

#### --distractors--

`:focus-within`

---

`:hover`

---

`:visited`

#### --answer--

`:target`

### --question--

#### --text--

`:only-child` 疑似クラスは何を選択しますか？

#### --distractors--

子が1つだけの親要素を選択します。

---

親要素内のすべての子要素を選択します。

---

1種類の子要素だけを含む親要素を選択します。

#### --answer--

親要素内で兄弟要素を持たない要素を選択します。

### --question--

#### --text--

親要素内でそのタイプの要素が1つだけの場合に選択する疑似クラスはどれですか？

#### --distractors--

`:only-child`

---

`:nth-of-type(n)`

---

`:first-of-type`

#### --answer--

`:only-of-type`

### --question--

#### --text--

次のHTMLで2番目の`color`要素に`yellow`を`p`で適用するCSSルールはどれですか？

```html
<div>
  <h1>Courses</h1>
  <p>HTML</p>
  <p>CSS</p>
  <p>JavaScript</p>
</div>
```

#### --distractors--

```css
p:nth-child(2) {
  color: yellow;
}
```

---

```css
p:first-of-type {
  color: yellow;
}
```

---

```css
p:last-of-type {
  color: yellow;
}
```

#### --answer--

```css
p:nth-of-type(2) {
  color: yellow;
}
```

### --question--

#### --text--

リスト項目のマーカーを選択してスタイル設定できる疑似要素はどれですか？

#### --distractors--

`::before`

---

`::after`

---

`:root`

#### --answer--

`::marker`

### --question--

#### --text--

ドキュメント内の最上位要素、通常は`html`要素を対象にできる疑似クラスはどれですか？

#### --distractors--

`:first-child`

---

`:in-range`

---

`:target`

#### --answer--

`:root`

### --question--

#### --text--

特異性がゼロで、他の特定のスタイルルールに干渉しないCSS疑似クラスはどれですか？

#### --distractors--

`:is()`

---

`:not()`

---

`:focus`

#### --answer--

`:where()`

### --question--

#### --text--

次のうち、ツリー構造に関する疑似クラスはどれですか？

#### --distractors--

`:where()`

---

`:valid`

---

`:link`

#### --answer--

`:root`

### --question--

#### --text--

指定された範囲外の値の場合に次の要素の`background-color`を`red`に設定するCSSルールはどれですか？

```html
<input type="number" min="10" max="25"/>
```

#### --distractors--

```css
input:in-range {
  background-color: red;
}
```

---

```css
input {
  background-color: red;
}
```

---

```css
input:valid {
  background-color: red;
}
```

#### --answer--

```css
input:out-of-range {
  background-color: red;
}
```

### --question--

#### --text--

要素の後にコンテンツを挿入するために`content`プロパティを使う疑似要素はどれですか？

#### --distractors--

`::before`

---

`::marker`

---

`::first-letter`

#### --answer--

`::after`

### --question--

#### --text--

値が指定範囲内の場合に要素にスタイルを適用する疑似クラスはどれですか？

#### --distractors--

`:out-of-range`

---

`:enabled`

---

`:checked`

#### --answer--

`:in-range`
