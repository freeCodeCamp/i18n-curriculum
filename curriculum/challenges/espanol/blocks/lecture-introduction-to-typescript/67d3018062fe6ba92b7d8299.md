---
id: 67d3018062fe6ba92b7d8299
title: ¿Cómo funcionan los tipos primitivos en TypeScript?
challengeType: 19
dashedName: how-do-primitive-types-work-in-typescript
---

# --description--

En esta lección, aprenderás cómo trabajar con tipos primitivos básicos y cómo funcionan las anotaciones de tipo para variables.

Para los tipos de datos primitivos `string`, `null`, `undefined`, `number`, `boolean` y `bigint`, TypeScript ofrece palabras clave de tipo correspondientes. 

Para esta lección, nos enfocaremos en los primitivos comúnmente usados y no entraremos en mucho detalle sobre `bigint`. El tipo de dato `bigint` es un tipo numérico en JavaScript para representar números más grandes que el tipo entero estándar. 

Veamos cada uno de estos tipos primitivos comenzando con `string`. Aquí está cómo puedes escribir una cadena en TypeScript:

```ts
let firstName: string = "Angie";
```

A diferencia de JavaScript puro, la variable `firstName` espera un tipo `string`. Así que si intentas reasignarla con un valor numérico, obtendrás un error como este:

```ts
let firstName: string = "Angie";
firstName = 9 // Type 'number' is not assignable to type 'string'.
```

El siguiente tipo que veremos es el tipo `number`. Aquí tienes un ejemplo:

```ts
let age: number = 16;
```

Puedes asignar enteros y flotantes al tipo `number`. Pero si intentas reasignar un valor que es del tipo `bigint`, entonces obtendrás un error como este:

```ts
let age: number = 16;

// Type 'bigint' is not assignable to type 'number'.
age = 123n;
```

El siguiente tipo que veremos es el tipo `boolean`. Aquí hay algunos ejemplos:

```ts
let isLoggedIn: boolean = true;
let isAdmin: boolean = false;
```

Los dos últimos tipos son los tipos `null` y `undefined`. 

Como recuerdas de lecciones anteriores de JavaScript, `null` representa una ausencia intencional de un valor. `undefined` significa que una variable ha sido declarada pero no se le ha asignado un valor.

Verás ejemplos de cómo trabajar con estos tipos más adelante cuando aprendas sobre tipos de `function` y tipos de unión.

Ahora que has visto cómo funcionan las anotaciones de tipo con las variables, surge la pregunta de si siempre necesitas escribir explícitamente el tipo de tus variables. 

Si tienes una anotación de tipo simple como esta, entonces puede que no necesites usar un tipo explícito.

```ts
let firstName: string = "Angie";
```

La razón es que TypeScript intentará inferir automáticamente los tipos basándose en su valor. Así que si escribes esto, TypeScript entenderá que es una cadena.

```js
let firstName = "Angie";
```

Debes tipar explícitamente tus variables cuando TypeScript no pueda inferir claramente el tipo o cuando quieras forzar un tipo específico para situaciones como parámetros de funciones u objetos complejos.

# --questions--

## --text--

¿Cuál de las siguientes es la forma correcta de agregar explícitamente una anotación de tipo a una variable de número?

## --answers--

```ts
let age: number = 16;
```

---

```ts
let age<> number = 16;
```

### --feedback--

Consulta al inicio de la lección para los tipos de números.

---

```ts
let age>> number = 16;
```

### --feedback--

Consulta al principio de la lección para los tipos de números.

---

```ts
let age == number = 16;
```

### --feedback--

Consulta al principio de la lección para los tipos de números.

## --video-solution--

1

## --text--

¿Cuál de los siguientes NO es un tipo de dato primitivo?

## --answers--

`string`

### --feedback--

Consulta al principio de la lección.

---

`boolean`

### --feedback--

Consulta al principio de la lección.

---

`object`

---

`number`

### --feedback--

Consulta al principio de la lección.

## --video-solution--

3

## --text--

¿Cuándo debes escribir explícitamente el tipo de tus variables en TypeScript?

## --answers--

Cuando trabajas con los tipos `number` y `bigint`.

### --feedback--

Consulta al final de la lección.

---

Cuando trabajas con los tipos `null` y `undefined`.

### --feedback--

Consulta al final de la lección.

---

Cuando TypeScript no puede inferir claramente el tipo.

---

Siempre debes tener tipos explícitos en tu código.

### --feedback--

Consulta al final de la lección.

## --video-solution--

3
