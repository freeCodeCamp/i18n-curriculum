---
id: 681cb1afdab50c87ddb2e517
title: "Zoezi la 6: Kagua anagramu"
challengeType: 28
dashedName: challenge-6
---

# --description--

Ukizingatia mfuatano wa herufi mbili, amua kama ni anagramu za kila mmoja (zina herufi sawa kwa mpangilio wowote).

- Puuza tofauti za herufi kubwa na ndogo pamoja na nafasi tupu.

# --hints--

`areAnagrams("listen", "silent")` inapaswa kurudisha `true`.

```js
assert.isTrue(areAnagrams("listen", "silent"));
```

`areAnagrams("School master", "The classroom")` inapaswa kurudisha `true`.

```js
assert.isTrue(areAnagrams("School master", "The classroom"));
```

`areAnagrams("A gentleman", "Elegant man")` inapaswa kurudisha `true`.

```js
assert.isTrue(areAnagrams("A gentleman", "Elegant man"));
```

`areAnagrams("Hello", "World")` inapaswa kurudisha `false`.

```js
assert.isFalse(areAnagrams("Hello", "World"));
```

`areAnagrams("apple", "banana")` inapaswa kurudisha `false`.

```js
assert.isFalse(areAnagrams("apple", "banana"));
```

`areAnagrams("cat", "dog")` inapaswa kurudisha `false`.

```js
assert.isFalse(areAnagrams("cat", "dog"));
```

# --seed--

## --seed-contents--

```js
function areAnagrams(str1, str2) {

  return str1;
}
```

# --solutions--

```js
function areAnagrams(str1, str2) {
  const clean = (str) =>
    str.replace(/\s+/g, '').toLowerCase().split('').sort().join('');
    
  return clean(str1) === clean(str2);
}
```
