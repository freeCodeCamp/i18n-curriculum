---
id: 699c6f4d4e468a10d3ae25ff
title: 자료 구조 퀴즈
challengeType: 8
dashedName: quiz-data-structures-js
---

# --description--

퀴즈를 통과하려면 아래 20문제 중 최소 18문제를 올바르게 답해야 합니다.

# --quizzes--

## --quiz--

### --question--

#### --text--

알고리즘 분석에서 Big O 표기법은 무엇을 설명하나요?

#### --distractors--

특정 컴퓨터에서의 정확한 실행 시간(초 단위).

---

실행 중 실행된 코드 줄의 비율.

---

다른 개발자가 코드를 얼마나 읽기 쉬운지.

#### --answer--

입력 크기에 따라 시간 또는 공간이 어떻게 증가하는지(상한선).

### --question--

#### --text--

알고리즘 도전 과제를 시작할 때 가장 좋은 첫 단계는 무엇인가요?

#### --distractors--

모멘텀을 얻기 위해 즉시 코딩을 시작한다.

---

문제를 이해하기 전에 성능 최적화를 한다.

---

해결책을 완성한 후에만 단위 테스트를 작성한다.

#### --answer--

예제와 극한 사례로 문제와 제약 조건을 명확히 한다.

### --question--

#### --text--

동적 배열과 정적 배열의 주요 차이점은 무엇인가요?

#### --distractors--

동적 배열은 서로 다른 타입의 값을 저장하고, 정적 배열은 그렇지 않다.

---

정적 배열은 중복 값을 허용하지만, 동적 배열은 허용하지 않는다.

---

동적 배열은 모든 작업에서 정적 배열보다 빠르다.

#### --answer--

동적 배열은 크기를 조절해 늘어나거나 줄어들 수 있지만, 정적 배열은 크기가 고정되어 있다.

### --question--

#### --text--

동적 배열 끝에 요소를 추가하는 데 걸리는 상쇄 시간 복잡도는 무엇인가요?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` 상쇄

### --question--

#### --text--

단일 연결 리스트에서 인덱스 k번째 요소에 접근하는 데 `O(n)` 시간이 걸리는 이유는 무엇인가요?

#### --distractors--

접근 전에 리스트 크기를 조절해야 한다.

---

인덱스가 해시되어 표에서 조회된다.

---

노드가 연속적으로 저장되어 있어 이동이 필요하다.

#### --answer--

헤드 노드부터 k번째 노드까지 하나씩 순회해야 한다.

### --question--

#### --text--

단일 연결 리스트에는 없고 이중 연결 리스트에 있는 기능은 무엇인가요?

#### --distractors--

`O(1)` 시간에 임의 인덱스에 랜덤 접근.

---

더 빠른 반복을 위한 내장 배열 버퍼.

---

리스트 길이를 자동으로 일정하게 유지.

#### --answer--

다음 노드와 이전 노드 모두를 가리키는 포인터로 뒤로 탐색 가능.

### --question--

#### --text--

스택을 가장 잘 설명하는 것은 무엇인가요?

#### --distractors--

앞에서 제거하는 선입선출(`FIFO`) 구조.

---

`O(1)` 시간에 임의 요소를 제거할 수 있는 구조.

---

상수 시간 랜덤 접근이 가능한 원형 버퍼.

#### --answer--

후입선출(`LIFO`) 구조로, `push`와 `pop`가 맨 위에서 이루어진다.

### --question--

#### --text--

큐에서 앞쪽 요소를 제거하는 연산은 무엇인가요?

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

해시 맵에서 키로 값을 조회할 때 일반적인 평균 시간 복잡도는 무엇인가요?

#### --distractors--

모든 키를 순차 검색하므로 `O(n)`.

---

버킷 내 이진 검색으로 `O(log n)`.

---

삽입 시 키가 정렬되어 있어 `O(n log n)`.

#### --answer--

좋은 해시 함수와 낮은 부하율에서 평균적으로 `O(1)`.

### --question--

#### --text--

집합 자료 구조가 제공하는 보장은 무엇인가요?

#### --distractors--

기본적으로 요소가 정렬된 순서로 저장된다.

---

중복 값을 허용하고 함께 저장한다.

---

요소가 삽입 위치별로 인덱싱된다.

#### --answer--

중복 없이 고유한 요소만 저장한다.

### --question--

#### --text--

동적 배열에서 끝이 아닌 인덱스 i에 요소를 삽입할 때 최악의 시간 복잡도는 무엇인가요?

#### --distractors--

`O(1)`

---

`O(log n)`

---

`O(1)` 상쇄

#### --answer--

`O(n)`

### --question--

#### --text--

단일 연결 리스트의 헤드에 새 노드를 삽입하는 시간 복잡도는 무엇인가요?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

스택에서 요소를 제거하지 않고 맨 위 요소를 반환하는 연산은 무엇인가요?

#### --distractors--

`push`

---

`pop`

---

맨 아래에 삽입.

#### --answer--

`peek`

### --question--

#### --text--

큐를 가장 잘 설명하는 것은 무엇인가요?

#### --distractors--

맨 위에서 제거하는 후입선출(`LIFO`).

---

`O(1)` 시간에 임의 인덱스에 랜덤 접근.

---

요소가 항상 자동으로 정렬된 상태로 유지된다.

#### --answer--

선입선출(`FIFO`) 구조로, `enqueue`는 뒤에서, `dequeue`는 앞에서 이루어진다.

### --question--

#### --text--

언어에 독립적인 알고리즘 논리의 고수준 설명을 무엇이라 하나요?

#### --distractors--

구문

---

기계어

---

디버거

#### --answer--

의사 코드

### --question--

#### --text--

문제를 이해하기 위해 해야 할 일 중 하나는 무엇인가요?

#### --distractors--

즉시 코딩을 시작한다.

---

극한 사례를 무시한다.

---

바로 최적화로 넘어간다.

#### --answer--

문제 설명을 여러 번 읽는다.

### --question--

#### --text--

문제를 해결할 때 식별해야 할 두 가지 구성 요소는 무엇인가요?

#### --distractors--

프로그래밍 언어와 IDE.

---

변수 이름과 기타 식별자.

---

색상 구성표와 코드 포맷팅.

#### --answer--

입력과 예상 출력.

### --question--

#### --text--

n이 커질 때 `O(n log n)`보다 더 빠르게 증가하는 시간 복잡도는 무엇인가요?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

브루트포스 해결책을 구현한 후 좋은 다음 단계는 무엇인가요?

#### --distractors--

측정 전에 상수 계수를 미세 최적화한다.

---

테스트를 버리고 해결책을 처음부터 다시 작성한다.

---

코드를 단순하게 유지하려고 극한 사례를 고려하지 않는다.

#### --answer--

시간/공간 복잡도를 분석하고 병목 구간을 최적화한다.

### --question--

#### --text--

공간 복잡도는 무엇을 측정하나요?

#### --distractors--

프로그램이 사용하는 CPU 코어 수.

---

프로그램 코드 줄 수.

---

프로그램 컴파일 시간.

#### --answer--

입력 크기에 따라 메모리 사용량이 어떻게 증가하는지.
