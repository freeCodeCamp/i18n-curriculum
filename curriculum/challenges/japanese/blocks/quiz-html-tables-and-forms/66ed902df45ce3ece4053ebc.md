---
id: 66ed902df45ce3ece4053ebc
title: HTMLのテーブルとフォームのクイズ
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

フォームデータを送信するURLを指定するために使われる属性はどれですか？

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

テーブルで行と行のヘッダーを指定するために使われる2つの要素はどれですか？

#### --distractors--

`r`要素は行に使われ、`thead`要素はヘッダーに使われます。

---

`row`要素は行に使われ、`th`要素はヘッダーに使われます。

---

`tr`要素は行に使われ、`head`要素はヘッダーに使われます。

#### --answer--

`tr`要素は行に使われ、`th`要素はヘッダーに使われます。

### --question--

#### --text--

`td`要素は何のために使われますか？

#### --distractors--

テーブルのデータ型を定義するために使われます。

---

2つの列をマージするために使われます。

---

2つの行をマージするために使われます。

#### --answer--

テーブルのセルを定義するために使われます。

### --question--

#### --text--

`colspan`属性の役割は何ですか？

#### --distractors--

テーブルの列の長さを定義します。

---

列内のテキストの改行を削除します。

---

列の数を定義するために使われます。

#### --answer--

テーブルのセルがまたがる列数を定義します。

### --question--

#### --text--

`placeholder`属性の役割は何ですか？

#### --distractors--

フォームが提出されたときにデータをサーバーに送信するために使われます。

---

`label`要素と`input`要素を関連付けるために使われます。

---

入力フィールドをユーザーに対して読み取り専用に指定するために使われます。

#### --answer--

入力フィールドのヒントを提供するために使われます。

### --question--

#### --text--

`thead`要素の目的は何ですか？

#### --distractors--

HTMLテーブルの本文の内容をグループ化するために使われます。

---

HTMLテーブルの説明をグループ化するために使われます。

---

HTMLテーブルのフッターの内容をグループ化するために使われます。

#### --answer--

HTMLテーブルのヘッダーの内容をグループ化するために使われます。

### --question--

#### --text--

ラベルと入力を明示的に関連付ける例はどれですか？

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

HTMLバリデーターは何のために使われますか？

#### --distractors--

バリデーターはHTMLコードを自動的にフォーマットするツールです。

---

バリデーターはHTMLにスタイルを適用するツールです。

---

バリデーターはHTMLコードの実行速度を速くするツールです。

#### --answer--

バリデーターはHTMLコードの構文をチェックして有効かどうかを確認するツールです。

### --question--

#### --text--

テーブルのセルを定義するために使われる要素はどれですか？

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

ウェブページのHTML構造を検査・変更できるツールはどれですか？

#### --distractors--

DOMの削除。

---

DOMテスター。

---

DOMバリデーション。

#### --answer--

DOMインスペクター。

### --question--

#### --text--

関連する入力フィールドをグループ化する正しい方法はどれですか？

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

入力フィールドを読み取り専用に指定するために使われるのはどれですか？

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

フォームを提出する前に入力必須であることを指定する属性はどれですか？

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

開発者ツールは何のために使われますか？

#### --distractors--

これらのツールはブラウザに直接組み込まれており、コードのリンティング問題を見つけるために使われます。

---

これらのツールはブラウザに直接組み込まれており、コードを自動的にフォーマットするために使われます。

---

これらのツールはブラウザに直接組み込まれており、コードの100％のテストカバレッジを保証するために使われます。

#### --answer--

これらのツールはブラウザに直接組み込まれており、ウェブページのデバッグ、プロファイル、解析を支援します。

### --question--

#### --text--

入力を無効にする正しい方法はどれですか？

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

`type`属性に対して有効な値はどれですか？

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

`size`属性の正しい使い方はどれですか？

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

入力フィールドの最小および最大文字数を指定する属性はどれですか？

#### --distractors--

`minlen`と`maxlen`

---

`min`と`max`

---

`minimumLen`と`maximumLen`

#### --answer--

`minlength`と`maxlength`

### --question--

#### --text--

次のうち、`type`属性に対して正しくない値を使っているボタンはどれですか？

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

ボタンの値を指定するために使われる属性はどれですか？

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

開く`action`タグ内の`form`属性の役割は何ですか？

#### --distractors--

入力フィールドを読み取り専用に指定するために使われます。

---

ユーザーが入力する際に表示される文字数を定義するために使われます。

---

入力フィールドに何を入力すればよいかを示すヒントを表示するために使われます。

#### --answer--

フォームデータを送信するURLを指定するために使われます。

### --question--

#### --text--

フォームデータを送信する際に使うHTTPメソッドを指定する属性はどれですか？

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

フォームを扱う際によく使われるメソッドはどれですか？

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

`type`属性に対して無効な値はどれですか？

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

フォーム内でボタンを作成する正しい方法はどれですか？

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

暗黙的なフォームの関連付けとは何ですか？

#### --distractors--

これは`action`と`for`属性を`label`要素に使って入力とラベルを関連付ける方法です。

---

これは`required`と`for`属性を`label`要素に使って入力とラベルを関連付ける方法です。

---

これは`for`属性を`label`要素に使って入力とラベルを関連付ける方法です。

#### --answer--

これは入力フィールドを`label`要素で囲むことで入力とラベルを関連付ける方法です。

### --question--

#### --text--

`fieldset`要素の役割は何ですか？

#### --distractors--

HTMLテーブルのヘッダーの内容をグループ化するために使われます。

---

入力フィールドのラベルを作成するために使われます。

---

入力グループの説明を追加するために使われます。

#### --answer--

関連する入力をグループ化するために使われます。

### --question--

#### --text--

フォームでチェックボックスを作成するために使われるのはどれですか？

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

HTMLテーブルのタイトルを追加するために使われる要素はどれですか？

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

`legend`要素の役割は何ですか？

#### --distractors--

HTMLテーブルの本文の内容をグループ化するために使われます。

---

テーブルのセルを説明するキャプションを追加するために使われます。

---

関連する入力をグループ化するために使われます。

#### --answer--

入力グループの説明を追加するために使われます。

### --question--

#### --text--

HTMLテーブルのヘッダーの内容をグループ化するために使われる要素はどれですか？

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

テーブルに行とデータセルを追加する正しい方法はどれですか？

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

DOMインスペクターとは何ですか？

#### --distractors--

DOMからすべての要素を削除するツール。

---

ブラウザに直接組み込まれたツールセットで、ウェブページのデバッグ、プロファイル、解析を支援します。

---

HTMLコードの構文をチェックして有効かどうかを確認するツール。

#### --answer--

ウェブページのHTML構造を検査・変更できるツール。

### --question--

#### --text--

HTMLコードの構文をチェックして有効かどうかを確認するツールはどれですか？

#### --distractors--

HTML validate

---

HTML isValid

---

HTML valid

#### --answer--

HTML validator

### --question--

#### --text--

テーブルのデータセルの列数を指定する正しい方法はどれですか？

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

入力フィールドのフォーカス状態とは何ですか？

#### --distractors--

ユーザーによってフォーカスが外された状態の入力フィールド。

---

ユーザーによって読み取り専用に設定された入力フィールド。

---

ユーザーによって無効化された入力フィールド。

#### --answer--

ユーザーによって選択された入力フィールドの状態。

### --question--

#### --text--

ラジオボタンを作成する正しい方法はどれですか？

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

`for`属性の役割は何ですか？

#### --distractors--

ユーザーにヒントを表示するために使われます。

---

入力の値を指定するために使われます。

---

入力フィールドのラベルを作成するために使われます。

#### --answer--

ラベルがどの入力フィールドに対応するかを指定するために使われます。

### --question--

#### --text--

ユーザーが入力する際に表示される文字数を定義する属性はどれですか？

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

入力フィールドに何を入力すればよいかを示すヒントを表示する属性はどれですか？

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
