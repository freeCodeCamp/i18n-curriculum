---
id: 66edc25ae5ea80bf6f785552
title: JavaScript 变量和数据类型测验
challengeType: 8
dashedName: quiz-javascript-variables-and-data-types
---

# --description--

要通过测验，你必须正确回答以下 20 道题中的至少 18 题。

# --quizzes--

## --quiz--

### --question--

#### --text--

以下哪项最恰当地描述了 JavaScript？

#### --distractors--

一种用于设计网站的标记语言。

---

一种服务器端编程语言。

---

数据库管理系统。

#### --answer--

一种用于网络开发的编程语言。

### --question--

#### --text--

以下哪项不是 JavaScript 数据类型？

#### --distractors--

`Number`

---

`Undefined`

---

`Object`

#### --answer--

`Double`

### --question--

#### --text--

以下哪项不是有效的 JavaScript 变量声明？

#### --distractors--

`const x = 0;`

---

`let x;`

---

`var x;`

#### --answer--

`public Int x;`

### --question--

#### --text--

以下哪项是 JavaScript 中变量的常见命名规范？

#### --distractors--

JavaScript case。

---

Lightning case。

---

Giraffe case。

#### --answer--

Camel case。

### --question--

#### --text--

在 JavaScript 中，`let` 和 `const` 变量声明有什么区别？

#### --distractors--

`const` 不能重新赋值，也不能重新声明。 `let` 可以重新赋值和声明。

---

`const` 可以重新赋值和声明。 `let` 不能重新赋值，也不能重新声明。

---

`const` 可以重新赋值，但不能重新声明。 `let` 可以重新声明，但不能重新赋值。

#### --answer--

`const` 不能重新赋值或重新声明。 `let` 可以重新赋值，但不能重新声明。

### --question--

#### --text--

为什么在 JavaScript 中字符串是不可变的？

#### --distractors--

不能使用变量创建字符串。

---

字符串只能使用字面量创建。

---

可以更改字符串，但只能通过全局变量。

#### --answer--

字符串一旦创建，就不能直接更改其字符。

### --question--

#### --text--

如何在 JavaScript 的控制台中打印出 `Hello, world!` 字符串？

#### --distractors--

`print("Hello, world!")`

---

`alert("Hello, world");`

---

`System.out.println("Hello, world!");`

#### --answer--

`console.log("Hello, world!");`

### --question--

#### --text--

下面的代码有什么作用？

```js
let hello = "Hello";
hello += " world";
console.log(hello);
```

#### --distractors--

将 `" world"` 重新赋值给变量 `hello` 并在控制台中打印出来。

---

在控制台中打印出 `Hello world` 中的字符数。

---

将 `"Hello"` 和 `" world"` 合并为一个新字符串并打印出来，但 `hello` 的值仍然是 `"Hello"`。

#### --answer--

将 `hello` 的当前值与字符串 `" world"` 相结合，将其重新赋值给 `hello`，并打印到控制台。

### --question--

#### --text--

哪个 JavaScript 字符用于标记语句的结束？

#### --distractors--

`:`

---

`,`

---

`.`

#### --answer--

`;`

### --question--

#### --text--

以下哪项是 JavaScript 中有效的单行注释语法？

#### --distractors--

`# This is a single line comment`

---

`-- This is a single line comment`

---

`/* This is a single line comment */`

#### --answer--

`// This is a single line comment`

### --question--

#### --text--

JavaScript 中的动态类型是指：

#### --distractors--

在使用变量之前，需要声明其数据类型。

---

不能将一种数据类型的值赋值给另一种类型的变量。

---

JavaScript 在执行前会进行类型检查，这意味着与类型相关的错误会在代码运行前抛出。

#### --answer--

JavaScript 会根据分配给变量的值推断类型。

### --question--

#### --text--

以下哪项会在控制台中打印出变量 `x` 的类型？

#### --distractors--

`console.log(x.type);`

---

`console.log(typeOf(x));`

---

`console.log(isNaN(x));`

#### --answer--

`console.log(typeof x);`

### --question--

#### --text--

`console.log(typeof null);` 的输出结果是什么？

#### --distractors--

`"undefined"`

---

`"null"`

---

`"NaN"`

#### --answer--

`"object"`

### --question--

#### --text--

以下哪项是 JavaScript 中有效的多行注释？

#### --distractors--

```js
// This is a
// multi-line
// comment
```

---

```py
'''
This is a
multi-line
comment
'''
```

---

```html
<!--
This is a
multi-line
comment
-->
```

#### --answer--

```js
/*
This is a
multi-line
comment
*/
```

### --question--

#### --text--

以下哪项声明了 JavaScript 中不可更改的变量？

#### --distractors--

`var x = 0;`

---

`let x = 0;`

---

`final x = 0;`

#### --answer--

`const x = 0;`

### --question--

#### --text--

以下哪项是 JavaScript 中有效的变量名？

#### --distractors--

`tart@n`

---

`4cats`

---

`!!dogs`

#### --answer--

`$number`

### --question--

#### --text--

以下哪项不是 JavaScript 中有效的字符串连接方法？

#### --distractors--

`console.log(string1 + " " + string2);`

---

`console.log("string1 " + string2);`

---

`console.log(string1.concat(" ", string2));`

#### --answer--

`console.log(string1 string2);`

### --question--

#### --text--

以下哪个变量名正确使用了驼峰大小写？

#### --distractors--

`a_long_variable_name`

---

`alongvariableName`

---

`ALongVariableName`

#### --answer--

`aLongVariableName`

### --question--

#### --text--

为什么即使 JavaScript 具有自动分号插入功能，明确使用分号仍有好处？

#### --distractors--

提高代码执行速度。

---

与其他编程语言保持一致。

---

以便更好地进行调试和错误跟踪。

#### --answer--

提高代码的可读性和可靠性。

### --question--

#### --text--

以下哪项不是 JavaScript 数据类型？

#### --distractors--

`Symbol`

---

`Boolean`

---

`Null`

#### --answer--

`Float`
