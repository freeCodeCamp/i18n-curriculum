---
id: 66edcd0ecb4b25dc64a34804
title: JavaScript-Objekte-Quiz
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 Fragen unten richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche der folgenden Aussagen beschreibt kein Objekt in JavaScript?

#### --distractors--

Eine Sammlung von Schlüssel-Wert-Paaren.

---

Ein Container für Eigenschaften und Methoden.

---

Ein nicht-primitiver Datentyp.

#### --answer--

Ein primitiver Datentyp.

### --question--

#### --text--

Welche der folgenden Objektdefinitionen ist ungültig?

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

Wie können Sie auf die `job`-Eigenschaft dieses `person`-Objekts zugreifen?

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

Wie können Sie auf die `street`-Eigenschaft des untenstehenden `person`-Objekts zugreifen?

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

Welche dieser Eigenschaftsschlüssel können nur mit Klammernotation aufgerufen werden?

#### --distractors--

Ein Eigenschaftsschlüssel, der bekannt und statisch ist.

---

Ein Eigenschaftsschlüssel, der in mehreren Ebenen eines Objekts verschachtelt ist.

---

Ein Eigenschaftsschlüssel, der möglicherweise nicht im Objekt existiert.

#### --answer--

Ein Eigenschaftsschlüssel, der dynamisch ist.

### --question--

#### --text--

Welche der folgenden Varianten verwendet Destrukturierung korrekt, um auf die `name`-Eigenschaft des untenstehenden `person`-Objekts zuzugreifen?

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

Welche der folgenden Varianten verwendet Destrukturierung korrekt, um auf die `flour`-Eigenschaft des untenstehenden `recipe`-Objekts zuzugreifen?

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

Wie können Sie beim Verwenden von Destrukturierung in JavaScript einem Eigenschaftsschlüssel einen Standardwert zuweisen?

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

Wie können Sie überprüfen, ob ein `car`-Objekt eine `year`-Eigenschaft besitzt?

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

Was ist eine Objektmethode?

#### --distractors--

Ein String, der mit einem Objekt verbunden ist.

---

Ein Array, das mit einem Objekt verbunden ist.

---

Eine Zahl, die mit einem Objekt verbunden ist.

#### --answer--

Eine Funktion, die mit einem Objekt verbunden ist.

### --question--

#### --text--

Wie können Sie die `add`-Methode dieses `calculator`-Objekts aufrufen, um die Zahlen `10` und `7` zu addieren?

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

Welcher Operator entfernt eine Eigenschaft aus einem Objekt?

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

Wie können Sie überprüfen, ob eine `score`-Eigenschaft in einem `player`-Objekt existiert?

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

Wie können Sie in JavaScript ein neues leeres Objekt erstellen?

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

Wie lautet die korrekte Syntax für die Verwendung von optionaler Verkettung in JavaScript?

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

Wozu wird optionale Verkettung in JavaScript verwendet?

#### --distractors--

Wird verwendet, um Variablen mit Standardwerten zu definieren.

---

Wird verwendet, um optionale Methoden innerhalb eines Objekts zu definieren, die möglicherweise aufgerufen werden oder nicht.

---

Wird verwendet, um kürzeren Code zu schreiben, indem mehrere Methodenaufrufe in einer einzigen Anweisung verkettet werden.

#### --answer--

Wird verwendet, um sicher auf Eigenschaften eines Objekts zuzugreifen, die möglicherweise `null` oder `undefined` sind.

### --question--

#### --text--

Welche Methode wird verwendet, um ein JavaScript-Objekt in einen JSON-String umzuwandeln?

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

Welche Methode wandelt einen JSON-String zurück in ein JavaScript-Objekt um?

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

Welche dieser Optionen greift mit Klammernotation auf die `color`-Eigenschaft eines `toy`-Objekts zu?

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

Wie können Sie in JavaScript mit Punktnotation auf das `home`-Telefon dieses `learner`-Objekts zugreifen?

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`

