---
id: 6a298d88d1606c0f1ef322a9
title: 认证和授权测验
challengeType: 8
dashedName: quiz-authentication-and-authorization
---

# --description--

要通过测验，你必须正确回答下面 20 个问题中的至少 18 个。

# --quizzes--

## --quiz--

### --question--

#### --text--

JSON Web Token 的三个部分是什么？

#### --distractors--

头部、体和页脚

---

令牌、载荷和哈希

---

ID、声明和证书

#### --answer--

头部、载荷和签名

### --question--

#### --text--

以下哪项是“你拥有的东西”作为认证因素的示例？

#### --distractors--

指纹扫描。

---

你记住的密码。

---

安全问题的答案。

#### --answer--

发送到你智能手机的一次性代码。

### --question--

#### --text--

以下哪项最能描述授权？

#### --distractors--

验证用户的凭据是否与存储的记录匹配。

---

用户成功登录后生成会话令牌。

---

在将用户密码保存到数据库之前加密它。

#### --answer--

决定经过认证的用户被允许执行哪些操作。

### --question--

#### --text--

什么是 Passport.js 策略？

#### --distractors--

将应用路由映射到认证规则的配置文件。

---

一个内置函数，用于安全地哈希和存储用户密码。

---

用户成功完成登录流程后生成的令牌。

#### --answer--

定义如何使用特定方法或提供者认证用户的插件。

### --question--

#### --text--

Helmet.js 默认对 X-Powered-By 头部做什么？

#### --distractors--

用通用占位符值替换该头部。

---

在发送给客户之前加密头部值。

---

将头部信息移入 HTTP-only cookie。

#### --answer--

完全移除 X-Powered-By 头部。

### --question--

#### --text--

将 cookie 的 `SameSite` 属性设置为 `Strict` 会怎样？

#### --distractors--

在存储到浏览器之前加密 cookie。

---

确保 cookie 仅通过 HTTPS 连接发送。

---

阻止服务器在每个响应中设置多个 cookie。

#### --answer--

阻止浏览器在任何跨站请求中发送该 cookie。

### --question--

#### --text--

在 JWT 中，用户的声明数据（如用户 ID 或角色）存储在哪里？

#### --distractors--

在签名部分。

---

在头部部分。

---

在令牌外的加密容器中。

#### --answer--

在载荷部分。

### --question--

#### --text--

Helmet 设置的哪个 HTTP 头部防止你的网站被嵌入到另一个域的 iframe 中？

#### --distractors--

内容安全策略（Content-Security-Policy）

---

X-Powered-By

---

严格传输安全（Strict-Transport-Security）

#### --answer--

X-Frame-Options

### --question--

#### --text--

CSRF 攻击如何利用浏览器？

#### --distractors--

它向受害者的浏览器注入 JavaScript 以直接窃取 cookie。

---

它拦截受害者浏览器和可信服务器之间的流量。

---

它发送钓鱼链接，将受害者重定向到假登录页面。

#### --answer--

它欺骗浏览器自动发送包含受害者会话 cookie 的请求。

### --question--

#### --text--

在 Express 应用中，`app.use(passport.initialize())` 做什么？

#### --distractors--

它向应用注册一个新的认证策略。

---

它为每个访问应用的用户创建会话 cookie。

---

它自动安装所有可用的 Passport 策略。

#### --answer--

它将 Passport 设置为中间件，处理每个传入请求的认证。

### --question--

#### --text--

基于角色的授权系统中，哪个角色通常拥有系统设置和用户管理的完全访问权限？

#### --distractors--

编辑者

---

版主

---

普通用户

#### --answer--

管理员

### --question--

#### --text--

哪个命令安装 Passport.js 核心包和本地策略？

#### --distractors--

`npm install passport.js passport-local`

---

`npm install passport passport-auth`

---

`npm install passportjs local-strategy`

#### --answer--

`npm install passport passport-local`

### --question--

#### --text--

为什么绝不应该在 JWT 载荷中存储密码？

#### --distractors--

载荷有大小限制，无法存储密码长度的字符串。

---

在载荷中存储密码会自动使令牌签名无效。

---

JWT 令牌过快过期，存储密码没用。

#### --answer--

载荷是 Base64 编码的，不是加密的，任何持有令牌的人都能解码。

### --question--

#### --text--

CSRF 令牌的目的是什么？

#### --distractors--

在用户会话 cookie 存储到浏览器之前加密它。

---

验证用户在每个发送到服务器的 API 请求中的身份。

---

用短期认证凭证替换会话 cookie。

#### --answer--

确认请求来自合法前端，恶意网站无法读取。

### --question--

#### --text--

Helmet 设置哪个 HTTP 响应头来防御跨站脚本攻击（XSS）？

#### --distractors--

X-Frame-Options

---

X-Powered-By

---

严格传输安全性

#### --answer--

内容安全策略（Content-Security-Policy）

### --question--

#### --text--

当用户尝试访问受保护资源时，哪个过程应先发生？

#### --distractors--

授权

---

角色分配

---

会话创建

#### --answer--

认证

### --question--

#### --text--

用户成功认证后，Passport.js 会附加什么到请求对象？

#### --distractors--

活动会话令牌，作为 `req.sessionToken`。

---

用于登录的策略名称，作为 `req.strategy`。

---

用户的哈希密码，作为 `req.credentials`。

#### --answer--

经过认证的用户对象，作为 `req.user`。

### --question--

#### --text--

服务器收到载荷被篡改的 JWT 会发生什么？

#### --distractors--

服务器解码载荷并用新数据更新用户记录。

---

服务器默默忽略篡改数据，使用原始令牌声明。

---

服务器删除令牌并将用户送回注册页面。

#### --answer--

签名不匹配，服务器拒绝请求。

### --question--

#### --text--

与 `SameSite=Strict` 相比，`SameSite=Lax` 允许什么？

#### --distractors--

允许所有跨站请求的 cookie，包括表单提交。

---

阻止所有来自同一域外的请求的 cookie。

---

要求用户在任何跨站导航时重新登录。

#### --answer--

允许顶层导航时发送 cookie，但仍阻止跨站表单提交和 API 请求。

### --question--

#### --text--

Helmet 设置的 Strict-Transport-Security 头部强制执行什么？

#### --distractors--

迫使服务器拒绝所有非 HTTPS 请求并返回错误响应。

---

在发送给浏览器之前加密整个 HTTP 响应体。

---

要求所有 cookie 设置 Secure 和 HttpOnly 标记。

#### --answer--

迫使浏览器仅通过 HTTPS 与服务器通信。
