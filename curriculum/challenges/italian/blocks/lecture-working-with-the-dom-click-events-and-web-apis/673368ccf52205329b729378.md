---
id: 673368ccf52205329b729378
title: "Qual è la differenza tra innerText, textContent e innerHTML?"
challengeType: 19
dashedName: what-is-the-difference-between-innertext-textcontent-and-innerhtml
---

# --interactive--

Impariamo a conoscere `innerText`, `textContent` e `innerHTML`.

Queste sono proprietà che puoi consultare in JavaScript per ottenere o modificare il contenuto di un elemento HTML. Anche se all’inizio possono sembrare molto simili, hanno differenze chiave. Scegliere quella giusta dipende dal tuo caso d’uso specifico, quindi entriamo nel dettaglio.

Iniziamo con `innerText`.

`innerText` rappresenta il contenuto testuale visibile dell’elemento HTML e dei suoi discendenti. Questa proprietà non include il testo nascosto o i tag HTML, solo il testo renderizzato.

Ad esempio, qui puoi vedere un elemento `div` che contiene due paragrafi:

```html
<div id="container">
  <p>Hello, World!</p>
  <p>I'm learning JavaScript</p>
</div>
```

Se otteniamo un riferimento a questo elemento HTML nel nostro codice JavaScript usando `getElementById()`, possiamo consultare la proprietà `innerText` di questo elemento:

:::interactive_editor

```html
<div id="container">
  <p>Hello, World!</p>
  <p>I'm learning JavaScript</p>
</div>
<script src="index.js"></script>
```

```js
const container = document.getElementById("container");
console.log(container.innerText);
```

:::

Questo è il testo interno di questo elemento:

```md
Hello, World!
I'm learning JavaScript
```

La proprietà restituisce una stringa con il testo contenuto all’interno dell’elemento, incluso il testo dei suoi discendenti.

Devi sapere che `innerText` restituisce solo il testo che è visibile nel momento in cui la stringa viene richiesta. Se un elemento figlio è nascosto, il suo testo non sarà visibile.

Ecco un esempio in cui il secondo paragrafo è nascosto:

:::interactive_editor

```html
<div id="container">
  <p>Hello, World!</p>
  <p hidden>I'm learning JavaScript</p>
</div>
```

:::

Se proviamo a registrare di nuovo `innerText`, ora l’output non conterrà il testo del secondo paragrafo:

:::interactive_editor

```html
<div id="container">
  <p>Hello, World!</p>
  <p hidden>I'm learning JavaScript</p>
</div>
<script src="index.js"></script>
```

```js
const container = document.getElementById("container");
console.log(container.innerText);
```

:::

Puoi impostare la proprietà `innerText` di un elemento HTML in questo modo, ma questo sostituirà il testo esistente e aggiungerà un elemento di interruzione di riga (`br`) per ogni interruzione di riga:

:::interactive_editor

```html
<div id="container">
  <p>Hello, World!</p>
  <p>I'm learning JavaScript</p>
</div>
<script src="index.js"></script>
```

```js
const container = document.getElementById("container");
container.innerText = "JavaScript is awesome!";
```

:::

Poiché `innerText` tiene conto della visibilità, ottenere il suo valore attiva un processo chiamato "reflow", che ricalcola la posizione di certi elementi sul sito web. Questo processo può essere intensivo dal punto di vista computazionale, quindi dovresti evitarlo se possibile.

Ottimo. Ora parliamo di `textContent`.

`textContent` restituisce il contenuto testuale semplice di un elemento, incluso tutto il testo all’interno dei suoi discendenti.

La differenza più importante tra `innerText` e `textContent` è che `textContent` restituisce sempre il contenuto testuale completo di un elemento HTML e dei suoi discendenti, indipendentemente dal fatto che sia visibile o nascosto.

Qui abbiamo lo stesso esempio in HTML:

```html
<div id="container">
  <p>Hello, World!</p>
  <p>I'm learning JavaScript</p>
</div>
```

Se provi a consultare questa proprietà, vedrai il testo dell’elemento e dei suoi discendenti come output, mantenendo l’indentazione e gli spazi:

:::interactive_editor

```html
<div id="container">
  <p>Hello, World!</p>
  <p>I'm learning JavaScript</p>
</div>
<script src="index.js"></script>
```

```js
const container = document.getElementById("container");
console.log(container.textContent);
```

:::

Se un elemento HTML non è visibile, come puoi vedere qui, dove abbiamo nascosto il secondo paragrafo, il suo testo sarà comunque incluso in questa proprietà:

:::interactive_editor

```html
<div id="container">
  <p>Hello, World!</p>
  <p hidden>I'm learning JavaScript</p>
</div>
<script src="index.js"></script>
```

```js
const container = document.getElementById("container");
console.log(container.textContent);
```

:::
  
`textContent` includerà anche il contenuto di elementi come `script` e `style`.

Se provi a sostituire il valore di `textContent` su un nodo, rimuoverà tutti i suoi nodi figli e li sostituirà con un singolo nodo di testo contenente la nuova stringa:

:::interactive_editor

```html
<div id="container">
  <p>Hello, World!</p>
  <p hidden>I'm learning JavaScript</p>
</div>
<script src="index.js"></script>
```

```js
const container = document.getElementById("container");
container.textContent = "New content";
```

:::

E infine, parliamo di come `textContent` e `innerText` differiscono da `innerHTML`.

Ricorda che con `innerHTML` puoi impostare il contenuto HTML interno di un elemento. Questo è utile per inserire dinamicamente nuovo HTML nel DOM.

Tuttavia, ricorda che questo comporta un rischio per la sicurezza se non hai il controllo sulla stringa, come stringhe contenenti dati inseriti dall’utente. Se quei dati sono dannosi, possono causare seri problemi di sicurezza.

Per evitare questo, si consiglia di usare la proprietà `textContent` per inserire testo semplice invece.

Le proprietà `innerText`, `textContent` e `innerHTML` in JavaScript offrono modi diversi per consultare e manipolare il contenuto degli elementi HTML. Dovresti capire le differenze tra queste proprietà se vuoi lavorare efficacemente con contenuti HTML in JavaScript.

# --questions--

## --text--

Quale delle seguenti proprietà restituisce il contenuto testuale visibile di un elemento HTML, escludendo qualsiasi testo nascosto o testo all’interno di elementi nascosti?

## --answers--

`innerHTML`

### --feedback--

Pensa a come queste proprietà gestiscono il contenuto nascosto.

---

`textContent`

### --feedback--

Pensa a come queste proprietà gestiscono il contenuto nascosto.

---

`innerText`

---

Tutte le precedenti.

### --feedback--

Pensa a come queste proprietà gestiscono il contenuto nascosto.

## --video-solution--

3

## --text--

Quale delle seguenti proprietà può essere usata per inserire dinamicamente nuovo contenuto HTML in un elemento?

## --answers--

`innerHTML`

---

`textContent`

### --feedback--

Pensa a quale proprietà ti permette di modificare la struttura HTML di un elemento.

---

`innerText`

### --feedback--

Pensa a quale proprietà ti permette di modificare la struttura HTML di un elemento.

---

Nessuna delle precedenti.

### --feedback--

Pensa a quale proprietà ti permette di modificare la struttura HTML di un elemento.

## --video-solution--

1

## --text--

Quale delle seguenti proprietà restituisce il contenuto testuale semplice di un elemento, incluso tutto il testo all’interno degli elementi figli, indipendentemente dal fatto che sia visibile o nascosto?

## --answers--

`innerHTML`

### --feedback--

Pensa a come queste proprietà gestiscono il contenuto nascosto.

---

`textContent`

---

`innerText`

### --feedback--

Pensa a come queste proprietà gestiscono il contenuto nascosto.

---

Tutte le precedenti.

### --feedback--

Pensa a come queste proprietà gestiscono il contenuto nascosto.

## --video-solution--

2
