---
id: 67fe8599c83979345ff9a91a
title: Como a Função Print Funciona?
challengeType: 19
dashedName: how-does-the-print-function-work
---

# --description--

Toda linguagem de programação tem alguma forma de exibir dados no terminal com um método, função, propriedade ou palavra-chave embutida. Em Python, você pode usar a função `print` para imprimir dados no terminal. Vamos dar uma olhada mais de perto na função `print` para que você possa começar a usá-la com confiança.

Uma das primeiras coisas que você faz quando está aprendendo qualquer linguagem de programação é escrever um programa simples `Hello world!`. Você pode fazer isso muito facilmente em Python com apenas a função `print`.

Para fazer isso, você só precisa colocar a string `Hello world!` entre os parênteses de abertura e fechamento que você usa para chamar a função `print`:

```python
print('Hello world!') # Hello world!
```

Você aprenderá mais sobre strings e funções em Python em lições futuras. Por enquanto, apenas considere strings como uma sequência de caracteres cercada por aspas simples (`'`) ou duplas (`"`).

No exemplo `print('Hello world!')`, a string `'Hello world!'` é um **argumento** passado para a função `print`. Você também pode usar a função `print` para mostrar múltiplos valores ou argumentos ao mesmo tempo separando-os com vírgulas. Por exemplo:

```python
print('My favorite colors are', 'blue', 'green', 'red')

# Output: My favorite colors are blue green red
```

Python adiciona automaticamente um espaço entre cada item quando você os separa com vírgulas. Isso é útil quando você quer imprimir várias informações juntas.

# --questions--

## --text--

Por que o seguinte código imprime todos os valores na mesma linha com espaços?

```python
print('My favorite colors are', 'blue', 'green', 'red')
```

## --answers--

Porque separar valores com vírgulas em `print()` adiciona espaços entre eles.

---

Porque as strings são automaticamente unidas sem separador.

### --feedback--

Revise como o Python formata a saída quando múltiplos valores são passados para `print()`.

---

Porque cada chamada para `print()` adiciona um espaço por padrão.

### --feedback--

Revise como o Python formata a saída quando múltiplos valores são passados para `print()`.

---

Porque o Python converte automaticamente todas as strings para maiúsculas na saída.

### --feedback--

Revise como o Python formata a saída quando múltiplos valores são passados para `print()`.

## --video-solution--

1

## --text--

Qual seria a saída do seguinte código?

```python
print('Hello', 'world!')
```

## --answers--

`Hello world!`

---

`Helloworld!`

### --feedback--

Pense em como múltiplos argumentos são tratados na função `print()`.

---

`Hello, world!`

### --feedback--

Pense em como múltiplos argumentos são tratados na função `print()`.

---

`Hello\nworld!`

### --feedback--

Pense em como múltiplos argumentos são tratados na função `print()`.

## --video-solution--

1

## --text--

Qual é o propósito das aspas ao redor de `Hello world!` no código a seguir?

```python
print('Hello world!')
```

## --answers--

Eles definem uma string a ser impressa pela função `print()`.

---

Eles são usados para chamar uma função chamada `Hello world!`.

### --feedback--

Considere como o Python identifica valores de string no código.

---

Eles são necessários apenas se a string contiver espaços.

### --feedback--

Considere como o Python identifica valores de string no código.

---

Eles informam ao Python para imprimir uma variável chamada `Hello world!`.

### --feedback--

Considere como o Python identifica valores de string no código.

## --video-solution--

1
