---
id: 5900f52c1000cf542c51003d
title: '问题446：撤回B.'
challengeType: 1
forumTopicId: 302118
dashedName: problem-446-retractions-b
---

# --description--

对于每个整数 $n > 1$，函数族 $f_{n, a, b}$ 被定义为：

$f_{n, a, b}(x) ≡ ax + b\bmod n$，其中 $a, b, x$ 为整数且 $0 \lt a \lt n$，$0 \le b \lt n$，$0 \le x \lt n$。

如果对于每个 $0 \le x \lt n$，都有 $f_{n, a, b}(f_{n, a, b}(x)) \equiv f_{n, a, b}(x)\bmod n$，我们将称 $f_{n, a, b}$ 为一个收缩映射。

令 $R(n)$ 为 $n$ 的收缩数。

$F(N) = \displaystyle\sum_{n = 1}^N R(n^4 + 4)$。

$F(1024) = 77\\,532\\,377\\,300\\,600$。

查找 $F({10}^7)$。给出你的答案对 $1\,000\,000\,007$ 取模。

# --hints--

`retractionsB()` 应该返回 `907803852`。

```js
assert.strictEqual(retractionsB(), 907803852);
```

# --seed--

## --seed-contents--

```js
function retractionsB() {

  return true;
}

retractionsB();
```

# --solutions--

```js
// solution required
```
