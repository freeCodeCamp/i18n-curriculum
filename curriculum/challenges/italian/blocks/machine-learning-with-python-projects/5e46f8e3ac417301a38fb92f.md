---
id: 5e46f8e3ac417301a38fb92f
title: Motore di raccomandazione libri usando KNN
challengeType: 10
forumTopicId: 462378
dashedName: book-recommendation-engine-using-knn
---

# --description--

Lavorerai su questo progetto con <a href="https://colab.research.google.com/github/freeCodeCamp/boilerplate-book-recommendation-engine/blob/master/fcc_book_recommendation_knn.ipynb" target="_blank" rel="noopener noreferrer nofollow">Google Colaboratory</a>.

Dopo aver aperto quel collegamento, crea una copia del notebook nel tuo account o localmente. Una volta completato il progetto e superati i test (inclusi in quel collegamento), invia il link del progetto qui sotto. Se invii un link di Google Colaboratory, assicurati di attivare la condivisione del collegamento per "chiunque abbia il collegamento".

Stiamo ancora sviluppando i contenuti didattici interattivi per il curriculum di machine learning. Per ora, puoi seguire le sfide video di questa certificazione. Potresti anche dover cercare risorse di apprendimento aggiuntive, come faresti lavorando su un progetto reale.

# --instructions--

In questa sfida, creerai un algoritmo di raccomandazione di libri usando **K-Nearest Neighbors**.

In questo progetto userai il dataset Book-Crossings, che contiene 1,1 milioni di valutazioni (su scala da 1 a 10) di 270.000 libri da parte di 90.000 utenti. Il dataset è già importato nel notebook, quindi non è necessario scaricarlo.

Usa `NearestNeighbors` da `sklearn.neighbors` per sviluppare un modello che mostri libri simili a un libro dato. L’algoritmo Nearest Neighbors misura la distanza per determinare la “vicinanza” tra le istanze.

Crea una funzione chiamata `get_recommends` che prende come argomento il titolo di un libro (dal dataset) e restituisce una lista di 5 libri simili con le loro distanze rispetto al libro passato come argomento.

Questo codice:

```py
get_recommends("The Queen of the Damned (Vampire Chronicles (Paperback))")
```

dovrebbe restituire:

```py
[
  'The Queen of the Damned (Vampire Chronicles (Paperback))',
  [
    ['Catch 22', 0.793983519077301], 
    ['The Witching Hour (Lives of the Mayfair Witches)', 0.7448656558990479], 
    ['Interview with the Vampire', 0.7345068454742432],
    ['The Tale of the Body Thief (Vampire Chronicles (Paperback))', 0.5376338362693787],
    ['The Vampire Lestat (Vampire Chronicles, Book II)', 0.5178412199020386]
  ]
]
```

Nota che i dati restituiti da `get_recommends()` sono una lista. Il primo elemento della lista è il titolo del libro passato alla funzione. Il secondo elemento della lista è una lista di altre cinque liste. Ognuna delle cinque liste contiene un libro consigliato e la distanza tra il libro consigliato e il libro passato alla funzione.

Se rappresenti graficamente il dataset (opzionale), noterai che la maggior parte dei libri non viene valutata frequentemente. Per garantire significatività statistica, rimuovi dal dataset gli utenti con meno di 200 valutazioni e i libri con meno di 100 valutazioni.

Le prime tre celle importano le librerie che potresti usare e i dati da utilizzare. L’ultima cella serve per i test. Scrivi tutto il tuo codice tra queste celle.

# --hints--

Dovrebbe superare tutti i test Python.

```js

```

# --solutions--

```py
  # Python challenges don't need solutions,
  # because they would need to be tested against a full working project.
  # Please check our contributing guidelines to learn more.
```
