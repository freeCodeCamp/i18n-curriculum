---
id: 6900a59dedf6c5c52824304f
title: NodeJS 入門測驗
challengeType: 8
dashedName: quiz-node-js-intro
---

# --description--

要通過測驗，你必須正確回答以下 10 題中的至少 9 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

為什麼客戶端的 JavaScript 對區域的檔案系統有受限的存取？

#### --distractors--

因為 JavaScript 是一種執行速度較慢的程式設計語言。

---

因為瀏覽器沒有足夠的記憶體來控制代碼檔案。

---

因為 DOM API 防止檔案存取。

#### --answer--

因為它是設計用於在網頁瀏覽器中執行，而不受限制的檔案存取會帶來安全風險。

### --question--

#### --text--

Node.js 是「跨平台」意味著什麼？

#### --distractors--

它可以同時執行多種程式設計語言。

---

它可以將不同的資料庫連接在一起。

---

每個作業系統都需要不同的安裝方法。

#### --answer--

它適用於任何作業系統，包括 Windows、macOS 和 Linux。

### --question--

#### --text--

哪個 API 可以在瀏覽器環境中存取，但不能在 Node.js 環境中存取？

#### --distractors--

檔案系統 API

---

網路 API

---

作業系統 API

#### --answer--

DOM API

### --question--

#### --text--

在 Node.js 的背景關係中，什麼是 `npm`？

#### --distractors--

一個用於建置使用者介面的 JavaScript 框架。

---

為 Node.js 應用程式最佳化的網頁瀏覽器。

---

Node.js 應用程式的偵錯工具。

#### --answer--

一個用於 Node.js 的軟體包管理工具，允許你安裝和管理軟體包與模組。

### --question--

#### --text--

當執行 `nvm install --lts` 時，`--lts` 旗標顯示什麼？

#### --distractors--

它會安裝最新的測試螢幕快照版本。

---

它安裝了可用的最輕量且最小版本。

---

它安裝具有最多特性的版本。

#### --answer--

它會安裝優先考量穩定性、可靠性和安全性的長期支援版本。

### --question--

#### --text--

在 Node.js 的非同步程式設計背景關係中，什麼是「回呼」？

#### --distractors--

重新啟動失敗操作的函式。

---

用來儲存同步操作結果的變數。

---

一個監控應用程式效率、性能的工具。

#### --answer--

定義非同步操作完成後發生什麼的函式（程式）。

### --question--

#### --text--

由 `npm init` 產生的 `package.json` 檔案的用途是什麼？

#### --distractors--

用於儲存應用程式的環境變數。

---

組態網頁伺服器設定。

---

定義應用程式的 HTML 結構。

#### --answer--

追蹤專案的詳細資訊和相依性。

### --question--

#### --text--

為什麼 CPU 密集型任務在 Node.js 中可能會有問題？

#### --distractors--

Node.js 無法執行數學操作。

---

Node.js 不支援加密學或影像處理。

---

事件迴圈防止 CPU 操作行為被執行。

#### --answer--

它們可能會阻塞主執行緒，因為 Node.js 在單一執行緒上執行，導致效率問題。

### --question--

#### --text--

你會使用什麼指令來使用 Node.js 執行名為 `server.js` 的 JavaScript 檔案？

#### --distractors--

`npm run server.js`

---

`nvm execute server.js`

---

`javascript server.js`

#### --answer--

`node server.js`

### --question--

#### --text--

開發者在從 npm 選擇軟體包前應該檢查什麼？

#### --distractors--

軟體包是否至少有 1000 次下載。

---

軟體包是否最近被創建。

---

該軟體包是否需要付費。

#### --answer--

該軟體包是否被維護並遵循品質及安全最佳實踐。
