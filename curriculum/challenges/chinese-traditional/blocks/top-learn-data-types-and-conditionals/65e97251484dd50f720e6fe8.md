---
id: 65e97251484dd50f720e6fe8
title: 學習資料型別和條件語句 第 C 課
challengeType: 15
dashedName: learn-data-types-and-conditionals-lesson-c
---

# --description--

看到以下範例，你想確定字串中有 `loves` 這個字。

```javascript
let sentence = "freeCodeCamp loves The Odin Project!";
```

<a href="https://www.freecodecamp.org/news/javascript-string-handbook" target="_blank"> 聯結至指南 </a>

# --questions--

## --text--

應該使用哪一種方法來檢查字串中是否存在字 `loves`？

## --answers--

應該使用 `includes()` 方法來檢查字串中是否存在字 `loves`。

---

應該使用 `indexOf()` 方法來檢查字串中是否存在字 `loves`。

---

應該使用 `search()` 方法來檢查字串中是否存在字 `loves`。

---

應該使用 `toUpperCase()` 方法來檢查字串中是否存在字 `loves`。

## --video-solution--

1
