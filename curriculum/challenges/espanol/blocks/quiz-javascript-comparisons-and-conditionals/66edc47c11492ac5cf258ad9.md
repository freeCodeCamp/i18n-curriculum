---
id: 66edc47c11492ac5cf258ad9
title: Cuestionario sobre Comparaciones y Condicionales en JavaScript
challengeType: 8
dashedName: quiz-javascript-comparisons-and-conditionals
---

# --description--

Para pasar el cuestionario, debe responder correctamente al menos 9 de las 10 preguntas a continuación.

# --quizzes--

## --quiz--

### --question--

#### --text--

¿Cuál es el resultado de la expresión `undefined > 0`?

#### --distractors--

`true`

---

`undefined`

---

`null`

#### --answer--

`false`

### --question--

#### --text--

¿Qué operación lógica representa `||`?

#### --distractors--

XOR

---

Y

---

NO

#### --answer--

O

### --question--

#### --text--

¿Cuál es la salida del siguiente código?

```js
console.log(5 === 2 + 3 || 4 == 2);
```

#### --distractors--

`undefined`

---

`false`

---

Se genera un error.

#### --answer--

`true`

### --question--

#### --text--

¿Qué es un valor truthy y falsy?

#### --distractors--

Un valor que es tanto `true` como `false`.

---

Un valor que cambia dependiendo del contexto.

---

Un valor que funciona de manera diferente a uno `true` o `false`.

#### --answer--

Un valor que se considera `true` o `false` cuando se encuentra en un contexto booleano.

### --question--

#### --text--

¿Qué se registra en la consola desde el código a continuación?

```js
if (1) {
  console.log("True!");
}
```

#### --distractors--

`false`

---

Nada se imprime en la consola.

---

Se genera un error.

#### --answer--

`True!`

### --question--

#### --text--

¿Cuál es la diferencia entre `undefined` y `null`?

#### --distractors--

`null` y `undefined` apuntan a una ubicación de memoria fuera de rango y son inaccesibles.

---

`null` es el valor implícito asignado a variables y no puede cambiarse, mientras que `undefined` es el valor explícito asignado a variables.

---

`null` es una propiedad global, `undefined` no lo es.

#### --answer--

Las variables sin un valor son `undefined`, mientras que `null` representa una ausencia intencional de un valor de objeto.

### --question--

#### --text--

¿Qué sucede cuando no se incluye `break` al implementar una instrucción `switch`?

#### --distractors--

La instrucción `switch` se detendrá abruptamente.

---

La instrucción `switch` lanzará un error después de la sentencia anterior.

---

La instrucción `switch` saldrá después de la primera coincidencia.

#### --answer--

El código continúa evaluando las siguientes declaraciones `case`, incluso después de encontrar una coincidencia.

### --question--

#### --text--

¿Qué se imprime en la consola desde el código a continuación?

```js
const a = 2;
if (1 == "1") {
  let b = 3;
  console.log(a + b);
}
console.log(b);
```

#### --distractors--

Se genera un error.

---

```js
5
3
```

---

```js
5
undefined
```

#### --answer--

`5`, y luego se genera un error.

### --question--

#### --text--

¿Cuál será la salida del siguiente código JavaScript?

```js
let vehicle = "car";

switch (vehicle) {
  case "bike":
    console.log("Bikes are two-wheelers.");
    break;
  case "car":
    console.log("Some cars are 4x4.");
  case "truck":
    console.log("Trucks can carry heavy loads.");
    break;
  default:
    console.log("Unknown vehicle.");
}
```

#### --distractors--

```md
Some cars are 4x4.
```

---

```md
Some cars are 4x4.
Trucks can carry heavy loads.
Unknown vehicle.
```

---

```md
Unknown vehicle.
```

#### --answer--

```md
Some cars are 4x4.
Trucks can carry heavy loads.
```

### --question--

#### --text--

¿Qué se imprime en la consola con el código siguiente?

```js
let x = 5;
if (x > 1 && x < 10) {
  console.log("x is between 1 and 10");
} else {
  console.log("x is not between 1 and 10");
}
```

#### --distractors--

Se genera un error.

---

No se imprime nada.

---

`x is not between 1 and 10`

#### --answer--

`x is between 1 and 10`

