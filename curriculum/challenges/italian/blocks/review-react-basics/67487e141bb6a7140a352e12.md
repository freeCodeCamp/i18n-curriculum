---
id: 67487e141bb6a7140a352e12
title: Revisione delle basi di React
challengeType: 31
dashedName: review-react-basics
---

# --description--

## Librerie e framework JavaScript

- Le librerie e i framework JavaScript offrono soluzioni rapide a problemi comuni e accelerano lo sviluppo fornendo codice predefinito.
- Le librerie sono generalmente più focalizzate nel fornire soluzioni a compiti specifici, come manipolare il DOM, gestire eventi o gestire richieste AJAX.
- Un paio di esempi di librerie JavaScript sono jQuery e React.
- I framework, invece, offrono una struttura più definita per costruire applicazioni. Spesso includono un insieme di regole e convenzioni che gli sviluppatori devono seguire.
- Esempi di framework includono Angular e Next.js, un meta framework per React.
- Le **single page application** (SPA) sono applicazioni web che caricano una singola pagina HTML e aggiornano dinamicamente quella pagina mentre l’utente interagisce con l’applicazione senza ricaricare l’intera pagina.
- Le SPA usano JavaScript per gestire lo stato dell’applicazione e renderizzare il contenuto. Questo viene spesso fatto usando framework che offrono ottimi strumenti per costruire interfacce utente complesse.
- Alcuni problemi legati alle SPA includono:

  - I lettori di schermo hanno difficoltà con contenuti aggiornati dinamicamente.
  - L’URL non cambia quando l’utente naviga all’interno dell’applicazione, il che può rendere difficile aggiungere ai preferiti, tornare indietro o condividere pagine specifiche.
  - Il tempo di caricamento iniziale può essere lento se l’applicazione è grande, poiché tutte le risorse devono essere caricate subito.

## React

- React è una popolare libreria JavaScript per costruire interfacce utente e applicazioni web.
- Un concetto fondamentale di React è la creazione di componenti UI riutilizzabili che possono aggiornarsi e renderizzarsi indipendentemente quando i dati cambiano.
- React permette agli sviluppatori di descrivere come dovrebbe apparire l’interfaccia utente in base allo stato dell’applicazione. React poi aggiorna e renderizza i componenti giusti quando i dati o lo stato cambiano.

## Componenti React

- I componenti sono i mattoni delle applicazioni React che permettono agli sviluppatori di suddividere interfacce utente complesse in pezzi più piccoli e gestibili.
- L’interfaccia utente viene descritta usando JSX, un’estensione della sintassi JavaScript che permette agli sviluppatori di scrivere codice simile all’HTML all’interno di JavaScript.
- I componenti sono fondamentalmente funzioni JS o classi che restituiscono un pezzo di interfaccia utente.

Ecco un esempio di un semplice componente React che renderizza un messaggio di saluto:

```jsx
function Greeting() {
  const name = 'Anna';
  return <h1>Welcome, {name}!</h1>;
}
```

Per usare il componente, puoi semplicemente chiamare:
    
```jsx
  <Greeting />
```

## Importare ed esportare componenti React

- I componenti React possono essere esportati da un file e importati in un altro file.
- Supponiamo che tu abbia un componente chiamato `City` in un file chiamato `City.js`. Puoi esportare il componente usando la parola chiave `export`:

```jsx
// City.js
function City() {
  return <p>New York</p>;
}

export default City;
```

- Per importare il componente `City` in un altro file, puoi usare la parola chiave `import`:

```jsx
// App.js
import City from './City';

function App() {
  return (
    <div>
      <h1>My favorite city is:</h1>
      <City />
    </div>
  );
}
```

- La parola chiave `default` viene usata perché è l’esportazione predefinita dal file `City.js`.

- Puoi anche scegliere di esportare il componente sulla stessa riga della definizione del componente in questo modo:

```jsx
export default function City() {
  return <p>New York</p>;
}
```

## Configurare un progetto React usando Vite

- Gli strumenti di configurazione del progetto e le interfacce a riga di comando offrono un modo rapido e semplice per iniziare nuovi progetti, permettendo agli sviluppatori di concentrarsi sulla scrittura del codice invece che sulla configurazione.
- Vite è uno strumento di configurazione popolare e può essere usato con React.
- Per creare un nuovo progetto con Vite, puoi usare il seguente comando nel terminale:

```bash
npm create vite@latest my-react-app -- --template react
```

Questo comando crea un nuovo progetto React chiamato `my-react-app` usando il template React di Vite. Nella directory del progetto vedrai un file `package.json` con elencate le dipendenze e i comandi del progetto.

- Per eseguire il progetto, naviga nella directory del progetto ed esegui i seguenti comandi:

```bash
cd my-react-app # path to the project directory
npm install # installs the dependencies listed in the package.json file
```

- Una volta installate le dipendenze, noterai una nuova cartella nel progetto chiamata `node_modules`.

- La cartella `node_modules` è dove sono archiviate tutte le librerie e i pacchetti richiesti dal progetto.

- Per eseguire il progetto, usa il seguente comando:

```bash
npm run dev
```

- Dopo di che, apri il browser e vai su `http://localhost:5173` per vedere la tua applicazione React in esecuzione.

- Per vedere effettivamente il codice del template iniziale, puoi entrare nel progetto dentro la cartella `src` e dovresti vedere il file `App.jsx`.

## Passare props nei componenti React

- In React, le props (abbreviazione di proprietà) sono un modo per passare dati da un componente genitore a un componente figlio. Questo meccanismo è necessario per creare elementi UI riutilizzabili e dinamici.
- Le props possono essere qualsiasi valore JavaScript. Per passare props da un genitore a un figlio, aggiungi le props come attributi quando usi il componente figlio nel JSX del genitore. Ecco un esempio semplice:

```jsx
// Parent component
function Parent() {
  const name = 'Anna';
  return <Child name={name} />;
}

// Child component
function Child(props) {
  return <h1>Hello, {props.name}!</h1>;
}
```

Puoi passare più props usando l’operatore di espansione `(...)`, dopo averle convertite in un oggetto. Ecco un esempio:
    
```jsx
// Parent component
function Parent() {
  const person = {
    name: 'Anna',
    age: 25,
    city: 'New York'
  };
  return <Child {...person} />;
}
```

In questo codice, l’operatore di espansione `{...person}` converte l’oggetto person in singole props che vengono passate al componente Child.

## Renderizzazione condizionale in React

- La renderizzazione condizionale in React ti permette di creare interfacce utente dinamiche. Serve a mostrare contenuti diversi in base a certe condizioni o stati all’interno dell’applicazione.
- Ci sono diversi modi per renderizzare contenuti condizionalmente in React. Un approccio comune è usare l’operatore ternario. Ecco un esempio:

```jsx
function Greeting({ isLoggedIn }) {
  return (
    <div>
      {isLoggedIn ? <h1>Welcome back!</h1> : <h1>Please log in</h1>}
    </div>
  );
}
```

- Un altro modo per renderizzare contenuti condizionalmente è usare l’operatore logico AND (`&&`). Questo è utile quando vuoi renderizzare contenuti solo se una certa condizione è vera. Ecco un esempio:

```jsx
function Greeting({ user }) {
  return (
    <div>
      {user && <h1>Welcome, {user.name}!</h1>}
    </div>
  );
}
```

Nel codice sopra, l’elemento `h1` viene renderizzato solo se l’oggetto user è truthy.

Puoi anche usare direttamente un’istruzione `if` in questo modo:

```jsx
function Greeting({ isLoggedIn }) {
  if (isLoggedIn) {
    return <h1>Welcome back!</h1>;
  }
  return <h1>Please sign in</h1>;
}
```

## Renderizzare liste in React

- Renderizzare liste in React è un’attività comune quando si costruiscono interfacce utente.
- Le liste possono essere renderizzate usando il metodo `map()` dell’array JS per iterare su un array di elementi e restituire un nuovo array di elementi JSX.
- Per esempio, se hai un array di nomi che vuoi renderizzare come lista, puoi fare così:

```jsx
function NameList({ names }) {
  return (
    <ul>
      {names.map((name, index) => (
        <li key={`${name}-${index}`}>{name}</li>
      ))}
    </ul>
  );
}
```

- Ricorda sempre di fornire una chiave unica per ogni elemento della lista per aiutare React a gestire l’aggiornamento e la renderizzazione. Con queste tecniche puoi creare liste flessibili, efficienti e dinamiche nelle tue applicazioni React.

## Stili in linea in React

- Gli stili in linea in React ti permettono di applicare stili CSS direttamente agli elementi JSX usando oggetti JavaScript.
- Per applicare stili in linea in React, puoi usare l’attributo style sugli elementi JSX. L’attributo style prende un oggetto dove le chiavi sono proprietà CSS in camelCase e i valori sono i valori corrispondenti. Ecco un esempio:

```jsx
function Greeting() {
  return (
    <h1
      style={{ color: 'blue', fontSize: '24px', backgroundColor: 'lightgray' }}
    >
      Hello, world!
    </h1>
  );
}

export default Greeting;
```

Puoi anche estrarre gli stili in un oggetto separato e farvi riferimento nell’attributo `style` in questo modo:

```jsx
function Greeting() {
  const styles = {
    color: 'blue',
    fontSize: '24px',
    backgroundColor: 'lightgray'
  };

  return <h1 style={styles}>Hello, world!</h1>;
}

export default Greeting;
```

- Gli stili in linea supportano la definizione dinamica dello stile permettendoti di applicare condizionatamente gli stili in base alle props o allo stato. Ecco un esempio di come puoi applicare condizionatamente gli stili basandoti su una prop:

```jsx
function Greeting({ isImportant }) {
  const styles = {
    color: isImportant ? 'red' : 'black',
    fontSize: isImportant ? '24px' : '16px'
  };

  return <h1 style={styles}>Hello, world!</h1>;
}

export default Greeting;
```

- Nel codice sopra, gli stili `color` e `fontSize` sono impostati condizionatamente in base alla prop `isImportant`.

# --assignment--

Rivedi gli argomenti e i concetti base di React.
