---
id: 68ff6d2f232c60f0fa68ef86
title: Was ist Node und was sind einige Unterschiede zwischen der Browser- und der Node-Laufzeitumgebung?
challengeType: 19
dashedName: what-is-node-and-what-are-some-differences-between-the-browser-and-node-runtime-environment
---

# --description--

Bisher haben Sie clientseitigen Code mit JavaScript geschrieben, also Code, der auf der Clientseite einer Webanwendung läuft, meist in einem Webbrowser.

Clientseitiges JavaScript kann sehr leistungsfähig sein, hat aber bestimmte Einschränkungen bei komplexeren Aufgaben.

Zum Beispiel:

- Es hat sehr eingeschränkten Zugriff auf lokale Dateien.
- Es ist nicht wirklich dafür ausgelegt, komplexe Anwendungslogik zu verarbeiten.
- Und es gibt potenzielle Sicherheitsbedenken. Zum Beispiel möchten Sie keine sensiblen Informationen wie Datenbank-Zugangsdaten im clientseitigen Code verfügbar machen.
    
Clientseitiges JavaScript hat diese Einschränkungen, weil es ursprünglich ausschließlich für die Ausführung in Webbrowsern entwickelt wurde.

Webbrowser stellen die Umgebung bereit, die benötigt wird, um JavaScript-Code auszuführen, einschließlich der JavaScript-Engine. Sie bieten auch Zugriff auf das Document Object Model (DOM), sodass Sie HTML-Elemente in Ihrem Code ansprechen können.

Früher konnten wir JavaScript nicht außerhalb eines Browsers ausführen, aber jetzt ist das mit Node.js möglich.

Node.js ist eine JavaScript-Laufzeitumgebung. Offiziell wird sie definiert als „eine Open-Source- und plattformübergreifende JavaScript-Laufzeitumgebung“.

Sehen wir uns an, was das bedeutet:

- Node.js ist Open Source, weil sein Code öffentlich verfügbar ist und von einer großen Community von Entwicklern gepflegt wird.
- Es ist plattformübergreifend, weil es auf jedem Betriebssystem funktioniert, einschließlich Windows, macOS und Linux.
- Und es ist eine JavaScript-Laufzeitumgebung, weil es Ihnen erlaubt, JavaScript-Code außerhalb eines Browsers auszuführen.
    
Node.js wird in vielen Branchen eingesetzt. Entwickler verwenden es, um Webserver und APIs zu erstellen, die HTTP-Anfragen verarbeiten, sowie Web- und mobile Anwendungen.

Wenn Sie eine Webseite oder Webanwendung verwenden oder entwickeln, die mit einer Datenbank interagiert oder komplexe Daten verarbeitet, ist die Wahrscheinlichkeit hoch, dass Sie irgendwann Node.js im Hintergrund verwenden.

Es ist effizient, skalierbar und hat eine große Community von Entwicklern und Betreuern weltweit.

Jetzt, wo Sie wissen, was Node.js ist, sehen wir uns einige Unterschiede zwischen der Browser- und der Node-Laufzeitumgebung an.

Die Browser-Umgebung ist hauptsächlich für die Frontend-Webentwicklung konzipiert, daher läuft dort clientseitiges JavaScript. Im Gegensatz dazu ist die Node-Laufzeitumgebung hauptsächlich für die Backend-Webentwicklung konzipiert, daher läuft dort serverseitiges JavaScript. Das bestimmt auch die angebotenen APIs.

Zum Beispiel können Sie vom Browser aus auf die DOM-API zugreifen, aber der Zugriff auf das lokale Dateisystem ist eingeschränkt. Im Gegensatz dazu können Sie von der Node-Laufzeitumgebung aus auf fast alle Systemressourcen zugreifen, einschließlich des Dateisystems, aber nicht auf das DOM.

Beide JavaScript-Umgebungen stellen ein globales Objekt bereit. In der Browser-JavaScript-Laufzeit heißt dieses `window`. Dieses `window`-Objekt bietet Zugriff auf browserbezogene Funktionen, wie Methoden zur Manipulation des DOM, Verwaltung von Cookies und Behandlung von Browser-Ereignissen. In der Node.js-Umgebung heißt dieses globale Objekt `global`. Das `global`-Objekt bietet Zugriff auf Node.js-spezifische Funktionen, wie eingebaute Module zur Arbeit mit lokalen Dateien, Netzwerken und zur Interaktion mit dem Betriebssystem.

Schließlich ist ein weiterer wichtiger Unterschied, dass Sie die Version von Node.js, die Sie auf Ihrem Server verwenden möchten, selbst auswählen können. Sie haben jedoch keine Kontrolle über die Version der Browser-Umgebung, die Ihre Nutzer zum Besuch Ihrer Webseite verwenden.

Node.js ist für Entwickler weltweit sehr wichtig geworden. Es ist ein leistungsfähiges, skalierbares und vielseitiges Werkzeug, das die Webentwicklung revolutioniert hat, indem es Entwicklern ermöglicht, JavaScript sowohl für das Frontend als auch das Backend von Full-Stack-Anwendungen zu verwenden. Dadurch wird der Entwicklungsprozess effizienter, weil Entwickler keine neue Programmiersprache nur für die Backend-Entwicklung lernen müssen.

# --questions--

## --text--

Welche der folgenden Aussagen beschreibt Node.js am besten?

## --answers--

Eine JavaScript-Bibliothek, die für die Frontend-Entwicklung verwendet wird.

### --feedback--

Denken Sie daran, wo und wie JavaScript-Code bei der Verwendung von Node.js ausgeführt wird.

---

Eine JavaScript-Laufzeitumgebung, die serverseitiges JavaScript ausführt.

---

Ein Webbrowser, der für das Ausführen von JavaScript-Anwendungen entwickelt wurde.

### --feedback--

Denken Sie daran, wo und wie JavaScript-Code bei der Verwendung von Node.js ausgeführt wird.

---

Ein JavaScript-Framework zum Erstellen von Benutzeroberflächen.

### --feedback--

Denken Sie daran, wo und wie JavaScript-Code bei der Verwendung von Node.js ausgeführt wird.

## --video-solution--

2

---

## --text--

Welcher der folgenden Punkte ist ein wesentlicher Unterschied zwischen der Browser- und der Node-Laufzeitumgebung?

## --answers--

Browser haben direkten Zugriff auf das Dateisystem, während die Node-Laufzeitumgebung dies nicht hat.

### --feedback--

Denken Sie an die Rollen und Zwecke jeder Umgebung.

---

Die Node-Laufzeitumgebung verwendet das window-Objekt als globales Objekt, während Browser das global-Objekt verwenden.

### --feedback--

Denken Sie an die Rollen und Zwecke jeder Umgebung.

---

Browser werden hauptsächlich für die Frontend-Entwicklung verwendet, während die Node-Laufzeitumgebung hauptsächlich für die Backend-Entwicklung verwendet wird.

---

Browser interagieren typischerweise mit dem Betriebssystem, während die Node-Laufzeitumgebung mit dem DOM interagiert.

### --feedback--

Denken Sie an die Rollen und Zwecke jeder Umgebung.

## --video-solution--

3

---

## --text--

Für welche der folgenden Aufgaben wird Node.js häufig verwendet?

## --answers--

Manipulation der HTML-Struktur einer Webseite.

### --feedback--

Denken Sie an die serverseitigen Fähigkeiten von Node.js.

---

Erstellen eines Webservers zur Verarbeitung von HTTP-Anfragen.

---

Styling von Webseiten-Elementen mit CSS.

### --feedback--

Denken Sie an die serverseitigen Fähigkeiten von Node.js.

---

Erstellen interaktiver Animationen innerhalb eines Webbrowsers.

### --feedback--

Denken Sie an die serverseitigen Fähigkeiten von Node.js.

## --video-solution--

2
