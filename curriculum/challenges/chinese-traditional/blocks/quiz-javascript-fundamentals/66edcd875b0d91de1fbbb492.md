---
id: 66edcd875b0d91de1fbbb492
title: JavaScript 基礎測驗
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

要通過測驗，你必須在以下 20 題中正確回答至少 18 題。

# --quizzes--

## --quiz--

### --question--

#### --text--

這些哪一個會創建一個 String 物件？

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

你如何將一個 String 物件轉換成字串字面值？

#### --distractors--

使用 `String()` 建構式。

---

使用 `.stringify()` 方法。

---

將 `String` 物件用雙引號括起來。

#### --answer--

使用 `.toString()` 方法。

### --question--

#### --text--

為什麼你可以在 JavaScript 的字串原始值上使用像 `.length`、`.repeat()`、`.concat()` 和 `.slice()` 這些方法？

#### --distractors--

那些方法原生存在於字串原始值上。

---

字串原始值在內部以物件形式儲存。

---

JavaScript 臨時將字串原始值轉換為陣列。

#### --answer--

JavaScript 臨時將字串原始值包裝在 `String` 物件中。

### --question--

#### --text--

以下程式碼會在主控台記錄什麼？

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

你如何將字串字面值轉換成 Number 物件？

#### --distractors--

使用 `Object()` 建構式。

---

使用 `.toNumber()` 方法。

---

使用 `.parseInt()` 方法。

#### --answer--

使用 `Number()` 建構式。

### --question--

#### --text--

`Number(true)` 和 `Number(false)` 分別會傳回什麼？

#### --distractors--

`undefined` 和 `undefined`

---

`NaN` 和 `NaN`

---

`true` 和 `false`

#### --answer--

`1` 和 `0`

### --question--

#### --text--

`Number(undefined)` 和 `Number(null)` 分別會傳回什麼？

#### --distractors--

`NaN` 和 `NaN`

---

`0` 和 `0`

---

`undefined` 和 `null`

#### --answer--

`NaN` 和 `0`

### --question--

#### --text--

以下哪一個是用來檢查使用者是否被允許在你網站上執行某些操作的布林值變數的最佳名稱？

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

以下哪一個是當使用者提交表單時執行的函式（程式）最佳名稱？

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

什麼是稀疏陣列？

#### --distractors--

一個陣列，其中一個或多個索引的值為 `null`。

---

一個陣列，其中一個或多個索引的值為 `undefined`。

---

一個陣列，其中一個或多個索引的值為空物件字面值。

#### --answer--

一個有一個或多個索引未被指定值的陣列。

### --question--

#### --text--

以下哪個範例中 `arr` 不會是稀疏陣列？

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

linter 的主要目的是什麼？

#### --distractors--

自動格式化程式碼。

---

為了最佳化 `code` 的效率、性能。

---

為程式碼創建文件。

#### --answer--

偵測程式碼中潛在的錯誤和問題。

### --question--

#### --text--

在程式碼庫中使用格式化工具的利益是什麼？

#### --distractors--

它們允許開發者更高效地編譯程式碼。

---

他們將程式碼轉換為特定的程式設計語言。

---

它們會偵測邏輯錯誤。

#### --answer--

它們確保一致的程式碼風格。

### --question--

#### --text--

在 JavaScript 中，通常是誰或什麼負責為變數分配記憶體？

#### --distractors--

開發者。

---

作業系統。

---

伺服器。

#### --answer--

執行期引擎。

### --question--

#### --text--

以下程式碼中會記錄到主控台的是什麼？

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

什麼是 JavaScript 中的 hoisting？

#### --distractors--

將錯誤拋出到程式碼頂層。

---

建立新變數的處理程序。

---

自動將 `var` 宣告移動到區塊頂端。

#### --answer--

將變數宣告式移動到其作用域頂端的處理程序。

### --question--

#### --text--

為什麼在現代 JavaScript 中使用 `var` 關鍵字被視為不良習慣？

#### --distractors--

它們比 `let` 和 `const` 慢。

---

它們會導致語法錯誤。

---

它們不允許型別檢查。

#### --answer--

它們在區塊外可被存取。

### --question--

#### --text--

JavaScript 中模組的目的為何？

#### --distractors--

為了提升程式碼效率、性能。

---

使程式碼更難理解。

---

創建新的程式語言。

#### --answer--

將程式碼組織成可重複使用的單元。

### --question--

#### --text--

如何將 `app.js` 腳本作為模組添加到 HTML 文件中？

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

以下哪個範例會從 `utils.js` 模組匯出 `calculateSum` 函式，並在 `app.js` 檔案中正確匯入及使用它？

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
