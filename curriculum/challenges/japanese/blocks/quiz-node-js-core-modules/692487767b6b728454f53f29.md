---
id: 692487767b6b728454f53f29
title: NodeJSコアモジュールクイズ
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

このクイズに合格するには、以下の20問のうち少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

既存のファイルを上書きせずに内容を追加する`fs`メソッドはどれですか？

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

`fs`モジュールの同期メソッドを使うと何が起こりますか？

#### --distractors--

既存のファイルが自動的に削除されます。

---

非同期メソッドより常に高速に動作します。

---

デフォルトでPromiseを返します。

#### --answer--

操作が完了するまでプログラムがブロックされます。

### --question--

#### --text--

`utf8`の`fs.readFile()`パラメータは何をしますか？

#### --distractors--

エラーがスローされるのを防ぎます。

---

ファイルの拡張子を変更します。

---

大きなファイルを圧縮します。

#### --answer--

Nodeにバッファではなく読み取り可能な文字列を返すよう指示します。

### --question--

#### --text--

ゼロで埋められた新しいバッファを作成する`Buffer`メソッドはどれですか？

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

`Buffer.toString()`は何をしますか？

#### --distractors--

バッファをバイナリコードに変換します。

---

バッファの内容をクリアします。

---

バッファのバイト長を表示します。

#### --answer--

バッファの内容を読み取り可能な文字列に変換します。

### --question--

#### --text--

署名を生成するのに秘密鍵が必要な`crypto`機能はどれですか？

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

なぜ自分で暗号アルゴリズムを直接書くのを避けるべきですか？

#### --distractors--

Node.jsアプリケーションでは違法です。

---

Node.jsは暗号化をサポートしていません。

---

JavaScriptはハッシュを計算できません。

#### --answer--

十分にテストされたライブラリなしでは安全でない場合があるからです。

### --question--

#### --text--

`crypto`の代わりに安全なランダムトークンを生成するのに推奨される`Math.random()`メソッドはどれですか？

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

CPUアーキテクチャを返すOSモジュールのメソッドはどれですか？

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

`os.userInfo()`は何を返しますか？

#### --distractors--

ユーザー名のみ。

---

デバイスのIPアドレスのみ。

---

ログイン中のユーザーIDのみ。

#### --answer--

現在のシステムユーザーに関する情報。

### --question--

#### --text--

セグメントを結合し、余分なスラッシュを正規化する`path`メソッドはどれですか？

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

カレントワーキングディレクトリを基に絶対パスを生成する`path`メソッドはどれですか？

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

グローバル変数`__dirname`は何を表しますか？

#### --distractors--

Nodeを実行しているユーザーの名前。

---

現在のファイルの拡張子。

---

`./`で始まる相対フォルダパス。

#### --answer--

現在のファイルが含まれるディレクトリの絶対パス。

### --question--

#### --text--

`NODE_ENV`のような環境変数を含むprocessのプロパティはどれですか？

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

`process.on("exit")`イベントは何をしますか？

#### --distractors--

Nodeアプリケーションを再起動します。

---

子プロセスに`exit`シグナルを送ります。

---

イベントループのキューをクリアします。

#### --answer--

Nodeプロセスが終了する直前にコードを実行します。

### --question--

#### --text--

読み書き両方ができるが変換はしないストリームタイプはどれですか？

#### --distractors--

Readable stream

---

Transform stream

---

Writable stream

#### --answer--

Duplex stream

### --question--

#### --text--

大きなファイルの読み書きを効率的に実装するには何が必要ですか？

#### --distractors--

`Buffer.alloc()`だけを使うこと

---

`crypto`モジュールをインポートすること

---

ファイル全体を最初にメモリに保存すること

#### --answer--

ストリームを使ってデータをチャンク単位で処理すること

### --question--

#### --text--

読み取り可能ストリームをWritableストリームに接続するメソッドはどれですか？

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

書き込み可能なデータがもうないことを示すストリームイベントはどれですか？

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

`toString()`なしで読み取り可能ストリームからチャンクを出力すると何がログに表示されますか？

#### --distractors--

プレーンテキストの内容

---

プロセスのPID

---

オペレーティングシステム名

#### --answer--

バイナリデータを含むバッファオブジェクト
