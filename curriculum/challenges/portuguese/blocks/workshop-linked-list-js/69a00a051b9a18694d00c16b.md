---
id: 69a00a051b9a18694d00c16b
title: Passo 1
challengeType: 1
dashedName: step-1
---

# --description--

Nesta oficina, você vai implementar uma lista ligada.

Para começar, crie uma função chamada `initList`. Dentro dela, retorne um objeto com uma propriedade `head` definida como `null` e uma propriedade `length` com valor `0`.

Esta é a função que você vai chamar para inicializar uma lista ligada. O `head` representa o primeiro elemento da lista, inicialmente, ele é `null` porque não há nenhum elemento na lista. O `length` acompanha o número de nós (elementos) na lista, que você vai incrementar ou decrementar ao adicionar ou remover um nó da lista.

# --hints--

Você deve criar uma função `initList`.

```js
assert.isFunction(initList)
```

Sua função `initList` deve retornar um objeto.

```js
assert.isObject(initList());
```

Sua função `initList` deve retornar um objeto com um `head` definido como `null`.

```js
assert.propertyVal(initList(), "head", null);
```

Sua função `initList` deve retornar um objeto com um `length` definido como `0`.

```js
assert.propertyVal(initList(), "length", 0);
```

# --seed--

## --seed-contents--

```js
--fcc-editable-region--

--fcc-editable-region--
```
