---
id: 6a22d77ddf034bc4e35b1d5a
title: "Sfida 353: valutazione del rapporto di contrasto 2"
challengeType: 28
dashedName: challenge-353
---

# --description--

Dato due valori di luminanza relativa e un booleano che indica se il testo è grande, restituisci la valutazione del contrasto WCAG usando il seguente metodo:

Calcola il rapporto di contrasto aggiungendo 0.05 a ciascun valore di luminanza, poi dividendo il valore più chiaro per quello più scuro. Il valore più chiaro sarà sempre il primo argomento.

Restituisci la valutazione basata sul rapporto di contrasto usando la seguente tabella:

| Valutazione | Testo normale | Testo grande |
|--------|-------------|------------|
| `"AAA"` | 7.0+ | 4.5+ |
| `"AA"` | 4.5+ | 3.0+ |
| `"Fail"` | sotto 4.5 | sotto 3.0 |

# --hints--

`getContrastRating(1.0, 0.0, false)` dovrebbe restituire `"AAA"`.

```js
assert.equal(getContrastRating(1.0, 0.0, false), "AAA");
```

`getContrastRating(0.9015, 0.1364, false)` dovrebbe restituire `"AA"`.

```js
assert.equal(getContrastRating(0.9015, 0.1364, false), "AA");
```

`getContrastRating(0.8965, 0.1628, false)` dovrebbe restituire `"Fail"`.

```js
assert.equal(getContrastRating(0.8965, 0.1628, false), "Fail");
```

`getContrastRating(0.7469, 0.0957, true)` dovrebbe restituire `"AAA"`.

```js
assert.equal(getContrastRating(0.7469, 0.0957, true), "AAA");
```

`getContrastRating(0.7489, 0.2018, true)` dovrebbe restituire `"AA"`.

```js
assert.equal(getContrastRating(0.7489, 0.2018, true), "AA");
```

`getContrastRating(0.6571, 0.1974, true)` dovrebbe restituire `"Fail"`.

```js
assert.equal(getContrastRating(0.6571, 0.1974, true), "Fail");
```

# --seed--

## --seed-contents--

```js
function getContrastRating(l1, l2, isLargeText) {

  return l1;
}
```

# --solutions--

```js
function getContrastRating(l1, l2, isLargeText) {
  const ratio = (l1 + 0.05) / (l2 + 0.05);

  if (isLargeText) {
    if (ratio >= 4.5) return "AAA";
    if (ratio >= 3.0) return "AA";
  } else {
    if (ratio >= 7.0) return "AAA";
    if (ratio >= 4.5) return "AA";
  }
  return "Fail";
}
```
