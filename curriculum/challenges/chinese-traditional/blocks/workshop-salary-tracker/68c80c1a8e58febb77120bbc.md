---
id: 68c80c1a8e58febb77120bbc
title: 步驟 13
challengeType: 20
dashedName: step-13
---

# --description--

從你的程式碼中移除最後一次的 `print` 呼叫。

# --hints--

你應該從你的程式碼中移除 `print(repr(charlie_brown))`。

```js
({ test: () => assert(runPython(`not _Node(_code).has_stmt("print(repr(charlie_brown))")`)) })
```

# --seed--

## --seed-contents--

```py
class Employee:
    def __init__(self, name, level):
        self._name = name
        self._level = level

    def __str__(self):
        return f'{self.name}: {self.level}'

    def __repr__(self):
        return f"Employee('{self.name}', '{self.level}')"

    @property
    def name(self):
        return self._name

    @property
    def level(self):
        return self._level

charlie_brown = Employee('Charlie Brown', 'trainee')
print(charlie_brown)
--fcc-editable-region--
print(repr(charlie_brown))
--fcc-editable-region--
```
