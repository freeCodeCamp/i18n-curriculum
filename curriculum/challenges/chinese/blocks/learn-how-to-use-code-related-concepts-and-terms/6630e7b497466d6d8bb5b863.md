---
id: 6630e7b497466d6d8bb5b863
title: 任务 10
challengeType: 19
dashedName: task-10
lang: en-US
---

# --description--

在编程中，`string` 是一种用于存储文本的数据类型。`string` 可以包含字母、数字和符号。它通常通过将文本括在引用中来表现。

例如，`"Hello, world!"` 是一个 `string`。

# --questions--

## --text--

在编程上下文中，什么是 `string`？

## --answers--

一段文本

---

一种数字类型

### --feedback--

虽然 `string` 可以包含数字，但它主要用于文本，而不仅仅是数值。

---

一个编程工具

### --feedback--

`string` 是编程中使用的一种数据类型，专门用于文本，而不是工具。

---

启动程序的命令

### --feedback--

`String` 是一种用于文本的数据类型，而不是执行程序的命令。

## --video-solution--

1
