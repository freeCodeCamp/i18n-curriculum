---
id: 67f39dac6c3fac29c3d54918
title: Revisione della programmazione orientata agli oggetti
challengeType: 31
dashedName: review-object-oriented-programming
---

# --description--

## Che cos’è la programmazione orientata agli oggetti?

- **Programmazione orientata agli oggetti**: Uno stile di programmazione in cui gli sviluppatori trattano tutto nel loro codice come un oggetto del mondo reale. È comunemente chiamata OOP. I quattro principi chiave che ti aiutano a organizzare e gestire il codice in modo efficace sono **incapsulamento**, **ereditarietà**, **polimorfismo** e **astrazione**.
- **Classi**: Il modello per creare oggetti. Ogni singolo oggetto creato da una classe ha attributi che definiscono i dati e metodi che determinano i comportamenti degli oggetti.

## Che cos’è l’incapsulamento?

- **Incapsulamento**: Il raggruppamento degli attributi e dei metodi di un oggetto in un’unità singola. Ti permette di nascondere lo stato interno dell’oggetto dietro un semplice insieme di metodi e attributi pubblici che funzionano come porte. Dietro quelle porte ci sono attributi e metodi privati che controllano come i dati cambiano e chi può vederli.
- **Esempio di incapsulamento**: Se vuoi tenere traccia del saldo di un portafoglio, permetterai depositi e prelievi, ma non vorrai che nessuno possa manomettere direttamente il saldo del portafoglio:

```py
class Wallet:
   def __init__(self, balance):
       self.__balance = balance # Private attribute

   def deposit(self, amount):
       if amount > 0:
           self.__balance += amount # Add to the balance safely

   def withdraw(self, amount):
       if 0 < amount <= self.__balance:
           self.__balance -= amount # Remove from the balance safely

account = Wallet(500)
print(account.__balance) # AttributeError: 'Wallet' object has no attribute '__balance'
```

- **Differenza tra prefissare gli attributi con un singolo o doppio underscore**: Prefissare attributi e metodi con un singolo underscore significa che sono destinati all’uso interno. Questa è una convenzione e non impedisce di consultare gli attributi dall’esterno. Prefissare attributi e metodi con un doppio underscore impedisce effettivamente che vengano consultati dall’esterno della loro classe.

## Che cosa sono i getter e i setter?

- **Getter e setter**: Metodi che ti permettono di controllare come gli attributi di una classe vengono consultati e modificati. Recuperi i valori con i getter e li imposti con i setter.
- **Proprietà**: Collegano getter e setter e permettono di consultare i dati. Eseguono logica aggiuntiva dietro le quinte quando ottieni, imposti o elimini valori.
- **Perché usare le proprietà invece dei metodi**: Le proprietà si usano invece dei metodi per una migliore leggibilità e un codice più pulito. Ti permettono di consultare i valori con la notazione a punto, come normali attributi, senza parentesi tonde.
- **Creare un getter**: Per creare un getter, usi il decoratore `@property`. Ecco un getter che ottiene il raggio di un cerchio:

```py
class Circle:
    def __init__(self, radius):
        self._radius = radius

    @property
    def radius(self): # A getter to get the radius
        return self._radius
  
    @property
    def area(self):  # A getter to calculate area
        return 3.14 * (self._radius ** 2)

my_circle = Circle(3)

print(my_circle.radius) # 3
print(my_circle.area) # 28.26
```

- **Creare un setter**: Per creare il setter che imposta il raggio, devi definire un altro metodo con lo stesso nome e usare `@<property_name>.setter` sopra di esso:

```py
class Circle:
    def __init__(self, radius):
        self._radius = radius

    @property
    def radius(self):  # A getter to get the radius
        return self._radius

    @radius.setter
    def radius(self, value):  # A setter to set the radius
        if value <= 0:
            raise ValueError('Radius must be positive')
        self._radius = value

my_circle = Circle(3)
print('Initial radius:', my_circle.radius) # Initial radius: 3

my_circle.radius = 8
print('After modifying the radius:', my_circle.radius) # After modifying the radius: 8
```

- **Come Python gestisce getter e setter**: Una volta definiti getter e setter, Python li chiama automaticamente sotto il cofano ogni volta che usi la sintassi normale degli attributi in questo modo:

```py
my_circle.radius # This will call the getter
my_circle.radius = 4 # This will call the setter
```

 Quando imposti un valore, non dovresti assegnare direttamente al nome della proprietà perché questo causerebbe un `RecursionError`. Usa un nome interno separato, spesso con un underscore, per archiviare il valore.

- **Deleter**: Dopo aver impostato e ottenuto un valore con setter e getter, puoi controllare come viene eliminato con un `deleter`. Un deleter esegue logica personalizzata quando usi l’istruzione `del` su una proprietà. Per creare un deleter, usi il decoratore `@<property_name>.deleter`.

```py
  # Deleter
    @radius.deleter
    def radius(self):
        print("Deleting radius...")
        del self._radius
```

## Che cos’è l’ereditarietà e come favorisce il riuso del codice?

- **Ereditarietà**: Il processo con cui una classe figlia usa gli attributi e i metodi di una classe genitore. L’ereditarietà favorisce il riuso del codice, fornisce gerarchie chiare e personalizza il comportamento senza riscrivere tutto. Per implementare l’ereditarietà, una classe figlia prende il nome di una classe genitore:

```py
class Parent:
    # Parent attributes and methods

class Child(Parent):
    # Child inherits, extends, and/or overrides where necessary
```

- **Ereditarietà singola e multipla**: Quando una classe figlia eredita proprietà e metodi da un solo genitore, come vedi sopra, il processo si chiama **ereditarietà singola**. Quando una classe figlia eredita proprietà e metodi da più di un genitore, si chiama **ereditarietà multipla**. Ecco la sintassi per questo:

```py
class Parent:
    # Attributes and methods for Parent

class Child:
    # Attributes and methods for Child

class GrandChild(Parent, Child):
    # GrandChild inherits from both Parent and Child
    # GrandChild can combine or override behavior from each
```

- **Funzione `super()`**: Una funzione che ti permette di **chiamare** un metodo di una classe genitore, quando una classe ha un’implementazione diversa di quel metodo o lo estende, senza duplicare codice.

## Che cos’è il polimorfismo e come favorisce il riuso del codice?

- **Polimorfismo**: Il principio OOP che permette a classi diverse di usare lo stesso nome di metodo, ma ogni classe lo implementa in modo diverso quando viene chiamato. Ecco la sintassi per questo:

```py
class A:
   def action(self): ...

class B:
   def action(self): ...

class C:
   def action(self): ...

Class().method()  # Works for A, B, or C
```

- **Polimorfismo basato sull’ereditarietà**: Un genitore definisce un metodo e ogni classe figlia lo adatta al proprio uso.

## Che cos’è il name mangling e come funziona?

- **Name mangling**: Un processo in cui Python rinomina internamente un attributo prefissato con un doppio underscore aggiungendo un underscore e il nome della classe come prefisso, trasformando `__attribute` in `_ClassName__attribute`.
- **Scopo del name mangling**: Lo scopo principale del name mangling è prevenire la sovrascrittura accidentale di attributi e metodi quando usi l’ereditarietà. Ecco un codice che rende questo più comprensibile:

```py
class Parent:
    def __init__(self):
        self.__data = 'Parent data'

class Child(Parent):
    def __init__(self):
        super().__init__()
        self.__data = 'Child data'

c = Child()
print(c.__dict__) # {'_Parent__data': 'Parent data', '_Child__data': 'Child data'}
```

## Che cos’è l’astrazione e come aiuta a mantenere organizzati i sistemi complessi?

- **Astrazione**: Un concetto di programmazione in cui i dettagli complessi di implementazione di un oggetto o sistema sono nascosti e vengono mostrati solo gli aspetti essenziali. In Python e in altri linguaggi di programmazione, l’astrazione semplifica i sistemi complessi aumentando il riuso.
- **Esempio di astrazione**: Un buon esempio di astrazione nella vita quotidiana è un’auto che ti permette di usare solo il volante, i pedali e il cambio senza sapere come funzionano il motore o i freni.
- **Come Python implementa l’astrazione**: Python implementa l’astrazione tramite il modulo `abc`. Il modulo fornisce la classe `ABC` (classe base astratta) e il decoratore `@abstractmethod`. Una classe base astratta (ABC) definisce i metodi e le proprietà comuni che le sottoclassi devono implementare. Non può essere istanziata.
- **Come si definisce un metodo astratto**: Un metodo astratto si definisce con `@abstractmethod` e deve essere sovrascritto nelle sottoclassi, anche se ha un’implementazione predefinita. La sintassi base dell’astrazione è questa:

```py
from abc import ABC, abstractmethod

# Define an abstract base class
class AbstractClass(ABC):
    @abstractmethod
    def abstract_method(self):
        pass

# Concrete subclass that implements the abstract method
class ConcreteClassOne(AbstractClass):
    def abstract_method(self):
        print('Implementation in ConcreteClassOne')

# Another concrete subclass
class ConcreteClassTwo(AbstractClass):
    def abstract_method(self):
        print('Implementation in ConcreteClassTwo')
```

# --assignment--

Rivedi gli argomenti e i concetti della programmazione orientata agli oggetti.
