---
id: 66edcd875b0d91de1fbbb492
title: JavaScript-Grundlagen-Quiz
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

Um das Quiz zu bestehen, müssen Sie mindestens 18 der 20 folgenden Fragen richtig beantworten.

# --quizzes--

## --quiz--

### --question--

#### --text--

Welche dieser Optionen erzeugt ein String-Objekt?

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

Wie können Sie ein String-Objekt in einen String-Literal umwandeln?

#### --distractors--

Mit dem `String()` Konstruktor.

---

Mit der `.stringify()` Methode.

---

Den `String` Objekt in Anführungszeichen einschließen.

#### --answer--

Mit der `.toString()` Methode.

### --question--

#### --text--

Warum können Sie Methoden wie `.length`, `.repeat()`, `.concat()` und `.slice()` auf einem String-Primitiv in JavaScript verwenden?

#### --distractors--

Diese Methoden existieren nativ auf String-Primitiven.

---

String-Primitiven werden intern als Objekte gespeichert.

---

JavaScript wandelt das String-Primitiv vorübergehend in ein Array um.

#### --answer--

JavaScript umhüllt das String-Primitiv vorübergehend mit einem String-Objekt.

### --question--

#### --text--

Was wird der folgende Code in der Konsole ausgeben?

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

Welche der folgenden Optionen gibt ein `Number` Objekt aus dem String `"123"` zurück?

#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--

Was werden `Number(true)` und `Number(false)` jeweils zurückgeben?

#### --distractors--

`undefined` und `undefined`

---

`NaN` und `NaN`

---

`true` und `false`

#### --answer--

`1` und `0`

### --question--

#### --text--

Was werden `Number(undefined)` und `Number(null)` jeweils zurückgeben?

#### --distractors--

`NaN` und `NaN`

---

`0` und `0`

---

`undefined` und `null`

#### --answer--

`NaN` und `0`

### --question--

#### --text--

Welcher der folgenden Namen ist am besten geeignet für eine Boolean-Variable, die überprüft, ob ein Benutzer auf Ihrer Website etwas tun darf?

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

Welcher der folgenden Namen ist am besten geeignet für eine Funktion, die ausgeführt wird, wenn ein Benutzer ein Formular absendet?

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

Was ist ein Sparse-Array?

#### --distractors--

Ein Array, bei dem ein oder mehrere Indizes den Wert `null` haben.

---

Ein Array, bei dem ein oder mehrere Indizes den Wert `undefined` haben.

---

Ein Array, bei dem ein oder mehrere Indizes den Wert eines leeren Objekt-Literals haben.

#### --answer--

Ein Array, bei dem ein oder mehrere Indizes keinen Wert zugewiesen bekommen haben.

### --question--

#### --text--

In welchem der folgenden Beispiele ist `arr` KEIN Sparse-Array?

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

Was ist der Hauptzweck eines Linters?

#### --distractors--

Code automatisch formatieren.

---

Code für Performance optimieren.

---

Dokumentation für Code erstellen.

#### --answer--

Potenzielle Fehler und Probleme im Code erkennen.

### --question--

#### --text--

Was ist ein Vorteil der Verwendung eines Formatters in einem Codebestand?

#### --distractors--

Sie ermöglichen Entwicklern, Code effizienter zu kompilieren.

---

Sie wandeln Code in eine bestimmte Programmiersprache um.

---

Sie erkennen logische Fehler.

#### --answer--

Sie sorgen für einen einheitlichen Code-Stil.

### --question--

#### --text--

Wer oder was ist in JavaScript typischerweise für die Speicherzuweisung an Variablen verantwortlich?

#### --distractors--

Der Entwickler.

---

Das Betriebssystem.

---

Der Server.

#### --answer--

Die Laufzeitumgebung.

### --question--

#### --text--

Was wird in der Konsole ausgegeben bei folgendem Code?

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

Was versteht man unter Hoisting in JavaScript?

#### --distractors--

Fehler an den Anfang des Codes heben.

---

Der Prozess, neue Variablen zu erstellen.

---

Automatisches Verschieben von `var` Deklarationen an den Anfang des Blocks.

#### --answer--

Der Prozess, Variablendeklarationen an den Anfang ihres Gültigkeitsbereichs zu verschieben.

### --question--

#### --text--

Warum gilt die Verwendung des Schlüsselworts `var` in modernem JavaScript als schlechte Praxis?

#### --distractors--

Sie sind langsamer als `let` und `const`.

---

Sie verursachen Syntaxfehler.

---

Sie erlauben keine Typprüfung.

#### --answer--

Sie sind außerhalb ihres Blocks zugreifbar.

### --question--

#### --text--

Was ist der Zweck von Modulen in JavaScript?

#### --distractors--

Code-Performance verbessern.

---

Code schwerer verständlich machen.

---

Neue Programmiersprachen erstellen.

#### --answer--

Code in wiederverwendbare Einheiten organisieren.

### --question--

#### --text--

Wie fügen Sie ein `app.js` Skript als Modul in ein HTML-Dokument ein?

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

Welches der folgenden Beispiele exportiert die `calculateSum` Funktion aus einem `utils.js` Modul und importiert und verwendet sie korrekt in einer `app.js` Datei?

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
