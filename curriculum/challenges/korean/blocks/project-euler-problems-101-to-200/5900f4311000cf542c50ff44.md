---
id: 5900f4311000cf542c50ff44
title: '문제 197: 재귀적으로 정의된 수열의 동작 조사'
challengeType: 1
forumTopicId: 301835
dashedName: problem-197-investigating-the-behaviour-of-a-recursively-defined-sequence
---

# --description--

함수 $f(x) = ⌊{2}^{30.403243784 - x^2}⌋ × {10}^{-9}$ (⌊ ⌋는 바닥 함수)와 수열 $u_n$이 $u_0 = -1$ 및 $u_{n + 1} = f(u_n)$로 정의되어 있습니다.

$n = {10}^{12}$일 때 $u_n + u_{n + 1}$ 값을 구하세요. 소수점 아래 9자리까지 답을 제시하세요.

# --hints--

`recursivelyDefinedSequence()`는 `1.710637717`를 반환해야 합니다.

```js
assert.strictEqual(recursivelyDefinedSequence(), 1.710637717);
```

# --seed--

## --seed-contents--

```js
function recursivelyDefinedSequence() {

  return true;
}

recursivelyDefinedSequence();
```

# --solutions--

```js
// solution required
```
