---
id: 66edcd875b0d91de1fbbb492
title: JavaScript 基礎クイズ
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

このクイズに合格するには、以下の20問中少なくとも18問に正しく答える必要があります。

# --quizzes--

## --quiz--

### --question--

#### --text--

次のうち、Stringオブジェクトを作成するものはどれですか？

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

Stringオブジェクトを文字列リテラルに変換するにはどうすればよいですか？

#### --distractors--

`String()` コンストラクタを使う。

---

`.stringify()` メソッドを使う。

---

`String` オブジェクトを二重引用符で囲む。

#### --answer--

`.toString()` メソッドを使う。

### --question--

#### --text--

なぜJavaScriptの文字列プリミティブに対して、`.length`、`.repeat()`、`.concat()`、`.slice()` のようなメソッドを使うことができるのでしょうか？

#### --distractors--

これらのメソッドは文字列プリミティブにネイティブで存在している。

---

文字列プリミティブは内部的にオブジェクトとして保存されている。

---

JavaScriptは文字列プリミティブを一時的に配列に変換している。

#### --answer--

JavaScriptは文字列プリミティブを一時的にStringオブジェクトでラップしている。

### --question--

#### --text--

次のコードはコンソールに何を出力しますか？

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

次のうち、文字列`Number`から`"123"`オブジェクトを返すものはどれですか？

#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--

`Number(true)` と `Number(false)` はそれぞれ何を返しますか？

#### --distractors--

`undefined` と `undefined`

---

`NaN` と `NaN`

---

`true` と `false`

#### --answer--

`1` と `0`

### --question--

#### --text--

`Number(undefined)` と `Number(null)` はそれぞれ何を返しますか？

#### --distractors--

`NaN` と `NaN`

---

`0` と `0`

---

`undefined` と `null`

#### --answer--

`NaN` と `0`

### --question--

#### --text--

ウェブサイトでユーザーが何かを行う許可があるかどうかをチェックするブール値変数の最適な名前はどれですか？

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

ユーザーがフォームを提出したときに実行される関数の最適な名前はどれですか？

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

スパース配列とは何ですか？

#### --distractors--

1つ以上のインデックスに`null`の値がある配列。

---

1つ以上のインデックスに`undefined`の値がある配列。

---

1つ以上のインデックスに空のオブジェクトリテラルの値がある配列。

#### --answer--

1つ以上のインデックスに値が代入されていない配列。

### --question--

#### --text--

次の例のうち、`arr` がスパース配列でないのはどれですか？

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

リンターの主な目的は何ですか？

#### --distractors--

コードを自動的にフォーマットすること。

---

コードのパフォーマンスを最適化すること。

---

コードのドキュメントを作成すること。

#### --answer--

コードの潜在的なエラーや問題を検出すること。

### --question--

#### --text--

コードベースでフォーマッターを使う利点は何ですか？

#### --distractors--

開発者がより効率的にコードをコンパイルできるようにする。

---

コードを特定のプログラミング言語に変換する。

---

論理エラーを検出する。

#### --answer--

一貫したコードスタイルを保証する。

### --question--

#### --text--

JavaScriptで変数にメモリを割り当てるのは通常誰または何ですか？

#### --distractors--

開発者。

---

オペレーティングシステム。

---

サーバー。

#### --answer--

ランタイムエンジン。

### --question--

#### --text--

次のコードはコンソールに何を出力しますか？

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

JavaScriptのホイスティングとは何ですか？

#### --distractors--

エラーをコードの先頭に持ち上げること。

---

新しい変数を作成するプロセス。

---

`var` 宣言を自動的にブロックの先頭に移動すること。

#### --answer--

変数宣言をスコープの先頭に移動するプロセス。

### --question--

#### --text--

なぜ現代のJavaScriptで`var`キーワードの使用は悪い習慣とされているのですか？

#### --distractors--

`let` と `const` より遅いから。

---

構文エラーを引き起こすから。

---

型チェックができないから。

#### --answer--

ブロックの外からアクセスできてしまうから。

### --question--

#### --text--

JavaScriptのモジュールの目的は何ですか？

#### --distractors--

コードのパフォーマンスを向上させること。

---

コードを理解しにくくすること。

---

新しいプログラミング言語を作ること。

#### --answer--

コードを再利用可能な単位に整理すること。

### --question--

#### --text--

HTMLドキュメントに`app.js`スクリプトをモジュールとして追加するにはどうすればよいですか？

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

次の例のうち、`calculateSum`モジュールから`utils.js`関数をエクスポートし、`app.js`ファイルで正しくインポートして使用しているのはどれですか？

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
